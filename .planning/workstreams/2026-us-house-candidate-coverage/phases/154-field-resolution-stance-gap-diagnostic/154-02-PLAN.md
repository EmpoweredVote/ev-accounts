---
phase: 154-field-resolution-stance-gap-diagnostic
plan: 02
type: execute
wave: 2
depends_on: ["154-01"]
files_modified:
  - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
  - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
  - backend/scripts/diag-154-validate-field-table.py
  - backend/scripts/154-verify.sql
autonomous: true
requirements: [USHC2-01]
must_haves:
  truths:
    - "A per-district Nov-3 general-ballot field table exists for all 113 districts: 89 rows (PA/IL/OH/GA/NC/NJ) tagged decided, 24 rows (MI 13 + VA 11) tagged pending-primary (Aug-4)"
    - "Every district where the 2024 incumbent is NOT the 2026 nominee is flagged via the nominee-status taxonomy, resolved from official/results sources never from incumbency (D-04); the four special seats GA-13/NJ-11/VA-11/GA-14 are explicitly handled"
    - "The set of genuinely-new candidates needing records (challengers + open/special-seated) is enumerated per district, distinct from incumbents that reuse existing records; no ghost incumbents"
    - "A write-free 154-verify.sql gate asserts the read-only DB baseline (district counts, pre-seeding zero-races) and does NOT assert a decided field for MI or VA"
  artifacts:
    - path: ".planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv"
      provides: "113-row field table; 89 decided + 24 pending-primary; nominee_status + new_records_needed per district"
      contains: "field_status"
    - path: "backend/scripts/diag-154-validate-field-table.py"
      provides: "Read-only CSV shape validator: 113 rows, per-state counts, field_status partition, required columns"
      contains: "EXPECTED"
    - path: "backend/scripts/154-verify.sql"
      provides: "Write-free DB baseline gate (DO $$ ... RAISE EXCEPTION); 8-state district counts + 0 pre-seeded 2026 races"
      contains: "RAISE EXCEPTION"
  key_links:
    - from: ".planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv"
      to: ".planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv"
      via: "incumbent_pid / geo_id carried from Plan 01"
      pattern: "incumbent_pid"
    - from: "backend/scripts/154-verify.sql"
      to: "essentials.districts + essentials.races + essentials.elections"
      via: "NATIONAL_LOWER + substr(geo_id,1,2) IN (8 fips) baseline assertions"
      pattern: "NATIONAL_LOWER"
---

<objective>
Assemble the human + machine field-resolution artifacts for Wave-2: a per-district Nov-3 general-ballot field table for all 113 districts (89 decided, 24 pending-primary), a read-only CSV shape validator, and a write-free `154-verify.sql` DB baseline gate. This is the authoritative input each seeding phase (155/156/157/159) consumes so no phase creates a duplicate incumbent, surfaces a non-candidate, or seeds against a non-existent baseline.

Purpose: Lock the field so the two costliest traps (duplicate incumbent, lost-incumbent-primary) cannot occur downstream, and prove the DB baseline (correct district counts, zero pre-seeded 2026 races) before any seeding.
Output: `154-field-table.csv`, `154-FIELD-TABLE.md`, `backend/scripts/diag-154-validate-field-table.py`, `backend/scripts/154-verify.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-RESEARCH.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-VALIDATION.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv

<interfaces>
<!-- Templates the executor adapts; read directly. -->

backend/scripts/diag-148-validate-field-table.py — PRIMARY VALIDATOR TEMPLATE:
  - 15 REQUIRED_COLS: state, cd, geo_id, target_election, existing_race_id, incumbent_name,
    incumbent_pid, incumbent_external_id, incumbent_stance_count, incumbent_top_up_tier,
    nominee_status, general_candidates, new_records_needed, field_status, source_url.
  - Asserts row count, per-state EXPECTED counts, non-empty source_url + nominee_status,
    non-empty incumbent_pid for non-open-seat-vacancy rows, UUID-shape existing_race_id.
  - Wave-1 specifics to REPLACE: EXPECTED={'CA':52,'TX':38,'FL':28,'NY':26}; 144 rows;
    the CA-has-existing_race_id / non-CA-blank rule (Wave-2 has NO pre-seeded races -> all blank);
    CSV_PATH points at the 148 phase dir.

backend/scripts/149-verify.sql — GATE STYLE TEMPLATE:
  - `\set ON_ERROR_STOP on` + `DO $$ DECLARE ... BEGIN ... END $$;`
  - Per-assertion `IF <cond> THEN RAISE EXCEPTION 'FAIL ...'; END IF; RAISE NOTICE 'PASS ...';`
  - `CREATE TEMP TABLE ... ON COMMIT DROP` for diffing (the only write; SELECT-only otherwise).
  - Join path: races.office_id -> offices.id; offices.district_id -> districts.id; district geo via districts.geo_id.
  NOTE: 154-verify.sql is a PRE-SEEDING baseline gate (RESEARCH §Pattern 3), NOT 149's post-seeding
  candidate assertions. Assert DB baseline facts only.

nominee_status taxonomy (7 values, Phase-148 set): renominated / retired / lost-primary /
  redistricted / vacancy / deceased / special-seated  (+ open-seat-vacancy for GA-13-style rows).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble 154-field-table.csv + 154-FIELD-TABLE.md (113 rows; 89 decided + 24 pending)</name>
  <files>.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv, .planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md</files>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (Plan 01 output — incumbent_pid / external_id / stance_count / tier per geo_id; carry these forward)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (the 15-column row layout + nominee_status values to mirror)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-RESEARCH.md (§"Special Seat Resolutions" VA-11/NJ-11/GA-13; §"Pattern 4 field-table schema"; §"Per-State Primary Status" source recommendations; §"Critical Finding: VA primary Aug 4")
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-CONTEXT.md (D-01..D-04: 89-decided/24-pending split, inclusion bar, special-seat resolution-from-results)
  </read_first>
  <action>
    Author `154-field-table.csv` with the same 15 columns as `148-field-table.csv` (state, cd, geo_id, target_election, existing_race_id, incumbent_name, incumbent_pid, incumbent_external_id, incumbent_stance_count, incumbent_top_up_tier, nominee_status, general_candidates, new_records_needed, field_status, source_url) for all 113 districts. Carry incumbent_pid/external_id/stance_count/tier from `154-incumbent-map.csv` by geo_id. Set:
    - `existing_race_id` = BLANK for all 113 rows (no pre-seeded 2026 House races for any of the 8 states — verified by 154-verify.sql Task 3).
    - `target_election` = e.g. "PA 2026 Statewide General" per state.
    - `field_status` = `decided` for the 89 rows of PA(42)/IL(17)/OH(39)/GA(13)/NC(37)/NJ(34); `pending-primary (Aug-4)` for the 24 rows of MI(26, 13 districts) + VA(51, 11 districts). Do NOT resolve MI or VA nominees (both primaries Aug 4, 2026 — RESEARCH Critical Finding + Pitfall 1). For MI/VA rows, `general_candidates` may list declared/incumbent candidates only with the pending tag; `new_records_needed` stays provisional.
    Resolve the 89 decided-state fields from official/results sources, never from incumbency (D-04). Per-state source per RESEARCH §"Per-State Primary Status" (Wikipedia raw-wikitext via `?action=raw` for long pages; Ballotpedia via Playwright (Cloudflare-walled to WebFetch); state SoS results for GA-13/NJ). Field-resolution source per state is Claude's Discretion (D-04) — prefer primary state results where reachable; cite the URL in `source_url` for every row.
    Encode the four special seats explicitly (RESEARCH §Special Seat Resolutions):
    - GA-13 (1313): `nominee_status=open-seat-vacancy` (Scott deceased; seat VACANT, special July 28 not yet held); Nov-3 field DECIDED = Jasmine Clark (D) vs Jonathan Chavez (R), both `new_records_needed`. Do NOT block on the July 28 special (Pitfall 2).
    - NJ-11 (3411): the Sherrill record reads `nominee_status=vacancy` (resigned → NJ governor); the field table OVERRIDES the Query-A incumbent with Analilia Mejia (D, `special-seated`, new record) vs Joe Hathaway (R, new record). Add an explicit note that Query A returns Sherrill but she is not the 2026 nominee (Pitfall 5). field_status=`decided`.
    - VA-11 (5111): `nominee_status=special-seated` for the current member James Walkinshaw (seated Sep 2025, NOT in DB → new_records_needed); field_status=`pending-primary (Aug-4)` (Aug-4 primary for the full term). Do NOT resolve the VA-11 Nov-3 nominee now.
    - GA-14 (1314): use the Plan-01 Query-C result — if 0-holder, Clay Fuller (R, special-seated Apr 7) is a `new_records_needed`; if mapped to MTG, flag the stale-incumbent anomaly per Plan 01. Resolve the Nov-3 GA-14 field from GA results.
    Also write `154-FIELD-TABLE.md` — a human-readable summary: per-state district counts, the decided/pending split (89/24), the four special-seat resolutions with their cited URLs, the per-state new-record counts, and a NJ-11 Sherrill-override callout. Use the inclusion bar from D-03 (every Nov-3-general-ballot-qualified candidate; exclude primary-only also-rans + uncertified write-ins).
    No fenced code in this artifact; it is data + prose.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv')));d=[x for x in r if x['field_status']=='decided'];p=[x for x in r if 'pending-primary' in x['field_status']];print('total',len(r),'decided',len(d),'pending',len(p));assert len(r)==113 and len(d)==89 and len(p)==24,(len(r),len(d),len(p))"</automated>
  </verify>
  <acceptance_criteria>
    `154-field-table.csv` has exactly 113 data rows with the 15-column header. Partition: exactly 89 rows `field_status=decided` (PA17/IL17/OH15/GA14/NC14/NJ12) and exactly 24 rows `field_status` containing `pending-primary` (MI13+VA11). Every row has a non-empty `source_url` and `nominee_status`. GA-13 row uses `open-seat-vacancy`; NJ-11 lists Mejia+Hathaway with a Sherrill-override note; VA-11 is `special-seated` + `pending-primary`. `154-FIELD-TABLE.md` documents the 89/24 split, the four special seats with cited URLs, and the per-state new-record counts.
  </acceptance_criteria>
  <done>The 113-row field table exists with the correct 89/24 decided/pending partition, every non-incumbent-nominee flagged via the taxonomy from results sources, the four special seats handled, and a human-readable .md summary written. MI/VA nominees are NOT resolved (correctly deferred to Phase 159).</done>
</task>

<task type="auto">
  <name>Task 2: Write diag-154-validate-field-table.py (CSV shape gate; 113 rows, 89/24 partition)</name>
  <files>backend/scripts/diag-154-validate-field-table.py</files>
  <read_first>
    - backend/scripts/diag-148-validate-field-table.py (PRIMARY TEMPLATE — clone; adapt CSV_PATH, EXPECTED, row count, and the existing_race_id rule)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (the artifact this validates — Task 1 output)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-VALIDATION.md (§"Wave 0 Requirements" — Part B contract)
  </read_first>
  <action>
    Clone `backend/scripts/diag-148-validate-field-table.py` to `backend/scripts/diag-154-validate-field-table.py`. Keep the 15 `REQUIRED_COLS` header check and the per-row source_url/nominee_status non-empty checks. Replace:
    - `CSV_PATH` -> the 154 phase dir `154-field-table.csv`.
    - `EXPECTED = {'PA':17,'IL':17,'OH':15,'GA':14,'NC':14,'MI':13,'NJ':12,'VA':11}` (total 113).
    - Row count assertion 144 -> 113.
    - REMOVE the CA-existing_race_id-UUID rule; REPLACE with: `existing_race_id` MUST be blank for ALL 113 rows (no pre-seeded Wave-2 races). Flag any non-blank existing_race_id as a problem.
    - ADD a field_status partition assertion: exactly 89 rows with `field_status=='decided'` and exactly 24 rows whose `field_status` contains `pending-primary`; any other field_status value is a problem.
    - Keep the non-open-seat-vacancy `incumbent_pid` non-empty check, BUT relax it so the 24 `pending-primary` MI/VA rows are exempt where a special-seated/new incumbent legitimately has a blank pid (e.g. VA-11 Walkinshaw not in DB). Reuse the `nominee_status in ('open-seat-vacancy','special-seated','vacancy')` exemption set for blank-pid rows.
    Print `PASS` with the partition summary on success; print `FAIL` + offending rows and exit 1 otherwise. Pure CSV-shape gate — no DB access.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && python3 scripts/diag-154-validate-field-table.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    `cd /c/EV-Accounts/backend && python3 scripts/diag-154-validate-field-table.py` prints `PASS` and exits 0 against the Task-1 CSV. The validator asserts: 113 rows; per-state PA17/IL17/OH15/GA14/NC14/MI13/NJ12/VA11; the 89-decided/24-pending field_status partition; all source_url + nominee_status non-empty; all existing_race_id blank; incumbent_pid non-empty except for exempt special/open/vacancy rows. Introducing a wrong row count or a bad field_status causes a `FAIL` + non-zero exit.
  </acceptance_criteria>
  <done>diag-154-validate-field-table.py exists, validates the 113-row CSV shape including the 89/24 partition and the all-blank existing_race_id rule, exits 0 on the valid artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Write 154-verify.sql (write-free DB baseline gate; does NOT assert MI/VA decided field)</name>
  <files>backend/scripts/154-verify.sql</files>
  <read_first>
    - backend/scripts/149-verify.sql (GATE STYLE TEMPLATE — `\set ON_ERROR_STOP on`, DO $$ DECLARE/BEGIN/END, per-assertion RAISE EXCEPTION/RAISE NOTICE, CREATE TEMP TABLE ON COMMIT DROP, the races->offices->districts join path)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-RESEARCH.md (§"Pattern 3: 154-verify.sql Gate Shape" — the 4 baseline assertions; this is a PRE-SEEDING gate, not 149's post-seeding shape)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (Plan 01 output — the expected 0-holder special-seat geo_ids to encode in the vacancy assertion)
  </read_first>
  <action>
    Author `backend/scripts/154-verify.sql` as a write-free pre-seeding baseline gate in the `149-verify.sql` style (`\set ON_ERROR_STOP on` + single `DO $$ ... END $$;`). SELECT-only; the only allowed write is `CREATE TEMP TABLE ... ON COMMIT DROP`. Assert exactly these read-only baseline facts (RESEARCH §Pattern 3):
    1. Per-state NATIONAL_LOWER district counts via `substr(d.geo_id,1,2)`: PA('42')=17, IL('17')=17, OH('39')=15, GA('13')=14, NC('37')=14, MI('26')=13, NJ('34')=12, VA('51')=11; total 113. RAISE EXCEPTION on any mismatch; RAISE NOTICE 'PASS' on the full set.
    2. Pre-seeding baseline: 0 active `2026-11-03` races for any of the 8 Wave-2 states (join essentials.races r -> elections el (el.election_date='2026-11-03') -> offices o -> districts d, filtered NATIONAL_LOWER + the 8 fips). RAISE EXCEPTION if != 0.
    3. Vacancy/holder invariant: every NATIONAL_LOWER district in the 8 states has exactly 1 incumbent holder EXCEPT the known 0-holder special seats from `154-incumbent-map.csv` (the resolved set — GA-13 '1313', NJ-11 '3411', VA-11 '5111', and GA-14 '1314' iff Plan 01 reported it 0-holder). Encode the expected 0-holder geo_id set in a TEMP TABLE; assert that the set of districts with `COUNT(politician_id) <> 1` equals exactly the expected set (no surprises in either direction). RAISE EXCEPTION + name the offending geo_ids on mismatch.
    CRITICAL: Do NOT assert any decided-field / nominee / race_candidates fact for MI or VA — both primaries are Aug 4, 2026; asserting they have nominees would false-fail (RESEARCH Pitfall 1; CONTEXT D-01). The gate asserts only the read-only DB baseline above; it has NO assertion that depends on MI/VA nominee data. Emit a final `RAISE NOTICE 'ALL ASSERTIONS PASSED (154 baseline)';`. Add a header comment documenting that this is a pre-seeding baseline gate and that MI/VA decided-field assertions are intentionally absent.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/154-verify.sql 2>&1 | grep -E "ALL ASSERTIONS PASSED|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/154-verify.sql` exits 0 and prints `ALL ASSERTIONS PASSED (154 baseline)` after per-assertion PASS notices for: (1) the 8-state district counts summing to 113, (2) 0 pre-seeded 2026-11-03 Wave-2 races, (3) the holder-invariant set matching exactly the expected 0-holder special seats. The script contains NO assertion referencing MI or VA nominees / 2026 race_candidates. `grep -i "race_candidates" scripts/154-verify.sql` returns no nominee-existence assertion for MI/VA. The only writes are `CREATE TEMP TABLE ... ON COMMIT DROP`.
  </acceptance_criteria>
  <done>154-verify.sql is a write-free pre-seeding baseline gate that asserts 8-state district counts (=113), zero pre-seeded 2026 Wave-2 races, and the exact 0-holder special-seat set — and asserts NOTHING about MI/VA nominees. Exits 0 against the live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator/gate -> production DB | `154-verify.sql` runs SELECT-only via existing `DATABASE_URL`. The Python validator touches no DB. Field-table assembly reads external public sources (state results / Wikipedia / Ballotpedia) — no writes anywhere. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-02 | Tampering | 154-verify.sql gate | accept | Write-free by construction: `\set ON_ERROR_STOP on`, SELECT-only, only `CREATE TEMP TABLE ... ON COMMIT DROP` writes (149-verify.sql precedent). No INSERT/UPDATE/DELETE into essentials|inform; never `--commit`. RESEARCH §Security Domain: ASVS N/A for a read-only diagnostic. |
| T-154-03 | Information Disclosure | field-table source citations | accept | Sources are public election records (state SoS results, Wikipedia, Ballotpedia). No PII beyond public-figure candidate names already in the DB. |

This phase is purely read-only diagnostic against the production database using existing credentials, plus public-source research — no new API surface, no user input, no network endpoints serving the app. ASVS V2-V6 are Not Applicable (RESEARCH §Security Domain).
</threat_model>

<verification>
- `python3 scripts/diag-154-validate-field-table.py` exits 0 (113 rows, 89/24 partition, all blank existing_race_id).
- `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/154-verify.sql` exits 0 with `ALL ASSERTIONS PASSED (154 baseline)`.
- `154-verify.sql` contains no MI/VA nominee assertion.
- `154-FIELD-TABLE.md` documents the four special seats with cited URLs.
</verification>

<success_criteria>
The verified Nov-3 field is locked for the 89 decided districts (every non-incumbent-nominee flagged from results sources), MI+VA's 24 districts are tagged pending-primary with no invented nominees, the four special seats are correctly resolved, and the write-free baseline gate proves the DB is ready for seeding without asserting any MI/VA decided-field fact.
</success_criteria>

<output>
Create `.planning/phases/154-field-resolution-stance-gap-diagnostic/154-02-SUMMARY.md` when done.
</output>
