---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 01
status: complete
completed: 2026-07-07
requirements: [D-01, D-02, D-11, DELIV-2]
migrations_applied: [1246]
---

# 164.1-01 SUMMARY — Dual-map opt-in backend code + D-11 Connected fallback + deploy

## What was built
The full dual-map opt-in code surface, deployed live to Render (`ev-accounts-api`, deploy `dep-d96at858nd3s73b5cqog` → `live`) and proven fail-safe with zero `G5200V26` rows present.

1. **Task 1 — catch-all exclusions (D-01/D-02).** `'G5200V26'` appended to the `NOT IN (...)` unknown-MTFCC fallback in BOTH guards: `geoIdGuard.ts` `MTFCC_DISTRICT_TYPE_GUARD` and the byte-for-byte inline duplicate in `essentialsService.ts` (reps feed). Lists stay identical; `NOT LIKE 'X%'` untouched. Commit `801e78ec`.
2. **Task 2 — vintage-preference opt-in.** `getElectionsByCoordinate` Part A now uses the RESEARCH-specified `JOIN LATERAL` (V26 branch gated on `d.district_type='NATIONAL_LOWER'`, UNION ALL fallback guarded by `NOT EXISTS` on a V26 row, `LIMIT 1`). Parameterized `$1/$2::float8` preserved; `fetchDistrictRaceRows` and `ELECTION_VISIBILITY_WINDOW` untouched. Commit `97f83117`.
3. **Task 3 — D-11 Connected-tier live fallback.** Migration `1246_resolve_congressional_2026_rpc.sql` (applied to PROD): `connect.resolve_congressional_2026(uuid)` — read-only SECURITY DEFINER, `SET search_path=''`, Vault+pgcrypto decrypt preamble cloned from the live `resolve_user_jurisdiction` (`extensions.pgp_sym_decrypt_bytea`, `public.ST_*` — the live analog uses `public.`, not `extensions.`, for PostGIS), `location_consent=true` filter, FIPS filter `substring(geo_id,1,2) IN ('47','29','01','22','49')`, returns NATIONAL_LOWER geo_id or NULL (NULL — not an exception — when no coords on file). Grants mirror the analog: `service_role` only. Route `essentials.ts`: `resolveCongressional2026()` helper + substitution in Path 1 (cached slots) AND Path 1.5 (post-`resolve_user_jurisdiction`, which is G5200-hardcoded); pair keeps mtfcc `'G5200'`; RPC error/null → cached behavior unchanged; no cache write. Commit `02123346`.
4. **Task 4 — deploy + fail-safe proof.** Pushed master, triggered `RENDER_DEPLOY_HOOK`, polled Render API to `live`. `/api/health` → 200. Re-ran ALL THREE prior coordinate smokes against PROD post-deploy: 161 GREEN (4/4 + TN severe negative), 162 GREEN (4/4 + MO severe negative), 163 GREEN (5/5 + AL 0102 + LA 2206 severe negatives). `connect.resolve_congressional_2026` returns NULL for an arbitrary uuid; `count(*) FROM geofence_boundaries WHERE mtfcc='G5200V26'` = 0 → fallback is a proven no-op today.

## Deviations
- The plan's Task-1 automated verify expected `grep -c G5200V26` = 1 per guard file; actual count is 2 because the plan's own action also mandates an explanatory comment containing the token. Acceptance criteria (NOT IN line present, tsc clean, no other branch altered) all met.
- `/health` lives at `/api/health` (mounted `app.use('/api/health', ...)`) — 200 confirmed there.

## Key facts for later plans
- RPC name: `connect.resolve_congressional_2026(p_user_id uuid) RETURNS text`; sentinel-probe callable via `service_role`; returns NULL unless the decrypted point is ST_Covers'd by a G5200V26 row in FIPS 47/29/01/22/49.
- Migration high-water mark is now **1246** (next free: 1247).
- Elections coordinate path prefers V26 per-geo_id automatically as each state's rows land — no further code changes needed by Wave-2 plans.

## Files
- `backend/src/lib/geoIdGuard.ts` (catch-all exclusion + comment)
- `backend/src/lib/essentialsService.ts` (inline duplicate exclusion + comment)
- `backend/src/lib/electionService.ts` (vintage-preference LATERAL)
- `backend/src/routes/essentials.ts` (REFRESHED_2026_FIPS + resolveCongressional2026 + Path 1/1.5 substitution)
- `backend/migrations/1246_resolve_congressional_2026_rpc.sql` (applied to PROD)

## Self-Check: PASSED
tsc clean; /api/health 200 post-deploy; 161/162/163 smokes GREEN unchanged; RPC exists, read-only, returns NULL today; all 5 must_have artifacts contain their required tokens.
