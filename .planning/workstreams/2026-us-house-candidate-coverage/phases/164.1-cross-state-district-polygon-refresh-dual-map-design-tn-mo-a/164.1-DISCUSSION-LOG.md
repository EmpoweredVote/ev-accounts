# Phase 164.1: Cross-State District Polygon Refresh + Dual-Map Design (TN/MO/AL/LA/UT) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-06
**Phase:** 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
**Areas discussed:** Dual-map backend design, MO referendum hedge, UT scope split vs 165, Polygon sourcing + verify bar

---

## Dual-map backend design

### Q1 — Data model for coexisting boundary vintages

| Option | Description | Selected |
|--------|-------------|----------|
| Parallel layer (Recommended) | New geofence_boundaries rows, same geo_id, distinct vintage discriminator; untouched consumers fail safe to old boundaries; only elections opts in | ✓ |
| Vintage columns | Use valid_from/valid_to properly; relax unique index; every consumer gains a vintage filter (fail-dangerous if one is missed) | |
| Separate table | New election_boundaries table; total isolation but forks GIST/import infra | |

**User's choice:** Parallel layer

### Q2 — Which consumers resolve on 2026 boundaries

| Option | Description | Selected |
|--------|-------------|----------|
| Content-type rule (Recommended) | 2026-ballot content → 2026 boundaries; current-representation content → old; researcher audits all ~10 ST_Covers call sites against the rule | ✓ |
| electionService only | Minimal touch; map/coverage services keep drawing old boundaries | |
| Hand-pick now | Decide the exact file list in discussion | |

**User's choice:** Content-type rule

### Q3 — Jan 2027 reps-feed transition

| Option | Description | Selected |
|--------|-------------|----------|
| Queued date-gated phase (Recommended) | Explicit phase ≥ Jan-3-2027 promotes 2026 polygons to canonical, retires old, re-resolves caches; 153/159/167 precedent | ✓ |
| Date-driven query switch | CURRENT_DATE branch flips automatically; untested path fires unattended in prod | |
| Config flag | boundary_vintage config flipped manually; runtime plumbing for a one-time event | |

**User's choice:** Queued date-gated phase

### Q4 — connect.user_districts cache handling

| Option | Description | Selected |
|--------|-------------|----------|
| No-touch + gate assert (Recommended) | Cache stays on current boundaries; gate asserts user_districts + essentials.offices byte-identical pre/post | ✓ |
| Proactive re-resolve now | Rejected premise — would show users their FUTURE rep | |
| Let planner decide | No locked invariant | |

**User's choice:** No-touch + gate assert

---

## MO referendum hedge

### Q1 — Sequencing vs ~Jul-27 certification

| Option | Description | Selected |
|--------|-------------|----------|
| Date-gate MO slice (Recommended) | Own plan executable ≥ certification decision; no MO polygon work before then; MO does not block Phase 165 | ✓ |
| Refresh MO now | Serves lookups sooner but risks revert + wrong-boundary window on MO-1/7/8 | |
| Import now, flip later | Polygons early, un-withhold gated — still moves surfacing MO-1/7/8 onto unsettled boundaries | |

**User's choice:** Date-gate MO slice
**Notes:** Key subtlety — early import affects the 3 surfacing MO districts, not just the 5 withheld ones.

### Q2 — Revert branch if referendum qualifies

| Option | Description | Selected |
|--------|-------------|----------|
| Divert to 167 (Recommended) | MO exits 164.1 (old polygons already correct); field re-resolution + re-wire + un-withhold → Phase 167 MO cluster | ✓ |
| Handle inside 164.1 | Absorb field re-resolution into this phase | |
| Decide at certification | No pre-commitment | |

**User's choice:** Divert to 167

---

## UT scope split vs 165

### Q1 — What 164.1 delivers for Utah

| Option | Description | Selected |
|--------|-------------|----------|
| Polygons + wiring contract (Recommended) | 164.1: UT 2026-vintage polygons (4901–4904) + written wiring contract (old→new incumbent correspondence; races wire to existing geo_id offices; incumbents as race_candidates on NEW district's race; offices untouched until Jan-2027); 165 executes | ✓ |
| Polygons only | 165's researcher works out re-key rules itself | |
| Full re-key in 164.1 | Rejected premise — re-keying offices now falsifies the reps feed | |

**User's choice:** Polygons + wiring contract
**Notes:** Resolves the roadmap-note vs 160-06 ambiguity over where "UT re-key" lives. No UT withholding mechanism ever needed.

---

## Polygon sourcing + verify bar

### Q1 — Shapefile source standard

| Option | Description | Selected |
|--------|-------------|----------|
| Official-first + RDH fallback (Recommended) | Official state GIS/legislature/court shapefiles preferred; Redistricting Data Hub fallback; per-state provenance recorded | ✓ |
| Official-only | Blocks a state if no official shapefile published | |
| Any reputable source | No preference ordering | |

**User's choice:** Official-first + RDH fallback

### Q2 — Verification before un-withholding

| Option | Description | Selected |
|--------|-------------|----------|
| 3-layer per state (Recommended) | Topology tiling sanity + per-district positive anchor coordinate + ≥1 differential point per state (elections→NEW, reps→OLD simultaneously) | ✓ |
| Positive coords only | Skips the dual-map differential proof | |
| State-level smoke | One coordinate per state | |

**User's choice:** 3-layer per state

### Q3 — Refresh scope within each state

| Option | Description | Selected |
|--------|-------------|----------|
| Whole-state (Recommended) | All districts per state get 2026-vintage polygons; per-state atomic flip; prior gate assertions flip per state | ✓ |
| Severe-only | Intra-state vintage mixing with overlaps/gaps | |

**User's choice:** Whole-state

---

## Claude's Discretion

- Exact vintage discriminator mechanism (mtfcc variant vs vintage column + partial unique index)
- Elections-query opt-in SQL shape; plan count/splitting
- Gate-edit mechanics for flipping the 161/162/163 severe assertions
- Choice of anchor/differential test coordinates
- Import script structure (clone/extend load-national-house-districts.ts vs new loader)
- UT wiring-contract artifact format

## Deferred Ideas

- Jan-2027 boundary-promotion phase (queued, date-gated ≥ 2027-01-03)
- MO revert branch → Phase 167 MO cluster (if referendum qualifies)
- LA-4 borderline spot-check folded into LA verify work
- Phase 165 UT seeding + incumbent pid re-link (executes the wiring contract)
