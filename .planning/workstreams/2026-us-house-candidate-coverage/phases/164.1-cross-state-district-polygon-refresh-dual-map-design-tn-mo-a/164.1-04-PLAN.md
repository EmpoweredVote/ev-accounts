---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 04
type: execute
wave: 2
depends_on: [164.1-01, 164.1-02]
files_modified:
  - backend/scripts/1641-tn-generate-polygon-import.mts
  - backend/migrations/1247_unwithhold_tn_2026_house_races.sql
  - backend/scripts/161-verify.sql
  - backend/scripts/161-coordinate-smoke.ts
autonomous: true
requirements: [D-08, D-09, D-10, DELIV-1, DELIV-3]
must_haves:
  truths:
    - "Every TN congressional district (geo_ids 4701-4709) has a 2026-vintage G5200V26 polygon"
    - "TN's 9 G5200V26 districts pass the D-10 3-layer verify bar (topology + per-district anchor + ≥1 differential point)"
    - "The 5 withheld TN severe races (4704,4705,4706,4708,4709) surface on /elections after re-pointing election_id from 'TN 2026 Congressional Redistricting - Polygon Pending' to 'TN 2026 Statewide General'"
    - "After the flip, 1641-coordinate-smoke.ts (re-run read-only) auto-detects the severe races surfacing and proves a SEVERE-district differential point for TN (NEW severe race on elections + OLD rep on reps feed)"
    - "161-verify.sql and 161-coordinate-smoke.ts assert the 5 TN districts as SURFACING (positive), not withheld"
    - "essentials.offices and essentials.geo_districts for TN (FIPS 47) are untouched (D-04)"
  artifacts:
    - path: "backend/scripts/1641-tn-generate-polygon-import.mts"
      provides: "TN 2026 polygon import (geofence_boundaries only)"
      contains: "G5200V26"
    - path: "backend/migrations/1247_unwithhold_tn_2026_house_races.sql"
      provides: "Guarded un-withhold UPDATE for the 5 TN severe races"
      contains: "Polygon Pending"
  key_links:
    - from: "backend/migrations/1247_unwithhold_tn_2026_house_races.sql"
      to: "essentials.races.election_id → 'TN 2026 Statewide General'"
      via: "guarded UPDATE where election_id = Polygon Pending"
      pattern: "TN 2026 Statewide General"
---

<objective>
Import Tennessee's enacted 2026 congressional polygons (HB 7003, signed 2026-05-07) as `G5200V26`, pass the full D-10 3-layer verify bar for TN, then un-withhold the 5 severe TN races (4704/4705/4706/4708/4709) by re-pointing their `election_id` to the TN general election — and flip the Phase-161 gate assertions to positive. After the flip, re-run the data-driven 1641 smoke so it proves the severe-district differential (which is boundary-only until the flip).

Purpose: Deliver TN's slice of D-09 (whole-state refresh) + D-10 (per-state-atomic verify-then-un-withhold) + DELIV-3 (un-withhold + gate flips). TN's polygons refresh from approximately-right to exactly-right shapes; the 5 severe districts parked on the past-dated "Polygon Pending" marker election become visible.
Output: TN import script, 9 G5200V26 TN rows, one un-withhold migration, and flipped Phase-161 gate scripts, all GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-PATTERNS.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-06-SUMMARY.md

<interfaces>
<!-- TN source (MEDIUM confidence — confirm exact artifact at execution time): -->
TN Comptroller GIS "Redistricting and Land Use Maps" (comptroller.tn.gov) — PDF + KMZ confirmed; direct .shp NOT confirmed. If only KMZ, convert/parse KMZ; if neither .shp nor KMZ is usable, fall back to Redistricting Data Hub (RDH) per D-08. Enactment: HB 7003 (2nd Extraordinary Session), signed 2026-05-07.
Import constants: MTFCC='G5200V26', SOURCE='tn_comptroller_2026' (or 'tn_rdh_2026' if fallback used), geo_ids 4701-4709, OMIT geo_districts insert.

TN severe set (withheld, from 161-06): geo_ids 4704,4705,4706,4708,4709 (NOT 4707 — 4707 is surfacing/non-severe).
Withheld marker election: 'TN 2026 Congressional Redistricting - Polygon Pending' (dated 2026-05-07). Un-withhold target: 'TN 2026 Statewide General'. office_id is never null and never changes.

Migration numbering: live high-water mark = 1245 as of 2026-07-07, BUT Plan 01 (Wave 1) now also authors a migration (the resolve_congressional_2026 RPC), so it will have taken the first free number ahead of this plan. Re-run `ls backend/migrations | grep -E '^[0-9]' | sort | tail` immediately before authoring and use the NEXT free number — the `1247_...` filename in files_modified is a PLACEHOLDER only. Parallel Phases 177/178 may also have taken numbers. Coordinate with Plan 05 (also allocates migration numbers) since execution is inline/sequential.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and run 1641-tn-generate-polygon-import.mts</name>
  <read_first>
    - backend/scripts/load-national-house-districts.ts (resolveColumn, downloadWithRedirects/extractZip/streamShapefile, the geofence_boundaries insert; the geo_districts insert to OMIT)
    - 164.1-RESEARCH.md "Import Pipeline" + "D-08" TN row + Assumption A2
    - 164.1-PATTERNS.md import-script section
  </read_first>
  <action>
    Confirm the TN source artifact first: attempt the Comptroller GIS .shp/KMZ; if unavailable, use the RDH fallback (D-08). Clone load-national-house-districts.ts into backend/scripts/1641-tn-generate-polygon-import.mts with: a header documenting TN provenance (HB 7003, 2026-05-07) + the EXPLICIT geo_districts-omitted note; the download/parse path for the confirmed source (shapefile via adm-zip/shapefile, or KMZ conversion, or RDH shapefile); resolveColumn() mapping to geo_ids 4701-4709; and inserts ONLY into geofence_boundaries with MTFCC='G5200V26', SOURCE set per the actual source used, `ON CONFLICT (geo_id,mtfcc) DO NOTHING`, ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON(...)),4326). Preserve --dry-run + DATABASE_URL check. Parameterize all inserts. Run --dry-run (expect 9), then real.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='47'" | grep -qx 9 && echo TN_9_ROWS</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) ... WHERE mtfcc='G5200V26' AND state='47'` returns exactly 9 (geo_ids 4701-4709)
    - `SELECT count(*) FROM essentials.geo_districts WHERE layer='us_house' AND geoid LIKE '47%'` unchanged
    - Script has no geo_districts INSERT; provenance + source recorded in the `source` column; all inserts parameterized
  </acceptance_criteria>
  <done>9 TN G5200V26 rows imported; geo_districts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the D-10 3-layer verify bar for TN (pre-flip)</name>
  <read_first>
    - backend/scripts/1641-verify.sql + backend/scripts/1641-coordinate-smoke.ts (Plan 02)
    - 164.1-RESEARCH.md "D-10: Verify Bar Mechanics" (all 3 layers) + Pitfall 3 (Layer-3 uncached live path + Connected geo-id path)
  </read_first>
  <action>
    Run 1641-verify.sql (TN/FIPS-47 Layer-1 tiling + area-ratio + D-04 NOTOUCH checksums now assert PASS) and 1641-coordinate-smoke.ts (TN Layer-2 per-district anchors + TN Layer-3). All must pass for TN BEFORE proceeding to Task 3. This is per-state-atomic (D-09) — TN's bar must pass independent of AL/LA/MO status.
    NOTE: TN's severe races are still withheld at this point, so the smoke's GUARANTEED Layer-3 differential uses a NON-SEVERE NEW district (the script selects it automatically), and the severe-district differential is asserted BOUNDARY-ONLY pre-flip (the script auto-detects the withheld state). The full severe-district race-surfacing differential is proven in Task 3 after the flip by re-running the same script (no edit needed).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql && node --import tsx scripts/1641-coordinate-smoke.ts && echo TN_VERIFY_BAR_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 1641-verify.sql exits 0 with TN Layer-1 tiling + area-ratio PASS
    - 1641-coordinate-smoke.ts exits 0 with a TN Layer-3 GUARANTEED differential (non-severe NEW district on elections, OLD district on reps feed, same coordinate) proven, plus the boundary-only severe differential pre-flip
    - D-04 NOTOUCH checksums for FIPS 47 pass
  </acceptance_criteria>
  <done>TN's 3-layer D-10 verify bar is GREEN pre-flip (guaranteed non-severe differential proven).</done>
</task>

<task type="auto">
  <name>Task 3: Un-withhold the 5 TN severe races, prove the severe differential, flip the Phase-161 gate</name>
  <read_first>
    - backend/migrations/1230_fix_edmonds_la5_to_la6_rekey.sql (guarded-UPDATE + BEGIN/COMMIT + idempotency-note migration convention)
    - backend/scripts/161-verify.sql (TN severe block) + backend/scripts/161-coordinate-smoke.ts (SAMPLES + any TN negatives)
    - backend/scripts/1641-coordinate-smoke.ts (re-run post-flip — it auto-proves the severe differential AND directly invokes connect.resolve_congressional_2026 via a transaction-scoped, always-ROLLBACK'd sentinel test row; makes no committed writes; do NOT edit it)
    - 164.1-RESEARCH.md "Un-Withhold Flip Surface" + 164.1-PATTERNS.md "Per-state un-withhold migrations" + "161/162/163-verify.sql" + "161/162/163-coordinate-smoke.ts" edit-mechanics sections
    - 161-06-SUMMARY.md (severe set + withholding mechanism)
  </read_first>
  <action>
    Re-check the live migration high-water mark FIRST (`ls backend/migrations | grep -E '^[0-9]' | sort | tail`) — Plan 01 and parallel Phases 177/178 may have advanced it; the number in files_modified is provisional. Author backend/migrations/{next}_unwithhold_tn_2026_house_races.sql: a BEGIN/COMMIT-wrapped, idempotent, guarded UPDATE of essentials.races that sets election_id to the id of 'TN 2026 Statewide General' WHERE the race's office → district geo_id is IN (4704,4705,4706,4708,4709) AND election_id currently equals the id of 'TN 2026 Congressional Redistricting - Polygon Pending' (the guard makes re-runs a no-op). Do NOT delete the 'Polygon Pending' marker election (historical artifact per 163-11). Resolve the 5 severe races via office_id → districts.geo_id. Apply the migration to PROD.
    Then RE-RUN 1641-coordinate-smoke.ts (unedited; it makes no committed writes — its D-11 RPC probe inserts a sentinel test row inside an always-ROLLBACK'd transaction): now that TN's severe races point at the general election, the script auto-detects them surfacing and proves the SEVERE-district differential (NEW severe race on elections + OLD rep on reps feed) in addition to the guaranteed non-severe one. It must exit 0.
    Then flip the Phase-161 gate assertions to positive (per the 163-11 standing instruction): in 161-verify.sql, change the TN severe block so the 5 geo_ids are asserted to point at the TN general election (merge them into the surfacing set); update the PASS NOTICE text + header. In 161-coordinate-smoke.ts, move the 5 TN severe entries out of any negative array into the positive SAMPLES array with appropriate minActive, and update the green summary string. Re-run the 161 gate — it must be GREEN with the TN districts now surfacing.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections e ON e.id=r.election_id WHERE d.geo_id IN ('4704','4705','4706','4708','4709') AND d.district_type='NATIONAL_LOWER' AND e.name='TN 2026 Statewide General'" | grep -qx 5 && node --import tsx scripts/1641-coordinate-smoke.ts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/161-verify.sql && node --import tsx scripts/161-coordinate-smoke.ts && echo TN_UNWITHHELD_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - The 5 severe TN races (4704,4705,4706,4708,4709) now have election_id = 'TN 2026 Statewide General'
    - Migration is BEGIN/COMMIT-wrapped, guarded on the OLD election_id, and idempotent (re-run = no-op)
    - The 'TN 2026 Congressional Redistricting - Polygon Pending' election row still exists (not deleted)
    - 1641-coordinate-smoke.ts (re-run, unedited) exits 0 proving the TN severe-district differential now surfaces a race
    - 161-verify.sql exits 0 with the TN block asserting all 9 districts surfacing
    - 161-coordinate-smoke.ts exits 0 with the 5 TN geo_ids now positive samples
  </acceptance_criteria>
  <done>5 TN severe races surfacing on the general election; severe differential proven by the re-run 1641 smoke; Phase-161 gate flipped to positive and GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shapefile source → import | Untrusted geometry inserted to geofence_boundaries |
| migration → prod races | A wrong-row UPDATE could surface/hide the wrong races |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-09 | Tampering | Un-withhold UPDATE hits wrong races | mitigate | Guard on OLD election_id + explicit geo_id set resolved via office→district join; BEGIN/COMMIT; idempotent; verified by exact count = 5 |
| T-1641-10 | Tampering / data integrity | Mis-imported TN geometry | mitigate | D-10 Layer-1 tiling + area-ratio must pass before Task 3; ST_* normalization |
| T-1641-11 | Injection (SQLi) | Shapefile-derived text | mitigate | Parameterized inserts throughout |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pg/shapefile/adm-zip already present |
</threat_model>

<verification>
- 9 TN G5200V26 rows; geo_districts/offices untouched.
- D-10 3-layer bar GREEN for TN (guaranteed non-severe differential pre-flip; severe differential post-flip via the re-run smoke).
- 5 severe races re-pointed to TN general; 161 gate GREEN.
</verification>

<success_criteria>
TN 2026 polygons imported as G5200V26, D-10 3-layer verify bar passes, the 5 severe TN races surface on the general election, the severe-district differential is proven post-flip by the re-run data-driven smoke, and the Phase-161 gate asserts them positive — all GREEN, per-state-atomic, offices/geo_districts untouched.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
