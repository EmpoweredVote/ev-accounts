---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 03
type: execute
wave: 2
depends_on: [164.1-01, 164.1-02]
files_modified:
  - backend/scripts/1641-ut-generate-polygon-import.mts
  - .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md
autonomous: true
requirements: [D-07, D-08, D-09, DELIV-1, DELIV-4]
must_haves:
  truths:
    - "Every UT congressional district (geo_ids 4901-4904) has a 2026-vintage G5200V26 polygon in essentials.geofence_boundaries"
    - "UT's 4 G5200V26 districts tile Utah (Layer-1 topology passes for FIPS 49)"
    - "essentials.geo_districts and essentials.offices for UT (FIPS 49) are untouched by the import (D-04)"
    - "A written UT wiring contract records the old→new incumbent correspondence and the rule Phase 165 must follow"
  artifacts:
    - path: "backend/scripts/1641-ut-generate-polygon-import.mts"
      provides: "UT 2026 polygon import (ArcGIS REST GeoJSON → geofence_boundaries only)"
      contains: "G5200V26"
    - path: ".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md"
      provides: "Old→new UT incumbent correspondence + Phase 165 wiring rule"
      contains: "Moore"
  key_links:
    - from: "1641-ut-generate-polygon-import.mts"
      to: "essentials.geofence_boundaries (mtfcc='G5200V26', source='ut_agrc_2026')"
      via: "ON CONFLICT (geo_id, mtfcc) DO NOTHING insert"
      pattern: "G5200V26"
---

<objective>
Import Utah's 2026-2032 congressional-district polygons as `G5200V26` rows (geo_ids 4901-4904 persist; only shapes change), verify they tile Utah, and author the written UT wiring contract. Utah is the ONLY part of this phase that blocks Phase 165 — so it runs in the earliest possible wave after the code + harness foundation.

Purpose: Deliver D-07 (UT polygons + wiring contract). Phase 165 wires new-map UT races to the EXISTING geo_id offices and places incumbents on their NEW district's race as `race_candidates`; `essentials.offices` is never touched until the Jan-2027 flip phase. UT has NO withholding mechanism and no un-withhold step — UT races don't exist until Phase 165 and are born onto correct 2026 boundaries.
Output: A UT import script (geofence_boundaries only), 4 G5200V26 UT rows on PROD, and the wiring-contract artifact Phase 165 consumes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-PATTERNS.md

<interfaces>
<!-- UT source (confirmed HIGH confidence, live this planning session): -->
Utah AGRC/SGID ArcGIS REST FeatureServer:
  https://services1.arcgis.com/99lidPhWCzftIe9K/ArcGIS/rest/services/political_us_congress_districts_2026_to_2032/FeatureServer/0/query?where=1=1&outFields=*&f=geojson
Returns GeoJSON directly over HTTPS — NO shapefile download/unzip needed for UT (skip adm-zip/shapefile; use https.get/fetch).

UT correspondence (from 160-06-SUMMARY.md NOTE-UT-REDISTRICTED-2026, cited in CONTEXT canonical_refs):
  Moore old-D1 → new-D2; Maloy old-D2 → new-D3; Kennedy old-D3 → new-D4; Owens retired; new-D1 open (McAdams D field). geo_ids 4901-4904 persist; only shapes + incumbent↔geo_id mapping change.

Import constants for UT: MTFCC='G5200V26', SOURCE='ut_agrc_2026', omit the essentials.geo_districts insert entirely (Pitfall 2 / D-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and run 1641-ut-generate-polygon-import.mts</name>
  <read_first>
    - backend/scripts/load-national-house-districts.ts (full file — resolveColumn(), the idempotent geofence_boundaries insert at lines ~244-258, and the geo_districts insert that MUST be omitted)
    - 164.1-RESEARCH.md "Import Pipeline" (clone-and-adapt rationale; UT ArcGIS REST direct-GeoJSON path; geo_districts drop) + "D-08" UT row
    - 164.1-PATTERNS.md "1641-{tn,al,la,ut,mo}-generate-polygon-import.mts" section (header/constants/insert deviations)
  </read_first>
  <action>
    Clone `load-national-house-districts.ts`'s primitives into backend/scripts/1641-ut-generate-polygon-import.mts:
    - Header comment documenting UT provenance (Utah AGRC/SGID 2026-2032 map, LWV v. Utah Legislature remedial map eff. 2025-11-10) and an EXPLICIT note that the `essentials.geo_districts` insert is intentionally OMITTED (D-04 no-touch / Pitfall 2).
    - Fetch GeoJSON directly from the ArcGIS REST FeatureServer query endpoint (the interfaces URL) via https.get/fetch — no adm-zip/shapefile for UT. Reuse the resolveColumn() pattern to map the FeatureServer's district-number field to geo_id (state FIPS 49 + 2-digit district → 4901, 4902, 4903, 4904).
    - Insert ONLY into essentials.geofence_boundaries with `MTFCC='G5200V26'`, `SOURCE='ut_agrc_2026'`, using the exact `ON CONFLICT (geo_id, mtfcc) DO NOTHING` shape + ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON(...)),4326). Do NOT insert into essentials.geo_districts.
    - Preserve `--dry-run` support and the DATABASE_URL presence check verbatim. Parameterize all inserts (never interpolate shapefile-derived name/geo_id text).
    - Run with `--dry-run` first (expect 4 features), then run for real. Expect 4 inserted (or already_exists on re-run — idempotent).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='49'" | grep -qx 4 && echo UT_4_ROWS</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='49'` returns exactly 4 (geo_ids 4901,4902,4903,4904)
    - `SELECT count(*) FROM essentials.geo_districts WHERE layer='us_house' AND geoid LIKE '49%'` is UNCHANGED from before the import (import omitted geo_districts)
    - Script has no essentials.geo_districts INSERT block; all inserts parameterized
    - Re-running the script is a no-op (already_exists via ON CONFLICT)
  </acceptance_criteria>
  <done>4 UT G5200V26 rows exist in geofence_boundaries; geo_districts untouched; script idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Verify UT topology + no-touch + live 2026 resolution</name>
  <read_first>
    - backend/scripts/1641-verify.sql (authored in Plan 02)
    - 164.1-RESEARCH.md "D-10 ... Layer 1" + "Sampling Rate"
  </read_first>
  <action>
    Run 1641-verify.sql (it will now light up the UT/FIPS-49 blocks: Layer-1 tiling of Utah by the 4 G5200V26 districts + per-district area-ratio sanity vs the G5200 counterparts + D-04 NOTOUCH for FIPS 49). Then, against the deployed dual-map code from Plan 01, confirm that an interior point of a UT G5200V26 district resolves to the 2026 boundary via the live coordinate path (there are no UT races yet, so assert the geometry/geo_id resolved is the G5200V26 one, not that a race surfaces). UT has NO un-withhold step and NO Layer-2-race / Layer-3-differential requirement (those apply only to states with withheld races).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql && echo UT_TOPOLOGY_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 1641-verify.sql exits 0 with the UT/FIPS-49 Layer-1 tiling + area-ratio blocks now asserting PASS
    - D-04 NOTOUCH block for FIPS 49 passes (geo_districts + offices unchanged)
    - A UT interior coordinate resolves to a G5200V26 UT boundary via the deployed live path
  </acceptance_criteria>
  <done>UT topology + no-touch green; UT resolves on 2026 boundaries via the deployed code.</done>
</task>

<task type="auto">
  <name>Task 3: Author the UT wiring contract artifact</name>
  <read_first>
    - .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-tn-correspondence-audit.md (the genre analog — research artifact consumed by a later phase)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-06-SUMMARY.md (NOTE-UT-REDISTRICTED-2026 — the correspondence source to cite, not re-derive)
    - 164.1-CONTEXT.md D-07 + 164.1-PATTERNS.md "UT wiring contract artifact" section
  </read_first>
  <action>
    Create .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md following the 161-tn-correspondence-audit.md structure. It MUST state up front: "No DB writes. No code. essentials.offices was not touched. Pure phase artifact consumed by Phase 165." Include:
    - A correspondence table: old_geo_id | old_incumbent | new_geo_id | new_incumbent | notes — Moore old-D1(4901)→new-D2(4902); Maloy old-D2(4902)→new-D3(4903); Kennedy old-D3(4903)→new-D4(4904); Owens (old-D4/4904) retired; new-D1(4901) open (McAdams D field per NOTE-UT-REDISTRICTED-2026).
    - An explicit Phase-165 wiring RULE: "Phase 165 wires new-map races to the EXISTING geo_id offices (4901-4904 persist); incumbents go on their NEW district's race as race_candidates (candidacy ≠ office); essentials.offices is never touched until the Jan-2027 flip phase re-keys it. No UT withholding mechanism is needed — UT races are born onto correct 2026 boundaries."
    - Cite 160-06-SUMMARY.md's NOTE-UT-REDISTRICTED-2026 paragraph as the background source (do not re-derive the correspondence).
  </action>
  <verify>
    <automated>test -f ".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md" && grep -q "Moore" ".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md" && grep -q "4901" ".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md" && echo CONTRACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and contains the full old→new correspondence table (Moore/Maloy/Kennedy/Owens/new-D1)
    - It states the Phase-165 wiring rule (offices untouched; incumbents as race_candidates on their NEW district)
    - It cites 160-06-SUMMARY.md NOTE-UT-REDISTRICTED-2026
    - It declares "No DB writes / offices not touched"
  </acceptance_criteria>
  <done>UT wiring contract artifact exists with the correspondence table + Phase-165 rule + source citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ArcGIS REST → import script | Untrusted GeoJSON parsed and inserted into geofence_boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-06 | Tampering / data integrity | Malformed geometry from ArcGIS feed | mitigate | ST_Force2D/ST_SetSRID/ST_GeomFromGeoJSON normalization + Layer-1 tiling/area-ratio catches bad polygons before any consumer opts in |
| T-1641-07 | Tampering | Accidental geo_districts overwrite (D-04 break) | mitigate | geo_districts INSERT omitted from the script; NOTOUCH assertion for FIPS 49 |
| T-1641-08 | Injection (SQLi) | Shapefile/GeoJSON-derived name/geo_id | mitigate | Parameterized pool.query throughout; no interpolation |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pg already present; UT uses direct GeoJSON fetch, no new deps |
</threat_model>

<verification>
- 4 UT G5200V26 rows; geo_districts unchanged.
- 1641-verify.sql UT/FIPS-49 blocks GREEN.
- Wiring-contract artifact present with correspondence + rule + citation.
</verification>

<success_criteria>
UT 2026 polygons imported as G5200V26 (geo_ids 4901-4904), tile Utah, geo_districts/offices untouched, resolve on 2026 boundaries via deployed code, and a written UT wiring contract exists — Phase 165's only 164.1 dependency is satisfied.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-03-SUMMARY.md` when done.
</output>
