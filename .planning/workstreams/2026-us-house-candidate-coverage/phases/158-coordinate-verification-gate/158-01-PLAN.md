---
phase: 158-coordinate-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/158-verify.sql
  - backend/scripts/158-coordinate-smoke.ts
requirements: [USHC2-06]
autonomous: true

must_haves:
  truths:
    - "158-verify.sql runs read-only against prod .env and exits 0 with final stdout line 'ALL ASSERTIONS PASSED'"
    - "USHC2-06-SCOPE confirms exactly PA 17 + IL 17 + OH 15 + GA 14 + NC 14 + NJ 12 = 89 distinct NATIONAL_LOWER races"
    - "USHC2-06-ACTIVE proves every one of the 89 races has >=1 active candidate; <2-active count is a NOTICE (uncontested allowance NJ-8/PA-3), never a failure"
    - "USHC2-06-NULLPID proves 0 active race_candidates with NULL politician_id across all 89 districts"
    - "USHC2-06-DUPNAME proves 0 duplicate lower(full_name) among active candidates within each state"
    - "USHC2-06-DUPINCUMBENT proves 0 politician_id active in 2+ distinct races across the 89 (v2.4 two-Andy-Barrs invariant)"
    - "USHC2-06-UNSOURCED is an EXISTENCE check (answer row lacking a sourced context row) that returns 0 — NOT a per-candidate coverage assertion"
    - "USHC2-06-VACANCY proves GA-13 (1313) Clark + Chavez both active, non-null pid, neither is_incumbent=true; NJ-12 (3412) Watson Coleman 0 active + Hamawy/Mele active; NJ-8 (3408) exactly 1 active Menendez"
    - "USHC2-06-PARTY proves essentials.race_candidates has no party/party_affiliation column (antipartisan invariant)"
    - "158-coordinate-smoke.ts runs read-only against prod .env, exits 0, prints 'COORDINATE SMOKE GREEN' surfacing 6/6 states with a challenger (active is_incumbent=false >=1) present in each in-district sample"
  artifacts:
    - path: "backend/scripts/158-verify.sql"
      provides: "Consolidated read-only SQL milestone gate over all 89 decided-state Wave-2 districts"
      contains: "USHC2-06-SCOPE"
    - path: "backend/scripts/158-coordinate-smoke.ts"
      provides: "6-state ST_Covers coordinate-surfacing smoke with Pitfall-5 challenger-present assertion"
      contains: "COORDINATE SMOKE GREEN"
  key_links:
    - from: "backend/scripts/158-verify.sql"
      to: "essentials.elections (PA/IL/OH/GA/NC/NJ 2026 Statewide General)"
      via: "SELECT id INTO ..._eid FROM essentials.elections WHERE name = ..."
      pattern: "2026 Statewide General"
    - from: "backend/scripts/158-coordinate-smoke.ts"
      to: "src/lib/electionService.ts getElectionsByCoordinate Part A"
      via: "ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(lng,lat),4326)) join"
      pattern: "ST_Covers"
---

<objective>
Author the consolidated Phase 158 Coordinate Verification Gate for the v2.21 Wave-2 milestone: a
read-only SQL gate (`158-verify.sql`) that asserts milestone-level invariants fresh across all 89
decided-state US House districts (PA 17 / IL 17 / OH 15 / GA 14 / NC 14 / NJ 12), plus a coordinate
surfacing smoke (`158-coordinate-smoke.ts`) that proves one in-district address per state resolves to
its House race with the full challenger-inclusive candidate field.

Purpose: Prove the three decided-state seeding phases (155 PA+IL / 156 OH+GA+NC / 157 NJ) delivered a
structurally correct, party-free, fully-linked field at the full 89-district scale — the correctness
that only makes sense at milestone scope, not per-phase. Mirrors the Phase 152 v2.20 consolidated gate.

Output: `backend/scripts/158-verify.sql` + `backend/scripts/158-coordinate-smoke.ts`, both write-free,
both green against prod .env. MI (13) + VA (11) are seeded and asserted separately by the date-gated
Phase 159 (both primaries Aug 4, 2026); the full 113-district milestone is the union of this gate +
Phase 159's MI+VA gate. Do NOT assert MI or VA here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Analog scripts — model the new files structurally on these (do NOT copy per-candidate skip pins here).
@backend/scripts/152-verify.sql
@backend/scripts/152-coordinate-smoke.ts
@backend/scripts/155-verify.sql
@backend/scripts/156-verify.sql
@backend/scripts/157-verify.sql
@backend/scripts/157-coordinate-smoke.ts
@backend/src/lib/electionService.ts

<interfaces>
<!-- Prod-verified election ids + geo prefixes (from the three per-phase gate headers). -->
<!-- Resolve by EXACT name at runtime (do NOT hardcode ids in the gate body); RAISE EXCEPTION on NULL. -->

essentials.elections (resolve by name):
  'PA 2026 Statewide General'  — ed54a0a5-4204-462b-9f3e-c3debed12501  — PA FIPS 42 / geo 4201..4217 (17 races)
  'IL 2026 Statewide General'  — 804d07cf-11c9-4e3d-a85d-9ff8cbffb828  — IL FIPS 17 / geo 1701..1717 (17 races)
  'OH 2026 Statewide General'  — 32657ce8-8606-420b-8782-ca2c7f01c5ae  — OH FIPS 39 / geo 3901..3915 (15 races)
  'GA 2026 Statewide General'  — e4d22b2e-2c87-4fec-9b5a-c0680e99d2c7  — GA FIPS 13 / geo 1301..1314 (14 races; 1313 = vacancy)
  'NC 2026 Statewide General'  — 12e0bce3-d4c3-4e09-ba5d-18e8bc68be5e  — NC FIPS 37 / geo 3701..3714 (14 races)
  'NJ 2026 Statewide General'  — cdb3f77b-7f10-4d0a-ae79-0d8329cbd026  — NJ FIPS 34 / geo 3401..3412 (12 races; 3408 = uncontested)

Geo prefixes (substr(d.geo_id,1,2)): PA='42', IL='17', OH='39', GA='13', NC='37', NJ='34'.

getElectionsByCoordinate Part A (src/lib/electionService.ts ~L281-303) — the surfacing join to mirror:
  JOIN essentials.geofence_boundaries gb
    ON gb.geo_id = d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc = d.mtfcc)
  WHERE ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($lng, $lat), 4326))

State-specific wrinkles the gate + smoke MUST tolerate (do NOT let them false-fail):
  GA-13 (geo 1313) — TRUE VACANCY (David Scott died Apr 2026): NO incumbent. Assert Jasmine Clark AND
    Jonathan Chavez both active, non-null pid, neither is_incumbent=true.
  NJ-12 (geo 3412) — RETIREMENT (Watson Coleman a75a3e6e-31ff-4870-8aea-501e41326fd1): 0 active rows for
    her; nominees Adam Hamawy + Gregg Mele active; NO vacancy office.
  NJ-8 (geo 3408)  — UNCONTESTED: exactly 1 active = Robert Menendez (fc7a00d6-c552-4627-87f7-b0fc5cfe486c,
    is_incumbent=true), 0 challengers. Documented single-candidate general — an allowance, NOT a failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 158-verify.sql — consolidated read-only 89-district milestone gate</name>
  <files>backend/scripts/158-verify.sql</files>
  <read_first>
    - backend/scripts/152-verify.sql (THE structural analog — 8 labeled assertions, single _house TEMP TABLE, DO $$ block; clone its shape re-scoped from 4 states/144 to 6 states/89)
    - backend/scripts/155-verify.sql (PA/IL geo prefixes 42/17, PA-3 Rabb uncontested allowance, election-id-by-name resolution)
    - backend/scripts/156-verify.sql (OH/GA/NC geo prefixes 39/13/37, the GA-13 D-04-GA13 vacancy handling to fold into USHC2-06-VACANCY)
    - backend/scripts/157-verify.sql (NJ geo prefix 34, the NJ-12 retirement + NJ-8 uncontested handling to fold into USHC2-06-VACANCY)
  </read_first>
  <action>
    Create backend/scripts/158-verify.sql modeled structurally on 152-verify.sql (the v2.20 consolidated
    gate), re-scoped from 4 states/144 districts to the 6 decided Wave-2 states / 89 districts. Open with
    `\set ON_ERROR_STOP on`. Precede the DO block with a header comment block covering: (a) read-only /
    write-free rationale (SELECT-only + CREATE TEMP TABLE ... ON COMMIT DROP only; no INSERT/UPDATE/DELETE,
    no --commit, no migration, no deploy); (b) the six-election scope with geo prefixes (PA=42/17 races,
    IL=17/17, OH=39/15, GA=13/14, NC=37/14, NJ=34/12 = 89); (c) a note that MI (13) + VA (11) are asserted
    by the date-gated Phase 159 and the full 113-district milestone is the union of the two gates — this
    gate MUST NOT reference MI or VA; (d) a note that per-candidate granular stance/headshot honest-skip
    pins are OWNED by the three passing per-phase gates (155/156/157-verify.sql) and are NOT re-pinned here
    — this gate asserts milestone INVARIANTS fresh; (e) the exact read-only run command.

    Resolve all six election ids by EXACT name at runtime (`SELECT id INTO <st>_eid FROM essentials.elections
    WHERE name = '<ST> 2026 Statewide General'`) and RAISE EXCEPTION if ANY is NULL (report all six). Build
    ONE working-set TEMP TABLE `_house ON COMMIT DROP` with a `st` CASE column (PA/IL/OH/GA/NC/NJ keyed on
    election_id) plus race_id, geo_id, race_desc (r.description), rc_id, politician_id, full_name,
    candidate_status, is_incumbent, and p.external_id — sourced from essentials.races JOIN offices JOIN
    districts LEFT JOIN race_candidates LEFT JOIN politicians, scoped WHERE r.election_id IN (the 6 eids)
    AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('42','17','39','13','37','34'). Every
    subsequent assertion reads from `_house` (never re-queries the base tables except the party structural
    check + the unsourced existence check against inform tables).

    Author these 8 labeled assertions inside one DO $$ block, each ending in either RAISE EXCEPTION (fail)
    or RAISE NOTICE 'PASS ...' with a trailing `-- criterion N` comment mapping to the ROADMAP success
    criteria; end the block with RAISE NOTICE 'ALL ASSERTIONS PASSED (...)' listing every assertion name:

    1. USHC2-06-SCOPE (criterion 1) — per-state COUNT(DISTINCT race_id) FILTER: assert PA=17, IL=17, OH=15,
       GA=14, NC=14, NJ=12 (integer literals) AND total COUNT(DISTINCT race_id)=89.
    2. USHC2-06-ACTIVE (criterion 1/3) — 0 races with <1 active candidate (RAISE EXCEPTION if any); then a
       RAISE NOTICE reporting the count of races with <2 active as an explicit uncontested-seat allowance
       (NJ-8 Menendez-only, PA-3 Rabb, and any other genuinely safe single-candidate seat) — NEVER a failure.
    3. USHC2-06-NULLPID (criterion 3) — 0 active race_candidates with NULL politician_id across all 89.
    4. USHC2-06-DUPNAME (criterion 2) — 0 duplicate lower(full_name) among ACTIVE candidates GROUP BY st
       (per-state; cross-state same-name is not an error).
    5. USHC2-06-DUPINCUMBENT (criterion 2) — 0 politician_id active in 2+ distinct race_id across the 89
       (GROUP BY politician_id HAVING COUNT(DISTINCT race_id) > 1); the v2.4 two-Andy-Barrs invariant.
    6. USHC2-06-UNSOURCED (criterion 2) — EXISTENCE check: 0 rows in inform.politician_answers for any
       politician_id that is active-in-_house where NOT EXISTS a matching inform.politician_context row with
       sources IS NOT NULL AND array_length(sources,1) >= 1. This MUST NOT be a per-candidate coverage
       assertion (that would false-fail on all-partial incumbents + deferred/honest-skip candidates — the
       149/155 chairs-not-polarity standard). Mirror 152-verify.sql's USHC-06-UNSOURCED exactly.
    7. USHC2-06-VACANCY (criterion 1/3) — the state-wrinkle pins as sub-assertions: GA-13 (st='GA',
       geo_id='1313') Jasmine Clark AND Jonathan Chavez both present active with non-null pid AND
       0 active is_incumbent=true rows in 1313; NJ-12 (st='NJ', geo_id='3412') Watson Coleman
       (a75a3e6e-31ff-4870-8aea-501e41326fd1) appears 0 times active AND Adam Hamawy + Gregg Mele both
       active; NJ-8 (st='NJ', geo_id='3408') exactly 1 active row = Robert Menendez
       (fc7a00d6-c552-4627-87f7-b0fc5cfe486c) is_incumbent=true.
    8. USHC2-06-PARTY (criterion 3) — information_schema.columns check: essentials.race_candidates has 0
       columns named 'party' or 'party_affiliation' (party reads from races.primary_party only —
       antipartisan structural invariant).

    Do NOT re-pin the per-candidate stance/headshot honest-skip lists from 155/156/157 — reference their
    ownership in the header comment only. Keep the file write-free: the only DDL permitted is CREATE TEMP
    TABLE ... ON COMMIT DROP.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/158-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - Exact run command `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/158-verify.sql` exits 0.
    - Final stdout line is exactly `ALL ASSERTIONS PASSED` (with the parenthetical listing all 8 assertion names).
    - All 8 labeled assertion names present in the file: USHC2-06-SCOPE, USHC2-06-ACTIVE, USHC2-06-NULLPID, USHC2-06-DUPNAME, USHC2-06-DUPINCUMBENT, USHC2-06-UNSOURCED, USHC2-06-VACANCY, USHC2-06-PARTY.
    - USHC2-06-SCOPE NOTICE confirms `PA 17 + IL 17 + OH 15 + GA 14 + NC 14 + NJ 12 = 89` distinct NATIONAL_LOWER races.
    - USHC2-06-ACTIVE reports the <2-active count as a NOTICE allowance, not a RAISE EXCEPTION.
    - File is write-free: contains no INSERT/UPDATE/DELETE against essentials|inform, no `--commit`; only CREATE TEMP TABLE ... ON COMMIT DROP.
    - Header comment states MI+VA asserted by Phase 159 and per-candidate skip pins owned by 155/156/157-verify.sql.
  </acceptance_criteria>
  <done>158-verify.sql exists, runs read-only against prod .env, exits 0, prints ALL ASSERTIONS PASSED; all 8 assertions labeled + mapped to ROADMAP criteria; scope confirmed 89 across 6 states.</done>
</task>

<task type="auto">
  <name>Task 2: Author 158-coordinate-smoke.ts — 6-state ST_Covers surfacing smoke with challenger-present guard</name>
  <files>backend/scripts/158-coordinate-smoke.ts</files>
  <read_first>
    - backend/scripts/152-coordinate-smoke.ts (THE structural analog — per-state STATE_CONFIG, ST_PointOnSurface centroid, ST_Covers surfacing query, minActive + challenger + null_pid checks, MIN_DISTRICTS gate; clone re-scoped to 6 states)
    - backend/scripts/157-coordinate-smoke.ts (the most recent smoke — reuse its active/challenger/incumbent COUNT FILTER pattern and open-seat/uncontested sample handling if a wrinkle sample is chosen)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate Part A ~L281-303 — the ST_Covers join the smoke mirrors, mtfcc-tolerant geofence match)
  </read_first>
  <action>
    Create backend/scripts/158-coordinate-smoke.ts modeled on 152-coordinate-smoke.ts, extended from 4 to
    6 states. Import `pool` from '../src/lib/db.js'. Define a STATE_CONFIG map for PA/IL/OH/GA/NC/NJ giving
    each state its exact election name ('<ST> 2026 Statewide General') and geo prefix (PA=42, IL=17, OH=39,
    GA=13, NC=37, NJ=34). Resolve all six election ids up front by exact name and throw if any is missing.

    Pick ONE contested in-district sample per state (a district with an incumbent AND at least one
    challenger, so the Pitfall-5 challenger-present guard is exercised), minActive=2:
      PA geoId '4201' (PA-1 Fitzpatrick + Harvie, contested)
      IL geoId '1701' (IL-1 Jackson + Maxwell, contested)
      OH geoId '3901' (OH-1 Landsman + Conroy, contested)
      GA geoId '1305' (GA-5 Nikema Williams + Salvesen, renominated-contested)
      NC geoId '3706' (NC-6 McDowell + Jefferson, contested)
      NJ geoId '3406' (NJ-6 Pallone + Herzig, contested)

    For each sample: compute the interior centroid of that district's NATIONAL_LOWER geofence via
    public.ST_X/ST_Y(public.ST_PointOnSurface(gb.geometry)) (mtfcc-tolerant join, LIMIT 1), then run the
    surfacing query mirroring getElectionsByCoordinate Part A — JOIN geofence_boundaries gb ON gb.geo_id=d.geo_id
    AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc), WHERE r.election_id=$eid AND
    d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)=$geoPrefix AND ST_Covers(gb.geometry,
    ST_SetSRID(ST_MakePoint($lng,$lat),4326)) GROUP BY r.id — selecting COUNT(*) FILTER active,
    COUNT(*) FILTER active AND is_incumbent=false AS challengers, COUNT(*) FILTER active AND politician_id
    IS NULL AS null_pid, and array_agg(lower(full_name)) FILTER active. Assert per sample: exactly 1 House
    race surfaced (>1 = fail), active >= minActive (2), challengers >= 1 (Pitfall-5 two-path guard —
    incumbent-only is a failure), null_pid = 0. Count a clean sample toward `surfaced`.

    Set MIN_DISTRICTS = 6 and require all 6 states to surface cleanly; on any failure collect messages and
    process.exit(1). On success print a final line beginning `COORDINATE SMOKE GREEN` stating 6/6 states
    surface their US House race with the full challenger-inclusive field (PA/IL/OH/GA/NC/NJ), then
    `await pool.end()`. End with `main().catch((e) => { console.error(e); process.exit(1); });`. SELECT-only —
    no writes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/158-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exact run command `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/158-coordinate-smoke.ts` exits 0.
    - Final stdout line begins `COORDINATE SMOKE GREEN` and reports 6/6 states surfacing (PA/IL/OH/GA/NC/NJ).
    - Each of the 6 samples prints a PASS line showing >=2 active AND >=1 challenger AND 0 null pid.
    - MIN_DISTRICTS = 6 and the script fails (exit 1) if fewer than 6 states surface cleanly.
    - The surfacing query mirrors getElectionsByCoordinate Part A (mtfcc-tolerant ST_Covers geofence join) and is scoped by election id + NATIONAL_LOWER + per-state geo prefix.
    - Script is SELECT-only (no INSERT/UPDATE/DELETE); imports pool from ../src/lib/db.js.
  </acceptance_criteria>
  <done>158-coordinate-smoke.ts exists, runs read-only against prod .env, exits 0, prints COORDINATE SMOKE GREEN 6/6 with a challenger present in every state sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script → prod DB | The gate reads live prod data via DATABASE_URL; a mis-scoped or write-bearing query could mutate or misreport milestone state |
| assertion logic → milestone truth | The gate's assertions ARE the milestone acceptance signal; a relaxed or false-passing assertion silently ships a broken field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-01 | Tampering | 158-verify.sql relaxed to force green | mitigate | Assertions are exact integer-literal counts (89 total, per-state 17/17/15/14/14/12) + 0-invariants; USHC2-06-UNSOURCED is the same existence check as the passing 152 gate; no assertion is softened to a NOTICE except the documented uncontested-seat allowance. Acceptance criteria require the exact `ALL ASSERTIONS PASSED` final line. |
| T-158-02 | Repudiation | uncontested seat false-fails "every race >=2 active" | mitigate | Hard floor is >=1 active per race (RAISE EXCEPTION); the <2-active count is reported as a RAISE NOTICE allowance only (NJ-8 Menendez-only, PA-3 Rabb), never a failure — mirrors 152/155/157. |
| T-158-03 | Information Disclosure | retired incumbent silently surfaced as active | mitigate | USHC2-06-VACANCY pins NJ-12 Watson Coleman 0-active + GA-13 0-incumbent explicitly; the coordinate smoke's Pitfall-5 guard (challengers >= 1) exercises the two-path field so an incumbent-only regression fails. |
| T-158-04 | Tampering | gate writes to prod essentials/inform | mitigate | WRITE-FREE mandate: SELECT-only + CREATE TEMP TABLE ... ON COMMIT DROP only; no INSERT/UPDATE/DELETE, no --commit, no migration, no deploy. Acceptance criteria assert the file contains no write DML. |
| T-158-05 | Spoofing | wrong election resolved / MI+VA leakage | mitigate | Elections resolved by EXACT name with RAISE EXCEPTION on NULL; scope pinned to the 6 geo prefixes; header comment + scope literals exclude MI/VA (asserted by Phase 159). |
</threat_model>

<verification>
Run both scripts read-only against prod .env from backend/:

1. SQL gate: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/158-verify.sql`
   → exit 0, all 8 assertions PASS, final line `ALL ASSERTIONS PASSED`, SCOPE confirms 89 across 6 states.
2. Coordinate smoke: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/158-coordinate-smoke.ts`
   → exit 0, `COORDINATE SMOKE GREEN` 6/6 with a challenger present per state.

Neither command writes to the database (verify by grepping the files for INSERT/UPDATE/DELETE/--commit — none present).
</verification>

<success_criteria>
Maps to the three ROADMAP Phase 158 success criteria:

- **Criterion 1** (in-district address → House race with challenger field, Pitfall-5 two-path guard; all 89 have >=1 active): covered by USHC2-06-SCOPE + USHC2-06-ACTIVE + USHC2-06-VACANCY in the gate AND by 158-coordinate-smoke.ts surfacing 6/6 states each with challengers >= 1.
- **Criterion 2** (0 unsourced stance rows; 0 duplicate-incumbent records; documented whole-record skips pinned by id): covered by USHC2-06-UNSOURCED (existence check) + USHC2-06-DUPINCUMBENT + USHC2-06-DUPNAME; granular per-candidate skip pins referenced as owned by 155/156/157-verify.sql.
- **Criterion 3** (every race_candidates row has non-null politician_id; no party on cards; all 89 have >=1 active): covered by USHC2-06-NULLPID + USHC2-06-PARTY + USHC2-06-ACTIVE.
</success_criteria>

<output>
Create `.planning/phases/158-coordinate-verification-gate/158-01-SUMMARY.md` when done, recording: the two
scripts authored, the gate's 8 assertion results (with the SCOPE breakdown 17/17/15/14/14/12=89 and the
<2-active allowance count), the smoke's 6/6 surfacing result, and confirmation both ran write-free against
prod .env (exit 0). Note that MI+VA remain to Phase 159 and the milestone closes as the union of the two gates.
</output>
