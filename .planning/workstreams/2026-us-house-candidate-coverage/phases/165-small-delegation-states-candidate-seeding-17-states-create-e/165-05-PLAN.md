---
phase: 165-small-delegation-states-candidate-seeding-17-states-create-e
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/165-wv-generate.mts
  - backend/scripts/165-id-generate.mts
  - backend/migrations/12NN_seed_wv_2026_house_election_races.sql
  - backend/migrations/12NN_seed_wv_2026_house_candidates.sql
  - backend/migrations/12NN_seed_id_2026_house_election_races.sql
  - backend/migrations/12NN_seed_id_2026_house_candidates.sql
  - backend/scripts/seed-wv-house-headshots.py
  - backend/scripts/seed-id-house-headshots.py
  - backend/data/seed-wv-2026-house/165-05-wv-id-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "WV has 1 election ('WV 2026 Statewide General') + 2 races and ID has 1 election ('ID 2026 Statewide General') + 2 races, each office_id -> the existing NATIONAL_LOWER office (never null); DECIDED field (no PROVISIONAL); full field surfaces on /elections"
    - "Both WV incumbents (-54001/-54002) and both ID incumbents (-16001/-16002) renominated -> reuse existing pids (is_incumbent=true); new challengers created at -(fips*10000+cd*100+seq), standard seq start 1 (no D-04 collisions for WV/ID); ID is multi-party (Constitution, Libertarian, Independent) — parties normalized in CSV context only, never on card"
    - "0 duplicate full_name per state; every new WV/ID candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/12NN_seed_wv_2026_house_candidates.sql"
      provides: "WV 2 districts new challengers + incumbent reuse"
      contains: "essentials.race_candidates"
    - path: "backend/migrations/12NN_seed_id_2026_house_candidates.sql"
      provides: "ID 2 districts multi-party new challengers + incumbent reuse"
      contains: "essentials.race_candidates"
  key_links:
    - from: "WV/ID races"
      to: "existing NATIONAL_LOWER offices (geo prefixes 54, 16)"
      via: "join essentials.districts -> essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Track 4 — WV (2) + ID (2), both DECIDED. Vanilla create-election-first pattern (clone 162-mn-generate.mts). Author 1 election + 2 races per state on existing offices, create new records + race_candidates (reuse renominated incumbents), then headshots. DECIDED — no PROVISIONAL. ID is multi-party (Constitution/Libertarian/Independent challengers). Continues USHC3-02/03/04.

Output: 2 generators, 4 migrations (run on prod), 2 headshot scripts, 1 reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-RESEARCH.md
@.planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-PATTERNS.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv
@backend/scripts/162-mn-generate.mts

<interfaces>
WV FIPS=54, geo_ids 5401-5402, geo prefix '54'. Incumbents renominated (reuse): WV-1 -54001, WV-2 -54002. New challengers -(54*10000+cd*100+seq), standard seq start 1 (no D-04 collision — not in the audit). 2-3 new challengers per district (read exact roster from field table + staging/p165-WV.csv). Band query -540299..-540101. DECIDED.
ID FIPS=16, geo_ids 1601-1602, geo prefix '16'. Incumbents renominated (reuse): ID-1 -16001, ID-2 -16002. New challengers -(16*10000+cd*100+seq), standard seq start 1. 3-5 new challengers per district, MULTI-PARTY (Constitution, Libertarian, Independent as well as D/R) — read exact roster + parties from the field table + staging/p165-ID.csv. Band query -160299..-160101. DECIDED.
Standing shape: 1 election per state ('WV 2026 Statewide General' / 'ID 2026 Statewide General', 2026-11-03); races office_id = existing NATIONAL_LOWER office; race_candidates non-null politician_id, candidate_status='active', incumbents is_incumbent=true; party NEVER on card (normalized in CSV/primary_party context only); sqlStr() all free-text; NOT EXISTS guards.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-54 + FIPS-16 collision checks + confirm incumbent reuse map</name>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (WV/ID absent -> standard seq start 1)
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv (WV/ID rows — incumbent pids, rosters, parties)
  </read_first>
  <action>
    In one compound psql command per state, confirm 0 UNPLANNED occupancy in -540299..-540101 (excluding legacy -54001/-54002) and -160299..-160101 (excluding -16001/-16002), and confirm both WV and both ID incumbents exist for reuse. Cross-check any band hit against the field table. Record the confirmed per-state incumbent reuse map + the multi-party ID roster for Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -540299 AND -540101), (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -160299 AND -160101)"</automated>
  </verify>
  <acceptance_criteria>
    - Both band queries return 0 (or every hit is a confirmed legacy incumbent)
    - Both WV incumbents (-54001/-54002) and both ID incumbents (-16001/-16002) confirmed live for reuse
  </acceptance_criteria>
  <done>WV + ID bands confirmed collision-free and incumbent reuse map recorded before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply WV and ID elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (the vanilla single-election analog)
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv (WV/ID full rosters + parties)
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/staging/p165-WV.csv and staging/p165-ID.csv (per-candidate provenance/source URLs)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts twice -> 165-wv-generate.mts and 165-id-generate.mts. WV: FIPS 54, 2 districts, INC_EXT -54001/-54002 (renominated, is_incumbent=true), FIELD = WV new challengers from the field table, seq start 1, election 'WV 2026 Statewide General', DECIDED. ID: FIPS 16, 2 districts, INC_EXT -16001/-16002 (renominated), FIELD = ID new challengers incl. Constitution/Libertarian/Independent (parties normalized in reconciliation context only, never on card), seq start 1, election 'ID 2026 Statewide General', DECIDED. Keep -(fips*10000+cd*100+seq) formula, election+races + candidates migrations, NOT EXISTS idempotency, sqlStr() all free-text. Re-verify live migration HWM via `ls backend/migrations | sort -n | tail`, use next free contiguous numbers (4 migrations). Run both generators, apply all 4 via psql -v ON_ERROR_STOP=1. Write backend/data/seed-wv-2026-house/165-05-wv-id-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT e.name, count(r.*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name IN ('WV 2026 Statewide General','ID 2026 Statewide General') GROUP BY e.name"</automated>
  </verify>
  <acceptance_criteria>
    - WV 2 races + ID 2 races, all office_id NOT NULL; all 4 incumbents reuse existing pids (is_incumbent=true)
    - 0 duplicate full_name per state; descriptions NOT PROVISIONAL; ID multi-party challengers present
    - Re-running all 4 migrations is a 0-row no-op
  </acceptance_criteria>
  <done>WV + ID races + candidates seeded on prod (decided field), with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: WV + ID headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-ms-house-headshots.py (band-scoped analog; wrong-person guards byte-for-byte)
  </read_first>
  <action>
    Clone backend/scripts/seed-ms-house-headshots.py -> seed-wv-house-headshots.py (BANDS = {'WV': (-540299, -540101, 'West Virginia')}) and seed-id-house-headshots.py (BANDS = {'ID': (-160299, -160101, 'Idaho')}). Change ONLY the docstring, BANDS, RESULTS_JSON filename, and the localized place-token; DO NOT modify the wrong-person guard regexes. Run each with `py`. Incumbents fall outside the new bands. Any candidate with no free-license portrait gets a documented honest-skip in the results JSON.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -540299 AND -540101 OR p.external_id BETWEEN -160299 AND -160101"</automated>
  </verify>
  <acceptance_criteria>
    - Both headshot scripts use band-scoped BANDS; wrong-person guards unchanged
    - Every new WV/ID candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>WV/ID new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| headshot fetch -> politician_images | Wrong-person image mis-attribution risk |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD, no runtime code. Data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-05-01 | Tampering (SQLi) | Unescaped candidate names | mitigate | sqlStr() on every free-text field |
| T-165-05-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse existing pids by geo_id; full_name dedup; 0-dup assertion in 165-17 |
| T-165-05-03 | Information disclosure | Party leaking onto candidate card (esp. ID multi-party) | mitigate | race_candidates has no party column; party only in CSV/primary_party context |
| T-165-05-04 | Spoofing | Wrong-person headshot | mitigate | Frozen wrong-person guard suite unchanged |
| T-165-05-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- WV 2 + ID 2 races on prod, all office_id NOT NULL, DECIDED description; all 4 incumbents reused; 0 duplicate full_name per state
- Every new WV/ID candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
WV + ID full 2026 House fields seeded on prod (decided), surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (165-13).
</success_criteria>

<output>
Create .planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-05-SUMMARY.md when done, recording applied migration numbers, WV/ID new-candidate external_id ranges, and any headshot honest-skips for the 165-17 gate pin.
</output>
