---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/164-ct-generate.mts
  - backend/migrations/1233_seed_ct_2026_house_elections_races.sql
  - backend/migrations/1234_seed_ct_2026_house_candidates.sql
  - backend/scripts/seed-ct-house-headshots.py
  - backend/data/seed-ct-2026-house/164-02-ct-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "CT has 1 election ('CT 2026 Statewide General', general, 2026-11-03) and 5 races, each office_id → the existing NATIONAL_LOWER office (never null); full PROVISIONAL pre-primary convention/petition field surfaces on /elections (Aug-11 primary, cull >= 2026-08-12)"
    - "16 new CT politicians created (collision-free -(9*10000+cd*100+seq) band, query -90599..-90101); all 5 incumbents reuse existing pid (-9001..-9005) INCLUDING Larson CT-1 who lost convention endorsement but is a genuine primary candidate (D-01); 0 duplicate full_name per state; every new candidate has a headshot or documented honest-skip"
    - "Unconfirmed candidates handled per D-02: FEC-filed or convention-qualified names seeded PROVISIONAL-with-note (Bueno, Cerreta, Miressi IN); pure-secondhand names with no fetched filing/convention signal HELD for Phase 167 (Botelho HOLD); each include/hold decision re-verified against a directly-fetched source and documented in the reconciliation CSV"
  artifacts:
    - path: "backend/migrations/1233_seed_ct_2026_house_elections_races.sql"
      provides: "CT general election + 5 races wired to existing offices"
      contains: "CT 2026 Statewide General"
    - path: "backend/migrations/1234_seed_ct_2026_house_candidates.sql"
      provides: "16 new CT politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "CT races"
      to: "existing NATIONAL_LOWER offices (geo prefix 09)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
    - from: "CT-1 four-way Dem primary"
      to: "Larson pid a59cd26d (incumbent, lost endorsement) reused as a primary candidate"
      via: "is_incumbent=true reuse row + 4 new records"
      pattern: "race_candidates"
---

<objective>
Seed CT's full 2026 US House field (5 districts, 16 new records) — vanilla single-election pattern (CT is NOT redistricted, no withholding), but with the phase's most nuanced field: convention-endorsed + 15%-threshold-qualified + petitioned PROVISIONAL primary candidates (Aug-11 primary), including incumbent John B. Larson (CT-1) who LOST the Dem convention endorsement to Luke Bronin yet faces a genuine 4-way primary (D-01/D-01a). Apply the D-02 concrete-signal-in / rumor-out rule to the unconfirmed candidates, re-verifying each against a directly-fetched source at seed time. Author 1 election + 5 races, create 16 new records + race_candidates (reuse 5 incumbent pids -9001..-9005), then headshots. Continues USHC3-02/03/04.

Output: 164-ct-generate.mts, two CT migrations (run on prod), seed-ct-house-headshots.py, CT reconciliation CSV documenting every include/hold judgment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
CT FIPS=09; geo_ids 0901-0905; geo prefix '09'. Incumbents keep existing external_id (-9001 Larson CT-1, -9002 Courtney CT-2, -9003 DeLauro CT-3, -9004 Himes CT-4, -9005 Hayes CT-5) — reuse by (district_type='NATIONAL_LOWER', geo_id) join, never recompute. ALL 5 incumbents are running (Larson lost the endorsement but is still a primary candidate → gets an is_incumbent=true reuse row, D-01). New challengers: external_id = -(9*10000 + cd*100 + seq), standard seq start 1 (CT NOT in the collision-audit set — but still run a fresh live 0-collision check). Query band = -90599..-90101 (5-digit; distinct from 4-digit legacy incumbent ids -9001..-9005). One election: "CT 2026 Statewide General" (election_type general, election_date 2026-11-03). All 5 races: office_id = the district's existing NATIONAL_LOWER office (join via geo prefix '09'). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party NEVER on card. Description: 'PROVISIONAL: pre-primary convention/petition qualified field, cull >= 2026-08-12'. Parties normalized (Democratic/Republican/Independent — never "Democrat"). sqlStr()-escape every free-text field.
Per-district new records (from 160-field-table-p164.csv CT rows, AFTER the D-02 include/hold pass): CT-1=4 (Luke Bronin D convention-endorsed; Jillian Gilchrest D 15%-qualified; Ruth Fortune D petitioned 3,743 sigs; Amy Chai R by acclamation) + Larson reused; CT-2=1 (George Austin R) + Courtney reused; CT-3=3 (Christopher Lancia R; Rafael Irizarry R; Andrew Rice Independent petitioning) + DeLauro reused; CT-4=5 (Michael Goldstein R; Daniel Miressi R convention-qualified; Luz Helena Bueno R FEC-filed 4/8/2026; Joseph Perez-Caputo Independent; Damon Lawrence Cerreta Independent FEC-filed) + Himes reused; CT-5=3 (Chris Shea R; Jonathan De Barros R; Jackson Taddeo-Waite Independent FEC-filed) + Hayes reused — Michele Botelho (R) HELD for Phase 167 (only in a two-way-primary report, no FEC/convention signal per D-02a). Total = 16 new (with Botelho held). The include/hold split is the STARTING point — re-verify each unconfirmed name (Bueno, Cerreta, Miressi, Botelho) against a directly-fetched source in Task 1 before finalizing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIPS-09 collision check + D-02 unconfirmed-candidate re-verification</name>
  <read_first>
    - .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md (D-01/D-01a Larson-primary rule; D-02/D-02a concrete-signal-in / rumor-out rule + the four names to re-verify)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (CT rows — the general_candidates NOTE fields carry the convention/FEC/petition signals)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/staging/p164-CT.csv (per-candidate source URLs — ctmirror.org / ctpublic.org / ctinsider.com)
  </read_first>
  <action>
    In one compound command (cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql) confirm 0 rows in -90599..-90101 excluding legacy incumbent ids -9001..-9005. Then perform the D-02 re-verification: for each unconfirmed name (Luz Helena Bueno CT-4, Damon Lawrence Cerreta CT-4, Daniel Miressi CT-4, Michele Botelho CT-5), directly fetch a primary source (FEC candidate query, CT SoS, or a convention roll-call report) and record the concrete signal (FEC Statement of Candidacy date OR convention roll-call / 15%-threshold qualification) or its absence. Apply the rule: concrete-signal → INCLUDE as PROVISIONAL-with-note; pure-secondhand-only → HOLD for Phase 167. Starting expectation (re-confirm, do not assume): Bueno/Cerreta/Miressi IN, Botelho HOLD. Confirm all 5 incumbents (incl. Larson) are running as primary candidates. Write the include/hold judgment + evidence URL per candidate — this becomes the provenance section of the reconciliation CSV in Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -90599 AND -90101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -90599..-90101 (or every hit is a confirmed legacy incumbent id)
    - Each of the 4 unconfirmed names has a directly-fetched-source include/hold decision recorded with an evidence URL; the final new-record count reflects that split (16 with Botelho held, unless re-verification changes it)
    - Larson CT-1 confirmed as a genuine primary candidate (gets an is_incumbent=true reuse row, not omitted)
  </acceptance_criteria>
  <done>CT band collision-free; every unconfirmed candidate resolved to include/hold from a directly-fetched source with documented evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply CT elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (vanilla single-election analog: INC_EXT, single-election template, candidate-insert template, sqlStr, NOT EXISTS guards)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (the CT field + incumbent reuse rows)
    - Task 1 output (final include/hold list of new records)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/164-ct-generate.mts. Change: FIPS 9, 5 districts, INC_EXT = -9001..-9005 (ALL renominated/running, all get is_incumbent=true reuse rows — Larson included per D-01); FIELD = the final CT new records from Task 1 (16 with Botelho held; parties normalized, never "Democrat"); geo prefix '09'; election name 'CT 2026 Statewide General'; description 'PROVISIONAL: pre-primary convention/petition qualified field, cull >= 2026-08-12'. Keep the -(9*10000+cd*100+seq) formula (standard seq start 1), single-election template, NOT EXISTS idempotency guards, sqlStr() on every free-text field. Emit two migration files — reserved pair 1233 (elections/races) + 1234 (candidates); FIRST re-verify with `ls backend/migrations | sort -n | tail` that 1233/1234 are free (parallel 177/178 session may have advanced the HWM); shift both forward to the next free contiguous pair if taken and record actual numbers in the SUMMARY. Run node --import tsx scripts/164-ct-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-ct-2026-house/164-02-ct-reconciliation.csv with every CT candidate's new/reuse disposition + external_id + (for the four unconfirmed names) the Task 1 include/hold judgment and evidence URL.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='CT 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both CT migrations applied cleanly (psql exit 0); 5 races exist, all office_id NOT NULL (FILTER count is 0)
    - 16 new politicians in -90599..-90101; 5 incumbents reuse -9001..-9005 with is_incumbent=true (Larson present); CT-1 has 5 active candidates (Larson + 4 new); 0 duplicate full_name; description carries 'PROVISIONAL:'
    - Botelho (or any HELD name) absent from prod; each included unconfirmed name has an evidence URL in the reconciliation CSV
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>CT races + candidates seeded on prod, PROVISIONAL-marked, with a reconciliation CSV documenting the D-02 judgments.</done>
</task>

<task type="auto">
  <name>Task 3: CT headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (band-scoped analog; wrong-person guards byte-for-byte)
    - .claude/skills/find-headshots/SKILL.md (trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-ct-house-headshots.py. Change ONLY the module docstring, BANDS = {'CT': (-90599, -90101, 'Connecticut')}, and RESULTS_JSON = '_ct-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py. Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-9001..-9005) fall outside the band (already imaged).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -90599 AND -90101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-ct-house-headshots.py uses the band-scoped BANDS; wrong-person guards unchanged
    - Every new CT candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>CT new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| unconfirmed candidate signal -> seeded name | A phantom name could surface on /elections if seeded on rumor alone |
| headshot fetch -> politician_images | Wrong-person image mis-attribution |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD. Genuine data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-02-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field |
| T-164-02-02 | Repudiation | Duplicate incumbent record (esp. Larson) | mitigate | Incumbents reuse -9001..-9005 by geo_id join; 0-duplicate assertion in 164-13 |
| T-164-02-03 | Spoofing | Phantom unconfirmed candidate surfaced on rumor | mitigate | D-02 concrete-signal gate in Task 1; each include re-verified against a directly-fetched source; HELD names excluded |
| T-164-02-04 | Information disclosure | Party leaking onto candidate card | mitigate | race_candidates has no party column; party on races.primary_party only |
| T-164-02-05 | Spoofing | Wrong-person headshot | mitigate | find-headshots guards unchanged |
| T-164-02-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- Both CT migrations applied on prod (psql exit 0); 5 races, all office_id NOT NULL; PROVISIONAL description
- 16 new politicians in-band; 5 incumbents reused (Larson present); CT-1 has 5 candidates; 0 duplicate full_name
- Every unconfirmed name has a documented include/hold judgment; HELD names absent from prod
- Every new CT candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
CT's full 2026 House convention/petition field is seeded on prod (16 new + 5 incumbent reuse), PROVISIONAL-marked, surfacing on /elections, with headshots, a reconciliation CSV, and a documented D-02 include/hold trail — ready for stance research (164-08).
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-02-SUMMARY.md when done, recording applied migration numbers, the CT new-candidate external_id range, the final include/hold list (which unconfirmed names were seeded vs HELD for Phase 167), and any headshot honest-skips for the 164-13 gate pin.
</output>
