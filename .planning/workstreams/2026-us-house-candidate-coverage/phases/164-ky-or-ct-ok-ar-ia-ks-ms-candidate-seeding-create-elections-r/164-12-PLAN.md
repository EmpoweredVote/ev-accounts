---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 12
type: execute
wave: 2
depends_on: ["164-06"]
files_modified:
  - backend/data/stance-research/ar-2026-house/_merge.ts
  - backend/data/stance-research/ar-2026-house/_push.ts
  - backend/data/stance-research/ar-2026-house/_push_uuid.ts
  - backend/data/stance-research/ar-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/ar-2026-house/164-12-ar-stances.csv
  - backend/data/stance-research/ms-2026-house/_merge.ts
  - backend/data/stance-research/ms-2026-house/_push.ts
  - backend/data/stance-research/ms-2026-house/_push_uuid.ts
  - backend/data/stance-research/ms-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/ms-2026-house/164-12-ms-stances.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "All 6 new AR candidates and all 8 new MS candidates have sourced chairs-not-polarity federal-24 stances (or a gate-pinned whole-record skip with a written trail), 0 unsourced, primary-source-verified, pushed to PROD per state"
    - "Every AR/MS answer pairs to an inform.politician_context row with a real fetched source_url"
    - "AR's partial-tier incumbents (-5001..-5004) and MS's (-28001..-28004) are skipped via the diagnostic (out of scope)"
  artifacts:
    - path: "backend/data/stance-research/ar-2026-house/164-12-ar-stances.csv"
      provides: "AR new-candidate stance rows"
      contains: "source_url"
    - path: "backend/data/stance-research/ms-2026-house/164-12-ms-stances.csv"
      provides: "MS new-candidate stance rows"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push_uuid.ts / _push.ts"
      pattern: "politician_context"
---

<objective>
Research + push AR (6 new) and MS (8 new) stances — the two smallest decided states seeded in 164-06, grouped in one stance plan (each with its own dir, pushed per state). AR/MS incumbents are all partial-tier → skipped. Set up both scaffolds, regenerate each _TOPIC_SCALE_FULL.txt LIVE, run agents at 3-concurrency, primary-source verification pass, push each state to PROD as it completes. Final per-state stance plan (all 8 states stance-complete after this). Continues USHC3-05.

Output: ar-2026-house + ms-2026-house scaffolds + stance CSVs pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-06-SUMMARY.md
@backend/data/stance-research/mn-2026-house/_push.ts
@backend/data/stance-research/mn-2026-house/_merge.ts

<interfaces>
Two separate scaffolds: ar-2026-house/ and ms-2026-house/, each with _push.ts / _merge.ts / _push_uuid.ts copied byte-for-byte from mn-2026-house/ and its own _TOPIC_SCALE_FULL.txt regenerated LIVE (federal-24 subset; re-verify live active count once, reuse for both). AR targets = 6 new AR candidates (band -50499..-50101, from 164-06-SUMMARY.md); MS targets = 8 new MS candidates (band -280499..-280101). AR incumbents (-5001..-5004) and MS incumbents (-28001..-28004) are partial-tier → OUT of scope. Standards (project standing): chairs-not-polarity, never party-inferred, honest-skip with written search trail (whole-record gate-pinned), 0-unsourced floor, mandatory primary-source verification pass, 3-concurrency cap, first-wave validation, embed exact 1-5 scale texts. Sources: AR Wikipedia + MS Clarion-Ledger + candidate sites/socials + Ballotpedia via Playwright.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ar-2026-house + ms-2026-house + regenerate topic scale live</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (scaffold + topic conventions)
    - backend/data/stance-research/mn-2026-house/_push.ts (pipeline scripts to copy verbatim)
  </read_first>
  <action>
    Create backend/data/stance-research/ar-2026-house/ and backend/data/stance-research/ms-2026-house/. Copy _push.ts, _merge.ts, _push_uuid.ts byte-for-byte from mn-2026-house/ into each. Regenerate _TOPIC_SCALE_FULL.txt via the LIVE compass_topics query filtered to the federal-24 subset (re-verify the live active total once; write into both dirs). Do NOT copy a stale file. Set each _merge.ts IN_SCOPE to its state's new-candidate band.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/ar-2026-house/_TOPIC_SCALE_FULL.txt && test -f backend/data/stance-research/ms-2026-house/_TOPIC_SCALE_FULL.txt && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both ar-2026-house/ and ms-2026-house/ contain _merge.ts, _push.ts, _push_uuid.ts (verbatim) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt regenerated from a live query, filtered to the federal-24 subset (count re-verified), with exact 1-5 texts
  </acceptance_criteria>
  <done>AR + MS stance scaffolds ready; topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research AR stances (6 new) and push to PROD</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (3-concurrency, chairs-not-polarity, 0-unsourced, honest-skip with trail)
    - backend/data/stance-research/ar-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 164-06-SUMMARY.md (AR candidate external_ids)
  </read_first>
  <action>
    Target the 6 new AR candidates (band -50499..-50101). Majors (D nominees) first, Libertarian/independent last. Skip AR's partial-tier incumbents. Dispatch agents at 3-concurrency MAX; validate the first wave. Embed the exact 1-5 scale texts. chairs-not-polarity; never party-inferred; honest-skip only with a written trail. Every answer pairs to a real fetched source_url. Primary-source verification pass. Write 164-12-ar-stances.csv, merge + push AR to PROD.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -50499 AND -50101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed AR rows (query returns 0)
    - Every new AR candidate has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written trail
    - Agents ran at ≤3 concurrency; primary-source verification pass done; no partial-tier AR incumbent topped up
  </acceptance_criteria>
  <done>AR's full field is stance-complete on PROD, 0 unsourced.</done>
</task>

<task type="auto">
  <name>Task 3: Research MS stances (8 new) and push to PROD</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (3-concurrency, chairs-not-polarity, 0-unsourced, honest-skip with trail)
    - backend/data/stance-research/ms-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 164-06-SUMMARY.md (MS candidate external_ids)
  </read_first>
  <action>
    Target the 8 new MS candidates (band -280499..-280101). Majors first, Libertarian/independent last. Skip MS's partial-tier incumbents. Dispatch agents at 3-concurrency MAX; validate the first wave. Embed the exact 1-5 scale texts. chairs-not-polarity; never party-inferred; honest-skip only with a written trail. Every answer pairs to a real fetched source_url. Primary-source verification pass. Write 164-12-ms-stances.csv, merge + push MS to PROD.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -280499 AND -280101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed MS rows (query returns 0)
    - Every new MS candidate has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written trail
    - Agents ran at ≤3 concurrency; primary-source verification pass done; no partial-tier MS incumbent topped up
  </acceptance_criteria>
  <done>MS's full field is stance-complete on PROD, 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — offline research + data push, no runtime code. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-12-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query; chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-164-12-02 | Tampering | Surname-leak in selected quotes | mitigate | _push.ts surname-leak guard (verbatim) |
| T-164-12-03 | Denial of service | Stance-agent rate-limit death | mitigate | 3-concurrency cap; first-wave validation; run on Sonnet |
</threat_model>

<verification>
- 0 unsourced answers among pushed AR rows and pushed MS rows
- Every new AR/MS candidate has sourced stances or a gate-pinned whole-record skip
- Both scaffolds present; topic scale live-regenerated
</verification>

<success_criteria>
AR's and MS's full 2026 House fields are stance-complete on PROD, 0 unsourced, chairs-not-polarity, primary-source-verified — all 8 phase states now stance-complete.
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-12-SUMMARY.md when done, recording targets covered per state, gate-pinned whole-record skips (external_ids) for 164-13, and the live federal-24 count.
</output>
