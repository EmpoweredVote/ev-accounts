---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/164-ar-generate.mts
  - backend/scripts/164-ms-generate.mts
  - backend/migrations/1242_seed_ar_2026_house_elections_races.sql
  - backend/migrations/1243_seed_ar_2026_house_candidates.sql
  - backend/migrations/1244_seed_ms_2026_house_elections_races.sql
  - backend/migrations/1245_seed_ms_2026_house_candidates.sql
  - backend/scripts/seed-ar-house-headshots.py
  - backend/scripts/seed-ms-house-headshots.py
  - backend/data/seed-ar-2026-house/164-06-ar-reconciliation.csv
  - backend/data/seed-ms-2026-house/164-06-ms-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "AR has 1 election ('AR 2026 Statewide General') + 4 races and MS has 1 election ('MS 2026 Statewide General') + 4 races, each office_id → the existing NATIONAL_LOWER office (never null); decided general fields surface on /elections"
    - "AR: 6 new politicians (-(5*10000+cd*100+seq); query -50499..-50101); all 4 incumbents renominated → reuse -5001..-5004. MS: 8 new politicians (-(28*10000+cd*100+seq); query -280499..-280101); all 4 incumbents renominated → reuse -28001..-28004. 0 duplicate full_name per state; no open seats in either state"
    - "Every new AR/MS candidate has a headshot or documented honest-skip; no candidate card surfaces party"
  artifacts:
    - path: "backend/migrations/1243_seed_ar_2026_house_candidates.sql"
      provides: "6 new AR politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
    - path: "backend/migrations/1245_seed_ms_2026_house_candidates.sql"
      provides: "8 new MS politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "AR/MS races"
      to: "existing NATIONAL_LOWER offices (geo prefix 05 / 28)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Seed AR (4 districts, 6 new) and MS (4 districts, 8 new) — the phase's two smallest, simplest decided states, grouped in one plan. Both vanilla single-election, all incumbents renominated (no open seats, no collision sub-bands). Author each state's 1 election + 4 races, create new records + race_candidates (reuse all 4 incumbents each), then headshots. Continues USHC3-02/03/04.

Output: 164-ar-generate.mts + 164-ms-generate.mts, four migrations (run on prod), two headshot scripts, two reconciliation CSVs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
AR FIPS=05; geo_ids 0501-0504; geo prefix '05'. All 4 incumbents renominated → reuse (is_incumbent=true): Crawford -5001 (AR-1), Hill -5002 (AR-2), Womack -5003 (AR-3), Westerman -5004 (AR-4). New: external_id = -(5*10000+cd*100+seq), standard seq start 1. Query band -50499..-50101 (5-digit new vs 4-digit legacy -5001..-5004). Election "AR 2026 Statewide General" (general, 2026-11-03). Decided → NOT PROVISIONAL. AR per-district new records: AR-1=2 (Terri Yarbrough Green Democratic; Steve Parsons Libertarian) + Crawford reused; AR-2=1 (Chris Jones Democratic) + Hill reused; AR-3=2 (Robb Ryerse Democratic; Bobby Wilson Libertarian) + Womack reused; AR-4=1 (James Russell Democratic) + Westerman reused. AR total = 6 new; 4 incumbent reuse rows.
MS FIPS=28; geo_ids 2801-2804; geo prefix '28'. All 4 incumbents renominated → reuse: Kelly -28001 (MS-1), Thompson -28002 (MS-2), Guest -28003 (MS-3), Ezell -28004 (MS-4). New: external_id = -(28*10000+cd*100+seq), standard seq start 1. Query band -280499..-280101 (5-digit new vs legacy -28001..-28004). Election "MS 2026 Statewide General". Decided → NOT PROVISIONAL. MS per-district new records: MS-1=2 (Cliff Johnson Democratic; Johnny Baucom Libertarian) + Kelly reused; MS-2=2 (Ron Eller Republican; Bennie Foster Independent) + Thompson reused; MS-3=2 (Michael Chiaradio Democratic; Erik Kiehle Libertarian) + Guest reused; MS-4=2 (Jeffrey Hulum III Democratic; Carl Boyanton Independent) + Ezell reused. MS total = 8 new; 4 incumbent reuse rows.
Both states: all races office_id = the district's existing NATIONAL_LOWER office; race_candidates non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party NEVER on card; parties normalized (never "Democrat"); sqlStr()-escape every free-text field; single-election template + NOT EXISTS idempotency guards. Source: MS = Clarion-Ledger; AR = Wikipedia 2026-House-AR page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-05 + FIPS-28 collision checks</name>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (AR + MS not listed → standard seq start; still mandatory to run a fresh live check per D-04)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (AR/MS rows — all renominated, no open seats)
  </read_first>
  <action>
    In one compound command (cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql) confirm 0 UNPLANNED rows in -50499..-50101 excluding legacy -5001..-5004, and 0 UNPLANNED rows in -280499..-280101 excluding legacy -28001..-28004 (cross-check any hit against 160-field-table-p164.csv). Confirm all 4 AR and all 4 MS incumbents are renominated (no open seats).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -50499 AND -50101) AS ar_band, (SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -280499 AND -280101) AS ms_band"</automated>
  </verify>
  <acceptance_criteria>
    - AR band (-50499..-50101) and MS band (-280499..-280101) both return 0 (or only confirmed legacy-incumbent hits)
    - All 8 incumbents confirmed renominated (no open-seat exclusions in either state)
  </acceptance_criteria>
  <done>Both bands collision-free; AR/MS fields confirmed all-incumbent-returning.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply AR elections/races + candidates migrations + AR headshots</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (vanilla single-election analog)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (the 6 AR new records + 4 incumbent reuse rows)
    - backend/scripts/seed-mo-house-headshots.py (headshot analog)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/164-ar-generate.mts. Change: FIPS 5, 4 districts, INC_EXT = -5001..-5004 (all renominated, is_incumbent=true); FIELD = the 6 AR new records (parties normalized); geo prefix '05'; election name 'AR 2026 Statewide General'; NOT PROVISIONAL; standard seq start 1. Keep -(5*10000+cd*100+seq), single-election template, NOT EXISTS guards, sqlStr(). Emit two migrations — reserved 1242 (elections/races) + 1243 (candidates); re-verify free with `ls backend/migrations | sort -n | tail` first and shift forward if taken (record actual numbers). Run node --import tsx scripts/164-ar-generate.mts, apply both to prod via psql -v ON_ERROR_STOP=1. Then clone seed-mo-house-headshots.py → seed-ar-house-headshots.py with BANDS = {'AR': (-50499, -50101, 'Arkansas')}, RESULTS_JSON '_ar-house-headshot-results.json', guards unchanged, run with py. Write backend/data/seed-ar-2026-house/164-06-ar-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='AR 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both AR migrations applied cleanly (psql exit 0); 4 races, all office_id NOT NULL (FILTER count 0)
    - 6 new AR politicians in -50499..-50101; 4 incumbents reuse -5001..-5004 is_incumbent=true; 0 duplicate full_name; idempotent
    - Every new AR candidate has a headshot row or documented honest-skip
  </acceptance_criteria>
  <done>AR seeded on prod (decided), headshots + reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: Generate + apply MS elections/races + candidates migrations + MS headshots</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (vanilla single-election analog)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (the 8 MS new records + 4 incumbent reuse rows)
    - backend/scripts/seed-mo-house-headshots.py (headshot analog)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/164-ms-generate.mts. Change: FIPS 28, 4 districts, INC_EXT = -28001..-28004 (all renominated, is_incumbent=true); FIELD = the 8 MS new records (parties normalized); geo prefix '28'; election name 'MS 2026 Statewide General'; NOT PROVISIONAL; standard seq start 1. Keep -(28*10000+cd*100+seq), single-election template, NOT EXISTS guards, sqlStr(). Emit two migrations — reserved 1244 (elections/races) + 1245 (candidates); re-verify free first (after AR's numbers land) and shift forward if taken (record actual numbers). Run node --import tsx scripts/164-ms-generate.mts, apply both to prod via psql -v ON_ERROR_STOP=1. Then clone seed-mo-house-headshots.py → seed-ms-house-headshots.py with BANDS = {'MS': (-280499, -280101, 'Mississippi')}, RESULTS_JSON '_ms-house-headshot-results.json', guards unchanged, run with py. Write backend/data/seed-ms-2026-house/164-06-ms-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='MS 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both MS migrations applied cleanly (psql exit 0); 4 races, all office_id NOT NULL (FILTER count 0)
    - 8 new MS politicians in -280499..-280101; 4 incumbents reuse -28001..-28004 is_incumbent=true; 0 duplicate full_name; idempotent
    - Every new MS candidate has a headshot row or documented honest-skip
  </acceptance_criteria>
  <done>MS seeded on prod (decided), headshots + reconciliation CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD arrays -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| headshot fetch -> politician_images | Wrong-person mis-attribution |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD. Genuine data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-06-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field |
| T-164-06-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -5001..-5004 / -28001..-28004 by geo_id join; 0-duplicate assertion in 164-13 |
| T-164-06-03 | Information disclosure | Party leaking onto candidate card | mitigate | race_candidates has no party column; party on races.primary_party only |
| T-164-06-04 | Spoofing | Wrong-person headshot | mitigate | find-headshots guards unchanged |
| T-164-06-05 | Tampering | AR + MS migration-number collision (4 migrations in one plan) | mitigate | Apply AR pair first, re-verify HWM before MS pair; record actual numbers in SUMMARY |
| T-164-06-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- AR: 4 races all office_id NOT NULL; 6 new in-band; incumbents reused; 0 dup full_name
- MS: 4 races all office_id NOT NULL; 8 new in-band; incumbents reused; 0 dup full_name
- Every new AR/MS candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
AR (6 new + 4 reuse) and MS (8 new + 4 reuse) full decided 2026 House fields are seeded on prod, surfacing on /elections, with headshots and reconciliation CSVs — ready for stance research (164-12).
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-06-SUMMARY.md when done, recording the four applied migration numbers, the AR + MS new-candidate external_id ranges, and any headshot honest-skips for the 164-13 gate pin.
</output>
