---
phase: 173-discovery-sweep-anthropic-cost-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/discoveryAgentRunner.ts
  - backend/src/lib/discoveryAgentRunner.test.ts
autonomous: true
requirements: [OPS-01, OPS-03]

must_haves:
  truths:
    - "A model turn ending without invoking report_candidates yields a zero-candidate completed result (candidates:[]), never a thrown error (OPS-03)."
    - "checkAnthropicAvailability() returns available:false with reason missing_key when ANTHROPIC_API_KEY is unset, and reason unusable when a canary Anthropic.APIError has status 401/402/403 (OPS-01 building block)."
    - "checkAnthropicAvailability() re-throws (inconclusive) for any non-401/402/403 canary failure (529/5xx/network) so the caller can proceed rather than false-positive-skip."
  artifacts:
    - backend/src/lib/discoveryAgentRunner.ts
    - backend/src/lib/discoveryAgentRunner.test.ts
  key_links:
    - "runDiscoveryAgent zero-candidate return → discoveryService.runDiscoveryForJurisdiction's existing zero-candidate completed path (unchanged; locked by 173-03)."
    - "checkAnthropicAvailability export → consumed by discoveryCron.runDiscoverySweep preflight (wired in 173-02)."
  prohibitions:
    - "MUST NOT include the raw ANTHROPIC_API_KEY in the canary detail string, console, or any returned value — only .status/.type/.message."
    - "MUST NOT treat a non-401/402/403 canary failure as unusable (re-throw it — inconclusive)."
    - "MUST NOT add the canary/preflight to the manual dashboard trigger route (discoveryDashboard.ts) — out of scope (RESEARCH Pitfall 4)."
    - "MUST NOT install any new package or bump @anthropic-ai/sdk — 0.91.0 already has the full APIError hierarchy needed."
---

<objective>
Harden the sole Anthropic contact point (`discoveryAgentRunner.ts`) for the weekly discovery sweep: (1) OPS-03 — a model turn that ends without invoking `report_candidates` becomes a clean zero-candidate return instead of a thrown per-jurisdiction failure; (2) OPS-01 building block — add and export a one-shot `checkAnthropicAvailability()` canary helper that the sweep orchestrator (173-02) will call once before iterating jurisdictions.

Purpose: Eliminate the 21× "Claude did not invoke report_candidates" hard-error path, and give the sweep a cheap, typed pre-flight so a missing key or exhausted credit aborts before spending per-jurisdiction paid calls.
Output: Modified `discoveryAgentRunner.ts` + new `discoveryAgentRunner.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-RESEARCH.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-VALIDATION.md
</context>

## Artifacts this plan produces
- `checkAnthropicAvailability(): Promise<AnthropicAvailability>` + exported `AnthropicAvailability` union type — in `discoveryAgentRunner.ts`
- `runDiscoveryAgent()` zero-candidate return on no-report turns (was: throw) — in `discoveryAgentRunner.ts`
- `backend/src/lib/discoveryAgentRunner.test.ts` (new)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author discoveryAgentRunner.test.ts (RED)</name>
  <files>backend/src/lib/discoveryAgentRunner.test.ts</files>
  <read_first>
    - backend/src/lib/discoveryAgentRunner.ts (the file under test — note `runDiscoveryAgent`, the agentic loop lines ~139-205, the missing-key guard lines 105-109)
    - backend/src/lib/essentialsBrowseService.test.ts (the codebase Vitest + `vi.mock` convention to mirror)
    - backend/node_modules/@anthropic-ai/sdk/core/error.d.ts (APIError constructor signature: `(status, error, message, headers, type)`)
    - 173-RESEARCH.md "Code Examples" (OPS-03 before/after) and "Pattern 1" (canary)
  </read_first>
  <behavior>
    - OPS-03: `runDiscoveryAgent` returns `{ candidates: [], stopReason: 'end_turn' }` (no throw) when the model's final response has `stop_reason: 'end_turn'` and no `report_candidates` tool_use block.
    - OPS-03: after a `pause_turn` response followed by an `end_turn` response with no tool_use, still returns zero candidates (does not throw).
    - OPS-01 helper: `checkAnthropicAvailability()` returns `{ available: false, reason: 'missing_key' }` when `env.ANTHROPIC_API_KEY` is unset.
    - OPS-01 helper: returns `{ available: false, reason: 'unusable' }` when the canary `messages.create` throws an `Anthropic.APIError` with status 402 (repeat for 401 and 403).
    - OPS-01 helper: throws (inconclusive) when the canary throws an `Anthropic.APIError` with status 529, and when it throws a plain non-APIError network error.
    - Security: the `detail` string returned for an unusable result never contains the API key value.
  </behavior>
  <action>
    Create the test file. Mock `@anthropic-ai/sdk` using `vi.mock` with an `importOriginal`-style factory that spreads the actual module (so the real `APIError` class is preserved and both `err instanceof Anthropic.APIError` and direct `new Anthropic.APIError(...)` construction work) and overrides the `default` export with a subclass whose `messages.create` is a shared `vi.fn()` the tests drive. Mock `./env.js` to toggle `ANTHROPIC_API_KEY` per test (mutable mock object). Construct SDK errors as `new Anthropic.APIError(status, { type }, message, undefined, type)`. Import `runDiscoveryAgent` and `checkAnthropicAvailability` from `./discoveryAgentRunner.js`. Name tests so they satisfy the VALIDATION `-t` labels: "end_turn without report_candidates", "missing key", "unusable credit", "inconclusive canary". These tests are RED until Task 2 and Task 3 land (symbol `checkAnthropicAvailability` does not yet exist and the no-report path still throws) — that is the intended starting state. Do NOT place implementation logic in the test beyond the mocks and assertions.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryAgentRunner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/lib/discoveryAgentRunner.test.ts` exists and contains tests titled with the four VALIDATION `-t` labels above.
    - Run is RED (fails to import `checkAnthropicAvailability` and/or the no-report assertion) — expected before Tasks 2/3.
    - The `@anthropic-ai/sdk` mock preserves a constructible `Anthropic.APIError` (instanceof + `new` both work).
  </acceptance_criteria>
  <done>discoveryAgentRunner.test.ts authored, encodes OPS-03 + OPS-01-helper behavior, currently RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OPS-03 — replace terminal throw with zero-candidate return</name>
  <files>backend/src/lib/discoveryAgentRunner.ts</files>
  <read_first>
    - backend/src/lib/discoveryAgentRunner.ts (lines ~191-206: the `pause_turn` branch, the `break`, and the post-loop `throw`)
    - 173-RESEARCH.md "Code Examples → Full before/after for discoveryAgentRunner.ts's terminal throw (OPS-03)"
  </read_first>
  <action>
    In `runDiscoveryAgent`, make every non-`pause_turn`, non-`report_candidates` exit path return a zero-candidate `DiscoveryAgentResult` instead of throwing. Replace the in-loop `break` (the "any other stop reason" case) with a `console.warn` (stating the turn ended without invoking the reporting tool, include `stop_reason`) followed by `return { model: lastModel, inputTokens: totalInputTokens, outputTokens: totalOutputTokens, candidates: [], stopReason: lastStopReason }`. Replace the post-loop `throw new Error(...)` with the same warn+return shape (MAX_TURNS-exhausted defensive fallback). Per OPS-03, this is benign (the model searched and found nothing reportable, or exhausted its turn budget). Do NOT modify `discoveryService.ts` — its zero-candidate completed path is already correct (locked by 173-03).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryAgentRunner.test.ts -t "end_turn without report_candidates"</automated>
  </verify>
  <acceptance_criteria>
    - No throw remains for the "did not invoke report_candidates" outcome; both the in-loop and post-loop exits return `DiscoveryAgentResult` with `candidates: []` and the observed `stopReason`.
    - The `end_turn without report_candidates` test is GREEN.
    - The `report_candidates`-found path and `pause_turn` continuation path are unchanged.
  </acceptance_criteria>
  <done>runDiscoveryAgent returns zero candidates (not thrown) on any no-report exit; OPS-03 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OPS-01 — add and export checkAnthropicAvailability() canary helper</name>
  <files>backend/src/lib/discoveryAgentRunner.ts</files>
  <read_first>
    - backend/src/lib/discoveryAgentRunner.ts (existing missing-key guard, lines 105-109; the `import Anthropic` + `import { env }` at top)
    - 173-RESEARCH.md "Pattern 1" and "Open Question 2" (canary model choice)
    - 173-RESEARCH.md "Security Domain" (never log the key; log only .status/.type/.message)
  </read_first>
  <action>
    Export an `AnthropicAvailability` union type: `{ available: true }` | `{ available: false; reason: 'missing_key' | 'unusable'; detail: string }`. Export an async `checkAnthropicAvailability(): Promise<AnthropicAvailability>` that: (1) if `env.ANTHROPIC_API_KEY` is falsy, returns `{ available: false, reason: 'missing_key', detail: 'ANTHROPIC_API_KEY is not configured.' }`; (2) otherwise constructs an `Anthropic` client and awaits ONE cheap canary `client.messages.create({ model: 'claude-haiku-4-5', max_tokens: 1, messages: [{ role: 'user', content: 'ping' }] })` with NO tools; on success returns `{ available: true }`; (3) in `catch`, if `err instanceof Anthropic.APIError` and `err.status` is one of 401/402/403, returns `{ available: false, reason: 'unusable', detail: ` + a string built from `err.status`, `err.type ?? ''`, and `err.message` (NEVER the API key) + `}`; for any other error, re-throw it (inconclusive — the caller logs and proceeds, per RESEARCH Pitfall 3). Add a short comment noting the fallback: if the account rejects `claude-haiku-4-5`, switch the canary model to `claude-sonnet-4-6` (the production model) — either satisfies OPS-01 since 401/402/403 are account-level.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryAgentRunner.test.ts -t "missing key" && npx vitest run src/lib/discoveryAgentRunner.test.ts -t "unusable credit" && npx vitest run src/lib/discoveryAgentRunner.test.ts -t "inconclusive canary"</automated>
  </verify>
  <acceptance_criteria>
    - `checkAnthropicAvailability` and `AnthropicAvailability` are exported from `discoveryAgentRunner.ts`.
    - Missing key → `{ available: false, reason: 'missing_key' }`; APIError 401/402/403 → `{ available: false, reason: 'unusable' }`; APIError 529 or plain network error → re-thrown.
    - The `detail` string never contains the raw API key.
    - `cd /c/EV-Accounts/backend && npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>Canary helper exported with correct 401/402/403-only "unusable" classification; helper-level tests green; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → Anthropic API | Server-to-server; the canary reuses the existing `ANTHROPIC_API_KEY` credential. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-01 | Information Disclosure | `checkAnthropicAvailability` detail string / logs | high | mitigate | Build the `detail` only from `.status`/`.type`/`.message`; never interpolate `env.ANTHROPIC_API_KEY`. Test asserts the key is absent from the returned detail. |
| T-173-02 | Denial of Service | canary false-positive whole-sweep skip | medium | mitigate | Only 401/402/403 classify as unusable; all other canary failures re-throw (inconclusive) so 173-02's caller proceeds (RESEARCH Pitfall 3). |

No package installs occur in this plan — no T-173-SC supply-chain threat (RESEARCH: Package Legitimacy Audit N/A).
</threat_model>

<verification>
Run `cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryAgentRunner.test.ts` — all tests green. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
OPS-03 satisfied at the runner level (no-report → zero candidates, not thrown). OPS-01 canary helper exported and correctly classifies only 401/402/403 as unusable, missing key as missing_key, everything else as inconclusive (re-thrown). No API key ever logged/returned.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-01-SUMMARY.md` when done.
</output>
