---
phase: 173-discovery-sweep-anthropic-cost-reliability-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/discoveryService.test.ts
  - backend/src/cron/discoverySweep.ts
autonomous: true
requirements: [OPS-03, OPS-04]

must_haves:
  truths:
    - "runDiscoveryForJurisdiction returns status:'completed' with candidatesFound:0 (NOT 'failed', no throw) when runDiscoveryAgent returns zero candidates — the OPS-03 caller contract, regression-locked."
    - "The discovery-sweep cron expression (0 2 * * 0, Sunday 02:00 UTC) carries a comment documenting the deliberate weekly cost cadence (OPS-04)."
  artifacts:
    - backend/src/lib/discoveryService.test.ts
    - backend/src/cron/discoverySweep.ts
  key_links:
    - "discoveryService zero-candidate completed path ← locked so the 173-01 OPS-03 change (runner returns [] instead of throwing) surfaces as a completed run, not a failure."
  prohibitions:
    - "MUST NOT modify discoveryService.ts source — its zero-candidate path is already correct (RESEARCH Pattern 3); this plan only adds a regression test."
    - "MUST NOT change the cron cadence or timezone — OPS-04 is confirm-and-document, not adjust (unless the operator step in 173-04 decides otherwise)."
---

<objective>
Lock in the OPS-03 caller-side contract with a focused regression test, and add the OPS-04 cadence documentation at the cron registration site. `discoveryService.ts` already treats a zero-candidate agent result as a valid completed run (RESEARCH Pattern 3) — no source change is needed there, but a regression test guarantees the 173-01 runner change (throw → zero-candidate return) surfaces as a `completed` run rather than a `failed` one. Independently, document that the weekly Sunday-02:00-UTC cron is a deliberate cost choice.

Purpose: Guarantee the benign no-report case counts as zero-found (not a hard failure) end-to-end, and make the weekly cadence a visible, intentional decision (OPS-04).
Output: New `discoveryService.test.ts` + a documented cron expression in `discoverySweep.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-RESEARCH.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-VALIDATION.md
</context>

## Artifacts this plan produces
- `backend/src/lib/discoveryService.test.ts` (new) — OPS-03 caller-contract regression lock
- OPS-04 cost-rationale comment on the `cron.schedule('0 2 * * 0', ...)` expression — in `discoverySweep.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author discoveryService.test.ts — OPS-03 regression lock</name>
  <files>backend/src/lib/discoveryService.test.ts</files>
  <read_first>
    - backend/src/lib/discoveryService.ts (runDiscoveryForJurisdiction, lines ~283-666; the zero-candidate regression-alert block ~593-620; the DiscoveryRunSummary interface ~247; the final `return { ... status: 'completed' ... }`)
    - backend/src/lib/essentialsBrowseService.test.ts (vi.mock convention)
    - 173-RESEARCH.md "Pattern 3" (why discoveryService.ts needs no source change)
  </read_first>
  <behavior>
    - OPS-03 caller contract: `runDiscoveryForJurisdiction` returns a summary with `status: 'completed'` and `candidatesFound: 0` (NOT `'failed'`, no throw) when `runDiscoveryAgent` resolves to `{ candidates: [], stopReason: 'end_turn' }`.
    - The `discovery_runs` row is updated to `completed` (not `failed`) — assert via the mocked `pool.query` UPDATE call arguments or the returned status.
    - The zero-candidate regression alert is NOT sent when there is no prior completed run with candidates_found > 0 (silent, per RESEARCH Pattern 3).
  </behavior>
  <action>
    Create the test file. Mock `./discoveryAgentRunner.js` so `runDiscoveryAgent` resolves to `{ model: 'claude-sonnet-4-6', inputTokens: 0, outputTokens: 0, candidates: [], stopReason: 'end_turn' }`. Mock `./db.js` (`pool.query` = `vi.fn()`) with a sequenced `mockResolvedValueOnce(...)` chain matching the query order in `runDiscoveryForJurisdiction` (config-row load, known-races load, run-row INSERT returning an id, the staging/upsert writes, the prior-run lookup returning no prior with candidates>0, the final completed UPDATE). Mock `./emailService.js` (`sendEmail` = `vi.fn()`) and `./fetchPageContent.js`. Invoke `runDiscoveryForJurisdiction(<id>, { triggeredBy: 'cron', autoUpsert: true, suppressRunEmail: true })` and assert the resolved summary has `status === 'completed'` and `candidatesFound === 0` with no rejection. Title the test "zero candidates is not a failure". This test is expected GREEN immediately — `discoveryService.ts` is already correct; the test is a regression lock, not RED.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryService.test.ts -t "zero candidates is not a failure"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/lib/discoveryService.test.ts` exists with the titled test.
    - Test is GREEN against the current `discoveryService.ts` (no source change made).
    - Asserts `status === 'completed'` and `candidatesFound === 0` and no throw on a zero-candidate agent result.
  </acceptance_criteria>
  <done>OPS-03 caller contract regression-locked; discoveryService.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: OPS-04 — document the weekly cron cadence as a deliberate cost choice</name>
  <files>backend/src/cron/discoverySweep.ts</files>
  <read_first>
    - backend/src/cron/discoverySweep.ts (the cron.schedule('0 2 * * 0', ...) call, lines 20-35)
    - 173-RESEARCH.md "phase_requirements → OPS-04"
  </read_first>
  <action>
    Add a comment on the `cron.schedule('0 2 * * 0', ...)` expression stating that the weekly Sunday-02:00-UTC cadence is a deliberate cost choice: each sweep spends paid Anthropic calls proportional to the number of `discovery_jurisdictions` whose `election_date` falls within `SWEEP_HORIZON_DAYS` (defined in `discoveryCron.ts`), so the weekly cadence bounds the recurring spend; cross-reference `SWEEP_HORIZON_DAYS` in `discoveryCron.ts`. Comment-only change — do not alter the schedule string, timezone, or behavior.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The cron expression carries a comment explaining the weekly cadence as a bounded-cost choice and cross-referencing `SWEEP_HORIZON_DAYS`.
    - No behavior change; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Cron cadence documented as a deliberate cost choice (OPS-04, code side).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test and a comment; it introduces no new runtime path or external call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-06 | Tampering | OPS-03 regression could silently break if discoveryService.ts is later changed | low | mitigate | The regression test fails loudly if the zero-candidate path ever regresses to `failed`/throw. |

No package installs — no supply-chain threat.
</threat_model>

<verification>
`cd /c/EV-Accounts/backend && npx vitest run src/lib/discoveryService.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
OPS-03 caller contract locked (zero candidates → completed, not failed). OPS-04 documented at the cron registration site. No source behavior changed.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-03-SUMMARY.md` when done.
</output>
