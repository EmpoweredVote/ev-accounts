---
phase: 173-discovery-sweep-anthropic-cost-reliability-hardening
plan: 04
type: execute
wave: 3
depends_on: ["173-01", "173-02", "173-03"]
files_modified:
  - .planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-OPS-04-cadence-decision.md
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04]

must_haves:
  truths:
    - "The full backend Vitest suite is green — all three new test files plus the pre-existing suite (OPS-01/02/03 behaviors all pass)."
    - "The live count of discovery_jurisdictions within the 180-day horizon is captured read-only, and the weekly/180-day cadence decision is recorded (OPS-04)."
    - "The code changes are deployed to Render (this is CODE, not data — a deploy is required) and the service is live."
  artifacts:
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-OPS-04-cadence-decision.md
  key_links:
    - "green full suite → Render deploy → next weekly sweep exhibits OPS-01/02/03 behavior in prod."
  prohibitions:
    - "MUST NOT mutate any prod data — the horizon-count query is read-only (SELECT count(*))."
    - "MUST NOT deploy before the full suite is green."
---

<objective>
Terminal gate + ship. Run the full backend test suite (all three new files + existing tests) to prove OPS-01/02/03 hold end-to-end, confirm the OPS-04 cadence decision against live data with a read-only horizon-count query, and deploy the code to Render (data-only changes need no deploy, but this phase changes backend TypeScript, so a deploy is required).

Purpose: Close Phase 173 by proving the reliability fixes green and shipping them.
Output: A recorded OPS-04 cadence decision note + a live Render deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-VALIDATION.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-01-SUMMARY.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-02-SUMMARY.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-03-SUMMARY.md
</context>

## Artifacts this plan produces
- `173-OPS-04-cadence-decision.md` — records the live horizon jurisdiction count + the confirmed cadence decision

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate</name>
  <files>(no source changes — runs the suite)</files>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-VALIDATION.md (the 8 automated behaviors + full-suite command)
  </read_first>
  <action>
    Run the full backend Vitest suite. All three new test files (`discoveryCron.test.ts`, `discoveryAgentRunner.test.ts`, `discoveryService.test.ts`) and the pre-existing suite must be green. If any pre-existing test regressed due to the `discoveryCron.ts`/`discoveryAgentRunner.ts` edits, fix it (or surface it) before proceeding. Confirm a clean typecheck too.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with all suites green, including the eight VALIDATION `-t` behaviors across OPS-01/02/03.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Full suite + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: OPS-04 — confirm live horizon jurisdiction count and record the cadence decision</name>
  <files>.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-OPS-04-cadence-decision.md</files>
  <read_first>
    - 173-RESEARCH.md "Open Questions → item 1" (the read-only count query + why research did not run it)
    - backend/src/lib/discoveryCron.ts (the jurisdiction query + SWEEP_HORIZON_DAYS, for the exact predicate)
  </read_first>
  <action>
    Run the read-only prod query counting jurisdictions in the sweep horizon against project ref `kxsdzaojfaibhuzmclfq`: `SELECT count(*) FROM essentials.discovery_jurisdictions WHERE election_date > now() AND election_date <= now() + interval '180 days'`. This is read-only — no mutation. Write `173-OPS-04-cadence-decision.md` recording: the returned count, the date checked, the confirmation that the weekly Sunday-02:00-UTC cadence + 180-day horizon remain the deliberate cost choice (default: keep — each sweep now makes one canary call + one paid discovery call per in-horizon jurisdiction, bounded weekly), and a note that if the count is unexpectedly large (cost concern) the operator may adjust cadence/horizon as a follow-up. Cross-reference the OPS-04 code comments landed in `discoveryCron.ts` (173-02) and `discoverySweep.ts` (173-03).
  </action>
  <verify>
    <automated>test -f .planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-OPS-04-cadence-decision.md && grep -qi "horizon" .planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-OPS-04-cadence-decision.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The note file exists and records the live horizon count + date + the confirmed (or operator-adjusted) cadence decision.
    - No prod data was mutated (query is a bare `SELECT count(*)`).
  </acceptance_criteria>
  <done>OPS-04 cadence decision recorded against live data.</done>
</task>

<task type="auto">
  <name>Task 3: Render deploy (code change requires deploy)</name>
  <files>(deploy action — no source changes)</files>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-01-PLAN.md (the established RENDER_DEPLOY_HOOK curl + /health poll + human-action fallback pattern)
  </read_first>
  <action>
    Ensure the phase's committed changes are on `master` (the orchestrator commits plans/code; confirm the three source files + three test files are committed). Trigger the Render deploy by `curl`-ing `$RENDER_DEPLOY_HOOK` (value in backend/.env or the operator's env per the render-deploy reference), then poll the service `/api/health` (or `/health`) endpoint until it returns 200 (deploy complete). If `RENDER_DEPLOY_HOOK` is unset/unreachable, STOP and create a `checkpoint:human-action` asking the operator to trigger the deploy from the Render dashboard, then resume the health poll. Post-deploy behavioral verification (that the next sweep — or a manual dashboard trigger with a deliberately-bad key — produces a single skip-alert rather than a flood) is a manual verification per the VALIDATION Manual-Only table; note it in the SUMMARY for the operator, do not block the plan on the next Sunday fire.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && curl -fsS "$RENDER_DEPLOY_HOOK" >/dev/null && echo DEPLOY_TRIGGERED || echo "RENDER_DEPLOY_HOOK unset/unreachable — operator checkpoint required"</automated>
  </verify>
  <acceptance_criteria>
    - Render deploy triggered (hook returned success) and `/api/health` returns 200 after the deploy, OR an operator checkpoint was raised because the hook was unavailable.
    - The manual post-deploy behavioral check is documented in the SUMMARY for the operator.
  </acceptance_criteria>
  <done>Code deployed to Render and live; post-deploy behavioral check handed to the operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → prod DB | The horizon-count query reads `essentials.discovery_jurisdictions` (read-only). |
| executor → Render | The deploy hook triggers a production deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-07 | Tampering | prod horizon-count query | low | mitigate | Query is a bare `SELECT count(*)` — no write; acceptance asserts read-only. |
| T-173-08 | Denial of Service | shipping a broken build | high | mitigate | Deploy is gated on the full suite + typecheck being green (Task 1 precedes Task 3). |

No package installs — no supply-chain threat.
</threat_model>

<verification>
Full suite + typecheck green; OPS-04 decision note present; Render deploy triggered and `/api/health` 200 (or operator checkpoint raised).
</verification>

<success_criteria>
Phase 173 shipped: OPS-01/02/03 proven green end-to-end, OPS-04 cadence confirmed and recorded against live data, and the backend code live on Render.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/173-discovery-sweep-anthropic-cost-reliability-hardening/173-04-SUMMARY.md` when done.
</output>
