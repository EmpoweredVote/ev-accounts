---
phase: 166-consolidated-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/166-derive-pins.ts
  - backend/scripts/166-pins.generated.sql
autonomous: true
requirements: [USHC3-06]

must_haves:
  truths:
    - "The Wave-3 in-scope universe is derived LIVE from prod at 166-authoring time (2026-07-26), not copied from the 162/163/164/165 gate snapshots frozen 2026-07-05..07."
    - "Every candidate who has ACQUIRED a headshot or a stance since its source gate was frozen DROPS OFF the pin list; every candidate who has since gone inactive or out-of-band drops off too."
    - "Candidates that are newly 0-stance and NOT in the frozen 124-pin set are surfaced as a visibly distinct Phase-167 queue set, never silently folded into the researched honest-skip list."
    - "The live PROVISIONAL census and the live withheld-election census are recorded so the gate asserts the CURRENT state, not the state as of early July."
  artifacts:
    - "backend/scripts/166-derive-pins.ts — re-runnable, read-only live derivation harness"
    - "backend/scripts/166-pins.generated.sql — ready-to-embed SQL VALUES fragments plus census header, consumed by 166-03 and 166-04"
  key_links:
    - "166-derive-pins.ts scopes by the SAME 42 election names and 38 geo prefixes that 166-verify.sql will use — a scope mismatch here silently produces pins for the wrong universe"
    - "166-pins.generated.sql _img_skip / _stance_skip VALUES blocks are pasted verbatim into 166-verify.sql (plan 166-03, Task 3)"
    - "The PROVISIONAL and withheld censuses feed 166-verify.sql (166-03 Task 2) and 166-verify-invariants.sql (166-04 Task 1)"
---

<objective>
Re-derive, LIVE against prod, the full Phase-166 in-scope universe and every pin list the consolidated gate will carry — headshot honest-skips, whole-record stance honest-skips, the PROVISIONAL census, and the withheld-election census — and emit them as a committed, re-runnable script plus a ready-to-embed SQL fragment.

Purpose: the five per-phase gates froze their pin lists at their own authoring dates (161 and 162 on 2026-07-04/05, 163 on 2026-07-06, 164 and 165 on 2026-07-07). Today is 2026-07-26. Filings, withdrawals, culls and the 164.2-04 FL/CA candidate-field repairs (2026-07-22) have all landed since. A gate built on those snapshots would be green for the wrong reasons.

Output: `backend/scripts/166-derive-pins.ts` (read-only harness) and `backend/scripts/166-pins.generated.sql` (the artifact 166-03 and 166-04 consume).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (166-01) produces:

- `backend/scripts/166-derive-pins.ts` — read-only live derivation harness, run via `node --import tsx`.
- `backend/scripts/166-pins.generated.sql` — generated SQL fragment containing:
  - a `-- === 166 CENSUS HEADER ===` comment block (per-state race and active-candidate counts, PROVISIONAL census, withheld census, delta summary)
  - `INSERT INTO _img_skip (external_id) VALUES ...` — the live headshot honest-skip list
  - `INSERT INTO _stance_skip (external_id, reason) VALUES ...` — the surviving researched whole-record stance skips
  - `INSERT INTO _stance_queue_167 (external_id, reason) VALUES ...` — newly 0-stance banded candidates queued to Phase 167 (may be empty)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author 166-derive-pins.ts scope layer — 42 elections, 38 geo prefixes, 38 external_id bands, per-state census</name>

  <read_first>
    - `backend/scripts/165-verify.sql` — the structural template: 17-election resolution, the `_house` working set built from `substr(d.geo_id,1,2)`, banded `_new_cands` construction
    - `backend/scripts/162-verify.sql` — the 4-election plus withheld-election variant and its band comments
    - `backend/scripts/165-coordinate-smoke.ts` — the `pool` import convention (`import { pool } from '../src/lib/db.js'`) and the `node --import tsx` run line
    - `CLAUDE.md` — sections "Officeholder occupancy", "Repo facts worth knowing", "Key Architecture Rules"
  </read_first>

  <files>backend/scripts/166-derive-pins.ts</files>

  <action>
Create a read-only Node/TSX harness that establishes the exact Phase-166 in-scope universe. Use `pool.query()` from `../src/lib/db.js` (never PostgREST — `essentials` is not an exposed schema) and issue zero INSERT/UPDATE/DELETE against essentials or inform.

Resolve 42 elections by EXACT name and throw naming the offender if any resolves NULL.

38 general elections, as state / exact name / 2-digit FIPS geo prefix / expected district count:
AZ 'AZ 2026 Statewide General' 04 9; WA 'WA 2026 Statewide General' 53 10; TN 'TN 2026 Statewide General' 47 9; MA '2026 Massachusetts General Election' 25 9; IN 'IN 2026 Statewide General' 18 9; MD '2026 Maryland General Election' 24 8; MN 'MN 2026 Statewide General' 27 8; MO 'MO 2026 Statewide General' 29 (3 of MO's 8); WI 'WI 2026 Statewide General' 55 8; CO 'CO 2026 Statewide General' 08 8; AL 'AL 2026 Statewide General' 01 7; SC 'SC 2026 Statewide General' 45 7; LA 'LA 2026 Statewide General' 22 6; KY 'KY 2026 Statewide General' 21 6; OR 'OR 2026 General' 41 6; CT 'CT 2026 Statewide General' 09 5; OK 'OK 2026 Statewide General' 40 5; AR 'AR 2026 Statewide General' 05 4; IA 'IA 2026 Statewide General' 19 4; KS 'KS 2026 Statewide General' 20 4; MS 'MS 2026 Statewide General' 28 4; NV 'NV 2026 Statewide General' 32 4; UT 'UT 2026 Statewide General' 49 4; NM 'NM 2026 Statewide General' 35 3; NE 'NE 2026 Statewide General' 31 3; WV 'WV 2026 Statewide General' 54 2; ID 'ID 2026 Statewide General' 16 2; HI 'HI 2026 Statewide General' 15 2; ME '2026 Maine General Election' 23 2; NH 'NH 2026 Statewide General' 33 2; RI 'RI 2026 Statewide General' 44 2; MT 'MT 2026 Statewide General' 30 2; AK 'AK 2026 Statewide General' 02 1; DE 'DE 2026 Statewide General' 10 1; ND 'ND 2026 Statewide General' 38 1; SD 'SD 2026 Statewide General' 46 1; VT 'VT 2026 Statewide General' 50 1; WY 'WY 2026 Statewide General' 56 1.

4 withheld/marker elections: 'TN 2026 Congressional Redistricting - Polygon Pending', 'MO 2026 Congressional Redistricting - Polygon Pending', 'AL 2026 Congressional Redistricting - Polygon Pending', 'LA 2026 Congressional Redistricting - Polygon Pending'.

Build the in-scope working set exactly as 165-verify.sql does: `essentials.races r JOIN essentials.offices o ON o.id = r.office_id JOIN essentials.districts d ON d.id = o.district_id LEFT JOIN essentials.race_candidates rc ON rc.race_id = r.id LEFT JOIN essentials.politicians p ON p.id = rc.politician_id`, filtered by `r.election_id IN (all 42)` AND `d.district_type = 'NATIONAL_LOWER'` AND `substr(d.geo_id,1,2) IN` the 38 prefixes above. Derive the state abbreviation from `substr(d.geo_id,1,2)` through a FIPS-to-abbreviation map, NOT from election_id — 42 elections make the CASE-on-election_id form used by 152 and 162 unmanageable.

Do not read the occupancy column that ADR 0002 / migration 1463 removed from `essentials.offices`. Occupancy is not needed here at all, and the CI guard `check:occupancy` pattern-matches newly added files including their comment text.

Encode the 38 new-candidate external_id bands verbatim from the source gates:
161 — AZ -40901..-40101, WA -531005..-530101, TN -470910..-470101, MA -250902..-250101.
162 — IN -180999..-180101, MD -240899..-240101, MN -270899..-270101, MO -290899..-290101.
163 — WI -550899..-550101, CO -80899..-80101, AL -10799..-10101, SC -450799..-450101, LA -220699..-220101.
164 — KY -210899..-210101, OR -410699..-410101, CT -90599..-90101, OK -400599..-400101, AR -50499..-50101, IA -190499..-190101, KS -200499..-200101, MS -280499..-280101.
165 — NV -320499..-320101, UT -490499..-490101, AK -20099..-20001, NM -350399..-350101, NE -310399..-310101, WV -540299..-540101, ID -160299..-160101, HI -150299..-150101, ME -230299..-230101, NH -330299..-330101, RI -440299..-440101, MT -300299..-300101, DE -100099..-100001, ND -380099..-380001, SD -460099..-460001, VT -500099..-500001, WY -560099..-560001.

Define the banded universe with the SUPERSET filter `candidate_status='active'` (the form 161, 162, 163 and 164 used) rather than 165's stricter `active AND is_incumbent=false`. Print BOTH cardinalities and the exact set difference so the choice is auditable — a superset is the safe direction for a gate, because it demands an image-or-pin from strictly more candidates.

Print a per-state census table: state, geo prefix, distinct NATIONAL_LOWER race count, active candidate count, banded-new-candidate count. Throw on any per-state mismatch and on a total that is not 178.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-derive-pins.ts | tee /tmp/166-t1.txt &amp;&amp; grep -q "TOTAL DISTRICTS: 178" /tmp/166-t1.txt</automated>
  </verify>

  <acceptance_criteria>
    - `node --import tsx scripts/166-derive-pins.ts` exits 0.
    - stdout contains the line `TOTAL DISTRICTS: 178`.
    - Per-state census lines match the documented decomposition exactly: WA 10, AZ 9, TN 9, MA 9, IN 9, MD 8, MN 8, MO 8, WI 8, CO 8, AL 7, SC 7, LA 6, KY 6, OR 6, CT 5, OK 5, AR 4, IA 4, KS 4, MS 4, NV 4, UT 4, NM 3, NE 3, WV 2, ID 2, HI 2, ME 2, NH 2, RI 2, MT 2, AK 1, DE 1, ND 1, SD 1, VT 1, WY 1; the script throws if any single state's count differs.
    - stdout contains a line beginning `BAND FILTER DELTA:` reporting the count under `active` versus under `active AND is_incumbent=false`, plus the explicit external_id list of every row in the difference.
    - All 42 election names resolve; the script throws with the offending name when any is NULL.
    - `npm run check:occupancy --prefix backend` exits 0 with the new file staged.
  </acceptance_criteria>

  <done>The in-scope Wave-3 universe is established live from prod, provably 178 districts across 38 states, with both candidate-band filter definitions measured and their difference enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the headshot pin list live; intersect the stance pin list and split out the Phase-167 queue</name>

  <read_first>
    - `backend/scripts/162-verify.sql` — the `_stance_skip` VALUES-join-politicians pattern, the `_img_skip` external_id list layout, and the two assertions that consume them
    - `backend/scripts/165-verify.sql` — the same pattern at 17-state scale, plus the race-scoped UNSOURCED form
    - `backend/scripts/161-verify.sql`, `backend/scripts/163-verify.sql`, `backend/scripts/164-verify.sql` — the remaining three frozen `_stance_skip` reason sets that must be carried over verbatim
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-11-SUMMARY.md` — states 110 headshot / 24 stance pins and the "reconstructed live at authoring time" precedent
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-11-SUMMARY.md` — 95 / 10, plus the Edmonds -220605 re-key that invalidated a stale pin
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-13-SUMMARY.md` — 85 / 13
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-17-SUMMARY.md` — 109 / 18
  </read_first>

  <files>backend/scripts/166-derive-pins.ts</files>

  <action>
Extend the harness with the two pin derivations. Treat them differently, because they are epistemically different.

Headshot pins are pure derivation, not judgment — the frozen lists were themselves just "who lacks an `essentials.politician_images` row". Regenerate the whole list from scratch: every banded active candidate for whom no `essentials.politician_images` row exists. Emit ordered by external_id descending, grouped by state with a per-state count comment. Then emit `HEADSHOT DELTA vs frozen 566` where 566 is 161's 167 plus 162's 110 plus 163's 95 plus 164's 85 plus 165's 109, listing every external_id that DROPPED (was frozen-pinned, now has an image or is no longer active or in-band) and every one that is NEWLY pinned.

Stance whole-record skips are judgment — each frozen pin carries a documented search trail in its phase SUMMARY, and a trail cannot be regenerated by a query. So compute three disjoint sets:
  - `live_zero` = banded active candidates whose `inform.politician_answers` count is below 1.
  - `surviving` = frozen intersect live_zero. These keep their original reason text, carried across verbatim from 161-verify.sql, 162-verify.sql, 163-verify.sql, 164-verify.sql and 165-verify.sql. Emit as the `_stance_skip` fragment.
  - `retired` = frozen minus live_zero. These have since acquired a stance, or gone inactive or out-of-band. They must NOT appear in the gate. List each with which of those reasons applies.
  - `queue_167` = live_zero minus frozen. These are 0-stance today but carry no documented search trail. Do NOT invent a reason for them and do NOT merge them into `_stance_skip`. Emit them into a separately named `_stance_queue_167` fragment carrying the literal reason string `new/unresearched 0-stance banded candidate detected at 166 authoring (2026-07-26); stance research queued to Phase 167`. This keeps the gate honest: it records an actual dated state of the world instead of asserting a research judgment nobody made.

Hardcode the frozen baselines as data inside the script (external_id to reason, sourced from the five gate files) so the delta is computed rather than asserted from memory. Total frozen stance pins is 124: 161's 59 plus 162's 24 plus 163's 10 plus 164's 13 plus 165's 18.

Print five labelled sections: `HEADSHOT PINS (live)`, `HEADSHOT DELTA vs frozen 566`, `STANCE PINS surviving`, `STANCE retired`, `STANCE queue-167`.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-derive-pins.ts | tee /tmp/166-t2.txt &amp;&amp; grep -q "HEADSHOT DELTA vs frozen 566" /tmp/166-t2.txt &amp;&amp; grep -q "STANCE queue-167" /tmp/166-t2.txt</automated>
  </verify>

  <acceptance_criteria>
    - stdout contains `HEADSHOT PINS (live): N` followed by the per-state breakdown.
    - stdout contains `HEADSHOT DELTA vs frozen 566: dropped=X added=Y`; when X or Y is non-zero the individual external_ids are listed on following lines, not merely counted.
    - stdout contains `STANCE PINS surviving: S of frozen 124`, `STANCE retired: R` and `STANCE queue-167: Q`, and S plus R equals 124.
    - Every surviving stance entry carries a non-empty reason string copied verbatim from its source gate file — no entry has an empty, placeholder, or newly-invented reason.
    - Every queue-167 entry carries the exact literal reason `new/unresearched 0-stance banded candidate detected at 166 authoring (2026-07-26); stance research queued to Phase 167` and appears only under the `_stance_queue_167` heading.
    - Two consecutive runs produce byte-identical pin sections (idempotent, read-only).
  </acceptance_criteria>

  <done>Both pin lists reflect prod as of 2026-07-26, every stale pin is enumerated as dropped, and no candidate has been silently assigned a research judgment that was never made.</done>
</task>

<task type="auto">
  <name>Task 3: Derive the PROVISIONAL and withheld-election censuses; emit 166-pins.generated.sql</name>

  <read_first>
    - `backend/scripts/164-verify.sql` — the CT/KS PROVISIONAL block, the only Wave-3 PROVISIONAL precedent outside 165
    - `backend/scripts/165-verify.sql` — the 10-marked / 13-unmarked PROVISIONAL block and its NV/UT/ME exclusion
    - `backend/scripts/162-verify.sql` — the MO-SEVERE withheld-versus-surfacing assertion this census must feed
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-04-SUMMARY.md` — migration 1247 emptied the TN marker election
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-05-SUMMARY.md` — migrations 1248 and 1249 emptied the AL and LA marker elections
  </read_first>

  <files>backend/scripts/166-derive-pins.ts, backend/scripts/166-pins.generated.sql</files>

  <action>
Add two censuses, then write the consumable artifact.

PROVISIONAL census: for all 178 races report which carry `description LIKE '%PROVISIONAL:%'` and which do not, grouped by state, as two explicit state lists with per-state race counts. Phases 161, 162 and 163 authored no PROVISIONAL assertion at all, so this census is a genuinely new derivation and is the only authority for what 166-verify.sql may assert. Emit it in the shape the gate consumes: a marked-state list with expected race count and an unmarked-state list with expected race count. Record in the emitted comment that Phase 167 clears these flags per primary-date cluster, so the gate asserts today's state and 167 will move states between the two lists.

Withheld census: for all 178 races report `race.election_id` grouped by the 4 Polygon Pending marker elections versus the 38 generals. Expected outcome given migrations 1247 (TN), 1248 (AL) and 1249 (LA): the TN, AL and LA marker elections hold 0 races each, and only the MO marker election holds races — exactly geo_ids 2902, 2903, 2904, 2905 and 2906. Report the actual per-marker counts and the actual geo_id list. If TN, AL or LA is non-empty, or the MO set differs from those five, print a `CENSUS MISMATCH` line and exit non-zero rather than emitting a fragment that encodes a wrong world.

Then write `backend/scripts/166-pins.generated.sql` containing, in order:
  1. A `-- === 166 CENSUS HEADER ===` comment block carrying the derivation date, the per-state district census, the band-filter delta, the headshot delta, the stance surviving/retired/queue counts, the PROVISIONAL marked and unmarked state lists, and the withheld census.
  2. `INSERT INTO _img_skip (external_id) VALUES ...;` — ten ids per line, grouped by state with a `-- {ST} ({n})` comment above each group, matching the 162 and 165 house layout.
  3. `INSERT INTO _stance_skip (external_id, reason) VALUES ...;` — one per line, ordered by external_id, each with its verbatim carried-over reason.
  4. `INSERT INTO _stance_queue_167 (external_id, reason) VALUES ...;` — same shape; when the queue is empty emit the heading with a `-- (empty)` comment and no INSERT.

The fragment is pasted into 166-verify.sql by plan 166-03, so it must contain only INSERTs into those three temp tables and comments — no DDL, and nothing that writes to `essentials` or `inform`.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-derive-pins.ts &amp;&amp; test -s scripts/166-pins.generated.sql &amp;&amp; grep -q "=== 166 CENSUS HEADER ===" scripts/166-pins.generated.sql &amp;&amp; grep -q "INSERT INTO _img_skip" scripts/166-pins.generated.sql &amp;&amp; grep -q "INSERT INTO _stance_skip" scripts/166-pins.generated.sql</automated>
  </verify>

  <acceptance_criteria>
    - `backend/scripts/166-pins.generated.sql` exists, is non-empty, and opens with `-- === 166 CENSUS HEADER ===`.
    - The header block names the derivation date 2026-07-26 and carries all six census sections listed in the action.
    - stdout contains `WITHHELD CENSUS: TN 0 / AL 0 / LA 0 / MO 5` and lists the MO geo_ids as `2902, 2903, 2904, 2905, 2906`; any deviation prints `CENSUS MISMATCH` and the script exits non-zero.
    - stdout contains `PROVISIONAL CENSUS: marked=<states+counts> unmarked=<states+counts>` covering all 178 races with no race unaccounted for.
    - The generated file contains an `INSERT INTO _img_skip` block and an `INSERT INTO _stance_skip` block, and either an `INSERT INTO _stance_queue_167` block or that heading followed by `-- (empty)`.
    - `grep -cE "^(CREATE|ALTER|DROP)" backend/scripts/166-pins.generated.sql` returns 0 — the fragment declares no DDL of its own.
  </acceptance_criteria>

  <done>166-pins.generated.sql exists with a dated census header and three temp-table INSERT fragments, and the withheld census independently confirms that only MO's five severe districts remain behind a Polygon Pending election.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script to production Postgres | A derivation harness holding prod credentials from `backend/.env` runs against `kxsdzaojfaibhuzmclfq` |
| frozen SUMMARY text to gate assertion | Human-authored pin reasons are transcribed into a machine-enforced gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-01-01 | Tampering | 166-derive-pins.ts | high | mitigate | Harness is SELECT-only; the sole write is the local `166-pins.generated.sql` file. Acceptance criteria assert no DML against essentials or inform. |
| T-166-01-02 | Repudiation | pin provenance | high | mitigate | Stance reasons are carried verbatim from the five source gate files; newly 0-stance candidates go to a separately named `_stance_queue_167` set with an explicit dated reason, so no research judgment is fabricated. |
| T-166-01-03 | Information disclosure | prod credentials | medium | mitigate | Credentials sourced from `backend/.env` in-process only; never echoed to stdout or into the generated file. |
| T-166-01-04 | Denial of service | Supabase pooler | low | accept | Read-only census queries; 163-11 recorded a transient pooler stall during heavy authoring — retry is sufficient. |
| T-166-01-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages; `tsx` and `pg` are already in `backend/package.json`. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --import tsx scripts/166-derive-pins.ts` exits 0 and prints `TOTAL DISTRICTS: 178`.
- `backend/scripts/166-pins.generated.sql` exists with a dated census header and the three temp-table INSERT fragments.
- Withheld census reports TN 0 / AL 0 / LA 0 / MO 5 with geo_ids 2902-2906.
- `npm run check:occupancy --prefix backend` exits 0.
- Both files committed.
</verification>

<success_criteria>
The consolidated gate authored in 166-03 and 166-04 can be built entirely from artifacts derived from prod on 2026-07-26; no pin id, PROVISIONAL claim, or withholding claim in Phase 166 traces back to an early-July snapshot without having been re-confirmed live.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-01-SUMMARY.md` when done. The SUMMARY must record: the headshot delta counts with the dropped ids, the stance surviving/retired/queue counts with the queue-167 ids enumerated in full (Phase 167 consumes that list), the PROVISIONAL census, and the withheld census.
</output>

