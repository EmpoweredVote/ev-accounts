---
phase: 166-consolidated-verification-gate
plan: 03
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - backend/scripts/166-verify.sql
autonomous: true
requirements: [USHC3-06]

must_haves:
  truths:
    - "All 178 Wave-3 districts are structurally sound in one assertion pass: no NULL office_id, no NULL politician_id on an active card, no duplicate name within a state, no politician active in two races, and no party column on the candidate card."
    - "Every active banded new candidate has a headshot or a live-derived pin, and every one has at least one sourced stance or a live-derived pin."
    - "There are 0 unsourced stance rows across the whole in-scope 178-district candidate set."
    - "The PROVISIONAL census asserted is the one measured on 2026-07-26, not the one that was true when 164 and 165 authored their gates."
    - "The gate is write-free and runs to completion under psql with ON_ERROR_STOP."
  artifacts:
    - "backend/scripts/166-verify.sql — the milestone-level structural gate for the 178 Wave-3 districts"
  key_links:
    - "The `_house` working set here must use the identical 42 elections and 38 geo prefixes as 166-derive-pins.ts, or the pins describe a different universe than the assertions"
    - "The `_img_skip` / `_stance_skip` / `_stance_queue_167` blocks are pasted verbatim from backend/scripts/166-pins.generated.sql (166-01 Task 3)"
    - "The NATIONAL-TOTAL footer names backend/scripts/152-verify.sql and backend/scripts/158-verify.sql as the owners of the 144 and 89 non-Wave-3 segments"
---

<objective>
Author `backend/scripts/166-verify.sql`: the read-only, milestone-level structural gate asserting USHC3-06 across all 178 Wave-3 districts, carrying the live-derived pin lists from 166-01 and closing with the national-coverage footer.

Purpose: this is the assertion half of the milestone proof. Its sibling `166-verify-invariants.sql` (plan 166-04) carries the fifteen per-phase invariants inherited from 161 through 165; splitting them keeps each file authorable in one pass and each independently runnable.

Output: one committed SQL gate that exits 0 under `psql -v ON_ERROR_STOP=1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/REQUIREMENTS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (166-03) produces `backend/scripts/166-verify.sql` with these named assertion blocks, each emitting one `PASS {NAME}:` notice or raising:

- `SCOPE` — 178 distinct NATIONAL_LOWER races with the per-state decomposition
- `ACTIVE` — every one of the 178 races has at least 1 active candidate
- `NULLOFFICE` — 0 races with NULL office_id, including the 5 withheld MO races
- `NULLPID` — 0 active race_candidates with NULL politician_id
- `DUPNAME` — 0 duplicate lower(full_name) groups within any state among active candidates
- `DUPINCUMBENT` — 0 politician_id active in 2 or more distinct races
- `RC-UNIQUE` — exactly 1 race_candidates row per (race_id, politician_id) across all 178 races
- `PARTY` — essentials.race_candidates carries no party column
- `PROVISIONAL` — the live 2026-07-26 marked/unmarked census holds
- `HEADSHOT` — every active banded new candidate has an image row or a live-derived pin
- `UNSOURCED` — 0 unsourced stance rows in the in-scope set
- `COVERAGE` — every active banded new candidate has at least 1 stance or a pin
- Temp tables: `_house`, `_new_cands`, `_img_skip`, `_stance_skip`, `_stance_queue_167`
- Terminal `ALL ASSERTIONS PASSED` notice plus the `NATIONAL TOTAL` footer notice
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Working set plus the eight structural assertions — SCOPE, ACTIVE, NULLOFFICE, NULLPID, DUPNAME, DUPINCUMBENT, RC-UNIQUE, PARTY</name>

  <read_first>
    - `backend/scripts/152-verify.sql` — the milestone-level precedent: its ACTIVE, DUPINCUMBENT and UNSOURCED-as-existence-check blocks, and its "per-phase gates own the granular pins" framing
    - `backend/scripts/158-verify.sql` — the second milestone precedent, and its explicit header note that it must not reference MI or VA (the reason the national arithmetic is 89 + 24, not 113)
    - `backend/scripts/165-verify.sql` — the 17-election `_house` construction with the geo-prefix-derived state abbreviation, which scales to 42 elections
    - `backend/scripts/162-verify.sql` — the withheld-election-inclusive `_house` variant and its NULLOFFICE wording covering withheld races
    - `backend/scripts/166-pins.generated.sql` — the census header from 166-01, the authority for the counts asserted here
    - `CLAUDE.md` — the `is_vacant` trap, the mixed-case `districts.state` rule, and the migration policy (this phase should need no migration)
  </read_first>

  <files>backend/scripts/166-verify.sql</files>

  <action>
Create the gate with the house preamble: a header comment block, `\set ON_ERROR_STOP on`, then a single `DO $$ ... END $$;` with a DECLARE section. Write-free: the only permitted writes are `CREATE TEMP TABLE ... ON COMMIT DROP`. Run line documented in the header as `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql`.

Resolve the same 42 elections by exact name as 166-derive-pins.ts (38 generals plus the TN, MO, AL and LA Polygon Pending markers), raising if any is NULL. Build `_house` exactly as 165-verify.sql does but with the state abbreviation derived from `substr(d.geo_id,1,2)` across all 38 FIPS prefixes: 04, 53, 47, 25, 18, 24, 27, 29, 55, 08, 01, 45, 22, 21, 41, 09, 40, 05, 19, 20, 28, 32, 49, 35, 31, 54, 16, 15, 23, 33, 44, 30, 10, 38, 46, 50, 56, 02. Select race_id, election_id, office_id, description, primary_party, geo_id, rc id, politician_id, full_name, candidate_status, is_incumbent and the politician's external_id. Do not filter on `offices.is_vacant` anywhere — the CLAUDE.md trap is that such a filter, when applied to a downstream join rather than the match, emits a spurious all-NULL office row. Do not reference the occupancy column ADR 0002 / migration 1463 removed from `essentials.offices`, in code or in comments.

Then write eight blocks, each raising with a specific message on failure and otherwise emitting `RAISE NOTICE 'PASS {NAME}: ...'`.

SCOPE: assert per-state distinct race counts WA 10, AZ 9, TN 9, MA 9, IN 9, MD 8, MN 8, MO 8, WI 8, CO 8, AL 7, SC 7, LA 6, KY 6, OR 6, CT 5, OK 5, AR 4, IA 4, KS 4, MS 4, NV 4, UT 4, NM 3, NE 3, WV 2, ID 2, HI 2, ME 2, NH 2, RI 2, MT 2, AK 1, DE 1, ND 1, SD 1, VT 1, WY 1, using 165's "count the states whose n is wrong" pattern so the failure message names the offenders; then assert the total is 178.

ACTIVE: every one of the 178 races has at least 1 active candidate; report separately, as a NOTICE not a failure, how many have fewer than 2 (the uncontested-seat allowance from 152).

NULLOFFICE: 0 races with NULL office_id; word the pass notice to state that this includes the 5 withheld severe MO races, whose office_id is populated and only whose election_id is withheld.

NULLPID: 0 active race_candidates with NULL politician_id.

DUPNAME: 0 duplicate `lower(full_name)` groups within a state among active candidates. Per-state only — cross-state homonyms are not a data error.

DUPINCUMBENT and RC-UNIQUE are NEW at this scale (152 and 158 carry DUPINCUMBENT; no Wave-3 gate does; RC-UNIQUE generalises 162's MD-DEDUP and 164's OR-REUSE duplicate clause to all 178 races). PROBE BOTH FIRST as plain SELECTs and inspect the output before converting them to assertions. If either returns non-zero, do not weaken or drop the assertion and do not assume a data bug: report the offending politician_id or (race_id, politician_id) pairs in the plan SUMMARY, determine whether they are a legitimate modelling case (for example a candidate appearing in two primary races under a separate primary election that should not be in `_house` at all) or a real defect, and adjust the scope or escalate accordingly.

PARTY: `information_schema.columns` shows no `party` or `party_affiliation` column on `essentials.race_candidates`.

Close with a temporary `RAISE NOTICE 'ALL ASSERTIONS PASSED (structural, 178 districts)'` so the file is runnable and verifiable after this task; Task 3 replaces it with the final footer.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql 2>&amp;1 | tee /tmp/166-verify-t1.txt &amp;&amp; grep -q "PASS SCOPE" /tmp/166-verify-t1.txt &amp;&amp; grep -q "PASS DUPINCUMBENT" /tmp/166-verify-t1.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify.sql` exits 0.
    - Output contains a `PASS SCOPE:` line naming the total 178.
    - Output contains one `PASS ` line for each of ACTIVE, NULLOFFICE, NULLPID, DUPNAME, DUPINCUMBENT, RC-UNIQUE and PARTY — eight `PASS ` lines in total with SCOPE.
    - The ACTIVE notice reports the count of races with fewer than 2 active candidates as an allowance, not a failure.
    - The file contains no INSERT, UPDATE or DELETE against `essentials` or `inform`; the only `CREATE` statements are `CREATE TEMP TABLE ... ON COMMIT DROP`.
    - `npm run check:occupancy --prefix backend` exits 0 with the new file staged.
    - The DUPINCUMBENT and RC-UNIQUE probe results are recorded in the plan SUMMARY even when both are zero.
  </acceptance_criteria>

  <done>A runnable, green, write-free gate proves the eight structural invariants across all 178 Wave-3 districts.</done>
</task>

<task type="auto">
  <name>Task 2: PROVISIONAL block from the live 2026-07-26 census</name>

  <read_first>
    - `backend/scripts/166-pins.generated.sql` — the `-- === 166 CENSUS HEADER ===` PROVISIONAL section, the ONLY authority for what this block may assert
    - `backend/scripts/164-verify.sql` — the CT/KS PROVISIONAL block shape (missing-marker count plus leak count)
    - `backend/scripts/165-verify.sql` — the same shape across 17 states, including its deliberate NV/UT/ME exclusion and why
    - `.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md` — the Phase 167 section, which clears these flags per primary-date cluster
  </read_first>

  <files>backend/scripts/166-verify.sql</files>

  <action>
Add a `PROVISIONAL` block asserting exactly the census 166-01 measured on 2026-07-26 — two counts, in the 164 and 165 shape: the number of in-scope races in the marked-state list that do NOT match `description LIKE '%PROVISIONAL:%'` must be 0, and the number in the unmarked-state list that DO match must be 0.

Take the two state lists and their expected race counts verbatim from the generated census header. Do not carry forward 164's CT-plus-KS list or 165's AK/HI/NH/RI/DE/VT/WY list as if they were still authoritative: phases 161, 162 and 163 authored no PROVISIONAL assertion at all, so their states were never censused, and 164 and 165 measured theirs three weeks ago.

Where a state appears in neither list because its marker wording is inherited from a reused pre-existing race (the NV, UT and ME exclusion 165 documented, and by the same logic MD, MA and OR), exclude it explicitly and name the exclusion and its reason in the block comment and in the pass notice, so an excluded state is a visible decision rather than an omission.

Add a comment on the block stating that Phase 167 clears these flags per primary-date cluster as each state's primary passes, so this assertion is expected to require an update at each 167 cluster execution — and that the correct update is to move the state between the two lists, never to delete the assertion.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql 2>&amp;1 | tee /tmp/166-verify-t2.txt &amp;&amp; grep -q "PASS PROVISIONAL" /tmp/166-verify-t2.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify.sql` exits 0 and emits `PASS PROVISIONAL:`.
    - The pass notice names the marked states with their race count, the unmarked states with their race count, and the explicitly excluded states with the reason for exclusion.
    - Marked plus unmarked plus excluded race counts sum to 178.
    - The state lists in the block match the `166-pins.generated.sql` census header exactly; no state list is copied from 164-verify.sql or 165-verify.sql.
    - The block comment states that Phase 167 moves states between the two lists rather than deleting the assertion.
  </acceptance_criteria>

  <done>The gate asserts the PROVISIONAL state of the world as measured on 2026-07-26, with every one of the 178 races accounted for as marked, unmarked, or explicitly excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Pin blocks (HEADSHOT, UNSOURCED, COVERAGE) plus the NATIONAL-TOTAL footer; full green run</name>

  <read_first>
    - `backend/scripts/166-pins.generated.sql` — the three INSERT fragments pasted here verbatim
    - `backend/scripts/165-verify.sql` — the `_new_cands` banded construction, the `_img_skip` / `_stance_skip` temp tables, and the HEADSHOT, UNSOURCED and COVERAGE assertion bodies
    - `backend/scripts/162-verify.sql` — the broadened UNSOURCED scope covering incumbents as well as challengers, and its rationale
    - `backend/scripts/152-verify.sql` — the UNSOURCED-as-existence-check framing and why a coverage assertion would false-fail at milestone scale
    - `backend/scripts/158-verify.sql` — its header statement that MI and VA are asserted separately by the date-gated Phase 159, which fixes the national arithmetic
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-17-SUMMARY.md` — the race-scoped UNSOURCED form that keeps polluted-band legacy rows out
  </read_first>

  <files>backend/scripts/166-verify.sql</files>

  <action>
Build `_new_cands` from `_house` using `candidate_status = 'active'` and the 38 external_id bands, matching the superset filter 166-01 Task 1 settled on. Bands: AZ -40901..-40101, WA -531005..-530101, TN -470910..-470101, MA -250902..-250101, IN -180999..-180101, MD -240899..-240101, MN -270899..-270101, MO -290899..-290101, WI -550899..-550101, CO -80899..-80101, AL -10799..-10101, SC -450799..-450101, LA -220699..-220101, KY -210899..-210101, OR -410699..-410101, CT -90599..-90101, OK -400599..-400101, AR -50499..-50101, IA -190499..-190101, KS -200499..-200101, MS -280499..-280101, NV -320499..-320101, UT -490499..-490101, AK -20099..-20001, NM -350399..-350101, NE -310399..-310101, WV -540299..-540101, ID -160299..-160101, HI -150299..-150101, ME -230299..-230101, NH -330299..-330101, RI -440299..-440101, MT -300299..-300101, DE -100099..-100001, ND -380099..-380001, SD -460099..-460001, VT -500099..-500001, WY -560099..-560001.

Create the three temp tables `_img_skip (external_id bigint)`, `_stance_skip (politician_id uuid, external_id bigint, reason text)` and `_stance_queue_167 (politician_id uuid, external_id bigint, reason text)`, then paste the three INSERT fragments from `166-pins.generated.sql` verbatim. Resolve external_id to politician_id via a join on `essentials.politicians` for the two reason-bearing tables, following the 162 and 165 pattern.

HEADSHOT: every `_new_cands` row must either have an `essentials.politician_images` row or appear in `_img_skip`; the failure message must string_agg the offending state-and-external_id list, never just a count.

UNSOURCED: an existence check, race-scoped through `_house` so polluted-band legacy records never enter — count `inform.politician_answers` rows for active in-scope politician_ids (challengers AND incumbents, the broadened 162 scope) that have no matching `inform.politician_context` row with a non-null `sources` array of length 1 or more. Must be 0. State in the block comment that this is an existence check and not a coverage assertion, per the 152 and 158 rationale.

COVERAGE: every `_new_cands` row not present in `_stance_skip` and not present in `_stance_queue_167` must have at least 1 `inform.politician_answers` row. The pass notice must report the two pin counts separately — the researched honest-skip count and the Phase-167 queue count — so a queued candidate is never mistaken for a researched skip.

Replace the temporary closing notice with two final notices. First `ALL ASSERTIONS PASSED` naming USHC3-06, the 178-district scope and the twelve block names. Second, a `NATIONAL TOTAL` notice stating: 178 Wave-3 districts asserted by this gate together with `backend/scripts/166-verify-invariants.sql` and `backend/scripts/166-coordinate-smoke.ts`; 144 v2.20 Wave-1 districts owned by `backend/scripts/152-verify.sql`; 89 v2.21 decided-state districts owned by `backend/scripts/158-verify.sql`; 24 v2.21 MI and VA districts seeded and gate-pending plan 159-06, date-gated on or after 2026-08-05; giving 411 gate-proven plus 24 gate-pending equals 435 US House districts nationally. Do not write 113 as a single v2.21 figure: 158-verify.sql's own header states it must not reference MI or VA, so the 24 are not covered by any currently passing gate.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql 2>&amp;1 | tee /tmp/166-verify-final.txt &amp;&amp; grep -q "PASS HEADSHOT" /tmp/166-verify-final.txt &amp;&amp; grep -q "PASS UNSOURCED" /tmp/166-verify-final.txt &amp;&amp; grep -q "PASS COVERAGE" /tmp/166-verify-final.txt &amp;&amp; grep -q "ALL ASSERTIONS PASSED" /tmp/166-verify-final.txt &amp;&amp; grep -q "NATIONAL TOTAL" /tmp/166-verify-final.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify.sql` exits 0.
    - Output contains twelve `PASS ` lines: SCOPE, ACTIVE, NULLOFFICE, NULLPID, DUPNAME, DUPINCUMBENT, RC-UNIQUE, PARTY, PROVISIONAL, HEADSHOT, UNSOURCED, COVERAGE.
    - `PASS UNSOURCED:` reports 0 unsourced rows.
    - `PASS COVERAGE:` reports the researched honest-skip pin count and the Phase-167 queue count as two separate figures.
    - The `NATIONAL TOTAL` notice contains the arithmetic `411 gate-proven + 24 gate-pending = 435` and names `152-verify.sql`, `158-verify.sql`, `166-verify-invariants.sql`, `166-coordinate-smoke.ts` and plan `159-06`.
    - The `_img_skip` and `_stance_skip` id lists in the file are byte-identical to the corresponding fragments in `backend/scripts/166-pins.generated.sql`.
    - The file remains write-free: the only `CREATE` statements are `CREATE TEMP TABLE ... ON COMMIT DROP`, and there is no DML against `essentials` or `inform`.
  </acceptance_criteria>

  <done>The structural gate is complete and green, carrying only live-derived pins, and its footer states the national coverage arithmetic with each segment attributed to the gate that owns it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script to production Postgres | A DO-block gate executes against `kxsdzaojfaibhuzmclfq` with the full `DATABASE_URL` role |
| generated fragment to gate assertion | Machine-generated pin lists are embedded into a human-reviewed assertion file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-03-01 | Tampering | 166-verify.sql | critical | mitigate | Write-free by construction: only `CREATE TEMP TABLE ... ON COMMIT DROP`; acceptance criteria assert the absence of DML against essentials and inform. |
| T-166-03-02 | Repudiation | pin provenance | high | mitigate | Pins pasted verbatim from `166-pins.generated.sql`, whose header records the 2026-07-26 derivation date and full deltas; acceptance requires byte-identity. |
| T-166-03-03 | Spoofing | false-green gate | critical | mitigate | The two new milestone-scale blocks (DUPINCUMBENT, RC-UNIQUE) must be probed as SELECTs before being asserted, and a non-zero probe escalates rather than being scoped away. |
| T-166-03-04 | Information disclosure | national coverage claim | high | mitigate | Footer separates 411 gate-proven from 24 seeded-but-gate-pending districts and names the owning gate for each segment. |
| T-166-03-05 | Elevation of privilege | is_vacant filter trap | medium | mitigate | No `offices.is_vacant` filter anywhere; CLAUDE.md documents that filtering it on a downstream join emits a spurious all-NULL office row. |
| T-166-03-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; the gate is plain SQL executed by the existing psql client. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `psql -v ON_ERROR_STOP=1 -f scripts/166-verify.sql` exits 0 with twelve PASS lines, `ALL ASSERTIONS PASSED` and the `NATIONAL TOTAL` footer.
- Pin blocks byte-match `backend/scripts/166-pins.generated.sql`.
- `npm run check:occupancy --prefix backend` exits 0.
- No migration is required by this plan; the gate is read-only and adds no schema. If the executor concludes otherwise, that conclusion must be stated explicitly in the SUMMARY with its reason before any migration is authored.
- File committed.
</verification>

<success_criteria>
USHC3-06's structural half is proven: across all 178 Wave-3 districts there are 0 NULL office_ids, 0 NULL politician_ids on active cards, 0 in-state duplicate names, 0 cross-race duplicate politicians, 0 unsourced stance rows, no party on the candidate card, and every banded new candidate carries a headshot and a stance or a dated, live-derived pin.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-03-SUMMARY.md` when done. Record the DUPINCUMBENT and RC-UNIQUE probe results, the PROVISIONAL census as asserted, the final pin counts split into researched skips versus Phase-167 queue, and the exact NATIONAL TOTAL footer text.
</output>
