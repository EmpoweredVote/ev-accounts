---
phase: 166-consolidated-verification-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/166-coordinate-smoke.ts
autonomous: true
requirements: [USHC3-06]

must_haves:
  truths:
    - "For a real in-district coordinate in each of the 38 Wave-3 states, the live surfacing join returns exactly one US House race."
    - "Each of those 38 races surfaces the CHALLENGER field, not merely the incumbent — the Pitfall-5 two-path guard named in ROADMAP success criterion 1."
    - "A coordinate inside a severe MO district surfaces ZERO races on the surfacing MO election, proving the withholding mechanism still holds end-to-end."
    - "TN, AL, LA and UT resolve through the deployed post-164.1 vintage-preference geometry, so the smoke mirrors what a voter actually gets rather than the retired boundaries."
  artifacts:
    - "backend/scripts/166-coordinate-smoke.ts — 38 positive samples plus 1 severe-MO negative sample"
  key_links:
    - "The surfacing query must mirror src/lib/electionService.ts getElectionsByCoordinate Part A; a plain geofence join double-matches old and new boundaries for dual-map states"
    - "Sample geo_ids must be selected LIVE from prod, not copied from the 161/162/163/164/165 smoke files whose picks were frozen in early July"
    - "The MO positive sample must come from 2901, 2907 or 2908; the five severe MO geo_ids are ineligible as positive samples"
---

<objective>
Author `backend/scripts/166-coordinate-smoke.ts`: a read-only coordinate-surfacing smoke covering all 38 Wave-3 states with one contested in-district sample each, plus the severe-MO negative sample, and prove it green against prod.

Purpose: ROADMAP success criterion 1 for Phase 166 is a coordinate-path proof, not a SQL-only proof. It must check the challenger field is present (Pitfall-5), because a gate that only proves the incumbent resolves passes on a half-seeded district.

Output: one committed script printing 38/38 PASS plus the MO negative.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (166-02) produces:

- `backend/scripts/166-coordinate-smoke.ts` — read-only coordinate smoke. Internal symbols:
  - `STATE_CONFIG` — 38 entries mapping state abbreviation to exact election name and 2-digit geo prefix
  - `SAMPLES` — 38 `{ state, geoId, minActive }` positive samples
  - `SEVERE_MO_GEO_ID` — the negative sample constant
  - `MIN_DISTRICTS` — 38
  - Console contract: one `PASS {ST} {geoId}: ...` line per state, one `PASS MO {geoId} (severe negative sample): ...` line, and a terminal `166 COORDINATE SMOKE GREEN:` summary line
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Clone the vintage-preference surfacing engine and wire STATE_CONFIG for all 38 states</name>

  <read_first>
    - `backend/scripts/163-coordinate-smoke.ts` — the post-164.1 vintage-preference LATERAL, which is the CURRENT correct mirror of the deployed resolution path (the plain geofence join in older smokes double-matches for dual-map states)
    - `backend/scripts/165-coordinate-smoke.ts` — the 17-state STATE_CONFIG shape, the `Sample` interface, the `MIN_DISTRICTS` gate, and the failure-collection style
    - `backend/scripts/152-coordinate-smoke.ts` and `backend/scripts/158-coordinate-smoke.ts` — the two prior milestone-scale smokes and their Pitfall-5 challenger assertion
    - `backend/src/lib/electionService.ts` — `getElectionsByCoordinate` Part A, the behaviour being mirrored
    - `CLAUDE.md` — the PostGIS `public.` prefix rule and the `essentials.districts.state` mixed-case rule
  </read_first>

  <files>backend/scripts/166-coordinate-smoke.ts</files>

  <action>
Create the smoke by cloning 163-coordinate-smoke.ts's engine verbatim — specifically the anchor-derivation query (which orders `CASE WHEN gb.mtfcc = 'G5200V26' THEN 0 ELSE 1 END` so a 2026-vintage polygon wins when one exists) and the surfacing query's `JOIN LATERAL` that selects the G5200V26 geometry for a geo_id when present and otherwise falls back to the district-mtfcc branch. Do not clone 165's hard `mtfcc` pin form: a hard pin is correct only for UT, whereas the LATERAL generalises to TN, AL, LA, UT and every non-dual-map state at once, and is what the deployed service does.

All PostGIS calls use the `public.` prefix: `public.ST_X`, `public.ST_Y`, `public.ST_PointOnSurface`, `public.ST_Covers`, `public.ST_SetSRID`, `public.ST_MakePoint`. Import `pool` from `../src/lib/db.js`. SELECT-only.

Populate `STATE_CONFIG` with all 38 Wave-3 states as abbreviation, exact election name, geo prefix:
AZ 'AZ 2026 Statewide General' 04; WA 'WA 2026 Statewide General' 53; TN 'TN 2026 Statewide General' 47; MA '2026 Massachusetts General Election' 25; IN 'IN 2026 Statewide General' 18; MD '2026 Maryland General Election' 24; MN 'MN 2026 Statewide General' 27; MO 'MO 2026 Statewide General' 29; WI 'WI 2026 Statewide General' 55; CO 'CO 2026 Statewide General' 08; AL 'AL 2026 Statewide General' 01; SC 'SC 2026 Statewide General' 45; LA 'LA 2026 Statewide General' 22; KY 'KY 2026 Statewide General' 21; OR 'OR 2026 General' 41; CT 'CT 2026 Statewide General' 09; OK 'OK 2026 Statewide General' 40; AR 'AR 2026 Statewide General' 05; IA 'IA 2026 Statewide General' 19; KS 'KS 2026 Statewide General' 20; MS 'MS 2026 Statewide General' 28; NV 'NV 2026 Statewide General' 32; UT 'UT 2026 Statewide General' 49; NM 'NM 2026 Statewide General' 35; NE 'NE 2026 Statewide General' 31; WV 'WV 2026 Statewide General' 54; ID 'ID 2026 Statewide General' 16; HI 'HI 2026 Statewide General' 15; ME '2026 Maine General Election' 23; NH 'NH 2026 Statewide General' 33; RI 'RI 2026 Statewide General' 44; MT 'MT 2026 Statewide General' 30; AK 'AK 2026 Statewide General' 02; DE 'DE 2026 Statewide General' 10; ND 'ND 2026 Statewide General' 38; SD 'SD 2026 Statewide General' 46; VT 'VT 2026 Statewide General' 50; WY 'WY 2026 Statewide General' 56.

Note in the file header that OR, MD, MA, ME and NV surface via PRE-EXISTING election names that were reused rather than authored (per 164-03, 162-08, 161 and 165-01), which is why five names deviate from the `{ABBR} 2026 Statewide General` convention.

Add a `--select` argv mode that, for each of the 38 states, queries the in-scope races and prints every district with its active-candidate and challenger counts ordered by challenger count descending. This is the LIVE selection input for Task 2; nothing is hardcoded yet. Exclude MO geo_ids 2902, 2903, 2904, 2905 and 2906 from MO's candidate list, since those races are deliberately not on the surfacing election.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-coordinate-smoke.ts --select | tee /tmp/166-select.txt &amp;&amp; grep -c "^SELECT-CANDIDATES " /tmp/166-select.txt</automated>
  </verify>

  <acceptance_criteria>
    - `node --import tsx scripts/166-coordinate-smoke.ts --select` exits 0.
    - stdout contains exactly 38 lines beginning `SELECT-CANDIDATES ` — one per state — each listing that state's districts with active and challenger counts.
    - All 38 election names resolve; the script throws naming the missing election otherwise.
    - The MO line omits geo_ids 2902, 2903, 2904, 2905 and 2906.
    - The surfacing query in the file uses `JOIN LATERAL` with the `G5200V26` preference branch, and every PostGIS function call is `public.`-prefixed (`grep -c "extensions\.ST_" backend/scripts/166-coordinate-smoke.ts` returns 0).
  </acceptance_criteria>

  <done>The engine mirrors the deployed dual-map resolution path and can enumerate, live, which district in each of the 38 states is the best contested sample.</done>
</task>

<task type="auto">
  <name>Task 2: Hardcode the 38 live-selected positive samples and run the Pitfall-5 challenger guard green</name>

  <read_first>
    - `backend/scripts/166-coordinate-smoke.ts` — the file being extended
    - `backend/scripts/165-coordinate-smoke.ts` — the `SAMPLES` array with its per-line justification comments, and the four failure conditions (multi-race match, below `minActive`, zero challengers, non-zero null pid)
    - `/tmp/166-select.txt` — Task 1's live selection output, the ONLY authority for which geo_id each state contributes
  </read_first>

  <files>backend/scripts/166-coordinate-smoke.ts</files>

  <action>
Using Task 1's `--select` output as the sole authority, hardcode a 38-entry `SAMPLES` array — one per state, choosing that state's district with the highest challenger count, preferring an open seat where counts tie. Annotate every entry with an end-of-line comment naming the district and why it was chosen (for example `// NV-2 open (Amodei retired)`), matching the 165 layout. Do NOT copy sample geo_ids from 161-, 162-, 163-, 164- or 165-coordinate-smoke.ts; those picks were frozen in early July and candidate fields have changed since.

Set `minActive: 2` for every sample. Set `MIN_DISTRICTS = 38`.

Retain all four failure conditions from the 165 engine: more than one matched House race, active count below `minActive`, zero challengers (the Pitfall-5 two-path guard — the assertion must be on `is_incumbent = false` count being at least 1, not on the race merely resolving), and any active candidate with a NULL politician_id.

If any state has NO district with at least one challenger, do not relax the guard and do not lower `minActive`. Stop, and record that state and district in the plan SUMMARY as a genuine seeding gap for operator decision — a state with a fully uncontested delegation would contradict every per-phase smoke that already passed, so this is a signal, not a nuisance.

Run it green. The terminal summary line must read `166 COORDINATE SMOKE GREEN: 38/38 states surface their US House race with full challenger-inclusive field`.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-coordinate-smoke.ts | tee /tmp/166-smoke.txt &amp;&amp; grep -c "^PASS " /tmp/166-smoke.txt &amp;&amp; grep -q "166 COORDINATE SMOKE GREEN: 38/38" /tmp/166-smoke.txt</automated>
  </verify>

  <acceptance_criteria>
    - `node --import tsx scripts/166-coordinate-smoke.ts` exits 0.
    - stdout contains exactly 38 lines beginning `PASS ` (one per state) before the summary line.
    - stdout contains `166 COORDINATE SMOKE GREEN: 38/38 states surface their US House race with full challenger-inclusive field`.
    - Every `PASS` line reports at least 1 challenger and 0 null pid.
    - `SAMPLES` has 38 entries and `MIN_DISTRICTS` equals 38.
    - The MO sample geo_id is one of 2901, 2907, 2908.
    - Each sample entry carries an end-of-line comment naming its district.
  </acceptance_criteria>

  <done>All 38 Wave-3 states prove, through the live coordinate path, that a resident sees their House race with a challenger-inclusive field.</done>
</task>

<task type="auto">
  <name>Task 3: Add the severe-MO negative sample and the national-coverage footer; full green run</name>

  <read_first>
    - `backend/scripts/162-coordinate-smoke.ts` — the severe-MO negative sample block being cloned, including `SEVERE_MO_GEO_ID` and the assertion that the surfacing election returns zero races
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-11-SUMMARY.md` — the MO-SEVERE invariant and the five withheld geo_ids
    - `.planning/workstreams/2026-us-house-candidate-coverage/STATE.md` — Operator Next Steps, the `≥ 2026-08-04` 164.1-07 entry that will eventually flip this sample
  </read_first>

  <files>backend/scripts/166-coordinate-smoke.ts</files>

  <action>
Clone 162-coordinate-smoke.ts's negative-sample block. Set `SEVERE_MO_GEO_ID = '2905'` (MO-5, the same district 162 used). Derive a point on that district's surface, then query for House races scoped to `MO 2026 Statewide General` with geo prefix 29 covering that point, and assert the result set is empty. On success print `PASS MO 2905 (severe negative sample): coordinate (lat,lng) surfaced ZERO House races on MO 2026 Statewide General — withholding confirmed end-to-end`.

Mark the whole block with a clearly delimited comment region headed `-- === MO POST-2026-08-04 FLIP REGION (see 166-mo-flip-runbook.md) ===` explaining both branches of plan 164.1-07: in the map-holds branch the five severe MO races move to `MO 2026 Statewide General`, at which point this negative sample must become five positive samples and `MIN_DISTRICTS` rises accordingly; in the referendum-qualifies branch MO stays withheld for this cycle and this block needs NO edit.

Extend the terminal summary to state the national arithmetic explicitly and to name the owning gates, so the milestone claim is checkable rather than asserted: 178 Wave-3 districts proven here and by `backend/scripts/166-verify.sql`; 144 v2.20 districts owned by `backend/scripts/152-verify.sql`; 89 v2.21 decided-state districts owned by `backend/scripts/158-verify.sql`; 24 v2.21 MI and VA districts seeded and gate-pending plan 159-06 (date-gated on or after 2026-08-05); total 435. Do not claim the MI/VA 24 are gate-proven — 158-verify.sql's own header states it must not reference MI or VA.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; node --import tsx scripts/166-coordinate-smoke.ts | tee /tmp/166-smoke-final.txt &amp;&amp; grep -q "severe negative sample" /tmp/166-smoke-final.txt &amp;&amp; grep -q "166 COORDINATE SMOKE GREEN: 38/38" /tmp/166-smoke-final.txt &amp;&amp; grep -q "435" /tmp/166-smoke-final.txt</automated>
  </verify>

  <acceptance_criteria>
    - `node --import tsx scripts/166-coordinate-smoke.ts` exits 0.
    - stdout contains a line matching `PASS MO 2905 (severe negative sample):` reporting zero races surfaced.
    - The summary block prints the arithmetic `178 + 144 + 89 + 24 = 435` and names `152-verify.sql`, `158-verify.sql` and plan `159-06` as the owners of the non-Wave-3 segments.
    - The summary distinguishes gate-proven districts (411) from seeded-but-gate-pending districts (24 MI+VA) — it does not describe all 435 as gate-proven.
    - The file contains the delimiter `-- === MO POST-2026-08-04 FLIP REGION` and documents both 164.1-07 branches, including that the referendum-qualifies branch requires no edit.
  </acceptance_criteria>

  <done>The coordinate smoke proves 38 positive states plus the MO withholding negative, and its footer states the national coverage arithmetic honestly, naming which gate owns each segment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script to production Postgres | Read-only smoke against `kxsdzaojfaibhuzmclfq` using `backend/.env` credentials |
| smoke query versus deployed service | The smoke asserts on a hand-written mirror of `getElectionsByCoordinate`, not the service itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-02-01 | Spoofing | smoke-versus-service drift | high | mitigate | Clone 163-coordinate-smoke.ts's post-164.1 vintage-preference LATERAL verbatim; 164.1-04 recorded that the pre-164.1 plain join double-matches for dual-map states and would spuriously surface two races. |
| T-166-02-02 | Repudiation | stale sample geo_ids | high | mitigate | Samples are chosen from Task 1's live `--select` output, never copied from the frozen 161-165 smoke files. |
| T-166-02-03 | Tampering | production data | high | mitigate | SELECT-only; no INSERT/UPDATE/DELETE and no temp tables. |
| T-166-02-04 | Information disclosure | national coverage claim | medium | mitigate | Footer separates 411 gate-proven from 24 seeded-but-pending districts rather than asserting a blanket 435. |
| T-166-02-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; `tsx` and `pg` already present. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --import tsx scripts/166-coordinate-smoke.ts` exits 0 with 38 PASS lines plus the MO negative.
- Terminal summary reads `166 COORDINATE SMOKE GREEN: 38/38 ...` and carries the 178 + 144 + 89 + 24 = 435 arithmetic.
- `npm run check:occupancy --prefix backend` exits 0.
- File committed.
</verification>

<success_criteria>
A resident of any of the 38 Wave-3 states resolves to their US House race with a challenger-inclusive candidate field through the same code path production uses, and a resident of a severe MO district correctly sees no House race at all.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-02-SUMMARY.md` when done. Record the 38 chosen sample geo_ids with their active and challenger counts, and note any state where the best available sample was thinner than expected.
</output>
