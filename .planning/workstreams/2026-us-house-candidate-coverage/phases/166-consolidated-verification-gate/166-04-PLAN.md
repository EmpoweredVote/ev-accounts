---
phase: 166-consolidated-verification-gate
plan: 04
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - backend/scripts/166-verify-invariants.sql
autonomous: true
requirements: [USHC3-06]

must_haves:
  truths:
    - "The fifteen standing invariants that phases 161 through 165 each declared as carry-forward are asserted in one place, so none can silently regress after those per-phase gates stop being run."
    - "TN, AL and LA assert SURFACING (positive) — the 164.1 un-withholding flipped them on 2026-07-07 and inheriting the old withheld assertions would be wrong."
    - "MO's five severe districts (2902-2906) are the ONLY districts asserted withheld, and that assertion is correct in both branches of the date-gated plan 164.1-07."
    - "The exact edit required after 2026-08-04 lives in a clearly delimited, named region of the file, not buried in prose."
  artifacts:
    - "backend/scripts/166-verify-invariants.sql — the fifteen inherited per-phase invariants, independently runnable"
  key_links:
    - "The MO-SEVERE block and the MO POST-2026-08-04 FLIP REGION must reference each other and the runbook authored in plan 166-05"
    - "The UT-REKEY NOTOUCH md5 is the binding contract in 164.1-ut-wiring-contract.md; it is expected to change only at the Jan-2027 boundary promotion"
    - "The withheld census in backend/scripts/166-pins.generated.sql (166-01 Task 3) is the live authority confirming TN, AL and LA marker elections are empty"
---

<objective>
Author `backend/scripts/166-verify-invariants.sql`: the read-only companion gate carrying the fifteen standing invariants that phases 161 through 165 each declared as carry-forward to Phase 166, including the MO withholding assertion and its clearly delimited post-2026-08-04 flip region.

Purpose: once Phase 166 supersedes the per-phase gates, nothing else runs 162's IN9-FLAG, 163's CO1-DEGETTE, 164's OPEN-SEAT and OR-REUSE, or 165's UT-REKEY NOTOUCH. Each was explicitly named "standing invariant for Phase 166" by its own SUMMARY. If they are not carried here, they stop being checked.

Output: one committed SQL gate that exits 0 under `psql -v ON_ERROR_STOP=1`, alongside `166-verify.sql`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (166-04) produces `backend/scripts/166-verify-invariants.sql` with these named blocks:

- `TN-SURFACING` — all 9 TN races on the general; 0 on the TN marker election
- `AL-SURFACING` — all 7 AL races on the general; 0 on the AL marker election
- `LA-SURFACING` — all 6 LA races on the general; 0 on the LA marker election; all 6 `primary_party` NULL; no December-2026 runoff race
- `MO-SEVERE` — 5 severe MO geo_ids withheld, 3 non-severe surfacing (contains the delimited `MO POST-2026-08-04 FLIP REGION`)
- `IN9-FLAG` — exactly 1 incumbent across the two IN-9 primary races
- `AZ-RECONCILE` — the 5 ballot-ineligible AZ candidates are not active
- `MA-INCUMBENT-DEDUP` — Clark and Pressley each have exactly 1 race_candidates row
- `CO1-DEGETTE` — CO-1 has exactly 2 active candidates and DeGette is absent
- `OPEN-SEAT` — 5 departing incumbents have 0 active race_candidates rows
- `OR-REUSE` — OR's 6 races are exactly the pre-existing UUIDs
- `NV-RECONCILE` — NV's 4 races are the pre-existing UUIDs; Chapman's row has a non-null politician_id
- `ME-RECONCILE` — ME's 2 pre-existing races each carry exactly 2 active candidates
- `UT-REKEY` — FIPS-49 offices md5 NOTOUCH plus incumbent placement plus retired Owens
- `AK-FIELD` — 1 jungle race, `primary_party` NULL, live-derived active count
- `COLLISION-BAND` — 19 external_id sub-band seq floors
- Temp table: `_house`
- Terminal `ALL INHERITED INVARIANTS PASSED` notice
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Working set plus the four redistricting-state blocks — TN-SURFACING, AL-SURFACING, LA-SURFACING, and MO-SEVERE with its delimited flip region</name>

  <read_first>
    - `backend/scripts/161-verify.sql` — CRITERION 6, the post-flip `TN-SURFACING` block and the `TN-STILL-WITHHELD` zero-check that precedes it
    - `backend/scripts/163-verify.sql` — CRITERION 6 and 7, the post-flip `AL-SURFACING` and `LA-SURFACING` blocks including the jungle-model and no-December-runoff clauses
    - `backend/scripts/162-verify.sql` — CRITERION 6, the `MO-SEVERE` withheld-versus-surfacing block being cloned
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-04-SUMMARY.md` — migration 1247, the 5 TN severe races re-pointed, the 161 gate flip
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-05-SUMMARY.md` — migrations 1248 and 1249, AL-2 and LA-2/LA-6 re-pointed, the 163 gate flip
    - `.planning/workstreams/2026-us-house-candidate-coverage/STATE.md` — the "Phase 166 inheritance" bullet and the `≥ 2026-08-04` 164.1-07 entry with both branches
    - `backend/scripts/166-pins.generated.sql` — the live withheld census confirming TN 0 / AL 0 / LA 0 / MO 5
  </read_first>

  <files>backend/scripts/166-verify-invariants.sql</files>

  <action>
Create the companion gate with the same house preamble as `166-verify.sql`: header comment, `\set ON_ERROR_STOP on`, a single `DO $$ ... END $$;`, write-free apart from `CREATE TEMP TABLE ... ON COMMIT DROP`. Document the run line and state in the header that this file is run alongside `scripts/166-verify.sql` and `scripts/166-coordinate-smoke.ts`, and that it exists as a separate file purely so each half stays authorable and reviewable in one pass. Do not reference the occupancy column ADR 0002 / migration 1463 removed from `essentials.offices`, in code or comments.

Rebuild the same `_house` working set as `166-verify.sql` (42 elections, 38 geo prefixes, state abbreviation from `substr(d.geo_id,1,2)`), including `race_election_id`, `description` and `primary_party`. The duplication of roughly forty lines across the two files is deliberate: each must be independently runnable.

TN-SURFACING: assert 0 TN races still wired to `TN 2026 Congressional Redistricting - Polygon Pending`, and all 9 TN geo_ids 4701 through 4709 wired to `TN 2026 Statewide General`. The pass notice must state that the 5 severe TN districts 4704, 4705, 4706, 4708 and 4709 were un-withheld on 2026-07-07 by migration 1247 once G5200V26 polygons landed — inheriting 161's original withheld assertion would be wrong.

AL-SURFACING: assert 0 AL races on `AL 2026 Congressional Redistricting - Polygon Pending`, and all 7 AL geo_ids 0101 through 0107 on `AL 2026 Statewide General`. Note migration 1248 un-withheld AL-2 (0102) on 2026-07-07.

LA-SURFACING: assert 0 LA races on `LA 2026 Congressional Redistricting - Polygon Pending`, all 6 LA geo_ids 2201 through 2206 on `LA 2026 Statewide General`, all 6 LA races carrying `primary_party` NULL (the jungle model), and no December-2026 runoff race in scope. Note migration 1249 un-withheld LA-2 (2202) and LA-6 (2206) on 2026-07-07.

MO-SEVERE: clone 162-verify.sql CRITERION 6 exactly. Assert that every MO geo_id in 2902, 2903, 2904, 2905, 2906 has its race wired to `MO 2026 Congressional Redistricting - Polygon Pending` (leaked count 0), and that all 3 of 2901, 2907 and 2908 are wired to `MO 2026 Statewide General`. These five are the ONLY districts in the whole 178-district set asserted withheld.

Wrap the MO-SEVERE block in a delimited region opened by the comment line `-- === MO POST-2026-08-04 FLIP REGION — see 166-mo-flip-runbook.md ===` and closed by `-- === END MO POST-2026-08-04 FLIP REGION ===`. Inside, document both branches of the date-gated plan 164.1-07, which executes on or after 2026-08-04 when the Missouri Secretary of State's Hoskins certification decision lands:
  - Map-holds branch: MO G5200V26 polygons are imported and the five severe races are re-pointed to `MO 2026 Statewide General`. This block must then invert — the withheld array becomes empty, the surfacing set becomes all 8 MO geo_ids 2901 through 2908, and the pass notice is rewritten to the AL-SURFACING wording. The `166-coordinate-smoke.ts` negative sample flips to positive at the same time.
  - Referendum-qualifies branch: MO does zero polygon work, the five districts stay withheld for this cycle, and the revert diverts to Phase 167's MO cluster. This block requires NO edit — as written it already asserts exactly that steady state.
State plainly that the referendum branch is a no-op for this file, so nobody edits it defensively.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql 2>&amp;1 | tee /tmp/166-inv-t1.txt &amp;&amp; grep -q "PASS TN-SURFACING" /tmp/166-inv-t1.txt &amp;&amp; grep -q "PASS AL-SURFACING" /tmp/166-inv-t1.txt &amp;&amp; grep -q "PASS LA-SURFACING" /tmp/166-inv-t1.txt &amp;&amp; grep -q "PASS MO-SEVERE" /tmp/166-inv-t1.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql` exits 0.
    - Output contains `PASS TN-SURFACING`, `PASS AL-SURFACING`, `PASS LA-SURFACING` and `PASS MO-SEVERE`.
    - `PASS MO-SEVERE` names all five withheld geo_ids 2902, 2903, 2904, 2905, 2906 and all three surfacing geo_ids 2901, 2907, 2908.
    - `grep -c "MO POST-2026-08-04 FLIP REGION" backend/scripts/166-verify-invariants.sql` returns 2 (region open and close markers).
    - The flip region text describes both 164.1-07 branches and explicitly states the referendum-qualifies branch needs no edit.
    - The TN, AL and LA blocks assert surfacing, not withholding, and each names the migration (1247, 1248, 1249) that flipped it.
    - The file is write-free: only `CREATE TEMP TABLE ... ON COMMIT DROP`, no DML against `essentials` or `inform`.
  </acceptance_criteria>

  <done>The four redistricting-state assertions reflect the post-164.1 world: thirteen previously withheld districts assert surfacing, and only MO's five remain withheld behind a clearly marked, dual-branch flip region.</done>
</task>

<task type="auto">
  <name>Task 2: The six seeding-integrity blocks — IN9-FLAG, AZ-RECONCILE, MA-INCUMBENT-DEDUP, CO1-DEGETTE, OPEN-SEAT, OR-REUSE</name>

  <read_first>
    - `backend/scripts/162-verify.sql` — CRITERION 8, the corrected `IN9-FLAG` block and the header note explaining why the plan's original ids were stale
    - `backend/scripts/161-verify.sql` — CRITERION 7 `AZ-RECONCILE` and CRITERION 8 `MA-INCUMBENT-DEDUP`
    - `backend/scripts/163-verify.sql` — CRITERION 8 `CO1-DEGETTE`
    - `backend/scripts/164-verify.sql` — CRITERION 6 `OPEN-SEAT` and CRITERION 7 `OR-REUSE`
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-13-SUMMARY.md` — the "STANDING INVARIANTS for Phase 166" section
  </read_first>

  <files>backend/scripts/166-verify-invariants.sql</files>

  <action>
Add six blocks, each cloned from its source gate with the identifiers below. Before asserting, run each as a plain SELECT against prod and confirm the live figure matches the expected one; where it does not, record the discrepancy in the plan SUMMARY and resolve it rather than adjusting the expectation to whatever prod happens to say.

IN9-FLAG (from 162, migration 1212): across the two IN-9 primary races `7d3f0042-eb15-462b-bf14-df15244c5d16` (Democratic primary) and `9d2de2ae-2fef-48b6-b3d4-2787166b78df` (Republican primary), exactly 1 row has `is_incumbent = true`; race `7d3f0042` has 0 incumbent-flagged rows; and race_candidates row `a61ab808-846a-4bb7-9544-f9f33357b078` (Houchin) is that one row. Carry 162's header note that `9d2de2ae` is a RACE id, not Houchin's row id — the original plan had these transposed.

AZ-RECONCILE (from 161, migration 1204): the 5 ballot-ineligible AZ candidates with external_ids -40108 (Ajluni), -40201 (Descheenie), -40402 (Davison), -40503 (Bracht) and -40602 (Bah) have 0 active rows in scope.

MA-INCUMBENT-DEDUP (from 161): on geo_id 2505, exactly 1 race_candidates row with `lower(full_name) = 'katherine clark'`; on geo_id 2507, exactly 1 with `lower(full_name) = 'ayanna pressley'`.

CO1-DEGETTE (from 163): geo_id 0801 has exactly 2 active candidates and politician `610bb358` (DeGette) is absent from them. Carry 163's note that this is the REUSE-NO-ROW lost-primary pattern: her record, office and stances are intentionally untouched, merely not wired into the CO-1 race.

OPEN-SEAT (from 164): each of politician ids `4d92b909-6add-4486-b0cf-f317b4e10e9b` (Massie, KY-4 lost primary), `164fb70e-b8c1-48cd-a6ef-12d80165c67d` (Barr, KY-6 retired), `96e589b9-d04a-4749-bf9f-9b66eaea5083` (Hern, OK-1 retired), `91aa37bf-dc8a-45f3-9bf7-e887b8dd99bd` (Hinson, IA-2 retired) and `c25acca0-f280-469c-b708-c314d8036036` (Feenstra, IA-4 retired) has 0 active `essentials.race_candidates` rows anywhere, not merely 0 in scope.

OR-REUSE (from 164): OR's races are exactly the 6 pre-existing UUIDs `8dfd6e35-f91d-4d14-bcec-ae983035da51`, `504a156a-d4e6-4abe-9a85-a418c0135805`, `61297fac-c98d-4ad4-b93f-b2e36722bd6a`, `c5023da0-985e-4e1c-817c-a843849ef6e9`, `17a4b696-5c15-4f2b-9390-ce1864d57f37` and `dd25e913-2496-40fa-a735-c9b1d7f68df8`, with 0 OR races outside that set and no new OR general authored. The per-(race_id, politician_id) uniqueness clause 164 folded into this block is already generalised to all 178 races by `166-verify.sql`'s RC-UNIQUE block, so reference that in a comment rather than duplicating it.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql 2>&amp;1 | tee /tmp/166-inv-t2.txt &amp;&amp; grep -c "^PASS \|PASS " /tmp/166-inv-t2.txt &amp;&amp; grep -q "PASS IN9-FLAG" /tmp/166-inv-t2.txt &amp;&amp; grep -q "PASS OPEN-SEAT" /tmp/166-inv-t2.txt &amp;&amp; grep -q "PASS OR-REUSE" /tmp/166-inv-t2.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql` exits 0.
    - Output now contains ten `PASS ` lines: TN-SURFACING, AL-SURFACING, LA-SURFACING, MO-SEVERE, IN9-FLAG, AZ-RECONCILE, MA-INCUMBENT-DEDUP, CO1-DEGETTE, OPEN-SEAT, OR-REUSE.
    - Every UUID and external_id listed in the action appears verbatim in the file.
    - `PASS IN9-FLAG` names Houchin's row id `a61ab808` and states that race `7d3f0042` has 0 incumbents.
    - `PASS OPEN-SEAT` names all five departing incumbents.
    - Each block's live SELECT probe result is recorded in the plan SUMMARY, including where it matched.
  </acceptance_criteria>

  <done>The six seeding-integrity invariants that 161 through 164 declared as carry-forward are asserted in the consolidated gate and green against prod.</done>
</task>

<task type="auto">
  <name>Task 3: The five Phase-165 and cross-phase blocks — NV-RECONCILE, ME-RECONCILE, UT-REKEY, AK-FIELD, COLLISION-BAND; full green run</name>

  <read_first>
    - `backend/scripts/165-verify.sql` — CRITERION 6 through 11: `NV-RECONCILE`, `ME-RECONCILE`, `UT-REKEY` with its md5 NOTOUCH, `AK-FIELD`, and the 14-floor `COLLISION-BAND`
    - `backend/scripts/164-verify.sql` — CRITERION 9, the other 5 `COLLISION-BAND` floors
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-17-SUMMARY.md` — the "STANDING for Phase 166" section naming NV-RECONCILE, ME-RECONCILE and UT-REKEY
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md` — the binding offices NOTOUCH contract behind the UT md5
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-jan2027-boundary-promotion-spec.md` — the only sanctioned future change to that md5
  </read_first>

  <files>backend/scripts/166-verify-invariants.sql</files>

  <action>
Add the final five blocks, then close the file.

NV-RECONCILE (from 165): NV's races are exactly the 4 pre-existing UUIDs `a5295941-38b1-4c7f-8edf-39097ad3fb0a`, `0c470cc0-5250-43f1-b7f7-57778cadacc6`, `79e7fb35-a73a-478c-847d-553e9ad11e7c` and `81eb1a27-b710-42c3-a27c-a2c0153c2820`, with 0 NV races outside them; exactly 1 election named `NV 2026 Statewide General`; race_candidates row `08dfb911-9ddc-4c0d-85a2-fb14884a9160` (Chapman) has a non-null politician_id; and politicians with external_ids -320174, -320175, -320251, -320301, -320479 and -320480 all exist.

ME-RECONCILE (from 165): races `d92aa73a-17db-4ba1-bfaf-46640f66f8f5` and `aa63d55d-80b8-42b9-9aa5-0713387f65fb` each carry exactly 2 active candidates; 0 ME races outside those two; exactly 1 election named `2026 Maine General Election`.

UT-REKEY (from 165, cloned originally from 1641-verify.sql D04-NOTOUCH): the md5 over FIPS-49 NATIONAL_LOWER offices — computed as `md5(string_agg(o.id || ':' || coalesce(o.district_id,'') || ':' || coalesce(o.representing_state,''), ',' ORDER BY o.id))` joining `essentials.districts` to `essentials.offices` on `district_id` where `district_type = 'NATIONAL_LOWER'` and `length(geo_id) = 4` and `substr(geo_id,1,2) = '49'` — equals `4d8bbfb221d4babca6e9f7201bce391e`; incumbents Moore `e365a1d4-2de3-4fb6-b416-d78227836553` on geo_id 4902, Maloy `a7983eb6-bae0-4269-856b-f4554fb5ce29` on 4903 and Kennedy `9e3164d5-ce71-4c50-9220-b969265ce551` on 4904 are each active with `is_incumbent = true`; retired Owens `cb87ddbb-5a83-45b7-b67a-789e63f0e58b` has 0 active race_candidates rows. Add a comment that this md5 is the binding contract in `164.1-ut-wiring-contract.md`, that a mismatch means the offices table was perturbed and the dual-map design is breached, and that the only sanctioned change is the Jan-2027 boundary promotion, date-gated on or after 2027-01-03.

AK-FIELD (from 165): AK has exactly 1 jungle race with `primary_party` NULL. RE-DERIVE the active candidate count live before asserting — 165 froze it at 15 on 2026-07-07, and AK's field is `PROVISIONAL:` with an August primary, so filings and withdrawals since then are expected. Assert the live figure and record both the 165 figure and today's in the block comment and in the plan SUMMARY.

COLLISION-BAND: union all 19 sub-band seq floors, computing seq as `-external_id - (fips*10000 + cd*100)` for districted seats and `-external_id - fips*10000` for at-large seats, over active non-incumbent candidates only. From 164: KY-1 (2101) at least 200, OK-1 (4001) at least 44 — the in-century fix, since the audit's 200 would have collided with OK-3 seq 1 — OR-1 (4101) at least 14, KS-1 (2001) at least 3, KS-2 (2002) at least 10. From 165: NV-1 (3201) at least 74, NV-2 (3202) at least 51, NV-4 (3204) at least 79, ME-1 (2301) at least 3, ME-2 (2302) at least 3, NM-1 (3501) at least 26, NM-2 (3502) at least 51, NM-3 (3503) at least 82, NE-3 (3103) at least 60, NH-1 (3301) at least 33, MT-2 (3002) at least 85, AK (0200) at least 5, DE (1000) at least 48, VT (5000) at least 6.

Close with `RAISE NOTICE 'ALL INHERITED INVARIANTS PASSED (...)'` naming all fifteen block names, the source phase of each, and a pointer that `scripts/166-verify.sql` carries the structural half and `scripts/166-coordinate-smoke.ts` the coordinate half.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql 2>&amp;1 | tee /tmp/166-inv-final.txt &amp;&amp; grep -q "PASS UT-REKEY" /tmp/166-inv-final.txt &amp;&amp; grep -q "PASS COLLISION-BAND" /tmp/166-inv-final.txt &amp;&amp; grep -q "ALL INHERITED INVARIANTS PASSED" /tmp/166-inv-final.txt</automated>
  </verify>

  <acceptance_criteria>
    - `psql -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql` exits 0.
    - Output contains fifteen `PASS ` lines: TN-SURFACING, AL-SURFACING, LA-SURFACING, MO-SEVERE, IN9-FLAG, AZ-RECONCILE, MA-INCUMBENT-DEDUP, CO1-DEGETTE, OPEN-SEAT, OR-REUSE, NV-RECONCILE, ME-RECONCILE, UT-REKEY, AK-FIELD, COLLISION-BAND.
    - `PASS UT-REKEY` reports the md5 matching `4d8bbfb221d4babca6e9f7201bce391e`.
    - `PASS AK-FIELD` reports a live-derived active count, and the block comment records both that figure and 165's frozen 15.
    - `PASS COLLISION-BAND` names all 19 floors.
    - Output ends with `ALL INHERITED INVARIANTS PASSED` naming all fifteen blocks and pointing at the two sibling artifacts.
    - The file is write-free: only `CREATE TEMP TABLE ... ON COMMIT DROP`, no DML against `essentials` or `inform`.
    - `npm run check:occupancy --prefix backend` exits 0 with the new file staged.
  </acceptance_criteria>

  <done>All fifteen inherited invariants are asserted in one runnable gate, green against prod, so none can silently regress once the per-phase gates stop being run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script to production Postgres | A DO-block gate executes against `kxsdzaojfaibhuzmclfq` with the full `DATABASE_URL` role |
| frozen SUMMARY invariant to live assertion | Invariants recorded in early-July SUMMARYs are re-asserted against a three-weeks-later database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-04-01 | Spoofing | inherited severe assertions | critical | mitigate | TN, AL and LA assert SURFACING, matching migrations 1247, 1248 and 1249; inheriting 161's and 163's original withheld assertions would produce a gate that is green about a world that no longer exists. |
| T-166-04-02 | Repudiation | MO flip step lost | high | mitigate | The MO block sits inside an explicitly delimited, greppable region naming the runbook, documenting both 164.1-07 branches, and stating that the referendum branch needs no edit. |
| T-166-04-03 | Tampering | UT offices perturbation | high | mitigate | The UT-REKEY md5 NOTOUCH check reproduces the binding `164.1-ut-wiring-contract.md` baseline byte-for-byte and fails loudly on any drift. |
| T-166-04-04 | Spoofing | stale AK field count | medium | mitigate | AK's active count is re-derived live rather than inheriting 165's 2026-07-07 figure of 15; AK is a `PROVISIONAL:` late-primary state. |
| T-166-04-05 | Tampering | production data | critical | mitigate | Write-free by construction; only `CREATE TEMP TABLE ... ON COMMIT DROP`. |
| T-166-04-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; plain SQL run by the existing psql client. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `psql -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql` exits 0 with fifteen PASS lines and `ALL INHERITED INVARIANTS PASSED`.
- `grep -c "MO POST-2026-08-04 FLIP REGION" backend/scripts/166-verify-invariants.sql` returns 2.
- `npm run check:occupancy --prefix backend` exits 0.
- No migration is required by this plan. If the executor concludes otherwise, that conclusion must be stated explicitly in the SUMMARY with its reason before any migration is authored.
- File committed.
</verification>

<success_criteria>
Every invariant that phases 161 through 165 named as "standing for Phase 166" is asserted in a single runnable gate reflecting the post-164.1 world; MO's five severe districts are the only withheld set, and the exact edit required after the 2026-08-04 certification decision is delimited, named and greppable.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-04-SUMMARY.md` when done. Record every block's live probe figure against its frozen expectation, calling out any that differed, and quote the flip region's opening and closing marker lines verbatim.
</output>
