---
phase: 166-consolidated-verification-gate
plan: 05
type: execute
wave: 3
depends_on: ["166-02", "166-03", "166-04"]
files_modified:
  - .planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md
  - .planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
  - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md
autonomous: true
requirements: [USHC3-06]

must_haves:
  truths:
    - "Whoever executes plan 164.1-07 on or after 2026-08-04 can flip the Phase-166 gate correctly without re-deriving anything, in either branch of the certification decision."
    - "All three Phase-166 artifacts run green together in one session against prod, proving the milestone end-to-end."
    - "The two repo-level CI guards pass with the new scripts staged, so nothing in Phase 166 references the dropped occupancy column or collides on a migration number."
    - "ROADMAP and STATE record USHC3-06 as closed and carry the pointer that 164.1-07 must apply the runbook."
  artifacts:
    - "166-mo-flip-runbook.md — the exact, branch-by-branch edit list for the post-2026-08-04 MO decision"
  key_links:
    - "The runbook is referenced by name from the delimited flip regions in 166-verify-invariants.sql and 166-coordinate-smoke.ts"
    - "STATE.md's `≥ 2026-08-04` Operator Next Steps entry must point at the runbook, or 164.1-07 will flip the 162 gate and miss the 166 one"
    - "The combined green run is the evidence USHC3-06 is closed"
---

<objective>
Close Phase 166: author the MO post-2026-08-04 flip runbook, run the repo CI guards, run all three Phase-166 artifacts green together against prod in a single session, and record the outcome in ROADMAP and STATE with the forward pointer 164.1-07 needs.

Purpose: the gate is only as durable as the instructions for maintaining it. Plan 164.1-07 is date-gated to a decision that lands on or after 2026-08-04, by which point this session's context is gone. Without a runbook, the most likely failure is that 164.1-07 flips the Phase-162 gate it already knows about and leaves Phase 166 asserting a world that no longer exists.

Output: one runbook document, a combined green run, and updated ROADMAP and STATE entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/REQUIREMENTS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (166-05) produces:

- `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md` — branch-by-branch edit list for the post-2026-08-04 MO certification decision, with sections: `## Branch A — map holds`, `## Branch B — referendum qualifies`, `## Verification after either branch`
- Updated `ROADMAP.md` Phase 166 section (Goal, Plans count, plan list) and Progress table row
- Updated `STATE.md` Current Position, Operator Next Steps and v2.22 Requirement Coverage table

Full Phase-166 artifact inventory across all five plans:
- `backend/scripts/166-derive-pins.ts` (166-01)
- `backend/scripts/166-pins.generated.sql` (166-01)
- `backend/scripts/166-coordinate-smoke.ts` (166-02)
- `backend/scripts/166-verify.sql` (166-03)
- `backend/scripts/166-verify-invariants.sql` (166-04)
- `166-mo-flip-runbook.md` (166-05)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author 166-mo-flip-runbook.md — the named, branch-by-branch post-2026-08-04 flip step</name>

  <read_first>
    - `backend/scripts/166-verify-invariants.sql` — the delimited `MO POST-2026-08-04 FLIP REGION` this runbook expands
    - `backend/scripts/166-coordinate-smoke.ts` — its matching flip region and the `SEVERE_MO_GEO_ID` negative sample
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-04-SUMMARY.md` — the worked TN precedent: import, D-10 bar, guarded un-withhold migration, gate flip, smoke flip
    - `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-05-SUMMARY.md` — the AL and LA precedent, including the in-migration count assertion
    - `.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md` — the Phase 164.1 section and the 164.1-07 plan line with both branches
    - `.planning/workstreams/2026-us-house-candidate-coverage/STATE.md` — the Deferred Items "MO revert branch (164.1-07)" row
    - `CLAUDE.md` — the migration policy: next free number taken from `origin/master`, idempotent, `DO $$` post-verify gate, dry-run wrapped in BEGIN/ROLLBACK
  </read_first>

  <files>.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md</files>

  <action>
Write the runbook as an executable checklist, not a narrative. Open with a one-paragraph statement of the decision point: on or after 2026-08-04 the Missouri Secretary of State's Hoskins certification decision determines whether the 2026 congressional map holds or the referendum qualifies, and plan 164.1-07 executes one of two branches. Name the five affected geo_ids 2902, 2903, 2904, 2905 and 2906 and the two elections `MO 2026 Congressional Redistricting - Polygon Pending` and `MO 2026 Statewide General`.

`## Branch A — map holds`. List, in order, every artifact that changes and the exact change:
  1. `backend/scripts/166-verify-invariants.sql`, inside the delimited flip region: the MO-SEVERE block inverts. The withheld geo_id array becomes empty and the assertion becomes "0 MO races remain on the marker election"; the surfacing set becomes all 8 MO geo_ids 2901 through 2908 on `MO 2026 Statewide General`; the pass notice is rewritten to the AL-SURFACING wording, naming the un-withholding migration number and its date. Cross-reference the exact 161 and 163 flips (migrations 1247, 1248, 1249) as the worked precedent.
  2. `backend/scripts/166-coordinate-smoke.ts`: the `SEVERE_MO_GEO_ID` negative-sample block is deleted and MO gains positive samples for the previously withheld districts; `MIN_DISTRICTS` is unchanged at 38 because MO already contributes one positive sample, but the summary line must stop claiming a severe negative. State that 164.1-04 found the 1641 smoke auto-proved the post-flip differential when re-run unedited, so the 1641 harness itself needs no edit.
  3. `backend/scripts/162-verify.sql` and `backend/scripts/162-coordinate-smoke.ts`: the per-phase gate flip 162-11 already flagged as a carry-forward. Note that flipping 166 without also flipping 162 leaves a stale contradiction on disk.
  4. `backend/scripts/166-derive-pins.ts`: the withheld census expectation changes from `MO 5` to `MO 0`. Its `CENSUS MISMATCH` guard will fire on the first post-flip run — that is the intended behaviour and the fix is to update the expectation, not to remove the guard.
  5. Re-run all three Phase-166 artifacts plus 162's pair and confirm green.

`## Branch B — referendum qualifies`. State plainly that MO does zero polygon work, the five districts stay withheld for this cycle, the revert diverts to Phase 167's MO cluster, and NO Phase-166 artifact requires any edit — as written, all three already assert exactly that steady state. Add a short "do not defensively edit" note so nobody touches a correct gate.

`## Verification after either branch`. The three commands to run and the exact strings each must emit, plus `npm run check:occupancy --prefix backend` and `npm run check:migrations --prefix backend`.

Do not embed SQL or TypeScript source in the runbook. Reference block names, geo_ids, election names, file paths and the delimiter lines; the executor reads the actual files.
  </action>

  <verify>
    <automated>test -s ".planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md" &amp;&amp; grep -q "## Branch A — map holds" ".planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md" &amp;&amp; grep -q "## Branch B — referendum qualifies" ".planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md" &amp;&amp; grep -q "## Verification after either branch" ".planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-mo-flip-runbook.md"</automated>
  </verify>

  <acceptance_criteria>
    - `166-mo-flip-runbook.md` exists and contains all three required headings verbatim: `## Branch A — map holds`, `## Branch B — referendum qualifies`, `## Verification after either branch`.
    - Branch A enumerates all five numbered change items above, each naming the exact file path.
    - Branch B states explicitly that no Phase-166 artifact requires an edit and includes the do-not-defensively-edit note.
    - The runbook names geo_ids 2902, 2903, 2904, 2905, 2906 and both MO election names verbatim.
    - The verification section lists the three artifact commands plus `npm run check:occupancy --prefix backend` and `npm run check:migrations --prefix backend`, with the expected output string for each.
    - The runbook is referenced by filename from the flip regions in both `backend/scripts/166-verify-invariants.sql` and `backend/scripts/166-coordinate-smoke.ts`.
  </acceptance_criteria>

  <done>A future executor of 164.1-07 can flip Phase 166 correctly in either branch from the runbook alone, without re-deriving the withholding design.</done>
</task>

<task type="auto">
  <name>Task 2: Repo CI guards plus the combined three-artifact green run against prod</name>

  <read_first>
    - `backend/scripts/check-office-occupancy.mjs` — what the occupancy guard scans (files added or modified on the branch, including comment text) and its two failure modes
    - `backend/package.json` — the `check:occupancy` and `check:migrations` script definitions
    - `backend/scripts/166-verify.sql`, `backend/scripts/166-verify-invariants.sql`, `backend/scripts/166-coordinate-smoke.ts` — the three artifacts being run together
    - `CLAUDE.md` — the migration numbering policy and the note that pure-data and read-only changes need no Render deploy
  </read_first>

  <files>backend/scripts/166-verify.sql, backend/scripts/166-verify-invariants.sql, backend/scripts/166-coordinate-smoke.ts</files>

  <action>
Run the two repo guards with all Phase-166 files staged. `npm run check:occupancy --prefix backend` must exit 0 — it pattern-matches newly added and modified files including their comments, so a Phase-166 script that merely mentions the occupancy column removed by ADR 0002 / migration 1463 will fail it. If it does fail, rephrase the offending comment by concept rather than suppressing the guard.

Note that `backend/scripts/154-verify.sql`, `backend/scripts/160-verify.sql`, `backend/scripts/1641-coordinate-smoke.ts` and `backend/scripts/1642-coordinate-smoke.ts` are pre-existing files that reference that removed column and are therefore broken at runtime today. They are out of Phase 166's scope and must NOT be modified by this plan — the guard only scans files touched on this branch, so they will not fail it. Record them in the plan SUMMARY as a discovered defect for a follow-up quick task, with the note that `1641-coordinate-smoke.ts` and `1642-coordinate-smoke.ts` are the D-02 reps-feed halves of the 164.1 and 164.2 dual-map proofs and so cannot currently be re-run as evidence.

Run `npm run check:migrations --prefix backend` and confirm it exits 0. Phase 166 authors no migration; this run confirms no accidental one crept in.

Then run all three Phase-166 artifacts in one session against prod, in order, and capture the full output:
  1. `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql`
  2. `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql`
  3. `node --import tsx scripts/166-coordinate-smoke.ts`

All three must exit 0 in the same session, so the evidence describes one consistent snapshot of prod rather than three moments hours apart. If any fails, fix the gate rather than the expectation — a red gate at this stage means either a real regression or a wrong assertion, and both need diagnosis, not relaxation.

No Render deploy is required: Phase 166 is read-only and touches no runtime code.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts/backend &amp;&amp; npm run check:occupancy &amp;&amp; npm run check:migrations &amp;&amp; set -a &amp;&amp; source .env &amp;&amp; set +a &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify.sql &amp;&amp; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/166-verify-invariants.sql &amp;&amp; node --import tsx scripts/166-coordinate-smoke.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run check:occupancy --prefix backend` exits 0 with all Phase-166 files staged.
    - `npm run check:migrations --prefix backend` exits 0, and `backend/migrations/` gains no new file in this phase.
    - All three artifact commands exit 0 in one session; captured output shows twelve `PASS ` lines plus `ALL ASSERTIONS PASSED` and `NATIONAL TOTAL` from `166-verify.sql`, fifteen `PASS ` lines plus `ALL INHERITED INVARIANTS PASSED` from `166-verify-invariants.sql`, and thirty-eight `PASS ` lines plus the severe-MO negative and `166 COORDINATE SMOKE GREEN: 38/38` from the smoke.
    - `backend/scripts/154-verify.sql`, `backend/scripts/160-verify.sql`, `backend/scripts/1641-coordinate-smoke.ts` and `backend/scripts/1642-coordinate-smoke.ts` are unmodified by this plan (`git diff --name-only` lists none of them).
    - The SUMMARY records those four files as a discovered pre-existing defect, naming the reps-feed consequence for the 1641 and 1642 dual-map proofs.
  </acceptance_criteria>

  <done>All three Phase-166 artifacts are green together against one prod snapshot, both repo guards pass, and the pre-existing broken scripts are recorded rather than silently fixed or silently ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Record closure in ROADMAP and STATE with the 164.1-07 forward pointer</name>

  <read_first>
    - `.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md` — the Phase 166 section and the Progress table
    - `.planning/workstreams/2026-us-house-candidate-coverage/STATE.md` — Current Position, the v2.22 Requirement Coverage table, Deferred Items, and Operator Next Steps
    - `.planning/workstreams/2026-us-house-candidate-coverage/REQUIREMENTS.md` — the USHC3-06 checkbox and the coverage table row
    - `166-mo-flip-runbook.md` — the artifact the forward pointer must name
  </read_first>

  <files>.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md, .planning/workstreams/2026-us-house-candidate-coverage/STATE.md</files>

  <action>
Use scoped `Edit` replacements only. Never rewrite either file wholesale — ROADMAP.md is 1281 lines of long-lived phase history and STATE.md carries a decision log; a whole-file write destroys everything outside the diff window.

In `ROADMAP.md`: set the Phase 166 `**Plans:**` line to `5/5 plans complete`, add the five-item plan list under it in the house `- [x] 166-NN-PLAN.md — {objective} [Wave N]` format, and update the Progress table row `166. Consolidated Verification Gate` to `5/5 | Complete | 2026-07-26`. Leave the Goal and Success Criteria text as written — it was accurate.

In `STATE.md`:
  - Current Position: Phase 166 complete; the remaining v2.22 work is Phase 167 (date-gated Aug–Sep 2026) and plan 164.1-07 (date-gated on or after 2026-08-04).
  - v2.22 Requirement Coverage table: mark USHC3-06 closed.
  - Operator Next Steps: amend the existing `≥ 2026-08-04` 164.1-07 entry so it names `166-mo-flip-runbook.md` and states that BOTH the Phase-162 gate and the Phase-166 gate pair need the map-holds flip, while the referendum branch needs neither. This is the single highest-value edit in the plan — without it the runbook exists but nobody is routed to it.
  - Remove the `NEXT / anytime` line that instructs the operator to run `/gsd-plan-phase 166`, and replace it with the next unblocked step.
  - Add the Phase-167 queue handoff: if `166-pins.generated.sql` produced a non-empty `_stance_queue_167` set, record its size and point Phase 167 at the 166-01 SUMMARY where the ids are enumerated.

In `REQUIREMENTS.md`: tick the USHC3-06 checkbox and set its coverage-table row to Complete.
  </action>

  <verify>
    <automated>cd /c/EV-Accounts &amp;&amp; grep -q "166-mo-flip-runbook.md" .planning/workstreams/2026-us-house-candidate-coverage/STATE.md &amp;&amp; grep -q "166-05-PLAN.md" .planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md &amp;&amp; grep -q "USHC3-06" .planning/workstreams/2026-us-house-candidate-coverage/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `ROADMAP.md` Phase 166 shows `5/5 plans complete` with all five plan lines listed, and the Progress table row reads `5/5 | Complete | 2026-07-26`.
    - `ROADMAP.md` line count is within 30 lines of its pre-edit value — proof no wholesale rewrite occurred.
    - `STATE.md` Operator Next Steps `≥ 2026-08-04` entry names `166-mo-flip-runbook.md` and states that the map-holds branch flips both the 162 gate and the 166 gate pair while the referendum branch flips neither.
    - `STATE.md` no longer instructs the operator to run `/gsd-plan-phase 166`.
    - `REQUIREMENTS.md` USHC3-06 is ticked and its coverage-table row reads Complete.
    - If the Phase-167 stance queue was non-empty, `STATE.md` records its size and points at `166-01-SUMMARY.md` for the ids.
  </acceptance_criteria>

  <done>The milestone record reflects a closed USHC3-06, and the operator path to the 2026-08-04 MO decision routes through the runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents to future executor | ROADMAP and STATE are the only channel to a session that happens after 2026-08-04 |
| local shell to production Postgres | Three read-only artifacts run against `kxsdzaojfaibhuzmclfq` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-05-01 | Repudiation | MO flip instruction lost | critical | mitigate | The runbook is a named file, referenced from both flip regions and from the STATE.md `≥ 2026-08-04` operator entry — three independent routes to it. |
| T-166-05-02 | Tampering | wholesale ROADMAP or STATE rewrite | high | mitigate | Scoped `Edit` only; acceptance criterion bounds the ROADMAP line-count delta to 30 lines. |
| T-166-05-03 | Spoofing | inconsistent evidence snapshot | medium | mitigate | All three artifacts run in one session so the green evidence describes one snapshot of prod. |
| T-166-05-04 | Information disclosure | silently fixing out-of-scope broken scripts | medium | mitigate | The four scripts referencing the removed occupancy column are recorded as a discovered defect and explicitly left unmodified; the SUMMARY names the consequence for the 1641 and 1642 dual-map proofs. |
| T-166-05-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; only existing npm scripts are invoked. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `166-mo-flip-runbook.md` exists with all three required headings and is referenced from both flip regions.
- `npm run check:occupancy --prefix backend` and `npm run check:migrations --prefix backend` both exit 0.
- All three Phase-166 artifacts exit 0 in one session with their expected PASS counts.
- ROADMAP, STATE and REQUIREMENTS updated via scoped edits; ROADMAP line-count delta within 30 lines.
- No migration authored, no Render deploy performed.
- All changes committed.
</verification>

<success_criteria>
USHC3-06 is closed with reproducible evidence — three read-only artifacts green against one prod snapshot covering all 178 Wave-3 districts — and the phase leaves behind a maintenance path that survives to the 2026-08-04 Missouri decision.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/166-consolidated-verification-gate/166-05-SUMMARY.md` when done. Include the full captured output of the three-artifact combined run, the four out-of-scope broken scripts as a follow-up item, and the exact STATE.md Operator Next Steps text as amended.
</output>