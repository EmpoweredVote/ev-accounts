---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 05
type: execute
wave: 3
depends_on: ["155-03"]
files_modified:
  - backend/scripts/seed-pa-il-house-headshots.py
  - backend/data/seed-pa-2026-house/155-05-pa-headshots.manual.txt
autonomous: true
requirements: [USHC2-04]
must_haves:
  truths:
    - "Every newly-seeded PA candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row + photo_origin_url, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/scripts/seed-pa-il-house-headshots.py"
      provides: "Headshot pipeline targeting the new PA/IL candidate external_id band (clone of the validated Phase-150 seed-tx-ny-house-headshots.py)"
    - path: "backend/data/seed-pa-2026-house/155-05-pa-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source PA portraits where Wikipedia has none"
  key_links:
    - from: "new PA candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-pa-il-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded PA candidate a headshot following the established find-headshots conventions (600x750, free-license, Storage-mirrored + politician_images row + photo_origin_url), reusing the validated Phase 150 headshot pipeline.

Purpose: USHC2-04 (PA slice). race_candidates resolve photos via COALESCE(rc.photo_url, pi.url) lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`. Reused incumbents already have headshots — only the 155-03 new PA candidates need them.
Output: The PA/IL headshot script (created here, reused by 155-06 for IL) + a PA manual-source list; every new PA candidate has an image row or a documented honest-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md
@.claude/skills/find-headshots/SKILL.md
@backend/scripts/seed-tx-ny-house-headshots.py
@backend/scripts/seed-ca-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the headshot pipeline to target the new PA/IL candidate band + run it for PA</name>
  <read_first>
    - backend/scripts/seed-tx-ny-house-headshots.py (the Phase-150 two-state pipeline — already parameterized by state/external_id band; clone + retarget for PA/IL)
    - backend/scripts/seed-ca-house-headshots.py (the original validated pipeline: Wikipedia pageimages → free-license check → hardened wrong-person guard → 4:5 crop → 600x750 LANCZOS q90 → PUT politician_photos/{uuid}-headshot.jpg x-upsert → INSERT politician_images WHERE NOT EXISTS; --manual reads external_id|image_url|license)
    - .claude/skills/find-headshots/SKILL.md (find-headshots conventions: Storage-mirrored 600×750 + politician_images row + photo_origin_url; free-license; wrong-person guard)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md (the authoritative new PA-candidate external_id list)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md (the TX headshot run: auto-pass rate, manual/honest-skip handling, the wrong-person guard hardening)
  </read_first>
  <action>
    Clone seed-tx-ny-house-headshots.py to backend/scripts/seed-pa-il-house-headshots.py. Swap ONLY the target-selection: parameterize it to accept a state/band argument (or two explicit external_id lists — PA from 155-03-SUMMARY, IL from 155-04-SUMMARY) so the SAME script serves both 155-05 (PA) and 155-06 (IL). Because new challengers have NO essentials.offices row, the target query selects by explicit negative external_id list AND NOT EXISTS a politician_images row. Keep EVERY guard intact: free-license-only, hardened wrong-person guard (title must contain candidate first+surname; reject election/place/event/non-political disambiguators), 4:5 crop → 600x750, x-upsert, idempotent INSERT, photo_origin_url set.

    Run the automatic Wikipedia pass for the PA band: `python backend/scripts/seed-pa-il-house-headshots.py` with the PA external_id list. For PA candidates with no free Wikipedia portrait, build backend/data/seed-pa-2026-house/155-05-pa-headshots.manual.txt with external_id|image_url_or_path|license lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL — MEMORY one-try rule), then run with --manual.

    For any PA candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 155-verify.sql can pin it — NEVER a placeholder or wrong-person image.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md','utf8'); const ids=[...sum.matchAll(/-42[0-9]{4}/g)].map(m=>+m[0]).filter(x=>x<-420000); if(!ids.length){console.error('no new PA external_ids found in 155-03-SUMMARY (check band)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new PA candidates still missing headshot:', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-pa-il-house-headshots.py targets the new-candidate set by explicit external_id list (not the offices-join query) and keeps all original guards; parameterized so 155-06 reuses it for IL.
    - Every new PA candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed, photo_origin_url set; wrong-person guard intact.
    - Re-running inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script created + run for PA; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) → Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-19 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained from the Phase-150 pipeline; manual sources human-verified; spot-check in 155-09 |
| T-155-20 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-155-21 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-155-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (155-09): sample new PA headshots against source pages for identity.
</verification>

<success_criteria>
- Every new PA candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact; the shared script is ready for IL (155-06).
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-05-SUMMARY.md` when done (record: PA candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs, the shared script path for 155-06).
</output>
