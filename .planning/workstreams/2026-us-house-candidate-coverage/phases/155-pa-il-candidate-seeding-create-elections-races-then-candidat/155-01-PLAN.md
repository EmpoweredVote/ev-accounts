---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_pa_il_2026_house_elections_races.sql
autonomous: true
requirements: [USHC2-03]
must_haves:
  truths:
    - "An essentials.elections row exists for 'PA 2026 Statewide General' and one for 'IL 2026 Statewide General', election_date 2026-11-03, mirroring the CA/TX/NY 2026 Statewide General shape"
    - "Exactly 17 essentials.races rows exist for PA (one per geo_id 42NN) and 17 for IL (one per geo_id 17NN), each office_id = that district's existing U.S. Representative office, never office_id IS NULL"
    - "Every PA/IL House race links to the correct existing NATIONAL_LOWER office by geo_id (PA 4201..4217, IL 1701..1717) — no office created, none orphaned"
  artifacts:
    - path: "backend/migrations/NNNN_seed_pa_il_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT 2 elections + 34 races (17 PA + 17 IL) on existing House offices; no race_candidates yet"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing NATIONAL_LOWER U.S. Representative office for each PA 42NN / IL 17NN geo_id"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "essentials.races.election_id"
      to: "the newly-authored PA / IL 2026 Statewide General election rows"
      via: "INSERT … SELECT id FROM essentials.elections WHERE name=…"
      pattern: "election_id"
---

<objective>
Author the `essentials.elections` + `essentials.races` rows that PA and IL require before any candidates can be wired. Both states have ZERO 2026 House races (all 34 rows in 154-field-table.csv carry a BLANK existing_race_id), so this Wave-1 plan creates the election + per-district race scaffold that Wave 2 wires `race_candidates` onto. This is the create-races-first structural step (identical to Phase 150 TX/NY 150-01, NOT the CA turnkey path).

Purpose: USHC2-03 prerequisite — the elections-feed surfaces a race only if an `essentials.races` row exists on the district's office.
Output: One idempotent migration creating 2 elections + 34 races; `/elections` still shows no field (no candidates yet) but the scaffold is live for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv

<interfaces>
<!-- essentials.elections shape (mirror the CA/TX/NY 2026 Statewide General rows):
     columns include id uuid (gen_random_uuid()), name text, election_date timestamptz,
     election_type text ('general'), jurisdiction_level text ('state'), state text ('PA'/'IL'),
     description NULL. Confirm the live CA/TX/NY row shape before authoring (SELECT * FROM
     essentials.elections WHERE name='TX 2026 Statewide General'). -->
<!-- essentials.races shape: id uuid, election_id uuid FK->elections, office_id uuid FK->offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL ok here),
     seats int (1), description NULL. NO geo_id column on races. -->
<!-- PA/IL US Representative offices ALREADY EXIST: 17 NATIONAL_LOWER offices for PA geo_id 4201..4217,
     17 for IL 1701..1717. office_id lookup is by district geo_id. NEVER create an office. -->
<!-- VERIFY LIVE before authoring: 0 essentials.races on any PA/IL NATIONAL_LOWER office; no
     'PA/IL 2026 Statewide General' election exists (STATE.md Open Blockers query #3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the PA/IL elections + races scaffold migration</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (PA rows geo_id 42NN + IL rows geo_id 17NN: the 17 PA + 17 IL geo_ids, target_election names 'PA 2026 Statewide General' / 'IL 2026 Statewide General', cd for position_name)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-01-PLAN.md (the validated create-races-first authoring template — mirror exactly for PA/IL)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-01-SUMMARY.md (the TX/NY election UUIDs + per-state race counts + the idempotency precedent actually applied)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md (D-06: office_id NEVER NULL; never party on card; specifics: election names + 2026-11-03 date)
    - backend/migrations (find the most-recent idempotent BEGIN/COMMIT seed migration on disk for the NOT EXISTS guard + header precedent)
  </read_first>
  <action>
    Pick the next free migration number by listing backend/migrations and incrementing the highest on disk (disk wins over DB MAX). Name it `NNNN_seed_pa_il_2026_house_elections_races.sql`. All writes inside one BEGIN; … COMMIT;. Header comment cites the field source (154-FIELD-TABLE.md + the per-state Wikipedia URLs in 154-field-table.csv source_url) and the antipartisan invariant (D-06: party is NOT stored on race_candidates; races.primary_party stays NULL — there is no single primary party for a multi-candidate general).

    Migration body, in order:
    1. INSERT the 2 elections idempotently (NOT EXISTS guard on name): 'PA 2026 Statewide General' (state 'PA') and 'IL 2026 Statewide General' (state 'IL'), election_date '2026-11-03', election_type 'general', jurisdiction_level 'state', description NULL — mirror the live TX/NY rows exactly.
    2. INSERT one race per PA/IL House district onto the EXISTING office, resolving office_id by district geo_id via the join essentials.offices JOIN essentials.districts ON district_id WHERE district_type='NATIONAL_LOWER' AND geo_id matches. For each of the 17 PA geo_ids (4201..4217) + 17 IL geo_ids (1701..1717): election_id = the matching state's just-inserted election (resolve by name), office_id = the looked-up office, position_name='U.S. Representative District '||cd, primary_party=NULL, seats=1. Guard each with NOT EXISTS on (election_id, office_id) — application-enforced idempotency, no DB unique constraint.
    3. NEVER create an essentials.offices or districts row. NEVER set office_id NULL.

    Add a post-COMMIT verification block (or run separately) asserting: exactly 1 'PA 2026 Statewide General' + 1 'IL 2026 Statewide General' election; exactly 17 races on PA NATIONAL_LOWER offices + 17 on IL; 0 races with NULL office_id; every PA geo_id 4201..4217 and IL 1701..1717 has exactly 1 race.

    Run dry first (review generated SQL), then apply against prod (kxsdzaojfaibhuzmclfq) from backend/ with `set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`. Do not inline the SQL here — author it per the 150-01 mechanics in the read_first files.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT state, count(*) n FROM essentials.elections WHERE name IN ('PA 2026 Statewide General','IL 2026 Statewide General') GROUP BY state\"); const r=await pool.query(\"SELECT substr(d.geo_id,1,2) fips, count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name IN ('PA 2026 Statewide General','IL 2026 Statewide General') AND d.district_type='NATIONAL_LOWER' GROUP BY 1 ORDER BY 1\"); const pa=r.rows.find(x=>x.fips==='42'); const il=r.rows.find(x=>x.fips==='17'); if(e.rows.length!==2){console.error('FAIL elections',e.rows);process.exit(1)}; if(!pa||+pa.races!==17){console.error('FAIL PA races',pa);process.exit(1)}; if(!il||+il.races!==17){console.error('FAIL IL races',il);process.exit(1)}; console.log('OK: 2 elections, PA 17 races, IL 17 races, 0 null office_id'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 'PA 2026 Statewide General' + 1 'IL 2026 Statewide General' election row, each election_date 2026-11-03, election_type 'general', jurisdiction_level 'state'.
    - Exactly 17 races on PA NATIONAL_LOWER offices (geo_id 4201..4217) + 17 on IL (1701..1717); each office_id non-null and = the existing district office.
    - 0 races created with NULL office_id; 0 new essentials.offices or districts rows.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven).
  </acceptance_criteria>
  <done>Migration applied; 2 elections + 34 races live; verification passes; idempotent on re-run. Record the two election UUIDs in the SUMMARY for Wave-2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 2 elections + 34 races to live prod |
| 154 field artifacts → seed input | geo_id list locked + human-reviewed in Phase 154 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-01 | Tampering | duplicate election/race on re-run | mitigate | NOT EXISTS guards on (name) and (election_id, office_id); re-run inserts 0 |
| T-155-02 | Tampering | race wired to wrong/NULL office | mitigate | office_id resolved by district geo_id join; post-COMMIT asserts 0 NULL office_id + exact 17/17 counts |
| T-155-03 | DoS | accidental write to an existing PA/IL election | mitigate | All inserts scoped to the two named 2026 Statewide General elections; never touch existing elections |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 155-verify.sql (authored in 155-02) USHC2-03 race-scaffold assertions pass after this runs.
- Manual: spot-check 3 PA + 3 IL races resolve to the correct office by geo_id.
</verification>

<success_criteria>
- 2 elections + 34 races (17 PA + 17 IL) live, all office_id non-null, idempotent.
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-01-SUMMARY.md` when done (record: migration number, the PA + IL election UUIDs, per-state race count, any geo_id that failed office lookup).
</output>
