---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 09
type: execute
wave: 4
depends_on: ["155-01", "155-02", "155-03", "155-04", "155-05", "155-06", "155-07", "155-08"]
files_modified:
  - backend/scripts/155-coordinate-smoke.ts
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05]
autonomous: true
must_haves:
  truths:
    - "155-verify.sql runs read-only against prod and EVERY labeled assertion (USHC2-02/03/04/05 + D-01/D-02/D-03/D-04) PASSES with exit 0"
    - "For >=3 in-district PA + >=3 in-district IL House test coordinates, the surfacing join returns the resident's House race with its full candidate field (CHALLENGER present, not just incumbent)"
    - "0 unsourced stance rows for the in-scope set + 0 duplicate full_name per state across all 17 PA + 17 IL districts; honest-skips pinned by UUID; lost/retired incumbents absent"
  artifacts:
    - path: "backend/scripts/155-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers a PA + IL House geofence centroid -> assert race + field surfaces with challenger"
  key_links:
    - from: "155-verify.sql + 155-coordinate-smoke.ts"
      to: "the 17 PA + 17 IL House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 155 end-to-end: run the per-state PA/IL-scoped `155-verify.sql` gate green and add a coordinate-surfacing smoke that confirms >=3 PA + >=3 IL House races actually surface for an in-district address.

Purpose: USHC2-02/03/04/05 consolidated verification for PA + IL — the FIRST (anchor) of three Wave-2 seeding-phase proofs. This gate + smoke feed Phase 158's full-113-district gate.
Output: A passing gate + coordinate smoke; Phase 155 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/scripts/155-verify.sql
@backend/scripts/150-coordinate-smoke.ts
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-05-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-06-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-07-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the PA+IL coordinate-surfacing smoke + finalize honest-skip pins in 155-verify.sql</name>
  <read_first>
    - backend/scripts/150-coordinate-smoke.ts (the validated read-only ST_Covers surfacing smoke for two states — mirror it for PA geo_ids 42NN + IL geo_ids 17NN)
    - backend/scripts/155-verify.sql (the gate authored in 155-02; its honest-skip + in-scope TEMP-table pins)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers -> district geo_id -> office -> race -> race_candidates; the join discriminator d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc))
    - the 155-05/06 SUMMARYs (headshot honest-skips) + 155-07/08 SUMMARYs (whole-record-stance honest-skips) — the pids to pin by UUID
  </read_first>
  <action>
    Author backend/scripts/155-coordinate-smoke.ts (read-only) by cloning 150-coordinate-smoke.ts and parameterizing for PA + IL: pick >=3 spread PA geo_ids (e.g. 4201/4203/4210 — include PA-3 open seat Rabb + PA-10 multi-candidate field) + >=3 spread IL geo_ids (e.g. 1701/1704/1709 — include IL-4 retirement open seat + IL-9 Biss). For each, resolve the election by name ('PA 2026 Statewide General' / 'IL 2026 Statewide General'), compute an interior centroid via ST_PointOnSurface(gb.geometry), run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point -> district -> office -> race within that election, NATIONAL_LOWER, geo prefix '42'/'17' -> race_candidates active count + linked count + challenger count). Assert each sample race returns >=2 active candidates (>=1 for PA-3 if Rabb is unopposed; >=3 for IL-4 if the certified field is multi-candidate), non-null politician_id, AND >=1 challenger OR >=1 non-incumbent (the open-seat two-path guard). For IL-4/IL-9 also assert the active candidate is the certified NOMINEE, not the retired incumbent (by full_name match).

    Gather the documented honest-skips (headshots from 155-05/06; whole-record stances from 155-07/08 SUMMARYs) and confirm they are pinned by exact UUID/external_id with explicit ORDER BY inside 155-verify.sql's pin sets; if any are missing, add them (this pin-list update is the only edit allowed to 155-verify.sql here — not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/155-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 155-coordinate-smoke.ts is read-only and asserts >=3 PA + >=3 IL House races surface with their active field incl. >=1 challenger/non-incumbent for an in-district centroid.
    - IL-4 + IL-9 (retirement open seats) sample assert the certified NOMINEE is active (retired incumbent absent).
    - All documented headshot + whole-record-stance honest-skips are pinned by exact UUID/external_id with explicit ORDER BY in 155-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for >=3 PA + >=3 IL districts; honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 155-verify.sql gate green</name>
  <read_first>
    - backend/scripts/155-verify.sql (the full gate)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-02-SUMMARY.md (the assertion labels + in-scope-set convention + PA/IL all-partial-incumbent exclusion)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-12-PLAN.md (Task 2: the gate-green run convention — route failures to the owning wave plan, never relax an assertion)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/155-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (elections/races -> 155-01; PA records -> 155-03; IL records -> 155-04; headshots -> 155-05/06; stances -> 155-07/08) and re-run after fix — do NOT relax an assertion to make it pass (149 T-149-18). Confirm the gate stays per-state-scoped (no cross-state contamination), honors the D-01 PA+IL all-partial-incumbent exclusion, and is write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/155-verify.sql >/tmp/155gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -25 /tmp/155gate.log; exit 1; fi; grep -c 'PASS' /tmp/155gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 155-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 17 PA + 17 IL House races each carry their active field, 0 null politician_id; 0 duplicate full_name per state; lost/retired incumbents (PA-3 + IL-2/4/7/8/9) absent + certified nominees present; D-02 minor-line/multi-candidate present; every new candidate has a headshot (or pinned skip); 0 unsourced stances for the in-scope set; federal-24 coverage or pinned whole-record skip; PA/IL partials untouched.
    - Coordinate smoke (Task 1) passes for >=3 PA + >=3 IL districts.
  </acceptance_criteria>
  <done>155-verify.sql green (exit 0); coordinate smoke green; Phase 155 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke -> production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-37 | Repudiation | gate passes while a district silently has no challenger | mitigate | Coordinate smoke asserts >=1 non-incumbent candidate per sample race (open-seat two-path guard) |
| T-155-38 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-155-39 | Repudiation | cross-state contamination false-passes a count | mitigate | Gate per-state scoped (geo '42'/'17' within named elections); re-confirmed write-free |
| T-155-40 | Repudiation | retired IL incumbent silently surfaced as active | mitigate | Smoke + gate assert certified nominee active, retired incumbent absent |
| T-155-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 155-verify.sql exits 0, all PASS.
- 155-coordinate-smoke.ts surfaces >=3 PA + >=3 IL House races with challenger present + retirement-seat certified nominee.
</verification>

<success_criteria>
- All USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 assertions PASS; coordinate smoke green; PA + IL Wave-2 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-09-SUMMARY.md` when done (record: final per-state per-district candidate counts, total new records PA + IL, total stanced candidates, all honest-skips pinned, gate PASS log, smoke results — this becomes the PA+IL input to the Phase 158 full-113 gate).
</output>
