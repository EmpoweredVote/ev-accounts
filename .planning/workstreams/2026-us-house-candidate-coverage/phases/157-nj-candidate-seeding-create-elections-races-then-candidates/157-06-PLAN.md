---
phase: 157-nj-candidate-seeding-create-elections-races-then-candidates
plan: 06
type: execute
wave: 4
depends_on: ["157-01", "157-02", "157-03", "157-04", "157-05"]
files_modified:
  - backend/scripts/157-coordinate-smoke.ts
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05]
autonomous: true
must_haves:
  truths:
    - "157-verify.sql runs read-only against prod and EVERY labeled assertion (USHC2-02/03/04/05 + D-01/D-02/D-03/D-04) PASSES with exit 0"
    - "For >=3 in-district NJ House test coordinates (incl. NJ-12 open seat, NJ-8 uncontested, NJ-11 Mejia special-seated) the surfacing join returns the resident's House race with its full candidate field (challenger/non-incumbent present where contested)"
    - "0 unsourced stance rows for the new-NJ-candidate in-scope set + 0 duplicate full_name across all 12 NJ districts; honest-skips pinned by UUID; NJ-12 Watson Coleman absent + Hamawy/Mele active; NJ-8 exactly Menendez; NJ-11 Mejia reused-incumbent + Hathaway active"
  artifacts:
    - path: "backend/scripts/157-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers an NJ House geofence centroid -> assert race + field surfaces (NJ-12 open-seat + NJ-8 uncontested + NJ-11 special-seated samples included)"
  key_links:
    - from: "157-verify.sql + 157-coordinate-smoke.ts"
      to: "the 12 NJ House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 157 end-to-end: run the NJ-scoped `157-verify.sql` gate green and add a coordinate-surfacing smoke that confirms >=3 NJ House races actually surface for an in-district address (incl. the NJ-12 open-seat race, the NJ-8 uncontested race, and the NJ-11 Mejia special-seated race). Mirrors Phase 156's 156-10 (gate + smoke), reduced to one state with the NJ wrinkles.

Purpose: USHC2-02/03/04/05 consolidated verification for NJ — the THIRD of the Wave-2 seeding-phase proofs (after PA/IL Phase 155 and OH/GA/NC Phase 156). This gate + smoke feed Phase 158's full 89-decided-district gate.
Output: A passing gate + coordinate smoke; Phase 157 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/scripts/157-verify.sql
@backend/scripts/156-coordinate-smoke.ts
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-01-SUMMARY.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-02-SUMMARY.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-SUMMARY.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-04-SUMMARY.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the NJ coordinate-surfacing smoke + finalize honest-skip pins in 157-verify.sql</name>
  <read_first>
    - backend/scripts/156-coordinate-smoke.ts (the validated read-only ST_Covers surfacing smoke for OH/GA/NC — mirror it for NJ geo_ids 34NN, incl. the vacancy/open-seat + uncontested sample handling)
    - backend/scripts/157-verify.sql (the gate authored in 157-02; its honest-skip + in-scope TEMP-table pins — NO incumbent in the stance in-scope set)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers -> district geo_id -> office -> race -> race_candidates; the join discriminator d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc))
    - the 157-04 SUMMARY (headshot honest-skips) + 157-05 SUMMARY (whole-record-stance honest-skips) — the pids to pin by UUID
  </read_first>
  <action>
    Author backend/scripts/157-coordinate-smoke.ts (read-only) by cloning 156-coordinate-smoke.ts and parameterizing for NJ: pick >=3 spread NJ geo_ids INCLUDING NJ-12 (3412 open seat) + NJ-8 (3408 uncontested) + NJ-11 (3411 Mejia special-seated) + one renominated contested (e.g. 3406 Pallone). For each, resolve the election by name ('NJ 2026 Statewide General'), compute an interior centroid via ST_PointOnSurface(gb.geometry), run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point -> district -> office -> race within that election, NATIONAL_LOWER, geo prefix '34' -> race_candidates active count + linked count + challenger count).

    Assertions per sample:
    - Contested renominated sample (e.g. NJ-6/3406): >=2 active candidates, non-null politician_id, >=1 challenger (non-incumbent).
    - NJ-12 (3412) open-seat sample: >=2 active candidates (Hamawy + Mele) with 0 is_incumbent=true rows (retired open seat — no incumbent in the race); assert Watson Coleman (full_name match) is ABSENT.
    - NJ-8 (3408) uncontested sample: exactly 1 active candidate = Robert Menendez, is_incumbent=true (documented single-candidate allowance).
    - NJ-11 (3411) special-seated sample: Mejia active is_incumbent=true (reused, pid 93874414-…) + Joe Hathaway active challenger.

    Gather the documented honest-skips (headshots from 157-04; whole-record stances from 157-05 SUMMARY) and confirm they are pinned by exact UUID/external_id with explicit ORDER BY inside 157-verify.sql's pin sets; if any are missing, add them (this pin-list update is the only edit allowed to 157-verify.sql here — not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/157-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 157-coordinate-smoke.ts is read-only and asserts >=3 NJ House races surface with their active field for an in-district centroid.
    - The NJ-12 (3412) sample asserts >=2 active candidates (Hamawy+Mele), 0 is_incumbent rows, Watson Coleman absent; the NJ-8 (3408) sample asserts exactly 1 active (Menendez, is_incumbent); the NJ-11 (3411) sample asserts Mejia reused-incumbent + Hathaway active.
    - All documented headshot + whole-record-stance honest-skips are pinned by exact UUID/external_id with explicit ORDER BY in 157-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for >=3 NJ districts (incl. NJ-12 open, NJ-8 uncontested, NJ-11 special-seated); honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 157-verify.sql gate green</name>
  <read_first>
    - backend/scripts/157-verify.sql (the full gate)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-02-SUMMARY.md (the assertion labels + in-scope-set convention + all-partial-incumbent exclusion + NJ-8/NJ-12/NJ-11 handling)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-10-PLAN.md (Task 2: the gate-green run convention — route failures to the owning wave plan, never relax an assertion)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/157-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (elections/races -> 157-01; records -> 157-03; headshots -> 157-04; stances -> 157-05) and re-run after fix — do NOT relax an assertion to make it pass (149 T-149-18). Confirm the gate stays NJ-scoped, honors the D-01 all-partial-incumbent exclusion (no incumbent in stance scope) + the NJ-8 uncontested + NJ-12 retirement handling, and is write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/157-verify.sql >/tmp/157gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -25 /tmp/157gate.log; exit 1; fi; grep -c 'PASS' /tmp/157gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 157-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 12 NJ House races each carry their active field, 0 null politician_id; 0 duplicate full_name; NJ-12 Watson Coleman absent + Hamawy/Mele active; NJ-8 exactly Menendez; NJ-11 Mejia reused-incumbent + Hathaway active; D-02 NJ-3/NJ-5 minor-line present; every new candidate has a headshot (or pinned skip); 0 unsourced stances for the new-candidate in-scope set; federal-24 coverage or pinned whole-record skip; all 12 NJ partials untouched.
    - Coordinate smoke (Task 1) passes for >=3 NJ districts (incl. NJ-12 open, NJ-8 uncontested, NJ-11 special-seated).
  </acceptance_criteria>
  <done>157-verify.sql green (exit 0); coordinate smoke green; Phase 157 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke -> production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-46 | Repudiation | gate passes while a contested district silently has no challenger | mitigate | Coordinate smoke asserts >=1 non-incumbent candidate per contested sample race |
| T-157-47 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-157-48 | Repudiation | NJ-8 uncontested falsely failed for <2 candidates | mitigate | Smoke + gate treat NJ-8 as a documented single-candidate general (exactly 1 active) |
| T-157-49 | Repudiation | retired NJ-12 incumbent silently surfaced OR ghost office created | mitigate | Smoke + gate assert NJ-12 0 incumbent rows + Watson Coleman absent + Hamawy/Mele active; no office created |
| T-157-50 | Repudiation | NJ-11 Mejia duplicated or wrong-seated | mitigate | Gate reuse_pid asserts Mejia 93874414-… is the single active is_incumbent NJ-11 candidate |
| T-157-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 157-verify.sql exits 0, all PASS.
- 157-coordinate-smoke.ts surfaces >=3 NJ House races with challenger/non-incumbent present (NJ-12 open + NJ-8 uncontested + NJ-11 special-seated samples).
</verification>

<success_criteria>
- All USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 assertions PASS; coordinate smoke green; NJ Wave-2 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-06-SUMMARY.md` when done (record: final per-district candidate counts, total new records NJ, total stanced candidates, all honest-skips pinned, NJ-8/NJ-12/NJ-11 confirmations, gate PASS log, smoke results — this becomes the NJ input to the Phase 158 full-gate).
</output>
