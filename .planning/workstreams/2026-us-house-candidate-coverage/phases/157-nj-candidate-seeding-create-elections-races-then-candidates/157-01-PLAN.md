---
phase: 157-nj-candidate-seeding-create-elections-races-then-candidates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_nj_2026_house_elections_races.sql
autonomous: true
requirements: [USHC2-03]
must_haves:
  truths:
    - "An essentials.elections row exists for 'NJ 2026 Statewide General', election_date 2026-11-03, election_type 'general', jurisdiction_level 'state', state 'NJ', mirroring the live PA/IL/OH/GA/NC 2026 Statewide General shape"
    - "Exactly 12 essentials.races rows exist for NJ (one per geo_id 3401..3412), each office_id = that district's existing U.S. Representative office, never office_id IS NULL"
    - "No new essentials.offices or districts rows are created — NJ has NO true vacancy (NJ-12 is a retirement; NJ-11 special-seat + its office already exist), unlike GA-13 in Phase 156"
  artifacts:
    - path: "backend/migrations/NNNN_seed_nj_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT 1 election + 12 races on existing NJ House offices; no race_candidates yet"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing NATIONAL_LOWER U.S. Representative office for each NJ geo_id 3401..3412"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "essentials.races.election_id"
      to: "the newly-authored 'NJ 2026 Statewide General' election row"
      via: "INSERT … SELECT id FROM essentials.elections WHERE name='NJ 2026 Statewide General'"
      pattern: "election_id"
---

<objective>
Author the `essentials.elections` + `essentials.races` rows that NJ requires before any candidates can be wired. NJ has ZERO 2026 House races (all 12 rows in 154-field-table.csv carry a BLANK existing_race_id), so this Wave-1 plan creates the election + per-district race scaffold that Wave 2 wires `race_candidates` onto. This is the create-races-first structural step — a near-verbatim, single-state repeat of Phase 156's 156-01 (OH/GA/NC), WITHOUT the GA-13 vacancy-office branch (NJ has no true vacancy).

Purpose: USHC2-03 prerequisite — the elections-feed surfaces a race only if an `essentials.races` row exists on the district's office.
Output: One idempotent migration creating 1 election + 12 races; `/elections` still shows no field (no candidates yet) but the scaffold is live for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv

<interfaces>
<!-- essentials.elections shape (mirror the live PA/IL/OH/GA/NC 2026 Statewide General rows):
     columns include id uuid (gen_random_uuid()), name text, election_date timestamptz,
     election_type text ('general'), jurisdiction_level text ('state'), state text ('NJ'),
     description NULL. Confirm the live OH row shape before authoring (SELECT * FROM
     essentials.elections WHERE name='OH 2026 Statewide General'). -->
<!-- essentials.races shape: id uuid, election_id uuid FK->elections, office_id uuid FK->offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL ok here),
     seats int (1), description NULL. NO geo_id column on races. -->
<!-- NJ US Representative offices ALREADY EXIST for all 12 districts: 12 NATIONAL_LOWER offices
     for NJ geo_id 3401..3412. office_id lookup is by district geo_id. NEVER create an office. -->
<!-- NJ has NO true vacancy: NJ-12 (Watson Coleman) is a RETIREMENT — office + incumbent record
     already exist; NJ-11 (Mejia) is special-seated but already in DB with its office. There is NO
     GA-13-style office-creation step in this phase. -->
<!-- VERIFY LIVE before authoring: 0 essentials.races on any NJ NATIONAL_LOWER office; no
     'NJ 2026 Statewide General' election exists. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the NJ elections + races scaffold migration</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (NJ rows geo_id 3401..3412: the 12 geo_ids, target_election name 'NJ 2026 Statewide General', cd for position_name)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (NJ geo_id 34NN → incumbent_pid; all 12 present incl. Mejia -34011 + Watson Coleman -34012 already in DB)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-PLAN.md (the validated create-races-first authoring template — mirror for single-state NJ, DROP the GA-13 vacancy-office branch)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md (the OH/GA/NC election UUIDs + per-state race counts + the idempotency precedent actually applied)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md (D-04 NJ has no vacancy; D-06 office_id NEVER NULL; never party on card; specifics: election name + 2026-11-03 date + FIPS NJ=34)
    - backend/migrations (find backend/migrations/1127_* as the OH/GA/NC scaffold precedent; find the highest migration number on disk for the next free number — 1136 is currently highest, so use 1137 unless a higher one appears)
  </read_first>
  <action>
    Pick the next free migration number by listing backend/migrations and incrementing the highest on disk (disk wins over DB MAX; currently 1136 → 1137). Name it `NNNN_seed_nj_2026_house_elections_races.sql`. All writes inside one BEGIN; … COMMIT;. Header comment cites the field source (154-FIELD-TABLE.md + the NJ Wikipedia/newjerseyglobe URLs in 154-field-table.csv source_url) and the antipartisan invariant (D-06: party is NOT stored on race_candidates; races.primary_party stays NULL — there is no single primary party for a multi-candidate general).

    Migration body, in order:
    1. INSERT the 1 election idempotently (NOT EXISTS guard on name): 'NJ 2026 Statewide General' (state 'NJ'), election_date '2026-11-03', election_type 'general', jurisdiction_level 'state', description NULL — mirror the live OH/GA/NC rows exactly.
    2. INSERT one race per NJ House district onto the EXISTING office, resolving office_id by district geo_id via the join essentials.offices JOIN essentials.districts ON district_id WHERE district_type='NATIONAL_LOWER' AND geo_id matches. For each of the 12 NJ geo_ids (3401..3412): election_id = the just-inserted 'NJ 2026 Statewide General' election (resolve by name), office_id = the looked-up office, position_name='U.S. Representative District '||cd, primary_party=NULL, seats=1. Guard each with NOT EXISTS on (election_id, office_id) — application-enforced idempotency, no DB unique constraint.
    3. NEVER create an essentials.offices or districts row (NJ has no true vacancy). NEVER set office_id NULL.

    Add a post-COMMIT verification block (or run separately) asserting: exactly 1 'NJ 2026 Statewide General' election; exactly 12 races on NJ NATIONAL_LOWER offices; 0 races with NULL office_id; every NJ geo_id 3401..3412 has exactly 1 race.

    Run dry first (review generated SQL), then apply against prod (kxsdzaojfaibhuzmclfq) from backend/ with `set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`. Do not inline the SQL here — author it per the 156-01 mechanics in the read_first files.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT count(*) n FROM essentials.elections WHERE name='NJ 2026 Statewide General'\"); const r=await pool.query(\"SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='NJ 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='34'\"); if(+e.rows[0].n!==1){console.error('FAIL election',e.rows);process.exit(1)}; if(+r.rows[0].races!==12){console.error('FAIL NJ races',r.rows);process.exit(1)}; if(+r.rows[0].nulloff!==0){console.error('FAIL null office_id',r.rows);process.exit(1)}; console.log('OK: 1 election, NJ 12 races, 0 null office_id'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 'NJ 2026 Statewide General' election row, election_date 2026-11-03, election_type 'general', jurisdiction_level 'state', state 'NJ'.
    - Exactly 12 races on NJ NATIONAL_LOWER offices (geo 3401..3412); each office_id non-null and = the existing district office.
    - 0 races created with NULL office_id; 0 new essentials.offices or districts rows (NJ has no vacancy).
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven).
  </acceptance_criteria>
  <done>Migration applied; 1 election + 12 races live; verification passes; idempotent on re-run. Record the NJ election UUID in the SUMMARY for Wave-2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 1 election + 12 races to live prod |
| 154 field artifacts → seed input | geo_id list locked + human-reviewed in Phase 154 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-01 | Tampering | duplicate election/race on re-run | mitigate | NOT EXISTS guards on (name) and (election_id, office_id); re-run inserts 0 |
| T-157-02 | Tampering | race wired to wrong/NULL office | mitigate | office_id resolved by district geo_id join; post-COMMIT asserts 0 NULL office_id + exact 12 count |
| T-157-03 | Tampering | accidental office/district creation | mitigate | Migration creates NO offices/districts (NJ has no vacancy); assertion checks row counts unchanged |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 157-verify.sql (authored in 157-02) USHC2-03 race-scaffold assertions pass after this runs.
- Manual: spot-check 3 NJ races resolve to the correct office by geo_id.
</verification>

<success_criteria>
- 1 election + 12 races live, all office_id non-null, no offices/districts created, idempotent.
</success_criteria>

<output>
Create `.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-01-SUMMARY.md` when done (record: migration number, the NJ election UUID, race count, any geo_id that failed office lookup).
</output>
