---
phase: 157-nj-candidate-seeding-create-elections-races-then-candidates
plan: 03
type: execute
wave: 2
depends_on: ["157-01", "157-02"]
files_modified:
  - backend/migrations/NNNN_seed_nj_2026_house_candidates.sql
  - backend/data/seed-nj-2026-house/157-03-nj-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 12 NJ US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'; NJ-8 carries exactly the reused Menendez incumbent (uncontested)"
    - "Every renominated/reused NJ incumbent (NJ-1..11, incl. NJ-11 Mejia reused as the special-seated incumbent) reuses its 154 incumbent_pid as the active is_incumbent=true candidate; the challenger(s) are new records"
    - "NJ-12 Watson Coleman (retired) has NO active race_candidates row; her nominees Adam Hamawy + Gregg Mele are new active records — this is a retirement, NOT a vacancy, so NO office is created"
    - "Every NJ minor-line candidate (NJ-3 Steven Welzer Green + Ryan Michael Kelly Affordability Accountability People, NJ-5 Adam Rueda Humane Sustainable Future) is seeded as an active party-agnostic race_candidate (D-02 seed-all)"
    - "Every 'new' NJ candidate is live-DB dedup-checked before insert; zero duplicate full_name; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_nj_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT ~15 new NJ politicians + INSERT race_candidates onto the 12 NJ races authored in 157-01"
      contains: "race_candidates"
    - path: "backend/data/seed-nj-2026-house/157-03-nj-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 12 NJ districts"
      min_lines: 22
  key_links:
    - from: "race_candidates.race_id"
      to: "the 12 NJ House races (157-01 scaffold)"
      via: "office_id → districts.geo_id 34NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (154-incumbent-map.csv) or newly-inserted politician id (live-confirmed)"
      via: "157-03-nj-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 12 NJ US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every incumbent / previously-seeded figure — after a MANDATORY per-candidate live-DB dedup check (D-03). Near-verbatim repeat of Phase 156's OH records plan (156-03), retargeted to NJ (fips=34, geo 34NN), single state.

Purpose: USHC2-02 + USHC2-03 for NJ. NJ is mostly renominated (NJ-1..10 incumbents are the 2026 nominees, reused is_incumbent=true) with three wrinkles: NJ-8 uncontested (Menendez only), NJ-11 Mejia special-seated reused-incumbent (already in DB), NJ-12 Watson Coleman retired (no active row; both nominees new). The ~15 new records are the challengers + NJ's minor-line candidates (D-02 seed-all).
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every NJ House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates schema (verified in 150-03/155-03/156-03):
     id uuid PK; race_id uuid NOT NULL FK->races; politician_id uuid (D-06: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL -> feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite source_url); external_id text optional. NO unique constraint -> idempotency app-enforced:
     check (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool.
     NO updated_at column (149-01 Rule-3 fix). -->
<!-- NJ renominated/reused incumbents (reuse incumbent_pid from 154-incumbent-map.csv): NJ-1..11 (geo 3401..3411),
     INCLUDING NJ-11 Analilia Mejia 93874414-6d14-4c12-87b2-d254b3855570 (special-seated, ALREADY IN DB — REUSE, do NOT
     create a new Mejia record). NJ-12 Watson Coleman a75a3e6e-… is RETIRED -> keeps pid, NO active row. -->
<!-- external_id band for new NJ challengers (D-06): -(34*10000 + cd*100 + seq) -> e.g. NJ-1 seq1 = -340101;
     NJ-3 McGuire/Welzer/Kelly = -340301/-340302/-340303; NJ-12 Hamawy/Mele = -341201/-341202.
     NJ incumbents occupy -34001..-34012 (strictly greater than -340101). Verify the -340101..-341299 band
     is collision-free vs live external_id<0 before insert. -->
<!-- D-02 seed-all NJ minor lines: NJ-3 Steven Welzer (Green) + Ryan Michael Kelly (Affordability Accountability People);
     NJ-5 Adam Rueda (Humane Sustainable Future). Seed every ballot-qualified general candidate; never store party. -->
<!-- NJ-8 uncontested: Robert Menendez (D) is the ONLY Nov-3 general candidate -> wire only the reused incumbent
     (is_incumbent=true), 0 new records for NJ-8. NOT an error. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per NJ candidate + re-confirm certified generals (D-03 mandatory)</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (NJ rows geo_id 34NN: general_candidates, new_records_needed, nominee_status, incumbent_pid; relax-parse names with commas/quotes — e.g. "Herbert C. Conaway, Jr.")
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (NJ geo_id → incumbent_pid + external_id, all 12 incumbents incl. Mejia -34011 + Watson Coleman -34012)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-PLAN.md (the validated reuse-vs-new reconciliation pattern + external_id-band collision check + live-name-query dedup mechanics)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-04-PLAN.md (the GA lost/retired/vacancy handling — NJ-12 retired mirrors the GA lost-incumbent no-active-row pattern; NJ has NO vacancy so ignore the GA-13 office-creation branch)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md (D-03 dedup + certified-general re-confirm; D-02 seed-all minor NJ lines; D-04 NJ-8 uncontested + NJ-11 Mejia reused + NJ-12 retired; NJ fips=34)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-nj-2026-house/157-03-nj-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 12 NJ districts (~26 rows: 11 reused incumbents active + ~15 new + NJ-12 Watson Coleman flagged NO-ACTIVE-ROW). Resolve race_id by joining the 157-01 NJ races to districts.geo_id.

    Re-confirm certified Nov-3 general nominees per district (D-03) before deciding rows — the 154 general_candidates column reflects the field as of diagnostic time; the NJ primary is held so the field is reliable. Seed only ballot-qualified general candidates (D-02: seed ALL ballot-qualified incl. Green/minor lines; drop only genuinely-uncertified). Parse the party in parens for context only, NEVER store it (D-06).

    For EACH candidate:
    1. If the name matches the district's renominated/reused NJ incumbent (NJ-1..11) → REUSE, target = the 154 incumbent_pid, is_incumbent=true. **NJ-11 Analilia Mejia = REUSE 93874414-… (special-seated, already in DB), NOT a new record.**
    2. **NJ-8 (Menendez)**: single REUSE row (incumbent, is_incumbent=true), 0 new — uncontested general.
    3. **NJ-12 (Watson Coleman)**: emit a NO-ACTIVE-ROW marker row for the retired incumbent (decision=REUSE-NO-ROW / do NOT wire an active race_candidate for her); both nominees Adam Hamawy (D) + Gregg Mele (R) = NEW active records. This is a retirement — do NOT create any office.
    4. For every other challenger / minor-line / new name → run a LIVE name query against essentials.politicians (full_name ILIKE + sanity on external_id/office context). If a real matching record exists → decision=REUSE with that UUID. If 0 matches → decision=NEW, assign a collision-free negative external_id.
    5. Confirm every NJ incumbent NJ-1..11 has exactly one active is_incumbent=true row; NJ-12 has none.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id. NJ incumbents occupy -34001..-34012. Use the scheme -(34*10000 + cd*100 + seq) (e.g. NJ-1 seq1 = -340101) and VERIFY the -340101..-341299 band is empty before use; if any collision, pick another verified-empty band and document it (149-01 collision lesson).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-nj-2026-house/157-03-nj-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<20){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; if(newids.length<13||newids.length>17){console.error('WARN new count',newids.length)}; console.log('NJ reconciliation OK rows',body.length,'new',newids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 157-03-nj-reconciliation.csv has one row per NJ general candidate across 12 districts (~26 rows incl. D-02 minor lines + the NJ-12 no-active-row marker), each with a resolved race_id, re-confirmed against the certified field.
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred); NJ-1..11 incumbents REUSE their 154 incumbent_pid, incl. NJ-11 Mejia; NJ-12 Watson Coleman marked NO-ACTIVE-ROW.
    - NJ-3 Welzer (Green) + Kelly (Affordability Accountability People) + NJ-5 Rueda (Humane Sustainable Future) present as NEW (or REUSE if pre-existing) — no minor candidate dropped (D-02).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live external_id<0 set; band -340101..-341299 verified empty.
  </acceptance_criteria>
  <done>nj-reconciliation.csv exists, all REUSE UUIDs live-confirmed (incl. Mejia), NJ-12 retired flagged no-active-row, certified generals + minor lines re-confirmed, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent NJ House seed migration (new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-nj-2026-house/157-03-nj-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map, incl. NJ-12 no-active-row marker)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics — INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at; no challenger offices)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-01-SUMMARY.md (the NJ election UUID — confirm races resolve to it)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md (D-06 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card; D-04 NJ-12 retired = no active row, no office)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk). Name `NNNN_seed_nj_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (154-FIELD-TABLE.md NJ section + per-district source URLs) + the antipartisan invariant (D-06).

    Migration body, in order (per the 156-03 Task-2 mechanics):
    1. INSERT the ~15 new NJ politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers OR for NJ-12 (retirement, not vacancy — office already exists; a challenger/ghost office would pollute the reps feed / geofence search, 149-01 A2).
    2. INSERT the race_candidates onto the 12 NJ races: for each reconciliation row EXCEPT the NJ-12 Watson Coleman no-active-row marker, race_id=resolved NJ race, politician_id=REUSE target or just-inserted new UUID (resolve new via SELECT id … WHERE external_id=…), full_name/first_name/last_name, is_incumbent per CSV, candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name). Do NOT insert an active row for Watson Coleman (NJ-12 retired). NJ-8 gets only the reused Menendez incumbent.
    3. NEVER touch the races rows (157-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate; NEVER create an office.

    Post-COMMIT verification block (or run separately), scoped to NJ NATIONAL_LOWER (geo_id prefix '34') within the NJ 2026 Statewide General election: all 12 NJ races have ≥1 active candidate (NJ-8 exactly 1, others ≥2); 0 NULL politician_id; 0 duplicate full_name within NJ; NJ-1..11 incumbents present as is_incumbent=true; NJ-12 Watson Coleman absent from active field; NJ-3/NJ-5 minor lines present.

    Run dry first (review SQL), then apply against prod via psql -v ON_ERROR_STOP=1. Do not inline the SQL here — author per the 156-03 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='NJ 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='34'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='NJ 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='34' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); const wc=await pool.query(\"SELECT count(*) n FROM essentials.race_candidates WHERE politician_id='a75a3e6e-31ff-4870-8aea-501e41326fd1' AND candidate_status='active'\"); if(+row.races!==12||+row.nullpid>0||+dup.rows[0].n>0||+wc.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n,'watsoncoleman_active',wc.rows[0].n);process.exit(1)}; console.log('NJ OK: 12 races,',row.active_rc,'active rc, 0 null pid, 0 dup, Watson Coleman absent'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 NJ House races (NATIONAL_LOWER geo '34' within the NJ 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id; NJ-8 exactly 1 active (Menendez).
    - 0 duplicate full_name within NJ (D-03).
    - NJ-1..11 incumbents present as is_incumbent=true (incl. NJ-11 Mejia reused); NJ-12 Watson Coleman ABSENT from the active field; Hamawy + Mele active.
    - NJ-3 Welzer + Kelly + NJ-5 Rueda minor lines active (D-02).
    - No new essentials.offices rows created; no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; NJ 12-race assertion passes; dedup correct; Mejia reused; Watson Coleman absent; minor lines seeded; idempotent on re-run. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes ~15 new NJ politicians + race_candidates to live prod |
| 154 field artifacts → seed input | Field/identity locked + human-reviewed in Phase 154; reuse-vs-new + certified-general re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-12 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector); esp. a duplicate Mejia | mitigate | Live name-check per candidate; reuse 154 incumbent_pid incl. Mejia -34011; 0-dup-full_name gate |
| T-157-13 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-157-14 | DoS | write to wrong election / cross-state contamination | mitigate | All inserts scoped to NJ 2026 Statewide General + NATIONAL_LOWER geo '34' |
| T-157-15 | Tampering | minor-party candidate silently dropped | mitigate | D-02 seed-all; reconciliation includes NJ-3 + NJ-5 lines; gate asserts presence |
| T-157-16 | Tampering | retired Watson Coleman wired as active OR a ghost NJ-12 office created | mitigate | NJ-12 no-active-row marker honored; verify asserts Watson Coleman absent; NO office insert |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 157-verify.sql USHC2-02/03 + D-02/D-03/D-04 NJ assertions pass after this runs.
- Manual spot-check: cross-check ~5 NJ districts against 154-FIELD-TABLE.md source_url citations.
</verification>

<success_criteria>
- 12 NJ House races each carry their active field, all politician_id non-null; ~15 new politician rows; every incumbent NJ-1..11 reused by id (0 duplicates, Mejia reused); NJ-12 retired absent; NJ minor lines seeded; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list for downstream headshot/stance plans, any 154-name flips/certified-general changes, minor-line confirmations, NJ-12 no-active-row confirmation).
</output>
