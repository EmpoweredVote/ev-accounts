---
phase: 157-nj-candidate-seeding-create-elections-races-then-candidates
plan: 04
type: execute
wave: 3
depends_on: ["157-03"]
files_modified:
  - backend/scripts/seed-nj-house-headshots.py
  - backend/data/seed-nj-2026-house/157-04-nj-headshots.manual.txt
autonomous: true
requirements: [USHC2-04]
must_haves:
  truths:
    - "Every newly-seeded NJ candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row + photo_origin_url, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced, incl. the 156 homonym/historical-year hardening)"
  artifacts:
    - path: "backend/scripts/seed-nj-house-headshots.py"
      provides: "Headshot pipeline targeting the new NJ candidate external_id band (clone of the validated seed-oh-ga-nc-house-headshots.py from Phase 156)"
    - path: "backend/data/seed-nj-2026-house/157-04-nj-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source portraits where Wikipedia has none"
  key_links:
    - from: "new NJ candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-nj-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded NJ candidate a headshot following the established find-headshots conventions (600x750, free-license, Storage-mirrored + politician_images row + photo_origin_url), reusing the validated Phase 156 headshot pipeline (which itself cloned Phase 155/150). One script, run once for the NJ external_id band.

Purpose: USHC2-04 (NJ). race_candidates resolve photos via COALESCE(rc.photo_url, pi.url) lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`. Reused incumbents (NJ-1..11, incl. Mejia) already have headshots — only the 157-03 new candidates need them.
Output: The NJ headshot script + manual-source list; every new candidate has an image row or a documented honest-skip. Expect mostly honest-skips for thin minor-line candidates (155/156 precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-SUMMARY.md
@.claude/skills/find-headshots/SKILL.md
@backend/scripts/seed-oh-ga-nc-house-headshots.py
@backend/scripts/seed-pa-il-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the headshot pipeline to target the new NJ candidate band + run it</name>
  <read_first>
    - backend/scripts/seed-oh-ga-nc-house-headshots.py (the Phase-156 pipeline — already parameterized by state/external_id band, with the HARDENED wrong-person guard (dropped 'american' keyword + pre-1940 historical-year reject that caught 2 wrong-person attaches in 156); clone + add the NJ band)
    - backend/scripts/seed-pa-il-house-headshots.py (the earlier validated pipeline: Wikipedia pageimages → free-license check → wrong-person guard → 4:5 crop → 600x750 LANCZOS q90 → PUT politician_photos/{uuid}-headshot.jpg x-upsert → INSERT politician_images WHERE NOT EXISTS; --manual reads external_id|image_url|license)
    - .claude/skills/find-headshots/SKILL.md (find-headshots conventions: Storage-mirrored 600×750 + politician_images row + photo_origin_url; free-license; wrong-person guard)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-SUMMARY.md (the authoritative new-NJ-candidate external_id list)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-06-SUMMARY.md (the OH/GA/NC headshot run: auto-pass rate, the 2 wrong-person reverts + guard hardening, honest-skip handling)
  </read_first>
  <action>
    Clone seed-oh-ga-nc-house-headshots.py to backend/scripts/seed-nj-house-headshots.py. Swap ONLY the target-selection: parameterize it to accept the NJ external_id list (from 157-03-SUMMARY) so the script targets the new NJ candidates. Because new challengers have NO essentials.offices row, the target query selects by explicit negative external_id list AND NOT EXISTS a politician_images row. Keep EVERY guard intact, INCLUDING the 156 hardening: free-license-only, wrong-person guard (title must contain candidate first+surname; reject election/place/event/non-political disambiguators; reject pre-1940 historical-year pages — the 156 John-Hancock/Paul-Barringer lesson), 4:5 crop → 600x750, x-upsert, idempotent INSERT, photo_origin_url set.

    Run the automatic Wikipedia pass: `python backend/scripts/seed-nj-house-headshots.py` with the NJ list. For candidates with no free Wikipedia portrait, build backend/data/seed-nj-2026-house/157-04-nj-headshots.manual.txt with external_id|image_url_or_path|license lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL — MEMORY one-try rule), then run with --manual.

    For any candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 157-verify.sql can pin it — NEVER a placeholder or wrong-person image. Expect many minor-line honest-skips (NJ-3/NJ-5 third-party candidates rarely have free portraits).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const s=readFileSync('../.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-SUMMARY.md','utf8'); let ids=[...new Set([...s.matchAll(/-34[0-9]{4}/g)].map(m=>+m[0]).filter(x=>x<=-340101))]; if(!ids.length){console.error('no new NJ external_ids found in 157-03-SUMMARY (check band <= -340101)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new NJ candidates still missing headshot (should be only documented honest-skips):', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-nj-house-headshots.py targets the new-candidate set by explicit external_id list (not the offices-join query) and keeps all original + 156-hardened guards.
    - Every new NJ candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed, photo_origin_url set; wrong-person + historical-year guards intact.
    - Re-running inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script created + run for NJ; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) → Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-25 | Spoofing | wrong-person image attached to a candidate (156 homonym vector) | mitigate | Hardened wrong-person + pre-1940 historical-year guard retained from Phase 156; manual sources human-verified; spot-check in 157-06 |
| T-157-26 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-157-27 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-157-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (157-06): sample new NJ headshots against source pages for identity.
</verification>

<success_criteria>
- Every new NJ candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact.
</success_criteria>

<output>
Create `.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-04-SUMMARY.md` when done (record: candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs, the script path).
</output>
