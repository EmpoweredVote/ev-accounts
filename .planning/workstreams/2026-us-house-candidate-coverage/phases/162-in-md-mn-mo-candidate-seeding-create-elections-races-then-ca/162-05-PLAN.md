---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 05
type: execute
wave: 5
depends_on: ["162-04"]
files_modified:
  - backend/scripts/162-mn-generate.mts
  - backend/migrations/{N}_seed_mn_2026_house_elections_races.sql
  - backend/migrations/{N1}_seed_mn_2026_house_candidates.sql
  - backend/scripts/seed-mn-house-headshots.py
  - backend/data/seed-mn-2026-house/162-05-mn-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "MN has 1 election ('MN 2026 Statewide General', general, 2026-11-03) and 8 races, each office_id → the existing NATIONAL_LOWER office (never null); full PROVISIONAL pre-primary field surfaces on /elections (Aug-11 primary)"
    - "35 new MN politicians created (collision-free -(27*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-27001..-27008); Craig MN-2 (US Senate run) has no active row; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
    - "MN is not redistricted — no withholding, vanilla new-election pattern"
  artifacts:
    - path: "backend/migrations/{N}_seed_mn_2026_house_elections_races.sql"
      provides: "MN general election + 8 races wired to existing offices"
      contains: "MN 2026 Statewide General"
    - path: "backend/migrations/{N1}_seed_mn_2026_house_candidates.sql"
      provides: "35 new MN politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "MN races"
      to: "existing NATIONAL_LOWER offices (geo prefix 27)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Seed MN's full 2026 US House field (8 districts, 35 new records) — vanilla new-election pattern (MN is NOT redistricted, no withholding). MN runs second per D-03 (Aug-11 primary, second provisional field). Author 1 election + 8 races wired to existing offices, create 35 new records + race_candidates (reuse incumbent pids; Craig MN-2 filed for US Senate → no active row), then headshots. Full field marked PROVISIONAL: (late-primary). Continues USHC3-02/03/04.

Output: 162-mn-generate.mts, two MN migrations (run on prod), seed-mn-house-headshots.py (election-scoped, 1-election variant), MN reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv
@backend/scripts/161-az-generate.mts
@backend/scripts/seed-ma-house-headshots.py

<interfaces>
MN FIPS=27; geo_ids 2701-2708. Incumbents keep existing external_id (-27001..-27008); MN-2/2702 Craig filed for US Senate → no active House row (vacate:true). New challengers: external_id = -(27*10000 + cd*100 + seq). One election: "MN 2026 Statewide General" (election_type general, election_date 2026-11-03). All races: office_id = the district's existing NATIONAL_LOWER office (join essentials.districts d ON substr(d.geo_id,1,2)='27' → essentials.offices o ON o.district_id=d.id). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party never on the card (races.primary_party NULL). Description: 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12' (Aug-11 primary). Per-district new-record counts: MN-1=4, MN-2=7 (open, Craig→Senate), MN-3=2, MN-4=4, MN-5=9 (Omar's district, largest MN field), MN-6=3, MN-7=2, MN-8=4 (=35). MN source: candidates.sos.mn.gov ASP.NET page rendered via r.jina.ai with the exact GET params (URL in 160-field-table-p162.csv). sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-27 collision check</name>
  <read_first>
    - 162-RESEARCH.md (Must-Answer #3 — 160-negative-id-audit.csv does NOT cover MN)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (MN incumbent pids/external_ids; Craig → Senate)
  </read_first>
  <action>
    Run the fresh scoped collision check for MN's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -270899..-270101, excluding legacy incumbent ids -27001..-27008 (different formula — cross-check any hit against 160-incumbent-map.csv). Confirm Craig MN-2 is departing (US Senate) so no active House row is authored for her.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -270899 AND -270101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -270899..-270101 (or every hit confirmed a legacy incumbent id, not a real collision)
    - Craig MN-2 confirmed departing → no active row planned
  </acceptance_criteria>
  <done>MN band confirmed collision-free before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply MN elections/races + candidates migrations</name>
  <read_first>
    - 162-PATTERNS.md (162-mn-generate.mts section — clone 161-az-generate.mts; INC_EXT, single-election migration template, candidate-insert template, sqlStr, NOT EXISTS guards)
    - backend/scripts/161-az-generate.mts (the vanilla new-election analog to clone)
    - 160-field-table-p162.csv (the 35 MN new records + incumbent reuse rows)
  </read_first>
  <action>
    Clone backend/scripts/161-az-generate.mts → backend/scripts/162-mn-generate.mts. Change: FIPS 27, 8 districts, INC_EXT = -27001..-27008 (MN-2 Craig vacate:true, no active row); FIELD = the 35 MN new records from 160-field-table-p162.csv (parties normalized Democratic-Farmer-Labor/Republican/etc, never "Democrat"); geo prefix '27'; election name 'MN 2026 Statewide General'; description 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12'. Keep the -(27*10000+cd*100+seq) formula, single-election migration template, and NOT EXISTS idempotency guards on politicians/race_candidates. Use sqlStr() on every free-text field. Emit two migration files with the next unused numbers — re-check live (ls backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail -1; parallel OR session climbing; pick a free pair). Run node --import tsx scripts/162-mn-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-mn-2026-house/162-05-mn-reconciliation.csv listing every MN candidate with new/reuse disposition + external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='MN 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both MN migrations applied cleanly (psql exit 0); 8 races exist, all office_id NOT NULL (the FILTER count is 0)
    - 35 new politicians in -270899..-270101; incumbents reuse -27001..-27008; Craig has no active row; 0 duplicate full_name per state
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>MN races + candidates seeded on prod, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: MN headshots (election-scoped, 1-election variant)</name>
  <read_first>
    - 162-PATTERNS.md (seed-mn-house-headshots.py section — 4-tuple election-scoped BANDS; wrong-person guards byte-for-byte)
    - backend/scripts/seed-ma-house-headshots.py (the 1-election analog to clone)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-ma-house-headshots.py → backend/scripts/seed-mn-house-headshots.py. Change ONLY the module docstring, BANDS = {'MN': (-270899, -270101, 'Minnesota', 'MN 2026 Statewide General')} (4-tuple, election-scoped), and RESULTS_JSON = '_mn-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-27001..-27008) fall outside the band.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -270899 AND -270101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-mn-house-headshots.py uses the 4-tuple election-scoped BANDS; wrong-person guards unchanged
    - Every new MN candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>MN new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-05-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-162-05-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -27001..-27008 by geo_id join; full_name dedup guard; 0-duplicate assertion in 162-11 |
| T-162-05-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted |
</threat_model>

<verification>
- Both MN migrations applied on prod (psql exit 0); 8 races, all office_id NOT NULL
- 35 new politicians in-band; incumbents reused; Craig no active row; 0 duplicate full_name
- Every new MN candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
MN's full 2026 House field is seeded on prod (35 new + incumbent reuse), PROVISIONAL-marked, surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (162-06).
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-05-SUMMARY.md when done, recording applied migration numbers and the MN new-candidate external_id range.
</output>
