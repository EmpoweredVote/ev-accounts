---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 04
type: execute
wave: 4
depends_on: ["162-03"]
files_modified:
  - backend/data/stance-research/mo-2026-house/162-04-mo-stances-B.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "MO batch B (the remaining fringe/minor-party filers) have sourced chairs-not-polarity federal-24 stances, 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every batch-B answer pairs to an inform.politician_context row with a real fetched source_url; whole-record honest-skips are gate-pinned with a written search trail"
    - "All 58 new MO candidates now have either sourced stances or a gate-pinned whole-record skip (batch A + batch B complete)"
  artifacts:
    - path: "backend/data/stance-research/mo-2026-house/162-04-mo-stances-B.csv"
      provides: "Batch-B MO stance rows (remaining filers)"
      contains: "source_url"
  key_links:
    - from: "batch-B stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push.ts / _push_uuid.ts (scaffold from 162-03)"
      pattern: "politician_context"
---

<objective>
Research + push MO stance batch B — the remaining MO filers not covered in batch A (fringe / minor-party / lower-evidence candidates), completing MO's stance coverage. Reuses the mo-2026-house scaffold created in 162-03. Split from 162-03 for context-window safety on MO's 58-record load (comparable to TN's 73-into-2-plans in 161). 3-concurrency stance-agent cap, mandatory primary-source verification pass, push to PROD as it completes. Continues USHC3-05.

Purpose: Close MO's full-field stance coverage before moving to MN (D-03 per-state completion).
Output: batch-B CSV pushed to PROD; MO stance coverage complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-02-SUMMARY.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-03-SUMMARY.md

<interfaces>
Reuse the mo-2026-house/ scaffold from 162-03 (_merge.ts, _push.ts, _push_uuid.ts, _TOPIC_SCALE_FULL.txt) — do NOT recreate them (that would collide with 162-03's file ownership). Batch B = the MO new candidates NOT in 162-03's batch A (per 162-03-SUMMARY's covered list). Same standards: chairs-not-polarity, never party-inferred, honest-skip with written search trail (whole-record gate-pinned), 0-unsourced floor, mandatory primary-source verification pass, 3-concurrency cap, first-wave validation. MO fetch-wall: SoS ASPX unfetchable → Wikipedia/Ballotpedia/news. Embed the exact 1-5 scale texts from the scaffold's _TOPIC_SCALE_FULL.txt per agent prompt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research batch-B MO stances (remaining filers) and push</name>
  <read_first>
    - .claude/skills/ (research-stances — 3-concurrency cap, chairs-not-polarity, 0-unsourced, honest-skip with written trail)
    - backend/data/stance-research/mo-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 162-03-SUMMARY.md (batch-A covered list — batch B is the complement)
    - 162-02-SUMMARY.md (MO candidate external_ids)
  </read_first>
  <action>
    Determine batch B = the 58 MO new candidates minus those covered in 162-03 batch A. Dispatch stance-research agents at 3-concurrency MAX; validate the first wave before dispatching more. Embed the exact 1-5 scale texts from the scaffold's _TOPIC_SCALE_FULL.txt in every agent prompt. Score chairs-not-polarity; never infer from party; honest-skip per-topic or whole-record ONLY with a written search trail (whole-record gate-pinnable). Every answer pairs to a real fetched source_url (MO SoS ASPX unfetchable → Wikipedia/Ballotpedia/news). Run a mandatory primary-source verification pass. Write 162-04-mo-stances-B.csv, then merge + push to PROD via the scaffold's _push.ts / _push_uuid.ts. Push to PROD as it completes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians p WHERE p.external_id BETWEEN -290899 AND -290101 AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id = p.id) AND NOT EXISTS (SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id = p.id)"</automated>
  </verify>
  <acceptance_criteria>
    - Every new MO candidate (-290899..-290101) has ≥1 sourced stance OR a gate-pinned whole-record skip (the query counts candidates with neither answers nor context — pinned whole-record skips are the only allowed non-zero, and must be enumerated in the SUMMARY)
    - 0 unsourced answers among pushed batch-B rows
    - Stance agents ran at ≤3 concurrency; a primary-source verification pass was performed before push
  </acceptance_criteria>
  <done>MO batch-B stances sourced, verified, pushed; MO's full field is stance-complete (0 unsourced).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-04-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query; chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-162-04-02 | Tampering | Duplicate push overwriting 162-03 batch-A rows | mitigate | Batch B is the strict complement of batch A (per 162-03-SUMMARY); _push.ts is idempotent per (politician, topic) |
| T-162-04-03 | Denial of service | Stance-agent rate-limit death | mitigate | 3-concurrency cap; first-wave validation |
</threat_model>

<verification>
- Every new MO candidate has sourced stances or a gate-pinned whole-record skip
- 0 unsourced answers among pushed batch-B rows
- Batch B did not re-push batch-A candidates
</verification>

<success_criteria>
MO's full 2026 House field is stance-complete on PROD (batch A + B), 0 unsourced, chairs-not-polarity, primary-source-verified — MO fully end-to-end per D-03, ready to move to MN.
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-04-SUMMARY.md when done, recording batch-B targets covered and the full list of MO gate-pinned whole-record skips (external_ids) for 162-11's verify.sql pin table.
</output>
