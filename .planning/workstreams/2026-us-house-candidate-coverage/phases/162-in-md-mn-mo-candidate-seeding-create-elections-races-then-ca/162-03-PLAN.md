---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 03
type: execute
wave: 3
depends_on: ["162-02"]
files_modified:
  - backend/data/stance-research/mo-2026-house/_merge.ts
  - backend/data/stance-research/mo-2026-house/_push.ts
  - backend/data/stance-research/mo-2026-house/_push_uuid.ts
  - backend/data/stance-research/mo-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/mo-2026-house/162-03-mo-stances-A.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "The mo-2026-house stance directory scaffold exists (_merge/_push/_push_uuid copied verbatim, _TOPIC_SCALE_FULL.txt regenerated LIVE to the federal-24 subset)"
    - "MO batch A (incumbents + evidenced majors first, incl. MO-1 Cori Bush per D-03a) have sourced chairs-not-polarity federal-24 stances, 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every batch-A answer pairs to an inform.politician_context row with a real fetched source_url; whole-record honest-skips are gate-pinned with a written search trail"
  artifacts:
    - path: "backend/data/stance-research/mo-2026-house/_TOPIC_SCALE_FULL.txt"
      provides: "Live-regenerated federal-24 topic set + exact 1-5 scale texts for MO agents"
      contains: "1"
    - path: "backend/data/stance-research/mo-2026-house/162-03-mo-stances-A.csv"
      provides: "Batch-A MO stance rows (incumbents + evidenced majors incl. Bush)"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push.ts (existing pid) / _push_uuid.ts (uuid-keyed new records)"
      pattern: "politician_context"
---

<objective>
Research + push MO stance batch A — incumbents (only the zero-tier ones; MO's incumbents are all partial-tier and OUT of scope per the 160 diagnostic, so batch A is dominated by evidenced-major challengers) plus MO's evidenced-major challengers, with MO-1 Cori Bush prioritized first (D-03a: incumbents + evidenced majors first, fringe last — minimizes discarded work if the Aug-4 primary arrives mid-phase). Set up the mo-2026-house stance directory scaffold, regenerate _TOPIC_SCALE_FULL.txt LIVE (never copy stale — Pitfall 5), run stance agents at 3-concurrency max with a mandatory primary-source verification pass, then push to PROD (stance CSVs are gitignored → PROD is the durable store, the 161 resilience lesson). Continues USHC3-05.

Purpose: MO's earliest civic moment (Aug-4) gets sourced stances before its primary. Push per state as it completes.
Output: mo-2026-house scaffold (_merge/_push/_push_uuid/_TOPIC_SCALE_FULL) + batch-A CSV pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-02-SUMMARY.md
@backend/data/stance-research/tn-2026-house/_push.ts
@backend/data/stance-research/quick-candidates-2026/_push_uuid.ts

<interfaces>
Scaffold: copy backend/data/stance-research/tn-2026-house/_push.ts and _merge.ts BYTE-FOR-BYTE into mo-2026-house/ (only the relative import path stays '../../../src/lib/db.js'; zero other changes — state-agnostic scripts). Copy quick-candidates-2026/_push_uuid.ts into mo-2026-house/ (UUID-keyed path — needed if Bush is a genuinely new record whose external_id isn't resolved yet). Regenerate _TOPIC_SCALE_FULL.txt LIVE: SELECT id, key, category FROM inform.compass_topics WHERE is_active = true ORDER BY category, key; then exclude the city-only + judicial-only + data-centers topics to leave the federal-24 subset — re-verify the total count live (was 44 in 161; do NOT assume). Embed the exact 1-5 scale texts per topic in every agent prompt. Stance standards: chairs-not-polarity (value = the exact scale position the EVIDENCE supports); never party-inferred; honest-skip (per-topic or whole-record) only with a written search trail; whole-record skips gate-pinned; 0-unsourced floor; mandatory primary-source verification pass before push. MO fetch-wall: MO SoS ASPX unfetchable → Wikipedia/Ballotpedia/news per candidate. 3-concurrency stance-agent cap; validate the first wave before dispatching more.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold mo-2026-house + regenerate the federal-24 topic scale live</name>
  <read_first>
    - 162-PATTERNS.md (Stance pipeline reuse section — copy _push.ts/_merge.ts/_push_uuid.ts verbatim; _TOPIC_SCALE_FULL.txt regenerate live, do NOT copy)
    - backend/data/stance-research/tn-2026-house/_push.ts (the state-agnostic push script)
    - 162-RESEARCH.md (Must-Answer #6 — federal-24 derivation; Pitfall 5 — stale topic risk)
  </read_first>
  <action>
    Create backend/data/stance-research/mo-2026-house/. Copy _push.ts and _merge.ts from tn-2026-house/ byte-for-byte; copy _push_uuid.ts from quick-candidates-2026/. Regenerate _TOPIC_SCALE_FULL.txt by running the LIVE query (cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql) against inform.compass_topics WHERE is_active=true, then filtering to the federal-24 subset (exclude city-only + judicial-only + data-centers) — re-verify the live total count before subtracting; write the exact 1-5 scale texts per federal topic into the file. Do NOT copy an old state's _TOPIC_SCALE_FULL.txt.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/mo-2026-house/_push.ts && test -f backend/data/stance-research/mo-2026-house/_TOPIC_SCALE_FULL.txt && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - mo-2026-house/ contains _merge.ts, _push.ts, _push_uuid.ts (verbatim copies) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt was regenerated from a live compass_topics query, filtered to the federal subset (count re-verified, not assumed 44), with exact 1-5 texts per topic
  </acceptance_criteria>
  <done>MO stance scaffold ready; the topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research batch-A stances (incumbents + evidenced majors, Bush first) and push</name>
  <read_first>
    - .claude/skills/ (research-stances — 3-concurrency cap, chairs-not-polarity, 0-unsourced, honest-skip with written search trail)
    - backend/data/stance-research/mo-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 162-02-SUMMARY.md (MO candidate external_ids / Bush disposition)
    - 160-incumbent-map.csv (confirm which MO incumbents are zero-tier vs partial-tier: MO incumbents are all partial-tier → OUT of scope)
  </read_first>
  <action>
    Identify batch-A MO targets: MO's evidenced-major challengers and MO-1 Cori Bush FIRST (D-03a). Do NOT research MO's partial-tier incumbents (all 8 are partial-tier per the diagnostic → skipped). Dispatch stance-research agents at 3-concurrency MAX; validate the first wave's output before dispatching more. Embed the exact 1-5 scale texts from _TOPIC_SCALE_FULL.txt in every agent prompt. Score chairs-not-polarity (the exact scale position the fetched EVIDENCE supports); never infer from party; honest-skip per-topic or whole-record ONLY with a written search trail (whole-record skips must be gate-pinnable). MO fetch-wall: SoS ASPX unfetchable → use Wikipedia/Ballotpedia/news; every answer must pair to a real fetched source_url. Run a mandatory primary-source verification pass. Write 162-03-mo-stances-A.csv, then merge + push to PROD via _push.ts (existing external_id) or _push_uuid.ts (uuid-keyed, e.g. Bush if new). Push MO batch A to PROD as it completes (durable store).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -290899 AND -290101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed MO batch-A rows (the query returns 0)
    - Bush and MO's evidenced-major challengers each have ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written search trail
    - Stance agents ran at ≤3 concurrency; a primary-source verification pass was performed before push
    - No partial-tier MO incumbent was topped up (out of scope)
  </acceptance_criteria>
  <done>MO batch-A stances sourced, verified, and pushed to PROD with 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data; unsourced or party-inferred rows would misrepresent a candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-03-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query; chairs-not-polarity; every answer paired to a politician_context source_url; honest-skip with written trail |
| T-162-03-02 | Tampering | Surname-leak in readrank_selected quotes | mitigate | _push.ts's built-in surname-leak guard (copied verbatim, not modified) |
| T-162-03-03 | Denial of service | Stance-agent rate-limit death from over-concurrency | mitigate | 3-concurrency cap; validate first wave before dispatching more (standing preference) |
</threat_model>

<verification>
- 0 unsourced answers among pushed MO batch-A rows
- Batch-A evidenced-majors + Bush have sourced federal-24 stances or gate-pinned skips with written trails
- Scaffold (_push/_merge/_push_uuid/_TOPIC_SCALE_FULL) present; topic scale live-regenerated
</verification>

<success_criteria>
MO's incumbents-skipped, evidenced-major (Bush-first) stance batch is sourced, primary-source-verified, and pushed to PROD with 0 unsourced — the higher-value half of MO's stance load, landing before the Aug-4 primary.
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-03-SUMMARY.md when done, recording batch-A targets covered, any gate-pinned whole-record skips (with external_ids), and the live federal-24 topic count.
</output>
