---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 03
type: execute
wave: 2
depends_on: ["156-01", "156-02"]
files_modified:
  - backend/migrations/NNNN_seed_oh_2026_house_candidates.sql
  - backend/data/seed-oh-2026-house/156-03-oh-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 15 OH US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'"
    - "Every renominated OH incumbent (all 15) reuses its 154 incumbent_pid as the active is_incumbent=true candidate; the challenger(s) are new records"
    - "Every OH third-party / minor-line candidate (OH-1/9/15 Libertarian, OH-4 Independent Tamie Wilson, and any other certified minor line) is seeded as an active party-agnostic race_candidate (D-02 seed-all)"
    - "Every 'new' OH candidate is live-DB dedup-checked before insert; zero duplicate full_name within OH; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_oh_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT new OH politicians + INSERT race_candidates onto the 15 OH races authored in 156-01"
      contains: "race_candidates"
    - path: "backend/data/seed-oh-2026-house/156-03-oh-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 15 OH districts"
      min_lines: 30
  key_links:
    - from: "race_candidates.race_id"
      to: "the 15 OH House races (156-01 scaffold)"
      via: "office_id → districts.geo_id 39NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (154-incumbent-map.csv) or newly-inserted politician id (live-confirmed)"
      via: "156-03-oh-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 15 OH US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every incumbent / previously-seeded figure — after a MANDATORY per-candidate live-DB dedup check (D-03). Near-verbatim repeat of Phase 155's PA records plan (155-03), retargeted to OH (fips=39, geo 39NN).

Purpose: USHC2-02 + USHC2-03 for OH. OH is all-renominated: all 15 incumbents are the 2026 nominees and are reused as is_incumbent=true actives. The ~19 new records are the challengers + OH's third-party/minor-line candidates (D-02 seed-all).
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every OH House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates schema (verified in 150-03/155-03):
     id uuid PK; race_id uuid NOT NULL FK->races; politician_id uuid (D-06: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL -> feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite source_url); external_id text optional. NO unique constraint -> idempotency app-enforced:
     check (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool.
     NO updated_at column (149-01 Rule-3 fix). -->
<!-- OH renominated incumbents (reuse incumbent_pid from 154-incumbent-map.csv): OH-1..15, all 15 (geo 3901..3915). -->
<!-- external_id band for new OH challengers (D-06): -(39*10000 + cd*100 + seq) -> e.g. OH-1 seq1 = -390101.
     OH incumbents occupy -39001..-39015. Verify the -390000..-399999 band (excl. the 15 incumbent ids) is
     collision-free vs live external_id<0 before insert. -->
<!-- D-02 seed-all OH minor lines: OH-1/9/15 Libertarian; OH-4 Independent Tamie Wilson; plus any other certified
     Nov-3 general minor candidate. Seed every ballot-qualified general candidate; never store party on the card. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per OH candidate + re-confirm certified generals (D-03 mandatory)</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (OH rows geo_id 39NN: general_candidates, new_records_needed, nominee_status, incumbent_pid; relax-parse names with commas/quotes)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (OH geo_id → incumbent_pid + external_id, all 15 incumbents)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-PLAN.md (the validated reuse-vs-new reconciliation pattern + external_id-band collision check + live-name-query dedup mechanics)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md (the PA all-renominated reuse pattern — OH mirrors PA, all incumbents renominated)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-03 dedup + certified-general re-confirm; D-02 seed-all minor OH lines; OH fips=39)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-oh-2026-house/156-03-oh-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 15 OH districts (~30+ rows incl. minor lines). Resolve race_id by joining the 156-01 OH races to districts.geo_id.

    Re-confirm certified Nov-3 general nominees per district from the OH Secretary of State certified ballot list (D-03) before deciding rows — the 154 general_candidates column reflects the field as of diagnostic time; seed only ballot-qualified general candidates (D-02: seed ALL ballot-qualified incl. Libertarian/Independent minor lines; drop only genuinely-uncertified). Parse the party in parens for context only, NEVER store it (D-06).

    For EACH candidate:
    1. If the name matches the district's renominated OH incumbent (all 15) → REUSE, target = the 154 incumbent_pid, is_incumbent=true.
    2. For every challenger / minor-line / new name → run a LIVE name query against essentials.politicians (full_name ILIKE + sanity on external_id/office context). If a real matching record exists → decision=REUSE with that UUID. If 0 matches → decision=NEW, assign a collision-free negative external_id.
    3. Confirm 0 renominated incumbent appears as inactive; every OH incumbent has exactly one active is_incumbent=true row.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id. OH incumbents occupy -39001..-39015. Use the scheme -(39*10000 + cd*100 + seq) (e.g. OH-1 seq1 = -390101) and VERIFY the -390000..-399999 band (excluding the 15 incumbent ids) is empty before use; if any collision, pick another verified-empty band and document it (149-01 collision lesson).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-oh-2026-house/156-03-oh-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<25){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; console.log('OH reconciliation OK rows',body.length,'new',newids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 156-03-oh-reconciliation.csv has one row per OH general candidate across 15 districts (~30+ rows incl. D-02 minor lines), each with a resolved race_id, re-confirmed against the OH SoS certified ballot list.
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred); all 15 renominated incumbents REUSE their 154 incumbent_pid.
    - OH-1/9/15 Libertarian + OH-4 Independent (Tamie Wilson) + any other certified minor line are present as NEW (or REUSE if pre-existing) — no minor candidate dropped (D-02).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live external_id<0 set; band verified empty.
  </acceptance_criteria>
  <done>oh-reconciliation.csv exists, all REUSE UUIDs live-confirmed, certified generals + minor lines re-confirmed, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent OH House seed migration (new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-oh-2026-house/156-03-oh-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics — INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at; no challenger offices)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md (the OH election UUID — confirm races resolve to it)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-06 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk). Name `NNNN_seed_oh_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (154-FIELD-TABLE.md OH section + per-district source URLs) + the antipartisan invariant (D-06).

    Migration body, in order (per the 155-03 Task-2 mechanics):
    1. INSERT the new OH politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers (a challenger office would pollute the reps feed / geofence search — 149-01 A2).
    2. INSERT the race_candidates onto the 15 OH races: for each reconciliation row, race_id=resolved OH race, politician_id=REUSE target or just-inserted new UUID (resolve new via SELECT id … WHERE external_id=…), full_name/first_name/last_name, is_incumbent per CSV, candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name).
    3. NEVER touch the races rows (156-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate.

    Post-COMMIT verification block (or run separately), scoped to OH NATIONAL_LOWER (geo_id prefix '39') within the OH 2026 Statewide General election: every OH race has ≥1 active candidate (most ≥2); 0 NULL politician_id; 0 duplicate full_name within OH; all 15 renominated incumbents present as is_incumbent=true; OH-1/9/15 + OH-4 minor lines present.

    Run dry first (review SQL), then apply against prod via psql -v ON_ERROR_STOP=1. Do not inline the SQL here — author per the 155-03 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='OH 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='39'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='OH 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='39' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==15||+row.nullpid>0||+dup.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n);process.exit(1)}; console.log('OH OK: 15 races,',row.active_rc,'active rc, 0 null pid, 0 dup full_name'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 OH House races (NATIONAL_LOWER geo '39' within the OH 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within OH (D-03 zero-dup-per-state).
    - All 15 renominated incumbents present as is_incumbent=true; OH-1/9/15 Libertarian + OH-4 Independent minor lines active (D-02).
    - No new essentials.offices rows created; no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; OH 15-race assertion passes; dedup correct; minor lines seeded; idempotent on re-run. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes new OH politicians + race_candidates to live prod |
| 154 field artifacts → seed input | Field/identity locked + human-reviewed in Phase 154; reuse-vs-new + certified-general re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-12 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Live name-check per candidate; reuse 154 incumbent_pid; 0-dup-full_name-per-state gate |
| T-156-13 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-156-14 | DoS | write to wrong election / cross-state contamination | mitigate | All inserts scoped to OH 2026 Statewide General + NATIONAL_LOWER geo '39' |
| T-156-15 | Tampering | minor-party candidate silently dropped | mitigate | D-02 seed-all; reconciliation includes OH-1/9/15 + OH-4 lines; gate asserts presence |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 156-verify.sql USHC2-02/03 + D-02/D-03 OH assertions pass after this runs.
- Manual spot-check: cross-check ~5 OH districts against 154-FIELD-TABLE.md source_url citations.
</verification>

<success_criteria>
- 15 OH House races each carry their active field, all politician_id non-null; ~19 new politician rows; every incumbent reused by id (0 duplicates); OH minor lines seeded; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list for downstream headshot/stance plans, any 154-name flips/certified-general changes, minor-line confirmations).
</output>
