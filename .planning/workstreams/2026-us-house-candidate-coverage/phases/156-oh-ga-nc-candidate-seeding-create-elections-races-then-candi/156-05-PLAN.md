---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 05
type: execute
wave: 2
depends_on: ["156-01", "156-02"]
files_modified:
  - backend/migrations/NNNN_seed_nc_2026_house_candidates.sql
  - backend/data/seed-nc-2026-house/156-05-nc-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 14 NC US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'"
    - "Every renominated NC incumbent (all 14, incl. NC-6 Addison P. McDowell -37006) reuses its 154 incumbent_pid as the active is_incumbent=true candidate; the challenger(s) are new records"
    - "Every NC third-party / minor-line candidate (Libertarian in NC-1/2/3/4/5/7/10/11/13, Green in NC-13, Independent John Rogers in NC-11) is seeded as an active party-agnostic race_candidate (D-02 seed-all)"
    - "Every 'new' NC candidate is live-DB dedup-checked before insert; zero duplicate full_name within NC; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_nc_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT new NC politicians + INSERT race_candidates onto the 14 NC races authored in 156-01"
      contains: "race_candidates"
    - path: "backend/data/seed-nc-2026-house/156-05-nc-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 14 NC districts (heavy third-party field)"
      min_lines: 30
  key_links:
    - from: "race_candidates.race_id"
      to: "the 14 NC House races (156-01 scaffold)"
      via: "office_id → districts.geo_id 37NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (154-incumbent-map.csv, incl. NC-6 McDowell -37006) or newly-inserted politician id (live-confirmed)"
      via: "156-05-nc-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 14 NC US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every renominated incumbent — after a MANDATORY per-candidate live-DB dedup check (D-03). NC has the heaviest third-party field of the three states (Libertarian in 9 districts, a Green, an Independent). Mirrors Phase 155's PA/IL records plans, retargeted to NC (fips=37, geo 37NN). NOTE: NC-6 McDowell (-37006) is reused as an active incumbent HERE (records/wiring); his ZERO-stance status makes him the only in-scope incumbent for STANCES (handled in 156-09), not here.

Purpose: USHC2-02 + USHC2-03 for NC. NC is all-renominated: all 14 incumbents are the 2026 nominees and are reused as is_incumbent=true actives. The ~25 new records are the challengers + NC's heavy third-party/minor-line field (D-02 seed-all).
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every NC House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates schema (verified in 150-03/155-03): id uuid PK; race_id uuid NOT NULL;
     politician_id uuid (D-06: non-null for House); full_name/first_name/last_name; photo_url NULL;
     is_incumbent bool; candidate_status 'active'; source. NO unique constraint -> (race_id, full_name) NOT EXISTS. -->
<!-- essentials.politicians: id uuid, external_id int (negative), full_name, first_name, last_name, is_active. NO updated_at. -->
<!-- NC renominated incumbents (REUSE incumbent_pid from 154-incumbent-map.csv): NC-1..14, all 14 (geo 3701..3714).
     NC-6 Addison P. McDowell 74579547-1454-475e-ab35-12cf88a998b9/-37006 IS renominated -> REUSE is_incumbent=true HERE.
     (His zero-stance status is handled in 156-09 stances, NOT this records plan.) -->
<!-- external_id band for new NC challengers (D-06): -(37*10000 + cd*100 + seq) -> e.g. NC-1 seq1 = -370101.
     NC incumbents occupy -37001..-37014. Verify -370000..-379999 band (excl. incumbent ids) collision-free before insert. -->
<!-- D-02 seed-all NC minor lines: Libertarian in NC-1/2/3/4/5/7/10/11/13; Green in NC-13; Independent John Rogers in NC-11;
     plus any other certified Nov-3 general minor candidate. Seed every ballot-qualified general candidate; never party on card. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per NC candidate + re-confirm certified generals + heavy third-party field (D-03/D-02 mandatory)</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (NC rows geo_id 37NN: general_candidates incl. the Libertarian/Green/Independent lines, new_records_needed, nominee_status, incumbent_pid; relax-parse names)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (NC geo_id → incumbent_pid + external_id, all 14 incumbents incl. NC-6 McDowell -37006)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-PLAN.md (the validated reuse-vs-new reconciliation + external_id-band collision check + live-name-query dedup mechanics)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md (the all-renominated reuse pattern — NC mirrors PA, all 14 incumbents renominated)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-03 dedup + certified-general re-confirm; D-02 seed-all NC third-party lines; D-01 NC-6 McDowell reused as active incumbent here, stanced in 156-09; NC fips=37)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-nc-2026-house/156-05-nc-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 14 NC districts (~30+ rows incl. the heavy minor-line field). Resolve race_id by joining the 156-01 NC races to districts.geo_id.

    Re-confirm certified Nov-3 general nominees per district from the NC State Board of Elections certified ballot list (D-03) before deciding rows. D-02: seed ALL ballot-qualified general candidates incl. every Libertarian/Green/Independent line; drop only genuinely-uncertified. Parse party for context only, NEVER store it (D-06).

    For EACH candidate:
    1. If the name matches the district's renominated NC incumbent (all 14) → REUSE, target = the 154 incumbent_pid, is_incumbent=true. NC-6 Addison P. McDowell (-37006) IS renominated → REUSE is_incumbent=true (his stances come in 156-09; here he is just wired as active).
    2. For every challenger / minor-line / new name → run a LIVE name query against essentials.politicians. If a real matching record exists → REUSE with that UUID. If 0 matches → decision=NEW, assign a collision-free negative external_id.
    3. Confirm every NC incumbent has exactly one active is_incumbent=true row (incl. McDowell).

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id. NC incumbents occupy -37001..-37014. Use -(37*10000 + cd*100 + seq) (e.g. NC-1 seq1 = -370101) and VERIFY the -370000..-379999 band (excluding the 14 incumbent ids) is empty before use; document any collision remap (149-01 lesson).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-nc-2026-house/156-05-nc-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<25){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; const nc6=body.map(r=>r.split(',')).find(c=>c[1]==='3706' && c[8]==='true'); if(!nc6){console.error('FAIL NC-6 McDowell must have an is_incumbent=true row');process.exit(1)}; console.log('NC reconciliation OK rows',body.length,'new',newids.length,'NC-6 incumbent row present')"</automated>
  </verify>
  <acceptance_criteria>
    - 156-05-nc-reconciliation.csv has one row per NC general candidate across 14 districts (~30+ rows incl. D-02 third-party lines), each with a resolved race_id, re-confirmed against the NC SBE certified ballot list.
    - Every REUSE row's target_politician_id is a real live UUID; all 14 renominated incumbents REUSE their 154 incumbent_pid; NC-6 McDowell (-37006) REUSE is_incumbent=true.
    - Every certified Libertarian (NC-1/2/3/4/5/7/10/11/13), Green (NC-13), and Independent (NC-11 John Rogers) line present — no minor candidate dropped (D-02).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live external_id<0 set; band verified empty.
  </acceptance_criteria>
  <done>nc-reconciliation.csv exists, all REUSE UUIDs live-confirmed (incl. McDowell), certified generals + third-party lines re-confirmed, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent NC House seed migration (new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-nc-2026-house/156-05-nc-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics — INSERT politicians WHERE NOT EXISTS + INSERT race_candidates WHERE NOT EXISTS; canonical accents; no updated_at; no challenger offices)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md (the NC election UUID — confirm races resolve to it)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-06 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk). Name `NNNN_seed_nc_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (154-FIELD-TABLE.md NC section + per-district source URLs) + the antipartisan invariant (D-06).

    Migration body, in order (per the 155-03 Task-2 mechanics):
    1. INSERT the new NC politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers (149-01 A2).
    2. INSERT the race_candidates onto the 14 NC races: for each reconciliation row, race_id=resolved NC race, politician_id=REUSE target or just-inserted new UUID, full_name/first_name/last_name, is_incumbent per CSV (NC-6 McDowell is_incumbent=true), candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name).
    3. NEVER touch the races rows (156-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate.

    Post-COMMIT verification block (or run separately), scoped to NC NATIONAL_LOWER (geo prefix '37') within the NC 2026 Statewide General election: every NC race has ≥1 active candidate (most ≥2); 0 NULL politician_id; 0 duplicate full_name within NC; all 14 renominated incumbents present as is_incumbent=true (incl. NC-6 McDowell); the third-party lines (NC-1/2/3/4/5/7/10/11/13 + NC-13 Green + NC-11 Independent) present.

    Run dry first (review SQL), then apply against prod via psql -v ON_ERROR_STOP=1. Do not inline the SQL here — author per the 155-03 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='NC 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='37'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='NC 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='37' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==14||+row.nullpid>0||+dup.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n);process.exit(1)}; console.log('NC OK: 14 races,',row.active_rc,'active rc, 0 null pid, 0 dup full_name'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 14 NC House races (NATIONAL_LOWER geo '37' within the NC 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within NC (D-03 zero-dup-per-state).
    - All 14 renominated incumbents present as is_incumbent=true (incl. NC-6 McDowell); NC third-party/minor lines active (D-02).
    - No new essentials.offices rows created; no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; NC 14-race assertion passes; dedup correct; third-party lines seeded; McDowell wired as active incumbent; idempotent. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes new NC politicians + race_candidates to live prod |
| 154 field artifacts → seed input | Field/identity locked in Phase 154; reuse-vs-new + certified-general re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-21 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Live name-check per candidate; reuse 154 incumbent_pid; 0-dup-full_name-per-state gate |
| T-156-22 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-156-23 | DoS | write to wrong election / cross-state contamination | mitigate | All inserts scoped to NC 2026 Statewide General + NATIONAL_LOWER geo '37' |
| T-156-24 | Tampering | heavy NC third-party field silently dropped | mitigate | D-02 seed-all; reconciliation includes all 9 Libertarian + Green + Independent lines; gate asserts presence |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 156-verify.sql USHC2-02/03 + D-02/D-03 NC assertions pass after this runs.
- Manual spot-check: cross-check ~5 NC districts (incl. NC-6 + a third-party-heavy district) against 154-FIELD-TABLE.md citations.
</verification>

<success_criteria>
- 14 NC House races each carry their active field, all politician_id non-null; ~25 new politician rows; every incumbent reused by id (0 duplicates, incl. McDowell); NC third-party lines seeded; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-05-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list, third-party-line confirmations, NC-6 McDowell active-incumbent-wiring confirmation for 156-09, any 154-name flips).
</output>
