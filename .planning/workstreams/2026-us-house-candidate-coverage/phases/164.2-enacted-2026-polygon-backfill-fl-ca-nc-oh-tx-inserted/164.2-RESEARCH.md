# DRAFT — Phase 164.2: Enacted-2026 Congressional Polygon Backfill (FL/CA/NC/OH/TX)

**Status:** DRAFT for Chris review (2026-07-21). Not yet added to ROADMAP / planned via gsd.
**Workstream:** 2026-us-house-candidate-coverage. Sibling to 164.1 (same dual-map family).

## Goal
`/elections` resolves the enacted-2026 congressional map for **FL, CA, NC, OH, TX** (both anonymous Path-B and Connected-tier), while the reps feed ("who represents you now") stays on current boundaries until the Jan-2027 promotion phase. Each state passes the 164.1 D-10 verify bar. ~147 districts (FL28/CA52/NC14/OH15/TX38).

## Why (evidence — 2026-07-21 audit)
All 5 states enacted new 2026 maps but have NO `G5200V26` rows; `/elections` resolves the OLD map (confirmed via ST_Contains: e.g. TX Liberty→CD36 not new CD9; CA Redding→CD1 not new CD2; OH Clinton→CD2 not new CD1; NC Morehead→CD3 not new CD1; FL South-FL incumbents). 164.1 covered only TN/MO/AL/LA/UT. See [[project_fl_2026_redistricting_polygon_gap]].

## Reuse (already built in 164.1 — no rebuild)
- **Anon Path B opt-in JOIN** (`electionService.ts` L294-309): GENERIC — auto-prefers V26 for any geo_id with a V26 row, else G5200. **No code change; auto-applies once rows land.** (Add a regression test asserting the 5 new states resolve V26.)
- **D-10 verify harness**: `scripts/1641-verify.sql` (topology/anchor/NOTOUCH) + `scripts/1641-coordinate-smoke.ts` (differential). Reuse per state.
- **Reps-feed exclusion** (`essentialsService`/`geoIdGuard`/`essentialsBrowseService`): correctly excludes V26 — NO CHANGE (reps stay on G5200 until Jan 2027).

## Plans (proposed)
- **164.2-01 — Connected-tier allowlist extend + deploy.** Add FIPS `12,06,37,39,48` to `REFRESHED_2026_FIPS` (`src/routes/essentials.ts:55`) AND to `connect.resolve_congressional_2026` RPC (new migration = CREATE OR REPLACE of mig 1246 with expanded IN-list). Render deploy. Regression test: anon Path B resolves V26 for all 5.
- **164.2-02 — Per-state enacted-shapefile import as `G5200V26`.** One idempotent migration per state → `essentials.geofence_boundaries` (geo_id `12xx/06xx/37xx/39xx/48xx`, mtfcc `G5200V26`, geometry reprojected EPSG:4326, `source`+`imported_at` set; NOT EXISTS on (geo_id,mtfcc)). Sources: **TX = PlanC2333 (ALREADY in scratchpad: old.geojson/new.geojson)**; CA = Statewide DB / WeDrawTheLines (Prop 50); NC = NCGA enacted 2025; OH = Ohio Redistricting Commission (Oct-31-2025); FL = FL Legislature (signed May-4-2026). Verify each is the correct enacted plan + full district count.
- **164.2-03 — D-10 verify per state.** Reuse 1641 harness. Anchor coordinates ALREADY sourced this session (reuse as smoke anchors): CA Redding/Palm Springs/Indian Wells; NC Greene/Morehead/Kill Devil Hills; OH Clinton/Henry/Kent; TX Liberty/Rockwall/Mission; FL Wasserman Schultz/Moskowitz/Frankel zones. Each must resolve NEW district under V26; NOTOUCH asserts G5200 unchanged.
- **164.2-04 — Candidate-field vintage cross-check ✅ RESOLVED 2026-07-21.** All 5 states' candidate seeds VERIFIED NEW-MAP (agent + source): FL (incumbents in new districts); CA (Calvert gone CD-41→Sánchez, Issa gone CD-48→von Wilpert); TX (Al Green gone TX-9, Julie Johnson gone TX-32); NC (Asa Buck Carteret→CD-1, Raymond Smith Jr Wayne→CD-3); OH (primaries run under Oct-2025 map, rosters match). → **NO candidate re-seed needed; fix is POLYGON-ONLY for all 5.** Small candidate-field nits only: FL repairs (164.2-05) + CA-1 wrongly flags James Gallagher incumbent (open/redrawn seat).
- **164.2-05 — FL candidate-field repairs** (small, from this session's audit; can run independently first): set is_incumbent on Wasserman Schultz(FL-20)/Frankel(FL-23)/Moskowitz(FL-25); rename "Kedner MaximeDe"→"Kedner Maxime", "Seth Haskins"→"Seth Haskin"; FL-11 Webster untangle (rename -12011→Daniel Webster + drop from active race + add Royal Webster D); add D10 4 GOP + D6 Gist + D11 Wilnau/Harden Hall. FL field audited otherwise CLEAN.

## Open questions / risks
1. ✅ CLOSED — candidate-field vintage: ALL 5 (FL/CA/NC/OH/TX) = NEW_MAP verified → POLYGON-ONLY fix, no re-seed. (Was the highest unknown; now retired.) Small candidate nits only: FL repairs + CA-1 Gallagher incumbent flag.
2. Shapefile provenance/quality per state (correct enacted plan, clean reprojection, no self-intersections → ST_IsValid).
3. Jan-2027 promotion phase must add these 5 states to its re-entry list (currently only 164.1's 5).
4. Existing Connected users' cached `congressional_geo_id` in these states — live fallback (164.2-01) covers read-time; no cache rewrite until promotion.

## Sequencing
FL first (Aug-18 primary + repairs pending) → TX (shapefile in hand) → CA/NC/OH/TX. Independent of Phase 166 gate and 164.1-07 (MO). Not date-blocked (do now).

## Goal-backward verification
Per state: ≥3 anchor coords resolve to NEW district via /elections (anon + connected) AND reps feed still returns current district AND 1641-verify.sql passes (valid topology, full coverage, G5200 NOTOUCH).
