---
phase: 164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/1642-verify.sql
  - backend/scripts/1642-fl-generate.mts
  - backend/scripts/1642-tx-generate.mts
  - backend/scripts/1642-ca-generate.mts
  - backend/scripts/1642-nc-generate.mts
  - backend/scripts/1642-oh-generate.mts
autonomous: true
requirements: [D-01, D-04, D-08, D-09]
must_haves:
  truths:
    - "Every FL (12xx), CA (06xx), NC (37xx), OH (39xx), TX (48xx) congressional district has a 2026-vintage G5200V26 polygon in essentials.geofence_boundaries (147 rows: FL 28 + CA 52 + NC 14 + OH 15 + TX 38)"
    - "Each state's G5200V26 union tiles that state's G4000 outline (uncovered < 2%) and every V26 geometry is ST_IsValid + non-empty"
    - "Each import is NEW-map identity verified before insert: a known anchor point resolves to its NEW district under V26, differing from the OLD (G5200) district (e.g. TX Liberty -94.7955,30.0577 -> new 4809 not old 4836)"
    - "essentials.offices, essentials.geo_districts, and connect.user_districts for FIPS 12/06/37/39/48 are byte-identical after import (D-04 NOTOUCH md5 baselines captured pre-import hold)"
    - "1642-verify.sql exits 0 GREEN with all 5 states' Layer-1 tiling + area-ratio + Layer-2 anchor + D-04 NOTOUCH blocks passing"
  artifacts:
    - path: "backend/scripts/1642-verify.sql"
      provides: "D-10 topology + NOTOUCH gate for FL/CA/NC/OH/TX (clone of 1641-verify.sql)"
      contains: "G5200V26"
    - path: "backend/scripts/1642-tx-generate.mts"
      provides: "TX 2026 (PlanC2333) polygon import into geofence_boundaries as G5200V26"
      contains: "G5200V26"
    - path: "backend/scripts/1642-fl-generate.mts"
      provides: "FL 2026 enacted polygon import into geofence_boundaries as G5200V26"
      contains: "G5200V26"
  key_links:
    - from: "backend/scripts/1642-*-generate.mts"
      to: "essentials.geofence_boundaries (mtfcc='G5200V26')"
      via: "ON CONFLICT (geo_id, mtfcc) DO NOTHING parameterized insert"
      pattern: "G5200V26"
---

<objective>
Import the enacted-2026 congressional polygons for FL (28), CA (52), NC (14), OH (15), and TX (38) into `essentials.geofence_boundaries` as a parallel `mtfcc='G5200V26'` vintage — 147 rows total — and stand up the `1642-verify.sql` D-10 topology + D-04 NOTOUCH gate (a clone of `1641-verify.sql` retargeted to FIPS 12/06/37/39/48). This is a polygon-only backfill: candidate fields are already new-map in all five states, so NO candidate re-seed happens here. Because the dual-map opt-in JOIN and the reps-feed exclusion were already deployed in Phase 164.1 and are GENERIC, anonymous `/elections` auto-resolves the new map the moment these rows land — no code change is required for the anonymous path.

Purpose: Close the live geographic-accuracy bug where `/elections` still resolves the OLD congressional map for ~147 districts in five states that enacted new maps. FL is sequenced first (Aug-18 primary); TX second (shapefile already fetched). The reps feed stays on current boundaries (G5200) until the Jan-2027 promotion phase (D-02, unchanged).
Output: One `1642-verify.sql` gate (pre-import NOTOUCH baselines pinned) + five per-state import scripts + 147 G5200V26 rows, all GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-RESEARCH.md

<interfaces>
<!-- Reference the proven 164.1 import + verify pattern. Read these to clone; do not re-derive. -->
Clone target (import script): backend/scripts/1641-tn-generate-polygon-import.mts — the canonical pattern: dotenv + pg Pool with ssl.rejectUnauthorized=false, --dry-run flag, DATABASE_URL guard, constants (MTFCC='G5200V26', SOURCE, STATE_FIPS, EXPECTED), fetch/parse GeoJSON, map source District -> geo_id, parameterized INSERT into essentials.geofence_boundaries ON CONFLICT (geo_id, mtfcc) DO NOTHING with ST_SetSRID(ST_Force2D(ST_GeomFromGeoJSON(...)),4326). The essentials.geo_districts insert of the base loader (load-national-house-districts.ts) is INTENTIONALLY OMITTED (D-04 / Pitfall-2).
Clone target (verify gate): backend/scripts/1641-verify.sql — per-state Layer-1 tiling (union vs G4000 outline, TILING_TOL_FRAC=0.02), Layer-1 area-ratio [0.02,50], Layer-2 anchor (ST_IsValid + ST_Covers of ST_PointOnSurface), and three D-04 NOTOUCH md5 blocks (offices / geo_districts / connect.user_districts). Skip-if-no-V26-rows = GREEN no-op; partial import (0 < n != expected) = hard FAIL.

geo_id scheme (congressional NATIONAL_LOWER): 2-digit state FIPS + 2-digit zero-padded district. FL District 20 -> '1220'; CA 41 -> '0641'; NC 3 -> '3703'; OH 1 -> '3901'; TX 9 -> '4809'. FIPS: FL=12, CA=06, NC=37, OH=39, TX=48. Expected counts: FL=28, CA=52, NC=14, OH=15, TX=38.
essentials.geofence_boundaries columns: geo_id, ocd_id, name, state, mtfcc, geometry (SRID 4326), source, valid_from, valid_to, imported_at, quality_flag. Set state=STATE_FIPS (the 164.1 verify count uses `WHERE mtfcc='G5200V26' AND state='47'`), source=per-state provenance string, imported_at=now(), mtfcc='G5200V26'. essentials.districts already has matching NATIONAL_LOWER geo_id rows — no new district rows.

Shapefile sources (enacted 2026 plans — confirm the exact artifact + NEW-map identity at execution time):
- TX = PlanC2333 (Texas Legislative Council). ALREADY fetched + reprojected to EPSG:4326: the session scratchpad `new.geojson` (38 features, single property `District` = 1..38). If that scratchpad file is not present in the execution session, re-fetch PlanC2333 from the TLC redistricting data portal (data.capitol.texas.gov) and reproject. SOURCE='tx_tlc_planc2333_2026'.
- FL = Florida Legislature enacted plan (signed 2026-05-04). Prefer the official FL redistricting GIS artifact (flsenate.gov / myfloridahouse.gov redistricting, or FGDL). SOURCE='fl_legislature_2026'.
- CA = California Statewide Database / WeDrawTheLines Prop-50 enacted plan (Nov-2025). SOURCE='ca_swdb_prop50_2026'.
- NC = North Carolina General Assembly enacted 2025 congressional plan. SOURCE='nc_ncga_2025'.
- OH = Ohio Redistricting Commission enacted plan (adopted 2025-10-31). SOURCE='oh_orc_2025'.

NEW-map identity anchors (each MUST resolve to the NEW district under the imported polygons, differing from OLD): TX Liberty (-94.7955,30.0577) old 4836 -> new 4809 | CA Redding (-122.3775,40.5922) old 0601 -> new 0602 | NC Morehead City (-76.7261,34.7226) old 3703 -> new 3701 | OH Wilmington (-83.8286,39.4453) old 3902 -> new 3901 | FL: South-FL incumbents shifted (Wasserman Schultz FL-20, Frankel FL-23, Moskowitz FL-25) — derive a differential anchor from the enacted map at execution and confirm it flips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-import NOTOUCH baselines and author 1642-verify.sql</name>
  <read_first>
    - backend/scripts/1641-verify.sql (full — clone its structure, block-for-block)
  </read_first>
  <action>
    FIRST capture the D-04 NOTOUCH baselines LIVE, BEFORE any polygon import, so the gate genuinely proves the imports never wrote offices/geo_districts. For each of FIPS 12/06/37/39/48 compute: (a) essentials.offices per-row md5 over `o.id::text||':'||coalesce(o.district_id::text,'')||':'||coalesce(o.representing_state,'')` ORDER BY o.id for NATIONAL_LOWER districts with length(geo_id)=4 and matching FIPS prefix; (b) essentials.geo_districts per-row md5 over `g.geoid||':'||g.district_num` ORDER BY g.geoid for layer='us_house' geoid LIKE fips||'%'; (c) connect.user_districts DISTINCT (layer,geoid) SET md5 + cardinality + row count for layer='us_house' substr(geoid,1,2) IN the five FIPS. Record each value with a dated `-- captured live YYYY-MM-DD, pre-import` comment.
    Then clone backend/scripts/1641-verify.sql to backend/scripts/1642-verify.sql, retargeting the STATES VALUES list to `('12','FL',28),('06','CA',52),('37','NC',14),('39','OH',15),('48','TX',38)` in every DO block, and replacing the three NOTOUCH baseline VALUES tables with the literals captured above. Keep TILING_TOL_FRAC=0.02, the area-ratio bounds [0.02,50], the skip-if-no-V26-rows NOTICE, and the partial-import hard FAIL exactly. Update the header prose (states, FIPS, capture date) and the final `SELECT '1642-verify GREEN'`. All PostGIS calls use the `public.` prefix. Run it once now: with zero G5200V26 rows for these states it must exit 0, printing SKIP for all five (GREEN no-op) and PASS for the three NOTOUCH blocks.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && grep -q "G5200V26" scripts/1642-verify.sql && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1642-verify.sql | grep -q "1642-verify GREEN" && echo VERIFY_SCAFFOLD_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/1642-verify.sql exists, STATES list = FL/CA/NC/OH/TX with expected counts 28/52/14/15/38
    - The three D-04 NOTOUCH baselines are captured live PRE-import and pinned with a dated comment
    - Running it now exits 0: five SKIP notices (no V26 rows yet) + three NOTOUCH PASS + `1642-verify GREEN`
  </acceptance_criteria>
  <done>1642-verify.sql authored with pre-import NOTOUCH baselines; GREEN no-op before any import.</done>
</task>

<task type="auto">
  <name>Task 2: Import all five states' enacted-2026 polygons as G5200V26</name>
  <read_first>
    - backend/scripts/1641-tn-generate-polygon-import.mts and backend/scripts/1641-ut-generate-polygon-import.mts (clone the download/parse/insert body; UT proves a legit large shape change so wide area-ratio bounds are expected)
    - 164.2-RESEARCH.md "Reuse" + the source list in <interfaces> above
  </read_first>
  <action>
    Author one import script per state cloned from 1641-tn-generate-polygon-import.mts: backend/scripts/1642-{tx,fl,ca,nc,oh}-generate.mts. Each sets MTFCC='G5200V26', its SOURCE string, STATE_FIPS, EXPECTED count, maps the source District field to geo_id (FIPS + 2-digit zero-padded district), inserts ONLY into essentials.geofence_boundaries (NO geo_districts insert — D-04) with parameterized values, ON CONFLICT (geo_id, mtfcc) DO NOTHING, geometry via `public.ST_SetSRID(public.ST_Force2D(public.ST_GeomFromGeoJSON($n)),4326)`, and preserves --dry-run + the DATABASE_URL guard. Header documents provenance + the geo_districts-omitted note.
    - TX: read the reprojected PlanC2333 GeoJSON (session scratchpad `new.geojson` if present, else re-fetch PlanC2333 from the TLC portal and reproject to 4326). 38 features, `District` 1..38 -> geo_ids 4801..4838.
    - FL/CA/NC/OH: confirm the exact enacted artifact per the source list, reproject to EPSG:4326 if the source is not already 4326.
    Order: FL first (Aug-18 primary), then TX, then CA, NC, OH. For EACH state, BEFORE the real insert: run --dry-run (expect the exact count) AND run the NEW-map identity anchor probe from <interfaces> — confirm the state's anchor point falls in its NEW district and differs from the OLD (G5200) district; if a state's anchor does not flip, STOP and re-confirm the source is the enacted plan (do not insert a wrong/old map). Then run the real import. Parameterize every insert (V5 — never string-interpolate shapefile-derived text or coordinates).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT string_agg(state||':'||c::text, ',' ORDER BY state) FROM (SELECT state, count(*) c FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state IN ('12','06','37','39','48') GROUP BY state) s" | grep -qx "06:52,12:28,37:14,39:15,48:38" && echo ALL_147_ROWS</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 28 FL + 52 CA + 14 NC + 15 OH + 38 TX G5200V26 rows (147 total), geo_ids covering 1201-1228 / 0601-0652 / 3701-3714 / 3901-3915 / 4801-4838
    - Each state's NEW-map identity anchor resolved to its NEW district (differs from OLD) before insert
    - No script contains a geo_districts INSERT; every insert parameterized; ON CONFLICT (geo_id,mtfcc) DO NOTHING (idempotent re-run)
    - `SELECT count(*) FROM essentials.geo_districts WHERE layer='us_house' AND (geoid LIKE '12%' OR geoid LIKE '06%' OR geoid LIKE '37%' OR geoid LIKE '39%' OR geoid LIKE '48%')` unchanged
  </acceptance_criteria>
  <done>147 G5200V26 rows imported across FL/CA/NC/OH/TX; each NEW-map verified pre-insert; geo_districts untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Pass the D-10 topology + NOTOUCH gate for all five states</name>
  <read_first>
    - backend/scripts/1642-verify.sql (from Task 1)
  </read_first>
  <action>
    Run backend/scripts/1642-verify.sql against PROD. Now that all 147 rows are present, every state's Layer-1 tiling (union vs G4000 outline, uncovered < 2%), Layer-1 area-ratio, and Layer-2 anchor (valid + non-empty + covers own ST_PointOnSurface) blocks must PASS, and the three D-04 NOTOUCH md5 blocks must still PASS (offices / geo_districts / user_districts unchanged by the imports). If a Layer-1 tiling block fails for a state, the imported plan is likely mis-projected or mis-keyed — re-check that state's source and reprojection (do not widen the tolerance to force GREEN). If a NOTOUCH block fails, an import wrote a table it must not have (Pitfall-2 breach) — fix the offending script.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1642-verify.sql | grep -q "1642-verify GREEN" && echo TOPOLOGY_NOTOUCH_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 1642-verify.sql exits 0 with Layer-1 tiling + area-ratio + Layer-2 anchor PASS for all 5 states (no SKIP, no PARTIAL)
    - All three D-04 NOTOUCH blocks PASS (offices / geo_districts / user_districts byte-identical to pre-import baselines)
  </acceptance_criteria>
  <done>D-10 topology + D-04 NOTOUCH gate GREEN for FL/CA/NC/OH/TX; the anonymous `/elections` coordinate path now resolves the enacted map (generic 164.1 opt-in, auto-applied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shapefile source -> import | Untrusted / possibly-wrong-vintage geometry inserted to geofence_boundaries |
| import script -> prod DB | A stray geo_districts/offices write would breach D-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1642-01 | Tampering / data integrity | Wrong-vintage or mis-projected polygons imported | high | mitigate | Per-state NEW-map anchor probe before insert + 1642-verify.sql Layer-1 tiling (2%) + area-ratio bounds; ST_Force2D/ST_SetSRID normalization |
| T-1642-02 | Injection (SQLi) | Shapefile-derived text/coords in inserts | high | mitigate | Fully parameterized inserts; ST_GeomFromGeoJSON via bound param |
| T-1642-03 | Tampering (D-04 breach) | Import accidentally writes offices/geo_districts | high | mitigate | geo_districts insert omitted; NOTOUCH md5 baselines captured PRE-import and asserted in Task 3 |
| T-1642-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — pg/shapefile/adm-zip already present; no supply-chain surface |
</threat_model>

<verification>
- 147 G5200V26 rows (FL 28 / CA 52 / NC 14 / OH 15 / TX 38); geo_districts + offices + user_districts unchanged.
- 1642-verify.sql GREEN: Layer-1 tiling + area-ratio + Layer-2 anchor for all 5 states + 3 NOTOUCH blocks.
- Each state's NEW-map identity confirmed by anchor differential before insert.
</verification>

<success_criteria>
FL/CA/NC/OH/TX enacted-2026 congressional polygons imported as a parallel G5200V26 vintage (147 rows), each NEW-map verified, tiling + topology + D-04 NOTOUCH all GREEN via 1642-verify.sql — the anonymous `/elections` path now resolves the enacted map for these states via the already-deployed generic 164.1 opt-in, with the reps feed untouched on G5200.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-02-SUMMARY.md` when done.
</output>
