---
phase: 164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/1380_fl_incumbent_flags_and_renames.sql
  - backend/migrations/1381_fl11_webster_untangle.sql
  - backend/migrations/1382_fl_add_candidates_and_ca1_gallagher.sql
autonomous: true
requirements: [FL-FIELD-FIX, CA-FIELD-FIX]
must_haves:
  truths:
    - "Wasserman Schultz (FL-20), Frankel (FL-23), and Moskowitz (FL-25) are flagged is_incumbent=true on their 2026 race_candidates rows (new-map incumbency)"
    - "Politician 'Kedner MaximeDe' is renamed 'Kedner Maxime' and 'Seth Haskins' is renamed 'Seth Haskin'"
    - "FL-11: external_id -12011 is renamed to 'Daniel Webster' (retains its stances), deactivated from the active FL-11 2026 race (retiring), and a new challenger 'Royal Webster' (party D) is added to the FL-11 race"
    - "The missing FL candidates are added and surface on /elections: FL-10 Stuart Farber/Willie Montague/Angela Marie Walls-Windhauser/Vibert White (R); FL-6 Michael Gist (NPA); FL-11 Mike Wilnau (R) + Barbie Harden Hall (D)"
    - "James Gallagher's incorrect is_incumbent flag is removed on CA-1 (open/redrawn seat)"
    - "No candidate card surfaces party; party stays on races.primary_party only; no politician_id is NULL on any active race_candidates row"
  artifacts:
    - path: "backend/migrations/1381_fl11_webster_untangle.sql"
      provides: "FL-11 Daniel/Royal Webster untangle (rename + deactivate + add challenger)"
      contains: "Daniel Webster"
    - path: "backend/migrations/1382_fl_add_candidates_and_ca1_gallagher.sql"
      provides: "Missing FL candidate adds + CA-1 Gallagher incumbent-flag removal"
      contains: "Gallagher"
  key_links:
    - from: "backend/migrations/1382_fl_add_candidates_and_ca1_gallagher.sql"
      to: "essentials.race_candidates (FL-10/FL-6/FL-11 races)"
      via: "INSERT politician + race_candidates with NON-NULL politician_id"
      pattern: "candidate_status"
---

<objective>
Apply the small FL and CA candidate-field corrections identified in the 2026-07-21 audit. These are DATA FIXES, not re-seeds: the candidate fields are already new-map in all five states (verified), so this plan only repairs incumbency flags, garbled names, one tangled record (FL-11 Webster), and a handful of missing FL ballot-qualified candidates, plus removes one wrong incumbent flag in CA-1. FL is prioritized because its primary is 2026-08-18 and voters must see the correct field.

Purpose: The polygon backfill (Plan 02) fixes WHICH district a voter resolves to; this plan makes sure the candidate field shown in those FL/CA districts is correct. Independent of the polygon work (different tables), so it runs in Wave 1 alongside the import.
Output: Three idempotent migrations correcting FL incumbency/names, the FL-11 Webster untangle, the missing FL candidate adds, and the CA-1 Gallagher flag — applied to PROD.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-RESEARCH.md

<interfaces>
<!-- Data-work standing rules (project memory). Confirm live IDs at execution; treat migration numbers as placeholders. -->
race_candidates insert shape: race_id (the district race), politician_id (NON-NULL), full_name, is_incumbent, candidate_status='active', source. NEVER office_id IS NULL on a House race. NEVER party on the candidate card — party lives on races.primary_party only (antipartisan invariant); third-party/independent label rides on politicians.party.
external_id scheme for NEW challengers: -(state_fips*10000 + cd*100 + seq), FL=12 (e.g. FL-10 band -1210xx, FL-6 -1206xx, FL-11 -1211xx). VERIFY 0 collisions against live negative IDs before insert; pick a safe starting seq per district. NOTE external_id -12011 already exists (the mislabeled Daniel Webster record) — the new Royal Webster gets a DIFFERENT id from the current scheme.
Dedup before creating a politician: require first+last match; exclude FEC-committee ALLCAPS junk + cross-state homonyms; scope by race_candidates joins, not the raw external_id band.
Two-path retire (for the retiring Daniel Webster candidacy): race_candidates.candidate_status deactivated AND politicians.is_active=false — NEVER hard-DELETE (preserve record + stances).
Migrations: idempotent (NOT EXISTS / guarded UPDATE with WHERE on the current value), BEGIN/COMMIT-wrapped, psql -v ON_ERROR_STOP=1. Re-check the live migration high-water mark (`ls backend/migrations | grep -E '^[0-9]' | sort -n | tail`) immediately before authoring — HWM was 1379 (next free 1380) at planning time; parallel Phases 177/178 may have advanced it. The 1380/1381/1382 filenames are PLACEHOLDERS.
Resolve races/candidates via joins: essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND d.geo_id=<'12'+pad2(cd)> AND the FL 2026 general election. Resolve the exact FL 2026 election name/id and the exact politician_id targets by name at execution (do not hardcode ids from this plan).
pool.query() / psql for all essentials + connect writes — NEVER PostgREST (essentials not in the exposed schema).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FL incumbency flags + name renames + CA-1 Gallagher flag removal</name>
  <read_first>
    - a recent guarded-UPDATE migration for the idempotency convention (e.g. backend/migrations/1230_fix_edmonds_la5_to_la6_rekey.sql)
    - the FL audit notes in the phase RESEARCH + scratchpad fl_house_audit.md if present
  </read_first>
  <action>
    Re-check the migration HWM, then author backend/migrations/{next}_fl_incumbent_flags_and_renames.sql (BEGIN/COMMIT, idempotent). In it:
    - Set is_incumbent=true on the active FL 2026 race_candidates rows for Debbie Wasserman Schultz (FL-20, geo_id 1220), Lois Frankel (FL-23, 1223), and Jared Moskowitz (FL-25, 1225), resolved via the office->district->geo_id join to the FL 2026 general race (they are incumbents on the new map). Guard each UPDATE on is_incumbent IS DISTINCT FROM true so re-runs are no-ops.
    - Rename the politician 'Kedner MaximeDe' -> 'Kedner Maxime' and 'Seth Haskins' -> 'Seth Haskin' (essentials.politicians.full_name; also fix any denormalized race_candidates.full_name copy for the same politician_id). Guard on the current (wrong) value.
    - Then, in a SEPARATE migration backend/migrations/{next+2}_fl_add_candidates_and_ca1_gallagher.sql is authored in Task 3; for THIS task's CA piece, include the CA-1 Gallagher flag removal in that Task-3 migration (keep CA changes in the CA/adds migration). [This task = FL flags + renames only.]
    Verify each rename resolves to exactly one politician (no homonym collision) before applying.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND d.geo_id IN ('1220','1223','1225') AND rc.is_incumbent=true AND rc.candidate_status='active'" | grep -qx 3 && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE full_name IN ('Kedner Maxime','Seth Haskin')" | grep -qx 2 && echo FL_FLAGS_RENAMES_OK</automated>
  </verify>
  <acceptance_criteria>
    - The three FL-20/23/25 incumbents are is_incumbent=true on their active 2026 rows
    - 'Kedner Maxime' and 'Seth Haskin' exist (old garbled names gone); no other politician renamed
    - Migration is idempotent (guarded on current values); BEGIN/COMMIT-wrapped
  </acceptance_criteria>
  <done>FL incumbency flags corrected and two garbled names fixed; idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: FL-11 Webster untangle</name>
  <read_first>
    - the two-path retire convention (race_candidates.candidate_status + politicians.is_active) from the Phase 167 spec / project memory
    - the current state of external_id -12011: `SELECT id, external_id, full_name, party, is_active FROM essentials.politicians WHERE external_id=-12011` and its race_candidates rows
  </read_first>
  <action>
    Author backend/migrations/{next+1}_fl11_webster_untangle.sql (BEGIN/COMMIT, idempotent). The record external_id=-12011 is actually Daniel Webster (it carries his stances) but is mislabeled 'Royal Webster'. Do all three, guarded:
    1. Rename politician external_id=-12011 full_name -> 'Daniel Webster' (guard on current value 'Royal Webster'); fix any race_candidates.full_name copy for that politician_id. Keep the record and its stances intact.
    2. Daniel Webster is retiring: deactivate his FL-11 candidacy via BOTH paths — set his FL-11 (geo_id 1211, FL 2026 general) race_candidates.candidate_status to the deactivated value AND essentials.politicians.is_active=false for external_id=-12011. NEVER hard-DELETE.
    3. Add a NEW politician 'Royal Webster' party 'D' (real, separate challenger) with a collision-free external_id in the FL-11 band (verify 0 collisions), and INSERT an active race_candidates row linking him to the FL-11 2026 race with NON-NULL politician_id, candidate_status='active', source recorded. Do NOT put party on the candidate card (party on politicians.party only).
    Verify -12011 is the SAME politician_id throughout (stances preserved), and that FL-11's active field now shows Royal Webster (D challenger) and NOT Daniel Webster.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT full_name||'|'||is_active FROM essentials.politicians WHERE external_id=-12011" | grep -qx "Daniel Webster|f" && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=rc.politician_id WHERE d.geo_id='1211' AND d.district_type='NATIONAL_LOWER' AND p.full_name='Royal Webster' AND p.party='D' AND rc.candidate_status='active'" | grep -qx 1 && echo WEBSTER_UNTANGLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - external_id=-12011 is 'Daniel Webster', is_active=false, its stances preserved, deactivated from the active FL-11 field (both paths)
    - A distinct new 'Royal Webster' (party D) politician exists with an active FL-11 race_candidates row, NON-NULL politician_id, collision-free external_id
    - No hard-DELETE; migration idempotent + BEGIN/COMMIT
  </acceptance_criteria>
  <done>FL-11 Webster records untangled: Daniel retired, Royal added as challenger.</done>
</task>

<task type="auto">
  <name>Task 3: Add missing FL candidates + remove CA-1 Gallagher incumbent flag</name>
  <read_first>
    - the external_id collision-check recipe + dedup rule (project memory Data-Work Standing Patterns)
    - the CA-1 race_candidates row for James Gallagher: `SELECT rc.id, rc.is_incumbent FROM essentials.race_candidates rc JOIN ... WHERE d.geo_id='0601' AND p.full_name ILIKE 'James Gallagher'`
  </read_first>
  <action>
    Author backend/migrations/{next+2}_fl_add_candidates_and_ca1_gallagher.sql (BEGIN/COMMIT, idempotent NOT EXISTS guards). Add the missing FL ballot-qualified candidates as politician + active race_candidates rows (NON-NULL politician_id, candidate_status='active', collision-free external_id in the correct FL-district band, dedup-checked so no existing person is duplicated):
    - FL-10 (geo_id 1210): Stuart Farber (R), Willie Montague (R), Angela Marie Walls-Windhauser (R), Vibert White (R)
    - FL-6 (geo_id 1206): Michael Gist (NPA)
    - FL-11 (geo_id 1211): Mike Wilnau (R), Barbie Harden Hall (D)
    Party goes on politicians.party (NPA = Florida's no-party-affiliation term); NEVER on the candidate card. Each linked to its district's FL 2026 general race via the office->district join; NEVER office_id IS NULL.
    Also in this migration: remove the incorrect is_incumbent flag on James Gallagher in CA-1 (geo_id 0601) — the seat is open/redrawn, he is not the incumbent there. Guard the UPDATE on is_incumbent=true.
    After applying, record a dated follow-up todo (`.planning/todos/`) noting that federal-24 stances + headshots for the newly-added FL challengers (Farber, Montague, Walls-Windhauser, White, Gist, Wilnau, Harden Hall, Royal Webster) are pending via the standing research-stances + headshot-sweep pipelines before the Aug-18 FL primary — these are NOT dropped, just handled by their dedicated pipelines outside this polygon phase.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=rc.politician_id WHERE d.district_type='NATIONAL_LOWER' AND rc.candidate_status='active' AND rc.politician_id IS NOT NULL AND ((d.geo_id='1210' AND p.full_name IN ('Stuart Farber','Willie Montague','Angela Marie Walls-Windhauser','Vibert White')) OR (d.geo_id='1206' AND p.full_name='Michael Gist') OR (d.geo_id='1211' AND p.full_name IN ('Mike Wilnau','Barbie Harden Hall')))" | grep -qx 7 && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=rc.politician_id WHERE d.geo_id='0601' AND p.full_name ILIKE 'James Gallagher' AND rc.is_incumbent=true" | grep -qx 0 && echo FL_ADDS_CA1_OK</automated>
  </verify>
  <acceptance_criteria>
    - 7 new FL candidates added (4 FL-10 + 1 FL-6 + 2 FL-11) as active race_candidates with NON-NULL politician_id and collision-free external_ids; no duplicates of existing people
    - Party recorded on politicians.party only (NPA for Gist); no party on candidate cards
    - James Gallagher CA-1 is_incumbent flag removed (0 rows with is_incumbent=true)
    - Follow-up todo created for the new FL candidates' stances + headshots (not silently dropped)
    - Migration idempotent (NOT EXISTS guards) + BEGIN/COMMIT
  </acceptance_criteria>
  <done>Missing FL candidates added, CA-1 Gallagher flag removed, stance/headshot follow-up tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> prod candidate rows | A wrong-row UPDATE/INSERT could surface/hide the wrong candidate or duplicate a person |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1642-11 | Tampering | Incumbent flag / rename hits the wrong politician (homonym) | medium | mitigate | Resolve by external_id or office->district->geo_id join; guard each UPDATE on the current value; pre-verify single-row match |
| T-1642-12 | Tampering / data integrity | New candidate duplicates an existing person or collides external_id | medium | mitigate | Dedup (first+last, exclude cross-state homonyms) + external_id collision check before insert; NOT EXISTS guards |
| T-1642-13 | Tampering (data loss) | Retiring Daniel Webster via hard-DELETE loses his stances | medium | mitigate | Two-path retire (candidate_status + is_active=false), never DELETE |
| T-1642-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — SQL migrations only |
</threat_model>

<verification>
- FL-20/23/25 incumbents flagged; Kedner Maxime + Seth Haskin renamed.
- FL-11 untangled: Daniel Webster retired (both paths, stances kept), Royal Webster (D) added.
- 7 missing FL candidates added; CA-1 Gallagher flag removed.
- No NULL politician_id on active rows; no party on candidate cards.
</verification>

<success_criteria>
FL and CA candidate fields are correct for the enacted-2026 map: incumbency flags fixed, garbled names repaired, the FL-11 Webster records untangled, the missing FL ballot-qualified candidates surfacing on /elections, and the CA-1 Gallagher incumbent flag removed — all via idempotent migrations, with new-candidate stance/headshot follow-ups tracked (not dropped).
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-04-SUMMARY.md` when done.
</output>
