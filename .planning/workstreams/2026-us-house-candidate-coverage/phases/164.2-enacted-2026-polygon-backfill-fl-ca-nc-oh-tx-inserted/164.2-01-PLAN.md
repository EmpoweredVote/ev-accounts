---
phase: 164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted
plan: 01
type: execute
wave: 2
depends_on: [164.2-02]
files_modified:
  - backend/src/routes/essentials.ts
  - backend/migrations/1383_extend_resolve_congressional_2026_5_states.sql
  - backend/scripts/1642-anon-path-b-check.ts
autonomous: true
requirements: [D-01, D-11]
must_haves:
  truths:
    - "REFRESHED_2026_FIPS in src/routes/essentials.ts includes '12','06','37','39','48' (FL/CA/NC/OH/TX) in addition to the original 5 (47/29/01/22/49)"
    - "connect.resolve_congressional_2026 RPC's FIPS IN-list includes '12','06','37','39','48' so a Connected-tier user in one of the new states re-resolves live against G5200V26"
    - "For a known in-district anon coordinate in each of FL/CA/NC/OH/TX, getElectionsByCoordinate resolves the NEW (G5200V26) congressional district's race (proven by 1642-anon-path-b-check.ts)"
    - "The dual-map opt-in code is deployed to Render and /health responds 200"
    - "No write to connected_profiles / user_districts / geo_districts is introduced; the RPC stays read-only SECURITY DEFINER with SET search_path=''"
  artifacts:
    - path: "backend/src/routes/essentials.ts"
      provides: "REFRESHED_2026_FIPS extended to 10 states for the Connected-tier live fallback"
      contains: "REFRESHED_2026_FIPS"
    - path: "backend/migrations/1383_extend_resolve_congressional_2026_5_states.sql"
      provides: "CREATE OR REPLACE of connect.resolve_congressional_2026 with the 5 new FIPS added to the IN-list"
      contains: "resolve_congressional_2026"
    - path: "backend/scripts/1642-anon-path-b-check.ts"
      provides: "Regression check asserting anon Path-B resolves V26 for all 5 new states"
      contains: "G5200V26"
  key_links:
    - from: "backend/src/routes/essentials.ts /api/elections/me Path 1"
      to: "connect.resolve_congressional_2026 (now covering 10 states)"
      via: "REFRESHED_2026_FIPS membership test on cached congressional_geo_id"
      pattern: "resolve_congressional_2026"
---

<objective>
Extend the Phase 164.1 dual-map Connected-tier live fallback to the five newly-backfilled states (FL=12, CA=06, NC=37, OH=39, TX=48) so a logged-in Connected-tier user in a differential zone of those states re-resolves their congressional district LIVE against the G5200V26 vintage — matching what anonymous Path-B already does automatically (the anon opt-in JOIN is generic and needs no change). This is exactly the 164.1 D-11 mechanism, widened by five FIPS: add the five codes to `REFRESHED_2026_FIPS` in the route and to the FIPS IN-list inside `connect.resolve_congressional_2026`, then deploy to Render.

Purpose: Without this, a Connected user in a FL/CA/NC/OH/TX differential zone keeps seeing their OLD district's race through the Nov-2026 general even though the anon path is already corrected by Plan 02's imports. Runs after Plan 02 so the regression check asserts against real landed polygons.
Output: One route edit + one CREATE-OR-REPLACE RPC migration + one anon-resolution regression check + a live Render deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-RESEARCH.md
@backend/src/routes/essentials.ts
@backend/src/lib/electionService.ts

<interfaces>
<!-- Exact current targets (verified live this planning session). grep the tokens; line numbers drift. -->
backend/src/routes/essentials.ts ~line 55 — the ORIGINAL allowlist:
  `const REFRESHED_2026_FIPS = new Set(['47', '29', '01', '22', '49']);`
  Extend to `['47','29','01','22','49','12','06','37','39','48']`. The route logic (resolveCongressional2026 helper + its call sites in GET /api/elections/me Path 1 and Path 1.5) is UNCHANGED — only the Set membership widens.

connect.resolve_congressional_2026(uuid) — the D-11 read-only SECURITY DEFINER RPC (migration 1246). Its body filters `substring(gb.geo_id FROM 1 FOR 2) IN ('47', '29', '01', '22', '49')`. CREATE OR REPLACE it (idempotent) with the IN-list extended to include '12','06','37','39','48'. Keep everything else byte-identical: SET search_path='', the Vault decrypt preamble (extensions.pgp_sym_decrypt_bytea + convert_from(...,'UTF8')::float8), public.ST_SetSRID/ST_MakePoint/ST_Covers, the essentials.geofence_boundaries WHERE mtfcc='G5200V26' JOIN essentials.districts d ON d.geo_id=gb.geo_id AND d.district_type='NATIONAL_LOWER', ORDER BY d.geo_id LIMIT 1, and the REVOKE/GRANT to service_role. Read the current definition first: `psql "$DATABASE_URL" -c "\sf connect.resolve_congressional_2026"`.

electionService.ts getElectionsByCoordinate: the V26-preferring JOIN LATERAL is ALREADY deployed (164.1) and generic — it needs NO change; anon /elections auto-resolves V26 for any geo_id with a V26 row. Do not touch it.
Reps feed exclusion (geoIdGuard.ts / essentialsService.ts) already excludes G5200V26 — do not touch.

Render deploy: reference $HOME/.claude/projects/C--EV-Accounts/memory/reference_render_deploy.md — curl the RENDER_DEPLOY_HOOK, poll /health until 200. Data changes need NO deploy, but this ROUTE code change DOES.
Migration HWM was 1379 (next free 1380) at planning; Plan 04 also allocates 1380-1382 and parallel Phases 177/178 may advance it — re-check `ls backend/migrations | grep -E '^[0-9]' | sort -n | tail` and take the next free number. The 1383 filename is a PLACEHOLDER.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the RPC FIPS IN-list (CREATE OR REPLACE migration)</name>
  <files>backend/migrations/1383_extend_resolve_congressional_2026_5_states.sql</files>
  <read_first>
    - the live RPC definition: `psql "$DATABASE_URL" -c "\sf connect.resolve_congressional_2026"`
    - backend/migrations/1246_resolve_congressional_2026_rpc.sql (the original — clone its body verbatim, only the IN-list changes)
  </read_first>
  <action>
    Re-check the migration HWM and take the next free number. Author backend/migrations/{next}_extend_resolve_congressional_2026_5_states.sql: a BEGIN/COMMIT-wrapped CREATE OR REPLACE FUNCTION connect.resolve_congressional_2026(p_user_id uuid) identical to migration 1246 EXCEPT the FIPS IN-list becomes `IN ('47','29','01','22','49','12','06','37','39','48')`. Keep SECURITY DEFINER, SET search_path='', the Vault decrypt preamble, the public.ST_* point build + ST_Covers, the G5200V26 + NATIONAL_LOWER join, ORDER BY d.geo_id LIMIT 1, read-only (no DML), and re-run the REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO service_role. Add a header comment noting this widens D-11 to the Phase-164.2 states. Apply to PROD.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT pg_get_functiondef(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='connect' AND p.proname='resolve_congressional_2026'" | grep -q "'12'" && psql "$DATABASE_URL" -tAc "SELECT pg_get_functiondef(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='connect' AND p.proname='resolve_congressional_2026'" | grep -q "'48'" && echo RPC_EXTENDED_OK</automated>
  </verify>
  <acceptance_criteria>
    - connect.resolve_congressional_2026 on PROD includes all 10 FIPS ('47','29','01','22','49','12','06','37','39','48') in its IN-list
    - Function stays SECURITY DEFINER, SET search_path='', read-only; GRANT EXECUTE to service_role intact
    - Migration is a CREATE OR REPLACE (idempotent), BEGIN/COMMIT-wrapped
  </acceptance_criteria>
  <done>The D-11 RPC now resolves G5200V26 for FL/CA/NC/OH/TX as well as the original 5.</done>
</task>

<task type="auto">
  <name>Task 2: Extend REFRESHED_2026_FIPS + author the anon Path-B regression check</name>
  <files>backend/src/routes/essentials.ts, backend/scripts/1642-anon-path-b-check.ts</files>
  <read_first>
    - backend/src/routes/essentials.ts (the REFRESHED_2026_FIPS Set ~line 55 and the resolveCongressional2026 helper + call sites)
    - backend/scripts/1641-coordinate-smoke.ts (the anon coordinate-resolution query shape to reuse in the check)
  </read_first>
  <action>
    In backend/src/routes/essentials.ts add '12','06','37','39','48' to the REFRESHED_2026_FIPS Set (10 entries total) and update the adjacent comment to note the Phase-164.2 states. Change NOTHING else in the route (the helper + call sites already gate on Set membership).
    Author backend/scripts/1642-anon-path-b-check.ts: for each of FL/CA/NC/OH/TX, take a V26 anchor (public.ST_PointOnSurface of one of that state's G5200V26 geometries) and assert, via the anon coordinate path, that (a) a NATIONAL_LOWER geo_id with mtfcc='G5200V26' covers the point (V26 present + valid) and (b) for at least one differential anchor per state the V26-covering geo_id differs from the G5200-covering geo_id — i.e. the enacted map genuinely moved that point. Follow the 1641-coordinate-smoke.ts query shape (parameterized, public.ST_* prefix, pool from src/lib/db.js). Skip-if-no-V26-rows -> the check would only be meaningful post-import; since this plan runs after Plan 02, all five states must be PRESENT and PASS (fail if any state has 0 rows). Run `npx tsc --noEmit` then the check.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && grep -q "'12'" src/routes/essentials.ts && grep -q "'48'" src/routes/essentials.ts && grep -q "G5200V26" scripts/1642-anon-path-b-check.ts && npx tsc --noEmit && node --import tsx scripts/1642-anon-path-b-check.ts && echo ANON_PATHB_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - REFRESHED_2026_FIPS has 10 entries including 12/06/37/39/48; no other route logic changed
    - 1642-anon-path-b-check.ts exits 0 proving anon Path-B resolves the NEW (V26) district for all 5 states, with a genuine differential for each
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Connected-tier allowlist widened; anon V26 resolution regression-proven for all 5 states.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy the route change to Render and confirm the Connected-tier fallback is live</name>
  <files>backend/src/routes/essentials.ts</files>
  <read_first>
    - $HOME/.claude/projects/C--EV-Accounts/memory/reference_render_deploy.md (deploy-hook usage)
  </read_first>
  <action>
    The RPC IN-list extension (Task 1) is already applied to PROD (data change, no deploy needed), but the REFRESHED_2026_FIPS route change (Task 2) requires a Render deploy. Trigger it via `curl -fsS -X POST "$RENDER_DEPLOY_HOOK"` (value in backend/.env per reference_render_deploy.md), then poll the service /health until it returns HTTP 200 (deploy complete). If RENDER_DEPLOY_HOOK is unset or unreachable, STOP and create a checkpoint:human-action asking the operator to deploy ev-accounts-api from the Render dashboard, then resume the /health poll. After /health is 200, spot-confirm the Connected-tier fallback resolves a NEW state: invoke connect.resolve_congressional_2026 for a differential-zone coordinate in FL/CA/NC/OH/TX via the rolled-back sentinel pattern (as in 1641-coordinate-smoke.ts) and confirm it returns the NEW geo_id (Plan 03's 1642-coordinate-smoke.ts does this comprehensively).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && curl -fsS "${RENDER_HEALTH_URL:-https://ev-accounts-api.onrender.com/health}" -o /dev/null -w '%{http_code}' | grep -qx 200 && echo RENDER_HEALTH_200</automated>
  </verify>
  <done>Route change deployed to Render; /health 200; Connected-tier fallback confirmed returning NEW geo_id for a FL/CA/NC/OH/TX differential point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged-in Connected user -> /api/elections/me Path 1 | Cached (stale) congressional_geo_id vs live G5200V26 re-resolution for 5 more states |
| resolve_congressional_2026 (SECURITY DEFINER) -> encrypted coordinate | Decrypts a user's stored location server-side |
| deploy -> prod | Route change alters live Connected-tier elections resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1642-21 | Information Disclosure | RPC decrypts a user's coordinate | high | mitigate | Unchanged read-only SECURITY DEFINER, SET search_path='', returns only a geo_id; GRANT limited to service_role |
| T-1642-22 | Tampering (D-04 breach) | Fallback writes connected_profiles / user_districts | high | mitigate | RPC is read-only (no DML); route only mutates in-memory geoPairs; only the FIPS Set + IN-list change |
| T-1642-23 | DoS / wrong-result | Deploy breaks Connected-tier resolution | medium | mitigate | Widening an allowlist is additive; other states unaffected; /health gate + Plan 03 smoke as regression proof |
| T-1642-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — dependency set unchanged |
</threat_model>

<verification>
- RPC on PROD includes all 10 FIPS; still read-only SECURITY DEFINER.
- REFRESHED_2026_FIPS has 10 entries; tsc clean.
- 1642-anon-path-b-check.ts GREEN for all 5 states.
- Render /health 200 post-deploy (blocking human-verify checkpoint).
</verification>

<success_criteria>
The Connected-tier live fallback (D-11) covers FL/CA/NC/OH/TX in both the route allowlist and the RPC IN-list, deployed to Render (/health 200), with anon Path-B V26 resolution regression-proven for all five states — no cache mutation, RPC still read-only.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-01-SUMMARY.md` when done.
</output>
