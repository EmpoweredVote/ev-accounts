---
phase: 164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted
plan: 03
type: execute
wave: 3
depends_on: [164.2-01, 164.2-02]
files_modified:
  - backend/scripts/1642-coordinate-smoke.ts
autonomous: true
requirements: [D-02, D-10, D-11]
must_haves:
  truths:
    - "1642-coordinate-smoke.ts asserts, for each of FL/CA/NC/OH/TX, a Layer-2 self-resolve for every V26 district anchor and a Layer-3 differential where the coordinate surfaces the NEW district's race on /elections while the reps feed still returns the OLD district's seated rep"
    - "The 12 verified anchor coordinates (CA Redding/Palm Springs/Indian Wells; NC Snow Hill/Morehead City/Kill Devil Hills; OH Wilmington/Napoleon/Kent; TX Liberty/Rockwall/Mission) each resolve to their expected NEW district under V26 and to their OLD district under G5200"
    - "The D-11 Connected-tier RPC probe (rolled-back sentinel) returns the NEW geo_id for each state's differential point (proves the Plan 01 RPC IN-list extension works)"
    - "The reps feed (G5200 ST_Covers) still returns the OLD district for every differential point (D-02 dual-map holds)"
  artifacts:
    - path: "backend/scripts/1642-coordinate-smoke.ts"
      provides: "D-10 Layer-2/Layer-3 coordinate differential for FL/CA/NC/OH/TX (anon + Connected-tier + reps-feed)"
      contains: "G5200V26"
  key_links:
    - from: "backend/scripts/1642-coordinate-smoke.ts"
      to: "connect.resolve_congressional_2026 (extended IN-list) + getElectionsByCoordinate V26 LATERAL"
      via: "rolled-back sentinel RPC probe + live V26-preferring coordinate query"
      pattern: "resolve_congressional_2026"
---

<objective>
Complete the D-10 verify bar for FL/CA/NC/OH/TX by cloning `1641-coordinate-smoke.ts` to `1642-coordinate-smoke.ts`, retargeting it to the five new states (all with empty severe sets — there is no seed-and-withhold this phase), and asserting the full three-layer differential: every V26 district anchor self-resolves (Layer-2); a differential coordinate surfaces the NEW district's race on `/elections` while the reps feed still returns the OLD district's seated rep (Layer-3 anon + reps-feed); and the D-11 Connected-tier RPC returns the NEW geo_id for that point (Layer-3 Connected, via the rolled-back sentinel probe). The 12 verified anchor coordinates are added as explicit positive assertions on top of the script's auto-discovered differentials.

Purpose: This is the goal-backward proof that the polygon backfill actually changed what a voter sees — anon AND Connected-tier resolve the enacted map, while the reps feed stays on current boundaries (D-02). Runs last (Wave 3) because the Connected D-11 probe needs Plan 01's extended RPC IN-list, and every layer needs Plan 02's landed polygons.
Output: One `1642-coordinate-smoke.ts` proving the D-10 bar GREEN for all five states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-RESEARCH.md
@backend/scripts/1641-coordinate-smoke.ts

<interfaces>
<!-- Clone target: backend/scripts/1641-coordinate-smoke.ts — fully data-driven; only STATES + anchor assertions + election-name lookup change. -->
STATES config for 1642: `[{fips:'12',abbr:'FL',expected:28,severe:[]},{fips:'06',abbr:'CA',expected:52,severe:[]},{fips:'37',abbr:'NC',expected:14,severe:[]},{fips:'39',abbr:'OH',expected:15,severe:[]},{fips:'48',abbr:'TX',expected:38,severe:[]}]`. All severe sets EMPTY — no withheld races this phase, so the script's guaranteed (severeMode='exclude') differential covers every state and the severe branch is skipped.

Election-name lookup: 1641 keys race-level asserts off an election named `${abbr} 2026 Statewide General`. FL/CA/NC/OH/TX 2026 House races were seeded in earlier waves (v2.20/v2.21) and may be named differently. At execution, GREP the actual 2026 general-election name(s) for these states: `psql "$DATABASE_URL" -c "SELECT DISTINCT e.name FROM essentials.elections e JOIN essentials.races r ON r.election_id=e.id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('12','06','37','39','48')"`. Adapt the generalEid lookup to match the real names (or resolve the general election by state FIPS + election_date='2026-11-03' + a NATIONAL_LOWER race, rather than by hardcoded name) so the race-level Layer-3 asserts FIRE (do not leave them deferred — these states have live races, unlike UT in 164.1).

VINTAGE_LATERAL, discoverDifferential, surfacingRacesAt, the SENTINEL_UUID rollback RPC probe, MIN_DIFF_AREA, and VISIBILITY_WINDOW are reused verbatim — the RPC probe calls connect.resolve_congressional_2026 which Plan 01 extended to the 5 new FIPS.

Verified anchor coordinates (lon,lat -> expected OLD/NEW geo_id) to add as an EXPLICIT positive-assertion block (each must: cover NEW under G5200V26, cover OLD under G5200, OLD != NEW, and — where the state's general race exists — surface the NEW district's race via surfacingRacesAt):
- CA: Redding (-122.3775,40.5922) 0601->0602 | Palm Springs (-116.5072,33.8216) 0641->0648 | Indian Wells (-116.3397,33.7133) 0641->0648
- NC: Snow Hill/Greene (-77.6759,35.4515) 3701->3703 | Morehead City (-76.7261,34.7226) 3703->3701 | Kill Devil Hills (-75.6676,36.0146) 3703->3701
- OH: Wilmington/Clinton (-83.8286,39.4453) 3902->3901 | Napoleon/Henry (-84.1258,41.3920) 3905->3909 | Kent (-81.3421,41.1489) 3914->3913
- TX: Liberty (-94.7955,30.0577) 4836->4809 | Rockwall (-96.4591,32.9297) 4804->4832 | Mission (-98.3200,26.2073) 4815->4828
- FL: no fixed anchor list — rely on the auto-discovered guaranteed differential (severe:[]); the enacted-map shift for Wasserman Schultz (FL-20/1220), Frankel (FL-23/1223), Moskowitz (FL-25/1225) makes a South-FL differential certain to be discovered.

Run: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/1642-coordinate-smoke.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 1642-coordinate-smoke.ts (STATES + adapted election lookup + verbatim differential engine)</name>
  <read_first>
    - backend/scripts/1641-coordinate-smoke.ts (full — clone; understand the Layer-2 self-resolve, the guaranteed/severe differential discovery, the reps-feed assert, the Connected geo-id path, and the rolled-back D-11 RPC probe)
  </read_first>
  <action>
    Clone backend/scripts/1641-coordinate-smoke.ts to backend/scripts/1642-coordinate-smoke.ts. Replace the STATES array with the 5 new states (all severe:[]) per <interfaces>. Adapt the general-election lookup so race-level Layer-3 asserts fire for these states: resolve each state's 2026 general election by a robust key (state FIPS + election_date='2026-11-03' with a NATIONAL_LOWER race, or the actual name grepped from the DB) instead of the hardcoded `${abbr} 2026 Statewide General`. Keep VINTAGE_LATERAL, discoverDifferential, surfacingRacesAt, MIN_DIFF_AREA, VISIBILITY_WINDOW, and the SENTINEL_UUID rollback RPC probe verbatim. Update the header prose (states, provenance, no-withholding note) and the final green summary string to '1642 COORDINATE SMOKE GREEN'. All PostGIS via public.; all inputs parameterized; the D-11 probe stays inside an always-ROLLBACK transaction (no committed writes).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -q "G5200V26" scripts/1642-coordinate-smoke.ts && grep -q "resolve_congressional_2026" scripts/1642-coordinate-smoke.ts && npx tsc --noEmit && echo SMOKE_AUTHORED</automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/1642-coordinate-smoke.ts exists; STATES = FL/CA/NC/OH/TX with expected 28/52/14/15/38 and severe:[]
    - The general-election lookup is adapted so race-level asserts are NOT deferred for these states
    - The rolled-back sentinel D-11 RPC probe is preserved (no committed writes); tsc clean
  </acceptance_criteria>
  <done>1642-coordinate-smoke.ts authored, retargeted, tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the explicit 12-anchor positive-assertion block and run the full D-10 bar GREEN</name>
  <read_first>
    - the anchor table in <interfaces>
    - Plan 02's 1642-verify.sql result (topology already GREEN) and Plan 01's extended RPC (IN-list includes 12/06/37/39/48)
  </read_first>
  <action>
    Add an explicit anchor-assertion block to 1642-coordinate-smoke.ts driven by the 12 verified coordinates (CA/NC/OH/TX). For each anchor assert: (a) the G5200V26-covering NATIONAL_LOWER geo_id equals the expected NEW geo_id; (b) the G5200-covering geo_id equals the expected OLD geo_id; (c) OLD != NEW; (d) where that state's 2026 general race exists, surfacingRacesAt(lng,lat,fips) returns exactly the NEW district's race. Accumulate into the same failures[] array so any mismatch exits non-zero. This complements — does not replace — the auto-discovered guaranteed differential (which also covers FL, which has no fixed anchor list).
    Then run the full smoke against PROD. It must exit 0: every state's Layer-2 anchors self-resolve, a guaranteed differential proves NEW-on-elections + OLD-on-reps-feed, the 12 explicit anchors pass, and the D-11 RPC probe returns the NEW geo_id for each differential point (proving Plan 01's extension). Also re-run 1642-verify.sql as the topology half of the bar. If the D-11 probe returns NULL for a new state, Plan 01's RPC IN-list did not include that FIPS or the deploy is stale — fix Plan 01, do not weaken the assert.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1642-verify.sql | grep -q "1642-verify GREEN" && node --import tsx scripts/1642-coordinate-smoke.ts | grep -q "1642 COORDINATE SMOKE GREEN" && echo D10_BAR_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 1642-coordinate-smoke.ts exits 0: all 5 states fully asserted (Layer-2 + guaranteed Layer-3 + 12 explicit anchors + D-11 RPC probe), 0 skipped
    - The 12 verified anchors each resolve NEW under V26 and OLD under G5200 (OLD != NEW), and surface the NEW race where a general race exists
    - The reps feed returns the OLD district's seated rep for every differential point (D-02 holds)
    - 1642-verify.sql still GREEN (topology + NOTOUCH)
  </acceptance_criteria>
  <done>D-10 3-layer verify bar GREEN for FL/CA/NC/OH/TX — anon + Connected-tier resolve the enacted map, reps feed stays on current boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke sentinel row -> connect tables | The D-11 RPC probe inserts a sentinel user row |
| client coordinate -> live V26-preferring query | Differential points fed through the live coordinate shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1642-31 | Tampering (test pollution) | Sentinel RPC probe persisting a fake user | high | mitigate | Probe runs inside an always-ROLLBACK transaction (cloned verbatim from 1641); nothing commits |
| T-1642-32 | Wrong-result | Smoke passes on a mis-imported map | high | mitigate | Explicit 12-anchor OLD/NEW assertions + auto-discovered differential + verify.sql topology cross-check |
| T-1642-33 | Injection (SQLi) | Coordinates/geo_ids in smoke queries | medium | mitigate | Parameterized queries throughout; public.ST_* prefix |
| T-1642-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs — pg/tsx already present |
</threat_model>

<verification>
- 1642-coordinate-smoke.ts GREEN: Layer-2 self-resolve + guaranteed Layer-3 differential + 12 explicit anchors + D-11 RPC probe for all 5 states.
- Reps feed returns OLD district for every differential point (D-02).
- 1642-verify.sql still GREEN (topology + NOTOUCH).
</verification>

<success_criteria>
The D-10 verify bar passes for FL/CA/NC/OH/TX: anon and Connected-tier `/elections` resolve the enacted-2026 congressional map at differential coordinates, the 12 verified anchors resolve NEW-under-V26 / OLD-under-G5200, the reps feed stays on current boundaries, and the Connected D-11 RPC returns the NEW geo_id — all GREEN.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/164.2-enacted-2026-polygon-backfill-fl-ca-nc-oh-tx-inserted/164.2-03-SUMMARY.md` when done.
</output>
