---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 06
type: execute
wave: 6
depends_on: ["160-05"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01: a 34-row field-table partial exists for the Phase-165 group (17 states: NV4 UT4 NM3 NE3 WV2 ID2 HI2 ME2 NH2 RI2 MT2 AK1 DE1 ND1 SD1 VT1 WY1): 24 decided + 10 late-primary"
    - "D-04a RCV over-indulgence: AK (ballot_system=top-four-rcv, late-primary) and ME (ballot_system=rcv-general, decided) are flagged rcv=true with EXHAUSTIVE field capture (full top-four for AK, complete RCV general field for ME) verified against official lists"
    - "NV's 4 rows carry existing_race_id (ALREADY WIRED — verify, do not duplicate) with the NV-2 NULL-politician_id anomaly noted as fix-not-recreate; ME's 2 rows carry existing_race_id; UT rows note primary-derived resolution (resolve Nov-3 nominees from the already-known Jun-23 primary winners, do not re-research from scratch)"
    - "ME-2 (Golden not seeking re-election; Dunlap won the D primary) and ME-1 (Pingree renomination re-checked) resolved from results sources; ME's zero-stance incumbents flagged; D-02 source-URL-required, unresolvable districts Playwright-swept"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv"
      provides: "34-row Phase-165 field partial; 24 decided + 10 late-primary; AK/ME RCV over-indulgence; NV/UT/ME reconciliation"
      contains: "top-four-rcv"
  key_links:
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv"
      via: "NV (4) + ME (2) existing_race_id + NV-2 anomaly note"
      pattern: "existing_race_id"
---

<objective>
Resolve the Phase-165 seeding-group field (17 small-delegation states = 34 districts) into `160-field-table-p165.csv` — 24 decided + 10 late-primary — with deliberately maximal thoroughness for the RCV races (AK top-four-rcv, ME rcv-general) per D-04a, and careful reconciliation of NV's pre-wired candidates, ME's pre-existing races, and UT's primary-derived nominees.

Purpose: Deliver the RCV depth Empowered Vote most needs (ranking communities), reuse NV/ME's pre-existing races (avoid duplicates), fix-not-recreate the NV-2 NULL-pid anomaly, and resolve UT's Nov-3 nominees from the known primary winners rather than re-researching.
Output: `160-field-table-p165.csv` (34 rows, 24 decided + 10 late-primary; AK+ME rcv=true).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv

<interfaces>
<!-- Reuse the 19-column schema + agent prompt spec validated in Plan 02. -->

Group 165 classification (160-RESEARCH.md Critical Finding 1):
  DECIDED (24): NV Jun-9 (4) | UT Jun-23 (4) | NM Jun-2 (3) | NE May-12 (3) | WV May-12 (2) | ID May-19 (2) |
    ME Jun-9 RCV (2) | MT Jun-2 (2) | ND Jun-9 (1) | SD Jun-2 (1).
  LATE-PRIMARY (10): HI Aug-8 (2) | NH Sep-8 (2) | RI Sep-9 (2) | AK Aug-18 top-four (1) | DE Sep-15 (1) |
    VT Aug-11 (1) | WY Aug-18 (1).
RCV (D-04a maximal thoroughness): AK ballot_system=top-four-rcv (rcv=true; full top-four field);
  ME ballot_system=rcv-general (rcv=true; complete RCV general field). All other 15 states = standard.
Pre-existing (Critical Finding 4): NV 4 scaffolded races + 9 REAL candidates incl. all 4 incumbents →
  existing_race_id carried; NV-2 Lynn Chapman row has NULL politician_id (fix-not-recreate, Phase 165).
  ME 2 general races (Pingree ME-1, LePage ME-2) + 8 stale primary rows → existing_race_id carried.
  UT primary races (Jun-23, 12 candidates, incumbents Blake Moore UT-2 / Celeste Maloy UT-3 / Mike Kennedy UT-4)
  → resolve Nov-3 nominees FROM these known primary winners; UT existing_race_id BLANK (primary-dated, no Nov-3 race).
Zero-tier incumbents (Critical Finding 5): ME's Pingree + Golden. Assumptions A2 (Pingree renomination re-check),
  A3 (Golden ME-2 not seeking re-election — Dunlap won the D primary).

Agent prompt spec: identical to Plan 02's — one agent per state, MAX 3 CONCURRENT, source-URL-required,
  flag-don't-guess, re-confirm primary date vs official SoS, nominee_status taxonomy, inclusion bar,
  new_records_needed. For AK+ME apply MAXIMAL thoroughness (every candidate on the RCV ballot).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the special/reconciliation states — AK, ME, NV, UT (3 concurrent; RCV over-indulgence + pre-existing reconciliation)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (validated 19-column template + agent-prompt discipline)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (AK/ME/NV/UT incumbent grounding + ME zero-tier)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (NV 4 + ME 2 existing_race_id; NV-2 NULL-pid anomaly)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 4/5"; §"Ballot System Reference" AK/ME; §"Pitfall 4" NV; §Assumptions A2/A3; §D-04a)
  </read_first>
  <action>
    Dispatch one research agent per state for AK, ME, NV, UT, MAX 3 CONCURRENT (the 4th launches when a slot frees), embedding Plan 02's validated <agent_prompt_spec>. State-specific instructions:
    - AK agent (rcv=true, ballot_system=top-four-rcv, field_status=late-primary Aug-18): apply D-04a MAXIMAL thoroughness — capture the FULL top-four field that will appear on the Nov-3 RCV ballot, verified against the AK Division of Elections official list; minor candidates are NOT low-priority here.
    - ME agent (rcv=true, ballot_system=rcv-general, field_status=decided): capture the COMPLETE RCV general field for both districts; ME-1 re-check Pingree's renomination (Assumption A2); ME-2 resolve the open-seat outcome (Golden not seeking re-election per Assumption A3; Dunlap won the D primary) with nominee_status from results; flag Pingree/Golden as zero-tier incumbents; ME's 2 general races have existing_race_id (carried by orchestrator).
    - NV agent (field_status=decided): NV's 4 races are ALREADY WIRED with 9 real candidates incl. all 4 incumbents — VERIFY completeness/correctness against the official NV SoS list, do NOT duplicate; flag NV-2's Lynn Chapman NULL-politician_id row as fix-not-recreate (Phase 165).
    - UT agent (field_status=decided): UT's Jun-23 primary is resolved with 12 known candidates (incumbents Blake Moore/UT-2, Celeste Maloy/UT-3, Mike Kennedy/UT-4) — resolve the Nov-3 general nominees FROM those known primary winners, do NOT re-research from scratch; UT existing_race_id blank (primary-dated rows only).
    Collect these 12 districts' rows (AK1+ME2+NV4+UT4=11... note NM etc. are in Task 2); stage flagged-UNRESOLVED for the Task-3 sweep. Do NOT write the CSV yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv && echo "template present; AK/ME/NV/UT special-state rows collected with RCV over-indulgence + reconciliation (CSV written in Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    AK/ME/NV/UT agents dispatched at max 3 concurrent. AK's row is the full top-four RCV field; ME's 2 rows are the complete RCV general field with ME-1 Pingree renomination re-checked and ME-2 open-seat resolved from results. NV's 4 rows verify (not duplicate) the pre-wired candidates with the NV-2 NULL-pid fix note. UT's nominees are derived from the known primary winners. Every row has a real unwalled source_url or is flagged UNRESOLVED. AK+ME rows have rcv=true.
  </acceptance_criteria>
  <done>The RCV (AK/ME) and pre-existing-data (NV/UT/ME) states are researched with maximal thoroughness and correct reconciliation; residue staged for the sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch the 13 routine states — NM/NE/WV/ID/HI/NH/RI/MT/DE/ND/SD/VT/WY (batched, 3 concurrent)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (validated template + agent-prompt discipline)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (13-state incumbent grounding)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 1" dates for these 13 states)
  </read_first>
  <action>
    Dispatch one research agent per state for the 13 routine states (NM, NE, WV, ID, HI, NH, RI, MT, DE, ND, SD, VT, WY), MAX 3 CONCURRENT at all times (batches of 3), embedding Plan 02's validated <agent_prompt_spec>. All 13 are ballot_system=standard, rcv=false. DECIDED (resolve Nov-3 general): NM(3), NE(3), WV(2), ID(2), MT(2), ND(1), SD(1) = 14 districts. LATE-PRIMARY (full pre-primary field): HI(2), NH(2), RI(2), DE(1), VT(1), WY(1) = 9 districts. Re-confirm each state's primary date against an official SoS source (Assumption A1). Collect these 23 districts' rows; stage flagged-UNRESOLVED for the Task-3 sweep. Do NOT write the CSV yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv && echo "13 routine-state agent rows collected in <=3-concurrent batches (CSV written in Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    All 13 routine states dispatched one-per-state, never more than 3 concurrent. Every collected row has a real unwalled source_url or is flagged UNRESOLVED. Decided states (NM/NE/WV/ID/MT/ND/SD) carry the resolved Nov-3 general; late states (HI/NH/RI/DE/VT/WY) carry the full pre-primary field. All 13 states are ballot_system=standard, rcv=false.
  </acceptance_criteria>
  <done>The 13 routine Phase-165 states are researched from real sources at max-3-concurrent; residue staged for the sweep.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright residue sweep + assemble 160-field-table-p165.csv (34-row / 24-decided-10-late + RCV/NV/ME guards)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (join incumbent columns by geo_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (NV 4 + ME 2 existing_race_id; NV-2 anomaly)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (column order + quoting reference)
  </read_first>
  <action>
    Playwright-sweep every flagged-UNRESOLVED district (Ballotpedia/official SoS/AK Division of Elections) and resolve with a real source_url, or record declared-so-far with filing_open_deadline for any still-open late-primary filing. Assemble `160-field-table-p165.csv` (19-column header) merging the Task-1 (AK/ME/NV/UT) and Task-2 (13 routine) rows. Carry incumbent columns by geo_id; seeding_phase=165; ballot_system per state (AK=top-four-rcv, ME=rcv-general, all others=standard); rcv=true for AK+ME rows only; existing_race_id populated for NV's 4 + ME's 2 rows (blank elsewhere incl. UT); field_status=decided for the 24 rows (NV4+UT4+NM3+NE3+WV2+ID2+ME2+MT2+ND1+SD1) and late-primary for the 10 rows (HI2+NH2+RI2+AK1+DE1+VT1+WY1). Preserve the NV-2 NULL-pid fix note and UT primary-derived note. HARD GUARD: refuse to write unless exactly 34 rows, exactly 24 decided + 10 late-primary, all source_url non-empty, AK+ME rcv=true (exactly 3 rcv rows: AK1+ME2), NV has 4 rows with non-empty existing_race_id, ME has 2 rows with non-empty existing_race_id. Data assembly only — no DB write.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv')));print('rows',len(r));assert len(r)==34,len(r);d=[x for x in r if x['field_status']=='decided'];l=[x for x in r if x['field_status']=='late-primary'];assert len(d)==24 and len(l)==10,(len(d),len(l));assert all(x['source_url'].strip() for x in r);assert all(x['seeding_phase']=='165' for x in r);rcv=[x for x in r if str(x['rcv']).lower() in ('true','1','yes')];assert {x['state'] for x in rcv}=={'AK','ME'},{x['state'] for x in rcv};ak=[x for x in r if x['state']=='AK'][0];assert ak['ballot_system']=='top-four-rcv';nv=[x for x in r if x['state']=='NV'];assert len(nv)==4 and all(x['existing_race_id'].strip() for x in nv);me=[x for x in r if x['state']=='ME'];assert len(me)==2 and all(x['existing_race_id'].strip() for x in me)"</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table-p165.csv` has exactly 34 rows: 24 decided + 10 late-primary. Only AK and ME rows have rcv=true (AK ballot_system=top-four-rcv, ME=rcv-general). NV's 4 rows and ME's 2 rows have non-empty existing_race_id; UT and all others blank. The NV-2 NULL-pid fix note and UT primary-derived note are present. Every row has a non-empty source_url and seeding_phase=165. No UNRESOLVED rows remain.
  </acceptance_criteria>
  <done>The Phase-165 partial is complete (34 rows, 24/10), AK+ME RCV fields captured exhaustively, NV/ME existing_race_id carried, NV-2 + UT reconciliation notes present, residue swept.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agents / orchestrator -> public web sources | Read-only fetches; no writes; only reads Plan-01 CSVs. |
| field partial CSV -> downstream seeding | Fabricated field data, incomplete RCV capture, or duplicated NV/ME races would corrupt Phase-165 seeding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-12 | Tampering | RCV field completeness (AK/ME) | mitigate | D-04a maximal thoroughness verified against official Division-of-Elections/SoS lists; rcv=true guard; source-URL-required per row. |
| T-160-13 | Tampering | NV/ME pre-existing rows | mitigate | existing_race_id carried (no duplicate races); NV-2 NULL-pid flagged fix-not-recreate; UT primary-derived note; residue Playwright-swept. |
| T-160-14 | Information Disclosure | source citations | accept | Public election records only. |

Read-only phase: no prod writes, no API surface, no user input. ASVS V2–V6 N/A. No installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table-p165.csv` = 34 rows, 24 decided + 10 late-primary, all source_url non-empty.
- Only AK+ME rcv=true (AK top-four-rcv, ME rcv-general); RCV fields captured exhaustively.
- NV's 4 + ME's 2 existing_race_id carried; NV-2 + UT reconciliation notes present.
- Residue swept; agent dispatch respected max-3-concurrent.
</verification>

<success_criteria>
The Phase-165 group field is resolved (34 rows, 24/10) with the RCV races (AK/ME) captured with maximal thoroughness per D-04a, NV/ME pre-existing races reused, and the NV-2/UT/ME reconciliation notes carried — every row sourced.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-06-SUMMARY.md` when done.
</output>
