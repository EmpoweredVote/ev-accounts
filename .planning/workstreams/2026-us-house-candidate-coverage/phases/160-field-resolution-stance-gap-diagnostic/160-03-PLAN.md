---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 03
type: execute
wave: 3
depends_on: ["160-02"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01: a 33-row field-table partial exists for the Phase-162 group (IN 9 + MD 8 + MN 8 + MO 8): 17 rows decided (IN+MD), 16 rows late-primary (MN+MO)"
    - "MD's 8 rows carry existing_race_id from the race-preexistence audit (8 scaffolded races); MD's 8 zero-stance incumbents and IN's 3 zero-stance incumbents are marked incumbent_top_up_tier=zero (IN SCOPE for downstream stance research per D-02's zero-tier carve-out)"
    - "The IN-9 incumbent-flag bug (Houchin flagged non-incumbent; her primary losers flagged incumbent) is carried forward as a documented fix-for-Phase-162 note in new_records_needed/general_candidates — NOT propagated as truth; IN's Nov-3 general races do not yet exist (existing_race_id blank for IN, stale primary rows noted)"
    - "D-02: every row cites a real fetched source_url; unresolvable districts are Playwright-swept, never guessed"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv"
      provides: "33-row Phase-162 field partial (IN/MD/MN/MO); 17 decided + 16 late-primary; zero-tier incumbents flagged"
      contains: "seeding_phase"
  key_links:
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv"
      via: "incumbent columns carried by geo_id; zero-tier flags (MD 8 + IN 3)"
      pattern: "incumbent_top_up_tier"
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv"
      via: "MD existing_race_id + IN-9 bug note"
      pattern: "existing_race_id"
---

<objective>
Resolve the Phase-162 seeding-group field (IN 9 + MD 8 + MN 8 + MO 8 = 33 districts) into `160-field-table-p162.csv` — 17 decided (IN May-5, MD Jun-23) + 16 late-primary (MN/MO Aug-11/Aug-4) — reusing the validated 19-column template from Plan 02.

Purpose: Lock the IN/MD/MN/MO field, carry MD's 8 existing_race_id (avoid duplicate races), flag the 11 zero-tier incumbents (MD 8 + IN 3) that ARE in scope for downstream stance research, and document (never propagate) the IN-9 incumbent-flag bug.
Output: `160-field-table-p162.csv` (33 rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv

<interfaces>
<!-- Reuse the 19-column schema + agent prompt spec validated in Plan 02 (160-field-table-p161.csv is the reference template). -->

Group 162 classification (160-RESEARCH.md Critical Finding 1):
  IN May-5 standard DECIDED (DB has IN primary rows) | MD Jun-23 standard DECIDED |
  MN Aug-11 standard late-primary | MO Aug-4 standard late-primary.
Pre-existing (Critical Finding 4): MD 8 scaffolded races (2026-11-03, 0 candidates) → existing_race_id carried.
  IN has stale MAY-5 primary rows only (no Nov-3 general race yet) → IN existing_race_id BLANK; carry the
  IN-9 incumbent-flag-bug note (Houchin=true incumbent geo_id 1809; Graham/Meyer/Peck/Roark are NOT).
Zero-tier incumbents (Critical Finding 5): MD's entire 8-member delegation + IN's Baird/Carson/Messmer = 11 total.
  Mark incumbent_top_up_tier=zero (already in the incumbent map); note in the SUMMARY that these are IN SCOPE downstream.

Agent prompt spec: identical to Plan 02's <agent_prompt_spec> — one agent per state, MAX 3 CONCURRENT,
  source-URL-required, flag-don't-guess, re-confirm primary date vs official SoS, nominee_status taxonomy,
  inclusion bar, new_records_needed enumeration. For DECIDED states (IN, MD) capture the CONFIRMED Nov-3
  general field; for late-primary (MN, MO) capture the FULL qualified pre-primary field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch IN/MD/MN/MO research agents (3 concurrent)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (the validated 19-column template + agent-prompt discipline from Plan 02 — reuse verbatim)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (IN/MD/MN/MO incumbent grounding + the 11 zero-tier flags)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (MD's 8 existing_race_id; IN-9 bug row)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 1/4/5"; §"Pitfall 5" IN-9 bug)
  </read_first>
  <action>
    Dispatch one research agent per state (IN, MD, MN, MO), MAX 3 CONCURRENT, embedding Plan 02's validated <agent_prompt_spec> plus each state's incumbent grounding. Tell the IN agent: IN's May-5 primary is DECIDED — resolve the confirmed Nov-3 general nominees; the existing DB rows are PRIMARY-era and contain a known incumbent-flag bug (Houchin is the true IN-9 incumbent, geo_id 1809; her primary opponents are NOT) — do NOT treat those stale flags as truth; IN's Nov-3 general race does not exist yet (existing_race_id blank for IN). Tell the MD agent: MD's Jun-23 primary is DECIDED but all 8 races are empty scaffolds — resolve nominees; all 8 MD incumbents are zero-tier. Tell MN/MO agents: late-primary — capture the full qualified pre-primary field. Collect all 33 rows; stage flagged-UNRESOLVED districts for the Task-2 Playwright sweep. Do NOT write the CSV yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv && echo "template present; IN/MD/MN/MO agent rows collected (CSV written in Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    Agents dispatched one-per-state at max 3 concurrent (agent log in SUMMARY). Every collected row has a real unwalled source_url or is flagged UNRESOLVED. IN rows reflect the resolved Nov-3 general (not the stale primary flags); the IN-9 bug is noted, not propagated. MD's 8 rows carry existing_race_id from the audit and incumbent_top_up_tier=zero. MN/MO rows are the full pre-primary field, field_status=late-primary.
  </acceptance_criteria>
  <done>IN/MD/MN/MO field rows are researched from real sources, the IN-9 bug is documented (not treated as truth), MD existing_race_id + zero-tier flags are carried, and unresolvable districts are staged for the sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright residue sweep + assemble 160-field-table-p162.csv (33-row / 17-decided-16-late guard)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (join incumbent columns by geo_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (MD existing_race_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (column order + quoting reference)
  </read_first>
  <action>
    Playwright-sweep every flagged-UNRESOLVED district (Ballotpedia/official SoS results) and resolve with a real source_url, or record declared-so-far with filing_open_deadline for any still-open filing. Assemble `160-field-table-p162.csv` (19-column header). Carry incumbent columns from 160-incumbent-map.csv by geo_id; seeding_phase=162; ballot_system=standard for all four states; rcv=false; existing_race_id populated for MD's 8 rows (blank for IN/MN/MO); field_status=decided for the 17 IN+MD rows and late-primary for the 16 MN+MO rows. Preserve the IN-9 bug note in new_records_needed/general_candidates text (documented, not fixed). HARD GUARD: refuse to write unless exactly 33 rows, exactly 17 decided + 16 late-primary, every row has a non-empty source_url. Data assembly only — no DB write.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv')));print('rows',len(r));assert len(r)==33,len(r);d=[x for x in r if x['field_status']=='decided'];l=[x for x in r if x['field_status']=='late-primary'];assert len(d)==17 and len(l)==16,(len(d),len(l));assert all(x['source_url'].strip() for x in r);assert all(x['seeding_phase']=='162' for x in r);md=[x for x in r if x['state']=='MD'];assert len(md)==8 and all(x['existing_race_id'].strip() for x in md),'MD existing_race_id';assert all(x['incumbent_top_up_tier']=='zero' for x in md),'MD not all zero-tier'"</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table-p162.csv` has exactly 33 rows: 17 field_status=decided (IN 9 + MD 8), 16 late-primary (MN 8 + MO 8). Every row has a non-empty source_url and seeding_phase=162. MD's 8 rows have non-empty existing_race_id and incumbent_top_up_tier=zero; IN/MN/MO rows have blank existing_race_id. The IN-9 bug is present as a documentation note, not as a wrong incumbent flag. No UNRESOLVED rows remain.
  </acceptance_criteria>
  <done>The Phase-162 partial is complete (33 rows, 17 decided/16 late-primary), MD existing_race_id + 11 zero-tier flags carried, IN-9 bug documented, residue swept.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agents / orchestrator -> public web sources | Read-only fetches; no writes; only reads Plan-01 CSVs. |
| field partial CSV -> downstream seeding | Fabricated field data would corrupt Phase-162 seeding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-06 | Tampering | field-table-p162 rows (esp. IN-9) | mitigate | Source-URL-required per row; the IN-9 stale-flag bug is explicitly documented-not-propagated and cross-checked against the incumbent map; unresolvable districts Playwright-swept, never guessed. |
| T-160-07 | Information Disclosure | source citations | accept | Public election records only. |

Read-only phase: no prod writes, no API surface, no user input. ASVS V2–V6 N/A. No installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table-p162.csv` = 33 rows, 17 decided + 16 late-primary, all source_url non-empty.
- MD's 8 existing_race_id carried; MD 8 + IN 3 zero-tier flagged.
- IN-9 incumbent-flag bug documented, not propagated.
- Residue swept; agent dispatch respected max-3-concurrent.
</verification>

<success_criteria>
The Phase-162 group field is resolved (33 rows), MD's scaffolded races linked via existing_race_id, the 11 zero-tier incumbents flagged for downstream stance scope, and the IN-9 bug carried as a fix-note rather than propagated.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-03-SUMMARY.md` when done.
</output>
