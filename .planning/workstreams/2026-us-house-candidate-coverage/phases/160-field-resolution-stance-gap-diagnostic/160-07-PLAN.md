---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 07
type: execute
wave: 7
depends_on: ["160-01", "160-02", "160-03", "160-04", "160-05", "160-06"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-FIELD-TABLE.md
  - backend/scripts/diag-160-validate-field-table.py
  - backend/scripts/160-verify.sql
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01 / D-05: a single master 160-field-table.csv exists (178 rows = the 5 partials merged, one canonical artifact) with the seeding_phase column (161-165) so each seeding phase filters its slice, plus ballot_system / rcv / filing_open_deadline / nominee_status columns and the 160-FIELD-TABLE.md human view — partitioned exactly 88 decided / 90 late-primary, every row carrying a real source_url"
    - "A CSV shape validator (diag-160-validate-field-table.py) passes: 178 rows, per-state + AL-district-split counts, 88/90 partition, 19 required columns, existing_race_id present exactly for the row-level ME/MD/MA/NV/OR set and blank elsewhere"
    - "A write-free 160-verify.sql gate passes asserting the DISCOVERED 29-race baseline (ME2/MD8/MA9/NV4/OR6) — NOT a blanket 0-races claim — plus 178 district counts and an empty unexpected-vacancy set; SELECT-only, only CREATE TEMP TABLE ... ON COMMIT DROP writes"
    - "160-FIELD-TABLE.md documents the 88/90 split, the AL district-split, LA jungle-primary, AK/ME RCV over-indulgence, per-state new-record counts, and a Phase-167 primary-date cluster table (per-state exact primary date grouped by calendar week)"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv"
      provides: "Master 178-row field table with seeding_phase column; the single canonical artifact all 5 seeding phases filter"
      contains: "seeding_phase"
    - path: "backend/scripts/diag-160-validate-field-table.py"
      provides: "Read-only CSV shape gate: 178 rows, 38-state + AL-split counts, 88/90 partition, row-level existing_race_id rule"
      contains: "EXPECTED"
    - path: "backend/scripts/160-verify.sql"
      provides: "Write-free DB baseline gate asserting the discovered 29-race baseline + 178 counts + empty vacancy set"
      contains: "RAISE EXCEPTION"
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-FIELD-TABLE.md"
      provides: "Human view: 88/90 split, AL/LA/RCV special cases, per-state new-record counts, Phase-167 primary-date clusters"
      contains: "Phase-167"
  key_links:
    - from: "backend/scripts/160-verify.sql"
      to: "essentials.races + essentials.elections + essentials.districts"
      via: "discovered 29-race baseline assertion (ME/MD/MA/NV/OR), NOT blanket 0-races"
      pattern: "RAISE EXCEPTION"
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv"
      to: "160-field-table-p161.csv .. 160-field-table-p165.csv"
      via: "concatenation of the 5 seeding-group partials"
      pattern: "seeding_phase"
---

<objective>
Merge the five seeding-group partials into the single master `160-field-table.csv` (178 rows, seeding_phase column), author the CSV shape validator (`diag-160-validate-field-table.py`) and the write-free DB baseline gate (`160-verify.sql`) — the latter asserting the DISCOVERED 29-race baseline, not a blanket zero — and write the human `160-FIELD-TABLE.md` with the Phase-167 primary-date cluster table.

Purpose: Produce the one canonical field-table artifact every seeding phase (161–165) filters by seeding_phase, prove the DB baseline is exactly as discovered before any seeding, and hand Phase 167 its date-cluster input. This plan is the phase gate.
Output: `160-field-table.csv`, `160-FIELD-TABLE.md`, `diag-160-validate-field-table.py`, `160-verify.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv

<interfaces>
<!-- Templates the executor adapts; read directly. -->

backend/scripts/diag-154-validate-field-table.py — PRIMARY VALIDATOR TEMPLATE (132 lines):
  - REQUIRED_COLS (base 15) + fail()/pass() style + per-row source_url/nominee_status non-empty +
    PID_EXEMPT set for open-seat/special/vacancy rows + UUID-shape existing_race_id check.
  - Wave-2 specifics to REPLACE: EXPECTED (8 states) -> the 38-state map (total 178);
    EXPECTED_DECIDED 89 / EXPECTED_PENDING 24 -> EXPECTED_DECIDED 88 / EXPECTED_LATE 90;
    the state-blanket existing_race_id rule -> a row-level EXISTING_RACE_STATES={'ME','MD','MA','NV','OR'} check;
    ADD the 4 D-05 columns to REQUIRED_COLS: seeding_phase, ballot_system, rcv, filing_open_deadline;
    ADD AL district-split assertion (AL-3/4/5 field_status=decided, AL-1/2/6/7 field_status=late-primary).

backend/scripts/154-verify.sql — PRIMARY GATE TEMPLATE (154 lines):
  - "\set ON_ERROR_STOP on" + single DO block (DECLARE/BEGIN/END) with per-assertion RAISE EXCEPTION/RAISE NOTICE.
  - CREATE TEMP TABLE ... ON COMMIT DROP for diffing (the only write; SELECT-only otherwise).
  - Join path: races.office_id -> offices.id; offices.district_id -> districts.id; district geo via districts.geo_id.
  - A2 must be REWRITTEN (not copied): assert the discovered 29-race baseline (ME2/MD8/MA9/NV4/OR6) + 0 races for
    the other 33 states + race_candidates counts (NV=9, MA=2, ME=2 general; MD=0, OR=0) — NOT a blanket 0-candidate claim.
  - A3 expected-vacant set is EMPTY for Wave-3 (Query C returned 0 rows); assert all 178 districts have exactly 1 holder.

Master merge math: p161(37)+p162(33)+p163(36)+p164(38)+p165(34)=178. Decided 0+17+18+29+24=88. Late 37+16+18+9+10=90.
Phase-167 cluster input (Claude's discretion, D-05): per-state exact 2026 primary date grouped by calendar week
  (spans Jul-21 AZ through Sep-15 DE; LA's Aug-5-7 qualifying is its own case).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge the 5 partials into master 160-field-table.csv + write 160-FIELD-TABLE.md</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv, .planning/phases/160-field-resolution-stance-gap-diagnostic/160-FIELD-TABLE.md</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (through p165 — the 5 partials to concatenate; same 19-column header)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (confirm the ME/MD/MA/NV/OR existing_race_id set matches the merged rows)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 1" per-state dates for the Phase-167 cluster table; §"Open Questions 3" clustering guidance)
  </read_first>
  <action>
    Concatenate the 5 partials (160-field-table-p161.csv .. p165.csv) into a single master `160-field-table.csv` with ONE 19-column header row followed by all 178 data rows (dedupe the per-partial header lines; preserve each partial's rows verbatim). Sort by state_fips then geo_id for stable ordering. Then write `160-FIELD-TABLE.md` (prose + tables, no fenced code) documenting: the 88-decided / 90-late-primary split; per-state district counts (all 38 states); the AL district-split (AL-3/4/5 decided vs AL-1/2/6/7 late-primary); LA's jungle-primary (open-primary-nov3, qualifying Aug 5-7); the AK/ME RCV over-indulgence (rcv=true, exhaustive fields per D-04a); the per-state new-record counts (sum of new_records_needed); the 13 zero-tier incumbents (MD 8 + IN 3 + ME 2) in downstream scope; the pre-existing-race reuse (ME/MD/MA/NV/OR = 29 rows) with the NV-2 and IN-9 anomaly callouts; and a Phase-167 Primary-Date Cluster table grouping every state's exact 2026 primary date by calendar week (LA's Aug 5-7 qualifying as its own row). Data + prose only — no DB writes.
  </action>
  <verify>
    <automated>python3 -c "import csv,collections;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv')));assert len(r)==178,len(r);d=[x for x in r if x['field_status']=='decided'];l=[x for x in r if x['field_status']=='late-primary'];assert len(d)==88 and len(l)==90,(len(d),len(l));sp=dict(collections.Counter(x['seeding_phase'] for x in r));assert sp=={'161':37,'162':33,'163':36,'164':38,'165':34},sp;assert all(x['source_url'].strip() for x in r)" && grep -q "Phase-167" .planning/phases/160-field-resolution-stance-gap-diagnostic/160-FIELD-TABLE.md && echo MERGE_OK</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table.csv` has exactly 178 data rows with one 19-column header. The seeding_phase distribution is exactly {161:37, 162:33, 163:36, 164:38, 165:34}. The field_status partition is exactly 88 decided + 90 late-primary. Every row has a non-empty source_url. `160-FIELD-TABLE.md` contains the 88/90 split, AL district-split, LA jungle-primary, AK/ME RCV notes, per-state new-record counts, and a Phase-167 primary-date cluster table.
  </acceptance_criteria>
  <done>The master 178-row field table exists (seeding_phase 161-165, 88/90 partition) and the human FIELD-TABLE.md documents all special cases + the Phase-167 clusters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write diag-160-validate-field-table.py (178 rows, 88/90, AL split, row-level existing_race_id)</name>
  <files>backend/scripts/diag-160-validate-field-table.py</files>
  <read_first>
    - backend/scripts/diag-154-validate-field-table.py (PRIMARY TEMPLATE — clone; adapt CSV_PATH, EXPECTED, row count, partition, existing_race_id rule, add 4 columns + AL split)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table.csv (the Task-1 artifact this validates)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md (§"diag-160-validate-field-table.py" — the exact adaptations)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md (§"Wave 0 Requirements" — Part D contract)
  </read_first>
  <behavior>
    - Passes on the valid Task-1 CSV: 178 rows; per-state EXPECTED counts for all 38 states; 88 decided + 90 late-primary; 19 required columns present; source_url + nominee_status non-empty per row.
    - existing_race_id required (UUID-shape) exactly for rows whose state is in {ME, MD, MA, NV, OR}; blank for all other rows (row-level check, not a state-blanket check).
    - AL split assertion: AL rows with cd in {3,4,5} are field_status=decided; AL rows with cd in {1,2,6,7} are field_status=late-primary.
    - Fails (non-zero exit) on a wrong row count, a bad field_status value, a mis-partition, or a misplaced existing_race_id.
  </behavior>
  <action>
    Clone `backend/scripts/diag-154-validate-field-table.py` to `backend/scripts/diag-160-validate-field-table.py`. Set CSV_PATH to the 160 phase dir `160-field-table.csv`. Replace EXPECTED with the 38-state per-state map (WA10 AZ9 TN9 MA9 IN9 MD8 MN8 MO8 WI8 CO8 AL7 SC7 LA6 KY6 OR6 CT5 OK5 AR4 IA4 KS4 MS4 NV4 UT4 NM3 NE3 WV2 ID2 HI2 ME2 NH2 RI2 MT2 AK1 DE1 ND1 SD1 VT1 WY1), EXPECTED_TOTAL=178. Replace the decided/pending partition with EXPECTED_DECIDED=88 / EXPECTED_LATE=90 checking field_status in {decided, late-primary}. Extend REQUIRED_COLS with seeding_phase, ballot_system, rcv, filing_open_deadline (19 total). Replace the state-blanket existing_race_id rule with a row-level rule: `EXISTING_RACE_STATES = {'ME','MD','MA','NV','OR'}` — require a UUID-shape existing_race_id when state is in that set, require blank otherwise. Add an AL district-split assertion (AL cd 3/4/5 => decided; AL cd 1/2/6/7 => late-primary). Keep the PID_EXEMPT set for open-seat/special/vacancy blank-pid rows. Print PASS with the partition summary on success; print FAIL + offending rows and exit 1 otherwise. Pure CSV-shape gate — no DB access.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && python3 scripts/diag-160-validate-field-table.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    `cd /c/EV-Accounts/backend && python3 scripts/diag-160-validate-field-table.py` prints PASS and exits 0 against the Task-1 CSV. The validator asserts 178 rows; the 38-state per-state counts; the 88-decided/90-late partition; the 19 required columns; source_url + nominee_status non-empty; the row-level ME/MD/MA/NV/OR existing_race_id rule; and the AL district-split. Corrupting the row count or a field_status causes FAIL + non-zero exit. `grep -q "no DB\|import csv" scripts/diag-160-validate-field-table.py` confirms no DB import (psycopg/pg) is present.
  </acceptance_criteria>
  <done>diag-160-validate-field-table.py validates the master CSV shape (178 rows, 88/90, AL split, row-level existing_race_id, 19 columns) and exits 0 on the valid artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write 160-verify.sql (write-free gate; discovered 29-race baseline + 178 counts + empty vacancy set)</name>
  <files>backend/scripts/160-verify.sql</files>
  <read_first>
    - backend/scripts/154-verify.sql (PRIMARY TEMPLATE — the write-free DO-block gate; clone the A1/A2/A3 assertion structure)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md (§"160-verify.sql" — the exact A1/A2/A3 adaptations, esp. the rewritten A2 baseline)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (the discovered 29-race + candidate counts to assert)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (confirm the empty-vacancy expectation — all 178 have exactly 1 holder)
  </read_first>
  <behavior>
    - A1: per-state NATIONAL_LOWER district counts via substr(geo_id,1,2) for all 38 fips sum to 178; RAISE EXCEPTION on any mismatch.
    - A2: races on 2026-11-03 for the 5 pre-seeded states = ME 2, MD 8, MA 9, NV 4, OR 6 (sum 29); 0 pre-seeded 2026-11-03 races for the other 33 states; race_candidates counts NV=9, MA=2, ME=2 (general), MD=0, OR=0 — NOT a blanket 0-candidate claim.
    - A3: every one of the 178 NATIONAL_LOWER districts has exactly 1 holder (expected-vacant set is EMPTY); RAISE EXCEPTION naming any district with COUNT(politician_id) <> 1.
    - Final RAISE NOTICE 'ALL ASSERTIONS PASSED (160 baseline)'.
  </behavior>
  <action>
    Author `backend/scripts/160-verify.sql` as a write-free pre-seeding baseline gate cloning the `154-verify.sql` style ("\set ON_ERROR_STOP on" + one DO block; SELECT-only; only CREATE TEMP TABLE ... ON COMMIT DROP writes). Assert:
    A1 — build a 38-state _expected_counts temp table and assert per-state NATIONAL_LOWER counts via substr(d.geo_id,1,2), total 178; RAISE EXCEPTION with a string_agg mismatch diff, else RAISE NOTICE PASS.
    A2 — REWRITE the Wave-2 "0 races except VA" assertion: build an _expected_races temp table with ('23','ME',2),('24','MD',8),('25','MA',9),('32','NV',4),('41','OR',6); assert (a) the 5 states' 2026-11-03 NATIONAL_LOWER race counts equal those values (sum 29), (b) the other 33 states have 0 pre-seeded 2026-11-03 races, (c) race_candidates counts match the discovered baseline (NV=9, MA=2, ME=2 general; MD=0, OR=0) — do NOT assert 0 candidates blanket. RAISE EXCEPTION + name offenders on mismatch.
    A3 — holder invariant with an EMPTY expected-vacant set: assert the set of NATIONAL_LOWER districts (38 fips) with COUNT(politician_id) <> 1 is empty; RAISE EXCEPTION naming any offender. Do NOT hardcode any special-seat geo_id (Query C was empty this session).
    Optionally RAISE NOTICE (not EXCEPTION) the 16-collision count as informational only. End with RAISE NOTICE 'ALL ASSERTIONS PASSED (160 baseline)'. Header comment must state this is a write-free pre-seeding gate asserting the DISCOVERED 29-race baseline (not 0 races).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/160-verify.sql 2>&1 | grep -E "ALL ASSERTIONS PASSED \(160 baseline\)"</automated>
  </verify>
  <acceptance_criteria>
    `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/160-verify.sql` exits 0 and prints `ALL ASSERTIONS PASSED (160 baseline)` after per-assertion PASS notices for A1 (178 counts), A2 (29-race discovered baseline + 0 races for the other 33 + the NV=9/MA=2/ME=2 candidate counts), and A3 (empty unexpected-vacancy set). The script contains no INSERT/UPDATE/DELETE into essentials|inform (only CREATE TEMP TABLE ... ON COMMIT DROP). `grep -c "RAISE EXCEPTION" scripts/160-verify.sql` returns >= 3. It does NOT assert a blanket "0 races" or "0 candidates" claim.
  </acceptance_criteria>
  <done>160-verify.sql is a write-free gate asserting the 178 district counts, the discovered 29-race baseline (ME2/MD8/MA9/NV4/OR6 + NV=9/MA=2/ME=2 candidates, not blanket zero), and the empty unexpected-vacancy set — exits 0 against the live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 160-verify.sql -> production DB | SELECT-only via existing DATABASE_URL; only CREATE TEMP TABLE ... ON COMMIT DROP writes. |
| validator -> filesystem | Python validator reads only the local CSV; no DB, no network. |
| master field table -> downstream seeding | The single canonical artifact all 5 seeding phases consume; must faithfully merge the 5 sourced partials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-15 | Tampering | 160-verify.sql gate | mitigate | Write-free by construction: "\set ON_ERROR_STOP on", SELECT-only, only ON COMMIT DROP temp tables; acceptance criteria assert no INSERT/UPDATE/DELETE. Never --commit. |
| T-160-16 | Tampering | master merge fidelity | mitigate | Merge is verbatim concatenation of the 5 sourced partials with a 178-row + 88/90 + seeding_phase-distribution guard; the validator re-checks shape and the row-level existing_race_id rule. |
| T-160-17 | Information Disclosure | .env / DATABASE_URL | mitigate | .env uncommitted; gate reads DATABASE_URL via psql env, never echoes it; artifacts contain only public data. |

Read-only phase: no prod writes, no new API surface, no user input. ASVS V2–V6 Not Applicable (RESEARCH §Security Domain). No npm/pip/cargo installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table.csv` = 178 rows, seeding_phase {161:37,162:33,163:36,164:38,165:34}, 88 decided + 90 late-primary, all source_url non-empty.
- `diag-160-validate-field-table.py` exits 0 (178 rows, 88/90, AL split, row-level existing_race_id, 19 columns).
- `160-verify.sql` exits 0 with `ALL ASSERTIONS PASSED (160 baseline)` asserting the discovered 29-race baseline (not 0 races) + 178 counts + empty vacancy set.
- `160-FIELD-TABLE.md` documents the special cases + Phase-167 clusters.
</verification>

<success_criteria>
The single master field table exists (178 rows, seeding_phase, 88/90), the CSV shape validator and the write-free DB gate both pass — the gate asserting the DISCOVERED 29-race baseline rather than a blanket zero — and the human FIELD-TABLE.md hands Phase 167 its primary-date clusters. Phase 160 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-07-SUMMARY.md` when done.
</output>
