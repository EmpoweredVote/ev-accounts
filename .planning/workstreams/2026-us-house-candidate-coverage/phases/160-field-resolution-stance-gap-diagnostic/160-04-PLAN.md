---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 04
type: execute
wave: 4
depends_on: ["160-03"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01: a 36-row field-table partial exists for the Phase-163 group (WI 8 + CO 8 + AL 7 + SC 7 + LA 6): 18 decided + 18 late-primary"
    - "Alabama is district-split (Critical Finding 2): AL-3/4/5 tagged field_status=decided (May-19 primary + Jun-16 runoff resolved); AL-1/2/6/7 tagged field_status=late-primary (court-ordered special primary Aug-11, no runoff) — NEVER tagged uniformly as one state"
    - "Louisiana is jungle-primary (Critical Finding 3): all 6 LA rows tagged ballot_system=open-primary-nov3, field_status=late-primary, filing_open_deadline=2026-08-07 (qualifying Aug 5-7); LA has no separate House primary — the Nov-3 ballot is round one"
    - "D-02: every row cites a real fetched source_url from an unwalled route; AL district-split rows and LA rows each cite a district/state-specific official source; unresolvable districts Playwright-swept, never guessed"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv"
      provides: "36-row Phase-163 field partial with AL district-level split + LA jungle-primary handling"
      contains: "open-primary-nov3"
  key_links:
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv"
      via: "incumbent columns carried by geo_id"
      pattern: "incumbent_pid"
---

<objective>
Resolve the Phase-163 seeding-group field (WI 8 + CO 8 + AL 7 + SC 7 + LA 6 = 36 districts) into `160-field-table-p163.csv`, handling the two nonstandard cases: Alabama's district-level decided/late split and Louisiana's cancelled-closed-primary reversion to a jungle primary.

Purpose: Prevent the two biggest Wave-3 classification errors — tagging AL uniformly (it is split AL-3/4/5 decided vs AL-1/2/6/7 late-primary) and treating LA as a normal late-primary state with a knowable primary date (it has none; qualifying closes Aug 7 and the Nov-3 ballot is round one).
Output: `160-field-table-p163.csv` (36 rows, 18 decided + 18 late-primary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv

<interfaces>
<!-- Reuse the 19-column schema + agent prompt spec validated in Plan 02. -->

Group 163 classification (160-RESEARCH.md Critical Findings 1/2/3):
  WI Aug-11 standard late-primary (8) | CO Jun-30 standard DECIDED (8, re-verify — razor-close date) |
  SC Jun-9 standard DECIDED (7) |
  AL SPLIT: AL-3/4/5 DECIDED (May-19 primary + Jun-16 runoff, 3 districts) ;
    AL-1/2/6/7 late-primary (court-ordered special primary Aug-11, NO runoff, 4 districts) |
  LA open/jungle primary (6): ballot_system=open-primary-nov3, field_status=late-primary,
    filing_open_deadline=2026-08-07 (qualifying Aug 5-7); NO separate House primary; verify CD-6 (Cleo Fields)
    district lines are final for Nov-3 (redistricting litigation risk).
Decided rows = CO8 + SC7 + AL-3 = 18. Late-primary rows = WI8 + AL-4 + LA6 = 18. Total 36.

Agent prompt spec: identical to Plan 02's — one agent per state, MAX 3 CONCURRENT, source-URL-required,
  flag-don't-guess, re-confirm primary date vs official SoS, nominee_status taxonomy, inclusion bar,
  new_records_needed. The AL agent MUST resolve each of the 7 districts to its own field_status
  (3 decided + 4 late) with a district-specific source. The LA agent MUST NOT invent a primary date.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch WI/CO/AL/SC/LA research agents (3 concurrent; AL district-split + LA jungle emphasis)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (validated 19-column template + agent-prompt discipline)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (WI/CO/AL/SC/LA incumbent grounding)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 2" AL split dates/districts; §"Critical Finding 3" LA jungle primary + qualifying Aug 5-7 + CD-6 litigation; §"Pitfall 1/2")
  </read_first>
  <action>
    Dispatch one research agent per state (WI, CO, AL, SC, LA), MAX 3 CONCURRENT, embedding Plan 02's validated <agent_prompt_spec>. State-specific instructions:
    - CO agent: Jun-30 primary is decided but razor-close to the build date — re-verify it was held and resolve the Nov-3 general; field_status=decided.
    - SC agent: Jun-9 decided; resolve Nov-3 general.
    - WI agent: Aug-11 late-primary; capture the full pre-primary field.
    - AL agent: DISTRICT-SPLIT — resolve AL-3/4/5 as field_status=decided (May-19 primary + Jun-16 runoff, already resolved) and AL-1/2/6/7 as field_status=late-primary (court-ordered special primary Aug-11, NO runoff possible); every AL row must cite a DISTRICT-specific official source; re-verify the split dates have not moved again (Assumption A6).
    - LA agent: jungle primary — there is NO separate 2026 House primary; set ballot_system=open-primary-nov3, field_status=late-primary, filing_open_deadline=2026-08-07 (qualifying Aug 5-7); the Nov-3 ballot is round one; capture declared-so-far from LA SoS; verify CD-6 (Cleo Fields) district lines are final for Nov-3 and note if unresolved.
    Collect all 36 rows; stage flagged-UNRESOLVED districts (likely LA + AL special-primary districts) for the Task-2 Playwright sweep. Do NOT write the CSV yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv && echo "template present; WI/CO/AL/SC/LA agent rows collected with AL-split + LA-jungle handling (CSV written in Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    Agents dispatched one-per-state at max 3 concurrent. Every collected row has a real unwalled source_url or is flagged UNRESOLVED. AL's 7 rows are split 3 decided (AL-3/4/5) + 4 late-primary (AL-1/2/6/7), each with a district-specific source. LA's 6 rows are ballot_system=open-primary-nov3 / late-primary / filing_open_deadline=2026-08-07, with a note on CD-6 line finality. No AL row is tagged uniformly with the wrong sibling-district status.
  </acceptance_criteria>
  <done>WI/CO/AL/SC/LA field rows are researched from real sources with AL correctly district-split and LA correctly modeled as a jungle primary; residue staged for the sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright residue sweep + assemble 160-field-table-p163.csv (36-row / 18-18 + AL-split + LA guards)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (join incumbent columns by geo_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (column order + quoting reference)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 2/3" for the exact AL district numbers + LA tags)
  </read_first>
  <action>
    Playwright-sweep every flagged-UNRESOLVED district (Ballotpedia/official SoS) and resolve with a real source_url, or record declared-so-far with filing_open_deadline for LA and the AL Aug-11 special districts. Assemble `160-field-table-p163.csv` (19-column header). Carry incumbent columns by geo_id; seeding_phase=163; ballot_system=standard for WI/CO/SC/AL, open-primary-nov3 for LA's 6 rows; rcv=false; existing_race_id blank for all (none pre-seeded in this group). Set field_status per district: decided for CO(8)+SC(7)+AL-3/4/5(3)=18; late-primary for WI(8)+AL-1/2/6/7(4)+LA(6)=18. Set filing_open_deadline=2026-08-07 for LA's 6 rows (and any AL special/WI row where filing is still open). HARD GUARD: refuse to write unless exactly 36 rows, exactly 18 decided + 18 late-primary, all source_url non-empty, AL has exactly 3 decided + 4 late-primary rows, and LA has exactly 6 open-primary-nov3 rows. Data assembly only — no DB write.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv')));print('rows',len(r));assert len(r)==36,len(r);d=[x for x in r if x['field_status']=='decided'];l=[x for x in r if x['field_status']=='late-primary'];assert len(d)==18 and len(l)==18,(len(d),len(l));assert all(x['source_url'].strip() for x in r);al=[x for x in r if x['state']=='AL'];assert len(al)==7 and len([x for x in al if x['field_status']=='decided'])==3 and len([x for x in al if x['field_status']=='late-primary'])==4,'AL split wrong';la=[x for x in r if x['state']=='LA'];assert len(la)==6 and all(x['ballot_system']=='open-primary-nov3' and x['filing_open_deadline']=='2026-08-07' for x in la),'LA tags wrong'"</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table-p163.csv` has exactly 36 rows: 18 decided + 18 late-primary. AL's 7 rows split exactly 3 decided (AL-3/4/5) + 4 late-primary (AL-1/2/6/7). LA's 6 rows all have ballot_system=open-primary-nov3 and filing_open_deadline=2026-08-07. Every row has a non-empty source_url and seeding_phase=163. No UNRESOLVED rows remain.
  </acceptance_criteria>
  <done>The Phase-163 partial is complete (36 rows, 18/18), AL is correctly district-split, LA is correctly a jungle-primary with the Aug-7 qualifying deadline, and residue is swept.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agents / orchestrator -> public web sources | Read-only fetches; no writes; only reads Plan-01 CSVs. |
| field partial CSV -> downstream seeding | Mis-classified AL/LA rows would corrupt Phase-163 seeding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-08 | Tampering | AL district-split + LA jungle rows | mitigate | AL rows require district-specific sources and a hard 3-decided/4-late guard; LA rows require the open-primary-nov3 + Aug-7 tags with a guard; source-URL-required per row; residue Playwright-swept. |
| T-160-09 | Information Disclosure | source citations | accept | Public election records only. |

Read-only phase: no prod writes, no API surface, no user input. ASVS V2–V6 N/A. No installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table-p163.csv` = 36 rows, 18 decided + 18 late-primary, all source_url non-empty.
- AL split exactly 3 decided (AL-3/4/5) + 4 late-primary (AL-1/2/6/7).
- LA's 6 rows = open-primary-nov3 + filing_open_deadline 2026-08-07.
- Residue swept; agent dispatch respected max-3-concurrent.
</verification>

<success_criteria>
The Phase-163 group field is resolved (36 rows) with Alabama correctly district-split and Louisiana correctly modeled as a jungle primary — the two hardest classification cases in Wave 3 — every row sourced.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-04-SUMMARY.md` when done.
</output>
