---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 05
type: execute
wave: 5
depends_on: ["160-04"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01: a 38-row field-table partial exists for the Phase-164 group (KY 6 + OR 6 + CT 5 + OK 5 + AR 4 + IA 4 + KS 4 + MS 4): 29 decided (KY/OR/OK/AR/IA/MS) + 9 late-primary (CT/KS)"
    - "OR's 6 rows carry existing_race_id from the race-preexistence audit (6 scaffolded races) so Phase-164 seeding does not duplicate them"
    - "KY-CD1 (98/99 seq slots occupied) and OK-CD1 (43/99) carry a collision note referencing 160-negative-id-audit.csv so Phase-164 seeding uses the documented alternate 200+ sub-band"
    - "D-02: every row cites a real fetched source_url from an unwalled route; unresolvable districts Playwright-swept, never guessed"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv"
      provides: "38-row Phase-164 field partial; 29 decided + 9 late-primary; OR existing_race_id + KY/OK collision notes"
      contains: "seeding_phase"
  key_links:
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv"
      via: "OR existing_race_id carried"
      pattern: "existing_race_id"
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv"
      via: "KY-CD1 / OK-CD1 alternate-sub-band collision note"
      pattern: "safe_start_seq"
---

<objective>
Resolve the Phase-164 seeding-group field (KY 6 + OR 6 + CT 5 + OK 5 + AR 4 + IA 4 + KS 4 + MS 4 = 38 districts) into `160-field-table-p164.csv` — 29 decided + 9 late-primary (CT/KS) — carrying OR's 6 existing_race_id and the KY-CD1/OK-CD1 collision notes.

Purpose: Lock the largest decided-heavy group, reuse OR's scaffolded races (avoid duplicates), and surface the two near-saturated negative-ID bands (KY-CD1, OK-CD1) so Phase-164 seeding uses the documented alternate sub-band. OR data is READ-only here (a parallel session owns OR Phases 177/178 — write nothing to those dirs).
Output: `160-field-table-p164.csv` (38 rows, 29 decided + 9 late-primary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv

<interfaces>
<!-- Reuse the 19-column schema + agent prompt spec validated in Plan 02. -->

Group 164 classification (160-RESEARCH.md Critical Finding 1):
  KY May-19 DECIDED (6) | OR May-19 DECIDED (6) | OK Jun-16 DECIDED (5) | AR Mar-3 DECIDED (4) |
  IA Jun-2 DECIDED (4) | MS Mar-10 DECIDED (4) | CT Aug-11 late-primary (5) | KS Aug-4 late-primary (4).
  Decided = 29 (KY6+OR6+OK5+AR4+IA4+MS4); late-primary = 9 (CT5+KS4). All ballot_system=standard.
Pre-existing (Critical Finding 4): OR has 6 scaffolded races (2026-11-03, 0 candidates) → existing_race_id carried.
Collisions (Critical Finding 6): KY-CD1 = 98/99 seq occupied; OK-CD1 = 43/99 — both need the alternate 200+
  sub-band documented in 160-negative-id-audit.csv; carry a short note in these two rows.

Agent prompt spec: identical to Plan 02's — one agent per state, MAX 3 CONCURRENT, source-URL-required,
  flag-don't-guess, re-confirm primary date vs official SoS, nominee_status taxonomy, inclusion bar,
  new_records_needed. DECIDED states → confirmed Nov-3 general field; CT/KS (late) → full pre-primary field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch KY/OR/OK/AR/IA/MS/CT/KS research agents (batched, 3 concurrent)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (validated 19-column template + agent-prompt discipline)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (8-state incumbent grounding)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (OR's 6 existing_race_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 1" 8-state dates; §"Critical Finding 6" KY-CD1/OK-CD1)
  </read_first>
  <action>
    Dispatch one research agent per state across the 8 states in batches, MAX 3 CONCURRENT at all times (roughly three batches: e.g. KY/OR/OK, then AR/IA/MS, then CT/KS), embedding Plan 02's validated <agent_prompt_spec> plus each state's incumbent grounding. Tell the OR agent: OR's May-19 primary is DECIDED and OR already has 6 scaffolded (empty) Nov-3 races — resolve the general nominees; existing_race_id will be carried by the orchestrator; OR data is READ-only here (do not touch OR Phase-177/178 dirs). Tell KY/OK/AR/IA/MS agents: decided — resolve the Nov-3 general. Tell CT/KS agents: late-primary — capture the full pre-primary field. Collect all 38 rows; stage flagged-UNRESOLVED districts for the Task-2 Playwright sweep. Do NOT write the CSV yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv && echo "template present; 8-state agent rows collected in <=3-concurrent batches (CSV written in Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    Agents dispatched one-per-state, never more than 3 concurrent (batch log in SUMMARY). Every collected row has a real unwalled source_url or is flagged UNRESOLVED. OR's 6 rows are the resolved Nov-3 general (existing_race_id to be carried in Task 2). CT/KS rows are the full pre-primary field, field_status=late-primary. No OR Phase-177/178 dir was written.
  </acceptance_criteria>
  <done>The 8-state field rows are researched from real sources at max-3-concurrent, OR general nominees resolved against its scaffolded races, and residue staged for the sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright residue sweep + assemble 160-field-table-p164.csv (38-row / 29-decided-9-late + OR/KY/OK notes)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (join incumbent columns by geo_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (OR existing_race_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (KY-CD1 / OK-CD1 safe_start_seq)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv (column order + quoting reference)
  </read_first>
  <action>
    Playwright-sweep every flagged-UNRESOLVED district and resolve with a real source_url, or record declared-so-far with filing_open_deadline for any still-open CT/KS filing. Assemble `160-field-table-p164.csv` (19-column header). Carry incumbent columns by geo_id; seeding_phase=164; ballot_system=standard for all 8 states; rcv=false; existing_race_id populated for OR's 6 rows (blank for the other 7 states); field_status=decided for the 29 KY/OR/OK/AR/IA/MS rows and late-primary for the 9 CT/KS rows. In the KY-CD1 and OK-CD1 rows, append a short note in new_records_needed referencing 160-negative-id-audit.csv (alternate 200+ sub-band required at seeding). HARD GUARD: refuse to write unless exactly 38 rows, exactly 29 decided + 9 late-primary, all source_url non-empty, and OR has exactly 6 rows with non-empty existing_race_id. Data assembly only — no DB write.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv')));print('rows',len(r));assert len(r)==38,len(r);d=[x for x in r if x['field_status']=='decided'];l=[x for x in r if x['field_status']=='late-primary'];assert len(d)==29 and len(l)==9,(len(d),len(l));assert all(x['source_url'].strip() for x in r);assert all(x['seeding_phase']=='164' for x in r);orr=[x for x in r if x['state']=='OR'];assert len(orr)==6 and all(x['existing_race_id'].strip() for x in orr),'OR existing_race_id';ky1=[x for x in r if x['state']=='KY' and x['cd']=='1'];assert ky1 and 'sub-band' in ky1[0]['new_records_needed'].lower(),'KY-CD1 collision note missing'"</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table-p164.csv` has exactly 38 rows: 29 decided (KY/OR/OK/AR/IA/MS) + 9 late-primary (CT/KS). Every row has a non-empty source_url and seeding_phase=164. OR's 6 rows have non-empty existing_race_id; the other 7 states blank. The KY-CD1 and OK-CD1 rows carry a collision/sub-band note. No UNRESOLVED rows remain.
  </acceptance_criteria>
  <done>The Phase-164 partial is complete (38 rows, 29/9), OR existing_race_id carried, KY-CD1/OK-CD1 collision notes present, residue swept, no OR-phase-dir writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agents / orchestrator -> public web sources | Read-only fetches; no writes; only reads Plan-01 CSVs. |
| field partial CSV -> downstream seeding | Fabricated field data or duplicated OR races would corrupt Phase-164 seeding. |
| this phase -> OR Phase-177/178 dirs (parallel session) | OR data is READ-only; no writes to those phase dirs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-10 | Tampering | field-table-p164 rows | mitigate | Source-URL-required per row; OR existing_race_id carried (no duplicate races); KY-CD1/OK-CD1 collision notes prevent silent external_id overwrite at seeding; residue Playwright-swept. |
| T-160-11 | Tampering | OR Phase-177/178 dirs | mitigate | This plan writes only 160-field-table-p164.csv; OR is read-only; no cross-session collision. |

Read-only phase: no prod writes, no API surface, no user input. ASVS V2–V6 N/A. No installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table-p164.csv` = 38 rows, 29 decided + 9 late-primary, all source_url non-empty.
- OR's 6 existing_race_id carried; KY-CD1/OK-CD1 collision notes present.
- Residue swept; agent dispatch respected max-3-concurrent; no OR-phase-dir writes.
</verification>

<success_criteria>
The Phase-164 group field is resolved (38 rows, 29/9), OR's scaffolded races linked, and the two near-saturated negative-ID bands (KY-CD1, OK-CD1) flagged for the alternate sub-band — every row sourced.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-05-SUMMARY.md` when done.
</output>
