---
phase: 174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha
plan: 03
type: execute
wave: 2
depends_on: ["174-01", "174-02"]
files_modified:
  - backend/src/lib/adapters/fecAdapter.ts
  - backend/src/lib/adapters/fecAdapter.test.ts
  - backend/src/lib/fecResearch.ts
  - backend/src/lib/fecResearch.test.ts
  - backend/src/cron/campaignFinanceCron.ts
autonomous: true
requirements: [FEC-03]
tags: [fec, rate-limit, cron, cadence, backend-reliability]

must_haves:
  truths:
    - "All three FEC API call sites acquire from the shared limiter before every outbound fetch: fetchWithRetry (Schedule A), resolveCommitteeIds' API fallback, and runFecAutoMatch's searchFecCandidates (FEC-03)."
    - "On a 429 the code honors a server Retry-After (clamped to the existing 120s ceiling) when present, then X-RateLimit-Remaining, then exponential backoff as the final fallback (FEC-03)."
    - "The fec-ingest cron cadence is daily (once per day), not every 6 hours — load_date is date-granularity so 6-hourly yields zero extra freshness (FEC-03)."
  artifacts:
    - "backend/src/lib/adapters/fecAdapter.ts — acquireFecSlot() gate + defensive Retry-After/X-RateLimit-Remaining parsing in both HTTP functions."
    - "backend/src/lib/fecResearch.ts — acquireFecSlot() gate before the candidate-search fetch."
    - "backend/src/cron/campaignFinanceCron.ts — fec-ingest schedule changed 6h → daily."
    - "backend/src/lib/fecResearch.test.ts — new Vitest coverage asserting the limiter gate."
  key_links:
    - "runFecAutoMatch is admin-triggered and does NOT acquire FEC_LOCK_KEY — if it skips the limiter it can collide with the daily cron and reintroduce the 429 tail. It must import and call acquireFecSlot (Pitfall 2)."
  prohibitions:
    - "MUST NOT read or rely on X-RateLimit-Reset — that header is not sent by api.data.gov/FEC (Pitfall 1); the fallback must be the existing exponential backoff."
    - "MUST NOT sleep on an unclamped server-supplied Retry-After value — clamp to the existing 120s max (V5/DoS)."
    - "MUST NOT leave any FEC-host fetch() ungated — grep api.open.fec.gov across backend/src and confirm all three sites acquire."
---

<objective>
Wire the shared limiter (174-01) into all three FEC API call sites, add defensive server-signal backoff on 429, and change the cron cadence from 6-hourly to daily. These are the FEC-03 backstop + cadence changes layered on top of the root-cause volume cut in 174-02.

Purpose: guarantee the aggregate FEC request rate stays under the ~1,000 req/hr ceiling regardless of source count or a concurrent admin auto-match run, and stop wasting the budget re-scanning the same calendar day 4×/day.
Output: limiter gates + header-aware backoff in `fecAdapter.ts` and `fecResearch.ts`; daily cron in `campaignFinanceCron.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH-amendments.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- MODIFIED `fecAdapter.ts`: `acquireFecSlot()` called before every `fetch` in `fetchWithRetry` and in the `resolveCommitteeIds` API-fallback loop; new `parseRetryAfterMs`/`readRemaining` helpers; 429 branches prefer a clamped server delay.
- MODIFIED `fecResearch.ts`: `acquireFecSlot()` called before `searchFecCandidates`' fetch.
- NEW `backend/src/lib/fecResearch.test.ts`.
- MODIFIED `campaignFinanceCron.ts`: fec-ingest cron expression 6h → daily + updated comments/log strings.
- No migration, no schema change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FEC-03 — gate fecAdapter HTTP with acquireFecSlot + honor Retry-After/X-RateLimit-Remaining</name>
  <files>backend/src/lib/adapters/fecAdapter.ts, backend/src/lib/adapters/fecAdapter.test.ts</files>
  <read_first>
    - backend/src/lib/adapters/fecAdapter.ts lines 87-132 (`resolveCommitteeIds` API-fallback loop — post 174-02 this is the fallback branch) and 471-524 (`fetchWithRetry`) — the two 429/backoff loops the limiter and header parsing hook into.
    - backend/src/lib/fecRateLimiter.ts (created in 174-01) — `acquireFecSlot()` signature to import.
    - 174-RESEARCH.md "Pattern 2" (parseRetryAfterMs/readRemaining, clamp to 120s), "Pitfall 1" (no X-RateLimit-Reset), and Security Domain V5 (clamp server-supplied delay).
  </read_first>
  <behavior>
    - Every outbound fetch in fetchWithRetry and in resolveCommitteeIds' API-fallback branch is preceded by await acquireFecSlot() (including before each retry attempt).
    - On a 429, when a Retry-After header is present its parsed value (seconds or HTTP-date), clamped to <= 120000ms, is used as the sleep; when absent the existing exponential delay is used.
    - X-RateLimit-Remaining is read defensively from responses; no code path reads or waits on X-RateLimit-Reset.
  </behavior>
  <action>
    Import `acquireFecSlot` from `../fecRateLimiter.js` into `fecAdapter.ts`. In `fetchWithRetry`, add `await acquireFecSlot()` as the first statement inside the retry loop (before the `fetch`), so it gates every attempt including retries (FEC-03). Do the same in the API-fallback branch of `resolveCommitteeIds` (the candidate-search loop retained by 174-02). Add two pure helpers: `parseRetryAfterMs(response)` — reads the `retry-after` header, returns `seconds*1000` for a numeric value or `Date.parse(...) - Date.now()` for an HTTP-date, null if absent/unparseable; and `readRemaining(response)` — returns `Number` of `x-ratelimit-remaining` or null. In both 429 branches, compute `const serverDelay = parseRetryAfterMs(response)` and sleep `Math.min(serverDelay ?? delayMs, 120_000)` (clamp any server-supplied value to the existing 120s ceiling — never sleep on an unclamped header, V5/DoS), keeping the existing `delayMs = Math.min(delayMs*2, 120_000)` progression as the fallback. Do NOT reference `x-ratelimit-reset` anywhere (not sent by this API — Pitfall 1). Extend `fecAdapter.test.ts`: assert `acquireFecSlot` (mock `../fecRateLimiter.js`) is called before a mocked `fetch`; assert a 429 response carrying a numeric `retry-after` sleeps the parsed (clamped) value rather than the raw exponential; assert an absent header falls back to exponential.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/adapters/fecAdapter.test.ts -t "limiter|retry-after|rate limit"</automated>
  </verify>
  <acceptance_criteria>
    - `acquireFecSlot` is awaited before every FEC fetch in both `fetchWithRetry` and the `resolveCommitteeIds` API fallback.
    - A present numeric/HTTP-date Retry-After drives the 429 sleep, clamped to <= 120000ms; absent → exponential fallback.
    - No occurrence of `x-ratelimit-reset` in the file.
    - Tests green.
  </acceptance_criteria>
  <done>Both FEC adapter HTTP paths acquire the shared limiter and honor a clamped server Retry-After, with exponential backoff as the documented fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FEC-03 — gate the admin auto-match FEC call site (fecResearch)</name>
  <files>backend/src/lib/fecResearch.ts, backend/src/lib/fecResearch.test.ts</files>
  <read_first>
    - backend/src/lib/fecResearch.ts lines 23-24 and 177-228 (`FEC_CANDIDATES_URL`, `searchFecCandidates` — the bare `fetch()` with no lock/limiter today) and 281-342 (`runFecAutoMatch` orchestration).
    - backend/src/lib/fecRateLimiter.ts — `acquireFecSlot()` to import.
    - 174-RESEARCH.md "Pitfall 2" (this admin-triggered site shares the FEC key but not the lock; it must acquire the same limiter for FEC-03's 'every outbound request' claim to hold).
    - backend/src/lib/discoveryCron.test.ts lines 1-53 — the `vi.mock('./env.js', ...)` + `vi.mock('./db.js', ...)` startup-guard convention this new test file must follow.
  </read_first>
  <behavior>
    - searchFecCandidates awaits acquireFecSlot() before its fetch, on every call (including the last-name fallback search).
    - A new fecResearch.test.ts proves the limiter is acquired before the candidate-search fetch.
  </behavior>
  <action>
    Import `acquireFecSlot` from `./fecRateLimiter.js` into `fecResearch.ts` and add `await acquireFecSlot()` immediately before the `fetch(${FEC_CANDIDATES_URL}?...)` in `searchFecCandidates` (FEC-03) so both the primary and the last-name-fallback searches in `runFecAutoMatch` are gated. Create `backend/src/lib/fecResearch.test.ts` following the repo mocking convention (`vi.mock('./env.js', ...)`, `vi.mock('./db.js', ...)`, `vi.mock('./fecRateLimiter.js', ...)` with a spy on `acquireFecSlot`, and `vi.stubGlobal('fetch', ...)`); assert that calling `searchFecCandidates` awaits `acquireFecSlot` before issuing the `fetch`.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/fecResearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `searchFecCandidates` acquires the shared limiter before every candidate-search fetch.
    - `fecResearch.test.ts` exists and asserts the acquire-before-fetch ordering.
    - `grep api.open.fec.gov` across backend/src shows all three FEC hosts (2 in fecAdapter, 1 in fecResearch) acquire the limiter.
  </acceptance_criteria>
  <done>The admin auto-match path can no longer collide with the daily cron to breach the ceiling — all three FEC call sites share one limiter.</done>
</task>

<task type="auto">
  <name>Task 3: FEC-03 — change fec-ingest cron cadence from 6-hourly to daily</name>
  <files>backend/src/cron/campaignFinanceCron.ts</files>
  <read_first>
    - backend/src/cron/campaignFinanceCron.ts lines 26-42 — the `cron.schedule('0 */6 * * *', ...)` fec-ingest registration + its log string and header comment.
    - 174-RESEARCH-amendments.md section 2 point 3 (daily, ~06:00 UTC — a safety margin after FEC's observed ~03:05 UTC nightly load) and section 1 (load_date is date-granularity so sub-daily yields no freshness).
  </read_first>
  <action>
    Change the fec-ingest cron expression from `0 */6 * * *` to `0 6 * * *` (once daily at 06:00 UTC — several hours after FEC's observed ~03:05 UTC nightly load cluster, per RESEARCH-amendments A3) (FEC-03). Update the registration's trailing `console.log` string and the file header comment to say "daily at 06:00 UTC" instead of "every 6 hours". Leave the netfile ('0 3 1 * *') and ocpf ('0 4 1 * *') schedules untouched. Keep `timezone: 'UTC'` and `name: 'fec-ingest'`.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -n "0 6 \* \* \*" src/cron/campaignFinanceCron.ts && ! grep -n "0 \*/6 \* \* \*" src/cron/campaignFinanceCron.ts</automated>
  </verify>
  <acceptance_criteria>
    - The fec-ingest schedule is `0 6 * * *` (daily); the `0 */6 * * *` expression no longer appears.
    - The log string and header comment describe the daily cadence.
    - netfile + ocpf schedules unchanged.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The FEC ingest cron fires once daily at 06:00 UTC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → api.data.gov/FEC (3 call sites) | Untrusted response headers (Retry-After) may be malformed/adversarial. |
| admin route → runFecAutoMatch | Authenticated admin action that shares the FEC key but not the cron lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-07 | Denial of Service | malformed/huge Retry-After header | medium | mitigate | Clamp any parsed server delay to the existing 120s ceiling before sleeping (V5); never sleep on an unclamped value. |
| T-174-08 | Denial of Service | unthrottled admin auto-match colliding with cron | high | mitigate | runFecAutoMatch's searchFecCandidates now acquires the shared limiter (Pitfall 2). |
| T-174-09 | Tampering | reliance on nonexistent X-RateLimit-Reset | low | mitigate | Code reads only Retry-After/X-RateLimit-Remaining; exponential backoff is the fallback (Pitfall 1). |
| T-174-SC | Tampering | dependencies | low | accept | No package-manager install — nothing to audit. |
</threat_model>

<verification>
- `cd backend && npx vitest run src/lib/adapters/fecAdapter.test.ts src/lib/fecResearch.test.ts` green.
- `cd backend && npx tsc --noEmit` clean.
- `grep -rn "api.open.fec.gov" backend/src` returns only the two `fecAdapter.ts` sites + the one `fecResearch.ts` site, each preceded by an `acquireFecSlot()` acquire.
</verification>

<success_criteria>
Every FEC API request across all three sites is limiter-gated and honors a clamped server Retry-After on 429; the ingest cron fires once daily.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-03-SUMMARY.md` when done.
</output>
