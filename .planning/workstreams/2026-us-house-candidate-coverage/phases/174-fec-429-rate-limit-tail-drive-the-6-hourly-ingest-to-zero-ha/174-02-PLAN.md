---
phase: 174-fec-429-rate-limit-tail
plan: 02
type: execute
wave: 2
depends_on: ["174-01"]
files_modified:
  - backend/src/lib/adapters/fecAdapter.ts
  - backend/src/lib/adapters/fecAdapter.test.ts
autonomous: true
requirements: [FEC-01, FEC-02, FEC-03]
must_haves:
  truths:
    - "A warm resolveCommitteeIds() call for a candidate with cached committee IDs issues zero FEC HTTP requests"
    - "A cold resolveCommitteeIds() call (cache miss/expiry) falls through to the live FEC lookup unchanged, then populates the cache with a 30-day TTL"
    - "Both resolveCommitteeIds() and fetchWithRetry() call acquireFecSlot() before every outbound fetch attempt (including retries)"
    - "On a 429, both paths back off by the server Retry-After interval when the header is present and parseable, otherwise by the existing exponential delay"
    - "Any server-provided delay is clamped to the existing 120s ceiling before sleeping"
  artifacts:
    - backend/src/lib/adapters/fecAdapter.ts
    - backend/src/lib/adapters/fecAdapter.test.ts
  key_links:
    - "resolveCommitteeIds imports the existing cache singleton from ../cache.js (FEC-01)"
    - "Both HTTP functions import acquireFecSlot from ../fecRateLimiter.js (FEC-03, created in Plan 01)"
  prohibitions:
    - "MUST NOT cache an empty ([]) committee-ID result — a newly-filing candidate must be re-checked next run, not suppressed for 30 days"
    - "MUST NOT read or depend on a rate-limit reset-timestamp header — that header is not documented for api.data.gov/FEC; the true fallback is the existing exponential backoff"
    - "MUST NOT sleep on an unclamped server-supplied delay value (bounded-DoS guard)"
    - "MUST NOT add any new npm dependency"
---

<objective>
Harden the two `fecAdapter.ts` FEC HTTP call sites: (FEC-01) cache `resolveCommitteeIds` results in the existing `cache.ts` singleton with a 30-day TTL and a correct miss/empty-result path; (FEC-02) parse `Retry-After` defensively on 429 and read `X-RateLimit-Remaining` on every response, keeping the existing exponential backoff as the true fallback; (FEC-03) gate both functions through `acquireFecSlot()` before every fetch attempt. Add the first-ever unit test for this adapter.

Purpose: Halve request volume (cache) and pace + correctly back off the remaining requests, eliminating the residual 429 tail at the two cron call sites.
Output: Modified `fecAdapter.ts` + new `fecAdapter.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH.md
@backend/src/lib/cache.ts
@backend/src/lib/adapters/fecAdapter.ts
@backend/src/lib/discoveryCron.test.ts
</context>

<artifacts_produced>
- MODIFIED `backend/src/lib/adapters/fecAdapter.ts`: cache wrap in `resolveCommitteeIds`, `parseRetryAfterMs`/`readRemaining` helpers, `acquireFecSlot()` gate in both loops
- NEW test `backend/src/lib/adapters/fecAdapter.test.ts` (first test for this adapter)
- NEW module-level constant `COMMITTEE_CACHE_TTL_SECONDS` (30 days)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing adapter tests for cache + header backoff + limiter gate (RED)</name>
  <files>backend/src/lib/adapters/fecAdapter.test.ts</files>
  <read_first>
    - backend/src/lib/adapters/fecAdapter.ts lines 82-132 (resolveCommitteeIds) and 471-528 (fetchWithRetry + sleep)
    - backend/src/lib/cache.ts (the get/set contract to mock)
    - backend/src/lib/discoveryCron.test.ts (vi.mock/vi.hoisted conventions; mock ../db.js and ../env.js to avoid env.ts startup exit)
    - 174-RESEARCH.md "Pattern 1", "Pattern 2", "Wave 0 Gaps"
  </read_first>
  <behavior>
    - FEC-01 cache hit: cache.get returns a non-empty array -> resolveCommitteeIds returns it and fetch is NEVER called.
    - FEC-01 cache miss: cache.get returns null -> fetch is called once, returns committee IDs, and cache.set is called with a 30-day TTL and the resolved IDs.
    - FEC-01 empty result NOT cached: fetch returns results with zero principal committees -> resolveCommitteeIds returns [] and cache.set is NEVER called.
    - FEC-02 Retry-After seconds: a 429 whose Retry-After is a numeric-seconds string causes the next retry to sleep that many ms (assert via fake timers), not the default exponential.
    - FEC-02 Retry-After HTTP-date: a 429 whose Retry-After is an HTTP-date string is parsed to a positive ms delay.
    - FEC-02 Retry-After absent: a 429 with no Retry-After falls back to the existing exponential delay (2000ms first).
    - FEC-02 clamp: a Retry-After far larger than 120s sleeps at most 120000ms.
    - FEC-03 limiter gate: acquireFecSlot (mocked) is called before every fetch attempt in BOTH resolveCommitteeIds and fetchWithRetry, including once per retry.
  </behavior>
  <action>
    Create the Vitest suite. Mock `../db.js` and `../env.js` (per discoveryCron.test.ts) so importing the adapter does not trip env startup validation. Mock `../cache.js` exporting a `cache` object with `vi.fn()` `get`/`set`/`del`. Mock `../fecRateLimiter.js` exporting `acquireFecSlot` as a `vi.fn()` resolving immediately, and assert its call count. Use `vi.stubGlobal('fetch', vi.fn())` to script `Response`-like objects (status, ok, `headers.get`, `json`). Drive retry delays with `vi.useFakeTimers()` and assert the sleep duration between attempts. Because `resolveCommitteeIds` and `fetchWithRetry` are module-private, exercise them through the adapter's public surface — export a thin test hook if needed OR call `createFecAdapter(cycle).fetchStream(...)` with a scripted politician-source and a fake `onBatch`, whichever the executor finds cleaner after reading the file; document the chosen approach in a comment. This suite MUST fail initially (RED) since the source lacks caching, header parsing, and the limiter gate.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/adapters/fecAdapter.test.ts 2>&1 | grep -q "failed\|FAIL\|No test files" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/lib/adapters/fecAdapter.test.ts` exists with all eight behaviors above.
    - Suite fails before Task 2 (source not yet modified).
  </acceptance_criteria>
  <done>Test file encodes cache hit/miss/empty, Retry-After seconds/date/absent/clamp, and limiter-gate-per-attempt; fails RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FEC-01 cache + FEC-02 header backoff + FEC-03 limiter gate (GREEN)</name>
  <files>backend/src/lib/adapters/fecAdapter.ts</files>
  <read_first>
    - backend/src/lib/adapters/fecAdapter.ts lines 82-132 and 471-528
    - backend/src/lib/cache.ts
    - 174-RESEARCH.md "Pattern 1", "Pattern 2", "Common Pitfalls" (Pitfalls 1 and 3)
  </read_first>
  <action>
    Add imports: `cache` from `../cache.js` and `acquireFecSlot` from `../fecRateLimiter.js`. Add a module constant `COMMITTEE_CACHE_TTL_SECONDS = 60 * 60 * 24 * 30`.

    FEC-01 in `resolveCommitteeIds`: before the retry loop, compute cache key `fec:committee-ids:` + candidateId and `await cache.get` it; on a non-null value return it immediately (zero requests). After the existing successful lookup resolves `committees`, only when `committees.length > 0` call `await cache.set(cacheKey, committees, COMMITTEE_CACHE_TTL_SECONDS)`. Leave the empty-result branch (existing warn) uncached. Add a one-line comment stating why an empty result is deliberately not cached (newly-filing candidate must re-check next run).

    FEC-02: add two small module-private helpers — one that reads the `retry-after` response header, parsing both the numeric-seconds form and the HTTP-date form, returning null when absent/unparseable; and one that reads the `x-ratelimit-remaining` header as a number or null. In BOTH 429 branches (resolveCommitteeIds and fetchWithRetry), compute the server delay from the retry-after helper and sleep `Math.min(serverDelay ?? delayMs, 120_000)` before the existing `delayMs = Math.min(delayMs * 2, 120_000)` step. Do NOT reference any reset-timestamp header — the documented api.data.gov headers are the limit/remaining pair only; the existing exponential value remains the fallback when retry-after is absent. Optionally log `x-ratelimit-remaining` when low, but do not change control flow based on it beyond the limiter.

    FEC-03: call `await acquireFecSlot()` at the top of each retry-loop iteration (before `fetch`) in BOTH functions, so every attempt — initial and every retry — first acquires a slot.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/adapters/fecAdapter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/adapters/fecAdapter.test.ts` passes all cases.
    - `grep -c "acquireFecSlot" backend/src/lib/adapters/fecAdapter.ts` is >= 2 (both call sites gated).
    - `grep -c "fec:committee-ids:" backend/src/lib/adapters/fecAdapter.ts` is >= 1 and `grep -c "COMMITTEE_CACHE_TTL_SECONDS" backend/src/lib/adapters/fecAdapter.ts` is >= 2.
    - `grep -vi '^\s*//' backend/src/lib/adapters/fecAdapter.ts | grep -ci "x-ratelimit-reset"` returns 0 (the non-existent reset-timestamp header is never read in code).
    - `grep -c "Math.min(serverDelay ?? delayMs, 120_000)\|Math.min(serverDelay ?? delayMs, 120000)" backend/src/lib/adapters/fecAdapter.ts` is >= 2 (clamped server delay in both 429 branches).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Cache halves committee lookups (empty never cached), 429 branches honor clamped Retry-After with exponential fallback, both fetches gated by the limiter; tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node backend → api.data.gov FEC API | Untrusted upstream response headers (Retry-After) cross here |
| Node backend → cache (Redis/in-memory) | Cached committee-ID state affects whether finance data is collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-01 | Denial of Service | Retry-After header parsing | high | mitigate | Clamp any server-provided delay to the existing 120s exponential ceiling before sleeping (`Math.min(serverDelay ?? delayMs, 120_000)`) — a malformed or adversarial header cannot induce an unbounded sleep. |
| T-174-02 | Tampering (internal data integrity) | committee-ID cache | medium | mitigate | Never cache an empty result; only non-empty committee lists get the 30-day TTL, so a newly-filing candidate is re-checked next run rather than silently suppressed. |
</threat_model>

<verification>
- `cd backend && npx vitest run src/lib/adapters/fecAdapter.test.ts` green.
- `cd backend && npx tsc --noEmit` clean.
- `grep -rn "api.open.fec.gov" backend/src` still returns only fecAdapter.ts + fecResearch.ts (no new unlimited call site introduced).
</verification>

<success_criteria>
Both cron FEC call sites are cached (FEC-01), header-aware on 429 with a clamped fallback (FEC-02), and pass through the shared limiter (FEC-03), with the adapter's first unit test green.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-02-SUMMARY.md` when done.
</output>
