---
phase: 174-fec-429-rate-limit-tail
plan: 04
type: execute
wave: 3
depends_on: ["174-02", "174-03"]
files_modified:
  - .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md
autonomous: true
requirements: [FEC-04]
user_setup:
  - service: render
    why: "Deploy the backend TypeScript changes (this phase is code, not data)"
    env_vars:
      - name: RENDER_DEPLOY_HOOK
        source: "Render Dashboard -> backend service -> Settings -> Deploy Hook"
    dashboard_config:
      - task: "(Optional lever) request a higher/dedicated api.data.gov FEC key limit"
        location: "email APIinfo@fec.gov requesting 7,200/hr"
  - service: supabase
    why: "Run the read-only zero-429 verification query against prod (out-of-process, >= 6h after deploy)"
    env_vars: []
must_haves:
  truths:
    - "The full backend unit suite is green modulo the documented 21 pre-existing sandbox failures, and tsc --noEmit is clean"
    - "The FEC-01/02/03 changes are deployed to the Render backend"
    - "The request-budget/cadence decision and the FEC-key-upgrade decision are written down in a phase decision note"
    - "The read-only zero-429 verification query is documented for the operator to run >= 6h post-deploy"
  artifacts:
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md
  key_links:
    - "Deploy is the point at which FEC-01/02/03 take effect in prod; FEC-04's zero-429 outcome can only be observed after a full 6h cycle post-deploy"
  prohibitions:
    - "MUST NOT block the phase on the live 6h zero-429 observation — it is an out-of-process post-deploy check, documented as a manual verification"
    - "MUST NOT attempt to fix the 21 pre-existing unrelated sandbox suite failures (no live DB/env in sandbox)"
---

<objective>
Terminal integration + ship plan for Phase 174: run the full backend unit suite + `tsc --noEmit` across all Wave 1/2 changes, deploy the backend to Render, and record the FEC-04 documented lever — the request-budget/cadence decision (keep 6h vs drop to daily; `FEC_RATE_LIMIT_PER_MINUTE` default) and the FEC-key-upgrade decision (request 7,200/hr vs code-only pacing) — plus the exact read-only zero-429 verification query the operator runs >= 6h after deploy.

Purpose: FEC-04 requires a verified zero-429 outcome AND a documented budget/cadence/key decision. The outcome is observed out-of-process; this plan lands the code in prod and captures the decision + verification procedure.
Output: Green suite, Render deploy, and `174-FEC04-DECISION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-VALIDATION.md
@backend/src/cron/campaignFinanceCron.ts
</context>

<artifacts_produced>
- NEW `174-FEC04-DECISION.md` — budget/cadence + FEC-key-upgrade decision note and the read-only verification query
- A Render deploy of the backend (triggered via RENDER_DEPLOY_HOOK)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + typecheck</name>
  <files>backend/src/lib/fecRateLimiter.test.ts, backend/src/lib/adapters/fecAdapter.test.ts, backend/src/lib/fecResearch.test.ts</files>
  <read_first>
    - 174-VALIDATION.md "Sampling Rate" (full suite command + the 21 pre-existing-failures caveat)
    - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md (Phase 173-04 decision: 21 pre-existing unrelated failures documented, not fixed)
  </read_first>
  <action>
    From `backend/`, run the full unit suite (`npm run test:unit`) and `npx tsc --noEmit`. Confirm the three new FEC test files are green and that the only failures are within the known set of 21 pre-existing unrelated failures caused by the sandbox lacking a live DB/env (documented at Phase 173-04). Do NOT fix the pre-existing failures. If any NEW failure appears in the FEC-touched files or in tsc, stop and fix it before proceeding.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/fecRateLimiter.test.ts src/lib/adapters/fecAdapter.test.ts src/lib/fecResearch.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The three FEC test files pass.
    - `npx tsc --noEmit` is clean.
    - Full `npm run test:unit` shows no NEW failures beyond the documented 21 pre-existing sandbox failures.
  </acceptance_criteria>
  <done>All Phase-174 tests green, typecheck clean, no new regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Write the FEC-04 decision + verification note</name>
  <files>.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md</files>
  <read_first>
    - 174-RESEARCH.md "Open Questions" (budget default, cadence, key-upgrade lever) and "Validation Architecture > Phase Requirements -> Test Map" (FEC-04 row, the exact verification SQL)
  </read_first>
  <action>
    Write `174-FEC04-DECISION.md` capturing four sections. (1) Request budget — `FEC_RATE_LIMIT_PER_MINUTE` default 15 (~900/hr, ~10% margin under the 1,000/hr ceiling), env-tunable after observing real X-RateLimit-Remaining in prod logs; state the rationale for per-minute (not hourly) bucketing (avoids burst-then-stall). (2) Cadence — keep the 6-hourly cron unless the post-deploy observation still shows a tail, in which case dropping to daily is the documented fallback; record the current decision (keep 6h) with rationale. (3) FEC-key upgrade — the one-email lever to APIinfo@fec.gov for 7,200/hr; record whether it was requested and its status (requested/pending/not-pursued) — note FEC-04 can close on the code fixes alone. (4) Verification procedure — the exact read-only query for the operator to run >= 6h after deploy against prod on `transparent_motivations.ingestion_runs` filtering `adapter_name='fec'`, `status='failed'`, notes matching the 429/rate-limit ILIKE patterns, and `started_at > now() - interval '7 hours'`, expecting 0 rows; note it is run via Supabase MCP execute_sql against prod ref `kxsdzaojfaibhuzmclfq`, matching the cron-audit item-0 verification method.
  </action>
  <verify>
    <automated>test -f .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md && grep -qi "FEC_RATE_LIMIT_PER_MINUTE" .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md && grep -qi "ingestion_runs" .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC04-DECISION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `174-FEC04-DECISION.md` exists with all four sections (budget, cadence, key-upgrade, verification query).
    - The verification query targets `ingestion_runs` with the 429/rate-limit `notes ILIKE` filter and a bounded `started_at` window.
    - The `FEC_RATE_LIMIT_PER_MINUTE` default + per-minute-bucket rationale is recorded.
  </acceptance_criteria>
  <done>Decision note captures budget, cadence, key-upgrade status, and the runnable zero-429 verification query.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy backend to Render</name>
  <files>backend/src/lib/fecRateLimiter.ts, backend/src/lib/adapters/fecAdapter.ts, backend/src/lib/fecResearch.ts</files>
  <read_first>
    - $HOME/.claude/projects memory reference_render_deploy.md pattern (RENDER_DEPLOY_HOOK curl); data changes need NO deploy, but this phase is backend TypeScript so a deploy IS required
  </read_first>
  <action>
    After Task 1 is green and Task 2's note is written, trigger a Render backend deploy by curling `$RENDER_DEPLOY_HOOK`. If `RENDER_DEPLOY_HOOK` is not set in the environment, emit an auth/setup checkpoint requesting the operator supply it (per the user_setup frontmatter) rather than failing silently. Capture the returned deploy id in the SUMMARY. Do not deploy before the suite is green.
  </action>
  <verify>
    <human-check>Confirm the Render dashboard shows a new deploy for the backend service that includes the Phase-174 commit, and that the service returns to "live".</human-check>
  </verify>
  <acceptance_criteria>
    - A Render deploy was triggered post-green-suite and its id recorded in the SUMMARY.
    - FEC-04's live zero-429 observation is explicitly deferred to the operator per `174-FEC04-DECISION.md` (>= 6h post-deploy), NOT blocking phase completion.
  </acceptance_criteria>
  <done>Backend deployed with FEC-01/02/03; the out-of-process zero-429 check is documented for the operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/operator → Render deploy hook | Deploying unverified code; gated behind a green suite + typecheck |
| Operator → prod read-only query | Verification is read-only against prod; no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-06 | Tampering | Deploying a regressed build | medium | mitigate | Deploy is gated on a green full suite + `tsc --noEmit`; deploy step must not run before Task 1 passes. |
</threat_model>

<verification>
- `cd backend && npm run test:unit` shows only the documented 21 pre-existing failures.
- `cd backend && npx tsc --noEmit` clean.
- `174-FEC04-DECISION.md` exists and contains the four required sections.
- Render deploy triggered and recorded.
</verification>

<success_criteria>
Phase-174 code is green, typechecked, and deployed; the FEC-04 budget/cadence/key decision and the read-only zero-429 verification procedure are documented; the live zero-429 confirmation is handed to the operator as a documented post-deploy check.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-04-SUMMARY.md` when done.
</output>
