---
phase: 174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha
plan: 05
type: execute
wave: 4
depends_on: ["174-01", "174-02", "174-03", "174-04"]
files_modified:
  - .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC05-DECISION.md
autonomous: true
requirements: [FEC-05]
tags: [fec, deploy, verification, decision-doc, backend-reliability]

must_haves:
  truths:
    - "The full backend unit suite is green (modulo the 21 documented pre-existing sandbox failures) and tsc --noEmit is clean before deploy (FEC-05)."
    - "The Phase 174 TypeScript changes are deployed to Render (the daily cadence change is out-of-process and only takes effect after deploy) (FEC-05)."
    - "A decision doc records the daily-cadence choice, states that NO higher/dedicated api.data.gov FEC key is required (the code reaches zero-429 under the current 1,000/hr registered key), and gives the read-only zero-429 verification query as a post-deploy operator check over a ~25h window (FEC-05)."
  artifacts:
    - "174-FEC05-DECISION.md — daily-cadence + no-key-upgrade decision + the operator verification query."
  key_links:
    - "The zero-429 confirmation depends on the first real daily cron fire post-deploy (out-of-process) — it is a documented operator check, NOT a phase-blocking gate."
  prohibitions:
    - "MUST NOT block phase completion on the live zero-429 result — the first daily fire is out-of-process; capture it as a documented operator check."
    - "MUST NOT attempt to fix the 21 known pre-existing unrelated sandbox suite failures (documented in deferred-items.md) — they are not this phase's responsibility."
---

<objective>
Terminal verification + ship: run the full suite and type-check, deploy the daily-cadence code to Render, and write the FEC-05 decision doc that locks the daily cadence, records that no FEC key upgrade is required, and hands the operator the read-only zero-429 confirmation query.

Purpose: FEC-05 — verified outcome + a durable decision record so the key-upgrade option is not silently reconsidered and the daily-cadence rationale is captured.
Output: `174-FEC05-DECISION.md` + a live Render deploy of the phase changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-RESEARCH-amendments.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- NEW `174-FEC05-DECISION.md` in this phase directory.
- A Render deploy of the Phase 174 backend changes.
- No code change, no migration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + type-check green</name>
  <files>(verification only — no file writes)</files>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-VALIDATION.md — the full-suite + tsc gate and the documented 21-known-failures caveat.
    - backend/deferred-items.md (if present) — the enumerated 21 pre-existing sandbox failures to distinguish from any new regression.
  </read_first>
  <action>
    Run the full backend unit suite and the type-check. Confirm every Phase-174 test file (fecRateLimiter.test.ts, fecAdapter.test.ts, fecResearch.test.ts) is green and that no NEW failures appear beyond the 21 pre-existing unrelated sandbox failures documented for Phase 173 (no live DB/env vars in sandbox). Do NOT attempt to fix the pre-existing 21. If any NEW failure traces to a Phase-174 change, stop and fix it before deploy.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` clean.
    - All three Phase-174 test files pass.
    - No new suite failures beyond the documented 21 pre-existing ones.
  </acceptance_criteria>
  <done>The phase's code is type-clean and its tests pass under the full suite.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the daily-cadence changes to Render</name>
  <files>(deploy action — no file writes)</files>
  <read_first>
    - CLAUDE.md + memory reference_render_deploy — the `RENDER_DEPLOY_HOOK` curl deploy method (TypeScript changes need a Render deploy; pure-data changes do not — this phase changed TS, so it does).
  </read_first>
  <action>
    Trigger a Render deploy of the backend so the cadence change and the FEC-01..04 code take effect (they are out-of-process — the running dyno keeps the old 6h cron until redeployed) (FEC-05). Use the `RENDER_DEPLOY_HOOK` curl if the hook URL is available in the environment; capture the returned deploy id. If the hook is not available in this environment, record the exact deploy command in the SUMMARY and flag it for the operator to run — do not treat deploy unavailability as a code failure.
  </action>
  <verify>
    <automated>echo "Deploy triggered — record deploy id in SUMMARY (RENDER_DEPLOY_HOOK curl); operator-run if hook unavailable"</automated>
  </verify>
  <acceptance_criteria>
    - A Render deploy of the backend was triggered (deploy id captured) OR the exact deploy command is recorded for the operator with the reason it could not be auto-triggered.
  </acceptance_criteria>
  <done>The Phase 174 changes are deploying (or the deploy step is explicitly handed to the operator with the command).</done>
</task>

<task type="auto">
  <name>Task 3: Write 174-FEC05-DECISION.md</name>
  <files>.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC05-DECISION.md</files>
  <read_first>
    - 174-RESEARCH.md Open Questions 1-2 (budget default; the FEC key-upgrade lever) and 174-RESEARCH-amendments.md section 2 point 3 (daily-cadence rationale) + the confirmed 1,000/hr registered-key ceiling.
    - 174-VALIDATION.md "Manual-Only Verifications" — the FEC-05 zero-429 query and why it is operator-run.
  </read_first>
  <action>
    Write `174-FEC05-DECISION.md` recording (FEC-05): (1) DECISION — the fec-ingest cron cadence is DAILY (06:00 UTC), with the rationale that `min_load_date` is date-granularity so a sub-daily cadence yields zero additional freshness while wasting the rate-limit budget; (2) DECISION — NO higher/dedicated api.data.gov FEC key is required: the code (bulk committee resolution + incremental `min_load_date` + shared limiter/backoff) reaches zero-429 under the current 1,000/hr registered key, so the upgrade option is explicitly declined here to prevent silent reconsideration (note the 7,200/hr upgrade lever exists via APIinfo@fec.gov as a documented non-blocker); (3) the limiter budget default (`FEC_RATE_LIMIT_PER_MINUTE=15` ≈ 900/hr, ~10% margin, env-tunable); (4) the OPERATOR VERIFICATION step — after the first daily cron fire post-deploy, run the read-only query `SELECT count(*) FROM transparent_motivations.ingestion_runs WHERE adapter_name='fec' AND status='failed' AND (notes ILIKE '%429%' OR notes ILIKE '%rate limit%') AND started_at > now() - interval '25 hours'` (via Supabase MCP against prod ref `kxsdzaojfaibhuzmclfq`) and expect 0 rows; state explicitly that this confirmation is a documented operator check over a ~25h window (daily cadence, out-of-process first fire), NOT a phase-blocking gate.
  </action>
  <verify>
    <automated>test -f ".planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC05-DECISION.md" && grep -qi "daily" ".planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC05-DECISION.md" && grep -qi "not required\|no.*upgrade\|NOT required" ".planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-FEC05-DECISION.md"</automated>
  </verify>
  <acceptance_criteria>
    - The doc exists and records the daily-cadence decision + rationale.
    - The doc explicitly states no FEC key upgrade is required and why.
    - The doc includes the read-only zero-429 verification query framed as a ~25h-window operator check, not a blocking gate.
    - The doc records the limiter budget default + env knob.
  </acceptance_criteria>
  <done>The daily-cadence + no-key-upgrade decisions and the operator zero-429 check are durably recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod (Supabase MCP read query) | Read-only zero-429 verification query against production. |
| deploy hook → Render | Ships the phase's TS changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-12 | Denial of Service | premature phase closure before first daily fire | low | accept | Zero-429 confirmation is documented as an operator check over a ~25h window, not a blocking gate — matches the cron-audit item-0 verification precedent. |
| T-174-13 | Repudiation | undocumented key-upgrade reconsideration | low | mitigate | Decision doc explicitly declines the key upgrade with rationale so it is not silently revisited. |
| T-174-SC | Tampering | dependencies | low | accept | No package-manager install — nothing to audit. |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit && npm run test:unit` (green modulo the 21 documented pre-existing failures).
- `174-FEC05-DECISION.md` exists with the daily-cadence + no-key-upgrade decisions and the operator zero-429 query.
- Render deploy triggered (deploy id captured) or the deploy command handed to the operator.
</verification>

<success_criteria>
Suite + type-check green, changes deployed (or deploy handed off), and the FEC-05 decision doc durably records the daily cadence, the no-key-upgrade decision, and the operator zero-429 verification.
</success_criteria>

<output>
Create `.planning/workstreams/2026-us-house-candidate-coverage/phases/174-fec-429-rate-limit-tail-drive-the-6-hourly-ingest-to-zero-ha/174-05-SUMMARY.md` when done.
</output>
