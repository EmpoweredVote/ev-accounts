---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 09
type: execute
wave: 3
depends_on: ["163-06"]
files_modified:
  - backend/data/stance-research/la-2026-house/_merge.ts
  - backend/data/stance-research/la-2026-house/_push.ts
  - backend/data/stance-research/la-2026-house/_push_uuid.ts
  - backend/data/stance-research/la-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/la-2026-house/163-09-la-stances.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "All 27 new LA candidates (incl. severe-district candidates on the withheld election, and LA-5's 13 open-seat filers) have sourced chairs-not-polarity federal-24 stances (or a gate-pinned whole-record skip with a written search trail), 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every LA answer pairs to an inform.politician_context row with a real fetched source_url"
    - "LA's partial-tier incumbents are skipped via the diagnostic (out of scope)"
  artifacts:
    - path: "backend/data/stance-research/la-2026-house/163-09-la-stances.csv"
      provides: "LA new-candidate stance rows"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push.ts / _push_uuid.ts"
      pattern: "politician_context"
---

<objective>
Research + push LA stances — all 27 new LA candidates (the phase's heaviest stance slice; LA-5 alone has 13 open-seat filers), INCLUDING candidates in severe (withheld) districts. LA's incumbents are all partial-tier → skipped via the diagnostic. Set up the la-2026-house scaffold, regenerate _TOPIC_SCALE_FULL.txt LIVE, run agents at 3-concurrency (1 per district) with a primary-source verification pass, push to PROD. Continues USHC3-05.

Output: la-2026-house scaffold + stance CSV pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-06-SUMMARY.md
@backend/data/stance-research/mn-2026-house/_push.ts
@backend/data/stance-research/mn-2026-house/_merge.ts

<interfaces>
Scaffold: copy _push.ts/_merge.ts and _push_uuid.ts byte-for-byte from mn-2026-house/ into la-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt LIVE (federal-24 subset; re-verify the live active count). LA targets = the 27 new LA candidates (external_id band -220699..-220101, from 163-06-SUMMARY.md) — INCLUDES severe-district candidates AND LA-5's 13 open-seat filers. LA incumbents (-22001..-22006) are all partial-tier → OUT of scope. Standards: chairs-not-polarity, never party-inferred, honest-skip with written trail (whole-record gate-pinned), 0-unsourced, mandatory primary-source verification pass, 3-concurrency cap, first-wave validation, embed exact 1-5 scale texts. LA-5's 13-candidate open-seat field will have many thin-evidence filers — honest-skip with a written trail rather than infer. Because LA runs a jungle primary (all parties on one ballot), do NOT let a candidate's party shortcut the evidence bar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold la-2026-house + regenerate the federal-24 topic scale live</name>
  <read_first>
    - .claude/skills/ (research-stances, compass-topic-builder)
    - backend/data/stance-research/mn-2026-house/_push.ts (pipeline scripts to copy verbatim)
    - 163-RESEARCH.md (Stance Pipeline — regenerate topic scale live)
  </read_first>
  <action>
    Create backend/data/stance-research/la-2026-house/. Copy _push.ts, _merge.ts, _push_uuid.ts byte-for-byte from mn-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt via the LIVE compass_topics query filtered to the federal-24 subset (re-verify the live active total first), with exact 1-5 scale texts per topic. Do NOT copy a stale file.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/la-2026-house/_push.ts && test -f backend/data/stance-research/la-2026-house/_TOPIC_SCALE_FULL.txt && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - la-2026-house/ contains _merge.ts, _push.ts, _push_uuid.ts (verbatim) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt regenerated from a live query, filtered to federal-24 (count re-verified), with exact 1-5 texts
  </acceptance_criteria>
  <done>LA stance scaffold ready; topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research LA stances (27 new candidates incl. severe-district + LA-5's 13, majors first) and push to PROD</name>
  <read_first>
    - .claude/skills/ (research-stances — 3-concurrency, chairs-not-polarity, 0-unsourced, honest-skip with written trail)
    - backend/data/stance-research/la-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 163-06-SUMMARY.md (LA candidate external_ids incl. which are in severe/withheld districts)
  </read_first>
  <action>
    Target the 27 new LA candidates (band -220699..-220101), INCLUDING severe-district candidates and LA-5's 13 open-seat filers. Order evidenced majors first, thin-evidence filers last. Do NOT research LA's partial-tier incumbents (out of scope). Dispatch politician-stance-researcher agents at 3-concurrency MAX, 1 per district (LA-5's 13 candidates are one district-agent's load — split into a couple of waves if needed, still ≤3 concurrency). Validate the first wave before dispatching more. Embed exact 1-5 scale texts per agent prompt. Score chairs-not-polarity; never infer from party (jungle ballot — party is not a stance shortcut); honest-skip per-topic or whole-record ONLY with a written search trail (whole-record gate-pinnable). Every answer pairs to a real fetched source_url. Run a mandatory primary-source verification pass. Write 163-09-la-stances.csv, merge + push to PROD via _push.ts / _push_uuid.ts as each wave completes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -220699 AND -220101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed LA rows (query returns 0)
    - Every new LA candidate (incl. severe-district + LA-5's 13) has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written search trail
    - Stance agents ran at ≤3 concurrency; a primary-source verification pass was performed; no partial-tier LA incumbent was topped up
  </acceptance_criteria>
  <done>LA's full field is stance-complete on PROD, 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — offline research + data push, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-09-01 | Repudiation | Unsourced / party-inferred stance rows (esp. LA-5's thin field) | mitigate | 0-unsourced gate query; chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-163-09-02 | Tampering | Surname-leak in selected quotes | mitigate | _push.ts surname-leak guard (verbatim) |
| T-163-09-03 | Denial of service | Stance-agent rate-limit death (heaviest slice, 27 targets) | mitigate | 3-concurrency cap; first-wave validation; run on Sonnet |
</threat_model>

<verification>
- 0 unsourced answers among pushed LA rows
- Every new LA candidate (incl. severe-district + LA-5's 13) has sourced stances or a gate-pinned whole-record skip
- Scaffold present; topic scale live-regenerated
</verification>

<success_criteria>
LA's full 2026 House field (incl. withheld severe-district candidates and LA-5's 13 open-seat filers) is stance-complete on PROD, 0 unsourced, chairs-not-polarity, primary-source-verified.
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-09-SUMMARY.md when done, recording targets covered, gate-pinned whole-record skips (external_ids) for 163-11, and the live federal-24 count.
</output>
