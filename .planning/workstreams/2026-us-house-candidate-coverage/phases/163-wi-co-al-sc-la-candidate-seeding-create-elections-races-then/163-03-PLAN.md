---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/163-co-generate.mts
  - backend/migrations/1222_seed_co_2026_house_elections_races.sql
  - backend/migrations/1223_seed_co_2026_house_candidates.sql
  - backend/scripts/seed-co-house-headshots.py
  - backend/data/seed-co-2026-house/163-03-co-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "CO has 1 election ('CO 2026 Statewide General', general, 2026-11-03) and 8 races, each office_id → the existing NATIONAL_LOWER office (never null); decided general field surfaces on /elections (no PROVISIONAL prefix)"
    - "9 new CO politicians created (collision-free -(08*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-8001..-8008); DeGette CO-1 (lost primary) is NOT wired to the new CO-1 race and her existing record/office/stances are untouched; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
    - "CO-1's race_candidates has exactly 2 rows — Melat Kiros (D, new) + Christy Peterson (R, new); DeGette does not appear"
  artifacts:
    - path: "backend/migrations/1222_seed_co_2026_house_elections_races.sql"
      provides: "CO general election + 8 races wired to existing offices"
      contains: "CO 2026 Statewide General"
    - path: "backend/migrations/1223_seed_co_2026_house_candidates.sql"
      provides: "9 new CO politicians + active race_candidates rows (idempotent), DeGette excluded from CO-1"
      contains: "essentials.race_candidates"
  key_links:
    - from: "CO-1 race"
      to: "Kiros + Peterson only (NOT DeGette)"
      via: "nominee_status=lost-primary field-table signal, not the raw office→politician_id join"
      pattern: "0801"
---

<objective>
Seed CO's full 2026 US House field (8 districts, 9 new records) — vanilla single-election pattern (CO is NOT redistricted, no withholding; primaries decided 2026-06-30). The one special case: CO-1 incumbent Diana DeGette LOST her June-30 primary to Melat Kiros — a NEW Wave-3 transition pattern ("incumbent-lost-primary, stays incumbent-only, does not appear on the new race"). DeGette remains the sitting Rep until Jan 2027 (her politicians/offices rows and 19 stances stay untouched) but is NOT a CO-1 general candidate; only Kiros (D) and Christy Peterson (R) are wired to CO-1. Author 1 election + 8 races, create 9 new records + race_candidates (reuse renominated incumbent pids for CO-2..CO-8), then headshots. Decided field — NO PROVISIONAL prefix. Continues USHC3-02/03/04.

Output: 163-co-generate.mts, two CO migrations (run on prod), seed-co-house-headshots.py, CO reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
CO FIPS=08; geo_ids 0801-0808. Incumbents keep existing external_id (-8001..-8008). New challengers: external_id = -(8*10000 + cd*100 + seq); query band = -80899..-80101. One election: "CO 2026 Statewide General" (election_type general, election_date 2026-11-03). All races: office_id = the district's existing NATIONAL_LOWER office (join essentials.districts d ON substr(d.geo_id,1,2)='08' → essentials.offices o ON o.district_id=d.id). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party never on the card (races.primary_party NULL). Description: DECIDED general field (do NOT add a 'PROVISIONAL:' prefix — CO primaries are complete). Per-district new-record counts (from 160-field-table-p163.csv): CO-1=2 (Kiros + Peterson; DeGette EXCLUDED), CO-2=1, CO-3=1, CO-4=1, CO-5=1, CO-6=1, CO-7=1, CO-8=1 (=9). CO-1 CRITICAL (RESEARCH Pitfall 3): DeGette's pid 610bb358-bae9-4ce8-beaf-a33a27d5ba49 is still the CO-1 office.politician_id (sitting Rep) — do NOT let a naive "incumbent = office.politician_id" join wire her into the CO-1 race. Use nominee_status=lost-primary from the field table as the authoritative signal. CO-1 gets 2 NEW records (Kiros, Peterson), NO is_incumbent=true row. CO-2..CO-8 each reuse their renominated incumbent (is_incumbent=true) + 1 new challenger. Parties normalized. sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-08 collision check + DeGette exclusion confirmation</name>
  <read_first>
    - 163-RESEARCH.md (Critical Question 3 — 0 collisions verified in -80101..-80899; incumbent departures/transitions — DeGette lost-primary detail; Common Pitfalls #3)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv (CO incumbent pids/external_ids, rows 10-17; CO-1 nominee_status=lost-primary)
  </read_first>
  <action>
    Run the fresh scoped collision check for CO's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -80899..-80101, excluding legacy incumbent ids -8001..-8008 (different formula — cross-check any hit against 160-field-table-p163.csv). Confirm DeGette (pid 610bb358-bae9-4ce8-beaf-a33a27d5ba49) is CO-1 nominee_status=lost-primary: she is REUSE-NO-ROW — her existing politicians/offices/stances rows are left completely untouched and she is NOT added to the new CO-1 race_candidates. Record the collision result + DeGette's exclusion decision in the reconciliation CSV header notes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -80899 AND -80101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -80899..-80101 (or every hit confirmed a legacy incumbent id, not a real collision)
    - DeGette confirmed lost-primary → REUSE-NO-ROW; not planned for the CO-1 race; her existing rows untouched
  </acceptance_criteria>
  <done>CO band confirmed collision-free and DeGette's exclusion is locked before any SQL is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply CO elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (the vanilla single-election analog to clone; INC_EXT reuse handling)
    - 160-field-table-p163.csv (the 9 CO new records + incumbent reuse rows, rows 10-17)
    - 163-RESEARCH.md (Pitfall 3 — do not wire DeGette; Migration Numbering — re-verify next-free)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/163-co-generate.mts. Change: FIPS 08, 8 districts, INC_EXT = -8001..-8008 BUT with CO-1/DeGette flagged vacate:true (lost-primary → no active incumbent row on CO-1, and do NOT recompute/touch her external_id); FIELD = the 9 CO new records from 160-field-table-p163.csv (CO-1 = Kiros + Peterson, both new; CO-2..CO-8 = 1 challenger each; parties normalized, never "Democrat"); geo prefix '08'; election name 'CO 2026 Statewide General'; description = decided general field with NO 'PROVISIONAL:' prefix. Keep the -(8*10000+cd*100+seq) formula, single-election migration template, and NOT EXISTS idempotency guards. Ensure CO-1's race_candidates block wires ONLY the 2 new records (no is_incumbent=true row for CO-1). Use sqlStr() on every free-text field. Emit two migration files — reserved pair is 1222 + 1223; FIRST re-verify with `ls backend/migrations | sort -n | tail` that they are free; if taken, shift both forward to the next free contiguous pair and record actuals in the SUMMARY. Run node --import tsx scripts/163-co-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-co-2026-house/163-03-co-reconciliation.csv listing every CO candidate with new/reuse disposition + external_id (and DeGette as REUSE-NO-ROW).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FILTER (WHERE r.office_id IS NULL) AS null_off, (SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r2 ON r2.id=rc.race_id JOIN essentials.offices o ON o.id=r2.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections e2 ON e2.id=r2.election_id WHERE e2.name='CO 2026 Statewide General' AND d.geo_id='0801' AND rc.candidate_status='active') AS co1_cands FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='CO 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both CO migrations applied cleanly (psql exit 0); 8 races exist, all office_id NOT NULL (null_off=0)
    - CO-1 race has exactly 2 active candidates (Kiros + Peterson); DeGette (610bb358-...) is NOT among CO-1's race_candidates and her existing rows are unchanged
    - 9 new politicians in -80899..-80101; CO-2..CO-8 reuse -8002..-8008; 0 duplicate full_name; re-running migrations is a 0-row no-op
  </acceptance_criteria>
  <done>CO races + candidates seeded on prod (decided field), DeGette correctly excluded, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: CO headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped 3-tuple analog; wrong-person guards byte-for-byte)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-co-house-headshots.py. Change ONLY the module docstring, BANDS = {'CO': (-80899, -80101, 'Colorado')} (3-tuple band-scoped), and RESULTS_JSON = '_co-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-8001..-8008) and DeGette fall outside the band.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -80899 AND -80101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-co-house-headshots.py uses the 3-tuple band-scoped BANDS; wrong-person guards unchanged
    - Every new CO candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>CO new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |
| office→politician_id join -> CO-1 candidate set | A naive incumbent join would wrongly surface DeGette on the new race |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-03-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-163-03-02 | Tampering | DeGette wrongly wired into CO-1 (lost-primary incumbent surfacing) | mitigate | vacate:true on CO-1 driven by nominee_status=lost-primary, not the office join; 163-11 asserts CO-1 has exactly 2 candidates and DeGette absent |
| T-163-03-03 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -8001..-8008 by geo_id join; full_name dedup guard; 0-duplicate assertion in 163-11 |
</threat_model>

<verification>
- Both CO migrations applied on prod (psql exit 0); 8 races, all office_id NOT NULL
- CO-1 has exactly 2 active candidates (Kiros + Peterson); DeGette excluded and untouched
- 9 new politicians in-band; incumbents reused; 0 duplicate full_name; every new candidate has a headshot row or honest-skip
</verification>

<success_criteria>
CO's full 2026 House field is seeded on prod (9 new + incumbent reuse), decided general field surfacing on /elections, DeGette correctly excluded from CO-1 while her record stays intact, with headshots and a reconciliation CSV — ready for stance research (163-10).
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-03-SUMMARY.md when done, recording applied migration numbers, the CO new-candidate external_id range, and DeGette's REUSE-NO-ROW disposition.
</output>
