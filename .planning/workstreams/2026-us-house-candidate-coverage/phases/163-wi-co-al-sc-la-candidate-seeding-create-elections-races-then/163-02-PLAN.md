---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/163-wi-generate.mts
  - backend/migrations/1220_seed_wi_2026_house_elections_races.sql
  - backend/migrations/1221_seed_wi_2026_house_candidates.sql
  - backend/scripts/seed-wi-house-headshots.py
  - backend/data/seed-wi-2026-house/163-02-wi-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "WI has 1 election ('WI 2026 Statewide General', general, 2026-11-03) and 8 races, each office_id → the existing NATIONAL_LOWER office (never null); full PROVISIONAL pre-primary field surfaces on /elections (Aug-11 primary)"
    - "28 new WI politicians created (collision-free -(55*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-55001..-55008); Tiffany WI-7 (retired) has no active row; 0 duplicate full_name per state; every new candidate has a headshot or documented honest-skip"
    - "WI is not redistricted — no withholding, vanilla single-election pattern; WI-2 legitimately has only 2 all-Democratic candidates (no Republican filed)"
  artifacts:
    - path: "backend/migrations/1220_seed_wi_2026_house_elections_races.sql"
      provides: "WI general election + 8 races wired to existing offices"
      contains: "WI 2026 Statewide General"
    - path: "backend/migrations/1221_seed_wi_2026_house_candidates.sql"
      provides: "28 new WI politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "WI races"
      to: "existing NATIONAL_LOWER offices (geo prefix 55)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Seed WI's full 2026 US House field (8 districts, 28 new records) — vanilla single-election pattern (WI is NOT redistricted, no withholding). Author 1 election + 8 races wired to existing offices, create 28 new records + race_candidates (reuse incumbent pids; Tiffany WI-7 retired → no active row), then headshots. Full field marked PROVISIONAL: (Aug-11 late primary). Continues USHC3-02/03/04.

Output: 163-wi-generate.mts, two WI migrations (run on prod), seed-wi-house-headshots.py, WI reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
WI FIPS=55; geo_ids 5501-5508. Incumbents keep existing external_id (-55001..-55008); WI-7/5507 Tiffany retired (running for Governor) → no active House row (vacate:true). New challengers: external_id = -(55*10000 + cd*100 + seq); query band = -550899..-550101. One election: "WI 2026 Statewide General" (election_type general, election_date 2026-11-03). All races: office_id = the district's existing NATIONAL_LOWER office (join essentials.districts d ON substr(d.geo_id,1,2)='55' → essentials.offices o ON o.district_id=d.id). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party never on the card (races.primary_party NULL). Description: 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12' (Aug-11 primary). Per-district new-record counts (from 160-field-table-p163.csv new_records_needed): WI-1=4, WI-2=1, WI-3=3, WI-4=4, WI-5=1, WI-6=5, WI-7=7 (open, Tiffany→Governor), WI-8=3 (=28). WI-2/5502 legitimately has only 2 all-D candidates (Pocan inc + Douglas Alexander) — no Republican filed, verified, NOT an error. Parties normalized (Democratic/Republican/Green/Independent, never "Democrat"). sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-55 collision check</name>
  <read_first>
    - 163-RESEARCH.md (Critical Question 3 — 0 collisions verified 2026-07-05 in -550101..-550899; incumbent legacy scheme -55001..-55008 is DIFFERENT, never recompute)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv (WI incumbent pids/external_ids; Tiffany retired)
  </read_first>
  <action>
    Run the fresh scoped collision check for WI's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -550899..-550101, excluding legacy incumbent ids -55001..-55008 (different formula — cross-check any hit against 160-field-table-p163.csv). Confirm Tiffany WI-7 is departing (Governor) so no active House row is authored for him.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -550899 AND -550101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -550899..-550101 (or every hit confirmed a legacy incumbent id, not a real collision)
    - Tiffany WI-7 confirmed departing → no active row planned
  </acceptance_criteria>
  <done>WI band confirmed collision-free before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply WI elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (the vanilla single-election analog to clone: INC_EXT, single-election migration template, candidate-insert template, sqlStr, NOT EXISTS guards)
    - 160-field-table-p163.csv (the 28 WI new records + incumbent reuse rows, rows 2-9)
    - 163-RESEARCH.md (Migration Numbering — re-verify next-free at authoring time)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/163-wi-generate.mts. Change: FIPS 55, 8 districts, INC_EXT = -55001..-55008 (WI-7 Tiffany vacate:true, no active row); FIELD = the 28 WI new records from 160-field-table-p163.csv (parties normalized, never "Democrat"); geo prefix '55'; election name 'WI 2026 Statewide General'; description 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12'. Keep the -(55*10000+cd*100+seq) formula, single-election migration template, and NOT EXISTS idempotency guards on politicians/race_candidates. Use sqlStr() on every free-text field. Emit two migration files — reserved pair is 1220 (elections/races) + 1221 (candidates); FIRST re-verify with `ls backend/migrations | sort -n | tail` that 1220/1221 are still free (a parallel OR-session on phases 177/178 may have landed work since 2026-07-05). If either is taken, shift both WI migrations forward to the next free contiguous pair and record the actual numbers in the SUMMARY. Run node --import tsx scripts/163-wi-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-wi-2026-house/163-02-wi-reconciliation.csv listing every WI candidate with new/reuse disposition + external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='WI 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both WI migrations applied cleanly (psql exit 0); 8 races exist, all office_id NOT NULL (the FILTER count is 0)
    - 28 new politicians in -550899..-550101; incumbents reuse -55001..-55008; Tiffany has no active row; WI-2 has exactly 2 candidates (Pocan + Alexander); 0 duplicate full_name per state
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>WI races + candidates seeded on prod, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: WI headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped 3-tuple analog to clone; wrong-person guards byte-for-byte)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-wi-house-headshots.py. Change ONLY the module docstring, BANDS = {'WI': (-550899, -550101, 'Wisconsin')} (3-tuple band-scoped), and RESULTS_JSON = '_wi-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes (_NON_PERSON_TITLE / _BAD_DISAMBIG / _HISTORICAL_YEAR). Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-55001..-55008) fall outside the band (already imaged in prior phases).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -550899 AND -550101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-wi-house-headshots.py uses the 3-tuple band-scoped BANDS; wrong-person guards unchanged
    - Every new WI candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>WI new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-02-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-163-02-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -55001..-55008 by geo_id join; full_name dedup guard; 0-duplicate assertion in 163-11 |
| T-163-02-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted in 148-162 |
</threat_model>

<verification>
- Both WI migrations applied on prod (psql exit 0); 8 races, all office_id NOT NULL
- 28 new politicians in-band; incumbents reused; Tiffany no active row; WI-2 exactly 2 candidates; 0 duplicate full_name
- Every new WI candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
WI's full 2026 House field is seeded on prod (28 new + incumbent reuse), PROVISIONAL-marked, surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (163-07).
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-02-SUMMARY.md when done, recording applied migration numbers and the WI new-candidate external_id range.
</output>
