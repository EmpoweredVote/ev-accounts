---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 06
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - backend/scripts/163-la-generate.mts
  - backend/migrations/1228_seed_la_2026_house_elections_races.sql
  - backend/migrations/1229_seed_la_2026_house_candidates.sql
  - backend/scripts/seed-la-house-headshots.py
  - backend/data/seed-la-2026-house/163-06-la-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "LA has 2 elections: 'LA 2026 Statewide General' (general, 2026-11-03) and 'LA 2026 Congressional Redistricting - Polygon Pending' (special, dated to SB121 so ELECTION_VISIBILITY_WINDOW is false); each district is ONE jungle race (primary_party NULL) with every qualified candidate — no separate party primaries, no Dec-12 runoff modeled"
    - "Non-severe LA districts surface their jungle field on /elections; severe LA districts (per the 163-01 LA audit) surface ZERO races (election_id points at the withheld election); office_id is NEVER null; essentials.offices is never touched"
    - "27 new LA politicians created (collision-free -(22*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-22001..-22006) and appear ON the jungle ballot; Letlow LA-5 (retired) has no incumbent row (13 open-seat candidates); full field PROVISIONAL (qualifying closes Aug-7); 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/1228_seed_la_2026_house_elections_races.sql"
      provides: "LA general + withheld 'Polygon Pending' election + 6 jungle races (primary_party NULL), severity-routed"
      contains: "Congressional Redistricting"
    - path: "backend/migrations/1229_seed_la_2026_house_candidates.sql"
      provides: "27 new LA politicians + active race_candidates rows on single jungle races (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "severe LA races"
      to: "LA 2026 Congressional Redistricting - Polygon Pending election"
      via: "election_id substitution (visibility-window false)"
      pattern: "Polygon Pending"
    - from: "each LA district"
      to: "ONE race, primary_party NULL, all qualified candidates"
      via: "CA jungle-primary convention (idx_races_election_position_no_party)"
      pattern: "primary_party"
---

<objective>
Seed LA's full 2026 US House field (6 districts, 27 new records) using the jungle/open-primary ballot model — ONE race per district on 2026-11-03 with primary_party=NULL and EVERY qualified candidate (all parties + incumbent) wired to that single race (clone the CA convention; do NOT create separate party primaries; do NOT model the contingent Dec-12 runoff — Phase 167). LA is redistricted (SB121/Act 2 dissolved CD-6): SEVERE districts (from the 163-01 LA audit) are seeded-but-withheld from /elections via a "Polygon Pending" election (D-01b) — NEVER office_id NULL, NEVER touching essentials.offices. Author the 2 elections + 6 jungle races, create 27 new records + race_candidates (reuse renominated incumbent pids ON the jungle ballot; Letlow LA-5 retired → no incumbent row, 13 open-seat candidates), then headshots. Full field marked PROVISIONAL (qualifying closes Aug-7). Continues USHC3-02/03/04.

Output: 163-la-generate.mts, two LA migrations (run on prod), seed-la-house-headshots.py (band-scoped, 2-election variant), LA reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-la-correspondence-audit.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-01-SUMMARY.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
@backend/scripts/162-mo-generate.mts
@backend/scripts/ingest-ca-sos-2026-challengers.ts
@backend/src/lib/electionService.ts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
LA FIPS=22; geo_ids 2201-2206. Incumbents keep existing external_id (-22001..-22006) and RUN ON the jungle ballot (is_incumbent=true, same single race as challengers); LA-5/2205 Letlow RETIRED (running for Senate) → no incumbent row (open seat, 13 declared candidates). New challengers: external_id = -(22*10000 + cd*100 + seq); query band = -220699..-220101. Two elections:
- "LA 2026 Statewide General" (election_type general, election_date 2026-11-03) — the surfacing election
- "LA 2026 Congressional Redistricting - Polygon Pending" (election_type special, election_date = SB121 signing 2026-05-29; must be strictly >30 days before the execution date so ELECTION_VISIBILITY_WINDOW is false — if too close, use the earlier SCOTUS Callais date 2026-04-29)
JUNGLE MODEL (Critical Question 2): each district = exactly ONE race with primary_party=NULL (the schema's idx_races_election_position_no_party unique index supports this exact shape — clone the CA convention in ingest-ca-sos-2026-challengers.ts / seed-la-citywide-races-2026.sql). ALL qualified candidates for a district (incumbent + every party) go on that ONE race's race_candidates. Do NOT create per-party primary races. Do NOT create any December 2026 runoff election/race (Pitfall 2). Non-severe races wire election_id → general; severe races (from 163-la-correspondence-audit.md's "Severe geo_id list:") → withheld. ALL races: office_id = the district's existing NATIONAL_LOWER office (NEVER null). race_candidates joined by geo_id. Description: 'PROVISIONAL: declared-so-far field, re-pull after 2026-08-07 qualifying close' (all 6 late-primary). Per-district new-record counts (from 160-field-table-p163.csv new_records_needed): LA-1=2 (Scalise inc reuse -22001), LA-2=1 (Carter inc reuse -22002), LA-3=3 (Higgins inc reuse -22003), LA-4=4 (Johnson inc reuse -22004), LA-5=13 (open, Letlow→Senate), LA-6=4 (Fields inc reuse -22006) (=27). Parties normalized. sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-22 collision check + load LA severe geo_id list + re-verify SB121 + Aug-7 qualifying note</name>
  <read_first>
    - 163-RESEARCH.md (Critical Question 2 — jungle model + no-runoff; Critical Question 3 — 0 collisions verified in -220101..-220699; Open Questions #2 — re-verify LA litigation status before seeding; LA qualifying nuance — Aug-7 declared-so-far)
    - 163-la-correspondence-audit.md (the "Severe geo_id list:" to load into SEVERE_GEO_IDS)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv (LA incumbent pids/external_ids, rows 32-37; Letlow retired)
  </read_first>
  <action>
    Run the fresh scoped external_id collision check for LA's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -220699..-220101, excluding legacy incumbent ids -22001..-22006 (cross-check any hit against 160-field-table-p163.csv). Load SEVERE_GEO_IDS from 163-la-correspondence-audit.md's "Severe geo_id list:" line (use whatever the audit scored, not the predicted 2201/2204/2205/2206). Confirm Letlow LA-5 is retired → no incumbent row. Re-verify SB121 is still the operative map (no live injunction) as of execution date, and confirm the withheld election_date (2026-05-29, or 2026-04-29 if within 30 days of execution) is strictly >30 days before today. Note that LA qualifying closes 2026-08-07 → the seeded field is declared-so-far and must be re-pulled in Phase 167. Record collision result, loaded severe set, chosen withheld date, and the Aug-7 re-pull flag in the reconciliation CSV header notes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -220699 AND -220101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -220699..-220101 (or every hit confirmed a legacy incumbent id, not a real collision)
    - LA severe geo_id set loaded verbatim from the 163-01 audit; Letlow LA-5 confirmed retired (no incumbent row)
    - SB121 confirmed still operative; withheld election_date confirmed >30 days before execution; Aug-7 re-pull flagged
  </acceptance_criteria>
  <done>LA band confirmed collision-free, the audit severe set is loaded, the withheld-date validated, and the Aug-7 re-pull is flagged before any SQL is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply LA elections/races + candidates migrations (jungle model, severity-routed)</name>
  <read_first>
    - backend/scripts/162-mo-generate.mts (the 2-election severity-routed analog: SEVERE_GEO_IDS, INC_EXT, migration templates, join-by-geo_id, office_id-never-null, severe?withheld:general branch)
    - backend/scripts/ingest-ca-sos-2026-challengers.ts (the CA jungle-primary convention: single race, primary_party NULL, all candidates on one race)
    - 163-la-correspondence-audit.md (the severe geo_id list)
    - 160-field-table-p163.csv (the 27 LA new records + incumbent reuse rows, rows 32-37)
    - 163-RESEARCH.md (Critical Question 2 — no per-party races, no runoff; Migration Numbering)
  </read_first>
  <action>
    Clone backend/scripts/162-mo-generate.mts → backend/scripts/163-la-generate.mts, adapting the race-creation block to the CA jungle convention (ingest-ca-sos-2026-challengers.ts): each of LA's 6 districts gets EXACTLY ONE race with primary_party=NULL, and ALL qualified candidates for that district (incumbent + every party) are wired to that single race's race_candidates. Do NOT emit per-party primary races and do NOT emit any December-2026 runoff election or race. Change: SEVERE_GEO_IDS = the geo_ids from 163-la-correspondence-audit.md's "Severe geo_id list:"; INC_EXT = LA's -22001..-22006 (LA-5 Letlow vacate:true, no incumbent row; LA-1 Scalise / LA-2 Carter / LA-3 Higgins / LA-4 Johnson / LA-6 Fields reuse their ids inc:true ON the jungle race); FIELD = the 27 LA new records from 160-field-table-p163.csv (parties normalized, never "Democrat"); geo prefix '22'; 6 districts. Keep the -(22*10000+cd*100+seq) formula and the severe?withheld:general election branch verbatim. Set the withheld election name 'LA 2026 Congressional Redistricting - Polygon Pending' and election_date to the Task-1-validated date (SB121 2026-05-29 or Callais 2026-04-29). Description: all 6 districts 'PROVISIONAL: declared-so-far field, re-pull after 2026-08-07 qualifying close'. Copy the "office_id is NEVER null; essentials.offices is NEVER touched" invariant into the migration header comment verbatim. Use sqlStr() on every free-text field. Emit two migration files — reserved pair is 1228 + 1229; FIRST re-verify with `ls backend/migrations | sort -n | tail` that they are free; if taken, shift both forward to the next free contiguous pair and record actuals in the SUMMARY. Idempotent NOT EXISTS guards on all inserts. Run node --import tsx scripts/163-la-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-la-2026-house/163-06-la-reconciliation.csv listing every LA candidate with new/reuse disposition + external_id + severity routing.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) AS races, count(*) FILTER (WHERE r.office_id IS NULL) AS null_off, count(*) FILTER (WHERE r.primary_party IS NOT NULL) AS with_party FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name LIKE 'LA 2026%'"</automated>
  </verify>
  <acceptance_criteria>
    - Both LA migrations applied cleanly (psql exit 0); exactly 6 races (one per district), all office_id NOT NULL (null_off=0), all primary_party NULL (with_party=0); essentials.offices untouched
    - Severe districts' races have election_id = the 'LA 2026 Congressional Redistricting - Polygon Pending' election; non-severe = the general (matching the 163-01 severe set); NO Dec-2026 runoff election/race exists
    - 27 new politicians in -220699..-220101; incumbents reuse -22001..-22006 on the jungle race; Letlow has no row; 0 duplicate full_name; re-running migrations is a 0-row no-op
  </acceptance_criteria>
  <done>LA races + candidates seeded on prod as single jungle races, severity-routed, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: LA headshots (band-scoped, includes severe-district candidates)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped 3-tuple analog — NO election-name join, so withheld-election candidates still get headshots; wrong-person guards byte-for-byte)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-la-house-headshots.py. Change ONLY the module docstring, BANDS = {'LA': (-220699, -220101, 'Louisiana')} (3-tuple band-scoped-only — NO election-name join, because severe-district candidates on the withheld election still need headshots), and RESULTS_JSON = '_la-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-22001..-22006) fall outside the band.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -220699 AND -220101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-la-house-headshots.py uses the 3-tuple band-scoped BANDS (no election join); wrong-person guards unchanged
    - Headshot attempted for every new LA candidate INCLUDING severe-district candidates; every new candidate has a politician_images row OR a documented honest-skip
  </acceptance_criteria>
  <done>LA new-candidate headshots ingested (or honest-skipped) via the band-scoped script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text candidate names interpolated into SQL executed against prod |
| audit severe list -> election_id routing | The severity classification decides which LA races surface to voters |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-06-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-163-06-02 | Tampering | A severe LA race accidentally surfacing (wrong-map data to voters) | mitigate | Severe races routed to the non-general past-dated withheld election; office_id never null; 163-11 LA-SEVERE block + coordinate-smoke negative sample re-prove non-surfacing |
| T-163-06-03 | Tampering | Wrong ballot shape (per-party primaries or a premature runoff) | mitigate | Single primary_party=NULL race per district (CA convention); no Dec-2026 election created; 163-11 asserts exactly 6 LA races all primary_party NULL |
| T-163-06-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted |
</threat_model>

<verification>
- Both LA migrations applied on prod (psql exit 0); exactly 6 jungle races, all office_id NOT NULL, all primary_party NULL; no Dec-2026 runoff; essentials.offices untouched
- Severe races → withheld election; non-severe → general (matches the 163-01 severe set); 27 new politicians in-band; incumbents reused on the jungle race; 0 duplicate full_name
- Every new LA candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
LA's full 2026 House field is seeded on prod (27 new + incumbent reuse) as single jungle races (primary_party NULL), severity-routed so severe districts are withheld while office_id stays populated, PROVISIONAL-marked with an Aug-7 re-pull flag, no runoff modeled, with headshots and a reconciliation CSV — ready for stance research (163-09).
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-06-SUMMARY.md when done, recording applied migration numbers, the severe LA geo_ids routed to the withheld election, the withheld election_date used, and the Aug-7 re-pull flag.
</output>
