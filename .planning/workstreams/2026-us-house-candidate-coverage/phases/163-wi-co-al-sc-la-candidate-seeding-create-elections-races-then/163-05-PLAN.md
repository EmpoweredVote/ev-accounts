---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/163-sc-generate.mts
  - backend/migrations/1224_seed_sc_2026_house_elections_races.sql
  - backend/migrations/1225_seed_sc_2026_house_candidates.sql
  - backend/scripts/seed-sc-house-headshots.py
  - backend/data/seed-sc-2026-house/163-05-sc-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "SC has 1 election ('SC 2026 Statewide General', general, 2026-11-03) and 7 races, each office_id → the existing NATIONAL_LOWER office (never null); decided general field surfaces on /elections"
    - "16 new SC politicians created (collision-free -(45*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-45001..-45007); Mace SC-1 + Norman SC-5 (retired) have no active row; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
    - "SC is not redistricted — no withholding, vanilla single-election pattern; the 2026-07-15 independent/petition window was re-checked at execution and any newly-qualified filers folded in (or flagged for Phase 167)"
  artifacts:
    - path: "backend/migrations/1224_seed_sc_2026_house_elections_races.sql"
      provides: "SC general election + 7 races wired to existing offices"
      contains: "SC 2026 Statewide General"
    - path: "backend/migrations/1225_seed_sc_2026_house_candidates.sql"
      provides: "16 new SC politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "SC races"
      to: "existing NATIONAL_LOWER offices (geo prefix 45)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Seed SC's full 2026 US House field (7 districts, 16 new records) — vanilla single-election pattern (SC is NOT redistricted, no withholding; primaries decided). Author 1 election + 7 races wired to existing offices, create 16 new records + race_candidates (reuse renominated incumbent pids; Mace SC-1 and Norman SC-5 retired → no active rows), then headshots. Decided field — NO PROVISIONAL prefix. One execution-time gate: the SC independent/petition window (filing_open_deadline 2026-07-15) — re-check per Pitfall 4. Continues USHC3-02/03/04.

Output: 163-sc-generate.mts, two SC migrations (run on prod), seed-sc-house-headshots.py, SC reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
SC FIPS=45; geo_ids 4501-4507. Incumbents keep existing external_id (-45001..-45007); SC-1/4501 Mace (running for Governor) and SC-5/4505 Norman (running for Governor) both retired → no active House rows (vacate:true). New challengers: external_id = -(45*10000 + cd*100 + seq); query band = -450799..-450101. One election: "SC 2026 Statewide General" (election_type general, election_date 2026-11-03). All races: office_id = the district's existing NATIONAL_LOWER office (join essentials.districts d ON substr(d.geo_id,1,2)='45' → essentials.offices o ON o.district_id=d.id). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party never on the card (races.primary_party NULL). Description: DECIDED general field (NO 'PROVISIONAL:' prefix). Per-district new-record counts (from 160-field-table-p163.csv): SC-1=4 (open, Mace→Governor; Honeycutt/Lacore/Reeside/Ellis), SC-2=2, SC-3=2, SC-4=2, SC-5=3 (open, Norman→Governor; Climer/Dittmer/Kaplan), SC-6=2, SC-7=1 (=16). Note minor-party labels (Libertarian, Workers, Alliance, Forward) normalize to their formal party names, never on the card. sqlStr()-escape every free-text field (e.g. "William R. Timmons IV").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-45 collision check + Jul-15 independent-window re-check</name>
  <read_first>
    - 163-RESEARCH.md (Critical Question 3 — 0 collisions verified in -450101..-450799; Common Pitfalls #4 — SC independent window drift, filing_open_deadline 2026-07-15)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv (SC incumbent pids/external_ids, rows 25-31; Mace + Norman retired)
  </read_first>
  <action>
    Run the fresh scoped collision check for SC's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -450799..-450101, excluding legacy incumbent ids -45001..-45007 (cross-check any hit against 160-field-table-p163.csv). Confirm Mace SC-1 and Norman SC-5 are departing (Governor) → no active House rows. THEN, per Pitfall 4: if the execution date is on or after 2026-07-15, do a quick web re-check (SC Election Commission / Ballotpedia / news) for any newly-qualified SC independent or petition candidates that filed after the Phase-160 snapshot. Fold any new filers into the FIELD for Task 2; if none, record "no new SC independents as of {date}". If execution is before 2026-07-15, note that the window is still open and flag SC for a Phase-167 re-pull. Record the outcome in the reconciliation CSV header notes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -450799 AND -450101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -450799..-450101 (or every hit confirmed a legacy incumbent id, not a real collision)
    - Mace SC-1 + Norman SC-5 confirmed departing → no active rows
    - Jul-15 independent window re-checked (or flagged for Phase 167 if execution is pre-window); outcome recorded
  </acceptance_criteria>
  <done>SC band confirmed collision-free, retirements confirmed, and the independent-window status is resolved before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply SC elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (the vanilla single-election analog to clone)
    - 160-field-table-p163.csv (the 16 SC new records + incumbent reuse rows, rows 25-31)
    - 163-RESEARCH.md (Migration Numbering — re-verify next-free)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/163-sc-generate.mts. Change: FIPS 45, 7 districts, INC_EXT = -45001..-45007 (SC-1 Mace + SC-5 Norman vacate:true, no active rows); FIELD = the 16 SC new records from 160-field-table-p163.csv plus any Jul-15 additions from Task 1 (parties normalized, never "Democrat"); geo prefix '45'; election name 'SC 2026 Statewide General'; description = decided general field, NO 'PROVISIONAL:' prefix. Keep the -(45*10000+cd*100+seq) formula, single-election migration template, and NOT EXISTS idempotency guards. Use sqlStr() on every free-text field. Emit two migration files — reserved pair is 1224 + 1225; FIRST re-verify with `ls backend/migrations | sort -n | tail` that they are free; if taken, shift both forward to the next free contiguous pair and record actuals in the SUMMARY. Run node --import tsx scripts/163-sc-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-sc-2026-house/163-05-sc-reconciliation.csv listing every SC candidate with new/reuse disposition + external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='SC 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both SC migrations applied cleanly (psql exit 0); 7 races exist, all office_id NOT NULL (the FILTER count is 0)
    - 16 (+ any Jul-15 additions) new politicians in -450799..-450101; incumbents reuse -45001..-45007; Mace + Norman have no active rows; 0 duplicate full_name
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>SC races + candidates seeded on prod (decided field), with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: SC headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped 3-tuple analog; wrong-person guards byte-for-byte)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-sc-house-headshots.py. Change ONLY the module docstring, BANDS = {'SC': (-450799, -450101, 'South Carolina')} (3-tuple band-scoped), and RESULTS_JSON = '_sc-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-45001..-45007) fall outside the band.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -450799 AND -450101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-sc-house-headshots.py uses the 3-tuple band-scoped BANDS; wrong-person guards unchanged
    - Every new SC candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>SC new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-05-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-163-05-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -45001..-45007 by geo_id join; full_name dedup guard; 0-duplicate assertion in 163-11 |
| T-163-05-03 | Information disclosure | Missing a Jul-15 independent filer | accept-with-check | Task-1 window re-check; residual filers caught by Phase-167 reconciliation |
</threat_model>

<verification>
- Both SC migrations applied on prod (psql exit 0); 7 races, all office_id NOT NULL
- 16 (+ any additions) new politicians in-band; incumbents reused; Mace + Norman no active rows; 0 duplicate full_name
- Jul-15 window resolved; every new SC candidate has a headshot row or honest-skip
</verification>

<success_criteria>
SC's full 2026 House field is seeded on prod (16 new + incumbent reuse), decided general field surfacing on /elections, the Jul-15 independent window resolved, with headshots and a reconciliation CSV — ready for stance research (163-10).
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-05-SUMMARY.md when done, recording applied migration numbers, the SC new-candidate external_id range, and the Jul-15 window outcome.
</output>
