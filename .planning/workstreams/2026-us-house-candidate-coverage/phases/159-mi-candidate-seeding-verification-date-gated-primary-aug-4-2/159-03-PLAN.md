---
phase: 159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_va_2026_house_candidates.sql
  - backend/data/seed-va-2026-house/159-03-va-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 11 EXISTING VA US House races (geo 5101..5111, already scaffolded 0-candidates) carries its full pre-primary ballot-qualified field as active race_candidates — no new VA election or race rows are created (VA already has 1 election dated 2026-11-03 + 11 races)"
    - "All 11 VA incumbents are reused by 154 pid as active is_incumbent=true, INCLUDING VA-11 James Walkinshaw (-5102011) who ALREADY EXISTS — the stale roadmap 'Walkinshaw needs a new record' note is superseded; reuse, never duplicate"
    - "Every genuinely-new VA challenger + declared indep/Libertarian is a NEW record in the -(51*10000+cd*100+seq) band, verified collision-free (the legacy -51020xx incumbent band is FAR more negative and outside the new band)"
    - "Zero duplicate full_name among active VA candidates; no party stored on any candidate card"
  artifacts:
    - path: "backend/data/seed-va-2026-house/159-03-va-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision + assigned external_id + resolved existing race_id for all 11 VA districts, reconciled against the VA Dept. of Elections primary spreadsheet"
      min_lines: 50
    - path: "backend/migrations/NNNN_seed_va_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: ~40 new VA politicians + race_candidates onto the 11 EXISTING VA races; Walkinshaw reused not duplicated"
      contains: "race_candidates"
  key_links:
    - from: "race_candidates.race_id"
      to: "the 11 EXISTING VA House races (geo 5101..5111, from the live VA 2026 Statewide General election)"
      via: "office_id → districts.geo_id 51NN join to resolve the existing race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "154 incumbent_pid (reuse, incl. Walkinshaw -5102011) or newly-inserted VA politician id"
      via: "159-03-va-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full pre-primary ballot-qualified field (all parties) onto the 11 EXISTING VA US House races as active `race_candidates`, reusing all 11 VA incumbents by pid (INCLUDING VA-11 Walkinshaw -5102011, who already exists — do NOT create a duplicate) and creating new records ONLY for genuinely-new challengers + declared indep/Libertarian. VA's election + 11 race rows already exist (DB-confirmed, 0 candidates) — this is a records-only plan, NO election/race authoring. Near-verbatim single-state repeat of Phase 157's 157-03 records mechanics, retargeted to VA (fips=51, geo 51NN) with the reuse-onto-existing-races wrinkle.

Purpose: USHC2-02 + USHC2-03 for VA. VA had races scaffolded but zero candidates wired — the tool has been dark for VA voters. Seeding the full qualified field NOW serves VA's Aug-4 primary voters.
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every VA House race surfaces its full pre-primary field on /elections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv

<interfaces>
<!-- VA election + races ALREADY EXIST (DB-confirmed): 1 essentials.elections row dated 2026-11-03 +
     11 essentials.races (geo 5101..5111, existing race_ids listed in the 159-RESEARCH VA field table),
     0 candidates. This plan creates NO election or race rows — resolve the existing races by
     office_id → districts.geo_id 51NN join. Confirm the exact VA election name live before wiring
     (SELECT name FROM essentials.elections WHERE ... the election owning the 11 VA races). If VA races
     lack a PROVISIONAL description marker, ADD it to those 11 rows via UPDATE (FL-151 D-04 convention)
     so 159-C can flip it on prune — this is the ONE permitted write to the races rows, and only the
     description column. -->
<!-- essentials.race_candidates: id uuid PK; race_id uuid NOT NULL FK; politician_id uuid (non-null for
     House); full_name/first_name/last_name; photo_url NULL; is_incumbent bool; candidate_status
     'active'; source text; external_id optional. Idempotency app-enforced: (race_id, full_name) NOT
     EXISTS. essentials.politicians: NO updated_at column. -->
<!-- VA incumbent reuse pids (154-incumbent-map.csv), legacy external_id -5102001..-5102011:
     VA-1 Wittman -5102001 · VA-2 Kiggans -5102002 · VA-3 Scott -5102003 · VA-4 McClellan -5102004 ·
     VA-5 McGuire -5102005 · VA-6 Cline -5102006 · VA-7 Vindman -5102007 (THIN) · VA-8 Beyer -5102008 ·
     VA-9 Griffith -5102009 (THIN) · VA-10 Subramanyam -5102010 (THIN) · VA-11 Walkinshaw -5102011.
     ALL 11 reused active is_incumbent=true. Walkinshaw ALREADY EXISTS — REUSE, do NOT create. -->
<!-- NEW challenger external_id band (D-06): -(51*10000+cd*100+seq) — e.g. VA-1 seq1 = -510101;
     VA-11 seq3 = -511103. The legacy VA incumbent band -5102001..-5102011 is ~5.1M — FAR more
     negative than -510101, so OUTSIDE the new band. VA statewide -510001/2/3 are cd=00 (less negative
     than -510101, outside). RESEARCH DB-verified -519999..-510101 is EMPTY (0 rows) 2026-07-01;
     re-verify live before insert. -->
<!-- VA source-disagreement caveat (RESEARCH Pitfall 2): Wikipedia/politics1/vademocrats list different
     Dem rosters for VA-1/2/6 (post-May-8-ruling churn). The VA Dept. of Elections official primary
     spreadsheet (elections.virginia.gov candidate-bulletins, revised 6/3/26) is the TIEBREAKER — pull
     it at execution; the RESEARCH VA table is the reconciliation STARTING POINT, not the final roster. -->
<!-- VA indep/3rd-party filing deadline = Aug 4 (open): seed declared indeps/Libertarians as-declared,
     mark provisional; 159-C reconciles the indep slice (add certified late-filers, prune non-filers). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the full VA qualified field against the VA DoE spreadsheet (D-03 mandatory) + resolve existing race_ids</name>
  <read_first>
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (the VA 11-district field table with existing race_ids: Dem/Rep primary + declared indep/Libertarian per district; the VA-11 Walkinshaw REUSE flag; the thin incumbents Vindman/Subramanyam/McGuire; Pitfall 2 source-disagreement + the VA DoE spreadsheet tiebreaker; the May-8 redistricting note)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-PLAN.md (Task 1: the validated reuse-vs-new reconciliation + external_id band collision check + live-name dedup mechanics; the reuse-onto-existing-races pattern)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md (the FL full-provisional-field reconciliation at scale + accent-normalized dedup + common-name-collision safety)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (VA geo_id 51NN → incumbent_pid + external_id, all 11 incl. Walkinshaw -5102011)
    - backend/src/lib/db.js (pg pool)
  </read_first>
  <action>
    Produce `backend/data/seed-va-2026-house/159-03-va-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per ballot-qualified candidate across all 11 VA districts (upper bound ~51 rows: 11 reused incumbents active + ~40 new). Resolve race_id by joining the EXISTING VA races to districts.geo_id 51NN (the races already exist — confirm the existing race_ids match the RESEARCH VA table).

    RECONCILE the field against the VA Dept. of Elections official primary spreadsheet (elections.virginia.gov candidate-bulletins, "2026 August Democratic Primary: Federal Offices" + "2026 August Republican Primary: All Offices", revised 6/3/26) — this is the TIEBREAKER for the VA-1/2/6 Dem-roster source disagreement and the post-May-8-ruling district assignments. The RESEARCH table is the STARTING POINT. Seed ALL ballot-qualified major-party primary candidates + declared indep/Libertarian (mark provisional). Parse party for context ONLY, NEVER store it.

    For EACH candidate:
    1. If the name matches the district's incumbent (VA-1..11) → REUSE, target = 154 incumbent_pid, is_incumbent=true. **VA-11 Walkinshaw = REUSE -5102011 (already exists), NOT a new record — the stale roadmap note is superseded.**
    2. For every other challenger / indep / Libertarian → run a LIVE name query against essentials.politicians (full_name ILIKE + external_id/office sanity). If a real matching record exists → REUSE that UUID. If 0 matches → NEW, assign a collision-free negative external_id via -(51*10000+cd*100+seq).
    3. Confirm each VA-1..11 incumbent has exactly one active is_incumbent=true row.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id and VERIFY the -519999..-510101 band is empty before use (RESEARCH says empty; re-verify); if any collision, pick another verified-empty band and document it.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-va-2026-house/159-03-va-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<40){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; const wk=body.map(r=>r.split(',')).find(c=>/walkinshaw/i.test(c[3])); if(!wk||wk[5]!=='REUSE'||wk[6]!=='-5102011'){console.error('FAIL Walkinshaw must be REUSE -5102011',wk);process.exit(1)}; console.log('VA reconciliation OK rows',body.length,'new',newids.length,'Walkinshaw REUSE -5102011')"</automated>
  </verify>
  <acceptance_criteria>
    - 159-03-va-reconciliation.csv has one row per VA ballot-qualified candidate across 11 districts (~51 rows incl. declared indep/Libertarian), each with a resolved EXISTING race_id, reconciled against the VA DoE primary spreadsheet (tiebreaker for VA-1/2/6).
    - Every REUSE row's target_politician_id is a real live UUID; VA-1..11 incumbents reuse their 154 incumbent_pid as is_incumbent=true; **VA-11 Walkinshaw = REUSE -5102011 (NOT new)**.
    - Assigned NEW external_ids unique + collision-free vs live external_id<0; band -519999..-510101 verified empty.
    - No minor/indep candidate dropped except genuinely-uncertified per the VA DoE list.
  </acceptance_criteria>
  <done>va-reconciliation.csv exists, all REUSE UUIDs live-confirmed (incl. Walkinshaw -5102011 NOT duplicated), field reconciled against the VA DoE spreadsheet, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent VA candidates seed migration onto the existing races</name>
  <read_first>
    - backend/data/seed-va-2026-house/159-03-va-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + existing race_id map)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics — INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at; no challenger offices)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-01-SUMMARY.md (the PROVISIONAL: description marker convention — apply to the 11 existing VA races via UPDATE if they lack it)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md (D invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card; VA-11 Walkinshaw reuse)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk — coordinate with 159-01's MI migration numbers so VA + MI do not collide; the VA migration takes a distinct number). Name `NNNN_seed_va_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (159-RESEARCH VA section + VA DoE spreadsheet URL) + the antipartisan invariant.

    Migration body, in order (per 157-03 Task-2 mechanics):
    1. (Optional, once) UPDATE the 11 EXISTING VA races SET description = 'PROVISIONAL: pre-primary qualified field, cull ≥ 2026-08-05' WHERE they lack a PROVISIONAL marker (FL-151 D-04) — this is the ONLY permitted write to the races rows, description column only; never touch office_id/election_id.
    2. INSERT the ~40 new VA politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers.
    3. INSERT race_candidates onto the 11 EXISTING VA races: for each reconciliation row, race_id=the resolved existing VA race, politician_id=REUSE target (incl. Walkinshaw -5102011) or just-inserted new UUID, is_incumbent per CSV, candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name).
    4. NEVER create an election or race row (VA already has them); NEVER set office_id NULL; NEVER store party; NEVER create an office; NEVER create a duplicate Walkinshaw.

    Post-COMMIT verification (or run separately), scoped to VA NATIONAL_LOWER (geo prefix '51') within the VA 2026 election: all 11 VA races have ≥1 active candidate; 0 NULL politician_id; 0 duplicate full_name within VA; VA-1..11 incumbents present as is_incumbent=true (incl. Walkinshaw exactly once); 0 new VA elections/races created.

    Run dry first, then apply via psql -v ON_ERROR_STOP=1.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='51'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='51' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); const wk=await pool.query(\"SELECT count(*) n FROM essentials.politicians WHERE external_id=-5102011\"); if(+row.races!==11||+row.nullpid>0||+dup.rows[0].n>0||+wk.rows[0].n!==1){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n,'walkinshaw_records',wk.rows[0].n);process.exit(1)}; console.log('VA OK: 11 races,',row.active_rc,'active rc, 0 null pid, 0 dup, exactly 1 Walkinshaw record'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 VA House races (NATIONAL_LOWER geo '51') carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within VA; exactly 1 Walkinshaw record (external_id -5102011) — no duplicate created.
    - VA-1..11 incumbents present as is_incumbent=true (incl. reused Walkinshaw); declared indep/Libertarian active.
    - 0 new essentials.elections/races/offices rows created (VA already scaffolded); no party stored.
    - The 11 existing VA races carry the PROVISIONAL description marker (added if absent); office_id/election_id untouched.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; VA 11-race assertion passes; dedup correct; Walkinshaw reused (single record); declared indep/Libertarian seeded provisional; no new election/race/office; idempotent. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes ~40 new VA politicians + race_candidates + a description-only UPDATE to 11 existing races on live prod |
| VA DoE spreadsheet → seed input | Field reconciled live against the authoritative VA Dept. of Elections primary spreadsheet before insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-20 | Tampering | duplicate Walkinshaw / duplicate incumbent record | mitigate | Live name-check + reuse 154 incumbent_pid incl. Walkinshaw -5102011; gate asserts exactly 1 Walkinshaw record + 0 dup full_name |
| T-159-21 | Tampering | duplicate VA election/race created (VA already scaffolded) | mitigate | Plan creates NO election/race; resolves existing race_ids; verify asserts 0 new + 11 races |
| T-159-22 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-159-23 | Tampering | phantom candidate from source disagreement (VA-1/2/6) | mitigate | VA DoE spreadsheet is the reconciliation tiebreaker; single-source names flagged |
| T-159-24 | Tampering | party stored on candidate card | mitigate | party parsed for context only; races.primary_party untouched; gate asserts no party column |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 159-verify.sql (authored in 159-06) VA-section USHC2-02/03 assertions pass after this runs.
- Manual: cross-check ~4 VA districts against the VA DoE spreadsheet; confirm exactly 1 Walkinshaw record.
</verification>

<success_criteria>
- 11 VA races each carry their active field, all politician_id non-null; ~40 new records; every incumbent VA-1..11 reused by id (0 dup, Walkinshaw single); declared indep/Libertarian seeded provisional; no new election/race/office; idempotent.
</success_criteria>

<output>
Create `.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-03-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list for downstream headshot/stance plans, VA DoE reconciliation flips vs the RESEARCH table [esp. VA-1/2/6 Dem rosters], Walkinshaw single-record confirmation, declared indep/Libertarian confirmations).
</output>
