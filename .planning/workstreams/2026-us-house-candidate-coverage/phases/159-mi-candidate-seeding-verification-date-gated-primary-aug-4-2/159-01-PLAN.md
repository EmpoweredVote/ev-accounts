---
phase: 159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_mi_2026_house_elections_races.sql
  - backend/migrations/NNNN_seed_mi_2026_house_candidates.sql
  - backend/data/seed-mi-2026-house/159-01-mi-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "An essentials.elections row exists for 'MI 2026 Statewide General', election_date 2026-11-03, election_type 'general', jurisdiction_level 'state', state 'MI', mirroring the live NJ/OH row shape"
    - "Exactly 13 essentials.races rows exist for MI (one per geo_id 2601..2613), each office_id = that district's existing NATIONAL_LOWER U.S. Representative office, never office_id IS NULL, each marked provisional (description LIKE 'PROVISIONAL:%' per the FL-151 D-04 convention)"
    - "Every MI district carries its full ballot-qualified pre-primary field as active race_candidates: all 13 incumbents reused by 154 pid EXCEPT James (-26010) and Stevens (-26011) who VACATE and get NO active row; every genuinely-new challenger + declared indep/Green is a NEW record in the -(26*10000+cd*100+seq) band"
    - "Zero duplicate full_name among active MI candidates; no party stored on any candidate card (party lives on races.primary_party only)"
  artifacts:
    - path: "backend/data/seed-mi-2026-house/159-01-mi-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision + assigned external_id + resolved race_id for all 13 MI districts (major-party primary field + declared indep/Green)"
      min_lines: 60
    - path: "backend/migrations/NNNN_seed_mi_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: 1 MI election + 13 provisional races on existing MI House offices"
      contains: "essentials.races"
    - path: "backend/migrations/NNNN_seed_mi_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: ~56 new MI politicians + race_candidates onto the 13 MI races; James/Stevens excluded from active wiring"
      contains: "race_candidates"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing NATIONAL_LOWER U.S. Representative office for each MI geo_id 2601..2613"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "race_candidates.politician_id"
      to: "154 incumbent_pid (reuse) or newly-inserted MI politician id"
      via: "159-01-mi-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Author the MI `essentials.elections` + 13 provisional `essentials.races` scaffold (MI is fully dark — 0 elections/0 races), then reconcile the full pre-primary ballot-qualified field and wire every qualified Aug-4 primary candidate (all parties) + declared indep/Green as active `race_candidates`, reusing all 13 MI incumbents by pid EXCEPT the two vacating open-seat incumbents (MI-10 James → Governor, MI-11 Stevens → US Senate) who get NO active row. This is the MI half of the seed-now provisional field, a near-verbatim single-state repeat of Phase 157 (157-01 scaffold + 157-03 records) with the FL-151 provisional marker and the two-open-seat wrinkle.

Purpose: USHC2-02 + USHC2-03 for MI. The elections feed surfaces a race only if an essentials.races row exists on the district office; the full qualified field attaches to the Nov-3 general race rows as a provisional field (FL-151 precedent), NOT separate primary-election rows.
Output: Two idempotent migrations + a live-confirmed reconciliation CSV; after they run, every MI House race surfaces its full pre-primary field on /elections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv

<interfaces>
<!-- essentials.elections shape (mirror the live NJ/OH 2026 Statewide General row): id uuid
     gen_random_uuid(), name text, election_date DATE ('2026-11-03'::date — NOT timestamptz, 151-01
     lesson), election_type 'general', jurisdiction_level 'state', state 'MI', description NULL.
     Confirm live NJ row: SELECT * FROM essentials.elections WHERE name='NJ 2026 Statewide General'. -->
<!-- essentials.races shape: id uuid, election_id uuid FK, office_id uuid FK->offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL — antipartisan),
     seats int (1), description text. NO geo_id on races. Provisional marker lives in description
     ('PROVISIONAL:%' per FL-151 D-04; 159-C flips it on prune). Chamber lookup, if needed, MUST be
     exact name = 'U.S. House of Representatives' (NOT ILIKE 'U.S. House%' — 151-01 matched Indiana rows). -->
<!-- MI U.S. Representative offices ALREADY EXIST for all 13 districts (v2.15) — 13 NATIONAL_LOWER
     offices for MI geo_id 2601..2613. office_id lookup is by district geo_id. NEVER create an office.
     MI-10 and MI-11 are OPEN SEATS (incumbent vacates) but the district office STILL EXISTS — this is
     NOT a vacancy-office-creation case like GA-13; no office insert. -->
<!-- essentials.race_candidates: id uuid PK; race_id uuid NOT NULL FK; politician_id uuid (MUST be
     non-null for House); full_name/first_name/last_name; photo_url NULL (feed resolves via
     politician_images); is_incumbent bool; candidate_status text default 'active' CHECK IN
     ('active','withdrawn','filed'); source text; external_id optional. NO unique constraint →
     idempotency app-enforced: (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name,
     last_name, is_active bool. NO updated_at column (149-01 Rule-3). -->
<!-- MI incumbent reuse pids (154-incumbent-map.csv), external_id -26001..-26013:
     MI-1 Bergman -26001 · MI-2 Moolenaar -26002 · MI-3 Scholten -26003 · MI-4 Huizenga -26004 ·
     MI-5 Walberg -26005 · MI-6 Dingell -26006 · MI-7 Barrett -26007 · MI-8 McDonald Rivet -26008 ·
     MI-9 McClain -26009 · MI-10 James -26010 (VACATES, no active row) · MI-11 Stevens -26011
     (VACATES, no active row) · MI-12 Tlaib -26012 · MI-13 Thanedar -26013. -->
<!-- NEW challenger external_id band (D-06): -(26*10000 + cd*100 + seq) — e.g. MI-1 seq1 = -260101;
     MI-13 seq6 = -261306. Incumbents -26001..-26013 are STRICTLY GREATER (less negative) than -260101,
     so the ≤ -260101 cut cleanly separates new from incumbent. RESEARCH DB-verified the band
     -269999..-260101 is EMPTY (0 rows) as of 2026-07-01; re-verify live before insert. -->
<!-- Provisional/open-field caveat: MI minor-party (Green MI-5 Bronke, MI-6 Shabazz) are convention-
     nominated with a 7/16 filing deadline, and declared independents are still filing. Seed the
     major-party primary field + declared indep/Green as-declared, mark provisional; 159-C reconciles. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the MI elections + 13 provisional races scaffold migration</name>
  <read_first>
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (the MI 13-district field table geo_id 2601..2613; Pattern 1 create-races-first; MI-10/MI-11 open-seat notes; the collision-free band check code example)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-01-PLAN.md (the validated create-races-first authoring template — mirror for single-state MI)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-01-SUMMARY.md (the PROVISIONAL: description marker convention + the election_date DATE-not-timestamptz + exact-chamber-name lessons)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (MI geo_id 26NN → incumbent_pid + external_id, all 13 present)
    - backend/migrations (find the highest migration number on disk — currently 1144 — for the next free number; disk wins over DB MAX)
  </read_first>
  <action>
    Pick the next free migration number by listing backend/migrations and incrementing the highest on disk (currently 1144 → use 1145 unless a higher one appears). Name it `NNNN_seed_mi_2026_house_elections_races.sql`. All writes inside one BEGIN; … COMMIT;. Header comment cites the field source (159-RESEARCH.md MI section + the MI BOE report URL) and the antipartisan invariant (party NOT stored on race_candidates; races.primary_party stays NULL).

    Migration body, in order:
    1. INSERT the 1 election idempotently (NOT EXISTS guard on name): 'MI 2026 Statewide General' (state 'MI'), election_date '2026-11-03'::date, election_type 'general', jurisdiction_level 'state', description NULL — mirror the live NJ 2026 Statewide General row exactly.
    2. INSERT one PROVISIONAL race per MI House district onto the EXISTING office, resolving office_id by district geo_id via the join essentials.offices JOIN essentials.districts ON district_id WHERE district_type='NATIONAL_LOWER' AND geo_id matches. For each of the 13 MI geo_ids (2601..2613): election_id = the just-inserted 'MI 2026 Statewide General' election (resolve by name), office_id = the looked-up office, position_name='U.S. Representative District '||cd, primary_party=NULL, seats=1, description='PROVISIONAL: pre-primary qualified field, cull ≥ 2026-08-05' (FL-151 D-04 marker). Guard each with NOT EXISTS on (election_id, office_id).
    3. NEVER create an essentials.offices or districts row (MI-10/MI-11 are open seats, not vacancies — the district office already exists). NEVER set office_id NULL.

    Post-COMMIT verification (or run separately): exactly 1 'MI 2026 Statewide General' election; exactly 13 races on MI NATIONAL_LOWER offices; 0 races with NULL office_id; all 13 marked description LIKE 'PROVISIONAL:%'; every MI geo_id 2601..2613 has exactly 1 race.

    Run dry first (review generated SQL), then apply against prod (kxsdzaojfaibhuzmclfq) from backend/ with `set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`. Do not inline SQL here — author per the 157-01 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT count(*) n FROM essentials.elections WHERE name='MI 2026 Statewide General'\"); const r=await pool.query(\"SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff, count(*) FILTER (WHERE r.description LIKE 'PROVISIONAL:%') prov FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='MI 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='26'\"); if(+e.rows[0].n!==1){console.error('FAIL election',e.rows);process.exit(1)}; if(+r.rows[0].races!==13){console.error('FAIL MI races',r.rows);process.exit(1)}; if(+r.rows[0].nulloff!==0){console.error('FAIL null office_id',r.rows);process.exit(1)}; if(+r.rows[0].prov!==13){console.error('FAIL provisional marker',r.rows);process.exit(1)}; console.log('OK: 1 election, MI 13 provisional races, 0 null office_id'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 'MI 2026 Statewide General' election row, election_date 2026-11-03 (DATE), election_type 'general', jurisdiction_level 'state', state 'MI'.
    - Exactly 13 races on MI NATIONAL_LOWER offices (geo 2601..2613); each office_id non-null and = the existing district office; all 13 description LIKE 'PROVISIONAL:%'.
    - 0 races with NULL office_id; 0 new essentials.offices or districts rows.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven).
  </acceptance_criteria>
  <done>Migration applied; 1 election + 13 provisional races live; verification passes; idempotent. Record the MI election UUID in the SUMMARY for Task 2 + downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the full MI qualified field + author/run the idempotent MI candidates seed migration</name>
  <read_first>
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (the authoritative MI 13-district field table: Dem/Rep primary candidates + declared indep/Green per district; MI-10/MI-11 open-seat handling; MI-12 Downer news-vs-BOE reconciliation note; ~56 new-record upper bound)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-03-PLAN.md (the validated reuse-vs-new reconciliation + external_id band collision check + live-name dedup + idempotent seed-migration mechanics; the open-seat no-active-row pattern for NJ-12 — apply to MI-10/MI-11)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md (the FL full-provisional-field seed at scale: 158 new records + accent-normalized dedup + common-name-collision safety + reconciliation CSV generator pattern)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (MI incumbent_pid + external_id for the reuse set; James -26010 + Stevens -26011 flagged vacating)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    First produce `backend/data/seed-mi-2026-house/159-01-mi-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW|REUSE-NO-ROW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per ballot-qualified candidate across all 13 MI districts (upper bound ~69 rows: 11 reused incumbents active + ~56 new + James/Stevens marked REUSE-NO-ROW). Resolve race_id by joining the Task-1 MI races to districts.geo_id.

    Re-reconcile the field against the live MI BOE PRI-2026 report before deciding rows (the RESEARCH table is the reconciliation STARTING POINT — the BOE report is authoritative over news; MI-12 Allen Downer is news-sourced and must be confirmed on the BOE list). Seed ALL ballot-qualified major-party primary candidates + declared indep/Green (mark provisional). Parse party in parens for context ONLY, NEVER store it.

    For EACH candidate:
    1. If the name matches the district's incumbent (MI-1..9, MI-12, MI-13) → REUSE, target = 154 incumbent_pid, is_incumbent=true.
    2. **MI-10 James (-26010) and MI-11 Stevens (-26011): emit REUSE-NO-ROW marker rows** — they VACATE (James→Gov, Stevens→Senate); keep their existing record, do NOT wire any active race_candidate for them in their old seat (the NJ-12/PA-3 open-seat convention). All MI-10 + MI-11 primary candidates (both parties) are NEW active records.
    3. For every other challenger / indep / Green → run a LIVE name query against essentials.politicians (full_name ILIKE + external_id/office sanity). If a real matching record exists → REUSE that UUID. If 0 matches → NEW, assign a collision-free negative external_id via -(26*10000+cd*100+seq).
    4. Confirm each MI-1..9/12/13 incumbent has exactly one active is_incumbent=true row; MI-10 + MI-11 have NO active incumbent row.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id and VERIFY the -269999..-260101 band is empty before use (RESEARCH says empty 2026-07-01; re-verify); if any collision, pick another verified-empty band and document it (149-01 lesson).

    Then pick the next free migration number, name `NNNN_seed_mi_2026_house_candidates.sql`, all writes in one BEGIN; … COMMIT;. Body (per 157-03 Task-2 mechanics): (1) INSERT the ~56 new MI politicians (NOT EXISTS on external_id), canonical accents, is_active=true, NO essentials.offices rows for challengers. (2) INSERT race_candidates onto the 13 MI races for every reconciliation row EXCEPT the James/Stevens REUSE-NO-ROW markers: race_id resolved, politician_id=REUSE target or new UUID, is_incumbent per CSV, candidate_status='active', source=district source_url; guard each with NOT EXISTS on (race_id, full_name). Do NOT wire James or Stevens active. (3) NEVER touch races rows, NEVER office_id NULL, NEVER store party, NEVER create an office.

    Run dry first, then apply via psql -v ON_ERROR_STOP=1.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='MI 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='26'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='MI 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='26' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); const vac=await pool.query(\"SELECT count(*) n FROM essentials.race_candidates rc JOIN essentials.politicians p ON p.id=rc.politician_id WHERE p.external_id IN (-26010,-26011) AND rc.candidate_status='active'\"); if(+row.races!==13||+row.nullpid>0||+dup.rows[0].n>0||+vac.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n,'james_stevens_active',vac.rows[0].n);process.exit(1)}; console.log('MI OK: 13 races,',row.active_rc,'active rc, 0 null pid, 0 dup, James+Stevens absent'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - 159-01-mi-reconciliation.csv has one row per MI ballot-qualified candidate across 13 districts (~69 rows incl. declared indep/Green + James/Stevens REUSE-NO-ROW markers), each with a resolved race_id, re-confirmed against the MI BOE PRI-2026 report.
    - All 13 MI races carry their active field; 0 active rows with NULL politician_id.
    - Every REUSE row's target_politician_id is a real live UUID; MI-1..9/12/13 incumbents reuse their 154 incumbent_pid as is_incumbent=true; MI-10 James (-26010) + MI-11 Stevens (-26011) ABSENT from the active field (0 active rows for either).
    - 0 duplicate full_name among active MI candidates; no party stored on any candidate card.
    - Assigned NEW external_ids unique + collision-free vs live external_id<0; band -269999..-260101 verified empty.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Both migrations applied; MI 13-race assertion passes; dedup correct; James+Stevens absent from active field; declared indep/Green seeded provisional; idempotent. Record the new-candidate external_id list + the MI election UUID in the SUMMARY for downstream headshot/stance/gate plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migrations write 1 election + 13 races + ~56 new MI politicians + race_candidates to live prod |
| MI BOE report → seed input | Field reconciled live against the authoritative MI Bureau of Elections PRI-2026 report before insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Tampering | duplicate election/race/candidate on re-run | mitigate | NOT EXISTS guards on (name), (election_id, office_id), (race_id, full_name); re-run inserts 0 |
| T-159-02 | Tampering | duplicate incumbent record (v2.4 two-Andy-Barrs) | mitigate | Live name-check per candidate; reuse 154 incumbent_pid; 0-dup-full_name gate |
| T-159-03 | Information Disclosure | vacating James/Stevens wired as active in old seat | mitigate | REUSE-NO-ROW markers honored; verify asserts James -26010 + Stevens -26011 absent from active field |
| T-159-04 | Tampering | race wired to wrong/NULL office or accidental office creation | mitigate | office_id resolved by district geo_id join; 0 NULL office_id + 0 new offices asserted (open seat ≠ vacancy) |
| T-159-05 | Tampering | party stored on candidate card | mitigate | party parsed for context only; races.primary_party NULL; gate asserts no party column |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migrations) |
</threat_model>

<verification>
- 159-verify.sql (authored in 159-06) MI-section USHC2-02/03 assertions pass after this runs.
- Manual: spot-check 3 MI races resolve to the correct office by geo_id; confirm MI-10/MI-11 have no incumbent active row.
</verification>

<success_criteria>
- 1 MI election + 13 provisional races live; full pre-primary field wired active; James+Stevens absent; ~56 new records; 0 dup; 0 null pid; idempotent.
</success_criteria>

<output>
Create `.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-01-SUMMARY.md` when done (record: both migration numbers, the MI election UUID, per-district active counts, the new-candidate external_id list for downstream headshot/stance plans, any BOE-list flips vs the RESEARCH table [esp. MI-12 Downer], declared indep/Green confirmations, James/Stevens no-active-row confirmation).
</output>
