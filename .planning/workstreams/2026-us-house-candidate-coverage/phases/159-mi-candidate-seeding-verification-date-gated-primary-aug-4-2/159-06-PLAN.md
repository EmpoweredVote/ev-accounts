---
phase: 159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2
plan: 06
type: execute
wave: 4
depends_on: ["159-05"]
files_modified:
  - backend/scripts/159-verify.sql
  - backend/scripts/159-coordinate-smoke.ts
autonomous: true
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05, USHC2-06]
must_haves:
  truths:
    - "159-verify.sql runs read-only against prod .env and exits 0 with final stdout line 'ALL ASSERTIONS PASSED', asserting the MI+VA milestone invariants that (combined with the passing Phase 158 89-district gate) prove the full 113-district milestone"
    - "USHC2-06-SCOPE confirms exactly MI 13 + VA 11 = 24 distinct NATIONAL_LOWER races (geo prefixes '26' and '51')"
    - "USHC2-06-VACATE proves MI-10 James (-26010) and MI-11 Stevens (-26011) have 0 active race_candidates rows (open-seat vacate pins, mirroring NJ-12 Watson Coleman in 158); USHC2-06-REUSE proves VA-11 Walkinshaw exists as exactly 1 record (external_id -5102011), never duplicated"
    - "USHC2-06-UNSOURCED is an EXISTENCE check returning 0 (any active MI/VA candidate answer row lacking a sourced context row) — NOT a per-candidate coverage assertion; USHC2-06-DUPINCUMBENT proves 0 politician_id active in 2+ races; USHC2-06-NULLPID proves 0 active rows with NULL politician_id; USHC2-06-PARTY proves no party column on race_candidates"
    - "159-coordinate-smoke.ts runs read-only, exits 0, prints 'COORDINATE SMOKE GREEN' surfacing ≥3 MI + ≥3 VA in-district samples each with an active field present (incl. an MI open-seat sample with 0 is_incumbent rows)"
  artifacts:
    - path: "backend/scripts/159-verify.sql"
      provides: "Consolidated read-only SQL gate over all 24 MI+VA districts (mirrors 158-verify.sql dual-state shape + per-state pins from 159-01/03/05 SUMMARYs)"
      contains: "USHC2-06-SCOPE"
    - path: "backend/scripts/159-coordinate-smoke.ts"
      provides: "MI+VA ST_Covers coordinate-surfacing smoke with Pitfall-5 field-present assertion (clone of 158-coordinate-smoke.ts)"
      contains: "COORDINATE SMOKE GREEN"
  key_links:
    - from: "backend/scripts/159-verify.sql"
      to: "essentials.elections ('MI 2026 Statewide General' + the VA 2026 general)"
      via: "SELECT id INTO ..._eid FROM essentials.elections WHERE name = ..."
      pattern: "2026 Statewide General"
    - from: "backend/scripts/159-coordinate-smoke.ts"
      to: "src/lib/electionService.ts getElectionsByCoordinate Part A"
      via: "ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(lng,lat),4326)) join"
      pattern: "ST_Covers"
---

<objective>
Author the consolidated MI+VA verification gate for Phase 159 (mirroring Phase 158's dual-state gate): a read-only SQL gate (`159-verify.sql`) asserting milestone-level invariants across all 24 MI+VA districts (MI 13 + VA 11), plus a coordinate surfacing smoke (`159-coordinate-smoke.ts`) proving in-district MI + VA addresses resolve to their House race with the field present. Combined with the passing Phase 158 89-district gate, this closes the full 113-district v2.21 milestone.

Purpose: USHC2-06 (MI+VA portion) + consolidated USHC2-02/03/04/05 verification. Runs AFTER the 159-05 cull so it asserts the settled post-primary field (each district shows its advancing nominee(s)); the vacate/reuse/two-path invariants hold both pre- and post-cull.
Output: `backend/scripts/159-verify.sql` + `backend/scripts/159-coordinate-smoke.ts`, both write-free, both green against prod .env. Do NOT re-assert MI or VA in 158's gate — 159 owns MI+VA, 158 owns the 89 decided districts; the milestone is the union.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-01-SUMMARY.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-02-SUMMARY.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-03-SUMMARY.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-04-SUMMARY.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-05-SUMMARY.md

# Analog scripts — model the new files structurally on these.
@backend/scripts/158-verify.sql
@backend/scripts/158-coordinate-smoke.ts
@backend/scripts/157-verify.sql
@backend/scripts/157-coordinate-smoke.ts
@backend/src/lib/electionService.ts

<interfaces>
<!-- Resolve elections by EXACT name at runtime (RAISE EXCEPTION on NULL): 'MI 2026 Statewide General'
     (MI FIPS 26 / geo 2601..2613 / 13 races) + the VA 2026 general election (confirm exact name from
     159-03-SUMMARY; VA FIPS 51 / geo 5101..5111 / 11 races). Geo prefixes substr(d.geo_id,1,2):
     MI='26', VA='51'. Do NOT hardcode ids; resolve by name. -->
<!-- getElectionsByCoordinate Part A (src/lib/electionService.ts) surfacing join to mirror:
     JOIN essentials.geofence_boundaries gb ON gb.geo_id=d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR
     gb.mtfcc=d.mtfcc) WHERE ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($lng,$lat),4326)). -->
<!-- State-specific wrinkles the gate + smoke MUST tolerate (mirror the 158 vacancy handling):
     MI-10 (geo 2610) OPEN SEAT — James (-26010) vacated (Gov run): 0 active rows for James; the seat's
       nominees active, 0 is_incumbent=true in the seat (no incumbent). Mirror the NJ-12 pin shape.
     MI-11 (geo 2611) OPEN SEAT — Stevens (-26011) vacated (Senate run): same shape.
     VA-11 (geo 5111) — Walkinshaw (-5102011) REUSED, active is_incumbent=true, exactly 1 record (assert
       0 duplicate Walkinshaw records — the stale roadmap "new record" note is superseded). -->
<!-- Post-cull note: this gate runs after 159-05, so the field is the settled Nov-3 field (advancing
     nominees). USHC2-06-ACTIVE hard floor = ≥1 active per race; report <2-active as a NOTICE allowance
     (safe-seat single-candidate generals), never a failure. -->
<!-- Per-candidate stance/headshot honest-skip pins are owned by the per-state seed SUMMARYs
     (159-02 for MI, 159-04 for VA) — pin whole-record skips by exact UUID/external_id with explicit
     ORDER BY (143 lesson). USHC2-06-UNSOURCED is the existence check (mirror 158/152), NOT per-candidate
     coverage — it must not false-fail on the 8 untouched partial VA incumbents. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 159-verify.sql — consolidated read-only MI+VA 24-district gate</name>
  <files>backend/scripts/159-verify.sql</files>
  <read_first>
    - backend/scripts/158-verify.sql (THE structural analog — dual-state consolidated gate with _house TEMP TABLE, DO $$ block, labeled assertions, the NJ-12 vacate + existence-check-unsourced patterns; clone re-scoped to MI+VA / 24 districts)
    - backend/scripts/157-verify.sql (single-state per-phase gate — the reuse_pid pin-set + honest-skip ORDER BY + open-seat handling patterns)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-01-SUMMARY.md + 159-03-SUMMARY.md (MI + VA election names/UUIDs, per-district counts, new-candidate external_id bands, James/Stevens vacate + Walkinshaw reuse confirmations)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-02-SUMMARY.md + 159-04-SUMMARY.md (the headshot + whole-record-stance honest-skip pids/external_ids to pin)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-05-SUMMARY.md (the settled post-cull advancing nominees — the field the gate now asserts)
  </read_first>
  <action>
    Create backend/scripts/159-verify.sql modeled structurally on 158-verify.sql, re-scoped to MI (13) + VA (11) = 24 districts. Open with `\set ON_ERROR_STOP on`. Header comment covers: (a) read-only/write-free rationale (SELECT-only + CREATE TEMP TABLE … ON COMMIT DROP only; no INSERT/UPDATE/DELETE, no --commit); (b) the two-election scope with geo prefixes (MI=26/13 races, VA=51/11 races = 24); (c) a note that the 89 decided districts are asserted by the passing Phase 158 gate and the full 113-district milestone is the UNION of the two gates — this gate MUST NOT reference the other 6 states; (d) that per-candidate stance/headshot honest-skip pins are documented from 159-02 (MI) + 159-04 (VA) and pinned by exact UUID/external_id with explicit ORDER BY; (e) the exact read-only run command.

    Resolve both election ids by EXACT name at runtime (SELECT id INTO mi_eid / va_eid) and RAISE EXCEPTION if either is NULL. Build ONE working-set TEMP TABLE `_house ON COMMIT DROP` with a `st` CASE column (MI/VA keyed on election_id) plus race_id, geo_id, rc_id, politician_id, full_name, candidate_status, is_incumbent, p.external_id — sourced from races JOIN offices JOIN districts LEFT JOIN race_candidates LEFT JOIN politicians, scoped WHERE r.election_id IN (mi_eid, va_eid) AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('26','51'). Every assertion reads from `_house` except the party structural check + the unsourced existence check.

    Author these labeled assertions inside one DO $$ block, each ending in RAISE EXCEPTION (fail) or RAISE NOTICE 'PASS ...'; end with RAISE NOTICE 'ALL ASSERTIONS PASSED (...)':
    1. USHC2-06-SCOPE — per-state COUNT(DISTINCT race_id): assert MI=13, VA=11, total=24.
    2. USHC2-06-ACTIVE — 0 races with <1 active candidate (RAISE EXCEPTION); report <2-active count as a NOTICE allowance (safe-seat single-candidate generals), never a failure.
    3. USHC2-06-NULLPID — 0 active race_candidates with NULL politician_id across all 24.
    4. USHC2-06-DUPNAME — 0 duplicate lower(full_name) among ACTIVE candidates GROUP BY st (per-state).
    5. USHC2-06-DUPINCUMBENT — 0 politician_id active in 2+ distinct race_id (v2.4 two-Andy-Barrs invariant).
    6. USHC2-06-UNSOURCED — EXISTENCE check (mirror 158/152 exactly): 0 rows in inform.politician_answers for any politician_id active-in-_house where NOT EXISTS a matching inform.politician_context with sources IS NOT NULL AND array_length(sources,1) >= 1. NOT a per-candidate coverage assertion (must not false-fail on the 8 untouched partial VA incumbents).
    7. USHC2-06-VACATE — MI-10 (st='MI', geo_id='2610') James (external_id -26010) appears 0 times active AND 0 active is_incumbent=true rows in 2610 (open seat, no incumbent); MI-11 (geo_id='2611') Stevens (-26011) same shape (0 active + 0 is_incumbent in 2611).
    8. USHC2-06-REUSE — VA-11 (st='VA', geo_id='5111') Walkinshaw exists as EXACTLY 1 essentials.politicians record (external_id -5102011) AND is active is_incumbent=true in 5111 (assert 0 duplicate Walkinshaw records — the stale roadmap note is superseded).
    9. USHC2-06-PARTY — information_schema.columns: essentials.race_candidates has 0 columns named 'party' or 'party_affiliation'.

    Define the honest-skip pin sets (headshot from 159-02/04, whole-record stance from 159-02/04) as TEMP tables with explicit ORDER BY (143 lesson). Keep the file write-free (only CREATE TEMP TABLE … ON COMMIT DROP).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/159-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - Exact run command exits 0; final stdout line is exactly `ALL ASSERTIONS PASSED` (parenthetical listing all assertion names).
    - All labeled assertion names present: USHC2-06-SCOPE, -ACTIVE, -NULLPID, -DUPNAME, -DUPINCUMBENT, -UNSOURCED, -VACATE, -REUSE, -PARTY.
    - USHC2-06-SCOPE NOTICE confirms `MI 13 + VA 11 = 24` distinct NATIONAL_LOWER races.
    - USHC2-06-VACATE proves James -26010 + Stevens -26011 have 0 active rows + 0 is_incumbent in their seats; USHC2-06-REUSE proves exactly 1 Walkinshaw record (-5102011).
    - USHC2-06-UNSOURCED is the existence check (mirror 158/152), NOT per-candidate coverage.
    - File is write-free: no INSERT/UPDATE/DELETE against essentials|inform, no `--commit`; only CREATE TEMP TABLE … ON COMMIT DROP.
    - Header states the 89 districts are asserted by Phase 158 and the milestone is the union; honest-skip pins owned by 159-02/04.
  </acceptance_criteria>
  <done>159-verify.sql exists, runs read-only against prod .env, exits 0, prints ALL ASSERTIONS PASSED; all assertions labeled; scope confirmed MI 13 + VA 11 = 24.</done>
</task>

<task type="auto">
  <name>Task 2: Author 159-coordinate-smoke.ts — MI+VA ST_Covers surfacing smoke</name>
  <files>backend/scripts/159-coordinate-smoke.ts</files>
  <read_first>
    - backend/scripts/158-coordinate-smoke.ts (THE structural analog — per-state STATE_CONFIG, ST_PointOnSurface centroid, ST_Covers surfacing query, minActive + field-present + null_pid checks, MIN_DISTRICTS gate; clone re-scoped to MI+VA)
    - backend/scripts/157-coordinate-smoke.ts (the open-seat/uncontested sample handling if a wrinkle sample is chosen — the MI-10/MI-11 open-seat samples mirror the NJ-12 open-seat sample)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate Part A — the mtfcc-tolerant ST_Covers geofence join to mirror)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-05-SUMMARY.md (the settled advancing nominees per district — pick contested samples with a known active field post-cull)
  </read_first>
  <action>
    Create backend/scripts/159-coordinate-smoke.ts modeled on 158-coordinate-smoke.ts, scoped to MI + VA. Import `pool` from '../src/lib/db.js'. Define a STATE_CONFIG map for MI/VA giving each state its exact election name ('MI 2026 Statewide General' + the VA 2026 general name from 159-03-SUMMARY) and geo prefix (MI=26, VA=51). Resolve both election ids up front by exact name; throw if either missing.

    Pick ≥3 in-district samples per state (≥6 total) from the settled post-cull field, INCLUDING at least one MI open-seat sample (MI-10 geo 2610 or MI-11 geo 2611 — assert 0 is_incumbent=true rows, James/Stevens absent) and at least one VA renominated-contested sample (e.g. VA-7 geo 5107 with Vindman + a challenger). For contested samples set minActive=2; for a settled uncontested seat set minActive=1 (documented allowance).

    For each sample: compute the interior centroid via public.ST_X/ST_Y(public.ST_PointOnSurface(gb.geometry)) (mtfcc-tolerant join, LIMIT 1), then run the surfacing query mirroring getElectionsByCoordinate Part A — JOIN geofence_boundaries gb ON gb.geo_id=d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc), WHERE r.election_id=$eid AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)=$geoPrefix AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint($lng,$lat),4326)) GROUP BY r.id — selecting COUNT(*) FILTER active, COUNT(*) FILTER active AND is_incumbent=false AS challengers, COUNT(*) FILTER active AND politician_id IS NULL AS null_pid, array_agg(lower(full_name)) FILTER active. Assert per sample: exactly 1 House race surfaced, active >= minActive, null_pid = 0; for the open-seat sample additionally assert 0 is_incumbent=true rows.

    Set MIN_DISTRICTS = 6 and require ≥3 MI + ≥3 VA to surface cleanly; on any failure collect messages + process.exit(1). On success print a final line beginning `COORDINATE SMOKE GREEN` stating ≥3 MI + ≥3 VA surface their US House race with the settled field, then `await pool.end()`. End with `main().catch((e) => { console.error(e); process.exit(1); });`. SELECT-only.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/159-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exact run command exits 0; final stdout line begins `COORDINATE SMOKE GREEN` and reports ≥3 MI + ≥3 VA surfacing.
    - Each sample prints a PASS line showing >=minActive active AND 0 null pid; the MI open-seat sample shows 0 is_incumbent rows (James/Stevens absent).
    - MIN_DISTRICTS = 6; the script fails (exit 1) if fewer than 6 districts surface cleanly.
    - The surfacing query mirrors getElectionsByCoordinate Part A (mtfcc-tolerant ST_Covers) scoped by election id + NATIONAL_LOWER + per-state geo prefix.
    - Script is SELECT-only; imports pool from ../src/lib/db.js.
  </acceptance_criteria>
  <done>159-coordinate-smoke.ts exists, runs read-only, exits 0, prints COORDINATE SMOKE GREEN with ≥3 MI + ≥3 VA samples incl. an MI open-seat sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke → prod DB (read) | Both read live prod via DATABASE_URL; a mis-scoped or write-bearing query could mutate or misreport milestone state |
| assertion logic → milestone truth | The gate's assertions ARE the MI+VA milestone acceptance signal; a relaxed/false-passing assertion silently ships a broken field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-50 | Tampering | 159-verify.sql relaxed to force green | mitigate | Assertions are exact integer-literal counts (24 total, MI 13 / VA 11) + 0-invariants; USHC2-06-UNSOURCED is the same existence check as the passing 158/152 gates; acceptance requires the exact `ALL ASSERTIONS PASSED` line |
| T-159-51 | Repudiation | settled uncontested seat false-fails "every race >=2 active" | mitigate | Hard floor >=1 active per race (RAISE EXCEPTION); <2-active reported as a NOTICE allowance only |
| T-159-52 | Information Disclosure | vacating James/Stevens silently surfaced as active | mitigate | USHC2-06-VACATE pins MI-10/MI-11 0-active + 0-incumbent; smoke's open-seat sample asserts 0 is_incumbent |
| T-159-53 | Tampering | duplicate Walkinshaw record slips through | mitigate | USHC2-06-REUSE asserts exactly 1 Walkinshaw record (-5102011) |
| T-159-54 | Tampering | gate writes to prod | mitigate | WRITE-FREE mandate: SELECT-only + CREATE TEMP TABLE … ON COMMIT DROP only; acceptance asserts no write DML |
| T-159-55 | Spoofing | wrong election resolved / decided-state leakage | mitigate | Elections resolved by EXACT name with RAISE EXCEPTION on NULL; scope pinned to geo '26'/'51'; header excludes the 89 decided districts (asserted by 158) |
</threat_model>

<verification>
Run both scripts read-only against prod .env from backend/:
1. SQL gate: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/159-verify.sql` → exit 0, all assertions PASS, final line `ALL ASSERTIONS PASSED`, SCOPE confirms MI 13 + VA 11 = 24.
2. Coordinate smoke: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/159-coordinate-smoke.ts` → exit 0, `COORDINATE SMOKE GREEN` ≥3 MI + ≥3 VA.

Neither command writes to the DB (grep the files for INSERT/UPDATE/DELETE/--commit — none present).
</verification>

<success_criteria>
Maps to the ROADMAP Phase 159 success criteria (gate portion, criterion 5):
- In-district MI + VA address → the House race with the settled field present (Pitfall-5 guard): USHC2-06-SCOPE + -ACTIVE + -VACATE + the coordinate smoke.
- 0 unsourced: USHC2-06-UNSOURCED (existence check). 0 duplicate-incumbent: USHC2-06-DUPINCUMBENT + -REUSE (single Walkinshaw). All 24 districts >=1 active: USHC2-06-SCOPE + -ACTIVE. Combined with Phase 158, the full 113-district milestone is proven.
</success_criteria>

<output>
Create `.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-06-SUMMARY.md` when done, recording: the two scripts authored, the gate's assertion results (with the SCOPE breakdown MI 13 / VA 11 = 24 and the <2-active allowance count), the smoke's MI+VA surfacing result (incl. the open-seat sample), and confirmation both ran write-free against prod .env (exit 0). Note the milestone closes as the union of the 158 (89) + 159 (24) gates = 113 districts.
</output>
