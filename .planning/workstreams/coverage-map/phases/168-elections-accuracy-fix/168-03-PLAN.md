---
phase: 168-elections-accuracy-fix
plan: 03
type: execute
wave: 2
depends_on: ["168-01"]
files_modified:
  - admin/src/pages/admin/coverageTypes.ts
  - admin/src/pages/admin/StatewideRacesPanel.tsx
  - admin/src/pages/admin/CoveragePage.tsx
  - admin/src/pages/admin/CoverageMap.tsx
autonomous: false
requirements: [ELEC-01, ELEC-02, ELEC-03]
must_haves:
  truths:
    - "The state choropleth fill in elections mode is colored by the statewide/legislative coverage number (D-01), not the old lumped number"
    - "Clicking a state in elections mode shows a statewide-races panel listing its statewide/legislative races with candidate coverage, rendered together with the existing county view (D-02)"
    - "A state with no county-pinnable races shows its county number as 'N/A — no county-level races', visually distinct from a real 0% (D-03)"
    - "The Michigan case: a statewide-only state shows real statewide coverage in the fill/readout, an N/A county number, and a populated statewide-races panel on click — nothing in the state view contradicts the county drill-down"
  artifacts:
    - path: "admin/src/pages/admin/coverageTypes.ts"
      provides: "StateElection extended to mirror the Plan-01 backend contract (countyCoverage + statewideRaces)"
      contains: "countyCoverage"
    - path: "admin/src/pages/admin/StatewideRacesPanel.tsx"
      provides: "net-new panel component listing statewide/legislative races with candidate coverage (ELEC-02)"
      min_lines: 25
    - path: "admin/src/pages/admin/CoveragePage.tsx"
      provides: "panel mounted in the elections+selected branch, rendered alongside the county view (D-02)"
      contains: "StatewideRacesPanel"
    - path: "admin/src/pages/admin/CoverageMap.tsx"
      provides: "state fill/readout repointed to statewide coverage + N/A vs 0% county-number distinction (D-01, D-03)"
      contains: "countyCoverage"
  key_links:
    - from: "admin/src/pages/admin/CoveragePage.tsx"
      to: "admin/src/pages/admin/StatewideRacesPanel.tsx"
      via: "conditional render gated on metric === 'elections' && selected, fed from elecStatesByFips"
      pattern: "StatewideRacesPanel"
    - from: "admin/src/pages/admin/CoverageMap.tsx"
      to: "StateElection.countyCoverage"
      via: "state hover readout branch mirroring electionCountyColor's unknown/scored distinction"
      pattern: "countyCoverage\\.status"
---

<objective>
Surface Plan 01's corrected numbers in the admin elections UI. Per D-01, repoint the state choropleth fill and hover readout to the statewide/legislative coverage number (the number the Michigan bug wrongly reported as 100%). Per D-02, add a net-new `StatewideRacesPanel` and mount it in the currently panel-less elections branch so clicking a state shows the statewide-races panel AND the existing county view together. Per D-03, render a state's county-pinnable number as "N/A — no county-level races" when `countyCoverage.status === 'unknown'`, visually distinct from a real 0% — reusing the exact `unknown`/`scored` vocabulary the county fill already implements.

Mirror the Plan 01 backend `StateElection` contract (read 168-01-SUMMARY.md for the exact final shape) in the frontend `coverageTypes.ts`. No new fetch, no new query, no new endpoint — `statewideRaces` and `countyCoverage` arrive as part of the already-lazily-fetched `elecStates` array; `onSelectState` already sets `selected` on any state click regardless of metric, so only new render output is required.

Implements ELEC-01 (D-01 map coloring / surfacing the two numbers), ELEC-02 (D-02 statewide-races panel), and ELEC-03 (D-03 N/A vs 0% so the state view never contradicts the county drill-down).

Purpose: make "never contradicts its own map score" visible to the admin in one click.
Output: extended frontend type, new panel component, panel mount in `CoveragePage`, repointed fill + N/A readout in `CoverageMap`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-CONTEXT.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-RESEARCH.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-PATTERNS.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-01-SUMMARY.md
@admin/src/pages/admin/coverageTypes.ts
@admin/src/pages/admin/CoverageMap.tsx
@admin/src/pages/admin/CoveragePage.tsx
@admin/src/pages/admin/CoverageTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend StateElection type + build StatewideRacesPanel + repoint CoverageMap fill/readout</name>
  <files>admin/src/pages/admin/coverageTypes.ts, admin/src/pages/admin/StatewideRacesPanel.tsx, admin/src/pages/admin/CoverageMap.tsx</files>
  <action>Extend `StateElection` in `admin/src/pages/admin/coverageTypes.ts` (lines 31-34) to mirror the EXACT final backend shape recorded in 168-01-SUMMARY.md — add `countyCoverage: { status: 'unknown' | 'scored'; coverage: number; races_total: number; races_covered: number }` and `statewideRaces: ElectionRace[]` (reuse the existing `ElectionRace` shape at line 35 — same fields as the backend `RaceRow`). Do NOT introduce a numeric sentinel for N/A; the `status` field is the codebase vocabulary (mirrors `CountyElection.status`). Then create `admin/src/pages/admin/StatewideRacesPanel.tsx` following PATTERNS.md analog `CoverageTable.tsx` `UsOverviewTable` (lines 163-216) for the table shell/header/empty-state styling: prop `{ stateElection: StateElection | null }`; render nothing if null; render a titled panel (e.g. "Statewide & legislative races (N)") with a plain `<table className="w-full text-sm">` listing each `statewideRaces` row — `position_name` and a candidate cell (e.g. `{r.candidate_count}/{r.seats} candidates` or similar). Include an empty-state row ("No statewide/legislative races for this election") when `statewideRaces.length === 0`; no loading skeleton (data arrives pre-fetched, per RESEARCH.md Pitfall 4). Optionally surface the state's `countyCoverage` "N/A — no county-level races" line in the panel header for at-a-glance consistency. In `admin/src/pages/admin/CoverageMap.tsx`: per D-01, `electionStateColor` keeps reading `s.coverage` (now correctly the statewide-only number after the Plan-01 fix — no change needed to that function). Add the N/A distinction to the STATE-level readout: mirror the existing `electionCountyColor` / county hover-readout `unknown`/`scored` pattern (lines 38-42 and 152-160) so the state readout shows the county-pinnable number as "N/A — no county-level races" when `es.countyCoverage.status === 'unknown'`, else `{es.countyCoverage.coverage}%` — reusing the `NO_RACE_DATA` constant (line 22) for any legend/swatch. Grep `\.coverage\b` across `CoverageMap.tsx` after editing to confirm both the fill (line 37) and the hover readout (line 145) read intentionally and agree (RESEARCH.md Pitfall 1).</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -iE 'coverageTypes|StatewideRacesPanel|CoverageMap' | grep -c error | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <done>`StateElection` extended with `countyCoverage` + `statewideRaces` matching the backend contract. `StatewideRacesPanel.tsx` exists, renders the statewide race list + empty state, uses `CoverageTable`'s table styling. `CoverageMap.tsx` state readout distinguishes N/A (`status:'unknown'`) from a real 0% and the fill/readout agree. Admin TypeScript compiles clean for these files. No numeric N/A sentinel introduced (grep confirms `status` used, not `-1`/`null` for the county number).</done>
</task>

<task type="auto">
  <name>Task 2: Mount StatewideRacesPanel in CoveragePage elections branch</name>
  <files>admin/src/pages/admin/CoveragePage.tsx</files>
  <action>In `admin/src/pages/admin/CoveragePage.tsx`, add a new conditional render block gated `metric === 'elections' && selected`, rendering `<StatewideRacesPanel stateElection={elecStatesByFips.get(selected.fips) ?? null} />`, inserted directly after the map wrapper `</div>` (line ~168) and before the existing `{metric === 'completeness' && <CoverageTable .../>}` block (lines 170-180) — a sibling to that block, gated the opposite way. Import `StatewideRacesPanel`. `elecStatesByFips` is already computed via `useMemo` (line 52); `onSelectState` (lines 75-83) already sets `selected` and calls `loadCounties(code)` on any state click regardless of metric, so the county view continues to render alongside the panel (D-02: both visible together) with no new click-handling. Do NOT add a new fetch — the panel data comes from the already-populated `elecStates` array.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -i 'CoveragePage' | grep -c error | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <done>`CoveragePage.tsx` renders `StatewideRacesPanel` in an `metric === 'elections' && selected` branch, sibling to the completeness table block, fed from `elecStatesByFips`. County view still renders alongside it (D-02). Admin TypeScript compiles clean for the page. No new fetch/effect added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Michigan-case manual walkthrough (D-01/D-02/D-03 visual verify)</name>
  <files>admin/src/pages/admin/CoveragePage.tsx, admin/src/pages/admin/CoverageMap.tsx, admin/src/pages/admin/StatewideRacesPanel.tsx</files>
  <action>PAUSE for human verification. No frontend test runner is configured for `admin/` (RESEARCH.md), so the Michigan-case walkthrough is verified manually after Tasks 1-2 land. Present the how-to-verify steps to the user and wait for the resume signal — do not auto-advance.</action>
  <what-built>The corrected elections coverage map: state fill colored by statewide/legislative coverage (D-01), a statewide-races panel shown on state click alongside the county view (D-02), and an "N/A — no county-level races" county number distinct from a real 0% (D-03).</what-built>
  <how-to-verify>
    1. Run the admin app locally (start backend + `cd admin && npm run dev`, or the project's usual admin dev command) and open the coverage page.
    2. Toggle to elections mode. Confirm the state map fill reflects statewide/legislative coverage — a state previously showing an undifferentiated 100% (the Michigan case) now shows its real statewide number, not 100%.
    3. Click that state. Confirm BOTH appear together: (a) the new statewide-races panel listing its statewide/legislative races with candidate coverage, and (b) the existing county choropleth/drill-down.
    4. Confirm the state's county-pinnable number reads "N/A — no county-level races" (NOT 0% and NOT 100%) for a state with no county races — and that this is visually distinct from a state that has county races with zero candidates (a real 0%).
    5. Confirm nothing in the state view contradicts the county drill-down (ELEC-03).
  </how-to-verify>
  <verify>
    <human-check>Michigan-case walkthrough steps 1-5 pass; fill = statewide number, panel + county view visible together, N/A distinct from 0%, no state/county contradiction.</human-check>
  </verify>
  <done>Human confirms: state fill reflects the statewide/legislative number (D-01), the statewide-races panel renders alongside the county view on state click (D-02), the N/A county number is distinct from a real 0% (D-03), and the state view does not contradict the county drill-down (ELEC-03).</done>
  <resume-signal>Type "approved" or describe what looks wrong (fill number, panel contents, N/A text, or a state/county contradiction).</resume-signal>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → admin SPA render | Renders only data already fetched from the admin-gated `/coverage/map` endpoint; no new fetch, no new input surface, no new field beyond the admin-visible coverage counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-04 | Information Disclosure | `StatewideRacesPanel` renders `statewideRaces` (position_name, seats, candidate_count) | accept | Renders only admin-already-visible race coverage data behind the existing admin-authenticated coverage page; no PII, no new field leaked to unauthorized callers. Panel renders client-side from data the admin already received. |
| T-168-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — panel copies the plain `<table>` styling from `CoverageTable.tsx`; no new UI library (RESEARCH.md: none needed, none expected). If an executor adds a package, run the legitimacy gate first. |
</threat_model>

<verification>
- `cd admin && npx tsc --noEmit` — admin app typechecks clean across the four touched files.
- Manual Michigan-case walkthrough (Task 3 checkpoint) confirms fill = statewide number, panel + county view visible together, N/A county number distinct from 0%, no state/county contradiction.
</verification>

<success_criteria>
- State choropleth fill + readout reflect the statewide/legislative coverage number (D-01 / ELEC-01 surfacing).
- Clicking a state shows the statewide-races panel alongside the county view (D-02 / ELEC-02).
- A no-county-races state shows "N/A — no county-level races", distinct from a real 0% (D-03 / ELEC-03).
- The Michigan case is visibly consistent: state view and county drill-down no longer contradict.
</success_criteria>

<output>
Create `.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-03-SUMMARY.md` when done.
</output>
