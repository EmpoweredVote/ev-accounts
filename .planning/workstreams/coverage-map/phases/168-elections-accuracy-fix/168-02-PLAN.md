---
phase: 168-elections-accuracy-fix
plan: 02
type: execute
wave: 2
depends_on: ["168-01"]
files_modified:
  - backend/src/routes/admin.test.ts
autonomous: true
requirements: [ELEC-03]
must_haves:
  truths:
    - "A route-level test proves GET /coverage/map?metric=elections&level=state forwards the partitioned StateElection payload (two numbers + statewideRaces) unmodified through the route layer"
    - "The test asserts a state's countyCoverage denominator is internally consistent — never contradicting what the county endpoint would report for the same state (ELEC-03 contract at the route glue layer)"
    - "The test runs without a live DB by mocking electionsMapService and the auth middleware, following the people.test.ts convention"
  artifacts:
    - path: "backend/src/routes/admin.test.ts"
      provides: "route-level integration test for the /coverage/map elections branches (ELEC-03 wiring guarantee)"
      contains: "coverage/map"
  key_links:
    - from: "backend/src/routes/admin.test.ts"
      to: "backend/src/routes/admin.ts"
      via: "supertest against the mounted admin router with electionsMapService mocked"
      pattern: "getElectionsStateScores|getElectionsCountyScores"
---

<objective>
Close the route-layer verification gap flagged in RESEARCH.md (Assumption A2, VALIDATION.md Wave 0): there is currently NO `admin.test.ts`, so ELEC-03's "the route never returns contradictory numbers" contract is only covered at the pure-helper level. This plan adds a narrowly-scoped route-level integration test for the `/coverage/map?metric=elections` branches, proving the route forwards Plan 01's partitioned payload unmodified and that the state and county branches agree on denominators when fed the same underlying race data.

This is pure test infrastructure — it adds no production behavior. It exists so a regression in the route glue (not the pure partition) cannot silently reintroduce a Michigan-shaped contradiction.

Implements ELEC-03 verification at the route/contract layer.

Purpose: guarantee the cross-endpoint denominator consistency is checked automatically, not just by manual QA.
Output: new `backend/src/routes/admin.test.ts` scoped to the elections coverage-map branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-RESEARCH.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-PATTERNS.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-01-SUMMARY.md
@backend/src/routes/admin.ts
@backend/src/routes/people.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin.test.ts route-level test for the elections coverage-map branches</name>
  <files>backend/src/routes/admin.test.ts</files>
  <behavior>
    - `GET /api/admin/coverage/map?metric=elections&level=state` returns 200 and the exact `{ states: StateElection[] }` payload the mocked `getElectionsStateScores` returns — the route performs no transformation, so the partitioned shape (repointed `coverage`, `countyCoverage`, `statewideRaces`) flows through unmodified.
    - `GET /api/admin/coverage/map?metric=elections&level=county&state=mi` returns 200 and the mocked `getElectionsCountyScores` payload.
    - ELEC-03 consistency assertion: given a mocked state payload where a state's `countyCoverage.races_total` = N, and a mocked county payload for that same state whose per-county `races` sum to N, the test asserts the two match — encoding "the state's county-pinnable denominator equals the county drill-down's total" as an automated contract. Use fixtures whose numbers are internally consistent by construction (both derived from the same fixture race set).
    - `GET /api/admin/coverage/map?metric=elections&level=county` with no `state` param returns 400 (existing route guard, unchanged).
  </behavior>
  <action>Create `backend/src/routes/admin.test.ts` by copying the `backend/src/routes/people.test.ts` template (PATTERNS.md "Route-level test scaffold"): `vi.hoisted` mock factories, `vi.mock`, bare `express()` app, `supertest`. Adaptation notes: (1) `admin.ts` applies `router.use(requireAuth as any, requireAdmin as any)` at module scope — mock BOTH `../middleware/auth.js` (or wherever `requireAuth` lives) and the `requireAdmin` source as pass-through `(_req,_res,next)=>next()`; read the actual import lines in `admin.ts` to get the exact module specifiers before mocking (do not guess paths). (2) `vi.mock('../lib/electionsMapService.js', () => ({ getElectionsStateScores: mockStateFn, getElectionsCountyScores: mockCountyFn }))`. (3) Mount `app.use('/api/admin', adminRouter)` and hit the routes with `supertest`. Scope the file narrowly to the elections `/coverage/map` branches only — do NOT attempt to exercise the rest of `admin.ts` (it has many routes with heavy service deps; over-mocking risks a brittle file). Read the exact final `StateElection` shape from the 168-01-SUMMARY.md before writing fixtures so the mock payload matches the real contract. If `admin.ts` has module-load-time side effects that crash under mock (e.g. a top-level DB call), mock `../lib/db.js` as `{ pool: { query: vi.fn() } }` per the `essentialsService.test.ts` convention.</action>
  <verify>
    <automated>cd backend && npx vitest run src/routes/admin.test.ts</automated>
  </verify>
  <done>`backend/src/routes/admin.test.ts` exists and passes: state branch returns the partitioned payload unmodified (200), county branch returns 200, the ELEC-03 consistency assertion (state `countyCoverage.races_total` matches the county drill-down total) passes, and the missing-`state` 400 guard is covered. Auth middleware is mocked pass-through so no real auth is exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked router | Test-only; auth middleware is mocked pass-through so the test does not exercise or weaken the real auth boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-03 | Elevation of Privilege | Mocked `requireAuth`/`requireAdmin` in test | accept | Auth bypass exists only inside the test file (`vi.mock`), never in production code. The real router-level guard at admin.ts:81 is unchanged. Test-only pass-through is the established convention (people.test.ts mocks `optionalAuth`). |
| T-168-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — `vitest`, `supertest`, `express` are already devDependencies (confirmed in RESEARCH.md Standard Stack). If an executor adds a package, run the legitimacy gate first. |
</threat_model>

<verification>
- `cd backend && npx vitest run src/routes/admin.test.ts` — new route test green.
- `cd backend && npm test` — full backend suite green (new test file integrates without breaking others).
</verification>

<success_criteria>
- A route-level test exists and automatically verifies the elections `/coverage/map` branches forward Plan 01's partitioned payload unmodified (ELEC-03 wiring).
- The state/county denominator-consistency contract is encoded as an automated assertion, closing the RESEARCH.md A2 gap.
- No production code changed; auth boundary untouched.
</success_criteria>

<output>
Create `.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-02-SUMMARY.md` when done.
</output>
