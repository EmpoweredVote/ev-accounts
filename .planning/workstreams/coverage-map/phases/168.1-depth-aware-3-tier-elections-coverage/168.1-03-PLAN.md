---
phase: 168.1-depth-aware-3-tier-elections-coverage
plan: 03
type: execute
wave: 3
depends_on: ["168.1-02"]
files_modified:
  - admin/src/pages/admin/coverageTypes.ts
  - admin/src/pages/admin/StatewideRacesPanel.tsx
  - admin/src/pages/admin/CoverageMap.tsx
autonomous: false
requirements: [ELEC-05, ELEC-06]
must_haves:
  truths:
    - "The frontend StateElection/ElectionRace types mirror the extended backend payload (depthScore, tierCounts, per-race tier)"
    - "StatewideRacesPanel shows a per-tier covered/total readout in the header and a per-race tier on each row"
    - "The state choropleth fill is driven by the weighted depth score, not the breadth coverage number"
    - "A names-only state renders visibly less-covered than fully-covered and its hover shows the depth score"
    - "Zero-race states still render N/A (NO_RACE_DATA), never a fake 0 or 100"
  artifacts:
    - path: "admin/src/pages/admin/coverageTypes.ts"
      provides: "depthScore + tierCounts on StateElection; tier on ElectionRace"
      contains: "tierCounts"
    - path: "admin/src/pages/admin/StatewideRacesPanel.tsx"
      provides: "Per-tier header readout + per-race tier badge"
      contains: "Tier"
    - path: "admin/src/pages/admin/CoverageMap.tsx"
      provides: "electionStateColor repointed to depthScore; hover shows depth score"
      contains: "depthScore"
  key_links:
    - from: "admin/src/pages/admin/CoverageMap.tsx"
      to: "StateElection.depthScore"
      via: "electionStateColor(s) => scoreColor(s.depthScore)"
      pattern: "depthScore"
    - from: "admin/src/pages/admin/StatewideRacesPanel.tsx"
      to: "StateElection.tierCounts / ElectionRace.tier"
      via: "render per-tier totals + per-race tier"
      pattern: "tierCounts|\\.tier"
---

<objective>
Repoint the admin elections UI to the tiered depth metric: mirror the extended payload in `coverageTypes.ts`, render a per-tier covered/total readout + per-race tier in `StatewideRacesPanel.tsx` (ELEC-05), and drive the state choropleth fill + hover readout from the weighted depth score in `CoverageMap.tsx` (ELEC-06). A human verifies the visual result.

Purpose: This closes success criteria 2 and 3 — admin sees depth-of-data by tier per race, and a names-only state no longer colors as fully covered. The frontend consumes the JSON as-is (no transform layer), so the types must stay in lockstep with the backend.

Output: extended frontend types, tiered panel readout, depth-score-driven fill/hover, and a human-verify checkpoint confirming the map + panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/coverage-map/ROADMAP.md
@.planning/workstreams/coverage-map/REQUIREMENTS.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror the tiered payload in coverageTypes.ts</name>
  <files>admin/src/pages/admin/coverageTypes.ts</files>
  <read_first>
    - admin/src/pages/admin/coverageTypes.ts — `ElectionRace` and `StateElection` interfaces (lines 31-37); `CountyElection` (line 306 region — stays untouched).
    - backend/src/lib/electionsMapService.ts — the extended `StateElection` interface from Plan 02 (depthScore, tierCounts, per-race tier) to mirror exactly.
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — coverageTypes.ts section (lines 295-308): "must stay structurally in lockstep with their backend counterparts; frontend consumes JSON as-is (no transform layer)."
  </read_first>
  <action>
    Add the same additive fields the backend `StateElection` gained in Plan 02: a `depthScore: number` and `tierCounts: { t0: number; t1: number; t2: number; t3: number }` on the frontend `StateElection`, and a per-race `tier: 0 | 1 | 2 | 3` on `ElectionRace`. Field names and shapes MUST match the backend exactly (JSON consumed as-is). Leave `coverage`/`races_total`/`races_covered`/`countyCoverage`/`statewideRaces` and the entire `CountyElection` interface untouched (county side out of scope, D-02).
  </action>
  <verify>
    <automated>cd admin && npx tsc --noEmit 2>&1 | grep -i coverageTypes | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - `coverageTypes.ts` `StateElection` contains `depthScore` and `tierCounts` with `t0/t1/t2/t3`.
    - `ElectionRace` contains a `tier` field typed `0 | 1 | 2 | 3`.
    - `CountyElection` is unchanged.
    - Field names match the backend `StateElection`/`RaceRow` names from Plan 02 (spot-check: `grep depthScore` matches both files).
  </acceptance_criteria>
  <done>Frontend types mirror the backend tiered payload additively; county types untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Tiered readout in StatewideRacesPanel + depth-score fill/hover in CoverageMap</name>
  <files>admin/src/pages/admin/StatewideRacesPanel.tsx, admin/src/pages/admin/CoverageMap.tsx</files>
  <read_first>
    - admin/src/pages/admin/StatewideRacesPanel.tsx — panel header (lines 33-45), per-race table row (lines 62-69), and the loading/`!stateElection` early-return block (lines 19-27, MUST stay unchanged).
    - admin/src/pages/admin/CoverageMap.tsx — `electionStateColor` (lines 37-41), the `scoreColor` ramp (lines 28-36, UNCHANGED), and the elections state hover readout (lines 146-165). The county branch is out of scope.
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — StatewideRacesPanel section (lines 312-355) and CoverageMap section (lines 359-413), including the `races_total === 0` → NO_RACE_DATA and `<= 0 ? 0.01 : ...` idioms that MUST be preserved (D-05 N/A semantics).
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md — D-04 (fill = depthScore), D-05 (N/A preserved), and the Claude's-Discretion note on panel readout shape (per-tier totals like "Tier 3: 2/13 · Tier 2: 4/13 · Tier 1: 13/13").
    - Skill: admin/.claude or repo skill `ui-ux-pro-max` SKILL.md if present — follow its styling conventions for the badge/readout.
  </read_first>
  <action>
    In StatewideRacesPanel.tsx: add per-tier totals to the header-right area using `stateElection.tierCounts` (format e.g. "Tier 3: {t3}/{total} · Tier 2: {t2}/{total} · Tier 1: {t1}/{total}", where total = races_total), styled consistent with the existing `text-xs text-gray-400` header-right pattern; add a per-race tier indicator on each row using `r.tier` (reuse `tabular-nums text-gray-600` styling; render Tier 0 distinctly from Tier 1 per the T0-vs-T1 note). Do NOT touch the loading/`!stateElection` early-return block. In CoverageMap.tsx: repoint `electionStateColor` to use `s.depthScore` in place of `s.coverage`, keeping the `s.races_total === 0` → `NO_RACE_DATA` guard and the `<= 0 ? 0.01 : ...` distinct-from-true-zero idiom exactly; in the state hover readout replace `{es.coverage}%` with the depth score (optionally append per-tier tallies — Discretion). Leave `scoreColor`, the `county:` sub-readout, `NO_RACE_DATA`, and the county branch untouched. No legend restyle beyond what the new fill requires (deferred to a UI phase).
  </action>
  <verify>
    <automated>cd admin && npx tsc --noEmit 2>&1 | grep -Ei 'StatewideRacesPanel|CoverageMap' | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - StatewideRacesPanel.tsx references `tierCounts` and renders per-race `.tier`; `grep -c "Tier" admin/src/pages/admin/StatewideRacesPanel.tsx` >= 1.
    - The loading/`!stateElection` early-return block (spinner + "Loading statewide races…") is byte-unchanged.
    - CoverageMap.tsx `electionStateColor` uses `depthScore` (`grep -c "s.coverage" ...electionStateColor region` no longer feeds the fill; `grep depthScore` matches).
    - The `races_total === 0` → `NO_RACE_DATA` guard and `<= 0 ? 0.01 :` idiom remain present in `electionStateColor`.
    - `scoreColor` ramp constants (`RAMP_FROM`/`RAMP_TO`) unchanged; county hover branch unchanged.
    - `cd admin && npx tsc --noEmit` reports no new errors in these two files.
  </acceptance_criteria>
  <done>Panel shows per-tier covered/total + per-race tier; map fill + hover are driven by the weighted depth score; N/A and loading affordances preserved; county untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Backend now computes a 3-tier depth metric per statewide/legislative race (Plans 01-02) and the admin UI renders it: StatewideRacesPanel shows per-tier covered/total + a per-race tier, and the state choropleth fill + hover use the weighted depth score instead of the breadth "≥1 candidate" number.
  </what-built>
  <how-to-verify>
    1. Run the admin app locally (existing dev command for the admin/ workspace) and open the Coverage page in elections mode (`metric=elections`, state level).
    2. Confirm a state known to have candidate NAMES but no stances/motivations (e.g. a bulk-loaded state) now colors as PARTIAL (~⅓ of the ramp), NOT fully covered.
    3. Hover that state — the readout shows the depth score (not 100%), and the `county:` sub-readout still shows its own number or "N/A — no county-level races".
    4. Click the state — StatewideRacesPanel opens with per-tier totals in the header (e.g. "Tier 3: x/n · Tier 2: y/n · Tier 1: n/n") and each race row shows its own tier; Tier 0 (no active candidates) reads distinctly from Tier 1.
    5. Confirm a state with zero statewide/legislative races still renders N/A (NO_RACE_DATA), not a fake 0% or 100%.
    6. Confirm the loading spinner still appears before data arrives.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (fill color, tier tallies, N/A, loading).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API → admin client | The extended StateElection JSON is rendered as-is; admin-authenticated, read-only display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168.1-05 | Information disclosure | StatewideRacesPanel / CoverageMap render | accept | Renders only the tier tallies + depth score already returned by the admin-authenticated route; no new fields exposed beyond the admin-internal metric; no PII |
| T-168.1-06 | Tampering | electionStateColor N/A regression | mitigate | Human-verify checkpoint explicitly checks that zero-race states still render N/A (never fake 0/100) and the loading affordance survives |
| T-168.1-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; extends existing admin components |
</threat_model>

<verification>
- `cd admin && npx tsc --noEmit` clean for the three files.
- Human-verify checkpoint approved: names-only state colors partial, hover shows depth score, panel shows per-tier totals + per-race tier, N/A + loading preserved.
</verification>

<success_criteria>
The admin elections UI reflects the tiered depth metric end-to-end: per-tier covered/total + per-race tier in the panel (ELEC-05), and the choropleth fill + hover driven by the weighted depth score so a names-only state no longer reads as fully covered (ELEC-06), with N/A and loading affordances intact and the county side untouched.
</success_criteria>

<output>
Create `.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-03-SUMMARY.md` when done.
</output>
