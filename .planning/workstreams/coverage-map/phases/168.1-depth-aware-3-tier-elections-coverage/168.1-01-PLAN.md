---
phase: 168.1-depth-aware-3-tier-elections-coverage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/electionsMap.ts
  - backend/src/lib/electionsMap.test.ts
autonomous: true
requirements: [ELEC-04]
must_haves:
  truths:
    - "classifyRaceTier returns the tier of a race's weakest active candidate (min/ALL rule)"
    - "A race with any name-only or null-politician active candidate caps at Tier 1"
    - "weightedDepthScore maps T0=0, T1=1/3, T2=2/3, T3=1, averaged and scaled to 0..100"
    - "Both helpers are pure (no DB import) and unit-tested at every tier boundary"
  artifacts:
    - path: "backend/src/lib/electionsMap.ts"
      provides: "classifyRaceTier + weightedDepthScore pure helpers; RaceRow tier-signal fields"
      contains: "export function classifyRaceTier"
    - path: "backend/src/lib/electionsMap.test.ts"
      provides: "Tier-boundary + weighted-score unit tests"
      contains: "describe('classifyRaceTier'"
  key_links:
    - from: "backend/src/lib/electionsMap.test.ts"
      to: "backend/src/lib/electionsMap.ts"
      via: "import classifyRaceTier, weightedDepthScore"
      pattern: "classifyRaceTier|weightedDepthScore"
---

<objective>
Define the 3-tier depth classifier and the weighted-depth-score mapping as PURE, unit-tested helpers in `electionsMap.ts` — the single source of truth for tier thresholds that both the backend computation (Plan 02) and the UI (Plan 03) reuse (ELEC-04, success criterion 1: "thresholds defined once and reused by backend computation and UI").

Purpose: The min/ALL tier rule (D-01) and the T0=0/T1=⅓/T2=⅔/T3=1 weighting (D-04) are the anti-inflation heart of the phase. Encoding them once as pure functions guarantees the panel, the fill, and the tally can never diverge from the computation.

Output: `classifyRaceTier` + `weightedDepthScore` exports, extended `RaceRow`, and parallel `describe` blocks in `electionsMap.test.ts` covering every tier boundary and the weighted-score mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/coverage-map/STATE.md
@.planning/workstreams/coverage-map/ROADMAP.md
@.planning/workstreams/coverage-map/REQUIREMENTS.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — tier-classifier and weighted-score unit tests</name>
  <files>backend/src/lib/electionsMap.test.ts, backend/src/lib/electionsMap.ts</files>
  <read_first>
    - backend/src/lib/electionsMap.test.ts — reuse the `race()` factory (lines 4-6) and the `describe('raceCoverage', ...)` block shape (lines 8-17); mirror the one-`it`-per-boundary style of the `resolveRaceCountyFips` describe block (lines 19-39). Extend the import line (line 2).
    - backend/src/lib/electionsMap.ts — the existing `RaceRow` interface (lines 6-12) and `raceCoverage` empty-guard idiom (`if (races.length === 0) return 0`); the top-of-file "no DB" JSDoc convention.
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md — D-01 (min/ALL weakest-candidate rule), D-03 (null politician_id caps at Tier 1), D-04 (weighted mapping), D-05 (N/A handled by caller, NOT this helper).
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — the electionsMap.ts pattern section (lines 99-151) spelling out `classifyRaceTier({active, stanced, motivated})` and `weightedDepthScore(tiers)` signatures.
  </read_first>
  <behavior>
    classifyRaceTier({ active, stanced, motivated }) returns 0|1|2|3:
    - active=0 → 0 (Tier 0, no active candidates)
    - active>0, stanced<active, motivated<active → 1 (name-only / null-politician / withdrawn-only capping)
    - active>0, stanced===active, motivated<active → 2 (stance-only)
    - active>0, motivated===active, stanced<active → 2 (motivation-only)
    - active>0, stanced===active, motivated===active → 3 (all three)
    - a partial-both case (e.g. active=3, stanced=2, motivated=2) → 1 (neither signal reaches ALL, so weakest candidate has neither)
    weightedDepthScore(tiers: number[]) returns 0..100 (one decimal, matching raceCoverage precision):
    - [0] → 0; [1] → 33.3; [2] → 66.7; [3] → 100
    - [3,3] → 100; [1,3] → 66.7 (avg of ⅓ and 1 = ⅔ → 66.7); [0,1,2,3] → 50
    - [] → 0 (empty guard; N/A is the caller's concern per D-05)
  </behavior>
  <action>
    Add the new export names `classifyRaceTier` and `weightedDepthScore` to the existing import on line 2 of electionsMap.test.ts. Add two new `describe` blocks: `describe('classifyRaceTier', ...)` with one `it` per tier boundary listed in <behavior> (T0, T1 name-only, T1 partial-both, T2 stance-only, T2 motivation-only, T3 all-three), and `describe('weightedDepthScore', ...)` with the fractional-mapping cases and the empty-array guard. Assert exact numeric returns per <behavior>. Do NOT parametrize — enumerate boundaries explicitly like the resolveRaceCountyFips block. Run the test to confirm it FAILS (RED) because the helpers do not yet exist. Do not add scope-reduced placeholders — this is the real tier contract.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/electionsMap.test.ts 2>&1 | grep -Eiq 'classifyRaceTier|weightedDepthScore.*(fail|is not|not a function)' && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - electionsMap.test.ts import line contains `classifyRaceTier` and `weightedDepthScore`.
    - Test file contains `describe('classifyRaceTier'` and `describe('weightedDepthScore'`.
    - `npx vitest run src/lib/electionsMap.test.ts` exits non-zero (RED) with the new tests failing because the helpers are undefined.
  </acceptance_criteria>
  <done>Two new describe blocks exist with per-boundary assertions; the suite fails only because classifyRaceTier/weightedDepthScore are not yet implemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement classifyRaceTier + weightedDepthScore, extend RaceRow</name>
  <files>backend/src/lib/electionsMap.ts</files>
  <read_first>
    - backend/src/lib/electionsMap.ts — `RaceRow` (lines 6-12), `raceCoverage` formula + one-line JSDoc + `Math.round(x * 1000) / 10` rounding idiom (lines 30-35), top-of-file "no DB" JSDoc (lines 1-4).
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — electionsMap.ts section (lines 143-151): exact D-01 rule (T3 iff active>0 AND stanced===active AND motivated===active; T2 iff active>0 AND (stanced===active OR motivated===active); T1 iff active>0; else T0) and the D-04 fraction mapping.
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md — D-05 confirms N/A (zero-race) is the CALLER's job, not this helper's; the helper returns 0 for an empty array.
  </read_first>
  <action>
    In electionsMap.ts, extend `RaceRow` additively with the three per-race tier-signal fields (`active_count`, `stanced_count`, `motivated_count`) as numbers — leave existing `candidate_count` untouched (the county/breadth path still reads it, D-02). Add `export function classifyRaceTier(r: { active: number; stanced: number; motivated: number }): 0 | 1 | 2 | 3` implementing the D-01 min/ALL rule exactly as in <read_first>. Add `export function weightedDepthScore(tiers: number[]): number` mapping T0→0, T1→1/3, T2→2/3, T3→1, averaging, scaling ×100, and rounding with the existing `Math.round(x * 1000) / 10` one-decimal idiom; return 0 for an empty array (mirror raceCoverage's guard). Add a one-line JSDoc above each export stating the exact formula, matching the file's convention. Do NOT touch `raceCoverage`, `classifyRaces`, `resolveRaceCountyFips`, or add any DB import. Run the test to confirm GREEN.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/lib/electionsMap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/electionsMap.test.ts` exits 0 (all tier + weighted-score tests pass).
    - electionsMap.ts contains `export function classifyRaceTier` and `export function weightedDepthScore`.
    - `classifyRaceTier({active:3,stanced:3,motivated:3})===3`; `classifyRaceTier({active:3,stanced:3,motivated:2})===2`; `classifyRaceTier({active:2,stanced:1,motivated:1})===1`; `classifyRaceTier({active:0,stanced:0,motivated:0})===0`.
    - `weightedDepthScore([1])===33.3`, `weightedDepthScore([2])===66.7`, `weightedDepthScore([3])===100`, `weightedDepthScore([])===0`.
    - `RaceRow` contains `active_count`, `stanced_count`, `motivated_count`; `candidate_count` still present.
    - `grep -v '^ *[/*]' backend/src/lib/electionsMap.ts | grep -Eic 'import.*(pg|pool|supabase|db)'` returns 0 (helper stays pure, no DB import).
  </acceptance_criteria>
  <done>Both helpers implemented and green; RaceRow additively extended; file remains pure with no DB import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure computation over already-fetched in-memory data; no I/O, no user input, no new attack surface this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168.1-01 | Tampering | classifyRaceTier / weightedDepthScore logic drift | mitigate | Pure functions with exhaustive boundary unit tests (T0/T1/T2/T3 + weighted mapping) prevent silent scoring corruption; thresholds defined once here per ELEC-04 |
| T-168.1-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed this plan (uses existing vitest); no package-manager surface |
</threat_model>

<verification>
- `cd backend && npx vitest run src/lib/electionsMap.test.ts` exits 0.
- The tier thresholds and weighted mapping exist in exactly one place (electionsMap.ts) for downstream reuse.
</verification>

<success_criteria>
Tier classifier + weighted-score helpers are pure, exported, and exhaustively unit-tested at every tier boundary; RaceRow carries the three per-race signal counts additively. This satisfies the "defined once" half of ELEC-04 success criterion 1.
</success_criteria>

<output>
Create `.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-01-SUMMARY.md` when done.
</output>
