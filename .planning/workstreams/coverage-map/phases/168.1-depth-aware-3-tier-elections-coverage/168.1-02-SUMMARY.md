---
phase: 168.1-depth-aware-3-tier-elections-coverage
plan: 02
subsystem: elections
tags: [postgres, sql, express, vitest, elections-coverage]

# Dependency graph
requires:
  - phase: 168.1-01
    provides: classifyRaceTier/weightedDepthScore pure helpers, extended RaceRow type
provides:
  - racesForStateDate SQL now computes real per-race active/stanced/motivated aggregates in one round trip
  - getElectionsStateScores emits depthScore + tierCounts + per-race tier on StateElection, served through admin.ts unmodified
  - electionsMapService.test.ts — the only automated coverage of the SQL-row → tier translation
affects: [168.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stance/motivation FILTER-join idiom (inform.politician_answers / transparent_motivations.contributions) reused verbatim across coverageMapService and electionsMapService — one signal definition in the codebase"

key-files:
  created:
    - backend/src/lib/electionsMapService.test.ts
  modified:
    - backend/src/lib/electionsMapService.ts

key-decisions:
  - "Extended racesForStateDate's single parameterized query rather than adding a second query — mirrors coverageMapService.statsByJurisdiction's two LEFT JOIN (SELECT DISTINCT ...) FILTER idiom exactly, at per-race (not per-jurisdiction) grain"
  - "active_count keeps null-politician_id rows (no politician_id IS NOT NULL filter on the base count) per D-03 — only the stanced/motivated FILTER predicates implicitly exclude them via the LEFT JOIN yielding NULL"
  - "StateElection extended additively (depthScore, tierCounts, per-race tier) — coverage/races_covered/countyCoverage untouched; cache key stays 'elections:us'"
  - "Zero-statewide-race states get depthScore=0 via weightedDepthScore's empty-array guard, but D-05 N/A semantics are preserved because the frontend already gates on races_total === 0 (unchanged from Phase 168), not on depthScore itself"

requirements-completed: [ELEC-04, ELEC-06]

# Metrics
duration: 12min
completed: 2026-07-05
---

# Phase 168.1 Plan 02: Backend Tier Computation Summary

**racesForStateDate now aggregates real active/stanced/motivated candidate counts per race via SQL FILTER joins, and getElectionsStateScores wires those counts through classifyRaceTier/weightedDepthScore into a new depthScore + tierCounts + per-race tier on StateElection — replacing the Plan-01 zero-default placeholder**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-05T01:09:00Z
- **Completed:** 2026-07-05T01:21:26Z
- **Tasks:** 3
- **Files modified:** 2 (1 new)

## Accomplishments
- `racesForStateDate` extends its existing parameterized SQL with two `LEFT JOIN (SELECT DISTINCT ...) FILTER` subqueries mirrored verbatim from `coverageMapService.statsByJurisdiction` (stances via `inform.politician_answers`, motivations via `transparent_motivations.politician_sources`/`.contributions`), plus the withdrawn-exclusion predicate from `readrankService.ts`. Removes the `TODO(168.1-02)` zero-default placeholder Plan 01 left behind.
- `getElectionsStateScores` maps each statewide race through `classifyRaceTier`, accumulates `tierCounts`, and computes `depthScore` via `weightedDepthScore` — all under the existing `'elections:us'` cache key, with `getElectionsCountyScores` byte-unchanged.
- `StateElection` gains `depthScore` (0..100) and `tierCounts: {t0,t1,t2,t3}`; each `statewideRaces` entry now carries its per-race `tier`.
- New `electionsMapService.test.ts` behaviorally proves the SQL-row → tier pipeline: raw string-typed DB rows in, numeric aggregates + tier + tierCounts + depthScore out — covering D-02 (withdrawn exclusion), D-03 (null-politician_id caps at Tier 1), D-06 (stance-only = Tier 2), and a fully-covered Tier 3 case, plus the exact weighted-average depthScore (50) for a 1-race-per-tier mix.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend racesForStateDate SQL with per-race stance/motivation aggregates** - `22be6ea4` (feat)
2. **Task 2: Wire tier classifier + weighted score into getElectionsStateScores and extend StateElection** - `31c387fd` (feat)
3. **Task 3: Behavioral test — seed a representative candidate mix through the real SQL-row → tier pipeline** - `ff8fa978` (test)

## Files Created/Modified
- `backend/src/lib/electionsMapService.ts` - `racesForStateDate` SQL extended with active/stanced/motivated FILTER aggregates; `getElectionsStateScores` wires `classifyRaceTier`/`weightedDepthScore`; `StateElection` extended with `depthScore`/`tierCounts`/per-race `tier`
- `backend/src/lib/electionsMapService.test.ts` - new behavioral test: `pool.query`-mocked (vi.hoisted + vi.mock, mirroring `readrankService.test.ts`), asserts the raw-row → `RaceRow` translation and the full `getElectionsStateScores` tier/depthScore pipeline against a representative candidate mix

## Decisions Made
- Followed the plan and `168.1-PATTERNS.md` exactly: same query shape/grain change (jurisdiction → per-race), same join idiom, same additive `StateElection` extension, same single cache key. No deviation from the documented SQL or wiring approach.
- Test file did not previously exist despite the prior-wave note referencing "already committed" — the referenced commit (`8abf38bc`) only edited PLAN.md during planning (added Task 3's description), so Task 3 created the actual test file per the plan's spec.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

`cd backend && npm test` (full suite) has 10 pre-existing failing test files unrelated to this plan (architecture/coordinateLeakage checks on `admin.ts`/`auth.ts`/`campaignFinanceAdmin.ts`/`essentialsLegislativeService.ts`, compass/gems/treasury/env-validation integration tests, browseResolution, arcgis-sources-coverage, tribal-land, and two TIGER-boundary scripts that call `process.exit(1)`). None touch files this plan modified. Logged to `deferred-items.md` per the scope-boundary rule rather than fixed. All elections/admin-relevant suites pass: `electionsMap.test.ts` (28), `electionsMapService.test.ts` (2, new), `admin.test.ts` (4), `essentials-elections.test.ts` (9), `admin-compass.test.ts` (12).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 03 (UI) can now consume `depthScore`, `tierCounts`, and per-race `tier` directly from `GET /coverage/map?metric=elections&level=state` (no route change) to repoint `electionStateColor`, the panel header per-tier totals, and the hover readout.
- No blockers.

---
*Phase: 168.1-depth-aware-3-tier-elections-coverage*
*Completed: 2026-07-05*
