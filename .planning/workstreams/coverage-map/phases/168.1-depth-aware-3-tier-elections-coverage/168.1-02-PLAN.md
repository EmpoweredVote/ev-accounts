---
phase: 168.1-depth-aware-3-tier-elections-coverage
plan: 02
type: execute
wave: 2
depends_on: ["168.1-01"]
files_modified:
  - backend/src/lib/electionsMapService.ts
autonomous: true
requirements: [ELEC-04, ELEC-06]
must_haves:
  truths:
    - "racesForStateDate returns per-race active/stanced/motivated counts from live DB in one round trip"
    - "The active count excludes withdrawn candidates but includes null-politician_id candidates"
    - "Stance signal = >=1 inform.politician_answers row; motivation signal = >=1 transparent_motivations contribution"
    - "getElectionsStateScores emits a weighted depth score + per-tier tallies per state"
    - "A state with zero statewide/legislative races still reports N/A (never a fake 0 or 100)"
  artifacts:
    - path: "backend/src/lib/electionsMapService.ts"
      provides: "Extended SQL, tier wiring, weighted-score field + tierCounts on StateElection"
      contains: "classifyRaceTier"
  key_links:
    - from: "backend/src/lib/electionsMapService.ts"
      to: "backend/src/lib/electionsMap.ts"
      via: "import classifyRaceTier, weightedDepthScore"
      pattern: "classifyRaceTier|weightedDepthScore"
    - from: "racesForStateDate SQL"
      to: "inform.politician_answers + transparent_motivations.contributions"
      via: "LEFT JOIN ... FILTER"
      pattern: "politician_answers|transparent_motivations"
---

<objective>
Extend the backend elections service to compute the 3-tier depth metric from live data: enrich the `racesForStateDate` SQL with per-race active/stanced/motivated counts, wire the pure `classifyRaceTier`/`weightedDepthScore` helpers into `getElectionsStateScores`, and extend the `StateElection` contract with the weighted depth score + per-tier tallies (ELEC-04 computation half; ELEC-06 payload half).

Purpose: This is where the anti-inflation rule meets real data — the min/ALL SQL aggregates and the withdrawn-exclusion (D-02) / null-politician-cap (D-03) make a names-only state read ~33%, satisfying success criterion 3. Reusing the exact completeness-map signal SQL (D-06/07/08) keeps one definition of "has stances"/"has motivations" in the codebase.

Output: extended `racesForStateDate` query, tier + weighted-score wiring in `getElectionsStateScores`, and an additively-extended `StateElection` interface serving through `admin.ts` with no route change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/coverage-map/ROADMAP.md
@.planning/workstreams/coverage-map/REQUIREMENTS.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md
@.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md
@.planning/workstreams/coverage-map/phases/168-elections-accuracy-fix/168-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend racesForStateDate SQL with per-race stance/motivation aggregates</name>
  <files>backend/src/lib/electionsMapService.ts</files>
  <read_first>
    - backend/src/lib/electionsMapService.ts — `racesForStateDate` full function (lines 50-72): the existing `LEFT JOIN essentials.race_candidates`, `GROUP BY r.id...`, and the `rows.map` projection to extend.
    - backend/src/lib/coverageMapService.ts — `statsByJurisdiction` (lines 197-230): the AUTHORITATIVE stance/motivation FILTER-join idiom to mirror; and the signal-definition comment (lines 182-196) with the `last_stances_researched_at` landmine warning to copy verbatim above the new SQL.
    - backend/src/lib/readrankService.ts — lines 298-303: the exact withdrawn-exclusion predicate `COALESCE(rc.candidate_status, 'active') <> 'withdrawn'`. NOTE: do NOT copy this file's `rc.politician_id IS NOT NULL` filter onto the active-candidate base count (D-03).
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — the racesForStateDate section (lines 212-238) and the two Shared-Patterns blocks (lines 419-430) spelling the two joins + the withdrawn predicate.
    - docs/INTEGRATION-GUIDE-v2.md §"transparent_motivations" (~line 200) — confirm the schema is NOT PostgREST-exposed; access via `pool.query()` only (never `supabaseAdmin.schema(...)`).
  </read_first>
  <action>
    In `racesForStateDate`, extend the existing `pool.query` (keep it parameterized with `$1`/`$2` — no string interpolation of state code or date). Add `LEFT JOIN essentials.politicians p ON p.id = rc.politician_id`, plus the two DISTINCT-subquery LEFT JOINs mirrored from coverageMapService.statsByJurisdiction: `ans` = `(SELECT DISTINCT politician_id FROM inform.politician_answers)` joined on `p.id`, and `don` = `(SELECT DISTINCT ps.essentials_politician_id AS politician_id FROM transparent_motivations.politician_sources ps JOIN transparent_motivations.contributions c ON c.politician_source_id = ps.id)` joined on `p.id`. Add three aggregate columns to the SELECT: `active_count` = `COUNT(rc.id) FILTER (WHERE COALESCE(rc.candidate_status,'active') <> 'withdrawn')`, `stanced_count` = `COUNT(rc.id) FILTER (WHERE COALESCE(rc.candidate_status,'active') <> 'withdrawn' AND ans.politician_id IS NOT NULL)`, `motivated_count` = `COUNT(rc.id) FILTER (WHERE COALESCE(rc.candidate_status,'active') <> 'withdrawn' AND don.politician_id IS NOT NULL)`. Keep the existing raw `candidate_count` column unchanged (county/breadth path per D-02). Extend the row type and `rows.map` projection to populate `active_count`/`stanced_count`/`motivated_count` (via `Number(...)`) onto the extended `RaceRow`. Copy the signal-definition landmine comment verbatim above the query. Do NOT add a new query or a new cache key.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i electionsMapService | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inform.politician_answers" backend/src/lib/electionsMapService.ts` >= 1 and `grep -c "transparent_motivations" backend/src/lib/electionsMapService.ts` >= 1.
    - The `racesForStateDate` SQL contains `COALESCE(rc.candidate_status, 'active') <> 'withdrawn'` on the active count.
    - The SQL contains `active_count`, `stanced_count`, `motivated_count` in the SELECT.
    - `grep -c "supabaseAdmin.schema" backend/src/lib/electionsMapService.ts` returns 0 (uses pool.query only).
    - The query remains parameterized: `grep -Eq '\\$1|\\$2' backend/src/lib/electionsMapService.ts` (no interpolated `${stateAbbr}`/`${date}`).
    - `npx tsc --noEmit` reports no new errors in electionsMapService.ts.
  </acceptance_criteria>
  <done>racesForStateDate returns per-race active/stanced/motivated counts in a single parameterized round trip, mirroring the completeness-map signal joins, with the raw candidate_count preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tier classifier + weighted score into getElectionsStateScores and extend StateElection</name>
  <files>backend/src/lib/electionsMapService.ts</files>
  <read_first>
    - backend/src/lib/electionsMapService.ts — `StateElection` interface (lines 13-26) to extend; `getElectionsStateScores` per-state loop + `'elections:us'` cached() call (lines 126-155); the `countyCoverage` `status: 'unknown' | 'scored'` N/A construction (lines 147-149).
    - backend/src/lib/electionsMap.ts — the `classifyRaceTier`/`weightedDepthScore` exports and extended `RaceRow` from Plan 01.
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-CONTEXT.md — D-04 (weighted fill number), D-05 (zero-race → N/A via unknown/scored, never fake 0/100).
    - .planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-PATTERNS.md — getElectionsStateScores section (lines 240-273): map each statewide RaceRow through classifyRaceTier, then weightedDepthScore over the tiers; keep cache key `'elections:us'`.
  </read_first>
  <action>
    Import `classifyRaceTier` and `weightedDepthScore` from `./electionsMap.js`. Extend the `StateElection` interface additively: add a weighted depth score field (name it `depthScore`) and a per-tier tally object (`tierCounts: { t0: number; t1: number; t2: number; t3: number }`) applying to the statewide/legislative set only; keep `coverage`/`races_total`/`races_covered`/`countyCoverage`/`statewideRaces` untouched. In the `getElectionsStateScores` loop, after `classifyRaces` yields `statewide`, map each race through `classifyRaceTier({ active: r.active_count, stanced: r.stanced_count, motivated: r.motivated_count })` to get a per-race tier, accumulate the tally into `tierCounts`, and compute `depthScore = weightedDepthScore(tiers)`. Attach the per-race tier to each `statewideRaces` entry (add a `tier` field on the row so the panel can render it). Push `depthScore` and `tierCounts` onto the pushed `StateElection`. Preserve the D-05 N/A path: when `statewide.length === 0` the state should carry a depthScore/tierCounts that the frontend treats as N/A via the existing `races_total === 0` guard — do NOT invent a fake 0 or 100; keep `races_total` as the denominator signal exactly as Phase 168 does. Keep the cache key `'elections:us'` — no new cache key. Do NOT touch `getElectionsCountyScores`/county-pinnable handling.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i electionsMapService | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - electionsMapService.ts imports `classifyRaceTier` and `weightedDepthScore`.
    - `StateElection` contains a `depthScore` field and a `tierCounts` field with `t0/t1/t2/t3`; each `statewideRaces` entry carries a per-race `tier`.
    - `getElectionsStateScores` still calls `cached('elections:us', ...)` — `grep -c "elections:us" backend/src/lib/electionsMapService.ts` unchanged at 1 (no new cache key).
    - `getElectionsCountyScores` and `countyCoverage` construction are byte-unchanged for county logic (county stays on breadth, D-02/scope).
    - `npx tsc --noEmit` reports no new errors in electionsMapService.ts.
  </acceptance_criteria>
  <done>getElectionsStateScores emits a weighted depth score + per-tier tallies + per-race tier per state, computed via the pure Plan-01 helpers, under the existing single cache key, serving through admin.ts unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → PostgreSQL | New aggregate SQL crosses into the DB; state code + date are the only external-ish inputs (already validated upstream in the admin route) |
| admin route → client | `GET /coverage/map?metric=elections&level=state` returns the extended StateElection JSON (admin-authenticated, read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168.1-02 | Injection (Tampering) | racesForStateDate SQL | mitigate | State code + date bound via `$1`/`$2` parameters (pool.query), never string-interpolated — mirrors existing coverageMapService/readrankService parameterization |
| T-168.1-03 | Information disclosure | transparent_motivations schema access | mitigate | Access via `pool.query()` only; the schema is not PostgREST-exposed (never `supabaseAdmin.schema(...)`), per D-07 and INTEGRATION-GUIDE landmine |
| T-168.1-04 | Elevation of privilege | GET /coverage/map endpoint | accept | Admin authz already enforced upstream on the existing route (no route change this phase); existing mitigation, not new work |
| T-168.1-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; extends existing service against existing pool |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit` clean for electionsMapService.ts.
- SQL parameterized (`$1`/`$2`), no `supabaseAdmin.schema`, single cache key.
- A names-only state's `depthScore` computes to ~33 (T1-dominant) rather than ~100 (feeds success criterion 3, verified end-to-end in Plan 03).
</verification>

<success_criteria>
Backend computes the tiered metric from live data using the Plan-01 helpers: per-race tier + per-state weighted depth score + per-tier tallies, withdrawn excluded, null-politician candidates capping at Tier 1, N/A preserved for zero-race states — served through the unmodified admin route (ELEC-04 computation, ELEC-06 payload).
</success_criteria>

<output>
Create `.planning/workstreams/coverage-map/phases/168.1-depth-aware-3-tier-elections-coverage/168.1-02-SUMMARY.md` when done.
</output>
