---
phase: quick
plan: 260405-ez5
type: execute
wave: 1
depends_on: []
files_modified:
  - ev-ui/src/PoliticianCard.jsx
  - essentials/src/pages/Results.jsx
autonomous: true
must_haves:
  truths:
    - "All politician cards in a grid row have equal height"
    - "Cards with fewer icons/content stretch to match tallest card in row"
    - "Card content (image, name, title, footer) remains properly laid out at full height"
  artifacts:
    - path: "ev-ui/src/PoliticianCard.jsx"
      provides: "Card component with height: 100% to fill grid cells"
    - path: "essentials/src/pages/Results.jsx"
      provides: "Wrapper divs with height: 100% for grid cell stretching"
  key_links:
    - from: "CategorySection grid"
      to: "PoliticianCard"
      via: "CSS grid align-items: stretch (default) + height: 100% chain"
---

<objective>
Fix politician card height inconsistency in the essentials Results page. Cards in the same grid row should all match the height of the tallest card. Currently, cards with more content (e.g., icon overlays with ballot/compass icons) are taller than cards without, creating an uneven grid.

Purpose: Visual polish — uniform card heights create a cleaner, more professional grid layout.
Output: All cards in a row render at the same height.
</objective>

<context>
@essentials/src/pages/Results.jsx
@ev-ui/src/PoliticianCard.jsx
@ev-ui/src/CategorySection.jsx

<interfaces>
<!-- CategorySection grid layout (ev-ui/src/CategorySection.jsx line 87-89) -->
grid: {
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fill, minmax(min(250px, 100%), 1fr))',
  gap: spacing[4],
}

<!-- PoliticianCard card style (ev-ui/src/PoliticianCard.jsx line 48-61) -->
card: {
  display: 'flex',
  flexDirection: isHorizontal ? 'row' : 'column',
  alignItems: 'stretch',
  minHeight: isHorizontal ? '96px' : undefined,
  // NOTE: missing height: '100%' — this is the root cause
}

<!-- Results.jsx wrapper divs around each card (lines 674-688, 697-733) -->
<!-- Vacant: <div key={pol.id} style={{ opacity: 0.55 }}> — no height -->
<!-- Normal: <div key={pol.id} data-pol-id={pol.id}> — no height -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add height: 100% to PoliticianCard and wrapper divs</name>
  <files>ev-ui/src/PoliticianCard.jsx, essentials/src/pages/Results.jsx</files>
  <action>
The CSS grid in CategorySection uses `align-items: stretch` (the default), which makes grid cells equal height per row. But the card and its wrapper divs don't fill that height. Fix the chain:

1. **ev-ui/src/PoliticianCard.jsx** — In the `styles.card` object (around line 48), add `height: '100%'` so the card fills its container. This works for both horizontal and vertical variants.

2. **essentials/src/pages/Results.jsx** — The wrapper `<div>` elements around each `<PoliticianCard>` in `renderPoliticianCard()` need `style={{ height: '100%' }}` (or merge with existing style):
   - Line ~676 (vacant card wrapper): Change `<div key={...} style={{ opacity: 0.55 }}>` to `<div key={...} style={{ opacity: 0.55, height: '100%' }}>`
   - Line ~697 (normal card wrapper): Change `<div key={pol.id} data-pol-id={pol.id}>` to `<div key={pol.id} data-pol-id={pol.id} style={{ height: '100%' }}>`

This ensures the height chain is unbroken: grid cell (stretch) -> wrapper div (100%) -> PoliticianCard (100%).

Do NOT add a fixed min-height or hardcoded pixel value. The tallest card in each row naturally sets the row height via CSS grid, and all other cards stretch to match.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All politician cards in the same grid row render at equal height. Cards with fewer icons stretch to match the tallest card. No visual regressions — card content remains properly aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ev-ui and update essentials dependency</name>
  <files>ev-ui/dist/*</files>
  <action>
Since PoliticianCard lives in the ev-ui shared library, the change needs to be built and consumed:

1. `cd ev-ui && npm run build` — Rebuild the ev-ui package with the PoliticianCard height fix.
2. Bump the patch version in `ev-ui/package.json` (e.g., from current version to +0.0.1).
3. `cd essentials && npm update @chrisandrewsedu/ev-ui` — Pull the updated package.

Note: If essentials is using a local file reference or workspace link to ev-ui (check package.json), the build step alone may suffice without a version bump. Check `essentials/package.json` for the ev-ui dependency format first.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>essentials builds successfully with the updated ev-ui PoliticianCard that includes height: 100%.</done>
</task>

</tasks>

<verification>
- Run essentials dev server (`cd essentials && npm run dev`)
- Search for an address that returns multiple politicians
- Verify all cards in each CategorySection row are the same height
- Check that cards with icon overlays (ballot, compass, branch) and cards without both render at the row's tallest height
- Verify horizontal variant cards still look correct (image on left, content on right)
</verification>

<success_criteria>
All politician cards within the same grid row render at identical heights, matching the tallest card. No fixed pixel heights — pure CSS grid stretch behavior.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-ez5-fix-politician-card-height-inconsistency/260405-ez5-SUMMARY.md`
</output>
