---
phase: quick
plan: 260425-ri7
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/components/CalibrationOverlay.jsx
autonomous: true
requirements:
  - visual-consistency
must_haves:
  truths:
    - "Welcome step SVG shows user polygon in dusk purple (#7C6B9E) to match the live radar chart"
    - "Welcome step SVG shows comparison polygon in sage green (#5A9A6E) to match the live radar chart"
    - "Accent dots remain yellow (#fed12e) with white border, consistent with live chart dots"
  artifacts:
    - path: "CompassV2/src/components/CalibrationOverlay.jsx"
      provides: "Updated welcome step SVG illustration"
      contains: "#7C6B9E"
  key_links:
    - from: "CalibrationOverlay.jsx welcome step SVG"
      to: "RadarChartCore.jsx polygon colors"
      via: "matching hex values"
      pattern: "7C6B9E.*5A9A6E"
---

<objective>
Update the static SVG radar chart illustration on the onboarding welcome screen to use the current Dusk/Sage color scheme (#7C6B9E purple and #5A9A6E green) instead of the old coral/light-blue colors.

Purpose: Quick task 260418-t6w changed the live radar chart to a green-and-purple scheme. The welcome step illustration still renders the old coral (#ff5740) and light-blue (#59b0c4) polygons, creating a mismatch between what users see on the welcome screen and what their actual compass looks like.

Output: CalibrationOverlay.jsx with updated welcome SVG polygon colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update welcome step SVG polygon colors to Dusk/Sage scheme</name>
  <files>CompassV2/src/components/CalibrationOverlay.jsx</files>
  <action>
In the `step === "welcome"` render block (around line 642), locate the two `<polygon>` elements in the static SVG illustration and update their color values:

1. User compass polygon (currently coral):
   - `fill="#ff5740"` → `fill="#7C6B9E"`
   - `fillOpacity="0.25"` — keep as-is
   - `stroke="#ff5740"` → `stroke="#7C6B9E"`

2. Comparison polygon (currently light-blue):
   - `fill="#59b0c4"` → `fill="#5A9A6E"`
   - `fillOpacity="0.2"` — keep as-is
   - `stroke="#59b0c4"` → `stroke="#5A9A6E"`

3. Accent dots (the `<circle>` elements after the polygons, currently `fill="#fed12e" stroke="white"`):
   - Keep yellow fill and white stroke unchanged — these match the live chart dot style.

These are the ONLY color changes needed. Do not touch any other colors in the file (the `#59b0c4` references in the pick-topics step checkboxes, topic pill borders, and answer step "View Compass" button are UI chrome, not chart illustration colors, and are out of scope for this task).

Reference colors confirmed from ev-ui/src/RadarChartCore.jsx:
- Dusk/user: `#7C6B9E` (stroke on user polygon, lines ~259-269)
- Sage/comparison: `#5A9A6E` (stroke on comparison polygon, lines ~302-312)
  </action>
  <verify>
    <automated>grep -n "7C6B9E\|5A9A6E" /Users/chrisandrews/Documents/GitHub/CompassV2/src/components/CalibrationOverlay.jsx</automated>
  </verify>
  <done>
    - `grep` returns lines in the welcome SVG polygons with `#7C6B9E` (user) and `#5A9A6E` (comparison)
    - `grep -n "ff5740" CalibrationOverlay.jsx` returns 0 hits inside the SVG polygon attributes (the ComparePanel legend dots at lines ~183/190 are in a different file and unaffected)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static SVG | Inline SVG with hardcoded values — no user input, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ri7-01 | Tampering | CalibrationOverlay.jsx | accept | Purely cosmetic color change in static SVG — no logic, no data, no user input |
</threat_model>

<verification>
After edit:
1. `grep -n "7C6B9E\|5A9A6E" CompassV2/src/components/CalibrationOverlay.jsx` — should return 4 hits (fill + stroke for each polygon)
2. `grep -n "#ff5740" CompassV2/src/components/CalibrationOverlay.jsx` — should return 0 hits (the old coral is gone from the SVG)
3. Visual: run `cd CompassV2 && npm run dev`, navigate to compass.empowered.vote (or localhost), confirm welcome screen SVG shows purple + green polygons matching the live chart colors
</verification>

<success_criteria>
Welcome step SVG illustration uses #7C6B9E (dusk purple) for the user polygon and #5A9A6E (sage green) for the comparison polygon, matching the colors produced by RadarChartCore for live compass data.
</success_criteria>

<output>
After completion, create `.planning/quick/260425-ri7-update-the-first-page-of-the-onboarding-/260425-ri7-SUMMARY.md`
</output>
