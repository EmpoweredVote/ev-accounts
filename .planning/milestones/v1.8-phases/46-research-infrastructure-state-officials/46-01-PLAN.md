---
phase: 46-research-infrastructure-state-officials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-01
  - STANCE-02
  - STANCE-03

must_haves:
  truths:
    - "A stance research CSV file exists with the defined schema"
    - "Governor Newsom has integer stance values (1-5) with source URLs for applicable compass topics"
    - "Lt. Governor Kounalakis has integer stance values (1-5) with source URLs for applicable compass topics"
    - "Every stance row has at least one source URL pointing to a specific article, vote record, or official statement"
    - "No row uses advocacy group ratings as a source"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data for CA state officials"
      contains: "full_name,external_id,topic_key,value,source_url_1"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "healthcare|abortion|tariffs|taxes|same-sex-marriage"
---

<objective>
Create the stance research CSV file with the defined schema and populate it with sourced stance data for California's Governor Newsom and Lt. Governor Kounalakis across all 21 compass topics.

Purpose: Establishes the CSV infrastructure that all subsequent research phases (47, 48) will append to, and provides the first real politician stance data for the compass comparison feature.
Output: `EV-Backend/data/stance_research.csv` with header row and CA state official stance rows.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-research-infrastructure-state-officials/46-CONTEXT.md

Key reference — the 21 compass topic_keys (from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`):
healthcare, abortion, tariffs, taxes, same-sex-marriage, religious-freedom, trans-athletes, ukraine-support, medicare, fossil-fuels, voting-rights, deportation, social-security, ai-regulation, climate-change, civil-rights, housing, campaign-finance, immigration, misinformation, redistricting

Stance scale reference (from the same CSV — each topic has 5 defined stances):
- 1 = Most progressive/expansive government role
- 5 = Most conservative/limited government role
- 3 = Moderate/centrist position

The import target (Phase 50) is `compass.answers` table:
- `politician_id` (UUID, resolved from `external_id` via `essentials.politicians.external_id`)
- `topic_id` (UUID, resolved from `topic_key` via `compass.topics.topic_key`)
- `value` (float64, but research uses integers 1-5)

And `compass.contexts` table:
- `politician_id`, `topic_id`, `reasoning` (string), `sources` (text[])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSV schema and research Governor Newsom stances</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Create the file `EV-Backend/data/stance_research.csv` with header row:
```
full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3
```

Then research Governor Gavin Newsom's positions on all 21 compass topics using web search. For each topic:

1. Search for Newsom's documented positions — legislation signed/vetoed, executive orders, official statements, press conferences, interviews, news coverage
2. Match his position to the closest of the 5 defined stances for that topic (read the stance descriptions from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`)
3. Assign an integer value 1-5
4. Record 1-3 source URLs pointing to specific articles, vote records, bills signed/vetoed, or official statements

**Research guidelines (from CONTEXT.md decisions — these are NON-NEGOTIABLE):**
- Integers only (1-5), no decimals
- When evidence conflicts across time, use the MOST RECENT position
- Only use the individual politician's documented positions — NO party platform fallbacks
- Valid sources: voting records/legislation, official statements/speeches, news from major outlets
- Advocacy group ratings are NOT valid sources
- Omit the row entirely if no documented position exists for a topic
- Minimum 1 source URL per row (required); up to 3 source URLs
- Sources should point to specific articles/votes/speeches — general "Issues" pages acceptable as fallback only

Newsom's BallotReady external_id: look up via web search or use placeholder if not findable (Phase 50 import can also match by name). Use the actual BallotReady ID if discoverable.

Write all Newsom rows to the CSV after the header.
  </action>
  <verify>
    <automated>head -1 EV-Backend/data/stance_research.csv | grep -q "full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3" && grep -c "Gavin Newsom" EV-Backend/data/stance_research.csv</automated>
  </verify>
  <done>CSV file exists with correct header. Newsom has stance rows with integer values 1-5 and at least one source URL per row. Topics without documented positions are omitted (no empty/placeholder rows).</done>
</task>

<task type="auto">
  <name>Task 2: Research Lt. Governor Kounalakis stances</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Research Lt. Governor Eleni Kounalakis's positions on all 21 compass topics using web search. For each topic:

1. Search for Kounalakis's documented positions — official statements, Senate tie-breaking votes (if applicable), campaign positions, speeches, news coverage, her role as chair of various commissions
2. Match her position to the closest of the 5 defined stances for that topic
3. Assign an integer value 1-5
4. Record 1-3 source URLs

**Important context:** Lt. governors typically have fewer publicly documented positions than governors. Per CONTEXT.md: "Equal research effort for governors and lt. governors, but accept that lt. governors will naturally have fewer documented positions." Omit rows where no documented position exists — do NOT guess or infer from party affiliation.

Kounalakis's BallotReady external_id: look up via web search or use placeholder if not findable.

Append all Kounalakis rows to the CSV after the Newsom rows.
  </action>
  <verify>
    <automated>grep -c "Eleni Kounalakis" EV-Backend/data/stance_research.csv</automated>
  </verify>
  <done>Kounalakis has stance rows appended to the CSV with integer values 1-5 and at least one source URL per row. Fewer rows than Newsom is acceptable and expected. No rows exist for topics without documented positions.</done>
</task>

</tasks>

<verification>
1. CSV file exists at `EV-Backend/data/stance_research.csv`
2. Header row matches schema: `full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3`
3. All `topic_key` values match one of the 21 defined keys exactly
4. All `value` entries are integers 1-5
5. Every row has at least one non-empty source URL
6. No advocacy group rating URLs appear as sources
7. Newsom has rows for topics where documented positions were found
8. Kounalakis has rows for topics where documented positions were found
</verification>

<success_criteria>
- The stance research CSV exists with correct schema
- Governor Newsom has sourced stance data for applicable compass topics
- Lt. Governor Kounalakis has sourced stance data for applicable compass topics
- All stance values are integers 1-5 matching the defined stance descriptions
- Every row has at least one source URL to a specific article, bill, or speech
</success_criteria>

<output>
After completion, create `.planning/phases/46-research-infrastructure-state-officials/46-01-SUMMARY.md`
</output>
