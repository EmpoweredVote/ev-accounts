---
phase: 49-quote-collection
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - EV-Backend/data/quote_collection.csv
autonomous: true
requirements:
  - QUOTE-01
  - QUOTE-02
  - QUOTE-03

must_haves:
  truths:
    - "Braun has verbatim quotes with source URLs for multiple compass topics"
    - "Beckwith has verbatim quotes where discoverable (limited public record expected)"
    - "Thomson has verbatim quotes where discoverable (new local mayor, limited record expected)"
    - "Every quote appeared in quotation marks in the original source"
    - "Every row includes source_url and source_name"
  artifacts:
    - path: "EV-Backend/data/quote_collection.csv"
      provides: "Quote collection CSV with IN officials added"
      contains: "Mike Braun"
  key_links:
    - from: "EV-Backend/data/quote_collection.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "healthcare|abortion|tariffs"
---

<objective>
Collect verbatim, sourced quotes from Indiana state officials and mayor (Mike Braun, Micah Beckwith, Kerry Thomson) across compass topics and append to the quote collection CSV.

Purpose: Covers the IN delegation — governor (former senator with extensive record), lt. governor (newer figure), and Bloomington mayor (local executive).
Output: Updated `EV-Backend/data/quote_collection.csv` with IN official quotes appended.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-quote-collection/49-CONTEXT.md
@.planning/phases/49-quote-collection/49-01-SUMMARY.md

Quote CSV schema (NON-NEGOTIABLE):
```
full_name,topic_key,quote_text,source_url,source_name
```

The 21 compass topic_keys:
healthcare, abortion, tariffs, taxes, same-sex-marriage, religious-freedom, trans-athletes, ukraine-support, medicare, fossil-fuels, voting-rights, deportation, social-security, ai-regulation, climate-change, civil-rights, housing, campaign-finance, immigration, misinformation, redistricting

Existing source URLs are in `EV-Backend/data/stance_research.csv`:
- Mike Braun: 21 topics (extensive Senate record + governor actions, congress.gov, votesmart, indystar)
- Micah Beckwith: 13 topics (newer figure, limited record)
- Kerry Thomson: 12 topics (Bloomington mayor since Jan 2024, bloomington.in.gov sources)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Mike Braun quotes</name>
  <files>EV-Backend/data/quote_collection.csv</files>
  <action>
Research verbatim quotes from Governor Mike Braun (R-IN) across all 21 compass topics.

Braun served as US Senator (2019-2025) before becoming governor in January 2025. His Senate record provides extensive material — floor speeches, committee hearings, press conferences, campaign debates (especially the 2024 gubernatorial debates on WISH-TV and ABC7 Chicago).

**Research process for each topic:**
a. Check source URLs in stance_research.csv for Braun rows — IndyStar, congress.gov, in.gov/gov press releases
b. Search web for: `Mike Braun quote [topic]` or `Braun statement [topic]`
c. Gubernatorial debate transcripts are especially valuable (WISH-TV Governor's Debate — note the Read & Rank mock data already has Braun debate quotes)
d. Senate floor speeches and committee hearing transcripts

**Quote quality standards (NON-NEGOTIABLE from CONTEXT.md):**
- Verbatim quotes only — must appear in quotation marks in the original source
- No indirect quotes or paraphrases under any circumstances
- Preferred length: 2-4 sentences
- Recent quotes preferred (2022-present) but older acceptable
- Omit row entirely if no verbatim quote exists

**CSV formatting:**
- Wrap `quote_text` in double quotes; escape internal quotes by doubling them (`""`)
- `source_name` examples: "WISH-TV Governor's Debate", "Indianapolis Star", "Governor's Office Press Release"

**URL AUTHENTICITY (CRITICAL):**
- DO NOT fabricate URLs. Only include URLs verified from web search results.
- congress.gov member pages and vote pages are generally reliable
- in.gov/gov/newsroom press release URLs must be verified

Append all Braun rows after the last existing row in the CSV.
  </action>
  <verify>
    <automated>grep -c "Mike Braun" EV-Backend/data/quote_collection.csv && echo "---" && python3 -c "
import csv
with open('EV-Backend/data/quote_collection.csv') as f:
    rows = list(csv.reader(f))
braun = [r for r in rows[1:] if r[0] == 'Mike Braun']
print(f'Braun quotes: {len(braun)}')
print(f'Topics covered: {len(set(r[1] for r in braun))}')
print(f'All have URLs: {all(r[3].startswith(\"http\") for r in braun)}')
"</automated>
  </verify>
  <done>Braun has verbatim quote rows for multiple compass topics. Each row has a direct quotation with source_url and source_name. Topics without discoverable verbatim quotes are omitted.</done>
</task>

<task type="auto">
  <name>Task 2: Research Beckwith and Thomson quotes, then validate</name>
  <files>EV-Backend/data/quote_collection.csv</files>
  <action>
1. **Research Micah Beckwith quotes:**
Lt. Governor Beckwith (R-IN) is a newer political figure — a pastor who won the 2024 lt. governor nomination at the state convention. His public record is more limited than Braun's, but he has made statements on social/cultural issues.

Research approach:
- Check stance_research.csv source URLs for Beckwith (13 topics)
- Search for Beckwith statements, interviews, campaign speeches, pastoral statements
- Expect fewer quotes than Braun — Beckwith's documented verbatim quotes may be sparse
- Apply same verbatim-only standards; omit rows where no direct quote found

Append Beckwith rows after Braun's rows.

2. **Research Kerry Thomson quotes:**
Mayor Thomson (D-Bloomington, IN) took office January 2024. She is a local executive with limited national media coverage.

Research approach:
- Check stance_research.csv source URLs for Thomson (12 topics, mostly bloomington.in.gov)
- Search for Thomson statements at city council meetings, press conferences, Herald-Times interviews, WFIU/WTIU coverage
- Local mayors often have fewer widely-quoted statements — expect limited quote coverage
- Apply same verbatim-only standards; omit rows where no direct quote found

Append Thomson rows after Beckwith's rows.

3. **Validate all IN officials in CSV:**
- All topic_key values match one of 21 defined keys
- All rows have non-empty quote_text, source_url, source_name
- All quotes are verbatim (spot-check for paraphrase patterns)
- All URLs start with `https://` or `http://`
- CSV parses correctly

Report final counts for Braun, Beckwith, Thomson.
  </action>
  <verify>
    <automated>python3 -c "
import csv
with open('EV-Backend/data/quote_collection.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    rows = list(reader)

valid_topics = {'healthcare','abortion','tariffs','taxes','same-sex-marriage','religious-freedom','trans-athletes','ukraine-support','medicare','fossil-fuels','voting-rights','deportation','social-security','ai-regulation','climate-change','civil-rights','housing','campaign-finance','immigration','misinformation','redistricting'}

errors = []
for name in ['Mike Braun', 'Micah Beckwith', 'Kerry Thomson']:
    person_rows = [r for r in rows if r[0] == name]
    for r in person_rows:
        if r[1] not in valid_topics:
            errors.append(f'Invalid topic: {r[1]} for {r[0]}')
        if not r[2].strip():
            errors.append(f'Empty quote for {r[0]} {r[1]}')
        if not r[3].strip().startswith('http'):
            errors.append(f'Bad URL for {r[0]} {r[1]}')
        if not r[4].strip():
            errors.append(f'Empty source_name for {r[0]} {r[1]}')

braun = len([r for r in rows if r[0] == 'Mike Braun'])
beckwith = len([r for r in rows if r[0] == 'Micah Beckwith'])
thomson = len([r for r in rows if r[0] == 'Kerry Thomson'])

if errors:
    for e in errors[:10]:
        print(f'FAIL: {e}')
else:
    print(f'Braun quotes: {braun}')
    print(f'Beckwith quotes: {beckwith}')
    print(f'Thomson quotes: {thomson}')
    print(f'Total CSV rows: {len(rows)}')
    print('PASS: IN officials validation complete')
"</automated>
  </verify>
  <done>Beckwith and Thomson quotes appended. All IN officials validated: valid topic_keys, verbatim quotes, source URLs and names present. Gap report shows which topics lack quotes for each politician.</done>
</task>

</tasks>

<verification>
1. Mike Braun has verbatim quote rows for multiple compass topics
2. Micah Beckwith has verbatim quote rows where discoverable
3. Kerry Thomson has verbatim quote rows where discoverable
4. Every quote is a direct quotation from the original source
5. Every row has source_url and source_name
6. All topic_key values match the 21 defined compass topics
7. CSV parses correctly
8. No fabricated URLs
</verification>

<success_criteria>
- IN state officials and Bloomington mayor have sourced verbatim quotes
- All quotes are genuinely verbatim (not paraphrased)
- All source URLs are real and verifiable
- CSV remains well-formed with prior CA state official rows intact
</success_criteria>

<output>
After completion, create `.planning/phases/49-quote-collection/49-02-SUMMARY.md`
</output>
