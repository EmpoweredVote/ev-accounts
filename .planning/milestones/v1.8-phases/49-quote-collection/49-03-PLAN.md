---
phase: 49-quote-collection
plan: 03
type: execute
wave: 3
depends_on:
  - 49-02
files_modified:
  - EV-Backend/data/quote_collection.csv
autonomous: true
requirements:
  - QUOTE-01
  - QUOTE-02
  - QUOTE-03

must_haves:
  truths:
    - "Padilla has verbatim quotes with source URLs for multiple compass topics"
    - "Schiff has verbatim quotes with source URLs for multiple compass topics"
    - "Bass has verbatim quotes with source URLs for multiple compass topics"
    - "Every quote appeared in quotation marks in the original source"
    - "Every row includes source_url and source_name"
  artifacts:
    - path: "EV-Backend/data/quote_collection.csv"
      provides: "Quote collection CSV with CA senators and LA mayor added"
      contains: "Alex Padilla"
  key_links:
    - from: "EV-Backend/data/quote_collection.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "healthcare|abortion|immigration"
---

<objective>
Collect verbatim, sourced quotes from CA US Senators (Alex Padilla, Adam Schiff) and LA Mayor (Karen Bass) across compass topics and append to the quote collection CSV.

Purpose: Covers 3 high-profile CA officials with extensive public records — two US Senators with congressional records and a mayor with both congressional and mayoral history.
Output: Updated `EV-Backend/data/quote_collection.csv` with CA senator and LA mayor quotes appended.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-quote-collection/49-CONTEXT.md
@.planning/phases/49-quote-collection/49-02-SUMMARY.md

Quote CSV schema (NON-NEGOTIABLE):
```
full_name,topic_key,quote_text,source_url,source_name
```

The 21 compass topic_keys:
healthcare, abortion, tariffs, taxes, same-sex-marriage, religious-freedom, trans-athletes, ukraine-support, medicare, fossil-fuels, voting-rights, deportation, social-security, ai-regulation, climate-change, civil-rights, housing, campaign-finance, immigration, misinformation, redistricting

Existing source URLs in `EV-Backend/data/stance_research.csv`:
- Alex Padilla: 21 topics (US Senator, padilla.senate.gov, congress.gov)
- Adam Schiff: 21 topics (US Senator, former House Intel Committee chair, schiff.senate.gov, congress.gov)
- Karen Bass: 21 topics (LA Mayor, former congresswoman, mayor.lacity.org, congress.gov member B001270)

Note from Phase 47: Many senate.gov/house.gov slug-only press release URLs were cleared as fabricated. The congress.gov member page and vote URLs are more reliable. Focus web search on verified news articles and official transcripts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Padilla and Schiff quotes</name>
  <files>EV-Backend/data/quote_collection.csv</files>
  <action>
1. **Research Alex Padilla quotes:**
Senator Padilla (D-CA) was appointed to replace Kamala Harris in 2021 and won election in 2022. He was previously CA Secretary of State. Key issue areas: immigration (as the first Latino CA senator), voting rights, climate, civil rights.

Research approach:
- Check stance_research.csv source URLs (congress.gov member/vote pages, news articles)
- Search for Padilla floor speeches, committee hearing statements, press conference quotes
- Senate.gov press releases and official statements
- LA Times, Sacramento Bee, Politico, and national outlet coverage

2. **Research Adam Schiff quotes:**
Senator Schiff (D-CA) served 22 years in the House (2001-2024) including as chair of the House Intelligence Committee. Elected to Senate in 2024. Extremely well-quoted in national media due to impeachment proceedings and Intel Committee work.

Research approach:
- Check stance_research.csv source URLs (congress.gov, news articles)
- Search for Schiff floor speeches, committee hearing statements, debate quotes
- Intel Committee hearings and impeachment proceedings provide extensive quotable material
- Abundant national media coverage (NYT, Washington Post, CNN, etc.)

**Quote quality standards (NON-NEGOTIABLE from CONTEXT.md):**
- Verbatim quotes only — must appear in quotation marks in the original source
- No indirect quotes or paraphrases
- Preferred length: 2-4 sentences
- Recent quotes preferred (2022-present) but older acceptable
- Omit row if no verbatim quote found

**URL AUTHENTICITY (CRITICAL):**
- DO NOT fabricate URLs
- congress.gov vote and member pages are reliable
- Verify news article URLs exist before including them

Append Padilla rows, then Schiff rows, after the last existing row in the CSV.
  </action>
  <verify>
    <automated>python3 -c "
import csv
with open('EV-Backend/data/quote_collection.csv') as f:
    rows = list(csv.reader(f))
padilla = [r for r in rows[1:] if r[0] == 'Alex Padilla']
schiff = [r for r in rows[1:] if r[0] == 'Adam Schiff']
print(f'Padilla quotes: {len(padilla)}, topics: {len(set(r[1] for r in padilla))}')
print(f'Schiff quotes: {len(schiff)}, topics: {len(set(r[1] for r in schiff))}')
print(f'All Padilla URLs valid: {all(r[3].startswith(\"http\") for r in padilla)}')
print(f'All Schiff URLs valid: {all(r[3].startswith(\"http\") for r in schiff)}')
"</automated>
  </verify>
  <done>Padilla and Schiff have verbatim quote rows for multiple compass topics. Each row has a direct quotation with source_url and source_name.</done>
</task>

<task type="auto">
  <name>Task 2: Research Karen Bass quotes and validate batch</name>
  <files>EV-Backend/data/quote_collection.csv</files>
  <action>
1. **Research Karen Bass quotes:**
Mayor Bass (D-Los Angeles) served in the US House (2011-2022) representing CA-37, was chair of the Congressional Black Caucus, and became LA Mayor in December 2022. She has both congressional and mayoral records to draw from.

Research approach:
- Check stance_research.csv source URLs (congress.gov member B001270, mayor.lacity.org)
- Search for Bass floor speeches, committee statements, mayoral press conferences, State of the City addresses
- Key topics where she is well-quoted: housing/homelessness (ED1 executive directive), civil rights (CBC chair), immigration, healthcare
- LA Times and LA-area media provide extensive mayoral coverage
- Congressional Record has floor speech transcripts

Apply same verbatim-only, URL authenticity standards.

Append Bass rows after Schiff's rows.

2. **Validate this batch:**
- All Padilla, Schiff, Bass rows have valid topic_keys
- All rows have non-empty quote_text, source_url, source_name
- All URLs start with `https://` or `http://`
- CSV parses correctly
- Prior rows (CA state officials, IN officials) remain intact

Report counts for Padilla, Schiff, Bass, and running total.
  </action>
  <verify>
    <automated>python3 -c "
import csv
with open('EV-Backend/data/quote_collection.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    rows = list(reader)

valid_topics = {'healthcare','abortion','tariffs','taxes','same-sex-marriage','religious-freedom','trans-athletes','ukraine-support','medicare','fossil-fuels','voting-rights','deportation','social-security','ai-regulation','climate-change','civil-rights','housing','campaign-finance','immigration','misinformation','redistricting'}

errors = []
for name in ['Alex Padilla', 'Adam Schiff', 'Karen Bass']:
    person_rows = [r for r in rows if r[0] == name]
    if len(person_rows) == 0:
        errors.append(f'No rows for {name}')
    for r in person_rows:
        if r[1] not in valid_topics:
            errors.append(f'Invalid topic: {r[1]} for {r[0]}')
        if not r[2].strip():
            errors.append(f'Empty quote for {r[0]} {r[1]}')
        if not r[3].strip().startswith('http'):
            errors.append(f'Bad URL for {r[0]} {r[1]}')
        if not r[4].strip():
            errors.append(f'Empty source_name for {r[0]} {r[1]}')

if errors:
    for e in errors[:10]:
        print(f'FAIL: {e}')
else:
    by_name = {}
    for r in rows:
        by_name.setdefault(r[0], []).append(r)
    for name in sorted(by_name.keys()):
        print(f'{name}: {len(by_name[name])} quotes, {len(set(r[1] for r in by_name[name]))} topics')
    print(f'Total rows: {len(rows)}')
    print(f'Total politicians: {len(by_name)}')
    print('PASS: CA senators + LA mayor validation complete')
"</automated>
  </verify>
  <done>Bass quotes appended. All 3 officials validated: valid topic_keys, verbatim quotes, source URLs and names present. Running total reported with per-politician breakdown.</done>
</task>

</tasks>

<verification>
1. Alex Padilla has verbatim quote rows for multiple compass topics
2. Adam Schiff has verbatim quote rows for multiple compass topics
3. Karen Bass has verbatim quote rows for multiple compass topics
4. Every quote is a direct quotation from the original source
5. Every row has source_url and source_name
6. All topic_key values match the 21 defined compass topics
7. Prior rows (CA state officials, IN officials) remain intact
8. CSV parses correctly
</verification>

<success_criteria>
- CA senators and LA mayor have sourced verbatim quotes
- All quotes are genuinely verbatim
- All source URLs are real and verifiable
- CSV remains well-formed with all prior rows intact
</success_criteria>

<output>
After completion, create `.planning/phases/49-quote-collection/49-03-SUMMARY.md`
</output>
