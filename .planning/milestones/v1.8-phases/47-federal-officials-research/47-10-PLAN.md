---
phase: 47-federal-officials-research
plan: 10
type: execute
wave: 4
depends_on:
  - 47-09
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-08

must_haves:
  truths:
    - "Every source URL for Tony Cardenas rows resolves to a real, accessible web page"
    - "Every source URL for Judy Chu rows resolves to a real, accessible web page"
    - "Every source URL for Pete Aguilar rows resolves to a real, accessible web page"
    - "Every source URL for Jimmy Gomez rows resolves to a real, accessible web page"
    - "No AP News URLs use the fabricated year-suffix slug pattern"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data with verified source URLs for LA County House reps batch 2"
      contains: "Tony Cardenas"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "Real web pages"
      via: "Every source_url must be a working URL or empty"
      pattern: "No hallucinated URLs remain for these politicians"
---

<objective>
Verify and replace hallucinated source URLs for the second batch of LA County House representatives (Tony Cardenas CA-29, Judy Chu CA-28, Pete Aguilar CA-33, Jimmy Gomez CA-34) in the stance research CSV.

Purpose: These 4 experienced House members account for 84 rows and ~252 URLs. All have been in Congress for multiple terms, making it more likely that congress.gov bill URLs are valid but AP News and house.gov URLs need verification/replacement.
Output: Updated `EV-Backend/data/stance_research.csv` with verified or replaced source URLs for these representatives.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**URL domain breakdown for these 4 representatives (84 rows, ~252 URLs total):**
- Tony Cardenas (CA-29): 21 apnews.com, 21 cardenas.house.gov, 20 congress.gov
- Judy Chu (CA-28): 21 apnews.com, 21 chu.house.gov, 20 congress.gov
- Pete Aguilar (CA-33): 21 apnews.com, 21 aguilar.house.gov, 16 congress.gov
- Jimmy Gomez (CA-34): 21 apnews.com, 21 gomez.house.gov, 20 congress.gov

**Member tenure:**
- Tony Cardenas: In Congress since 2013 (CA-29, San Fernando Valley)
- Judy Chu: In Congress since 2009 (CA-28, Pasadena/West Covina area)
- Pete Aguilar: In Congress since 2015 (CA-33, Inland Empire area). Note: Aguilar's district inclusion in LA County needs verification in Plan 47-12.
- Jimmy Gomez: In Congress since 2017 (CA-34, Downtown LA/East LA)

All 4 have multi-term records with established house.gov websites and voting records.

**CRITICAL RULES:**
- Do NOT change stance values — only fix URLs
- Do NOT change politician names or topic_keys
- Do NOT reorder rows or modify other politicians' rows
- Every row MUST retain at least source_url_1
- Use WebSearch to find real URLs — never fabricate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Cardenas + Chu source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Tony Cardenas (21 rows) and Judy Chu (21 rows).

**For Tony Cardenas (CA-29, 21 rows, ~62 URLs):**

Cardenas has been in Congress since 2013, representing the San Fernando Valley. He should have a substantial press release archive.

For each of Cardenas's 21 topic rows:
1. **cardenas.house.gov URLs (21):** Cardenas has been in office long enough to have real press releases. Verify via WebSearch. If a specific press release doesn't exist, search for real alternatives.
2. **apnews.com URLs (21):** ALL fabricated. Use WebSearch to find real news articles about Cardenas and each topic. Good sources: LA Times, LA Daily News (covers San Fernando Valley), The Hill, congress.gov, Politico.
3. **congress.gov URLs (20):** Spot-check bill numbers for validity. Cardenas has many co-sponsored bills.

**For Judy Chu (CA-28, 21 rows, ~62 URLs):**

Chu has been in Congress since 2009 (special election). She chairs the Congressional Asian Pacific American Caucus and has been prominent on immigration, healthcare, and civil rights issues.

For each of Chu's 21 topic rows:
1. **chu.house.gov URLs (21):** Chu's long tenure means real press releases likely exist. Verify via WebSearch.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: LA Times, Pasadena Star-News, The Hill, congress.gov, Politico.
3. **congress.gov URLs (20):** Spot-check bill numbers.

After fixing all URLs, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
names = ['Tony Cardenas', 'Judy Chu']
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    counts = {n: 0 for n in names}
    for row in reader:
        if row[0] in names:
            counts[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    for n, c in counts.items():
        print(f'{n}: {c} rows')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert all(c == 21 for c in counts.values())
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Cardenas and Chu URLs verified')
"</automated>
  </verify>
  <done>All 42 rows for Cardenas and Chu have verified real source URLs. Zero fabricated AP News URLs remain. Every row retains at least source_url_1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Aguilar + Gomez source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Pete Aguilar (21 rows) and Jimmy Gomez (21 rows).

**For Pete Aguilar (CA-33, 21 rows, ~58 URLs):**

Aguilar has been in Congress since 2015 and serves in House Democratic leadership (Chair of the House Democratic Caucus). He has high media visibility.

For each of Aguilar's 21 topic rows:
1. **aguilar.house.gov URLs (21):** Aguilar's leadership role means his office produces frequent press releases. Verify via WebSearch. Many may be real.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Aguilar gets significant national coverage due to his leadership role. Good sources: LA Times, San Bernardino Sun, The Hill, congress.gov, Politico, NBC News, Washington Post.
3. **congress.gov URLs (16):** Spot-check bill numbers.

Note: Aguilar's CA-33 district primarily covers San Bernardino County. The question of whether it overlaps LA County will be addressed in Plan 47-12. For this plan, just fix the URLs regardless — if the district question results in removing Aguilar later, the URL fix work is small and the rest of the CSV benefits.

**For Jimmy Gomez (CA-34, 21 rows, ~62 URLs):**

Gomez has been in Congress since 2017 (special election), representing Downtown LA and East LA. Previously served in CA State Assembly.

For each of Gomez's 21 topic rows:
1. **gomez.house.gov URLs (21):** Verify via WebSearch. Gomez should have a reasonable press release archive.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: LA Times, The Hill, congress.gov, Politico.
3. **congress.gov URLs (20):** Spot-check bill numbers.

After fixing all URLs, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
names = ['Pete Aguilar', 'Jimmy Gomez']
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    counts = {n: 0 for n in names}
    for row in reader:
        if row[0] in names:
            counts[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    for n, c in counts.items():
        print(f'{n}: {c} rows')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert all(c == 21 for c in counts.values())
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Aguilar and Gomez URLs verified')
"</automated>
  </verify>
  <done>All 42 rows for Aguilar and Gomez have verified real source URLs. Zero fabricated AP News URLs remain. Every row retains at least source_url_1.</done>
</task>

</tasks>

<verification>
1. Zero AP News URLs with year-suffix pattern remain for Cardenas, Chu, Aguilar, Gomez
2. Every row has at least source_url_1 populated
3. All replacement URLs found via WebSearch
4. Stance values, politician names, topic_keys, row order unchanged
5. Row counts: 21 each (84 total)
6. No rows for other politicians modified
</verification>

<success_criteria>
- All 84 rows have verified, working source URLs
- Zero hallucinated AP News URLs remain
- House.gov URLs verified or replaced
- Congress.gov URLs spot-checked
- Stance values unchanged
- CSV structure maintained
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-10-SUMMARY.md`
</output>
