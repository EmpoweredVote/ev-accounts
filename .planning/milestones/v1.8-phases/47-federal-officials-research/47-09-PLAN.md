---
phase: 47-federal-officials-research
plan: 09
type: execute
wave: 3
depends_on:
  - 47-08
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-07
  - STANCE-08

must_haves:
  truths:
    - "Every source URL for Erin Houchin rows resolves to a real, accessible web page about Houchin and the relevant topic"
    - "Every source URL for George Whitesides rows resolves to a real, accessible web page"
    - "Every source URL for Laura Friedman rows resolves to a real, accessible web page"
    - "Every source URL for Brad Sherman rows resolves to a real, accessible web page"
    - "No AP News URLs use the fabricated year-suffix slug pattern"
    - "No house.gov URLs use fabricated press release slugs that don't resolve"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data with verified source URLs for Monroe County rep + first 3 LA County reps"
      contains: "Erin Houchin"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "Real web pages"
      via: "Every source_url must be a working URL or empty"
      pattern: "No hallucinated URLs remain for these politicians"
---

<objective>
Verify and replace hallucinated source URLs for Erin Houchin (Monroe County IN, IN-9) and the first batch of LA County House representatives (George Whitesides CA-27, Laura Friedman CA-30, Brad Sherman CA-32) in the stance research CSV.

Purpose: These 4 House members account for 84 rows and ~252 URLs. AP News and house.gov URLs are the primary concern domains. Whitesides is a freshman (Jan 2025) with limited press release history, requiring extra care in source selection.
Output: Updated `EV-Backend/data/stance_research.csv` with verified or replaced source URLs for these representatives.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**URL domain breakdown for these 4 representatives (84 rows, ~252 URLs total):**
- Erin Houchin (IN-9): 21 apnews.com, 21 houchin.house.gov, 21 congress.gov
- George Whitesides (CA-27): 21 apnews.com, 21 whitesides.house.gov (FRESHMAN - sworn in Jan 2025, very few press releases expected)
- Laura Friedman (CA-30): 21 apnews.com, 21 friedman.house.gov
- Brad Sherman (CA-32): 21 apnews.com, 21 sherman.house.gov, 20 congress.gov

**Key considerations:**
- Houchin has been in Congress since Jan 2023 — moderate number of press releases and votes
- Whitesides is a FRESHMAN (Jan 2025) — may have very few press releases on most topics. Campaign website positions and news coverage of his 2024 campaign are likely the best sources. Do NOT expect 21 house.gov press releases.
- Friedman is a FRESHMAN (Jan 2025) — previously served in CA State Assembly. Similar situation to Whitesides.
- Sherman has been in Congress since 1997 — extensive legislative record, congress.gov bill references likely checkable

**CRITICAL RULES:**
- Do NOT change stance values — only fix URLs
- Do NOT change politician names or topic_keys
- Do NOT reorder rows or modify other politicians' rows
- Every row MUST retain at least source_url_1
- Use WebSearch to find real URLs — never fabricate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Erin Houchin source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all 21 rows for Erin Houchin.

Houchin has 3 URL columns per row: typically houchin.house.gov (source_url_1), apnews.com (source_url_2), and congress.gov (source_url_3).

**For each of Houchin's 21 topic rows:**

1. **houchin.house.gov URLs (21):** These are press release URLs with generic slug patterns. Use WebSearch to verify each one exists. Houchin has been in Congress since Jan 2023 and likely has SOME press releases, but not necessarily on all 21 topics. If a URL is broken:
   - Search for real Houchin press releases on that topic via WebSearch
   - Try houchin.house.gov site search or Google "site:houchin.house.gov [topic]"
   - If no house.gov press release exists, find an alternative: news articles, congress.gov votes, official statements

2. **apnews.com URLs (21):** ALL fabricated. For each:
   - Use WebSearch to find real news coverage of Houchin's position on that topic
   - Good sources: Indianapolis Star, Indiana Capital Chronicle, The Hill, congress.gov, Politico
   - Replace with real URLs found

3. **congress.gov URLs (21):** Spot-check bill number format. These may be partially valid. Verify at least 5-7 of them.

Special attention: Houchin's `abortion` row (value=5) cites H.R. 431 (Life at Conception Act, 118th Congress). Verify this bill number is correct.
Houchin's `healthcare` row uses congress.gov/member/ as source_url_1 — replace with a specific bill or vote URL.

After fixing all URLs, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    count = 0
    for row in reader:
        if row[0] == 'Erin Houchin':
            count += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    print(f'Houchin rows: {count}')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert count == 21
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Houchin URLs verified')
"</automated>
  </verify>
  <done>All 21 Houchin rows have verified real source URLs. Zero fabricated AP News URLs remain. Every row retains at least source_url_1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Whitesides, Friedman, Sherman source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for George Whitesides (21 rows), Laura Friedman (21 rows), and Brad Sherman (21 rows).

**For George Whitesides (CA-27, 21 rows, ~42 URLs):**

Whitesides is a FRESHMAN member sworn in January 2025. He is unlikely to have house.gov press releases on all 21 topics.

1. **whitesides.house.gov URLs (21):** Very likely fabricated — a freshman would not have 21 topic-specific press releases in ~2 months. Use WebSearch to check if any real press releases exist. For topics without press releases:
   - Search for Whitesides's 2024 campaign positions (campaign website, news coverage)
   - Search for news articles about Whitesides and each topic
   - Good sources: Los Angeles Times, Ventura County Star, The Hill, congress.gov (any votes cast since Jan 2025)

2. **apnews.com URLs (21):** ALL fabricated. Replace each via WebSearch with real news coverage.

Note: For a freshman, it is acceptable if some rows only have 1 source URL instead of 2-3. Quality over quantity.

**For Laura Friedman (CA-30, 21 rows, ~42 URLs):**

Friedman is also a FRESHMAN member (Jan 2025). Previously served in CA State Assembly. Similar approach to Whitesides.

1. **friedman.house.gov URLs (21):** Likely fabricated for same reason as Whitesides. Verify via WebSearch.
   - Search for Friedman's CA State Assembly record and positions
   - Search for 2024 campaign coverage in Pasadena/Glendale area media
   - Good sources: LA Times, Pasadena Star-News, congress.gov, CA legislature records (leginfo.legislature.ca.gov)

2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch.

**For Brad Sherman (CA-32, 21 rows, ~62 URLs):**

Sherman has been in Congress since 1997 — extensive record. More URLs likely verifiable.

1. **sherman.house.gov URLs (21):** Sherman's long tenure means he likely has real press releases. Verify via WebSearch. Many may actually exist.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch.
3. **congress.gov URLs (20):** Spot-check bill numbers. Sherman has a long voting record — these may be valid.

After fixing all URLs for all 3 reps, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
names = ['George Whitesides', 'Laura Friedman', 'Brad Sherman']
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    counts = {n: 0 for n in names}
    for row in reader:
        if row[0] in names:
            counts[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    for n, c in counts.items():
        print(f'{n}: {c} rows')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert all(c == 21 for c in counts.values())
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Whitesides, Friedman, Sherman URLs verified')
"</automated>
  </verify>
  <done>All 63 rows for these 3 LA County reps have verified real source URLs. Zero fabricated AP News or house.gov URLs remain. Every row retains at least source_url_1.</done>
</task>

</tasks>

<verification>
1. Zero AP News URLs with year-suffix pattern remain for Houchin, Whitesides, Friedman, Sherman
2. Every row has at least source_url_1 populated
3. All replacement URLs were found via WebSearch
4. Freshman members (Whitesides, Friedman) may have fewer source URLs per row — that is acceptable
5. Stance values, politician names, topic_keys, row order unchanged
6. Row counts: 21 each (84 total)
7. No rows for other politicians were modified
</verification>

<success_criteria>
- All 84 rows have verified, working source URLs
- Zero hallucinated AP News URLs remain
- House.gov URLs verified or replaced with real alternatives
- Congress.gov URLs spot-checked
- Stance values unchanged
- CSV structure maintained
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-09-SUMMARY.md`
</output>
