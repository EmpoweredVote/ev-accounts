---
phase: 47-federal-officials-research
plan: 12
type: execute
wave: 6
depends_on:
  - 47-11
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-05
  - STANCE-06
  - STANCE-07
  - STANCE-08

must_haves:
  truths:
    - "Zero AP News URLs with year-suffix pattern exist anywhere in the CSV"
    - "Every row in the CSV has at least source_url_1 populated with a non-empty URL"
    - "Pete Aguilar's CA-33 district overlap with LA County is confirmed or his rows are flagged/handled"
    - "A spot-check of 10+ stance values against their source URLs confirms accuracy"
    - "CSV structural integrity maintained: 422 data rows, 21 unique politicians, all topic_keys valid"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Fully validated stance research CSV with all source URLs verified"
      contains: "422 data rows with verified URLs"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "21 valid topic_keys, no invalid values"
---

<objective>
Perform final validation of the entire stance research CSV after all URL fixes from Plans 47-07 through 47-11, verify Pete Aguilar's CA-33 district overlap with LA County, and spot-check stance value accuracy.

Purpose: This plan closes all verification gaps identified in 47-VERIFICATION.md. It runs after all URL fix plans are complete and provides the final quality gate before the CSV is used for Phase 50 data import.
Output: Updated `EV-Backend/data/stance_research.csv` (if corrections needed) and validation report in the summary.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**Gaps being closed:**
1. **GAP-1 (CRITICAL):** Source URL authenticity — Plans 07-11 replaced ~723 hallucinated URLs. This plan validates that no hallucinated URLs remain.
2. **Secondary Issue 1:** Pete Aguilar CA-33 district boundary — need to confirm CA-33 overlaps LA County.
3. **Secondary Issue 2:** Stance value accuracy — spot-check values against real sources.
4. **Secondary Issue 3:** Congress.gov member profile pages as primary source (low priority, 3 rows).

**Expected CSV state after Plans 07-11:**
- 422 data rows + 1 header = 423 lines
- 21 unique politicians
- All 21 topic_keys used
- Zero AP News year-suffix URLs
- All rows have source_url_1 populated

**Stance descriptions reference:** `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Aguilar district + spot-check stance values</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
**Part A: Pete Aguilar CA-33 District Verification**

Use WebSearch to determine whether California's 33rd Congressional District (119th Congress, post-2022 redistricting) overlaps with Los Angeles County.

Search for: "California 33rd congressional district 119th Congress map boundaries" and "CA-33 Pete Aguilar district Los Angeles County"

Key question: Does any portion of CA-33 fall within the boundaries of Los Angeles County?

Context: CA-33 (Pete Aguilar) primarily covers Redlands, Ontario, Rancho Cucamonga, and Fontana in San Bernardino County. The question is whether the western edge (Pomona, Claremont, La Verne area) crosses into LA County.

**If CA-33 overlaps LA County:** Aguilar's inclusion is correct. Document the overlap area in the summary.

**If CA-33 does NOT overlap LA County:**
1. Aguilar was incorrectly included as an LA County rep
2. Do NOT remove his rows from the CSV — his data is still valid federal official data
3. Note in the summary that CA-33 does not overlap LA County, and flag this for the Phase 47 verification update
4. Check via WebSearch if any LA County district was missed (the current 12 districts are: CA-27, 28, 29, 30, 32, 33, 34, 36, 37, 38, 43, 44)

**Part B: Stance Value Spot-Check**

Read the stance descriptions from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv` to understand what each value 1-5 means for each topic.

Select 10 rows across different politicians and topics for spot-checking. Prioritize:
- Rows flagged in VERIFICATION.md: Todd Young same-sex-marriage (value=2), Jim Banks medicare (value=5), Erin Houchin abortion (value=5), Alex Padilla tariffs (value=2)
- Additional rows covering different value ranges (1, 3, 4, 5) and different politicians
- Rows where the source_url_1 is a congress.gov bill or vote URL (easier to verify)

For each selected row:
1. Use WebSearch to look up the source URL or search for the politician's position on that topic
2. Compare the assigned stance value against the stance descriptions
3. Document whether the value is accurate, debatable, or incorrect

If any stance values are clearly wrong (off by 2+ points), correct them and note the change in the summary.

If stance values are debatable (reasonable people might assign adjacent value), leave them as-is and note in the summary.
  </action>
  <verify>
    <automated>python3 -c "
print('Aguilar district check: Manual verification via WebSearch — see summary')
print('Stance spot-check: Manual verification via WebSearch — see summary')
print('PASS: Task requires WebSearch verification documented in summary')
"</automated>
  </verify>
  <done>Aguilar's CA-33 district overlap with LA County is confirmed or flagged. At least 10 stance values spot-checked against real sources. Any clearly incorrect values corrected. Results documented in summary.</done>
</task>

<task type="auto">
  <name>Task 2: Full CSV integrity validation</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Run a comprehensive validation of the entire CSV to confirm all URL fixes from Plans 07-11 are complete and no integrity issues were introduced.

**Validation checks (all must pass):**

1. **Row count:** Exactly 422 data rows (423 lines including header)
2. **Column count:** Every row has exactly 7 columns
3. **Politician count:** Exactly 21 unique politician names
4. **Topic key validation:** All topic_key values match one of the 21 defined keys
5. **Value range:** All stance values are integers 1-5
6. **No duplicates:** No duplicate (full_name, topic_key) pairs
7. **Source URL coverage:** Every row has non-empty source_url_1
8. **AP News check:** ZERO AP News URLs with year-suffix pattern (e.g., ending in `-2021`) anywhere in the CSV
9. **URL format:** All URLs start with `https://` or `http://`
10. **No empty rows:** No rows with empty full_name or topic_key

**Domain diversity check:** Report the final URL domain distribution to confirm AP News hallucinated URLs were replaced with a variety of real sources.

**Per-politician summary:** For each of the 21 politicians, report:
- Number of rows
- Number of URLs (source_url_1, source_url_2, source_url_3)
- Top URL domains used

Write the validation results as a structured report in the summary.

If any checks fail, fix the issue in the CSV. If a fix requires re-running WebSearch for a specific row, do so.

**Final sanity check:** Read 5 random rows and confirm the URLs look like real, specific articles/pages (not generic slugs that could be fabricated).
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
from collections import Counter

with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    rows = list(reader)

valid_topics = {'healthcare','abortion','tariffs','taxes','same-sex-marriage','religious-freedom','trans-athletes','ukraine-support','medicare','fossil-fuels','voting-rights','deportation','social-security','ai-regulation','climate-change','civil-rights','housing','campaign-finance','immigration','misinformation','redistricting'}

errors = []

# Check 1: Row count
if len(rows) != 422:
    errors.append(f'Row count: {len(rows)} (expected 422)')

# Check 2: Column count
bad_cols = [i+2 for i, r in enumerate(rows) if len(r) != 7]
if bad_cols:
    errors.append(f'Bad column count on lines: {bad_cols[:5]}')

# Check 3: Politician count
politicians = set(r[0] for r in rows)
if len(politicians) != 21:
    errors.append(f'Politician count: {len(politicians)} (expected 21)')

# Check 4: Topic keys
bad_topics = [r[2] for r in rows if r[2] not in valid_topics]
if bad_topics:
    errors.append(f'Invalid topic_keys: {set(bad_topics)}')

# Check 5: Value range
bad_values = [(r[0], r[2], r[3]) for r in rows if not r[3].strip().isdigit() or int(r[3]) < 1 or int(r[3]) > 5]
if bad_values:
    errors.append(f'Invalid values: {bad_values[:5]}')

# Check 6: Duplicates
pairs = [(r[0], r[2]) for r in rows]
dupes = [p for p, c in Counter(pairs).items() if c > 1]
if dupes:
    errors.append(f'Duplicate pairs: {dupes[:5]}')

# Check 7: Source URL coverage
missing_url1 = [(r[0], r[2]) for r in rows if not r[4].strip()]
if missing_url1:
    errors.append(f'Missing source_url_1: {missing_url1[:5]}')

# Check 8: AP News year-suffix
ap_hallucinated = 0
for r in rows:
    for url in r[4:7]:
        if 'apnews.com' in url and re.search(r'-\d{4}$', url):
            ap_hallucinated += 1
if ap_hallucinated > 0:
    errors.append(f'Hallucinated AP URLs remaining: {ap_hallucinated}')

# Check 9: URL format
bad_urls = []
for r in rows:
    for url in r[4:7]:
        if url.strip() and not url.strip().startswith('http'):
            bad_urls.append(url[:50])
if bad_urls:
    errors.append(f'Bad URL format: {bad_urls[:5]}')

if errors:
    for e in errors:
        print(f'FAIL: {e}')
    assert False, f'{len(errors)} validation checks failed'
else:
    print(f'Total rows: {len(rows)}')
    print(f'Politicians: {len(politicians)}')
    print(f'All 422 rows have valid structure')
    print(f'Zero hallucinated AP URLs')
    print(f'All rows have source_url_1')
    print('PASS: Full CSV validation complete')
"</automated>
  </verify>
  <done>Full CSV validation passes all 10 checks. Zero hallucinated AP News URLs. All 422 rows structurally valid. Domain distribution shows diverse, real sources. Validation report included in summary.</done>
</task>

</tasks>

<verification>
1. All 10 structural validation checks pass
2. Zero hallucinated AP News URLs in entire CSV
3. Pete Aguilar CA-33 district question resolved
4. At least 10 stance values spot-checked and documented
5. Domain distribution reported — shows real sources replaced hallucinated ones
6. CSV ready for Phase 50 data import
</verification>

<success_criteria>
- Full CSV integrity validation passes with zero errors
- Zero hallucinated URLs remain in the entire CSV
- Aguilar district question answered
- Stance accuracy spot-checked with results documented
- CSV confirmed ready for Phase 50 import
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-12-SUMMARY.md`
</output>
