---
phase: 47-federal-officials-research
plan: 11
type: execute
wave: 5
depends_on:
  - 47-10
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-08

must_haves:
  truths:
    - "Every source URL for Ted Lieu rows resolves to a real, accessible web page"
    - "Every source URL for Sydney Kamlager-Dove rows resolves to a real, accessible web page"
    - "Every source URL for Linda Sanchez rows resolves to a real, accessible web page"
    - "Every source URL for Maxine Waters rows resolves to a real, accessible web page"
    - "Every source URL for Nanette Barragan rows resolves to a real, accessible web page"
    - "No AP News URLs use the fabricated year-suffix slug pattern"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data with verified source URLs for LA County House reps batch 3"
      contains: "Ted Lieu"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "Real web pages"
      via: "Every source_url must be a working URL or empty"
      pattern: "No hallucinated URLs remain for these politicians"
---

<objective>
Verify and replace hallucinated source URLs for the final batch of LA County House representatives (Ted Lieu CA-36, Sydney Kamlager-Dove CA-37, Linda Sanchez CA-38, Maxine Waters CA-43, Nanette Barragan CA-44) in the stance research CSV.

Purpose: These 5 House members account for 105 rows and ~307 URLs — the largest batch. All but Kamlager-Dove are experienced members with extensive records, making congress.gov URLs more likely valid. This plan completes the URL verification for all LA County representatives.
Output: Updated `EV-Backend/data/stance_research.csv` with verified or replaced source URLs for these representatives.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**URL domain breakdown for these 5 representatives (105 rows, ~307 URLs total):**
- Ted Lieu (CA-36): 21 apnews.com, 21 lieu.house.gov, 17 congress.gov
- Sydney Kamlager-Dove (CA-37): 21 apnews.com, 21 kamlager-dove.house.gov, 13 congress.gov
- Linda Sanchez (CA-38): 21 apnews.com, 21 sanchez.house.gov, 16 congress.gov
- Maxine Waters (CA-43): 21 apnews.com, 21 waters.house.gov, 16 congress.gov
- Nanette Barragan (CA-44): 21 apnews.com, 21 barragan.house.gov, 15 congress.gov

**Member tenure:**
- Ted Lieu: In Congress since 2015 (CA-36, West LA/beach cities). High media profile, frequent Twitter/media presence.
- Sydney Kamlager-Dove: In Congress since 2023 (special election, CA-37, South LA/Baldwin Hills). Previously CA State Senator. Moderate record length.
- Linda Sanchez: In Congress since 2003 (CA-38, Whittier/Cerritos area). Very long tenure, extensive record.
- Maxine Waters: In Congress since 1991 (CA-43, South LA/Inglewood). One of the longest-serving members. Chair of Financial Services Committee (past). Extremely high media visibility.
- Nanette Barragan: In Congress since 2017 (CA-44, Compton/San Pedro). Chair of Congressional Hispanic Caucus. Active on environmental justice.

**CRITICAL RULES:**
- Do NOT change stance values — only fix URLs
- Do NOT change politician names or topic_keys
- Do NOT reorder rows or modify other politicians' rows
- Every row MUST retain at least source_url_1
- Use WebSearch to find real URLs — never fabricate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Lieu, Kamlager-Dove, and Sanchez source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Ted Lieu (21 rows), Sydney Kamlager-Dove (21 rows), and Linda Sanchez (21 rows).

**For Ted Lieu (CA-36, 21 rows, ~59 URLs):**

Lieu is highly media-active and has been in Congress since 2015. He frequently makes public statements and has a robust social media presence.

For each of Lieu's 21 topic rows:
1. **lieu.house.gov URLs (21):** Lieu's active communications office likely produces real press releases. Verify via WebSearch. Many may exist.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: LA Times, The Hill, Politico, congress.gov, MSNBC/CNN transcripts (Lieu is a frequent guest).
3. **congress.gov URLs (17):** Spot-check bill numbers. Lieu sponsors many bills.

**For Sydney Kamlager-Dove (CA-37, 21 rows, ~55 URLs):**

Kamlager-Dove won a special election in 2023 and was re-elected in 2024. Previously served as CA State Senator. She has a moderate Congressional record but CA state legislative history.

For each of Kamlager-Dove's 21 topic rows:
1. **kamlager-dove.house.gov URLs (21):** She's been in office since mid-2023 — some press releases should exist but not as many as longer-serving members. Verify via WebSearch.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: LA Times (covers her district well), LA Sentinel, The Hill, congress.gov. For state-level positions, search for her CA State Senate record.
3. **congress.gov URLs (13):** Spot-check bill numbers.

**For Linda Sanchez (CA-38, 21 rows, ~58 URLs):**

Sanchez has been in Congress since 2003 — one of the longer-serving members in this batch. Extensive voting record.

For each of Sanchez's 21 topic rows:
1. **sanchez.house.gov URLs (21):** Long tenure means a large press release archive. Verify via WebSearch. Many should be real.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: Whittier Daily News, LA Times, The Hill, congress.gov.
3. **congress.gov URLs (16):** Spot-check bill numbers.

After fixing all URLs, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
names = ['Ted Lieu', 'Sydney Kamlager-Dove', 'Linda Sanchez']
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    counts = {n: 0 for n in names}
    for row in reader:
        if row[0] in names:
            counts[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    for n, c in counts.items():
        print(f'{n}: {c} rows')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert all(c == 21 for c in counts.values())
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Lieu, Kamlager-Dove, Sanchez URLs verified')
"</automated>
  </verify>
  <done>All 63 rows for Lieu, Kamlager-Dove, and Sanchez have verified real source URLs. Zero fabricated AP News URLs remain. Every row retains at least source_url_1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Waters + Barragan source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Maxine Waters (21 rows) and Nanette Barragan (21 rows).

**For Maxine Waters (CA-43, 21 rows, ~58 URLs):**

Waters has been in Congress since 1991 — one of the longest-serving members of Congress. She is extremely well-known nationally, especially on financial regulation, housing, and civil rights.

For each of Waters's 21 topic rows:
1. **waters.house.gov URLs (21):** Waters's office has decades of press releases. Verify via WebSearch. Many should exist.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Waters gets extensive national coverage. Good sources: LA Times, The Hill, Politico, congress.gov, Washington Post, Reuters.
3. **congress.gov URLs (16):** Spot-check bill numbers. Waters has authored major legislation (especially on banking/housing).

Special attention: Waters's `deportation` row (value=1) is described as "most vocal opponent, stop-all-deportations position" per State.md decisions. Find a strong source confirming this.

**For Nanette Barragan (CA-44, 21 rows, ~57 URLs):**

Barragan has been in Congress since 2017, representing Compton, San Pedro, and parts of South LA. She is Chair of the Congressional Hispanic Caucus and prominent on environmental justice and immigration.

For each of Barragan's 21 topic rows:
1. **barragan.house.gov URLs (21):** Barragan should have a reasonable press release archive since 2017. Verify via WebSearch.
2. **apnews.com URLs (21):** ALL fabricated. Replace via WebSearch. Good sources: LA Times, Daily Breeze (covers San Pedro area), The Hill, congress.gov, Politico.
3. **congress.gov URLs (15):** Spot-check bill numbers.

Special attention: Barragan's `fossil-fuels` row (value=1) is described as "Green New Deal, port district interests" per State.md decisions. Find a strong source.

After fixing all URLs, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
names = ['Maxine Waters', 'Nanette Barragan']
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    counts = {n: 0 for n in names}
    for row in reader:
        if row[0] in names:
            counts[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    for n, c in counts.items():
        print(f'{n}: {c} rows')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert all(c == 21 for c in counts.values())
    assert ap_year_suffix == 0
    assert empty_url1 == 0
print('PASS: Waters and Barragan URLs verified')
"</automated>
  </verify>
  <done>All 42 rows for Waters and Barragan have verified real source URLs. Zero fabricated AP News URLs remain. Every row retains at least source_url_1.</done>
</task>

</tasks>

<verification>
1. Zero AP News URLs with year-suffix pattern remain for Lieu, Kamlager-Dove, Sanchez, Waters, Barragan
2. Every row has at least source_url_1 populated
3. All replacement URLs found via WebSearch
4. Stance values, politician names, topic_keys, row order unchanged
5. Row counts: 21 each (105 total)
6. No rows for other politicians modified
7. All 12 LA County representatives now have verified URLs (combined with Plans 09, 10)
</verification>

<success_criteria>
- All 105 rows have verified, working source URLs
- Zero hallucinated AP News URLs remain
- House.gov URLs verified or replaced
- Congress.gov URLs spot-checked
- Stance values unchanged
- CSV structure maintained
- All LA County House representative URL fixes complete
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-11-SUMMARY.md`
</output>
