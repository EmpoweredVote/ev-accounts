---
phase: 47-federal-officials-research
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-05
  - STANCE-06
  - STANCE-07
  - STANCE-08

must_haves:
  truths:
    - "Every source URL for Gavin Newsom rows resolves to a real, accessible web page about that politician and topic"
    - "Every source URL for Eleni Kounalakis rows resolves to a real, accessible web page about that politician and topic"
    - "No AP News URLs use the fabricated year-suffix slug pattern (e.g., ending in -2021, -2022)"
    - "URLs that cannot be verified are replaced with working alternatives or removed entirely"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data with verified source URLs for CA state officials"
      contains: "Gavin Newsom"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "Real web pages"
      via: "Every source_url_1, source_url_2, source_url_3 must be a working URL or empty"
      pattern: "No hallucinated URLs remain"
---

<objective>
Verify and replace hallucinated source URLs for California and Indiana state officials (Gavin Newsom, Eleni Kounalakis, Mike Braun, Micah Beckwith) in the stance research CSV.

Purpose: Phase 47 verification found ~723 fabricated URLs across the CSV (AP News year-suffix slugs, generic house.gov press release paths). This plan fixes the 4 Phase 46 state officials who share the same CSV file — their URL integrity must be restored before the CSV is used for data import in Phase 50.
Output: Updated `EV-Backend/data/stance_research.csv` with verified or replaced source URLs for all state official rows.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**Gap being closed:** The verification report found that ~407 AP News URLs use fabricated year-suffix slugs (e.g., `-2021`) instead of real AP hex-hash identifiers. House.gov and other domain URLs are also suspect. This plan addresses the state officials subset.

**URL domain breakdown for these 4 politicians (65 rows, 75 URLs total):**
- Gavin Newsom: 30 gov.ca.gov, 10 apnews.com, 5 calmatters.org, 5 latimes.com, 1 washingtonpost.com, 1 sfgate.com, 1 govtrack.us
- Eleni Kounalakis: 11 ltgov.ca.gov, 9 apnews.com, 2 calmatters.org
- Mike Braun: 21 apnews.com, 13 votesmart.org, 9 congress.gov, 9 indystar.com, 5 in.gov, 1 politico.com
- Micah Beckwith: 13 apnews.com, 13 indystar.com, 1 fox59.com, 1 wrtv.com

**Likely valid domains:** gov.ca.gov (official CA governor site), ltgov.ca.gov (official CA lt. governor site), in.gov (official IN state site) — these should be spot-checked but are likely real since they are .gov domains with specific paths.

**Likely fabricated domains:** apnews.com (year-suffix pattern confirmed as hallucinated), votesmart.org (need verification).

**Strategy for each URL:**
1. First, check if the URL uses a known-hallucinated pattern (AP News year-suffix, generic slug-only paths)
2. If suspicious: use WebSearch to find a REAL source URL for that politician + topic combination
3. If the URL looks legitimate (e.g., gov.ca.gov with specific bill/executive order paths): use WebSearch to verify it exists, or find a replacement if it doesn't
4. Replace broken URLs with real ones. If no real source can be found, clear the URL cell (empty string) rather than keep a broken link
5. NEVER fabricate or guess URLs — only use URLs found via WebSearch results

**CSV schema:** `full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3`

**CRITICAL RULES:**
- Do NOT change stance values (column 4) — only fix URLs (columns 5-7)
- Do NOT change politician names or topic_keys
- Do NOT reorder rows
- Do NOT modify rows for politicians not in this plan's scope
- Every row MUST retain at least source_url_1 (find a replacement if the original is broken)
- Use WebSearch to find real URLs — the whole problem is that URLs were hallucinated by AI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Newsom + Kounalakis source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Gavin Newsom (21 rows) and Eleni Kounalakis (10 rows).

**For Gavin Newsom (31 URLs across 21 rows):**

Newsom's gov.ca.gov URLs (30 total) are likely valid — they point to the official CA governor website with specific paths. Spot-check 3-5 of them using WebSearch to confirm the pages exist. If any are broken, find replacements.

Newsom's AP News URLs (10 total) are almost certainly fabricated (year-suffix pattern). For each AP News URL:
1. Use WebSearch to search for the real article about Newsom and that specific topic
2. Replace with the real URL found via search
3. If no real AP News article exists, find an alternative source (LA Times, Sacramento Bee, Reuters, official statements)

Newsom's calmatters.org URLs (5 total): Verify via WebSearch. CalMatters is a real CA news outlet — URLs may be valid but need checking.

Newsom's other URLs (latimes.com, washingtonpost.com, sfgate.com, govtrack.us — 7 total): Verify via WebSearch. Replace any broken ones.

**For Eleni Kounalakis (22 URLs across 10 rows):**

Kounalakis's ltgov.ca.gov URLs (11 total): Spot-check 3-4 using WebSearch. These are official CA Lt. Governor site URLs and may be valid.

Kounalakis's AP News URLs (9 total): Almost certainly fabricated. For each:
1. Use WebSearch to find real coverage of Kounalakis's position on that topic
2. Replace with real URLs (may need to use ltgov.ca.gov official statements, Sacramento Bee, CalMatters, or other CA news outlets as Kounalakis has less national media coverage than Newsom)

Kounalakis's calmatters.org URLs (2 total): Verify via WebSearch.

**Important:** Kounalakis only has 10 rows (10 of 21 topics) — this is intentional limited coverage per Phase 46 decisions. Do not add or remove rows.

After fixing all URLs, update the CSV in place. Preserve exact row order and all non-URL columns unchanged.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    for row in reader:
        if row[0] in ('Gavin Newsom', 'Eleni Kounalakis'):
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    assert ap_year_suffix == 0, 'Hallucinated AP URLs still present'
print('PASS: No hallucinated AP URLs for CA state officials')
"</automated>
  </verify>
  <done>All source URLs for Newsom and Kounalakis rows are verified real URLs or replaced with working alternatives. Zero AP News year-suffix pattern URLs remain. Every row retains at least source_url_1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Braun + Beckwith source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Mike Braun (21 rows) and Micah Beckwith (13 rows).

**For Mike Braun (58 URLs across 21 rows):**

Braun's AP News URLs (21 total): All fabricated (year-suffix pattern). For each:
1. Use WebSearch to search for real coverage of Braun's position on that topic (e.g., "Mike Braun Indiana senator healthcare position")
2. Replace with real URLs — good sources include Indianapolis Star, Indiana Public Media, congress.gov vote records, senate.gov press releases, Reuters, AP News (with real hex-hash URLs if found)

Braun's votesmart.org URLs (13 total): VoteSmart is a real organization. Search for Braun's VoteSmart page to verify these URLs are real. If broken, find alternatives.

Braun's congress.gov URLs (9 total): Congress.gov uses predictable URL patterns for bills and members. Verify the bill numbers and member pages exist. These may be valid.

Braun's indystar.com URLs (9 total): Indianapolis Star is a real outlet. Verify via WebSearch — some may be real, some may be fabricated with plausible slugs.

Braun's in.gov URLs (5 total): Official Indiana state website. Spot-check — may be valid for governor-era content (Braun became governor Jan 2025).

Braun's politico.com URL (1 total): Verify via WebSearch.

**For Micah Beckwith (28 URLs across 13 rows):**

Beckwith's AP News URLs (13 total): All fabricated. For each:
1. Use WebSearch to find real coverage of Beckwith's positions
2. Note: Beckwith is Indiana's Lt. Governor (took office Jan 2025) and was previously a pastor/activist. He may have limited policy coverage — local Indiana outlets (IndyStar, WISH-TV, WTHR, Indiana Capital Chronicle) are likely best sources
3. Replace with real URLs found via search

Beckwith's indystar.com URLs (13 total): Verify via WebSearch. Indianapolis Star covered Beckwith extensively during the 2024 campaign — some of these may be real.

Beckwith's fox59.com and wrtv.com URLs (1 each): Verify via WebSearch.

After fixing all URLs, update the CSV in place. Preserve exact row order and all non-URL columns unchanged.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    for row in reader:
        if row[0] in ('Mike Braun', 'Micah Beckwith'):
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert ap_year_suffix == 0, 'Hallucinated AP URLs still present'
    assert empty_url1 == 0, 'Some rows missing primary source URL'
print('PASS: No hallucinated AP URLs for IN state officials, all rows have source_url_1')
"</automated>
  </verify>
  <done>All source URLs for Braun and Beckwith rows are verified real URLs or replaced with working alternatives. Zero AP News year-suffix pattern URLs remain. Every row retains at least source_url_1.</done>
</task>

</tasks>

<verification>
1. Zero AP News URLs with year-suffix pattern remain for any of the 4 state officials
2. Every row for Newsom, Kounalakis, Braun, and Beckwith has at least source_url_1 populated
3. All replacement URLs were found via WebSearch (not fabricated)
4. Stance values, politician names, topic_keys, and row order are unchanged
5. Row counts unchanged: Newsom 21, Kounalakis 10, Braun 21, Beckwith 13
6. No rows for other politicians were modified
</verification>

<success_criteria>
- All 65 state official rows have verified, working source URLs
- Zero hallucinated AP News URLs remain for these politicians
- Stance values are unchanged (only URLs modified)
- CSV structure and integrity maintained
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-07-SUMMARY.md`
</output>
