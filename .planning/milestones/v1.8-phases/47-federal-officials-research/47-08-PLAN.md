---
phase: 47-federal-officials-research
plan: 08
type: execute
wave: 2
depends_on:
  - 47-07
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-05
  - STANCE-06

must_haves:
  truths:
    - "Every source URL for Alex Padilla rows resolves to a real, accessible web page about Padilla and the relevant topic"
    - "Every source URL for Adam Schiff rows resolves to a real, accessible web page about Schiff and the relevant topic"
    - "Every source URL for Todd Young rows resolves to a real, accessible web page about Young and the relevant topic"
    - "Every source URL for Jim Banks rows resolves to a real, accessible web page about Banks and the relevant topic"
    - "No AP News URLs use the fabricated year-suffix slug pattern"
    - "No house.gov or senate.gov URLs use fabricated press release slugs"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data with verified source URLs for all 4 US Senators"
      contains: "Alex Padilla"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "Real web pages"
      via: "Every source_url_1, source_url_2, source_url_3 must be a working URL or empty"
      pattern: "No hallucinated URLs remain for senators"
---

<objective>
Verify and replace hallucinated source URLs for all four US Senators (Alex Padilla, Adam Schiff, Todd Young, Jim Banks) in the stance research CSV.

Purpose: These 4 senators account for 84 rows and ~252 URLs in the CSV. AP News URLs (84 total) are confirmed fabricated. Senate.gov URLs (44 total) and congress.gov URLs (89 total) need verification. This plan restores citation integrity for all senate-level federal officials.
Output: Updated `EV-Backend/data/stance_research.csv` with verified or replaced source URLs for all senator rows.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-federal-officials-research/47-CONTEXT.md
@.planning/phases/47-federal-officials-research/47-VERIFICATION.md

**URL domain breakdown for these 4 senators (84 rows, ~252 URLs total):**
- Alex Padilla: 21 apnews.com, 21 padilla.senate.gov, 19 congress.gov
- Adam Schiff: 21 apnews.com, 19 schiff.house.gov, 2 schiff.senate.gov, 20 congress.gov
- Todd Young: 21 apnews.com, 21 young.senate.gov, 21 congress.gov
- Jim Banks: 21 apnews.com, 21 banks.house.gov, 21 congress.gov

**Verification priorities:**
1. **apnews.com (84 URLs):** ALL fabricated (year-suffix pattern). Must be replaced via WebSearch.
2. **senate.gov (44 URLs):** padilla.senate.gov (21), young.senate.gov (21), schiff.senate.gov (2). These may be real — senators do have press release pages. But the slug-only pattern (no date codes) is suspicious. Verify via WebSearch.
3. **house.gov (40 URLs):** schiff.house.gov (19), banks.house.gov (21). Schiff moved to Senate in Jan 2025 — his old house.gov site may still be up or may redirect. Banks moved to Senate in Jan 2025 — same situation. Verify via WebSearch.
4. **congress.gov (81 URLs):** Uses predictable URL patterns (bill numbers, member IDs). Spot-check a sample. These are more likely valid than other domains.

**Strategy:**
- For each senator, work through all 21 rows
- For each row, evaluate each URL (source_url_1, source_url_2, source_url_3)
- Use WebSearch to find real sources for that politician + topic combination
- Replace fabricated URLs with real ones
- Congress.gov bill URLs can be verified by checking the bill number format matches a real bill

**CRITICAL RULES:**
- Do NOT change stance values (column 4) — only fix URLs (columns 5-7)
- Do NOT change politician names or topic_keys
- Do NOT reorder rows
- Do NOT modify rows for politicians not in this plan's scope
- Every row MUST retain at least source_url_1
- Use WebSearch to find real URLs — the whole problem is that URLs were hallucinated by AI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Padilla + Schiff source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Alex Padilla (21 rows) and Adam Schiff (21 rows).

**For Alex Padilla (21 rows, ~61 URLs):**

Work through each of Padilla's 21 topic rows. For each row:

1. **source_url_1** — typically padilla.senate.gov or congress.gov. Use WebSearch to verify the URL exists. If broken, search for a real Padilla press release or bill page on that topic.
2. **source_url_2** — typically apnews.com (fabricated) or congress.gov. If AP News, replace via WebSearch with a real news article about Padilla and that topic. If congress.gov, verify the bill/vote URL format.
3. **source_url_3** — if present and apnews.com, replace. If other domain, verify.

Good replacement sources for Padilla: padilla.senate.gov official press releases, congress.gov bill pages, Los Angeles Times, Sacramento Bee, CalMatters, Reuters, real AP News articles (with hex-hash IDs).

**For Adam Schiff (21 rows, ~62 URLs):**

Note: Schiff served in the House (CA-28/CA-30) until Jan 2025, then became a Senator. His schiff.house.gov URLs may redirect or be archived. His schiff.senate.gov URLs are new (only 2 exist currently).

For each of Schiff's 21 topic rows:

1. **source_url_1** — typically schiff.house.gov or congress.gov. Verify via WebSearch. If schiff.house.gov is down, find replacement (Schiff's Senate site, news articles, congress.gov records).
2. **source_url_2** — typically apnews.com (fabricated) or congress.gov. Replace AP News URLs via WebSearch.
3. **source_url_3** — if present, verify or replace.

Good replacement sources for Schiff: schiff.senate.gov, congress.gov (extensive House voting record 2001-2024), Los Angeles Times, Politico (covered Schiff extensively during impeachment era), Reuters.

After fixing all URLs for both CA senators, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    row_count = {'Alex Padilla': 0, 'Adam Schiff': 0}
    for row in reader:
        if row[0] in ('Alex Padilla', 'Adam Schiff'):
            row_count[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
    print(f'Padilla rows: {row_count[\"Alex Padilla\"]}')
    print(f'Schiff rows: {row_count[\"Adam Schiff\"]}')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    assert row_count['Alex Padilla'] == 21, f'Expected 21 Padilla rows, got {row_count[\"Alex Padilla\"]}'
    assert row_count['Adam Schiff'] == 21, f'Expected 21 Schiff rows, got {row_count[\"Adam Schiff\"]}'
    assert ap_year_suffix == 0, 'Hallucinated AP URLs still present'
    assert empty_url1 == 0, 'Some rows missing primary source URL'
print('PASS: CA senators URLs verified')
"</automated>
  </verify>
  <done>All source URLs for Padilla and Schiff are verified real URLs or replaced with working alternatives. Zero fabricated AP News URLs remain. Every row retains at least source_url_1. Row counts unchanged (21 each).</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Young + Banks source URLs</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Read the CSV and identify all rows for Todd Young (21 rows) and Jim Banks (21 rows).

**For Todd Young (21 rows, ~63 URLs):**

Young has been a US Senator from Indiana since 2017. He has extensive legislative records.

For each of Young's 21 topic rows:

1. **source_url_1** — typically young.senate.gov or congress.gov. Verify via WebSearch. Young's Senate site should have real press releases.
2. **source_url_2** — typically apnews.com (fabricated) or congress.gov. Replace AP News URLs via WebSearch.
3. **source_url_3** — typically congress.gov. Verify bill/vote URL format.

Special attention: Young's `same-sex-marriage` row (value=2) cites congress.gov bill S.4556 (Respect for Marriage Act). This congress.gov URL is likely valid — verify the bill number.

Good replacement sources for Young: young.senate.gov, congress.gov, Indianapolis Star, Indiana Capital Chronicle, Reuters, Politico.

**For Jim Banks (21 rows, ~63 URLs):**

Note: Banks served in the House (IN-3) until Jan 2025, then became a Senator. His banks.house.gov URLs may redirect or be archived.

For each of Banks's 21 topic rows:

1. **source_url_1** — typically banks.house.gov or congress.gov. Verify via WebSearch. If banks.house.gov is down, find replacement.
2. **source_url_2** — typically apnews.com (fabricated) or congress.gov. Replace AP News URLs via WebSearch.
3. **source_url_3** — typically congress.gov. Verify.

Special attention: Banks's `healthcare` row uses congress.gov/member/ profile as source_url_1 — this is a weak source. Find a specific bill or vote URL as replacement while keeping the profile as a secondary source.

Good replacement sources for Banks: banks.senate.gov (new Senate site), congress.gov (House voting record), Indianapolis Star, Washington Examiner, The Hill, Politico.

After fixing all URLs for both IN senators, update the CSV in place.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    ap_year_suffix = 0
    empty_url1 = 0
    generic_member = 0
    row_count = {'Todd Young': 0, 'Jim Banks': 0}
    for row in reader:
        if row[0] in ('Todd Young', 'Jim Banks'):
            row_count[row[0]] += 1
            for url in row[4:7]:
                if 'apnews.com' in url and re.search(r'-\d{4}$', url):
                    ap_year_suffix += 1
            if not row[4].strip():
                empty_url1 += 1
            if row[4].strip().endswith('/member/james-banks/B001299') or row[4].strip().endswith('/member/todd-young/Y000064'):
                generic_member += 1
    print(f'Young rows: {row_count[\"Todd Young\"]}')
    print(f'Banks rows: {row_count[\"Jim Banks\"]}')
    print(f'AP year-suffix URLs remaining: {ap_year_suffix}')
    print(f'Rows missing source_url_1: {empty_url1}')
    print(f'Generic member page as source_url_1: {generic_member}')
    assert row_count['Todd Young'] == 21
    assert row_count['Jim Banks'] == 21
    assert ap_year_suffix == 0, 'Hallucinated AP URLs still present'
    assert empty_url1 == 0, 'Some rows missing primary source URL'
print('PASS: IN senators URLs verified')
"</automated>
  </verify>
  <done>All source URLs for Young and Banks are verified real URLs or replaced with working alternatives. Zero fabricated AP News URLs remain. Every row retains at least source_url_1. Row counts unchanged (21 each). Generic congress.gov member profile pages replaced with specific bill/vote URLs as primary sources where possible.</done>
</task>

</tasks>

<verification>
1. Zero AP News URLs with year-suffix pattern remain for any of the 4 senators
2. Every row for Padilla, Schiff, Young, and Banks has at least source_url_1 populated
3. All replacement URLs were found via WebSearch (not fabricated)
4. Stance values, politician names, topic_keys, and row order are unchanged
5. Row counts unchanged: 21 per senator (84 total)
6. No rows for other politicians were modified
7. Congress.gov bill URLs use valid bill number formats
8. Senate.gov/house.gov URLs point to real press release pages
</verification>

<success_criteria>
- All 84 senator rows have verified, working source URLs
- Zero hallucinated AP News URLs remain for these politicians
- Senate.gov and house.gov URLs verified or replaced
- Congress.gov URLs spot-checked for validity
- Stance values are unchanged (only URLs modified)
- CSV structure and integrity maintained
</success_criteria>

<output>
After completion, create `.planning/phases/47-federal-officials-research/47-08-SUMMARY.md`
</output>
