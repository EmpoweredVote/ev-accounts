---
phase: 48-mayors-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-09

must_haves:
  truths:
    - "Mayor Thomson has integer stance values (1-5) with source URLs for applicable compass topics"
    - "Every Thomson stance row has at least one source URL pointing to a specific article, official page, or news report"
    - "No row uses advocacy group ratings as a source"
    - "No fabricated URLs — all URLs use verifiable patterns (official city sites, major news outlets with real slugs, or general fallback pages)"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Stance research data including Bloomington IN Mayor Thomson"
      contains: "Kerry Thomson"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "healthcare|abortion|tariffs|taxes|same-sex-marriage"
---

<objective>
Research and populate sourced stance data for Bloomington IN Mayor Kerry Thomson across all 21 compass topics, appending to the existing stance research CSV.

Purpose: Captures local executive stance data for Bloomington's mayor, the first local-level politician in the stance CSV. Thomson is a newer mayor (January 2024) so documented positions may be limited compared to federal officials.
Output: Updated `EV-Backend/data/stance_research.csv` with Thomson stance rows appended.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-mayors-research/48-CONTEXT.md

Key reference — the 21 compass topic_keys (from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`):
healthcare, abortion, tariffs, taxes, same-sex-marriage, religious-freedom, trans-athletes, ukraine-support, medicare, fossil-fuels, voting-rights, deportation, social-security, ai-regulation, climate-change, civil-rights, housing, campaign-finance, immigration, misinformation, redistricting

Stance scale reference (from the same CSV — each topic has 5 defined stances):
- 1 = Most progressive/expansive government role
- 5 = Most conservative/limited government role
- 3 = Moderate/centrist position

CSV schema (established in Phase 46):
```
full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3
```

The CSV currently has 422 data rows (header + 422 data rows = 423 lines) for 21 politicians after Phase 47 completion.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Mayor Kerry Thomson stances</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Research Bloomington IN Mayor Kerry Thomson's (D) positions on all 21 compass topics using web search. Thomson took office in January 2024 after defeating incumbent Republican John Hamilton. She previously served on the Bloomington City Council and worked in local government.

For each topic:

1. Search for Thomson's documented positions — city council votes, executive actions as mayor, official statements, press releases, campaign platform, interviews, local news coverage (Herald-Times, Indiana Daily Student, WFIU/WTIU, Indianapolis Star)
2. Match her position to the closest of the 5 defined stances for that topic (read the stance descriptions from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`)
3. Assign an integer value 1-5
4. Record 1-3 source URLs pointing to specific articles, official pages, or news reports

**Research guidelines (from CONTEXT.md decisions — these are NON-NEGOTIABLE):**
- Integers only (1-5), no decimals
- When evidence conflicts across time, use the MOST RECENT position
- Only use the individual politician's documented positions — NO party platform fallbacks
- Valid sources: official city government pages, local and state news outlets, campaign websites, city council records
- Advocacy group ratings are NOT valid sources
- Omit the row entirely if no documented position exists for a topic
- Minimum 1 source URL per row (required); up to 3 source URLs
- Sources should point to specific articles/actions/speeches — general "Issues" pages acceptable as fallback only

**URL AUTHENTICITY (CRITICAL — learned from Phase 47):**
- DO NOT fabricate or guess URLs. Phase 47 produced ~723 fabricated URLs requiring extensive cleanup.
- If you cannot verify a specific article URL exists, use one of these verified fallback patterns:
  - City of Bloomington official site: `https://bloomington.in.gov/` (with real subpages if known)
  - Thomson's official mayor page (if discoverable via search)
  - A general news outlet page about Thomson
- When citing a news article, only include the URL if you are confident it is a real article with a correct slug
- It is better to have fewer URLs that are real than many URLs that might be fabricated

**Coverage expectations:** Thomson is a relatively new local mayor in a small city. It is acceptable and expected to have fewer rows than federal officials. Do not force stances on topics where Thomson has no documented position. Quality over quantity.

Thomson's BallotReady external_id: look up via web search or leave blank if not findable.

Append all Thomson rows to the CSV after the existing rows (after Nanette Barragan's last row).
  </action>
  <verify>
    <automated>grep -c "Kerry Thomson" EV-Backend/data/stance_research.csv && echo "---" && wc -l EV-Backend/data/stance_research.csv && echo "---" && awk -F',' '{print $1}' EV-Backend/data/stance_research.csv | sort | uniq -c | sort -rn | head -25</automated>
  </verify>
  <done>Thomson has stance rows appended to the CSV with integer values 1-5 and at least one source URL per row. Topics without documented positions are omitted (no empty/placeholder rows). All URLs are verifiable real pages. Fewer rows than federal officials is acceptable.</done>
</task>

<task type="auto">
  <name>Task 2: Validate Thomson rows and CSV integrity</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
After adding Thomson's rows, validate the CSV:

1. **Topic key validation:** All new Thomson topic_key values match one of the 21 defined keys exactly (case-sensitive)
2. **Value range:** All new value entries are integers 1-5
3. **Source URL coverage:** Every new row has at least one non-empty source_url_1
4. **No duplicates:** No duplicate (full_name, topic_key) pairs for Thomson
5. **URL format check:** All URLs start with `https://` or `http://`
6. **URL authenticity spot-check:** For each Thomson URL, verify it looks like a real page (not a guessed slug). If any URL looks suspicious (generic slug that could be fabricated, non-existent path), replace it with a verified fallback URL or remove it (promoting url_2 to url_1 if needed)
7. **Existing data preserved:** The first 422 data rows are unchanged

Report the final Thomson row count and any URLs that were corrected.
  </action>
  <verify>
    <automated>python3 -c "
import csv
with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    rows = list(reader)

valid_topics = {'healthcare','abortion','tariffs','taxes','same-sex-marriage','religious-freedom','trans-athletes','ukraine-support','medicare','fossil-fuels','voting-rights','deportation','social-security','ai-regulation','climate-change','civil-rights','housing','campaign-finance','immigration','misinformation','redistricting'}

thomson_rows = [r for r in rows if r[0] == 'Kerry Thomson']
errors = []

if len(thomson_rows) == 0:
    errors.append('No Thomson rows found')

for r in thomson_rows:
    if r[2] not in valid_topics:
        errors.append(f'Invalid topic: {r[2]}')
    if not r[3].strip().isdigit() or int(r[3]) < 1 or int(r[3]) > 5:
        errors.append(f'Invalid value: {r[3]} for {r[2]}')
    if not r[4].strip():
        errors.append(f'Missing source_url_1 for {r[2]}')
    for url in r[4:7]:
        if url.strip() and not url.strip().startswith('http'):
            errors.append(f'Bad URL format: {url[:50]}')

# Check no dupes
pairs = [(r[0], r[2]) for r in thomson_rows]
if len(pairs) != len(set(pairs)):
    errors.append('Duplicate Thomson topic_key pairs found')

if errors:
    for e in errors:
        print(f'FAIL: {e}')
else:
    print(f'Thomson rows: {len(thomson_rows)}')
    print(f'Total CSV rows: {len(rows)}')
    print(f'Total politicians: {len(set(r[0] for r in rows))}')
    print('PASS: Thomson validation complete')
"</automated>
  </verify>
  <done>Thomson rows validated: all topic_keys valid, all values 1-5, all rows have source_url_1, no duplicates, all URLs properly formatted and verified. CSV integrity maintained.</done>
</task>

</tasks>

<verification>
1. CSV file at `EV-Backend/data/stance_research.csv` contains rows for Kerry Thomson
2. All Thomson `topic_key` values match one of the 21 defined keys exactly
3. All Thomson `value` entries are integers 1-5
4. Every Thomson row has at least one non-empty source URL
5. No advocacy group rating URLs appear as sources
6. All URLs are verifiable real pages (no fabricated slugs)
7. The existing 422 rows for the prior 21 politicians remain unchanged
8. The CSV remains valid (no malformed rows, proper comma separation)
</verification>

<success_criteria>
- Mayor Thomson has sourced stance data for applicable compass topics
- All stance values are integers 1-5 matching the defined stance descriptions
- Every row has at least one source URL to a specific article, official page, or news report
- All URLs are real and verifiable
- The existing politician rows remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/48-mayors-research/48-01-SUMMARY.md`
</output>
