---
phase: 102-ev-ui-foundation-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ev-ui/src/icons.js
  - ev-ui/src/tokens.js
  - ev-ui/src/CategorySection.jsx
  - ev-ui/src/PoliticianCard.jsx
  - ev-ui/src/PoliticianProfile.jsx
  - ev-ui/src/index.js
  - ev-ui/package.json
autonomous: true
requirements: [VIS-03, DATA-03]

must_haves:
  truths:
    - "ev-ui exports BallotIcon, CompassIcon, and BranchIcon as inline SVG React components"
    - "ev-ui exports tierColors token with federal/state/local keys referencing teal colorScales"
    - "CategorySection accepts an optional tier prop and applies tierColors internally"
    - "PoliticianCard and PoliticianProfile headshot images use objectPosition center 20% by default"
    - "ev-ui is published as v0.1.55 to GitHub npm registry"
  artifacts:
    - path: "ev-ui/src/icons.js"
      provides: "BallotIcon, CompassIcon, BranchIcon inline SVG components"
      exports: ["BallotIcon", "CompassIcon", "BranchIcon"]
    - path: "ev-ui/src/tokens.js"
      provides: "tierColors semantic token"
      contains: "tierColors"
    - path: "ev-ui/src/CategorySection.jsx"
      provides: "tier prop support on CategorySection"
      contains: "tierColors"
    - path: "ev-ui/src/PoliticianCard.jsx"
      provides: "imageFocalPoint prop and objectPosition fix"
      contains: "imageFocalPoint"
    - path: "ev-ui/src/PoliticianProfile.jsx"
      provides: "objectPosition fix on profile headshot"
      contains: "objectPosition"
  key_links:
    - from: "ev-ui/src/index.js"
      to: "ev-ui/src/icons.js"
      via: "export * from './icons.js'"
      pattern: "export.*icons"
    - from: "ev-ui/src/CategorySection.jsx"
      to: "ev-ui/src/tokens.js"
      via: "tierColors import"
      pattern: "tierColors"
---

<objective>
Add icon system, tierColors token, headshot crop fix, and tier prop to ev-ui, then publish v0.1.55.

Purpose: Establishes the visual foundation (icons, tier colors, image cropping) that Phase 103 consumes when wiring icons and tier theming into essentials.
Output: Published @chrisandrewsedu/ev-ui v0.1.55 on GitHub npm registry with all new exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-ev-ui-foundation-quick-wins/102-CONTEXT.md
@.planning/phases/102-ev-ui-foundation-quick-wins/102-RESEARCH.md
@.planning/phases/102-ev-ui-foundation-quick-wins/102-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From ev-ui/src/tokens.js (existing colorScales.teal used by new tierColors):
```js
export const colorScales = {
  teal: {
    '050': '#F5F9FA', '100': '#E4F3F6', '200': '#C0E8F2', '300': '#7DD4E8',
    '400': '#3AABB8', '500': '#00657C', '600': '#005366', '700': '#003E4D',
    '800': '#03303A', '900': '#05262E', '950': '#051A1E',
  },
};

export const colors = {
  bgWhite: '#FFFFFF',
  textPrimary: '#00657C',
  borderMedium: '#C9CFD1',
};
```

From ev-ui/src/index.js (entry point — must add icons export):
```js
// Components
export { default as PoliticianCard } from "./PoliticianCard.jsx";
export { default as CategorySection } from "./CategorySection.jsx";
// ... other exports
// Design Tokens
export * from "./tokens.js";
```

From ev-ui/src/CategorySection.jsx (current props):
```jsx
export default function CategorySection({
  title, infoTooltip, websiteUrl, children, style = {},
})
```

From ev-ui/src/PoliticianCard.jsx (current image style at line 66-70):
```jsx
image: {
  width: '100%',
  height: '100%',
  objectFit: 'cover',
},
```

From ev-ui/src/PoliticianProfile.jsx (current photo style at line 450-456):
```jsx
photo: {
  width: '100%',
  height: '100%',
  borderRadius: borderRadius.lg,
  objectFit: 'cover',
  background: colors.borderLight,
},
```

From ev-ui/src/SocialLinks.jsx (inline SVG pattern to replicate):
```jsx
<svg style={styles.icon} viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="..." stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
</svg>
```

From ev-ui/package.json:
```json
{ "version": "0.1.54", "publishConfig": { "registry": "https://npm.pkg.github.com" } }
```

From ev-ui/tsup.config.js:
```js
export default { entry: ["src/index.js"], splitting: false, external: ["react", "react-dom", "@react-spring/web"] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create icons.js, add tierColors to tokens.js, add tier prop to CategorySection</name>
  <files>ev-ui/src/icons.js, ev-ui/src/tokens.js, ev-ui/src/CategorySection.jsx, ev-ui/src/index.js</files>
  <read_first>
    - ev-ui/src/tokens.js (current token exports — tierColors must reference existing colorScales.teal)
    - ev-ui/src/SocialLinks.jsx (inline SVG pattern to replicate for icons)
    - ev-ui/src/CategorySection.jsx (current props and styles.titlePill structure)
    - ev-ui/src/index.js (entry point — must add icons.js export)
    - ev-ui/tsup.config.js (confirm splitting:false and entry point)
  </read_first>
  <action>
**1. Create `ev-ui/src/icons.js`** (NEW file) per D-01 / D-02:

Three exported React components, each accepting `{ size = 16, color = 'currentColor' }` props. Each renders an `<svg>` with `viewBox="0 0 24 24"`, `fill="none"`, `stroke={color}`, `strokeWidth="2"`, `strokeLinecap="round"`, `strokeLinejoin="round"`. Import React at top.

**BallotIcon** (Lucide `vote`):
```jsx
export function BallotIcon({ size = 16, color = 'currentColor' }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none"
      stroke={color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
      xmlns="http://www.w3.org/2000/svg">
      <path d="m9 12 2 2 4-4" />
      <path d="M5 7c0-1.1.9-2 2-2h10a2 2 0 0 1 2 2v12H5V7Z" />
      <path d="M22 19H2" />
    </svg>
  );
}
```

**CompassIcon** (Lucide `compass`):
```jsx
export function CompassIcon({ size = 16, color = 'currentColor' }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none"
      stroke={color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
      xmlns="http://www.w3.org/2000/svg">
      <circle cx="12" cy="12" r="10" />
      <path d="m16.24 7.76-1.804 5.411a2 2 0 0 1-1.265 1.265L7.76 16.24l1.804-5.411a2 2 0 0 1 1.265-1.265z" />
    </svg>
  );
}
```

**BranchIcon** (Lucide `landmark`):
```jsx
export function BranchIcon({ size = 16, color = 'currentColor' }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none"
      stroke={color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
      xmlns="http://www.w3.org/2000/svg">
      <path d="M10 18v-7" />
      <path d="M11.12 2.198a2 2 0 0 1 1.76.006l7.866 3.847c.476.233.31.949-.22.949H3.474c-.53 0-.695-.716-.22-.949z" />
      <path d="M14 18v-7" />
      <path d="M18 18v-7" />
      <path d="M3 22h18" />
      <path d="M6 18v-7" />
    </svg>
  );
}
```

**2. Add `tierColors` export to `ev-ui/src/tokens.js`** per D-07 / D-08:

Add AFTER the `colorScales` declaration (after line 54), BEFORE the `colors` declaration:

```js
// ──────────────────────────────────────────────
// Tier Colors (teal-scale shade variation)
// ──────────────────────────────────────────────

export const tierColors = {
  federal: {
    bg:     colorScales.teal['100'],   // #E4F3F6
    accent: colorScales.teal['700'],   // #003E4D
    text:   colorScales.teal['700'],   // #003E4D — 7.5:1 on white, AA
  },
  state: {
    bg:     colorScales.teal['050'],   // #F5F9FA
    accent: colorScales.teal['500'],   // #00657C
    text:   colorScales.teal['500'],   // #00657C — 6.66:1 on white, AA
  },
  local: {
    bg:     colorScales.teal['050'],   // #F5F9FA
    accent: colorScales.teal['200'],   // #C0E8F2 — decorative only
    text:   colorScales.teal['600'],   // #005366 — 8.1:1 on white, AA
  },
};
```

IMPORTANT: `local.text` uses teal-600 (#005366), NOT teal-200. teal-200 (#C0E8F2) is only 1.8:1 on white — it MUST NOT be used as text.

**3. Add `tier` prop to `ev-ui/src/CategorySection.jsx`** per D-09:

- Add `import { tierColors }` to the existing token import at line 2 (append `tierColors` to the destructured imports from `'./tokens'`)
- Add `tier` to the destructured props: `{ title, infoTooltip, websiteUrl, children, style = {}, tier }`
- Inside the component, before `const styles`, add: `const tierStyle = tier ? tierColors[tier] : null;`
- Modify `styles.titlePill` to use tier colors when present:
  - `backgroundColor: tierStyle?.bg ?? colors.bgWhite,`
  - `border: \`1px solid ${tierStyle?.accent ?? colors.borderMedium}\`,`
  - `color: tierStyle?.text ?? colors.textPrimary,`

**4. Add icons.js export to `ev-ui/src/index.js`** per D-03:

Add this line after the StanceAccordion export (at end of file):
```js
// Icons
export { BallotIcon, CompassIcon, BranchIcon } from './icons.js';
```
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/ev-ui && grep -q "BallotIcon" src/icons.js && grep -q "CompassIcon" src/icons.js && grep -q "BranchIcon" src/icons.js && grep -q "tierColors" src/tokens.js && grep -q "tier" src/CategorySection.jsx && grep -q "icons.js" src/index.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ev-ui/src/icons.js exists and contains `export function BallotIcon`, `export function CompassIcon`, `export function BranchIcon`
    - Each icon function accepts `{ size = 16, color = 'currentColor' }` params
    - ev-ui/src/tokens.js contains `export const tierColors` with keys `federal`, `state`, `local`
    - tierColors.local.text references `colorScales.teal['600']` (NOT teal-200)
    - ev-ui/src/CategorySection.jsx destructured props include `tier`
    - CategorySection styles.titlePill uses `tierStyle?.bg ?? colors.bgWhite` for backgroundColor
    - ev-ui/src/index.js contains `export { BallotIcon, CompassIcon, BranchIcon } from './icons.js'`
  </acceptance_criteria>
  <done>Icons, tierColors, and tier prop all wired and exportable from ev-ui</done>
</task>

<task type="auto">
  <name>Task 2: Add imageFocalPoint prop to PoliticianCard and PoliticianProfile, bump version, build, publish</name>
  <files>ev-ui/src/PoliticianCard.jsx, ev-ui/src/PoliticianProfile.jsx, ev-ui/package.json</files>
  <read_first>
    - ev-ui/src/PoliticianCard.jsx (current props and styles.image at line 66-70)
    - ev-ui/src/PoliticianProfile.jsx (current props at line 258-269 and styles.photo at line 450-456)
    - ev-ui/package.json (current version 0.1.54)
  </read_first>
  <action>
**1. Add `imageFocalPoint` prop to PoliticianCard.jsx** per D-04 / D-05:

- Add `imageFocalPoint` to the destructured props at line 19-30 (after `badge,`): `imageFocalPoint,`
- Update `styles.image` (line 66-70) to add objectPosition:
  ```js
  image: {
    width: '100%',
    height: '100%',
    objectFit: 'cover',
    objectPosition: imageFocalPoint ?? 'center 20%',
  },
  ```
- Do NOT add objectPosition to `styles.imagePlaceholder` — it has no effect on a `<div>`.

**2. Add `imageFocalPoint` prop to PoliticianProfile.jsx** per D-06:

- Add `imageFocalPoint,` to the destructured props (after `onNavigateToRecord,` at line 268-269)
- Update `styles.photo` (line 450-456) to add objectPosition:
  ```js
  photo: {
    width: '100%',
    height: '100%',
    borderRadius: borderRadius.lg,
    objectFit: 'cover',
    objectPosition: imageFocalPoint ?? 'center 20%',
    background: colors.borderLight,
  },
  ```
- Do NOT modify or replace the existing `getTierColors` function in PoliticianProfile — it serves a different purpose (badge colors, not section theming).

**3. Bump version in ev-ui/package.json** from `"0.1.54"` to `"0.1.55"`.

**4. Build and publish:**
```bash
cd ev-ui
npm run build
npm publish
```

The build uses tsup with `splitting: false`. The publish goes to GitHub npm registry per `publishConfig`.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/ev-ui && grep -q "imageFocalPoint" src/PoliticianCard.jsx && grep -q "imageFocalPoint" src/PoliticianProfile.jsx && grep -q "center 20%" src/PoliticianCard.jsx && grep -q "center 20%" src/PoliticianProfile.jsx && grep -q '"0.1.55"' package.json && npm run build 2>&1 | tail -3 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ev-ui/src/PoliticianCard.jsx props include `imageFocalPoint`
    - PoliticianCard styles.image contains `objectPosition: imageFocalPoint ?? 'center 20%'`
    - PoliticianCard styles.imagePlaceholder does NOT contain `objectPosition`
    - ev-ui/src/PoliticianProfile.jsx props include `imageFocalPoint`
    - PoliticianProfile styles.photo contains `objectPosition: imageFocalPoint ?? 'center 20%'`
    - ev-ui/package.json version is `"0.1.55"`
    - `npm run build` exits 0 and dist/ contains output
    - `npm publish` exits 0 (v0.1.55 available on GitHub npm registry)
  </acceptance_criteria>
  <done>Headshot images default to face-centered cropping. ev-ui v0.1.55 published with all Phase 102 changes (icons, tierColors, tier prop, imageFocalPoint).</done>
</task>

</tasks>

<verification>
1. `cd ev-ui && npm run build` — exits 0
2. `grep -q "BallotIcon" ev-ui/dist/index.mjs` — icons included in bundle
3. `grep -q "tierColors" ev-ui/dist/index.mjs` — token included in bundle
4. `grep -q "imageFocalPoint" ev-ui/dist/index.mjs` — prop included in bundle
5. `npm view @chrisandrewsedu/ev-ui version` — returns `0.1.55`
</verification>

<success_criteria>
- ev-ui v0.1.55 published with BallotIcon, CompassIcon, BranchIcon exports
- tierColors token exported with federal/state/local keys (all teal scale, WCAG AA text colors)
- CategorySection accepts optional `tier` prop
- PoliticianCard and PoliticianProfile default to `objectPosition: 'center 20%'` with overridable `imageFocalPoint` prop
- Build completes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/102-ev-ui-foundation-quick-wins/102-01-SUMMARY.md`
</output>
