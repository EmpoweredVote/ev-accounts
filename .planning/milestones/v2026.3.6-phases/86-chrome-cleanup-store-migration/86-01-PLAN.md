---
phase: 86-chrome-cleanup-store-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-readrank/src/store/useReadRankStore.ts
  - EV-readrank/src/utils/matchingAlgorithm.ts
autonomous: true
requirements: [FLOW-06]

must_haves:
  truths:
    - "A returning user with old localStorage state (phase: 'ranking') lands on the hub without errors"
    - "TypeScript builds cleanly with no references to the old Phase union value 'ranking'"
    - "partialize persists only phase, currentIssueId, and issueProgress"
    - "matchingAlgorithm scores by rank position only — no badge bonuses"
  artifacts:
    - path: "EV-readrank/src/store/useReadRankStore.ts"
      provides: "v2 Zustand store with clean-reset migration"
      contains: "version: 2"
    - path: "EV-readrank/src/utils/matchingAlgorithm.ts"
      provides: "Rank-only scoring algorithm"
  key_links:
    - from: "useReadRankStore.ts persist config"
      to: "localStorage ev_readrank"
      via: "version: 2 migrate function resets old state to initial"
      pattern: "version:\\s*2"
---

<objective>
Migrate the Zustand store to v2 with a clean-reset migration, remove all badge system types and actions, strip legacy flat state fields, and convert matchingAlgorithm to rank-only scoring.

Purpose: This is the foundation for all Phase 86 work. The store type changes define the new shape that all consumers must conform to. Doing this first means TypeScript errors will guide the cleanup in Plan 02.

Output: v2 store with clean types, rank-only scoring algorithm, TypeScript build will have errors in consumers (expected — Plan 02 fixes those).
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-chrome-cleanup-store-migration/86-CONTEXT.md
@.planning/phases/86-chrome-cleanup-store-migration/86-RESEARCH.md
@EV-readrank/src/store/useReadRankStore.ts
@EV-readrank/src/utils/matchingAlgorithm.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Zustand store to v2 — remove badge system, legacy flat state, and 'ranking' phase</name>
  <files>EV-readrank/src/store/useReadRankStore.ts</files>
  <action>
  Read the full store file first. Then make these changes:

  **Type changes:**
  - Remove `BadgeType` and `BadgeAssignment` type/interface definitions entirely
  - Update `Phase` type: remove `'ranking'` — becomes `'hub' | 'evaluation' | 'results'`
  - Update `IssueProgress.phase`: remove `'ranking'` — becomes `'evaluation' | 'results'`
  - Remove `badgeAssignments` from `IssueProgress` interface
  - Remove ALL legacy flat fields from `ReadRankState` interface: `issueTitle`, `questionText`, `topicId`, flat `quotesToEvaluate`, `currentQuoteIndex`, `agreedQuotes`, `disagreedQuotes`, `rankedQuotes`, `badgeAssignments`, `candidateMatches`
  - Remove legacy method signatures from `ReadRankState`: `setQuotes`, `setIssueInfo`, `assignBadge`, `clearBadge`

  **Initial state changes:**
  - Remove all legacy flat fields from `initialState` object
  - Remove `badgeAssignments` from `createEmptyIssueProgress`

  **Action implementation changes:**
  - Delete `assignBadge` and `clearBadge` action implementations
  - Delete `setQuotes` and `setIssueInfo` action implementations
  - Update `selectIssue`: remove all legacy flat state writes (the writes that mirror data to flat fields like `state.agreedQuotes = ...`). Keep only the `issueProgress[issueId]` writes.
  - Update `nextQuote`: remove the `phase: 'ranking'` transition. When `nextIndex >= quotesToEvaluate.length`, do NOT auto-transition — just cap the index. Let `EvaluationPhase.handleComplete` handle explicit phase transitions. Remove any flat field writes.
  - Update `agreeWithQuote`, `disagreeWithQuote`, `rankQuote`, `setRankedQuotes`, `reorderAgreedQuotes`: remove any flat field mirror writes if present. Keep only `issueProgress[issueId]` updates.
  - Update `setCandidateMatches`: remove flat field write if present.

  **Persist config changes:**
  - Bump `version: 1` to `version: 2`
  - Replace `migrate` function body: ignore `persistedState` and `_version`, return `{ phase: 'hub', currentIssueId: null, issueProgress: {} }` — this causes any old v1 state to be wiped clean
  - Update `partialize` to return only `{ phase: state.phase, currentIssueId: state.currentIssueId, issueProgress: state.issueProgress }` — remove all legacy field references

  NOTE: After this task, consumers that read legacy flat fields will have TypeScript errors. That is expected and intentional — Plan 02 fixes them.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>Store is at version 2 with clean-reset migrate. Badge types/actions gone. Legacy flat fields gone. Phase union is 'hub' | 'evaluation' | 'results'. partialize returns only 3 fields. TypeScript errors exist only in consumer files (not in the store itself).</done>
</task>

<task type="auto">
  <name>Task 2: Convert matchingAlgorithm to rank-only scoring</name>
  <files>EV-readrank/src/utils/matchingAlgorithm.ts</files>
  <action>
  Read the full file first. Then make these changes:

  - Remove `import { BadgeAssignment }` (or wherever BadgeType/BadgeAssignment is imported from)
  - Remove `BADGE_POINTS` constant
  - Remove `badgeAssignments` parameter from `calculateAlignment` function signature
  - Remove `getPointsForQuote` helper function (badge-based scoring)
  - Remove `badge` field from `candidateQuoteMatches` entries
  - Rewrite scoring to use rank position: `points = totalQuotes - index` where index 0 = rank 1 = highest points. This gives a simple linear weighting where rank 1 gets the most points.
  - Update `maxPossiblePoints` to use triangular sum: `totalQuotes * (totalQuotes + 1) / 2`
  - Remove `badgeAssignments` parameter from `fetchMatchingResults` if it passes badge data to `calculateAlignment`
  - Update the return type of quote match entries to exclude `badge` field

  The scoring formula: for N ranked quotes, rank 1 gets N points, rank 2 gets N-1, ..., rank N gets 1.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npx tsc --noEmit 2>&1 | grep -c "matchingAlgorithm" || echo "0 errors in matchingAlgorithm"</automated>
  </verify>
  <done>matchingAlgorithm.ts has no badge references. Scoring is rank-position-only. No TypeScript errors within the file itself (caller errors in ResultsPhase expected — fixed in Plan 02).</done>
</task>

</tasks>

<verification>
- `cd EV-readrank && npx tsc --noEmit 2>&1 | head -80` — store and matchingAlgorithm compile; errors only in consumer components
- `grep -r "BadgeType\|BadgeAssignment\|assignBadge\|clearBadge\|BADGE_POINTS" EV-readrank/src/store/ EV-readrank/src/utils/matchingAlgorithm.ts` — returns nothing
- `grep "version:" EV-readrank/src/store/useReadRankStore.ts` — shows `version: 2`
- `grep "'ranking'" EV-readrank/src/store/useReadRankStore.ts` — returns nothing
</verification>

<success_criteria>
- Store version is 2 with clean-reset migrate function
- Phase type is 'hub' | 'evaluation' | 'results' (no 'ranking')
- All badge types, interfaces, and actions are removed from store
- All legacy flat fields removed from store interface and initial state
- partialize returns only { phase, currentIssueId, issueProgress }
- matchingAlgorithm uses rank-position scoring with no badge bonuses
- No TypeScript errors in store or matchingAlgorithm files themselves
</success_criteria>

<output>
After completion, create `.planning/phases/86-chrome-cleanup-store-migration/86-01-SUMMARY.md`
</output>
