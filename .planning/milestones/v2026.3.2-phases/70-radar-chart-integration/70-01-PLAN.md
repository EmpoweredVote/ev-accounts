---
phase: 70-radar-chart-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/components/CompassCard.jsx
  - essentials/src/pages/Profile.jsx
autonomous: false
requirements:
  - CARD-03

must_haves:
  truths:
    - "Logged-in user with calibrated compass sees dual-overlay radar chart (coral user + blue politician) on profile page"
    - "Guest user with calibrated compass sees dual-overlay radar chart on profile page"
    - "Only intersection topics (both user AND politician have answers) appear as spokes"
    - "Chart capped at 8 spokes maximum"
    - "Zero topic overlap shows CTA to add more topics (link to CompassV2)"
    - "Legend above chart shows coral dot + You and blue dot + Position LastName"
    - "Chart is ~300px on desktop, scales responsively on mobile"
  artifacts:
    - path: "essentials/src/components/CompassCard.jsx"
      provides: "RadarChartCore dual-overlay rendering in left zone"
      contains: "RadarChartCore"
    - path: "essentials/src/pages/Profile.jsx"
      provides: "politicianTitle prop passed to CompassCard"
      contains: "politicianTitle"
  key_links:
    - from: "essentials/src/components/CompassCard.jsx"
      to: "@chrisandrewsedu/ev-ui RadarChartCore"
      via: "import and render"
      pattern: "RadarChartCore"
    - from: "essentials/src/components/CompassCard.jsx"
      to: "essentials/src/lib/compass.js"
      via: "fetchPoliticianAnswers + buildAnswerMapByShortTitle"
      pattern: "fetchPoliticianAnswers|buildAnswerMapByShortTitle"
    - from: "essentials/src/components/CompassCard.jsx"
      to: "essentials/src/contexts/CompassContext.jsx"
      via: "useCompass hook for userAnswers, selectedTopics, allTopics, invertedSpokes"
      pattern: "useCompass"
---

<objective>
Wire RadarChartCore dual-overlay into the CompassCard left zone on Essentials profile pages. The user's compass (coral polygon) and the politician's stances (blue polygon) render on the same radar chart, with a legend, responsive sizing, and zero-overlap fallback.

Purpose: This is the visual centerpiece of the Compass on Profiles feature -- users see at a glance how their political positions compare to their elected officials.
Output: Working dual-overlay radar chart in CompassCard on profile pages.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-radar-chart-integration/70-CONTEXT.md

@essentials/src/components/CompassCard.jsx
@essentials/src/components/CompassPreview.jsx
@essentials/src/lib/compass.js
@essentials/src/contexts/CompassContext.jsx
@essentials/src/pages/Profile.jsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From essentials/src/lib/compass.js:
```javascript
export async function fetchPoliticianAnswers(politicianId)
// Returns [{ topic_id, value }, ...]

export function buildAnswerMapByShortTitle(allTopics, allAnswers, allowedShorts)
// Returns { topicsFiltered: Topic[], answersByShort: { [short_title]: number } }
```

From essentials/src/contexts/CompassContext.jsx (via useCompass()):
```javascript
{
  userAnswers,           // [{ topic_id, value, write_in_text }]
  selectedTopics,        // [uuid, ...]
  allTopics,             // [{ id, short_title, stances, ... }]
  invertedSpokes,        // { [short_title]: true }
  politicianIdsWithStances, // Set<string>
  compassLoading,        // boolean
  isLoggedIn,            // boolean
}
```

From @chrisandrewsedu/ev-ui RadarChartCore props:
```javascript
<RadarChartCore
  topics={topicsFiltered}     // Topic[] — defines spoke labels
  data={userAnswerMap}        // { [short_title]: number } — coral polygon
  compareData={polAnswerMap}  // { [short_title]: number } — blue polygon
  invertedSpokes={invertedSpokes} // { [short_title]: true }
  size={300}                  // pixel size
  labelFontSize={10}          // spoke label font size
  padding={45}                // chart padding
  labelOffset={14}            // label distance from spoke end
/>
```

From Profile.jsx politician object:
```javascript
pol.office_title  // e.g., "U.S. Senator", "Council Member"
pol.first_name    // e.g., "Todd"
pol.last_name     // e.g., "Young"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RadarChartCore into CompassCard with data flow, legend, and zero-overlap fallback</name>
  <files>essentials/src/components/CompassCard.jsx, essentials/src/pages/Profile.jsx</files>
  <action>
**Profile.jsx change (1 line):** Add `politicianTitle={pol.office_title}` prop to the CompassCard component alongside the existing `politicianId` and `politicianName` props.

**CompassCard.jsx changes:** Replace the skeleton left zone with a working RadarChartCore dual-overlay chart. Follow CompassPreview.jsx (lines 133-378) as the reference implementation, adapted for inline card rendering instead of popover.

1. **Add imports:**
   - `{ useState, useEffect }` from react
   - `{ RadarChartCore }` from `@chrisandrewsedu/ev-ui`
   - `{ fetchPoliticianAnswers, buildAnswerMapByShortTitle }` from `../lib/compass`

2. **Accept new prop:** `politicianTitle` (string, e.g., "U.S. Senator")

3. **Pull additional context from useCompass():** `selectedTopics`, `allTopics`, `invertedSpokes` (already has `userAnswers`, `politicianIdsWithStances`, `compassLoading`)

4. **Fetch politician answers:** Add a useEffect that fetches politician answers via `fetchPoliticianAnswers(politicianId)` with cleanup (cancelled flag pattern). Store in local state `polAnswers`. Track `polLoading` state.

5. **Build chart data** (follow CompassPreview pattern exactly):
   - Compute intersection: only topics where BOTH user AND politician have answers
   - Use `selectedTopics` order if available, otherwise fall back to politician's answered topic order
   - Filter to intersection topics, cap at `MAX_SPOKES = 8`
   - Call `buildAnswerMapByShortTitle()` twice: once for politician answers, once for user answers, using the intersected `allowedShorts`
   - Track `hasData = topics.length > 0`

6. **Replace left zone skeleton** with three states:
   - **Loading:** Show a spinner (same style as CompassPreview line 261-281) while `polLoading` is true
   - **Zero overlap (hasUserCompass && !hasData && !polLoading):** Show CTA similar to the existing no-compass CTA but with text like "Add more topics to see how you compare with [politicianName]" and a "Take the Quiz" link using the existing `ctaHref`. Use the same greyed compass SVG icon pattern.
   - **Chart ready (hasUserCompass && hasData && !polLoading):** Render legend + RadarChartCore

7. **Legend (above chart):** Position above the chart inside the left column.
   - Format: `<span style coral dot/color>You</span>` and `<span style blue dot/color>[Position] [Last Name]</span>`
   - Build legend name: extract position from `politicianTitle` (e.g., "U.S. Senator") and combine with last name from `politicianName.split(' ').pop()`. If `politicianTitle` is empty/missing, fall back to `politicianName`.
   - Use coral (#ff5740) and blue (#59b0c4) color coding consistent with CompassPreview (line 239)
   - Font size ~12px, subtle gray text for connecting words

8. **RadarChartCore props:**
   - `topics={topicsFiltered}` (intersection topics array)
   - `data={userData}` (user answer map — coral polygon)
   - `compareData={polData}` (politician answer map — blue polygon)
   - `invertedSpokes={invertedSpokes}` (carry over user's existing inversions, display-only per user decision)
   - `size={300}` on desktop
   - `labelFontSize={10}` (tuned for 300px chart)
   - `padding={45}` (slightly more than CompassPreview's 40 since chart is larger)
   - `labelOffset={14}` (proportional to larger size)

9. **Responsive sizing:** Wrap RadarChartCore in a container div. Use a simple approach: render at size={300} and let the container constrain width on mobile via `max-width: 100%` and `overflow: hidden` on the wrapper. The SVG inside RadarChartCore will scale naturally. Center the chart horizontally in its column.

10. **No border/frame** around chart area — blends into the white card background per user decision.

11. **Keep the right zone skeleton placeholder unchanged** — Phase 71 fills it.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CompassCard renders RadarChartCore with dual-overlay (coral user + blue politician) for politicians with stances when user has compass data
    - Legend above chart shows "You" (coral) and "[Position] [Last Name]" (blue)
    - Only intersection topics appear as spokes, capped at 8
    - Zero overlap shows CTA to take the quiz
    - Loading spinner shown while politician answers fetch
    - Chart is ~300px, centered, responsive on mobile
    - Right zone skeleton placeholder preserved for Phase 71
    - Build passes with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify radar chart visual integration on profile pages</name>
  <action>
Human verifies the dual-overlay radar chart renders correctly on politician profile pages.

**What was built:** Dual-overlay radar chart on politician profile pages showing user's compass (coral) vs politician's stances (blue), with legend and responsive layout.

**How to verify:**
1. Run `cd essentials && npm run dev` to start the dev server
2. Navigate to a politician profile page for a politician WITH compass stances (e.g., one of the 23 politicians with stance data)
3. If you have compass data (logged in or guest with cached data), verify:
   - Dual-overlay radar chart appears in the left column of the Compass card
   - Legend above chart shows coral "You" and blue "[Position] [Last Name]"
   - Spokes show only topics where both you and the politician have answers
   - Chart animates on first render (react-spring)
   - Right column still shows skeleton placeholder lines
4. Resize browser to mobile width -- chart should scale down gracefully
5. Check a politician WITHOUT compass stances -- CompassCard should not render (same as before)
6. If you have no compass data, verify the CTA "Take the Quiz" link appears instead of the chart
  </action>
  <done>User confirms radar chart displays correctly with dual overlay, legend, responsive sizing, and proper gating behavior</done>
</task>

</tasks>

<verification>
- `cd essentials && npm run build` passes with no errors
- CompassCard imports and renders RadarChartCore from ev-ui
- Profile.jsx passes politicianTitle prop to CompassCard
- Chart shows dual polygons for intersection topics only
- Legend correctly formats Position + Last Name
</verification>

<success_criteria>
- Logged-in or guest user with calibrated compass sees dual-overlay radar chart on profile pages for politicians with stances
- Chart uses intersection-only topics, capped at 8 spokes
- Legend clearly identifies which polygon is the user vs the politician
- Zero-overlap state shows CTA fallback
- No visual regressions to existing profile page layout
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/70-radar-chart-integration/70-01-SUMMARY.md`
</output>
