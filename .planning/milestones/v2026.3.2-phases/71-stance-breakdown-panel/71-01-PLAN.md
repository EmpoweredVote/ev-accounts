---
phase: 71-stance-breakdown-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/components/Favicon.jsx
  - essentials/src/components/StanceAccordion.jsx
  - essentials/src/components/CompassCard.jsx
autonomous: true
requirements:
  - CARD-04
  - CARD-05
  - CARD-06

must_haves:
  truths:
    - "Each topic the politician has stances on appears as a collapsed row in the right-side panel"
    - "Each row shows the politician's stance position label (e.g., Strongly Support)"
    - "Tapping a row expands it to show reasoning text and numbered source links with favicons"
    - "Only one row can be expanded at a time (true accordion)"
    - "Source links open in a new tab"
    - "The panel is readable on both desktop (right column) and mobile (stacked below chart)"
  artifacts:
    - path: "essentials/src/components/Favicon.jsx"
      provides: "Google favicon service component"
      min_lines: 10
    - path: "essentials/src/components/StanceAccordion.jsx"
      provides: "Accordion topic list with lazy context fetching"
      min_lines: 80
      exports: ["default"]
    - path: "essentials/src/components/CompassCard.jsx"
      provides: "Updated card with StanceAccordion replacing skeleton"
      contains: "StanceAccordion"
  key_links:
    - from: "essentials/src/components/StanceAccordion.jsx"
      to: "/compass/politicians/{pid}/{tid}/context"
      via: "fetch on accordion expand"
      pattern: "compass/politicians.*context"
    - from: "essentials/src/components/CompassCard.jsx"
      to: "essentials/src/components/StanceAccordion.jsx"
      via: "import and render in right zone"
      pattern: "import.*StanceAccordion"
    - from: "essentials/src/components/StanceAccordion.jsx"
      to: "essentials/src/components/Favicon.jsx"
      via: "import for source link display"
      pattern: "import.*Favicon"
---

<objective>
Build the right-side stance breakdown panel for the CompassCard, replacing the Phase 69 skeleton placeholder with an accordion-style topic list showing politician stance labels, reasoning text, and source links.

Purpose: This completes the CompassCard — the radar chart (left) provides the visual "what", and the stance accordion (right) provides the textual "why" with source attribution.
Output: StanceAccordion component, ported Favicon utility, updated CompassCard wiring.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-stance-breakdown-panel/71-CONTEXT.md

@essentials/src/components/CompassCard.jsx
@essentials/src/lib/compass.js
@essentials/src/contexts/CompassContext.jsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From essentials/src/components/CompassCard.jsx (lines 74-132):
- `topicsFiltered` — array of topic objects with { id, short_title, stances: [{ text }] }
- `polAnswers` — array of { topic_id, value } for politician answers
- `polData` — { [short_title]: value } map
- `politicianId` — UUID string prop
- Right zone skeleton at lines 290-296 — replace with StanceAccordion

From CompassV2/src/components/ComparePanel.jsx (lines 47-73):
- Context fetch pattern: `GET /compass/politicians/{politician.id}/{selectedTopicID}/context`
- Returns `{ reasoning: string, sources: string[] }`
- 404 → empty reasoning/sources

From CompassV2/src/components/ComparePanel.jsx (lines 313-323):
```javascript
function getDisplayUrl(url) {
  try {
    const u = new URL(url);
    let host = u.hostname.replace(/^www\./, "");
    const path = u.pathname === "/" ? "" : u.pathname;
    const display = host + path;
    return display.length > 60 ? display.slice(0, 57) + "..." : display;
  } catch {
    return url.length > 60 ? url.slice(0, 57) + "..." : url;
  }
}
```

From CompassV2/src/components/Favicon.jsx:
```javascript
function Favicon({ url, size = 32 }) {
  try {
    const domain = new URL(url).hostname;
    return (
      <img
        src={`https://www.google.com/s2/favicons?sz=${size}&domain=${domain}`}
        alt="" width={size} height={size}
        style={{ verticalAlign: "middle" }}
      />
    );
  } catch {
    return <div>...</div>;
  }
}
```

From essentials/src/contexts/CompassContext.jsx:
- `allTopics` — full topic objects with stances array per topic
- `useCompass()` hook provides all compass state

Stance label resolution pattern (from CONTEXT.md):
- `topic.stances[answer.value - 1].text` maps answer value to label like "Strongly Support"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Favicon component and StanceAccordion with lazy context fetching</name>
  <files>essentials/src/components/Favicon.jsx, essentials/src/components/StanceAccordion.jsx</files>
  <action>
**Favicon.jsx** — Port from CompassV2/src/components/Favicon.jsx. Identical logic: Google favicon service `https://www.google.com/s2/favicons?sz={size}&domain={domain}`. Default size=16 (not 32, since these are inline source links). Fallback: small globe SVG icon sized appropriately (16px, not the 64px in CompassV2's fallback).

**StanceAccordion.jsx** — New component. Props: `{ topics, polAnswers, politicianId, allTopics }`.

Structure:
- Receives `topics` (the filtered intersection topics from CompassCard's `topicsFiltered`) and `polAnswers` (array of `{ topic_id, value }`).
- Renders a vertical list of clickable rows, one per topic.
- Each collapsed row shows: topic `short_title` + politician stance label. Resolve stance label via: find the topic in `allTopics` by ID, then `topic.stances[polAnswer.value - 1].text`. If value is out of range or missing, show "No position".
- True accordion: `expandedTopicId` state (single ID or null). Clicking a row toggles it — if already open, close; if different, switch. All start collapsed.
- Chevron indicator on each row (right side): rotates 90 degrees when expanded (CSS transition).

Expanded content (shown below the row when expanded):
- Lazy fetch: on expand, check a `contextCache` Map (React useRef) keyed by topic ID. If cached, display immediately. If not, fetch `GET {VITE_API_URL}/compass/politicians/{politicianId}/{topicId}/context` with `credentials: "include"`. Store result in cache. Handle 404 as empty reasoning/sources.
- Show a small spinner while fetching (same style as CompassCard's loading spinner but smaller, ~20px).
- Display reasoning text first (whitespace-pre-wrap, same text styling as ComparePanel).
- Below reasoning, "References" heading + numbered `<ol>` with Favicon + getDisplayUrl + truncated URL. Each link opens in new tab (`target="_blank" rel="noreferrer"`).
- Include `getDisplayUrl` as a local helper function (same implementation as ComparePanel lines 313-323).
- When no reasoning AND no sources: show "No detailed reasoning available for this topic." in muted italic text.

Styling:
- Rows: `border-b border-neutral-100` for subtle separators. Padding `py-3 px-2`. Cursor pointer.
- Stance label: muted color (`text-neutral-500`), smaller than topic name.
- Expanded area: `px-2 pb-4` with smooth height transition (use CSS `max-height` transition or grid-template-rows trick for animation — Claude's discretion).
- Use Manrope font family consistent with CompassCard.
- Mobile: full width, natural flow. No constrained scroll area — expanding pushes content down.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Favicon.jsx exists and renders Google favicon images. StanceAccordion.jsx renders topic rows with stance labels, expands on click with lazy-fetched reasoning and source links, true accordion behavior (one open at a time), context caching, and responsive layout.</done>
</task>

<task type="auto">
  <name>Task 2: Wire StanceAccordion into CompassCard replacing skeleton placeholder</name>
  <files>essentials/src/components/CompassCard.jsx</files>
  <action>
In CompassCard.jsx, replace the right-zone skeleton placeholder (lines 290-296, the div with animate-pulse bars) with the StanceAccordion component.

Changes:
1. Add import: `import StanceAccordion from './StanceAccordion';`
2. Replace lines 290-296 (the `<div className="flex flex-col justify-center gap-3">` with pulse bars) with:
   ```
   <StanceAccordion
     topics={topicsFiltered}
     polAnswers={polAnswers}
     politicianId={politicianId}
     allTopics={allTopics}
   />
   ```
3. The StanceAccordion should only render when `hasData` is true (it's already inside the `hasUserCompass` conditional and siblings with the chart). When `!hasData` (zero overlap), the accordion area should not appear — the existing zero-overlap CTA handles that case.
4. Also handle the `polLoading` state: while politician answers are loading, keep a minimal loading indicator in the right zone (or let StanceAccordion gracefully show empty). Since StanceAccordion receives `polAnswers` which is null during loading, add a guard: if `polLoading || !polAnswers`, render a simple spinner or nothing in the right zone.

The grid layout (`grid grid-cols-1 md:grid-cols-2 gap-6`) already exists — StanceAccordion renders in the right column on desktop and stacks below the chart on mobile. No layout changes needed.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>CompassCard right zone shows StanceAccordion with topic rows instead of skeleton pulse bars. Accordion displays stance labels, expands to show reasoning and sources on tap. Layout works on desktop (side-by-side) and mobile (stacked).</done>
</task>

</tasks>

<verification>
1. `cd essentials && npx vite build` — production build succeeds with no errors
2. Manual check: visit a politician profile page for a politician with compass stances (e.g., one of the 23 politicians with 455 stance rows). Verify:
   - Right side shows topic rows with stance labels (not skeleton bars)
   - Tapping a topic expands it, shows reasoning + numbered source links with favicons
   - Tapping another topic closes the first and opens the new one
   - Source links open in new tab
   - On mobile viewport: accordion stacks below chart
</verification>

<success_criteria>
- Skeleton placeholder fully replaced with functional StanceAccordion
- Each topic row shows politician stance label (CARD-04, CARD-05)
- Expanded rows show reasoning text and source links with favicons (CARD-06)
- True accordion behavior (one expanded at a time)
- Lazy context fetching with client-side caching
- Responsive: desktop side-by-side, mobile stacked
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/71-stance-breakdown-panel/71-01-SUMMARY.md`
</output>
