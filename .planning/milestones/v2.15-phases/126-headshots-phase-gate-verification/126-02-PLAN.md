---
phase: 126-headshots-phase-gate-verification
plan: 02
type: execute
wave: 2
depends_on: [126-01]
files_modified:
  - backend/scripts/verify-phase-125-126.sql
autonomous: true
requirements: [USHR-05]

must_haves:
  truths:
    - "A consolidated verify-phase-125-126.sql exists with labeled assertions for all of USHR-01..05"
    - "Coverage assertion: the ONLY unlinked NATIONAL_LOWER districts are the known 4 (1198 dup DC + FL-20/GA-13/TX-23 vacancies); every other district has a linked rep"
    - "No-orphan assertion: zero batch politicians (external_id -56999..-1000) with office_id NULL"
    - "Party-normalization assertion: zero batch reps with party='Democrat'"
    - "Photo assertion: batch reps with photo_origin_url NOT NULL >= 290 (remainder documented)"
    - "Path 0 spot checks across >= 5 states including one at-large state and DC resolve to the correct rep via the (tiger_geoid, district_type='NATIONAL_LOWER') join"
    - "All assertions pass when run against production"
  artifacts:
    - path: "backend/scripts/verify-phase-125-126.sql"
      provides: "Permanent audit record — labeled DO $$ ... RAISE EXCEPTION assertions for USHR-01..05 + Path 0 spot checks"
  key_links:
    - from: "connect.user_districts (layer='us_house')"
      to: "essentials.districts (tiger_geoid, district_type='NATIONAL_LOWER') -> offices -> politicians"
      via: "Path 0 join asserted in verify-phase-125-126.sql"
      pattern: "tiger_geoid.*NATIONAL_LOWER"
---

<objective>
Produce the permanent phase-gate audit script for v2.15 and prove all five USHR
requirements hold against production. This closes Phase 126 and milestone v2.15.

Output:
  - backend/scripts/verify-phase-125-126.sql (labeled assertions; follows verify-phase-120-124.sql pattern)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/125-national-house-rep-ingestion/125-02-SUMMARY.md
@.planning/phases/126-headshots-phase-gate-verification/126-01-PLAN.md

Pattern: follow backend/scripts/verify-phase-120-124.sql — a sequence of labeled
`DO $$ BEGIN IF NOT (...) THEN RAISE EXCEPTION ...; END IF; END $$;` blocks, one per
assertion, each printing a RAISE NOTICE on pass. Run via psql or Supabase MCP execute_sql.

Known intentional unlinked NATIONAL_LOWER districts (must remain excluded): 1198 (dup DC
delegate, already seeded under dc-prefixed geoids), 1220 (FL-20), 1313 (GA-13), 4823
(TX-23) — the last three are genuine current vacancies.
</context>

<task id="1" type="execute">
<title>Write verify-phase-125-126.sql with labeled USHR assertions</title>
<read_first>
- backend/scripts/verify-phase-120-124.sql (assertion style + RAISE pattern to mirror)
- .planning/phases/125-national-house-rep-ingestion/125-02-SUMMARY.md (expected numbers)
</read_first>
<action>
Create backend/scripts/verify-phase-125-126.sql with these labeled assertions:
1. USHR-01 coverage: assert COUNT of unlinked NATIONAL_LOWER districts = 4 AND those
   tiger_geoids are exactly ('1198','1220','1313','4823'); assert linked-rep districts
   count is consistent (>= 433).
2. USHR-01 batch size: assert COUNT(politicians WHERE external_id BETWEEN -56999 AND -1000) = 299.
3. USHR-02 no orphans: assert 0 batch politicians with office_id IS NULL.
4. USHR-02 untouched: assert CA(06)=53, VA(51)=11, MA(25)=9 linked NATIONAL_LOWER reps.
5. USHR-03 party normalization: assert 0 batch reps with party='Democrat'; assert all
   batch parties IN ('Democratic','Republican','Independent').
6. USHR-04 photos: assert COUNT(batch reps with photo_origin_url NOT NULL) >= 290 and all
   non-null batch photo URLs LIKE 'https://unitedstates.github.io/images/congress/225x275/%'.
7. USHR-05 Path 0 spot checks: for >= 5 sample tiger_geoids across different states
   INCLUDING one at-large state (e.g. '5600' WY) and DC, assert the
   (tiger_geoid, district_type='NATIONAL_LOWER') -> offices -> politicians join returns
   exactly one rep with the expected name. Use stable, well-known members (e.g. WY-AL
   Hageman '5600', NY-14 Ocasio-Cortez '3614', TX-36 Babin '4836', OH-5 Latta '3905',
   IL-1 Jackson '1701'); for DC assert the delegate resolves via the dc-prefixed geoid.
Each assertion: RAISE EXCEPTION on failure with the actual value; RAISE NOTICE on pass.
</action>
<acceptance_criteria>
- backend/scripts/verify-phase-125-126.sql exists with >= 7 labeled assertion blocks covering USHR-01..05
- Each assertion uses RAISE EXCEPTION on failure (not a silent SELECT) and RAISE NOTICE on pass
- Path 0 block covers >= 5 states including an at-large state and DC
</acceptance_criteria>
</task>

<task id="2" type="verify">
<title>Run the gate against production; all assertions pass</title>
<read_first>
- backend/scripts/verify-phase-125-126.sql
</read_first>
<action>
Run verify-phase-125-126.sql against production (psql -v ON_ERROR_STOP=1, or Supabase MCP
execute_sql). Confirm every assertion passes (no RAISE EXCEPTION). Capture the NOTICE
output in 126-02-SUMMARY.md. If any assertion fails, investigate and fix the data or the
assertion (a wrong assertion is a bug; a real data gap must be resolved before closing).
</action>
<acceptance_criteria>
- Running verify-phase-125-126.sql completes with zero exceptions (all assertions pass)
- NOTICE output for all USHR-01..05 assertions captured in 126-02-SUMMARY.md
- Any deviation (e.g. an additional vacancy seated since Phase 125) is documented, not silently absorbed
</acceptance_criteria>
</task>

<verification>
- verify-phase-125-126.sql runs clean (all USHR-01..05 assertions pass) against production
- Path 0 resolves correct reps for >= 5 states incl at-large + DC
- Phase 126 + milestone v2.15 ready to close
</verification>
