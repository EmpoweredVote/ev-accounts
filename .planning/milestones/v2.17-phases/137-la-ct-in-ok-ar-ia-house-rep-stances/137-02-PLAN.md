---
phase: 137-la-ct-in-ok-ar-ia-house-rep-stances
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-19-ct-house.csv
  - backend/data/stance-research/ct-house/_TOPIC_SCALE.txt
  - backend/data/stance-research/ct-house/_merge.ts
  - backend/data/stance-research/ct-house/_push.ts
autonomous: false
requirements: [USHS-11]

must_haves:
  truths:
    - "Each of the 5 CT House reps (external_id -9001..-9005) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation; no isidewith-only rows kept"
    - "Live topics fetched fresh; city + judicial-* topics skipped (25 federal topics in scope)"
    - "In-scope reps reach exact counts; verified via per-scope external_id counts (not the global counter)"
  artifacts:
    - path: "backend/data/stance-research/2026-06-19-ct-house.csv"
      provides: "Reviewable stance research CSV for CT House reps (5 reps, CT-1..CT-5)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for all 5 Connecticut US House reps
(CT-1..CT-5, external_id -9001..-9005), using the research-stances skill, following the
validated Phase 127-136 playbook. Concurrency cap = 3. WebFetch-only.

Purpose: Sixth wave of v2.17 (USHS-11, LA + CT + IN + OK + AR + IA). Each rep currently has 0 stances;
pure scale-out of the v2.16 pipeline. Output: CSV of record committed; stances + context + quotes
pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\136-wi-al-sc-ky-house-rep-stances\136-08-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (CT House, all Democrats):

| external_id | name | district |
|-------------|------|----------|
| -9001 | John Larson | CT-1 |
| -9002 | Joe Courtney | CT-2 |
| -9003 | Rosa DeLauro | CT-3 |
| -9004 | James Himes | CT-4 |
| -9005 | Jahana Hayes | CT-5 |

All currently have 0 stances. Resolve each politician_id by external_id->UUID at run time.
Document the actual record only, never infer from party.

KEY RULES (validated Phase 127-136 playbook):
- **25 federal topics** = 44 live minus 11 city-level + all judicial-*. Fetch fresh; write the scale to
  `backend/data/stance-research/ct-house/_TOPIC_SCALE.txt` (or copy ky-house-a/_TOPIC_SCALE.txt); each agent Reads it.
- **Concurrency: up to 3 agents**. WebFetch ONLY — never WebSearch or Playwright.
- **Per-rep CSV -> merge + RFC-4180-validate** (csv-parse/sync). CSV quote-repair: collapse `""""`->`"""`
  ONLY (4->3); NEVER 3->2. Use `replace(/"""""+/g,'"""').replace(/""""/g,'"""')` + relax_quotes (sed-clone ky-house-a/_merge.ts).
- **Stray-trailing-quote artifact**: agents may append a lone trailing `"` to the empty final field on every row
  (csv-parse "Invalid Closing Quote"). Fix per-file with `sed -i 's/,"$/,/'` (a line ending `,"` is the artifact).
- **isidewith.com is BELOW the bar** — DROP isidewith-only rows. AFA/candidate questionnaires OK.
- **Calibration discipline**: DROP over-read `same-sex-marriage=5` rows whose only evidence is "opposes legalization" / a religious-exemption bill / Equality-Act opposition / group-alignment; KEEP only with a documented anti-recognition vote OR explicit pro-amendment + Obergefell opposition. DROP topic rows resting only on a vague "core value" listing.
- **Resolve politician_id by external_id->UUID map**, not by name.
- **Five-chairs framing**: match documented record to exact stance text; never infer from party.
  Lean on Ballotpedia, OnTheIssues, Wikipedia, LCV, VoteSmart. house.gov/congress.gov/govtrack 403 to WebFetch.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research CT House (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\136-wi-al-sc-ky-house-rep-stances\136-08-SUMMARY.md (latest validated playbook)
- backend/data/stance-research/ky-house-a/_merge.ts (merge+repair to sed-clone)
- backend/data/stance-research/ky-house-a/_TOPIC_SCALE.txt (current 25-topic scale to copy)
</read_first>
<action>
1. Ensure `backend/data/stance-research/ct-house/_TOPIC_SCALE.txt` holds the current 25 federal topics (copy ky-house-a/_TOPIC_SCALE.txt, or re-fetch fresh and drop the 11 city + judicial-* topics).
2. Dispatch `politician-stance-researcher` agents for the 5 CT reps (external_id -9001..-9005), up to 3 concurrent. Each prompt: five-chairs framing, Read `_TOPIC_SCALE.txt`, WebFetch ONLY, explicit RFC-4180 escaping rule, drop isidewith-only rows, write to `backend/data/stance-research/ct-house/<surname>.csv`, include external_id on every row.
3. Verify each agent's rows before dispatching the next wave; retry empty-output agents solo.
4. Merge the 5 per-rep CSVs into `backend/data/stance-research/2026-06-19-ct-house.csv` (sed-clone ky-house-a/_merge.ts: set OUT + IN_SCOPE=[-9001,-9002,-9003,-9004,-9005]); validate every row.
</action>
<acceptance_criteria>
- 2026-06-19-ct-house.csv exists with rows for the 5 reps (or documented honest-skips); merge parser reports 0 problems
- Every row has value (1-5) + reasoning + >=1 real http source_url; topic_key is one of the 25 fetched federal topics; no isidewith-only rows
- Each of CT-1..CT-5 accounted for: stances OR an honest-skip noted (no party-inferred values)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push CT House to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/ky-house-a/_push.ts (reusable external_id-keyed push)
- backend/data/stance-research/2026-06-19-ct-house.csv
</read_first>
<action>
Present the STEP 3 approval summary. Standing user decision this milestone: auto-push batches that merge 0-problems AND 0-unsourced (still show the summary), no per-batch approval pause. Push via the external_id-keyed `_push.ts` pattern (copy to `backend/data/stance-research/ct-house/_push.ts`), resolving politician_id by external_id->UUID + topic_id by topic_key, upserting inform.politician_answers + inform.politician_context (ON CONFLICT DO UPDATE on both), pushing quotes to essentials.quotes with Read & Rank + surname leak-check. `_push.ts` does NOT load dotenv — run with `set -a && source .env && set +a && node --import tsx .../_push.ts <csv>`. Commit the CSV (force-add past gitignore). Verify per-scope: CT reps (-9001..-9005) reach exact counts, unsourced-in-scope = 0. If MCP Supabase tokens expire mid-run, fall back to `node --import tsx` + `pool` from `backend/src/lib/db.ts` (load `dotenv/config` first) for verification queries.
</action>
<acceptance_criteria>
- Each CT rep has >=1 inform.politician_answers row OR a documented honest-skip
- Every new answer has a paired sourced context row (0 unsourced)
- Push reported counts with zero rollbacks; per-scope external_id counts confirm isolation
- CSV committed (force-added past gitignore)
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content lands in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT DO UPDATE; re-runs safe; single BEGIN/COMMIT with ROLLBACK on error |
| T-137-05 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check; surname-leaking quotes never selected |
| T-137-06 | Integrity (scope creep) | non-in-scope reps | mitigate | push keyed strictly to CT external_ids -9001..-9005; verified via per-scope counts |

No high-severity threats: read-only web research + an idempotent service-role upsert. No package installs.
</threat_model>

<verification>
- 5 CT reps each have sourced stances or a documented skip; every answer has paired sourced context
- No party-inferred stances; no isidewith-only rows; 25 federal topics; concurrency held at 3
- Per-scope CT counts (-9001..-9005) confirm writes isolated (no global-counter reliance)
</verification>

<success_criteria>
USHS-11 advanced: CT-1..CT-5 (5 reps) covered with sourced stances or honest-skips, all answers paired with sourced context, CSV committed.
</success_criteria>

<output>
Create `.planning/phases/137-la-ct-in-ok-ar-ia-house-rep-stances/137-02-SUMMARY.md` when done.
</output>
