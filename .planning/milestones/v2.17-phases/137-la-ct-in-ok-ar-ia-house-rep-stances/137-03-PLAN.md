---
phase: 137-la-ct-in-ok-ar-ia-house-rep-stances
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-19-in-house.csv
  - backend/data/stance-research/in-house/_TOPIC_SCALE.txt
  - backend/data/stance-research/in-house/_merge.ts
  - backend/data/stance-research/in-house/_push.ts
autonomous: false
requirements: [USHS-11]

must_haves:
  truths:
    - "Each of the 5 in-scope IN House reps (external_id -18001,-18002,-18003,-18005,-18006 — NON-CONTIGUOUS, skips -18004) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation; no isidewith-only rows kept"
    - "Live topics fetched fresh; city + judicial-* topics skipped (25 federal topics in scope)"
    - "In-scope reps reach exact counts; verified via per-scope external_id counts (not the global counter); IN-4 (-18004) is NOT touched"
  artifacts:
    - path: "backend/data/stance-research/2026-06-19-in-house.csv"
      provides: "Reviewable stance research CSV for in-scope IN House reps (5 reps, IN-1,2,3,5,6)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for the 5 in-scope Indiana US House reps
(IN-1, IN-2, IN-3, IN-5, IN-6 — external_id -18001, -18002, -18003, -18005, -18006), using the
research-stances skill, following the validated Phase 127-136 playbook. Concurrency cap = 3. WebFetch-only.

**CRITICAL — Indiana is NON-CONTIGUOUS.** In-scope IN = exactly {-18001, -18002, -18003, -18005, -18006}.
IN-4 (-18004) is NOT in scope (already has answers / unseeded) and must NOT be touched. The merge
IN_SCOPE set MUST list these 5 external_ids explicitly — do NOT use a -18001..-18006 contiguous range.

Purpose: Sixth wave of v2.17 (USHS-11, LA + CT + IN + OK + AR + IA). Each in-scope rep currently has 0 stances;
pure scale-out of the v2.16 pipeline. Output: CSV of record committed; stances + context + quotes
pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\136-wi-al-sc-ky-house-rep-stances\136-08-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (IN House — NON-CONTIGUOUS):

| external_id | name | district |
|-------------|------|----------|
| -18001 | Frank Mrvan (D) | IN-1 |
| -18002 | Rudy Yakym | IN-2 |
| -18003 | Marlin Stutzman | IN-3 |
| -18005 | Victoria Spartz | IN-5 |
| -18006 | Jefferson Shreve | IN-6 |

IN-4 (-18004) and IN-7/8/9 are NOT in scope. All 5 in-scope reps currently have 0 stances.
Resolve each politician_id by external_id->UUID at run time. Document the actual record only, never infer from party.

EXPECTED RECORD NOTES:
- Marlin Stutzman IN-3 RETURNED to the House in 2025 after prior 2010-2017 service — his older record is documentable.
- Jefferson Shreve IN-6 is a 2024 freshman — thin record, likely honest-partial. Document the actual record; honest-skip topics with no evidence. Never infer from party.

KEY RULES (validated Phase 127-136 playbook):
- **25 federal topics** = 44 live minus 11 city-level + all judicial-*. Fetch fresh; write the scale to
  `backend/data/stance-research/in-house/_TOPIC_SCALE.txt` (or copy ky-house-a/_TOPIC_SCALE.txt); each agent Reads it.
- **Concurrency: up to 3 agents**. WebFetch ONLY — never WebSearch or Playwright.
- **Per-rep CSV -> merge + RFC-4180-validate** (csv-parse/sync). CSV quote-repair: collapse `""""`->`"""`
  ONLY (4->3); NEVER 3->2. Use `replace(/"""""+/g,'"""').replace(/""""/g,'"""')` + relax_quotes (sed-clone ky-house-a/_merge.ts).
- **Stray-trailing-quote artifact**: agents may append a lone trailing `"` to the empty final field on every row
  (csv-parse "Invalid Closing Quote"). Fix per-file with `sed -i 's/,"$/,/'` (a line ending `,"` is the artifact).
- **isidewith.com is BELOW the bar** — DROP isidewith-only rows. AFA/candidate questionnaires OK.
- **Calibration discipline**: DROP over-read `same-sex-marriage=5` rows whose only evidence is "opposes legalization" / a religious-exemption bill / Equality-Act opposition / group-alignment; KEEP only with a documented anti-recognition vote OR explicit pro-amendment + Obergefell opposition. DROP topic rows resting only on a vague "core value" listing.
- **Resolve politician_id by external_id->UUID map**, not by name.
- **Five-chairs framing**: match documented record to exact stance text; never infer from party.
  Lean on Ballotpedia, OnTheIssues, Wikipedia, LCV, VoteSmart. house.gov/congress.gov/govtrack 403 to WebFetch.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research in-scope IN House (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\136-wi-al-sc-ky-house-rep-stances\136-08-SUMMARY.md (latest validated playbook)
- backend/data/stance-research/ky-house-a/_merge.ts (merge+repair to sed-clone)
- backend/data/stance-research/ky-house-a/_TOPIC_SCALE.txt (current 25-topic scale to copy)
</read_first>
<action>
1. Ensure `backend/data/stance-research/in-house/_TOPIC_SCALE.txt` holds the current 25 federal topics (copy ky-house-a/_TOPIC_SCALE.txt, or re-fetch fresh and drop the 11 city + judicial-* topics).
2. Dispatch `politician-stance-researcher` agents for the 5 in-scope IN reps (external_id -18001, -18002, -18003, -18005, -18006 — SKIP -18004), up to 3 concurrent. Each prompt: five-chairs framing, Read `_TOPIC_SCALE.txt`, WebFetch ONLY, explicit RFC-4180 escaping rule, drop isidewith-only rows, write to `backend/data/stance-research/in-house/<surname>.csv`, include external_id on every row.
3. Verify each agent's rows before dispatching the next wave; retry empty-output agents solo.
4. Merge the 5 per-rep CSVs into `backend/data/stance-research/2026-06-19-in-house.csv` (sed-clone ky-house-a/_merge.ts: set OUT + IN_SCOPE to the EXPLICIT NON-CONTIGUOUS list [-18001,-18002,-18003,-18005,-18006] — NOT a -18001..-18006 range); validate every row.
</action>
<acceptance_criteria>
- 2026-06-19-in-house.csv exists with rows for the 5 in-scope reps (or documented honest-skips); merge parser reports 0 problems
- The merge IN_SCOPE set is exactly [-18001,-18002,-18003,-18005,-18006]; NO rows for -18004 are present in the merged CSV
- Every row has value (1-5) + reasoning + >=1 real http source_url; topic_key is one of the 25 fetched federal topics; no isidewith-only rows
- Each of IN-1,2,3,5,6 accounted for: stances OR an honest-skip noted (no party-inferred values); Stutzman IN-3 documented from his actual record incl. prior service; Shreve IN-6 honest-partial if thin
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push in-scope IN House to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/ky-house-a/_push.ts (reusable external_id-keyed push)
- backend/data/stance-research/2026-06-19-in-house.csv
</read_first>
<action>
Present the STEP 3 approval summary. Standing user decision this milestone: auto-push batches that merge 0-problems AND 0-unsourced (still show the summary), no per-batch approval pause. Push via the external_id-keyed `_push.ts` pattern (copy to `backend/data/stance-research/in-house/_push.ts`), resolving politician_id by external_id->UUID + topic_id by topic_key, upserting inform.politician_answers + inform.politician_context (ON CONFLICT DO UPDATE on both), pushing quotes to essentials.quotes with Read & Rank + surname leak-check. `_push.ts` does NOT load dotenv — run with `set -a && source .env && set +a && node --import tsx .../_push.ts <csv>`. Commit the CSV (force-add past gitignore). Verify per-scope: the 5 in-scope IN reps (-18001,-18002,-18003,-18005,-18006) reach exact counts, unsourced-in-scope = 0, and -18004 has zero new answers. If MCP Supabase tokens expire mid-run, fall back to `node --import tsx` + `pool` from `backend/src/lib/db.ts` (load `dotenv/config` first) for verification queries.
</action>
<acceptance_criteria>
- Each in-scope IN rep has >=1 inform.politician_answers row OR a documented honest-skip
- IN-4 (-18004) received ZERO new answers (non-contiguous scope honored)
- Every new answer has a paired sourced context row (0 unsourced)
- Push reported counts with zero rollbacks; per-scope external_id counts confirm isolation
- CSV committed (force-added past gitignore)
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content lands in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-07 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT DO UPDATE; re-runs safe; single BEGIN/COMMIT with ROLLBACK on error |
| T-137-08 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check; surname-leaking quotes never selected |
| T-137-09 | Integrity (scope creep) | non-in-scope reps, esp. IN-4 (-18004) | mitigate | push keyed strictly to the explicit NON-CONTIGUOUS set [-18001,-18002,-18003,-18005,-18006]; -18004 excluded; verified via per-scope counts |

No high-severity threats: read-only web research + an idempotent service-role upsert. No package installs.
</threat_model>

<verification>
- 5 in-scope IN reps each have sourced stances or a documented skip; every answer has paired sourced context
- IN-4 (-18004) untouched; merge IN_SCOPE was the explicit non-contiguous list
- No party-inferred stances; no isidewith-only rows; 25 federal topics; concurrency held at 3
- Per-scope IN counts confirm writes isolated (no global-counter reliance)
</verification>

<success_criteria>
USHS-11 advanced: IN-1,2,3,5,6 (5 in-scope reps, non-contiguous) covered with sourced stances or honest-skips, all answers paired with sourced context, IN-4 untouched, CSV committed.
</success_criteria>

<output>
Create `.planning/phases/137-la-ct-in-ok-ar-ia-house-rep-stances/137-03-SUMMARY.md` when done.
</output>
