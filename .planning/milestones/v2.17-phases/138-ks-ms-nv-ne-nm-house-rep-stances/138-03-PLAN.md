---
phase: 138-ks-ms-nv-ne-nm-house-rep-stances
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-20-nv-house.csv
  - backend/data/stance-research/nv-house/_TOPIC_SCALE.txt
  - backend/data/stance-research/nv-house/_merge.ts
  - backend/data/stance-research/nv-house/_push.ts
autonomous: false
requirements: [USHS-12]

must_haves:
  truths:
    - "Each of the 4 NV House reps (external_id -32001..-32004) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation; no isidewith-only rows kept"
    - "Live topics fetched fresh; city + judicial-* topics skipped (25 federal topics in scope)"
    - "In-scope reps reach exact counts; verified via per-scope external_id counts (not the global counter)"
  artifacts:
    - path: "backend/data/stance-research/2026-06-20-nv-house.csv"
      provides: "Reviewable stance research CSV for NV House reps (4 reps, NV-1..NV-4)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for all 4 Nevada US House reps
(NV-1..NV-4, external_id -32001..-32004), using the research-stances skill, following the
validated Phase 127-137 playbook. Concurrency cap = 3. WebFetch-only.

Purpose: Seventh wave of v2.17 (USHS-12, KS + MS + NV + NE + NM). Each rep currently has 0 stances;
pure scale-out of the v2.16 pipeline. Output: CSV of record committed; stances + context + quotes
pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\137-la-ct-in-ok-ar-ia-house-rep-stances\137-06-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (NV House):

| external_id | name | district |
|-------------|------|----------|
| -32001 | Dina Titus (D) | NV-1 |
| -32002 | Mark E. Amodei | NV-2 |
| -32003 | Susie Lee (D) | NV-3 |
| -32004 | Steven Horsford (D) | NV-4 |

All currently have 0 stances. Resolve each politician_id by external_id->UUID at run time.
Document the actual record only, never infer from party. All 4 are established multi-term members
with documentable records (no expected thin records).

KEY RULES (validated Phase 127-137 playbook):
- **25 federal topics** = 44 live minus 11 city-level + all judicial-*. Fetch fresh; write the scale to
  `backend/data/stance-research/nv-house/_TOPIC_SCALE.txt` (or copy ok-house/_TOPIC_SCALE.txt); each agent Reads it.
- **Concurrency: up to 3 agents**. WebFetch ONLY — never WebSearch or Playwright.
- **EFFICIENCY: try each source URL ONCE; if it 403s/404s/times out, move on immediately — do NOT retry or loop; finish in a few minutes.** (Phase 137 lesson: an agent hung ~6.5h on a WebFetch stall without this rule.)
- **Per-rep CSV -> merge + RFC-4180-validate** (csv-parse/sync). CSV quote-repair: collapse `""""`->`"""`
  ONLY (4->3); NEVER 3->2. Use `replace(/"""""+/g,'"""').replace(/""""/g,'"""')` + relax_quotes (sed-clone ok-house/_merge.ts).
- **Per-row artifacts:** stray trailing `"` on empty final field -> `sed -i 's/,"$/,/'`; malformed `,""text""` wrap (no escaped `""` inside reasoning, verify with grep) -> line-scoped `sed 's/""/"/g'`.
- **isidewith.com is BELOW the bar** — DROP isidewith-only rows. AFA/candidate questionnaires OK.
- **Calibration discipline**: DROP over-read `same-sex-marriage=5` rows whose only evidence is "opposes legalization" / a religious-exemption bill / Equality-Act opposition / a belief-quote / group-alignment; KEEP only with a documented anti-recognition vote OR explicit pro-amendment + Obergefell opposition. DROP topic rows resting only on a vague "core value" listing.
- **Resolve politician_id by external_id->UUID map**, not by name.
- **Five-chairs framing**: match documented record to exact stance text; never infer from party.
  Lean on Ballotpedia, OnTheIssues, Wikipedia, LCV, VoteSmart. house.gov/congress.gov/govtrack 403 to WebFetch.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research NV House (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\137-la-ct-in-ok-ar-ia-house-rep-stances\137-06-SUMMARY.md (latest validated playbook)
- backend/data/stance-research/ok-house/_merge.ts (merge+repair to sed-clone)
- backend/data/stance-research/ok-house/_TOPIC_SCALE.txt (current 25-topic scale to copy)
</read_first>
<action>
1. Ensure `backend/data/stance-research/nv-house/_TOPIC_SCALE.txt` holds the current 25 federal topics (copy ok-house/_TOPIC_SCALE.txt, or re-fetch fresh and drop the 11 city + judicial-* topics).
2. Dispatch `politician-stance-researcher` agents for the 4 NV reps (external_id -32001..-32004), up to 3 concurrent. Each prompt: five-chairs framing, Read `_TOPIC_SCALE.txt`, WebFetch ONLY, the EFFICIENCY one-try-per-URL rule, explicit RFC-4180 escaping rule, drop isidewith-only rows, write to `backend/data/stance-research/nv-house/<surname>.csv`, include external_id on every row.
3. Verify each agent's rows before dispatching the next wave; retry empty-output agents solo.
4. Merge the 4 per-rep CSVs into `backend/data/stance-research/2026-06-20-nv-house.csv` (sed-clone ok-house/_merge.ts: set OUT + IN_SCOPE=[-32001,-32002,-32003,-32004]); validate every row.
</action>
<acceptance_criteria>
- 2026-06-20-nv-house.csv exists with rows for the 4 reps (or documented honest-skips); merge parser reports 0 problems
- Every row has value (1-5) + reasoning + >=1 real http source_url; topic_key is one of the 25 fetched federal topics; no isidewith-only rows
- Each of NV-1..NV-4 accounted for: stances OR an honest-skip noted (no party-inferred values)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push NV House to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/ok-house/_push.ts (reusable external_id-keyed push)
- backend/data/stance-research/2026-06-20-nv-house.csv
</read_first>
<action>
Present the STEP 3 approval summary. Standing user decision this milestone: auto-push batches that merge 0-problems AND 0-unsourced (still show the summary), no per-batch approval pause. Push via the external_id-keyed `_push.ts` pattern (copy to `backend/data/stance-research/nv-house/_push.ts`), resolving politician_id by external_id->UUID + topic_id by topic_key, upserting inform.politician_answers + inform.politician_context (ON CONFLICT DO UPDATE on both), pushing quotes to essentials.quotes with Read & Rank + surname leak-check. `_push.ts` does NOT load dotenv — run with `set -a && source .env && set +a && node --import tsx .../_push.ts <csv>`. Commit the CSV (force-add past gitignore). Verify per-scope: NV reps (-32001..-32004) reach exact counts, unsourced-in-scope = 0. If MCP Supabase tokens expire mid-run, fall back to `node --import tsx` + `pool` from `backend/src/lib/db.ts` (load `dotenv/config` first) for verification queries.
</action>
<acceptance_criteria>
- Each NV rep has >=1 inform.politician_answers row OR a documented honest-skip
- Every new answer has a paired sourced context row (0 unsourced)
- Push reported counts with zero rollbacks; per-scope external_id counts confirm isolation
- CSV committed (force-added past gitignore)
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content lands in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-07 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT DO UPDATE; re-runs safe; single BEGIN/COMMIT with ROLLBACK on error |
| T-138-08 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check; surname-leaking quotes never selected |
| T-138-09 | Integrity (scope creep) | non-in-scope reps | mitigate | push keyed strictly to NV external_ids -32001..-32004; verified via per-scope counts |

No high-severity threats: read-only web research + an idempotent service-role upsert. No package installs.
</threat_model>

<verification>
- 4 NV reps each have sourced stances or a documented skip; every answer has paired sourced context
- No party-inferred stances; no isidewith-only rows; 25 federal topics; concurrency held at 3
- Per-scope NV counts (-32001..-32004) confirm writes isolated (no global-counter reliance)
</verification>

<success_criteria>
USHS-12 advanced: NV-1..NV-4 (4 reps) covered with sourced stances or honest-skips, all answers paired with sourced context, CSV committed.
</success_criteria>

<output>
Create `.planning/phases/138-ks-ms-nv-ne-nm-house-rep-stances/138-03-SUMMARY.md` when done.
</output>
