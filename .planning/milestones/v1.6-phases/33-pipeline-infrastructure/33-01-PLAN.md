---
phase: 33-pipeline-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/scripts/utils.py
  - EV-Backend/scripts/requirements.txt
  - EV-Backend/scripts/import_missing_geofences.py
  - EV-Backend/scripts/import_school_board_districts.py
  - EV-Backend/scripts/import_ca_legislative_geofences.py
  - EV-Backend/scripts/README.md
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]

must_haves:
  truths:
    - "New import scripts can `from utils import get_engine, load_env, next_ext_id` without duplicating function bodies"
    - "`pip install -r requirements.txt` with Python 3.10+ installs exact pinned versions of all import dependencies"
    - "A developer reading README.md knows to use Python 3.10+, direct connection port 5432, and where to set DATABASE_URL"
    - "Existing canonical scripts (import_missing_geofences.py, import_school_board_districts.py, import_ca_legislative_geofences.py) still run correctly after refactor"
    - "next_ext_id() returns values starting at -200001 (v1.6 range), not -100001 (v1.5 range)"
  artifacts:
    - path: "EV-Backend/scripts/utils.py"
      provides: "Shared get_engine, load_env, next_ext_id functions"
      contains: "_EXT_ID_COUNTER = -200001"
    - path: "EV-Backend/scripts/requirements.txt"
      provides: "Pinned dependency manifest"
      contains: "geopandas==1.1.2"
    - path: "EV-Backend/scripts/README.md"
      provides: "Updated setup documentation with Python 3.10+ and port 5432 requirements"
      contains: "Python 3.10"
  key_links:
    - from: "EV-Backend/scripts/import_missing_geofences.py"
      to: "EV-Backend/scripts/utils.py"
      via: "from utils import get_engine, load_env"
      pattern: "from utils import"
    - from: "EV-Backend/scripts/import_school_board_districts.py"
      to: "EV-Backend/scripts/utils.py"
      via: "from utils import get_engine, load_env"
      pattern: "from utils import"
    - from: "EV-Backend/scripts/import_ca_legislative_geofences.py"
      to: "EV-Backend/scripts/utils.py"
      via: "from utils import get_engine, load_env"
      pattern: "from utils import"
---

<objective>
Create the shared Python utility module and pinned dependency manifest for import scripts, then update existing canonical scripts to use the shared module and update README documentation.

Purpose: Prevents the sixth copy of `get_engine()` from appearing when Phase 34 adds TIGER import scripts. Gives any developer a one-command environment setup.

Output: `utils.py`, `requirements.txt`, updated canonical scripts, updated `README.md`
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pipeline-infrastructure/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared utils.py and requirements.txt</name>
  <files>EV-Backend/scripts/utils.py, EV-Backend/scripts/requirements.txt</files>
  <action>
Create `EV-Backend/scripts/utils.py` by consolidating existing implementations:

1. **`load_env()`** — Lift verbatim from `import_missing_geofences.py`. Reads `DATABASE_URL` from environment or falls back to `Path(__file__).parent.parent / ".env.local"`. Add docstring noting Supabase direct connection requirement (port 5432, not 6543 pooler).

2. **`get_engine()`** — Lift verbatim from `import_missing_geofences.py`. Creates SQLAlchemy engine with `urllib.parse.quote_plus` for password URL-encoding. Add guard: if `DATABASE_URL` is not set, print error and `sys.exit(1)`.

3. **`next_ext_id()`** — Adapted from `promote_scraped_officials.py`. Uses module-level `_EXT_ID_COUNTER = -200001` (NOT -100001 — that is the v1.5 range per STATE.md). Returns current value and decrements. Add docstring explaining v1.5 vs v1.6 range separation.

Top-level imports: `os`, `sys`, `pathlib.Path`. The `sqlalchemy.create_engine` import goes at module top. The `urllib.parse` imports stay inside `get_engine()` (matching the existing pattern).

Add module docstring:
```python
"""
Shared utilities for EV-Backend import scripts.

Usage in scripts:
    from utils import get_engine, load_env, next_ext_id
"""
```

Create `EV-Backend/scripts/requirements.txt` with exact pinned versions:
```
# EV-Backend/scripts/requirements.txt
#
# Import script dependencies with pinned versions.
# Requires Python >=3.10 — geopandas 1.1.2 dropped Python 3.9 support.
#
# Install:
#   python3.13 -m pip install -r requirements.txt
#
geopandas==1.1.2
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11
shapely==2.0.7
requests==2.32.5
```

Do NOT add `__init__.py` to scripts/ — scripts are run directly, not as a package.
Do NOT modify legacy scripts (`import_shapefiles.py`, `import_shapefiles_fixed.py`) — they have different function names and are not being extended.
Do NOT modify `promote_scraped_officials.py` — it keeps its own local `EXT_ID_COUNTER = -100001` for the v1.5 range.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub && python3.13 -c "import sys; sys.path.insert(0, 'EV-Backend/scripts'); from utils import get_engine, load_env, next_ext_id; assert next_ext_id() == -200001; assert next_ext_id() == -200002; print('utils.py imports OK, next_ext_id range correct')" && grep -c "geopandas==1.1.2" EV-Backend/scripts/requirements.txt</automated>
    <manual>Verify utils.py has all three functions and requirements.txt has all 5 pinned dependencies</manual>
  </verify>
  <done>utils.py exports get_engine, load_env, next_ext_id; next_ext_id starts at -200001; requirements.txt pins geopandas==1.1.2, SQLAlchemy==2.0.46, psycopg2-binary==2.9.11, shapely==2.0.7, requests==2.32.5</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical scripts and README documentation</name>
  <files>EV-Backend/scripts/import_missing_geofences.py, EV-Backend/scripts/import_school_board_districts.py, EV-Backend/scripts/import_ca_legislative_geofences.py, EV-Backend/scripts/README.md</files>
  <action>
**Update three canonical scripts** to import from utils instead of duplicating function bodies:

For each of `import_missing_geofences.py`, `import_school_board_districts.py`, `import_ca_legislative_geofences.py`:

1. Add near the top (after existing `import os, sys` and `from pathlib import Path`):
```python
sys.path.insert(0, str(Path(__file__).parent))
from utils import get_engine, load_env
```

2. Delete the inline `def load_env():` function body (approximately 10-12 lines each).

3. Delete the inline `def get_engine():` function body (approximately 12-15 lines each).

4. Keep all other code intact — the scripts still use `load_env()` and `get_engine()` by name, just now imported.

5. Remove the now-unnecessary `from sqlalchemy import create_engine` import from each script IF `create_engine` is only used inside the deleted `get_engine()` function. Check carefully — `import_missing_geofences.py` and `import_ca_legislative_geofences.py` also use `text` from sqlalchemy in their main code. Only remove `create_engine` from the import, keep `text` if used.

**Update `EV-Backend/scripts/README.md`:**

Rewrite the Prerequisites section to specify:
- Python 3.10+ required (geopandas 1.1.2 constraint). System `python3` on macOS is 3.9.6 and will NOT work.
- Use `python3.13` or `python3.12` from Homebrew (`/opt/homebrew/bin/python3.13`)
- `pip install -r requirements.txt` is the one-command setup
- DATABASE_URL must use direct connection (port 5432), NOT the Supabase pooler (port 6543) — pooler breaks bulk imports with `prepared statement already exists` errors
- Shared utilities: new scripts should `from utils import get_engine, load_env, next_ext_id` rather than duplicating these functions

Keep the existing "What Gets Imported", "Expected Feature Counts", and "Data Source" sections. Update the "Usage" section to reference `python3.13` instead of `python3`. Update the troubleshooting section to add the Python version mismatch issue.

Do NOT change the Bash script references — `import_shapefiles.sh` still works independently.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub && grep -c "from utils import" EV-Backend/scripts/import_missing_geofences.py EV-Backend/scripts/import_school_board_districts.py EV-Backend/scripts/import_ca_legislative_geofences.py && grep -c "def get_engine" EV-Backend/scripts/import_missing_geofences.py EV-Backend/scripts/import_school_board_districts.py EV-Backend/scripts/import_ca_legislative_geofences.py && grep -c "Python 3.10" EV-Backend/scripts/README.md && grep -c "requirements.txt" EV-Backend/scripts/README.md</automated>
    <manual>Each canonical script should have 1 "from utils import" line and 0 "def get_engine" lines. README should mention Python 3.10+ and requirements.txt.</manual>
  </verify>
  <done>Three canonical scripts import from utils.py with no duplicated get_engine/load_env bodies. README documents Python 3.10+ requirement, direct connection (port 5432), pip install -r requirements.txt setup, and shared utils import pattern for new scripts.</done>
</task>

</tasks>

<verification>
1. `python3.13 -c "import sys; sys.path.insert(0, 'EV-Backend/scripts'); from utils import get_engine, load_env, next_ext_id; print('OK')"` succeeds
2. `grep -c "def get_engine" EV-Backend/scripts/import_missing_geofences.py` returns 0
3. `grep -c "def get_engine" EV-Backend/scripts/import_school_board_districts.py` returns 0
4. `grep -c "def get_engine" EV-Backend/scripts/import_ca_legislative_geofences.py` returns 0
5. `grep -c "from utils import" EV-Backend/scripts/import_missing_geofences.py` returns 1
6. `grep "_EXT_ID_COUNTER = -200001" EV-Backend/scripts/utils.py` returns a match
7. `grep "geopandas==1.1.2" EV-Backend/scripts/requirements.txt` returns a match
8. `grep "Python 3.10" EV-Backend/scripts/README.md` returns a match (documentation updated)
</verification>

<success_criteria>
- utils.py is importable and exports all three functions
- next_ext_id() starts at -200001 (v1.6 range)
- requirements.txt pins all 5 dependencies with exact versions
- All three canonical scripts use `from utils import` with no inline duplicates
- README documents Python 3.10+, port 5432, and requirements.txt setup
- promote_scraped_officials.py is unchanged (keeps its own v1.5 -100001 counter)
- Legacy scripts (import_shapefiles.py, import_shapefiles_fixed.py) are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/33-pipeline-infrastructure/33-01-SUMMARY.md`
</output>
