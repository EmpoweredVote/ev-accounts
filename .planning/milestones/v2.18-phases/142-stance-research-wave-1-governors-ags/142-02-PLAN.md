---
phase: 142-stance-research-wave-1-governors-ags
plan: 02
type: execute
wave: 2
depends_on: [142-01]
files_modified:
  - backend/data/stance-research/gov-ag-batch-a/
  - backend/scripts/verify-stance-coverage.mjs
autonomous: true
requirements: [SEXS-02]
must_haves:
  truths:
    - "All 10 batch-A execs (FL/NY/IL/PA Gov+AG + TX Gov+AG) have >=1 sourced inform.politician_answers row"
    - "Every batch-A answer row has a paired inform.politician_context row whose sources[1] starts with http (zero unsourced)"
    - "No previously-stanced exec is touched (push is external_id-keyed to the 10 batch-A ids only)"
    - "No stance is inferred from party; topics with no documentable evidence are honest-skipped"
    - "Govs scored from bill signings/vetoes/EOs; AGs scored from lawsuits/amicus/multistate coalitions with an on-topic published position"
  artifacts:
    - path: "backend/data/stance-research/gov-ag-batch-a/"
      provides: "Per-exec research CSVs + _TOPIC_SCALE.txt + _merge.ts + _push.ts + dated merged CSV for batch A"
      contains: "_push.ts"
  key_links:
    - from: "backend/data/stance-research/gov-ag-batch-a/_push.ts"
      to: "inform.politician_answers + inform.politician_context (+ essentials.quotes)"
      via: "external_id->politician_id resolve, BEGIN/COMMIT upsert ON CONFLICT"
      pattern: "external_id|politician_answers|politician_context"
---

<objective>
Research and push sourced compass stances for **batch A - 10 execs**: the Governors and Attorneys General of **FL, NY, IL, PA, TX** (the largest-population in-scope states; CA is already stanced and excluded). Reuse the v2.16/v2.17 stance pipeline VERBATIM - per-exec CSV from the `politician-stance-researcher` agent at <=3 concurrency, merged + validated by `_merge.ts`, pushed external_id->UUID by `_push.ts`.

Purpose: SEXS-02 core. These execs currently lack any `inform.politician_answers` rows. Largest states first so calibration carries forward.
Output: a populated `backend/data/stance-research/gov-ag-batch-a/` dir + 10 execs' stances pushed to prod, zero unsourced.

**In-scope execs + EXACT external_ids (use literally - IDs are IRREGULAR, never derive from FIPS):**
- FL Governor (Ron DeSantis) - `-1200001`
- FL Attorney General (James Uthmeier) - `-1200003`
- NY Governor (Kathy Hochul) - `-3600001`
- NY Attorney General (Letitia James) - `-3600003`
- IL Governor (JB Pritzker) - `-1700001`
- IL Attorney General (Kwame Raoul) - `-1700003`
- PA Governor (Josh Shapiro) - `-4200001`
- PA Attorney General (Dave Sunday) - `-4200003`
- TX Governor (Greg Abbott) - `-100202` *(existing-but-unstanced; ad-hoc id)*
- TX Attorney General (Ken Paxton) - `-100204` *(existing-but-unstanced; ad-hoc id)*
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md
@.planning/phases/142-stance-research-wave-1-governors-ags/142-CONTEXT.md

# Pipeline scripts to sed-clone (DO NOT split on commas; use these exact files as the template):
@backend/data/stance-research/atlarge-house/_merge.ts
@backend/data/stance-research/atlarge-house/_push.ts
# _TOPIC_SCALE.txt is the 25 federal-topic 1-5 scale text - copy as-is (unchanged for state execs):
#   backend/data/stance-research/atlarge-house/_TOPIC_SCALE.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up batch-a dir + research FL/NY/IL Gov+AG (6 execs, <=3 concurrency)</name>
  <files>backend/data/stance-research/gov-ag-batch-a/</files>
  <read_first>
    - .planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md (paragraph "End-to-End Pipeline" Steps 0-3; "Calibration & Pitfalls"; "The 25 Federal Compass Topics")
    - backend/data/stance-research/atlarge-house/_merge.ts (the OUT + IN_SCOPE edit points; repair()/esc()/FEDERAL reused untouched)
    - backend/data/stance-research/atlarge-house/_push.ts (external_id->UUID resolve, txn upsert - no edits needed)
  </read_first>
  <action>
    SET UP: `mkdir backend/data/stance-research/gov-ag-batch-a`. Copy `_TOPIC_SCALE.txt`, `_merge.ts`, `_push.ts` from `atlarge-house/`. In `_merge.ts` HAND-EDIT exactly two values (watch the substring-clobber sed trap - hand-edit, do not sed): (1) `OUT` -> `.../2026-06-21-gov-ag-batch-a.csv`; (2) `IN_SCOPE` -> the Set of these 10 external_ids: -1200001,-1200003,-3600001,-3600003,-1700001,-1700003,-4200001,-4200003,-100202,-100204. Leave `DIR`, `FEDERAL`, `repair()`, `esc()` untouched.

    RESEARCH (<=3 concurrency - per [[feedback_stance_research_one_at_a_time]], NEVER mass-launch): dispatch `politician-stance-researcher` agents in serial groups of <=3, waiting for each agent's CSV before launching the next group. This task covers 6 execs: FL Gov DeSantis (-1200001), FL AG Uthmeier (-1200003), NY Gov Hochul (-3600001), NY AG James (-3600003), IL Gov Pritzker (-1700001), IL AG Raoul (-1700003). Each dispatch prompt MUST include: politician name + office/title + state (e.g. "Ron DeSantis (Governor of Florida)"); the absolute `--output-file` `C:/EV-Accounts/backend/data/stance-research/gov-ag-batch-a/{lastname}.csv`; the FULL `_TOPIC_SCALE.txt` contents pasted into the TOPIC SCALE REFERENCE placeholder (embed actual 1-5 texts per [[feedback_stance_scale_embed_texts]] - never "5=progressive"); the office-type evidence rule for that exec's office (Gov = bill signings/vetoes/EOs/budget; AG = lawsuits/amicus/multistate coalitions WITH an on-topic published position); the RFC-4180 escaping reminder (double-quote reasoning/quotes, escape embedded quotes by doubling, NO stray trailing double-quote on the empty final field); and the instruction to PUT THE EXACT external_id in the `external_id` column (the agent leaves it blank by default - the merge is external_id-keyed and validates IN_SCOPE.has(ext)). CSV column order: full_name,external_id,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3,quote_text,quote_deidentified. Validate the first triple completes with non-empty CSVs; if old 429 empty-output reappears, drop to 1-2 concurrency.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && ls backend/data/stance-research/gov-ag-batch-a/*.csv 2>/dev/null | grep -v '/_' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - batch-a dir exists with _TOPIC_SCALE.txt + _merge.ts (OUT + IN_SCOPE edited to the 10 ids) + _push.ts.
    - At least 6 non-underscore CSVs exist (FL/NY/IL Gov+AG), each non-empty with the exec's external_id in the external_id column.
    - No stance inferred from party; topics without evidence honest-skipped.
  </acceptance_criteria>
  <done>batch-a dir set up; 6 FL/NY/IL Gov+AG CSVs researched at <=3 concurrency with the exact external_ids.</done>
</task>

<task type="auto">
  <name>Task 2: Research PA + TX Gov+AG (4 execs), merge, proxy-row review, push, assert coverage</name>
  <files>backend/data/stance-research/gov-ag-batch-a/</files>
  <read_first>
    - backend/data/stance-research/gov-ag-batch-a/_merge.ts (the in-progress batch dir from Task 1)
    - .planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md ("Calibration & Pitfalls" - proxy-row gate, isidewith below the bar, SSM=5 rule, no-scope-creep verification)
  </read_first>
  <action>
    RESEARCH the remaining 4 execs at <=3 concurrency: PA Gov Shapiro (-4200001), PA AG Sunday (-4200003), TX Gov Abbott (-100202), TX AG Paxton (-100204) - same dispatch-prompt requirements as Task 1.

    MERGE: `cd C:/EV-Accounts/backend && node --import tsx data/stance-research/gov-ag-batch-a/_merge.ts`. Fix per-file CSV artifacts BEFORE re-merge if the merge reports problems: stray lone trailing double-quote on the empty final field -> `sed -i 's/,"$/,/'` on that file; quad-quote is auto-repaired by `_merge.ts repair()` (collapse quad+ to triple ONLY - NEVER triple to double). Regenerate an offending exec's CSV rather than global quote-replace.

    PROXY-ROW REVIEW GATE (expect ~several drops): drop any row whose only evidence is caucus/coalition membership WITHOUT a published position on that topic (for AGs: multistate-coalition membership counts ONLY when the coalition has a published position directly on that topic); drop rows sourced ONLY by isidewith.com (no ballotpedia/wikipedia/ontheissues/lcv/votesmart/.gov/state-AG-press); drop same-sex-marriage=5 unless backed by a documented anti-recognition vote/action or an explicit one-man-one-woman constitutional-amendment endorsement (Equality-Act opposition / religious-exemption bills support religious-freedom=4/5, not SSM=5). Keep when the proxy has a published on-topic platform, a concrete org role with documented on-topic action, or a campaign-platform priority the exec personally ran on.

    PUSH (only when merge reports 0 problems AND 0 unsourced - standing auto-push rule; still show the summary): `cd C:/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx data/stance-research/gov-ag-batch-a/_push.ts data/stance-research/2026-06-21-gov-ag-batch-a.csv` (the `set -a && source .env && set +a` prefix is MANDATORY - `_push.ts` does NOT load dotenv).

    ASSERT COVERAGE (no-scope-creep, per-scope external_id counts - NOT the global row counter): the verify command runs a read-only psql checking that all 10 batch-A external_ids are covered and zero of their answer rows lack a paired http-sourced context row.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node scripts/verify-stance-coverage.mjs 10 -1200001 -1200003 -3600001 -3600003 -1700001 -1700003 -4200001 -4200003 -100202 -100204</automated>
  </verify>
  <acceptance_criteria>
    - All 10 batch-A external_ids resolve and each has >=1 inform.politician_answers row.
    - Zero batch-A answer rows lack a paired inform.politician_context row with sources[1] LIKE 'http%'.
    - Proxy-only rows dropped per the review gate; no party-inferred stances.
    - Merge reported 0 problems AND 0 unsourced before push.
  </acceptance_criteria>
  <done>10 batch-A execs pushed; verify psql prints "batch-A PASS"; zero unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live source (state .gov / NAAG / ballotpedia) -> stance data | Officeholder positions sourced externally; mis-sourcing or party-inference is the correctness risk |
| author -> prod DB (kxsdzaojfaibhuzmclfq) | _push.ts writes inform.politician_answers/context directly to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-A-01 | Spoofing | Stance inferred from party (R gov assumed anti-abortion) rather than a documented signing/lawsuit | mitigate | [[feedback_stance_no_assumption]] strict mode in every dispatch; _merge.ts rejects rows lacking an http source; honest-skip topics with no evidence |
| T-142-A-02 | Tampering | Push writes to a previously-stanced or out-of-scope exec | mitigate | _push.ts is external_id-keyed; IN_SCOPE Set = exactly the 10 batch-A ids; per-scope coverage assertion confirms exactly these ids |
| T-142-A-03 | Information Disclosure | Answer row with no paired source-bearing context row (unsourced) | mitigate | _merge.ts requires >=1 http source per row; verify psql asserts zero unsourced for the batch ids |
| T-142-A-04 | Tampering | Proxy-only row (coalition membership w/o on-topic position; isidewith; over-read SSM=5) inflates coverage | mitigate | Proxy-row review gate in Task 2 before push (ROADMAP SC#4) |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pipeline uses existing deps csv-parse/pg); no supply-chain surface |
</threat_model>

<verification>
Per-batch coverage assertion (read-only psql, see Task 2 verify): covered=10, unsourced=0 across the batch-A external_id set. The consolidated Phase-144 gate re-asserts coverage + zero-unsourced for all 80 execs keyed on (STATE_EXEC, role_canonical IN (governor,attorney_general)).
</verification>

<success_criteria>
- All 10 batch-A Gov+AG (FL/NY/IL/PA/TX) have >=1 sourced stance; zero unsourced.
- No previously-stanced exec touched; no party-inferred stances; proxy gate applied.
- batch-a dir committed as the data record (CSVs + merged dated CSV + scripts).
</success_criteria>

<output>
Create `.planning/phases/142-stance-research-wave-1-governors-ags/142-02-SUMMARY.md` when done.
</output>
