---
phase: 141-roster-lock-seed-records-headshots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/947_in_sos_treasurer_seed_rolecanonical.sql
autonomous: true
requirements: [SEXR-02, SEXR-03]
must_haves:
  truths:
    - "Indiana has labeled STATE_EXEC districts for Secretary of State and Treasurer with state='IN' and geo_id='18'"
    - "Diego Morales (SoS) and Daniel Elliott (Treasurer) are linked to the new labeled IN districts via offices with role_canonical set"
    - "IN existing Gov/LtGov/AG offices have role_canonical backfilled"
    - "No duplicate politician rows are created for Morales/Elliott (existing external_ids reused via ON CONFLICT)"
    - "The IN duplicate-government trap is handled (canonical government UUID, not COUNT(*)=1)"
  artifacts:
    - path: "backend/migrations/947_in_sos_treasurer_seed_rolecanonical.sql"
      provides: "IN SoS+Treasurer labeled districts + offices + role_canonical backfill for all 5 IN Big-5"
      contains: "State of Indiana"
  key_links:
    - from: "essentials.offices (IN SoS/Treasurer)"
      to: "new essentials.districts (STATE_EXEC, IN) + existing politicians (Morales/Elliott)"
      via: "CTE office insert linking existing politician to new labeled district"
      pattern: "role_canonical = '(secretary_of_state|treasurer)'"
---

<objective>
Close Indiana's two record gaps (Secretary of State, Treasurer) by creating properly-labeled STATE_EXEC districts and offices that link the ALREADY-EXISTING politicians Diego Morales (external_id 642977) and Daniel Elliott (external_id 688298) — then backfill `role_canonical` on all 5 of Indiana's in-scope Big-5 offices.

Purpose: IN is the only seeded state with missing labeled Big-5 districts (SoS + Treasurer exist only under the shared "Indiana" district with geo_id=''). It is also the duplicate-government trap (22+ "State of Indiana" rows) and the dual-office risk (re-linking existing politicians without creating phantom duplicates). Isolating IN in its own plan keeps the empty-state seed batches (141-03..07) clean and parallel.
Output: migration 947 applied to prod with IN's 5 Big-5 offices role-tagged and SoS/Treasurer on labeled districts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md

# Seed pattern (CTE politician+office, per-office district, idempotent guards):
@backend/migrations/270_md_state_executives.sql
# Chamber-creation pattern (slug is GENERATED ALWAYS — never insert it; resolve government via name+state):
@backend/migrations/269_md_government_chambers.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve canonical IN government UUID + author migration 947</name>
  <files>backend/migrations/947_in_sos_treasurer_seed_rolecanonical.sql</files>
  <read_first>
    - backend/migrations/269_md_government_chambers.sql (chamber insert pattern; slug GENERATED ALWAYS caveat; government resolution via name+state — but IN has DUPLICATES so this exact lookup fails)
    - backend/migrations/270_md_state_executives.sql (CTE politician+office pattern, ON CONFLICT DO NOTHING, NOT EXISTS office guard, post-insert assertions)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Common Pitfalls" Pitfall 2 IN dual-office, Pitfall 4 IN government duplication; §"Open Questions" 1+2 — the recommended approach; §"Summary" item 3 IN SoS/Treasurer external_ids 642977/688298)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-CONTEXT.md (D-07 leave titles, D-09 no-reseed dedup on (STATE_EXEC, state, role_canonical))
  </read_first>
  <action>
    FIRST, run a live diagnostic against prod to resolve the canonical IN government row (the COUNT(*)=1 preflight from migration 270 WILL FAIL for IN — 22+ duplicate "State of Indiana" rows, Pitfall 4):
    `SELECT DISTINCT g.id, g.name FROM essentials.governments g JOIN essentials.chambers c ON c.government_id = g.id WHERE g.state = 'IN' AND g.name = 'State of Indiana'` — this returns the government row(s) actually referenced by existing IN chambers. Pick the row that the EXISTING IN Gov/LtGov/AG offices already use (join offices→chambers→government); capture that specific UUID. Also run `SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id IN (642977, 688298)` to confirm Morales + Elliott exist; and check their current office/district linkage.

    Then create backend/migrations/947_in_sos_treasurer_seed_rolecanonical.sql (confirm next disk number; bump if 946/947 taken). Wrap in BEGIN/COMMIT. Header comment must record the resolved canonical IN government UUID and document the dual-office decision (per Open Question 1: create new labeled districts + new offices linking the existing politicians; leave the old shared-"Indiana" offices in place as legacy — do NOT delete them, but the new labeled office becomes the role_canonical-tagged Big-5 record). Steps:
    - PREFLIGHT (IN-safe): assert the resolved canonical IN government UUID EXISTS (`COUNT(*) >= 1` keyed on the specific UUID, NOT `= 1` on name+state).
    - Create 2 chambers under the canonical IN government UUID if absent: 'Secretary of State', 'Treasurer' (names matching IN convention; check existing IN chamber names first; slug is GENERATED ALWAYS — exclude from column list). Guard WHERE NOT EXISTS on (name, government_id).
    - Create 2 labeled STATE_EXEC districts: 'Indiana Secretary of State', 'Indiana Treasurer', state='IN' UPPERCASE, geo_id='18' (Pitfall: existing shared "Indiana" district has geo_id='' — the NEW ones must be '18'), district_id='', mtfcc=''. Guard WHERE NOT EXISTS on (district_type, state, label).
    - For each (Morales/SoS, Elliott/Treasurer): CTE `ON CONFLICT (external_id) DO NOTHING` insert of the politician (they already exist → DO NOTHING returns nothing) THEN INSERT the office linking the EXISTING politician (resolve politician id by external_id, NOT the CTE return, since DO NOTHING yields no row) to the new labeled district + new chamber, title='Secretary of State'/'Treasurer' (D-07 real title), role_canonical='secretary_of_state'/'treasurer'. Guard the office insert WHERE NOT EXISTS on (district_id, chamber_id).
    - role_canonical backfill on the 3 EXISTING IN Big-5 offices (Gov 499460 / LtGov 499415 / AG 499453): UPDATE essentials.offices SET role_canonical='governor'/'lt_governor'/'attorney_general' scoped by those politicians' offices, `WHERE role_canonical IS NULL`.
    - POST-INSERT ASSERTIONS before COMMIT: (a) all 5 IN Big-5 offices have role_canonical NOT NULL; (b) the 2 new IN districts have state='IN' (uppercase) and geo_id='18' (non-empty); (c) exactly one office per (IN, secretary_of_state) and (IN, treasurer) role on a labeled (geo_id='18') district; (d) no new politician row created for external_id 642977/688298 (count unchanged).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/947_in_sos_treasurer_seed_rolecanonical.sql 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Migration 947 applies to prod inside BEGIN/COMMIT without error (the IN-safe government preflight does NOT use COUNT(*)=1).
    - Post-apply: `SELECT count(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state='IN' AND label IN ('Indiana Secretary of State','Indiana Treasurer') AND geo_id='18'` = 2.
    - Post-apply: `SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state='IN' AND o.role_canonical IN ('governor','lt_governor','attorney_general','secretary_of_state','treasurer')` >= 5.
    - Post-apply: `SELECT count(*) FROM essentials.politicians WHERE external_id IN (642977,688298)` = 2 (no duplicates created).
    - Re-running migration 947 affects 0 rows (idempotent).
  </acceptance_criteria>
  <done>Migration 947 applied; IN SoS+Treasurer on labeled geo_id='18' districts linked to existing Morales/Elliott; all 5 IN Big-5 offices role-tagged; no duplicate politicians; canonical-government trap handled; re-run is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → prod DB | SQL migration applied to production; correctness of officeholder linkage is the sole risk surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-04 | Tampering | New labeled office duplicates an existing officeholder (Morales/Elliott appear twice in feed) | mitigate | ON CONFLICT (external_id) DO NOTHING on politician; office insert resolves existing politician by external_id; NOT EXISTS office guard; leave legacy shared-"Indiana" offices untouched (Open Question 1 decision documented in header) |
| T-141-05 | Spoofing | Wrong government row chosen from 22+ duplicate "State of Indiana" rows → chamber under orphan government | mitigate | Resolve canonical UUID via offices→chambers→government join of EXISTING IN offices (Pitfall 4); preflight asserts that specific UUID exists; never COUNT(*)=1 on name+state |
| T-141-06 | Information Disclosure | New IN district seeded with geo_id='' (mirroring legacy shared district) → invisible/incorrect feed routing | mitigate | Explicit geo_id='18' on new districts; post-insert assertion geo_id non-empty + state uppercase |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure SQL) |
</threat_model>

<verification>
After apply, spot-check feed-shape: the new IN SoS/Treasurer offices share the same canonical government as IN Gov/LtGov/AG and carry role_canonical. Run `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql` (SEXR-02/03 for IN should now pass; SEXR-04 IN headshots land in plan 141-08).
</verification>

<success_criteria>
- IN SoS + Treasurer have labeled STATE_EXEC districts (geo_id='18', uppercase state) linked to existing Morales/Elliott.
- All 5 IN Big-5 offices carry role_canonical.
- Zero duplicate politician rows; IN duplicate-government trap handled via resolved canonical UUID.
- Migration 947 idempotent.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-02-SUMMARY.md` when done.
</output>
