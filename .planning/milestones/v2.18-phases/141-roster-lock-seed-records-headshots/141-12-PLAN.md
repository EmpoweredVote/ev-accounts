---
phase: 141-roster-lock-seed-records-headshots
plan: 12
type: execute
wave: 3
depends_on: [141-07, 141-08, 141-09, 141-10, 141-11]
files_modified:
  - backend/migrations/957_state_exec_headshots_batch_e.sql
autonomous: true
requirements: [SEXR-04]
must_haves:
  truths:
    - "Every newly-seeded exec in batch E (AZ, DE, ID, LA, MT, NM, SC, WY — 35) has a headshot OR a documented honest-skip"
    - "Headshots are stored in the politician_photos bucket and recorded in essentials.politician_images (column url)"
    - "Audit-only migration 957 records every politician_images INSERT with source URL + license"
  artifacts:
    - path: "backend/migrations/957_state_exec_headshots_batch_e.sql"
      provides: "Audit-only record of batch-E headshot politician_images INSERTs"
      contains: "politician_images"
  key_links:
    - from: "essentials.politician_images.url"
      to: "essentials.politicians (batch-E external_ids)"
      via: "INSERT ... SELECT politician id by external_id, WHERE NOT EXISTS"
      pattern: "politician_photos/.*-headshot.jpg"
---

<objective>
Source, process, store, and record a headshot for every newly-seeded exec in batch E (AZ, DE, ID, LA, MT, NM, SC, WY — 35 execs) via the migration-271 storage-mirror pattern. This is the final headshot batch.

Purpose: SEXR-04 for the batch-E states seeded by plan 141-07. Honest-skip is the documented fallback.
Output: headshots in the politician_photos bucket + audit-only migration 957. After this plan, every newly-seeded exec across all 41 empty states + IN has a headshot or documented skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md
@backend/migrations/271_md_executive_headshots.sql
@.planning/phases/141-roster-lock-seed-records-headshots/141-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source + process + upload headshots (batch E, 35 execs)</name>
  <files>backend/migrations/957_state_exec_headshots_batch_e.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (source priority; PIL crop-4:5-then-resize-600x750-LANCZOS-q90; bucket path; column `url`)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Reusable-Asset Playbook" Headshot Pattern + source priority + find-headshots no-disk-skill note)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-07-SUMMARY.md (batch-E external_ids + officeholder names)
  </read_first>
  <action>
    For each of the 35 batch-E newly-seeded execs (external_id in the -(fips*100000+seq) range for AZ/DE/ID/LA/MT/NM/SC/WY), resolve the politician UUID by external_id. Source a portrait in priority order (one fetch per source): (1) state .gov bio, (2) Ballotpedia, (3) Wikimedia Commons (public-domain/CC). Process with Python PIL (crop 4:5 from top, resize 600x750 LANCZOS q90). Upload to `politician_photos` as `{uuid}-headshot.jpg`. Capture source URL + license per exec. No portrait → HONEST-SKIP (document in SUMMARY).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -c "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state IN ('AZ','DE','ID','LA','MT','NM','SC','WY');" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each non-skipped batch-E exec has a `{uuid}-headshot.jpg` object in the politician_photos bucket (600x750, processed).
    - SUMMARY lists every exec with external_id, source URL, license, OR honest-skip + reason.
  </acceptance_criteria>
  <done>Headshots sourced + processed + uploaded for all batch-E execs with a portrait; honest-skips documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author audit-only migration 957 + run full phase gate</name>
  <files>backend/migrations/957_state_exec_headshots_batch_e.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (audit-only INSERT pattern: column `url`, WHERE NOT EXISTS, storage URL base, per-exec source comment)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-12-SUMMARY.md (Task 1 per-exec UUID + source URL + license)
    - backend/scripts/verify-phase-141.sql (the phase gate authored in plan 141-01 — runs SEXR-01..04 + D-09/D-10 across ALL states)
  </read_first>
  <action>
    Create backend/migrations/957_state_exec_headshots_batch_e.sql (confirm next disk number; bump if taken) as AUDIT-ONLY mirroring migration 271. One idempotent politician_images INSERT per non-skipped exec (resolve politician by external_id; WHERE NOT EXISTS; per-exec source-URL comment). Document honest-skips as comments. Apply to prod with `-v ON_ERROR_STOP=1`.

    THEN run the FULL phase gate `backend/scripts/verify-phase-141.sql` against prod — this is the final cross-state proof that all 209 in-scope offices exist (SEXR-02), all have role_canonical (SEXR-03), all newly-seeded execs have a headshot minus documented honest-skips (SEXR-04), roster denominator = 209 (SEXR-01), and D-10 uppercase/geo_id holds. If any assertion fails, fix the underlying data (do NOT weaken the assertion) and re-run. Record the final PASS output + the consolidated honest-skip list (across all 5 headshot batches) in the SUMMARY for the Phase 144 consolidated gate to inherit.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/957_state_exec_headshots_batch_e.sql 2>&1 | tail -6 && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Migration 957 applies to prod without error; politician_images rows for batch-E states = (35 minus documented honest-skips).
    - `verify-phase-141.sql` runs with `-v ON_ERROR_STOP=1` and prints the final "ALL ... ASSERTIONS PASSED" NOTICE (no RAISE EXCEPTION).
    - SEXR-01 (209 denominator), SEXR-02 (all offices present), SEXR-03 (role_canonical non-null), SEXR-04 (headshots minus skips), D-10a/b all PASS.
    - Consolidated honest-skip list recorded in SUMMARY.
  </acceptance_criteria>
  <done>Migration 957 applied; full verify-phase-141.sql passes all labeled assertions; phase 141 data-complete; honest-skip list recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image host → storage mirror | Untrusted binary downloaded, processed, re-hosted |
| author → prod DB + Supabase Storage | INSERT + object upload to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Spoofing | Wrong-person portrait mirrored | mitigate | Source from officeholder's own .gov bio first; record source URL per exec |
| T-141-12 | Information Disclosure | Copyrighted image mirrored without license | mitigate | Prefer public-domain .gov / CC; record photo_license; honest-skip over unlicensed mirror |
| T-141-13 | Denial of Service | Image-fetch loop hangs session | mitigate | One fetch per source; honest-skip on exhaustion |
| T-141-14 | Tampering | Gate assertion weakened to force a pass | mitigate | Acceptance criteria forbids weakening assertions — fix data, not the gate; final PASS must be a true RAISE NOTICE path |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | Python PIL already in existing stack (no new package) |
</threat_model>

<verification>
The full `verify-phase-141.sql` is the phase-local gate; it must PASS here. (The Phase 144 consolidated gate, SEXS-03, is separate and runs after stance phases 142-143.)
</verification>

<success_criteria>
- All batch-E newly-seeded execs have a headshot OR documented honest-skip.
- Migration 957 (audit-only) applied + idempotent; uses column `url`.
- verify-phase-141.sql passes ALL labeled assertions (SEXR-01..04 + D-10).
- Consolidated honest-skip list recorded for Phase 144.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-12-SUMMARY.md` when done.
</output>
