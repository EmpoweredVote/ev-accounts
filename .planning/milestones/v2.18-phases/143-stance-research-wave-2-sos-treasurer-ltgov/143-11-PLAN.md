---
phase: 143-stance-research-wave-2-sos-treasurer-ltgov
plan: 11
type: execute
wave: 2
depends_on: [143-01, 143-02, 143-03, 143-04, 143-05, 143-06, 143-07, 143-08, 143-09, 143-10]
files_modified:
  - backend/scripts/verify-phase-143.sql
requirements: [SEXS-02]
must_haves:
  truths:
    - "verify-phase-143.sql is a read-only labeled-assertion gate (psql -v ON_ERROR_STOP=1) keyed on (STATE_EXEC, role_canonical IN secretary_of_state/treasurer/lt_governor) - NOT external_id ranges"
    - "Coverage assertions: SoS=35, Treasurer=38, LtGov=43 distinct STATE_EXEC politicians with >=1 inform.politician_answers row, minus any documented LtGov whole-record honest-skips pinned to exact external_ids"
    - "Zero-unsourced assertion: zero answer rows for STATE_EXEC SoS/Treasurer/LtGov lack a paired inform.politician_context row with sources[1] LIKE 'http%'"
    - "Previously-stanced execs are counted in coverage (they legitimately have answers) but are NOT part of the wave-2 103 and are NOT flagged as unsourced"
  artifacts:
    - path: "backend/scripts/verify-phase-143.sql"
      provides: "Phase-143 read-only coverage + zero-unsourced gate for SoS+Treasurer+LtGov; contributes the Wave-2 assertions the Phase-144 consolidated gate will absorb"
      contains: "RAISE EXCEPTION"
  key_links:
    - from: "backend/scripts/verify-phase-143.sql"
      to: "inform.politician_answers + inform.politician_context via essentials.politicians->offices->districts"
      via: "JOIN on d.district_type='STATE_EXEC' AND o.role_canonical IN ('secretary_of_state','treasurer','lt_governor')"
      pattern: "role_canonical IN .'secretary_of_state','treasurer','lt_governor'."
---

<objective>
Author `backend/scripts/verify-phase-143.sql` — the Phase-143 read-only labeled-assertion gate proving SEXS-02 (SoS+Treasurer+LtGov wave) is complete: every in-scope Secretary of State, Treasurer, and Lieutenant Governor that lacked stances now has sourced compass stances (or is a documented LtGov whole-record honest-skip), and zero answer rows are unsourced.

Purpose: ROADMAP SC#1–SC#4 for Phase 143. This gate keys on `(district_type='STATE_EXEC', role_canonical IN ('secretary_of_state','treasurer','lt_governor'))` — NOT external_id ranges (STATE_EXEC ids are non-contiguous and irregular, incl. POSITIVE IN ids 642977/688298). It mirrors `verify-phase-142.sql` / `verify-phase-132-140.sql` and feeds the consolidated Phase-144 gate.
Output: `backend/scripts/verify-phase-143.sql`, all assertions PASS against prod.

DEPENDS ON all 10 batch plans (143-01..143-10) having pushed their stances — this is the Wave-2 closer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md

# Gate analogs (mirror their labeled DO $$ ... RAISE EXCEPTION/NOTICE structure exactly):
@backend/scripts/verify-phase-142.sql
@backend/scripts/verify-phase-132-140.sql
@backend/scripts/verify-phase-141.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run live diagnostic to fix assertion constants</name>
  <files>backend/scripts/verify-phase-143.sql</files>
  <read_first>
    - .planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md (gate denominators: SoS 35, Treasurer 38, LtGov 43; keying rule; honest-skip handling)
    - backend/scripts/verify-phase-142.sql (the labeled-assertion structure; how the OH AG honest-skip is pinned belt-and-suspenders to -3900003)
  </read_first>
  <action>
    BEFORE authoring the gate, run a live read-only diagnostic (psql with `set -a && source .env && set +a`, or node tsx + pool) to fix every assertion constant (v2.17 gate-design lesson — never author constants blind):
    - Per-role COVERED count: `SELECT o.role_canonical, count(DISTINCT p.id) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical IN ('secretary_of_state','treasurer','lt_governor') AND EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id=p.id) GROUP BY o.role_canonical`. Expect SoS=35 (32 wave-2 + 3 pre-stanced), treasurer=38 (34 + 4), lt_governor=43 (37 + 6).
    - Enumerate any UNCOVERED in-scope exec (LtGov whole-record honest-skip): `... AND NOT EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id=p.id)` — capture the EXACT external_id + name + state of each (expected: only LtGovs with no independent record, if any). Confirm with the user before treating any as a documented exemption, then plan to pin it belt-and-suspenders.
    - Unsourced count: `SELECT count(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id LEFT JOIN inform.politician_context c ON c.politician_id=pa.politician_id AND c.topic_id=pa.topic_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical IN ('secretary_of_state','treasurer','lt_governor') AND (c.politician_id IS NULL OR c.sources[1] NOT LIKE 'http%')`. Expect 0.
    Record the exact covered counts (and any pinned honest-skip external_ids) to use as the gate constants in Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -t -c "SELECT o.role_canonical, count(DISTINCT p.id) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical IN ('secretary_of_state','treasurer','lt_governor') AND EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id=p.id) GROUP BY o.role_canonical ORDER BY o.role_canonical"</automated>
  </verify>
  <acceptance_criteria>
    - Diagnostic ran against prod; covered counts per role recorded (secretary_of_state + treasurer + lt_governor).
    - Every uncovered in-scope exec (LtGov whole-record honest-skip) identified by exact external_id (or confirmed none), confirmed with the user.
    - Unsourced count confirmed (expected 0) before authoring the gate constants.
  </acceptance_criteria>
  <done>Live diagnostic complete; gate constants (covered counts + any pinned honest-skip ids) recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Author verify-phase-143.sql + run it green</name>
  <files>backend/scripts/verify-phase-143.sql</files>
  <read_first>
    - backend/scripts/verify-phase-142.sql (the DO $$ DECLARE ... BEGIN ... IF ... THEN RAISE EXCEPTION ...; RAISE NOTICE '... PASS'; END $$; block-per-assertion structure + final overall PASS NOTICE; the honest-skip pinning pattern)
    - .planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md (keying MUST be STATE_EXEC + role_canonical, NOT external_id ranges; coverage denominators)
  </read_first>
  <action>
    Create backend/scripts/verify-phase-143.sql as a read-only labeled-assertion gate mirroring verify-phase-142.sql (header comment block; one labeled DO block per assertion; final overall PASS NOTICE). Run header: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-143.sql`. Author these assertions, ALL keyed on the STATE_EXEC + role_canonical join (NEVER on external_id ranges):

    - SEXS-02e (SoS coverage): count of DISTINCT politicians with role_canonical='secretary_of_state' on a STATE_EXEC district that have >=1 inform.politician_answers row EQUALS the Task-1 constant (expected 35 = 32 wave-2 + 3 pre-stanced). Document the split in a comment.
    - SEXS-02f (Treasurer coverage): same for role_canonical='treasurer' (expected 38 = 34 wave-2 + 4 pre-stanced). Comment that FL CFO, NY/TX Comptroller are mapped to treasurer.
    - SEXS-02g (LtGov coverage): same for role_canonical='lt_governor' (expected 43 = 37 wave-2 + 6 pre-stanced). If a documented LtGov whole-record honest-skip exists, set covered = 43 minus the skip count AND add a belt-and-suspenders assertion pinning each skipped politician's EXACT external_id (mirroring SEXS-02a-skip / USHS-14a). Comment that AZ LtGov is NOT seeded (Prop 131, deferred to Jan 2027) so it is not in the denominator.
    - SEXS-02h (zero-unsourced): count of inform.politician_answers rows for STATE_EXEC SoS/Treasurer/LtGov whose paired inform.politician_context row is missing OR whose sources[1] does NOT LIKE 'http%' EQUALS 0. RAISE EXCEPTION listing the offending external_ids if > 0.
    - SEXS-02i (scope hygiene): assert zero SoS/Treasurer/LtGov-answered STATE_EXEC politicians with a NULL role_canonical (sanity that the join key is populated).
    Use a comment block at the top documenting: the gate keys on (district_type='STATE_EXEC', role_canonical IN the three Wave-2 roles) because STATE_EXEC external_ids are non-contiguous/irregular (incl. POSITIVE IN 642977/688298, TX -100203/-100205, AZ -400093/-400094, WV SoS -5400003); previously-stanced execs are counted in coverage but are not part of the wave-2 103; AZ LtGov deferred.
    APPLY/RUN it against prod and confirm all assertions PASS (green). If an assertion fails, the fix is in the data (re-run the relevant batch's research), not loosening the gate.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-143.sql 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/verify-phase-143.sql exists with labeled RAISE EXCEPTION/NOTICE assertions for SEXS-02e..i.
    - All assertions keyed on the STATE_EXEC + role_canonical join (grep confirms no `external_id BETWEEN`/range predicate is used for coverage; pinned honest-skip ids, if any, are the ONLY external_id literals and only as belt-and-suspenders).
    - Running the gate with `-v ON_ERROR_STOP=1` exits 0 (all PASS) against prod.
    - Zero-unsourced assertion (SEXS-02h) passes (0 unsourced SoS/Treasurer/LtGov answer rows).
  </acceptance_criteria>
  <done>verify-phase-143.sql committed; runs green against prod; SoS+Treasurer+LtGov coverage + zero-unsourced proven (SEXS-02 wave-2 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> prod DB (read-only) | The gate only reads; the risk is an assertion that is wrong (too loose hides a gap, too tight false-fails) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-G-01 | Repudiation | Gate keyed on external_id ranges silently misses irregular ids (POSITIVE IN, ad-hoc TX/AZ, WV seq-3 SoS) | mitigate | Key strictly on (STATE_EXEC, role_canonical); grep-assert no range predicate |
| T-143-G-02 | Tampering | Coverage constant authored blind, masking a real gap | mitigate | Task 1 live diagnostic fixes every constant before authoring (v2.17 lesson) |
| T-143-G-03 | Information Disclosure | A LtGov whole-record honest-skip silently counted as coverage shortfall vs a real miss | mitigate | Pin any documented honest-skip to its exact external_id belt-and-suspenders (SEXS-02a-skip pattern); confirm with user first |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | No package installs (read-only SQL); no supply-chain surface |
</threat_model>

<verification>
`cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-143.sql` exits 0 with all PASS NOTICEs. The Phase-144 consolidated gate (`verify-phase-141-144.sql`) will absorb these Wave-2 assertions.
</verification>

<success_criteria>
- verify-phase-143.sql exists; keyed on STATE_EXEC + role_canonical (secretary_of_state, treasurer, lt_governor); no external_id range predicate.
- SoS (35) + Treasurer (38) + LtGov (43, minus any pinned honest-skips) coverage assertions PASS.
- Zero-unsourced assertion PASSES (0 SoS/Treasurer/LtGov answer rows lack an http-sourced context row).
- Gate runs green against prod.
</success_criteria>

<output>
Create `.planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-11-SUMMARY.md` when done.
</output>
