---
phase: 144-phase-gate-feed-surfacing-consolidated-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-phase-141-144.sql
autonomous: true
requirements: [SEXR-05, SEXS-03]

must_haves:
  truths:
    - "A single consolidated read-only SQL gate file exists at backend/scripts/verify-phase-141-144.sql"
    - "Running the gate against production exits psql with code 0 (no RAISE EXCEPTION fires)"
    - "Every labeled assertion emits a RAISE NOTICE ... PASS line"
    - "The gate re-asserts the 209-office record matrix, role_canonical population, headshots, in-scope hygiene, 199-row stance coverage, the exact 10 honest-skips, and 0 unsourced rows — all currently TRUE in production"
    - "The gate simulates the production feed predicate for NC, WA, and CO and confirms exactly 5 execs surface for each"
    - "AZ Lt Gov deferral (Prop 131, eff. Jan 2027) is documented in the gate as a known exclusion, not a miss"
  artifacts:
    - path: "backend/scripts/verify-phase-141-144.sql"
      provides: "Consolidated v2.18 phase gate — SEXR-01..05 + SEXS-03 labeled assertions, read-only"
      contains: "SEXR-05-feed-A"
  key_links:
    - from: "backend/scripts/verify-phase-141-144.sql"
      to: "essentials.offices / essentials.districts / inform.politician_answers / inform.politician_context"
      via: "read-only DO $$ ... RAISE EXCEPTION/NOTICE assertions keyed on (district_type='STATE_EXEC', role_canonical IN (...))"
      pattern: "district_type\\s*=\\s*'STATE_EXEC'"
---

<objective>
Produce the single consolidated, read-only, labeled-assertion SQL phase gate for v2.18 (State Leaders):
`backend/scripts/verify-phase-141-144.sql`. It mirrors the v2.17 consolidated-gate template
(`verify-phase-132-140.sql`) and folds the three already-passing single-phase gates
(`verify-phase-141.sql`, `verify-phase-142.sql`, `verify-phase-143.sql`) into one pass that ALSO adds the
SEXR-05 feed-surfacing smoke test as a SQL simulation of the production feed predicate.

Purpose: This is the FINAL gate for the v2.18 milestone. It closes SEXR-05 (feed surfacing) and SEXS-03
(consolidated verification). Phases 141/142/143 are complete and their individual gates already PASS against
production; this file is a mechanical consolidation that re-asserts every v2.18 invariant in one psql run plus
the feed smoke-test, so a single command proves the whole milestone.

Output: one new SQL file. No schema changes, no data writes, no migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Run INLINE from the main checkout (like Phases 142/143) — this gate runs against production, NOT in a worktree.
Production project ref: kxsdzaojfaibhuzmclfq. Inline cwd resets between Bash calls — always
`cd /c/EV-Accounts/backend && ...` in the SAME compound command for run/verify.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The three source gates being consolidated (read all three — denominators and assertion shapes come from here):
@backend/scripts/verify-phase-141.sql
@backend/scripts/verify-phase-142.sql
@backend/scripts/verify-phase-143.sql

# The structural template to mirror for a consolidated multi-phase gate (header style, per-requirement DO blocks, closing RAISE NOTICE line 181):
@backend/scripts/verify-phase-132-140.sql

# The production feed query whose predicate the SEXR-05 smoke-test must replicate (identical at both sites):
@backend/src/lib/essentialsService.ts

<grounding>
All values below are PROD-VERIFIED 2026-06-22 and are currently TRUE in production. If you assert a count, it
MUST be one of these.

RECORDS (re-assert from verify-phase-141.sql):
- 209 distinct in-scope (state, role_canonical) STATE_EXEC office pairs.
- Per-role: governor=50, lt_governor=43, attorney_general=43, secretary_of_state=35, treasurer=38. TOTAL=209.
- role_canonical populated (non-NULL, in the Big-5 set) on all 209 in-scope offices.
- Every newly-seeded exec has an essentials.politician_images.url headshot (175 newly-seeded; 0 honest-skips).
  Newly-seeded set = states NOT IN ('CA','IN','MA','MD','ME','OR','TX','UT','VA') OR external_id IN (642977, 688298).
- AZ Lt Gov DEFERRED (Prop 131, eff. Jan 2027) — NOT seeded, NOT a miss (lt_governor=43 already accounts for this).

IN-SCOPE HYGIENE (success criterion 2) — CRITICAL SCOPING:
- The hygiene assertion MUST be scoped to districts hosting an in-scope Big-5 office (mirror 141 D-10a/D-10b).
  In-scope Big-5 hygiene violators = 0 in production.
- A BARE whole-STATE_EXEC count returns 2 (the legacy non-Big-5 rows CA "Board of Equalization Member" geo_id=''
  and IN "Indiana" geo_id='', both with NULL-role offices). DO NOT use the bare whole-district_type count — it
  is currently 2 and would FALSE-FAIL. Scope to `o.role_canonical IN (Big-5)` exactly as 141 does.

STANCE COVERAGE (re-assert from verify-phase-142.sql + verify-phase-143.sql):
- Governor coverage = 50, Attorney General = 42, Secretary of State = 34, Treasurer = 34, Lt Governor = 39.
- TOTAL covered = 199. In-scope = 209. Honest-skips = 10. 199 + 10 = 209.
- treasurer role_canonical includes functional aliases FL CFO + NY Comptroller + TX Comptroller.

THE 10 HONEST-SKIP external_ids (1 AG + 1 SoS + 4 Treasurer + 4 LtGov):
  AG:        -3900003 (OH AG Andy Wilson)
  SoS:       -4500004 (SC SoS Mark Hammond)
  Treasurer: -100005 (AL Boozer), -2100005 (KY Metcalf), -2800005 (MS McRae), -4600005 (SD Haeder)
  LtGov:     -1900002 (IA Cournoyer), -3100002 (NE Kelly), -3900002 (OH Tressel), -4000002 (OK Pinnell)

ZERO-UNSOURCED (SEXS-03 core):
- 0 STATE_EXEC answer rows (all 5 roles) lack a paired inform.politician_context row whose sources[1] LIKE 'http%'.

FEED PREDICATE (essentialsService.ts line 706-709 AND identical at 1589-1593):
  d.district_type IN ('NATIONAL_UPPER','NATIONAL_EXEC','STATE_EXEC','NATIONAL_JUDICIAL','JUDICIAL')
  AND (d.state = $1 OR d.district_type IN ('NATIONAL_EXEC','NATIONAL_JUDICIAL'))
  AND (p.is_active = true OR o.is_vacant = true)
  AND COALESCE(p.is_incumbent, true) = true
- For the SEXR-05 simulation, narrow district_type to STATE_EXEC and bind state to the test state, applying the
  same `(p.is_active OR o.is_vacant)` and `COALESCE(p.is_incumbent,true)` clauses.

FEED SMOKE-TEST STATES (PROD-VERIFIED, all newly-seeded, all clean full-Big-5 = 5, all active+incumbent):
  NC = 5 execs surface (governor -3700001 Josh Stein among them)
  WA = 5 execs surface (governor -5300001 Bob Ferguson among them)
  CO = 5 execs surface (governor -800001 Jared Polis among them)
</grounding>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the consolidated records + hygiene + stance-coverage assertions</name>
  <files>backend/scripts/verify-phase-141-144.sql</files>
  <read_first>
    - backend/scripts/verify-phase-141.sql (SEXR-01/02/03/04 + D-10a/D-10b assertion bodies and the 209/50/43/43/35/38 denominators; the in-scope-Big-5-scoped hygiene pattern; the SEXR-04 newly-seeded state-scoped headshot query)
    - backend/scripts/verify-phase-142.sql (SEXS-02a/02b coverage shapes; SEXS-02a-skip belt-and-suspenders pin pattern; SEXS-02c zero-unsourced join shape)
    - backend/scripts/verify-phase-143.sql (SEXS-02e/02f/02g coverage; the SEXS-02-skip 9-row ORDER BY o.role_canonical, p.external_id pin and exact expected literal; SEXS-02h zero-unsourced join)
    - backend/scripts/verify-phase-132-140.sql (the consolidated-gate header comment style, per-requirement DO $$ blocks, and the closing RAISE NOTICE summary at line 181)
    - The <grounding> block above for the exact PROD-verified denominators
  </read_first>
  <action>
    Create backend/scripts/verify-phase-141-144.sql with a header comment block (mirror the verify-phase-132-140.sql header style) stating: this is the consolidated v2.18 (State Leaders) phase gate folding phases 141/142/143 plus the SEXR-05 feed smoke-test; read-only; RAISE EXCEPTION on failure / RAISE NOTICE on pass; run line `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141-144.sql`. In the header, document the KEYING rule (key on district_type='STATE_EXEC' AND role_canonical IN the Big-5 set — NEVER external_id ranges, since STATE_EXEC ids are non-contiguous: positive IN 642977/688298, TX -100203/-100205, AZ -400093/-400094, FL CFO -1200004, NY Comptroller -3600004, WV SoS -5400003, AK LtGov -200009). Document the AZ Lt Gov Prop-131 deferral as a KNOWN EXCLUSION baked into the lt_governor=43 denominator (not a miss).

    Then author these labeled DO $$ assertion blocks, each RAISE EXCEPTION on mismatch and RAISE NOTICE '... PASS' on success, in this order:
    - SEXR-01/02-consolidated: COUNT(DISTINCT (d.state, o.role_canonical)) over in-scope STATE_EXEC = 209; AND per-role FILTER counts gov=50, lt=43, ag=43, sos=35, treasurer=38 (compute both in one block or two adjacent blocks — match 141's two-block form).
    - SEXR-03-consolidated: no in-scope (state, role_canonical) pair duplicated (HAVING COUNT(*)>1 returns 0 rows); role_canonical non-NULL and within the Big-5 set on all in-scope offices.
    - SEXR-04-consolidated: every newly-seeded exec (state NOT IN ('CA','IN','MA','MD','ME','OR','TX','UT','VA') OR external_id IN (642977,688298)) has an essentials.politician_images row; expected missing = 0. Copy 141's exact scoping verbatim.
    - SEXS-03-hygiene: COUNT of districts that host an in-scope Big-5 office AND (state <> upper(state) OR geo_id IS NULL OR geo_id = '') = 0. SCOPE TO o.role_canonical IN the Big-5 set exactly as 141 D-10a/D-10b — do NOT use a bare whole-STATE_EXEC count (that returns 2 legacy non-Big-5 rows and would false-fail). Add an inline comment noting the 2 excluded legacy rows (CA Board of Equalization geo_id='', IN "Indiana" geo_id='').
    - SEXS-03-coverage: five per-role coverage counts (politicians with >=1 inform.politician_answers row) = governor 50, attorney_general 42, secretary_of_state 34, treasurer 34, lt_governor 39 (199 total). Match 142/143 coverage join shape.

    Use concrete RAISE EXCEPTION messages that name the requirement label and the expected vs found values (mirror 141/142/143 message style). No fenced code in this plan — write the actual SQL into the file.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^--' scripts/verify-phase-141-144.sql | grep -Ec "SEXR-01/02-consolidated|SEXR-03-consolidated|SEXR-04-consolidated|SEXS-03-hygiene|SEXS-03-coverage"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/scripts/verify-phase-141-144.sql exists.
    - It contains DO blocks labeled SEXR-01/02-consolidated, SEXR-03-consolidated, SEXR-04-consolidated, SEXS-03-hygiene, and SEXS-03-coverage (the grep above returns >= 5 non-comment label hits).
    - The SEXS-03-hygiene block is scoped to `o.role_canonical IN ('governor','lt_governor','attorney_general','secretary_of_state','treasurer')` (NOT a bare whole-STATE_EXEC count) and asserts the violator count = 0.
    - The header documents the AZ Lt Gov Prop-131 deferral as a known exclusion and the key-on-role-canonical (never external_id range) rule.
    - The SEXR-04 block uses the state-list scoping `NOT IN ('CA','IN','MA','MD','ME','OR','TX','UT','VA') OR external_id IN (642977,688298)`.
  </acceptance_criteria>
  <done>The records, hygiene, and per-role coverage assertions are written into verify-phase-141-144.sql with PROD-true denominators (209 / 50-43-43-35-38 / 199 / 50-42-34-34-39) and correctly-scoped hygiene.</done>
</task>

<task type="auto">
  <name>Task 2: Author the honest-skip pin, zero-unsourced, SEXR-05 feed smoke-test, and closing summary</name>
  <files>backend/scripts/verify-phase-141-144.sql</files>
  <read_first>
    - The verify-phase-141-144.sql produced in Task 1 (append to it)
    - backend/scripts/verify-phase-143.sql (the SEXS-02-skip block: string_agg ... ORDER BY o.role_canonical, p.external_id, compared to an exact expected literal — and the 143 lesson that the expected literal MUST match the query ORDER BY exactly)
    - backend/scripts/verify-phase-142.sql (SEXS-02c) and backend/scripts/verify-phase-143.sql (SEXS-02h) for the zero-unsourced LEFT JOIN inform.politician_context ... sources[1] LIKE 'http%' shape
    - backend/src/lib/essentialsService.ts lines 706-709 and 1589-1593 for the exact feed predicate to replicate
    - backend/scripts/verify-phase-132-140.sql line 181 for the closing RAISE NOTICE summary style
    - The <grounding> block above for the 10 honest-skip external_ids and the NC=5 / WA=5 / CO=5 feed counts
  </read_first>
  <action>
    Append the following labeled DO $$ assertion blocks to backend/scripts/verify-phase-141-144.sql:

    - SEXS-03-skip: pin the COMPLETE in-scope-but-uncovered exec set across ALL FIVE roles to EXACTLY the 10
      documented honest-skips. Use string_agg of `external_id || ' ' || full_name || ' (' || state || '/' || role_canonical || ')'` with delimiter '; ' and `ORDER BY o.role_canonical, p.external_id` (combine the 142 AG skip and the 143 SoS/Treasurer/LtGov skips into one set across role_canonical IN ('governor','lt_governor','attorney_general','secretary_of_state','treasurer')). Declare a v_expected literal that lists all 10 in the EXACT order produced by `ORDER BY o.role_canonical, p.external_id` — role_canonical sorts alphabetically (attorney_general, lt_governor, secretary_of_state, treasurer) and within each role external_id ascends (more-negative first). The 10 ids: -3900003 (OH AG); -4000002 OK / -3900002 OH / -3100002 NE / -1900002 IA (lt_governor, ascending external_id means -4000002 first); -4500004 (SC SoS); -4600005 SD / -2800005 MS / -2100005 KY / -100005 AL (treasurer, ascending). CRITICAL (143 lesson): build the expected literal by deriving it from the SAME ORDER BY, then assert v_uncovered IS DISTINCT FROM v_expected → RAISE EXCEPTION. During execution, run the SELECT once standalone to capture the exact production string and paste it verbatim as v_expected (do not hand-order from memory).
    - SEXS-03-unsourced: across all STATE_EXEC answer rows for role_canonical IN the Big-5 set, COUNT of answer rows where the paired inform.politician_context row is missing OR sources[1] IS NULL OR sources[1] NOT LIKE 'http%' = 0. Use the LEFT JOIN shape from SEXS-02c/02h.
    - SEXR-05-feed-A (NC): a SELECT replicating the production STATE_EXEC feed predicate bound to state 'NC' — `d.district_type='STATE_EXEC' AND d.state='NC' AND (p.is_active=true OR o.is_vacant=true) AND COALESCE(p.is_incumbent,true)=true AND o.role_canonical IN (Big-5)` — must return exactly 5 surfaced execs, and the set must INCLUDE governor external_id -3700001 (Josh Stein). RAISE EXCEPTION if count <> 5 or Stein absent.
    - SEXR-05-feed-B (WA): same predicate bound to 'WA' — exactly 5, must include governor -5300001 (Bob Ferguson).
    - SEXR-05-feed-C (CO): same predicate bound to 'CO' — exactly 5, must include governor -800001 (Jared Polis).
      Add a comment on each feed block citing essentialsService.ts line 706 (and noting the predicate is identical at line 1589) and that NC/WA/CO are newly-seeded states (zero STATE_EXEC before v2.18).
    - Closing: a final DO $$ BEGIN RAISE NOTICE 'verify-phase-141-144: ALL SEXR-01..05 + SEXS-03 ASSERTIONS PASSED'; END $$; (mirror verify-phase-132-140.sql line 181) plus \echo banner lines noting the 209 records / 199 covered / 10 honest-skips / 0 unsourced / NC+WA+CO feed-surface summary and the AZ LtGov deferral.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-141-144.sql; echo "psql exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase-141-144.sql contains DO blocks labeled SEXS-03-skip, SEXS-03-unsourced, SEXR-05-feed-A, SEXR-05-feed-B, SEXR-05-feed-C, and a closing RAISE NOTICE 'verify-phase-141-144: ALL SEXR-01..05 + SEXS-03 ASSERTIONS PASSED'.
    - The SEXS-03-skip block pins exactly these 10 external_ids: -3900003, -4500004, -100005, -2100005, -2800005, -4600005, -1900002, -3100002, -3900002, -4000002 — via a v_expected literal whose order matches `ORDER BY o.role_canonical, p.external_id`.
    - SEXR-05-feed-A/B/C assert exactly 5 surfaced execs for NC, WA, CO respectively and each pins the governor external_id (-3700001 / -5300001 / -800001).
    - Running `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-141-144.sql` exits 0 and emits a RAISE NOTICE PASS line for EVERY assertion (no RAISE EXCEPTION fires).
  </acceptance_criteria>
  <done>The full consolidated gate runs read-only against production with psql exit 0; every labeled assertion (SEXR-01..05 + SEXS-03, incl. the 10-id honest-skip pin and the 3 feed smoke-tests) emits a PASS notice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate SQL → production DB | The gate runs against live production (kxsdzaojfaibhuzmclfq). It is READ-ONLY — only SELECT/RAISE inside DO blocks; no INSERT/UPDATE/DELETE/DDL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Tampering | gate SQL touching prod data | mitigate | File contains zero write statements (no INSERT/UPDATE/DELETE/CREATE/ALTER/DROP); only DO $$ blocks with SELECT + RAISE. Verify with a grep for write keywords before running. |
| T-144-02 | Information disclosure | hardcoded DATABASE_URL / secrets in the SQL file | mitigate | The gate references no credentials; connection is supplied via `$DATABASE_URL` env at run time (sourced from .env), never written into the file. |
| T-144-03 | Denial of (false) signal | a denominator drifts from the live single-phase gates and silently passes/fails | accept | All asserted counts (209 / 50-43-43-35-38 / 199 / 50-42-34-34-39 / 10 skips / NC+WA+CO=5) were prod-verified 2026-06-22 and equal the already-passing 141/142/143 gates; the honest-skip pin makes any future miss fail loudly. |
</threat_model>

<verification>
Run the consolidated gate once, top-to-bottom, against production and confirm psql exits 0 with a PASS notice for every label:

```
cd /c/EV-Accounts/backend && set -a && source .env && set +a && \
  psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-141-144.sql; echo "psql exit: $?"
```

Also confirm the gate is write-free:

```
cd /c/EV-Accounts/backend && grep -v '^--' scripts/verify-phase-141-144.sql | grep -Eic "insert |update |delete |create |alter |drop " ; echo "(0 = clean)"
```

Optional manual evidence (NOT the assertion): a `GET /representatives/me` curl for an NC/WA/CO in-state address may be spot-checked and noted in the SUMMARY, but the binding proof is the SQL pass above.
</verification>

<success_criteria>
- backend/scripts/verify-phase-141-144.sql exists, is read-only, and runs in a single psql pass.
- psql exits 0 (ON_ERROR_STOP=1) — no RAISE EXCEPTION fires.
- Every labeled assertion emits RAISE NOTICE '... PASS': SEXR-01/02-consolidated, SEXR-03-consolidated,
  SEXR-04-consolidated, SEXS-03-hygiene, SEXS-03-coverage, SEXS-03-skip, SEXS-03-unsourced,
  SEXR-05-feed-A/B/C, and the closing summary notice.
- The honest-skip pin matches exactly the 10 documented external_ids; the feed smoke-test confirms NC/WA/CO each
  surface 5 execs including their seeded governor.
- AZ Lt Gov deferral is documented in the gate as a known exclusion (not a miss).
- The grep write-keyword check returns 0.
</success_criteria>

<output>
Create `.planning/phases/144-phase-gate-feed-surfacing-consolidated-verification/144-01-SUMMARY.md` when done.
</output>
