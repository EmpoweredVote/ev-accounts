---
phase: 11-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/components/RadarChart.jsx
  - CompassV2/package.json
  - CompassV2/src/util/topic.js
  - CompassV2/src/pages/Library.jsx
  - CompassV2/src/components/ComparePanel.jsx
  - CompassV2/src/components/LibraryDrawer.jsx
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "RadarChart.jsx contains only the active 7-line component — no commented-out code remains"
    - "CompassV2 installs ev-ui ^0.1.19 matching the essentials project"
    - "The question_text fallback string 'What should the government do about ...' exists in exactly one file and is imported by all consumers"
  artifacts:
    - path: "CompassV2/src/components/RadarChart.jsx"
      provides: "Clean RadarChart wrapper component"
      contains: "RadarChartCore"
    - path: "CompassV2/package.json"
      provides: "Updated ev-ui dependency"
      contains: "0.1.19"
    - path: "CompassV2/src/util/topic.js"
      provides: "Shared question_text fallback helper"
      contains: "getQuestionText"
  key_links:
    - from: "CompassV2/src/pages/Library.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { getQuestionText }"
      pattern: "getQuestionText"
    - from: "CompassV2/src/components/ComparePanel.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { getQuestionText }"
      pattern: "getQuestionText"
    - from: "CompassV2/src/components/LibraryDrawer.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { getQuestionText }"
      pattern: "getQuestionText"
---

<objective>
Remove dead code from RadarChart.jsx, align CompassV2 ev-ui dependency version, and consolidate the duplicated question_text fallback string into a shared helper.

Purpose: Clean up tech debt so the CompassV2 codebase is leaner and easier to maintain going into the onboarding and UX phases.
Output: Cleaner RadarChart.jsx (7 lines), updated package.json, and new `util/topic.js` helper imported by 3 consumers.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CompassV2/src/components/RadarChart.jsx
@CompassV2/package.json
@CompassV2/src/util/name.js
@CompassV2/src/pages/Library.jsx
@CompassV2/src/components/ComparePanel.jsx
@CompassV2/src/components/LibraryDrawer.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove RadarChart dead code and update ev-ui version</name>
  <files>CompassV2/src/components/RadarChart.jsx, CompassV2/package.json</files>
  <action>
1. Open `CompassV2/src/components/RadarChart.jsx` and delete everything from line 8 onward (the entire commented-out block from line 9 through line 263). The file should contain only the 7 active lines:
   - Line 1: `import { useCompass } from "../components/CompassContext";`
   - Line 2: `import { RadarChartCore } from "@chrisandrewsedu/ev-ui";`
   - Line 3: blank
   - Line 4: `export default function RadarChart(props) {`
   - Line 5: `  const { topics } = useCompass();`
   - Line 6: `  return <RadarChartCore topics={topics} padding={70} {...props} />;`
   - Line 7: `}`
   Ensure file ends with a trailing newline after the closing brace.

2. Open `CompassV2/package.json` and change the `@chrisandrewsedu/ev-ui` version from `"^0.1.16"` to `"^0.1.19"`.

3. Run `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npm install` to update the lock file.
  </action>
  <verify>
- `wc -l CompassV2/src/components/RadarChart.jsx` returns 7 lines
- `grep '0.1.19' CompassV2/package.json` shows the updated version
- `grep '0.1.16' CompassV2/package.json` returns no matches
- `cd CompassV2 && npm ls @chrisandrewsedu/ev-ui` shows resolved version >= 0.1.19
  </verify>
  <done>RadarChart.jsx contains only the 7-line active component with no commented-out code. CompassV2 package.json pins ev-ui at ^0.1.19 matching essentials.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate question_text fallback into shared helper</name>
  <files>CompassV2/src/util/topic.js, CompassV2/src/pages/Library.jsx, CompassV2/src/components/ComparePanel.jsx, CompassV2/src/components/LibraryDrawer.jsx</files>
  <action>
1. Create `CompassV2/src/util/topic.js` with a single exported function:
   ```js
   export function getQuestionText(topic) {
     if (!topic) return "";
     return topic.question_text || `What should the government do about ${topic.short_title}?`;
   }
   ```
   This follows the pattern of the existing `util/name.js` (named export, simple helper).

2. Update `CompassV2/src/pages/Library.jsx`:
   - Add import: `import { getQuestionText } from "../util/topic";`
   - Replace line 43-44 (the `getQuestion` arrow function):
     ```js
     const getQuestion = (topic) =>
       topic.question_text || `What should the government do about ${topic.short_title}?`;
     ```
     with:
     ```js
     const getQuestion = getQuestionText;
     ```
     This preserves the existing `getQuestion` name used throughout Library.jsx while delegating to the shared helper. No other changes needed in this file.

3. Update `CompassV2/src/components/ComparePanel.jsx`:
   - Add import: `import { getQuestionText } from "../util/topic";`
   - Replace line 144:
     ```jsx
     {selectedTopic.question_text || `What should the government do about ${selectedTopic.short_title}?`}
     ```
     with:
     ```jsx
     {getQuestionText(selectedTopic)}
     ```

4. Update `CompassV2/src/components/LibraryDrawer.jsx`:
   - Add import: `import { getQuestionText } from "../util/topic";`
   - Replace lines 130-132:
     ```js
     const question = topic
       ? topic.question_text || `What should the government do about ${topic.short_title}?`
       : "";
     ```
     with:
     ```js
     const question = getQuestionText(topic);
     ```
     This works because `getQuestionText` already handles the `!topic` case by returning `""`.

NOTE: Do NOT change `Quiz.jsx` — it uses a different fallback (`currentTopic.title` instead of the template string) and is not part of this duplication.
  </action>
  <verify>
- `grep -r "What should the government do about" CompassV2/src/` returns exactly ONE match in `util/topic.js`
- `grep "getQuestionText" CompassV2/src/pages/Library.jsx` shows the import and usage
- `grep "getQuestionText" CompassV2/src/components/ComparePanel.jsx` shows the import and usage
- `grep "getQuestionText" CompassV2/src/components/LibraryDrawer.jsx` shows the import and usage
- `cd CompassV2 && npm run build` succeeds with no errors
  </verify>
  <done>The fallback string "What should the government do about {short_title}?" exists in exactly one place (util/topic.js) and is imported by Library.jsx, ComparePanel.jsx, and LibraryDrawer.jsx.</done>
</task>

</tasks>

<verification>
1. `wc -l CompassV2/src/components/RadarChart.jsx` returns 7 lines (no dead code)
2. `grep '"@chrisandrewsedu/ev-ui": "\\^0.1.19"' CompassV2/package.json` matches
3. `grep -r "What should the government do about" CompassV2/src/ | wc -l` returns exactly 1
4. `cd CompassV2 && npm run build` completes successfully
</verification>

<success_criteria>
- RadarChart.jsx is 7 lines with no commented-out code
- CompassV2 package.json has ev-ui ^0.1.19
- The question_text fallback template string appears in exactly 1 file (util/topic.js) and is imported by 3 consumers
- CompassV2 builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup/11-01-SUMMARY.md`
</output>
