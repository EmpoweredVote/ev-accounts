---
phase: 18-title-display-frontend
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - CompassV2/src/components/CalibrationOverlay.jsx
  - CompassV2/src/pages/Quiz.jsx
  - CompassV2/src/components/LibraryDrawer.jsx
  - CompassV2/src/components/ComparePanel.jsx
autonomous: true
requirements: [TITLE-02, TITLE-03]

must_haves:
  truths:
    - "LibraryDrawer shows tension title as header and QuestionText between title and stances"
    - "CalibrationOverlay answer step shows tension title as heading and QuestionText as a centered prompt above stances (no start_phrase reference)"
    - "Quiz.jsx shows tension title as heading and QuestionText as prompt above stances (no start_phrase reference)"
    - "ComparePanel dropdown uses tension titles (not short_title), shows QuestionText between title and stances"
    - "A user scanning Library, then calibration, then the compass sees the same topic name in all three places"
  artifacts:
    - path: "CompassV2/src/components/LibraryDrawer.jsx"
      provides: "Tension title header + QuestionText between title and stances"
      contains: "parseTensionTitle"
    - path: "CompassV2/src/components/CalibrationOverlay.jsx"
      provides: "Answer step with tension title heading + centered QuestionText prompt"
      contains: "parseTensionTitle"
    - path: "CompassV2/src/pages/Quiz.jsx"
      provides: "Quiz with tension title heading + QuestionText prompt, no start_phrase"
      contains: "parseTensionTitle"
    - path: "CompassV2/src/components/ComparePanel.jsx"
      provides: "Topic dropdown with tension titles + QuestionText placement"
      contains: "parseTensionTitle"
  key_links:
    - from: "CompassV2/src/components/LibraryDrawer.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { parseTensionTitle, getQuestionText }"
      pattern: "parseTensionTitle"
    - from: "CompassV2/src/pages/Quiz.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { parseTensionTitle, getQuestionText }"
      pattern: "parseTensionTitle"
    - from: "CompassV2/src/components/ComparePanel.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { parseTensionTitle, getQuestionText }"
      pattern: "parseTensionTitle"
---

<objective>
Update LibraryDrawer, CalibrationOverlay answer step, Quiz, and ComparePanel to use tension titles and properly place QuestionText between title and stances.

Purpose: After Plan 01 established the helpers and updated Library/calibration-pick, this plan completes the remaining views. The critical fix is removing broken `start_phrase` references (field was dropped in Phase 17) from Quiz.jsx and CalibrationOverlay answer step, and implementing the user's field-to-view mapping decisions for all stance-showing views.

Output: All five surfaces (Library cards, calibration pick/answer, quiz, compare panel, library drawer) display consistent topic names with QuestionText properly positioned.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-title-display-frontend/18-CONTEXT.md
@.planning/phases/18-title-display-frontend/18-01-SUMMARY.md
@CompassV2/src/components/LibraryDrawer.jsx
@CompassV2/src/components/CalibrationOverlay.jsx
@CompassV2/src/pages/Quiz.jsx
@CompassV2/src/components/ComparePanel.jsx
@CompassV2/src/util/topic.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LibraryDrawer and CalibrationOverlay answer step</name>
  <files>
    CompassV2/src/components/LibraryDrawer.jsx
    CompassV2/src/components/CalibrationOverlay.jsx
  </files>
  <action>
**LibraryDrawer.jsx:**

1. Import `parseTensionTitle` alongside the existing `getQuestionText` import from `"../util/topic"`.

2. **Header area** (line 252-253): Currently shows `topic.short_title` in uppercase tracking-wide text. Replace with the two-line tension title:
   - Use `parseTensionTitle(topic)` to get `{ name, poles }`.
   - Line 1: `name` in `text-base font-semibold text-neutral-800` (slightly larger than current, proper heading weight).
   - Line 2: `poles` in `text-sm text-gray-500 font-normal` (only if poles exists).
   - Remove the uppercase tracking-wide styling — tension titles should render naturally.

3. **QuestionText placement** (currently line 308-310): The `question` variable uses `getQuestionText(topic)`. The CONTEXT decision says QuestionText goes BETWEEN the tension title and stances. Update:
   - Keep the `question` variable computation.
   - Move the question text display so it appears after the tension title header and before the stance buttons.
   - Style: `italic font-medium text-gray-600 text-sm px-4 pt-2 pb-3` — visually distinct from bold title above and stance text below per user decision.
   - If `question` is empty (getQuestionText returns ""), skip rendering the question area entirely.

**CalibrationOverlay.jsx — Answer step (around line 455-457):**

1. Import `parseTensionTitle` from `"../util/topic"` (add to existing import from Plan 01).

2. **Question title** (line 455): Currently `<h1>{getQuestionText(currentTopic)}</h1>`. Replace with two-line tension title:
   - `const { name, poles } = parseTensionTitle(currentTopic)`
   - Line 1: `name` in `text-xl md:text-2xl font-semibold text-center`
   - Line 2: `poles` in `text-base text-gray-500 font-normal text-center mt-1` (only if poles exists)
   - Wrap in a `<div>` that replaces the `<h1>`.

3. **QuestionText as centered prompt** (new, between title and compass/stances): Add a QuestionText element below the tension title:
   - `const question = getQuestionText(currentTopic)`
   - If non-empty: render `<p className="text-center italic font-medium text-gray-600 text-sm px-4 mt-2 mb-3">{question}</p>`
   - If empty: skip. This gives the "standalone prompt feel" the user described for calibration.

4. **Fix broken start_phrase reference** (line 479-481): Currently renders `<h2>{currentTopic.start_phrase}...</h2>`. The `start_phrase` field was dropped in Phase 17 — this will show "undefined...". REMOVE this `<h2>` element entirely. The QuestionText added above serves as the bridge between the title and stances. If QuestionText is already shown above the stances, there's no need for a separate stance section heading.
  </action>
  <verify>
Run: `grep -rn "start_phrase" CompassV2/src/components/CalibrationOverlay.jsx` — should return zero matches.
Run: `grep -n "parseTensionTitle" CompassV2/src/components/LibraryDrawer.jsx CompassV2/src/components/CalibrationOverlay.jsx` — both files import it.
Run: `grep -n "italic" CompassV2/src/components/LibraryDrawer.jsx CompassV2/src/components/CalibrationOverlay.jsx` — QuestionText has italic styling in both.
  </verify>
  <done>
LibraryDrawer shows tension title as header with QuestionText below in italic. CalibrationOverlay answer step shows tension title + centered QuestionText prompt, no start_phrase reference. Both use parseTensionTitle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Quiz.jsx and ComparePanel.jsx</name>
  <files>
    CompassV2/src/pages/Quiz.jsx
    CompassV2/src/components/ComparePanel.jsx
  </files>
  <action>
**Quiz.jsx:**

1. Import `parseTensionTitle` alongside existing `getQuestionText` import from `"../util/topic"`.

2. **Full mode question title** (line 530-531): Currently `<h1>{getQuestionText(currentTopic)}</h1>`. Replace with two-line tension title:
   - `const { name: topicName, poles } = parseTensionTitle(currentTopic)` (compute once before the mode-specific renders)
   - Line 1: `topicName` in `text-xl md:text-2xl font-semibold text-center`
   - Line 2: `poles` in `text-base text-gray-500 font-normal text-center mt-1` (only if poles)
   - Below, add QuestionText: `const question = getQuestionText(currentTopic)` — if non-empty, render as `<p className="text-center italic font-medium text-gray-600 text-sm mt-2 mb-4 px-4">{question}</p>`

3. **Curated mode question title** (line 615): Same pattern as full mode — replace `<h1>{getQuestionText(currentTopic)}</h1>` with two-line tension title + QuestionText below.

4. **Fix broken start_phrase reference** (line 415-416 in stanceContent): Currently `<h2>{currentTopic.start_phrase}...</h2>`. The `start_phrase` field was dropped in Phase 17. REMOVE this `<h2>` entirely — the QuestionText now serves as the bridge between title and stances. The stances list follows directly after the QuestionText.

5. Clean up: If `getQuestionText` is no longer used elsewhere in Quiz.jsx after these changes, keep the import anyway (it's used for the QuestionText prompt).

**ComparePanel.jsx:**

1. Import `parseTensionTitle` alongside existing `getQuestionText` import from `"../util/topic"`.

2. **Topic dropdown options** (lines 131-136): Currently renders `{topic}` which is a `short_title` string. Replace with the topic's tension title (the Title field). Since `topicNames` is `Object.keys(answers)` (short_title keys), and we need the full title:
   - For each option, find the full topic object: `const fullTopic = topics.find(t => t.short_title === topic)`
   - Use `parseTensionTitle(fullTopic)` to get the name
   - Display: `{fullTopic ? parseTensionTitle(fullTopic).name : topic}` — shows just the topic name part in the dropdown (poles would be too long for a select option)
   - Keep the `value={topic}` as short_title (this is the data key, not the display)

3. **Question header** (line 144-146): Currently `<p>{getQuestionText(selectedTopic)}</p>`. Per CONTEXT: QuestionText appears between tension title and stances. Update:
   - First, add a tension title heading BEFORE the question text: `const { name, poles } = parseTensionTitle(selectedTopic)`
   - Render tension title: `<div className="px-5 pb-1"><h3 className="text-base font-semibold text-neutral-800">{name}</h3>{poles && <p className="text-sm text-gray-500 mt-0.5">{poles}</p>}</div>`
   - Then QuestionText below: style as `<p className="px-5 pb-3 text-sm italic font-medium text-gray-600">{getQuestionText(selectedTopic)}</p>` — only if non-empty.
   - Remove the existing `<p>` that just rendered getQuestionText in bold (line 144-146).

4. **QuestionText rule check**: Per CONTEXT: "If QuestionText is empty/missing: show nothing (skip the area entirely)." Wrap the QuestionText `<p>` in a conditional: only render if `getQuestionText(selectedTopic)` returns a non-empty string.
  </action>
  <verify>
Run: `grep -rn "start_phrase" CompassV2/src/pages/Quiz.jsx` — should return zero matches.
Run: `grep -rn "start_phrase" CompassV2/src/` — should return zero matches across entire frontend.
Run: `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build 2>&1 | tail -5` — build succeeds.
Run: `grep -n "parseTensionTitle" CompassV2/src/pages/Quiz.jsx CompassV2/src/components/ComparePanel.jsx` — both files import it.
  </verify>
  <done>
Quiz shows tension title + QuestionText prompt with no start_phrase. ComparePanel dropdown shows topic names from tension titles. QuestionText appears between title and stances in all stance-showing views. Zero start_phrase references remain in the frontend. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build` — succeeds with zero errors
2. `grep -rn "start_phrase" CompassV2/src/` — returns zero matches (broken field reference eliminated)
3. `grep -rn "Where do you stand" CompassV2/src/` — returns zero matches (prefix eliminated, confirmed from Plan 01)
4. `grep -n "parseTensionTitle" CompassV2/src/util/topic.js CompassV2/src/pages/Quiz.jsx CompassV2/src/components/LibraryDrawer.jsx CompassV2/src/components/CalibrationOverlay.jsx CompassV2/src/components/ComparePanel.jsx` — all five files reference the helper
5. `grep -n "italic" CompassV2/src/components/LibraryDrawer.jsx CompassV2/src/components/CalibrationOverlay.jsx CompassV2/src/pages/Quiz.jsx CompassV2/src/components/ComparePanel.jsx` — QuestionText has italic styling in all stance-showing views
</verification>

<success_criteria>
- LibraryDrawer header shows tension title (not short_title in uppercase), QuestionText appears italic between title and stances
- CalibrationOverlay answer step shows tension title heading + centered italic QuestionText prompt, no "undefined..." from start_phrase
- Quiz.jsx shows tension title heading + italic QuestionText prompt in both full and curated modes, no "undefined..." from start_phrase
- ComparePanel dropdown displays topic names from tension titles, QuestionText appears between title and stances
- Zero references to `start_phrase` remain in the frontend codebase
- A user scanning Library, calibration, and compass sees the same topic name in all three places
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-title-display-frontend/18-02-SUMMARY.md`
</output>
