---
phase: 63-headshot-research-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/scripts/generate_headshot_manifest.py
  - EV-Backend/scripts/headshot_research_manifest.csv
autonomous: true
requirements:
  - PHOTO-01

must_haves:
  truths:
    - "The manifest CSV includes every politician who needs research, including those from blocked cities"
    - "Each row has a politician_id column that maps directly to the essentials.politicians primary key"
    - "Each row has research tracking columns (found_url, source_page_url, research_status, research_date) initialized to empty/pending"
    - "Duplicate rows from the old manifest are eliminated"
  artifacts:
    - path: "EV-Backend/scripts/generate_headshot_manifest.py"
      provides: "Updated manifest generator with politician_id and research tracking columns"
      contains: "politician_id"
    - path: "EV-Backend/scripts/headshot_research_manifest.csv"
      provides: "Regenerated manifest with all columns, including blocked cities"
      contains: "politician_id,found_url,source_page_url,research_status,research_date"
  key_links:
    - from: "EV-Backend/scripts/generate_headshot_manifest.py"
      to: "essentials.politicians table"
      via: "SQL query joining politicians.id as politician_id"
      pattern: "p\\.id.*politician_id"
---

<objective>
Update the headshot manifest generator to include politician_id and research tracking columns, then regenerate the manifest with --include-blocked to produce the complete research checklist.

Purpose: The research plans (02-08) need a manifest with database IDs for direct Phase 64 upload and status tracking columns to record research progress. The current manifest lacks these fields and excludes blocked cities.

Output: Updated generate_headshot_manifest.py and regenerated headshot_research_manifest.csv with all ~300+ politicians including Burbank (blocked).
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-headshot-research-sprint/63-CONTEXT.md
@EV-Backend/scripts/generate_headshot_manifest.py
@EV-Backend/scripts/utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generate_headshot_manifest.py with politician_id and research columns</name>
  <files>EV-Backend/scripts/generate_headshot_manifest.py</files>
  <action>
Update the manifest generator script with these changes:

1. **Add politician_id to the SQL query and CSV output**: The existing MISSING_HEADSHOTS_SQL already selects `p.id` — ensure this maps to a `politician_id` column in the CSV output. This is the essentials.politicians primary key used for direct database operations in Phase 64.

2. **Add research tracking columns to CSV fieldnames**:
   - `politician_id` — integer, the `p.id` from the SQL query
   - `found_url` — string, empty by default, will hold the direct image URL (.jpg/.png)
   - `source_page_url` — string, empty by default, will hold the page where image was found
   - `research_status` — string, default `pending`; valid values: `pending`, `found`, `not_found`, `blocked`
   - `research_date` — string, empty by default, will hold ISO date when row was researched

3. **Update the matched row construction** (in `generate_manifest()`) to include the new columns:
   - `politician_id`: from `pol["id"]`
   - `found_url`: empty string
   - `source_page_url`: empty string
   - `research_status`: "pending"
   - `research_date`: empty string

4. **Update the CSV fieldnames list** to include the new columns after the existing ones:
   ```python
   fieldnames = [
       "city_name", "city_id", "council_url", "member_name", "role",
       "has_existing_override", "headshot_status",
       "politician_id", "found_url", "source_page_url", "research_status", "research_date",
   ]
   ```

5. **Fix duplicate rows**: The current manifest has 5 politicians appearing twice (300 rows, 295 unique). Add deduplication by tracking seen politician IDs:
   ```python
   seen_ids = set()
   for pol in missing:
       if pol["id"] in seen_ids:
           continue
       seen_ids.add(pol["id"])
       # ... rest of matching logic
   ```

6. **Update docstring** to reflect the new columns.

Do NOT change the DB query, city matching logic, or CLI argument handling — those work correctly.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-Backend/scripts && python3 -c "
import ast, sys
with open('generate_headshot_manifest.py') as f:
    source = f.read()
# Check new columns are in fieldnames
for col in ['politician_id', 'found_url', 'source_page_url', 'research_status', 'research_date']:
    assert col in source, f'Missing column: {col}'
# Check deduplication
assert 'seen_ids' in source or 'seen' in source, 'Missing deduplication logic'
print('All checks passed')
"</automated>
  </verify>
  <done>generate_headshot_manifest.py has politician_id column from DB query, 5 new CSV columns (politician_id, found_url, source_page_url, research_status, research_date), and deduplication logic</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate manifest with --include-blocked</name>
  <files>EV-Backend/scripts/headshot_research_manifest.csv</files>
  <action>
Run the updated manifest generator with `--include-blocked` to produce the complete research checklist:

```bash
cd EV-Backend/scripts
python3 generate_headshot_manifest.py --include-blocked
```

After generation, validate the output:
1. CSV has the correct header with all 12 columns
2. No duplicate politician rows (each politician_id appears exactly once)
3. All rows have `research_status` = `pending`
4. Blocked city (Burbank) politicians are included
5. politician_id column has integer values (not empty)
6. Total unique politicians should be ~295+ (the 295 unique from before, plus any Burbank politicians)

Print summary statistics for the SUMMARY.md:
- Total rows
- Unique cities
- Breakdown by headshot_status (scraped/failed/blocked)
- Count of rows needing research vs having overrides
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-Backend/scripts && python3 -c "
import csv
with open('headshot_research_manifest.csv') as f:
    reader = csv.DictReader(f)
    rows = list(reader)
    headers = reader.fieldnames
# Check headers
required = ['politician_id', 'found_url', 'source_page_url', 'research_status', 'research_date']
for h in required:
    assert h in headers, f'Missing header: {h}'
# Check no duplicates
ids = [r['politician_id'] for r in rows]
assert len(ids) == len(set(ids)), f'Duplicate IDs found: {len(ids)} rows vs {len(set(ids))} unique'
# Check all pending
statuses = set(r['research_status'] for r in rows)
assert statuses == {'pending'}, f'Expected all pending, got: {statuses}'
# Check politician_id not empty
empty_ids = [r for r in rows if not r['politician_id'].strip()]
assert len(empty_ids) == 0, f'{len(empty_ids)} rows with empty politician_id'
print(f'Valid manifest: {len(rows)} rows, {len(set(r[\"city_id\"] for r in rows))} cities, all pending, no duplicates')
"</automated>
  </verify>
  <done>headshot_research_manifest.csv regenerated with all columns, no duplicates, blocked cities included, all rows status=pending, every row has a valid politician_id</done>
</task>

</tasks>

<verification>
- `head -1 EV-Backend/scripts/headshot_research_manifest.csv` shows all 12 column headers
- `wc -l EV-Backend/scripts/headshot_research_manifest.csv` shows ~295+ rows (no duplicates, includes Burbank)
- Every row has a non-empty integer politician_id
- Every row has research_status=pending
</verification>

<success_criteria>
The manifest CSV is ready for the research sprint: every LA County local official without a Supabase headshot has a row with their database ID and pending research status, including those from Cloudflare-blocked cities.
</success_criteria>

<output>
After completion, create `.planning/phases/63-headshot-research-sprint/63-01-SUMMARY.md`
</output>
