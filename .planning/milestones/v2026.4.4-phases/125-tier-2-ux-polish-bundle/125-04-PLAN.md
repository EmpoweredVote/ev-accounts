---
phase: 125
plan: 04
type: execute
wave: 3
depends_on: [125-02]
files_modified:
  - treasury-tracker/src/components/BudgetSunburst.tsx
  - treasury-tracker/src/components/BudgetVisualization.tsx
autonomous: false
requirements: [UX-01]

must_haves:
  truths:
    - "G-114-025 resolved via one of two checkpoint-gated paths: (A) top-level sunburst segments display permanent labels readable without hover, OR (B) the sunburst toggle is removed and only the labeled bar chart remains"
    - "Path A is attempted first with a ≤2-hour bound; Path B is the fallback"
    - "Phase 125 closes with all 10 in-scope gaps either fixed in production or formally closed as already-resolved with evidence"
  artifacts:
    - path: "treasury-tracker/src/components/BudgetSunburst.tsx"
      provides: "EITHER permanent top-level labels (Path A) OR is removed/no-longer-imported (Path B)"
    - path: "treasury-tracker/src/components/BudgetVisualization.tsx"
      provides: "EITHER unchanged (Path A) OR sunburst option/branch removed from toggle (Path B)"
  key_links:
    - from: "BudgetSunburst.tsx"
      to: "BudgetVisualization.tsx"
      via: "import + render branch"
      pattern: "BudgetSunburst"
---

<objective>
Resolve G-114-025 (sunburst labels unreadable without hover) per CONTEXT D-08:
attempt Path A — add permanent labels for top-level budget categories with a width-threshold guard.
If Path A exceeds a 2-hour implementation budget, fall back to Path B — remove the sunburst
toggle entirely in favor of the already-labeled bar chart.

After Path A or Path B is committed and verified, this plan also runs the FINAL phase-125
production verification, signing off the full 10-gap set.

Purpose: Closes the last in-scope Tier 2 G-114 UX gap for UX-01. Wave 3 part B (parallel with 125-03).
Output: Production-deployed sunburst polish (or removal) + final phase verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-CONTEXT.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md
@treasury-tracker/src/components/BudgetSunburst.tsx
@treasury-tracker/src/components/BudgetVisualization.tsx

<interfaces>
From treasury-tracker/src/components/BudgetSunburst.tsx (lines 1-100 read in RESEARCH):
- D3 partition layout in useEffect; rebuilds SVG on every dep change
- formatCurrency, formatPercentage helpers at lines ~71-86
- Top-level partition nodes have depth === 1 (depth 0 is root)
- Each node has x0, x1 (angle in radians) and y0, y1 (radial extent)
- Arc length at outer radius ≈ (node.x1 - node.x0) * radius

From CONTEXT D-08 + RESEARCH §G-114-025:
- Path A preferred but optional; ≤2-hour internal checkpoint
- Path B is "remove sunburst toggle" — TRIVIAL, must locate the toggle render in BudgetVisualization.tsx

EV palette + Treasury palette unchanged. Antipartisan preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: G-114-025 Path A — Top-level sunburst labels (≤2-hour bounded)</name>
  <files>treasury-tracker/src/components/BudgetSunburst.tsx</files>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md §G-114-025 (Path A details)
    - .planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md "G-114-025" section
    - .planning/phases/125-tier-2-ux-polish-bundle/125-CONTEXT.md D-08 (acceptable to remove if complex)
    - treasury-tracker/src/components/BudgetSunburst.tsx (full file)
  </read_first>
  <action>
    INTERNAL TIME BUDGET: ≤2 hours of implementation effort. If at the 2-hour mark labels are not rendering correctly OR are causing visual regressions, STOP and proceed to Task 2 (Path B).

    Path A implementation:
    1. After d3 partition layout produces nodes, select top-level nodes only:
       ```ts
       const topLevel = root.descendants().filter(d => d.depth === 1);
       ```
    2. For each top-level node, compute arc length at the outer radius:
       ```ts
       const arcLengthPx = (node.x1 - node.x0) * radius;
       ```
       Hide labels for arcs below threshold (recommended start: 60px). This guards against unreadable narrow segments.
    3. Append `<text>` elements to the SVG group. Two viable approaches — pick whichever renders cleaner:
       - Straight horizontal text at the arc midpoint (compute centroid via d3.arc().centroid(node)). Add `text-anchor="middle"` and `dy="0.35em"` for vertical centering.
       - `<textPath>` along an invisible path following the arc curve. More effortful but visually nicer for wide arcs.
       Start with centroid-based horizontal text — simpler.
    4. Label content: use the node's category name. Truncate to fit (e.g., max 18 chars + "…").
    5. Styling: `fill="#1C1C1C"` (treasury dark), `fontSize="11px"`, `fontFamily="Manrope, sans-serif"`, `pointerEvents="none"` (don't block hover behavior).
    6. Re-render labels on resize (already in deps if useEffect lists containerRef-derived state).
    7. Test in local dev: load Bloomington operating budget — verify top-level category names visible without hover; narrow segments correctly suppress labels.

    If at 2-hour mark labels not stable: STOP, revert local changes via `git checkout treasury-tracker/src/components/BudgetSunburst.tsx`, set internal flag PATH_A_ABORTED=true in 125-04-SUMMARY.md draft, proceed to Task 2.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/treasury-tracker && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - EITHER: BudgetSunburst.tsx renders permanent top-level labels with width-threshold guard, no console errors, build passes (Path A success)
    - OR: changes reverted and Task 2 will execute (Path A aborted within budget)
    - SUMMARY.md notes which path was taken and why
    - **Path A success is human-judged at the Task 2 checkpoint** — the automated `<verify>` here only confirms `npm run build` succeeds and no console errors are emitted. Functional correctness (top-level labels readable at the default Treasury viewport without hover, narrow-arc threshold suppression behaving sensibly) is judged at Task 2's decision gate by the human reviewing the local dev render. If the human deems labels not readable / overlapping / regressing the visualization, they select `path-b-remove` and Task 3 executes the removal.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Path A vs Path B decision gate</name>
  <decision>Did Path A (Task 1) succeed? If not, switch to Path B (remove sunburst toggle).</decision>
  <context>
    Per CONTEXT D-08, removing the sunburst toggle entirely is an acceptable fix. If Path A
    produced clean labels and built successfully, skip Path B. If Path A was aborted at the
    2-hour bound or local testing reveals visual issues, execute Path B now.
  </context>
  <read_first>
    - Output of Task 1 (build status, local test result)
    - .planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md "Path B — remove sunburst toggle"
  </read_first>
  <options>
    <option id="path-a-keep">
      <name>Path A succeeded — keep sunburst with labels</name>
      <pros>Visualization preserved; users keep both view options</pros>
      <cons>More code to maintain; D3 label code is fiddly</cons>
    </option>
    <option id="path-b-remove">
      <name>Path B — remove sunburst toggle</name>
      <pros>Trivial; no fiddly D3 label code; bar chart already labeled</pros>
      <cons>Loses one visualization option (acceptable per D-08)</cons>
    </option>
  </options>
  <acceptance_criteria>
    - User selects "path-a-keep" OR "path-b-remove"
    - If "path-b-remove": executor proceeds to Task 3 (Path B implementation)
    - If "path-a-keep": Task 3 is SKIPPED; proceed directly to Task 4
  </acceptance_criteria>
  <resume-signal>Type "path-a-keep" or "path-b-remove".</resume-signal>
  <action>This is a human checkpoint task. The required human steps and decision criteria are documented in the &lt;how-to-verify&gt;, &lt;decision&gt;, &lt;options&gt;, and &lt;acceptance_criteria&gt; blocks above. The executor's role: present those instructions to the user, wait for the resume-signal response, and record the outcome in the plan SUMMARY.</action>
  <verify>
    <automated>echo "human checkpoint — see acceptance_criteria above"</automated>
  </verify>
  <done>Acceptance criteria listed above are met and the user has provided the resume-signal.</done>
</task>

<task type="auto">
  <name>Task 3: G-114-025 Path B — Remove sunburst toggle (only if Task 2 selected path-b-remove)</name>
  <files>treasury-tracker/src/components/BudgetVisualization.tsx, treasury-tracker/src/components/BudgetSunburst.tsx</files>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md "Path B" subsection
    - treasury-tracker/src/components/BudgetVisualization.tsx (full file — locate toggle render)
  </read_first>
  <action>
    SKIP this task if Task 2 selected "path-a-keep".

    Otherwise (Path B):
    1. Open treasury-tracker/src/components/BudgetVisualization.tsx. Identify:
       - The toggle UI (likely a button group or tabs switching between 'sunburst' and 'bar' modes)
       - The state variable holding the current visualization mode
       - The conditional render branch importing/using `<BudgetSunburst>`
    2. Remove from BudgetVisualization.tsx:
       - The toggle UI element offering the sunburst option (keep the bar option as the only/default)
       - The conditional render branch that mounts BudgetSunburst
       - The `import BudgetSunburst from './BudgetSunburst'` line
       - Any state initialization defaulting to 'sunburst' — change to 'bar' or remove the state if no longer needed
    3. Delete `treasury-tracker/src/components/BudgetSunburst.tsx` and any accompanying `BudgetSunburst.css` (use `git rm`).
    4. Run `npm run build` — verify no dangling references to BudgetSunburst.
    5. Run `grep -r "BudgetSunburst" treasury-tracker/src/` — should return no matches after this task.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/treasury-tracker && (! grep -r "BudgetSunburst" src/ 2>/dev/null) && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - BudgetSunburst.tsx (and any .css) deleted
    - BudgetVisualization.tsx no longer imports or renders BudgetSunburst
    - No grep matches for "BudgetSunburst" anywhere under treasury-tracker/src/
    - Treasury build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: FINAL phase-125 production verification (Wave 3 part B + full phase signoff)</name>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md "Validation Architecture" — full table
    - .planning/phases/125-tier-2-ux-polish-bundle/125-CONTEXT.md D-09 (per-wave gates)
    - .planning/phases/125-tier-2-ux-polish-bundle/125-01-SUMMARY.md (Wave 1 evidence)
    - .planning/phases/125-tier-2-ux-polish-bundle/125-02-SUMMARY.md (Wave 2 evidence)
    - .planning/phases/125-tier-2-ux-polish-bundle/125-03-SUMMARY.md (Wave 3 part A evidence)
  </read_first>
  <what-built>G-114-025 fix (Path A or Path B) deployed via Render. This is also the final phase signoff.</what-built>
  <how-to-verify>
    1. Wait for treasury-tracker Render redeploy.

    2. G-114-025 verification:
       - Path A taken: Visit Treasury, select Bloomington, switch to sunburst view. PASS if top-level segments display readable category names without hover. FAIL if labels missing or overlapping.
       - Path B taken: Visit Treasury, select Bloomington, look at the visualization toggle. PASS if no sunburst option exists (bar chart only). FAIL if sunburst toggle still present.

    3. FINAL phase 125 audit — re-verify each of the 10 in-scope gaps in production:
       | Gap | Quick check |
       |-----|-------------|
       | G-114-001 | essentials autocomplete (or document closed-as-resolved) |
       | G-114-002 | essentials Kirkwood header reads mixed-case |
       | G-114-004 | essentials Reps card shows "Also running" annotation |
       | G-114-011 | After Essentials IN search, Compass picker pre-selects IN |
       | G-114-014 | curl Pierce photo_origin_url returns CDN URL |
       | G-114-020 | Read & Rank tab title correct |
       | G-114-021 | Treasury Featured communities section visible |
       | G-114-023 | Monroe County FY notice visible |
       | G-114-024 | Bloomington 2025 in YearSelector |
       | G-114-025 | Sunburst labels OR sunburst toggle removed |

    4. Save complete evidence bundle to .planning/phases/125-tier-2-ux-polish-bundle/evidence/final/.

    5. Update .planning/STATE.md to mark Phase 125 complete; update ROADMAP.md Phase 125 row.

    6. Update REQUIREMENTS.md to check UX-01.
  </how-to-verify>
  <acceptance_criteria>
    - G-114-025 verified per chosen path
    - All 10 in-scope gaps either green-checked or documented as already-resolved with evidence
    - 125-04-SUMMARY.md and a top-level 125-PHASE-SUMMARY.md (consolidating waves) written
    - STATE.md, ROADMAP.md, REQUIREMENTS.md updated for phase + UX-01 closure
  </acceptance_criteria>
  <resume-signal>Type "phase 125 complete" or describe gaps still open.</resume-signal>
  <action>This is a human checkpoint task. The required human steps and decision criteria are documented in the &lt;how-to-verify&gt;, &lt;decision&gt;, &lt;options&gt;, and &lt;acceptance_criteria&gt; blocks above. The executor's role: present those instructions to the user, wait for the resume-signal response, and record the outcome in the plan SUMMARY.</action>
  <verify>
    <automated>echo "human checkpoint — see acceptance_criteria above"</automated>
  </verify>
  <done>Acceptance criteria listed above are met and the user has provided the resume-signal.</done>
</task>

</tasks>

<verification>
- G-114-025 resolved via Path A or Path B
- All 10 in-scope gaps independently verified in production
- Phase planning artifacts updated
</verification>

<success_criteria>
- UX-01 closed
- All in-scope gaps green or documented as already-resolved
- ROADMAP/STATE/REQUIREMENTS reflect phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/125-tier-2-ux-polish-bundle/125-04-SUMMARY.md`
</output>
