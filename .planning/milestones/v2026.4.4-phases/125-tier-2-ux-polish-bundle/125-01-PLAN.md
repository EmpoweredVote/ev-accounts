---
phase: 125
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/pages/Results.jsx
  - essentials/src/utils/address.js
autonomous: false
requirements: [UX-01]

must_haves:
  truths:
    - "G-114-001 verified in production: Google Places autocomplete dropdown either confirmed working OR confirmed broken with documented fix path"
    - "G-114-002 resolved: address in Essentials results header renders in mixed case (e.g., '200 W Kirkwood Ave, Bloomington, IN, 47404'), not ALL CAPS"
    - "G-114-004 resolved: Representatives card shows annotation when same person also appears in Elections data (e.g., 'Also running for [office]')"
    - "Wave 1 verified in production at essentials.empowered.vote before Wave 2 begins"
  artifacts:
    - path: "essentials/src/utils/address.js"
      provides: "toTitleCaseAddress() helper preserving state codes (IN), zip, directional letters (W,N,E,S)"
    - path: "essentials/src/pages/Results.jsx"
      provides: "Title-cased address render at line ~813; cross-reference annotation in renderPoliticianCard; eager elections fetch"
  key_links:
    - from: "essentials/src/pages/Results.jsx"
      to: "essentials/src/utils/address.js"
      via: "import { toTitleCaseAddress }"
      pattern: "toTitleCaseAddress\\("
    - from: "renderPoliticianCard"
      to: "electionsData (eager-loaded)"
      via: "useMemo Set<politician_id> lookup"
      pattern: "electionsCandidateIds\\.has"
---

<objective>
Polish the Essentials app: verify Google Places autocomplete in production (G-114-001),
title-case the address shown in the results header (G-114-002), and add a cross-reference
annotation on Representative cards when the same person also appears in the Elections tab
data (G-114-004).

Purpose: Closes 3 of 10 in-scope Tier 2 G-114 UX gaps for UX-01. First app-by-app wave per D-09.
Output: Production-deployed Essentials with verified autocomplete, mixed-case address display,
and cross-tab candidate annotations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-CONTEXT.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md
@.planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md
@essentials/src/pages/Results.jsx
@essentials/src/hooks/useGooglePlacesAutocomplete.js

<interfaces>
From essentials/src/pages/Results.jsx:
- formattedAddress (string, comes from usePoliticianData hook) — uppercase from Census Geocoder
- electionsData (array | null) — lazy-loaded by activeView==='elections' effect at lines 337-353
- electionsLoading (bool)
- renderPoliticianCard(pol) at lines 555-677 — returns the card JSX; pol.id is politician id
- ElectionsView candidate rows include politician_id linkage

Existing tab-dot indicator pattern (Results.jsx:800-803):
```jsx
{electionsData && electionsData.length > 0 && (
  <span className="w-2 h-2 rounded-full bg-[#FED12E] ml-1" />
)}
```

EV palette: ev-coral #ff5740, ev-muted-blue #00657c, ev-light-blue #59b0c4, ev-yellow #fed12e
Font: Manrope. Antipartisan: no party labels or red/blue color associations.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify G-114-001 in production (autocomplete smoke test)</name>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md §G-114-001
    - essentials/src/pages/Results.jsx (lines 392-398, 746-754) — autocomplete wiring
    - essentials/src/hooks/useGooglePlacesAutocomplete.js
  </read_first>
  <what-built>Per D-01 RESEARCH directs verification BEFORE coding. Hook is already wired; production state is unknown.</what-built>
  <how-to-verify>
    1. Open https://essentials.empowered.vote in a fresh browser (or incognito).
    2. Click the address input field.
    3. Type "200 W Kir" slowly (one character at a time) and wait 1-2 seconds.
    4. Expected PASS: Google Places dropdown appears showing "200 W Kirkwood Ave, Bloomington, IN..." or similar suggestions.
    5. Expected FAIL: No dropdown appears after 5 seconds of typing.
    6. Open browser DevTools Console — note any "Google Maps JavaScript API" errors or 401/403 responses.
    7. Note browser, OS, and any console errors.
  </how-to-verify>
  <acceptance_criteria>
    - User reports one of: "PASS — dropdown appeared" OR "FAIL — no dropdown, console shows: <error>"
    - Outcome recorded in .planning/phases/125-tier-2-ux-polish-bundle/125-01-SUMMARY.md under "G-114-001 verification"
    - If PASS: gap closed as already-resolved with timestamped evidence
    - If FAIL: open follow-up task in SUMMARY (env var check on Render: VITE_GOOGLE_MAPS_API_KEY; Google Cloud Console API key restrictions audit) — do NOT block Wave 1 on the autocomplete fix; treat as separate remediation
  </acceptance_criteria>
  <resume-signal>Type "PASS — closed", "FAIL — escalated to follow-up", or describe issue.</resume-signal>
  <action>This is a human checkpoint task. The required human steps and decision criteria are documented in the &lt;how-to-verify&gt;, &lt;decision&gt;, &lt;options&gt;, and &lt;acceptance_criteria&gt; blocks above. The executor's role: present those instructions to the user, wait for the resume-signal response, and record the outcome in the plan SUMMARY.</action>
  <verify>
    <automated>echo "human checkpoint — see acceptance_criteria above"</automated>
  </verify>
  <done>Acceptance criteria listed above are met and the user has provided the resume-signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G-114-002 — Title-case address in results header</name>
  <files>essentials/src/utils/address.js, essentials/src/pages/Results.jsx</files>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md §G-114-002
    - .planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md "G-114-002" section
    - essentials/src/pages/Results.jsx lines 805-820 (the render block to modify)
    - essentials/src/utils/sorters.js (existing util convention to match)
  </read_first>
  <behavior>
    - Test 1: toTitleCaseAddress('200 W KIRKWOOD AVE, BLOOMINGTON, IN, 47404') === '200 W Kirkwood Ave, Bloomington, IN, 47404'
    - Test 2: State code preserved — 'IN' stays 'IN' (not 'In')
    - Test 3: Directional letters preserved as uppercase — 'W' stays 'W', 'N' stays 'N', 'NE' stays 'NE'
    - Test 4: Zip preserved — '47404' stays '47404' (digits unaffected)
    - Test 5: Ordinal numerics intact — '1ST AVE' → '1st Ave'
    - Test 6: Empty/null input — toTitleCaseAddress('') === '' and toTitleCaseAddress(null) === ''
  </behavior>
  <action>
    1. Create essentials/src/utils/address.js exporting toTitleCaseAddress(input).
       Implementation approach: split input on comma, trim each segment; for each segment split on whitespace; for each word:
         - If matches /^[0-9]+(ST|ND|RD|TH)$/i → lowercase suffix (1st, 2nd, 3rd, 4th)
         - If is in known-uppercase set: ['IN','USA','US','CA','NY','TX','FL','OH','PA','IL','NC','MI','GA','NJ','VA','WA','AZ','MA','TN','MO','MD','WI','CO','MN','SC','AL','LA','KY','OR','OK','CT','UT','IA','NV','AR','MS','KS','NM','NE','WV','ID','HI','NH','ME','MT','RI','DE','SD','ND','AK','VT','WY','DC','N','S','E','W','NE','NW','SE','SW','NNE','NNW','SSE','SSW','ENE','ESE','WSW','WNW'] → keep uppercase
         - If pure digits → keep as-is
         - Else → lowercase, capitalize first letter
       Re-join words with space, segments with ', '.
    2. Add unit tests at essentials/src/utils/address.test.js (vitest if configured; else create minimal vitest setup; if no vitest, fall through to inline node assert script in package scripts).
    3. In essentials/src/pages/Results.jsx:
       - Add `import { toTitleCaseAddress } from '../utils/address';` at top
       - At line ~813 change `{formattedAddress}` → `{toTitleCaseAddress(formattedAddress)}`
    4. Do NOT modify backend, do NOT modify cache layer (per RESEARCH §G-114-002 Pitfall 2).
  </action>
  <verify>
    <automated>cd essentials && (npm test --silent 2>/dev/null || npx vitest run src/utils/address.test.js) && grep -q "toTitleCaseAddress(formattedAddress)" src/pages/Results.jsx</automated>
  </verify>
  <done>
    - essentials/src/utils/address.js exists with exported toTitleCaseAddress
    - Test file passes for all 6 behavior cases
    - Results.jsx imports and applies toTitleCaseAddress at the formattedAddress render site
    - No backend or geocodingService changes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: G-114-004 — Cross-reference Reps↔Elections candidate annotation</name>
  <files>essentials/src/pages/Results.jsx</files>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md §G-114-004
    - .planning/phases/125-tier-2-ux-polish-bundle/125-PATTERNS.md "G-114-004" section
    - essentials/src/pages/Results.jsx lines 337-353 (lazy-fetch effect to convert to eager)
    - essentials/src/pages/Results.jsx lines 555-677 (renderPoliticianCard)
    - essentials/src/pages/Results.jsx lines 800-803 (existing ev-yellow tab-dot pattern)
  </read_first>
  <behavior>
    - Test 1: When electionsData contains a candidate row whose politician_id matches the rendered pol.id, the card includes the annotation text "Also running"
    - Test 2: When no match, no annotation rendered
    - Test 3: While electionsData is null/loading, no annotation (graceful empty state, no flicker)
    - Test 4: electionsCandidateIds Set built only when electionsData changes (useMemo)
  </behavior>
  <action>
    1. In essentials/src/pages/Results.jsx, modify the elections-fetch useEffect at lines 337-353:
       - Remove the `if (activeView !== 'elections' || !activeQuery) return;` early return
       - Replace with `if (!activeQuery) return; if (electionsData !== null) return;`
       - This makes elections data load eagerly as soon as activeQuery is set, so cross-references are available on the Representatives view immediately.
    2. Add a useMemo after the elections-fetch effect:
       ```jsx
       const electionsCandidateIds = useMemo(() => {
         const set = new Set();
         if (!electionsData) return set;
         electionsData.forEach((election) => {
           (election.candidates || election.races || []).forEach((c) => {
             const pid = c.politician_id || c.politician?.id;
             if (pid) set.add(pid);
           });
           // Some shapes nest races->candidates; defensively handle nested shape:
           (election.races || []).forEach((race) => {
             (race.candidates || []).forEach((c) => {
               const pid = c.politician_id || c.politician?.id;
               if (pid) set.add(pid);
             });
           });
         });
         return set;
       }, [electionsData]);
       ```
       NOTE for executor: inspect actual electionsData shape from fetchElectionsByAddress before finalizing the traversal. Adjust traversal to match the actual schema. Add a one-line dev console.log during local testing if needed (remove before commit).
    3. In renderPoliticianCard (lines 555-677), find the existing ev-yellow accent block (~line 648). Append a sibling element when `electionsCandidateIds.has(pol.id)`:
       ```jsx
       {electionsCandidateIds.has(pol.id) && (
         <p className="text-xs text-[#fed12e] mt-1" style={{ fontFamily: "'Manrope', sans-serif" }}>
           Also running — see Elections tab
         </p>
       )}
       ```
       Use the matching candidate's office name when available: if the matched candidate row exposes `office` or `race.title`, prefer "Also running for {office} — see Elections tab".
    4. NO party labels, NO red/blue color associations (antipartisan, per CLAUDE.md and CONTEXT).
  </action>
  <verify>
    <automated>cd essentials && grep -n "electionsCandidateIds" src/pages/Results.jsx | head -5 && grep -q "Also running" src/pages/Results.jsx && grep -q "if (!activeQuery) return" src/pages/Results.jsx</automated>
  </verify>
  <done>
    - Eager-load: elections fetch no longer gated by activeView==='elections'
    - electionsCandidateIds useMemo built from electionsData
    - renderPoliticianCard appends "Also running..." annotation in ev-yellow text-xs when match
    - No party color or label introduced
    - Local dev verified: load Kirkwood address, see annotation on a known dual-status candidate (Deckard, Henry, or whoever is in the test data)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Production verification of Wave 1 (essentials.empowered.vote)</name>
  <read_first>
    - .planning/phases/125-tier-2-ux-polish-bundle/125-RESEARCH.md "Validation Architecture" table rows for 001/002/004
    - .planning/phases/125-tier-2-ux-polish-bundle/125-CONTEXT.md D-09 (per-wave production verification gate)
  </read_first>
  <what-built>Wave 1 fixes deployed via Render auto-deploy on merge to main of essentials repo.</what-built>
  <how-to-verify>
    1. Wait for Render to finish deploying essentials (check render dashboard or just wait ~3 min after push).
    2. Visit https://essentials.empowered.vote — fresh browser/incognito.
    3. G-114-001 (re-confirm): type "200 W Kir" — dropdown should appear (or matches outcome from Task 1).
    4. G-114-002: search "200 W Kirkwood Ave, Bloomington, IN, 47404". Header reads "Showing representatives for 200 W Kirkwood Ave, Bloomington, IN, 47404" (mixed case, NOT all caps).
    5. G-114-004: With same Kirkwood result loaded, look at a Representatives card that should also be a candidate (e.g., Deckard or Henry). Card displays "Also running..." annotation in ev-yellow.
    6. Capture screenshot of header (G-114-002) and the annotated card (G-114-004) into .planning/phases/125-tier-2-ux-polish-bundle/evidence/wave1/.
  </how-to-verify>
  <acceptance_criteria>
    - All three gaps (001 verified, 002 fixed, 004 fixed) confirmed in production
    - Screenshots saved under phase evidence directory
    - .planning/phases/125-tier-2-ux-polish-bundle/125-01-SUMMARY.md updated with production-verified status per gap
    - Wave 2 may begin only after this checkpoint approves (per D-09)
  </acceptance_criteria>
  <resume-signal>Type "Wave 1 verified — proceed to Wave 2" or describe issues.</resume-signal>
  <action>This is a human checkpoint task. The required human steps and decision criteria are documented in the &lt;how-to-verify&gt;, &lt;decision&gt;, &lt;options&gt;, and &lt;acceptance_criteria&gt; blocks above. The executor's role: present those instructions to the user, wait for the resume-signal response, and record the outcome in the plan SUMMARY.</action>
  <verify>
    <automated>echo "human checkpoint — see acceptance_criteria above"</automated>
  </verify>
  <done>Acceptance criteria listed above are met and the user has provided the resume-signal.</done>
</task>

</tasks>

<verification>
- All 3 Wave 1 gaps verified in production (or documented as already-resolved with evidence for 001)
- toTitleCaseAddress unit tests green
- No backend changes; no ev-ui changes
- Antipartisan principle preserved (no party colors / labels added)
</verification>

<success_criteria>
- G-114-001: production state recorded; either closed-as-resolved OR follow-up task filed for env-var/API-key fix
- G-114-002: production header displays mixed-case address
- G-114-004: production Representatives card displays cross-reference annotation when applicable
- Wave 1 production checkpoint approved before Wave 2 begins
</success_criteria>

<output>
After completion, create `.planning/phases/125-tier-2-ux-polish-bundle/125-01-SUMMARY.md`
</output>
