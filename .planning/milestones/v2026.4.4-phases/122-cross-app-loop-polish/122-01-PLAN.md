---
phase: 122-cross-app-loop-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/contexts/CompassContext.jsx
  - essentials/src/lib/compass.js
  - essentials/src/lib/treasury.js
  - essentials/src/lib/auth.js
  - essentials/src/pages/Results.jsx
  - essentials/.env.example
  - ev-accounts/backend/tests/treasury.cities.contract.test.ts
  - .planning/phases/122-cross-app-loop-polish/122-VALIDATION.md
  - .planning/phases/122-cross-app-loop-polish/evidence/
autonomous: false
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
user_setup:
  - service: render-essentials
    why: "New VITE_TREASURY_URL env var must be set on the Essentials Render service so production deep-links work."
    env_vars:
      - name: VITE_TREASURY_URL
        source: "Set to https://treasurytracker.empowered.vote on Render (Essentials service) > Environment"
    dashboard_config:
      - task: "Add VITE_TREASURY_URL build-time env var to essentials Render service and trigger redeploy"
        location: "Render Dashboard > Essentials > Environment"
must_haves:
  truths:
    - "A returning guest who previously calibrated the Compass and revisits an Essentials politician profile page (no #compass= fragment in URL) sees the CompassCard comparison overlay, not the Calibrate CTA."
    - "A returning logged-in user with empty API answers but a populated guestCompass localStorage entry still sees the overlay (D-14)."
    - "Every politician entry in the CompassV2 picker, when its 'View full profile on Essentials' link is clicked, opens an Essentials route that renders that politician's profile (INTG-02)."
    - "On essentials.empowered.vote Results for a Bloomington, IN address, a 'Explore Bloomington revenue and expenses' CTA link appears below the Bloomington Common Council local-tier section and links to https://treasurytracker.empowered.vote/?entity=bloomington-in."
    - "Local-tier sections whose body.title does not match a Treasury municipality with available_datasets>0 show NO CTA (no grayed-out state)."
    - "Production evidence (screenshots + DOM snippets) for each of the above is saved under .planning/phases/122-cross-app-loop-polish/evidence/ after Render deploy (D-13)."
  artifacts:
    - path: "essentials/src/lib/treasury.js"
      provides: "fetchTreasuryCities() wrapper around GET /api/treasury/cities + findMatchingMunicipality(bodyTitle, cities) + toTreasurySlug(city) helpers"
      exports: ["fetchTreasuryCities", "findMatchingMunicipality", "toTreasurySlug"]
    - path: "essentials/src/pages/Results.jsx"
      provides: "Treasury CTA rendered below each matched local-tier GovernmentBodySection"
      contains: "Explore"
    - path: "essentials/src/contexts/CompassContext.jsx"
      provides: "Fixed guest-priority chain that preserves guestCompass when authed user has empty API answers; dev-mode probe log for diagnosis"
    - path: "essentials/.env.example"
      provides: "VITE_TREASURY_URL default documented"
      contains: "VITE_TREASURY_URL"
    - path: "ev-accounts/backend/tests/treasury.cities.contract.test.ts"
      provides: "Vitest contract test locking /api/treasury/cities response shape (id, name, state, available_datasets, entity_type)"
  key_links:
    - from: "essentials/src/contexts/CompassContext.jsx"
      to: "essentials/src/lib/compass.js :: loadGuestCompass"
      via: "guest-priority fallback when authed answers are empty"
      pattern: "loadGuestCompass\\("
    - from: "essentials/src/pages/Results.jsx"
      to: "/api/treasury/cities"
      via: "fetchTreasuryCities() in useEffect on mount"
      pattern: "fetchTreasuryCities"
    - from: "essentials/src/pages/Results.jsx"
      to: "treasurytracker.empowered.vote"
      via: "anchor href built from VITE_TREASURY_URL + toTreasurySlug(match)"
      pattern: "VITE_TREASURY_URL|treasurytracker"
    - from: "CompassV2/src/components/ComparePanel.jsx"
      to: "essentials/src/pages/Profile.jsx (via /politician/:id)"
      via: "serializeCompassFragment() appended to link href — verification only"
      pattern: "serializeCompassFragment"
tags:
  - cross-app
  - compass
  - essentials
  - treasury
  - relay
---

<objective>
Close the three remaining voter-loop integration gaps (INTG-01, INTG-02, INTG-03) in a single plan,
single Render deploy, with atomic commits per task. The work is:

1. Wave 0 — add a dev-mode probe log in `CompassContext` guest-priority chain + a backend Vitest
   contract test locking the `/api/treasury/cities` shape. This is the diagnostic + verification
   scaffolding required by VALIDATION.md.
2. Wave 1, Task 1 — **diagnose INTG-01**: identify which of the three relay layers is broken
   (fragment write, localStorage read, or render gate) with evidence. Checkpoint-gated (D-02).
   Do not proceed until the root cause is confirmed.
3. Wave 1, Task 2 — **fix INTG-01 at the confirmed broken layer only** (D-04, no follow-through).
   Most likely site: `CompassContext.jsx §104–141` authed+empty priority-chain branch that
   calls `clearGuestCompass()` blindly.
4. Wave 2 — **verify INTG-02** via evidence on production picker entries. Add code ONLY if the
   picker surfaces a candidate-typed UUID that `/politician/:id` cannot resolve (D-07 evidence-only
   default).
5. Wave 3 — **implement INTG-03**: add `essentials/src/lib/treasury.js`, wire a one-shot fetch in
   `Results.jsx` `useEffect`, render the Treasury CTA below matched local-tier
   `GovernmentBodySection` blocks, add `VITE_TREASURY_URL` env var.
6. Final — production smoke + evidence capture under `evidence/` (D-13).

Purpose: the voter loop (Compass → click through to Essentials profile with compass state hydrated
→ click through to Treasury Tracker for the relevant municipality) must work end-to-end on
production before Tier 1 close.

Output: one merged PR containing the three fixes, production evidence saved in phase dir,
VALIDATION.md Per-Task Verification Map populated, `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/122-cross-app-loop-polish/122-CONTEXT.md
@.planning/phases/122-cross-app-loop-polish/122-RESEARCH.md
@.planning/phases/122-cross-app-loop-polish/122-VALIDATION.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-CONTEXT.md
@essentials/src/contexts/CompassContext.jsx
@essentials/src/lib/compass.js
@essentials/src/components/CompassCard.jsx
@essentials/src/pages/Results.jsx
@CompassV2/src/components/ComparePanel.jsx
@CompassV2/src/components/CompassContext.jsx
@ev-accounts/backend/src/routes/treasury.ts
@ev-accounts/backend/src/lib/treasuryService.ts
@treasury-tracker/src/App.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase via RESEARCH.md. -->

From `ev-accounts/backend/src/lib/treasuryService.ts` (getCities):
```ts
type TreasuryCity = {
  id: string;            // UUID
  name: string;          // e.g. "Bloomington"
  state: string;         // e.g. "IN"
  entity_type: string;   // e.g. "city" | "county" | "township"
  population: number | null;
  hero_image_url: string | null;
  created_at: string;
  updated_at: string;
  available_datasets: Array<{ fiscal_year: number; dataset_type: string }>;
};
```
Has-data predicate: `available_datasets.length > 0`.

From `essentials/src/lib/compass.js`:
```js
export const GUEST_COMPASS_KEY = "guestCompass";
export function saveGuestCompass(answers, selectedTopics, invertedSpokes): void;
export function loadGuestCompass(): { answers, selectedTopics, invertedSpokes } | null;
export function clearGuestCompass(): void;
export function parseCompassFragment(): { answers, selectedTopics, invertedSpokes } | null; // strips hash via history.replaceState
export function convertGuestAnswersToApiFormat(answers, topics): Array<{topic_id, value}>;
```

From `CompassV2/src/components/CompassContext.jsx` §281-295:
```js
export function serializeCompassFragment(): string; // "" if answers empty, else "#compass=" + btoa(JSON.stringify({a,s,i}))
```

Treasury Tracker deep-link (treasury-tracker/src/App.tsx §30):
```
slug = `${name.toLowerCase().replace(/\s+/g, '-')}-${state.toLowerCase()}`
URL  = `${VITE_TREASURY_URL}/?entity=${slug}`
```
</interfaces>

<prior_phase_reference>
Phase 118 (verdict badge fix) is the same component surface and established the production-
verification discipline (D-13/D-14 here mirror Phase 118 D-15). Read `.planning/phases/118-
read-rank-verdict-badge-fix/118-CONTEXT.md` for the cross-app verification pattern.
</prior_phase_reference>
</context>

<tasks>

<!-- ============================================================ -->
<!-- WAVE 0 — diagnostic harness + contract test (VALIDATION.md)   -->
<!-- ============================================================ -->

<task type="auto" tdd="true">
  <name>Task 0.1: Backend contract test for GET /api/treasury/cities</name>
  <files>ev-accounts/backend/tests/treasury.cities.contract.test.ts</files>
  <behavior>
    - Test 1: GET /api/treasury/cities returns HTTP 200 with an array body.
    - Test 2: Each element has keys: id (string), name (string), state (string), entity_type (string), available_datasets (array).
    - Test 3: available_datasets entries (when present) have fiscal_year (number) and dataset_type (string).
    - Test 4: At least one row named "Bloomington" with state "IN" exists in the dev fixture OR the test tolerates empty list but fails if a non-array is returned.
  </behavior>
  <action>
    Add a Vitest integration test protecting the /api/treasury/cities response shape relied on by
    Essentials INTG-03 (per VALIDATION.md Wave 0 requirement + D-09 dynamic detection).
    Use the existing vitest setup in `ev-accounts/backend/tests/` (follow sibling test file patterns
    for supertest-style Express bootstrapping; import the app from `src/app.ts` or equivalent,
    matching existing tests). Do NOT mock the Supabase layer beyond what sibling tests already do —
    this is a contract test; if the existing test harness uses a test DB fixture, use it; if it
    uses real dev-DB, ensure the assertions are shape-only, not value-equality on a specific row
    count. The Bloomington row assertion should be SKIPPED with a warning if the test DB does not
    contain Treasury fixtures (so the test does not become a deploy blocker for non-treasury PRs).
    Commit message: `test(122-01): lock /api/treasury/cities response shape for INTG-03`.
  </action>
  <verify>
    <automated>cd ev-accounts/backend && npm test -- treasury.cities.contract</automated>
  </verify>
  <done>Test file exists; `npm test -- treasury.cities.contract` passes locally; atomic commit made.</done>
</task>

<task type="auto">
  <name>Task 0.2: Dev-mode probe log in CompassContext guest-priority chain</name>
  <files>essentials/src/contexts/CompassContext.jsx</files>
  <action>
    Add a dev-only probe (gated on `import.meta.env.DEV`) inside the effect that runs the guest
    priority chain (~lines 104–147 per RESEARCH.md). The probe must log, in order, a single
    structured object: `{ stage: 'compass-priority', authedUser: !!authedUser, hasFragment:
    !!fragment, fragmentAnswerCount: fragment?.answers ? Object.keys(fragment.answers).length : 0,
    apiAnswerCount: apiAnswers?.length ?? 0, guestCacheAnswerCount: cached?.answers ? Object.keys(cached.answers).length : 0,
    resolvedSource: 'api' | 'fragment' | 'storage' | 'empty', clearedGuestCompass: boolean,
    userAnswersAfterConvertLength: number }`.

    This probe is REMOVED or left behind an `import.meta.env.DEV` gate — production builds must
    not emit it. Commit message: `chore(122-01): add dev-mode compass-priority probe for INTG-01 diagnosis`.

    Do NOT change behaviour in this task. This is instrumentation only.
  </action>
  <verify>
    <automated>cd essentials && npm run build</automated>
  </verify>
  <done>Probe logs in `npm run dev` console when loading a profile page; production build succeeds; no behaviour change.</done>
</task>

<task type="auto">
  <name>Task 0.3: Populate VALIDATION.md Per-Task Verification Map + flip nyquist_compliant</name>
  <files>.planning/phases/122-cross-app-loop-polish/122-VALIDATION.md</files>
  <action>
    Edit the Per-Task Verification Map section to list every task ID in this plan (0.1–3.3 below)
    with its `<automated>` verify command (or "manual-only" with link to the Manual-Only
    Verifications row when applicable). Confirm no 3-consecutive manual-only tasks. Once the map
    is populated and reviewed, flip `nyquist_compliant: true` in frontmatter.
    Commit: `docs(122): populate validation per-task map and flip nyquist_compliant`.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/122-cross-app-loop-polish/122-VALIDATION.md &amp;&amp; grep -q "Task 3.3" .planning/phases/122-cross-app-loop-polish/122-VALIDATION.md</automated>
  </verify>
  <done>VALIDATION.md map has rows for every task; frontmatter shows `nyquist_compliant: true`.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 1 — INTG-01 diagnose → fix (D-02, D-04)                  -->
<!-- ============================================================ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.1 (checkpoint): Diagnose INTG-01 root cause — confirm broken layer before writing fix</name>
  <what-built>
    Dev-mode probe from Task 0.2 is live. Fragment chain primitives (`serializeCompassFragment`,
    `parseCompassFragment`, `saveGuestCompass`, `loadGuestCompass`) are unchanged.
  </what-built>
  <how-to-verify>
    Run the three-layer diagnostic from RESEARCH.md §"INTG-01 Diagnostic Plan". Use Pierce UUID
    `72dd5219-490f-48bb-986e-183a6098d602`.

    **Layer A (arrival-time fragment write):**
    1. `cd essentials && npm run dev` + `cd ../CompassV2 && npm run dev` (different ports).
    2. As guest, on CompassV2, answer >=1 topic. Open Compare panel, click "View full profile on Essentials" for Pierce.
    3. On arrival at Essentials, record: `location.hash` before fragment strip; `localStorage.getItem('guestCompass')` after the CompassContext effect runs.
    4. Record the probe output from Task 0.2.

    **Layer B (return-visit localStorage read):**
    1. Close tab. Revisit `http://localhost:5173/politician/72dd5219-...` directly (no `#compass=`).
    2. Record: `localStorage.getItem('guestCompass')`; probe output; `userAnswers` length from React DevTools on CompassContext provider value.

    **Layer C (render condition):**
    1. In React DevTools inspect CompassCard props/render branch (line 71 `hasUserCompass`, line 69 `politicianIdsWithStances` gate).
    2. Confirm Pierce UUID is in `politicianIdsWithStances` set (Pitfall 4).

    **Authed path (D-14):**
    1. Log in, repeat Layer B.
    2. Record whether `authedUser` path at §128 calls `clearGuestCompass()` blindly.

    **Capture evidence:** Save DevTools console transcripts + React DevTools screenshots to
    `.planning/phases/122-cross-app-loop-polish/evidence/intg-01-diagnosis/`.

    **Report back:**
    - Which layer broke first? (A / B / C / authed-clear)
    - Root-cause hypothesis from RESEARCH.md confirmed or refuted?
    - Any surprise (e.g. `extractHashToken` wiping the fragment — Pitfall 2)?
    - Fix location (file + line range) the developer intends to edit in Task 1.2.
  </how-to-verify>
  <resume-signal>
    Reply with: "diagnosed: &lt;layer&gt; at &lt;file&gt;:&lt;line&gt;, fix plan: &lt;one sentence&gt;"
    OR "blocked: &lt;reason&gt;". Do not proceed to Task 1.2 until a root cause is confirmed (D-02).
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Fix INTG-01 at the confirmed broken layer (D-04 — single layer only)</name>
  <files>essentials/src/contexts/CompassContext.jsx, essentials/src/lib/compass.js, essentials/src/lib/auth.js</files>
  <behavior>
    After the fix, on a direct-visit reload of `/politician/{pierce-id}` with no fragment and a
    populated `guestCompass` localStorage entry:
    - Probe must log `resolvedSource: 'storage'` (guest) OR `'api'` with `userAnswersAfterConvertLength &gt; 0` (authed with API data) OR `'storage'` with fallback (authed with empty API data) — NOT `'empty'`.
    - `CompassCard` must render the `hasUserCompass` branch (line 165), not the guest-fallback branch (line 342).
    - `clearGuestCompass()` must NOT be called when `authedUser` is truthy but `apiAnswers.length === 0` AND `guestCompass` is present.
    - Guest-only path: `loadGuestCompass()` return value reaches `setUserAnswers` via `convertGuestAnswersToApiFormat`.
  </behavior>
  <action>
    Implement ONLY the fix for the layer identified in Task 1.1. Per D-04, do not refactor
    adjacent layers. Based on researcher's prior-probability ranking the most likely edit site
    is `CompassContext.jsx §104–128` — guard the `clearGuestCompass()` call so it fires ONLY when
    `authedUser` is truthy AND `apiAnswers.length > 0` (i.e. the API has authoritatively superseded
    the guest cache). When `apiAnswers.length === 0`, preserve the guest cache and fall through
    to the `loadGuestCompass()` branch to hydrate `userAnswers`. If diagnosis instead points at
    Pitfall 2 (extractHashToken wiping the compass fragment), fix by changing
    `extractHashToken` in `essentials/src/lib/auth.js` to only strip the `#access_token=` segment
    of the hash (or to run AFTER `parseCompassFragment`).

    Per D-16: if diagnosis unexpectedly points at `ev-ui` (e.g., StanceAccordion), STOP and
    convert this task to an `ev-ui` auto-bump task (separate commit: `npm version patch`, tag,
    CI publish, consumer PR). This is unlikely but must be flagged in the task summary.

    Reference the implemented decision IDs in the commit message. Commit:
    `fix(122-01): INTG-01 CompassCard state relay — preserve guestCompass when authed API answers empty (per D-01/D-04)`.
  </action>
  <verify>
    <automated>cd essentials && npm run build</automated>
  </verify>
  <done>
    Local smoke: Pierce profile direct-visit (no fragment) shows the CompassCard comparison
    overlay for both guest and logged-in-with-empty-API states. `npm run build` succeeds. Atomic
    commit made. Per D-04, NO additional layers modified in this commit.
  </done>
</task>

<!-- ============================================================ -->
<!-- WAVE 2 — INTG-02 verification (D-06, D-07, evidence-only default) -->
<!-- ============================================================ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.1 (checkpoint): Verify INTG-02 — cycle picker, capture link-resolution evidence</name>
  <what-built>
    No code change yet. This is the evidence-first step per D-06/D-07. INTG-01 fix is live
    locally so picker links now hydrate correctly on arrival (covers D-14 authed+guest).
  </what-built>
  <how-to-verify>
    On CompassV2 Compare panel (local dev):
    1. For EVERY politician in the picker, click "View full profile on Essentials". Confirm
       the link opens a working Essentials profile with CompassCard hydrated.
    2. If any entry is a candidate (`is_candidate=true` on the compass/politicians row — check
       Phase 117 plan 04 implementation), note whether `/politician/:id` resolves or whether
       the Essentials Profile page 404s / errors. If errors: the picker needs an `is_candidate`
       flag and `ComparePanel` needs to branch between `/politician/:id` and `/candidate/:id`.
    3. Save screenshot evidence per picker entry type (at minimum: 1 politician, 1 candidate
       if present) to `.planning/phases/122-cross-app-loop-polish/evidence/intg-02-verification/`.

    **Decide & report:**
    - All picker links resolve → no code change, INTG-02 closes evidence-only (D-07).
    - One or more picker entries fail → proceed to Task 2.2 (code fix); specify which file/line.
  </how-to-verify>
  <resume-signal>
    Reply with: "intg-02: evidence-only" OR "intg-02: code-fix-needed at &lt;file&gt;:&lt;line&gt;".
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2.2 (conditional): INTG-02 code fix — only if Task 2.1 surfaced a broken picker entry</name>
  <files>CompassV2/src/components/ComparePanel.jsx, ev-accounts/backend/src/lib/compassService.ts</files>
  <action>
    SKIP THIS TASK if Task 2.1 resolved as "evidence-only". If code fix is required:
    (a) Add `is_candidate` field to `/api/compass/politicians` response in
        `compassService.getCompassPoliticians` (minimal, additive change).
    (b) In `ComparePanel.jsx:120`, branch the link target:
        `${ESSENTIALS_URL}/${politician.is_candidate ? 'candidate' : 'politician'}/${politician.id}${serializeCompassFragment()}`.
    (c) Alternative per RESEARCH.md: make Essentials `/politician/:id` Profile page fall through
        to candidate rendering when the politician row has `is_candidate=true`. Prefer this if
        Phase 117 already unified the data model — discuss in Task 2.1 resume-signal.

    Commit: `fix(122-01): INTG-02 route candidate picker entries to /candidate/:id (per D-06)` OR
    document as skipped in task summary if D-07 path.
  </action>
  <verify>
    <automated>cd ev-accounts/backend && npm run typecheck &amp;&amp; cd ../../CompassV2 && npm run build</automated>
  </verify>
  <done>Skipped with evidence (D-07) OR committed fix with picker entries routing correctly.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 3 — INTG-03 Treasury CTA implementation                   -->
<!-- ============================================================ -->

<task type="auto" tdd="true">
  <name>Task 3.1: Create essentials/src/lib/treasury.js with fetch + match + slug helpers</name>
  <files>essentials/src/lib/treasury.js, essentials/.env.example</files>
  <behavior>
    - `toTreasurySlug({name:'Bloomington', state:'IN'})` returns `'bloomington-in'` (RESEARCH.md §"Don't Hand-Roll" — matches treasury-tracker/src/App.tsx §30 slug format).
    - `toTreasurySlug({name:'Monroe County', state:'IN'})` returns `'monroe-county-in'`.
    - `findMatchingMunicipality('Bloomington Common Council', cities)` returns the Bloomington city object when `available_datasets.length > 0`.
    - `findMatchingMunicipality('Monroe County Government', cities)` returns null when the only city in `cities` is Bloomington.
    - `findMatchingMunicipality(title, cities)` returns null for `available_datasets.length === 0` rows (has-data predicate).
    - When multiple municipalities match (e.g., "Bloomington" and "Bloomington Township"), the longest-name match wins.
    - `fetchTreasuryCities()` returns `[]` on network error or non-ok response (never throws).
  </behavior>
  <action>
    Create new file `essentials/src/lib/treasury.js` implementing:
    ```js
    import { apiFetch } from './auth';

    export async function fetchTreasuryCities() {
      try {
        const res = await apiFetch('/treasury/cities');
        if (!res || !res.ok) return [];
        const body = await res.json();
        return Array.isArray(body) ? body : [];
      } catch { return []; }
    }

    export function toTreasurySlug(city) {
      return `${city.name.toLowerCase().replace(/\s+/g, '-')}-${city.state.toLowerCase()}`;
    }

    function normalize(s) { return (s || '').toLowerCase().replace(/\s+/g, ' ').trim(); }

    export function findMatchingMunicipality(bodyTitle, cities) {
      if (!bodyTitle || !Array.isArray(cities)) return null;
      const t = normalize(bodyTitle);
      const candidates = cities.filter(c =>
        c.available_datasets?.length > 0 && t.startsWith(normalize(c.name))
      );
      if (candidates.length === 0) return null;
      return candidates.sort((a, b) => b.name.length - a.name.length)[0];
    }
    ```
    Verify the `apiFetch` import path matches sibling files in `essentials/src/lib/` (use the
    same helper already used by `compass.js` / `auth.js`).

    Add to `essentials/.env.example` (creating it if absent):
    ```
    VITE_TREASURY_URL=https://treasurytracker.empowered.vote
    ```
    Do NOT commit `.env` (per CLAUDE.md global rule). Confirm `.env*` is gitignored.

    Commit: `feat(122-01): INTG-03 add treasury.js helpers and VITE_TREASURY_URL env (per D-09/D-11)`.
  </action>
  <verify>
    <automated>cd essentials && npm run build</automated>
  </verify>
  <done>File exists with three exports; `.env.example` contains `VITE_TREASURY_URL`; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2: Wire Treasury CTA into Results.jsx under matched local-tier sections</name>
  <files>essentials/src/pages/Results.jsx</files>
  <behavior>
    - On Results mount, `fetchTreasuryCities()` is called once (memoized in state). No per-card fetch (anti-pattern §RESEARCH.md).
    - For each local-tier GovernmentBodySection where `findMatchingMunicipality(body.title, cities)` returns a non-null match, an anchor renders immediately AFTER the `</GovernmentBodySection>` closing tag (D-12 placement).
    - Anchor text: exactly `Explore {match.name} revenue and expenses →` (D-10, no dollar amount; arrow is inline SVG matching ReturnBanner style — ev-light-blue #59b0c4, hover #00657c, Manrope font).
    - Anchor href: `${TREASURY_URL}/?entity=${toTreasurySlug(match)}` where `TREASURY_URL = import.meta.env.VITE_TREASURY_URL || 'https://treasurytracker.empowered.vote'`.
    - Non-matching local-tier sections render NO CTA (D-12 — no disabled/grayed state).
    - Federal/state tier sections render NO CTA (only tier === 'Local').
    - If `fetchTreasuryCities()` failed (returned []), no CTAs render anywhere (graceful degradation).
  </behavior>
  <action>
    Edit `essentials/src/pages/Results.jsx`:
    1. Import at top: `import { fetchTreasuryCities, findMatchingMunicipality, toTreasurySlug } from '../lib/treasury';`
    2. Add `const TREASURY_URL = import.meta.env.VITE_TREASURY_URL || 'https://treasurytracker.empowered.vote';` near other env constants.
    3. Add state: `const [treasuryCities, setTreasuryCities] = useState([]);`
    4. Add one-shot effect: `useEffect(() => { fetchTreasuryCities().then(setTreasuryCities); }, []);`
    5. At the local-tier rendering site (per RESEARCH.md §920-954) — immediately after the
       `</GovernmentBodySection>` closing tag — insert:
       ```jsx
       {tier === 'Local' && (() => {
         const match = findMatchingMunicipality(body.title, treasuryCities);
         if (!match) return null;
         return (
           <div className="mt-2 mb-4">
             <a
               href={`${TREASURY_URL}/?entity=${toTreasurySlug(match)}`}
               target="_blank"
               rel="noopener noreferrer"
               className="inline-flex items-center gap-1 text-sm text-[#59b0c4] hover:text-[#00657c] transition-colors"
               style={{ fontFamily: "'Manrope', sans-serif" }}
             >
               Explore {match.name} revenue and expenses
               <svg viewBox="0 0 16 16" className="w-3 h-3 ml-1" fill="currentColor" aria-hidden="true">
                 <path fillRule="evenodd" d="M4.22 11.78a.75.75 0 010-1.06L9.44 5.5H5.75a.75.75 0 010-1.5h5.5a.75.75 0 01.75.75v5.5a.75.75 0 01-1.5 0V6.56l-5.22 5.22a.75.75 0 01-1.06 0z" clipRule="evenodd"/>
               </svg>
             </a>
           </div>
         );
       })()}
       ```
    6. Note: `target="_blank" rel="noopener noreferrer"` per OWASP / security_threat_model T-122-02.

    Commit: `feat(122-01): INTG-03 render Treasury CTA below matched local-tier sections (per D-08/D-10/D-12)`.
  </action>
  <verify>
    <automated>cd essentials && npm run build</automated>
  </verify>
  <done>
    Local dev smoke (Bloomington IN address): CTA appears below Bloomington Common Council
    section; clicks open `treasurytracker.empowered.vote/?entity=bloomington-in` in a new tab.
    No CTA under Monroe County Government or township sections (until those are added to
    Treasury). Build passes; atomic commit made.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.3 (checkpoint): Production smoke + evidence capture (D-13)</name>
  <what-built>
    All three INTG fixes merged locally. Render auto-deploys essentials + ev-accounts on main
    merge. `VITE_TREASURY_URL` set on Essentials Render service (user_setup — human must do this
    before merge or CTA links break on prod default).
  </what-built>
  <how-to-verify>
    After Render deploy:
    1. Hit `https://api.empowered.vote/api/treasury/cities` and confirm response includes
       Bloomington/IN with `available_datasets.length > 0`. Save `curl` output to
       `evidence/intg-03-prod/treasury-cities.json`.
    2. On `https://compass.empowered.vote`, as guest, answer >=1 topic, click "View full profile
       on Essentials" for Pierce. Confirm CompassCard renders hydrated on Essentials. Screenshot
       to `evidence/intg-01-prod/guest-arrival.png`.
    3. Close tab. Revisit `https://essentials.empowered.vote/politician/72dd5219-490f-48bb-986e-183a6098d602`
       directly (no fragment). Confirm CompassCard still renders with overlay. Screenshot to
       `evidence/intg-01-prod/guest-direct-revisit.png`.
    4. Log in, repeat step 3. Screenshot to `evidence/intg-01-prod/authed-direct-revisit.png`
       (D-14).
    5. On `https://compass.empowered.vote`, cycle picker. Confirm all links resolve. Save
       DevTools network/DOM evidence to `evidence/intg-02-prod/`.
    6. Enter a Bloomington IN address on `https://essentials.empowered.vote`. Scroll to local
       tier. Screenshot the CTA under Bloomington Common Council (`evidence/intg-03-prod/
       bloomington-cta.png`). Click it — confirm Treasury Tracker loads Bloomington view.
       Screenshot `evidence/intg-03-prod/treasury-landing.png`.
    7. Same address — confirm no CTA under Monroe County Government (negative case). Screenshot
       `evidence/intg-03-prod/county-no-cta.png`.

    All evidence saved to `.planning/phases/122-cross-app-loop-polish/evidence/`.
  </how-to-verify>
  <resume-signal>
    Reply with: "prod verified — all 3 INTG pass" AND list the saved evidence paths, OR
    "prod failed: &lt;which INTG&gt; at &lt;URL&gt; — &lt;symptom&gt;".
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompassV2 origin → URL fragment → Essentials origin | Cross-origin guest state relay. The `#compass=` payload crosses origin boundaries and is written to essentials-domain localStorage. |
| Essentials client → /api/treasury/cities | Public unauthenticated API read (optionalAuth). |
| Essentials client → treasurytracker.empowered.vote (outbound link) | Cross-app navigation to another empowered.vote subdomain. Destination URL is constructed from API data (municipality name + state). |
| Essentials client → /politician/:id and /candidate/:id routes | Internal same-origin navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | `parseCompassFragment` in `essentials/src/lib/compass.js` — decodes base64 JSON from untrusted URL fragment and writes to localStorage | mitigate | Existing implementation wraps `JSON.parse(atob(...))` in try/catch returning null on failure (RESEARCH.md VERIFIED compass.js §117-157). Additionally: before `saveGuestCompass`, validate shape — `typeof payload === 'object'` AND `payload !== null` AND `typeof payload.a === 'object'` AND `Array.isArray(payload.s)`. Reject non-conforming payloads silently. Confirm guard exists; add if missing as part of Task 1.2 only if diagnosis points here. |
| T-122-02 | Tampering (open-redirect) | Treasury CTA href in `Results.jsx` built from API-sourced municipality name + state | mitigate | Build URL from `VITE_TREASURY_URL` env-var base (production default `https://treasurytracker.empowered.vote`) concatenated with a sanitized slug derived ONLY from `[a-z0-9-]` via `toTreasurySlug` (which lowercases and replaces whitespace — input is kebab-restricted by construction). Never interpolate raw user-controlled strings into the href. `target="_blank" rel="noopener noreferrer"` set on the anchor (Task 3.2). Additionally: slug helper should strip any residual `/`, `?`, `#` characters — add as defensive check in `toTreasurySlug`. |
| T-122-03 | Information Disclosure | `guestCompass` localStorage entry on essentials origin | accept | guestCompass contains stance positions (numeric values) and topic UUIDs — no PII, no auth tokens. Origin-scoped (cannot be read by compass.empowered.vote). Low-value leak target. |
| T-122-04 | Tampering | `extractHashToken` and `parseCompassFragment` both read/write `window.location.hash` in same effect | mitigate | If diagnosis (Task 1.1) surfaces Pitfall 2 (hash wipe race), fix ordering so `parseCompassFragment` runs BEFORE `extractHashToken`, OR make `extractHashToken` preserve non-`access_token` hash segments. Evidence-driven — only mitigate if diagnosed. |
| T-122-05 | Denial of Service | `/api/treasury/cities` fetch on every Results page load | accept | Single fetch per page mount, memoized; response ~50 rows; endpoint already `optionalAuth` public. No amplification vector. |

## Security Gate
**Block on:** HIGH — all `mitigate` items must be verified implemented before merge. Specifically:
- T-122-01: shape validation in `parseCompassFragment` confirmed
- T-122-02: slug construction + `rel="noopener noreferrer"` confirmed in Task 3.2
</threat_model>

<verification>
Per-task `<automated>` blocks are listed above. Phase-level gates:

- `cd ev-accounts/backend && npm test` green (Task 0.1 contract test included).
- `cd essentials && npm run build` green after Task 3.2.
- `cd CompassV2 && npm run build` green (only if Task 2.2 executed).
- Manual-only verifications per VALIDATION.md §Manual-Only Verifications (Pierce guest, Pierce authed, picker cycle, Bloomington CTA present, county CTA absent, Treasury deep-link lands correctly).
- Production evidence captured per Task 3.3 saved under `evidence/`.

## Source Coverage Audit

| Source Item | Type | Covered By | Status |
|-------------|------|------------|--------|
| Phase 122 goal: full voter loop works without integration gaps | GOAL | Tasks 1.2 + 2.1 + 3.2 + 3.3 | COVERED |
| INTG-01: CompassCard state relay works across app boundaries | REQ | Tasks 0.2 + 1.1 + 1.2 + 3.3 | COVERED |
| INTG-02: Compass compare page links to Essentials politician profiles | REQ | Tasks 2.1 + 2.2 (conditional) + 3.3 | COVERED |
| INTG-03: Essentials→Treasury handoff functional | REQ | Tasks 0.1 + 3.1 + 3.2 + 3.3 | COVERED |
| Fragment relay primitives already exist | RESEARCH | Task 1.1 diagnosis (verification-first) | COVERED |
| Treasury deep-link slug format `{name}-{state}` | RESEARCH | Task 3.1 `toTreasurySlug` | COVERED |
| `/api/treasury/cities` response shape | RESEARCH | Task 0.1 contract test | COVERED |
| Body-title matching strategy (startsWith + longest-name) | RESEARCH | Task 3.1 `findMatchingMunicipality` | COVERED |
| D-01 guestCompass read on direct revisit | CONTEXT | Task 1.2 fix | COVERED |
| D-02 diagnose-first | CONTEXT | Task 1.1 checkpoint | COVERED |
| D-03 fragment chain is only cross-origin bridge | CONTEXT | Tasks 1.1/1.2 scope constraint | COVERED |
| D-04 no follow-through past first root cause | CONTEXT | Task 1.2 scope language | COVERED |
| D-05/D-06 verify link + ID mapping | CONTEXT | Task 2.1 | COVERED |
| D-07 evidence-only close if no code fix | CONTEXT | Task 2.1 + 2.2 conditional | COVERED |
| D-08 CTA below each local-tier section | CONTEXT | Task 3.2 placement | COVERED |
| D-09 dynamic Treasury API detection | CONTEXT | Tasks 3.1 + 3.2 | COVERED |
| D-10 CTA text "Explore {M} revenue and expenses →" | CONTEXT | Task 3.2 anchor text | COVERED |
| D-11 deep-link to municipality | CONTEXT | Task 3.1 `toTreasurySlug` | COVERED |
| D-12 no grayed-out for non-matches | CONTEXT | Task 3.2 `if (!match) return null` | COVERED |
| D-13 production verification required | CONTEXT | Task 3.3 | COVERED |
| D-14 both guest + authed paths verified | CONTEXT | Task 1.1 (local) + Task 3.3 (prod) | COVERED |
| D-15 single plan, single deploy sequencing INTG-01→02→03 | CONTEXT | Plan wave order | COVERED |
| D-16 ev-ui auto-bump if fix lands there | CONTEXT | Task 1.2 action clause | COVERED |

**No gaps.** All roadmap requirements, research findings, and locked context decisions are
explicitly mapped to tasks.
</verification>

<success_criteria>
- [ ] All three INTG requirements pass production smoke per Task 3.3 evidence.
- [ ] Each task produced an atomic commit with conventional-commits message.
- [ ] `nyquist_compliant: true` in VALIDATION.md frontmatter (Task 0.3).
- [ ] No ev-ui auto-bump triggered (expected) OR auto-bump shipped cleanly if INTG-01 landed there (D-16).
- [ ] `VITE_TREASURY_URL` set on Essentials Render service (user_setup).
- [ ] Dev-mode probe from Task 0.2 remains gated behind `import.meta.env.DEV` OR removed post-diagnosis.
- [ ] Evidence directory populated under `.planning/phases/122-cross-app-loop-polish/evidence/`.
- [ ] Threat model items T-122-01 and T-122-02 mitigations verified in code.
</success_criteria>

<output>
After completion, create `.planning/phases/122-cross-app-loop-polish/122-01-SUMMARY.md` per the
standard execute-plan summary template, including:
- Which INTG-01 layer was broken and what was fixed (Task 1.1 + 1.2 findings).
- Whether INTG-02 closed evidence-only (D-07) or required code (Task 2.2).
- Evidence file paths under `evidence/`.
- Any assumption from RESEARCH.md §Assumptions Log that was refuted.
- Confirmation that all `<threat_model>` mitigations are in place.
</output>
