---
phase: 121-county-council-d1-d4-geofence-repair
plan: 04
type: execute
wave: 3
depends_on: [121-03]
files_modified:
  - .planning/GAP-REPORT.md
  - .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt
autonomous: false
requirements: [GEO-01, GEO-02]
must_haves:
  truths:
    - "GAP-REPORT.md PATTERN-004 root-cause wording is corrected to match verified ground truth — Kirkwood is D4, not D1; the bug is 'all 4 districts returned' rather than 'D4 returned when D1 expected'"
    - "ROADMAP.md Phase 121 success criteria #1 wording is left unchanged (research confirmed it is already correct — 'D4, not D1' is the right phrasing)"
    - "Production smoke test against api.empowered.vote after Render deploy returns exactly 1 MCC Council race (District 4) for the Kirkwood address"
    - "A captured prod-verification artifact (smoke-kirkwood-prod.txt or equivalent HTTP response log) is preserved on disk for reviewer sign-off"
    - "Human reviewer has explicitly approved both the doc correction and the prod verification before the phase closes"
  artifacts:
    - path: ".planning/GAP-REPORT.md"
      provides: "PATTERN-004 section rewritten to match verified ground truth (Kirkwood = D4)"
      contains: "Kirkwood"
    - path: ".planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt"
      provides: "Captured production verification output (audit script run against production or curl output from api.empowered.vote)"
      contains: "[121-geo] PASS"
  key_links:
    - from: "GAP-REPORT.md PATTERN-004 text"
      to: "evidence/ground-truth-attestation.md"
      via: "cross-reference to the authoritative FeatureServer sources"
      pattern: "ground-truth-attestation"
    - from: "api.empowered.vote (prod Render deploy)"
      to: "essentials.geofence_boundaries + essentials.districts + essentials.offices (production DB)"
      via: "data imported to prod DB by running the same import + link scripts against PROD DATABASE_URL after dev sign-off"
      pattern: "api\\.empowered\\.vote"
---

<objective>
Wave 3 documentation correction and production verification. Fix the disputed
GAP-REPORT.md PATTERN-004 wording to match verified ground truth, then verify
the fix on production after the Render deploy. This is the phase gate: GEO-01
and GEO-02 close only after prod is green.

Purpose: Research confirmed that ROADMAP.md's "D4, not D1" phrasing is correct
and GAP-REPORT.md's "should resolve to D1" phrasing is wrong. The error arose
from comparing EV results against Ballotpedia's superset display. This plan
corrects the record (so future agents don't re-open the settled question) and
closes the loop with a live production check per D-06.

The human checkpoint at the end of Task 2 ensures the developer has actually
seen the prod response before the phase is marked complete — matching the
Phase 118 / Phase 119 discipline for deploy-gated verification.

Output:
  - Corrected PATTERN-004 section in `.planning/GAP-REPORT.md`
  - `.planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt` — prod smoke evidence
  - Human sign-off on both the doc correction and the prod verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/GAP-REPORT.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-CONTEXT.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-RESEARCH.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-VALIDATION.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-01-PLAN.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-02-PLAN.md
@.planning/phases/121-county-council-d1-d4-geofence-repair/121-03-PLAN.md

<interfaces>
<!-- Current PATTERN-004 text in GAP-REPORT.md around lines 523-530 — this is what needs correction. -->

Current (incorrect) wording in .planning/GAP-REPORT.md ~line 526:
```
- **Root cause:** A Kirkwood Bloomington address that should resolve to
  County Council District 1 returns District 4 instead. The geofence row
  exists (per AUDIT-01 race table linking CC D1) but the polygon binding
  is incorrect.
```

What research confirmed:
- Kirkwood = District 4 (Jennifer Crossley) per Monroe County GIS FeatureServer AND IN statewide FeatureServer.
- Ballotpedia shows all 4 council districts for every address (superset display — not an authoritative per-voter assignment).
- Current EV behavior returns all 4 MCC districts for any Monroe County address because all 4 MCC offices link to the shared county-wide 18105 geofence.
- D1 is NOT Kirkwood's correct district; D4 is.

Phase 118/119 production verification pattern (inherited per D-06):
- After Render deploy completes, run the same smoke harness against the prod API / prod DB
- Capture the output as evidence
- Human sign-off closes the phase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite GAP-REPORT.md PATTERN-004 to match verified ground truth</name>
  <files>.planning/GAP-REPORT.md</files>
  <read_first>
    - .planning/GAP-REPORT.md (full section around PATTERN-004, approximately lines 520-535)
    - .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/ground-truth-attestation.md (from Plan 01 — the reviewer-facing evidence that D4 is correct)
    - .planning/phases/121-county-council-d1-d4-geofence-repair/121-RESEARCH.md (§ Ground Truth Resolution (D-01))
    - .planning/phases/121-county-council-d1-d4-geofence-repair/121-CONTEXT.md (D-01 — whichever of ROADMAP/GAP-REPORT is wrong must be corrected here)
  </read_first>
  <action>
    Edit `.planning/GAP-REPORT.md` — locate the `### PATTERN-004 — County
    Council D1→D4 Geofence Binding Bug (benchmark-derived gap per D-06)`
    section. Replace the Root cause bullet with corrected wording. Do NOT
    change the section heading, the `**Tier:** 1` line, the `**Type:**`
    line, the `**Constituent gaps:**` line, the `**Tier rationale:**` line,
    or the `**Note:**` line. Change only the `**Root cause:**` bullet (and
    optionally add a `**Correction:**` bullet beneath it for transparency).

    Find the existing Root cause bullet:
    ```
    - **Root cause:** A Kirkwood Bloomington address that should resolve to
      County Council District 1 returns District 4 instead. The geofence row
      exists (per AUDIT-01 race table linking CC D1) but the polygon binding
      is incorrect.
    ```

    Replace with this exact block (wrap lines at ~90 chars):
    ```
    - **Root cause:** A Kirkwood Bloomington address (200 W Kirkwood Ave,
      Bloomington IN 47404; lat=39.166646, lng=-86.534947) resolves to all four
      Monroe County Council District races (D1/D2/D3/D4) instead of exactly
      one. The authoritative district for this address is **D4** (Jennifer
      Crossley) per two independent GIS sources (Monroe County GIS
      FeatureServer + IN Statewide Admin Boundaries FeatureServer). The bug
      is structural, not a polygon coordinate error: all four MCC District
      offices in `essentials.offices` are linked to the shared county-wide
      `geo_id='18105'` district row, so any Monroe County address inside the
      county polygon matches all four races at once. See
      `.planning/phases/121-county-council-d1-d4-geofence-repair/evidence/ground-truth-attestation.md`
      for the preserved FeatureServer URLs and query responses.
    - **Correction (Phase 121):** An earlier draft of this entry described the
      bug as "should resolve to D1, returns D4." That framing was derived from
      Ballotpedia's sample-ballot tool, which displays all four council
      districts for every Monroe County address as a superset listing — not
      an authoritative per-voter district assignment. Ballotpedia listing D1
      first was mistakenly treated as ground truth. Verified 2026-04-16 via
      Monroe County GIS (`gis.co.monroe.in.us`) and IN state GIS
      (`gisdata.in.gov`): Kirkwood = D4. ROADMAP.md §Phase 121 success
      criteria #1 wording ("D4, not D1") is therefore correct and is left
      unchanged.
    ```

    Do NOT modify ROADMAP.md. Research confirmed its wording is already correct.
    Do NOT modify any other section of GAP-REPORT.md.
  </action>
  <verify>
    <automated>grep -F "all four Monroe County Council District races (D1/D2/D3/D4)" .planning/GAP-REPORT.md && grep -F "**Correction (Phase 121):**" .planning/GAP-REPORT.md && grep -F "ground-truth-attestation.md" .planning/GAP-REPORT.md && (! grep -F "should resolve to County Council District 1 returns District 4" .planning/GAP-REPORT.md)</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/GAP-REPORT.md` contains the literal string `all four Monroe County Council District races (D1/D2/D3/D4)` (the new Root cause phrasing)
    - File contains the literal string `**Correction (Phase 121):**`
    - File contains the literal string `gis.co.monroe.in.us` (FeatureServer reference)
    - File contains the literal string `gisdata.in.gov` (IN statewide FeatureServer reference)
    - File contains the literal string `ground-truth-attestation.md` (cross-reference to Plan 01 evidence)
    - File contains the literal string `Jennifer Crossley` (D4 representative, matches verified ground truth)
    - File does NOT contain the old incorrect phrase `should resolve to County Council District 1 returns District 4` (that sentence was replaced)
    - The section heading `### PATTERN-004 — County Council D1→D4 Geofence Binding Bug (benchmark-derived gap per D-06)` is still present (heading unchanged)
    - The `**Tier:** 1` line for PATTERN-004 is still present and unchanged
    - ROADMAP.md phase 121 block is unchanged: `diff` of ROADMAP.md phase 121 lines vs pre-edit shows no changes (the research + this plan explicitly left ROADMAP as-is)
  </acceptance_criteria>
  <done>
    GAP-REPORT.md PATTERN-004 now matches verified ground truth. Future agents
    reading this entry will see the correct direction + a cross-reference to
    the evidence file. ROADMAP.md untouched (already correct).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Developer runs import + link against PROD DB, deploys, captures prod smoke</name>
  <files>.planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt</files>
  <read_first>
    - .planning/phases/121-county-council-d1-d4-geofence-repair/121-02-PLAN.md (import script authored in Wave 1)
    - .planning/phases/121-county-council-d1-d4-geofence-repair/121-03-PLAN.md (link SQL edits + dev smoke pattern to mirror on prod)
    - .planning/phases/121-county-council-d1-d4-geofence-repair/121-CONTEXT.md (D-06 production verification discipline inherited from Phase 118/119)
    - ev-accounts/backend/scripts/audit-112-geofence.ts (extended smoke harness)
  </read_first>
  <what-built>
    All automation through Plan 03 is complete on dev DB. The Kirkwood smoke
    test is green locally. Plans 01-03 delivered:
    - `scripts/diagnose-121-mcc-state.ts` (read-only diagnostic)
    - `scripts/fetch-mcc-district-polygons.ts` (HTTPS fetch from Monroe County FeatureServer)
    - `scripts/import-mcc-district-polygons.ts` (idempotent INSERT of 4 polygons + 4 districts)
    - Edited `scripts/link-monroe-county-races-to-geofences.sql` (§2a change + new §2e + new §3f)
    - Extended `scripts/audit-112-geofence.ts` (Kirkwood exclusivity assertion)
    - `evidence/diagnosis.csv`, `evidence/ground-truth-attestation.md`,
      `evidence/mcc-district-polygons.geojson`, `evidence/smoke-kirkwood-dev.txt`

    What still needs human action:
    1. Promote the same data change to PROD DB. Claude cannot set
       DATABASE_URL to the prod Supabase project and run the scripts without
       explicit human authorization (project convention: prod DB changes are
       never automated).
    2. Merge the SQL-edit + script additions into main so Render auto-deploys
       the ev-accounts backend (no code changes in this phase, but committing
       the new scripts + the SQL edit keeps the repo consistent with prod DB).
    3. After Render deploy completes, run the smoke test against production
       so the evidence file captures the LIVE prod response.
  </what-built>
  <action>
    [Human-gated — see `<how-to-verify>` for the exact steps the developer
    must run. Claude does NOT execute against prod automatically.]

    The human developer will:
    1. Back up the affected prod tables with preflight SELECTs.
    2. Run `scripts/fetch-mcc-district-polygons.ts` (network fetch — already
       idempotent).
    3. Run `scripts/import-mcc-district-polygons.ts` with
       `DATABASE_URL=$PROD_DATABASE_URL` (creates 4 geofence_boundaries rows
       + 4 districts rows on prod; idempotent via ON CONFLICT + WHERE NOT EXISTS).
    4. Run `psql $PROD_DATABASE_URL -f scripts/link-monroe-county-races-to-geofences.sql`
       (edited in Plan 03; creates 4 per-district offices, re-links 4 MCC
       races; idempotent).
    5. Merge + deploy through Render (standard pipeline).
    6. Run the extended `scripts/audit-112-geofence.ts` against prod DB OR
       hit `https://api.empowered.vote/api/essentials/races-by-address`
       directly (see Options C1 / C2 in `<how-to-verify>`).
    7. Capture the output into
       `.planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt`.
    8. Reply `approved` to Claude once the file contains a green PASS line
       (Option C1) or a single-match District-4 response (Option C2).

    Claude's role during this task: WAIT. Do not attempt to run prod commands.
    Do not fabricate evidence. If the developer reports a failure, open a
    gap-closure plan per the standard revision flow.
  </action>
  <how-to-verify>
    Perform these steps yourself — Claude cannot touch prod DB without your
    explicit go-ahead.

    STEP A — Promote data to PROD DB (run on your local machine):
    ```
    # Back up current prod state first — snapshot the affected tables
    psql $PROD_DATABASE_URL -c "SELECT geo_id, mtfcc, name FROM essentials.geofence_boundaries WHERE geo_id LIKE '18105%'" > /tmp/prod-preflight-boundaries.txt
    psql $PROD_DATABASE_URL -c "SELECT o.title, d.district_id, d.geo_id FROM essentials.offices o JOIN essentials.districts d ON d.id = o.district_id WHERE o.title LIKE 'Monroe County Council District%'" > /tmp/prod-preflight-offices.txt

    # Fetch polygons (idempotent — writes evidence/ file only if missing, safe to re-run)
    cd ev-accounts/backend && npx tsx scripts/fetch-mcc-district-polygons.ts

    # Run the import against PROD — temporarily set DATABASE_URL to prod
    DATABASE_URL=$PROD_DATABASE_URL npx tsx scripts/import-mcc-district-polygons.ts

    # Re-run the link SQL against PROD (idempotent)
    psql $PROD_DATABASE_URL -f scripts/link-monroe-county-races-to-geofences.sql

    # Verify prod state:
    psql $PROD_DATABASE_URL -c "SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id LIKE '18105-mcc-d%' AND mtfcc = 'X-MCC-DISTRICT'"  # expect 4
    psql $PROD_DATABASE_URL -c "SELECT COUNT(*) FROM essentials.districts WHERE district_id LIKE 'election-mcc-d%'"                                    # expect 4
    ```

    STEP B — Merge + deploy:
    - Commit Plans 01-03 script files + the SQL edit.
    - Merge the PR to main.
    - Watch Render auto-deploy the ev-accounts backend (typically ~3-5 min).
    - Visit https://api.empowered.vote/health (or the project's equivalent
      healthcheck endpoint) to confirm deploy completed.

    STEP C — Prod smoke (choose ONE of the two options):

    Option C1 — run the audit script directly against PROD DB (fastest):
    ```
    cd ev-accounts/backend && \
      DATABASE_URL=$PROD_DATABASE_URL npx tsx scripts/audit-112-geofence.ts \
      > /tmp/121-smoke-prod.out 2> /tmp/121-smoke-prod.err
    cat /tmp/121-smoke-prod.err /tmp/121-smoke-prod.out \
      > .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt
    echo "exit_code=$?" >> .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt
    ```

    Option C2 — hit the live API through the production HTTPS endpoint
    (validates the full request path, not just the DB query):
    ```
    curl -sS -X POST https://api.empowered.vote/api/essentials/races-by-address \
      -H 'Content-Type: application/json' \
      -d '{"address":"200 W Kirkwood Ave, Bloomington IN 47404"}' \
      > .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt
    # Then inspect manually: count "Monroe County Council District N" entries — must be exactly 1, must be District 4.
    ```

    Pick whichever option you trust more. Option C1 is cheaper and maps 1:1
    to the dev-side PASS line. Option C2 includes the full API+geocoder path
    but depends on the exact endpoint shape available in prod.

    EXPECTED RESULT:
    - `evidence/smoke-kirkwood-prod.txt` exists and is non-empty
    - Kirkwood resolves to exactly one MCC race: District 4
    - (Option C1) file contains `[121-geo] PASS: Kirkwood returns exactly 1 MCC Council race: Monroe County Council District 4` and `exit_code=0`
    - (Option C2) `grep -oE "Monroe County Council District [1-4]"` on the file returns a single match for `District 4`

    After confirming, reply to Claude with either:
    - `approved` if prod smoke is green and evidence captured
    - A description of the failure if anything went wrong (Claude will open
      a gap-closure revision if needed)
  </how-to-verify>
  <verify>
    <automated>test -s .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt && (grep -F "[121-geo] PASS" .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt || grep -E "Monroe County Council District 4" .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt) && (! grep -F "[121-geo] FAIL" .planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt)</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/121-county-council-d1-d4-geofence-repair/evidence/smoke-kirkwood-prod.txt` exists and is non-empty
    - (Option C1) file contains `[121-geo] PASS: Kirkwood returns exactly 1 MCC Council race: Monroe County Council District 4` AND `exit_code=0`
    - (Option C2) file contains a response with exactly one line matching `Monroe County Council District 4` AND zero lines matching `Monroe County Council District [123]`
    - File does NOT contain the literal string `[121-geo] FAIL`
    - File does NOT contain any `postgres://` URL (no DATABASE_URL leak) — grep returns zero matches
    - Developer has responded `approved` in the conversation (resume-signal)
  </acceptance_criteria>
  <done>
    Prod DB promoted, Render deploy live, smoke evidence captured showing
    Kirkwood → District 4 exclusively, human approval recorded.
  </done>
  <resume-signal>Type "approved" when prod smoke is green AND evidence/smoke-kirkwood-prod.txt is committed, OR describe the failure for revision planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer local machine to PROD Supabase | High-privilege write boundary — explicit human authorization required |
| Git (PR merge) to Render deploy pipeline | Trusted CI pipeline, existing boundary |
| Developer/Claude to `.planning/GAP-REPORT.md` | Local doc edit, trusted |
| api.empowered.vote to public caller (smoke test) | Public HTTPS endpoint — no auth required for essentials lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-04-01 | Tampering | Doc correction could accidentally change unrelated GAP-REPORT entries | mitigate | Task 1 acceptance criteria enumerates each string that must be present (new wording, cross-references, FeatureServer URLs) and each that must be absent (old phrasing). Section heading explicitly verified unchanged. Reviewer can diff the two bullets |
| T-121-04-02 | Elevation of Privilege | Running import/link scripts against PROD DB | mitigate | Explicitly human-gated — Claude does not auto-execute against prod. Developer must manually set DATABASE_URL to prod (`$PROD_DATABASE_URL` in the instructions) and run the scripts locally. Pre-flight backup queries capture prod state before mutation |
| T-121-04-03 | Tampering | Idempotent INSERTs could still write unexpectedly if prod DB has different existing state than dev | mitigate | The scripts use `ON CONFLICT (geo_id, mtfcc) DO NOTHING` and `WHERE NOT EXISTS` — if prod already has conflicting rows, they're preserved. Preflight queries captured in `/tmp/prod-preflight-*.txt` let the developer roll back if needed |
| T-121-04-04 | Repudiation | Prod smoke evidence could be fabricated | mitigate | Two independent verification paths (direct DB query + public HTTPS endpoint). Both must agree. api.empowered.vote response is a live network call reviewers can re-run |
| T-121-04-05 | Denial of Service | Prod smoke test hitting api.empowered.vote repeatedly | accept | Single smoke query during verification. No rate-limit risk |
| T-121-04-06 | Information Disclosure | Prod DATABASE_URL leaking into the committed smoke-prod file | mitigate | The audit script prints position_name/party/candidate_name only; it does not echo DATABASE_URL. Developer should grep the evidence file for any `postgres://` string before committing |
</threat_model>

<verification>
Task 1: Doc correction verified via grep (new wording present, old wording absent).

Task 2: Human-gated. Pass criteria:
- `evidence/smoke-kirkwood-prod.txt` exists and is non-empty
- Shows Kirkwood resolves to District 4, exactly one match
- Developer has replied `approved` and confirmed they personally ran the prod smoke
- Plan-04 SUMMARY records the exact production endpoint/query used and the observed response
</verification>

<success_criteria>
- GAP-REPORT.md PATTERN-004 section matches verified ground truth and cross-references the evidence file.
- ROADMAP.md unchanged (already correct).
- Production DB has the 4 new MCC geofence rows, 4 new district rows, 4 per-district offices, 4 MCC races re-linked.
- `evidence/smoke-kirkwood-prod.txt` captures a green `[121-geo] PASS` (Option C1) or a single-match District-4 curl response (Option C2).
- Human reviewer has explicitly approved.
- GEO-01 and GEO-02 satisfied in production — phase closes.
</success_criteria>

<output>
After completion, create `.planning/phases/121-county-council-d1-d4-geofence-repair/121-04-SUMMARY.md`
documenting:
- The exact diff applied to GAP-REPORT.md PATTERN-004 (old vs new Root cause text)
- The prod verification option chosen (C1 or C2) and the captured response
- Any deviation from the expected flow (e.g., prod had pre-existing rows with conflicting geo_ids)
- The human reviewer's approval statement
- Close-out note: GEO-01 and GEO-02 satisfied; phase 121 ships.
</output>
