---
phase: 118-read-rank-verdict-badge-fix
plan: 03
type: execute
wave: 3
depends_on: ["118-02"]
files_modified:
  - .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md
autonomous: false
requirements: [RR-01, RR-02]

must_haves:
  truths:
    - "≥1 verdict badge renders on the canonical Pierce profile URL on essentials.empowered.vote in production."
    - "Both the authed path AND the guest path render verdicts on production (D-15)."
    - "Both Pierce test URLs (candidate + politician) are inspected — whichever is canonical must show badges; the other is confirmed for parity."
    - "User has done final sign-off by visiting the Pierce URL and confirming badges are visible (D-16)."
    - "If ev-ui was bumped, the other 3 consumer apps (CompassV2, read-rank, civic-spaces) load without regressions in a smoke pass (D-12)."
  artifacts:
    - path: ".planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md"
      provides: "Production verification evidence with DOM snapshots / screenshot references, paths tested, and user sign-off line"
      contains: "User sign-off:"
  key_links:
    - from: "production essentials.empowered.vote"
      to: "118-VERIFICATION.md evidence"
      via: "Chrome MCP DOM inspection + user visual confirmation"
      pattern: "badge rendered on Pierce"
---

<objective>
Verify on production that the fix from 118-02 actually restored verdict badge
rendering on the canonical Pierce profile URL, across both authed and guest paths,
and capture user sign-off per D-16. Also spot-check the other 3 ev-ui consumer apps
if the fix was an ev-ui bump, to rule out cross-consumer regressions (D-12).

Purpose: RR-01 proof per D-13 (visual confirmation on production) and D-14
(production only — local dev does not count). RR-02 is already delivered in the fix
PR description (118-02); this plan confirms RR-01 and captures the sign-off trail.

Output: `118-VERIFICATION.md` with evidence per path and user sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-CONTEXT.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-01-SUMMARY.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production verification on both Pierce URLs × both auth paths</name>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md (canonical RR-01 surface + fix layer)
    - .planning/phases/118-read-rank-verdict-badge-fix/118-02-SUMMARY.md (confirm deploy status)
    - ev-ui/src/StanceAccordion.jsx (lines 329-395 — expected DOM structure for verdict badges)
  </read_first>
  <action>
    Create 118-VERIFICATION.md. Confirm the fix is actually deployed to production first:
    - If ev-ui bump: `npm view @empoweredvote/ev-ui version` shows the bumped version; Render essentials service latest deploy sha matches the auto-merged consumer PR.
    - If essentials/ev-accounts: Render service dashboard shows latest commit sha matches the fix PR merge commit.
    - If data fix: re-run the Pierce verdict count SELECT against prod Supabase.
    If deploy is not yet complete, wait for it before proceeding.

    Then, using the Chrome MCP (mcp__claude-in-chrome__*), execute a 2×2 verification matrix:

    **Matrix cells:**
    1. Candidate URL (https://essentials.empowered.vote/candidate/7e768cda-38f3-4511-ad7c-c8e877c5abfa) × Guest path
    2. Candidate URL × Authed path
    3. Politician URL (https://essentials.empowered.vote/politician/72dd5219-490f-48bb-986e-183a6098d602) × Guest path
    4. Politician URL × Authed path

    For each cell:
    1. Navigate, wait for CompassCard / StanceAccordion to render.
    2. For authed path: log in with a test account that has verdicts on Pierce quotes (or cast verdicts in-session on the 10 quotes to produce a populated state).
    3. For guest path: either clear localStorage and cast verdicts in-session, OR pre-populate `localStorage['&lt;guest-verdicts-key&gt;']` with a fixture map that covers the 10 Pierce quote IDs.
    4. Query the DOM for verdict badge elements inside StanceAccordion (use the selector derived from StanceAccordion.jsx:329-395 — read the file to get exact class names / data attributes).
    5. Record: badge count rendered, expected count (≥1), PASS/FAIL, and a DOM snippet showing at least one rendered badge.
    6. Take a screenshot via Chrome MCP and save the reference/filename in the doc.

    The canonical RR-01 surface (declared in 118-DIAGNOSIS.md) MUST have ≥1 badge rendered on both guest and authed cells. The other URL must also be inspected for parity (verdicts render if data exists, or explicit note "no data on this surface, render path verified via canonical URL").

    Record results in 118-VERIFICATION.md under a "Production Matrix" table with columns: URL, Auth, Badge Count, Expected, Evidence, Status.
  </action>
  <verify>
    <automated>test -f .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md &amp;&amp; grep -c "PASS" .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md | awk '{ exit ($1 &gt;= 2 ? 0 : 1) }' &amp;&amp; grep -q "7e768cda-38f3-4511-ad7c-c8e877c5abfa" .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md &amp;&amp; grep -q "72dd5219-490f-48bb-986e-183a6098d602" .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md</automated>
  </verify>
  <done>
    118-VERIFICATION.md Production Matrix has all 4 cells filled, canonical RR-01
    surface shows PASS on both guest and authed cells (≥1 badge rendered), evidence
    references DOM snippets / screenshot filenames, and both Pierce UUIDs appear in
    the document.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-consumer smoke check (only if ev-ui was bumped)</name>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md ("Root cause layer:" to decide if this task applies)
    - .planning/phases/118-read-rank-verdict-badge-fix/118-02-SUMMARY.md (to confirm which consumers received the auto-bump PR)
  </read_first>
  <action>
    If 118-DIAGNOSIS.md "Root cause layer:" is NOT "ev-ui" and NOT "css" (i.e., no ev-ui bump occurred), append "## Cross-consumer smoke: N/A (no ev-ui bump)" to 118-VERIFICATION.md and mark this task done.

    If ev-ui was bumped (D-12 — bump propagates to all 4 consumers), use Chrome MCP to load each consumer's production URL and confirm no load-blocking regressions:
    - https://compass.empowered.vote (CompassV2) — load home, confirm radar chart renders, no console errors.
    - https://readrank.empowered.vote (read-rank) — load home, confirm quote cards render, no console errors.
    - civic-spaces production URL (check `ev-ui/README-AUTOBUMP.md` for the consumer list; if civic-spaces is not live yet, skip and note "not deployed").
    - https://essentials.empowered.vote — already covered by Task 1.

    For each consumer, record: URL, load status (200/error), console errors (count + first 3 messages), and PASS/FAIL. Append as a "Cross-consumer Smoke" section in 118-VERIFICATION.md.
  </action>
  <verify>
    <automated>grep -qE "Cross-consumer smoke: N/A|Cross-consumer Smoke" .planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md</automated>
  </verify>
  <done>118-VERIFICATION.md contains either the N/A marker or a Cross-consumer Smoke section covering CompassV2, read-rank, and civic-spaces (or explicit skip note).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User final sign-off on Pierce profile (D-16)</name>
  <what-built>
    Task 1 completed the automated 2×2 production matrix and Task 2 covered any
    cross-consumer regression smoke. 118-VERIFICATION.md contains PASS entries
    and DOM/screenshot evidence. Per D-16, the user must personally visit the
    Pierce URL and confirm badges are visible before RR-01 is marked complete.
  </what-built>
  <how-to-verify>
    1. Open the canonical RR-01 surface declared in 118-DIAGNOSIS.md (either
       https://essentials.empowered.vote/candidate/7e768cda-38f3-4511-ad7c-c8e877c5abfa
       or https://essentials.empowered.vote/politician/72dd5219-490f-48bb-986e-183a6098d602).
    2. Confirm you can see ≥1 verdict badge (agreed/disagreed icon) on a Pierce quote
       inside the StanceAccordion. If you have no verdicts on file, cast one in-session
       via the Read &amp; Rank interaction and refresh the profile page.
    3. Load the OTHER Pierce URL and confirm it also renders sanely (badges if data
       exists, clean stance rows if not).
    4. If the fix was an ev-ui bump, load compass.empowered.vote and readrank.empowered.vote
       and confirm nothing obvious is broken.
  </how-to-verify>
  <resume-signal>Type "signed off" to mark RR-01 complete and close Phase 118, or describe what you see if badges are still missing (triggers revision into 118-04).</resume-signal>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-VERIFICATION.md</files>
  <action>See &lt;how-to-verify&gt; block above. This is a human checkpoint — Claude pauses and waits for the resume signal.</action>
  <verify>
    <automated>echo "human checkpoint — resumed by user"</automated>
  </verify>
  <done>User provided the resume signal described in &lt;resume-signal&gt;.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → production essentials.empowered.vote | Read-only verification |
| Chrome MCP → production URLs | No mutation; devtools inspection only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-06 | I (Info Disclosure) | Authed test account verdicts | accept | Use a dedicated test account or cast transient verdicts; do not record Authorization headers in 118-VERIFICATION.md. |
| T-118-07 | R (Repudiation) | User sign-off step | mitigate | Sign-off is captured verbatim in 118-VERIFICATION.md with timestamp, providing an audit trail per D-16. |
</threat_model>

<verification>
- 118-VERIFICATION.md exists with Production Matrix (2×2), Cross-consumer Smoke (or N/A), and User sign-off line.
- Both Pierce UUIDs appear in the doc.
- RR-01 marked complete only after user types "signed off" in the checkpoint.
</verification>

<success_criteria>
- Verdict badges confirmed rendering on production canonical Pierce URL, both authed and guest paths.
- Cross-consumer smoke complete (or N/A).
- User sign-off captured per D-16.
- RR-01 ready to mark complete in REQUIREMENTS.md; RR-02 already delivered via fix PR description in 118-02.
</success_criteria>

<output>
After completion, create `.planning/phases/118-read-rank-verdict-badge-fix/118-03-SUMMARY.md`
</output>
