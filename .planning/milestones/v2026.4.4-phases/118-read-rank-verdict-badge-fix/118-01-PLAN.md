---
phase: 118-read-rank-verdict-badge-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md
autonomous: true
requirements: [RR-01, RR-02]

must_haves:
  truths:
    - "The root cause layer of the verdict badge regression is identified (one of: ev-ui version, CompassCard prop wiring, CompassContext fetch chain, backend /compass/verdicts response, CSS, or DB data)."
    - "Both Pierce test URLs have been inspected against a live production account and a guest session."
    - "Each of hypotheses H1-H5 from D-06 has a documented PASS/FAIL/N-A verdict."
    - "The canonical Pierce RR-01 verification surface (candidate vs politician URL) is declared based on which one has the 10 quotes."
  artifacts:
    - path: ".planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md"
      provides: "Hypothesis test results and root cause identification"
      contains: "Root cause:"
  key_links:
    - from: "118-DIAGNOSIS.md"
      to: "Fix plan (118-02)"
      via: "'Root cause layer:' field drives the fix scope"
      pattern: "Root cause layer:"
---

<objective>
Diagnose the verdict badge regression by running the hypothesis-first investigation
from D-05/D-06 against both Pierce test URLs and both authed + guest paths. Produce
118-DIAGNOSIS.md identifying the exact layer where the render path is broken, which
becomes the input to the fix plan (118-02).

Purpose: Per D-05, avoid a full top-to-bottom trace by testing likely hypotheses
first. The exact file list for the fix plan cannot be known until this investigation
runs (D-08, no time budget — correctness over speed).

Output: `.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md` with per-hypothesis PASS/FAIL entries and a declared root cause.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-CONTEXT.md
@CLAUDE.md

@ev-ui/src/StanceAccordion.jsx
@essentials/src/components/CompassCard.jsx
@essentials/src/contexts/CompassContext.jsx
@essentials/src/lib/compass.js
@essentials/package.json
@ev-accounts/backend/src/lib/compassService.ts

<interfaces>
From ev-ui/src/StanceAccordion.jsx (canonical props):
```
// verdictsByTopic DEPRECATED (line :31)
// verdictsByQuote: { [quote.id]: 'agreed' | 'disagreed' }
// Render logic lines 329-395 — verdict icons keyed by quote.id
```

From essentials/src/lib/compass.js:
```
loadGuestVerdicts(): { [quote_id]: 'agreed' | 'disagreed' }  // line ~237 (localStorage)
fetchUserVerdicts(): Promise<{ [quote_id]: 'agreed' | 'disagreed' }>  // line ~262 (/api/compass/verdicts)
```

From essentials/src/contexts/CompassContext.jsx (lines ~149-161):
```
// Verdict priority chain: authed API → fragment → localStorage → {}
```

From essentials/src/components/CompassCard.jsx:
```
// Two StanceAccordion call sites: line :321 and line :415
// Both should pass verdictsByQuote={verdicts}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce in production and capture baseline DOM/network state</name>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/118-read-rank-verdict-badge-fix/118-CONTEXT.md
    - ev-ui/src/StanceAccordion.jsx (lines 1-50 for prop contract, 329-395 for render logic)
    - essentials/src/components/CompassCard.jsx (lines 300-340 for call site :321, lines 395-430 for call site :415)
    - essentials/src/contexts/CompassContext.jsx (lines 140-170 for verdict priority chain)
  </read_first>
  <action>
    Create 118-DIAGNOSIS.md with a "Reproduction" section. Visit both Pierce URLs on production in a guest session via the Chrome MCP (mcp__claude-in-chrome__*):
    - Candidate URL: https://essentials.empowered.vote/candidate/7e768cda-38f3-4511-ad7c-c8e877c5abfa
    - Politician URL: https://essentials.empowered.vote/politician/72dd5219-490f-48bb-986e-183a6098d602

    For each URL, in guest mode:
    1. Navigate to the URL, wait for the CompassCard / StanceAccordion to render.
    2. Capture: does any verdict badge DOM element render? Query the DOM for elements inside StanceAccordion that would correspond to verdict icons (inspect the rendered HTML structure — grep-style selectors based on what StanceAccordion.jsx:329-395 emits).
    3. Capture the network request to `/api/compass/*` and record response shapes and HTTP statuses.
    4. Open devtools console, run `localStorage.getItem('ev:guestVerdicts')` (or whatever key `loadGuestVerdicts` reads from in compass.js:~237 — read the file first to confirm the exact key) and record the returned value.
    5. Record whether the 10 Pierce quotes are present as stance rows (without badges) on each URL. The URL where the 10 quotes appear is the canonical RR-01 surface (D-03). Declare it in the doc.

    Record findings in 118-DIAGNOSIS.md under a "Reproduction" heading with sub-sections "Candidate URL (guest)" and "Politician URL (guest)".
  </action>
  <verify>
    <automated>test -f .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md &amp;&amp; grep -q "Reproduction" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md &amp;&amp; grep -q "Canonical RR-01 surface:" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md</automated>
  </verify>
  <done>118-DIAGNOSIS.md exists with Reproduction section, both Pierce URLs documented, and the canonical RR-01 surface declared.</done>
</task>

<task type="auto">
  <name>Task 2: Test hypotheses H1-H5 and declare root cause layer</name>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md (the Reproduction findings from Task 1)
    - essentials/package.json (for H1 — ev-ui version pin)
    - ev-ui/package.json (for H1 — latest published version)
    - ev-ui/src/StanceAccordion.jsx (for H1 — does current published version render verdictsByQuote; for H5 — CSS/display regression)
    - essentials/src/components/CompassCard.jsx (for H2 — both call sites :321 and :415)
    - essentials/src/lib/compass.js (for H3 — loadGuestVerdicts + fetchUserVerdicts shape)
    - essentials/src/contexts/CompassContext.jsx (for H3 — priority chain)
    - ev-accounts/backend/src/lib/compassService.ts (for H4 — verdicts query)
  </read_first>
  <action>
    Append a "Hypothesis Tests" section to 118-DIAGNOSIS.md. For each of H1-H5 below, record PASS (hypothesis confirmed, this is a cause), FAIL (hypothesis ruled out), or N/A with evidence. Do not stop at the first PASS — per D-07, test remaining hypotheses to rule out compounding regressions.

    **H1 — ev-ui version mismatch:**
    - Read essentials/package.json and grep for `@empoweredvote/ev-ui` to get the pinned version.
    - Read ev-ui/package.json for the current local version.
    - Run `npm view @empoweredvote/ev-ui version` to get the latest published version.
    - Open the currently pinned version's tag in ev-ui git history (`git log --all --oneline -- ev-ui/src/StanceAccordion.jsx | head -20`) and confirm whether `verdictsByQuote` render logic exists at that version. Inspect node_modules if the repo has essentials built: `cat essentials/node_modules/@empoweredvote/ev-ui/dist/*.js | grep -c verdictsByQuote` (or inspect the installed dist).
    - PASS if the pinned version predates the verdictsByQuote render branch. FAIL otherwise.

    **H2 — CompassCard prop wiring drift:**
    - Read CompassCard.jsx lines 300-340 (call site :321) and lines 395-430 (call site :415).
    - Confirm BOTH call sites pass `verdictsByQuote={verdicts}` (or equivalent non-empty object). Record the exact prop strings from both sites.
    - PASS if either site is missing the prop, passes `verdictsByTopic` (deprecated), or passes `{}` / `undefined`. FAIL if both sites pass a populated `verdictsByQuote`.

    **H3 — Fetch chain returns empty:**
    - Read lib/compass.js loadGuestVerdicts (§237) and fetchUserVerdicts (§262). Record their return shape and the localStorage key / API path used.
    - In the Chrome MCP guest session from Task 1, run `JSON.parse(localStorage.getItem('<key>'))` and record the value. Check: does any quote.id from the Pierce stance rows appear as a key?
    - For authed path: log in on production using a test account (if available; otherwise document the authed path was untested and mark H3-auth as N/A-no-test-account). Call `fetch('/api/compass/verdicts').then(r =&gt; r.json())` via Chrome MCP devtools console and record the payload.
    - Compare quote_id keys returned vs quote.id values rendered in StanceAccordion rows — any mismatch (UUID casing, string vs number, schema drift) is a PASS.
    - PASS if any layer in the priority chain returns `{}` or returns keys that do not match rendered quote IDs. FAIL otherwise.

    **H4 — Backend /compass/verdicts response shape:**
    - Read ev-accounts/backend/src/lib/compassService.ts for the verdicts query. Identify the expected response shape.
    - From the authed Chrome MCP session, hit `/api/compass/verdicts` and compare actual shape to expected. Record any drift.
    - Also query the DB (via Supabase dashboard or `supabase` CLI against project kxsdzaojfaibhuzmclfq): confirm 10 Pierce quotes exist and that `rr_verdicts` (or equivalent table — read compassService.ts for the table name) has rows referencing them.
    - PASS if backend returns unexpected shape, empty rows despite DB having verdicts, or the DB itself is missing verdict rows (data repair scenario). FAIL otherwise.

    **H5 — CSS / display regression in StanceAccordion:**
    - Read StanceAccordion.jsx lines 329-395. Record the class names / style props applied to verdict badge elements.
    - In Chrome MCP, inspect the live DOM on the canonical Pierce URL: if `verdictsByQuote` is populated (per H3) but badges still are not visible, grep the computed styles for `display: none`, `opacity: 0`, `visibility: hidden`, or `width: 0`.
    - PASS if badge DOM is present but hidden by CSS. FAIL if either the DOM is absent or CSS is correct.

    After all 5 hypothesis tests, append a "Root Cause" section declaring:
    - "Root cause layer: {ev-ui | essentials-wiring | essentials-fetch | backend | db-data | css}"
    - "Root cause file(s): {exact file paths}"
    - "One-line fix summary: {what change will be made}"
    - "Commit that introduced it (if identifiable via git blame / git log): {commit sha or 'unknown'}"

    If multiple hypotheses PASS, declare all passing layers in the root cause list — the fix plan will address each.
  </action>
  <verify>
    <automated>grep -q "Root cause layer:" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md &amp;&amp; grep -cE "^H[1-5] —" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md | awk '{ exit ($1 &gt;= 5 ? 0 : 1) }' &amp;&amp; grep -qE "^(PASS|FAIL|N/A)" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md</automated>
  </verify>
  <done>118-DIAGNOSIS.md contains all 5 hypothesis test results with PASS/FAIL/N/A verdicts and evidence, and declares a "Root cause layer:" with file paths and one-line fix summary. At least one hypothesis is PASS (otherwise escalate via the doc's "Escalation" section — layer-by-layer bisection per D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → essentials frontend | Read-only inspection of production DOM/network |
| essentials frontend → /api/compass/verdicts | Existing authed endpoint, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | I (Info Disclosure) | Diagnostic logging of verdict payloads | accept | Low risk: verdict data is user-scoped and not PII-sensitive. Do not paste raw verdict payloads into 118-DIAGNOSIS.md if it contains any session tokens; redact Authorization headers if recorded. |
| T-118-02 | T (Tampering) | Read-only diagnosis | accept | This plan writes only to .planning/ (docs), no code mutations. |
</threat_model>

<verification>
- 118-DIAGNOSIS.md exists and contains Reproduction, Hypothesis Tests (H1-H5), and Root Cause sections.
- Canonical Pierce RR-01 surface is declared (candidate vs politician).
- At least one hypothesis is marked PASS (or the doc explicitly escalates to full bisection).
</verification>

<success_criteria>
- Root cause layer declared with enough specificity that 118-02 can write concrete file paths into its frontmatter `files_modified`.
- Both Pierce URLs inspected. Both guest path and (if test account available) authed path inspected.
- Diagnosis committed and ready as input for 118-02.
</success_criteria>

<output>
After completion, create `.planning/phases/118-read-rank-verdict-badge-fix/118-01-SUMMARY.md`
</output>
