---
phase: 118-read-rank-verdict-badge-fix
plan: 02
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - TBD-from-diagnosis
autonomous: false
requirements: [RR-01, RR-02]

must_haves:
  truths:
    - "Minimal fix applied in the exact layer declared in 118-DIAGNOSIS.md under 'Root cause layer:'."
    - "If root cause is in ev-ui, a patch version is published via the auto-bump pipeline and the essentials consumer PR is auto-merged."
    - "If root cause is in essentials, backend, or data, the fix is committed to the appropriate repo and deployed to Render (or the DB is patched)."
    - "After fix deploys to production, local dev smoke confirms verdict badges render for the Pierce test account / guest session."
    - "RR-02 deliverable: fix PR description contains a brief root cause note (what broke, which layer, commit if identifiable, one-line fix summary)."
  artifacts:
    - path: "TBD — declared by 118-DIAGNOSIS.md 'Root cause file(s):'"
      provides: "The minimal code or data change that restores verdict badge rendering"
  key_links:
    - from: "fix commit(s)"
      to: "production essentials.empowered.vote"
      via: "Render auto-deploy on merge to main (or ev-ui auto-bump pipeline if fix is in ev-ui)"
      pattern: "render-deploy"
---

<objective>
Apply the minimal fix in the layer identified by 118-DIAGNOSIS.md and ship it to
production. Because the root cause layer is unknown at plan time (D-05, D-08), this
plan is deliberately scoped as "apply the fix declared by diagnosis" with a mandatory
human checkpoint (`autonomous: false`) before any code write.

Purpose: Restore verdict badge rendering on the canonical Pierce profile URL with
minimal blast radius. Respect the D-07 rule — after the primary fix, verify remaining
hypothesis layers are NOT also broken.

Output: One or more commits in the affected repo(s), a fix PR with the RR-02 root
cause note in its description, and (if ev-ui) an auto-bumped consumer merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-CONTEXT.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md
@.planning/phases/118-read-rank-verdict-badge-fix/118-01-SUMMARY.md
@CLAUDE.md

@ev-ui/README-AUTOBUMP.md
@ev-ui/src/StanceAccordion.jsx
@essentials/src/components/CompassCard.jsx
@essentials/src/contexts/CompassContext.jsx
@essentials/src/lib/compass.js
@ev-accounts/backend/src/lib/compassService.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm fix surface with user before writing code</name>
  <what-built>
    Task 118-01 produced 118-DIAGNOSIS.md with a declared root cause layer, root
    cause file(s), and one-line fix summary. No code has been written yet — this
    checkpoint exists because the fix surface cannot be known at plan time (D-05).
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md` and
       confirm the "Root cause layer:", "Root cause file(s):", and "One-line fix
       summary:" fields match what you expect given the symptom (verdict badges
       missing on Pierce URL).
    2. Confirm the declared fix layer is one of: ev-ui, essentials-wiring,
       essentials-fetch, backend, db-data, css (per D-10 all are in-scope).
    3. If the fix is in ev-ui, confirm you are prepared for the auto-bump pipeline
       to propagate to all 4 consumers (CompassV2, essentials, read-rank,
       civic-spaces) per D-12 — there is no way to scope the bump to one consumer.
    4. If the fix is a DB data repair, confirm you are prepared to run an UPDATE
       against the production Supabase project (kxsdzaojfaibhuzmclfq).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the fix, or describe a different fix direction.</resume-signal>
  <files>.planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md (read-only)</files>
  <action>See &lt;how-to-verify&gt; block above. This is a human checkpoint — Claude pauses and waits for the resume signal.</action>
  <verify>
    <automated>echo "human checkpoint — resumed by user"</automated>
  </verify>
  <done>User provided the resume signal described in &lt;resume-signal&gt;.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply minimal fix in the declared root cause layer</name>
  <files>Declared by 118-DIAGNOSIS.md "Root cause file(s):" — not known at plan time</files>
  <read_first>
    - .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md (the Root Cause section is authoritative)
    - The exact file(s) listed under "Root cause file(s):" — read them in full before editing
    - ev-ui/README-AUTOBUMP.md (ONLY if root cause layer is "ev-ui")
    - CLAUDE.md §"ev-ui (Component Library)" (for the auto-bump release command reference)
  </read_first>
  <action>
    Apply the fix specified by 118-DIAGNOSIS.md "One-line fix summary:". Scope strictly
    to the files listed under "Root cause file(s):". No unrelated changes, no drive-by
    refactors. Then run the layer-specific release path:

    **If root cause is in ev-ui (D-11, D-12):**
    1. Edit the specific file(s) in ev-ui/src/. Run `cd ev-ui &amp;&amp; npm run build` and verify dist/ builds clean.
    2. Commit with message `fix(ev-ui): restore verdict badge render path [118]`.
    3. Bump: `cd ev-ui &amp;&amp; npm version patch`.
    4. Push: `git push origin main --follow-tags`.
    5. Wait for GitHub Action `.github/workflows/publish.yml` to complete OIDC publish.
    6. Wait for `repository_dispatch` to open a PR in `essentials` via `.github/workflows/ev-ui-bump.yml`. Monitor via `gh pr list --repo chrisandrewsedu/essentials --state open --json number,title,headRefName`.
    7. Confirm the essentials PR auto-merges (patch bumps auto-merge when build-check.yml passes).
    8. Wait for Render to deploy the merged essentials main branch.
    9. Do NOT manually cherry-pick the bump into any consumer — the pipeline is authoritative.

    **If root cause is in essentials (CompassCard / CompassContext / lib/compass.js):**
    1. Edit the specific file(s). Run `cd essentials &amp;&amp; npm run build` to confirm no type/lint breakage.
    2. Commit with message `fix(essentials): restore verdict badge wiring [118]`.
    3. Open a PR against main, let CI pass, merge.
    4. Render auto-deploys essentials on merge to main.

    **If root cause is in ev-accounts backend (/compass/verdicts route or compassService.ts):**
    1. Edit the specific file(s). Run `cd ev-accounts/backend &amp;&amp; npm run typecheck &amp;&amp; npm test` to confirm no regressions.
    2. Commit with message `fix(ev-accounts): restore /compass/verdicts response shape [118]`.
    3. Open a PR against main, let CI pass, merge.
    4. Render auto-deploys ev-accounts on merge to main.

    **If root cause is DB data repair (essentials.rr_verdicts or similar):**
    1. Write the minimal UPDATE/INSERT SQL patch in a migration file at `ev-accounts/backend/migrations/NNN-fix-pierce-verdict-rows.sql` with the exact predicate scoped to the 10 Pierce quotes (by quote UUID or politician_id = 72dd5219-490f-48bb-986e-183a6098d602).
    2. Run the migration against the production Supabase project (kxsdzaojfaibhuzmclfq) per the standard migration procedure.
    3. Commit the migration file.

    **If root cause is CSS in ev-ui StanceAccordion:**
    Same as the ev-ui branch above.

    **After applying the fix, re-check D-07 (compounding regressions):** Re-read the
    5 hypothesis verdicts in 118-DIAGNOSIS.md. If any OTHER hypothesis was also PASS,
    the fix for that layer must also be applied in this task (do not defer). Update
    118-DIAGNOSIS.md to mark each PASS hypothesis as "FIXED in commit &lt;sha&gt;".

    **Write the RR-02 root cause note in the fix PR description (D-09):**
    Format exactly:
    ```
    ## Root Cause (RR-02)

    - **What broke:** &lt;one sentence&gt;
    - **Layer:** &lt;ev-ui | essentials | backend | db-data | css&gt;
    - **Commit that introduced it:** &lt;sha or "unknown"&gt;
    - **Fix:** &lt;one sentence&gt;
    ```
    Use `gh pr edit &lt;number&gt; --body "$(cat &lt;&lt;'EOF' ... EOF)"` or include the block in the initial `gh pr create` body.
  </action>
  <verify>
    <automated>bash -c 'LAYER=$(grep "Root cause layer:" .planning/phases/118-read-rank-verdict-badge-fix/118-DIAGNOSIS.md | head -1); echo "$LAYER"; test -n "$LAYER"' &amp;&amp; git log --oneline -5 | grep -E "\[118\]|fix.*verdict"</automated>
  </verify>
  <done>
    - Fix commit(s) exist on the appropriate repo's main branch.
    - If ev-ui: consumer auto-merge PR in essentials is merged and Render deploy is green.
    - If essentials/backend: Render deploy for that service is green.
    - If data: migration applied to prod Supabase with row count confirmation.
    - Fix PR description contains the RR-02 "Root Cause" block.
    - 118-DIAGNOSIS.md marks every PASS hypothesis as "FIXED in commit &lt;sha&gt;".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → ev-ui release pipeline | OIDC publish to public npm (no NPM_TOKEN) |
| Developer → production Supabase (if data fix) | Service role key, scoped migration |
| consumer repos ← repository_dispatch | DISPATCH_TOKEN fine-grained PAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03 | T (Tampering) | ev-ui published package | accept | Standard auto-bump with OIDC provenance attestations — no manual npm publish. |
| T-118-04 | I (Info Disclosure) | DB data fix SQL | mitigate | Scope any UPDATE/INSERT strictly by Pierce UUIDs (7e768cda-38f3-4511-ad7c-c8e877c5abfa, 72dd5219-490f-48bb-986e-183a6098d602); never run unscoped UPDATE on verdict tables. |
| T-118-05 | D (DoS / regression) | 4 consumer repos receive ev-ui bump | accept | D-12 explicitly accepts multi-consumer propagation. Build-check.yml gates consumer auto-merge. |
</threat_model>

<verification>
- `gh pr view &lt;fix-pr&gt; --json body | grep -c "Root Cause (RR-02)"` returns 1.
- The primary layer's Render deploy shows the new commit as deployed.
- No unrelated files modified (diff scoped to the declared "Root cause file(s):" list).
</verification>

<success_criteria>
- Fix shipped to production in the declared layer.
- RR-02 root cause note in fix PR description per D-09 format.
- Compounding regressions across H1-H5 addressed per D-07.
</success_criteria>

<output>
After completion, create `.planning/phases/118-read-rank-verdict-badge-fix/118-02-SUMMARY.md`
</output>
