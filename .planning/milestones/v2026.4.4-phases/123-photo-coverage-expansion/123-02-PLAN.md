---
phase: 123-photo-coverage-expansion
plan: 02
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - .planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md
autonomous: false
requirements: [PHOTO-01]
must_haves:
  truths:
    - "Every candidate from 123-AUDIT-OUTPUT.csv appears in 123-REVIEW-DATA.md with a sourcing decision"
    - "Each row has either a photo_source_url (with source tier 1-4 per D-05) or is marked NO_PHOTO (per D-04)"
    - "User has reviewed and approved the REVIEW-DATA table before any import runs"
  artifacts:
    - path: ".planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md"
      provides: "Per-candidate sourcing decisions — photo URL + source tier, or NO_PHOTO"
      contains: "| politician_id |"
  key_links:
    - from: "123-REVIEW-DATA.md rows"
      to: "123-AUDIT-OUTPUT.csv rows"
      via: "politician_id one-to-one"
      pattern: "politician_id"
---

<objective>
Research a photo for every candidate in `123-AUDIT-OUTPUT.csv` and compile a single review table (`123-REVIEW-DATA.md`) that the user approves before any import runs. Sourcing follows D-04 (Ballotpedia + one web/social search; if nothing, mark `NO_PHOTO`) and the D-05 priority order (gov > campaign > social > news). The user checkpoint at the end of this plan is the go/no-go gate for Plan 03.

Purpose: Claude-researches / user-approves is the established Phase 120 pattern (D-07). Batching all candidates into one review pass (D-08) means one import run in Plan 03.
Output: `123-REVIEW-DATA.md` with user-signed approval line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-photo-coverage-expansion/123-CONTEXT.md
@.planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv
@.planning/phases/120-contested-race-bio-photo-authoring/120-BIO-METHODOLOGY.md

<interfaces>
Downstream consumer contract — `import-123-photo-expansion.ts` (Plan 03) will read a JSON file with this shape (per PATTERNS.md):

```typescript
interface CandidateImport {
  politician_id: string;   // UUID
  full_name: string;
  slug: string;
  photo_source_url: string | null;   // null => NO_PHOTO => skip in import (initials fallback)
}
```

The REVIEW-DATA table columns must be sufficient to produce this JSON in Plan 03 Task 1. That means every row needs: politician_id, full_name, slug, photo_source_url (or NO_PHOTO), source_tier (1-4 per D-05), source_notes.

Sourcing priority (D-05, carried from Phase 120 D-08):
  1. Official government photo (gov domain)
  2. Campaign website
  3. Social media (LinkedIn, X/Twitter)
  4. News article photo

Sourcing cutoff (D-04): Ballotpedia + one other web/social search. If both fail, mark `NO_PHOTO`. Do NOT exceed two searches per candidate — breadth over perfection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Research photos and build 123-REVIEW-DATA.md</name>
  <files>.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md</files>
  <read_first>
    - .planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv (every row)
    - .planning/phases/120-contested-race-bio-photo-authoring/120-BIO-METHODOLOGY.md §photo sourcing, §fallback policy
    - .planning/phases/123-photo-coverage-expansion/123-CONTEXT.md D-04, D-05, D-06, D-07, D-08
  </read_first>
  <action>
    For each candidate in `123-AUDIT-OUTPUT.csv` (exact ordering preserved):
    1. Search Ballotpedia for the candidate by `full_name` + office context inferable from their politician record (look up office via DB if needed to disambiguate common names).
    2. If Ballotpedia has no photo, perform ONE additional web or social search (Google `<full_name> <office> photo`, or LinkedIn, or campaign site). Do NOT perform a third search — D-04 cutoff.
    3. If a photo is found, capture the direct image URL (must be a `.jpg/.jpeg/.png/.gif/.webp` resolvable URL; if only an HTML page is found, follow to the direct image). Never hotlink — Plan 03 will download and re-host (D-06). The URL here is the SOURCE, not the final CDN URL.
    4. If neither search surfaces a photo, mark the row `NO_PHOTO` and move on.

    Write results into `.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md` with this exact structure:

    ```markdown
    # Phase 123: Photo Sourcing Review Data

    **Audit source:** 123-AUDIT-OUTPUT.csv ({N} candidates)
    **Sourcing cutoff:** Ballotpedia + 1 web/social search per candidate (D-04)
    **Priority order:** gov > campaign > social > news (D-05)

    ## Summary
    - Total candidates: {N}
    - Photo sourced: {X}
    - NO_PHOTO: {Y}
    - Source tier breakdown: T1 {a} / T2 {b} / T3 {c} / T4 {d}

    ## Review Table

    | # | politician_id | full_name | slug | photo_source_url | source_tier | source_notes |
    |---|---------------|-----------|------|------------------|-------------|--------------|
    | 1 | {uuid}        | {name}    | {slug} | https://...    | 1           | Ballotpedia — gov photo |
    | 2 | {uuid}        | {name}    | {slug} | NO_PHOTO        | —           | Not on Ballotpedia; no campaign site; township trustee |
    ...

    ## User Approval

    - [ ] User approves this table; Plan 03 may proceed with import.
    ```

    Every row in the audit CSV MUST appear as exactly one row in the table. Count must reconcile.
  </action>
  <verify>
    <automated>AUDIT_ROWS=$(tail -n +2 .planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv | wc -l | tr -d ' ') && TABLE_ROWS=$(grep -c "^| [0-9]" .planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md) && [ "$AUDIT_ROWS" = "$TABLE_ROWS" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md` exists.
    - Row count in the review table equals the data-row count in `123-AUDIT-OUTPUT.csv` (exact match).
    - Every row has either a URL starting with `http` in `photo_source_url` OR the literal string `NO_PHOTO`.
    - Every sourced row has a `source_tier` in {1,2,3,4}.
    - Summary section's counts reconcile with table rows.
    - `grep -c "| —" 123-REVIEW-DATA.md` equals the NO_PHOTO count (source_tier is `—` for NO_PHOTO rows).
  </acceptance_criteria>
  <done>Review table complete; every audit candidate has a sourcing decision; summary counts reconcile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review and approval of 123-REVIEW-DATA.md</name>
  <files>.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md</files>
  <what-built>
    A complete sourcing table in `.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md` with one row per candidate from the audit, each marked with either a `photo_source_url` (+ source tier 1-4) or `NO_PHOTO`.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md`.
    2. Skim the summary counts — does "Photo sourced + NO_PHOTO" equal "Total candidates"?
    3. Spot-check 5–10 rows: click the `photo_source_url` and confirm it resolves to a headshot of the named person (not a different person, not a broken link, not a page-not-image).
    4. Spot-check 2–3 `NO_PHOTO` rows — confirm they are low-profile (township trustee, uncontested, etc.) and that accepting initials fallback is acceptable for them.
    5. If any row is wrong, mark it in the file and ask Claude to re-research.
    6. When satisfied, tick the `[ ]` box on the "User Approval" line to `[x]` and type "approved" in chat.
  </how-to-verify>
  <action>
    Pause execution. Present the review table location to the user, wait for "approved" or revision instructions. If revisions requested, re-run research for the flagged rows and regenerate the affected table rows, then re-request approval. Do not advance to Plan 03 until the User Approval checkbox is `[x]` and the user has typed "approved".
  </action>
  <verify>
    <automated>grep -q "^- \[x\] User approves this table" .planning/phases/123-photo-coverage-expansion/123-REVIEW-DATA.md</automated>
  </verify>
  <acceptance_criteria>
    - User has replied "approved" (or described changes needed).
    - The `[ ]` on the User Approval line becomes `[x]` before Plan 03 starts.
  </acceptance_criteria>
  <done>User approval checkbox ticked; chat contains "approved" reply.</done>
  <resume-signal>Type "approved" or describe required revisions.</resume-signal>
</task>

</tasks>

<verification>
- Audit row count == review table row count.
- Every row has a decision (URL or NO_PHOTO).
- Human approval recorded in the file.
</verification>

<success_criteria>
Plan 03 has a user-blessed, unambiguous list of what to upload and what to skip.
</success_criteria>

<output>
After completion, create `.planning/phases/123-photo-coverage-expansion/123-02-SUMMARY.md` noting:
- Final sourced vs NO_PHOTO counts
- Source tier breakdown
- Any candidates where sourcing was genuinely unclear (for Plan 03 dry-run attention)
</output>
