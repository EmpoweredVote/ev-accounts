---
phase: 123-photo-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ev-accounts/backend/scripts/audit-123-photo-gap.ts
  - .planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv
autonomous: true
requirements: [PHOTO-01]
must_haves:
  truths:
    - "An audit script exists that lists every linked candidate missing a type='default' photo row"
    - "The script runs read-only (no DB writes) and emits CSV to stdout"
    - "The phase directory contains a saved CSV snapshot of the photo-gap list used for planning the import"
  artifacts:
    - path: "ev-accounts/backend/scripts/audit-123-photo-gap.ts"
      provides: "Read-only audit listing linked candidates missing a default politician_images row"
      contains: "politician_id IS NOT NULL"
    - path: ".planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv"
      provides: "Authoritative photo-gap list captured at phase start"
  key_links:
    - from: "audit-123-photo-gap.ts"
      to: "essentials.politician_images + essentials.race_candidates"
      via: "LEFT JOIN on politician_id with type='default'"
      pattern: "LEFT JOIN essentials\\.politician_images"
---

<objective>
Produce the authoritative photo-gap list for Phase 123 by adapting `audit-112-headshots.ts` into `audit-123-photo-gap.ts`. The script enumerates every linked candidate (`politician_id IS NOT NULL`) missing a `type='default'` row in `essentials.politician_images`, including the 17 Phase 120 Priority C township candidates (per D-03). The roadmap estimate of 62 is not authoritative — this audit's output is (per D-01/D-02).

Purpose: All downstream work (research in Plan 02, import in Plan 03) is driven by this list. Running the audit at phase start prevents stale-estimate drift.
Output: New audit script + saved CSV snapshot committed to the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-photo-coverage-expansion/123-CONTEXT.md
@.planning/phases/123-photo-coverage-expansion/123-PATTERNS.md
@ev-accounts/backend/scripts/audit-112-headshots.ts

<interfaces>
From ev-accounts/backend/scripts/audit-112-headshots.ts (analog to copy):
- `dotenv.config({ path: path.resolve(__dirname, '..', '.env') })` bootstrap
- `new pg.Pool({ connectionString: process.env.DATABASE_URL })`
- CSV-to-stdout pattern with `process.stdout.write`
- Fatal-error tail: `main().catch(...) { pool.end(); process.exit(1); }`

Target DB tables (read-only):
- `essentials.race_candidates (full_name, politician_id, candidate_status, race_id, photo_url)`
- `essentials.races (id, election_id)`
- `essentials.elections (id, election_date, state)`
- `essentials.politician_images (politician_id, url, type)` — LEFT JOIN filter on `type = 'default'`
- `essentials.politicians (id, full_name, slug)` — JOIN to recover slug for downstream import

Phase 120 Priority C reference:
- `.planning/phases/120-contested-race-bio-photo-authoring/120-CANDIDATE-SCOPE.md` — Priority C lists the 17 deferred township contested-race candidates who will also surface here (D-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create audit-123-photo-gap.ts with broadened query</name>
  <files>ev-accounts/backend/scripts/audit-123-photo-gap.ts</files>
  <read_first>
    - ev-accounts/backend/scripts/audit-112-headshots.ts (full file — this is the exact analog to copy)
    - .planning/phases/123-photo-coverage-expansion/123-PATTERNS.md §"audit-123-photo-gap.ts" (header/query/CSV excerpts)
    - .planning/phases/123-photo-coverage-expansion/123-CONTEXT.md D-01, D-02, D-03
  </read_first>
  <action>
    Copy `audit-112-headshots.ts` to `audit-123-photo-gap.ts` and modify the core query per PATTERNS.md:
    1. Keep dotenv + pg.Pool bootstrap verbatim.
    2. Replace the WHERE clause: drop `e.election_date = '2026-05-05' AND e.state = 'IN'`. New filter: `rc.politician_id IS NOT NULL AND rc.candidate_status = 'active' AND pi.url IS NULL` — i.e. every active linked race_candidate missing a `type='default'` politician_images row.
    3. Add `p.slug` to the SELECT by JOINing `essentials.politicians p ON p.id = rc.politician_id` (required by the import script in Plan 03 to construct the Storage filepath).
    4. CSV header: `full_name,politician_id,slug,is_linked,photo_source,cdn_url,stub_photo_url`. `is_linked` is always `linked` given the WHERE; keep the column for symmetry with audit-112.
    5. Keep `--dry-run` flag (stdout CSV is default; `--dry-run` is a no-op for this read-only script but preserves convention).
    6. Keep fatal-error tail verbatim.
    7. Do NOT add any INSERT/UPDATE/DELETE — this script is read-only.
  </action>
  <verify>
    <automated>cd ev-accounts/backend && npx tsx scripts/audit-123-photo-gap.ts 2>/dev/null | head -1 | grep -q "full_name,politician_id,slug,is_linked,photo_source,cdn_url,stub_photo_url"</automated>
  </verify>
  <acceptance_criteria>
    - File `ev-accounts/backend/scripts/audit-123-photo-gap.ts` exists.
    - `grep -c "politician_id IS NOT NULL" ev-accounts/backend/scripts/audit-123-photo-gap.ts` >= 1.
    - `grep -c "pi.url IS NULL" ev-accounts/backend/scripts/audit-123-photo-gap.ts` >= 1.
    - `grep -c "e.election_date = '2026-05-05'" ev-accounts/backend/scripts/audit-123-photo-gap.ts` == 0 (old filter removed).
    - `grep -c "p.slug" ev-accounts/backend/scripts/audit-123-photo-gap.ts` >= 1.
    - No `INSERT INTO` / `UPDATE ` / `DELETE FROM` present (read-only).
    - Script runs without error against DATABASE_URL and emits CSV to stdout.
  </acceptance_criteria>
  <done>Script exists, runs read-only, emits one CSV row per linked candidate missing a default photo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture audit snapshot CSV into phase directory</name>
  <files>.planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv</files>
  <read_first>
    - ev-accounts/backend/scripts/audit-123-photo-gap.ts (just created)
  </read_first>
  <action>
    Run the audit script once and redirect stdout to `.planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv`. This is the authoritative list Plan 02 will research and Plan 03 will import from. Exact command:

    `cd ev-accounts/backend && npx tsx scripts/audit-123-photo-gap.ts > ../../.planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv`

    Then print the row count (minus 1 for the header) so the user can compare against the roadmap estimate of 62. Do not trim or filter the CSV — keep every row the query returned, per D-02 ("attempt photos for ALL candidates surfaced by the audit").
  </action>
  <verify>
    <automated>test -s .planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv && head -1 .planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv | grep -q "full_name,politician_id,slug"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/123-photo-coverage-expansion/123-AUDIT-OUTPUT.csv` exists and is non-empty.
    - Header line matches the script's header exactly.
    - `wc -l` on the file returns >= 2 (at least one data row; expected ~62 but actual is whatever the query returns).
    - The final line of this task's stdout is a printed candidate count for the user.
  </acceptance_criteria>
  <done>Phase directory contains the canonical photo-gap CSV; candidate count printed.</done>
</task>

</tasks>

<verification>
- `audit-123-photo-gap.ts` compiles under tsx and runs read-only.
- `123-AUDIT-OUTPUT.csv` exists with a header + at least one data row.
- No DB mutations occurred (script is SELECT-only by inspection).
</verification>

<success_criteria>
Plan 02 has an authoritative, phase-scoped list of candidates needing photos. The 17 Phase 120 Priority C names appear in the CSV (spot-check against `120-CANDIDATE-SCOPE.md`).
</success_criteria>

<output>
After completion, create `.planning/phases/123-photo-coverage-expansion/123-01-SUMMARY.md` noting:
- Final candidate count surfaced by the audit
- Any surprises vs the roadmap "62" estimate
- Whether the 17 Phase 120 Priority C candidates were present (spot-check)
</output>
