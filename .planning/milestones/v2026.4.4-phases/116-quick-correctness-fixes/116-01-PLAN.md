---
phase: 116-quick-correctness-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ev-ui/src/SiteHeader.jsx
  - .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md
autonomous: false
requirements: [CORR-01, CORR-02, CORR-03]

must_haves:
  truths:
    - "Election Central displays May 5, 2026 (verified data-driven; CORR-01 closed as misflag)"
    - "SiteHeader nav contains no broken links — About Us / Volunteer / FAQ are removed"
    - "Features dropdown points Treasury Tracker at https://treasurytracker.empowered.vote and Empowered Badges at https://badges.empowered.vote"
    - "ev-ui patch release published to npm and auto-bumped into all 4 consumer repos via repository_dispatch"
    - "essentials.empowered.vote production nav reflects the fix"
    - "Representatives tab default shows all elected officials (verified Results.jsx defaults; CORR-03 closed as misflag)"
  artifacts:
    - path: "ev-ui/src/SiteHeader.jsx"
      provides: "Cleaned defaultNavItems and corrected Features dropdown URLs"
      contains: "treasurytracker.empowered.vote"
    - path: ".planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md"
      provides: "Misflag closure notes for CORR-01 and CORR-03, plus CORR-02 verification log"
      contains: "CORR-01: MISFLAG"
  key_links:
    - from: "ev-ui patch tag push"
      to: "consumer auto-merge PRs (CompassV2, essentials, read-rank, civic-spaces)"
      via: "publish.yml OIDC npm publish + repository_dispatch"
      pattern: "npm version patch && git push origin main --follow-tags"
---

<objective>
Ship the three Phase 116 voter-correctness fixes by (a) editing ev-ui SiteHeader to remove three 404-bound nav links and update two stale Features dropdown URLs, (b) cutting an ev-ui patch release through the standard auto-bump pipeline so essentials.empowered.vote and other consumers pick up the fix, and (c) recording misflag closures for CORR-01 and CORR-03 in VERIFICATION.md. All implementation decisions are pre-locked in 116-CONTEXT.md (D-01 through D-14) — this plan executes them verbatim.

Purpose: Restore voter trust in nav and election information before the May 5, 2026 Indiana primary.
Output: Updated SiteHeader.jsx, new ev-ui patch version live on npm, consumer auto-merge PRs landed, 116-VERIFICATION.md with misflag closures and post-deploy verification log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-quick-correctness-fixes/116-CONTEXT.md
@ev-ui/src/SiteHeader.jsx
@ev-ui/README-AUTOBUMP.md

<interfaces>
<!-- Current SiteHeader nav structure (ev-ui/src/SiteHeader.jsx) -->

```jsx
const defaultNavItems = [
  { label: 'About Us', href: 'https://empowered.vote/about' },           // DELETE per D-04
  {
    label: 'Features',
    href: '#',
    dropdown: [
      { label: 'Political Compass', href: 'https://compass.empowered.vote' },        // KEEP per D-06
      { label: 'Find Representatives', href: 'https://essentials.empowered.vote' },  // KEEP per D-06
      { label: 'Read & Rank', href: 'https://readrank.empowered.vote' },             // KEEP per D-06
      { label: 'Treasury Tracker', href: 'https://ev-prototypes.netlify.app/treasury-tracker/dist' }, // REPLACE per D-05
      { label: 'Empowered Badges', href: 'https://ev-prototypes.netlify.app/empowered-badges/dist' }, // REPLACE per D-05
    ],
  },
  { label: 'Volunteer', href: 'https://empowered.vote/volunteer' },      // DELETE per D-04
  { label: 'FAQ', href: 'https://empowered.vote/faq' },                  // DELETE per D-04
];

const defaultCtaButton = {
  label: 'Donate',
  href: 'https://empowered.vote/donate',                                  // KEEP per D-07
};
```

After the edit, defaultNavItems must contain exactly ONE entry: the Features dropdown with 5 sub-items (Political Compass, Find Representatives, Read & Rank, Treasury Tracker → treasurytracker.empowered.vote, Empowered Badges → badges.empowered.vote). The Donate CTA is unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 116-VERIFICATION.md with CORR-01 and CORR-03 misflag closures</name>
  <files>.planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/116-quick-correctness-fixes/116-CONTEXT.md (decisions D-01, D-02, D-03, D-10, D-11, D-12)
    - .planning/REQUIREMENTS.md (Tier 1 Quick Correctness section — definitions of CORR-01 and CORR-03)
  </read_first>
  <action>
    Create .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md with three sections:

    1. `## CORR-01: MISFLAG` — Election date is correct.
       - Quote evidence from D-01: Supabase `essentials.elections` row for "2026 Indiana Primary" already holds `election_date = 2026-05-05`. `ElectionsView.jsx` is fully data-driven via `election.election_date` with timezone-safe `formatDate()` (parses as `T12:00:00` local). Wide grep across essentials, CompassV2, ev-ui, and empowered-vote-static turned up no hardcoded wrong date.
       - Note from D-02: stale `/** Format election date: "May 6, 2026" */` JSDoc comment in `essentials/src/components/ElectionsView.jsx:29` is a docstring example, not a bug — left as-is.
       - Disposition (D-03): No code change. Closed as misflag. No production smoke-test required (user accepted misflag classification explicitly).

    2. `## CORR-03: MISFLAG` — Representatives tab defaults already correct.
       - Quote evidence from D-10: `essentials/src/pages/Results.jsx` defaults — `activeView` defaults to `'representatives'` (line 181), `selectedFilter` defaults to `'All'` (line 247), `appointedFilter` defaults to `'All'` (line 252).
       - Architecture note from D-11: Sitting officials only on the Representatives tab. Challengers/candidates belong in the Elections tab — current architecture matches intent.
       - Disposition (D-12): No code change. Closed as misflag.

    3. `## CORR-02: Verification Log` — placeholder section to be filled in by Task 4 after the post-deploy check. Include the literal heading `## CORR-02: Verification Log` and a bullet stub: `- [ ] Pending post-deploy verification on essentials.empowered.vote (D-14)`.

    Use plain markdown. The strings `CORR-01: MISFLAG` and `CORR-03: MISFLAG` must appear verbatim in headings.
  </action>
  <verify>
    <automated>grep -c "CORR-01: MISFLAG" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md | grep -q "^1$" && grep -c "CORR-03: MISFLAG" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md | grep -q "^1$" && grep -q "CORR-02: Verification Log" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md` exists
    - `grep -c "CORR-01: MISFLAG" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md` returns `1`
    - `grep -c "CORR-03: MISFLAG" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md` returns `1`
    - `grep -q "CORR-02: Verification Log" .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md` succeeds
    - File quotes the specific evidence from D-01 and D-10 (Supabase row date, Results.jsx line numbers)
  </acceptance_criteria>
  <done>VERIFICATION.md exists with both misflag closures and the CORR-02 placeholder; greps above all pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit ev-ui/src/SiteHeader.jsx — delete 3 nav items and update 2 dropdown URLs</name>
  <files>ev-ui/src/SiteHeader.jsx</files>
  <read_first>
    - ev-ui/src/SiteHeader.jsx (current state — defaultNavItems array on lines 18–33)
    - .planning/phases/116-quick-correctness-fixes/116-CONTEXT.md (decisions D-04, D-05, D-06, D-07)
  </read_first>
  <action>
    Edit `ev-ui/src/SiteHeader.jsx` per D-04 and D-05.

    DELETE these three entries from `defaultNavItems` entirely (per D-04):
    ```
    { label: 'About Us', href: 'https://empowered.vote/about' },
    { label: 'Volunteer', href: 'https://empowered.vote/volunteer' },
    { label: 'FAQ', href: 'https://empowered.vote/faq' },
    ```

    REPLACE these two URLs inside the Features dropdown (per D-05):
    - `https://ev-prototypes.netlify.app/treasury-tracker/dist` → `https://treasurytracker.empowered.vote`
    - `https://ev-prototypes.netlify.app/empowered-badges/dist` → `https://badges.empowered.vote`

    LEAVE UNCHANGED (per D-06 and D-07):
    - Political Compass → `https://compass.empowered.vote`
    - Find Representatives → `https://essentials.empowered.vote`
    - Read & Rank → `https://readrank.empowered.vote`
    - `defaultCtaButton` Donate → `https://empowered.vote/donate`

    After edit, `defaultNavItems` must be a single-element array containing only the Features dropdown with exactly 5 sub-items in the order listed in the interfaces block above. No other code, exports, JSX, or props should change.
  </action>
  <verify>
    <automated>cd ev-ui && grep -c "'About Us'" src/SiteHeader.jsx | grep -q "^0$" && grep -c "'Volunteer'" src/SiteHeader.jsx | grep -q "^0$" && grep -c "'FAQ'" src/SiteHeader.jsx | grep -q "^0$" && grep -c "ev-prototypes.netlify.app" src/SiteHeader.jsx | grep -q "^0$" && grep -q "treasurytracker.empowered.vote" src/SiteHeader.jsx && grep -q "badges.empowered.vote" src/SiteHeader.jsx && grep -q "compass.empowered.vote" src/SiteHeader.jsx && grep -q "essentials.empowered.vote" src/SiteHeader.jsx && grep -q "readrank.empowered.vote" src/SiteHeader.jsx && grep -q "empowered.vote/donate" src/SiteHeader.jsx && npm run build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'About Us'" ev-ui/src/SiteHeader.jsx` returns `0`
    - `grep -c "'Volunteer'" ev-ui/src/SiteHeader.jsx` returns `0`
    - `grep -c "'FAQ'" ev-ui/src/SiteHeader.jsx` returns `0`
    - `grep -c "ev-prototypes.netlify.app" ev-ui/src/SiteHeader.jsx` returns `0`
    - `grep "treasurytracker.empowered.vote" ev-ui/src/SiteHeader.jsx` returns at least one match
    - `grep "badges.empowered.vote" ev-ui/src/SiteHeader.jsx` returns at least one match
    - `grep "compass.empowered.vote" ev-ui/src/SiteHeader.jsx` still matches (unchanged)
    - `grep "essentials.empowered.vote" ev-ui/src/SiteHeader.jsx` still matches (unchanged)
    - `grep "readrank.empowered.vote" ev-ui/src/SiteHeader.jsx` still matches (unchanged)
    - `grep "empowered.vote/donate" ev-ui/src/SiteHeader.jsx` still matches (Donate CTA unchanged)
    - `cd ev-ui && npm run build` exits 0
  </acceptance_criteria>
  <done>SiteHeader.jsx contains the cleaned nav, ev-ui builds without error, all greps pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cut ev-ui patch release via auto-bump pipeline</name>
  <read_first>
    - ev-ui/README-AUTOBUMP.md (release flow, secret setup, debugging)
    - .planning/phases/116-quick-correctness-fixes/116-CONTEXT.md (D-08, D-09, D-13)
  </read_first>
  <what-built>
    Task 2 has edited ev-ui/src/SiteHeader.jsx and built it locally. This task ships it via the standard ev-ui auto-bump pipeline so essentials.empowered.vote, compass.empowered.vote, readrank.empowered.vote, and civic-spaces all pick up the new nav within minutes.

    Claude executes the release commands automatically (these are git+npm CLI operations, not human-only):

    ```bash
    cd ev-ui
    git add src/SiteHeader.jsx
    git commit -m "fix(SiteHeader): remove 404 nav links, update Features URLs to Render subdomains

    - Remove About Us, Volunteer, FAQ from defaultNavItems (404 in empowered-vote-static)
    - Treasury Tracker: ev-prototypes.netlify.app → treasurytracker.empowered.vote
    - Empowered Badges: ev-prototypes.netlify.app → badges.empowered.vote

    Phase 116 / CORR-02"
    npm version patch
    git push origin main --follow-tags
    ```

    Per D-08, the tag push triggers `.github/workflows/publish.yml` which:
    1. Publishes the new `@empoweredvote/ev-ui` patch version to public npm via OIDC trusted publishing (no NPM_TOKEN needed)
    2. Fires `repository_dispatch` to CompassV2, essentials, read-rank, civic-spaces
    3. Each consumer's `ev-ui-bump.yml` opens an auto-merge PR; build-check.yml gates the merge
    4. Render auto-deploys each consumer on merge to main

    Per D-09, no pre-release smoke test of the changed URLs is required — user confirmed treasurytracker/badges/compass/essentials/readrank subdomains are live.
  </what-built>
  <how-to-verify>
    After Claude pushes the tag, the human verifies the pipeline ran end-to-end:

    1. Visit https://github.com/chrisandrewsedu/ev-ui/actions and confirm the `publish.yml` workflow for the new tag completed successfully (green).
    2. Visit https://www.npmjs.com/package/@empoweredvote/ev-ui and confirm the new patch version is listed.
    3. Visit the Actions tab of each consumer repo (CompassV2, essentials, read-rank, civic-spaces) and confirm an `ev-ui-bump` PR was opened and auto-merged.
    4. Confirm Render redeployed essentials.empowered.vote (Render dashboard or wait for the deploy to settle, ~3–5 min).

    If any step failed, see ev-ui/README-AUTOBUMP.md debugging section.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 consumer PRs have auto-merged and essentials.empowered.vote has redeployed. Or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Post-merge verification on essentials.empowered.vote (D-14) and update VERIFICATION.md</name>
  <read_first>
    - .planning/phases/116-quick-correctness-fixes/116-CONTEXT.md (D-14)
    - .planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md (CORR-02 placeholder section)
  </read_first>
  <what-built>
    Task 3 has shipped the ev-ui patch and consumer PRs have auto-merged. This task performs the live nav verification per D-14 and records the result in 116-VERIFICATION.md.
  </what-built>
  <how-to-verify>
    Manual check on production (per D-14):

    1. Open https://essentials.empowered.vote in a fresh browser tab.
    2. Inspect the SiteHeader nav. Confirm:
       - "About Us" link is GONE
       - "Volunteer" link is GONE
       - "FAQ" link is GONE
       - "Features" dropdown still exists
    3. Open the Features dropdown and confirm the 5 items resolve correctly:
       - Political Compass → opens https://compass.empowered.vote (200 OK)
       - Find Representatives → opens https://essentials.empowered.vote (200 OK)
       - Read & Rank → opens https://readrank.empowered.vote (200 OK)
       - Treasury Tracker → opens https://treasurytracker.empowered.vote (200 OK, NOT netlify.app)
       - Empowered Badges → opens https://badges.empowered.vote (200 OK, NOT netlify.app)
    4. Confirm the Donate CTA still links to https://empowered.vote/donate.

    After verification passes, Claude updates `.planning/phases/116-quick-correctness-fixes/116-VERIFICATION.md`:
    - Replace the `- [ ] Pending post-deploy verification on essentials.empowered.vote (D-14)` placeholder with a checked entry recording: ev-ui version shipped, consumer PR URLs (or commit SHAs), date verified, and a one-line confirmation that all 5 dropdown items + Donate CTA resolved 200 OK and the 3 deleted links are absent.
  </how-to-verify>
  <resume-signal>Type "approved" once production nav matches expectations and VERIFICATION.md has been updated with the live-check log. Or describe any discrepancy (e.g., a stale CDN, a consumer PR that didn't auto-merge).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ev-ui consumer PR auto-merge | New ev-ui patch version lands in 4 consumer repos via repository_dispatch and auto-merges if patch/minor — no human review on the merge itself |
| Hardcoded outbound URLs in SiteHeader | Constants reviewed in this diff become live links across all EV apps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | SiteHeader.jsx hardcoded URLs | mitigate | Exact strings reviewed in diff (`treasurytracker.empowered.vote`, `badges.empowered.vote`); grep-based acceptance criteria in Task 2 confirm no `netlify.app` strings remain and the new Render hosts are present |
| T-116-02 | Denial of Service (broken nav) | Consumer auto-merge PRs | mitigate | Each consumer's `build-check.yml` gates the auto-merge — a syntactically broken SiteHeader would fail the consumer build and block merge per ev-ui/README-AUTOBUMP.md |
| T-116-03 | Information Disclosure | This phase | accept | No user input, no server state, no PII, no auth surface touched. Pure UI nav config change. |
| T-116-04 | Elevation of Privilege | npm publish credentials | accept | OIDC trusted publishing — no long-lived NPM_TOKEN exists to leak; provenance attestations attached automatically |
</threat_model>

<verification>
Phase-level checks tied to roadmap success criteria:

1. **Election date correct (Success Criterion 1 / CORR-01):** VERIFICATION.md documents that `essentials.elections` row holds `2026-05-05` and `ElectionsView.jsx` is data-driven. Closed as misflag — no production check required per D-03.
2. **Nav links functional (Success Criterion 2 / CORR-02):** Task 4 manually confirms on essentials.empowered.vote that the 3 deleted items are gone and all 5 Features dropdown items + Donate CTA return 200 OK.
3. **Representatives tab default correct (Success Criterion 3 / CORR-03):** VERIFICATION.md documents Results.jsx defaults (`activeView='representatives'`, `selectedFilter='All'`, `appointedFilter='All'`). Closed as misflag per D-12.
</verification>

<success_criteria>
- ev-ui/src/SiteHeader.jsx contains zero references to `ev-prototypes.netlify.app`, About Us, Volunteer, or FAQ
- ev-ui/src/SiteHeader.jsx contains `treasurytracker.empowered.vote` and `badges.empowered.vote`
- A new `@empoweredvote/ev-ui` patch version is published to public npm
- All 4 consumer auto-bump PRs (CompassV2, essentials, read-rank, civic-spaces) have merged
- essentials.empowered.vote production nav reflects the fix (verified live)
- 116-VERIFICATION.md exists with `CORR-01: MISFLAG`, `CORR-03: MISFLAG`, and a CORR-02 verification log filled in
</success_criteria>

<output>
After completion, create `.planning/phases/116-quick-correctness-fixes/116-01-SUMMARY.md` documenting:
- The new ev-ui patch version number
- Links to the 4 consumer auto-merge PRs
- The live-verification result from Task 4
- Reference to 116-VERIFICATION.md for misflag closures
</output>
