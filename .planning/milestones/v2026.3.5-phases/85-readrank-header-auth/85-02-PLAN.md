---
phase: 85-readrank-header-auth
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified: []
autonomous: false
requirements: [RR-01, RR-02, RR-03]

must_haves:
  truths:
    - "A logged-in user sees their username in the ReadRank header with a working Sign out option"
    - "A logged-out user sees a Sign in link in the ReadRank header"
    - "After signing out, the header switches to Sign in state without a page reload"
  artifacts:
    - path: "EV-ReadRank/src/hooks/useAuthState.ts"
      provides: "Extended hook (from plan 01)"
    - path: "EV-ReadRank/src/App.tsx"
      provides: "profileMenu wired to SiteHeader (from plan 01)"
  key_links:
    - from: "ReadRank header profile button"
      to: "compass.empowered.vote/login"
      via: "Sign in link item in profileMenu"
      pattern: "Sign in"
---

<objective>
Human verification that the auth-aware ReadRank header works correctly end-to-end: logged-in state shows username + Sign out, logged-out state shows Sign in link, and the Sign out action clears state immediately.

Purpose: Confirm the three phase requirements (RR-01, RR-02, RR-03) are met before closing the milestone.
Output: Human-confirmed phase completion.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-readrank-header-auth/85-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Auth-aware ReadRank header (RR-01, RR-02, RR-03)</name>
  <files>none</files>
  <action>Human verification — no automated action. See how-to-verify below.</action>
  <what-built>
    Auth-aware ReadRank SiteHeader:
    - ev-ui upgraded to ^0.1.49
    - useAuthState extended to return userName and logout()
    - App.tsx passes conditional profileMenu to SiteHeader (hidden during load, username + Sign out when logged in, Sign in link when logged out)
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```bash
    cd /Users/chrisandrews/Documents/GitHub/EV-ReadRank && npm run dev
    ```
    Open http://localhost:5173 in a browser.

    Test RR-02 — Logged-out state:
    1. Ensure you are not logged in (clear cookies or open incognito)
    2. Confirm the header shows a profile icon / "Account" trigger
    3. Click it — confirm "Sign in" link appears in the dropdown
    4. Confirm clicking "Sign in" navigates to https://compass.empowered.vote/login

    Test RR-01 — Logged-in state:
    1. Log in via compass.empowered.vote/login (manually return to http://localhost:5173 after)
    2. Confirm the header profile button shows your username (not "Account")
    3. Click it — confirm "Sign out" option appears in the dropdown

    Test RR-03 — Sign out:
    1. While logged in, click Sign out from the profile dropdown
    2. Confirm the header immediately switches to "Account" / "Sign in" state (no page reload required)
    3. Confirm clicking Sign in again shows the Sign in link (session is cleared)

    No-flash check:
    4. Hard-refresh the page while logged in (Cmd+Shift+R)
    5. Confirm no "Sign in" flash appears briefly before the username loads — the profile button should simply be absent during the auth check, then snap to the username state
  </how-to-verify>
  <verify>Human confirms all three auth states above</verify>
  <done>All three requirements (RR-01, RR-02, RR-03) visually confirmed on dev server</done>
  <resume-signal>Type "approved" when all three states work correctly, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
All three requirements confirmed by human:
- RR-01: Logged-in user sees username + Sign out
- RR-02: Logged-out user sees Sign in link pointing to compass.empowered.vote/login
- RR-03: Sign out clears session and header switches state immediately
</verification>

<success_criteria>
Human confirms all three auth states render and behave correctly in the ReadRank dev server.
</success_criteria>

<output>
After completion, create `.planning/phases/85-readrank-header-auth/85-02-SUMMARY.md`
</output>
