---
phase: 112-va-delegate-stances
plan: 04
type: execute
wave: 1
depends_on: ["112-03"]
files_modified:
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv
  - supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql
autonomous: true
requirements: [VAST-03, VAST-05]
tags: [stance-research, virginia, house-of-delegates]

must_haves:
  truths:
    - "Each of the 10 Wave 4 delegates (HD-60 through HD-69, Hampton Roads Part 1) either appears in inform.politician_answers with >= 1 row OR is documented as an honest-skip (D-07)"
    - "Every Wave 4 politician_answers row has a paired politician_context row with sources array containing >= 1 non-empty URL (VAST-05 / D-13)"
    - "Migration 334 applies cleanly via psql with the DO $$ block reporting unsourced_count = 0 for external_id BETWEEN -5120069 AND -5120060"
  artifacts:
    - path: "backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv"
      provides: "Source-of-truth CSV with Wave 4 delegate stances (VAST-03)"
      contains: "header row + >= 1 data row per delegate with documentable stances"
    - path: "supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql"
      provides: "Atomic BEGIN/COMMIT migration upserting Wave 4 stances + paired context rows"
      contains: "INSERT INTO inform.politician_answers, INSERT INTO inform.politician_context, DO $$ verification block"
  key_links:
    - from: "backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv"
      to: "supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql"
      via: "CSV reasoning + source URL columns map 1:1 to politician_context rows (VAST-05)"
      pattern: "every CSV row -> matching politician_answers + politician_context insert"
    - from: "supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "psql session pooler apply"
      pattern: "psql \"\\$DATABASE_URL\" -f supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql"
---

<objective>
Research and ingest sourced compass stances for the 10 VA House delegates in Wave 4 (HD-60 through HD-69, Hampton Roads Part 1): Scott A. Wyatt, Michael J. Webert, Karen Fleming Hamilton, Phillip A. Scott, Stacey A. Carroll, Joshua G. Cole, Nicole Cole, Hillary Pugh Kent, M. Keith Hodges, and Mark C. Downey.

Purpose: Continues VAST-03 and contributes to VAST-05. Hampton Roads is a major metro with decent press coverage (Pilot Online, Daily Press). Expected yield 5-13 stances per delegate. Wave 4 is a CONTIGUOUS external_id range (BETWEEN -5120069 AND -5120060).

Output: One pre-flight JSON, one CSV, one numbered migration (334) applied, verified by the inline DO $$ block reporting 0 unsourced rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-va-delegate-stances/112-RESEARCH.md
@.planning/phases/112-va-delegate-stances/112-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 4 pre-flight (migration number + topics snapshot + UUID confirmation)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Patterns 2/3; Wave 4 composition)
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Pre-flight inline node scripts — Query A/B/C)
    - .claude/skills/research-stances/SKILL.md (Topic Resolution section)
    - backend/.env (DATABASE_URL — session pooler)
    - backend/src/lib/db.ts (pool export)
  </read_first>
  <action>
    Run four pre-flight checks; save to backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json with keys {max_migration, highest_file_on_disk, next_free_migration, topics, delegates, existing_rows}.

    Query A — migration number: SELECT MAX(version) FROM supabase_migrations.schema_migrations (expected 325). ALSO `ls supabase/migrations/ | sort | tail -1` — expected highest file 20260610000003_333_va_delegates_wave3_stances.sql. next_free_migration = 334. If highest disk file is not 333, halt and notify orchestrator.

    Query B — live topics snapshot (SKILL.md query). Record actual count; do not hardcode 44.

    Query C — UUID fetch (CONTIGUOUS): SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -5120069 AND -5120060 ORDER BY external_id DESC. Must return exactly 10 rows. Cross-check against Wave 4 roster: HD-60 Scott A. Wyatt d918e6be-5933-4b24-aada-84cbc461c207, HD-61 Michael J. Webert 28714014-4c7e-427e-8190-fcc21875377a, HD-62 Karen Fleming Hamilton 11074d6c-4c9c-4b9e-9606-42eae7a6537c, HD-63 Phillip A. Scott 597a4057-4ccc-43f6-bf97-bcc701d7e637, HD-64 Stacey A. Carroll ac78bc55-8fe9-4efb-a0ea-179842b6c44e, HD-65 Joshua G. Cole e2542ec1-213a-403b-bcda-e956a9384dcb, HD-66 Nicole Cole dcc9683b-f463-490f-aa9a-8b2ff2f7d6bb, HD-67 Hillary Pugh Kent 982ad606-8fc5-4d99-9275-c24b09c65c0c, HD-68 M. Keith Hodges 95cdc29b-18d1-45e1-84c3-ba601f5bec40, HD-69 Mark C. Downey aefff366-6345-45fc-b52d-0e4ceb64d121. Any mismatch is a halt condition.

    Query D — 0-existing-rows: SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120069 AND -5120060. Must return 0. Record as existing_rows.

    Use the standard `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."` Bash pattern. No PostgREST (D-14).
  </action>
  <verify>
    <automated>node -e "const j=require('./backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json'); if(j.next_free_migration!==334){console.error('Expected 334, got '+j.next_free_migration); process.exit(1)}; if(!Array.isArray(j.topics)||j.topics.length<25){console.error('Topics invalid'); process.exit(1)}; if(!Array.isArray(j.delegates)||j.delegates.length!==10){console.error('Expected 10, got '+(j.delegates||[]).length); process.exit(1)}; const ids=j.delegates.map(d=>d.id).sort(); const expected=['d918e6be-5933-4b24-aada-84cbc461c207','28714014-4c7e-427e-8190-fcc21875377a','11074d6c-4c9c-4b9e-9606-42eae7a6537c','597a4057-4ccc-43f6-bf97-bcc701d7e637','ac78bc55-8fe9-4efb-a0ea-179842b6c44e','e2542ec1-213a-403b-bcda-e956a9384dcb','dcc9683b-f463-490f-aa9a-8b2ff2f7d6bb','982ad606-8fc5-4d99-9275-c24b09c65c0c','95cdc29b-18d1-45e1-84c3-ba601f5bec40','aefff366-6345-45fc-b52d-0e4ceb64d121'].sort(); if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('UUID mismatch'); process.exit(1)}; if(j.existing_rows!==0){console.error('Expected 0 existing rows, got '+j.existing_rows); process.exit(1)}; console.log('preflight OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json exists, valid JSON
    - next_free_migration equals 334; highest_file_on_disk equals 333
    - topics array >= 25 objects, each with stances sub-array length 5
    - delegates is exactly 10 objects whose ids match the Wave 4 UUIDs
    - existing_rows equals 0
    - Returned full_name values include Wyatt, Webert, Hamilton, Phillip A. Scott, Stacey A. Carroll, Joshua G. Cole, Nicole Cole, Pugh Kent, Hodges, Downey
  </acceptance_criteria>
  <done>
    Pre-flight JSON written; migration 334 confirmed; 10 Wave 4 UUIDs verified; 0 existing rows; topics captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential research dispatch for 10 Wave 4 delegates (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 1 — dispatch rules, prompt template, FIVE-CHAIRS, --output-file)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Source Strategy — Hampton Roads section + Agent URL Fetch Pattern)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_research_one_at_a_time.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_no_assumption.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_scale_embed_texts.md
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json (UUID list + topic JSON)
  </read_first>
  <action>
    Dispatch politician-stance-researcher EXACTLY ONCE PER DELEGATE, SEQUENTIALLY (D-11). Never parallel. Wait for each CSV append before next.

    Dispatch order (HD-60 through HD-69):
    1. Scott A. Wyatt (VA HD-60, Republican) — d918e6be-5933-4b24-aada-84cbc461c207
    2. Michael J. Webert (VA HD-61, Republican) — 28714014-4c7e-427e-8190-fcc21875377a
    3. Karen Fleming Hamilton (VA HD-62, Democrat) — 11074d6c-4c9c-4b9e-9606-42eae7a6537c
    4. Phillip A. Scott (VA HD-63, Republican) — 597a4057-4ccc-43f6-bf97-bcc701d7e637
    5. Stacey A. Carroll (VA HD-64, Democrat) — ac78bc55-8fe9-4efb-a0ea-179842b6c44e
    6. Joshua G. Cole (VA HD-65, Democrat) — e2542ec1-213a-403b-bcda-e956a9384dcb
    7. Nicole Cole (VA HD-66, Democrat) — dcc9683b-f463-490f-aa9a-8b2ff2f7d6bb
    8. Hillary Pugh Kent (VA HD-67, Republican) — 982ad606-8fc5-4d99-9275-c24b09c65c0c
    9. M. Keith Hodges (VA HD-68, Republican) — 95cdc29b-18d1-45e1-84c3-ba601f5bec40
    10. Mark C. Downey (VA HD-69, Republican) — aefff366-6345-45fc-b52d-0e4ceb64d121

    Per-agent prompt requirements (SKILL.md STEP 1 template): full_name + "VA House of Delegates" + HD-NN; embed FULL topic JSON from preflight (D-15); attempt all live topics (D-06, city-level via honest-skip); verbatim honest-skip + no-party-inference instruction (D-07/08/09); --output-file C:/EV-Accounts/backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv (all 10 append to same CSV); source fetch order Hampton Roads: ballotpedia.org/[First_Last] -> lis.virginia.gov (H[district]) -> house.virginia.gov/member/[district] -> vpap.org/officials/[slug] -> Wikipedia -> Pilot Online (pilotonline.com), Daily Press (dailypress.com); WebFetch ONLY (D-12); CSV columns full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3 (+ quotes if produced).

    Note: Joshua G. Cole (HD-65) and Nicole Cole (HD-66) are distinct delegates — do not conflate. Keep their UUIDs and full_names exact.

    Between dispatches: verify CSV gained rows; flag < 2 min completions (Pitfall 1) and retry. No value=3 padding (Pitfall 5). 0 rows acceptable (D-10).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {parse}=require('csv-parse/sync'); if(!fs.existsSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv')){console.error('CSV missing'); process.exit(1)}; const rows=parse(fs.readFileSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv','utf8'),{columns:true,skip_empty_lines:true,relax_column_count:true}); const allowed=new Set(['Scott A. Wyatt','Michael J. Webert','Karen Fleming Hamilton','Phillip A. Scott','Stacey A. Carroll','Joshua G. Cole','Nicole Cole','Hillary Pugh Kent','M. Keith Hodges','Mark C. Downey']); const bad=rows.filter(r=>!allowed.has(r.full_name)); if(bad.length){console.error('Unexpected full_name: '+[...new Set(bad.map(b=>b.full_name))].join('|')); process.exit(1)}; const uns=rows.filter(r=>!r.source_url_1||r.source_url_1.trim()===''); if(uns.length){console.error(uns.length+' rows missing source_url_1'); process.exit(1)}; console.log('wave4 CSV OK: '+rows.length+' rows across '+new Set(rows.map(r=>r.full_name)).size+' delegates')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv exists (header present even if 0 data rows)
    - Every full_name is one of the 10 Wave 4 delegate names
    - Every CSV data row has non-empty source_url_1 (VAST-05)
    - No topic_key outside the preflight topics array
    - No two agents dispatched in parallel
    - Any 0-row delegate noted as documented honest-skip
  </acceptance_criteria>
  <done>
    Wave 4 CSV holds research for Hampton Roads delegates with documentable evidence; source URLs present; honest-skips noted; ready for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author migration 334 SQL (Wave 4 — contiguous BETWEEN -5120069 AND -5120060)</name>
  <files>supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Migration SQL files section — header, BEGIN/COMMIT, DO $$ block, ARRAY() sources filter, single-quote escaping)
    - supabase/migrations/20260609000005_330_va_senators_wave5_stances.sql (canonical analog)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave4.csv
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave4-preflight.json
  </read_first>
  <action>
    Author supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql translating every Wave 4 CSV data row into a paired INSERT (politician_answers + politician_context). Read CSV with csv-parse/sync (Pitfall 6).

    Header (mirror 330): "Phase 112-04: VA House Delegate Stances — Wave 4 (HD-60 through HD-69, Hampton Roads Part 1)"; "Requirements covered: VAST-03, VAST-05"; source CSV path; cross-check counts (equal); "max_migration at authoring: 325"; honest-skip list; 10-delegate UUID table; "Migration number: 334"; "Timestamp: 20260610000004"; "Applied: NOT YET (write-only)".

    Single BEGIN; ... COMMIT;. For each row emit paired INSERTs exactly as in analog (ON CONFLICT DO UPDATE on both; sources via ARRAY(SELECT u FROM unnest(ARRAY[url1,url2,url3]) AS u WHERE u IS NOT NULL AND trim(u) != '')). Escape every ' as ''. Skip empty-source rows (log in header).

    DO $$ verification — Wave 4 is CONTIGUOUS, use BETWEEN -5120069 AND -5120060 for both delegate_count and unsourced_count queries. Use pc.politician_id IS NULL (NOT pc.id IS NULL — Pitfall 2). ASSERT unsourced_count = 0.

    Do NOT apply yet (Task 4). If 0 CSV rows, still create the file with header skip note, empty body, and DO $$ block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql','utf8'); const ans=(sql.match(/INSERT INTO inform\.politician_answers/g)||[]).length; const ctx=(sql.match(/INSERT INTO inform\.politician_context/g)||[]).length; if(ans!==ctx){console.error('Pair mismatch: '+ans+' vs '+ctx); process.exit(1)}; if(!/BEGIN;[\s\S]+COMMIT;/.test(sql)){console.error('Missing BEGIN/COMMIT'); process.exit(1)}; if(!/ASSERT unsourced_count = 0/.test(sql)){console.error('Missing ASSERT'); process.exit(1)}; if(!/BETWEEN -5120069 AND -5120060/.test(sql)){console.error('DO \$\$ must scope to BETWEEN -5120069 AND -5120060'); process.exit(1)}; if(/pc\.id IS NULL/.test(sql)){console.error('Use pc.politician_id IS NULL'); process.exit(1)}; console.log('Migration 334 SQL OK: '+ans+' paired blocks')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql exists
    - Header lists all 10 Wave 4 UUIDs + honest-skips
    - answers INSERT count equals context INSERT count, both equal CSV rows with non-empty source_url_1
    - Exactly one BEGIN; and one COMMIT;
    - DO $$ block with ASSERT unsourced_count = 0 and external_id BETWEEN -5120069 AND -5120060
    - LEFT JOIN uses pc.politician_id IS NULL
    - No unescaped ' in any reasoning literal
  </acceptance_criteria>
  <done>
    Migration 334 SQL drafted; paired-insert invariant holds; verification block scoped to Wave 4 contiguous range with BETWEEN; ready for psql apply.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration 334 via psql and confirm DO $$ verification passes</name>
  <files>supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql</files>
  <read_first>
    - supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql
    - backend/.env (DATABASE_URL — session pooler aws-0-*.pooler.supabase.com:5432)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Environment Availability + psql apply pattern)
  </read_first>
  <action>
    Apply Wave 4 migration via psql session pooler.

    Step 1: set -a && source backend/.env && set +a. Confirm $DATABASE_URL points at aws-0-*.pooler.supabase.com:5432.
    Step 2: psql "$DATABASE_URL" -f supabase/migrations/20260610000004_334_va_delegates_wave4_stances.sql
    Step 3: Capture output. DO $$ emits "VA delegates with stances (Wave 4): <N>" (0..10) and "Unsourced VA delegate stances (Wave 4): 0". ASSERT rolls back if unsourced != 0.
    Step 4: Follow-up confirm: psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS delegates, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120069 AND -5120060;"

    Do NOT use supabase db push or the JS client. If duplicate-version conflict, rename file to (highest disk + 1) and notify orchestrator; never edit schema_migrations. On success update header "Applied: NOT YET" -> "Applied: 2026-06-10".
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5120069 AND -5120060 AND (pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);" | tr -d ' \n' | grep -qx '0' && echo "VAST-05 OK: 0 unsourced Wave 4 stances" || (echo "VAST-05 VIOLATION"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - psql apply exits 0 (no rollback)
    - DO $$ prints "Unsourced VA delegate stances (Wave 4): 0"
    - Follow-up SQL: COUNT(DISTINCT pa.politician_id) for BETWEEN -5120069 AND -5120060 is 0..10
    - Follow-up SQL: unsourced count for the same range returns 0
    - Header updated to "Applied: 2026-06-10"
  </acceptance_criteria>
  <done>
    Migration 334 applied; Wave 4 Hampton Roads stances live; VAST-05 holds for the Wave 4 range; next free migration is 335.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | Untrusted scraped content as plain text; ingested via psql, never executed |
| CSV → SQL migration | Single-quote-escaped literals in a static SQL file |
| psql client → Supabase DB | TLS session pooler; RLS enforces public-read/admin-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Party-inferred stance with no source | mitigate | D-09 in agent prompt; Task 2 verify rejects rows missing source_url_1; DO $$ ASSERT |
| T-112-02 | Tampering | FK violation | mitigate | Task 1 pre-flight confirms all 10 FK targets exist |
| T-112-03 | Tampering | Duplicate / over-count rows | mitigate | DO $$ block + ON CONFLICT upsert; Task 3 verify asserts equal insert counts |
| T-112-04 | Tampering | CSV reasoning contains SQL meta-chars | mitigate | Task 3 escapes ' as '' |
| T-112-05 | Denial of Service | Parallel agents exhaust WebFetch quota | mitigate | Task 2 sequential dispatch (D-11) |
| T-112-06 | Information Disclosure | Public-record research data | accept | Public web only; no PII |

No high-severity threats. No auth, no endpoints, no PII. ASVS not applicable.
</threat_model>

<verification>
- Wave 4 CSV passes csv-parse/sync; every full_name is a Wave 4 delegate; source_url_1 on every data row
- Migration 334: paired INSERTs (equal counts), single BEGIN/COMMIT, DO $$ ASSERT unsourced_count = 0 scoped to BETWEEN -5120069 AND -5120060, pc.politician_id IS NULL
- psql apply prints "Unsourced VA delegate stances (Wave 4): 0"
- Post-apply SQL: COUNT(DISTINCT pa.politician_id) for the range is 0..10; unsourced count returns 0
</verification>

<success_criteria>
- Each of the 10 Wave 4 delegates has >= 1 stance row OR a documented honest-skip
- 100% of inserted answers rows have a paired context row with sources length >= 1
- Migration 334 applied via psql (exit 0, ASSERT passed)
- CSV preserved as forensic artifact
- Plan 112-05 unblocked: next migration number is 335
</success_criteria>

<output>
Create `.planning/phases/112-va-delegate-stances/112-04-SUMMARY.md` when done, including total stances, per-delegate breakdown, honest-skips, migration number used, and VAST-05 confirmation (unsourced = 0 for the Wave 4 range).
</output>
