---
phase: 109-la-county-finance
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - backend/scripts/seed-la-city-confirmed.ts
  - backend/scripts/write-la-city-finance-summary.ts
autonomous: true
requirements: [LAFI-01]
tags: [campaign-finance, la-city, socrata, finance-summary]

must_haves:
  truths:
    - "Every LA City elected official with an extant Socrata committee has a confirmed la_socrata politician_sources row"
    - "Patrice Lattimore (City Clerk, appointed) is explicitly handled — either skipped with documented gap or seeded if a committee unexpectedly exists"
    - "la_socrata adapter ingest has been run end-to-end so transparent_motivations.contributions rows exist for every confirmed source"
    - "Every LA City official with contributions has finance_summary populated with {total_raised, cycle, source: 'LA_SOCRATA'}; officials with zero contributions have finance_summary = NULL with the gap documented in the script run log"
  artifacts:
    - path: "backend/scripts/write-la-city-finance-summary.ts"
      provides: "Aggregates la_socrata contributions into essentials.politicians.finance_summary JSONB"
      contains: "buildFinanceSummaryFromSocrata"
    - path: "backend/scripts/seed-la-city-confirmed.ts"
      provides: "Existing seed script extended to cover Patrice Lattimore (City Clerk, external_id=-700002)"
      contains: "Patrice Lattimore"
  key_links:
    - from: "backend/scripts/write-la-city-finance-summary.ts"
      to: "essentials.politicians.finance_summary"
      via: "pool.query UPDATE essentials.politicians SET finance_summary = $1::jsonb"
      pattern: "finance_summary = \\$1::jsonb"
    - from: "backend/scripts/write-la-city-finance-summary.ts"
      to: "transparent_motivations.contributions"
      via: "SELECT SUM(c.amount) JOIN committees JOIN politician_sources"
      pattern: "source_system = 'la_socrata'"
    - from: "backend/scripts/seed-la-city-confirmed.ts"
      to: "campaignFinanceScheduler.runAdapterForAll('la_socrata')"
      via: "direct function call after seeding"
      pattern: "runAdapterForAll\\('la_socrata'\\)"
---

<objective>
Close LAFI-01 — the LA City Socrata path. Extend the existing `seed-la-city-confirmed.ts` (which currently covers 18 officials but misses Patrice Lattimore, City Clerk, seeded in migration 303 with external_id=-700002), run it to seed confirmed sources for the remaining 15+ LA City officials, trigger the la_socrata adapter, then build the new `write-la-city-finance-summary.ts` aggregation script and run it to populate `essentials.politicians.finance_summary` for every LA City official.

Purpose: LAFI-01 requires that LA City Mayor, all 15 Council members, Controller, and Clerk each have either a populated `finance_summary` or a documented NULL. This plan delivers that. It reuses the existing seed script (already hardcoded for 18 officials, with Bass/Raman/Soto-Martinez already confirmed) plus a small extension for Lattimore, then builds the new finance summary aggregation script per RESEARCH.md Pattern 4 and PATTERNS.md "write-la-city-finance-summary.ts" guidance.

Output:
- Extended `seed-la-city-confirmed.ts` covering Patrice Lattimore (skipFinance handling for appointed officials)
- New `backend/scripts/write-la-city-finance-summary.ts` aggregating la_socrata contributions into finance_summary
- `essentials.politicians.finance_summary` populated for at least 15 of the 18 LA City officials with `source = 'LA_SOCRATA'`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-la-county-finance/109-RESEARCH.md
@.planning/phases/109-la-county-finance/109-PATTERNS.md
@backend/scripts/seed-la-city-confirmed.ts
@backend/scripts/run-fec-finance-summary.ts
@backend/src/lib/db.ts
@backend/src/lib/campaignFinanceScheduler.ts
@backend/migrations/268_finance_summary_column.sql
@backend/migrations/303_la_wave2_la_city_controller_clerk.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend seed-la-city-confirmed.ts to include Patrice Lattimore (City Clerk) with skipFinance handling</name>
  <files>backend/scripts/seed-la-city-confirmed.ts</files>
  <read_first>
    - backend/scripts/seed-la-city-confirmed.ts (full file — current TARGET_POLITICIANS list and TargetPolitician interface)
    - backend/migrations/303_la_wave2_la_city_controller_clerk.sql (Lattimore appointment notes, external_id=-700002)
    - .planning/phases/109-la-county-finance/109-RESEARCH.md (Pitfall 1 — Lattimore appointed Sept 2025, likely no committee)
  </read_first>
  <behavior>
    - Running `npx tsx scripts/seed-la-city-confirmed.ts --dry-run` lists all 19 LA City officials (was 18) including Patrice Lattimore
    - For Patrice Lattimore the dry-run output reads `[SKIP] Patrice Lattimore — appointed City Clerk (Sept 2025), no campaign committee expected` (or equivalent skip note)
    - The non-dry-run path does NOT call Socrata for Lattimore and does NOT insert any politician_sources row for her
    - Bass, Raman, Soto-Martinez continue to be detected as already_confirmed (no behavior change for those three)
  </behavior>
  <action>
    Extend the `TargetPolitician` interface in seed-la-city-confirmed.ts with an optional `skipFinance?: boolean` flag and an optional `skipReason?: string` field used only when skipFinance is true.

    Append one entry to `TARGET_POLITICIANS`:
    - fullName: 'Patrice Lattimore'
    - office: 'LA City Clerk (appointed)'
    - searchTerms: ['lattimore']
    - skipFinance: true
    - skipReason: 'Appointed by City Council Sept 2025; no campaign committee expected (migration 303 is_appointed=true).'

    In the main per-politician loop, before calling the Socrata search, branch on `skipFinance`: log `  [SKIP] {fullName} — {skipReason}` and `continue` to the next politician without firing any HTTP request and without inserting any politician_sources row. Add `'skipped_appointed'` to the SeedResult / PoliticianResult.status union and push it into the per-politician result for summary table output.

    Update the closing summary table totals to include a `skipped_appointed` count alongside the existing inserted/promoted/already_confirmed/no_match/ambiguous counts.

    DO NOT change the existing 18 entries' behavior. DO NOT change the file's `new Pool(...)` construction — the pre-existing pattern stays in this script (PATTERNS.md notes the new scripts use `pool` from db.js, but the existing script is exempt and a refactor is out of scope for this plan).
  </action>
  <verify>
    <automated>cd backend && npx tsx scripts/seed-la-city-confirmed.ts --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx tsx scripts/seed-la-city-confirmed.ts --dry-run` exits 0
    - Dry-run stdout contains `Patrice Lattimore`
    - Dry-run stdout contains `[SKIP]` paired with `Lattimore`
    - File contains the literal `skipFinance?: boolean` in the interface
    - File contains the literal `'Patrice Lattimore'`
    - grep of file shows existing entries (Bass, Raman, Soto-Martinez, Hernandez, Nazarian, Blumenfield, Yaroslavsky, Padilla, Rodriguez, Harris-Dawson, Price, Hutt, Park, Lee, Jurado, McOsker, Feldstein Soto, Mejia) remain present and unchanged
  </acceptance_criteria>
  <done>
    Seed script covers all 19 LA City officials from the Phase 108 roster. Lattimore is handled via skipFinance branch — no Socrata calls, no source rows inserted, gap documented in script output. Existing 18 entries unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run extended seed-la-city-confirmed.ts to seed Socrata sources and trigger la_socrata ingest</name>
  <files>backend/scripts/seed-la-city-confirmed.ts</files>
  <read_first>
    - backend/scripts/seed-la-city-confirmed.ts (Task 1 output — final TARGET_POLITICIANS list)
    - backend/src/lib/campaignFinanceScheduler.ts (runAdapterForAll signature, la_socrata dispatch)
    - .planning/phases/109-la-county-finance/109-RESEARCH.md (Shared Patterns: runAdapterForAll trigger; Pitfall 5: SOCRATA_APP_TOKEN)
  </read_first>
  <behavior>
    - Running `npx tsx scripts/seed-la-city-confirmed.ts` (no --dry-run) inserts confirmed la_socrata politician_sources rows for every official the Socrata search returns a committee for
    - The script logs `[Step N] Triggering runAdapterForAll("la_socrata")` and the adapter writes contribution rows into transparent_motivations.contributions
    - Lattimore is skipped (no source row inserted) and the closing summary table shows skipped_appointed = 1
    - After completion, `SELECT COUNT(*) FROM transparent_motivations.politician_sources WHERE source_system = 'la_socrata' AND research_status = 'confirmed'` returns >= 15
    - After completion, ASSERTION 1 of verify-la-county-109.sql reports `confirmed_sources >= 15`
  </behavior>
  <action>
    No code changes. This is an execution step.

    1. Confirm `DATABASE_URL` is set in backend/.env. Optionally set `SOCRATA_APP_TOKEN` from dev.socrata.com to relax rate limits (the script logs a warning if absent — that is acceptable).
    2. Run `cd backend && npx tsx scripts/seed-la-city-confirmed.ts` (no --dry-run). Capture stdout to a log for the SUMMARY (e.g. `cd backend && npx tsx scripts/seed-la-city-confirmed.ts > /tmp/109-02-seed.log 2>&1`).
    3. If any non-Lattimore official returns `no_match` or `ambiguous`, surface the row in the SUMMARY for follow-up — do NOT block the rest of the plan; the script non-aborting loop seeds the matches it found.
    4. Run `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` and confirm ASSERTION 1 reports `confirmed_sources >= 15`. ASSERTION 2 will show `officials_with_summary = 0` at this point — Task 3 closes that gap.
    5. NEVER trigger ingest via HTTP POST to accounts.empowered.vote — the seed script own `runAdapterForAll('la_socrata')` call handles this in-process per RESEARCH.md Shared Patterns.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) AS confirmed_sources FROM transparent_motivations.politician_sources WHERE source_system = 'la_socrata' AND research_status = 'confirmed';"</automated>
  </verify>
  <acceptance_criteria>
    - psql query above returns `confirmed_sources >= 15`
    - psql query `SELECT COUNT(*) FROM transparent_motivations.contributions c JOIN transparent_motivations.committees cm ON cm.id = c.committee_id JOIN transparent_motivations.politician_sources ps ON ps.id = cm.politician_source_id WHERE ps.source_system = 'la_socrata' AND ps.research_status = 'confirmed';` returns > 0 (some contributions exist for at least one LA City committee)
    - Run log contains `runAdapterForAll("la_socrata")` or `runAdapterForAll('la_socrata')`
    - Run log shows Lattimore was skipped, not name-matched (no HTTP request fired for Lattimore)
    - `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` ASSERTION 1 output shows confirmed_sources >= 15
  </acceptance_criteria>
  <done>
    Socrata sources seeded for the 15+ LA City officials that have a matching committee, ingest run end-to-end, contribution rows populated in transparent_motivations.contributions. Ready for finance_summary aggregation in Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build write-la-city-finance-summary.ts and run it to populate finance_summary for LA City officials</name>
  <files>backend/scripts/write-la-city-finance-summary.ts</files>
  <read_first>
    - backend/scripts/run-fec-finance-summary.ts (entire file — analog for structure: env guards, getXxxPoliticiansFromDb, FinanceSummary type, updateFinanceSummary, main loop with non-aborting try/catch, pool.end on success and on fatal error)
    - backend/src/lib/db.ts (pool import path and how it loads DATABASE_URL via env.ts)
    - backend/migrations/268_finance_summary_column.sql (finance_summary JSONB shape contract)
    - .planning/phases/109-la-county-finance/109-PATTERNS.md (section "backend/scripts/write-la-city-finance-summary.ts" — exact imports, types, aggregation SQL, main loop, pool shutdown patterns)
    - .planning/phases/109-la-county-finance/109-RESEARCH.md (Pattern 3 finance_summary write, Pattern 4 aggregation SQL, Open Question #3 total_spent inclusion, Pitfall 4 shape consistency)
  </read_first>
  <behavior>
    - `npx tsx scripts/write-la-city-finance-summary.ts` selects every essentials.politicians row joined to a confirmed la_socrata politician_sources, aggregates SUM(amount) for positive amounts (raised) and SUM(ABS(amount)) for negative amounts (spent) from transparent_motivations.contributions, writes a JSONB summary to essentials.politicians.finance_summary
    - For officials with at least one positive-amount contribution, finance_summary is written as `{ total_raised: number, total_spent: number, top_donors: [], cycle: 'all', source: 'LA_SOCRATA' }`
    - For officials with zero positive-amount contributions, finance_summary stays NULL and the run log emits `[SKIP] {full_name} — no contributions found; leaving finance_summary = NULL`
    - The script is idempotent — re-running it replaces (not appends) the JSONB value
    - The script never aborts on a single-politician error; the non-aborting try/catch records the error and continues
    - The script uses `import { pool } from '../src/lib/db.js'` — NOT `new Pool(...)`
  </behavior>
  <action>
    Create `backend/scripts/write-la-city-finance-summary.ts` modeled on the structure of run-fec-finance-summary.ts.

    Required file-level concerns:
    - `import 'dotenv/config'` and `import { pool } from '../src/lib/db.js'` (NOT `new Pool(...)` — PATTERNS.md "CRITICAL NOTE" applies)
    - Env guard: if !process.env.DATABASE_URL, console.error and process.exit(1)
    - Type `FinanceSummary` matching PATTERNS.md shape: `{ total_raised: number; total_spent?: number; top_donors: Array<{employer: string; amount: number; count: number}>; cycle: string; source: 'LA_SOCRATA' }`
    - Helper `getLACityPoliticiansWithSocrataSources(): Promise<Array<{id: string; full_name: string}>>` — runs the SELECT from PATTERNS.md "DB query pattern" returning DISTINCT politicians joined through politician_sources WHERE source_system = 'la_socrata' AND research_status = 'confirmed', ORDER BY full_name
    - Helper `buildFinanceSummaryFromSocrata(politicianId: string): Promise<FinanceSummary | null>` — runs the aggregation SQL from PATTERNS.md (SUM(c.amount) WHERE c.amount > 0 for total_raised) AND a second query for total_spent (SELECT COALESCE(ABS(SUM(c.amount)), 0) WHERE c.amount < 0). Returns null when total_raised is 0. cycle = 'all'. top_donors = []. source = 'LA_SOCRATA'.
    - Helper `updateFinanceSummary(politicianId, summary)` — runs `UPDATE essentials.politicians SET finance_summary = $1::jsonb WHERE id = $2` with `[JSON.stringify(summary), politicianId]`. The `::jsonb` cast is mandatory.
    - Main loop iterating over politicians from helper 1, calling helper 2, then helper 3 if non-null. Non-aborting try/catch per politician with `[ERROR]` log line. Counters: processed, succeeded, skipped_no_data, errors. Print a final summary block with totals + list of skipped names.
    - Pool shutdown: `await pool.end(); process.exit(0)` at end of main; in `.catch()` handler `try { await pool.end(); } catch {} process.exit(1)`.

    After creating the file, execute it: `cd backend && npx tsx scripts/write-la-city-finance-summary.ts > /tmp/109-02-finance.log 2>&1`. Capture the run log for the plan SUMMARY.

    Re-run `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` and confirm ASSERTION 2 now reports `officials_with_summary >= 15` and ASSERTION 3 reports `bad_shape = 0`.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) AS officials_with_summary FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_socrata' AND ps.research_status = 'confirmed' AND p.finance_summary IS NOT NULL;"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/write-la-city-finance-summary.ts` exists
    - File contains the literal `import { pool } from '../src/lib/db.js'`
    - File does NOT contain `new Pool(`
    - File contains the literal `source_system = 'la_socrata'`
    - File contains the literal `finance_summary = $1::jsonb`
    - File contains the literal `source: 'LA_SOCRATA'`
    - psql verify query above returns `officials_with_summary >= 15`
    - psql `SELECT COUNT(*) FROM essentials.politicians WHERE finance_summary IS NOT NULL AND finance_summary->>'source' = 'LA_SOCRATA' AND (finance_summary->>'total_raised' IS NULL OR finance_summary->>'cycle' IS NULL);` returns 0
    - Re-running the script a second time produces no errors and the same officials_with_summary count (idempotency)
  </acceptance_criteria>
  <done>
    finance_summary is populated for every LA City official with Socrata contributions; officials with zero contributions (e.g. Jurado, possibly Hutt) and Lattimore (skipped) have finance_summary = NULL with the gap documented in the script run log. LAFI-01 is closed.
  </done>
</task>

</tasks>

<verification>
- Run `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` after Task 3 — ASSERTION 1 confirmed_sources >= 15, ASSERTION 2 officials_with_summary >= 15, ASSERTION 3 bad_shape = 0
- Smoke-test the API for one LA City official: `curl http://localhost:3001/api/essentials/politicians/<bass-uuid>` returns finance_summary with source = 'LA_SOCRATA'
- Run the script twice back-to-back to confirm idempotency (second run produces the same officials_with_summary count)
</verification>

<success_criteria>
- LAFI-01 ASSERTION 1: confirmed_sources >= 15 (psql verify-la-county-109.sql)
- LAFI-01 ASSERTION 2: officials_with_summary >= 15 (psql verify-la-county-109.sql)
- LAFI-01 ASSERTION 3: bad_shape = 0 (psql verify-la-county-109.sql)
- write-la-city-finance-summary.ts is idempotent and committed
- Plan run logs (seed + finance) attached to plan SUMMARY
</success_criteria>

<output>
Create `.planning/phases/109-la-county-finance/109-02-SUMMARY.md` when done — list officials seeded, officials skipped (Lattimore + any zero-contribution council members), assertion counts, and links to the captured run logs.
</output>
