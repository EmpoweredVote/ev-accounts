---
phase: 109-la-county-finance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-la-county-109.sql
autonomous: true
requirements: [LAFI-01, LAFI-02]
tags: [campaign-finance, la-county, verification, sql-assertions]

must_haves:
  truths:
    - "An SQL phase-gate script exists that can be re-run after every wave to detect regressions"
    - "The scaffold contains explicit, labeled assertions for every LAFI-01 and LAFI-02 success criterion"
    - "Running the scaffold against the current DB exits 0 (psql returns success) even before later waves populate data"
  artifacts:
    - path: "backend/scripts/verify-la-county-109.sql"
      provides: "Phase 109 SQL phase-gate verification covering LAFI-01 + LAFI-02"
      contains: "ASSERTION 1: LAFI-01"
  key_links:
    - from: "backend/scripts/verify-la-county-109.sql"
      to: "transparent_motivations.politician_sources"
      via: "psql SELECT ... WHERE source_system = 'la_socrata'"
      pattern: "la_socrata|la_county_netfile"
    - from: "backend/scripts/verify-la-county-109.sql"
      to: "essentials.politicians.finance_summary"
      via: "psql SELECT COUNT(finance_summary)"
      pattern: "finance_summary"
---

<objective>
Create the Phase 109 verification SQL scaffold ahead of any ingestion work. This is the Nyquist Wave 0 deliverable: every later task in this phase has an automated check against this script, and the script itself must be runnable from day one (it tolerates zero rows until later waves populate them).

Purpose: Establish a single source of truth for "is Phase 109 done?" so the per-wave executors and the final phase gate share the same assertions, with no drift.

Output: `backend/scripts/verify-la-county-109.sql` containing labeled assertions for LAFI-01 (LA City Socrata source + finance_summary coverage) and LAFI-02 (Netfile probe outcomes + finance_summary coverage), following the verify-la-county-108.sql pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-la-county-finance/109-RESEARCH.md
@.planning/phases/109-la-county-finance/109-PATTERNS.md
@backend/scripts/verify-la-county-108.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold verify-la-county-109.sql with labeled assertion stubs (LAFI-01 + LAFI-02)</name>
  <files>backend/scripts/verify-la-county-109.sql</files>
  <read_first>
    - backend/scripts/verify-la-county-108.sql (header/echo/assertion style to mirror exactly)
    - .planning/phases/109-la-county-finance/109-PATTERNS.md (section "backend/scripts/verify-la-county-109.sql" with exact header and assertion patterns)
    - .planning/phases/109-la-county-finance/109-RESEARCH.md (Validation Architecture section — Wave 0 gap)
  </read_first>
  <behavior>
    - File runs via `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` and exits 0 regardless of current ingest state (assertions are SELECT counts, not RAISE statements)
    - Output begins with a banner identifying "Phase 109 LA County Finance — Verification Gate"
    - Output ends with a closing banner summarizing expected pass conditions for LAFI-01 and LAFI-02
    - Each assertion block is preceded by `\echo` lines describing the requirement (LAFI-01 or LAFI-02), the assertion intent, and the expected value
  </behavior>
  <action>
    Create the file with the exact header format from verify-la-county-108.sql lines 1-21 (file name, "Run:" line, requirement summary, opening `\echo` banner).

    Include the following labeled assertions (each as a numbered ASSERTION block with `\echo` description + `\echo 'Expected: ...'` + SELECT):

    1. ASSERTION 1 (LAFI-01): COUNT confirmed la_socrata sources — SELECT COUNT(*) FROM transparent_motivations.politician_sources WHERE source_system = 'la_socrata' AND research_status = 'confirmed'. Expected: confirmed_sources >= 15.
    2. ASSERTION 2 (LAFI-01): finance_summary coverage for la_socrata-sourced officials — SELECT COUNT(*) AS officials_with_confirmed_source, COUNT(p.finance_summary) AS officials_with_summary, COUNT(*) - COUNT(p.finance_summary) AS officials_null_summary FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_socrata' AND ps.research_status = 'confirmed'. Expected: officials_with_summary >= 15, officials_null_summary <= 3 (Lattimore, Jurado, and at most one other appointed/new official acceptable).
    3. ASSERTION 3 (LAFI-01): finance_summary shape integrity for la_socrata officials — SELECT COUNT(*) AS bad_shape FROM essentials.politicians p WHERE p.finance_summary IS NOT NULL AND p.finance_summary->>'source' = 'LA_SOCRATA' AND (p.finance_summary->>'total_raised' IS NULL OR p.finance_summary->>'cycle' IS NULL). Expected: bad_shape = 0.
    4. ASSERTION 4 (LAFI-02): COUNT confirmed la_county_netfile sources — SELECT COUNT(*) FROM transparent_motivations.politician_sources WHERE source_system = 'la_county_netfile' AND research_status = 'confirmed'. Expected: netfile_sources >= 1 (at least one city accessible).
    5. ASSERTION 5 (LAFI-02): finance_summary coverage for la_county_netfile-sourced officials — SELECT COUNT(*) AS officials_with_netfile_source, COUNT(p.finance_summary) AS officials_with_summary FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_county_netfile' AND ps.research_status = 'confirmed'. Expected: officials_with_summary >= 1 when officials_with_netfile_source > 0; NULL summary acceptable only when ingest returned zero contributions.
    6. ASSERTION 6 (LAFI-02): finance_summary shape integrity for la_county_netfile officials — same shape check as ASSERTION 3 but filtered to source='LA_COUNTY_NETFILE'. Expected: bad_shape = 0.
    7. ASSERTION 7 (LAFI-01 + LAFI-02 negative): No finance_summary contains placeholder/fabricated data — SELECT COUNT(*) AS placeholder_rows FROM essentials.politicians WHERE finance_summary IS NOT NULL AND (finance_summary->>'source' NOT IN ('FEC','LA_SOCRATA','LA_COUNTY_NETFILE') OR (finance_summary->>'total_raised')::numeric < 0). Expected: placeholder_rows = 0.
    8. ASSERTION 8 (LAFI-02 coverage report): Per-city Netfile coverage report — SELECT g.name AS city, COUNT(DISTINCT p.id) AS politicians_total, COUNT(DISTINCT ps.id) FILTER (WHERE ps.source_system = 'la_county_netfile' AND ps.research_status = 'confirmed') AS politicians_with_netfile_source, COUNT(DISTINCT p.id) FILTER (WHERE p.finance_summary IS NOT NULL) AS politicians_with_summary FROM essentials.governments g JOIN essentials.chambers ch ON ch.government_id = g.id JOIN essentials.offices o ON o.chamber_id = ch.id JOIN essentials.politicians p ON p.office_id = o.id LEFT JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE g.state = 'CA' AND g.name IN ( the 27 LA County city names from RESEARCH.md ) GROUP BY g.name ORDER BY g.name. Expected: zero-row failures are non-blocking; this is a coverage report consumed by reviewers — every row should show either politicians_with_netfile_source > 0 OR a documented gap in the wave 2 SUMMARY.

    Use the 27 city names: 'City of Los Angeles','City of Long Beach','City of Glendale','City of Burbank','City of Downey','City of El Monte','City of Inglewood','City of Lancaster','City of Norwalk','City of Palmdale','City of Pasadena','City of Pomona','City of Santa Clarita','City of Torrance','City of West Covina','City of Beverly Hills','City of Santa Monica','City of South Gate','City of Compton','City of Carson','City of Hawthorne','City of Whittier','City of Alhambra','City of Gardena','City of Culver City','City of West Hollywood','City of El Segundo'.

    Close the file with the same closing banner pattern as verify-la-county-108.sql lines 237-242 — restate expected pass conditions for LAFI-01 and LAFI-02.

    DO NOT use `RAISE EXCEPTION` or `DO $$ ... $$` blocks — psql must exit 0 on tolerant zero-row states so Wave 2 and Wave 3 can use this script as their automated verify command.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/verify-la-county-109.sql` exists
    - `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` exits with status 0
    - File contents include the literal string `ASSERTION 1: LAFI-01`
    - File contents include the literal string `ASSERTION 4: LAFI-02`
    - File contents include the literal string `ASSERTION 7` referencing placeholder/fabricated data check
    - File contains all 27 city government name literals (e.g. `'City of West Hollywood'`, `'City of El Segundo'`)
    - File contains the literal strings `source_system = 'la_socrata'` and `source_system = 'la_county_netfile'`
    - File contains the literal string `finance_summary->>'source'`
    - File does NOT contain `RAISE EXCEPTION` or `DO \$\$`
  </acceptance_criteria>
  <done>
    Verification scaffold exists, runs cleanly against an empty Phase 109 state, contains labeled assertions for both LAFI-01 and LAFI-02, and is committed so Wave 2 plans can use it as their automated verify command.
  </done>
</task>

</tasks>

<verification>
- Re-run `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` after each later task to confirm no regression and to track ingest progress.
- Grep file for `LAFI-01` and `LAFI-02` — both must appear in assertion labels.
- Confirm closing banner restates expected pass conditions.
</verification>

<success_criteria>
- `backend/scripts/verify-la-county-109.sql` exists and is psql-executable
- All 8 assertions are present and labeled by requirement ID
- File is committed to git with message `chore(109): scaffold phase 109 verification SQL`
</success_criteria>

<output>
Create `.planning/phases/109-la-county-finance/109-01-SUMMARY.md` when done — record the assertion list, expected thresholds, and a sample run output (truncated) for future reference.
</output>
