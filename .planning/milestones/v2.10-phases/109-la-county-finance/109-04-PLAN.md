---
phase: 109-la-county-finance
plan: 04
type: execute
wave: 3
depends_on: [109-02, 109-03]
files_modified:
  - backend/scripts/verify-la-county-109.sql
autonomous: false
requirements: [LAFI-01, LAFI-02]
tags: [phase-gate, verification, api-smoke-test, la-county]

must_haves:
  truths:
    - "Running verify-la-county-109.sql against the live DB exits 0 with all 8 assertions reporting their expected values"
    - "The API endpoint GET /api/essentials/politicians/:id returns a valid finance_summary JSONB for at least one LA City official (LA_SOCRATA) and one LA County city official (LA_COUNTY_NETFILE) — no 500 errors"
    - "Every populated finance_summary is traceable to a real fetched source (no placeholder/fabricated data) — assertion 7 confirms 0 placeholder rows"
    - "Per-city coverage report shows every Phase 108 city as either {has Netfile source + summary} or {documented gap with reason} — no unexplained gaps"
  artifacts:
    - path: "backend/scripts/verify-la-county-109.sql"
      provides: "Final phase gate — all assertions populated and runnable"
      contains: "ASSERTION 8"
  key_links:
    - from: "backend/scripts/verify-la-county-109.sql"
      to: "essentials.politicians.finance_summary"
      via: "psql full assertion suite"
      pattern: "officials_with_summary"
    - from: "human verification checkpoint"
      to: "live API"
      via: "curl GET /api/essentials/politicians/:id"
      pattern: "finance_summary"
---

<objective>
Run the Phase 109 phase gate. Re-execute `verify-la-county-109.sql` against the now-populated DB, confirm every assertion reports its expected value, smoke-test the API for one LA_SOCRATA and one LA_COUNTY_NETFILE official, and present results to the human operator for sign-off before this phase ships.

Purpose: LAFI-01 and LAFI-02 are both deliberately tolerant requirements (NULL with documented gap is acceptable). The phase gate must distinguish "all data ingested cleanly with no unexplained gaps" from "ingestion incomplete and silently failed". The verification script in 109-01 was scaffolded to be tolerant of zero-row states; this plan confirms the post-ingest state matches expectations and that the API surfaces finance_summary correctly.

Output:
- verify-la-county-109.sql full run log captured in plan SUMMARY
- API smoke-test results (two curl outputs) captured in plan SUMMARY
- Human sign-off recorded in plan SUMMARY
- Plan SUMMARY explicitly lists every documented gap (Lattimore + zero-contribution councilmembers + no-data Netfile cities) with rationale
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/109-la-county-finance/109-RESEARCH.md
@.planning/phases/109-la-county-finance/109-PATTERNS.md
@.planning/phases/109-la-county-finance/109-01-SUMMARY.md
@.planning/phases/109-la-county-finance/109-02-SUMMARY.md
@.planning/phases/109-la-county-finance/109-03-SUMMARY.md
@backend/scripts/verify-la-county-109.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full phase gate — verify-la-county-109.sql + per-source-system spot checks</name>
  <files>backend/scripts/verify-la-county-109.sql</files>
  <read_first>
    - backend/scripts/verify-la-county-109.sql (the scaffolded file from 109-01 — every assertion is already labeled by requirement ID)
    - .planning/phases/109-la-county-finance/109-02-SUMMARY.md (LA City Socrata coverage outcome — how many officials seeded, how many with finance_summary, who was skipped and why)
    - .planning/phases/109-la-county-finance/109-03-SUMMARY.md (LA County city Netfile coverage outcome — per-city probe outcomes and documented gaps)
  </read_first>
  <behavior>
    - Running `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql > /tmp/109-04-gate.log 2>&1` exits 0
    - The gate log shows ASSERTION 1 confirmed_sources >= 15, ASSERTION 2 officials_with_summary >= 15 + officials_null_summary <= 3, ASSERTION 3 bad_shape = 0, ASSERTION 4 netfile_sources >= 1, ASSERTION 5 officials_with_summary aligned with seed outcomes from 109-03 SUMMARY, ASSERTION 6 bad_shape = 0, ASSERTION 7 placeholder_rows = 0, ASSERTION 8 coverage report with no unexplained gaps
    - Each NULL summary row maps to either Lattimore (skipped, appointed), a zero-contribution councilmember (Jurado et al.), or a city documented as no-data in 109-03 SUMMARY
    - If any assertion fails or any NULL row is unaccounted for, this task does NOT proceed to Task 2 — the gap is documented and the phase is paused for replan
  </behavior>
  <action>
    1. Run `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql > /tmp/109-04-gate.log 2>&1`. Confirm exit code 0.
    2. Read the log. For each assertion confirm the reported value matches the expected threshold documented in the assertion `\echo 'Expected: ...'` line.
    3. Pull the list of NULL finance_summary rows for la_socrata-sourced officials: `psql "$DATABASE_URL" -c "SELECT p.full_name FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_socrata' AND ps.research_status = 'confirmed' AND p.finance_summary IS NULL ORDER BY p.full_name;"`. Cross-reference each row against 109-02 SUMMARY skipped/zero-contribution list — every NULL must be accounted for.
    4. Pull the same list for la_county_netfile-sourced officials: same query with source_system = 'la_county_netfile'. Cross-reference against 109-03 SUMMARY per-city gap table.
    5. Run two API smoke tests against the local dev server (start with `cd backend && npm run dev` if not running):
       - `curl -s http://localhost:3001/api/essentials/politicians/<karen-bass-uuid> | jq '.finance_summary'` — must return a non-null JSON object with `source` = `"LA_SOCRATA"`, `total_raised` >= 0, `cycle` present
       - `curl -s http://localhost:3001/api/essentials/politicians/<some-netfile-uuid> | jq '.finance_summary'` — must return a non-null JSON object with `source` = `"LA_COUNTY_NETFILE"` (skip this curl if 109-03 SUMMARY shows zero confirmed Netfile sources — record the skip with rationale in the SUMMARY)
    6. Resolve UUIDs ahead of time: `psql "$DATABASE_URL" -c "SELECT id, full_name FROM essentials.politicians WHERE full_name = 'Karen Ruth Bass';"` for the LA_SOCRATA UUID; `psql "$DATABASE_URL" -c "SELECT p.id, p.full_name FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_county_netfile' AND ps.research_status = 'confirmed' AND p.finance_summary IS NOT NULL LIMIT 1;"` for the LA_COUNTY_NETFILE UUID.
    7. Capture the gate log, both curl outputs, and the NULL-row cross-reference into the plan SUMMARY draft.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql</automated>
  </verify>
  <acceptance_criteria>
    - `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` exits 0
    - psql `SELECT COUNT(*) FROM transparent_motivations.politician_sources WHERE source_system = 'la_socrata' AND research_status = 'confirmed';` returns >= 15
    - psql `SELECT COUNT(p.finance_summary) FROM essentials.politicians p JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id WHERE ps.source_system = 'la_socrata' AND ps.research_status = 'confirmed';` returns >= 15
    - psql `SELECT COUNT(*) FROM essentials.politicians WHERE finance_summary IS NOT NULL AND finance_summary->>'source' NOT IN ('FEC','LA_SOCRATA','LA_COUNTY_NETFILE');` returns 0
    - psql `SELECT COUNT(*) FROM transparent_motivations.politician_sources WHERE source_system = 'la_county_netfile' AND research_status = 'confirmed';` returns >= 1
    - LA_SOCRATA curl returns HTTP 200 with finance_summary.source = "LA_SOCRATA"
    - LA_COUNTY_NETFILE curl returns HTTP 200 with finance_summary.source = "LA_COUNTY_NETFILE" (or skip is documented when no Netfile data ingested)
    - Every NULL finance_summary row for la_socrata or la_county_netfile sourced officials is accounted for in 109-02 or 109-03 SUMMARY
  </acceptance_criteria>
  <done>
    Full phase gate green; API smoke tests pass; every NULL finance_summary is justified by a documented gap. Plan SUMMARY draft contains gate log + curl outputs + NULL-row cross-reference.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on Phase 109 completion</name>
  <files>.planning/phases/109-la-county-finance/109-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/109-la-county-finance/109-04-SUMMARY.md (draft from Task 1 — gate log + curl outputs + gap accounting)
    - .planning/phases/109-la-county-finance/109-02-SUMMARY.md
    - .planning/phases/109-la-county-finance/109-03-SUMMARY.md
    - .planning/ROADMAP.md Phase 109 section (success criteria 1-4)
  </read_first>
  <what-built>
    - `verify-la-county-109.sql` scaffold (plan 109-01)
    - Extended `seed-la-city-confirmed.ts` covering Patrice Lattimore (plan 109-02 Task 1)
    - LA City Socrata ingest run + `write-la-city-finance-summary.ts` populating finance_summary for 15+ LA City officials (plan 109-02 Tasks 2-3)
    - New `seed-la-county-city-netfile.ts` probing 26 LA County cities under LACO + per-city agency codes, seeding confirmed la_county_netfile sources where accessible, triggering ingest (plan 109-03 Task 1)
    - New `write-la-county-city-finance-summary.ts` populating finance_summary for Netfile-sourced officials (plan 109-03 Task 2)
    - Phase gate run + API smoke tests + gap accounting (this plan Task 1)
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/109-la-county-finance/109-04-SUMMARY.md` — confirm gate log shows every assertion at its expected value and every NULL finance_summary is accounted for.
    2. Spot-check one LA City official in the live app at https://login.empowered.vote/profile or wherever profile pages render finance_summary — confirm total_raised displays correctly without a 500 error.
    3. Open `psql "$DATABASE_URL"` and run `SELECT full_name, finance_summary->>'source', finance_summary->>'total_raised' FROM essentials.politicians WHERE finance_summary->>'source' IN ('LA_SOCRATA', 'LA_COUNTY_NETFILE') ORDER BY full_name LIMIT 20;` — sanity-check that numbers look plausible (six-figure raised amounts for most LA City Council members, possibly seven figures for Mayor Bass).
    4. Confirm the Lattimore gap and any Netfile no-data cities are listed by name with rationale in the SUMMARY.
    5. Confirm `.planning/STATE.md` will be updated to mark Phase 109 complete (next plan-phase will do this).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 109 complete, or describe any gaps that need follow-up plans.</resume-signal>
  <verify>
    <human-check>Human operator confirms phase gate green, API smoke tests pass, every NULL finance_summary is accounted for, and Phase 109 is ready to ship.</human-check>
  </verify>
  <acceptance_criteria>
    - Plan SUMMARY exists at `.planning/phases/109-la-county-finance/109-04-SUMMARY.md`
    - SUMMARY contains the verbatim `verify-la-county-109.sql` gate log
    - SUMMARY contains both API curl outputs (or documented skip rationale for Netfile curl)
    - SUMMARY contains a NULL-row accounting table (name | source_system | reason)
    - Human operator has typed "approved" (or equivalent) in response to the resume-signal prompt
  </acceptance_criteria>
  <done>
    Human sign-off recorded. Phase 109 is complete and ready for `/gsd-verify-work` / next phase planning. LAFI-01 and LAFI-02 both closed.
  </done>
</task>

</tasks>

<verification>
- Final gate: `psql "$DATABASE_URL" -f backend/scripts/verify-la-county-109.sql` exits 0 with every assertion at its expected value
- API smoke tests confirm finance_summary is serialized correctly for both LA_SOCRATA and LA_COUNTY_NETFILE sources
- Human sign-off recorded in plan SUMMARY
</verification>

<success_criteria>
- All 8 assertions in verify-la-county-109.sql pass their expected thresholds
- API returns valid finance_summary for both source types
- Every NULL finance_summary on a Phase 108 LA County city official has a documented justification
- Plan SUMMARY captures gate log + curl outputs + NULL-row accounting + human sign-off
- Phase 109 ready for archive into milestone v2.10
</success_criteria>

<output>
Create `.planning/phases/109-la-county-finance/109-04-SUMMARY.md` when done — verbatim gate log, both curl outputs, NULL-row accounting table (name | source_system | reason), human sign-off note, links to upstream plan SUMMARYs (109-01, 109-02, 109-03).
</output>
