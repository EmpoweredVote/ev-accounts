---
phase: 113-va-federal-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-va-federal-113.sql
autonomous: true
requirements: [VAST-04, VAST-05, VAFI-01, VAFI-02]
tags: [verification, sql-assertions, va-federal, phase-gate]

must_haves:
  truths:
    - "A runnable SQL gate script exists that asserts stance coverage for all 11 VA federal House reps"
    - "The script asserts every politician_answers row in the ext_id range is paired with a politician_context row containing at least one real source URL"
    - "The script asserts FEC finance_summary coverage for the 11 VA House reps after Wave 2 populates it"
    - "The script asserts the VPAP assessment outcome is documented (either finance_summary rows or noted absence)"
    - "Running the script before Wave 2 executes exits psql 0 — all assertions are SELECT counts, not RAISE blocks"
  artifacts:
    - path: "backend/scripts/verify-va-federal-113.sql"
      provides: "Phase 113 SQL phase-gate covering VAST-04, VAST-05, VAFI-01, VAFI-02"
      contains: "ASSERTION 1: VAST-04"
  key_links:
    - from: "backend/scripts/verify-va-federal-113.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "psql SELECT scoped to external_id BETWEEN -5102011 AND -5102001"
      pattern: "BETWEEN -5102011 AND -5102001"
    - from: "backend/scripts/verify-va-federal-113.sql"
      to: "essentials.politicians.finance_summary"
      via: "psql SELECT COUNT(finance_summary)"
      pattern: "finance_summary"
---

<objective>
Create the Phase 113 verification SQL scaffold before any finance ingestion work. This is the Wave 0 / Nyquist deliverable: a single psql-runnable script that asserts every Phase 113 success criterion, tolerates zero-row states, and exits 0 so later waves can use it as their automated verify command.

Purpose: Establish a single source of truth for "is Phase 113 done?" — all requirement assertions (VAST-04, VAST-05, VAFI-01, VAFI-02) in one file, so Wave 2 executors and the final phase gate share the same checks.

Output: `backend/scripts/verify-va-federal-113.sql` with labeled assertions following the verify-la-county-109.sql style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-va-federal-stances/113-PREFLIGHT.md
@backend/scripts/verify-la-county-109.sql
</context>

<interfaces>
<!-- Key constants the executor needs. Sourced from PREFLIGHT.md and migration 341. -->

VA Federal House Reps — external_id range: BETWEEN -5102011 AND -5102001

| UUID | full_name | ext_id |
|------|-----------|--------|
| 8f4379fc-ae32-4f6a-8773-ac1d723106a5 | Rob Wittman        | -5102001 |
| 512f27a4-e24f-4f62-a288-18b1e37db463 | Jen Kiggans        | -5102002 |
| cc499c9a-d165-4cd7-831d-51611339ac29 | Bobby Scott        | -5102003 |
| 3e7c0e88-5e35-4d71-8022-98731af6461b | Jennifer McClellan | -5102004 |
| e4deeac3-b172-473d-9696-a07d874f4795 | Ben Cline          | -5102005 |
| 12eef223-444e-4bed-8081-f1fd26c43e42 | Morgan Griffith    | -5102006 |
| 9a9d6b64-60b3-40c9-b213-4088d9a51e68 | Eugene Vindman     | -5102007 |
| 0c1eef2f-19be-440f-b3d9-bd99d44ec056 | Don Beyer          | -5102008 |
| e603fa67-7992-409e-a1e2-1385c32dc217 | John McGuire       | -5102009 |
| 98b05c70-2a30-48ea-81f3-b3216ffb0ca0 | Suhas Subramanyam  | -5102010 |
| 32ea954f-8bfa-4d28-9f9e-12d1929cb853 | James Walkinshaw   | -5102011 |

Finance_summary column: essentials.politicians.finance_summary (JSONB, established migration 268)
Source system for FEC data: finance_summary->>'source' = 'FEC'
Honest-skip members with fewer stances (per migration 341 header):
  Vindman (VA-07): 1 topic, McGuire (VA-09): 2 topics,
  Subramanyam (VA-10): 1 topic, Walkinshaw (VA-11): 7 topics
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold verify-va-federal-113.sql with labeled assertions (VAST-04, VAST-05, VAFI-01, VAFI-02)</name>
  <files>backend/scripts/verify-va-federal-113.sql</files>
  <read_first>
    - backend/scripts/verify-la-county-109.sql (header style, \echo banner, assertion block format, closing banner — mirror exactly)
    - .planning/phases/113-va-federal-stances/113-PREFLIGHT.md (VAST-04/05 success criteria, external_id range, honest-skip notes)
    - supabase/migrations/20260610000011_341_va_federal_reps_stances.sql (header comments — honest-skip members documented there)
  </read_first>
  <action>
    Create backend/scripts/verify-va-federal-113.sql following the exact header format from verify-la-county-109.sql lines 1-14 (file name, phase description, Run: line, requirement summary, script behavior note).

    Include the following 7 labeled assertion blocks. Each block must have: two \echo lines (assertion label + expected value), then a SELECT statement. Use DO $$ only if needed for conditional logic — for all assertions here, plain SELECT COUNT is sufficient and DO $$ is forbidden (psql must exit 0 on empty state).

    Scope all stance-related assertions to external_id BETWEEN -5102011 AND -5102001 via a subquery or JOIN to essentials.politicians.

    ASSERTION 1 (VAST-04): Total stance rows for the 11 VA reps.
    Label: ASSERTION 1: VAST-04 — politician_answers count for 11 VA federal House reps.
    Expected: answer_rows = 105 (matches migration 341 header).
    SQL: SELECT COUNT(*) AS answer_rows FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001.

    ASSERTION 2 (VAST-05): Every politician_answers row has a matching politician_context row.
    Label: ASSERTION 2: VAST-05 — every answer has a matching politician_context row.
    Expected: answers_missing_context = 0.
    SQL: SELECT COUNT(*) AS answers_missing_context FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND NOT EXISTS ( SELECT 1 FROM inform.politician_context pc WHERE pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id ).

    ASSERTION 3 (VAST-05): Every politician_context row has at least one non-empty source URL.
    Label: ASSERTION 3: VAST-05 — every politician_context has at least one source URL (no empty-array rows).
    Expected: context_rows_without_sources = 0.
    SQL: SELECT COUNT(*) AS context_rows_without_sources FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND (pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL OR array_length(pc.sources, 1) = 0).

    ASSERTION 4 (VAST-04 negative): No unsourced stance rows (source guard — catches any row with NULL reasoning).
    Label: ASSERTION 4: VAST-04 negative — no politician_context rows with NULL reasoning.
    Expected: null_reasoning_rows = 0.
    SQL: SELECT COUNT(*) AS null_reasoning_rows FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND pc.reasoning IS NULL.

    ASSERTION 5 (VAFI-01): FEC finance_summary populated for VA federal House reps.
    Label: ASSERTION 5: VAFI-01 — finance_summary coverage for 11 VA federal House reps.
    Expected before Wave 2: reps_with_summary = 0 (tolerant). After Wave 2: reps_with_summary >= 9 (Vindman/McGuire/Subramanyam/Walkinshaw may have no FEC ID on first run).
    SQL: SELECT COUNT(*) AS reps_total, COUNT(p.finance_summary) AS reps_with_summary, COUNT(*) - COUNT(p.finance_summary) AS reps_null_summary FROM essentials.politicians p WHERE p.external_id BETWEEN -5102011 AND -5102001.

    ASSERTION 6 (VAFI-01): finance_summary shape integrity for reps that have it.
    Label: ASSERTION 6: VAFI-01 — finance_summary shape integrity (source=FEC, total_raised present, cycle present).
    Expected: bad_shape = 0.
    SQL: SELECT COUNT(*) AS bad_shape FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001 AND finance_summary IS NOT NULL AND ( finance_summary->>'source' IS DISTINCT FROM 'FEC' OR finance_summary->>'total_raised' IS NULL OR finance_summary->>'cycle' IS NULL ).

    ASSERTION 7 (VAFI-01 + VAFI-02 coverage report): Per-rep coverage report showing full_name, external_id, whether finance_summary is populated, and source.
    Label: ASSERTION 7: VAFI-01 + VAFI-02 — per-rep finance coverage report.
    Expected: coverage report only; zero-row summary populated column is non-blocking (document gaps in SUMMARY).
    SQL: SELECT p.full_name, p.external_id, CASE WHEN p.finance_summary IS NOT NULL THEN 'populated' ELSE 'NULL' END AS summary_status, p.finance_summary->>'source' AS source FROM essentials.politicians p WHERE p.external_id BETWEEN -5102011 AND -5102001 ORDER BY p.external_id DESC.

    Close the file with a closing \echo banner restating pass conditions for each requirement (same style as verify-la-county-109.sql lines 197-204):
    VAST-04: answer_rows = 105, answers_missing_context = 0
    VAST-05: context_rows_without_sources = 0, null_reasoning_rows = 0
    VAFI-01: reps_with_summary >= 9, bad_shape = 0
    VAFI-02: per-rep coverage report reviewed; gaps documented in SUMMARY

    DO NOT use RAISE EXCEPTION or DO $$ ... $$ blocks — psql must exit 0 on any state.
    DO NOT use backtick-fenced code blocks inside the SQL file.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql</automated>
  </verify>
  <acceptance_criteria>
    - File backend/scripts/verify-va-federal-113.sql exists
    - psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql exits with status 0
    - File contains the literal string ASSERTION 1: VAST-04
    - File contains the literal string ASSERTION 2: VAST-05
    - File contains the literal string ASSERTION 5: VAFI-01
    - File contains the literal string BETWEEN -5102011 AND -5102001 (at least once per stance assertion)
    - File contains the literal string finance_summary
    - File does NOT contain RAISE EXCEPTION
    - File does NOT contain DO $$
    - Running: psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5102011 AND -5102001" returns 105
  </acceptance_criteria>
  <done>
    Verification scaffold exists at backend/scripts/verify-va-federal-113.sql, runs cleanly against current DB state (VAST-04 + VAST-05 assertions pass with 105 rows and 0 missing-context), VAFI assertions show 0 finance_summary rows (Wave 2 will close those), and the script is committed so Wave 2 can use it as its automated verify command.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| psql → PostgreSQL | Read-only SELECT assertions; no writes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | verify SQL file | accept | Read-only SELECTs; no sensitive data returned beyond counts and politician names already public |
| T-113-SC | Tampering | no npm/pip installs in this plan | accept | No package installs; SQL file is plain text committed to git |
</threat_model>

<verification>
- psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql exits 0
- ASSERTION 1 (VAST-04) output: answer_rows = 105
- ASSERTION 2 (VAST-05) output: answers_missing_context = 0
- ASSERTION 3 (VAST-05) output: context_rows_without_sources = 0
- ASSERTION 5 (VAFI-01) output: reps_with_summary = 0 before Wave 2 (acceptable — tolerant)
- grep verify-va-federal-113.sql for VAST-04, VAST-05, VAFI-01, VAFI-02 — all four must appear
</verification>

<success_criteria>
- backend/scripts/verify-va-federal-113.sql exists and is psql-executable
- All 7 assertions are present and labeled by requirement ID
- VAST-04 + VAST-05 assertions pass against already-committed migration 341 data (105 rows, 0 missing context)
- VAFI-01 + VAFI-02 assertions show 0 (tolerant — Wave 2 will populate)
- File is committed to git
</success_criteria>

<output>
Create .planning/phases/113-va-federal-stances/113-01-SUMMARY.md when done — record the assertion list, expected thresholds, and the actual psql output showing VAST-04/VAST-05 already passing and VAFI-01/VAFI-02 at zero (pre-Wave 2 baseline).
</output>
