---
phase: 27-cache-only-candidates-warmer-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - EV-Backend/internal/essentials/setup.go
  - EV-Backend/internal/essentials/models.go
  - EV-Backend/internal/essentials/handlers.go
autonomous: true
requirements: [BR-03, BR-04, CAND-01]

must_haves:
  truths:
    - "Backend starts without initializing any BallotReady provider connection"
    - "ensureCandidacyData function no longer exists — profile pages read DB only"
    - "FederalCache, StateCache, ZipCache GORM models are removed"
    - "DROP TABLE IF EXISTS runs for federal_cache, state_caches, zip_caches on startup"
    - "The ballotready blank import is removed from setup.go"
  artifacts:
    - path: "EV-Backend/internal/essentials/setup.go"
      provides: "No BallotReady blank import, no Provider variable, no provider initialization, cache table DROP statements, no cache models in AutoMigrate"
    - path: "EV-Backend/internal/essentials/models.go"
      provides: "No FederalCache, StateCache, ZipCache struct definitions"
    - path: "EV-Backend/internal/essentials/handlers.go"
      provides: "No ensureCandidacyData function, no ExternalGlobalID in GetPoliticianByID"
  key_links:
    - from: "EV-Backend/internal/essentials/setup.go"
      to: "database startup"
      via: "Init() function"
      pattern: "DROP TABLE IF EXISTS essentials.federal_cache"
    - from: "EV-Backend/internal/essentials/handlers.go (GetPoliticianByID)"
      to: "database read only"
      via: "no ensureCandidacyData call"
---

<objective>
Remove the BallotReady provider from startup, delete the lazy-fetch candidacy goroutine, and drop the cache tracking tables — completing the full BallotReady API disconnection.

Purpose: After Plan 01 eliminated warmers and the live candidate fetch, this plan removes the remaining BallotReady touchpoints: the provider initialization at startup, the lazy-fetch goroutine triggered on profile views, and the cache tracking tables that are now unused.

Output: setup.go with no BallotReady provider, handlers.go with no ensureCandidacyData, models.go with no cache table structs, and DROP TABLE statements for the three cache tables.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-cache-only-candidates-warmer-cleanup/27-RESEARCH.md
@.planning/phases/27-cache-only-candidates-warmer-cleanup/27-01-SUMMARY.md
@EV-Backend/internal/essentials/setup.go
@EV-Backend/internal/essentials/models.go
@EV-Backend/internal/essentials/handlers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ensureCandidacyData lazy-fetch and clean up GetPoliticianByID</name>
  <files>
    EV-Backend/internal/essentials/handlers.go
  </files>
  <action>
    **Delete ensureCandidacyData function:**
    Remove the entire `ensureCandidacyData` function (~lines 1239-1290 in the original file). This function calls `brProvider.Client().FetchCandidacyData()` via a background goroutine when a politician profile is viewed. Per CONTEXT.md locked decision: "No lazy-fetch on profile view — profiles read from DB only, no background goroutine."

    **Clean up GetPoliticianByID:**
    In the `GetPoliticianByID` handler (~line 3122 in original), remove the call to `ensureCandidacyData(r.Context(), parsedID, r0.ExternalGlobalID, r0.ExternalID)`.

    Also remove `ExternalGlobalID` from the `row` struct in `GetPoliticianByID` (~line 2972) and from the SQL query (~line 3020) since it was only used as an argument to `ensureCandidacyData`. The `external_id` field should remain — it is used elsewhere.

    **Verify no remaining direct ballotready references:**
    After Plan 01 deleted GetCandidatesByZip's BallotReady call, raceChamberName, levelToDistrictType, and warmers, and this task deletes ensureCandidacyData — check if the `ballotready` import in handlers.go is still needed. If no remaining code in handlers.go references the `ballotready` package, remove the import line (e.g., `"github.com/EmpoweredVote/EV-Backend/internal/essentials/ballotready"`).

    **Leave these three profile sub-endpoints unchanged (they already read DB only):**
    - `GetPoliticianEndorsements` — reads from DB, returns empty array if no data
    - `GetPoliticianStances` — reads from DB, returns empty array if no data
    - `GetPoliticianElections` — reads from DB, returns empty array if no data
  </action>
  <verify>
    Run `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build ./...` — must compile without errors.
    Grep for `ensureCandidacyData` in handlers.go — should return zero matches.
    Grep for `ExternalGlobalID` in handlers.go — should return zero matches (unless used elsewhere; if so, only the GetPoliticianByID row struct and SQL column references should be gone).
  </verify>
  <done>
    ensureCandidacyData function deleted. GetPoliticianByID no longer triggers any background goroutine. Profile pages read from DB only. ExternalGlobalID removed from the profile query. ballotready import removed from handlers.go if no longer referenced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deregister BallotReady provider and drop cache tracking tables</name>
  <files>
    EV-Backend/internal/essentials/setup.go
    EV-Backend/internal/essentials/models.go
  </files>
  <action>
    **setup.go — remove BallotReady blank import:**
    Remove the line `_ "github.com/EmpoweredVote/EV-Backend/internal/essentials/ballotready"` from the import block. This prevents the ballotready package's `init()` function from registering with the provider registry at startup.

    **setup.go — remove Provider variable and initialization:**
    - Remove `var Provider provider.OfficialProvider` declaration
    - Remove the entire provider initialization block in `Init()`:
      ```
      cfg := provider.LoadFromEnv()
      var err error
      Provider, err = provider.NewProvider(cfg)
      if err != nil { ... }
      ```
    - Keep `var GeoClient` and its initialization block — the geocoding client is still used by SearchPoliticians for address search.

    **setup.go — remove cache models from AutoMigrate:**
    Remove `&FederalCache{}`, `&StateCache{}`, `&ZipCache{}` from the `db.DB.AutoMigrate(...)` call in `Init()`.

    **setup.go — add DROP TABLE statements:**
    After the schema creation (`CREATE SCHEMA IF NOT EXISTS essentials`) and before AutoMigrate, add:
    ```go
    db.DB.Exec("DROP TABLE IF EXISTS essentials.federal_cache")
    db.DB.Exec("DROP TABLE IF EXISTS essentials.state_caches")
    db.DB.Exec("DROP TABLE IF EXISTS essentials.zip_caches")
    ```
    This ensures the tables are dropped on the next server start. The statements are idempotent (IF EXISTS) so they are safe to run repeatedly.

    **models.go — remove cache table GORM models:**
    Delete the following struct definitions and their `TableName()` methods:
    - `FederalCache` struct and `func (FederalCache) TableName() string`
    - `StateCache` struct and `func (StateCache) TableName() string`
    - `ZipCache` struct and `func (ZipCache) TableName() string`

    **Check cicero blank import:**
    The research notes that the `cicero` blank import may also be removable after warmZip is deleted (Plan 01). Check if any code still references the cicero package after Plan 01's warmer deletions. If `upsertOfficial` and `fetchCiceroOfficialsByTypes` are now unreferenced, delete those functions and remove the cicero blank import. If they are still referenced by other code paths, leave them — Phase 29 will audit.

    **Check provider package usage:**
    After removing `Provider`, check if the `provider` package import in setup.go is still needed (e.g., for `provider.LocalDistrictTypes` or similar constants used elsewhere in the essentials package). If still referenced, keep the import. If not, remove it.
  </action>
  <verify>
    Run `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build ./...` — must compile without errors.
    Grep for `ballotready` in setup.go — should return zero matches (blank import gone).
    Grep for `var Provider` in setup.go — should return zero matches.
    Grep for `LoadFromEnv\|NewProvider` in setup.go — should return zero matches.
    Grep for `FederalCache\|StateCache\|ZipCache` in models.go — should return zero matches.
    Grep for `FederalCache\|StateCache\|ZipCache` in setup.go AutoMigrate — should return zero matches.
    Grep for `DROP TABLE IF EXISTS essentials.federal_cache` in setup.go — should return one match.
  </verify>
  <done>
    BallotReady blank import removed from setup.go — package init() no longer runs at startup. Provider variable and initialization block deleted. Cache tracking table models removed from models.go and AutoMigrate. DROP TABLE statements added for federal_cache, state_caches, zip_caches. Backend compiles cleanly and starts without any BallotReady provider.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build ./...` compiles without errors
2. `grep -rn "ballotready" EV-Backend/internal/essentials/setup.go` returns zero matches
3. `grep -rn "ensureCandidacyData" EV-Backend/internal/essentials/handlers.go` returns zero matches
4. `grep -n "var Provider" EV-Backend/internal/essentials/setup.go` returns zero matches
5. `grep -n "FederalCache\|StateCache\|ZipCache" EV-Backend/internal/essentials/models.go` returns zero matches
6. `grep -n "DROP TABLE IF EXISTS" EV-Backend/internal/essentials/setup.go` returns matches for all three cache tables
7. `GetPoliticianEndorsements`, `GetPoliticianStances`, `GetPoliticianElections` still present in handlers.go (NOT deleted — they already read DB only)
</verification>

<success_criteria>
- Backend starts without initializing BallotReady provider
- No lazy-fetch goroutine fires on politician profile view
- Cache tracking tables are dropped on startup
- Cache table GORM models removed from codebase
- Provider variable and initialization removed from setup.go
- All three profile sub-endpoints (endorsements, stances, elections) continue to work from DB
- Backend compiles and starts cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/27-cache-only-candidates-warmer-cleanup/27-02-SUMMARY.md`
</output>
