---
phase: 130-il-house-rep-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-18-il-house-batch-a.csv
autonomous: false
requirements: [USHS-04]

must_haves:
  truths:
    - "Each of the 9 IL House reps in batch A (external_id -17001..-17009) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation — every value matches the exact stance text and traces to a fetched URL"
    - "Live topics fetched fresh from inform.compass_topics; city + judicial-* topics skipped (25 federal topics in scope)"
    - "Stance counts for all politicians OTHER than the 9 batch-A reps are unchanged"
  artifacts:
    - path: "backend/data/stance-research/2026-06-18-il-house-batch-a.csv"
      provides: "Reviewable stance research CSV for IL House reps batch A (9 reps)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for the first 9 Illinois US House reps
(external_id -17001..-17009), using the research-stances skill, following the validated
Phase 127/128/129 playbook. Concurrency cap = 3 (validated). WebFetch-only.

Output: CSV of record committed; stances + context + quotes pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\129-pa-house-rep-stances\129-02-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (IL House, batch A):

| external_id | name | district | party |
|-------------|------|----------|-------|
| -17001 | Jonathan L. Jackson | IL-01 | D |
| -17002 | Robin L. Kelly | IL-02 | D |
| -17003 | Delia C. Ramirez | IL-03 | D |
| -17004 | Jesús G. "Chuy" García | IL-04 | D |
| -17005 | Mike Quigley | IL-05 | D |
| -17006 | Sean Casten | IL-06 | D |
| -17007 | Danny K. Davis | IL-07 | D |
| -17008 | Raja Krishnamoorthi | IL-08 | D |
| -17009 | Janice D. Schakowsky | IL-09 | D |

All currently have 0 stances. Resolve each politician_id by external_id at run time.

KEY RULES (validated Phase 127/128/129 playbook):
- **25 federal topics in scope** = 44 live topics minus 11 city-level + all judicial-* topics.
  Fetch live topics FRESH; write the formatted scale to
  `backend/data/stance-research/il-house-a/_TOPIC_SCALE.txt`; each agent Reads it.
- **Concurrency: up to 3 agents at a time** (validated). WebFetch ONLY.
- **Per-rep output files → merge + RFC-4180-validate** (csv-parse/sync) into the batch CSV.
- **ADD the explicit RFC-4180 CSV-escaping instruction to every agent prompt**, including the
  PA lesson: agents can emit systematic quad-quote (`""""`) artifacts + extra trailing commas.
  Robust fix = `""""`→`"""` pre-collapse, then canonical re-parse (relax_column_count) /
  re-stringify per CSV before merge.
- **Resolve politician_id by external_id→UUID map**, not by name (García has quotes/accents).
- **Five-chairs framing**: match documented record to exact stance text; never infer from party;
  honest-skip topics with no evidence. house.gov/congress.gov 403 — lean on Ballotpedia,
  OnTheIssues (IL/ pages), Wikipedia, LCV scorecard, candidate platform sites.
- **Push** via reusable `backend/data/stance-research/pa-house-a/_push.ts` (external_id-keyed,
  answers + context + quotes in one transaction; just pass the IL batch CSV path).
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research batch A (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\129-pa-house-rep-stances\129-02-SUMMARY.md (validated playbook + escaping lesson)
</read_first>
<action>
1. Run the SKILL's topic-resolution query fresh; drop city + judicial-* topics (25 remain). Write the formatted scale to `backend/data/stance-research/il-house-a/_TOPIC_SCALE.txt`.
2. Dispatch `politician-stance-researcher` agents for the 9 batch-A reps, up to 3 concurrent. Each prompt: five-chairs framing, instruct the agent to Read `_TOPIC_SCALE.txt`, WebFetch ONLY, explicit RFC-4180 escaping rule (incl. the no-quad-quote / no-trailing-comma guidance), write to `backend/data/stance-research/il-house-a/<surname>.csv`.
3. Verify each agent's rows before the next batch; retry empties solo.
4. Merge per-rep CSVs into `backend/data/stance-research/2026-06-18-il-house-batch-a.csv` with a real RFC-4180 parser (apply the `""""`→`"""` pre-collapse + canonical re-parse/re-stringify per file); validate every row (valid topic_key, value 1-5, reasoning, >=1 http source).
</action>
<acceptance_criteria>
- 2026-06-18-il-house-batch-a.csv exists with rows for the 9 reps (or documented honest-skips); merge parser reports 0 problems
- Every row has value + reasoning + >=1 real source_url
- Each of the 9 reps accounted for (stances or honest-skip)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push batch A to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/2026-06-18-il-house-batch-a.csv
</read_first>
<action>
Present the STEP 3 approval summary. On approval, push via the external_id-keyed script `backend/data/stance-research/pa-house-a/_push.ts` (resolve politician_id by external_id→UUID + topic_id by topic_key, upsert inform.politician_answers + inform.politician_context ON CONFLICT DO UPDATE, push quotes to essentials.quotes with Read & Rank selection + surname leak-check). Commit the CSV (force-add past the stance-research gitignore).
</action>
<acceptance_criteria>
- Each batch-A rep has >=1 inform.politician_answers row OR a documented honest-skip
- Every new answer has a paired sourced context row
- DB push reported counts with zero rollbacks; non-batch-A politicians unchanged
- CSV committed
</acceptance_criteria>
</task>

<verification>
- 9 batch-A IL reps each have sourced stances or a documented skip; every answer has paired sourced context
- No party-inferred stances; live topics fresh; city + judicial-* skipped (25 federal topics); concurrency 3
</verification>
