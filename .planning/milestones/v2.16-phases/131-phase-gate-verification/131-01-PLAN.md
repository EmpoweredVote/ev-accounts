---
phase: 131-phase-gate-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-phase-127-131.sql
autonomous: true
requirements: [USHS-05]

must_haves:
  truths:
    - "backend/scripts/verify-phase-127-131.sql exists with labeled per-state assertions (USHS-01..04) + a cross-cutting sourcing assertion (USHS-05)"
    - "Running the script with ON_ERROR_STOP=1 against production exits 0 (all assertions pass; final ALL-PASS notice emitted)"
    - "Each in-scope state asserts its full covered count: FL=27, NY=26, PA=17, IL=17 reps with >=1 inform.politician_answers row"
    - "USHS-05 assertion proves ZERO in-scope answer rows lack a paired inform.politician_context row whose sources array has >=1 real (http-prefixed, non-placeholder) URL"
    - "Script is read-only — no INSERT/UPDATE/DELETE; RAISE EXCEPTION on failure, RAISE NOTICE on pass (mirrors verify-phase-125-126.sql)"
  artifacts:
    - path: "backend/scripts/verify-phase-127-131.sql"
      provides: "Permanent v2.16 phase-gate audit record for USHS-01..05"
      contains: "labeled DO-block assertions per state + cross-cutting sourcing check"
---

<objective>
Produce the permanent v2.16 phase-gate audit: a read-only SQL script
`backend/scripts/verify-phase-127-131.sql` that asserts USHS-01..04 coverage per state and
USHS-05 (zero unsourced in-scope answers), mirroring the established
`backend/scripts/verify-phase-125-126.sql` pattern. Run it against production; all assertions
must pass. This closes milestone v2.16.

In-scope reps = FL/NY/PA/IL US House, identified by external_id state-FIPS ranges:
FL = -12999..-12001, NY = -36999..-36001, PA = -42999..-42001, IL = -17999..-17001.
Confirmed current state (2026-06-18): FL 27/27 covered (394 answers), NY 26/26 (412),
PA 17/17 (262), IL 17/17 (270) — 87/87 reps covered, 1338 answers, 0 unsourced.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\backend\scripts\verify-phase-125-126.sql
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq (use $DATABASE_URL from backend/.env).

Pattern to mirror: `backend/scripts/verify-phase-125-126.sql` — one `DO $$ ... $$` block per
labeled assertion, `RAISE EXCEPTION` on failure, `RAISE NOTICE` on pass, final ALL-PASS notice.
Run command: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-127-131.sql`
(or via `node --import tsx` + pg if psql is unavailable — execute each statement and surface failures).

Schema facts:
- `inform.politician_answers (politician_id, topic_id, value)` — one row per scored stance.
- `inform.politician_context (politician_id, topic_id, reasoning, sources text[])` — paired 1:1.
- A "real" source = a `sources` array element that starts with 'http'. Placeholder/empty = fail.
- Coverage = a rep with >=1 politician_answers row. All 87 in-scope reps are covered, so no
  rep-level honest-skip exemptions are needed (criterion 2 is satisfied by full coverage).
</context>

<task id="1" type="execute">
<title>Write verify-phase-127-131.sql with labeled assertions</title>
<read_first>
- C:\EV-Accounts\backend\scripts\verify-phase-125-126.sql (exact style to mirror)
- C:\EV-Accounts\.planning\REQUIREMENTS.md (USHS-01..05 wording)
</read_first>
<action>
Create `backend/scripts/verify-phase-127-131.sql`. Header comment: purpose, run command, in-scope
external_id ranges. Then labeled DO-blocks:
1. **USHS-01 (FL)**: COUNT reps in -12999..-12001 with >=1 answer = 27; RAISE EXCEPTION otherwise.
2. **USHS-02 (NY)**: same for -36999..-36001 = 26.
3. **USHS-03 (PA)**: same for -42999..-42001 = 17.
4. **USHS-04 (IL)**: same for -17999..-17001 = 17.
5. **USHS-05 (sourcing, cross-cutting)**: assert 0 in-scope `politician_answers` rows lack a paired
   `politician_context` row whose `sources` has >=1 element with the row's first source starting
   'http' (i.e. `EXISTS (... pc.sources[1] LIKE 'http%')`). Also assert combined covered = 87.
6. Final `RAISE NOTICE 'verify-phase-127-131: ALL USHS-01..05 ASSERTIONS PASSED'`.
Use exact counts confirmed in the objective. Read-only only — no DML.
</action>
<acceptance_criteria>
- File exists; contains 5 labeled assertion blocks + final ALL-PASS notice
- No INSERT/UPDATE/DELETE anywhere in the file
- Counts baked in: FL=27, NY=26, PA=17, IL=17, combined=87
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Run the gate against production; confirm all pass; commit</title>
<read_first>
- backend/scripts/verify-phase-127-131.sql
</read_first>
<action>
Run the script against production with ON_ERROR_STOP semantics (psql, or a tsx/pg runner that
stops on the first RAISE EXCEPTION). Confirm every assertion emits its PASS notice and the final
ALL-PASS line prints with no exception. If any assertion fails, STOP and report which one + the
actual vs expected — do NOT loosen the assertion to make it pass; investigate the data gap.
On full pass, commit the SQL script.
</action>
<acceptance_criteria>
- Script runs to completion with exit 0 and prints the final ALL-PASS notice
- All 5 assertions (USHS-01..05) emit PASS notices
- SQL script committed
</acceptance_criteria>
</task>

<verification>
- verify-phase-127-131.sql passes end-to-end against production (USHS-01..05 all PASS)
- 87/87 in-scope FL/NY/PA/IL reps covered; 0 in-scope answers lack a real-sourced context row
- Milestone v2.16 phase gate closed; permanent audit record committed
</verification>
