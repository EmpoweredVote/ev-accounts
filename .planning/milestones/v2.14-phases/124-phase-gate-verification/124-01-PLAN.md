---
phase: 124-phase-gate-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-phase-120-124.sql
autonomous: false
requirements:
  - MAOF-01
  - MAOF-02
  - MAOF-03
  - MAOF-04
  - MAOF-05
  - MAOF-06
  - MAOF-07
  - MAST-01
  - MAST-02
  - MAST-03
  - MAST-04
  - MAST-05
  - MAST-06
  - MAST-07
  - MAGE-16
  - MAGE-17
  - MAGE-18
  - MAGE-19
  - MAGE-20
  - MAGE-21
  - MAGE-22

must_haves:
  truths:
    - "A single SQL script at backend/scripts/verify-phase-120-124.sql covers all 21 v2.14 requirements with labeled assertion blocks"
    - "All 44 DO $$ assertion blocks execute without RAISE EXCEPTION"
    - "The script ends with a 'Phase 120-124 gate PASSED' summary notice"
    - "7 Path 0 spot checks are embedded and have been reviewed by a human for geographic correctness"
    - "Human has confirmed all 7 cities return the expected ward councillor or at-large councillors at the test coordinate"
  artifacts:
    - path: "backend/scripts/verify-phase-120-124.sql"
      provides: "Consolidated v2.14 phase gate script — permanent audit record for all 21 requirements"
      min_lines: 300
  key_links:
    - from: "verify-phase-120-124.sql MAOF section"
      to: "essentials.districts + essentials.politicians + essentials.offices"
      via: "JOIN chain on district.geo_id = city FIPS"
      pattern: "d\\.geo_id IN \\('2545560','2562535','2537490','2523000','2572600','2539835','2545000'\\)"
    - from: "verify-phase-120-124.sql MAST section"
      to: "inform.politician_answers + inform.politician_context"
      via: "NOT EXISTS subquery for zero-stance and unpaired-stance checks"
      pattern: "NOT EXISTS.*politician_answers.*WHERE.*politician_id"
    - from: "verify-phase-120-124.sql MAGE section"
      to: "essentials.geofence_boundaries"
      via: "geo_id LIKE '{city}-ma-council-ward-%' AND mtfcc = 'X0014'"
      pattern: "geo_id LIKE '.*-ma-council-ward-%'"
    - from: "Path 0 spot checks"
      to: "essentials.districts + essentials.geofence_boundaries"
      via: "ST_Contains(gb.geometry, ST_SetSRID(ST_Point(lon, lat), 4326))"
      pattern: "ST_Contains.*ST_SetSRID.*ST_Point"
---

<objective>
Consolidate all four per-phase gate scripts (verify-phase-120.sql through verify-phase-123.sql) into a single comprehensive audit script, run it against the DB, then get human approval of the 7 Path 0 spot check results.

Purpose: Produce the permanent v2.14 audit record that closes all 21 requirements (MAOF-01..07, MAST-01..07, MAGE-16..22) in a single executable gate script. This is the final deliverable of the v2.14 milestone.

Output: backend/scripts/verify-phase-120-124.sql — a read-only SQL script with 41 labeled DO $$ assertion blocks and 7 Path 0 SELECT spot checks.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/EV-Accounts/.planning/PROJECT.md
@C:/EV-Accounts/.planning/ROADMAP.md
@C:/EV-Accounts/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write verify-phase-120-124.sql — consolidated v2.14 gate script</name>
  <files>backend/scripts/verify-phase-120-124.sql</files>

  <read_first>
    - backend/scripts/verify-phase-120.sql — all 9 MAOF assertions (copy verbatim; renumber to 1–9)
    - backend/scripts/verify-phase-121.sql — 9 MAST Wave 1 assertions (renumber 10–18)
    - backend/scripts/verify-phase-122.sql — 12 MAST Wave 2 assertions (renumber 19–30)
    - backend/scripts/verify-phase-123.sql — 11 MAGE assertions + 7 Path 0 spot checks (renumber 31–41)
  </read_first>

  <action>
Create backend/scripts/verify-phase-120-124.sql by combining the four source scripts into one file. Exact structure:

SECTION HEADER (top of file):
  -- verify-phase-120-124.sql
  -- v2.14 MA City Expansion Wave 2 — Consolidated Phase Gate
  -- Covers Phases 120–123 (all 21 requirements: MAOF-01..07, MAST-01..07, MAGE-16..22)
  -- Run via Supabase MCP execute_sql. All 41 assertions must pass (RAISE EXCEPTION on failure).
  -- Cities: Newton (2545560), Somerville (2562535), Lynn (2537490), Fall River (2523000),
  --         Waltham (2572600), Medford (2539835), New Bedford (2545000)

PART 1 — MAOF (Officials Seeding), Assertions 1–9:
  Source: verify-phase-120.sql assertions 1–9.
  Copy all 9 DO $$ blocks verbatim. Renumber assertion labels to 1 through 9
  (i.e., replace "ASSERTION 1" with "ASSERTION 1", "ASSERTION 2" with "ASSERTION 2", etc. — they are already 1–9 in the source, no renumbering needed).
  Add section header: -- ============================================================
  --   PART 1: MAOF — Officials Seeding (Assertions 1–9)
  --   Phase 120 | Requirements: MAOF-01..07
  -- ============================================================

PART 2 — MAST Wave 1 (Newton/Somerville/Medford stances), Assertions 10–18:
  Source: verify-phase-121.sql assertions 1–9.
  Renumber labels: source "ASSERTION 1" → "ASSERTION 10", "ASSERTION 2" → "ASSERTION 11",
  through "ASSERTION 9" → "ASSERTION 18".
  Update RAISE EXCEPTION and RAISE NOTICE text to match the new numbers.
  Section header: -- PART 2: MAST Wave 1 — Newton/Somerville/Medford Stances (Assertions 10–18)
  -- Phase 121 | Requirements: MAST-01, MAST-02, MAST-06

PART 3 — MAST Wave 2 (Lynn/Fall River/Waltham/New Bedford stances), Assertions 19–30:
  Source: verify-phase-122.sql assertions 1–12.
  Renumber labels: source "ASSERTION 1" → "ASSERTION 19", "ASSERTION 2" → "ASSERTION 20",
  through "ASSERTION 12" → "ASSERTION 30".
  Update RAISE EXCEPTION and RAISE NOTICE text to match the new numbers.
  Section header: -- PART 3: MAST Wave 2 — Lynn/Fall River/Waltham/New Bedford Stances (Assertions 19–30)
  -- Phase 122 | Requirements: MAST-03, MAST-04, MAST-05, MAST-07

PART 4 — MAGE Ward Geofencing (Assertions 31–41):
  Source: verify-phase-123.sql assertions 1, 2, 3a, 3b, 4a, 4b, 5a, 5b, 6a, 6b, 7a, 7b, 7c, 7d.
  That is 11 DO $$ assertion blocks (2, 3a, 3b, 4a, 4b, 5a, 5b, 6a, 6b, 7a, 7b, 7c, 7d =
  actually count: assertion 1 + assertion 2 + 3a + 3b + 4a + 4b + 5a + 5b + 6a + 6b + 7a + 7b + 7c + 7d = 14 blocks but the summary in STATE.md says 11 assertion blocks).
  Re-read verify-phase-123.sql carefully: there are exactly 11 distinct DO $$ blocks before the
  path 0 section (assertions 1, 2, 3a, 3b, 4a, 4b, 5a, 5b, 6a, 6b, 7a — then 7b is in the same
  city as 7a, 7c and 7d are New Bedford). Actually the source has: 1, 2, 3a, 3b, 4a, 4b, 5a, 5b,
  6a, 6b, 7a, 7b, 7c, 7d = 14 distinct DO $$ assertion blocks. Copy all 14.
  Renumber: source block 1 → "ASSERTION 31", block 2 → "ASSERTION 32", 3a → "ASSERTION 33",
  3b → "ASSERTION 34", 4a → "ASSERTION 35", 4b → "ASSERTION 36", 5a → "ASSERTION 37",
  5b → "ASSERTION 38", 6a → "ASSERTION 39", 6b → "ASSERTION 40", 7a → "ASSERTION 41",
  7b → "ASSERTION 42", 7c → "ASSERTION 43", 7d → "ASSERTION 44".
  That gives MAGE assertions 31–44 (14 blocks). Total assertion count = 9 + 9 + 12 + 14 = 44.
  Update all RAISE EXCEPTION and RAISE NOTICE text to use the new assertion numbers.
  Section header: -- PART 4: MAGE — Ward Geofencing (Assertions 31–44)
  -- Phase 123 | Requirements: MAGE-16..22

FINAL SUMMARY DO $$ block (after all assertions, before Path 0):
  DO $$ BEGIN
    RAISE NOTICE 'Phase 120-124 gate PASSED: all 44 assertions passed. MAOF-01..07 (Phase 120), MAST-01..07 (Phases 121-122), MAGE-16..22 (Phase 123) all fulfilled. v2.14 MA City Expansion Wave 2 complete.';
  END $$;

PATH 0 SPOT CHECKS (copy verbatim from verify-phase-123.sql, lines 239–360 — the 7 SELECT
queries with their DO $$ BEGIN RAISE NOTICE '=== PATH 0 SPOT CHECKS ===' preamble):
  Copy the entire Path 0 section unchanged (no renumbering needed — these are SELECT-only, not assertion blocks).
  Precede with:
  -- ============================================================
  -- PATH 0 SPOT CHECKS — SELECT only, no RAISE EXCEPTION
  -- Review output for geographic correctness (7 cities).
  -- Ward-seat cities (Newton, Somerville, Lynn, Waltham, New Bedford): expect per-ward geo_id
  -- At-large cities (Fall River, Medford): expect citywide geo_id (2523000 / 2539835)
  -- ============================================================

IMPORTANT consistency rules:
  - Do not alter any WHERE clause, join condition, or expected count from the source scripts.
  - Preserve all comments within each block (e.g., "NOTE: Fall River is fully at-large", "Medford geo_id is '2539835' (corrected FIPS)").
  - The only changes allowed: renumbering assertion labels in RAISE EXCEPTION / RAISE NOTICE strings and section headers.
  - Keep the per-phase section divider comments so the reader can navigate back to the original source script.
  - File must be valid SQL that can be run via Supabase MCP execute_sql in a single shot.
  </action>

  <verify>
    <automated>powershell -Command "if (Test-Path 'C:/EV-Accounts/backend/scripts/verify-phase-120-124.sql') { $lines = (Get-Content 'C:/EV-Accounts/backend/scripts/verify-phase-120-124.sql').Count; Write-Host \"File exists: $lines lines\" } else { Write-Host 'FILE MISSING' }"</automated>
  </verify>

  <acceptance_criteria>
    - File exists at backend/scripts/verify-phase-120-124.sql
    - File contains exactly 44 DO $$ assertion blocks (grep for "DO \$\$" to count)
    - File contains the string "Phase 120-124 gate PASSED"
    - File contains the string "ASSERTION 1 " (space after, confirming section 1 present)
    - File contains the string "ASSERTION 44 " (confirming final MAGE assertion present)
    - File contains the string "PATH 0 SPOT CHECKS" section header
    - File contains all 7 Path 0 SELECT queries (grep for "ST_Point" should return 7 matches)
    - File contains the string "MAGE-16" and "MAGE-22" in RAISE EXCEPTION text
    - File contains the string "MAOF-01" and "MAOF-07" in RAISE EXCEPTION text
    - File contains the string "MAST-01" and "MAST-07" in RAISE EXCEPTION text
    - File does NOT contain "ASSERTION 1 PASSED [MAST" (MAST assertions start at 10, not 1)
    - File does NOT contain "ASSERTION 1 PASSED [MAGE" (MAGE assertions start at 31, not 1)
  </acceptance_criteria>

  <done>backend/scripts/verify-phase-120-124.sql exists and contains 44 labeled DO $$ assertion blocks (MAOF 1–9, MAST 10–30, MAGE 31–44) plus 7 Path 0 SELECT spot checks, with no WHERE clause or expected-count changes from the source scripts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run consolidated gate script and verify Path 0 results</name>

  <read_first>
    - backend/scripts/verify-phase-120-124.sql — the file just written (to understand what is being run)
  </read_first>

  <what-built>
    Task 1 wrote backend/scripts/verify-phase-120-124.sql — a 44-assertion SQL gate script consolidating all v2.14 verification across Phases 120–123. The executor runs it via Supabase MCP execute_sql (full file in one call) and then presents the 7 Path 0 spot check SELECT outputs for human review.

    Before presenting to human, executor must confirm:
    - All 44 DO $$ blocks completed without any "FAILED" or "EXCEPTION" in the output
    - The final summary notice "Phase 120-124 gate PASSED" appeared in the output

    If any assertion fails: stop, report which assertion number + requirement ID failed, and do NOT proceed to human verify.
  </what-built>

  <how-to-verify>
    Review the Path 0 spot check results from running verify-phase-120-124.sql.

    For each city, confirm the expected output:

    1. Newton (-71.209, 42.337 — City Hall area):
       Expected: at least 1 row with geo_id LIKE 'newton-ma-council-ward-N'
       Prior result (Phase 123 confirmed): Ward 2 / David Micley

    2. Somerville (-71.100, 42.387 — Davis Square area):
       Expected: at least 1 row with geo_id LIKE 'somerville-ma-council-ward-N'
       Prior result (Phase 123 confirmed): Ward 3 / Ben Ewen-Campen

    3. Lynn (-70.947, 42.467 — City Hall):
       Expected: at least 1 row with geo_id LIKE 'lynn-ma-council-ward-N'
       Prior result (Phase 123 confirmed): Ward 4 / Natasha S. Megie-Maddrey

    4. Fall River (-71.157, 41.701 — City Hall):
       Expected: geo_id = '2523000' (citywide); 9 at-large councillors (Raposo, Peckham, Ponte, Camara, Pereira, Canuel, Dionne, Hart, Cadime)
       Must NOT return any 'fall-river-ma-council-ward-N' geo_id rows

    5. Waltham (-71.236, 42.376 — City Hall / Moody St):
       Expected: at least 1 row with geo_id LIKE 'waltham-ma-council-ward-N'
       Prior result (Phase 123 confirmed): Ward 5 / Joseph LaCava

    6. Medford (-71.107, 42.418 — City Hall):
       Expected: geo_id = '2539835' (citywide); 7 at-large councillors (Callahan, Lazzaro, Scarpelli, Bears, Tseng, Mullane, Leming)
       Must NOT return any 'medford-ma-council-ward-N' geo_id rows

    7. New Bedford (-70.924, 41.635 — City Hall):
       Expected: at least 1 row with geo_id LIKE 'new-bedford-ma-council-ward-N'
       Prior result (Phase 123 confirmed): Ward 4 / Derek Baptiste

    Type "approved" if all 7 cities return geographically correct results, or describe any discrepancy.
  </how-to-verify>

  <resume-signal>Type "approved" to close Phase 124 and the v2.14 milestone, or describe any Path 0 result that looks wrong.</resume-signal>

  <acceptance_criteria>
    - Supabase MCP execute_sql run completed with no RAISE EXCEPTION output
    - "Phase 120-124 gate PASSED: all 44 assertions passed" notice appeared in output
    - All 7 Path 0 spot checks returned rows (none returned 0 rows)
    - Ward-seat cities (Newton, Somerville, Lynn, Waltham, New Bedford) returned per-ward geo_ids
    - At-large cities (Fall River, Medford) returned citywide geo_id (2523000 / 2539835), NOT per-ward geo_ids
    - Human typed "approved"
  </acceptance_criteria>

  <done>Human has approved Path 0 results for all 7 cities. verify-phase-120-124.sql is confirmed as the permanent v2.14 audit record. All 21 requirements (MAOF-01..07, MAST-01..07, MAGE-16..22) are verifiably closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → Supabase DB | Read-only SQL queries via Supabase MCP execute_sql |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Information Disclosure | verify-phase-120-124.sql | accept | Script is read-only (SELECT + DO $$ with no DML). No data written, no PII returned — only counts and geo_ids. |
| T-124-02 | Tampering | Script file on disk | accept | Script is checked into git; any modification is visible in diff. No CI/CD dependency on this file path. |
</threat_model>

<verification>
Run after Task 1 completes:

```
-- Quick smoke check (run before full script):
SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id LIKE '%-ma-council-ward-%' AND mtfcc = 'X0014';
-- Expected: >= 46 (8+7+7+9+9+8+6 = 54 from Phase 123)
```

Full gate: run verify-phase-120-124.sql via Supabase MCP execute_sql and confirm "Phase 120-124 gate PASSED" in output.
</verification>

<success_criteria>
1. backend/scripts/verify-phase-120-124.sql exists with 44 DO $$ assertion blocks.
2. All 44 assertions pass without RAISE EXCEPTION when run via execute_sql.
3. "Phase 120-124 gate PASSED: all 44 assertions passed" appears in script output.
4. All 7 Path 0 spot checks return rows (no city returns 0 results).
5. Human reviews Path 0 output and types "approved".
6. v2.14 milestone is closed.
</success_criteria>

<output>
Create .planning/phases/124-phase-gate-verification/124-01-SUMMARY.md when done.
</output>
