---
phase: 121-stance-research-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/700_liz_mullane_stances.sql
  - backend/data/stance-research/2026-06-XX-medford-mullane.csv
autonomous: false
requirements:
  - MAST-06

must_haves:
  truths:
    - "Liz Mullane (id=5846208f-d354-4e01-aa0c-4328574357f1) has at least 1 row in inform.politician_answers OR migration 700 contains an honest-skip comment documenting why no stances were found"
    - "Every stance row for Liz Mullane has a paired inform.politician_context row with non-empty sources array (real fetched URLs)"
    - "No stance was inferred from party affiliation — every row traces to a real fetched URL"
    - "Migration 700 committed to disk and applied to production (supabase_migrations.schema_migrations has version='700')"
    - "Newton, Somerville, and Medford stance counts are unchanged for all politicians OTHER than Liz Mullane (no accidental overwrites)"
  artifacts:
    - path: "backend/migrations/700_liz_mullane_stances.sql"
      provides: "Stance INSERT/UPSERT statements for Liz Mullane OR honest-skip documentation with reasoning"
      contains: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
    - path: "backend/data/stance-research/2026-06-XX-medford-mullane.csv"
      provides: "CSV research artifact for Liz Mullane"
      contains: "politician_id,topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research compass stance data for Liz Mullane (Ward-at-large City Councilor, Medford MA) — the only official in Newton/Somerville/Medford with 0 stance rows. All other 44 officials across the three cities already have sourced stances with paired context rows.

Purpose: Close the single gap blocking MAST-06. If evidence is found, insert into inform.politician_answers + inform.politician_context. If no evidence is found after a genuine search, write migration 700 as an honest-skip document with reasoning, so the record shows the attempt.
Output: Migration 700 applied to production; Liz Mullane appears in the compass system or is explicitly documented as skipped.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\PROJECT.md
@C:\EV-Accounts\.planning\ROADMAP.md
@C:\EV-Accounts\.planning\STATE.md
@C:\Users\Chris\.claude\skills\research-stances\SKILL.md
</execution_context>

<context>
<!-- Politician identity -->
Liz Mullane
- politician_id: 5846208f-d354-4e01-aa0c-4328574357f1
- Title: City Councilor (Ward-at-large)
- City: Medford, MA (geo_id='2539835')
- District: LOCAL (Medford city council at-large seat)

<!-- Migration pattern reference (most recent stance migration) -->
@C:\EV-Accounts\backend\migrations\690_lafauci_stances.sql

<!-- Topic UUID reference — fetch live from DB before researching -->
Run this query to get all live topics with stance texts:
  SELECT t.id, t.topic_key, t.title, t.question_text,
    json_agg(json_build_object('value', s.value, 'text', s.text) ORDER BY s.value) AS stances
  FROM inform.compass_topics t
  JOIN inform.compass_stances s ON s.topic_id = t.id
  WHERE t.is_live = true
  GROUP BY t.id, t.topic_key, t.title, t.question_text
  ORDER BY t.topic_key

<!-- Honest-skip pattern: if no evidence found, migration comment block -->
-- Honest-skip: Liz Mullane (5846208f-d354-4e01-aa0c-4328574357f1)
-- Attempted sources: [list URLs tried], [list URLs tried]
-- Reason: No public record found for [topic] — official is ward-at-large with limited
--   recorded policy positions beyond [specific areas searched].
-- No stance rows inserted. This comment documents the research attempt.
-- Apply this migration to mark the research as complete in the ledger.
</context>

<tasks>

<task id="T1" type="research">
  <title>Research Liz Mullane stances via WebFetch</title>
  <read_first>
    - C:\Users\Chris\.claude\skills\research-stances\SKILL.md (full skill — read before researching)
    - C:\EV-Accounts\backend\migrations\690_lafauci_stances.sql (migration format reference)
  </read_first>
  <action>
    Run the research-stances skill for Liz Mullane (Medford City Councilor, at-large).

    Dispatch ONE politician-stance-researcher agent using the Agent tool with subagent_type="politician-stance-researcher".

    CRITICAL rules (from MEMORY.md and SKILL.md):
    - Use WebFetch ONLY. Never WebSearch or Playwright.
    - NEVER infer stances from party affiliation. Every entry needs at least one real fetched URL.
    - Skip a topic entirely if no evidence found — do NOT insert neutral defaults.
    - If no evidence found for any topic after genuine search, proceed to T2 with honest-skip.

    Fetch live topics from DB first (mcp__supabase-local__execute_sql) and embed stance texts in the agent prompt per SKILL.md Step 0.

    Output CSV to: backend/data/stance-research/2026-06-15-medford-mullane.csv
    (Use today's date 2026-06-15)

    Suggested source URLs to try for Medford at-large councillors:
    - https://www.cityofmedford.org/city-council
    - https://www.medfordtranscript.com (search Liz Mullane)
    - https://ballotpedia.org/Liz_Mullane
    - https://www.facebook.com/LizMullaneForMedford (if public)
    - Search pattern: "Liz Mullane Medford" on local news sites
  </action>
  <acceptance_criteria>
    - CSV file exists at backend/data/stance-research/2026-06-15-medford-mullane.csv OR agent returns empty/honest-skip result
    - If CSV has rows: each row has politician_id='5846208f-d354-4e01-aa0c-4328574357f1', a valid topic_key, value 1-5, and a real source_url that was actually fetched
    - No row has value assigned without a corresponding fetched URL
  </acceptance_criteria>
</task>

<task id="T2" type="execute">
  <title>Write migration 700 (stances or honest-skip)</title>
  <read_first>
    - C:\EV-Accounts\backend\migrations\690_lafauci_stances.sql (INSERT/UPSERT pattern)
    - backend/data/stance-research/2026-06-15-medford-mullane.csv (T1 output — stances found, if any)
  </read_first>
  <action>
    Write C:\EV-Accounts\backend\migrations\700_liz_mullane_stances.sql

    IF stances were found in T1:
    - Header comment block: migration number, politician name + ID, topic scope, idempotency note
    - Topic UUID reference comment block (copy from 690_lafauci_stances.sql format)
    - BEGIN;
    - For each stance found: INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES (...) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;
    - For each stance: INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES (...) ON CONFLICT ... DO UPDATE SET reasoning=EXCLUDED.reasoning, sources=EXCLUDED.sources;
    - COMMIT;
    - Verification query comments at bottom (row count + unpaired check + citation check)

    IF no stances were found (honest-skip):
    - Header comment explaining this is an honest-skip migration
    - Document each source URL attempted and why it yielded no usable data
    - BEGIN;
    - INSERT INTO supabase_migrations.schema_migrations (version, name, hash) VALUES ('700', '700_liz_mullane_stances', md5('700_liz_mullane_stances_honest_skip')) ON CONFLICT DO NOTHING; -- ledger only, no data rows
    - COMMIT;
    - Note at bottom: "Honest-skip: no public policy record found for Liz Mullane after exhaustive search of [sources]"

    politician_id = '5846208f-d354-4e01-aa0c-4328574357f1'
  </action>
  <acceptance_criteria>
    - File exists at backend/migrations/700_liz_mullane_stances.sql
    - File begins with -- Migration 700: header comment
    - File contains BEGIN; and COMMIT;
    - IF stances present: each INSERT references politician_id='5846208f-d354-4e01-aa0c-4328574357f1' and a valid topic UUID from the live topic list
    - IF honest-skip: file contains "Honest-skip" in a comment and documents URLs attempted
    - ON CONFLICT clause present on all INSERT statements (idempotent)
  </acceptance_criteria>
</task>

<task id="T3" type="execute" blocking="true">
  <title>Apply migration 700 to production</title>
  <read_first>
    - backend/migrations/700_liz_mullane_stances.sql (verify before applying)
  </read_first>
  <action>
    Apply migration 700 using Supabase MCP (mcp__supabase-local__apply_migration):
    - Read the file contents
    - Apply via mcp__supabase-local__apply_migration with name="700_liz_mullane_stances"

    After applying, verify:
    SELECT version FROM supabase_migrations.schema_migrations WHERE version='700';
    -- Must return 1 row

    If stances were inserted:
    SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id='5846208f-d354-4e01-aa0c-4328574357f1';
    -- Must return >= 1

    SELECT COUNT(*) FROM inform.politician_answers pa
    LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
    WHERE pa.politician_id='5846208f-d354-4e01-aa0c-4328574357f1' AND pc.politician_id IS NULL;
    -- Must return 0 (all stances paired)
  </action>
  <acceptance_criteria>
    - supabase_migrations.schema_migrations has a row with version='700'
    - IF stances: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id='5846208f-...' returns >= 1
    - IF honest-skip: migration ledger row exists; inform.politician_answers count = 0 (expected)
    - No unpaired stances (missing_context = 0)
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Phase 121-01 Verification

Run after all tasks complete:

```sql
-- MAST-06 partial gate: Liz Mullane covered
SELECT
  p.full_name,
  COUNT(pa.topic_id) as stances,
  COUNT(pc.topic_id) as has_context
FROM essentials.politicians p
LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
WHERE p.id = '5846208f-d354-4e01-aa0c-4328574357f1'
GROUP BY p.id, p.full_name;
-- stances >= 1 OR honest-skip is documented in migration 700

-- Unpaired check (must return 0):
SELECT COUNT(*) FROM inform.politician_answers pa
LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id
WHERE pa.politician_id='5846208f-d354-4e01-aa0c-4328574357f1' AND pc.politician_id IS NULL;
```
</verification>
