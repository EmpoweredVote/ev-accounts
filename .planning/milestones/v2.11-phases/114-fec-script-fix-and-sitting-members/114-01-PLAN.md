---
phase: 114-fec-script-fix-and-sitting-members
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/fix-fec-name-mismatches.ts
autonomous: true
requirements:
  - FECF-01
  - FECF-02
  - FECF-03

must_haves:
  truths:
    - "Glenn Ivey, Keith Self, Raphael Warnock, and Ted Cruz each have non-null finance_summary"
    - "Doug LaMalfa and Eric Swalwell each have a non-null finance_summary and a politician_sources row with research_status = 'confirmed'"
    - "The script prints MATCH (or DIRECT) for these 6 politicians and no [skip] No committee lines for them"
    - "NULL finance_summary count for federal politicians drops from ~40 to approximately 34"
  artifacts:
    - path: "backend/scripts/fix-fec-name-mismatches.ts"
      provides: "patched FEC ingestion script"
      contains: "candidate/${fecId}/committees/"
    - path: "backend/scripts/fix-fec-name-mismatches.ts"
      contains: "resolveViaDirectSearch"
    - path: "backend/scripts/fix-fec-name-mismatches.ts"
      contains: "for (const cycle of"
  key_links:
    - from: "fix-fec-name-mismatches.ts fetchFecData()"
      to: "GET /v1/candidate/{id}/committees/"
      via: "fallback when principal_committees is empty"
      pattern: "candidate/.*committees/"
    - from: "fix-fec-name-mismatches.ts main()"
      to: "GET /v1/candidates/?q=NAME&state=CA&office=H"
      via: "resolveViaDirectSearch() when resolveFecId() returns null"
      pattern: "resolveViaDirectSearch"
    - from: "fix-fec-name-mismatches.ts"
      to: "transparent_motivations.politician_sources"
      via: "pool.query() INSERT ON CONFLICT ... DO UPDATE"
      pattern: "ON CONFLICT.*DO UPDATE"
---

<objective>
Patch `fix-fec-name-mismatches.ts` with three in-place edits that fix the two failure modes
seen in the 2026-06-05 dry run, then run the script live to ingest finance data for 6
sitting members.

Purpose: The prior script run produced 11 MATCH rows in `politician_sources` but wrote $0
finance data for senators not running in 2026 (committee lookup fails because
`principal_committees` is only populated for current-cycle candidates). LaMalfa and Swalwell
fall to NO_MATCH because the congress-legislators YAML has no `id.fec[]` entries for them.
Both problems are fixed by this plan, after which the script is run live.

Output: Patched `fix-fec-name-mismatches.ts`, finance_summary populated for 6 federal
politicians (Ivey, Self, Warnock, Cruz, LaMalfa, Swalwell), NULL count reduced by ~6.
</objective>

<execution_context>
@C:\Users\Chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-fec-script-fix-and-sitting-members/114-RESEARCH.md
@.planning/phases/114-fec-script-fix-and-sitting-members/114-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from backend/scripts/fix-fec-name-mismatches.ts -->

Current fetchFecData() lines 115–149 (the three-step structure being patched):
- Step 1: GET /v1/candidates/search/ → extract principal_committees[0].id as committeeId
  - Line 122: `const committeeId = searchData.results?.[0]?.principal_committees?.[0]?.id;`
  - Lines 123–126: early return null if !committeeId  ← THIS IS THE BUG
- Step 2: GET /v1/candidates/totals/ hardcoded cycle=FEC_CYCLE  ← ALSO NEEDS FIXING
- Step 3: GET /v1/schedules/schedule_a/by_employer/ hardcoded cycle=FEC_CYCLE  ← ALSO NEEDS FIXING
- Return: `{ total_raised, top_donors, cycle: FEC_CYCLE, source: 'FEC' }`

Current main() null-fecId branch lines 180–185:
- Logs NO_MATCH, pushes to noMatchList, continues  ← THIS IS THE BUG for LaMalfa/Swalwell

From backend/scripts/ehn-fec-finance.ts lines 136–145 (verbatim copy target for Patch 1):
```
async function fetchCommitteeIdFallback(fecCandidateId, apiKey): Promise<string | null>
  GET /v1/candidate/${fecCandidateId}/committees/?api_key=...&per_page=5
  return data.results[0]?.committee_id ?? null   // NOTE: field is .committee_id not .id
```

From backend/src/lib/fecResearch.ts (pattern for Patch 3):
  searchFecCandidates() → GET /v1/candidates/?api_key=X&q=NAME&state=CA&office=H&per_page=20
  scoreMatch() for name matching — last-name exact match is sufficient given state+office filters

DB write patterns (all pool.query, never supabaseAdmin):
  politician_sources INSERT: pool.query with ON CONFLICT DO NOTHING (existing rows)
  politician_sources UPSERT for LaMalfa/Swalwell: ON CONFLICT (essentials_politician_id, source_system) DO UPDATE SET external_id = EXCLUDED.external_id, research_status = EXCLUDED.research_status
  finance_summary UPDATE: UPDATE essentials.politicians SET finance_summary = $1::jsonb WHERE id = $2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply three patches to fix-fec-name-mismatches.ts</name>
  <files>backend/scripts/fix-fec-name-mismatches.ts</files>
  <read_first>
    - backend/scripts/fix-fec-name-mismatches.ts (read the whole file — it is 223 lines; understand every line before touching anything)
    - backend/scripts/ehn-fec-finance.ts lines 136–145 (fetchCommitteeIdFallback verbatim copy target for Patch 1)
    - backend/scripts/ehn-fec-finance.ts lines 151–166 (multi-cycle totals pattern for Patch 2)
    - backend/src/lib/fecResearch.ts lines 136–219 (scoreMatch, parseFecName, normalize, searchFecCandidates — pattern for Patch 3)
  </read_first>
  <action>
Apply exactly three patches to `backend/scripts/fix-fec-name-mismatches.ts`. Do not restructure any other code.

PATCH 1 — FECF-01 committee lookup fallback (inside `fetchFecData()`):

Replace lines 122–126 (the `const committeeId = ...; if (!committeeId) { return null; }` block) with:

  let committeeId: string | undefined = searchData.results?.[0]?.principal_committees?.[0]?.id;

  if (!committeeId) {
    await sleep(SLEEP_MS);
    const fbUrl = `${FEC_BASE}/candidate/${fecId}/committees/?api_key=${apiKey}&per_page=5`;
    const fbResp = await fetch(fbUrl, { signal: AbortSignal.timeout(30_000) });
    if (fbResp.ok) {
      const fbData = await fbResp.json() as { results?: Array<{ committee_id: string }> };
      committeeId = fbData.results?.[0]?.committee_id ?? undefined;
    }
  }
  if (!committeeId) {
    console.log(`  [skip] No committee for ${fecId}`);
    return null;
  }

CRITICAL field name note: The primary path uses `.id` (from `principal_committees`). The fallback uses `.committee_id` (from `/candidate/{id}/committees/`). These are different field names for the same value. Copy from ehn-fec-finance.ts lines 136–145 where this is already handled correctly.

PATCH 2 — FECF-01 multi-cycle totals fallback (inside `fetchFecData()`):

Replace Step 2 (lines 128–134, the single totals fetch) with a 3-cycle loop. Also update Step 3 and the return to use `usedCycle` instead of the hardcoded `FEC_CYCLE` constant:

  let totalRaised = 0;
  let usedCycle = FEC_CYCLE;
  for (const cycle of [FEC_CYCLE, '2024', '2022']) {
    const totalsUrl = `${FEC_BASE}/candidates/totals/?api_key=${apiKey}&candidate_id=${fecId}&cycle=${cycle}&per_page=1`;
    await sleep(SLEEP_MS);
    const totalsResp = await fetch(totalsUrl, { signal: AbortSignal.timeout(30_000) });
    if (!totalsResp.ok) throw new Error(`FEC totals HTTP ${totalsResp.status}`);
    const totalsData = await totalsResp.json() as { results?: Array<{ receipts?: number }> };
    const receipts = totalsData.results?.[0]?.receipts ?? 0;
    if (receipts > 0) { totalRaised = receipts; usedCycle = cycle; break; }
  }

Also change the donors URL (currently line 137) from `&cycle=${FEC_CYCLE}` to `&cycle=${usedCycle}`, and the return statement (currently line 148) from `cycle: FEC_CYCLE` to `cycle: usedCycle`.

PATCH 3 — FECF-03 direct FEC name search for null-fecId politicians (LaMalfa/Swalwell):

Add a new function `resolveViaDirectSearch()` above `main()` — after `fetchFecData()`:

  async function resolveViaDirectSearch(
    pol: FedPolitician,
    apiKey: string,
  ): Promise<string | null> {
    // LaMalfa (CA-01) and Swalwell (CA-14) are both California House members
    const state = 'CA';
    const searchUrl = `${FEC_BASE}/candidates/?api_key=${apiKey}&q=${encodeURIComponent(pol.full_name)}&state=${state}&office=H&per_page=20`;
    await sleep(SLEEP_MS);
    const resp = await fetch(searchUrl, { signal: AbortSignal.timeout(30_000) });
    if (!resp.ok) throw new Error(`FEC direct search HTTP ${resp.status}`);
    const data = await resp.json() as {
      results?: Array<{ candidate_id: string; name: string; state: string; office: string }>
    };
    if (!data.results?.length) return null;
    const lastName = pol.full_name.toLowerCase().split(' ').pop()!;
    const best = data.results.find(r => r.name.toLowerCase().includes(lastName));
    return best?.candidate_id ?? null;
  }

Then replace the null-fecId branch in `main()` (lines 180–185, the `if (!fecId) { NO_MATCH... continue; }` block) with:

  if (!fecId) {
    if (DRY_RUN) {
      console.log(`NO_MATCH  ${pol.full_name} (${pol.chamber_short}) — would attempt direct search`);
      stats.no_match++;
      continue;
    }
    const directId = await resolveViaDirectSearch(pol, apiKey);
    if (!directId) {
      console.log(`NO_MATCH  ${pol.full_name} (${pol.chamber_short})`);
      noMatchList.push(pol.full_name);
      stats.no_match++;
      continue;
    }
    console.log(`DIRECT    ${pol.full_name} → ${directId}`);
    stats.matched++;
    // Use DO UPDATE to overwrite any prior partial run that left an empty external_id
    await pool.query(`
      INSERT INTO transparent_motivations.politician_sources
        (essentials_politician_id, source_system, external_id, research_status, source_type)
      VALUES ($1, $2, $3, 'confirmed', 'candidate_committee')
      ON CONFLICT (essentials_politician_id, source_system)
      DO UPDATE SET external_id = EXCLUDED.external_id,
                    research_status = EXCLUDED.research_status
    `, [pol.id, 'fec_house', directId]);
    fecId = directId; // fall through to existing MATCH path (fetchFecData + finance_summary UPDATE)
  }

Note: The `fecId` variable is declared with `const` in the current code (line 178: `const fecId = resolveFecId(...)`). Change it to `let fecId` so the reassignment `fecId = directId` is valid.
  </action>
  <verify>
    <automated>cd backend && npx tsx scripts/fix-fec-name-mismatches.ts --dry-run 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/fix-fec-name-mismatches.ts` contains the string `candidate/${fecId}/committees/` (the fallback URL)
    - File contains `committee_id` (the fallback field name — distinct from `.id` used in the primary path)
    - File contains `for (const cycle of [FEC_CYCLE, '2024', '2022'])` (multi-cycle loop)
    - File contains `cycle: usedCycle` in the return statement of `fetchFecData()`
    - File contains `resolveViaDirectSearch` function definition
    - File contains `ON CONFLICT (essentials_politician_id, source_system) DO UPDATE` (upsert for LaMalfa/Swalwell)
    - File contains `let fecId` (not `const fecId`) in the main loop
    - `tsx scripts/fix-fec-name-mismatches.ts --dry-run` exits 0 and prints at least one MATCH line for Schumer/Sanders/Hassan/Coons (already-matched senators) and one line containing "would attempt direct search" for LaMalfa or Swalwell if they appear in the NULL set
    - Dry-run output does NOT print `[skip] No committee for S4NY` or similar for the 4 already-matched senators (Ivey/Self/Warnock/Cruz are House members so won't hit the senator-cycle issue; the dry-run skips FEC calls anyway — verify by reading the logic, not output)
  </acceptance_criteria>
  <done>All three patches applied. Dry-run exits 0. `fix-fec-name-mismatches.ts` contains all five required strings.</done>
</task>

<task type="auto">
  <name>Task 2: Run script live and verify finance_summary populated for all 6 targets</name>
  <files>backend/scripts/fix-fec-name-mismatches.ts</files>
  <read_first>
    - backend/scripts/fix-fec-name-mismatches.ts (confirm Task 1 patches are in place before running)
    - .env (confirm FEC_API_KEY and DATABASE_URL are set — read only the first 3 chars of each value to verify presence, never log the full values)
  </read_first>
  <action>
Pre-flight: before running live, confirm the four FECF-02 targets have politician_sources rows with confirmed status by running:

  SELECT p.full_name, ps.research_status, ps.external_id
  FROM essentials.politicians p
  JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id
  WHERE p.full_name IN ('Glenn Ivey', 'Keith Self', 'Raphael Warnock', 'Ted Cruz')
  ORDER BY p.full_name;

Expected: 4 rows with research_status = 'confirmed' and non-empty external_id. If any are missing, the script will create them during the live run — this is fine.

Run the script live from the `backend/` directory:

  cd backend && npx tsx scripts/fix-fec-name-mismatches.ts

Watch the console output. Expected behavior:
- Ivey, Self, Warnock, Cruz: MATCH lines, then [OK] $X written
- LaMalfa, Swalwell: DIRECT lines (after direct search), then [OK] $X written
- Non-2026-ballot senators (Schumer, Sanders, Hassan, Coons if in the NULL set): MATCH lines, cycle fallback tries 2024/2022, then [OK] $X written
- No [skip] No committee lines for the 6 targets above

After the script completes, run a SQL verification query:

  SELECT p.full_name,
         p.finance_summary IS NOT NULL AS has_summary,
         p.finance_summary->>'cycle' AS cycle,
         p.finance_summary->>'total_raised' AS total_raised
  FROM essentials.politicians p
  WHERE p.full_name IN (
    'Glenn Ivey', 'Keith Self', 'Raphael Warnock', 'Ted Cruz',
    'Doug LaMalfa', 'Eric Swalwell'
  )
  ORDER BY p.full_name;

Expected: 6 rows, all with has_summary = true, non-null cycle and total_raised values.

Also run:

  SELECT COUNT(*) AS null_finance_federal
  FROM essentials.politicians p
  JOIN essentials.offices o ON o.politician_id = p.id
  JOIN essentials.districts d ON d.id = o.district_id
  WHERE d.district_type IN ('NATIONAL_UPPER', 'NATIONAL_LOWER')
    AND p.is_active = true
    AND p.finance_summary IS NULL;

Expected: count approximately 34 (down from ~40 before this run).

Also verify LaMalfa/Swalwell politician_sources rows:

  SELECT p.full_name, ps.source_system, ps.external_id, ps.research_status
  FROM essentials.politicians p
  JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id
  WHERE p.full_name IN ('Doug LaMalfa', 'Eric Swalwell');

Expected: 2 rows, source_system = 'fec_house', research_status = 'confirmed', non-empty external_id starting with 'H'.
  </action>
  <verify>
    <automated>cd backend && npx tsx scripts/fix-fec-name-mismatches.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Script exits with code 0
    - Console output contains 6 or more `[OK]` lines (one per successfully written politician)
    - Console output contains `DIRECT    Doug LaMalfa` and `DIRECT    Eric Swalwell` lines (or MATCH if their finance was already populated — verify DB instead)
    - Console output does NOT contain `[skip] No committee for` for Ivey, Self, Warnock, Cruz, LaMalfa, or Swalwell
    - SQL query: `SELECT COUNT(*) FROM essentials.politicians WHERE full_name IN ('Glenn Ivey', 'Keith Self', 'Raphael Warnock', 'Ted Cruz', 'Doug LaMalfa', 'Eric Swalwell') AND finance_summary IS NOT NULL` returns 6
    - SQL query on politician_sources: both LaMalfa and Swalwell have rows with research_status = 'confirmed' and external_id starting with 'H'
    - SQL query for NULL finance count returns a value <= 38 (measurable reduction from ~40 baseline, target ~34)
    - Final stats JSON printed by script shows `written >= 6` and `error = 0`
  </acceptance_criteria>
  <done>Script ran without errors. finance_summary populated for all 6 target politicians. NULL federal finance count measurably reduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → FEC API | Outbound HTTP — FEC responses are external data; all field access is via optional chaining |
| FEC response → DB | FEC API JSON ingested into PostgreSQL — SQL injection vector if values string-interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | FEC response data → pool.query() INSERT/UPDATE | mitigate | All pool.query() calls use parameterized queries ($1, $2, etc.); employer names in finance_summary written as `JSON.stringify(summary)` with `::jsonb` cast — never string-interpolated |
| T-114-02 | Information Disclosure | FEC_API_KEY in console logs | mitigate | Log only `apiKey.slice(0,8)...` per established pattern in ehn-fec-finance.ts line 235; never log the full key |
| T-114-03 | Denial of Service | Rate limit exhaustion on FEC API | accept | 2000ms sleep between every call; ~44 calls max for Phase 114 scope; well within 1000 req/hr limit — no mitigation needed |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all dependencies already in backend/package.json; no install step in this plan |
</threat_model>

<verification>
After Task 2 completes, run these checks:

1. finance_summary populated for all 6 targets:
   SELECT p.full_name, p.finance_summary IS NOT NULL, p.finance_summary->>'cycle'
   FROM essentials.politicians p
   WHERE p.full_name IN ('Glenn Ivey','Keith Self','Raphael Warnock','Ted Cruz','Doug LaMalfa','Eric Swalwell')
   ORDER BY p.full_name;
   → 6 rows, all IS NOT NULL = true

2. LaMalfa/Swalwell politician_sources rows confirmed:
   SELECT p.full_name, ps.source_system, ps.external_id, ps.research_status
   FROM essentials.politicians p
   JOIN transparent_motivations.politician_sources ps ON ps.essentials_politician_id = p.id
   WHERE p.full_name IN ('Doug LaMalfa','Eric Swalwell');
   → 2 rows, source_system='fec_house', research_status='confirmed', external_id LIKE 'H%'

3. NULL count reduced:
   SELECT COUNT(*) FROM essentials.politicians p
   JOIN essentials.offices o ON o.politician_id = p.id
   JOIN essentials.districts d ON d.id = o.district_id
   WHERE d.district_type IN ('NATIONAL_UPPER','NATIONAL_LOWER')
     AND p.is_active = true AND p.finance_summary IS NULL;
   → value <= 38 (target ~34)

4. Script structure check (no regressions):
   tsx backend/scripts/fix-fec-name-mismatches.ts --dry-run
   → exits 0, prints MATCH lines for known politicians
</verification>

<success_criteria>
1. `fix-fec-name-mismatches.ts` contains committee fallback (`candidate/${fecId}/committees/`), multi-cycle totals loop, and `resolveViaDirectSearch()` — FECF-01 complete.
2. Glenn Ivey, Keith Self, Raphael Warnock, and Ted Cruz each have non-null `finance_summary` on `essentials.politicians` — FECF-02 complete.
3. Doug LaMalfa and Eric Swalwell each have non-null `finance_summary` and a `transparent_motivations.politician_sources` row with `research_status = 'confirmed'` and `external_id` starting with 'H' — FECF-03 complete.
4. NULL `finance_summary` count for active federal politicians drops from ~40 to approximately 34 — measurable milestone progress.
</success_criteria>

<output>
Create `.planning/phases/114-fec-script-fix-and-sitting-members/114-01-SUMMARY.md` when done.

The SUMMARY should include:
- Script console output (MATCH/DIRECT/[OK] lines and final stats JSON)
- Results of all 3 SQL verification queries above
- Any politicians that printed [skip] or NO_MATCH (residual gaps for Phase 115)
</output>
