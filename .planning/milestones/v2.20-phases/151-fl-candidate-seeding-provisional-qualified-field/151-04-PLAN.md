---
phase: 151-fl-candidate-seeding-provisional-qualified-field
plan: 04
type: execute
wave: 3
depends_on: ["151-03"]
files_modified:
  - backend/scripts/seed-fl-house-headshots.py
  - backend/data/seed-fl-2026-house/151-04-fl-headshots.manual.txt
autonomous: true
requirements: [USHC-04]
must_haves:
  truths:
    - "Every one of the 17 FL independent/NPA new candidates has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row, or is a documented honest-skip pinned by external_id"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced); the 27 incumbents + partisan new candidates are NOT in scope (D-01)"
  artifacts:
    - path: "backend/scripts/seed-fl-house-headshots.py"
      provides: "Headshot pipeline targeting the 17-independent external_id list (clone of seed-tx-ny-house-headshots.py)"
    - path: "backend/data/seed-fl-2026-house/151-04-fl-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source independent portraits where Wikipedia has none"
  key_links:
    - from: "the 17-independent negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-fl-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every one of the 17 FL independent/NPA new candidates a headshot following the established find-headshots conventions (600x750, free-license, Storage-mirrored + politician_images row), reusing the validated Phase 149/150 headshot pipeline. ONLY the 17 independents are in scope this phase (D-01 records-now/stances-at-153) — the 27 incumbents already have headshots and the ~138 partisan new candidates get theirs in Phase 153.

Purpose: USHC-04 (FL slice, 17 independents). race_candidates resolve photos via `COALESCE(rc.photo_url, pi.url)` lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`.
Output: The FL headshot script + a manual-source list; every one of the 17 independents has an image row or a documented honest-skip. Independents are often thin-sourced (the 149/150 pattern) — expect several honest-skips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md
@backend/scripts/seed-tx-ny-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the headshot pipeline to target the 17-independent band + run it for FL</name>
  <read_first>
    - backend/scripts/seed-tx-ny-house-headshots.py (the validated Phase-149/150 pipeline: Wikipedia pageimages -> free-license check -> hardened wrong-person guard -> 4:5 crop -> 600x750 LANCZOS q90 -> PUT politician_photos/{uuid}-headshot.jpg x-upsert -> INSERT politician_images WHERE NOT EXISTS; targets by explicit negative external_id list; `--manual` reads external_id|image_url|license)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md (the authoritative 17-independent external_id list)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md (the TX/NY headshot run: auto-pass rate, manual/honest-skip pattern, wrong-person guard hardening)
  </read_first>
  <action>
    Clone `seed-tx-ny-house-headshots.py` to `backend/scripts/seed-fl-house-headshots.py`. Swap ONLY the target-selection: accept the explicit 17-independent external_id list from 151-03-SUMMARY (the new -12NNXX band entries flagged as independents). The 17 are: FL-1 Tyler Davis, FL-3 Mike Klein, FL-4 Todd Schaefer, FL-6 Andrew Parrott, FL-6 Alec Pavlik, FL-12 Branden Scrivener, FL-13 Tony D'Arrigo, FL-16 Mark Davis, FL-17 Michael Quirk, FL-18 Deva Simmons, FL-19 Seth Haskins, FL-20 Kedner MaximeDe, FL-21 Alexander Cooke, FL-24 Andy Daro, FL-24 Patricia Gonzalez, FL-26 Deborah Ann Meidinger Hosey, FL-28 Eddy Rojas. Because new challengers have NO essentials.offices row, the target query selects by explicit negative external_id list `AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id = p.id)`. Keep EVERY guard intact: free-license-only, hardened wrong-person guard (title must contain candidate first+surname; reject election/place/event/non-political disambiguators), 4:5 crop -> 600x750, x-upsert, idempotent INSERT.

    Run the automatic Wikipedia pass: `python backend/scripts/seed-fl-house-headshots.py` with the 17-independent external_id list. For candidates with no free Wikipedia portrait, build `backend/data/seed-fl-2026-house/151-04-fl-headshots.manual.txt` with `external_id|image_url_or_path|license` lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL — MEMORY one-try rule), then run with `--manual backend/data/seed-fl-2026-house/151-04-fl-headshots.manual.txt`.

    For any independent with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 151-verify.sql can pin it — NEVER a placeholder or wrong-person image. Independents are commonly image-less; honest-skip is expected and acceptable.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md','utf8'); const ids=[...new Set([...sum.matchAll(/-12[0-9]{5}/g)].map(m=>+m[0]))]; if(!ids.length){console.error('no new FL external_ids found in 151-03-SUMMARY (check band)');process.exit(1)}; const r=await pool.query('SELECT p.external_id, p.full_name FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('FL new candidates still missing headshot (verify all are documented honest-skips or out-of-17-scope):', r.rows.map(x=>x.external_id+' '+x.full_name)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-fl-house-headshots.py targets the 17-independent set by explicit external_id list (not the offices-join query) and keeps all original guards.
    - Every one of the 17 independents has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed; wrong-person guard intact.
    - The 27 incumbents + partisan new candidates are NOT processed (out of scope per D-01).
    - Re-running inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script created + run for the 17 FL independents; the missing-headshot query returns only documented honest-skips (within the 17-scope).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) -> Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-16 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained from seed-tx-ny-house-headshots.py; manual sources human-verified; spot-check in 151-06 |
| T-151-17 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-151-18 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-151-19 | Repudiation | a partisan/incumbent candidate accidentally imaged (out of D-01 scope) | mitigate | Target list = exactly the 17 independents' external_ids; no offices-join selection |
| T-151-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips within the 17-scope.
- Manual spot-check (151-06): sample FL independent headshots against source pages for identity.
</verification>

<success_criteria>
- Every one of the 17 FL independents has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact.
</success_criteria>

<output>
Create `.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-04-SUMMARY.md` when done (record: independents imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs, the script path).
</output>
