---
phase: 151-fl-candidate-seeding-provisional-qualified-field
plan: 03
type: execute
wave: 2
depends_on: ["151-01"]
files_modified:
  - backend/migrations/1116_seed_fl_2026_house_candidates.sql
  - backend/data/seed-fl-2026-house/151-03-fl-reconciliation.csv
autonomous: true
requirements: [USHC-02, USHC-03]
must_haves:
  truths:
    - "Every one of the 28 FL US House races carries its full provisional Nov-3 active race_candidates field (every qualified candidate — multiple same-party allowed), each row non-null politician_id, candidate_status='active'"
    - "Exactly 3 reuse targets — Lois Frankel (-12022) active in FL-23, Jared Moskowitz (-12023) active in FL-25, Debbie Wasserman Schultz (-12025) active in FL-20 — reuse their existing pids; ALL other 'new' names (incl. Sheila Cherfilus-McCormick, FL-20) are created NEW"
    - "Sitting renominated FL incumbents reuse their 148 incumbent_pid with is_incumbent=true; retired/redistricted incumbents get NO active row in the seat they left"
    - "Zero duplicate full_name within FL; no party stored on any candidate card; ~155 new politician records created"
  artifacts:
    - path: "backend/migrations/1116_seed_fl_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT ~155 new FL politicians + INSERT race_candidates onto the 28 FL races authored in 151-01"
      contains: "race_candidates"
    - path: "backend/data/seed-fl-2026-house/151-03-fl-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 28 FL districts (~209 rows)"
      min_lines: 200
  key_links:
    - from: "race_candidates.race_id"
      to: "the 28 FL House races (151-01 scaffold)"
      via: "office_id -> districts.geo_id 12NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (148-incumbent-map.csv) / reuse pid (3 targets) / newly-inserted politician id"
      via: "151-03-fl-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full provisional Nov-3 general field onto all 28 FL US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for the 3 cross-district redistricted incumbents + every sitting incumbent — after a MANDATORY per-candidate live-DB dedup check (D-03).

Purpose: USHC-02 + USHC-03 for FL. FL is the partial-incumbent state (like NY) with a crowded pre-primary field (~209 qualified candidates, 155 new). The dedup risk is the cross-district redistricting set (Frankel/Moskowitz/Wasserman Schultz) + the FEC-committee-noise false-match trap (Pitfall 2) + the Cherfilus-is-actually-NEW correction (Pitfall 3).
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every FL House race surfaces its full provisional field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-01-SUMMARY.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates VERIFIED LIVE SCHEMA (150-03):
     id uuid PK; race_id uuid NOT NULL FK->races; politician_id uuid (D-05: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL -> feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite source_url); external_id text optional. NO unique constraint -> idempotency application-enforced:
     (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool.
     NO updated_at column (149-01 Rule-3 fix). NO essentials.offices row for challengers (would pollute reps feed — 149-01 A2). -->
<!-- D-03 LIVE PRE-CHECK (RESEARCH-confirmed live 2026-06-29) — the ONLY 3 reuse targets, matched by EXACT external_id:
     - Lois Frankel  -> b4040115-b3ea-4500-89cf-1ddaecafc94e (ext -12022, FL-22 incumbent) -> REUSE as active FL-23 candidate, is_incumbent=false. FL-22 own field is all-new; Frankel gets NO active row in FL-22.
     - Jared Moskowitz -> 1cb8827c-6ae0-4fcf-884c-94ad2246f15d (ext -12023, FL-23 incumbent) -> REUSE as active FL-25 candidate, is_incumbent=false. FL-23 own field is all-new; Moskowitz gets NO active row in FL-23.
     - Debbie Wasserman Schultz -> 097623b0-3063-4943-a13c-89d213ca5829 (ext -12025, FL-25 incumbent) -> REUSE as active FL-20 candidate, is_incumbent=false. FL-25 own field is all-new; WS gets NO active row in FL-25.
     PITFALL 2: FEC-committee-noise rows ("FRANKEL 4 PV SCHOOLS", "MOSKOWITZ FOR CITY COUNCIL, RON", etc.) exist with NULL external_id -> match ONLY by exact external_id, NEVER name substring. >1 match on a reuse name -> inspect external_id, reject noise. -->
<!-- PITFALL 3 (CONTEXT D-03 ERROR CORRECTED): Sheila Cherfilus-McCormick has 0 DB records (live-verified) -> she is GENUINELY NEW (FL-20). Create a new record; do NOT match the unrelated Dave/Richard/Jennifer/Jessica McCormick rows. Same for Alan Grayson, Madison Cawthorn, Kendrick Meek, Shevrin Jones, Scott Singer, Oliver Gilbert, Carla Spalding (all 0 DB records -> all NEW). RE-CONFIRM each live. -->
<!-- FL retired/redistricted incumbents keep their record, get NO active row in their OLD seat:
     FL-2 Dunn (-12002 retired), FL-16 Buchanan (-12016 retired), FL-19 Donalds (-12019 retired->Gov run),
     FL-24 Wilson (-12024 retired); FL-22 Frankel / FL-23 Moskowitz / FL-25 Wasserman Schultz (redistricted-away).
     FL-20 (1220) was VACANT (no incumbent) -> entire field is candidates (incl. the 3 reuse + Cherfilus NEW). -->
<!-- The 17 INDEPENDENTS (RESEARCH Q4 — created NEW here, stanced in 151-04/05): FL-1 Tyler Davis, FL-3 Mike Klein,
     FL-4 Todd Schaefer, FL-6 Andrew Parrott + Alec Pavlik, FL-12 Branden Scrivener, FL-13 Tony D'Arrigo,
     FL-16 Mark Davis, FL-17 Michael Quirk, FL-18 Deva Simmons, FL-19 Seth Haskins, FL-20 Kedner MaximeDe,
     FL-21 Alexander Cooke, FL-24 Andy Daro + Patricia Gonzalez, FL-26 Deborah Ann Meidinger Hosey, FL-28 Eddy Rojas. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per FL candidate across all 28 districts (D-03 mandatory)</name>
  <read_first>
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (FL rows geo 1201..1228: general_candidates = full party-labeled qualified field; new_records_needed; nominee_status; incumbent_pid per geo)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv (FL geo 12NN -> incumbent_pid + external_id -12NN, all 27 incumbents + FL-20 VACANT)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md (the validated reuse-vs-new reconciliation pattern + external_id-band-collision lesson)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md (Pitfall 2 FEC-noise; Pitfall 3 Cherfilus-NEW + 0-record names; external_id band -12NNXX verified empty)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md (D-02 seed-every-qualified incl. multi-same-party; D-03 dedup + 3 reuse; D-05 retired/redistricted-no-active-row)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-fl-2026-house/151-03-fl-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per qualified general candidate across all 28 FL districts (~209 rows; resolve race_id by joining the 151-01 FL races to districts.geo_id 12NN). Parse EVERY candidate from the 148 `general_candidates` field (party in parens read for context only, NEVER stored — D-05). Seed every listed qualified candidate incl. multiple same-party (D-02) — do NOT collapse or pre-prune.

    For EACH candidate:
    1. If the name matches the district's renominated SITTING FL incumbent (148-incumbent-map) AND that incumbent is NOT retired/redistricted -> REUSE, target = the 148 incumbent_pid, is_incumbent=true. (Renominated incumbents: FL-1 Patronis, FL-3 Cammack, FL-4 Bean, FL-5 Rutherford, FL-6 Fine, FL-7 Mills, FL-8 Haridopolos, FL-9 Soto, FL-10 Frost, FL-11 Webster, FL-12 Bilirakis, FL-13 Luna, FL-14 Castor, FL-15 Lee, FL-17 Steube, FL-18 Franklin, FL-21 Mast, FL-26 Diaz-Balart, FL-27 Salazar, FL-28 Giménez — verify each against the field.)
    2. CROSS-DISTRICT REUSE (D-03) — the ONLY 3: Frankel -> REUSE -12022 (b4040115…) as active FL-23, is_incumbent=false; Moskowitz -> REUSE -12023 (1cb8827c…) as active FL-25, is_incumbent=false; Wasserman Schultz -> REUSE -12025 (097623b0…) as active FL-20, is_incumbent=false. Match by EXACT external_id, never name substring (Pitfall 2). Their OLD seats (FL-22/FL-23/FL-25) get all-new fields and NO active row for the departed incumbent.
    3. For every other name (every `new_records_needed` entry + any name not matching a sitting home incumbent) -> run a LIVE name query against essentials.politicians (`full_name ILIKE` + sanity on external_id/office context). If a real matching POLITICIAN record exists (not an FEC committee row) -> flip to REUSE with that UUID. If 0 matching politician records (e.g. Cherfilus-McCormick, Grayson, Cawthorn, Meek, S.Jones, Singer, Gilbert, Spalding all returned 0 live per RESEARCH) -> decision=NEW, assign a collision-free negative external_id.
    4. Retired/redistricted incumbents (FL-2 Dunn, FL-16 Buchanan, FL-19 Donalds, FL-24 Wilson retired; FL-22 Frankel / FL-23 Moskowitz / FL-25 Wasserman Schultz redistricted-away) get NO active race_candidates row in their old seat — confirm their old seat's field is the listed (all-new) candidates.

    For new external_ids: query `SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id` to confirm the band is empty. FL incumbents occupy -12001..-12028. Use `-(1210000 + cd*100 + seq)` (e.g. FL-1 first new = -1210101; FL-20 first new = -1220001-style — pick a consistent NN*100+seq scheme; FL-20 e.g. -1220001..). VERIFY the -1229999..-1210000 band is empty (0 rows) before use; if occupied, pick another verified-empty band and document it (149-01 collision lesson). The 17 independents (RESEARCH Q4) are all NEW — flag a column or note so 151-04/05 can extract exactly those 17 external_ids for the stance/headshot in-scope set.

    Re-confirm: every FL district's active field matches the 148 field (multi-same-party preserved; retired/redistricted incumbents absent from old seat; the 3 reuse present in new seat; Cherfilus NEW in FL-20).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-fl-2026-house/151-03-fl-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<200){console.error('FAIL row count',body.length);process.exit(1)}; const rows=body.map(r=>r.split(',')); const newids=rows.filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; const reuse=rows.filter(c=>c[5]==='REUSE'); const cross=reuse.filter(c=>['b4040115-b3ea-4500-89cf-1ddaecafc94e','1cb8827c-6ae0-4fcf-884c-94ad2246f15d','097623b0-3063-4943-a13c-89d213ca5829'].includes(c[6])); if(cross.length!==3){console.error('FAIL expected exactly 3 cross-district reuse targets, got',cross.length);process.exit(1)}; const cherf=rows.find(c=>/cherfilus/i.test(c[3])); if(!cherf||cherf[5]!=='NEW'){console.error('FAIL Cherfilus must be NEW',cherf);process.exit(1)}; console.log('FL reconciliation OK rows',body.length,'new',newids.length,'cross-reuse 3, Cherfilus NEW')"</automated>
  </verify>
  <acceptance_criteria>
    - 151-03-fl-reconciliation.csv has one row per FL general candidate across 28 districts (~209 rows), each with a resolved race_id.
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred); the 3 cross-district reuse pids matched by exact external_id (no FEC-noise match).
    - Cherfilus-McCormick = NEW in FL-20 (live 0-match confirmed); all other 0-record names NEW; every sitting renominated incumbent REUSE (is_incumbent=true).
    - Retired/redistricted incumbents (FL-2/16/19/24 + FL-22/23/25) have NO REUSE row in their OLD seat.
    - Assigned NEW external_ids unique within the CSV and collision-free vs the live `external_id < 0` set; the -12NNXX band verified empty; the 17 independents' new external_ids flagged for 151-04/05.
    - Multi-same-party fields preserved (no collapse/prune) — D-02.
  </acceptance_criteria>
  <done>fl-reconciliation.csv exists, all REUSE UUIDs live-confirmed, 3 cross-district reuse + Cherfilus-NEW resolved, retired/redistricted incumbents absent from old seats, external_id band verified empty, 17-independent external_ids flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent FL House seed migration (~155 new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-fl-2026-house/151-03-fl-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map)
    - backend/migrations/1110_seed_tx_2026_house_candidates.sql (or the 150 TX/NY candidate seed migration — the validated idempotent BEGIN/COMMIT precedent: INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at; no offices rows for challengers)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-01-SUMMARY.md (the FL election UUID + FL-20 office UUID — confirm races resolve)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md (D-05 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card)
  </read_first>
  <action>
    Pick the next free migration number (`ls backend/migrations | sort -t_ -k1 -n | tail -1`, increment — expected `1116`; disk wins). Name `1116_seed_fl_2026_house_candidates.sql`. All writes in one `BEGIN; … COMMIT;`. Header cites the field source (148-field-table.csv FL section, Wikipedia per-district URLs; DoE extract verified empty per RESEARCH Q1) + the antipartisan invariant (D-05).

    Migration body, in order:
    1. INSERT the ~155 new FL politicians: `INSERT INTO essentials.politicians (id, external_id, full_name, first_name, last_name, is_active) SELECT gen_random_uuid(), v.ext::int, v.full_name, v.first, v.last, true FROM (VALUES …) v(…) WHERE NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id=v.ext::int)`. Canonical accents preserved (e.g. Giménez). NO essentials.offices rows for challengers (feed resolves via race_candidates.politician_id; an office row pollutes the reps feed / geofence search — 149-01 A2). Do NOT touch the FL-20 office (151-01 owns it; it stays politician_id NULL — challengers are wired via race_candidates, not the office).
    2. INSERT the race_candidates onto the 28 FL races: for each reconciliation row, `race_id`=resolved FL race, `politician_id`=REUSE target or just-inserted new UUID (resolve new via `(SELECT id FROM essentials.politicians WHERE external_id=…)`), `full_name`, `first_name`, `last_name`, `is_incumbent` per CSV, `candidate_status='active'`, `source`=district source_url. Guard each `WHERE NOT EXISTS (SELECT 1 FROM essentials.race_candidates rc WHERE rc.race_id=v.race_id::uuid AND rc.full_name=v.full_name)`.
    3. NEVER touch the `races` rows (151-01 owns them, incl. the PROVISIONAL description); NEVER set office_id NULL; NEVER store party on a candidate.

    Post-COMMIT verification block (or run separately), scoped to FL NATIONAL_LOWER (geo prefix '12') within the FL 2026 Statewide General election: every FL race has >=1 active candidate; 0 NULL politician_id; 0 duplicate full_name within FL; the 3 cross-district reuse pids active in their new seats (Frankel FL-23, Moskowitz FL-25, Wasserman Schultz FL-20); retired/redistricted incumbents (-12002/-12016/-12019/-12024/-12022/-12023/-12025) have 0 active rows in their OLD seats; Cherfilus active in FL-20 with a new-band external_id.

    Run dry first (review SQL), then apply against prod via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/1116_…sql`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='FL 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='12'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='FL 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='12' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==28||+row.nullpid>0||+dup.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n);process.exit(1)}; console.log('FL OK: 28 races,',row.active_rc,'active rc, 0 null pid, 0 dup full_name'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 28 FL House races (NATIONAL_LOWER geo '12' within the FL 2026 Statewide General) carry their full provisional active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within FL (D-03).
    - Frankel active FL-23, Moskowitz active FL-25, Wasserman Schultz active FL-20 (all is_incumbent=false, reused pids); Cherfilus active FL-20 (new record); retired/redistricted incumbents have 0 active rows in their old seats.
    - Multi-same-party fields present (D-02); no new essentials.offices rows; no party stored on any candidate; FL-20 office stays politician_id NULL.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; FL 28-race assertion passes; dedup correct; idempotent on re-run. Record the ~155 new-candidate external_id list AND the 17-independent external_ids in the SUMMARY for the headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor -> production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes ~155 new FL politicians + race_candidates to live prod |
| 148 field artifacts -> seed input | Field/identity locked + human-reviewed in Phase 148; reuse-vs-new re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-10 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Live name-check per candidate; reuse 148 incumbent_pid; 3 cross-district reuse by exact external_id; 0-dup-full_name gate |
| T-151-11 | Spoofing | FEC-committee-noise row reused as a candidate pid (Pitfall 2) | mitigate | Reuse matched by exact external_id (-12022/-12023/-12025), never name substring; >1-match inspection rule |
| T-151-12 | Repudiation | Cherfilus orphaned by treating as reuse (Pitfall 3) | mitigate | Cherfilus + all 0-record names created NEW; live 0-match re-confirmed in Task 1 |
| T-151-13 | Repudiation | retired/redistricted incumbent surfaced as active in old seat | mitigate | Old-seat incumbents get NO active row; gate lost-incumbent-absent assertion |
| T-151-14 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-151-15 | DoS | write to wrong election / office contamination | mitigate | All inserts scoped to FL 2026 Statewide General + NATIONAL_LOWER geo '12'; no offices rows for challengers |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 151-verify.sql USHC-02/03 + D-03/D-05 FL assertions pass after this runs.
- Manual spot-check: cross-check ~5 FL districts (incl. crowded FL-19, FL-20 open seat) against 148 source_url citations.
</verification>

<success_criteria>
- 28 FL House races each carry their full provisional active field, all politician_id non-null; ~155 new politician rows; the 3 cross-district incumbents + every sitting incumbent reused by id (0 duplicates); Cherfilus new; retired/redistricted incumbents absent from old seats; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md` when done (record: migration number, final NEW count, the 3 cross-district reuse + Cherfilus-NEW resolutions, any 148-name flips, the full new-candidate external_id list, AND the 17-independent external_ids for the headshot/stance in-scope set).
</output>
