---
phase: 151-fl-candidate-seeding-provisional-qualified-field
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/1115_seed_fl_2026_house_elections_races.sql
autonomous: true
requirements: [USHC-03]
must_haves:
  truths:
    - "An essentials.elections row exists for 'FL 2026 Statewide General', election_date 2026-11-03, election_type 'general', jurisdiction_level 'state', state 'FL', mirroring the CA 2026 Statewide General shape"
    - "A new essentials.offices row exists for FL-20 (geo_id 1220 NATIONAL_LOWER), title 'U.S. Representative', chamber US House, politician_id NULL (vacant seat), created BEFORE the FL-20 race insert"
    - "Exactly 28 essentials.races rows exist for FL (one per geo_id 1201..1228), each office_id = that district's existing-or-new U.S. Representative office, never office_id IS NULL"
    - "Every FL House race carries description = 'PROVISIONAL: pre-Aug-18-2026-primary qualified field; pruned to nominees in Phase 153' (per D-04)"
  artifacts:
    - path: "backend/migrations/1115_seed_fl_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT 1 election + 1 FL-20 office + 28 provisional races on FL House offices; no race_candidates yet"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing (or newly-created FL-20) NATIONAL_LOWER U.S. Representative office for each FL 12NN geo_id"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "essentials.offices (new FL-20)"
      to: "district da308220-… (geo_id 1220 NATIONAL_LOWER)"
      via: "INSERT … WHERE NOT EXISTS, district_id resolved by geo_id='1220'"
      pattern: "essentials.offices"
---

<objective>
Author the `essentials.elections` + new FL-20 `essentials.offices` + 28 `essentials.races` scaffold that FL requires before any candidates can be wired. Like TX/NY (not CA), FL has ZERO 2026 elections/races (verified live), so this Wave-1 plan creates the election + FL-20 office + per-district race scaffold that Wave 2 wires `race_candidates` onto.

Purpose: USHC-03 prerequisite — the elections-feed surfaces a race only if an `essentials.races` row exists on the district's office. FL adds TWO structural steps vs the CA turnkey path: (1) create the vacant-seat FL-20 office (it has no office row), and (2) mark every race PROVISIONAL via the `description` sentinel (D-04 — pre-Aug-18 crowded field, pruned in Phase 153).
Output: One idempotent migration creating 1 election + 1 FL-20 office + 28 provisional races; `/elections` still shows no field (no candidates yet) but the scaffold is live for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv

<interfaces>
<!-- essentials.elections — VERIFIED LIVE SHAPE (CA 2026 Statewide General row):
     columns: id uuid, name text, election_date timestamptz, election_type text ('general'),
     jurisdiction_level text ('state'), state text, description text NULL, created_at, updated_at.
     CA row: name='CA 2026 Statewide General', election_date='2026-11-03T08:00:00.000Z',
     election_type='general', jurisdiction_level='state', state='CA'. FL row mirrors with state='FL'. -->
<!-- essentials.races — VERIFIED LIVE SHAPE: id uuid, election_id uuid FK->elections, office_id uuid FK->offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL ok), seats int (1),
     description text NULL, created_at, updated_at. NO geo_id / NO status column on races.
     Provisional marking = sentinel string in description (RESEARCH Q3). -->
<!-- essentials.offices — VERIFIED LIVE SHAPE (FL-19 sibling, geo 1219):
     id (gen_random_uuid), politician_id (FL-20 must be NULL — vacant, no incumbent),
     chamber_id='c2facc31-7b13-428c-b7b9-32d0d3b95f76' (US House — single national chamber, resolve by name to be safe),
     district_id=(geo_id='1220' NATIONAL_LOWER -> da308220-76a5-4bb4-a446-8fbe6524ec33),
     title='U.S. Representative', representing_state='FL', representing_city NULL, description NULL, seats NULL,
     normalized_position_name NULL, partisan_type NULL, salary NULL, is_appointed_position=false,
     is_vacant=true (RESEARCH Q2 recommend true; sibling shows false because filled), vacant_since NULL,
     faces_retention_vote=false, role_canonical NULL. -->
<!-- VERIFIED LIVE (RESEARCH): 0 essentials.races on any FL NATIONAL_LOWER office; no 'FL 2026 Statewide General'
     election exists. 27 FL House offices exist (geo 1201..1219, 1221..1228); FL-20 (geo 1220) has 0 offices ->
     CREATE FIRST. All 28 FL NATIONAL_LOWER district rows (geo 1201..1228) exist — NEVER create a district. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the FL elections + FL-20 office + 28 provisional races scaffold migration</name>
  <read_first>
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (28 FL rows: geo_id 1201..1228, cd for position_name, target_election='FL 2026 Statewide General')
    - backend/migrations/1109_seed_tx_ny_2026_house_elections_races.sql (the TX/NY create-races-first precedent: idempotent BEGIN/COMMIT, NOT EXISTS guards, office-by-geo_id join, post-COMMIT asserts) — the DIRECT FL analog
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md (Q2 FL-20 office spec table; Q3 description sentinel; Wave-1 migration order; Pitfall 4 office-before-race)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md (D-04 provisional marking; D-05 office_id NEVER NULL; FL-20 office-creation trap)
  </read_first>
  <action>
    Use migration number `1115` (latest on disk is 1114; confirm with `ls backend/migrations | sort -t_ -k1 -n | tail -1` and increment if disk has moved). Name `1115_seed_fl_2026_house_elections_races.sql`. All writes inside one `BEGIN; … COMMIT;`. Header comment cites the field source — per RESEARCH Q1 the FL DoE `extractCanList.asp` extract is fetchable but EMPTY for 2026 federal today, so `148-field-table.csv` (Wikipedia-sourced) is the documented source-of-truth for this phase; cite that explicitly. Also cite the antipartisan invariant (D-05: party is NOT stored on race_candidates; races.primary_party stays NULL — a multi-candidate provisional general has no single primary party).

    Migration body, in STRICT order (Pitfall 4 — office must exist before the FL-20 race):
    1. INSERT the FL election idempotently, mirroring the CA row exactly: `INSERT INTO essentials.elections (id, name, election_date, election_type, jurisdiction_level, state) SELECT gen_random_uuid(), 'FL 2026 Statewide General', '2026-11-03T08:00:00.000Z'::timestamptz, 'general', 'state', 'FL' WHERE NOT EXISTS (SELECT 1 FROM essentials.elections e WHERE e.name='FL 2026 Statewide General')`. Leave description NULL.
    2. CREATE the FL-20 office (per RESEARCH Q2 spec): `INSERT INTO essentials.offices (id, politician_id, chamber_id, district_id, title, representing_state, is_appointed_position, is_vacant, faces_retention_vote) SELECT gen_random_uuid(), NULL, (SELECT id FROM essentials.chambers WHERE name ILIKE 'U.S. House%' OR name ILIKE '%US House%' LIMIT 1), d.id, 'U.S. Representative', 'FL', false, true, false FROM essentials.districts d WHERE d.geo_id='1220' AND d.district_type='NATIONAL_LOWER' AND NOT EXISTS (SELECT 1 FROM essentials.offices o JOIN essentials.districts dd ON dd.id=o.district_id WHERE dd.geo_id='1220' AND dd.district_type='NATIONAL_LOWER')`. politician_id MUST be NULL (vacant seat). Resolve chamber_id by name; if the name lookup is ambiguous, fall back to the verified literal `'c2facc31-7b13-428c-b7b9-32d0d3b95f76'` (the single US House chamber all 27 FL offices share) and document the choice in the header.
    3. INSERT one race per FL House district (geo 1201..1228) onto the EXISTING-or-just-created office, resolving office_id by the join `essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND d.geo_id=v.geo_id` (now including the new FL-20 office). For each of the 28 geo_ids: `election_id` = the FL election (resolve by name), `office_id` = the looked-up office, `position_name='U.S. Representative District '||cd`, `primary_party=NULL`, `seats=1`, `description='PROVISIONAL: pre-Aug-18-2026-primary qualified field; pruned to nominees in Phase 153'`. Guard each with `WHERE NOT EXISTS (SELECT 1 FROM essentials.races r WHERE r.election_id=<eid> AND r.office_id=<oid>)` (no DB unique constraint — application-enforced idempotency).
    4. NEVER create a districts row (all 28 exist). NEVER set office_id NULL. NEVER reuse any other FL election. The ONLY new office is FL-20.

    Add a post-COMMIT verification block (or run separately) asserting: exactly 1 'FL 2026 Statewide General' election; exactly 1 NEW FL-20 office (geo 1220, politician_id NULL); exactly 28 races on FL NATIONAL_LOWER offices; 0 races with NULL office_id; every geo_id 1201..1228 has exactly 1 race; all 28 races `description LIKE 'PROVISIONAL:%'`.

    Run dry first (review generated SQL), then apply against prod (`kxsdzaojfaibhuzmclfq`) via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/1115_…sql`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT count(*) n FROM essentials.elections WHERE name='FL 2026 Statewide General'\"); const off=await pool.query(\"SELECT count(*) n, count(*) FILTER (WHERE o.politician_id IS NULL) nullpid FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.geo_id='1220' AND d.district_type='NATIONAL_LOWER'\"); const r=await pool.query(\"SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff, count(*) FILTER (WHERE r.description LIKE 'PROVISIONAL:%') prov, count(DISTINCT d.geo_id) distinct_geos FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='FL 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='12'\"); const row=r.rows[0]; if(+e.rows[0].n!==1){console.error('FAIL election',e.rows[0]);process.exit(1)}; if(+off.rows[0].n!==1||+off.rows[0].nullpid!==1){console.error('FAIL FL-20 office',off.rows[0]);process.exit(1)}; if(+row.races!==28||+row.nulloff>0||+row.prov!==28||+row.distinct_geos!==28){console.error('FAIL races',row);process.exit(1)}; console.log('OK: 1 FL election, FL-20 office (NULL pid), 28 provisional races, 0 null office_id, 28 distinct geos'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 'FL 2026 Statewide General' election row, election_date 2026-11-03, election_type 'general', jurisdiction_level 'state', state 'FL'.
    - Exactly 1 NEW FL-20 office (geo 1220 NATIONAL_LOWER), title 'U.S. Representative', US House chamber, politician_id NULL, created before the FL-20 race insert.
    - Exactly 28 races on FL NATIONAL_LOWER offices (geo 1201..1228); each office_id non-null; every geo has exactly 1 race.
    - All 28 races carry `description LIKE 'PROVISIONAL:%'` (D-04).
    - 0 new districts rows; no FL office created besides FL-20.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven for election, office, and all 28 races).
  </acceptance_criteria>
  <done>Migration applied; 1 election + 1 FL-20 office + 28 provisional races live; verification passes; idempotent on re-run. Record the FL election UUID + the new FL-20 office UUID in the SUMMARY for Wave-2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 1 election + 1 office + 28 races to live prod |
| 148 field artifacts → seed input | geo_id list locked + human-reviewed in Phase 148; DoE extract verified empty (RESEARCH Q1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering | duplicate election/office/race on re-run | mitigate | NOT EXISTS guards on (election name), (FL-20 geo_id office), (election_id, office_id); re-run inserts 0 |
| T-151-02 | Tampering | FL-20 race wired to wrong/NULL office (Pitfall 4) | mitigate | FL-20 office created FIRST in same migration; office_id resolved by geo_id join; post-COMMIT asserts 0 NULL office_id + exactly 28 races |
| T-151-03 | Tampering | FL-20 office given a spurious politician_id (not vacant) | mitigate | politician_id explicitly NULL; post-COMMIT asserts nullpid=1 for geo 1220 |
| T-151-04 | Repudiation | FL field not recorded as provisional (D-04) | mitigate | description sentinel written + asserted (28/28 PROVISIONAL); Phase 153 flips it on prune |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 151-verify.sql (authored in 151-02) USHC-03 race-scaffold + FL-20-office + provisional assertions pass after this runs.
- Manual: spot-check 3 FL races (incl. FL-20) resolve to the correct office by geo_id.
</verification>

<success_criteria>
- 1 election + 1 FL-20 office + 28 provisional races live, all office_id non-null, idempotent.
</success_criteria>

<output>
Create `.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-01-SUMMARY.md` when done (record: migration number, the FL election UUID, the new FL-20 office UUID, the chamber_id used + resolution method, per-district race confirmation, any geo_id that failed office lookup).
</output>
