---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/150-verify.sql
autonomous: true
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
must_haves:
  truths:
    - "A standalone, write-free 150-verify.sql exists asserting every USHC-02/03/04/05 + D-01/D-02/D-03/D-05 success criterion, scoped per-state to the 38 TX + 26 NY House races"
    - "Every assertion is scoped via NATIONAL_LOWER + substr(geo_id,1,2) IN ('48','36') within the TX/NY 2026 Statewide General elections — no cross-state contamination"
    - "Lost-primary incumbents (TX-2 Crenshaw, NY-10 Goldman, NY-13 Espaillat) are asserted ABSENT from the active field; primary winners present"
    - "Any whole-record stance honest-skip and any headshot honest-skip is pinned by exact UUID with the query's exact ORDER BY (143 lesson)"
  artifacts:
    - path: "backend/scripts/150-verify.sql"
      provides: "Read-only labeled-assertion gate for Phase 150 (USHC-02/03/04/05 + decisions), per-state TX/NY-scoped, honest-skip-pinnable"
      contains: "NATIONAL_LOWER"
      min_lines: 120
  key_links:
    - from: "150-verify.sql assertions"
      to: "the 38 TX + 26 NY House races within the TX/NY 2026 Statewide General elections"
      via: "d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('48','36') predicate"
      pattern: "NATIONAL_LOWER"
---

<objective>
Author the read-only `backend/scripts/150-verify.sql` gate (Wave-0 test infrastructure, nyquist) that every Phase 150 wave runs to self-check and that 150-12 runs as the final consolidated proof. It asserts elections/races scaffold (USHC-03), records + dedup (USHC-02), race wiring, headshots (USHC-04), stances (USHC-05), the seed-all-minor-party invariant (D-02), the lost-incumbent-absent invariant (D-05), and zero-duplicate-full_name-per-state (D-03) — scoped per-state to the 38 TX + 26 NY House races.

Purpose: This gate must exist before later waves so each can verify its slice. The trap-prone parts are (a) per-state scoping (TX and NY are separate elections — never cross-count), (b) lost-incumbent absence, (c) the D-01 asymmetry (TX incumbents must be stanced; NY partial incumbents are deliberately untouched and must NOT be required to reach 24).
Output: A standalone, write-free `.sql` gate (the 149-verify.sql precedent, adapted for two states + the create-races structure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-FIELD-TABLE.md
@backend/scripts/149-verify.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 150-verify.sql — per-state TX/NY-scoped labeled assertions for USHC-02/03/04/05 + decisions</name>
  <read_first>
    - backend/scripts/149-verify.sql (the validated precedent: read-only DO-block labeled assertions; CREATE TEMP TABLE … ON COMMIT DROP working sets; _reuse_pid / _d04 / _new_cands / _zero_incumbents / _stance_skip / _fed24 pin-table patterns; the 52-vs-53 and 143-ordering lessons)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-FIELD-TABLE.md (non-incumbent-nominee flag list: TX-2/NY-10/NY-13 lost-primary; the NY-13 3-candidate + NY-21 3-candidate fusion rows for D-02; per-state stance-gap summary TX 37 zero / NY 0 zero 25 partial 1 done)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv (TX/NY incumbent geo_id → incumbent_pid + external_id, for the reuse-pid pin set and the lost-incumbent-absent pins)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md (D-01 asymmetry — TX incumbents stanced, NY partials untouched; D-02 seed-all-minor; D-03 zero-dup-full_name-per-state)
  </read_first>
  <action>
    Write `backend/scripts/150-verify.sql` as a write-free, labeled-assertion gate following 149-verify.sql exactly, but parameterized for TWO states. Resolve both election ids at the top (`SELECT id FROM essentials.elections WHERE name='TX 2026 Statewide General'` / `…NY…`). Build per-state House working sets via the join chain `races r JOIN offices o ON o.id=r.office_id JOIN districts d ON d.id=o.district_id` filtered `r.election_id IN (tx_eid, ny_eid) AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('48','36')`. NEVER write a cross-state or election-wide count.

    Labeled assertions (each `DO $$ … IF NOT (…) THEN RAISE EXCEPTION 'FAIL <label>: %' … ELSE RAISE NOTICE 'PASS <label>'; END $$;` style):
    - USHC-03a-TX / USHC-03a-NY — all 38 TX races (resp. 26 NY) have ≥1 active race_candidate (open generals can be 1-candidate edge cases; assert ≥1, and additionally assert the COUNT of races = 38 TX / 26 NY so no district is missing). [Most have ≥2; do NOT hard-require 2 because a few generals list a single candidate, e.g. FL-10 pattern — but TX/NY fields here are all ≥2; encode ≥2 with a documented allowance note, defaulting to ≥2.]
    - USHC-03b — 0 active race_candidates rows with NULL politician_id (per-state, House-scoped).
    - USHC-02a-TX / USHC-02a-NY — 0 duplicate full_name among ACTIVE candidates WITHIN each state (`GROUP BY lower(full_name) HAVING count(*)>1` over that state's races returns 0) — D-03 zero-duplicate-full_name-per-state.
    - USHC-02c — every renominated/home-running TX & NY incumbent reuses its 148 incumbent_pid (pin a _reuse_pid TEMP table of (geo_id, expected_pid) from 148-incumbent-map.csv for every renominated incumbent + the cross-district reuse cases [Greg Casar reused in TX-37; any live-confirmed reuse from 150-03]; assert each pinned pid appears as an ACTIVE House candidate). Document in a header comment which incumbents are intentionally NOT in the set (lost-primary + retired + redistricted-away).
    - D-05-lost-incumbent — the lost/retired/redistricted-away incumbent pids are ABSENT from the active field where their old seat now has a different nominee: assert TX-2 Crenshaw (deae1b6d…), NY-10 Goldman (c7f357ce…), NY-13 Espaillat (26636234…) each appear 0 times as an active candidate; assert their primary winners (Steve Toth TX-2, Brad Lander NY-10, Darializa Avila Chevalier NY-13) ARE active. (Use the live 150-03 reconciliation pids for the winners; pin by name match within the district as a fallback.)
    - D-02-minor — the named minor-line candidates are seeded: NY-13 Bob Cohen (Working Families) + NY-21 Robert Smullen (Conservative) each appear as an ACTIVE candidate in their district (party-agnostic — assert presence, not party).
    - USHC-04 — every newly-seeded TX/NY candidate (politician_id in the new-candidate external_id band assigned by 150-03) has an essentials.politician_images row; documented headshot honest-skips pinned by exact external_id with explicit ORDER BY.
    - USHC-05a — 0 unsourced stance rows for the in-scope set: 0 = count over inform.politician_answers for the in-scope pids WHERE NOT EXISTS a matching inform.politician_context with a non-empty sources array. **In-scope set (D-01 asymmetry):** the new TX/NY challengers (all) + the 37 TX zero-stance incumbents. **EXCLUDE the 25 NY partial incumbents** (left as-is this phase) and NY-14 AOC (done) — so their pre-existing partial coverage cannot false-fail a 0-unsourced or coverage check.
    - USHC-05b — each in-scope candidate (new challenger + TX zero incumbent) has ≥1 sourced federal stance OR is a pinned whole-record honest-skip (per the [[project-149-stance-gate-standard]] operator standard: ≥1-sourced-OR-pinned, full-24 NOT required; chairs-not-polarity forbids party-inference inflation). Whole-record skips pinned by UUID with exact ORDER BY. NY partial incumbents are NOT in this set (D-01).

    Define the in-scope stance/headshot set + honest-skip pins as TEMP tables at the top (the 149 pattern) so 150-06..11 can run the gate mid-wave. Header comment documents: (a) per-state scoping rule; (b) the D-01 NY-partial-exclusion; (c) data-dependent assertions legitimately FAIL pre-seed and go green only after the waves complete; (d) the 143 honest-skip-ORDER-BY lesson.

    Self-test that the SQL parses + runs read-only against prod NOW (pre-seed). The races scaffold (150-01) may already be live in this wave; USHC-03a may already pass for the races count while candidate/headshot/stance assertions FAIL pre-seed (expected).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^[[:space:]]*--' scripts/150-verify.sql > /tmp/150v.nc && grep -qi 'NATIONAL_LOWER' /tmp/150v.nc && grep -q "'48'" /tmp/150v.nc && grep -q "'36'" /tmp/150v.nc && ! grep -qiE 'DELETE |UPDATE |INSERT[[:space:]]+INTO[[:space:]]+(essentials|inform)' /tmp/150v.nc && echo PASS-scoped-and-write-free</automated>
  </verify>
  <acceptance_criteria>
    - 150-verify.sql contains all labeled assertions above; every data query is per-state House-scoped (NATIONAL_LOWER + geo_id prefix '48'/'36' within the TX/NY 2026 Statewide General elections).
    - The file is write-free: no DELETE/UPDATE/INSERT INTO essentials|inform (TEMP-table CREATE … ON COMMIT DROP is the only allowed write, matching 149-verify.sql).
    - D-01 asymmetry encoded: the 25 NY partial incumbents + NY-14 AOC are EXCLUDED from the USHC-05a/05b in-scope set (header-documented).
    - Lost-incumbent-absent assertions present for TX-2 / NY-10 / NY-13 (pids absent, winners present); D-02 minor-line presence assertions for NY-13 Cohen + NY-21 Smullen.
    - Honest-skip pins use explicit ORDER BY (143 lesson, header comment + pinned-set query).
    - The script parses + runs read-only against prod NOW; data-dependent assertions may FAIL pre-seed (documented as expected).
  </acceptance_criteria>
  <done>150-verify.sql authored, per-state TX/NY-scoped, write-free, all assertions present incl. D-01 asymmetry + lost-incumbent-absent + D-02 minor-line; parses + runs read-only against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → production DB (read) | 150-verify.sql runs SELECT-only against live prod; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-04 | Tampering | gate accidentally mutates prod | mitigate | Write-free enforced by verify check; only TEMP-table writes allowed |
| T-150-05 | Repudiation | gate false-fails on NY partial-incumbent pre-existing data | mitigate | NY partial incumbents + AOC EXCLUDED from in-scope stance set (D-01); header-documented |
| T-150-06 | Repudiation | cross-state contamination false-passes a count | mitigate | Every assertion per-state scoped (geo_id prefix '48'/'36' within the named election) |
| T-150-07 | Repudiation | lost incumbent silently surfaced as active | mitigate | D-05-lost-incumbent asserts TX-2/NY-10/NY-13 pids absent + winners present |
| T-150-08 | Repudiation | honest-skip ordering mismatch false-fails (143 lesson) | mitigate | Honest-skip pins use explicit ORDER BY matching the query |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- The verify automated check confirms per-state scoping + write-free.
- Manual: run the gate against prod pre-seed; confirm it executes (data assertions may FAIL pre-seed, expected).
</verification>

<success_criteria>
- 150-verify.sql exists, write-free, per-state TX/NY-scoped, with all USHC-02/03/04/05 + D-01/D-02/D-03/D-05 labeled assertions; runs read-only against prod without syntax/permission error.
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-02-SUMMARY.md` when done (record: assertion labels, the in-scope-set TEMP-table convention, the NY-partial-exclusion, and how 150-06..11 should invoke it mid-wave).
</output>
