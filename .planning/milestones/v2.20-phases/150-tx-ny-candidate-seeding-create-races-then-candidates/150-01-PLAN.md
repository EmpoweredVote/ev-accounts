---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_tx_ny_2026_house_elections_races.sql
autonomous: true
requirements: [USHC-03]
must_haves:
  truths:
    - "An essentials.elections row exists for 'TX 2026 Statewide General' and one for 'NY 2026 Statewide General', election_date 2026-11-03, mirroring the CA 2026 Statewide General shape"
    - "Exactly 38 essentials.races rows exist for TX (one per geo_id 48NN) and 26 for NY (one per geo_id 36NN), each office_id = that district's existing U.S. Representative office, never office_id IS NULL"
    - "Every TX/NY House race links to the correct existing NATIONAL_LOWER office by geo_id (TX 4801..4838, NY 3601..3626) — no office created, none orphaned"
  artifacts:
    - path: "backend/migrations/NNNN_seed_tx_ny_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT 2 elections + 64 races (38 TX + 26 NY) on existing House offices; no race_candidates yet"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing NATIONAL_LOWER U.S. Representative office for each TX 48NN / NY 36NN geo_id"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "essentials.races.election_id"
      to: "the newly-authored TX / NY 2026 Statewide General election rows"
      via: "INSERT … SELECT id FROM essentials.elections WHERE name=…"
      pattern: "election_id"
---

<objective>
Author the `essentials.elections` + `essentials.races` rows that TX and NY require before any candidates can be wired. CA was turnkey (53 races pre-seeded); TX/NY have ZERO House races (verified live), so this Wave-1 plan creates the election + per-district race scaffold that Wave 2 wires `race_candidates` onto.

Purpose: USHC-03 prerequisite — the elections-feed surfaces a race only if an `essentials.races` row exists on the district's office. This is the ONE structural addition vs Phase 149.
Output: One idempotent migration creating 2 elections + 64 races; `/elections` still shows no field (no candidates yet) but the scaffold is live for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv

<interfaces>
<!-- essentials.elections — VERIFIED LIVE SHAPE (CA 2026 Statewide General row):
     columns: id uuid, name text, election_date timestamptz, election_type text ('general'),
     jurisdiction_level text ('state'), state text ('CA'/'TX'/'NY'), description text NULL,
     created_at, updated_at. CA row: id=728d0074-8a8d-49e3-a68c-78ccdd15434f,
     name='CA 2026 Statewide General', election_date='2026-11-03T08:00:00.000Z',
     election_type='general', jurisdiction_level='state', state='CA'. -->
<!-- essentials.races — VERIFIED LIVE SHAPE (CA House race row):
     columns: id uuid, election_id uuid FK→elections, office_id uuid FK→offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL ok),
     seats int (1), description text NULL, created_at, updated_at. NO geo_id column on races. -->
<!-- TX/NY US Representative offices ALREADY EXIST (verified live): 38 NATIONAL_LOWER offices for
     TX geo_id 4801..4838, 26 for NY geo_id 3601..3626. TX office title = 'Representative';
     NY office title = 'U.S. Representative' (title differs but office_id lookup is by district geo_id).
     TX-23 (4823) office EXISTS despite the seat being vacant. mtfcc='G5200'. NEVER create an office. -->
<!-- VERIFIED LIVE: 0 essentials.races on any TX/NY NATIONAL_LOWER office; no 'TX/NY 2026 Statewide General'
     election exists. (TX has unrelated municipal-general + Longview council elections — do NOT reuse them.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run the TX/NY elections + races scaffold migration</name>
  <read_first>
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (TX rows 54–91 + NY rows: the 38 TX geo_ids 48NN + 26 NY geo_ids 36NN, target_election names, cd for position_name)
    - backend/migrations/1091_seed_ca_2026_house_candidates.sql (the most-recent idempotent BEGIN/COMMIT seed precedent: NOT EXISTS guards, header citing field source + antipartisan invariant)
    - backend/migrations/1072_seed_2026_statewide_general_candidates.sql (pure-SQL idempotent INSERT … SELECT FROM VALUES … WHERE NOT EXISTS general-election precedent)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md (D-04 wave seam; D-05 office_id NEVER NULL; authoring_specifics: mirror CA election shape)
  </read_first>
  <action>
    Pick the next free migration number: `ls backend/migrations | sort -t_ -k1 -n | tail -1` then increment (disk wins over DB MAX; latest on disk is 1108). Name it `NNNN_seed_tx_ny_2026_house_elections_races.sql`. All writes inside one `BEGIN; … COMMIT;`. Header comment cites the field source (148-FIELD-TABLE.md + per-district Wikipedia URLs) and the antipartisan invariant (D-05: party is NOT stored on race_candidates; races.primary_party stays NULL here — there is no single primary party for a multi-candidate general).

    Migration body, in order:
    1. INSERT the 2 elections idempotently: `INSERT INTO essentials.elections (id, name, election_date, election_type, jurisdiction_level, state) SELECT gen_random_uuid(), v.name, '2026-11-03'::timestamptz, 'general', 'state', v.st FROM (VALUES ('TX 2026 Statewide General','TX'),('NY 2026 Statewide General','NY')) v(name,st) WHERE NOT EXISTS (SELECT 1 FROM essentials.elections e WHERE e.name = v.name)`. Mirror the CA row exactly (election_type='general', jurisdiction_level='state'); leave description NULL.
    2. INSERT one race per TX/NY House district onto the EXISTING office, resolving office_id by district geo_id via the join `essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='NATIONAL_LOWER' AND d.geo_id=v.geo_id`. For each of the 38 TX geo_ids (4801..4838) + 26 NY geo_ids (3601..3626): `election_id` = the matching state's just-inserted election (resolve by name), `office_id` = the looked-up office, `position_name='U.S. Representative District '||cd`, `primary_party=NULL`, `seats=1`. Guard each with `WHERE NOT EXISTS (SELECT 1 FROM essentials.races r WHERE r.election_id=<eid> AND r.office_id=<oid>)` (no DB unique constraint — application-enforced idempotency).
    3. NEVER create an essentials.offices or districts row. NEVER set office_id NULL. NEVER reuse the TX municipal-general / Longview council elections.

    Add a post-COMMIT verification block (or run separately) asserting: exactly 1 'TX 2026 Statewide General' + 1 'NY 2026 Statewide General' election; exactly 38 races on TX NATIONAL_LOWER offices + 26 on NY; 0 races with NULL office_id; every TX geo_id 4801..4838 and NY 3601..3626 has exactly 1 race.

    Run dry first (review generated SQL), then apply against prod (`kxsdzaojfaibhuzmclfq`) via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT state, count(*) n FROM essentials.elections WHERE name IN ('TX 2026 Statewide General','NY 2026 Statewide General') GROUP BY state\"); const r=await pool.query(\"SELECT substr(d.geo_id,1,2) fips, count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name IN ('TX 2026 Statewide General','NY 2026 Statewide General') AND d.district_type='NATIONAL_LOWER' GROUP BY 1 ORDER BY 1\"); const tx=r.rows.find(x=>x.fips==='48'); const ny=r.rows.find(x=>x.fips==='36'); if(e.rows.length!==2){console.error('FAIL elections',e.rows);process.exit(1)}; if(!tx||+tx.races!==38){console.error('FAIL TX races',tx);process.exit(1)}; if(!ny||+ny.races!==26){console.error('FAIL NY races',ny);process.exit(1)}; console.log('OK: 2 elections, TX 38 races, NY 26 races, 0 null office_id'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 'TX 2026 Statewide General' + 1 'NY 2026 Statewide General' election row, each election_date 2026-11-03, election_type 'general', jurisdiction_level 'state'.
    - Exactly 38 races on TX NATIONAL_LOWER offices (geo_id 4801..4838) + 26 on NY (3601..3626); each office_id non-null and = the existing district office.
    - 0 races created with NULL office_id; 0 new essentials.offices or districts rows.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven).
  </acceptance_criteria>
  <done>Migration applied; 2 elections + 64 races live; verification passes; idempotent on re-run. Record the two election UUIDs in the SUMMARY for Wave-2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 2 elections + 64 races to live prod |
| 148 field artifacts → seed input | geo_id list locked + human-reviewed in Phase 148 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | duplicate election/race on re-run | mitigate | NOT EXISTS guards on (name) and (election_id, office_id); re-run inserts 0 |
| T-150-02 | Tampering | race wired to wrong/NULL office | mitigate | office_id resolved by district geo_id join; post-COMMIT asserts 0 NULL office_id + exact 38/26 counts |
| T-150-03 | DoS | accidental write to TX municipal-general election | mitigate | All inserts scoped to the two named 2026 Statewide General elections; never touch existing TX elections |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 150-verify.sql (authored in 150-02) USHC-03 race-scaffold assertions pass after this runs.
- Manual: spot-check 3 TX + 3 NY races resolve to the correct office by geo_id.
</verification>

<success_criteria>
- 2 elections + 64 races (38 TX + 26 NY) live, all office_id non-null, idempotent.
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-01-SUMMARY.md` when done (record: migration number, the TX + NY election UUIDs, per-state race count, any geo_id that failed office lookup).
</output>
