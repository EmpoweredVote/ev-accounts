---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 06
type: execute
wave: 3
depends_on: ["150-04", "150-05"]
files_modified:
  - backend/data/seed-ny-2026-house/150-06-ny-headshots.manual.txt
autonomous: true
requirements: [USHC-04]
must_haves:
  truths:
    - "Every newly-seeded NY candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/data/seed-ny-2026-house/150-06-ny-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source NY portraits where Wikipedia has none"
  key_links:
    - from: "new NY candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-tx-ny-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded NY candidate a headshot, reusing the shared `seed-tx-ny-house-headshots.py` pipeline created in 150-05.

Purpose: USHC-04 (NY slice). Reused incumbents (24 renominated + AOC) already have headshots -- only the 150-04 new NY candidates (challengers + lost-primary winners + retired-seat nominees + minor-line candidates) need them.
Output: A NY manual-source list; every new NY candidate has an image row or a documented honest-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md
@backend/scripts/seed-tx-ny-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the shared headshot pipeline for the new NY candidate band</name>
  <read_first>
    - backend/scripts/seed-tx-ny-house-headshots.py (the shared pipeline created in 150-05; invoke with the NY external_id band)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md (the authoritative new NY-candidate external_id list incl. lost-primary winners + minor-line candidates)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md (how the shared script is invoked per state/band)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-03-SUMMARY.md (auto-pass rate expectation + wrong-person guard hardening)
  </read_first>
  <action>
    Run the shared `seed-tx-ny-house-headshots.py` (created in 150-05) for the NY external_id band (from 150-04-SUMMARY). Do NOT re-clone the script. Run the automatic Wikipedia pass first (high-profile NY candidates like Brad Lander, Micah Lasher, Claire Valdez likely have free Commons portraits). For NY candidates with no free Wikipedia portrait, build `backend/data/seed-ny-2026-house/150-06-ny-headshots.manual.txt` with `external_id|image_url_or_path|license` lines from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL), then run with `--manual backend/data/seed-ny-2026-house/150-06-ny-headshots.manual.txt`.

    Keep all guards intact (free-license-only, hardened wrong-person guard, 4:5 -> 600x750, x-upsert, idempotent INSERT). For any NY candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 150-verify.sql can pin it -- NEVER a placeholder or wrong-person image. Pay special attention to minor-line candidates (Bob Cohen, Robert Smullen) which may be thin-source.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md','utf8'); const ids=[...sum.matchAll(/-361[0-9]{4}/g)].map(m=>+m[0]); if(!ids.length){console.error('no new NY external_ids found in 150-04-SUMMARY (check band)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new NY candidates still missing headshot:', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Every new NY candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + reason) in the SUMMARY.
    - The shared script was reused (not re-cloned); all guards intact.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed.
    - Re-running inserts 0 additional image rows (idempotent).
  </acceptance_criteria>
  <done>Headshot pipeline run for NY; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) -> Storage + DB | Untrusted external images fetched, cropped, uploaded via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-20 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained; manual sources human-verified; spot-check in 150-12 |
| T-150-21 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-150-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (150-12): sample new NY headshots against source pages for identity.
</verification>

<success_criteria>
- Every new NY candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all guards intact.
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-06-SUMMARY.md` when done (record: NY candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs).
</output>
