---
phase: 149-ca-candidate-seeding-race-candidates-only-turnkey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_ca_2026_house_candidates.sql
  - backend/data/seed-ca-2026-house/149-01-reconciliation.csv
autonomous: true
requirements: [USHC-02, USHC-03]
must_haves:
  truths:
    - "Every one of the 52 CA US House races carries ≥2 active race_candidates rows, each with a non-null politician_id"
    - "Every renominated/redistricted incumbent who is the 2026 candidate is wired to its EXISTING 148 incumbent_pid — zero new politician rows for a sitting/previously-seeded rep"
    - "The 38 genuinely-new CA candidates each have exactly one new essentials.politicians record with a collision-free negative external_id"
    - "The active duplicate Raul Ruiz record (05349fa0…) is is_active=false; CA-25 is wired to the canonical 5238b298… record"
    - "Both advancers are present in each of the 9 same-party-general districts (8 D-vs-D + CA-40 R-vs-R)"
    - "No candidate card stores party; party is absent from race_candidates entirely"
  artifacts:
    - path: "backend/migrations/NNNN_seed_ca_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT seed: retire Ruiz dup + INSERT 38 new politicians + INSERT 104 race_candidates onto the 52 House races"
      contains: "race_candidates"
    - path: "backend/data/seed-ca-2026-house/149-01-reconciliation.csv"
      provides: "Per-candidate reuse-vs-new decision (live-confirmed) with target politician_id / assigned external_id"
      min_lines: 105
  key_links:
    - from: "race_candidates.race_id"
      to: "the 52 existing_race_id UUIDs in 148-field-table.csv"
      via: "INSERT onto pre-existing CA House races"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (148-incumbent-map.csv) or newly-inserted politician id"
      via: "reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 52 pre-seeded CA US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every incumbent / previously-seeded figure. Resolve the Raul Ruiz CA-25 duplicate first.

Purpose: This is the anchor record+wiring step of the v2.20 seeding pipeline (USHC-02 + USHC-03). The mechanics locked here are inherited by Phases 150/151. Getting reuse-vs-new and the 52-vs-53-race scoping right here prevents the milestone's two costliest traps (duplicate incumbents; surfacing a non-nominee).
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every CA House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-CONTEXT.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-FIELD-TABLE.md

<interfaces>
<!-- essentials.race_candidates — VERIFIED LIVE SCHEMA (149-RESEARCH.md) -->
<!-- id uuid PK; race_id uuid NOT NULL FK→races; politician_id uuid (D-03: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL → feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite field source_url); external_id text (optional). NO unique constraint on (race_id,politician_id) or external_id
     → idempotency is application-enforced: check (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool -->
<!-- CA election: "CA 2026 Statewide General" = 728d0074-8a8d-49e3-a68c-78ccdd15434f; 53 races (52 House @ 0 candidates + 1 Governor @ ~76 candidates — OUT OF SCOPE). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per candidate + resolve Raul Ruiz dedup</name>
  <read_first>
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (CA rows 2–53: general_candidates, new_records_needed, incumbent_pid, existing_race_id per district)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv (geo_id → incumbent_pid)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md (§Runtime State Inventory — Raul Ruiz 3-record state; name-collision findings: Connie Chan -630001, Mai Vang -660017, Scott Wiener -6001011 already exist; §Common Pitfalls 3+4)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "…"`)
  </read_first>
  <action>
    Produce `backend/data/seed-ca-2026-house/149-01-reconciliation.csv` (columns: cd, geo_id, existing_race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 52 CA districts (104 rows).

    For EACH candidate parsed from the 148 `general_candidates` field (party in parens is read for the race, NEVER stored on the candidate per D-03):
    1. If the name matches the district's renominated/redistricted CA-House incumbent → decision=REUSE, target_politician_id = the 148 incumbent_pid, is_incumbent = (nominee_status='incumbent-renominated'). Redistricted incumbents running in their NEW district (Ami Bera→CA-3, Kevin Kiley→CA-6, Ken Calvert→CA-40, Linda Sánchez→CA-41) REUSE their existing pid but is_incumbent=false in the new district.
    2. If the name is in `new_records_needed` → run a LIVE name query against essentials.politicians (full_name ILIKE + jurisdiction/office context) to confirm it is genuinely new (Pitfall 4 — 148 used naive matching; the DB has FEC-committee junk + legitimate homonyms). If a real matching record exists, flip to REUSE with that UUID; otherwise decision=NEW, assign a collision-free negative external_id.
    3. If the name is NOT the home incumbent and NOT in new_records_needed (previously-seeded figure: e.g. Connie Chan, Scott Wiener, Mai Vang, Aisha Wahab, Jacqui Irwin, Joe Males, James Gallagher, Mike McGuire, Eric Ching, Jason Gibbs, April Verlato, Scott Meyers, Larry Thompson, Stephanie Vargas, Angela Gonzales-Torres, Mike Cargile, Houston Brignano, Samantha Mota, Steve Cohen, Marni von Wilpert, Luz Rivas, Nanette Barragán, Ted Lieu) → run the live name query, decision=REUSE with the confirmed UUID. Document the resolved UUID per candidate.

    For new external_ids: query `SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id` and assign a non-colliding scheme `-(6000000 + cd*100 + seq)` (e.g. CA-2 Littau → -6000201); verify 0 collisions against the live set (CA House incumbents occupy -6000301..-6000352 and -100013..-100045 — your scheme uses the -60001xx..-60005xx band; confirm empty).

    Raul Ruiz (D-02): confirm the 3 live records — canonical `5238b298-6004-4bcc-94c2-ee43a9c2999e` (ext -6000325, KEEP, wire CA-25); active duplicate `05349fa0-…` (ext NULL — set is_active=false in Task 2's migration); `eb9448bd-…` (already is_active=false). Confirm `05349fa0` has no race_candidates/stances orphaning to it before retiring. Record the Ruiz resolution in the reconciliation CSV header comment.

    Re-confirm against the live DB that all 52 `existing_race_id` UUIDs in 148-field-table.csv still resolve to a CA-general House race by geo_id (D-03 / Assumption A4) — if any drifted, re-query and update the CSV.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-ca-2026-house/149-01-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<104){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; console.log('reconciliation OK rows',body.length,'new',newids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - reconciliation.csv has 104 candidate rows (one per general candidate across 52 CA districts).
    - Exactly 38 rows are decision=NEW (or fewer if a live name-check flipped a 148 "new" name to REUSE — any reduction is documented in the CSV with the matched UUID).
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred from the CSV).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live `external_id < 0` set.
    - Raul Ruiz resolution recorded: CA-25 → 5238b298…; 05349fa0… flagged for is_active=false; no orphan race_candidates/stances to 05349fa0.
    - All 52 existing_race_id UUIDs re-confirmed live (or updated if drifted).
  </acceptance_criteria>
  <done>reconciliation.csv exists, 104 rows, ≤38 NEW, all REUSE UUIDs live-confirmed, Ruiz resolved, race UUIDs re-confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent CA House seed migration (Ruiz dedup + 38 politicians + 104 race_candidates)</name>
  <read_first>
    - backend/data/seed-ca-2026-house/149-01-reconciliation.csv (Task 1 output — the authoritative reuse/new + external_id + race_id map)
    - backend/migrations/1072_seed_2026_statewide_general_candidates.sql (most-recent precedent — pure-SQL idempotent `INSERT … SELECT FROM (VALUES …) WHERE NOT EXISTS` general-election candidate seed)
    - backend/scripts/ingest-ca-sos-2026-challengers.ts (template: election lookup, idempotent (race_id, full_name) guard, antipartisan no-party-on-candidate header)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md (§Code Examples — race_candidates insert SQL; §Common Pitfalls 1 House-scoping)
  </read_first>
  <action>
    Pick the next free migration number: `ls backend/migrations | sort -t_ -k1 -n | tail -1` then increment (disk wins over DB MAX per project convention). Name it `NNNN_seed_ca_2026_house_candidates.sql`. Put all writes inside one `BEGIN; … COMMIT;`. Header comment must cite the field source (148-FIELD-TABLE.md + Wikipedia per-district URLs) and state the antipartisan invariant (D-03: no party on race_candidates).

    Migration body, in order:
    1. `UPDATE essentials.politicians SET is_active=false WHERE id='05349fa0-…'` (Ruiz active duplicate; NEVER DELETE — D-02).
    2. INSERT the (≤)38 new politicians: `INSERT INTO essentials.politicians (id, external_id, full_name, first_name, last_name, is_active) SELECT gen_random_uuid(), v.ext::int, v.full_name, v.first, v.last, true FROM (VALUES …) v(...) WHERE NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id = v.ext::int)`. Party normalized in the source field is irrelevant — party is NOT a column here; full_name uses canonical accents (Sánchez, Barragán, Dueñas, Soulé). Do NOT create any essentials.offices row for new challengers (A2 — feed resolves via race_candidates.politician_id; an office row would pollute the reps feed / geofence search).
    3. INSERT the 104 race_candidates onto the 52 existing race UUIDs: for each reconciliation row, `race_id=existing_race_id`, `politician_id`=REUSE target or the just-inserted new UUID (resolve new ones by `(SELECT id FROM essentials.politicians WHERE external_id=…)`), `full_name`, `first_name`, `last_name`, `is_incumbent` per CSV, `candidate_status='active'`, `source`=district source_url. Guard each with `WHERE NOT EXISTS (SELECT 1 FROM essentials.race_candidates rc WHERE rc.race_id=v.race_id::uuid AND rc.full_name=v.full_name)` (no DB unique constraint — application-enforced idempotency, Pitfall: re-run must not duplicate).
    4. NEVER touch the `races` rows; NEVER set office_id NULL; NEVER insert onto the 53rd (Governor) race.

    Add a post-COMMIT verification block (or run separately) asserting, scoped to the 52 House races only (`d.district_type='NATIONAL_LOWER'` within election 728d0074): every House race has ≥2 active candidates, 0 NULL politician_id, both same-party advancers present in CA-4/7/11/12/14/29/34/37/40.

    Run dry first (review the generated SQL), then apply against prod (`kxsdzaojfaibhuzmclfq`) via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(*) FILTER (WHERE cands<2) AS under, count(*) FILTER (WHERE nullpid>0) AS nullp FROM (SELECT r.id, count(rc.id) cands, count(*) FILTER (WHERE rc.politician_id IS NULL AND rc.candidate_status='active') nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id AND rc.candidate_status='active' WHERE r.election_id='728d0074-8a8d-49e3-a68c-78ccdd15434f' AND d.district_type='NATIONAL_LOWER' GROUP BY r.id) x\`); const {under,nullp}=r.rows[0]; if(+under>0||+nullp>0){console.error('FAIL under',under,'nullpid',nullp);process.exit(1)}; console.log('52 House races OK: all ≥2 active, 0 null pid'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 52 CA House races (NATIONAL_LOWER within 728d0074) have ≥2 active race_candidates; 0 active rows with NULL politician_id.
    - The 9 same-party-general districts each have BOTH named advancers as active candidates (D-04).
    - `05349fa0…` (Ruiz dup) is_active=false; CA-25 active candidate wired to 5238b298…; exactly one active "Raul Ruiz" in CA.
    - No new essentials.offices rows created for challengers; the Governor race candidate count is unchanged (untouched).
    - Re-running the migration inserts 0 additional rows (idempotency proven by the NOT EXISTS guards).
  </acceptance_criteria>
  <done>Migration applied; the 52-House-race assertion passes; Ruiz deduped; idempotent on re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 38 politicians + 104 race_candidates to live prod |
| 148 field artifacts → seed input | Field/identity sourced from Wikipedia/FEC (external) but locked + human-reviewed in Phase 148 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | race_candidates insert (no unique constraint) | mitigate | Application-enforced idempotency via `(race_id, full_name)` NOT EXISTS guard; re-run inserts 0 rows |
| T-149-02 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Reuse 148 incumbent_pid; live name-check on all NEW; post-seed 0-duplicate-full_name gate (149-11) |
| T-149-03 | Repudiation | wrong/lost nominee surfaced | mitigate | Field locked in 148 (cited source_url per district); nominee_status drives is_incumbent; Ruiz dedup |
| T-149-04 | Denial of Service | accidental write to Governor race / wrong election | mitigate | All assertions + inserts scoped to 52 NATIONAL_LOWER races within 728d0074; never touch the 53rd race |
| T-149-05 | Elevation | hard-DELETE of a record (loss) | mitigate | Ruiz dup retired via is_active=false only (D-02); NEVER hard-DELETE |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (149-RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `149-verify.sql` (authored in 149-02) USHC-02/03 + D-04 assertions all PASS, House-scoped.
- Manual spot-check: cross-check ~5 seeded districts against 148-FIELD-TABLE.md source_url citations.
</verification>

<success_criteria>
- 52 CA House races each ≥2 active candidates, all politician_id non-null.
- ≤38 new politician rows; every incumbent/previously-seeded figure reused by id (0 duplicates).
- Raul Ruiz CA-25 deduped; same-party advancers both present in all 9 districts.
- Migration idempotent; Governor race untouched.
</success_criteria>

<output>
Create `.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md` when done (record: migration number, final NEW count, Ruiz resolution, any 148-name flips, the new-candidate external_id list for downstream headshot/stance plans).
</output>
