---
phase: 152-coordinate-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/152-verify.sql
  - backend/scripts/152-coordinate-smoke.ts
autonomous: true
requirements: [USHC-06]

must_haves:
  truths:
    - "Running 152-verify.sql against prod read-only exits 0 and ends with 'ALL ASSERTIONS PASSED'"
    - "The gate asserts exactly 144 NATIONAL_LOWER races across the four Wave-1 elections (CA 52 / TX 38 / FL 28 / NY 26)"
    - "The gate asserts 0 active race_candidates with NULL politician_id across all 144 districts"
    - "The gate asserts 0 unsourced stance rows across all newly-seeded Wave-1 candidates (existence check, FL-asymmetry-safe)"
    - "The gate asserts 0 duplicate full_name within any state's active field AND 0 politician_id active in 2+ distinct races"
    - "The gate asserts all 28 FL House races carry the PROVISIONAL: description sentinel and FL rows are present"
    - "Running 152-coordinate-smoke.ts against prod read-only exits 0, ends with a COORDINATE SMOKE GREEN line, and surfaces 4/4 states"
    - "For each of CA/TX/FL/NY an in-district test point surfaces exactly 1 House race with its full active field including >=1 challenger (is_incumbent=false)"
  artifacts:
    - path: "backend/scripts/152-verify.sql"
      provides: "Consolidated read-only SQL gate over all 144 Wave-1 House districts"
      contains: "ALL ASSERTIONS PASSED"
    - path: "backend/scripts/152-coordinate-smoke.ts"
      provides: "Four-state coordinate-surfacing smoke mirroring electionService Part A"
      contains: "COORDINATE SMOKE GREEN"
  key_links:
    - from: "backend/scripts/152-verify.sql"
      to: "essentials.races / essentials.race_candidates / essentials.offices / essentials.districts / inform.politician_answers / inform.politician_context"
      via: "election_id IN (4 Wave-1 elections) AND district_type='NATIONAL_LOWER' scoped SELECTs"
      pattern: "NATIONAL_LOWER"
    - from: "backend/scripts/152-coordinate-smoke.ts"
      to: "essentials.geofence_boundaries (ST_Covers) -> districts -> offices -> races -> race_candidates"
      via: "ST_PointOnSurface centroid -> ST_Covers surfacing join (mirrors getElectionsByCoordinate Part A)"
      pattern: "ST_Covers"
---

<objective>
Author the two consolidated, read-only milestone gate artifacts for v2.20 Wave 1 (USHC-06): a single SQL gate (`152-verify.sql`) that proves the milestone-level structural invariants across all 144 Wave-1 House districts, and a TypeScript coordinate-surfacing smoke (`152-coordinate-smoke.ts`) that proves one in-district address per state (CA/TX/FL/NY) resolves to its House race with the full challenger-inclusive candidate field via the live `getElectionsByCoordinate` surfacing join.

Purpose: This is the v2.20 Wave-1 milestone gate — the Phase 144 precedent (`verify-phase-141-144.sql` folded three passing single-phase gates into one consolidated all-states gate). Phases 149/150/151 already seeded all data live in prod; this phase proves the whole thing end-to-end at the full 144-district level without re-pinning every per-candidate skip (those are owned by the three passing per-phase gates).

Output: `backend/scripts/152-verify.sql` + `backend/scripts/152-coordinate-smoke.ts`. NO migrations, NO application code, NO deploy — both scripts are read-only against production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative success criteria for this phase
# ROADMAP.md "#### Phase 152: Coordinate Verification Gate" — 3 success criteria
# REQUIREMENTS.md USHC-06

# Analog gate scripts to model on (READ these before authoring)
@backend/scripts/151-verify.sql
@backend/scripts/150-verify.sql
@backend/scripts/149-verify.sql
@backend/scripts/151-coordinate-smoke.ts
@backend/scripts/149-coordinate-smoke.ts

# Surfacing logic the smoke mirrors (Part A: ST_Covers join, lines 281-330)
@backend/src/lib/electionService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author backend/scripts/152-verify.sql — consolidated read-only milestone gate over all 144 Wave-1 House districts</name>
  <files>backend/scripts/152-verify.sql</files>
  <read_first>
    - backend/scripts/151-verify.sql (the FL-asymmetry handling — single-state provisional gate; THE landmine pattern to fold safely)
    - backend/scripts/150-verify.sql (the multi-state two-election scoping structure — the model for folding multiple elections into one gate)
    - backend/scripts/149-verify.sql (the CA single-state invariant patterns: 0-dup full_name, 0-null-pid, reuse-pid presence)
    - .planning/ROADMAP.md Phase 152 section (the 3 authoritative success criteria each assertion must map to)
  </read_first>
  <action>
    Create `backend/scripts/152-verify.sql` modeled structurally on `150-verify.sql` (multi-election) and `151-verify.sql` (FL-asymmetry), folding the three passing per-phase gates' STRUCTURAL invariants into milestone-level all-144 assertions. Do NOT re-pin per-candidate stance/headshot honest-skips — those are OWNED by 149/150/151-verify.sql (which all pass); add a header comment documenting that granular per-phase coverage+skip pins live in the three per-phase gates and that this gate asserts the milestone INVARIANTS fresh.

    Start with `\set ON_ERROR_STOP on` and a header comment block (mirroring the 151/150 headers) explaining: read-only (SELECT-only + CREATE TEMP TABLE ... ON COMMIT DROP only), the four-election scope, the geo prefixes (CA='06', TX='48', NY='36', FL='12'), the FL-asymmetry-safe existence-check rationale for unsourced stances, and the exact run command. Then a single `DO $$ ... $$` block of labeled `RAISE EXCEPTION` assertions + `RAISE NOTICE` pass lines, ending with `RAISE NOTICE 'ALL ASSERTIONS PASSED'`.

    Resolve the four election ids by exact name into four uuid DECLARE vars: `CA 2026 Statewide General`, `TX 2026 Statewide General`, `FL 2026 Statewide General`, `NY 2026 Statewide General`; RAISE EXCEPTION if any is NULL. (Note: CA's id is the `728d0074%`-prefixed row; resolve CA by exact name like the others — do not rely on the prefix.)

    Build ONE working set TEMP TABLE `_house ON COMMIT DROP` with a `st` column (CASE on election_id → 'CA'/'TX'/'NY'/'FL'), columns race_id, st, geo_id, race_desc (r.description), rc_id, politician_id, full_name, candidate_status, is_incumbent, external_id — joining `essentials.races r` → `essentials.offices o` → `essentials.districts d` LEFT JOIN `essentials.race_candidates rc` LEFT JOIN `essentials.politicians p`, scoped `WHERE r.election_id IN (the 4 eids) AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('06','48','36','12')`. Every assertion reads from `_house` (or joins to it), never an election-wide or unscoped count.

    Author these labeled assertions, each mapped to a ROADMAP success criterion in a trailing comment:

    1. USHC-06-SCOPE (criterion 1/3) — exactly 52 CA + 38 TX + 28 NY + 28 FL = 144 distinct NATIONAL_LOWER races (assert each per-state count via `COUNT(DISTINCT race_id) FILTER (WHERE st=...)`, RAISE EXCEPTION on any mismatch with the per-state expected baked in: CA 52 / TX 38 / NY 26 / FL 28), and assert total distinct race_id = 144. Bake the denominators as integer literals.
    2. USHC-06-ACTIVE (criterion 1) — every one of the 144 races has >=1 active candidate (HAVING COUNT FILTER active < 1 must be 0); RAISE NOTICE the count of races with <2 active as the uncontested-seat allowance (NOT a failure — FL-10 Frost is uncontested; do NOT assert an upper bound, FL is pre-primary crowded by design per 151 D-02).
    3. USHC-06-NULLPID (criterion 3) — 0 active race_candidates with NULL politician_id across all 144 (so headshots+stances resolve). Bake the prod-verified expectation that this is 0 in every state into the comment.
    4. USHC-06-DUPNAME (criterion 2) — 0 duplicate `lower(full_name)` among ACTIVE candidates WITHIN each state (GROUP BY st, lower(full_name) HAVING COUNT(*)>1 → 0). Per-state, never cross-state.
    5. USHC-06-DUPINCUMBENT (criterion 2) — 0 politician_id appearing as ACTIVE in 2+ DISTINCT races across the 144 (GROUP BY politician_id HAVING COUNT(DISTINCT race_id)>1 → 0) — the cross-district dup-incumbent / v2.4 two-Andy-Barrs invariant. Bake the prod-verified 0 into the comment.
    6. USHC-06-UNSOURCED (criterion 2) — 0 UNSOURCED stance rows across all newly-seeded Wave-1 candidates. This MUST be an EXISTENCE check (an `inform.politician_answers` row joined to any active Wave-1 candidate politician_id in `_house` whose (politician_id, topic_id) has NO `inform.politician_context` row with `sources IS NOT NULL AND array_length(sources,1)>=1`). It MUST NOT be a coverage assertion ("every candidate has >=1 stance") — that would false-fail on FL's deferred-stance partisan candidates and uncontested seats (the FL-asymmetry trap). Scope the answer-row check to politician_ids that are active candidates in `_house` (all four states), so it folds CA/TX/NY/FL together while remaining satisfiable. Bake the prod-verified per-state unsourced=0 (CA/TX/NY/FL) into the comment.
    7. USHC-06-FL-PROVISIONAL (criterion 3) — all 28 FL House races carry the `PROVISIONAL:%` description sentinel (COUNT(DISTINCT race_id) FILTER (WHERE st='FL' AND race_desc LIKE 'PROVISIONAL:%') = 28) AND FL rows are present (the USHC-06-SCOPE FL=28 already covers presence). This documents that FL coverage is intentionally incomplete pending Phase 153.
    8. USHC-06-PARTY (criterion 3) — party-not-on-candidate-card. Check how 149/150/151 encode this. Party lives on `races.primary_party`, and `essentials.race_candidates` has no party column to leak. Assert this as a STRUCTURAL fact: assert `essentials.race_candidates` has no `party`/`party_affiliation` column via `information_schema.columns` (COUNT WHERE table_schema='essentials' AND table_name='race_candidates' AND column_name IN ('party','party_affiliation') = 0). Document in a comment that party reads from races.primary_party only (antipartisan invariant) — do NOT over-assert a value on candidate rows.

    Add a closing header/footer comment that the consolidated gate documents 415 active candidates (CA 104 / TX 76 / FL 181 / NY 54) and that per-phase granular skip pins (149: 17 CA UUID skips; 150: TX/NY skips; 151: 11 FL stance + 17 headshot skips) are owned by the three passing per-phase gates, not re-pinned here.

    WRITE-FREE: SELECT-only + `CREATE TEMP TABLE ... ON COMMIT DROP` only. No INSERT/UPDATE/DELETE against essentials|inform. No `--commit`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/152-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - Run command: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/152-verify.sql`
    - Exit code 0.
    - Final stdout line is `ALL ASSERTIONS PASSED` (via RAISE NOTICE).
    - These labeled NOTICE lines appear (assertion names present): USHC-06-SCOPE, USHC-06-ACTIVE, USHC-06-NULLPID, USHC-06-DUPNAME, USHC-06-DUPINCUMBENT, USHC-06-UNSOURCED, USHC-06-FL-PROVISIONAL, USHC-06-PARTY.
    - The SCOPE assertion confirms 52 CA + 38 TX + 26 NY + 28 FL = 144 distinct NATIONAL_LOWER races.
    - The file contains `\set ON_ERROR_STOP on`, exactly one `DO $$` block, and only `CREATE TEMP TABLE ... ON COMMIT DROP` writes (no INSERT/UPDATE/DELETE against essentials or inform).
    - USHC-06-UNSOURCED is an existence check on answer rows lacking a sourced context row (NOT a per-candidate coverage requirement).
  </acceptance_criteria>
  <done>152-verify.sql exists, runs read-only against prod, exits 0, prints all 8 labeled USHC-06 assertion NOTICEs and ends with ALL ASSERTIONS PASSED; SCOPE confirms 144 races; UNSOURCED is FL-asymmetry-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Author backend/scripts/152-coordinate-smoke.ts — four-state coordinate-surfacing smoke</name>
  <files>backend/scripts/152-coordinate-smoke.ts</files>
  <read_first>
    - backend/scripts/151-coordinate-smoke.ts (THE canonical pattern: ST_PointOnSurface centroid -> ST_Covers surfacing join -> challenger-presence assertion; per-sample minActive override; contains[] check; MIN_DISTRICTS gate)
    - backend/scripts/149-coordinate-smoke.ts (the simpler single-state CA precedent)
    - backend/src/lib/electionService.ts lines 281-330 (getElectionsByCoordinate Part A — the ST_Covers join the smoke mirrors exactly)
  </read_first>
  <action>
    Create `backend/scripts/152-coordinate-smoke.ts` mirroring `151-coordinate-smoke.ts` EXACTLY in structure, generalized to FOUR states. Import `{ pool }` from `'../src/lib/db.js'`. Read-only.

    Define a `Sample` interface `{ state: 'CA'|'TX'|'NY'|'FL'; geoId: string; minActive: number; contains?: string[] }` and a map of state → `{ election: string; geoPrefix: string }`: CA→`{ 'CA 2026 Statewide General', '06' }`, TX→`{ 'TX 2026 Statewide General', '48' }`, NY→`{ 'NY 2026 Statewide General', '36' }`, FL→`{ 'FL 2026 Statewide General', '12' }`. Resolve each election id by exact name up front; throw if any is missing.

    Pick ONE contested in-district sample per state (avoid uncontested seats so the challenger assertion holds; minActive=2). Use these geo_ids drawn from the per-phase smoke samples and the 149/151 reuse pins (all proven to surface a contested field):
      - CA geoId '0602' (Huffman CA-2, contested — from 149 smoke), minActive 2.
      - TX geoId '4807' (Fletcher TX-7, contested) OR a TX district known contested from 150; minActive 2. If unsure, choose a TX geo whose race has >=1 challenger active — the executor MUST verify the chosen sample has >=1 active is_incumbent=false candidate before finalizing (query `_house`-equivalent or run the smoke; if it fails the challenger check, swap to another contested TX geo).
      - NY geoId '3617' (Lawler NY-17, contested) OR another NY geo with a seeded challenger; minActive 2. Same executor-verify rule as TX.
      - FL geoId '1201' (FL-1 crowded ~5, from 151 smoke), minActive 2.

    Mirror 151's per-sample loop precisely: for each sample, compute the interior centroid via `ST_PointOnSurface(gb.geometry)` over the district's NATIONAL_LOWER geofence boundary (the exact subquery from 151-coordinate-smoke.ts, parameterized by geoId), then run the surfacing query that mirrors electionService Part A — JOIN geofence_boundaries gb ON gb.geo_id=d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc), LEFT JOIN race_candidates, WHERE r.election_id = (that state's eid) AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)=(that state's geoPrefix) AND ST_Covers(gb.geometry, ST_SetSRID(ST_MakePoint(lng,lat),4326)) GROUP BY r.id — selecting active_cands, challengers (active AND is_incumbent=false), null_pid (active AND politician_id IS NULL), active_names array.

    Apply the same failure checks as 151: surfaced >1 race → fail (expected exactly 1); active < minActive → fail; minActive>=2 AND challengers<1 → fail (Pitfall-5 two-path guard — assert the CHALLENGER, not just incumbent); null_pid>0 → fail; any contains[] name absent → fail. On clean pass, increment surfaced and `console.log` a PASS line tagged with the state+geoId.

    Set `MIN_DISTRICTS = 4` (all four states must surface). At the end: if failures, print them and `process.exit(1)`; if surfaced < 4, fail; else `console.log` a final line beginning `COORDINATE SMOKE GREEN:` reporting `4/4 states surface their US House race with full challenger-inclusive field (CA/TX/NY/FL)`, then `await pool.end()`. Wrap `main().catch(...)` exactly as 151 does.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/152-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Run command: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/152-coordinate-smoke.ts`
    - Exit code 0.
    - One `PASS` line per state (4 total: CA, TX, NY, FL), each reporting 1 House race, >=2 active, >=1 challenger, 0 null pid.
    - Final stdout line begins with `COORDINATE SMOKE GREEN:` and reports 4/4 states surfacing.
    - The script is read-only (SELECT-only; no INSERT/UPDATE/DELETE), imports `pool` from `../src/lib/db.js`, and the surfacing query uses `ST_Covers` + `ST_PointOnSurface` mirroring electionService Part A.
    - For each state the challenger assertion (active is_incumbent=false >= 1) passes — confirming the field is not incumbent-only (Pitfall-5).
  </acceptance_criteria>
  <done>152-coordinate-smoke.ts exists, runs read-only against prod, exits 0 with 4 PASS lines (CA/TX/NY/FL each surfacing 1 House race with >=1 challenger and 0 null pid), and ends with COORDINATE SMOKE GREEN 4/4.</done>
</task>

</tasks>

<verification>
Both gate artifacts run read-only against production and exit 0:
- `152-verify.sql` ends with `ALL ASSERTIONS PASSED`, with all 8 labeled USHC-06 assertions passing (SCOPE 144 races / ACTIVE / NULLPID / DUPNAME / DUPINCUMBENT / UNSOURCED existence-check / FL-PROVISIONAL / PARTY structural).
- `152-coordinate-smoke.ts` ends with `COORDINATE SMOKE GREEN:` and 4/4 states (CA/TX/NY/FL) surface their US House race with the full challenger-inclusive field.

Both are SELECT-only (the SQL gate uses only `CREATE TEMP TABLE ... ON COMMIT DROP`). No migrations, no application-code changes, no deploy.
</verification>

<success_criteria>
- USHC-06 satisfied: a consolidated read-only gate proves the milestone end-to-end across all 144 Wave-1 districts.
- ROADMAP criterion 1: an in-district address per Wave-1 state resolves to its House race showing the candidate field, challenger-asserted (not incumbent-only).
- ROADMAP criterion 2: 0 unsourced stance rows across newly-seeded Wave-1 candidates + 0 duplicate-incumbent records across the 144 districts.
- ROADMAP criterion 3: every active race_candidate has a non-null politician_id, no candidate card surfaces party (structural — party on races.primary_party), and FL rows are present + marked provisional.
</success_criteria>

<output>
Create `.planning/phases/152-coordinate-verification-gate/152-01-SUMMARY.md` when done.
</output>
