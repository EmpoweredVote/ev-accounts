---
plan: "105-01"
phase: "105"
title: "DCIN Infrastructure — DC Government Stub, Districts, TIGER Ward Import, RPC Extension"
wave: 1
depends_on: []
requirements: [DCIN-01, DCIN-02, DCIN-03, DCIN-04]
files_modified:
  - supabase/migrations/20260607000001_284_dc_government_districts.sql
  - supabase/migrations/20260607000002_285_dc_tiger_geoid_backfill_rpc_update.sql
  - backend/scripts/load-state-tiger-boundaries.ts
  - backend/scripts/seed-dc-ward-geo-districts.sh
autonomous: true
must_haves:
  truths:
    - "DC government row exists in essentials.governments with state='DC', type='LOCAL', geo_id='11'"
    - "19 DC district rows exist: 8 CITY_COUNCIL ward (dc-ward-1..8) + 1 CITY_COUNCIL at-large (dc-council-at-large) + 9 SCHOOL_BOARD (dc-sboe-ward-1..8 + dc-sboe-at-large) + 1 NATIONAL_LOWER (dc-national-lower)"
    - "essentials.geo_districts has 8 dc_ward rows with geoid='11001' through '11008'"
    - "essentials.geofence_boundaries has 8 DC sldl rows with mtfcc='G5220', state='11'"
    - "All 8 CITY_COUNCIL ward district rows have tiger_geoid populated ('11001' through '11008')"
    - "resolve_user_districts and cache_user_districts default p_layers includes 'dc_ward'"
  artifacts:
    - path: "supabase/migrations/20260607000001_284_dc_government_districts.sql"
      provides: "DC government stub + 19 district rows"
      contains: "INSERT INTO essentials.governments"
    - path: "supabase/migrations/20260607000002_285_dc_tiger_geoid_backfill_rpc_update.sql"
      provides: "tiger_geoid backfill + DROP/CREATE both RPCs with dc_ward in default"
      contains: "DROP FUNCTION IF EXISTS essentials.resolve_user_districts"
    - path: "backend/scripts/load-state-tiger-boundaries.ts"
      provides: "DC sldl allowlist entry + STATE_LAYER_TYPE_MAP + DC MTFCC assertion"
      contains: "DC: new Set"
    - path: "backend/scripts/seed-dc-ward-geo-districts.sh"
      provides: "geo_districts ward polygon seeder"
      contains: "dc_ward"
  key_links:
    - from: "essentials.districts (dc-ward-1..8)"
      to: "essentials.geo_districts (dc_ward layer)"
      via: "tiger_geoid = geoid"
      pattern: "tiger_geoid.*11001|11001.*tiger_geoid"
    - from: "essentials.resolve_user_districts"
      to: "essentials.geo_districts"
      via: "ST_Contains on dc_ward layer"
      pattern: "dc_ward"
---

<objective>
Deliver the complete DC geospatial infrastructure required for all downstream DC data phases (105-02 stances, finance). Creates the DC government stub, all 19 district records (DCIN-01/02), imports TIGER 2024 ward polygons into both essentials.geofence_boundaries and essentials.geo_districts (DCIN-03), backfills tiger_geoid on ward district rows, and extends both resolve_user_districts / cache_user_districts RPCs to include dc_ward in their default layer set (DCIN-04).

Purpose: After this plan, any DC user who sets a location will be geofenced to their ward via Path 0, and all FK targets needed by 105-02 (official records) exist.

Output:
- Migration 284: DC government + 19 district rows
- Migration 285: tiger_geoid backfill + RPC default extension
- load-state-tiger-boundaries.ts extended for DC sldl (writes to geofence_boundaries)
- seed-dc-ward-geo-districts.sh: ogr2ogr + psql seed writing to geo_districts
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-dc-infrastructure-official-records/105-CONTEXT.md
@.planning/phases/105-dc-infrastructure-official-records/105-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration 284 — DC Government Stub + 19 District Records (DCIN-01, DCIN-02)</name>

  <read_first>
    supabase/migrations/20260501054106_087_tx_schema_geo_id_state_county.sql
    (government INSERT pattern — name/type/state/city/geo_id shape)

    backend/migrations/217_sj_government_structure.sql
    (district INSERT pattern — bulk VALUES with government_id subquery, geo_id slug naming)

    .planning/phases/105-dc-infrastructure-official-records/105-RESEARCH.md
    (§"Migration Patterns" for the exact INSERT SQL skeletons and geo_id naming)
  </read_first>

  <files>supabase/migrations/20260607000001_284_dc_government_districts.sql</files>

  <action>
Create `supabase/migrations/20260607000001_284_dc_government_districts.sql`.

Pre-flight comment at top: verify with `SELECT MAX(version) FROM supabase_migrations.schema_migrations` (expected: 283) and `SELECT DISTINCT type FROM essentials.governments ORDER BY type` (confirm 'LOCAL' is a valid value) before applying.

Section 1 — Government stub (DCIN-01):
INSERT INTO essentials.governments (name, type, state, city, geo_id) with idempotent WHERE NOT EXISTS guard:
- name = 'District of Columbia'
- type = 'LOCAL'
- state = 'DC'
- city = 'Washington'
- geo_id = '11'

Section 2 — District records (DCIN-02): 19 rows total using bulk VALUES INSERT with government_id subquery (SELECT id FROM essentials.governments WHERE name = 'District of Columbia' AND state = 'DC'). Use WHERE NOT EXISTS guard on (geo_id, district_type, state). Columns: geo_id, district_type, label, state, government_id.

CITY_COUNCIL ward rows (8):
- ('dc-ward-1', 'CITY_COUNCIL', 'Ward 1', 'DC')
- ('dc-ward-2', 'CITY_COUNCIL', 'Ward 2', 'DC')
- ('dc-ward-3', 'CITY_COUNCIL', 'Ward 3', 'DC')
- ('dc-ward-4', 'CITY_COUNCIL', 'Ward 4', 'DC')
- ('dc-ward-5', 'CITY_COUNCIL', 'Ward 5', 'DC')
- ('dc-ward-6', 'CITY_COUNCIL', 'Ward 6', 'DC')
- ('dc-ward-7', 'CITY_COUNCIL', 'Ward 7', 'DC')
- ('dc-ward-8', 'CITY_COUNCIL', 'Ward 8', 'DC')

CITY_COUNCIL at-large row (1) — per research PITFALL-6 resolution (consistent with D-07 SBOE at-large pattern):
- ('dc-council-at-large', 'CITY_COUNCIL', 'DC Council At-Large', 'DC')

SCHOOL_BOARD rows (9):
- ('dc-sboe-ward-1', 'SCHOOL_BOARD', 'SBOE Ward 1', 'DC')
- ('dc-sboe-ward-2', 'SCHOOL_BOARD', 'SBOE Ward 2', 'DC')
- ('dc-sboe-ward-3', 'SCHOOL_BOARD', 'SBOE Ward 3', 'DC')
- ('dc-sboe-ward-4', 'SCHOOL_BOARD', 'SBOE Ward 4', 'DC')
- ('dc-sboe-ward-5', 'SCHOOL_BOARD', 'SBOE Ward 5', 'DC')
- ('dc-sboe-ward-6', 'SCHOOL_BOARD', 'SBOE Ward 6', 'DC')
- ('dc-sboe-ward-7', 'SCHOOL_BOARD', 'SBOE Ward 7', 'DC')
- ('dc-sboe-ward-8', 'SCHOOL_BOARD', 'SBOE Ward 8', 'DC')
- ('dc-sboe-at-large', 'SCHOOL_BOARD', 'SBOE At-Large', 'DC')  ← tiger_geoid = NULL per D-07

NATIONAL_LOWER row (1):
- ('dc-national-lower', 'NATIONAL_LOWER', 'District of Columbia At-Large', 'DC')

Wrap in BEGIN/COMMIT. All geo_ids are internal FK handles only — tiger_geoid is NULL for all rows at this point (backfilled in migration 285 and by the seed script).
  </action>

  <verify>
    <automated>
      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.governments WHERE state = 'DC' AND type = 'LOCAL' AND geo_id = '11'"
      -- Expected: 1

      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.districts WHERE state = 'DC'"
      -- Expected: 19

      psql $DATABASE_URL -c "SELECT district_type, COUNT(*) FROM essentials.districts WHERE state = 'DC' GROUP BY district_type ORDER BY district_type"
      -- Expected: CITY_COUNCIL=9, NATIONAL_LOWER=1, SCHOOL_BOARD=9

      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.districts WHERE state = 'DC' AND government_id IS NOT NULL"
      -- Expected: 19 (all FK'd to DC government)
    </automated>
  </verify>

  <done>
    - essentials.governments has exactly 1 row with state='DC', type='LOCAL', geo_id='11'
    - essentials.districts has 19 rows for state='DC': 9 CITY_COUNCIL (8 ward + 1 at-large) + 9 SCHOOL_BOARD + 1 NATIONAL_LOWER
    - All 19 district rows have government_id pointing to the DC government row (not NULL)
    - All tiger_geoid values are NULL (will be backfilled in migration 285 after geo_districts is seeded)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend load-state-tiger-boundaries.ts for DC sldl (DCIN-03 — geofence_boundaries write)</name>

  <read_first>
    backend/scripts/load-state-tiger-boundaries.ts
    (full file — read the STATE_LAYER_ALLOWLIST constant at line ~34, the LAYER_DISPATCH sldl entry at line ~245, the processLayer function, and the existing per-state MTFCC assertion blocks for MA/ME/TX/CA/OR/MD as the pattern to replicate for DC)
  </read_first>

  <files>backend/scripts/load-state-tiger-boundaries.ts</files>

  <action>
Three changes to `backend/scripts/load-state-tiger-boundaries.ts`:

Change 1 — Add DC to STATE_LAYER_ALLOWLIST (after line ~43, inside the existing object):
  DC: new Set(['sldl']),

Change 2 — Add STATE_LAYER_TYPE_MAP constant (immediately after the STATE_LAYER_ALLOWLIST block, before UT_TRIBE_NAMELSAD_ALLOWLIST):
  const STATE_LAYER_TYPE_MAP: Record<string, Record<string, string>> = {
    DC: { sldl: 'CITY_COUNCIL' },
  };

Change 3 — Add DC MTFCC pre-flight assertion block in processLayer (after the MD assertion block at line ~895, before the "Stream records" section). Pattern matches the existing MA/ME/TX/CA/OR/MD blocks:
  if (fipsArg === '11') {
    const EXPECTED_DC_MTFCC: Record<string, number> = {
      sldl: 8,  // 8 DC ward polygons (TIGER 2024 FIPS 11)
    };
    // ... same pattern as the MA/ME/TX/CA/OR/MD blocks:
    // count filtered records, compare to expected, throw MtfccAssertionError if mismatch
  }

Change 4 — In processLayer, after the line that resolves `layerDef.district_type` into the district insert call (line ~1061 area), apply the STATE_LAYER_TYPE_MAP override before calling insertDistrictIfMissing. The resolved district_type for the upsertGeofence call is fine (mtfcc stays G5220 regardless). But the insertDistrictIfMissing call must use the overridden type:
  const effectiveDistrictType = STATE_LAYER_TYPE_MAP[abbrevUpper]?.[layer] ?? layerDef.district_type;
  ... pass effectiveDistrictType instead of layerDef.district_type to insertDistrictIfMissing

IMPORTANT: The `writeDistrictRow` field for sldl is `true` in LAYER_DISPATCH (per 130-01 audit). Leave it true — the migration 284 already created the district rows with idempotent WHERE NOT EXISTS guards, so a re-insert via the script is safe and idempotent. Do NOT add a DC-specific `writeDistrictRow: false` override — that would deviate from the audit-established pattern.

Also add DC to STATE_RUN_MAKEVALID if appropriate (DC ward polygons are legislative boundaries, low risk of invalid geometry — set DC: new Set(['sldl']) in STATE_RUN_MAKEVALID to match the pattern for other legislative states like ME/OR/MD).

After changes, dry-run test command (verifies allowlist + FIPS → state round-trip, does NOT touch DB):
  npx tsx backend/scripts/load-state-tiger-boundaries.ts --state DC --fips 11 --layers sldl --dry-run
  </action>

  <verify>
    <automated>
      grep -n "DC: new Set" backend/scripts/load-state-tiger-boundaries.ts
      -- Expected: at least 1 match (STATE_LAYER_ALLOWLIST entry)

      grep -n "STATE_LAYER_TYPE_MAP" backend/scripts/load-state-tiger-boundaries.ts
      -- Expected: at least 2 matches (const declaration + usage in processLayer)

      grep -n "EXPECTED_DC_MTFCC" backend/scripts/load-state-tiger-boundaries.ts
      -- Expected: at least 1 match (DC assertion block present)

      npx tsx backend/scripts/load-state-tiger-boundaries.ts --state DC --fips 11 --layers sldl --dry-run
      -- Expected: exits 0, prints "[dry-run] sldl (G5220 → CITY_COUNCIL)" (effectiveDistrictType override visible)
    </automated>
  </verify>

  <done>
    - STATE_LAYER_ALLOWLIST contains DC: new Set(['sldl'])
    - STATE_LAYER_TYPE_MAP constant exists with DC: { sldl: 'CITY_COUNCIL' }
    - processLayer uses STATE_LAYER_TYPE_MAP override for effectiveDistrictType
    - DC MTFCC assertion block present for fipsArg === '11' expecting sldl: 8
    - Dry-run exits 0 and prints the sldl entry with CITY_COUNCIL district type
    - No other state's behavior is changed (STATE_LAYER_ALLOWLIST entries for CA/TX/UT/IN/MA/ME/OR/MD unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 3: Run geofence_boundaries import + Seed geo_districts + Create seed-dc-ward-geo-districts.sh (DCIN-03)</name>

  <read_first>
    supabase/migrations/20260509000001_089_tiger_geo_districts_schema.sql
    (geo_districts schema: layer+geoid UNIQUE, columns: layer, geoid, district_num, name, geom)

    .planning/phases/105-dc-infrastructure-official-records/105-RESEARCH.md
    (§"DCIN-03 Seed Script" for the exact ogr2ogr + psql INSERT SQL; §"Architecture Patterns §DCIN-03 Import Flow"; §"PITFALL-8" for Windows ogr2ogr session pooler requirement)
  </read_first>

  <files>backend/scripts/seed-dc-ward-geo-districts.sh</files>

  <action>
TWO operations in this task:

OPERATION A — Run the geofence_boundaries import (live, writes to geofence_boundaries):
  npx tsx backend/scripts/load-state-tiger-boundaries.ts --state DC --fips 11 --layers sldl
  Expected output: "Inserted (boundaries): 8", "Inserted (districts): 9" (8 ward + 1 at-large auto-created by script via insertDistrictIfMissing — all idempotent with migration 284 rows).

OPERATION B — Create and run backend/scripts/seed-dc-ward-geo-districts.sh:

Create the bash script following the Phase 69 seed-tiger-districts.sh pattern. The script:
1. Downloads tl_2024_11_sldl.zip from https://www2.census.gov/geo/tiger/TIGER2024/SLDL/tl_2024_11_sldl.zip
2. Unzips to a temp directory
3. Uses ogr2ogr to reproject NAD83 → EPSG:4326 into a temporary PostGIS staging table named `tiger_stage`
4. Runs a psql INSERT ... ON CONFLICT into essentials.geo_districts:
     INSERT INTO essentials.geo_districts (layer, geoid, district_num, name, geom)
     SELECT
       'dc_ward' AS layer,
       s."GEOID" AS geoid,
       CAST(CAST(s."SLDLST" AS INTEGER) AS TEXT) AS district_num,
       s."NAMELSAD" AS name,
       s.wkb_geometry AS geom
     FROM tiger_stage s
     ON CONFLICT (layer, geoid) DO UPDATE
       SET name = EXCLUDED.name, geom = EXCLUDED.geom;
5. Drops tiger_stage after use
6. Prints row count confirmation

Windows ogr2ogr requirements (per CONTEXT.md + RESEARCH PITFALL-8):
- Use SESSION POOLER URL, not direct DB host: extract from DATABASE_URL or use POOLER_URL env var
- Set PROJ_LIB="C:/Program Files/GDAL/projlib" before calling ogr2ogr
- ogr2ogr connection string: PG:"host=aws-0-us-west-1.pooler.supabase.com port=5432 dbname=postgres user=postgres.${PROJECT_REF} password=${DB_PASSWORD} sslmode=require"

Verify ogr2ogr is available first: `ogr2ogr --version` (should print GDAL 3.x).

After the script is created and made executable, run it:
  bash backend/scripts/seed-dc-ward-geo-districts.sh
  </action>

  <verify>
    <automated>
      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state = '11' AND mtfcc = 'G5220'"
      -- Expected: 8

      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.geo_districts WHERE layer = 'dc_ward'"
      -- Expected: 8

      psql $DATABASE_URL -c "SELECT geoid, district_num, name FROM essentials.geo_districts WHERE layer = 'dc_ward' ORDER BY geoid"
      -- Expected: rows with geoid '11001' through '11008', district_num '1' through '8', name 'Ward 1' through 'Ward 8'

      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.geo_districts WHERE layer = 'dc_ward' AND geom IS NOT NULL"
      -- Expected: 8
    </automated>
  </verify>

  <done>
    - essentials.geofence_boundaries has exactly 8 rows with state='11' and mtfcc='G5220'
    - essentials.geo_districts has exactly 8 rows with layer='dc_ward', geoid '11001'-'11008', district_num '1'-'8'
    - All 8 geo_districts rows have non-NULL geom (polygon geometries in EPSG:4326)
    - backend/scripts/seed-dc-ward-geo-districts.sh exists and is a runnable bash script
  </done>
</task>

<task type="auto">
  <name>Task 4: Migration 285 — tiger_geoid Backfill + RPC Default Extension (DCIN-04)</name>

  <read_first>
    supabase/migrations/20260509000002_090_tiger_resolve_user_districts_rpcs.sql
    (current resolve_user_districts + cache_user_districts function bodies — MUST be copied verbatim and extended with dc_ward in default array; current signatures shown: resolve_user_districts(float8, float8, text[]) and cache_user_districts(UUID, float8, float8))

    supabase/migrations/20260509000003_091_tiger_geoid_backfill.sql
    (backfill UPDATE pattern using geo_districts JOIN; note that DC uses slug geo_ids NOT TIGER GEOIDs, so the 091 d.geo_id = gd.geoid join does NOT work — use the hardcoded UPDATE approach instead)
  </read_first>

  <files>supabase/migrations/20260607000002_285_dc_tiger_geoid_backfill_rpc_update.sql</files>

  <action>
Create `supabase/migrations/20260607000002_285_dc_tiger_geoid_backfill_rpc_update.sql`.

Section 1 — tiger_geoid backfill (DCIN-04):
8 hardcoded UPDATE statements (do NOT use the 091 JOIN pattern — DC district geo_ids are slugs 'dc-ward-N', not TIGER GEOIDs):
  UPDATE essentials.districts SET tiger_geoid = '11001' WHERE geo_id = 'dc-ward-1' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11002' WHERE geo_id = 'dc-ward-2' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11003' WHERE geo_id = 'dc-ward-3' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11004' WHERE geo_id = 'dc-ward-4' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11005' WHERE geo_id = 'dc-ward-5' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11006' WHERE geo_id = 'dc-ward-6' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11007' WHERE geo_id = 'dc-ward-7' AND state = 'DC';
  UPDATE essentials.districts SET tiger_geoid = '11008' WHERE geo_id = 'dc-ward-8' AND state = 'DC';

Section 2 — Extend resolve_user_districts default (DCIN-04):
Per v2.2 migration 094 lesson: DROP first, then CREATE OR REPLACE. The function signatures do NOT change — same param count, same types. Adding 'dc_ward' to the DEFAULT array is the only change.

DROP FUNCTION IF EXISTS essentials.resolve_user_districts(float8, float8, text[]);

CREATE OR REPLACE FUNCTION essentials.resolve_user_districts(
  p_lat    float8,
  p_lng    float8,
  p_layers text[] DEFAULT ARRAY[
    'ca_assembly', 'ca_senate', 'us_house',
    'school_unified', 'school_elementary', 'school_secondary',
    'dc_ward'
  ]
)
RETURNS TABLE(layer text, geoid text, district_num text, name text)
LANGUAGE sql
STABLE
SECURITY DEFINER
SET search_path = ''
AS $$
  SELECT gd.layer, gd.geoid, gd.district_num, gd.name
  FROM essentials.geo_districts gd
  WHERE gd.layer = ANY(p_layers)
    AND public.ST_Contains(gd.geom, public.ST_SetSRID(public.ST_MakePoint(p_lng, p_lat), 4326))
$$;

GRANT EXECUTE ON FUNCTION essentials.resolve_user_districts(float8, float8, text[])
  TO authenticated, anon;

Section 3 — Extend cache_user_districts default:
NOTE: cache_user_districts (migration 090) has no p_layers param — it calls resolve_user_districts() with NO args, inheriting its default. Read migration 090 carefully before deciding whether a DROP is needed. If cache_user_districts already delegates to resolve_user_districts() with no explicit p_layers arg, then extending resolve_user_districts's default is sufficient and cache_user_districts needs no change. Only DROP/CREATE cache_user_districts if it passes a hardcoded ARRAY literal to resolve_user_districts. Verify by reading the function body in migration 090.

Wrap all in BEGIN/COMMIT.
  </action>

  <verify>
    <automated>
      psql $DATABASE_URL -c "SELECT tiger_geoid FROM essentials.districts WHERE state = 'DC' AND district_type = 'CITY_COUNCIL' AND geo_id LIKE 'dc-ward-%' ORDER BY geo_id"
      -- Expected: 8 rows, tiger_geoid values '11001' through '11008'

      psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.districts WHERE state = 'DC' AND district_type = 'CITY_COUNCIL' AND tiger_geoid IS NULL"
      -- Expected: 1 (only dc-council-at-large has NULL tiger_geoid)

      psql $DATABASE_URL -c "SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'resolve_user_districts' AND pronamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'essentials')" | grep dc_ward
      -- Expected: match found (dc_ward in function definition)

      psql $DATABASE_URL -c "SELECT essentials.resolve_user_districts(38.9072, -77.0369)"
      -- Expected: returns at least 1 row with layer='dc_ward' (coordinates = Washington DC downtown, Ward 2 area)
    </automated>
  </verify>

  <done>
    - All 8 dc-ward-N district rows have tiger_geoid populated ('11001'-'11008')
    - dc-council-at-large, dc-sboe-*, dc-national-lower tiger_geoid values remain NULL (correct)
    - resolve_user_districts function definition contains 'dc_ward' in the DEFAULT array
    - Live call SELECT essentials.resolve_user_districts(38.9072, -77.0369) returns a row with layer='dc_ward'
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TIGER FTP → script | Downloaded ZIP is public Census data; no auth required, but content must be asserted before DB write |
| ogr2ogr → PostgreSQL | Geometry data flows through GDAL projection; malformed polygons possible if TIGER vintage changes |
| Migration SQL → DB | Service role migration; no user input, but idempotency guards required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Tampering | TIGER ZIP download | mitigate | MTFCC pre-flight assertion in processLayer — aborts if record count != 8 before any DB write |
| T-105-02 | Information Disclosure | ogr2ogr DATABASE_URL in env | mitigate | Use session pooler URL (not direct DB host); $DATABASE_URL never logged to stdout |
| T-105-03 | Denial of Service | Tiger_geoid backfill runs on wrong state | accept | WHERE state = 'DC' filter on all 8 UPDATEs; DC rows are isolated |
| T-105-04 | Tampering | RPC DROP + recreate loses GRANT | mitigate | Migration 285 re-issues GRANT EXECUTE after CREATE OR REPLACE |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; existing GDAL + tsx already validated |
</threat_model>

<verification>
After all 4 tasks complete, run full phase verification:

```sql
-- DCIN-01: Government stub
SELECT name, type, state, geo_id FROM essentials.governments WHERE state = 'DC';
-- Expected: 1 row — 'District of Columbia', 'LOCAL', 'DC', '11'

-- DCIN-02: All district records
SELECT district_type, COUNT(*) FROM essentials.districts WHERE state = 'DC' GROUP BY district_type ORDER BY district_type;
-- Expected: CITY_COUNCIL=9, NATIONAL_LOWER=1, SCHOOL_BOARD=9

-- DCIN-03: geo_districts ward polygons
SELECT COUNT(*), MIN(geoid), MAX(geoid) FROM essentials.geo_districts WHERE layer = 'dc_ward';
-- Expected: 8, '11001', '11008'

-- DCIN-03: geofence_boundaries ward polygons
SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE state = '11' AND mtfcc = 'G5220';
-- Expected: 8

-- DCIN-04: tiger_geoid backfill
SELECT geo_id, tiger_geoid FROM essentials.districts WHERE state = 'DC' AND district_type = 'CITY_COUNCIL' ORDER BY geo_id;
-- Expected: dc-council-at-large=NULL, dc-ward-1='11001', ..., dc-ward-8='11008'

-- DCIN-04: RPC live test (DC downtown coordinates)
SELECT * FROM essentials.resolve_user_districts(38.9072, -77.0369);
-- Expected: at least 1 row with layer='dc_ward', geoid='11002' or '11003' (Ward 2/3 area)
```
</verification>

<success_criteria>
- Migration 284 applied: 1 DC government row, 19 DC district rows (all FK'd to government)
- Migration 285 applied: 8 tiger_geoid values set, dc_ward in both RPC defaults
- load-state-tiger-boundaries.ts contains DC in STATE_LAYER_ALLOWLIST and STATE_LAYER_TYPE_MAP
- seed-dc-ward-geo-districts.sh executed successfully: 8 rows in essentials.geo_districts with layer='dc_ward'
- essentials.geofence_boundaries has 8 rows with state='11', mtfcc='G5220'
- Live RPC call resolve_user_districts(38.9072, -77.0369) returns a dc_ward row
</success_criteria>

<output>
Create `.planning/phases/105-dc-infrastructure-official-records/105-01-SUMMARY.md` when done.
</output>
