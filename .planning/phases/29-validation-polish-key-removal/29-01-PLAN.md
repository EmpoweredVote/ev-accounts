---
phase: 29-validation-polish-key-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/.env.local
  - EV-Backend/internal/essentials/provider/config.go
  - EV-Backend/internal/essentials/provider/provider.go
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "Running the audit grep command against EV-Backend/ Go source files returns zero active BallotReady references (excluding dead ballotready/ package and comment-only lines)"
    - "BALLOTREADY_KEY and POLITICIAN_PROVIDER=ballotready lines are absent from EV-Backend/.env.local"
    - "The provider/config.go no longer contains BallotReady-specific constants, struct fields, or case branches"
    - "The backend compiles successfully with go build ./..."
  artifacts:
    - path: "EV-Backend/.env.local"
      provides: "Local env file without BALLOTREADY_KEY or POLITICIAN_PROVIDER=ballotready"
    - path: "EV-Backend/internal/essentials/provider/config.go"
      provides: "Provider config with BallotReady constants, fields, and case branches removed"
    - path: "EV-Backend/internal/essentials/provider/provider.go"
      provides: "Provider errors without ErrMissingBallotReadyKey"
  key_links:
    - from: "EV-Backend/internal/essentials/provider/config.go"
      to: "EV-Backend/internal/essentials/ballotready/"
      via: "ballotready/ package references provider.ProviderBallotReady and cfg.BallotReadyKey — if provider/ is cleaned, ballotready/ will fail to compile, confirming it is truly dead code"
      pattern: "ProviderBallotReady|BallotReadyKey|BallotReadyEndpoint"
---

<objective>
Run a codebase audit to confirm zero active BallotReady API references in Go source files, remove BALLOTREADY_KEY from the local environment file, and clean the provider/ config package of BallotReady-specific code.

Purpose: Proves the codebase has no active BallotReady dependency and removes local configuration artifacts. The ballotready/ package is intentionally kept as dead code (per STATE.md — preserved for admin import pipeline) but the provider/ config that would enable it is removed, making accidental reactivation impossible without deliberate code changes.

Output: Clean provider config, clean .env.local, audit results confirming zero active references.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-validation-polish-key-removal/29-RESEARCH.md
@.planning/phases/27-cache-only-candidates-warmer-cleanup/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run codebase audit and clean provider/ package of BallotReady references</name>
  <files>
    EV-Backend/internal/essentials/provider/config.go
    EV-Backend/internal/essentials/provider/provider.go
  </files>
  <action>
**Step 1: Run initial audit to establish baseline.**

Run the pragmatic audit grep command to find active BallotReady references (excluding the dead ballotready/ package and comment-only lines):

```bash
grep -rn "ballotReadyClient\|BallotReady\|BALLOTREADY" EV-Backend/ --include="*.go" \
  | grep -v "internal/essentials/ballotready/" \
  | grep -v "^\s*//.*BallotReady" \
  | grep -v "^\s*//.*BALLOTREADY"
```

Record the output. Most matches will be in `provider/config.go` and `provider/provider.go` — these are the files to clean.

**Step 2: Clean provider/config.go.**

Remove these specific items from `EV-Backend/internal/essentials/provider/config.go`:
- The `ProviderBallotReady ProviderType = "ballotready"` constant
- The `BallotReadyKey string` field from the Config struct
- The `BallotReadyEndpoint string` field from the Config struct
- The `DefaultBallotReadyEndpoint` constant
- The `case "ballotready":` branch in `LoadFromEnv()` (this includes `BallotReadyKey: os.Getenv(...)` and `BallotReadyEndpoint: ...` assignments)
- The `BALLOTREADY_ENDPOINT` environment variable read
- The `case ProviderBallotReady:` branch in `Validate()`

Leave the cicero-related code untouched — the server still uses the cicero provider actively (setup.go imports cicero).

**Step 3: Clean provider/provider.go.**

Remove:
- `ErrMissingBallotReadyKey` error variable declaration

Leave all other errors and types intact.

**Step 4: Verify the ballotready/ package is now expected to fail compilation.**

After removing `ProviderBallotReady`, `cfg.BallotReadyKey`, and `cfg.BallotReadyEndpoint` from provider/, the `ballotready/` package will reference undefined symbols. This is expected and desired — it confirms the package is truly dead (no imports from the main codebase). Do NOT fix the ballotready/ package. Do NOT delete it.

To verify the main server still compiles, run:
```bash
cd EV-Backend && go build -o /dev/null ./cmd/... ./internal/...
```

Note: `go build ./...` would fail because it includes the dead ballotready/ package. Instead, build only the packages that are actually imported. If the above command structure doesn't work, use:
```bash
cd EV-Backend && go build -o /dev/null .
```

This builds only the main binary (which does not import ballotready/).

**Step 5: Also check for any BallotReady references in YAML/JSON/TOML config files:**
```bash
grep -rn "BALLOTREADY" EV-Backend/ --include="*.yaml" --include="*.toml" --include="*.json"
```
Expected: zero matches (apprunner.yaml is already clean per research).

**Step 6: Run final audit to confirm zero active references.**

Re-run the pragmatic audit command from Step 1. After provider/ cleanup, the only remaining matches should be:
- Inside `internal/essentials/ballotready/` (dead package — excluded)
- Comment-only lines (excluded by grep -v)
- Possibly `cmd/bulk-import/main.go` Println deprecation message (cosmetic — update the text to remove "BallotReady" if present)

If `cmd/bulk-import/main.go` has a BallotReady string in its deprecation message, update it to a generic message like "This tool has been deprecated. Data import pipeline is no longer available."

The audit should return zero non-excluded matches.
  </action>
  <verify>
Run the full audit command:
```bash
grep -rn "ballotReadyClient\|BallotReady\|BALLOTREADY" EV-Backend/ --include="*.go" \
  | grep -v "internal/essentials/ballotready/" \
  | grep -v "[[:space:]]*//.*BallotReady" \
  | grep -v "[[:space:]]*//.*BALLOTREADY" \
  | grep -v "[[:space:]]*//.*ballotready"
```
Must return zero lines.

Also verify: `cd EV-Backend && go build -o /dev/null .` succeeds (main binary compiles).
  </verify>
  <done>
The pragmatic audit grep returns zero active BallotReady references outside the dead ballotready/ package and comments. The provider/config.go and provider/provider.go have no BallotReady-specific code. The main server binary compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove BALLOTREADY_KEY and POLITICIAN_PROVIDER from .env.local</name>
  <files>
    EV-Backend/.env.local
  </files>
  <action>
Read `EV-Backend/.env.local` and remove these two lines:
- `POLITICIAN_PROVIDER=ballotready` (or any line setting POLITICIAN_PROVIDER to ballotready)
- `BALLOTREADY_KEY=...` (the line containing the BallotReady API key value)

Do NOT remove:
- `DATABASE_URL=...` — still needed
- `BALLOTREADY_API_KEY=...` — check for this variant name too; remove if present
- `CICERO_KEY=...` — still needed (cicero is the active provider)
- Any Google Maps API keys — still needed
- Any other environment variables

After removal, the server will use `POLITICIAN_PROVIDER=""` which defaults to cicero in `LoadFromEnv()`. This is already the production behavior.

**IMPORTANT SECURITY NOTE:** The .env.local file is in .gitignore and must NEVER be committed. This is a local file edit only. Verify .env.local is listed in EV-Backend/.gitignore before proceeding.
  </action>
  <verify>
Verify the two lines are gone:
```bash
grep -n "BALLOTREADY\|POLITICIAN_PROVIDER=ballotready" EV-Backend/.env.local
```
Must return zero lines.

Verify other env vars are still present:
```bash
grep -c "DATABASE_URL" EV-Backend/.env.local
```
Must return 1.
  </verify>
  <done>
EV-Backend/.env.local no longer contains BALLOTREADY_KEY, BALLOTREADY_API_KEY, or POLITICIAN_PROVIDER=ballotready. All other environment variables remain intact. The file is not committed to git.
  </done>
</task>

</tasks>

<verification>
1. Audit grep returns zero active BallotReady references (excluding ballotready/ package and comments)
2. provider/config.go has no ProviderBallotReady, BallotReadyKey, BallotReadyEndpoint, or BALLOTREADY case branches
3. provider/provider.go has no ErrMissingBallotReadyKey
4. EV-Backend compiles: `go build -o /dev/null .` exits 0
5. .env.local has no BALLOTREADY or POLITICIAN_PROVIDER=ballotready lines
6. No BALLOTREADY references in YAML/JSON/TOML config files
</verification>

<success_criteria>
- Zero active BallotReady API references in Go source files (pragmatic audit scope)
- Provider config cleaned — BallotReady cannot be accidentally re-enabled without deliberate code changes
- Local environment file cleaned of BallotReady key artifacts
- Server compiles and starts correctly with cicero provider (default)
</success_criteria>

<output>
After completion, create `.planning/phases/29-validation-polish-key-removal/29-01-SUMMARY.md`
</output>
