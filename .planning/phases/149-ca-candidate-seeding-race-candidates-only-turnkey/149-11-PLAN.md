---
phase: 149-ca-candidate-seeding-race-candidates-only-turnkey
plan: 11
type: execute
wave: 4
depends_on: ["149-01", "149-02", "149-03", "149-04", "149-05", "149-06", "149-07", "149-08", "149-09", "149-10"]
files_modified:
  - backend/scripts/149-coordinate-smoke.ts
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
autonomous: true
must_haves:
  truths:
    - "149-verify.sql runs read-only against prod and EVERY labeled assertion (USHC-02/03/04/05 + D-04) PASSES with exit 0"
    - "For ≥3 in-district CA House test coordinates, getElectionsByCoordinate surfaces the resident's House race with its full candidate field (CHALLENGER present, not just incumbent)"
    - "0 unsourced stance rows + 0 duplicate-incumbent records across all 52 CA House districts; honest-skips pinned by UUID"
  artifacts:
    - path: "backend/scripts/149-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers a CA House geofence centroid → assert race + ≥2-candidate field surfaces"
  key_links:
    - from: "149-verify.sql + 149-coordinate-smoke.ts"
      to: "the 52 CA House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 149 end-to-end: run the House-scoped `149-verify.sql` gate green and add a coordinate-surfacing smoke that confirms ≥3 CA House races actually surface for an in-district address.

Purpose: USHC-02/03/04/05 consolidated verification for CA — the anchor proof that the seed + headshot + stance pattern works before Phases 150/151. This gate is re-used (extended) by Phase 152's full-144 gate.
Output: A passing gate + coordinate smoke; Phase 149 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-VALIDATION.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md
@backend/scripts/149-verify.sql
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the coordinate-surfacing smoke + finalize honest-skip pins in 149-verify.sql consumption</name>
  <read_first>
    - backend/scripts/149-verify.sql (the gate authored in 149-02; its honest-skip TEMP-table pins)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers → district geo_id → office → race → race_candidates → PHOTO_LATERAL; the join discriminator `d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc)`)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md (§Code Examples — surfacing smoke query; §Validation Architecture)
    - the 149-03..10 SUMMARYs (the documented headshot + whole-record-stance honest-skips to pin by UUID)
  </read_first>
  <action>
    Author `backend/scripts/149-coordinate-smoke.ts` (read-only): for ≥3 sample CA House districts (pick spread geo_ids, e.g. 0602/0625/0652), compute a centroid from `essentials.geofence_boundaries` (`ST_PointOnSurface(geometry)`), then run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point → district → office → race within election 728d0074, NATIONAL_LOWER → race_candidates active count + linked count). Assert each sample race returns ≥2 active candidates with non-null politician_id AND that the field includes a CHALLENGER (not only the incumbent — Pitfall-5 two-path confusion: assert `count(*) FILTER (WHERE is_incumbent=false) ≥ 1`).

    Gather the documented honest-skips (headshot from 149-03; whole-record stance skips from 149-04..10 SUMMARYs) and confirm they are pinned by exact UUID with explicit ORDER BY inside the 149-verify.sql TEMP-table pin set; if any are missing, add them to the gate's pin list (this is the only edit allowed to 149-verify.sql here — a pin-list update, not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/149-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 149-coordinate-smoke.ts is read-only and asserts ≥3 CA House races surface with ≥2 active candidates incl. ≥1 challenger for an in-district centroid.
    - All documented honest-skips are pinned by exact UUID with explicit ORDER BY in 149-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for ≥3 sample districts; honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 149-verify.sql gate green</name>
  <read_first>
    - backend/scripts/149-verify.sql (the full gate)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-VALIDATION.md (§Validation Sign-Off checklist)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/149-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (records → 149-01; headshots → 149-03; stances → 149-04..10) and re-run after fix — do NOT relax an assertion to make it pass. Confirm the gate remains House-scoped (no Governor-race contamination) and write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/149-verify.sql >/tmp/149gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -20 /tmp/149gate.log; exit 1; fi; grep -c 'PASS' /tmp/149gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 149-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 52 CA House races each ≥2 active candidates, 0 null politician_id; 0 duplicate full_name; Ruiz deduped; D-04 both-advancers present; every new candidate has a headshot (or pinned skip); 0 unsourced stances; federal-24 coverage or pinned whole-record skip.
    - Coordinate smoke (Task 1) passes for ≥3 districts.
  </acceptance_criteria>
  <done>149-verify.sql green (exit 0); coordinate smoke green; Phase 149 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke → production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-17 | Repudiation | gate passes while a district silently has no challenger | mitigate | Coordinate smoke asserts ≥1 is_incumbent=false candidate per sample race (Pitfall-5) |
| T-149-18 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-149-07 | Repudiation | Governor-race contamination false-passes a count | mitigate | Gate House-scoped (NATIONAL_LOWER + 728d0074); re-confirmed write-free here |
| T-149-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 149-verify.sql exits 0, all PASS.
- 149-coordinate-smoke.ts surfaces ≥3 CA House races with challenger present.
</verification>

<success_criteria>
- All USHC-02/03/04/05 + D-04 assertions PASS; coordinate smoke green; CA Wave-1 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-11-SUMMARY.md` when done (record: final per-district candidate counts, total new records, total stanced candidates, all honest-skips pinned, gate PASS log, smoke results — this becomes the CA half of the Phase 152 full-144 gate input).
</output>
