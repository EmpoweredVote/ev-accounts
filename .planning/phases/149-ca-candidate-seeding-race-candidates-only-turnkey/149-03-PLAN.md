---
phase: 149-ca-candidate-seeding-race-candidates-only-turnkey
plan: 03
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - backend/scripts/seed-ca-house-headshots.py
  - backend/data/seed-ca-2026-house/149-03-headshots.manual.txt
autonomous: true
requirements: [USHC-04]
must_haves:
  truths:
    - "Every newly-seeded CA candidate has a 600×750 headshot mirrored to the politician_photos bucket and a politician_images row, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/scripts/seed-ca-house-headshots.py"
      provides: "Headshot pipeline targeting exactly the 149-01 new-candidate set (clone of seed-state-exec-headshots.py)"
    - path: "backend/data/seed-ca-2026-house/149-03-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source portraits where Wikipedia has none"
  key_links:
    - from: "new candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-ca-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded CA candidate a headshot following the established find-headshots conventions (600×750, free-license, Storage-mirrored + politician_images row).

Purpose: USHC-04. race_candidates resolve photos via `COALESCE(rc.photo_url, pi.url)` lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`. Reused incumbents/previously-seeded figures already have headshots — only the 149-01 new candidates need them.
Output: The headshot script + a manual-source list; every new candidate has an image row or a documented honest-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md
@backend/scripts/seed-state-exec-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone headshot pipeline to target the 149-01 new-candidate set + run it</name>
  <read_first>
    - backend/scripts/seed-state-exec-headshots.py (the verified pipeline: Wikipedia pageimages → free-license check → 4:5 crop → 600×750 LANCZOS q90 → PUT politician_photos/{uuid}-headshot.jpg x-upsert → INSERT politician_images WHERE NOT EXISTS; `--manual` reads external_id|image_url|license and resolves UUID by external_id; the default target query self-selects via an offices join — see lines ~217–226)
    - backend/data/seed-ca-2026-house/149-01-reconciliation.csv (the NEW candidates + their assigned negative external_ids)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md (the authoritative new-candidate external_id list)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md (§Code Examples headshot; §Open Q2 — challengers have NO offices row, so the default offices-join target query will NOT find them: target by explicit external_id list instead)
  </read_first>
  <action>
    Clone `seed-state-exec-headshots.py` to `backend/scripts/seed-ca-house-headshots.py`, swapping ONLY the target-selection query: because new challengers have NO `essentials.offices` row (149-01 / A2), the original offices-join self-target query returns nothing for them. Replace it with a query that selects the new CA-2026-House candidate set by explicit negative external_id list (from 149-01-SUMMARY) `AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id = p.id)`. Keep every guard intact: free-license-only, wrong-person guard, 4:5 crop → 600×750, x-upsert to `politician_photos`, idempotent `INSERT … WHERE NOT EXISTS`.

    Run the automatic Wikipedia pass first: `python backend/scripts/seed-ca-house-headshots.py` (the script reads .env itself). For candidates with no free Wikipedia portrait, build `backend/data/seed-ca-2026-house/149-03-headshots.manual.txt` with `external_id|image_url_or_path|license` lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL — MEMORY one-try rule), then run `python backend/scripts/seed-ca-house-headshots.py --manual backend/data/seed-ca-2026-house/149-03-headshots.manual.txt`.

    For any candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 149-verify.sql can pin it — do NOT use a placeholder or wrong-person image.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md','utf8'); const ids=[...sum.matchAll(/-600[0-9]{4}/g)].map(m=>+m[0]); if(!ids.length){console.error('no new external_ids found in SUMMARY');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new candidates still missing headshot:', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-ca-house-headshots.py targets the new-candidate set by explicit external_id list (not the offices-join query) and keeps all original guards.
    - Every new CA candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) recorded in the SUMMARY.
    - Each image is 600×750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed.
    - Re-running the script inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script run; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) → Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-09 | Spoofing | wrong-person image attached to a candidate | mitigate | Pipeline wrong-person guard retained; manual-list sources are human-verified portraits; spot-check sample in 149-11 |
| T-149-10 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained from seed-state-exec-headshots.py |
| T-149-11 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on the candidate UUID |
| T-149-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo; 149-RESEARCH §Package Legitimacy Audit: n/a) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (149-11): sample new headshots against source pages for identity.
</verification>

<success_criteria>
- Every new CA candidate has a 600×750 politician_images row (or documented honest-skip).
- Idempotent; all pipeline guards intact.
</success_criteria>

<output>
Create `.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-03-SUMMARY.md` when done (record: candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs used).
</output>
