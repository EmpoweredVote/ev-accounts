---
phase: 149-ca-candidate-seeding-race-candidates-only-turnkey
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/149-verify.sql
autonomous: true
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
must_haves:
  truths:
    - "A standalone, write-free 149-verify.sql exists asserting every USHC-02/03/04/05 + D-04 success criterion, House-scoped to the 52 CA races"
    - "Every assertion excludes the 53rd (Governor) race so the ~76 pre-existing Gov candidates cannot false-fail any count"
    - "Any whole-record stance honest-skip is pinned by exact UUID with the query's exact ORDER BY"
  artifacts:
    - path: "backend/scripts/149-verify.sql"
      provides: "Read-only labeled-assertion gate for Phase 149 (USHC-02/03/04/05 + D-04), House-scoped, honest-skip-pinnable"
      contains: "NATIONAL_LOWER"
      min_lines: 80
  key_links:
    - from: "149-verify.sql assertions"
      to: "the 52 CA House races within election 728d0074"
      via: "d.district_type='NATIONAL_LOWER' predicate on every query"
      pattern: "NATIONAL_LOWER"
---

<objective>
Author the read-only `backend/scripts/149-verify.sql` gate that every Phase 149 wave runs to self-check, and that 149-11 runs as the final consolidated proof. It asserts records (USHC-02), race wiring (USHC-03), headshots (USHC-04), stances (USHC-05), and the same-party-general invariant (D-04) — all scoped to the 52 CA House races.

Purpose: Wave 0 test infrastructure (nyquist). The gate must exist before later waves so each can verify its slice. This is the single most trap-prone artifact — the 53rd Governor race carries ~76 pre-existing candidates and will false-fail any election-wide count.
Output: A standalone, write-free `.sql` gate (the 148-verify.sql / verify-phase-141-144.sql precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-VALIDATION.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@backend/scripts/148-verify.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 149-verify.sql — House-scoped labeled assertions for USHC-02/03/04/05 + D-04</name>
  <read_first>
    - backend/scripts/148-verify.sql (precedent: read-only DO-block labeled assertions; CREATE TEMP TABLE … ON COMMIT DROP for diffing is the accepted write-free pattern)
    - backend/scripts/verify-phase-141-144.sql (consolidated-gate precedent: labeled PASS/FAIL per requirement; honest-skip set pinned by exact id with exact ORDER BY — the 143 lesson)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-VALIDATION.md (§Gate Scoping Rules + Per-Task Verification Map — the exact assertions required)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-RESEARCH.md (§Pitfall 1 the 52-vs-53 trap; §Federal-24 Topic Set — the 24 topic_keys for the stance-coverage assertion)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-FIELD-TABLE.md (the 9 same-party-general districts: CA-4/7/11/12/14/29/34/37/40)
  </read_first>
  <action>
    Write `backend/scripts/149-verify.sql` as a write-free, labeled-assertion gate. Every query MUST be scoped to the 52 CA House races via the join chain `essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id` filtered `r.election_id='728d0074-8a8d-49e3-a68c-78ccdd15434f' AND d.district_type='NATIONAL_LOWER'`. NEVER write an election-wide count — the 53rd (Governor) race carries ~76 candidates and will false-fail (Pitfall 1).

    Labeled assertions (each as `DO $$ BEGIN IF NOT (…) THEN RAISE EXCEPTION 'FAIL <label>: %', detail; END IF; RAISE NOTICE 'PASS <label>'; END $$;`):
    - USHC-03a — all 52 House races have ≥2 active race_candidates.
    - USHC-03b — 0 active race_candidates rows with NULL politician_id (House-scoped).
    - USHC-02a — 0 duplicate full_name among ACTIVE CA House candidates (`GROUP BY lower(full_name) HAVING count(*)>1` over the 52 races returns 0 rows) — catches the v2.4 duplicate-incumbent trap.
    - USHC-02b — Raul Ruiz dedup: exactly 1 active "Raul Ruiz" wired to CA-25 (geo_id 0625), pinned to politician id 5238b298-6004-4bcc-94c2-ee43a9c2999e; 05349fa0-… is_active=false.
    - USHC-02c — every renominated incumbent's CA active candidate politician_id equals its 148 incumbent_pid (no new row) — diff a TEMP table loaded from a VALUES list of (geo_id, expected_pid) against live race_candidates.
    - D-04 — each of CA-4/7/11/12/14/29/34/37/40 has BOTH named advancers (from 148-FIELD-TABLE.md) as active candidates.
    - USHC-04 — every newly-seeded CA candidate (active race_candidate whose politician_id has a negative external_id in the 149-01 new-candidate band and is not a reused incumbent) has a row in essentials.politician_images; documented honest-skips pinned by exact UUID with explicit ORDER BY.
    - USHC-05a — 0 unsourced stance rows for the in-scope set: 0 = count(inform.politician_answers a over the in-scope CA candidate set WHERE NOT EXISTS an inform.politician_context c for (a.politician_id,a.topic_id) with a non-empty sources array).
    - USHC-05b — each in-scope candidate has federal-24 coverage OR is a pinned honest-skip: report per-candidate stance count over the 24 federal topic_keys; whole-record skips pinned by UUID with exact ORDER BY (USHS-14a pattern).

    Define the in-scope stance set + honest-skip pins as TEMP tables at the top so 149-04..10 can run the gate before the full set is populated; document (header comment) that data-dependent assertions legitimately FAIL pre-seed and go green only after the waves complete. Run via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/149-verify.sql`.

    Self-test that the SQL parses and runs read-only against prod NOW (pre-seed) — it must run without a syntax/permission error; data assertions may FAIL pre-seed (expected).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^[[:space:]]*--' scripts/149-verify.sql > /tmp/149v.nc && grep -qi 'NATIONAL_LOWER' /tmp/149v.nc && grep -q '728d0074' /tmp/149v.nc && ! grep -qiE 'DELETE |UPDATE |INSERT[[:space:]]+INTO[[:space:]]+(essentials|inform)' /tmp/149v.nc && echo PASS-scoped-and-write-free</automated>
  </verify>
  <acceptance_criteria>
    - 149-verify.sql contains all 9 labeled assertions above; every data query is House-scoped (`NATIONAL_LOWER` + election 728d0074).
    - The file is write-free: no DELETE/UPDATE/INSERT INTO essentials|inform (TEMP-table CREATE … ON COMMIT DROP is the only allowed write, matching 148-verify.sql).
    - Honest-skip pins use explicit ORDER BY (143 lesson encoded as a header comment + in the pinned-set query).
    - The script parses and runs read-only against prod (exit-clean syntactically) NOW; data-dependent assertions may FAIL pre-seed and that is documented as expected.
  </acceptance_criteria>
  <done>149-verify.sql authored, House-scoped, write-free, all 9 assertions present, parses + runs read-only against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → production DB (read) | 149-verify.sql runs SELECT-only against live prod; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-06 | Tampering | gate accidentally mutates prod | mitigate | Write-free enforced by the verify check (no DELETE/UPDATE/INSERT INTO essentials\|inform); only TEMP-table writes allowed |
| T-149-07 | Repudiation | gate false-passes by counting the Gov race | mitigate | Every assertion House-scoped (`NATIONAL_LOWER` + 728d0074); Pitfall 1 encoded |
| T-149-08 | Repudiation | honest-skip ordering mismatch false-fails (143 lesson) | mitigate | Honest-skip pins use explicit ORDER BY matching the query |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (149-RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- The verify automated check confirms House-scoping + write-free.
- Manual: run the gate against prod pre-seed; confirm it executes (data assertions may FAIL pre-seed, which is expected).
</verification>

<success_criteria>
- 149-verify.sql exists, write-free, House-scoped, with all USHC-02/03/04/05 + D-04 labeled assertions.
- Runs read-only against prod without syntax/permission error.
</success_criteria>

<output>
Create `.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-02-SUMMARY.md` when done (record: assertion labels, the in-scope-set TEMP-table convention, and how 149-04..10 should invoke it mid-wave).
</output>
