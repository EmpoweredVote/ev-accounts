---
phase: 57-ctc-civic-spaces-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/roleService.ts
  - tests/integration/ctcCivicSpaces.test.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/contributor/me returns ctc_content_editor grant with jurisdiction_geoid for a user holding that grant"
    - "POST /api/roles/check with feature_scope=volunteer and jurisdiction_geoid=18105 returns permitted:true for exact match, permitted:false for wrong jurisdiction"
    - "POST /api/roles/check returns permitted:true for NULL-scope volunteer grant regardless of requested jurisdiction_geoid"
    - "POST /api/roles/check returns permitted:false for a user with no volunteer grant"
    - "ROLE_CACHE_TTL_SECONDS env var controls cache TTL instead of hardcoded 90s"
  artifacts:
    - path: "backend/src/lib/roleService.ts"
      provides: "Configurable cache TTL via ROLE_CACHE_TTL_SECONDS env var"
      contains: "ROLE_CACHE_TTL_SECONDS"
    - path: "tests/integration/ctcCivicSpaces.test.ts"
      provides: "HTTP-level integration tests for CTC and Civic Spaces integration"
      min_lines: 140
  key_links:
    - from: "tests/integration/ctcCivicSpaces.test.ts"
      to: "backend/src/routes/contributor.ts"
      via: "supertest GET /api/contributor/me"
      pattern: "request.*contributor/me"
    - from: "tests/integration/ctcCivicSpaces.test.ts"
      to: "backend/src/routes/roles.ts"
      via: "supertest POST /api/roles/check"
      pattern: "request.*roles/check"
---

<objective>
Write the core integration test suite proving that `GET /api/contributor/me` and `POST /api/roles/check` correctly serve CTC and Civic Spaces use cases — and make the cache TTL configurable so tests can observe expiry without 90-second waits.

Purpose: Phase 57 success criteria 1-3 require automated proof that the two existing endpoints handle ctc_content_editor and volunteer grants correctly, including NULL-scope semantics and jurisdiction matching. The TTL env var is the one production code change needed to support the grant-revoke-check lifecycle test.

Output: Modified `roleService.ts` with env var TTL, new `ctcCivicSpaces.test.ts` passing all cases.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-ctc-civic-spaces-integration/57-CONTEXT.md
@.planning/phases/57-ctc-civic-spaces-integration/57-RESEARCH.md

@backend/src/lib/roleService.ts
@backend/src/routes/contributor.ts
@backend/src/routes/roles.ts
@tests/integration/revocation.test.ts
@tests/integration/requireRole.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make roleService cache TTL configurable via env var</name>
  <files>backend/src/lib/roleService.ts</files>
  <action>
In `getCachedUserRoles`, replace the hardcoded `90` in `await cache.set(key, grants, 90)` with:

```typescript
const ttl = parseInt(process.env['ROLE_CACHE_TTL_SECONDS'] ?? '90', 10);
await cache.set(key, grants, ttl);
```

Read the env var inside the function body (not at module scope) so it can be set per-test without module re-import issues. Default remains 90s — zero production behavior change unless explicitly overridden.

Do NOT change any other part of roleService.ts.
  </action>
  <verify>
`grep -n "ROLE_CACHE_TTL_SECONDS" backend/src/lib/roleService.ts` shows the env var read.
`npx vitest run --reporter=verbose 2>&1 | tail -30` — all existing tests still pass (no regression).
  </verify>
  <done>
`getCachedUserRoles` reads `ROLE_CACHE_TTL_SECONDS` env var with 90s default. Existing tests unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HTTP-level integration tests for CTC + Civic Spaces endpoints</name>
  <files>tests/integration/ctcCivicSpaces.test.ts</files>
  <action>
Create `tests/integration/ctcCivicSpaces.test.ts` following the established pattern from `revocation.test.ts`:

**Env vars at absolute top of file (before any imports):**
```typescript
process.env['NODE_ENV'] = 'test';
process.env['SUPABASE_URL'] = 'https://test.supabase.co';
process.env['SUPABASE_ANON_KEY'] = 'test-anon-key';
process.env['SUPABASE_SERVICE_ROLE_KEY'] = 'test-service-role-key';
process.env['DATABASE_URL'] = 'postgresql://postgres:password@localhost:5432/postgres';
process.env['SUPABASE_JWT_SECRET'] = 'test-secret-32-chars-minimum-for-hs256';
process.env['ROLE_CACHE_TTL_SECONDS'] = '1';
```

**Imports:** vitest (`describe, it, expect, beforeAll, beforeEach, vi`), `supertest`, `jose` (`SignJWT`), Express type.

**JWT helper:** `signTestJwt()` — same pattern as revocation.test.ts, HS256 with `test-secret-32-chars-minimum-for-hs256`, sub = `TEST_USER_ID` (`'00000000-0000-0000-0000-000000000001'`).

**Mock `getCachedUserRoles`:**
```typescript
vi.mock('../../backend/src/lib/roleService.js', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../../backend/src/lib/roleService.js')>();
  return { ...actual, getCachedUserRoles: vi.fn() };
});
```
Then import `getCachedUserRoles` and use `vi.mocked(getCachedUserRoles)` per test.

**App import:** Dynamic `beforeAll` import of `../../backend/src/index.js` to get `app`.

**Grant helper:** Same `grant()` factory as `requireRole.test.ts` — builds `UserRoleGrant` objects with configurable slug, geoid, resourceId.

**Test suite structure — three describe blocks:**

**describe('GET /api/contributor/me — CTC integration'):**

1. `ctc_content_editor grant with jurisdiction_geoid is returned` — mock returns `[grant('ctc_content_editor', '06037')]`, GET `/api/contributor/me` with auth header, assert 200, body is array containing object with `role_slug: 'ctc_content_editor'`, `jurisdiction_geoid: '06037'`.

2. `returns empty array when user has no grants` — mock returns `[]`, assert 200, body is `[]`.

3. `returns 401 without auth token` — no Authorization header, assert 401.

**describe('POST /api/roles/check — Civic Spaces integration'):**

4. `returns permitted:true for exact volunteer jurisdiction match` — mock returns `[grant('volunteer', '18105')]`, POST `/api/roles/check` with body `{ feature_scope: 'volunteer', jurisdiction_geoid: '18105' }`, assert 200 `{ permitted: true }`.

5. `returns permitted:false for wrong jurisdiction` — mock returns `[grant('volunteer', '06037')]`, same body with `jurisdiction_geoid: '18105'`, assert 200 `{ permitted: false }`.

6. `returns permitted:true for NULL-scope volunteer (unrestricted)` — mock returns `[grant('volunteer', null)]`, body `{ feature_scope: 'volunteer', jurisdiction_geoid: '18105' }`, assert 200 `{ permitted: true }`.

7. `returns permitted:false when user has no volunteer grant` — mock returns `[]`, same body, assert 200 `{ permitted: false }`.

8. `returns 400 when feature_scope missing` — POST with empty body, assert 400.

9. `returns 401 without auth token` — no Authorization header, assert 401.

**describe('Cache TTL lifecycle — revocation reflected after expiry'):**

10. `endpoint reflects post-revocation state when cache returns empty` — This test proves the endpoint logic correctly surfaces the underlying grant state. Approach:
    - First call: `vi.mocked(getCachedUserRoles).mockResolvedValueOnce([grant('volunteer', null)])` — simulates user having an active grant in cache.
    - POST `/api/roles/check` with `{ feature_scope: 'volunteer', jurisdiction_geoid: '18105' }` — assert 200 `{ permitted: true }`.
    - Second call: `vi.mocked(getCachedUserRoles).mockResolvedValueOnce([])` — simulates what happens after revocation + cache TTL expiry (cache refreshes and finds no grant).
    - POST `/api/roles/check` with same body — assert 200 `{ permitted: false }`.
    - Add a comment in the test explaining: "// We mock getCachedUserRoles to return different values on successive calls. In production, this is what happens when a grant is revoked and the cache TTL expires — the cache refreshes from the DB and finds no grant. No actual sleep needed because vi.mock controls the return value directly."

**Use `beforeEach(() => vi.mocked(getCachedUserRoles).mockReset())` to clear mocks between tests.**

Use geoid `'06037'` (LA County) for ctc_content_editor tests, `'18105'` for volunteer tests — established test geoids.
  </action>
  <verify>
`npx vitest run tests/integration/ctcCivicSpaces.test.ts --reporter=verbose` — all 10 tests pass.
`npx vitest run --reporter=verbose 2>&1 | tail -40` — full suite passes, no regressions.
  </verify>
  <done>
9 static tests cover criteria 1-3 (ctc_content_editor grant exposure, jurisdiction match/mismatch, NULL-scope volunteer, no-grant negative case); 10th test proves endpoint reflects post-revocation state (criteria 4). All pass green.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/ctcCivicSpaces.test.ts --reporter=verbose` — all 10 tests pass
- `npx vitest run --reporter=verbose` — full test suite passes (no regressions from TTL change)
- `grep "ROLE_CACHE_TTL_SECONDS" backend/src/lib/roleService.ts` — env var present
</verification>

<success_criteria>
- roleService.ts reads ROLE_CACHE_TTL_SECONDS with 90s default
- 10 integration tests pass covering all Phase 57 success criteria (1-3 via 9 static tests, 4 via lifecycle test)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/57-ctc-civic-spaces-integration/57-01-SUMMARY.md`
</output>
