---
phase: 57-ctc-civic-spaces-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/smoke-phase57.ts
  - docs/INTEGRATION-GUIDE-v2.md
autonomous: true

must_haves:
  truths:
    - "Smoke script proves the grant -> check -> revoke -> expire -> check lifecycle end-to-end"
    - "Integration guide documents GET /api/contributor/me and POST /api/roles/check for external devs"
    - "Cache behavior is called out from the external developer's perspective"
  artifacts:
    - path: "backend/scripts/smoke-phase57.ts"
      provides: "Standalone smoke script for Phase 57 lifecycle validation"
      min_lines: 80
    - path: "docs/INTEGRATION-GUIDE-v2.md"
      provides: "Contributor Roles section for external developers"
      contains: "Contributor Roles"
  key_links:
    - from: "backend/scripts/smoke-phase57.ts"
      to: "/api/roles/check"
      via: "fetch calls to check endpoint"
      pattern: "roles/check"
    - from: "docs/INTEGRATION-GUIDE-v2.md"
      to: "/api/contributor/me"
      via: "endpoint documentation"
      pattern: "contributor/me"
---

<objective>
Create a standalone smoke script that proves the grant-check-revoke-expire-check lifecycle works end-to-end, and append a "Contributor Roles" section to the integration guide documenting both endpoints for external developers.

Purpose: Success criterion 4 requires a smoke test confirming the full lifecycle. The integration guide update gives CTC and Civic Spaces developers everything they need to integrate without further communication.

Output: `backend/scripts/smoke-phase57.ts` runnable locally, updated `docs/INTEGRATION-GUIDE-v2.md` with Contributor Roles section.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-ctc-civic-spaces-integration/57-CONTEXT.md
@.planning/phases/57-ctc-civic-spaces-integration/57-RESEARCH.md

@backend/scripts/smokeTest.ts
@backend/src/routes/contributor.ts
@backend/src/routes/roles.ts
@docs/INTEGRATION-GUIDE-v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 57 smoke script</name>
  <files>backend/scripts/smoke-phase57.ts</files>
  <action>
Create `backend/scripts/smoke-phase57.ts` following the pattern of `backend/scripts/smokeTest.ts`:

**Config:**
- `BASE_URL` from `process.env['BASE_URL'] ?? 'http://localhost:3000'` (strip trailing slash)
- `TIMEOUT_MS = 10_000`
- Requires a valid JWT token: read from `process.env['SMOKE_TOKEN']` — the script cannot create JWTs without knowing the signing key. If not set, print usage instructions and exit 1.
- Requires `ROLE_CACHE_TTL_SECONDS=3` to be set on the target server for the TTL test to complete in reasonable time.

**Helpers:**
- `fetchWithTimeout(url, options)` — same AbortController pattern as smokeTest.ts
- `fetchAuth(path, method, body?)` — wraps fetchWithTimeout with `Authorization: Bearer ${SMOKE_TOKEN}`, `Content-Type: application/json`

**Checks (sequential — each depends on the previous):**

1. **Check: GET /api/contributor/me returns array** — GET, assert 200, assert body is array. Log the grants array for inspection. `[PASS]` / `[FAIL]`.

2. **Check: POST /api/roles/check with volunteer grant** — POST `/api/roles/check` with body `{ feature_scope: "volunteer", jurisdiction_geoid: "18105" }`, assert 200, log `{ permitted }` result. `[PASS]` / `[FAIL]` (pass if response is valid JSON with `permitted` boolean — the actual value depends on the user's grants).

3. **Check: POST /api/roles/check with no matching grant** — POST `/api/roles/check` with body `{ feature_scope: "nonexistent_role_slug_smoke_test" }`, assert 200, assert `permitted === false`. `[PASS]` / `[FAIL]`.

4. **Check: Cache TTL lifecycle** — This check is OPTIONAL (gated by `process.env['SMOKE_ADMIN_TOKEN']`):
   - POST admin grant volunteer role to the smoke user (requires admin token)
   - POST `/api/roles/check` with `{ feature_scope: "volunteer" }` — assert `permitted: true`
   - POST admin revoke volunteer role
   - Immediately POST `/api/roles/check` — log result (may still be cached as `permitted: true`)
   - Wait 4 seconds (TTL=3 + 1s buffer)
   - POST `/api/roles/check` again — assert `permitted: false`
   - If `SMOKE_ADMIN_TOKEN` not set, print `[SKIP] Cache TTL lifecycle — set SMOKE_ADMIN_TOKEN to enable` and continue

**Output format:**
```
Phase 57 Smoke Test — CTC + Civic Spaces Integration
=====================================================
Base URL: http://localhost:3000

[PASS] GET /api/contributor/me returns grant array
[PASS] POST /api/roles/check returns valid response
[PASS] POST /api/roles/check rejects nonexistent role
[SKIP] Cache TTL lifecycle — set SMOKE_ADMIN_TOKEN to enable

Results: 3/3 passed, 0 failed, 1 skipped
```

**Exit code:** 0 if all non-skip checks pass, 1 if any check fails.

**Usage header comment:**
```
Usage:
  SMOKE_TOKEN=<jwt> npx tsx backend/scripts/smoke-phase57.ts
  BASE_URL=https://api.empowered.vote SMOKE_TOKEN=<jwt> npx tsx backend/scripts/smoke-phase57.ts

  Optional: SMOKE_ADMIN_TOKEN=<admin-jwt> to enable grant/revoke lifecycle test
  Note: Server must have ROLE_CACHE_TTL_SECONDS=3 for the lifecycle test
```
  </action>
  <verify>
`npx tsx backend/scripts/smoke-phase57.ts 2>&1 | head -5` — prints usage instructions (no SMOKE_TOKEN set).
TypeScript compiles without errors: `npx tsc --noEmit backend/scripts/smoke-phase57.ts` or verify no red in editor.
  </verify>
  <done>
Smoke script runs locally, prints clear pass/fail output, covers the grant-check-revoke-expire-check lifecycle (when admin token provided), and serves as a reference implementation for CTC/Civic Spaces developers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Contributor Roles section to integration guide</name>
  <files>docs/INTEGRATION-GUIDE-v2.md</files>
  <action>
Read the full `docs/INTEGRATION-GUIDE-v2.md`. Append a new section **8.18 Contributor Roles** after the existing section 8.17 (Essentials Politicians), before section 9 (Anti-Patterns).

The section should contain:

**Section heading:** `### 8.18 Contributor Roles (`/api/contributor`, `/api/roles/check`)`

**Intro paragraph:** Brief explanation that these endpoints allow external systems (CTC, Civic Spaces) to check whether a user holds a specific role grant without accounts writing to external systems. External apps call these endpoints and enforce their own gates based on the response.

**Endpoint table:**

| Method | Path | Auth | Body | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/contributor/me` | Auth | — | Caller's active role grants as bare array. Each item: `{ role_slug, feature_scope, jurisdiction_geoid, resource_id }`. |
| `POST` | `/api/roles/check` | Auth | `{ feature_scope, jurisdiction_geoid?, resource_id? }` | Check if caller holds a matching grant. Returns `{ permitted: boolean }`. |

**`GET /api/contributor/me` response example:**
```json
[
  {
    "role_slug": "ctc_content_editor",
    "feature_scope": "platform",
    "jurisdiction_geoid": "06037",
    "resource_id": null
  }
]
```

**`POST /api/roles/check` request/response example:**
```json
// Request
{ "feature_scope": "volunteer", "jurisdiction_geoid": "18105" }

// Response
{ "permitted": true }
```

**NULL-scope grants note:** A grant with `jurisdiction_geoid: null` is unrestricted — `POST /api/roles/check` returns `{ permitted: true }` regardless of the `jurisdiction_geoid` in the request body. This is by design: an unrestricted volunteer can operate in any jurisdiction.

**Cache behavior note (framed from external dev perspective):**
> **Cache behavior:** Results from `POST /api/roles/check` are cached server-side. After a role grant is revoked, the check endpoint may still return `{ permitted: true }` for up to 90 seconds (configurable via `ROLE_CACHE_TTL_SECONDS`). Design your integration to tolerate this window — do not treat role checks as real-time.

**Integration pattern (CTC example):**
```
1. User opens CTC content editor
2. CTC calls GET /api/contributor/me (with user's JWT)
3. CTC scans array for role_slug === 'ctc_content_editor'
4. If found and jurisdiction_geoid matches → allow editing
5. If not found → show "no editor access" message
```

**Integration pattern (Civic Spaces example):**
```
1. User attempts privileged write in Civic Spaces
2. Civic Spaces calls POST /api/roles/check with { feature_scope: "volunteer", jurisdiction_geoid: "<user's jurisdiction>" }
3. If permitted: true → allow the write
4. If permitted: false → reject with 403
```

Also update the **Contents** list at the top of the file to add `8.18 Contributor Roles` if there is a contents section that lists subsections of section 8.

Do NOT add a new top-level section number — this goes under the existing section 8 (Endpoint Inventory).
  </action>
  <verify>
`grep -n "8.18 Contributor Roles" docs/INTEGRATION-GUIDE-v2.md` — section exists.
`grep -n "ROLE_CACHE_TTL_SECONDS" docs/INTEGRATION-GUIDE-v2.md` — cache note present.
`grep -n "contributor/me" docs/INTEGRATION-GUIDE-v2.md` — endpoint documented.
  </verify>
  <done>
Integration guide has a Contributor Roles section documenting both endpoints with request/response shapes, NULL-scope semantics, cache behavior note, and integration patterns for CTC and Civic Spaces.
  </done>
</task>

</tasks>

<verification>
- `npx tsx backend/scripts/smoke-phase57.ts 2>&1 | head -5` — prints usage (no token = exits with instructions)
- `grep "8.18 Contributor Roles" docs/INTEGRATION-GUIDE-v2.md` — section present
- `grep "ROLE_CACHE_TTL_SECONDS" docs/INTEGRATION-GUIDE-v2.md` — cache note present
- `grep "permitted" docs/INTEGRATION-GUIDE-v2.md` — response shape documented
</verification>

<success_criteria>
- Smoke script compiles and runs (prints usage when no token provided)
- Smoke script covers grant-check-revoke-expire-check lifecycle (success criterion 4)
- Integration guide Contributor Roles section documents both endpoints with response shapes
- Cache behavior called out from external developer perspective
</success_criteria>

<output>
After completion, create `.planning/phases/57-ctc-civic-spaces-integration/57-02-SUMMARY.md`
</output>
