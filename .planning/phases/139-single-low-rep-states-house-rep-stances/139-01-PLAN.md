---
phase: 139-single-low-rep-states-house-rep-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-20-hi-id-mt-house.csv
  - backend/data/stance-research/hi-id-mt-house/_TOPIC_SCALE.txt
  - backend/data/stance-research/hi-id-mt-house/_merge.ts
  - backend/data/stance-research/hi-id-mt-house/_push.ts
autonomous: false
requirements: [USHS-13]

must_haves:
  truths:
    - "Each of the 6 HI/ID/MT House reps (external_id -15001,-15002,-16001,-16002,-30001,-30002) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation; no isidewith-only rows kept; no caucus/committee-membership proxy rows kept"
    - "Live topics fetched fresh; city + judicial-* topics skipped (25 federal topics in scope)"
    - "In-scope reps reach exact counts; verified via per-scope external_id counts (not the global counter)"
  artifacts:
    - path: "backend/data/stance-research/2026-06-20-hi-id-mt-house.csv"
      provides: "Reviewable stance research CSV for HI+ID+MT House reps (6 reps)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for all 6 Hawaii + Idaho + Montana US House reps
(HI-1,2 / ID-1,2 / MT-1,2; external_id -15001,-15002,-16001,-16002,-30001,-30002), using the
research-stances skill, following the validated Phase 127-138 playbook. Concurrency cap = 3. WebFetch-only.

Purpose: Eighth (final research) wave of v2.17 (USHS-13, single/low-rep states). Each rep currently has
0 stances; pure scale-out of the v2.16 pipeline. Output: CSV of record committed; stances + context +
quotes pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\138-ks-ms-nv-ne-nm-house-rep-stances\138-05-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps:

| external_id | name | district |
|-------------|------|----------|
| -15001 | Ed Case (D) | HI-1 |
| -15002 | Jill N. Tokuda (D) | HI-2 |
| -16001 | Russ Fulcher | ID-1 |
| -16002 | Michael K. Simpson | ID-2 |
| -30001 | Ryan K. Zinke | MT-1 |
| -30002 | Troy Downing (2024 freshman) | MT-2 |

All currently have 0 stances. Resolve each politician_id by external_id->UUID at run time.
Document the actual record only, never infer from party. EXPECTED THIN RECORD: Troy Downing MT-2 (2024
freshman, prior MT State Auditor — that record is documentable); honest-skip topics with no evidence.

KEY RULES (validated Phase 127-138 playbook):
- **25 federal topics** = 44 live minus 11 city-level + all judicial-*. Fetch fresh; write the scale to
  `backend/data/stance-research/hi-id-mt-house/_TOPIC_SCALE.txt` (or copy ks-house/_TOPIC_SCALE.txt); each agent Reads it.
- **Concurrency: up to 3 agents**. WebFetch ONLY — never WebSearch or Playwright.
- **EFFICIENCY: try each source URL ONCE; if it 403s/404s/times out, move on immediately — do NOT retry or loop; finish in a few minutes.**
- **Per-rep CSV -> merge + RFC-4180-validate** (csv-parse/sync). CSV quote-repair: collapse `""""`->`"""`
  ONLY (4->3); NEVER 3->2 (sed-clone ks-house/_merge.ts). Per-row artifacts: stray trailing `"` -> `sed -i 's/,"$/,/'`; `,""text""` wrap (no escaped `""` inside, verify grep) -> `sed 's/""/"/g'`.
- **isidewith.com is BELOW the bar** — DROP isidewith-only rows. AFA/candidate questionnaires OK.
- **Proxy-row review gate (phase 138, 7 drops):** DROP rows scored from "no direct vote found, overall record alignment" / "committee membership" / "caucus member indicating support." KEEP a caucus row ONLY when that caucus has a published platform DIRECTLY on the topic.
- **SSM=5**: requires documented anti-recognition vote OR explicit one-man-one-woman amendment support; else cap/drop.
- **Resolve politician_id by external_id->UUID map**, not by name.
- **Five-chairs framing**: match documented record to exact stance text; never infer from party.
  Lean on Ballotpedia, OnTheIssues, Wikipedia, LCV, VoteSmart. house.gov/congress.gov/govtrack 403 to WebFetch.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research HI+ID+MT House (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\138-ks-ms-nv-ne-nm-house-rep-stances\138-05-SUMMARY.md (latest validated playbook)
- backend/data/stance-research/ks-house/_merge.ts (merge+repair to sed-clone)
- backend/data/stance-research/ks-house/_TOPIC_SCALE.txt (current 25-topic scale to copy)
</read_first>
<action>
1. Ensure `backend/data/stance-research/hi-id-mt-house/_TOPIC_SCALE.txt` holds the current 25 federal topics (copy ks-house/_TOPIC_SCALE.txt).
2. Dispatch `politician-stance-researcher` agents for the 6 reps, up to 3 concurrent. Each prompt: five-chairs framing, Read `_TOPIC_SCALE.txt`, WebFetch ONLY, the EFFICIENCY one-try-per-URL rule, explicit RFC-4180 escaping rule, drop isidewith-only AND caucus/committee-membership proxy rows, write to `backend/data/stance-research/hi-id-mt-house/<surname>.csv`, include external_id on every row.
3. Verify each agent's rows before dispatching the next wave; retry empty-output agents solo.
4. Merge the 6 per-rep CSVs into `backend/data/stance-research/2026-06-20-hi-id-mt-house.csv` (sed-clone ks-house/_merge.ts: set OUT + IN_SCOPE=[-15001,-15002,-16001,-16002,-30001,-30002]); validate every row.
</action>
<acceptance_criteria>
- 2026-06-20-hi-id-mt-house.csv exists with rows for the 6 reps (or documented honest-skips); merge parser reports 0 problems
- Every row has value (1-5) + reasoning + >=1 real http source_url; topic_key is one of the 25 fetched federal topics; no isidewith-only or membership-proxy rows
- Each of HI-1,2 / ID-1,2 / MT-1,2 accounted for: stances OR an honest-skip noted (no party-inferred values); Downing MT-2 documented from his actual record (incl. State Auditor tenure) or honest-skipped
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push HI+ID+MT House to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/ks-house/_push.ts (reusable external_id-keyed push)
- backend/data/stance-research/2026-06-20-hi-id-mt-house.csv
</read_first>
<action>
Present the STEP 3 approval summary. Standing user decision this milestone: auto-push batches that merge 0-problems AND 0-unsourced (still show the summary), no per-batch approval pause. Push via the external_id-keyed `_push.ts` pattern (copy to `backend/data/stance-research/hi-id-mt-house/_push.ts`), resolving politician_id by external_id->UUID + topic_id by topic_key, upserting inform.politician_answers + inform.politician_context (ON CONFLICT DO UPDATE on both), pushing quotes to essentials.quotes with Read & Rank + surname leak-check. `_push.ts` does NOT load dotenv — run with `set -a && source .env && set +a && node --import tsx .../_push.ts <csv>`. Commit the CSV (force-add past gitignore). Verify per-scope: the 6 reps reach exact counts, unsourced-in-scope = 0. If MCP Supabase tokens expire mid-run, fall back to a `node --import tsx` script inside backend/ that imports `pool` from `src/lib/db.js` (load `dotenv/config` first).
</action>
<acceptance_criteria>
- Each of the 6 reps has >=1 inform.politician_answers row OR a documented honest-skip
- Every new answer has a paired sourced context row (0 unsourced)
- Push reported counts with zero rollbacks; per-scope external_id counts confirm isolation
- CSV committed (force-added past gitignore)
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content lands in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT DO UPDATE; re-runs safe; single BEGIN/COMMIT with ROLLBACK on error |
| T-139-02 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check; surname-leaking quotes never selected |
| T-139-03 | Integrity (scope creep) | non-in-scope reps | mitigate | push keyed strictly to the 6 HI/ID/MT external_ids; verified via per-scope counts |

No high-severity threats: read-only web research + an idempotent service-role upsert. No package installs.
</threat_model>

<verification>
- 6 HI/ID/MT reps each have sourced stances or a documented skip; every answer has paired sourced context
- No party-inferred stances; no isidewith-only or membership-proxy rows; 25 federal topics; concurrency held at 3
- Per-scope counts confirm writes isolated (no global-counter reliance)
</verification>

<success_criteria>
USHS-13 advanced: HI-1,2 / ID-1,2 / MT-1,2 (6 reps) covered with sourced stances or honest-skips, all answers paired with sourced context, CSV committed.
</success_criteria>

<output>
Create `.planning/phases/139-single-low-rep-states-house-rep-stances/139-01-SUMMARY.md` when done.
</output>
