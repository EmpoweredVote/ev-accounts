---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 11
type: execute
wave: 4
depends_on: ["163-01", "163-02", "163-03", "163-04", "163-05", "163-06", "163-07", "163-08", "163-09", "163-10"]
files_modified:
  - backend/scripts/163-verify.sql
  - backend/scripts/163-coordinate-smoke.ts
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04, USHC3-05]
must_haves:
  truths:
    - "A read-only gate proves all 36 WI/CO/AL/SC/LA districts: correct race counts (WI 8 + CO 8 + AL 7 + SC 7 + LA 6 = 36), 0 NULL office_id/politician_id, 0 duplicate full_name per state, LA exactly-6-jungle-races (primary_party NULL), 0 unsourced stances, headshot coverage or pinned skip"
    - "NEW AL-SEVERE + LA-SEVERE blocks prove every severe AL/LA race's parent election is the withheld 'Polygon Pending' election (not the general); a NEW CO1-DEGETTE block proves CO-1 has exactly 2 candidates and DeGette (610bb358) is absent"
    - "A coordinate smoke proves a non-severe district in each of WI/CO/AL/SC/LA surfaces its challenger-inclusive field, AND a severe AL district + a severe LA district each surface ZERO races"
  artifacts:
    - path: "backend/scripts/163-verify.sql"
      provides: "Consolidated read-only 36-district phase gate incl. AL-SEVERE + LA-SEVERE + CO1-DEGETTE assertion blocks"
      contains: "Polygon Pending"
    - path: "backend/scripts/163-coordinate-smoke.ts"
      provides: "5-state positive samples + AL-severe + LA-severe negative samples"
      contains: "SEVERE"
  key_links:
    - from: "163-verify.sql AL-SEVERE + LA-SEVERE assertions"
      to: "the 163-01 severe geo_id lists + 163-04/06 withheld elections"
      via: "assert severe races' parent election = Polygon Pending"
      pattern: "Polygon Pending"
    - from: "163-verify.sql CO1-DEGETTE assertion"
      to: "DeGette pid 610bb358-bae9-4ce8-beaf-a33a27d5ba49"
      via: "assert CO-1 has exactly 2 candidates, DeGette absent"
      pattern: "610bb358"
---

<objective>
Author the phase-closing 36-district mini-gate: 163-verify.sql (clone 162-verify.sql structurally + NEW AL-SEVERE, LA-SEVERE, and CO1-DEGETTE blocks) and 163-coordinate-smoke.ts (clone 162-coordinate-smoke.ts, extended to WI/CO/AL/SC/LA + AL-severe and LA-severe negative samples). This is the phase gate proving USHC3-02/03/04/05 across all 36 districts before /gsd-verify-work. Read-only (no DB writes except CREATE TEMP TABLE ON COMMIT DROP). The AL/LA-non-surfacing and CO1-DeGette assertions become standing invariants Phase 166's consolidated gate inherits; Phase 164.1 must un-withhold AL+LA severe on polygon refresh (mirror the MO/TN carry-forward).

Output: 163-verify.sql (green) + 163-coordinate-smoke.ts (green: 5 positive + AL-severe + LA-severe negatives).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-al-correspondence-audit.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-la-correspondence-audit.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-04-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-06-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-07-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-08-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-09-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-10-SUMMARY.md
@backend/scripts/162-verify.sql
@backend/scripts/162-coordinate-smoke.ts

<interfaces>
163-verify.sql (clone 162-verify.sql): resolve 7 elections — wi_eid ('WI 2026 Statewide General'), co_eid ('CO 2026 Statewide General'), al_gen_eid ('AL 2026 Statewide General'), al_withheld_eid ('AL 2026 Congressional Redistricting - Polygon Pending'), sc_eid ('SC 2026 Statewide General'), la_gen_eid ('LA 2026 Statewide General'), la_withheld_eid ('LA 2026 Congressional Redistricting - Polygon Pending'). Combined _house temp table scoped by election_id IN (7 eids) AND district_type='NATIONAL_LOWER' AND substr(geo_id,1,2) IN ('55','08','01','45','22'). CRITERION 1 (race-count): WI 8 + CO 8 + AL 7 + SC 7 + LA 6 = 36. Keep the party-null-column check (races.race_candidates has no party column), 0 NULL office_id, 0 NULL active politician_id, 0 duplicate full_name per state.
NEW AL-SEVERE block (clone the MO-SEVERE structure): for each severe AL geo_id (from 163-al-correspondence-audit.md's "Severe geo_id list:"), assert its race's parent election_id = al_withheld_eid (leaked count = 0); assert each non-severe AL geo_id's race election_id = al_gen_eid.
NEW LA-SEVERE block (same structure): severe LA geo_ids → la_withheld_eid; non-severe LA geo_ids → la_gen_eid. ADDITIONALLY assert LA has exactly 6 races and every LA race has primary_party IS NULL (jungle-model invariant), and NO December-2026 LA runoff election exists.
NEW CO1-DEGETTE block (novel, analogous to 162's IN9-FLAG): assert the CO-1 race (geo_id 0801) has exactly 2 active race_candidates and that DeGette's pid 610bb358-bae9-4ce8-beaf-a33a27d5ba49 has 0 rows in it; RAISE EXCEPTION otherwise.
USHC3-04/05 blocks + honest-skip pin tables (_stance_skip ORDER BY resolved politician_id; _img_skip ORDER BY external_id) — populate pins from the 163-07/08/09/10 SUMMARY gate-pinned whole-record skips + the seed-plan headshot honest-skips (reconstruct live at gate-authoring time via the "active new-band candidate lacking an image" query). _new_cands band bounds = the 5 phase bands (-550899..-550101, -80899..-80101, -10799..-10101, -450799..-450101, -220699..-220101).
163-coordinate-smoke.ts (clone 162-coordinate-smoke.ts): STATE_CONFIG gets WI/CO/AL/SC/LA (AL uses 'AL 2026 Statewide General', LA uses 'LA 2026 Statewide General' — the surfacing elections). SAMPLES = one in-district contested coordinate per state (5 positive, MIN_DISTRICTS=5). NEW SEVERE_AL_GEO_ID + SEVERE_LA_GEO_ID constants (a severe geo_id from each audit) + negative-sample blocks proving a coordinate inside each severe district surfaces ZERO races on the state's surfacing election. All PostGIS calls use the public. schema prefix (public.ST_Covers / public.ST_PointOnSurface / public.ST_SetSRID / public.ST_MakePoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 163-verify.sql (clone 162 + AL-SEVERE + LA-SEVERE + CO1-DEGETTE blocks) and run it green</name>
  <read_first>
    - backend/scripts/162-verify.sql (the full structural analog — per-state scoping, MO-SEVERE + IN9-FLAG templates, pin-table ordering discipline)
    - 163-al-correspondence-audit.md + 163-la-correspondence-audit.md (the severe geo_id lists for the AL-SEVERE / LA-SEVERE blocks)
    - 163-04/06 SUMMARYs (severe geo_ids + withheld election names as applied); 163-07/08/09/10 SUMMARYs (gate-pinned whole-record stance skips)
  </read_first>
  <action>
    Clone backend/scripts/162-verify.sql → backend/scripts/163-verify.sql. Resolve the 7 elections (WI/CO/SC generals + AL gen/withheld + LA gen/withheld). Build the _house temp table scoped to geo prefixes 55/08/01/45/22. Substitute CRITERION 1 counts (WI 8 + CO 8 + AL 7 + SC 7 + LA 6 = 36). Keep the party-null-column, NULL-office_id, NULL-active-politician_id, and 0-duplicate-full_name-per-state criteria. Author the NEW AL-SEVERE block (clone MO-SEVERE): every severe AL geo_id's race parent election = al_withheld_eid; every non-severe AL geo_id → al_gen_eid. Author the NEW LA-SEVERE block (same structure) PLUS assert LA has exactly 6 races, every LA race has primary_party IS NULL, and no December-2026 LA runoff election row exists. Author the NEW CO1-DEGETTE block: CO-1 (geo_id 0801) has exactly 2 active race_candidates and DeGette pid 610bb358-... has 0 rows there; RAISE EXCEPTION otherwise. Populate _stance_skip (ORDER BY resolved politician_id) and _img_skip (ORDER BY external_id) from the 163-07/08/09/10 SUMMARYs' pinned whole-record skips + a live reconstruction of headshot honest-skips; set _new_cands band bounds to the 5 phase bands. Keep every block as SELECT ... INTO v_x; IF v_x <> expected THEN RAISE EXCEPTION ...; RAISE NOTICE 'PASS ...'. WRITE-FREE except CREATE TEMP TABLE ... ON COMMIT DROP. Run it against prod and iterate until green.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/163-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - 163-verify.sql runs to completion with psql exit 0 (all criteria PASS, no RAISE EXCEPTION)
    - AL-SEVERE + LA-SEVERE blocks assert every severe geo_id's race election = the state's Polygon Pending election (matching the 163-01 severe sets); LA-SEVERE also asserts 6 jungle races all primary_party NULL + no Dec-2026 runoff; CO1-DEGETTE asserts CO-1 has exactly 2 candidates and DeGette absent
    - 36 races counted; 0 NULL office_id; 0 NULL active politician_id; 0 duplicate full_name per state; 0 unsourced stances; headshot coverage or pinned skip
  </acceptance_criteria>
  <done>The consolidated 36-district SQL gate passes green against prod, including the AL-SEVERE, LA-SEVERE, and CO1-DEGETTE invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Author 163-coordinate-smoke.ts (5-state positive + AL-severe + LA-severe negative) and run it green</name>
  <read_first>
    - backend/scripts/162-coordinate-smoke.ts (the full analog — STATE_CONFIG/SAMPLES clone; SEVERE negative-sample block; public. PostGIS prefix)
    - 163-al-correspondence-audit.md + 163-la-correspondence-audit.md (a severe geo_id from each for the negative samples)
  </read_first>
  <action>
    Clone backend/scripts/162-coordinate-smoke.ts → backend/scripts/163-coordinate-smoke.ts. Add WI/CO/AL/SC/LA to STATE_CONFIG (AL uses 'AL 2026 Statewide General', LA uses 'LA 2026 Statewide General' — the surfacing elections; same "surfacing election only" comment as MO). Set SAMPLES = one contested in-district coordinate per state (5 positive samples, MIN_DISTRICTS=5). Add SEVERE_AL_GEO_ID + SEVERE_LA_GEO_ID constants (a severe geo_id from each audit) + copy the negative-sample block twice, proving a coordinate inside each severe district surfaces ZERO races on that state's surfacing election. Keep all PostGIS calls prefixed public. (public.ST_Covers / public.ST_PointOnSurface / public.ST_SetSRID / public.ST_MakePoint). Run against prod and iterate until green.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/163-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Coordinate smoke passes: 5 positive samples (one per WI/CO/AL/SC/LA) each surface a challenger-inclusive field with non-null politician_ids
    - The AL-severe and LA-severe negative samples each surface ZERO races on the state's surfacing election (proving both withholdings work end-to-end)
    - All PostGIS functions use the public. schema prefix
  </acceptance_criteria>
  <done>The 5-state positive + AL-severe + LA-severe negative coordinate smoke passes green against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded prod data -> read-only gate | The gate is the last automated line of defense before the phase is declared done |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — read-only SQL + coordinate smoke, no runtime code changes and no DB writes (only CREATE TEMP TABLE ON COMMIT DROP). Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-11-01 | Tampering | A severe AL/LA race silently surfacing (regression) | mitigate | AL-SEVERE + LA-SEVERE blocks assert parent election = Polygon Pending; coordinate-smoke negative samples re-prove 0 races end-to-end; Phase 166 + 164.1 inherit (standing invariant) |
| T-163-11-02 | Tampering | DeGette silently re-added to CO-1 by a future migration | mitigate | CO1-DEGETTE block asserts CO-1 has exactly 2 candidates and DeGette absent; Phase 166 inherits it |
| T-163-11-03 | Tampering | LA jungle model regressing to per-party races or gaining a runoff | mitigate | LA-SEVERE block asserts exactly 6 LA races all primary_party NULL + no Dec-2026 runoff election |
| T-163-11-04 | Repudiation | False-fail from pin-table ordering mismatch | mitigate | _stance_skip ORDER BY resolved politician_id; _img_skip ORDER BY external_id (the 143 ordering lesson) |
</threat_model>

<verification>
- 163-verify.sql green (psql exit 0): 36 races, 0 NULL office_id/politician_id, 0 duplicate full_name, AL-SEVERE + LA-SEVERE + CO1-DEGETTE assertions pass, LA 6 jungle races primary_party NULL + no runoff, 0 unsourced stances, headshot coverage or pinned skip
- 163-coordinate-smoke.ts green: 5 positive + AL-severe + LA-severe negative samples
</verification>

<success_criteria>
The consolidated 36-district read-only gate proves the phase end-to-end: correct race wiring, no duplicates, AL + LA severe-district withholding verified, LA jungle model + DeGette exclusion verified, 0 unsourced stances, headshots covered/pinned — the AL-SEVERE, LA-SEVERE, and CO1-DEGETTE invariants ready to be inherited by the Phase 166 gate, and the AL+LA severe carry-forward flagged for Phase 164.1 un-withholding.
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-11-SUMMARY.md when done, recording the gate PASS output, the severe AL + LA geo_ids asserted withheld, and a note that Phase 166 + Phase 164.1 must inherit the AL-SEVERE + LA-SEVERE + CO1-DEGETTE assertions (164.1 must un-withhold AL+LA severe on polygon refresh).
</output>
