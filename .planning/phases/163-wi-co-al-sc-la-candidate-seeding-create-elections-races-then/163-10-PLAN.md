---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 10
type: execute
wave: 2
depends_on: ["163-03", "163-05"]
files_modified:
  - backend/data/stance-research/co-2026-house/_merge.ts
  - backend/data/stance-research/co-2026-house/_push.ts
  - backend/data/stance-research/co-2026-house/_push_uuid.ts
  - backend/data/stance-research/co-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/co-2026-house/163-10-co-stances.csv
  - backend/data/stance-research/sc-2026-house/_merge.ts
  - backend/data/stance-research/sc-2026-house/_push.ts
  - backend/data/stance-research/sc-2026-house/_push_uuid.ts
  - backend/data/stance-research/sc-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/sc-2026-house/163-10-sc-stances.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "All 9 new CO candidates AND all 16 new SC candidates have sourced chairs-not-polarity federal-24 stances (or a gate-pinned whole-record skip with a written search trail), 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every CO/SC answer pairs to an inform.politician_context row with a real fetched source_url"
    - "CO's and SC's partial-tier incumbents are skipped via the diagnostic (out of scope); DeGette is not researched (lost-primary, not a candidate)"
  artifacts:
    - path: "backend/data/stance-research/co-2026-house/163-10-co-stances.csv"
      provides: "CO new-candidate stance rows"
      contains: "source_url"
    - path: "backend/data/stance-research/sc-2026-house/163-10-sc-stances.csv"
      provides: "SC new-candidate stance rows"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push.ts / _push_uuid.ts (per-state scaffold)"
      pattern: "politician_context"
---

<objective>
Research + push CO and SC stances in ONE consolidated plan — CO (9 new candidates) and SC (16 new candidates) are both small decided states, 25 targets combined, well under the single-plan precedent (MN's 35). Planner's-discretion consolidation per RESEARCH §Recommended Plan Structure (matching 162's precedent of keeping small states lean). Each state keeps its own scaffold dir and its own stance CSV so 163-11's per-state gate stays clean. CO's and SC's incumbents are all partial-tier → skipped; DeGette (CO-1 lost-primary) is not a candidate → not researched. Run agents at 3-concurrency (1 per district) with a primary-source verification pass, push to PROD. Continues USHC3-05.

Output: co-2026-house + sc-2026-house scaffolds + two stance CSVs pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-03-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-05-SUMMARY.md
@backend/data/stance-research/mn-2026-house/_push.ts
@backend/data/stance-research/mn-2026-house/_merge.ts

<interfaces>
Two scaffolds: co-2026-house/ and sc-2026-house/, each with _push.ts/_merge.ts/_push_uuid.ts copied byte-for-byte from mn-2026-house/ and its OWN live-regenerated _TOPIC_SCALE_FULL.txt (federal-24 subset; re-verify the live active count once, reuse for both). CO targets = 9 new CO candidates (band -80899..-80101, from 163-03-SUMMARY.md); SC targets = 16 new SC candidates (band -450799..-450101, from 163-05-SUMMARY.md). CO/SC incumbents (-8001..-8008 / -45001..-45007) all partial-tier → OUT of scope. DeGette (610bb358-...) is lost-primary → NOT a candidate, NOT researched. Standards: chairs-not-polarity, never party-inferred, honest-skip with written trail (whole-record gate-pinned), 0-unsourced, mandatory primary-source verification pass, 3-concurrency cap (shared across both states — never exceed 3 concurrent agents even when interleaving CO+SC), first-wave validation, embed exact 1-5 scale texts. Push CO rows via co-2026-house/_push.ts and SC rows via sc-2026-house/_push.ts (keep the CSVs state-separated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold co-2026-house + sc-2026-house and regenerate the federal-24 topic scale live</name>
  <read_first>
    - .claude/skills/ (research-stances, compass-topic-builder)
    - backend/data/stance-research/mn-2026-house/_push.ts (pipeline scripts to copy verbatim)
    - 163-RESEARCH.md (Stance Pipeline — regenerate topic scale live)
  </read_first>
  <action>
    Create backend/data/stance-research/co-2026-house/ and backend/data/stance-research/sc-2026-house/. Into each, copy _push.ts, _merge.ts, _push_uuid.ts byte-for-byte from mn-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt via the LIVE compass_topics query filtered to the federal-24 subset (re-verify the live active total once), and write the identical file into BOTH state dirs (the federal-24 set is state-agnostic). Do NOT copy a stale file.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/co-2026-house/_TOPIC_SCALE_FULL.txt && test -f backend/data/stance-research/sc-2026-house/_TOPIC_SCALE_FULL.txt && test -f backend/data/stance-research/co-2026-house/_push.ts && test -f backend/data/stance-research/sc-2026-house/_push.ts && echo SCAFFOLDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both co-2026-house/ and sc-2026-house/ contain _merge.ts, _push.ts, _push_uuid.ts (verbatim) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt regenerated from a live query, filtered to federal-24 (count re-verified), identical in both dirs, with exact 1-5 texts
  </acceptance_criteria>
  <done>CO + SC stance scaffolds ready; topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research CO (9) + SC (16) stances (majors first) and push to PROD</name>
  <read_first>
    - .claude/skills/ (research-stances — 3-concurrency, chairs-not-polarity, 0-unsourced, honest-skip with written trail)
    - backend/data/stance-research/co-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 163-03-SUMMARY.md (CO candidate external_ids); 163-05-SUMMARY.md (SC candidate external_ids)
  </read_first>
  <action>
    Target the 9 new CO candidates (band -80899..-80101) and the 16 new SC candidates (band -450799..-450101). Order evidenced majors first, fringe filers last. Do NOT research CO's/SC's partial-tier incumbents; do NOT research DeGette (lost-primary). Dispatch politician-stance-researcher agents at 3-concurrency MAX shared across both states (never more than 3 concurrent even when interleaving CO+SC districts); validate the first wave before dispatching more. Embed exact 1-5 scale texts per agent prompt. Score chairs-not-polarity; never infer from party; honest-skip per-topic or whole-record ONLY with a written search trail (whole-record gate-pinnable). Every answer pairs to a real fetched source_url. Run a mandatory primary-source verification pass. Write 163-10-co-stances.csv and 163-10-sc-stances.csv (state-separated); merge + push CO rows via co-2026-house/_push.ts + _push_uuid.ts and SC rows via sc-2026-house/_push.ts + _push_uuid.ts as each wave completes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -80899 AND -80101 OR external_id BETWEEN -450799 AND -450101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed CO + SC rows (query returns 0)
    - Every new CO candidate and every new SC candidate has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written search trail
    - Stance agents ran at ≤3 concurrency (shared); a primary-source verification pass was performed; no partial-tier CO/SC incumbent topped up; DeGette not researched
  </acceptance_criteria>
  <done>CO's and SC's full fields are stance-complete on PROD, 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSVs -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — offline research + data push, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-10-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query (both bands); chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-163-10-02 | Tampering | Surname-leak in selected quotes | mitigate | _push.ts surname-leak guard (verbatim) |
| T-163-10-03 | Denial of service | Stance-agent rate-limit death | mitigate | 3-concurrency cap SHARED across CO+SC; first-wave validation; run on Sonnet |
</threat_model>

<verification>
- 0 unsourced answers among pushed CO + SC rows
- Every new CO + SC candidate has sourced stances or a gate-pinned whole-record skip
- Both scaffolds present; topic scale live-regenerated
</verification>

<success_criteria>
CO's and SC's full 2026 House fields are stance-complete on PROD, 0 unsourced, chairs-not-polarity, primary-source-verified — the two small decided states closed in one consolidated plan.
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-10-SUMMARY.md when done, recording CO + SC targets covered, gate-pinned whole-record skips (external_ids, per state) for 163-11, and the live federal-24 count.
</output>
