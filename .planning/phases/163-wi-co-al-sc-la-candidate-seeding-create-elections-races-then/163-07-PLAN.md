---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 07
type: execute
wave: 2
depends_on: ["163-02"]
files_modified:
  - backend/data/stance-research/wi-2026-house/_merge.ts
  - backend/data/stance-research/wi-2026-house/_push.ts
  - backend/data/stance-research/wi-2026-house/_push_uuid.ts
  - backend/data/stance-research/wi-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/wi-2026-house/163-07-wi-stances.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "All 28 new WI candidates have sourced chairs-not-polarity federal-24 stances (or a gate-pinned whole-record skip with a written search trail), 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every WI answer pairs to an inform.politician_context row with a real fetched source_url"
    - "WI's partial-tier incumbents are skipped via the diagnostic (out of scope)"
  artifacts:
    - path: "backend/data/stance-research/wi-2026-house/163-07-wi-stances.csv"
      provides: "WI new-candidate stance rows"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push.ts / _push_uuid.ts"
      pattern: "politician_context"
---

<objective>
Research + push WI stances — all 28 new WI candidates (single plan, comparable to MN's 35-in-one-plan). WI's incumbents are all partial-tier → skipped via the diagnostic. Set up the wi-2026-house scaffold, regenerate _TOPIC_SCALE_FULL.txt LIVE, run agents at 3-concurrency (1 agent per district) with a primary-source verification pass, push to PROD (durable store — stance CSVs are gitignored). Continues USHC3-05.

Output: wi-2026-house scaffold + stance CSV pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-02-SUMMARY.md
@backend/data/stance-research/mn-2026-house/_push.ts
@backend/data/stance-research/mn-2026-house/_merge.ts

<interfaces>
Scaffold: copy _push.ts/_merge.ts and _push_uuid.ts byte-for-byte from an existing state dir (e.g. mn-2026-house/, which itself cloned tn-2026-house/ + quick-candidates-2026/) into wi-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt LIVE (SELECT ... FROM inform.compass_topics WHERE is_active=true; filter to the federal-24 subset; re-verify the live active count, do NOT assume 44). WI targets = the 28 new WI candidates (external_id band -550899..-550101, from 163-02-SUMMARY.md). WI incumbents (-55001..-55008) are all partial-tier → OUT of scope. Standards (project standing): chairs-not-polarity, never party-inferred, honest-skip with written search trail (whole-record gate-pinned), 0-unsourced floor, mandatory primary-source verification pass, 3-concurrency cap, first-wave validation, embed exact 1-5 scale texts per agent prompt. Sources: Ballotpedia renders via Playwright not WebFetch; WI WEC Cloudflare-walled even via r.jina.ai → use Wikipedia raw wikitext + WEC-quoting news + candidate sites/socials.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold wi-2026-house + regenerate the federal-24 topic scale live</name>
  <read_first>
    - .claude/skills/ (research-stances, compass-topic-builder — scaffold + topic conventions)
    - backend/data/stance-research/mn-2026-house/_push.ts (the pipeline scripts to copy verbatim)
    - 163-RESEARCH.md (Stance Pipeline — regenerate _TOPIC_SCALE_FULL.txt live; do not copy a stale file)
  </read_first>
  <action>
    Create backend/data/stance-research/wi-2026-house/. Copy _push.ts, _merge.ts, and _push_uuid.ts byte-for-byte from mn-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt via the LIVE compass_topics query filtered to the federal-24 subset (re-verify the live active total before subtracting the non-federal topics), writing the exact 1-5 scale texts per topic. Do NOT copy an old state's _TOPIC_SCALE_FULL.txt.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/wi-2026-house/_push.ts && test -f backend/data/stance-research/wi-2026-house/_TOPIC_SCALE_FULL.txt && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - wi-2026-house/ contains _merge.ts, _push.ts, _push_uuid.ts (verbatim) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt regenerated from a live query, filtered to the federal-24 subset (count re-verified), with exact 1-5 texts
  </acceptance_criteria>
  <done>WI stance scaffold ready; topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research WI stances (28 new candidates, majors first) and push to PROD</name>
  <read_first>
    - .claude/skills/ (research-stances — 3-concurrency cap, chairs-not-polarity, 0-unsourced, honest-skip with written trail)
    - backend/data/stance-research/wi-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 163-02-SUMMARY.md (WI candidate external_ids)
  </read_first>
  <action>
    Target the 28 new WI candidates (band -550899..-550101). Order evidenced majors first, fringe filers last. Do NOT research WI's partial-tier incumbents (out of scope). Dispatch politician-stance-researcher agents at 3-concurrency MAX, 1 agent per district; validate the first wave before dispatching more. Embed the exact 1-5 scale texts per agent prompt. Score chairs-not-polarity; never infer from party; honest-skip per-topic or whole-record ONLY with a written search trail (whole-record gate-pinnable). Every answer pairs to a real fetched source_url. Run a mandatory primary-source verification pass. Write 163-07-wi-stances.csv, merge + push to PROD via _push.ts / _push_uuid.ts as each wave completes (PROD is the durable store — CSVs are gitignored).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -550899 AND -550101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed WI rows (query returns 0)
    - Every new WI candidate has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written search trail
    - Stance agents ran at ≤3 concurrency; a primary-source verification pass was performed; no partial-tier WI incumbent was topped up
  </acceptance_criteria>
  <done>WI's full field is stance-complete on PROD, 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — offline research + data push, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-07-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query; chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-163-07-02 | Tampering | Surname-leak in selected quotes | mitigate | _push.ts surname-leak guard (verbatim) |
| T-163-07-03 | Denial of service | Stance-agent rate-limit death | mitigate | 3-concurrency cap; first-wave validation; run on Sonnet |
</threat_model>

<verification>
- 0 unsourced answers among pushed WI rows
- Every new WI candidate has sourced stances or a gate-pinned whole-record skip
- Scaffold present; topic scale live-regenerated
</verification>

<success_criteria>
WI's full 2026 House field is stance-complete on PROD, 0 unsourced, chairs-not-polarity, primary-source-verified.
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-07-SUMMARY.md when done, recording targets covered, gate-pinned whole-record skips (external_ids) for 163-11, and the live federal-24 count.
</output>
