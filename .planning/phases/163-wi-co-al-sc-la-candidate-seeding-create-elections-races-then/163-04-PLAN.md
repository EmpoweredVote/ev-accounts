---
phase: 163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then
plan: 04
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - backend/scripts/163-al-generate.mts
  - backend/migrations/1226_seed_al_2026_house_elections_races.sql
  - backend/migrations/1227_seed_al_2026_house_candidates.sql
  - backend/scripts/seed-al-house-headshots.py
  - backend/data/seed-al-2026-house/163-04-al-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "AL has 2 elections: 'AL 2026 Statewide General' (general, 2026-11-03) and 'AL 2026 Congressional Redistricting - Polygon Pending' (special, dated to the SCOTUS stay so ELECTION_VISIBILITY_WINDOW is false); 7 races wired by severity per the 163-01 AL audit"
    - "Non-severe AL districts surface their field on /elections; severe AL districts (per the 163-01 audit) are seeded but surface ZERO races (election_id points at the withheld election); office_id is NEVER null; essentials.offices is never touched"
    - "21 new AL politicians created (collision-free -(01*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-1001..-1007); Moore AL-1 (retired) has no active row; late-primary districts (AL-1/2/6/7) marked PROVISIONAL; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/1226_seed_al_2026_house_elections_races.sql"
      provides: "AL general + withheld 'Polygon Pending' election + 7 severity-routed races"
      contains: "Congressional Redistricting"
    - path: "backend/migrations/1227_seed_al_2026_house_candidates.sql"
      provides: "21 new AL politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "severe AL races"
      to: "AL 2026 Congressional Redistricting - Polygon Pending election"
      via: "election_id substitution (visibility-window false)"
      pattern: "Polygon Pending"
    - from: "race_candidates insert"
      to: "district old-CD NATIONAL_LOWER office"
      via: "join by geo_id (not election name)"
      pattern: "geo_id"
---

<objective>
Seed AL's full 2026 US House field (7 districts, 21 new records) wired to the EXISTING district rows (D-01), with SEVERE districts (from the 163-01 AL audit) seeded-but-withheld from /elections via a dedicated non-general "Polygon Pending" election row (D-01b) — NEVER office_id NULL, NEVER touching essentials.offices (keeps the reps feed on the true old-map incumbent). AL is a SPLIT state: AL-3/4/5 primaries are decided (normal general field), AL-1/2/6/7 are late-primary (Aug-11 SCOTUS-ordered special primary → PROVISIONAL) AND the predicted-severe redraw set. The description prefix (PROVISIONAL) tracks primary-timing; the election routing (withheld vs general) tracks the audit's severity call — handle both independently. Author the 2 elections + 7 severity-routed races, create 21 new records + race_candidates (reuse renominated incumbent pids; Moore AL-1 retired → no active row; Figures AL-2 reuse -1002), then headshots. Continues USHC3-02/03/04.

Output: 163-al-generate.mts, two AL migrations (run on prod), seed-al-house-headshots.py (band-scoped, 2-election variant), AL reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-RESEARCH.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-al-correspondence-audit.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-01-SUMMARY.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv
@backend/scripts/162-mo-generate.mts
@backend/src/lib/electionService.ts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
AL FIPS=01; geo_ids 0101-0107. Incumbents keep existing external_id (-1001..-1007); AL-1/0101 Moore RETIRED (running for Senate) → no active row (vacate:true). New challengers: external_id = -(1*10000 + cd*100 + seq); query band = -10799..-10101 (excludes the adjacent -10000 Sarah McBride/DE record — do NOT touch it). Two elections:
- "AL 2026 Statewide General" (election_type general, election_date 2026-11-03) — the surfacing election
- "AL 2026 Congressional Redistricting - Polygon Pending" (election_type special, election_date = the SCOTUS-stay date 2026-06-02; this must be strictly MORE than 30 days before the execution date so ELECTION_VISIBILITY_WINDOW is false — if executing within 30 days of 2026-06-02, use the earlier May-2026 3-judge-panel strike-down date instead)
ELECTION_VISIBILITY_WINDOW (electionService.ts): (e.election_type != 'general' AND e.election_date >= CURRENT_DATE - INTERVAL '30 days') OR (e.election_type = 'general' AND e.election_date >= DATE_TRUNC('year', CURRENT_DATE::date)). Non-severe races wire election_id → general; severe races (from 163-al-correspondence-audit.md's "Severe geo_id list:") wire election_id → withheld. ALL races: office_id = the district's existing NATIONAL_LOWER office (NEVER null). race_candidates joined by geo_id (NOT election name) so one insert block wires severe + non-severe without branching. Description: late-primary districts (AL-1/2/6/7) get 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12' (Aug-11 special primary); decided districts (AL-3/4/5) get the plain decided general description (no PROVISIONAL). Per-district new-record counts (from 160-field-table-p163.csv): AL-1=5 (open, Moore→Senate), AL-2=6 (Figures inc reuse -1002), AL-3=1, AL-4=1, AL-5=1, AL-6=5 (Palmer inc reuse -1006), AL-7=2 (Sewell inc reuse -1007) (=21). Parties normalized (Democratic/Republican). sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-01 collision check + load AL severe geo_id list + re-verify SCOTUS stay</name>
  <read_first>
    - 163-RESEARCH.md (Critical Question 3 — 0 collisions verified in -10101..-10799, -10000 is McBride/DE not a collision; Open Questions #2 — re-verify AL litigation status immediately before seeding)
    - 163-al-correspondence-audit.md (the "Severe geo_id list:" to load into SEVERE_GEO_IDS)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p163.csv (AL incumbent pids/external_ids, rows 18-24; Moore retired)
  </read_first>
  <action>
    Run the fresh scoped external_id collision check for AL's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -10799..-10101, excluding legacy incumbent ids -1001..-1007 (cross-check any hit against 160-field-table-p163.csv; confirm -10000 McBride/DE is out-of-band and untouched). Load SEVERE_GEO_IDS from 163-al-correspondence-audit.md's "Severe geo_id list:" line (do NOT hardcode the predicted 0101/0102/0106/0107 — use whatever the audit actually scored). Confirm Moore AL-1 is retired → no active row. Re-verify the SCOTUS stay is still operative (no injunction lifting the 2023 map) as of execution date, and confirm the withheld election_date (2026-06-02, or the earlier May date if within 30 days of execution) is strictly >30 days before today so the visibility window evaluates false. Record collision result, the loaded severe set, and the chosen withheld date in the reconciliation CSV header notes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -10799 AND -10101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -10799..-10101 (or every hit confirmed a legacy incumbent id; -10000 confirmed out-of-band)
    - AL severe geo_id set loaded verbatim from the 163-01 audit; Moore AL-1 confirmed retired (no active row)
    - SCOTUS stay confirmed still operative; withheld election_date confirmed >30 days before execution date
  </acceptance_criteria>
  <done>AL band confirmed collision-free, the audit severe set is loaded, and the withheld-date is validated before any SQL is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply AL elections/races + candidates migrations (severity-routed)</name>
  <read_first>
    - backend/scripts/162-mo-generate.mts (the exact 2-election severity-routed analog to clone: SEVERE_GEO_IDS, INC_EXT, external-id block, migration templates, join-by-geo_id, office_id-never-null invariant, severe?withheld:general branch)
    - 163-al-correspondence-audit.md (the severe geo_id list)
    - 160-field-table-p163.csv (the 21 AL new records + incumbent reuse rows, rows 18-24)
    - 163-RESEARCH.md (Migration Numbering — re-verify next-free)
  </read_first>
  <action>
    Clone backend/scripts/162-mo-generate.mts → backend/scripts/163-al-generate.mts. Change: SEVERE_GEO_IDS = the geo_ids from 163-al-correspondence-audit.md's "Severe geo_id list:"; INC_EXT = AL's -1001..-1007 (AL-1 Moore vacate:true, no active row); FIELD = the 21 AL new records from 160-field-table-p163.csv (parties normalized, never "Democrat"); geo prefix '01'; 7 districts. Wire Figures AL-2 inc:true (reuse -1002), Palmer AL-6 inc:true (reuse -1006), Sewell AL-7 inc:true (reuse -1007), and the AL-3/4/5 incumbents (reuse). Keep the -(1*10000+cd*100+seq) formula and the severe?withheld:general election branch verbatim. Set the withheld election name 'AL 2026 Congressional Redistricting - Polygon Pending' and election_date to the date validated in Task 1 (SCOTUS stay 2026-06-02, or the earlier May date). Description: AL-1/2/6/7 (late-primary) get 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-12'; AL-3/4/5 (decided) get the plain general description — key the PROVISIONAL prefix on primary-timing, NOT on severity. Copy the "office_id is NEVER null; essentials.offices is NEVER touched" invariant into the migration header comment verbatim. Keep race_candidates join-by-geo_id (not election name). Use sqlStr() on every free-text field. Emit two migration files — reserved pair is 1226 + 1227; FIRST re-verify with `ls backend/migrations | sort -n | tail` that they are free; if taken, shift both forward to the next free contiguous pair and record actuals in the SUMMARY. Idempotent NOT EXISTS guards on all inserts. Run node --import tsx scripts/163-al-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-al-2026-house/163-04-al-reconciliation.csv listing every AL candidate with new/reuse disposition + external_id + severity routing.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name LIKE 'AL 2026%'"</automated>
  </verify>
  <acceptance_criteria>
    - Both AL migrations applied cleanly (psql exit 0); 7 races exist, each office_id NOT NULL (FILTER count 0); essentials.offices untouched
    - Severe districts' races have election_id = the 'AL 2026 Congressional Redistricting - Polygon Pending' election; non-severe = the general (matching the 163-01 severe set exactly)
    - 21 new politicians in -10799..-10101; incumbents reuse -1001..-1007; Moore has no active row; 0 duplicate full_name; re-running migrations is a 0-row no-op
  </acceptance_criteria>
  <done>AL races + candidates seeded on prod, severity-routed, late-primary districts PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: AL headshots (band-scoped, includes severe-district candidates)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped 3-tuple analog — NO election-name join, so withheld-election candidates still get headshots; wrong-person guards byte-for-byte)
    - .claude/skills/ (find-headshots — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-al-house-headshots.py. Change ONLY the module docstring, BANDS = {'AL': (-10799, -10101, 'Alabama')} (3-tuple band-scoped-only — NO election-name join, because severe-district candidates on the withheld election still need headshots), and RESULTS_JSON = '_al-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes. Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip. Incumbents (-1001..-1007) fall outside the band.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -10799 AND -10101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-al-house-headshots.py uses the 3-tuple band-scoped BANDS (no election join); wrong-person guards unchanged
    - Headshot attempted for every new AL candidate INCLUDING severe-district candidates; every new candidate has a politician_images row OR a documented honest-skip
  </acceptance_criteria>
  <done>AL new-candidate headshots ingested (or honest-skipped) via the band-scoped script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text candidate names interpolated into SQL executed against prod |
| audit severe list -> election_id routing | The severity classification decides which AL races surface to voters |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding, no runtime code changes. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-04-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-163-04-02 | Tampering | A severe AL race accidentally surfacing (wrong-map data to voters) | mitigate | Severe races routed to the non-general past-dated withheld election; office_id never null; 163-11 AL-SEVERE block + coordinate-smoke negative sample re-prove non-surfacing |
| T-163-04-03 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -1001..-1007 by geo_id join; full_name dedup guard; 0-duplicate assertion in 163-11 |
| T-163-04-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted |
</threat_model>

<verification>
- Both AL migrations applied on prod (psql exit 0); 7 races, all office_id NOT NULL; essentials.offices untouched
- Severe races → withheld election; non-severe → general (matches the 163-01 severe set); 21 new politicians in-band; incumbents reused; 0 duplicate full_name
- Every new AL candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
AL's full 2026 House field is seeded on prod (21 new + incumbent reuse), severity-routed so severe districts are withheld from /elections while office_id stays populated, late-primary districts PROVISIONAL-marked, with headshots and a reconciliation CSV — ready for stance research (163-08).
</success_criteria>

<output>
Create .planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-04-SUMMARY.md when done, recording applied migration numbers, the severe AL geo_ids routed to the withheld election, and the withheld election_date used.
</output>
