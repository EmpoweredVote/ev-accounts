---
phase: 120-ma-city-officials-seeding
plan: 02
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - backend/scripts/verify-phase-120.sql
autonomous: true
requirements:
  - MAOF-01
  - MAOF-02
  - MAOF-03
  - MAOF-04
  - MAOF-05
  - MAOF-06
  - MAOF-07

must_haves:
  truths:
    - "All 7 cities (Newton/Somerville/Lynn/Fall River/Waltham/Medford/New Bedford) have politicians > 0 in the DB"
    - "All 7 cities have offices > 0 and zero NULL office_id on their politicians"
    - "All 7 cities' LOCAL + LOCAL_EXEC district rows have non-NULL tiger_geoid"
    - "A permanent audit SQL script documents Phase 120 requirements fulfillment and passes without RAISE EXCEPTION"
    - "REQUIREMENTS.md checkboxes MAOF-01..07 marked complete"
  artifacts:
    - path: "backend/scripts/verify-phase-120.sql"
      provides: "9-assertion phase gate confirming MAOF-01..07 coverage"
      contains: "ASSERTION"
  key_links:
    - from: "essentials.governments WHERE city IN ('Newton','Somerville','Lynn','Fall River','Waltham','Medford','New Bedford')"
      to: "essentials.districts → essentials.offices → essentials.politicians"
      via: "geo_id join chain — districts.geo_id = governments.geo_id"
      pattern: "JOIN essentials.districts d ON d.geo_id = g.geo_id AND d.state = 'ma'"
---

<objective>
Write verify-phase-120.sql — a SQL assertion script that confirms all 7 Phase 120 cities satisfy MAOF-01..07 success criteria. The script is the permanent audit record proving that migrations 578–592 + 622 + 687 collectively fulfill Phase 120. It follows the labeled DO $$ assertion pattern established by verify-phase-119.sql.

Purpose: Phase 120 has no execution work remaining after Plan 01. The gate script closes the loop, updates REQUIREMENTS.md checkboxes, and gives Phase 121/122/123 executors a clear FK-ready confirmation.

Output: backend/scripts/verify-phase-120.sql with 9 labeled assertions (one per MAOF requirement + 2 structural checks). Run it. All assertions must pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Assertion pattern from verify-phase-119.sql (each assertion is an independent DO block) -->
```sql
-- ============================================================
-- ASSERTION N — MAOF-XX ([City] — description)
-- ============================================================
DO $$ DECLARE v_count INTEGER; BEGIN
  SELECT COUNT(...) INTO v_count FROM ...;
  IF v_count ... THEN
    RAISE EXCEPTION 'ASSERTION N FAILED [MAOF-XX]: ...', v_count;
  END IF;
  RAISE NOTICE 'ASSERTION N PASSED [MAOF-XX]: ...', v_count;
END $$;
```

<!-- Known DB facts (from RESEARCH.md + migration headers): -->
City geo_ids (all FIPS place codes, all lowercase 'ma' state in districts):
  Newton      = '2545560'   (migration 578)
  Somerville  = '2562535'   (migration 581)
  Lynn        = '2537490'   (migration 584)
  Fall River  = '2523000'   (migration 590)
  Waltham     = '2572600'   (migration 592)
  Medford     = '2539835'   (migration 591 + geo_id fixed by 622)
  New Bedford = '2545000'   (migration 587)

Politician counts (from RESEARCH.md current state summary):
  Newton = 25, Somerville = 12, Lynn = 12, Fall River = 10,
  Waltham = 16, Medford = 8, New Bedford = 12  (total: 95)

District rows per city: 2 (LOCAL + LOCAL_EXEC)
Tiger_geoid = geo_id on all 7 cities after migration 687 (Plan 01)

essentials.offices join:
  essentials.offices.politician_id → essentials.politicians.id
  essentials.offices.district_id  → essentials.districts.id

Null-check pattern for office_id and chamber_id:
  SELECT COUNT(*) FROM essentials.politicians p
  JOIN essentials.offices o ON o.politician_id = p.id
  JOIN essentials.districts d ON d.id = o.district_id
  WHERE d.geo_id IN (...)
    AND d.state = 'ma'
    AND p.office_id IS NULL;
</interfaces>

<!-- Reference files -->
<!-- backend/scripts/verify-phase-119.sql — structural template for labeled assertions -->
<!-- .planning/phases/120-ma-city-officials-seeding/120-RESEARCH.md — per-city counts -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write verify-phase-120.sql (9-assertion phase gate)</name>
  <files>backend/scripts/verify-phase-120.sql</files>
  <read_first>
    - backend/scripts/verify-phase-119.sql — must read the full file to understand assertion numbering, DO block structure, RAISE NOTICE + RAISE EXCEPTION patterns, and the final summary notice. This is the direct template.
    - .planning/phases/120-ma-city-officials-seeding/120-RESEARCH.md — confirms per-city politician counts, geo_ids, and the exact tiger_geoid null situation fixed by migration 687.
    - .planning/phases/120-ma-city-officials-seeding/120-01-SUMMARY.md — read if it exists; confirms migration 687 was applied and what NOTICE messages were emitted.
  </read_first>
  <action>
Create backend/scripts/verify-phase-120.sql with exactly 9 labeled assertions following the DO $$ block pattern from verify-phase-119.sql.

File header (comment block):
```
-- ============================================================
-- verify-phase-120.sql
-- Phase 120: MA City Officials Seeding — Phase Gate Verification
--
-- Run via Supabase MCP execute_sql. All 9 assertions must pass
-- (RAISE EXCEPTION on failure).
--
-- Migrations that fulfill Phase 120:
--   578 — Newton city government (25 politicians)
--   581 — Somerville city government (12 politicians)
--   584 — Lynn city government (12 politicians)
--   587 — New Bedford city government (12 politicians)
--   590 — Fall River city government (10 politicians)
--   591 — Medford city government (8 politicians) [geo_id fixed by 622]
--   592 — Waltham city government (16 politicians)
--   622 — tiger_geoid backfill for 6 cities (Somerville/Lynn/Medford/Fall River/Waltham/New Bedford)
--   687 — Newton tiger_geoid backfill
--
-- MAOF-01: Newton — 25 politicians + 2 districts + tiger_geoid set
-- MAOF-02: Somerville — 12 politicians + 2 districts + tiger_geoid set
-- MAOF-03: Lynn — 12 politicians + 2 districts + tiger_geoid set
-- MAOF-04: Fall River — 10 politicians + 2 districts + tiger_geoid set
-- MAOF-05: Waltham — 16 politicians + 2 districts + tiger_geoid set
-- MAOF-06: Medford — 8 politicians + 2 districts + tiger_geoid set
-- MAOF-07: New Bedford — 12 politicians + 2 districts + tiger_geoid set
-- ============================================================
```

Assertions to write (each as an independent DO $$ DECLARE v_count INTEGER; BEGIN ... END $$ block):

ASSERTION 1 — MAOF-01 (Newton politician count):
  SELECT COUNT(DISTINCT p.id) INTO v_count
  FROM essentials.politicians p
  JOIN essentials.offices o ON o.politician_id = p.id
  JOIN essentials.districts d ON d.id = o.district_id
  WHERE d.geo_id = '2545560' AND d.state = 'ma'
  IF v_count <> 25 → RAISE EXCEPTION
  RAISE NOTICE '... % / 25', v_count

ASSERTION 2 — MAOF-02 (Somerville politician count = 12):
  Same pattern, geo_id = '2562535', expected = 12

ASSERTION 3 — MAOF-03 (Lynn politician count = 12):
  geo_id = '2537490', expected = 12

ASSERTION 4 — MAOF-04 (Fall River politician count = 10):
  geo_id = '2523000', expected = 10

ASSERTION 5 — MAOF-05 (Waltham politician count = 16):
  geo_id = '2572600', expected = 16

ASSERTION 6 — MAOF-06 (Medford politician count = 8):
  geo_id = '2539835', expected = 8

ASSERTION 7 — MAOF-07 (New Bedford politician count = 12):
  geo_id = '2545000', expected = 12

ASSERTION 8 — Structural: Zero NULL office_id across all 7 cities (MAOF-01..07 composite):
  SELECT COUNT(*) INTO v_count
  FROM essentials.politicians p
  JOIN essentials.offices o ON o.politician_id = p.id
  JOIN essentials.districts d ON d.id = o.district_id
  WHERE d.geo_id IN ('2545560','2562535','2537490','2523000','2572600','2539835','2545000')
    AND d.state = 'ma'
    AND p.office_id IS NULL
  IF v_count <> 0 → RAISE EXCEPTION 'ASSERTION 8 FAILED: % politicians have NULL office_id'
  RAISE NOTICE 'ASSERTION 8 PASSED: % politicians with NULL office_id (expected 0)', v_count

ASSERTION 9 — Structural: All 7 cities have non-NULL tiger_geoid (MAOF-01..07 composite, unblocks Phase 123):
  SELECT COUNT(*) INTO v_count
  FROM essentials.districts
  WHERE state = 'ma'
    AND district_type IN ('LOCAL','LOCAL_EXEC')
    AND geo_id IN ('2545560','2562535','2537490','2523000','2572600','2539835','2545000')
    AND tiger_geoid IS NULL
  IF v_count <> 0 → RAISE EXCEPTION 'ASSERTION 9 FAILED: % district rows have NULL tiger_geoid — Phase 123 will be blocked'
  RAISE NOTICE 'ASSERTION 9 PASSED: % district rows with NULL tiger_geoid (expected 0)', v_count

Final summary notice (outside any DO block):
```sql
DO $$ BEGIN
  RAISE NOTICE 'Phase 120 gate PASSED: all 9 assertions passed. Migrations 578-592+622+687 verified. MAOF-01..07 fulfilled.';
END $$;
```

Do NOT include Path 0 spot checks in this script — Phase 120 has no ward-level geofencing yet (that is Phase 123). The script is SELECT + assertion only, no DML.
  </action>
  <verify>
    <automated>grep -c "ASSERTION" C:/EV-Accounts/backend/scripts/verify-phase-120.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/scripts/verify-phase-120.sql
    - Contains exactly 9 ASSERTION blocks (grep -c "ASSERTION" = 18 — each assertion has both FAILED and PASSED occurrences)
    - All 7 geo_ids appear: 2545560, 2562535, 2537490, 2523000, 2572600, 2539835, 2545000
    - MAOF-01 through MAOF-07 all appear as labels in assertion headers
  </acceptance_criteria>
  <done>verify-phase-120.sql written with 9 labeled assertions covering all 7 MAOF requirements plus 2 structural checks (NULL office_id + NULL tiger_geoid).</done>
</task>

<task type="auto">
  <name>Task 2: Run gate assertions + mark REQUIREMENTS.md complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - backend/scripts/verify-phase-120.sql — the file written in Task 1. Read it fully before executing.
    - .planning/REQUIREMENTS.md — locate the MAOF-01..07 checkboxes to mark complete.
  </read_first>
  <action>
Step 1 — Execute verify-phase-120.sql via mcp__supabase-local__execute_sql.
Pass the full content of backend/scripts/verify-phase-120.sql. All 9 DO blocks must complete without RAISE EXCEPTION. Expected output: 9 "ASSERTION N PASSED" NOTICE messages followed by the Phase 120 gate summary.

If any assertion fails, stop and diagnose before marking REQUIREMENTS.md. Do not mark requirements complete if any assertion raises an exception.

Step 2 — Update .planning/REQUIREMENTS.md.
Find the MAOF section (## Officials Seeding (MAOF)) and change each checkbox from `- [ ]` to `- [x]` for MAOF-01 through MAOF-07. Also update the status column in the Requirements Coverage table from "Pending" to "Complete" for all 7 MAOF rows.

Do not modify any other requirements (MAST-*, MAGE-*) — those belong to later phases.
  </action>
  <verify>
    <automated>
Run via mcp__supabase-local__execute_sql (the gate script itself is the verification):
-- All 9 assertions must emit NOTICE without EXCEPTION.
-- On success, the final NOTICE will read:
-- "Phase 120 gate PASSED: all 9 assertions passed. Migrations 578-592+622+687 verified. MAOF-01..07 fulfilled."
    </automated>
  </verify>
  <acceptance_criteria>
    - execute_sql run of verify-phase-120.sql produces 9 PASSED notices and no EXCEPTION
    - REQUIREMENTS.md: MAOF-01 through MAOF-07 show `- [x]` (7 checked boxes)
    - REQUIREMENTS.md: status column for all 7 MAOF rows shows "Complete"
    - No other requirements (MAST-*, MAGE-*) are modified
  </acceptance_criteria>
  <done>All 9 assertions pass. MAOF-01..07 marked complete in REQUIREMENTS.md. Phase 120 is verified and closed. Phases 121, 122, and 123 can begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script → Supabase execute_sql | SQL is read-only SELECT + DO assertion blocks; no DML; safe to re-run |
| REQUIREMENTS.md → file write | Planner marks requirements complete; only checkbox state changes, no other edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05 | Information Disclosure | verify-phase-120.sql | accept | Script is SELECT-only; no PII queried; geo_ids and counts are non-sensitive civic data |
| T-120-06 | Tampering | REQUIREMENTS.md checkbox update | mitigate | Only MAOF-01..07 rows modified; executor must not touch MAST-* or MAGE-* rows |
| T-120-07 | Repudiation | Gate script as audit record | mitigate | Script stored in backend/scripts/ and committed to git — permanent proof of MAOF fulfillment |
| T-120-08 | Denial of Service | RAISE EXCEPTION on assertion failure | accept | Desired behavior — any miscounted row surfaces immediately rather than silently passing |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SQL + file edit only |
</threat_model>

<verification>
Phase 120 is complete when:
1. verify-phase-120.sql executes without RAISE EXCEPTION — all 9 assertions pass
2. REQUIREMENTS.md shows MAOF-01..07 all checked ([x])
3. 120-02-SUMMARY.md created and references the passing assertion output

Quick SQL smoke test (run after gate script passes):
```sql
SELECT g.city, COUNT(DISTINCT p.id) AS politicians
FROM essentials.governments g
JOIN essentials.districts d ON d.geo_id = g.geo_id AND d.state = 'ma'
JOIN essentials.offices o ON o.district_id = d.id
JOIN essentials.politicians p ON p.id = o.politician_id
WHERE g.state = 'MA'
  AND g.city IN ('Newton','Somerville','Lynn','Fall River','Waltham','Medford','New Bedford')
GROUP BY g.city
ORDER BY g.city;
-- Expected: 7 rows: Fall River=10, Lynn=12, Medford=8, New Bedford=12, Newton=25, Somerville=12, Waltham=16
```
</verification>

<success_criteria>
- verify-phase-120.sql exists at backend/scripts/ with 9 labeled assertions
- All 9 assertions pass when executed via execute_sql (no RAISE EXCEPTION)
- REQUIREMENTS.md: MAOF-01 through MAOF-07 marked [x] complete
- Phase 120 is confirmed closed — Phases 121, 122, 123 can proceed with FK-valid politician targets
</success_criteria>

<output>
Create .planning/phases/120-ma-city-officials-seeding/120-02-SUMMARY.md when done
</output>
