---
phase: 13-compassv2-backend-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/src/lib/compassService.ts
  - backend/src/routes/compass.ts
  - backend/src/types/database.types.ts
autonomous: true

must_haves:
  truths:
    - "DELETE /api/compass/answers/me returns 200 for an authenticated user with existing answers"
    - "After the DELETE, GET /api/compass/answers returns an empty array"
    - "After the DELETE, GET /api/compass/selected-topics returns empty topic_ids"
    - "DELETE /api/compass/answers/me with ?full=true requires admin and resets completed_onboarding"
    - "DELETE /api/compass/answers/me is idempotent: calling it on an already-empty account returns 200"
  artifacts:
    - path: "backend/src/lib/compassService.ts"
      provides: "resetCompassAnswers() function"
      contains: "resetCompassAnswers"
    - path: "backend/src/routes/compass.ts"
      provides: "DELETE /answers/me route handler"
      contains: "DELETE.*answers/me"
    - path: "backend/src/types/database.types.ts"
      provides: "deleted_at field on compass_responses"
      contains: "deleted_at"
  key_links:
    - from: "backend/src/routes/compass.ts"
      to: "backend/src/lib/compassService.ts"
      via: "resetCompassAnswers import"
      pattern: "resetCompassAnswers"
    - from: "backend/src/lib/compassService.ts"
      to: "public.reset_compass_answers RPC"
      via: "adminRpc call"
      pattern: "adminRpc.*reset_compass_answers"
---

<objective>
Implement the DELETE /api/compass/answers/me endpoint that soft-deletes a user's compass responses and clears their selected topics in a single atomic operation.

Purpose: CompassV2 needs a way for users to reset their calibration data. This endpoint closes requirement COMP2-01. The route delegates to a new resetCompassAnswers() service function that calls the reset_compass_answers SECURITY DEFINER RPC created in Plan 13-01.

Output: Updated compassService.ts with resetCompassAnswers(), updated compass.ts route with the DELETE handler, and updated database.types.ts with the deleted_at field on compass_responses.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-compassv2-backend-compatibility/13-CONTEXT.md
@.planning/phases/13-compassv2-backend-compatibility/13-RESEARCH.md
@.planning/phases/13-compassv2-backend-compatibility/13-01-SUMMARY.md
@backend/src/lib/compassService.ts
@backend/src/routes/compass.ts
@backend/src/types/database.types.ts
@backend/src/middleware/requireAdmin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resetCompassAnswers to compassService.ts</name>
  <files>backend/src/lib/compassService.ts</files>
  <action>
Append a new exported function resetCompassAnswers to backend/src/lib/compassService.ts. Place it after the existing saveSelectedTopics function.

The function signature:
  export async function resetCompassAnswers(
    userId: string,
    fullReset: boolean = false
  ): Promise<void>

Implementation:
- Call adminRpc('reset_compass_answers', { p_user_id: userId, p_full_reset: fullReset })
- adminRpc is already imported at the top of compassService.ts — do not add a duplicate import
- If error is returned from adminRpc, throw new Error(error.message)
- Do NOT throw on success (void return)

The function is intentionally thin — the reset_compass_answers SECURITY DEFINER RPC created in migration 027 handles the actual multi-table atomic update.

Do not modify any existing functions in compassService.ts.
  </action>
  <verify>tsc --noEmit exits 0. resetCompassAnswers is exported. The function calls adminRpc with the correct parameter names (p_user_id, p_full_reset).</verify>
  <done>resetCompassAnswers is exported from compassService.ts and calls the reset_compass_answers RPC via adminRpc.</done>
</task>

<task type="auto">
  <name>Task 2: Add DELETE /answers/me route to compass.ts</name>
  <files>backend/src/routes/compass.ts</files>
  <action>
Add the DELETE /answers/me route to backend/src/routes/compass.ts.

1. Import resetCompassAnswers from compassService.ts — add it to the existing destructured import at the top of the file.

2. Import requireAdmin from the middleware:
   import { requireAdmin } from '../middleware/requireAdmin.js';

3. Add the route handler. Position it BEFORE the existing POST /answers route (specific paths before parameterized paths rule). Place it after GET /categories and before GET /answers:

// ---------------------------------------------------------------------------
// DELETE /api/compass/answers/me
// Auth: required
// Soft-deletes all of the user's compass responses and clears selected_topic_ids.
// Idempotent — returns 200 even when the user has no responses.
// Optional: ?full=true — admin-only flag that also resets completed_onboarding.
// Delegates to reset_compass_answers SECURITY DEFINER RPC for atomicity.
// ---------------------------------------------------------------------------

router.delete(
  '/answers/me',
  requireAuth,
  async (req: Request, res: Response): Promise<void> => {
    const authReq = req as AuthenticatedRequest;

    const fullReset = req.query.full === 'true';

    // ?full=true is admin-only — enforce via requireAdmin check inline
    if (fullReset) {
      // Inline admin check: reuse requireAdmin middleware pattern
      // (cannot chain middleware conditionally without complex wrapper;
      // call the guard directly instead)
      const { data: adminRow } = await (await import('../lib/supabase.js'))
        .supabaseAdmin
        .from('admin_users')
        .select('user_id')
        .eq('user_id', authReq.userId)
        .maybeSingle();

      if (!adminRow) {
        res.status(403).json({ code: 'FORBIDDEN', message: 'Admin access required for full reset' });
        return;
      }
    }

    try {
      await resetCompassAnswers(authReq.userId, fullReset);
      res.status(200).json({ reset: true });
    } catch (err) {
      console.error('[DELETE /compass/answers/me] error:', err);
      res.status(500).json({ code: 'INTERNAL_ERROR', message: 'An unexpected error occurred' });
    }
  }
);

CRITICAL: The inline admin check above imports supabaseAdmin inside the route handler. This VIOLATES the architecture test (routes/ must not reference supabaseAdmin). Use a different approach:

Correct approach — call requireAdmin as a conditional middleware by wrapping the handler:

router.delete(
  '/answers/me',
  requireAuth,
  async (req: Request, res: Response): Promise<void> => {
    const authReq = req as AuthenticatedRequest;
    const fullReset = req.query.full === 'true';

    if (fullReset) {
      // requireAdmin is async middleware — invoke it manually and await
      await new Promise<void>((resolve, reject) => {
        requireAdmin(req, res, (err?: unknown) => {
          if (err) reject(err); else resolve();
        });
      }).catch(() => {
        // requireAdmin already sent the 403 response
      });
      // If requireAdmin sent a response, res.headersSent will be true
      if (res.headersSent) return;
    }

    try {
      await resetCompassAnswers(authReq.userId, fullReset);
      res.status(200).json({ reset: true });
    } catch (err) {
      console.error('[DELETE /compass/answers/me] error:', err);
      res.status(500).json({ code: 'INTERNAL_ERROR', message: 'An unexpected error occurred' });
    }
  }
);

This keeps supabaseAdmin out of the route file entirely — requireAdmin (in middleware/) handles the admin check.
  </action>
  <verify>
1. tsc --noEmit exits 0.
2. grep 'supabaseAdmin' backend/src/routes/compass.ts returns nothing.
3. The DELETE /answers/me handler is present in compass.ts.
4. The handler imports requireAdmin from middleware (NOT supabaseAdmin directly).
5. npm test passes (architecture test must not fail).
  </verify>
  <done>DELETE /answers/me route exists in compass.ts, calls resetCompassAnswers, conditionally enforces admin for ?full=true using requireAdmin middleware, and contains no direct supabaseAdmin reference.</done>
</task>

<task type="auto">
  <name>Task 3: Update database.types.ts — add deleted_at to compass_responses</name>
  <files>backend/src/types/database.types.ts</files>
  <action>
Update the manually-maintained inform section of database.types.ts to add the deleted_at field to compass_responses.

In the inform.Tables.compass_responses section:

Row — add:
  deleted_at: string | null

Insert — add:
  deleted_at?: string | null

Update — add:
  deleted_at?: string | null

The field goes after write_in_text in the Row/Insert/Update objects to maintain alphabetical ordering (d comes before i).

Wait — current Row ordering is: created_at, inverted, topic_id, updated_at, user_id, value, visibility, write_in_text. Insert deleted_at between created_at and inverted (d sorts after c, before i alphabetically):

Row:
  created_at: string
  deleted_at: string | null    ← new
  inverted: boolean
  topic_id: string
  updated_at: string
  user_id: string
  value: number
  visibility: string
  write_in_text: string | null

Apply the same addition to Insert and Update.

Also add is_candidate to inform.Tables.politicians:

Row — add:
  is_candidate: boolean

Insert — add:
  is_candidate?: boolean

Update — add:
  is_candidate?: boolean

Position is_candidate after id in alphabetical order (i after i: id comes before is_candidate).

Do not change any other part of database.types.ts.
  </action>
  <verify>tsc --noEmit exits 0. grep 'deleted_at' backend/src/types/database.types.ts shows the field in all three (Row, Insert, Update) of compass_responses. grep 'is_candidate' shows the field in all three of politicians.</verify>
  <done>database.types.ts has deleted_at on compass_responses and is_candidate on politicians, all three variants (Row/Insert/Update), and tsc compiles clean.</done>
</task>

</tasks>

<verification>
- tsc --noEmit exits 0 across the full backend source tree
- npm test passes (architecture test: no supabaseAdmin in routes/)
- DELETE /api/compass/answers/me is present in the route file
- resetCompassAnswers is exported from compassService.ts
- database.types.ts has both new fields
</verification>

<success_criteria>
An authenticated user can call DELETE /api/compass/answers/me and receive 200. The architecture test remains green. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/13-compassv2-backend-compatibility/13-02-SUMMARY.md`
</output>
