---
phase: 13-compassv2-backend-compatibility
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/src/lib/connectService.ts
  - backend/src/routes/connect.ts
autonomous: true

must_haves:
  truths:
    - "POST /api/connect/compass-import accepts a body with both calibrations and selected_topics"
    - "When selected_topics is provided and has 3-8 valid IDs, completed_onboarding is set to true"
    - "When any calibration is invalid (bad topic_id or value out of range), the entire import is rejected with 400"
    - "After a successful import, GET /api/compass/selected-topics returns the submitted topic IDs"
    - "Omitting selected_topics is still valid — the field is optional"
    - "An authenticated user calling without user_id in the body imports for themselves"
    - "An admin can include user_id in the body to import for a different user"
  artifacts:
    - path: "backend/src/lib/connectService.ts"
      provides: "importCompassCalibrations() function"
      contains: "importCompassCalibrations"
    - path: "backend/src/routes/connect.ts"
      provides: "Expanded compass-import handler accepting selected_topics"
      contains: "selected_topics"
  key_links:
    - from: "backend/src/routes/connect.ts"
      to: "backend/src/lib/connectService.ts"
      via: "importCompassCalibrations import"
      pattern: "importCompassCalibrations"
    - from: "backend/src/lib/connectService.ts"
      to: "public.import_compass_calibrations RPC"
      via: "adminRpc call"
      pattern: "adminRpc.*import_compass_calibrations"
---

<objective>
Expand POST /api/connect/compass-import to accept an optional selected_topics array alongside calibrations, and execute the full import atomically via the import_compass_calibrations RPC created in Plan 13-01.

Purpose: CompassV2 sends both calibrations and selected_topics in a single import call when a user completes onboarding. The current endpoint only handles calibrations as a draft. This closes requirement COMP2-03 by performing the actual write to inform.compass_responses (not just saving a draft) and optionally committing the user's selected topics.

Output: New importCompassCalibrations() function in connectService.ts, and an updated compass-import route handler in connect.ts that uses it.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-compassv2-backend-compatibility/13-CONTEXT.md
@.planning/phases/13-compassv2-backend-compatibility/13-RESEARCH.md
@.planning/phases/13-compassv2-backend-compatibility/13-01-SUMMARY.md
@backend/src/lib/connectService.ts
@backend/src/routes/connect.ts
@backend/src/lib/enrollService.ts
@backend/src/lib/compassService.ts
@backend/src/middleware/requireAdmin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add importCompassCalibrations to connectService.ts</name>
  <files>backend/src/lib/connectService.ts</files>
  <action>
Add two imports to the top of connectService.ts (after existing imports):
  import { adminRpc } from './supabase.js';
  import { saveSelectedTopics, validateTopicIds } from './compassService.js';

Wait — check if adminRpc is already imported. Currently connectService.ts imports only from './supabase.js' (createUserClient, supabaseAnon). Add adminRpc to the existing destructured import:
  import { createUserClient, supabaseAnon, adminRpc } from './supabase.js';

Also add these imports:
  import { saveSelectedTopics, validateTopicIds } from './compassService.js';
  import { completeOnboarding } from './enrollService.js';

Add a new exported function importCompassCalibrations after the existing saveCompassImportDraft function:

  export interface ImportCalibrationItem {
    topic_id: string;
    value: number;
    write_in_text?: string;
    inverted?: boolean;
  }

  export async function importCompassCalibrations(params: {
    userId: string;
    accessToken: string;
    calibrations: ImportCalibrationItem[];
    selectedTopics?: string[];
  }): Promise<{ imported: number; onboarding_complete: boolean }>

Implementation:
1. Determine if onboarding should complete: const shouldCompleteOnboarding = Array.isArray(params.selectedTopics) && params.selectedTopics.length >= 3 && params.selectedTopics.length <= 8;

2. If selectedTopics is provided, validate all IDs:
   - Call validateTopicIds(params.selectedTopics ?? []) — returns array of invalid IDs
   - If any invalid IDs: throw Object.assign(new Error('INVALID_TOPIC_IDS'), { code: 'INVALID_TOPIC_IDS', invalid_ids: invalidIds })

3. Call the import_compass_calibrations RPC:
   const { error } = await adminRpc('import_compass_calibrations', {
     p_user_id: params.userId,
     p_calibrations: JSON.stringify(params.calibrations),
     p_set_onboarding_complete: shouldCompleteOnboarding,
   });
   If error: check for 'INVALID_CALIBRATION' message and throw with that code. Otherwise throw new Error(error.message).

4. If selectedTopics is provided and valid:
   - Call saveSelectedTopics(params.accessToken, params.userId, params.selectedTopics)
   - This uses createUserClient (RLS-enforced) to write selected_topic_ids

5. Return { imported: params.calibrations.length, onboarding_complete: shouldCompleteOnboarding }

Do NOT use supabaseAdmin in this function. adminRpc is a wrapper around supabaseAdmin.rpc() that lives in supabase.ts — calling it from connectService.ts (a lib/ file) is permitted.
  </action>
  <verify>tsc --noEmit exits 0. importCompassCalibrations is exported from connectService.ts. The function calls adminRpc with p_calibrations parameter.</verify>
  <done>importCompassCalibrations is exported from connectService.ts and implements the full import flow with optional selected_topics and onboarding completion.</done>
</task>

<task type="auto">
  <name>Task 2: Expand compass-import route handler in connect.ts</name>
  <files>backend/src/routes/connect.ts</files>
  <action>
Update backend/src/routes/connect.ts to expand the POST /api/connect/compass-import handler.

1. Update the compassImportBodySchema to add the new optional fields:
   const compassImportBodySchema = z.object({
     calibrations: z.array(
       z.object({
         topic_id: z.string().uuid(),
         topic_version: z.number().int().positive(),
         stance_id: z.string().uuid(),
         inverted: z.boolean().optional().default(false),
       })
     ),
     selected_topics: z.array(z.string().uuid()).min(0).max(8).optional(),
     confirmed: z.boolean().optional().default(false),
     user_id: z.string().uuid().optional(),  // admin-only: import for another user
   });

2. Import importCompassCalibrations from connectService (add to existing destructured import at the top of the file).

3. Import requireAdmin from middleware:
   import { requireAdmin } from '../middleware/requireAdmin.js';

4. Update the POST /compass-import handler body. The Phase 1 validation path (confirmed: false) is unchanged. Update Phase 2 (confirmed: true):

Phase 2 — Save (confirmed: true):
   a. Determine target user: if user_id is provided in the body, this is an admin-for-another-user import. Perform an inline admin check using the same requireAdmin-via-promise pattern from Plan 13-02 before proceeding.

   b. const targetUserId = parsed.data.user_id ?? userId;

   c. Build calibrations in ImportCalibrationItem format from the submitted calibrations. Note: the existing CalibrationItem has topic_id and value (value comes from stance, but the import RPC expects a value integer 1–5). The import body's value field: the existing schema uses stance_id, not a direct value — the RPC needs an integer value. To avoid breaking the existing schema, map stance_id to a value by resolving it:
      WAIT — re-read the context. The existing compass-import handler saves calibrations as a draft and the actual promotion to compass_responses happens lazily via promoteCompassImportDraft. For Phase 13, the CompassV2 import should bypass the draft and write directly.

      However, the existing CalibrationItem type uses stance_id (not a numeric value). The RPC import_compass_calibrations expects a numeric value 1-5. To get the value from stance_id, we'd need a DB lookup.

      BETTER APPROACH: The CONTEXT.md research says "merge behavior: upsert by topic_id". The stance already has a value column. Add a direct value field to the import schema alongside stance_id (make stance_id optional for backwards compat) OR accept that CompassV2 sends numeric values directly.

      RESOLVED APPROACH: Extend the compassImportBodySchema calibrations object with an optional value field (integer 1-5). When value is present, use it directly. When only stance_id is present (legacy path), look up the value from inform.compass_stances. This makes the expansion backwards compatible.

      Simpler: Add value: z.number().int().min(1).max(5).optional() to the calibration item schema. The new import path requires value. The old draft-save path only needs stance_id.

   d. For the direct import path (confirmed: true), verify each calibration has either a value or stance_id. If stance_id only, skip the direct import (return the old saveCompassImportDraft behavior for legacy callers). If value is present, use the new importCompassCalibrations function.

   ACTUALLY — keep it clean. Add value to the schema as optional. The CONTEXT says CompassV2 sends calibrations with a numeric value. Don't over-engineer:

   Updated schema for calibration item:
     z.object({
       topic_id: z.string().uuid(),
       topic_version: z.number().int().positive(),
       stance_id: z.string().uuid().optional(),
       value: z.number().int().min(1).max(5).optional(),
       inverted: z.boolean().optional().default(false),
     })

   In the Phase 2 handler:
   - Build the ImportCalibrationItem array from calibrations where value is defined
   - If no calibrations have value, fall back to saveCompassImportDraft (old behavior)
   - If all calibrations have value, call importCompassCalibrations

   e. The full Phase 2 handler:

   if (confirmed) {
     // Admin path: if user_id provided, check admin permission
     if (parsed.data.user_id) {
       await new Promise<void>((resolve, reject) => {
         requireAdmin(req, res, (err?: unknown) => { if (err) reject(err); else resolve(); });
       }).catch(() => {});
       if (res.headersSent) return;
     }

     const targetUserId = parsed.data.user_id ?? userId;
     const calibrationsWithValue = calibrations.filter(c => c.value !== undefined);

     if (calibrationsWithValue.length > 0) {
       // New direct import path
       const importItems = calibrationsWithValue.map(c => ({
         topic_id: c.topic_id,
         value: c.value!,
         inverted: c.inverted ?? false,
       }));

       try {
         const result = await importCompassCalibrations({
           userId: targetUserId,
           accessToken,
           calibrations: importItems,
           selectedTopics: parsed.data.selected_topics,
         });
         res.status(200).json({ imported: true, count: result.imported, onboarding_complete: result.onboarding_complete });
       } catch (importErr: unknown) {
         const e = importErr as { code?: string; message?: string; invalid_ids?: string[] };
         if (e.code === 'INVALID_CALIBRATION') {
           res.status(400).json({ code: 'INVALID_CALIBRATION', message: 'One or more calibrations failed validation — nothing was saved' });
           return;
         }
         if (e.code === 'INVALID_TOPIC_IDS') {
           res.status(422).json({ code: 'INVALID_TOPIC_IDS', message: 'Invalid selected topic IDs', invalid_ids: e.invalid_ids });
           return;
         }
         throw importErr;
       }
     } else {
       // Legacy path: save draft for promoteCompassImportDraft lazy promotion
       await saveCompassImportDraft(accessToken, userId, calibrations as CalibrationItem[]);
       res.status(200).json({ imported: true, count: calibrations.length });
     }
     return;
   }

   Make sure to keep the Phase 1 validation path (confirmed: false) identical to the existing code.

CRITICAL: Do not break the existing Phase 1 validation path. Only the Phase 2 confirmed:true path is expanded.
  </action>
  <verify>
1. tsc --noEmit exits 0.
2. grep 'supabaseAdmin' backend/src/routes/connect.ts returns nothing.
3. The compassImportBodySchema includes selected_topics and value (optional) fields.
4. The confirmed:true path calls importCompassCalibrations when value fields are present.
5. npm test passes (architecture test green).
  </verify>
  <done>POST /api/connect/compass-import accepts selected_topics, calls importCompassCalibrations for direct-value calibrations, handles INVALID_CALIBRATION and INVALID_TOPIC_IDS error codes, and the architecture test remains green.</done>
</task>

</tasks>

<verification>
- tsc --noEmit exits 0
- npm test passes
- POST /api/connect/compass-import body schema includes selected_topics (optional array of UUIDs)
- importCompassCalibrations is exported from connectService.ts
- No supabaseAdmin reference in routes/connect.ts
</verification>

<success_criteria>
POST /api/connect/compass-import with calibrations containing numeric values and a valid selected_topics array succeeds. After the call, GET /api/compass/selected-topics returns the submitted topic IDs. The architecture test passes. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/13-compassv2-backend-compatibility/13-04-SUMMARY.md`
</output>
