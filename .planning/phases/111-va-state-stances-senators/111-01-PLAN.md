---
phase: 111-va-state-stances-senators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv
  - supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql
autonomous: true
requirements: [VAST-02, VAST-05]
tags: [stance-research, virginia, state-senate]

must_haves:
  truths:
    - "All 8 Wave 1 senators (SD-1 through SD-8) appear in inform.politician_answers with at least 1 row each (or have a documented honest-skip note in the migration header)"
    - "Every politician_answers row inserted in Wave 1 has a paired politician_context row with sources array containing >= 1 non-empty URL"
    - "Migration 324 applies cleanly via psql with the DO $$ verification block reporting unsourced_count = 0"
  artifacts:
    - path: "backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv"
      provides: "Source-of-truth CSV with all Wave 1 senator stances (per VAST-02)"
      contains: "header row + at least 1 data row per senator that had documentable stances"
    - path: "supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql"
      provides: "Atomic BEGIN/COMMIT migration upserting Wave 1 stances + paired context rows"
      contains: "INSERT INTO inform.politician_answers, INSERT INTO inform.politician_context, DO $$ verification block"
  key_links:
    - from: "backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv"
      to: "supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql"
      via: "CSV reasoning + source URL columns map 1:1 to politician_context rows (VAST-05)"
      pattern: "every CSV row -> matching politician_answers + politician_context insert"
    - from: "supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "psql session pooler apply"
      pattern: "psql \"\\$DATABASE_URL\" -f supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql"
---

<objective>
Research and ingest sourced compass stances for the 8 VA state senators in Wave 1 (SD-1 through SD-8, all Republican): Timmy French, Mark D. Obenshain, Christopher T. Head, David R. Suetterlein, T. Travis Hackworth, Todd E. Pillion, William M. Stanley Jr., and Mark J. Peake.

Purpose: Closes 8/40 of VAST-02 (sourced stances for all 40 VA state senators) and contributes to VAST-05 (every new stance paired with politician_context containing >= 1 real source URL). Establishes the pre-flight + sequential-agent + migration pattern that Waves 2-5 will replicate exactly.

Output: One CSV file with Wave 1 stance research, one numbered migration (324) applied to the live DB, verified by the inline DO $$ block reporting 0 unsourced rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-va-state-stances-senators/111-RESEARCH.md
@.planning/phases/111-va-state-stances-senators/111-VALIDATION.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 1 pre-flight (migration number + topics snapshot + UUID confirmation)</name>
  <files>backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json</files>
  <read_first>
    - .planning/phases/111-va-state-stances-senators/111-RESEARCH.md (Patterns 2 and 3 — pre-flight queries; DB State table — confirms 323 was last applied)
    - .claude/skills/research-stances/SKILL.md (Topic Resolution section — exact pre-flight node --import tsx incantation)
    - backend/.env (must contain DATABASE_URL pointing at Supabase session pooler aws-0-*.pooler.supabase.com:5432)
    - backend/src/lib/db.js (confirm pool export signature used by inline node scripts)
  </read_first>
  <action>
    Run three pre-flight queries against the live Supabase DB and save consolidated output to backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json:

    Query A: SELECT MAX(version) FROM supabase_migrations.schema_migrations — must return 323 per RESEARCH.md DB State table. If a higher number is returned, halt and update the migration filename to use (max+1) for this plan AND notify the orchestrator so subsequent waves shift accordingly.

    Query B: Fetch all live compass topics + per-stance text via the SKILL.md Topic Resolution query (SELECT t.id, t.topic_key, t.title, t.question_text, json_agg(...) FROM inform.compass_topics t JOIN inform.compass_stances s ...). Result must be a JSON array of 44 topics (per RESEARCH.md DB State) — if the count differs, record the actual count in the preflight JSON and proceed using whatever live topics are returned (do NOT hardcode the 44 number).

    Query C: SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -5110008 AND -5110001 ORDER BY external_id DESC. Must return exactly 8 rows. Cross-check each returned (full_name, UUID) pair against the RESEARCH.md Wave 1 roster (French cc91c3d5-18fa-478f-bb04-32d1d30dbcaf, Obenshain b7e9d159-b766-445c-b95f-9797b57247d9, Head 7eba070e-c4ed-404b-8a74-01794b2bd9ed, Suetterlein 8ed24df0-2a89-45d2-a236-1fe339b2a11c, Hackworth cd200e06-d726-4a12-b2e8-0295700d185e, Pillion eb7293ae-8a9d-4ae4-8deb-a32aa43e2c99, Stanley Jr. 1722c95b-7aed-430e-81a3-488cdf610afc, Peake ed60a0c7-252c-443f-98ff-5926bf9a58a3). Any mismatch is a halt condition.

    Use the cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..." pattern shown in RESEARCH.md Patterns 2 and 3. Concatenate all three results into a single JSON file with top-level keys {max_migration, topics, senators}. Do NOT use PostgREST for any of these queries — inform schema is not in the PostgREST exposed list per the v2.6 critical production pattern.
  </action>
  <verify>
    <automated>node -e "const j=require('./backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json'); if(j.max_migration!==323) {console.error('Expected max_migration=323, got '+j.max_migration); process.exit(1)}; if(!Array.isArray(j.topics)||j.topics.length<25) {console.error('Topics array invalid/too small'); process.exit(1)}; if(!Array.isArray(j.senators)||j.senators.length!==8) {console.error('Expected 8 Wave 1 senators, got '+(j.senators||[]).length); process.exit(1)}; const ids=j.senators.map(s=>s.id).sort(); const expected=['1722c95b-7aed-430e-81a3-488cdf610afc','7eba070e-c4ed-404b-8a74-01794b2bd9ed','8ed24df0-2a89-45d2-a236-1fe339b2a11c','b7e9d159-b766-445c-b95f-9797b57247d9','cc91c3d5-18fa-478f-bb04-32d1d30dbcaf','cd200e06-d726-4a12-b2e8-0295700d185e','eb7293ae-8a9d-4ae4-8deb-a32aa43e2c99','ed60a0c7-252c-443f-98ff-5926bf9a58a3'].sort(); if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('UUID mismatch'); process.exit(1)}; console.log('preflight OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json exists and is valid JSON
    - Top-level key max_migration equals 323 (if it differs, plan is halted and migration filename updated)
    - Top-level key topics is an array containing at least 25 topic objects, each with id, topic_key, question_text, and a stances sub-array of length 5
    - Top-level key senators is an array of exactly 8 objects whose id values match the 8 Wave 1 UUIDs listed in the action
    - The 8 returned full_name values include "Timmy French", "Mark D. Obenshain", "Christopher T. Head", "David R. Suetterlein", "T. Travis Hackworth", "Todd E. Pillion", "William M. Stanley, Jr.", and "Mark J. Peake" (substring match acceptable for middle initials/suffixes)
  </acceptance_criteria>
  <done>
    Preflight JSON committed, migration number confirmed as 324 (or recorded shift), Wave 1 senator UUIDs verified, live topic list captured for embedding in research agent prompts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential research dispatch for 8 Wave 1 senators (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 1 — dispatch rules, agent prompt template, FIVE-CHAIRS framing, city-level topic skip list, --output-file convention)
    - .planning/phases/111-va-state-stances-senators/111-RESEARCH.md (Source Strategy section — Tier 1/2/3 URL patterns for VA senators: ballotpedia.org, lis.virginia.gov, senate.virginia.gov, vpap.org)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_research_one_at_a_time.md (mass-launch hazard reminder)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_no_assumption.md (party-inference is forbidden)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_scale_embed_texts.md (stance-text-embed requirement)
    - backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json (UUID list + topic JSON to embed in prompts)
  </read_first>
  <action>
    Dispatch the politician-stance-researcher subagent EXACTLY ONCE PER SENATOR, SEQUENTIALLY. Never dispatch two agents in parallel — confirmed by SKILL.md STEP 1 dispatch rules and user memory feedback_stance_research_one_at_a_time.md. Wait for the previous CSV append to complete before triggering the next dispatch.

    Dispatch order (matches RESEARCH.md Wave 1 roster, SD-1 through SD-8):
    1. Timmy French (VA SD-1, Republican) — UUID cc91c3d5-18fa-478f-bb04-32d1d30dbcaf
    2. Mark D. Obenshain (VA SD-2, Republican) — UUID b7e9d159-b766-445c-b95f-9797b57247d9
    3. Christopher T. Head (VA SD-3, Republican) — UUID 7eba070e-c4ed-404b-8a74-01794b2bd9ed
    4. David R. Suetterlein (VA SD-4, Republican) — UUID 8ed24df0-2a89-45d2-a236-1fe339b2a11c
    5. T. Travis Hackworth (VA SD-5, Republican) — UUID cd200e06-d726-4a12-b2e8-0295700d185e
    6. Todd E. Pillion (VA SD-6, Republican) — UUID eb7293ae-8a9d-4ae4-8deb-a32aa43e2c99
    7. William M. Stanley, Jr. (VA SD-7, Republican) — UUID 1722c95b-7aed-430e-81a3-488cdf610afc
    8. Mark J. Peake (VA SD-8, Republican) — UUID ed60a0c7-252c-443f-98ff-5926bf9a58a3

    Per-agent prompt requirements (build by following SKILL.md STEP 1 template):
    - Include the senator's full_name AND VA Senate office title AND district number
    - Embed the FULL topic JSON from preflight (id, topic_key, question_text, stances array with text per value) — never hardcode topic lists
    - Restrict topic scope to the 25 state-eligible topics listed in RESEARCH.md Topic Scope section: abortion, ai-regulation, campaign-finance, childcare, civil-rights, climate-change, data-centers, deportation, fossil-fuels, healthcare, homelessness, housing, immigration, medicare/aid, misinformation, redistricting, religious-freedom, same-sex-marriage, school-vouchers, social-security, tariffs, taxes, trans-athletes, ukraine-support, voting-rights
    - Include explicit honest-skip instruction ("Skip any topic where you cannot find sufficient evidence. Do NOT infer from party affiliation.")
    - --output-file must be the absolute path C:/EV-Accounts/backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv (all 8 senators append to the SAME wave CSV per RESEARCH.md project structure recommendation)
    - Source URL fetch order: ballotpedia.org/[First_Last] -> lis.virginia.gov mbr query for S[district] -> senate.virginia.gov/senators/[district] -> official .gov page -> Wikipedia
    - Tool rule: WebFetch ONLY (no WebSearch, no Playwright) per SKILL.md
    - CSV column order: full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3 (plus quote_text,quote_deidentified if agent produces them)

    Between dispatches: verify the CSV file gained new rows for the prior senator (use Read or csv-parse check), confirm the agent did not finish suspiciously fast (< 2 min suggests rate-limit) — if either signal fails, retry that senator before moving on.

    For Stanley, Jr.: ensure the full_name passed to the agent and recorded in the CSV is exactly "William M. Stanley, Jr." (matching the essentials.politicians row); the comma must be preserved in the CSV via proper RFC-4180 quoting since the agent's csv-parse step depends on it.

    Honest-skip note: low-profile rural senators (Hackworth, Pillion, Peake) may yield 5-10 stances, which is acceptable. Do NOT fabricate value=3 neutral rows to inflate counts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {parse}=require('csv-parse/sync'); const csv=fs.readFileSync('backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv','utf8'); const rows=parse(csv,{columns:true,skip_empty_lines:true,relax_column_count:true}); if(rows.length<8){console.error('Expected at least 8 data rows across 8 senators, got '+rows.length); process.exit(1)}; const names=new Set(rows.map(r=>r.full_name)); const expected=['Timmy French','Mark D. Obenshain','Christopher T. Head','David R. Suetterlein','T. Travis Hackworth','Todd E. Pillion','William M. Stanley, Jr.','Mark J. Peake']; const missing=expected.filter(n=>!names.has(n)); if(missing.length){console.error('Missing senators in CSV: '+missing.join(', ')); process.exit(1)}; const bad=rows.filter(r=>!r.source_url_1||r.source_url_1.trim()===''); if(bad.length){console.error(bad.length+' rows missing source_url_1 (VAST-05 violation)'); process.exit(1)}; console.log('wave1 CSV OK: '+rows.length+' rows across '+names.size+' senators')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv exists with > 1 line (header + >= 8 data rows)
    - All 8 expected full_name values appear in the CSV (at minimum one row each, or honest-skip documented separately in agent response with no row for that senator)
    - Every CSV row has source_url_1 populated with a non-empty URL string (VAST-05)
    - No topic_key value appears outside the 25-topic state-eligible set defined in the action
    - No two research agents were dispatched in parallel (confirmable by ordering of tool_use timestamps in conversation log)
  </acceptance_criteria>
  <done>
    Wave 1 CSV contains research output for all 8 Republican senators; per-row source URLs present; CSV ready for migration translation in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author migration 324 SQL (Wave 1 stances + paired context, inline DO $$ verification)</name>
  <files>supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql</files>
  <read_first>
    - .planning/phases/111-va-state-stances-senators/111-RESEARCH.md (Pattern 4 — Migration Template for Stances with BEGIN/COMMIT, DO $$ block, ASSERT unsourced_count = 0)
    - supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql (most recent stance migration; mirror its header comments style, ARRAY(SELECT u FROM unnest(...) AS u WHERE u IS NOT NULL AND trim(u) != '') sources filter, and per-row INSERT pattern)
    - backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv (source CSV — every data row maps to one politician_answers INSERT + one politician_context INSERT)
    - backend/data/stance-research/2026-06-09-111-va-senators-wave1-preflight.json (UUID truth source)
  </read_first>
  <action>
    Author supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql by translating every data row of 2026-06-09-111-va-senators-wave1.csv into a paired INSERT (politician_answers + politician_context).

    Use the exact migration template from RESEARCH.md Pattern 4 as the structural skeleton. Mirror the header-comment style of 20260608000003_291_dc_shadow_senators_ehn_stances.sql, including:
    - Phase + Plan title line (Phase 111-01: VA State Senators Wave 1 Stances)
    - Requirements covered: VAST-02, VAST-05
    - Source CSV path
    - Pre-write cross-check counts (CSV data rows, politician_answers INSERT count, politician_context INSERT count — these must be equal)
    - Politician UUIDs table mapping (ext_id -> UUID for all 8 Wave 1 senators)
    - Migration number: 324
    - Timestamp: 20260609000001
    - Applied: NOT YET (write-only)

    Wrap all INSERTs inside a single BEGIN; ... COMMIT; block.

    For every CSV data row, emit:
    1. INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES ('<uuid>', (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), <value>) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;
    2. INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES ('<uuid>', (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), '<reasoning, single-quote-escaped>', ARRAY(SELECT u FROM unnest(ARRAY['<source_url_1>', '<source_url_2>', '<source_url_3>']) AS u WHERE u IS NOT NULL AND trim(u) != '')) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

    Single-quote escaping: replace every ' in reasoning text with ''. Use a real CSV parser (csv-parse/sync) when reading the CSV — never split on commas.

    Skip CSV rows where source_url_1 is empty (these would violate VAST-05). Log skipped rows in the migration header comment with reason.

    Append the DO $$ verification block from RESEARCH.md Pattern 4 verbatim, but with the external_id range -5110008 to -5110001 (Wave 1 only). The ASSERT must check unsourced_count = 0 for this Wave 1 range.

    Do NOT apply the migration yet — that is Task 4.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql','utf8'); const ansInserts=(sql.match(/INSERT INTO inform\.politician_answers/g)||[]).length; const ctxInserts=(sql.match(/INSERT INTO inform\.politician_context/g)||[]).length; if(ansInserts===0){console.error('No politician_answers INSERTs'); process.exit(1)}; if(ansInserts!==ctxInserts){console.error('Pair mismatch: '+ansInserts+' answers vs '+ctxInserts+' context'); process.exit(1)}; if(!/BEGIN;[\s\S]+COMMIT;/.test(sql)){console.error('Missing BEGIN/COMMIT wrapper'); process.exit(1)}; if(!/ASSERT unsourced_count = 0/.test(sql)){console.error('Missing ASSERT unsourced_count = 0'); process.exit(1)}; if(!/BETWEEN -5110008 AND -5110001/.test(sql)){console.error('Verification block must scope to Wave 1 range -5110008..-5110001'); process.exit(1)}; console.log('Migration 324 SQL OK: '+ansInserts+' paired insert blocks')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql exists
    - Header comment block lists all 8 Wave 1 senator UUIDs (French, Obenshain, Head, Suetterlein, Hackworth, Pillion, Stanley Jr., Peake)
    - Count of "INSERT INTO inform.politician_answers" equals count of "INSERT INTO inform.politician_context" (paired)
    - Both counts equal the number of data rows in the CSV that have a non-empty source_url_1
    - File contains exactly one BEGIN; and one COMMIT; wrapping all INSERTs
    - File contains the DO $$ verification block with ASSERT unsourced_count = 0 and external_id BETWEEN -5110008 AND -5110001
    - No ' character appears unescaped inside any string literal (every contraction in reasoning uses '')
  </acceptance_criteria>
  <done>
    Migration 324 SQL drafted, paired-insert invariant holds, verification block scoped to Wave 1 range, ready for psql apply in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration 324 via psql session pooler and confirm DO $$ verification passes</name>
  <files>supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql</files>
  <read_first>
    - supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql (the migration to apply)
    - backend/.env (DATABASE_URL — must be the Supabase session pooler URL aws-0-*.pooler.supabase.com:5432 per v2.8 carry-forward note in STATE.md)
    - .planning/phases/111-va-state-stances-senators/111-RESEARCH.md (Environment Availability table — confirms psql + DATABASE_URL availability)
  </read_first>
  <action>
    Apply the Wave 1 migration to the live Supabase DB using psql via the session pooler.

    Step 1: Source backend/.env into the shell environment. Confirm $DATABASE_URL is set and points at aws-0-*.pooler.supabase.com:5432 (NOT the IPv6 direct host, which fails on Windows). On PowerShell use Get-Content backend/.env; on Bash use set -a && source backend/.env && set +a.

    Step 2: Run exactly: psql "$DATABASE_URL" -f supabase/migrations/20260609000001_324_va_senators_wave1_stances.sql

    Step 3: Capture stdout/stderr. The DO $$ block emits two NOTICE lines:
    - NOTICE:  VA senators with stances: <N>   (must be 1..8 — every Wave 1 senator with any stance row)
    - NOTICE:  Unsourced VA senator stances: 0
    The ASSERT statement will raise an exception and rollback if unsourced_count != 0.

    Step 4: On success, run a follow-up confirmation query: psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS senators, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5110008 AND -5110001;"

    Do NOT use `supabase db push` — this project uses direct psql for stance migrations per established v2.8 pattern (DC, MD) and the schema_push_requirement of this phase. Do NOT attempt to apply via the Supabase JS client — inform schema is not exposed via PostgREST.

    If the migration fails due to a stale max_migration number (duplicate version conflict), do NOT manually edit the supabase_migrations.schema_migrations table. Instead: rename the file to use (current_max + 1), update Task 5 verification SQL accordingly, and re-run.

    On success, also update the migration file header to change "Applied: NOT YET (write-only)" to "Applied: 2026-06-09".
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5110008 AND -5110001 AND (pc.id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);" | tr -d ' \n' | grep -qx '0' && echo "VAST-05 OK: 0 unsourced Wave 1 stances" || (echo "VAST-05 VIOLATION: unsourced Wave 1 stances found"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - psql apply completes with exit code 0 (no rollback)
    - DO $$ block prints "Unsourced VA senator stances: 0" NOTICE
    - Migration row appears in supabase_migrations.schema_migrations with version 324
    - Follow-up SQL: COUNT(DISTINCT pa.politician_id) for external_id BETWEEN -5110008 AND -5110001 is >= 1 and <= 8
    - Follow-up SQL: unsourced count (politician_context missing OR sources NULL OR sources array empty) for the same range returns 0
    - Migration file header line updated from "Applied: NOT YET" to "Applied: 2026-06-09"
  </acceptance_criteria>
  <done>
    Migration 324 applied successfully; Wave 1 stances live in inform.politician_answers + inform.politician_context; VAST-05 invariant holds for Wave 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | Untrusted scraped content lands as plain text in CSV reasoning + URL columns; only ingested via psql, never executed |
| CSV → SQL migration | Single-quote-escaped string literals inserted into a parameterized-equivalent SQL file (no dynamic concatenation at runtime) |
| psql client → Supabase DB | TLS-protected session pooler; admin-write via DATABASE_URL service role; RLS on inform tables already enforces public-read/admin-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | CSV reasoning text could contain SQL meta-chars | mitigate | Author script (Task 3) replaces ' with '' before SQL emit; psql parses each INSERT as a literal string, no eval |
| T-111-02 | Information Disclosure | source URLs and reasoning are public-record political research | accept | All data sourced from public web (Ballotpedia, lis.virginia.gov, senate.virginia.gov); no PII, no internal endpoints, no credentials |
| T-111-03 | Repudiation | Migration applied without audit trail of which CSV row produced which DB row | mitigate | Migration header comment records source CSV path + row counts; CSV remains on disk as forensic artifact |
| T-111-04 | Denial of Service | Parallel research agents exhaust WebFetch quota, causing empty CSV | mitigate | Task 2 mandates sequential dispatch (one agent at a time) per SKILL.md and user-memory rule |
| T-111-05 | Elevation of Privilege | Wrong DB connection string writes to prod instead of expected target | accept | DATABASE_URL is single source of truth; project has no separate dev DB for inform schema; established pattern across Phases 103/106/108 |

No high-severity threats. ASVS Level 1 not applicable beyond standard input-sanitization (Task 3 single-quote-escape). No auth changes, no new endpoints, no PII.
</threat_model>

<verification>
- Wave 1 CSV passes csv-parse/sync read with all 8 senator full_names present and source_url_1 populated on every row
- Migration 324 file contains paired politician_answers + politician_context INSERTs (counts equal), BEGIN/COMMIT wrapper, and DO $$ ASSERT unsourced_count = 0 scoped to -5110008..-5110001
- psql apply prints "Unsourced VA senator stances: 0" NOTICE
- Post-apply SQL: SELECT COUNT(DISTINCT pa.politician_id) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5110008 AND -5110001 returns >= 1 (target: 8 minus any honest-skipped senators)
- Post-apply SQL: unsourced count for the same range returns 0 (VAST-05 invariant)
</verification>

<success_criteria>
- 8/8 Wave 1 senators have either >= 1 stance row in inform.politician_answers OR a documented honest-skip in the migration header
- 100% of inserted politician_answers rows have a paired politician_context row with sources array length >= 1
- Migration 324 recorded in supabase_migrations.schema_migrations
- CSV preserved on disk at backend/data/stance-research/2026-06-09-111-va-senators-wave1.csv as forensic artifact
- Plan 111-02 unblocked: next migration number is 325, Wave 2 senator UUIDs in RESEARCH.md ready for sequential dispatch
</success_criteria>

<output>
Create `.planning/phases/111-va-state-stances-senators/111-01-SUMMARY.md` when done, including:
- Total stances ingested for Wave 1
- Per-senator breakdown (topic count per senator)
- Any honest-skipped senators with rationale
- Migration number actually used (324 or shifted)
- Confirmation that VAST-05 invariant holds (unsourced count = 0)
</output>
</content>
</invoke>