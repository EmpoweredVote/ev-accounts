---
phase: 90-campaign-finance-schema-ingestion-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/268_finance_summary_column.sql
  - backend/test/essentialsService-finance-summary.test.ts
autonomous: true
requirements:
  - FINA-01
tags:
  - schema-migration
  - jsonb
  - essentials-schema

must_haves:
  truths:
    - "essentials.politicians has a new nullable JSONB column named finance_summary"
    - "The migration is applied to the remote Supabase DB before any ingestion runs"
    - "Wave-0 test scaffold for finance_summary surface in essentialsService exists and currently fails (RED)"
  artifacts:
    - path: "backend/migrations/268_finance_summary_column.sql"
      provides: "DDL adding finance_summary JSONB column to essentials.politicians with COMMENT"
      contains: "ADD COLUMN IF NOT EXISTS finance_summary JSONB"
    - path: "backend/test/essentialsService-finance-summary.test.ts"
      provides: "Vitest source-scan test that proves essentialsService surfaces finance_summary in both list + detail responses"
      contains: "finance_summary"
  key_links:
    - from: "backend/migrations/268_finance_summary_column.sql"
      to: "essentials.politicians table in remote Supabase"
      via: "ALTER TABLE essentials.politicians ADD COLUMN finance_summary JSONB"
      pattern: "ALTER TABLE essentials.politicians[\\s\\S]*ADD COLUMN[\\s\\S]*finance_summary[\\s\\S]*JSONB"
    - from: "backend/test/essentialsService-finance-summary.test.ts"
      to: "backend/src/lib/essentialsService.ts"
      via: "fs.readFileSync source scan looking for finance_summary in SELECT + interface + row mapper"
      pattern: "finance_summary"
---

<objective>
Add the `finance_summary` JSONB column to `essentials.politicians`, apply the migration to the remote Supabase database, and lay down the Wave-0 RED test that will gate Plan 03's API surface work. This is the schema foundation for FINA-01 and the test infrastructure for FINA-03.

Purpose: Phase 90 ships a denormalized campaign-finance JSONB on the unified politician table. The column MUST live on `essentials.politicians` (NOT `inform.politicians` — the REQUIREMENTS.md text predates Phase 35 deduplication and is wrong; the `GET /api/essentials/politicians` endpoint reads exclusively from `essentials.politicians` via `essentialsService.ts`). Without this column existing in the live DB, the Plan 02 ingestion script cannot UPDATE, and the Plan 03 API surface cannot SELECT.

Output:
- `backend/migrations/268_finance_summary_column.sql` — idempotent DDL (`ADD COLUMN IF NOT EXISTS`) with a column COMMENT documenting the shape
- Migration applied to the remote DB (verified via `information_schema.columns` query)
- `backend/test/essentialsService-finance-summary.test.ts` — vitest source-scan test (matches the pattern in `essentialsService-mtfcc-routing.test.ts`) that asserts `finance_summary` appears in the SELECT, the `PoliticianFlatRecord` interface, the `PoliticianDetail` interface, and both row mappers in `essentialsService.ts`. Test currently fails (RED) — Plan 03 makes it pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-campaign-finance-schema-ingestion-api/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 268 (finance_summary JSONB column)</name>
  <files>backend/migrations/268_finance_summary_column.sql</files>
  <read_first>
    - backend/migrations/267_ut_2026_primary.sql (most recent migration — copy header style, idempotency pattern)
    - backend/migrations/267_allen_mayor_office_fix.sql (sibling migration at 267 — verify next free number is 268)
    - .planning/phases/90-campaign-finance-schema-ingestion-api/90-RESEARCH.md (sections: "Critical Finding: inform.politicians vs. essentials.politicians", "Migration Pattern (268)", "finance_summary JSONB Schema", "Current Database State" — confirms next migration is 268, target table is essentials.politicians, shape is { total_raised, top_donors[], cycle, source })
  </read_first>
  <behavior>
    - Migration applies cleanly twice in a row (idempotent via `ADD COLUMN IF NOT EXISTS`)
    - After apply, `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='essentials' AND table_name='politicians' AND column_name='finance_summary'` returns one row with `data_type='jsonb'`
    - Column is nullable (no NOT NULL constraint; non-federal politicians will be NULL)
    - Column has a COMMENT documenting the JSONB shape so future developers don't grep blindly
  </behavior>
  <action>
    Create `backend/migrations/268_finance_summary_column.sql`. Top-of-file SQL comment header (per project convention, see migration 267) MUST cite: migration number 268, phase 90, requirement FINA-01, target table `essentials.politicians`, rationale that this is the unified table post-Phase-35-dedup (NOT `inform.politicians` as REQUIREMENTS.md mis-states).

    Single DDL statement: `ALTER TABLE essentials.politicians ADD COLUMN IF NOT EXISTS finance_summary JSONB;`

    Single COMMENT statement: `COMMENT ON COLUMN essentials.politicians.finance_summary IS '...'` with body that names every JSONB key the Plan 02 ingestion script will write: `total_raised` (number, from FEC receipts), `top_donors` (array of `{ employer: string, amount: number, count: number }`), `cycle` (string, "2026"), `source` (literal string "FEC"). Explicitly state in the comment that NULL means "no finance data ingested for this politician" (used for non-federal politicians and for federal politicians without a matched FEC ID).

    Do NOT create an index on the JSONB column — RESEARCH.md says it is read-only display and adds no index. Do NOT add finance_summary anywhere on `inform.politicians`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('backend/migrations/268_finance_summary_column.sql','utf8'); if(!/ALTER TABLE essentials\.politicians[\s\S]*ADD COLUMN IF NOT EXISTS finance_summary JSONB/.test(s)) throw new Error('missing ADD COLUMN'); if(!/COMMENT ON COLUMN essentials\.politicians\.finance_summary/.test(s)) throw new Error('missing COMMENT'); if(!/total_raised/.test(s) || !/top_donors/.test(s) || !/cycle/.test(s) || !/source/.test(s)) throw new Error('COMMENT must enumerate JSONB keys'); if(/inform\.politicians/.test(s)) throw new Error('must not reference inform.politicians'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - backend/migrations/268_finance_summary_column.sql exists
    - File contains the literal substring `ALTER TABLE essentials.politicians`
    - File contains the literal substring `ADD COLUMN IF NOT EXISTS finance_summary JSONB`
    - File contains a `COMMENT ON COLUMN essentials.politicians.finance_summary` statement
    - The COMMENT body contains all four key names: `total_raised`, `top_donors`, `cycle`, `source`
    - File does NOT contain the substring `inform.politicians` (FINA-01 target is essentials, not inform — per RESEARCH.md critical finding)
    - File does NOT create an index on finance_summary
    - Header comment cites requirement FINA-01 and phase 90
  </acceptance_criteria>
  <done>Migration 268 file exists, is idempotent, targets essentials.politicians only, and documents the JSONB shape in a column COMMENT.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 268 to remote Supabase DB</name>
  <files>backend/migrations/268_finance_summary_column.sql (no edit — apply only)</files>
  <read_first>
    - backend/migrations/268_finance_summary_column.sql (must read the file before applying to confirm contents match what landed in Task 1)
    - .planning/STATE.md (line 221: "Migration apply method (2026-05-09): Local Docker/Supabase not running; applied migrations directly via psql to remote Supabase using pooler DATABASE_URL")
    - .planning/phases/90-campaign-finance-schema-ingestion-api/90-RESEARCH.md (section "Environment Availability" — confirms DATABASE_URL pattern and `mcp__supabase-local__execute_sql` alternative)
  </read_first>
  <action>
    Apply the contents of `backend/migrations/268_finance_summary_column.sql` to the live remote Supabase database. This phase follows the established project pattern (NOT `npx supabase db push` — that command targets the `supabase/migrations/` directory, which mirrors a different lineage; the canonical migration history for `essentials.*` lives in `backend/migrations/` and is applied directly).

    Two acceptable application paths (use whichever is available; prefer A):

    A. **`mcp__supabase-local__execute_sql`** — invoke the MCP supabase tool with the full SQL body of the migration file as the query. This is the canonical apply path used by migration 267 (per `267_ut_2026_primary.sql` header: "Applied via mcp__supabase-local__execute_sql").

    B. **`psql "$DATABASE_URL" -f backend/migrations/268_finance_summary_column.sql`** — direct psql against the pooler (port 5432) using `DATABASE_URL` from `backend/.env`. This is the v2.2 pattern (per STATE.md line 221).

    After apply, verify by running this SQL against the remote DB (via the same channel used to apply):
    `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='essentials' AND table_name='politicians' AND column_name='finance_summary';`

    Expected result: exactly one row with `data_type='jsonb'` and `is_nullable='YES'`.

    Do NOT proceed to Plan 02 if this verification query returns zero rows — the ingestion script in Plan 02 depends on this column existing.
  </action>
  <verify>
    <automated>echo "Migration apply is a remote DB action; verification SQL must be run via mcp__supabase-local__execute_sql or psql. Executor must paste the verification result into the SUMMARY. Expected: one row, data_type=jsonb, is_nullable=YES."</automated>
  </verify>
  <acceptance_criteria>
    - The verification query `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='essentials' AND table_name='politicians' AND column_name='finance_summary'` returns exactly one row when run against the remote DB
    - The returned row has `data_type='jsonb'` and `is_nullable='YES'`
    - SUMMARY.md captures the apply channel used (MCP or psql) and the verification query output
    - If apply fails (e.g., permission denied, pooler unreachable), the SUMMARY documents the failure mode and the column is NOT silently assumed to exist
  </acceptance_criteria>
  <done>finance_summary JSONB column exists on essentials.politicians in the remote DB and is verified via information_schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 RED test for finance_summary API surface</name>
  <files>backend/test/essentialsService-finance-summary.test.ts</files>
  <read_first>
    - backend/test/essentialsService-mtfcc-routing.test.ts (full file — copy the `fs.readFileSync` + `stripComments` source-scan pattern verbatim; this test will fail until Plan 03 modifies essentialsService.ts)
    - backend/src/lib/essentialsService.ts (lines 90-138 for PoliticianFlatRecord interface, lines 388-524 for getPoliticiansFlatList including the SELECT and row mapper, lines 829-912 for PoliticianDetail interface, lines 950-1184 for getPoliticianById including the SELECT and row mapper — the test asserts finance_summary will be added to all four locations)
  </read_first>
  <behavior>
    - Test 1: PoliticianFlatRecord interface declares `finance_summary` field (asserts on source string match)
    - Test 2: PoliticianDetail interface declares `finance_summary` field
    - Test 3: getPoliticiansFlatList SELECT clause references `p.finance_summary`
    - Test 4: getPoliticianById SELECT clause references `p.finance_summary`
    - Test 5: Both row mappers map `finance_summary: row.finance_summary ?? null` (or equivalent — the test should accept either `?? null` or a `JSON.parse` wrapper as Plan 03 may add one per Pitfall 6)
    - All five tests FAIL when run now (RED) because essentialsService.ts has not been modified yet
    - All five tests PASS after Plan 03 lands the source changes (GREEN — Plan 03's verify gate)
  </behavior>
  <action>
    Create `backend/test/essentialsService-finance-summary.test.ts` modeled on `essentialsService-mtfcc-routing.test.ts`.

    Imports: `describe`, `it`, `expect` from `vitest`; `fs` from `node:fs`; `path` from `node:path`.

    Read the source file once: `const SRC = fs.readFileSync(path.resolve(__dirname, '../src/lib/essentialsService.ts'), 'utf-8');`

    Apply the same `stripComments` helper used in the MTFCC test to remove JS/SQL comments before grep so header prose cannot self-validate the test.

    Five test cases inside a single `describe('essentialsService finance_summary surface (FINA-03)')` block. Use regex matchers tolerant to whitespace and Prettier reformatting:

    1. `it('PoliticianFlatRecord interface declares finance_summary')` — assert `/export interface PoliticianFlatRecord[\s\S]*?finance_summary\s*:/i.test(SRC_NC)` truthy. The matcher must scope to the FlatRecord interface body (use a non-greedy match that ends before the next `export interface` or `}`).

    2. `it('PoliticianDetail interface declares finance_summary')` — same pattern, scoped to `PoliticianDetail`.

    3. `it('getPoliticiansFlatList SELECT references p.finance_summary')` — assert the SELECT clause inside `getPoliticiansFlatList` contains `p.finance_summary`. Use a scoped regex that anchors after `export async function getPoliticiansFlatList` and matches before the closing `\`\``.

    4. `it('getPoliticianById SELECT references p.finance_summary')` — same pattern, scoped to `getPoliticianById`.

    5. `it('row mappers project finance_summary into the returned record')` — assert that the file contains `/finance_summary:\s*row\.finance_summary/` (Prettier may add a comma; the match must allow trailing comma but require the row source). Permit either `?? null` or `JSON.parse(row.finance_summary as string)` as the value expression so Plan 03 has discretion if pg returns the JSONB as a string per Pitfall 6.

    The test file MUST NOT import from `essentialsService.ts` (no runtime call). It is a static source scan only — matches the pattern of `essentialsService-mtfcc-routing.test.ts`.

    The test will RED right now (Wave 0). It is the GREEN gate for Plan 03 (FINA-03).
  </action>
  <verify>
    <automated>cd backend && npx vitest run test/essentialsService-finance-summary.test.ts --reporter=verbose 2>&1 | grep -E "(FAIL|failed|Test Files)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/test/essentialsService-finance-summary.test.ts exists
    - File imports `describe, it, expect` from `vitest` and `fs`, `path` from node built-ins
    - File reads `../src/lib/essentialsService.ts` via `fs.readFileSync`
    - File applies a `stripComments` helper before regex assertions (so SQL/JS comments cannot satisfy grep)
    - File contains exactly five `it(...)` test cases inside one `describe(...)` block
    - All five test cases reference the literal token `finance_summary`
    - Running `cd backend && npx vitest run test/essentialsService-finance-summary.test.ts` reports 5 failed tests (RED state — expected before Plan 03 ships)
    - File does NOT import or call any function from `essentialsService.ts` (pure source-scan, mirroring `essentialsService-mtfcc-routing.test.ts`)
  </acceptance_criteria>
  <done>Wave-0 test file exists, runs, and reports 5 failing source-scan assertions — the RED that Plan 03 must turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> remote Supabase | Migration DDL crosses this boundary at apply time |
| migration file -> live DB | Schema change is persistent; rollback requires a follow-up DROP COLUMN migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | finance_summary JSONB stored as raw FEC response | mitigate (deferred to Plan 02) | Plan 02 builds an explicit `{ total_raised, top_donors, cycle, source }` object and never spreads the raw FEC response into the JSONB. Plan 01 only adds the empty column; no data path yet. |
| T-90-02 | Denial of Service | Adding a non-indexed JSONB column to a large table during business hours | accept | essentials.politicians is ~1,049 rows. `ADD COLUMN IF NOT EXISTS` with no default + no constraint is metadata-only in Postgres 11+; no table rewrite. |
| T-90-03 | Tampering | npm/pip/cargo installs introduce malicious package | mitigate | No new packages installed in Plan 01. Plan 02 uses only already-installed deps (pg, tsx, dotenv, native fetch). Package legitimacy audit in RESEARCH.md shows zero new packages. |
| T-90-SC | Supply Chain | Migration applied via untrusted channel | mitigate | Apply via mcp__supabase-local__execute_sql (project-canonical) or psql with $DATABASE_URL from `.env` (never inline). Verification SQL re-reads from information_schema to confirm column actually landed. |
</threat_model>

<verification>
1. `node -e "..."` source-scan against migration 268 passes (Task 1 automated gate)
2. `information_schema.columns` query against the remote DB returns one row with `data_type='jsonb'` for `essentials.politicians.finance_summary` (Task 2 acceptance)
3. `cd backend && npx vitest run test/essentialsService-finance-summary.test.ts` reports 5 failing tests in RED state (Task 3 expected output — Plan 03's GREEN gate)
4. Grep confirms zero references to `inform.politicians` anywhere in the new migration file
</verification>

<success_criteria>
- FINA-01: finance_summary JSONB column exists on essentials.politicians in the remote DB
- Migration file is idempotent (ADD COLUMN IF NOT EXISTS) and documented (COMMENT ON COLUMN)
- Wave-0 RED test scaffold exists at backend/test/essentialsService-finance-summary.test.ts and runs (failing, by design)
- No new npm dependencies added
- No inform.politicians edits (per RESEARCH.md critical finding)
</success_criteria>

<output>
Create `.planning/phases/90-campaign-finance-schema-ingestion-api/90-01-SUMMARY.md` when done. SUMMARY must record:
- Apply channel used (mcp__supabase-local__execute_sql or psql)
- Verification query output (column_name, data_type, is_nullable)
- Vitest output showing 5 failing tests in test/essentialsService-finance-summary.test.ts
- Confirmation that no inform.politicians reference exists in migration 268
</output>
