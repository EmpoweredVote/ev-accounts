---
phase: 12-quick-ux-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - CompassV2/src/util/topic.js
autonomous: false
requirements:
  - QFRM-02

must_haves:
  truths:
    - "Vague topic titles have been rewritten to short descriptive action phrases"
    - "Every rewritten title reads naturally in 'Where do you stand on ___?'"
    - "Radar chart labels remain short (using short_name or existing short_title)"
    - "User approved the before/after table before any changes were applied"
  artifacts:
    - path: "CompassV2/src/util/topic.js"
      provides: "getQuestionText with new framing (from Plan 01)"
      contains: "Where do you stand on"
  key_links:
    - from: "topic title in database"
      to: "Library card display"
      via: "getQuestionText reads topic.question_text first, falls back to framing + short_title"
      pattern: "question_text"
---

<objective>
Draft rewrites for vague topic titles, present a before/after table for user approval, then apply the approved changes. Also populate initial short_name values for radar chart labels.

Purpose: Topic titles that were too vague (e.g., "Misinformation") become specific action phrases (e.g., "Combating Online Misinformation") that read naturally in "Where do you stand on ___?" framing. Radar chart labels stay short via the new short_name field.

Output: Updated topic titles in the database (via question_text field or direct title update), populated short_name values for radar chart display.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-quick-ux-fixes/12-CONTEXT.md
@.planning/phases/12-quick-ux-fixes/12-01-SUMMARY.md
@CompassV2/src/util/topic.js
@EV-Backend/internal/compass/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch current topics and draft title rewrites</name>
  <files></files>
  <action>
    Connect to the database or use the API to fetch all current topics with their titles, short_titles, and question_text values.

    For each topic, evaluate whether the current title/short_title reads naturally in "Where do you stand on [title]?" If vague or awkward, draft a rewrite.

    Rewrite style (per user decision):
    - Short descriptive action phrases
    - Example: "Misinformation" becomes "Combating Online Misinformation"
    - Must read naturally in "Where do you stand on ___?"
    - The rewrite goes into the `question_text` field (which takes priority in `getQuestionText`)

    Also derive initial `short_name` values (Claude's discretion):
    - For topics whose `short_title` is already concise and clear, `short_name` can be left empty (frontend falls back to `short_title`)
    - For topics where the title was rewritten to something longer, the `short_name` should be the original short, punchy label suitable for radar chart display

    Produce a markdown table with columns: ID | Current short_title | Current question_text | Proposed question_text | Proposed short_name

    Present this table to the user.
  </action>
  <verify>
    Table is printed with all topics. Each proposed question_text reads naturally in "Where do you stand on" framing (or the full question_text IS the framing when it doesn't follow the pattern).
  </verify>
  <done>
    Before/after table of all topic rewrites is drafted and ready for user review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User approves topic title rewrites</name>
  <action>
    Present the before/after table from Task 1 to the user. Wait for approval or corrections before proceeding.
    What was built: Drafted topic title rewrites for all vague topics. Each proposed question_text is designed to read naturally as "Where do you stand on [topic]?" or as a standalone question when the topic title needs different framing.
    How to verify:
    1. Review the before/after table
    2. For each proposed rewrite, mentally test: "Where do you stand on [proposed text]?" — does it sound natural?
    3. Check that no radar chart label (short_name) is too long for the chart
    4. Approve the table or provide specific corrections
  </action>
  <verify>User has provided explicit approval or corrections.</verify>
  <done>User approved the topic title rewrites (or corrections were incorporated).</done>
</task>

<task type="auto">
  <name>Task 2: Apply approved topic title rewrites to database</name>
  <files></files>
  <action>
    Using the approved table from the checkpoint:

    1. For each topic that needs a rewrite, call the backend PATCH endpoint or execute SQL to update:
       - `question_text` — the new descriptive framing text
       - `short_name` — the radar chart label (if different from short_title)

    The `title` field itself should be updated to match the new descriptive title (per user decision: "Rewrite vague topic titles directly in the database, not display-only mapping").

    Execute the updates. Verify by fetching topics again and confirming the changes are persisted.

    Note: The backend must be running locally for API calls, OR use direct SQL via the database connection. Choose whichever is available. If neither is available (no local backend, no DB access), write a SQL migration script that the user can run.
  </action>
  <verify>
    Fetch topics from the API or DB after updates. Confirm:
    - Updated topics have the new `question_text` values
    - Updated topics have `short_name` populated where specified
    - No topics were accidentally cleared or corrupted
  </verify>
  <done>
    All approved topic title rewrites are persisted in the database. Topics with vague titles now have descriptive question_text values that read naturally in "Where do you stand on ___?" framing. Radar chart labels are set via short_name where needed.
  </done>
</task>

</tasks>

<verification>
1. Fetch all topics — every formerly vague title now has a descriptive `question_text`
2. Test framing: for each topic, `getQuestionText(topic)` returns a string that reads naturally
3. Radar chart labels (short_name or short_title fallback) are concise enough for chart display
4. No data corruption — all topic IDs, stances, and answers are intact
</verification>

<success_criteria>
- All vague topic titles have been rewritten with user approval
- Every topic reads naturally in "Where do you stand on [topic]?" framing
- Radar chart labels remain short via short_name field
- Database changes are persisted
</success_criteria>

<output>
After completion, create `.planning/phases/12-quick-ux-fixes/12-02-SUMMARY.md`
</output>
