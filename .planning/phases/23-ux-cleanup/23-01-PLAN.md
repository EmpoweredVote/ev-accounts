---
phase: 23-ux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/pages/Compass.jsx
  - CompassV2/src/pages/Library.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Compass page has no 'Edit Topics' button visible anywhere"
    - "Library page has no 'Clear' button visible anywhere"
    - "Answered/Remaining stat cards fill full width on mobile screens"
  artifacts:
    - path: "CompassV2/src/pages/Compass.jsx"
      provides: "Compass page without Edit Topics button"
      contains: "Compare"
    - path: "CompassV2/src/pages/Library.jsx"
      provides: "Library page without Clear button and with full-width mobile stat cards"
  key_links:
    - from: "CompassV2/src/pages/Compass.jsx"
      to: "ActionButtons component"
      via: "Button removal"
      pattern: "Compare.*button"
---

<objective>
Remove stale "Edit Topics" and "Clear" buttons, and fix mobile stat card layout on the Library page.

Purpose: Eliminate confusing controls that duplicate navigation (Edit Topics) or have unclear behavior (Clear), and ensure stat cards display correctly on all screen sizes.
Output: Updated Compass.jsx and Library.jsx with cleaner UX.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CompassV2/src/pages/Compass.jsx
@CompassV2/src/pages/Library.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Edit Topics button and Clear button</name>
  <files>CompassV2/src/pages/Compass.jsx, CompassV2/src/pages/Library.jsx</files>
  <action>
In `CompassV2/src/pages/Compass.jsx`:
1. In the `ActionButtons` function component (~line 200-218), remove the "Edit Topics" button entirely. Keep only the "Compare" button (which is already conditionally rendered when `showChart` is true). The resulting `ActionButtons` should contain just the Compare button wrapped in a `div className="flex gap-4 mt-4"`.
2. Since the `AddTopicModal` is only opened by the "Edit Topics" button (`setIsModalOpen(true)`), also remove:
   - The `isModalOpen` state (`useState(false)` on ~line 367)
   - The `AddTopicModal` JSX rendering block (~lines 714-720)
   - The `import AddTopicModal from "../components/AddTopicModal"` (~line 5)

In `CompassV2/src/pages/Library.jsx`:
3. Remove the "Clear" button and its handler:
   - Remove the `clearSelections` function (~line 230-232)
   - Remove the `compassTopicsRef` ref (~line 142) since it's only used by `clearSelections`
   - Remove the Clear button JSX inside the search/filter row (~lines 554-561): the `{selectedTopics.length > 0 && (` block containing the "Clear" underline button
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build` to confirm the app builds without errors. Grep for "Edit Topics" in Compass.jsx — should not appear. Grep for "Clear" as a button in Library.jsx — should not appear (the clearSelections function and button should both be gone). Grep for "AddTopicModal" in Compass.jsx — should not appear.
  </verify>
  <done>
Compass page renders with only a Compare button (no Edit Topics). Library page renders without a Clear button. No build errors. No dead imports or unused state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile stat card layout to fill full width</name>
  <files>CompassV2/src/pages/Library.jsx</files>
  <action>
The stat cards (Answered/Remaining) on the Library page use `grid grid-cols-2 gap-3` which can appear narrow on mobile when nested inside a flex row with the compass chart. The issue is that on mobile the compass preview section uses `flex flex-col md:flex-row` — so the stat cards are inside a `flex-1 min-w-0` div that may not expand fully.

Fix the stat cards in BOTH the "active compass" section (~line 380) and the "empty/uncalibrated compass" section (~line 439):

1. In the active compass stat cards grid (~line 380), ensure the grid container has `w-full` class: change `<div className="grid grid-cols-2 gap-3 mb-4">` to `<div className="grid grid-cols-2 gap-3 mb-4 w-full">`.

2. In the empty/uncalibrated compass stat cards grid (~line 439), ensure the grid container has `w-full` class: change `<div className="grid grid-cols-2 gap-3">` to `<div className="grid grid-cols-2 gap-3 w-full">`.

3. Also ensure the parent container `<div className="flex-1 min-w-0">` (which wraps the heading, stat cards, and button) fills width on mobile. On mobile (when flex-col), the parent should use `w-full`. Update both instances:
   - Active compass: `<div className="flex-1 min-w-0">` to `<div className="flex-1 min-w-0 w-full">`
   - Empty compass: `<div className="flex-1 min-w-0">` (in the uncalibrated section, ~line 418) to `<div className="flex-1 min-w-0 w-full">`
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build` to confirm the app builds. Inspect the stat cards container in the Library page source — both grid containers should have `w-full` class. Both parent flex-1 containers should have `w-full`.
  </verify>
  <done>
Answered/Remaining stat cards fill the full available width on mobile screens. The grid containers and their parent flex items all have `w-full` to prevent any width constraint on narrow viewports.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build` — builds successfully
2. No "Edit Topics" text in Compass.jsx
3. No "Clear" button or clearSelections function in Library.jsx
4. No AddTopicModal import in Compass.jsx
5. Stat card grid containers have `w-full` class in both compass states
</verification>

<success_criteria>
- Compass page shows only Compare button (no Edit Topics)
- Library page has no Clear button
- Stat cards fill full width on mobile in both active and empty compass states
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-ux-cleanup/23-01-SUMMARY.md`
</output>
