---
phase: 136-wi-al-sc-ky-house-rep-stances
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-19-sc-house-batch-b.csv
  - backend/data/stance-research/sc-house-b/_TOPIC_SCALE.txt
  - backend/data/stance-research/sc-house-b/_merge.ts
  - backend/data/stance-research/sc-house-b/_push.ts
autonomous: false
requirements: [USHS-10]

must_haves:
  truths:
    - "Each of the 3 SC House reps in batch B (external_id -45005..-45007) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation; no isidewith-only rows kept"
    - "Live topics fetched fresh; city + judicial-* topics skipped (25 federal topics in scope)"
    - "In-scope reps reach exact counts; verified via per-scope external_id counts (not the global counter)"
  artifacts:
    - path: "backend/data/stance-research/2026-06-19-sc-house-batch-b.csv"
      provides: "Reviewable stance research CSV for SC House reps batch B (3 reps, SC-5..SC-7)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for the remaining 3 South Carolina US House reps
(SC-5..SC-7, external_id -45005..-45007), using the research-stances skill, following the
validated Phase 127-135 playbook. Concurrency cap = 3. WebFetch-only. Closes the SC delegation.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\135-tn-co-mn-mo-house-rep-stances\135-08-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (SC House, batch B):

| external_id | name | district |
|-------------|------|----------|
| -45005 | Ralph Norman | SC-5 |
| -45006 | James Clyburn | SC-6 |
| -45007 | Russell Fry | SC-7 |

All currently have 0 stances. Resolve each politician_id by external_id->UUID at run time.
Document the actual record only, never infer from party.

KEY RULES (validated Phase 127-135 playbook):
- **25 federal topics** fetched fresh; scale to `sc-house-b/_TOPIC_SCALE.txt` (copy mo-house-a); each agent Reads it.
- **Concurrency: up to 3 agents**. WebFetch ONLY.
- **Per-rep CSV -> merge + RFC-4180-validate**. CSV quote-repair: `""""`->`"""` ONLY (4->3); NEVER 3->2 (sed-clone mo-house-a/_merge.ts).
- **Stray-trailing-quote artifact**: fix per-file with `sed -i 's/,"$/,/'` if csv-parse reports "Invalid Closing Quote".
- **isidewith.com BELOW the bar** — drop isidewith-only rows; AFA/candidate questionnaires OK.
- **Calibration discipline**: DROP over-read `same-sex-marriage=5` rows resting only on "opposes legalization"/religious-exemption bill/group-alignment; DROP topic rows resting only on a vague "core value" listing.
- **Resolve politician_id by external_id->UUID**. Five-chairs framing; never infer from party. Lean on Ballotpedia, OnTheIssues, Wikipedia, LCV, VoteSmart.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research batch B (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\135-tn-co-mn-mo-house-rep-stances\135-08-SUMMARY.md
- backend/data/stance-research/mo-house-a/_merge.ts (merge+repair to sed-clone)
- backend/data/stance-research/mo-house-a/_TOPIC_SCALE.txt (scale to copy)
</read_first>
<action>
1. Ensure `backend/data/stance-research/sc-house-b/_TOPIC_SCALE.txt` holds the current 25 federal topics (copy mo-house-a/_TOPIC_SCALE.txt).
2. Dispatch `politician-stance-researcher` agents for the 3 batch-B reps (external_id -45005..-45007), up to 3 concurrent. Each prompt: five-chairs framing, Read `_TOPIC_SCALE.txt`, WebFetch ONLY, RFC-4180 escaping rule, drop isidewith-only rows, write to `backend/data/stance-research/sc-house-b/<surname>.csv`, include external_id on every row.
3. Verify each agent's rows before the next wave; retry empty-output agents solo.
4. Merge into `backend/data/stance-research/2026-06-19-sc-house-batch-b.csv` (sed-clone _merge.ts: OUT + IN_SCOPE=[-45005..-45007]); validate every row.
</action>
<acceptance_criteria>
- 2026-06-19-sc-house-batch-b.csv exists with rows for the 3 reps (or documented honest-skips); merge reports 0 problems
- Every row has value (1-5) + reasoning + >=1 real http source_url; valid topic_key; no isidewith-only rows
- Each of SC-5..SC-7 accounted for (stances or honest-skip; no party-inferred values)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push batch B to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3/4)
- backend/data/stance-research/mo-house-a/_push.ts
- backend/data/stance-research/2026-06-19-sc-house-batch-b.csv
</read_first>
<action>
Present the STEP 3 approval summary. Standing user decision this milestone: auto-push batches that merge 0-problems AND 0-unsourced (still show the summary), no per-batch approval pause. Push via the external_id-keyed `_push.ts` (copy to `sc-house-b/_push.ts`), upserting answers + context (ON CONFLICT DO UPDATE on both) + quotes with Read & Rank + surname leak-check. `_push.ts` does NOT load dotenv — run with `set -a && source .env && set +a && node --import tsx .../_push.ts <csv>`. Commit the CSV (force-add). Verify per-scope: SC-B reps (-45005..-45007) reach exact counts, unsourced-in-scope = 0. If MCP Supabase tokens expire mid-run, fall back to `node --import tsx` + `pool` from `backend/src/lib/db.ts` (load `dotenv/config` first).
</action>
<acceptance_criteria>
- Each batch-B rep has >=1 answer row OR a documented honest-skip; every answer paired with sourced context (0 unsourced)
- Push reported counts with zero rollbacks; per-scope external_id counts confirm isolation
- CSV committed
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-16 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT DO UPDATE; single BEGIN/COMMIT with ROLLBACK |
| T-136-17 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check |
| T-136-18 | Integrity (scope creep) | non-in-scope reps | mitigate | push keyed strictly to batch-B external_ids; verified via per-scope counts |

No high-severity threats: read-only web research + an idempotent service-role upsert.
</threat_model>

<verification>
- 3 batch-B SC reps each have sourced stances or a documented skip; every answer paired with sourced context
- No party-inferred stances; no isidewith-only rows; 25 federal topics; concurrency held at 3; per-scope counts confirm isolation
</verification>

<success_criteria>
USHS-10 advanced: SC-5..SC-7 (3 reps) covered with sourced stances or honest-skips, all answers paired with sourced context, CSV committed. Closes the SC delegation.
</success_criteria>

<output>
Create `.planning/phases/136-wi-al-sc-ky-house-rep-stances/136-06-SUMMARY.md` when done.
</output>
