---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 09
type: execute
wave: 3
depends_on: ["156-05", "156-02"]
files_modified:
  - backend/data/stance-research/nc-2026-house/
autonomous: true
requirements: [USHC2-05]
must_haves:
  truths:
    - "Every in-scope new NC candidate (challengers + NC third-party/minor-line candidates) PLUS NC-6 Addison P. McDowell (the only zero-stance incumbent, -37006) has federal-24 chairs-not-polarity stances or a documented honest-skip"
    - "NC-6 McDowell's stances are pushed by his EXISTING external_id (-37006) via _push.ts (NOT _push_uuid.ts) — he is a reused existing record, not a new UUID"
    - "Every stance answer is paired to an inform.politician_context row with a real fetched source URL — 0 unsourced; all other NC partial incumbents are NOT touched (D-01)"
  artifacts:
    - path: "backend/data/stance-research/nc-2026-house/"
      provides: "Per-candidate federal-24 stance CSVs + merged batch for the new NC candidates + NC-6 McDowell, pushed to inform.politician_answers/context"
  key_links:
    - from: "inform.politician_answers (new NC candidates + NC-6 McDowell)"
      to: "inform.politician_context with non-empty sources[]"
      via: "_push_uuid.ts (new UUID challengers) / _push.ts (McDowell existing external_id -37006)"
      pattern: "politician_context"
---

<objective>
Research + push federal-24 chairs-not-polarity, evidence-only stances for the in-scope NC candidates seeded in 156-05 — the challengers + NC third-party/minor-line candidates — PLUS NC-6 Addison P. McDowell (-37006), the ONLY zero-stance incumbent in all of Wave 2 and therefore the ONLY in-scope incumbent (D-01, zero-stance-gets-full-set / TX-in-150 rule). Near-verbatim repeat of Phase 155's PA/IL stance plans, retargeted to NC, with the McDowell existing-record push as the distinguishing wrinkle.

Purpose: USHC2-05, NC. D-01 (NC): ALL other 13 NC incumbents are partial and DELIBERATELY untouched — they are NOT in scope. Only the genuinely-new NC candidates AND NC-6 McDowell get the full federal-24 set. McDowell is a REUSED EXISTING record (wired as active incumbent in 156-05); his stances push by existing external_id -37006 via _push.ts, NEVER _push_uuid.ts.
Output: Pushed stances with 0 unsourced rows for the new-NC-candidate in-scope set + NC-6 McDowell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-05-SUMMARY.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.claude/skills/research-stances/SKILL.md
@backend/data/stance-research/quick-candidates-2026/_TOPIC_SCALE_FULL.txt
@backend/data/stance-research/quick-candidates-2026/_push.ts
@backend/data/stance-research/quick-candidates-2026/_push_uuid.ts
@backend/data/stance-research/_fed24-scale-block.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the new-NC-candidate in-scope set + NC-6 McDowell + research federal-24 (≤3 concurrent agents)</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (orchestration: fetch live topics + 1-5 stance texts per topic fresh from DB; dispatch politician-stance-researcher agents; CSV format; chairs-not-polarity framing; embed the exact 1-5 scale text per topic — never "5=progressive")
    - backend/data/stance-research/_fed24-scale-block.txt (the 24 topic scales the agents READ; filter to the federal-24 set)
    - backend/data/stance-research/quick-candidates-2026/_TOPIC_SCALE_FULL.txt (1-5 stance scale text per topic)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-05-SUMMARY.md (new NC-candidate UUIDs/external_ids — the authoritative in-scope set)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (NC-6 McDowell pid 74579547-1454-475e-ab35-12cf88a998b9, external_id -37006, stance_count=0 — the ONLY in-scope incumbent)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-07-PLAN.md + 155-08-PLAN.md (the validated batch-stance pattern: in-scope resolution, federal-24 topic list, ≤3 concurrency, malformed-CSV anticipation, the all-partial-incumbent-exclusion analog)
    - MEMORY [[project_150_playwright_stance_method]] (McDowell as an INCUMBENT: Playwright→Ballotpedia "Key votes" for a sitting rep; WebFetch 403-walled on primary sources; Yea→chair clean map, Nay needs positive evidence — agent-dispatch NOT viable for incumbents, orchestrator-run Playwright)
  </read_first>
  <action>
    Determine the in-scope set by live diagnostic: an active race_candidate in an NC race whose politician_id is in the 156-05 new-candidate external_id band (NOT a renominated incumbent — D-01 excludes ALL NC partial incumbents) UNION the single NC-6 McDowell pid (74579547-1454-475e-ab35-12cf88a998b9, external_id -37006). Resolve exact UUIDs/external_ids from the 156-05 reconciliation. Candidate types: challengers in renominated districts + NC third-party/minor-line candidates + NC-6 McDowell (existing incumbent).

    Fetch live topics + 1-5 stance texts fresh from DB, filtered to the federal-24 (abortion, ai-regulation, campaign-finance, childcare, civil-rights, climate-change, deportation, fossil-fuels, healthcare, homelessness, housing, immigration, medicare/aid, misinformation, redistricting, religious-freedom, same-sex-marriage, school-vouchers, social-security, tariffs, taxes, trans-athletes, voting-rights, ukraine-support). Embed the exact 1-5 text per topic per agent prompt.

    For the NEW NC challengers: dispatch politician-stance-researcher at ≤3 concurrent (MEMORY cap; validate the first wave returns non-empty before launching more). For NC-6 McDowell (a SITTING incumbent with a congressional voting record): agent-dispatch is NOT reliable for incumbents (WebFetch 403-walled) — orchestrator-run Playwright → Ballotpedia "Key votes" is the proven method (MEMORY project_150_playwright_stance_method): Yea→chair clean map, Nay needs positive evidence, chairs-not-polarity. Produce McDowell's CSV keyed on his existing external_id -37006.

    chairs-not-polarity throughout: value = exact scale position the EVIDENCE supports; honest-skip a topic with no documentable evidence; whole-record honest-skip allowed where a candidate has no fetchable record (pin in SUMMARY for the gate). NEVER infer from party. One fetch attempt per URL. Output per-candidate CSV into backend/data/stance-research/nc-2026-house/. Anticipate malformed CSVs (149/150/155 TRAP) — relax-parse in Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && ls data/stance-research/nc-2026-house/*.csv >/dev/null 2>&1 && echo "CSVs present" || { echo "FAIL: no CSVs"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Per-candidate CSVs exist in nc-2026-house/ for the resolved in-scope set (new NC challengers + third-party lines) AND a dedicated NC-6 McDowell CSV keyed on external_id -37006; each row carries a real fetched source URL (no party-inference rows).
    - McDowell's CSV was produced via the incumbent method (orchestrator Playwright → Ballotpedia Key votes), not blind agent dispatch.
    - Topics with no evidence are honest-skipped (absent rows), not guessed; whole-record skips documented for the gate.
    - CSV columns match the push-script contract (full_name, politician_id or external_id, topic_key, value, reasoning, source_url_1..3, quote_text, quote_deidentified).
    - NO NC partial incumbent OTHER than McDowell is in the in-scope set (D-01).
  </acceptance_criteria>
  <done>Per-candidate federal-24 CSVs produced for the new-NC-candidate in-scope set + NC-6 McDowell (-37006); honest-skips documented; other NC incumbents untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory primary-source verification pass + repair + merge + push (NC + McDowell by external_id)</name>
  <read_first>
    - backend/data/stance-research/nc-2026-house/ (Task 1 CSVs incl. the McDowell -37006 CSV)
    - backend/data/stance-research/quick-candidates-2026/_push_uuid.ts (new-record push by UUID — the 156-05 new challengers)
    - backend/data/stance-research/quick-candidates-2026/_push.ts (existing-record push by external_id — NC-6 McDowell -37006 MUST use this path, NOT _push_uuid.ts, per D-01)
    - backend/data/stance-research/_push_relaxed.ts (the robust push helper carried forward from Phase 155)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-07-PLAN.md (Task 2: the mandatory primary-source verification pass + relax-parse/canonical-restringify repair pipeline + source_url_1 misalignment guard + essentials.quotes wipe-before-re-push)
  </read_first>
  <action>
    Run the MANDATORY primary-source verification pass (D-06) BEFORE any push: for each CSV row re-fetch the cited URL (Playwright where Cloudflare/JS-walled), confirm the value is the position the evidence supports. Delete any inference-only / fabricated-figure / off-by-one rows. For McDowell especially: every stance must trace to a real Ballotpedia Key-vote or documented statement (Nay needs positive evidence, not assumed opposition). On any quote correction, wipe essentials.quotes for that pid+topic before re-push.

    Apply the 149/150/155 repair pipeline: relax-parse + canonical RFC-4180 re-stringify each CSV; guard against source_url_1 column misalignment; field-count-validate; merge. Push: NEW challengers via _push_uuid.ts (keyed on politician_id UUID); NC-6 McDowell via _push.ts (keyed on existing external_id -37006 — CRITICAL: NOT _push_uuid.ts); use _push_relaxed.ts where warranted. Run with the dotenv wrapper: `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx data/stance-research/nc-2026-house/_push…ts <csv>`.

    After push, run the USHC2-05a slice of 156-verify.sql (0 unsourced for this batch's pids incl. McDowell).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const mc=await pool.query(\"SELECT count(*) sc, count(*) FILTER (WHERE NOT EXISTS (SELECT 1 FROM inform.politician_context c WHERE c.politician_id=a.politician_id AND c.topic_id=a.topic_id AND array_length(c.sources,1)>0)) un FROM inform.politician_answers a JOIN essentials.politicians p ON p.id=a.politician_id WHERE p.external_id=-37006\"); if(+mc.rows[0].sc<1){console.error('FAIL McDowell has 0 stances (must be >=1 or pinned whole-record skip)',mc.rows[0]);process.exit(1)}; if(+mc.rows[0].un>0){console.error('FAIL McDowell unsourced',mc.rows[0].un);process.exit(1)}; console.log('McDowell OK:',mc.rows[0].sc,'stances, 0 unsourced'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Primary-source verification pass completed; inference/fabricated rows deleted; corrected-quote pids had essentials.quotes wiped before re-push.
    - Malformed CSVs repaired via relax-parse + canonical re-stringify; source_url_1 misalignment guard applied.
    - Stances pushed; 0 unsourced rows for the batch's in-scope pids (every answer has a paired context row with non-empty sources[]).
    - NC-6 McDowell (-37006) has ≥1 sourced federal stance (or a pinned whole-record honest-skip) pushed via _push.ts by external_id — verified NOT pushed as a new UUID.
    - New challengers pushed by UUID; no stance pushed to the wrong person (surname-leak guard in push script).
  </acceptance_criteria>
  <done>NC batch + McDowell pushed; 0-unsourced slice of 156-verify.sql passes for the new-NC-candidate pids + McDowell; McDowell pushed by external_id -37006.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external stance sources → inform schema | Untrusted web content (agent/Playwright-fetched) becomes stance values in prod |
| stance-research agent output → prod push | Agents over-read / fabricate even when citing URLs (documented); CSVs malformed; McDowell is a walled-source incumbent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-40 | Tampering | fabricated/inference stance value (esp. McDowell Nay-inference) | mitigate | Mandatory primary-source verification pass before push; chairs-not-polarity (Nay needs positive evidence); 0-unsourced gate |
| T-156-41 | Spoofing | McDowell stances pushed to wrong pid / as a new UUID | mitigate | Push by external_id -37006 via _push.ts (NOT _push_uuid.ts); verify query keyed on -37006 |
| T-156-42 | Repudiation | unsourced stance | mitigate | Every answer paired to politician_context with non-empty sources[]; USHC2-05a assertion |
| T-156-43 | DoS | >3 concurrent agents → rate-limit empty output | mitigate | ≤3 concurrency (MEMORY); validate first wave non-empty; McDowell via orchestrator Playwright not dispatch |
| T-156-44 | Tampering | malformed CSV corrupts push | mitigate | relax-parse + canonical re-stringify + source_url_1 misalignment guard |
| T-156-45 | Repudiation | other NC partial incumbent accidentally re-stanced / required | mitigate | D-01: in-scope set is new candidates + McDowell ONLY; other NC incumbents excluded |
| T-156-SC | Tampering | npm installs | accept | No new packages (csv-parse/tsx already present) |
</threat_model>

<verification>
- 0-unsourced slice of 156-verify.sql passes for the new-NC-candidate pids + McDowell.
- McDowell has ≥1 sourced federal stance pushed by external_id -37006 (or a pinned whole-record skip).
</verification>

<success_criteria>
- New NC candidates stanced (federal-24 or honest-skip); NC-6 McDowell stanced by existing external_id; 0 unsourced; primary-source-verified; other NC incumbents untouched.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-09-SUMMARY.md` when done (record: candidates stanced, per-candidate topic counts, McDowell's topic count + push path confirmation [external_id -37006 via _push.ts], honest-skips [topic + whole-record] with pids for gate pinning, rows deleted in verification pass).
</output>
