---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 04
type: execute
wave: 2
depends_on: ["156-01", "156-02"]
files_modified:
  - backend/migrations/NNNN_seed_ga_2026_house_candidates.sql
  - backend/data/seed-ga-2026-house/156-04-ga-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 14 GA US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'"
    - "Every renominated GA incumbent (GA-2..9, 12, 14 incl. Clay Fuller -13014) reuses its 154 incumbent_pid as the active is_incumbent=true candidate; the challenger(s) are new records"
    - "GA-1 Carter (-13001), GA-10 Collins (-13010), GA-11 Loudermilk (-13011) get NO active race_candidates row; their certified nominees are active; GA-13 is a TRUE VACANCY — Jasmine Clark + Jonathan Chavez both new records, both active, neither is_incumbent, no ghost incumbent"
    - "Every 'new' GA candidate is live-DB dedup-checked before insert (some are sitting officials, e.g. GA-13 Jasmine Clark is a GA state rep); zero duplicate full_name within GA; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_ga_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT new GA politicians + INSERT race_candidates onto the 14 GA races authored in 156-01 (incl. GA-13 vacancy race)"
      contains: "race_candidates"
    - path: "backend/data/seed-ga-2026-house/156-04-ga-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 14 GA districts (incl. GA-13 vacancy + GA-1/10/11 open/retired)"
      min_lines: 26
  key_links:
    - from: "race_candidates.race_id"
      to: "the 14 GA House races (156-01 scaffold, incl. GA-13/1313 vacancy race)"
      via: "office_id → districts.geo_id 13NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (154-incumbent-map.csv) or newly-inserted politician id (live-confirmed)"
      via: "156-04-ga-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 14 GA US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every renominated incumbent — after a MANDATORY per-candidate live-DB dedup check (D-03). GA is the highest-wrinkle state: three open/retired seats (GA-1/10/11) whose incumbents get NO active row, and GA-13 which is a TRUE VACANCY (David Scott died Apr 2026, no incumbent). Mirrors Phase 155's IL records plan (155-04, which handled 5 open-seat retirements), retargeted to GA (fips=13, geo 13NN) plus the GA-13 vacancy.

Purpose: USHC2-02 + USHC2-03 for GA. GA-2..9, 12, 14 are renominated (reuse incumbent_pid incl. Clay Fuller -13014, already seeded via the Apr-2026 special). GA-1/10/11 nominees + GA-13 Clark/Chavez are new records. The ~18 new records are these nominees + challengers.
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every GA House race (incl. GA-13 vacancy) surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates schema (verified in 150-03/155-04): id uuid PK; race_id uuid NOT NULL;
     politician_id uuid (D-06: non-null for House); full_name/first_name/last_name; photo_url NULL;
     is_incumbent bool; candidate_status 'active'; source. NO unique constraint -> (race_id, full_name) NOT EXISTS. -->
<!-- essentials.politicians: id uuid, external_id int (negative), full_name, first_name, last_name, is_active. NO updated_at. -->
<!-- GA renominated incumbents (REUSE incumbent_pid from 154-incumbent-map.csv): GA-2 Bishop a172a95a…/-13002,
     GA-3 Jack eabb384b…/-13003, GA-4 Johnson 135f4a8f…/-13004, GA-5 Williams acb046eb…/-13005, GA-6 McBath 66e69f62…/-13006,
     GA-7 McCormick b295210e…/-13007, GA-8 Scott 247e657a…/-13008, GA-9 Clyde 94d6a044…/-13009, GA-12 Allen 3de9e882…/-13012,
     GA-14 Clay Fuller 8c4ce29b-84ae-445b-9531-9fdaceb5d420/-13014 (already seeded via Apr-2026 special — REUSE, faces Shawn Harris D). -->
<!-- GA open/retired (incumbent keeps record, NO active row): GA-1 Buddy Carter 4bf58192…/-13001 -> Jim Kingston (R) + Amanda Hollowell (D);
     GA-10 Mike Collins b6542655…/-13010 -> Houston Gaines (R) + Pamela DeLancy (D);
     GA-11 Barry Loudermilk 7f0bb866…/-13011 -> John Cowan (R) + Chris Harden (D). -->
<!-- GA-13 (geo 1313) TRUE VACANCY (no incumbent pid): Jasmine Clark (D) + Jonathan Chavez (R) both NEW records, both active,
     NEITHER is_incumbent=true. NOTE Jasmine Clark is a sitting GA state rep -> live-dedup (may already have a record). -->
<!-- external_id band for new GA challengers (D-06): -(13*10000 + cd*100 + seq) -> e.g. GA-1 seq1 = -130101.
     GA incumbents occupy -13001..-13014. Verify -130000..-139999 band (excl. incumbent ids) collision-free before insert. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — reuse-vs-new per GA candidate + certified-general re-confirm + GA-13 vacancy + GA-1/10/11 open (D-03/D-04 mandatory)</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (GA rows geo_id 13NN: general_candidates, nominee_status, incumbent_pid; GA-13 row = geo 1313, VACANT, blank pid; GA-1/10/11 = open-seat-vacancy/retired)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (GA geo_id → incumbent_pid; GA-13 empty pid; GA-14 Clay Fuller present)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-PLAN.md (the IL multi-retirement reuse-vs-new + lost-incumbent-absent + open-seat-nominee reconciliation pattern — GA mirrors IL's retirement handling)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-SUMMARY.md (the IL open-seat reconciliation actually applied — the lost-incumbent-no-active-row precedent)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-03 dedup + GA certified-general re-confirm; D-04 GA-1/10/11 no-active-row + GA-13 vacancy no-ghost-incumbent; GA fips=13)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-ga-2026-house/156-04-ga-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 14 GA districts (~26+ rows). Resolve race_id by joining the 156-01 GA races to districts.geo_id (incl. the GA-13/1313 vacancy race).

    Re-confirm certified Nov-3 general nominees per district from the GA Secretary of State certified ballot list (D-03) BEFORE deciding rows — GA has open seats and a vacancy where the field is most volatile. Parse party for context only, NEVER store it (D-06).

    For EACH candidate:
    1. Renominated incumbents (GA-2..9, 12, 14) → REUSE, target = the 154 incumbent_pid, is_incumbent=true. GA-14 Clay Fuller (-13014) IS renominated (already seeded via the Apr-2026 special) — REUSE, faces Shawn Harris (D) as a NEW record.
    2. GA-1 Carter → NO row; Jim Kingston (R) + Amanda Hollowell (D) → live name query, REUSE-if-exists else NEW. GA-10 Collins → NO row; Houston Gaines (R) + Pamela DeLancy (D). GA-11 Loudermilk → NO row; John Cowan (R) + Chris Harden (D).
    3. GA-13 TRUE VACANCY → NO incumbent row at all (no incumbent pid exists); Jasmine Clark (D) + Jonathan Chavez (R) both → live name query. NOTE Jasmine Clark is a sitting GA state representative — she may already have a record; if a real matching record exists → REUSE that UUID (is_incumbent=FALSE — she is NOT the US House incumbent), else NEW. Chavez → REUSE-if-exists else NEW. Do NOT create a ghost GA-13 incumbent.
    4. For every other challenger / new name → run a LIVE name query; REUSE-if-exists else NEW.
    5. Confirm GA-1 Carter (-13001), GA-10 Collins (-13010), GA-11 Loudermilk (-13011) each appear 0 times as an active candidate; GA-13 has 0 is_incumbent=true rows.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id. GA incumbents occupy -13001..-13014. Use -(13*10000 + cd*100 + seq) (e.g. GA-1 seq1 = -130101) and VERIFY the -130000..-139999 band (excluding the incumbent ids) is empty before use; document any collision remap (149-01 lesson).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-ga-2026-house/156-04-ga-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<22){console.error('FAIL row count',body.length);process.exit(1)}; const rows=body.map(r=>r.split(',')); const newids=rows.filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; const ga13=rows.filter(c=>c[1]==='1313'); if(ga13.length<2){console.error('FAIL GA-13 must have >=2 candidates (Clark+Chavez)',ga13.length);process.exit(1)}; if(ga13.some(c=>c[8]==='true')){console.error('FAIL GA-13 has is_incumbent row (vacancy must have none)');process.exit(1)}; console.log('GA reconciliation OK rows',body.length,'new',newids.length,'GA-13 candidates',ga13.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 156-04-ga-reconciliation.csv has one row per GA general candidate across 14 districts (~26+ rows), each with a resolved race_id, re-confirmed against the GA SoS certified ballot list.
    - Every REUSE row's target_politician_id is a real live UUID; GA-2..9, 12, 14 REUSE their 154 incumbent_pid (GA-14 Clay Fuller -13014).
    - GA-1 Carter/-13001, GA-10 Collins/-13010, GA-11 Loudermilk/-13011 have NO REUSE-as-active row; their certified nominees present. GA-13 has ≥2 candidates (Clark+Chavez), 0 is_incumbent rows, no ghost incumbent; Jasmine Clark live-dedup applied.
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live external_id<0 set; band verified empty.
  </acceptance_criteria>
  <done>ga-reconciliation.csv exists, all REUSE UUIDs live-confirmed, certified generals re-confirmed, GA-1/10/11 absent, GA-13 vacancy (Clark+Chavez, no ghost incumbent) confirmed, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent GA House seed migration (new politicians + race_candidates, incl. GA-13 vacancy race)</name>
  <read_first>
    - backend/data/seed-ga-2026-house/156-04-ga-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics with multi-retirement open seats — INSERT politicians WHERE NOT EXISTS + INSERT race_candidates WHERE NOT EXISTS; lost-incumbent no-active-row handling; canonical accents; no challenger offices)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md (the GA election UUID + whether the GA-13 office was created — confirm the GA-13 race resolves)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-06 invariants; D-04 GA-13 no ghost incumbent; never office_id NULL; never party on card)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk). Name `NNNN_seed_ga_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (154-FIELD-TABLE.md GA section + per-district source URLs) + the antipartisan invariant (D-06) + notes GA-13 is a true vacancy (Clark/Chavez both active, no incumbent).

    Migration body, in order (per the 155-04 Task-2 mechanics):
    1. INSERT the new GA politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers/nominees. Do NOT create a GA-13 incumbent politician.
    2. INSERT the race_candidates onto the 14 GA races (incl. the GA-13/1313 vacancy race): for each reconciliation row, race_id=resolved GA race, politician_id=REUSE target or just-inserted new UUID, full_name/first_name/last_name, is_incumbent per CSV (GA-13 rows is_incumbent=false), candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name). Do NOT insert any active row for GA-1 Carter / GA-10 Collins / GA-11 Loudermilk.
    3. NEVER touch the races rows (156-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate; NEVER create a GA-13 ghost incumbent.

    Post-COMMIT verification block (or run separately), scoped to GA NATIONAL_LOWER (geo prefix '13') within the GA 2026 Statewide General election: every GA race (incl. GA-13) has ≥1 active candidate; 0 NULL politician_id; 0 duplicate full_name within GA; GA-1 Carter (-13001)/GA-10 Collins (-13010)/GA-11 Loudermilk (-13011) 0 active rows; GA-13 (geo 1313) has ≥2 active candidates + 0 is_incumbent rows; renominated incumbents (incl. GA-14 Fuller) present as is_incumbent=true.

    Run dry first (review SQL), then apply against prod via psql -v ON_ERROR_STOP=1. Do not inline the SQL here — author per the 155-04 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='GA 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='13'\`); const row=r.rows[0]; const g13=await pool.query(\`SELECT count(*) FILTER (WHERE rc.candidate_status='active') act, count(*) FILTER (WHERE rc.is_incumbent) inc FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='GA 2026 Statewide General' AND d.geo_id='1313'\`); const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='GA 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='13' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==14||+row.nullpid>0||+dup.rows[0].n>0||+g13.rows[0].act<2||+g13.rows[0].inc>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n,'ga13act',g13.rows[0].act,'ga13inc',g13.rows[0].inc);process.exit(1)}; console.log('GA OK: 14 races,',row.active_rc,'active rc, 0 null pid, 0 dup, GA-13',g13.rows[0].act,'active 0 incumbent'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 14 GA House races (NATIONAL_LOWER geo '13' within the GA 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within GA (D-03 zero-dup-per-state).
    - GA-1 Carter/GA-10 Collins/GA-11 Loudermilk have no active row; their certified nominees active. GA-13 (geo 1313) has ≥2 active candidates (Clark+Chavez), 0 is_incumbent rows, no ghost incumbent. GA-14 Fuller + all renominated incumbents present as is_incumbent=true.
    - No new essentials.offices rows created (the GA-13 office, if any, came from 156-01); no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; GA 14-race assertion passes (incl. GA-13 vacancy ≥2 active 0 incumbent); GA-1/10/11 absent; dedup correct; idempotent. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes new GA politicians + race_candidates to live prod |
| 154 field artifacts → seed input | Field/identity locked in Phase 154; reuse-vs-new + GA certified-general re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-16 | Tampering | duplicate incumbent record (esp. GA-13 Clark, a sitting official) | mitigate | Live name-check per candidate; reuse existing pid if found; 0-dup-full_name-per-state gate |
| T-156-17 | Tampering | GA-13 ghost incumbent created | mitigate | No incumbent politician/row for GA-13; verify 0 is_incumbent rows in geo 1313; D-04 |
| T-156-18 | Repudiation | retired/open GA incumbent (Carter/Collins/Loudermilk) surfaced as active | mitigate | No active row for -13001/-13010/-13011; 156-verify.sql D-04 absence assertion |
| T-156-19 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-156-20 | Tampering | seeding a primary field instead of GA certified general | mitigate | D-03 GA SoS certified-general re-confirm before insert |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 156-verify.sql USHC2-02/03 + D-03/D-04 GA assertions pass after this runs (incl. GA-13 vacancy + GA-1/10/11 absence).
- Manual spot-check: cross-check GA-1/10/11/13 + ~3 renominated GA districts against 154-FIELD-TABLE.md citations.
</verification>

<success_criteria>
- 14 GA House races each carry their active field, all politician_id non-null; ~18 new politician rows; renominated incumbents reused (incl. GA-14 Fuller); GA-1/10/11 absent; GA-13 vacancy Clark+Chavez active no ghost incumbent; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-04-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list, GA-1/10/11 absence confirmation, GA-13 Clark/Chavez decisions [REUSE vs NEW], any 154-name flips).
</output>
