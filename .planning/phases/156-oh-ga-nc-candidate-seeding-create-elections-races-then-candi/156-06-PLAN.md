---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 06
type: execute
wave: 3
depends_on: ["156-03", "156-04", "156-05"]
files_modified:
  - backend/scripts/seed-oh-ga-nc-house-headshots.py
  - backend/data/seed-oh-2026-house/156-06-oh-headshots.manual.txt
  - backend/data/seed-ga-2026-house/156-06-ga-headshots.manual.txt
  - backend/data/seed-nc-2026-house/156-06-nc-headshots.manual.txt
autonomous: true
requirements: [USHC2-04]
must_haves:
  truths:
    - "Every newly-seeded OH, GA, and NC candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row + photo_origin_url, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/scripts/seed-oh-ga-nc-house-headshots.py"
      provides: "Headshot pipeline targeting the new OH/GA/NC candidate external_id bands (clone of the validated seed-pa-il-house-headshots.py from Phase 155)"
    - path: "backend/data/seed-oh-2026-house/156-06-oh-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source portraits where Wikipedia has none (OH; GA + NC files analogous)"
  key_links:
    - from: "new OH/GA/NC candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-oh-ga-nc-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded OH, GA, and NC candidate a headshot following the established find-headshots conventions (600x750, free-license, Storage-mirrored + politician_images row + photo_origin_url), reusing the validated Phase 155 headshot pipeline (which itself cloned the Phase-150 pipeline). One shared script serves all three states, run three times (once per external_id band).

Purpose: USHC2-04 (OH+GA+NC). race_candidates resolve photos via COALESCE(rc.photo_url, pi.url) lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`. Reused incumbents already have headshots — only the 156-03/04/05 new candidates need them.
Output: The OH/GA/NC headshot script + three manual-source lists; every new candidate has an image row or a documented honest-skip. Expect mostly honest-skips for thin minor-line candidates (155 precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-04-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-05-SUMMARY.md
@.claude/skills/find-headshots/SKILL.md
@backend/scripts/seed-pa-il-house-headshots.py
@backend/scripts/seed-ca-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the headshot pipeline to target the new OH/GA/NC candidate bands + run it for all three states</name>
  <read_first>
    - backend/scripts/seed-pa-il-house-headshots.py (the Phase-155 pipeline — already parameterized by state/external_id band; clone + add OH/GA/NC bands)
    - backend/scripts/seed-ca-house-headshots.py (the original validated pipeline: Wikipedia pageimages → free-license check → hardened wrong-person guard → 4:5 crop → 600x750 LANCZOS q90 → PUT politician_photos/{uuid}-headshot.jpg x-upsert → INSERT politician_images WHERE NOT EXISTS; --manual reads external_id|image_url|license)
    - .claude/skills/find-headshots/SKILL.md (find-headshots conventions: Storage-mirrored 600×750 + politician_images row + photo_origin_url; free-license; wrong-person guard)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-SUMMARY.md + 156-04-SUMMARY.md + 156-05-SUMMARY.md (the authoritative new-candidate external_id lists for OH/GA/NC)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-05-SUMMARY.md + 155-06-SUMMARY.md (the PA/IL headshot runs: auto-pass rate, manual/honest-skip handling, the wrong-person guard behavior on challengers)
  </read_first>
  <action>
    Clone seed-pa-il-house-headshots.py to backend/scripts/seed-oh-ga-nc-house-headshots.py. Swap ONLY the target-selection: parameterize it to accept a state/band argument (or explicit external_id lists — OH from 156-03-SUMMARY, GA from 156-04-SUMMARY, NC from 156-05-SUMMARY) so the SAME script serves all three states. Because new challengers have NO essentials.offices row, the target query selects by explicit negative external_id list AND NOT EXISTS a politician_images row. Keep EVERY guard intact: free-license-only, hardened wrong-person guard (title must contain candidate first+surname; reject election/place/event/non-political disambiguators), 4:5 crop → 600x750, x-upsert, idempotent INSERT, photo_origin_url set.

    Run the automatic Wikipedia pass for each band in turn: `python backend/scripts/seed-oh-ga-nc-house-headshots.py` with the OH list, then GA, then NC. For candidates with no free Wikipedia portrait, build the per-state manual file (backend/data/seed-oh-2026-house/156-06-oh-headshots.manual.txt, and the GA/NC analogs) with external_id|image_url_or_path|license lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL — MEMORY one-try rule), then run with --manual.

    For any candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 156-verify.sql can pin it — NEVER a placeholder or wrong-person image. Expect many minor-line honest-skips (OH/NC third-party candidates rarely have free portraits).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const base='../.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/'; let ids=[]; for(const [f,re] of [['156-03-SUMMARY.md',/-39[0-9]{4}/g],['156-04-SUMMARY.md',/-13[0-9]{4}/g],['156-05-SUMMARY.md',/-37[0-9]{4}/g]]){try{const s=readFileSync(base+f,'utf8'); ids.push(...[...s.matchAll(re)].map(m=>+m[0]).filter(x=>x<-130000));}catch(e){}} ids=[...new Set(ids)]; if(!ids.length){console.error('no new OH/GA/NC external_ids found in SUMMARYs (check bands)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new OH/GA/NC candidates still missing headshot (should be only documented honest-skips):', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-oh-ga-nc-house-headshots.py targets the new-candidate set by explicit external_id list (not the offices-join query) and keeps all original guards; parameterized to run per state (OH, GA, NC).
    - Every new OH/GA/NC candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed, photo_origin_url set; wrong-person guard intact.
    - Re-running inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script created + run for OH, GA, NC; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) → Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-25 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained from the Phase-155 pipeline; manual sources human-verified; spot-check in 156-10 |
| T-156-26 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-156-27 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-156-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (156-10): sample new OH/GA/NC headshots against source pages for identity.
</verification>

<success_criteria>
- Every new OH/GA/NC candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-06-SUMMARY.md` when done (record: candidates imaged per state, honest-skips with reasons + external_ids for gate pinning, manual-source URLs, the shared script path).
</output>
