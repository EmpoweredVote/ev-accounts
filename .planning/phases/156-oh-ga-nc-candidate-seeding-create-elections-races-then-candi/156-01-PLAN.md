---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/NNNN_seed_oh_ga_nc_2026_house_elections_races.sql
autonomous: true
requirements: [USHC2-03]
must_haves:
  truths:
    - "An essentials.elections row exists for each of 'OH 2026 Statewide General', 'GA 2026 Statewide General', 'NC 2026 Statewide General', election_date 2026-11-03, mirroring the PA/IL/TX/NY 2026 Statewide General shape"
    - "Exactly 15 essentials.races rows exist for OH (one per geo_id 39NN), 14 for GA (one per geo_id 13NN incl. GA-13/1313), 14 for NC (one per geo_id 37NN), each office_id = that district's existing U.S. Representative office, never office_id IS NULL"
    - "The GA-13 (geo 1313) NATIONAL_LOWER U.S. Representative office exists before its race is created — if absent (true vacancy, David Scott died Apr 2026), it is created first (the FL-20 / mig-1115 vacancy pattern), never a ghost incumbent"
  artifacts:
    - path: "backend/migrations/NNNN_seed_oh_ga_nc_2026_house_elections_races.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT 3 elections + 43 races (15 OH + 14 GA + 14 NC) on existing House offices (GA-13 office created if absent); no race_candidates yet"
      contains: "essentials.races"
  key_links:
    - from: "essentials.races.office_id"
      to: "the existing NATIONAL_LOWER U.S. Representative office for each OH 39NN / GA 13NN / NC 37NN geo_id"
      via: "office lookup by districts.geo_id (races has no geo_id column)"
      pattern: "office_id"
    - from: "essentials.races.election_id"
      to: "the newly-authored OH / GA / NC 2026 Statewide General election rows"
      via: "INSERT … SELECT id FROM essentials.elections WHERE name=…"
      pattern: "election_id"
---

<objective>
Author the `essentials.elections` + `essentials.races` rows that OH, GA, and NC require before any candidates can be wired. All three states have ZERO 2026 House races (all 43 rows in 154-field-table.csv carry a BLANK existing_race_id), so this Wave-1 plan creates the election + per-district race scaffold that Wave 2 wires `race_candidates` onto. This is the create-races-first structural step — a near-verbatim repeat of Phase 155's 155-01 (PA/IL), extended to 3 states, with the GA-13 true-vacancy office-existence check added (per D-04).

Purpose: USHC2-03 prerequisite — the elections-feed surfaces a race only if an `essentials.races` row exists on the district's office.
Output: One idempotent migration creating 3 elections + 43 races (GA-13 office created first if absent); `/elections` still shows no field (no candidates yet) but the scaffold is live for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv

<interfaces>
<!-- essentials.elections shape (mirror the live PA/IL/TX/NY 2026 Statewide General rows):
     columns include id uuid (gen_random_uuid()), name text, election_date timestamptz,
     election_type text ('general'), jurisdiction_level text ('state'), state text ('OH'/'GA'/'NC'),
     description NULL. Confirm the live PA row shape before authoring (SELECT * FROM
     essentials.elections WHERE name='PA 2026 Statewide General'). -->
<!-- essentials.races shape: id uuid, election_id uuid FK->elections, office_id uuid FK->offices,
     position_name text ('U.S. Representative District N'), primary_party text (NULL ok here),
     seats int (1), description NULL. NO geo_id column on races. -->
<!-- OH/GA/NC US Representative offices ALREADY EXIST for the incumbent-mapped districts:
     15 NATIONAL_LOWER offices for OH geo_id 3901..3915, 14 for NC 3701..3714,
     and 13 for GA (1301..1312, 1314) — GA-13 (geo 1313) is the TRUE VACANCY whose office
     MUST be verified/created first (see Task 1). office_id lookup is by district geo_id.
     NEVER create a NON-VACANCY office. -->
<!-- GA-13 (geo 1313): true 0-holder vacancy (David Scott died Apr 2026). No incumbent politician_id.
     The NATIONAL_LOWER office for 1313 MAY need creating (FL-20 / mig-1115 precedent) BEFORE its race
     if absent. Do NOT create a ghost incumbent politician or a race_candidates incumbent row. -->
<!-- VERIFY LIVE before authoring: 0 essentials.races on any OH/GA/NC NATIONAL_LOWER office; no
     'OH/GA/NC 2026 Statewide General' election exists (STATE.md Open Blockers query #3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify/create the GA-13 vacancy office, then author + run the OH/GA/NC elections + races scaffold migration</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (OH rows geo_id 39NN + GA rows geo_id 13NN + NC rows geo_id 37NN: the 15 OH + 14 GA + 14 NC geo_ids, target_election names 'OH/GA/NC 2026 Statewide General', cd for position_name; GA-13 row = geo 1313, VACANT, blank incumbent_pid)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (OH 39NN / GA 13NN / NC 37NN geo_id → incumbent_pid; GA-13 row has empty pid = vacancy; GA-14 Clay Fuller 8c4ce29b… already seeded)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-01-PLAN.md (the validated create-races-first authoring template — mirror exactly for OH/GA/NC)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-01-SUMMARY.md (the PA/IL election UUIDs + per-state race counts + the idempotency precedent actually applied)
    - backend/migrations/1115_* (the FL-20 vacancy office-creation precedent for GA-13; find on disk — the pattern for creating a NATIONAL_LOWER office row on a vacant district)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-CONTEXT.md (D-04 GA-13 vacancy office-check; D-06: office_id NEVER NULL; never party on card; specifics: election names + 2026-11-03 date + FIPS OH=39/GA=13/NC=37)
    - backend/migrations (find the most-recent idempotent BEGIN/COMMIT seed migration on disk for the NOT EXISTS guard + header precedent)
  </read_first>
  <action>
    First, live-check the GA-13 office: query essentials.offices JOIN essentials.districts WHERE district_type='NATIONAL_LOWER' AND geo_id='1313'. If the office row is ABSENT, the migration must CREATE it first (mirror backend/migrations/1115_* FL-20 vacancy office-creation: an office row on the existing district 1313 with the canonical U.S. Representative title, no politician_id / vacant seat) BEFORE inserting the GA-13 race. If it already exists, reuse it. NEVER create a ghost incumbent politician or an incumbent race_candidates row for GA-13.

    Pick the next free migration number by listing backend/migrations and incrementing the highest on disk (disk wins over DB MAX). Name it `NNNN_seed_oh_ga_nc_2026_house_elections_races.sql`. All writes inside one BEGIN; … COMMIT;. Header comment cites the field source (154-FIELD-TABLE.md + the per-state Wikipedia/Ballotpedia URLs in 154-field-table.csv source_url) and the antipartisan invariant (D-06: party is NOT stored on race_candidates; races.primary_party stays NULL — there is no single primary party for a multi-candidate general).

    Migration body, in order:
    1. (If GA-13 office absent) INSERT the GA-13 (geo 1313) NATIONAL_LOWER office idempotently (NOT EXISTS guard), FL-20 pattern, no politician_id.
    2. INSERT the 3 elections idempotently (NOT EXISTS guard on name): 'OH 2026 Statewide General' (state 'OH'), 'GA 2026 Statewide General' (state 'GA'), 'NC 2026 Statewide General' (state 'NC'), election_date '2026-11-03', election_type 'general', jurisdiction_level 'state', description NULL — mirror the live PA/IL rows exactly.
    3. INSERT one race per OH/GA/NC House district onto the EXISTING office (incl. the just-verified/created GA-13 office), resolving office_id by district geo_id via the join essentials.offices JOIN essentials.districts ON district_id WHERE district_type='NATIONAL_LOWER' AND geo_id matches. For each of the 15 OH geo_ids (3901..3915) + 14 GA geo_ids (1301..1312, 1313, 1314) + 14 NC geo_ids (3701..3714): election_id = the matching state's just-inserted election (resolve by name), office_id = the looked-up office, position_name='U.S. Representative District '||cd, primary_party=NULL, seats=1. Guard each with NOT EXISTS on (election_id, office_id) — application-enforced idempotency, no DB unique constraint.
    4. NEVER create a non-vacancy essentials.offices or districts row. NEVER set office_id NULL.

    Add a post-COMMIT verification block (or run separately) asserting: exactly 1 each of 'OH/GA/NC 2026 Statewide General' election; exactly 15 races on OH NATIONAL_LOWER offices + 14 on GA + 14 on NC; 0 races with NULL office_id; every OH geo_id 3901..3915, GA 1301..1314 (incl. 1313), NC 3701..3714 has exactly 1 race.

    Run dry first (review generated SQL), then apply against prod (kxsdzaojfaibhuzmclfq) from backend/ with `set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_…sql`. Do not inline the SQL here — author it per the 155-01 mechanics in the read_first files.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const e=await pool.query(\"SELECT state, count(*) n FROM essentials.elections WHERE name IN ('OH 2026 Statewide General','GA 2026 Statewide General','NC 2026 Statewide General') GROUP BY state\"); const r=await pool.query(\"SELECT substr(d.geo_id,1,2) fips, count(DISTINCT r.id) races, count(*) FILTER (WHERE r.office_id IS NULL) nulloff FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name IN ('OH 2026 Statewide General','GA 2026 Statewide General','NC 2026 Statewide General') AND d.district_type='NATIONAL_LOWER' GROUP BY 1 ORDER BY 1\"); const oh=r.rows.find(x=>x.fips==='39'); const ga=r.rows.find(x=>x.fips==='13'); const nc=r.rows.find(x=>x.fips==='37'); if(e.rows.length!==3){console.error('FAIL elections',e.rows);process.exit(1)}; if(!oh||+oh.races!==15){console.error('FAIL OH races',oh);process.exit(1)}; if(!ga||+ga.races!==14){console.error('FAIL GA races',ga);process.exit(1)}; if(!nc||+nc.races!==14){console.error('FAIL NC races',nc);process.exit(1)}; console.log('OK: 3 elections, OH 15, GA 14, NC 14 races, 0 null office_id'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - The GA-13 (geo 1313) NATIONAL_LOWER office is confirmed present or created (FL-20 pattern) before its race; no ghost incumbent politician or incumbent race_candidates row for GA-13.
    - Exactly 1 each 'OH/GA/NC 2026 Statewide General' election row, each election_date 2026-11-03, election_type 'general', jurisdiction_level 'state'.
    - Exactly 15 races on OH NATIONAL_LOWER offices (geo 3901..3915) + 14 on GA (1301..1314 incl. 1313) + 14 on NC (3701..3714); each office_id non-null and = the existing district office.
    - 0 races created with NULL office_id; 0 new non-vacancy essentials.offices or districts rows.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency proven).
  </acceptance_criteria>
  <done>Migration applied; 3 elections + 43 races live (GA-13 office created if it was absent); verification passes; idempotent on re-run. Record the three election UUIDs + whether the GA-13 office was created in the SUMMARY for Wave-2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes 3 elections + 43 races (+ possibly 1 GA-13 office) to live prod |
| 154 field artifacts → seed input | geo_id list locked + human-reviewed in Phase 154 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Tampering | duplicate election/race/office on re-run | mitigate | NOT EXISTS guards on (name), (election_id, office_id), (geo_id) for the GA-13 office; re-run inserts 0 |
| T-156-02 | Tampering | race wired to wrong/NULL office | mitigate | office_id resolved by district geo_id join; post-COMMIT asserts 0 NULL office_id + exact 15/14/14 counts |
| T-156-03 | Tampering | ghost GA-13 incumbent created | mitigate | GA-13 office created with NO politician_id; no incumbent race_candidates row; D-04 |
| T-156-04 | DoS | accidental write to an existing OH/GA/NC election | mitigate | All inserts scoped to the three named 2026 Statewide General elections; never touch existing elections |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 156-verify.sql (authored in 156-02) USHC2-03 race-scaffold assertions pass after this runs.
- Manual: spot-check 3 OH + 3 GA (incl. GA-13) + 3 NC races resolve to the correct office by geo_id.
</verification>

<success_criteria>
- 3 elections + 43 races (15 OH + 14 GA + 14 NC) live, all office_id non-null, GA-13 office present, idempotent.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md` when done (record: migration number, the OH + GA + NC election UUIDs, per-state race count, whether the GA-13 office was created, any geo_id that failed office lookup).
</output>
