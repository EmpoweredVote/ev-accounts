---
phase: 156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi
plan: 10
type: execute
wave: 4
depends_on: ["156-01", "156-02", "156-03", "156-04", "156-05", "156-06", "156-07", "156-08", "156-09"]
files_modified:
  - backend/scripts/156-coordinate-smoke.ts
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05]
autonomous: true
must_haves:
  truths:
    - "156-verify.sql runs read-only against prod and EVERY labeled assertion (USHC2-02/03/04/05 + D-01/D-02/D-03/D-04) PASSES with exit 0"
    - "For >=3 in-district OH + >=3 in-district GA (incl. GA-13 vacancy) + >=3 in-district NC House test coordinates, the surfacing join returns the resident's House race with its full candidate field (CHALLENGER/non-incumbent present)"
    - "0 unsourced stance rows for the in-scope set (incl. NC-6 McDowell) + 0 duplicate full_name per state across all 15 OH + 14 GA + 14 NC districts; honest-skips pinned by UUID; GA-1/10/11 absent; GA-13 Clark+Chavez active with no ghost incumbent"
  artifacts:
    - path: "backend/scripts/156-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers an OH + GA + NC House geofence centroid -> assert race + field surfaces with challenger/non-incumbent (GA-13 vacancy sample included)"
  key_links:
    - from: "156-verify.sql + 156-coordinate-smoke.ts"
      to: "the 15 OH + 14 GA + 14 NC House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 156 end-to-end: run the per-state OH/GA/NC-scoped `156-verify.sql` gate green and add a coordinate-surfacing smoke that confirms >=3 OH + >=3 GA (incl. the GA-13 vacancy race) + >=3 NC House races actually surface for an in-district address. Mirrors Phase 155's 155-09 (gate + smoke), extended to 3 states with the GA-13 vacancy + NC-6 McDowell checks.

Purpose: USHC2-02/03/04/05 consolidated verification for OH + GA + NC — the SECOND of three Wave-2 seeding-phase proofs. This gate + smoke feed Phase 158's full-113-district (100-decided) gate.
Output: A passing gate + coordinate smoke; Phase 156 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/scripts/156-verify.sql
@backend/scripts/155-coordinate-smoke.ts
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-01-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-03-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-04-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-05-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-06-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-07-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-08-SUMMARY.md
@.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the OH+GA+NC coordinate-surfacing smoke + finalize honest-skip pins in 156-verify.sql</name>
  <read_first>
    - backend/scripts/155-coordinate-smoke.ts (the validated read-only ST_Covers surfacing smoke for two states — mirror it for OH geo_ids 39NN + GA geo_ids 13NN + NC geo_ids 37NN)
    - backend/scripts/156-verify.sql (the gate authored in 156-02; its honest-skip + in-scope TEMP-table pins incl. the McDowell UNION)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers -> district geo_id -> office -> race -> race_candidates; the join discriminator d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc))
    - the 156-06 SUMMARY (headshot honest-skips) + 156-07/08/09 SUMMARYs (whole-record-stance honest-skips) — the pids to pin by UUID
  </read_first>
  <action>
    Author backend/scripts/156-coordinate-smoke.ts (read-only) by cloning 155-coordinate-smoke.ts and parameterizing for OH + GA + NC: pick >=3 spread OH geo_ids (e.g. 3901/3904/3909) + >=3 spread GA geo_ids INCLUDING GA-13 (1313 vacancy open seat) + one open/retired seat (e.g. 1301 GA-1 or 1310 GA-10) + one renominated (e.g. 1305) + >=3 spread NC geo_ids INCLUDING NC-6 (3706 McDowell). For each, resolve the election by name ('OH/GA/NC 2026 Statewide General'), compute an interior centroid via ST_PointOnSurface(gb.geometry), run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point -> district -> office -> race within that election, NATIONAL_LOWER, geo prefix '39'/'13'/'37' -> race_candidates active count + linked count + challenger count).

    Assert each sample race returns >=2 active candidates (documented allowance for any pinned unopposed district), non-null politician_id, AND >=1 challenger OR >=1 non-incumbent (the open-seat/challenger two-path guard). For the GA-13 (1313) sample specifically: assert >=2 active candidates (Clark+Chavez) with 0 is_incumbent=true rows (true vacancy). For the GA-1/10/11 sample: assert the certified NOMINEE is active and the retired/open incumbent (Carter/Collins/Loudermilk) is absent (by full_name match).

    Gather the documented honest-skips (headshots from 156-06; whole-record stances from 156-07/08/09 SUMMARYs) and confirm they are pinned by exact UUID/external_id with explicit ORDER BY inside 156-verify.sql's pin sets; if any are missing, add them (this pin-list update is the only edit allowed to 156-verify.sql here — not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/156-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 156-coordinate-smoke.ts is read-only and asserts >=3 OH + >=3 GA + >=3 NC House races surface with their active field incl. >=1 challenger/non-incumbent for an in-district centroid.
    - The GA-13 (1313) sample asserts >=2 active candidates (Clark+Chavez), 0 is_incumbent rows (vacancy); the GA-1/10/11 sample asserts the certified nominee active + retired/open incumbent absent.
    - All documented headshot + whole-record-stance honest-skips are pinned by exact UUID/external_id with explicit ORDER BY in 156-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for >=3 OH + >=3 GA (incl. GA-13 vacancy) + >=3 NC districts; honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 156-verify.sql gate green</name>
  <read_first>
    - backend/scripts/156-verify.sql (the full gate)
    - .planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-02-SUMMARY.md (the assertion labels + in-scope-set convention + all-partial-incumbent-except-McDowell exclusion + GA-13 no-absence-pin note)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-09-PLAN.md (Task 2: the gate-green run convention — route failures to the owning wave plan, never relax an assertion)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/156-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (elections/races -> 156-01; OH records -> 156-03; GA records -> 156-04; NC records -> 156-05; headshots -> 156-06; OH stances -> 156-07; GA stances -> 156-08; NC stances/McDowell -> 156-09) and re-run after fix — do NOT relax an assertion to make it pass (149 T-149-18). Confirm the gate stays per-state-scoped (no cross-state contamination), honors the D-01 all-partial-incumbent-except-McDowell exclusion + the GA-13 vacancy handling, and is write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/156-verify.sql >/tmp/156gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -25 /tmp/156gate.log; exit 1; fi; grep -c 'PASS' /tmp/156gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 156-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 15 OH + 14 GA + 14 NC House races each carry their active field, 0 null politician_id; 0 duplicate full_name per state; GA-1/10/11 absent + certified nominees present; GA-13 Clark+Chavez active with 0 is_incumbent rows; D-02 OH/NC minor-line present; every new candidate has a headshot (or pinned skip); 0 unsourced stances for the in-scope set incl. NC-6 McDowell; federal-24 coverage or pinned whole-record skip; all other OH/GA/NC partials untouched.
    - Coordinate smoke (Task 1) passes for >=3 OH + >=3 GA (incl. GA-13) + >=3 NC districts.
  </acceptance_criteria>
  <done>156-verify.sql green (exit 0); coordinate smoke green; Phase 156 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke -> production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-46 | Repudiation | gate passes while a district silently has no challenger | mitigate | Coordinate smoke asserts >=1 non-incumbent candidate per sample race (open-seat/challenger two-path guard) |
| T-156-47 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-156-48 | Repudiation | cross-state contamination false-passes a count | mitigate | Gate per-state scoped (geo '39'/'13'/'37' within named elections); re-confirmed write-free |
| T-156-49 | Repudiation | GA-13 ghost incumbent or GA-1/10/11 retired incumbent silently surfaced | mitigate | Smoke + gate assert GA-13 0 incumbent rows + certified nominee active for GA-1/10/11, retired incumbent absent |
| T-156-50 | Repudiation | NC-6 McDowell left zero-stance despite being in-scope | mitigate | Gate USHC2-05b asserts McDowell >=1 sourced stance (or pinned skip); 156-09 verify keyed on -37006 |
| T-156-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 156-verify.sql exits 0, all PASS.
- 156-coordinate-smoke.ts surfaces >=3 OH + >=3 GA (incl. GA-13 vacancy) + >=3 NC House races with challenger/non-incumbent present + open-seat certified nominee.
</verification>

<success_criteria>
- All USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 assertions PASS; coordinate smoke green; OH + GA + NC Wave-2 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/156-oh-ga-nc-candidate-seeding-create-elections-races-then-candi/156-10-SUMMARY.md` when done (record: final per-state per-district candidate counts, total new records OH + GA + NC, total stanced candidates incl. McDowell, all honest-skips pinned, GA-13 vacancy confirmation, gate PASS log, smoke results — this becomes the OH+GA+NC input to the Phase 158 full-gate).
</output>
