---
phase: 22-radar-label-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - ev-ui/package.json
  - CompassV2/package.json
autonomous: false
requirements:
  - LABEL-01
  - LABEL-02
  - LABEL-03

must_haves:
  truths:
    - "ev-ui is published at a new patch version (0.1.22) to GitHub npm registry"
    - "CompassV2 package.json references the new ev-ui version"
    - "CompassV2 dev server renders radar chart labels without clipping"
    - "Labels render correctly in both personal compass and Compare views"
  artifacts:
    - path: "ev-ui/package.json"
      provides: "Bumped version 0.1.22"
      contains: "0.1.22"
    - path: "CompassV2/package.json"
      provides: "Updated ev-ui dependency"
      contains: "0.1.22"
  key_links:
    - from: "CompassV2/src/components/RadarChart.jsx"
      to: "@chrisandrewsedu/ev-ui"
      via: "npm import of RadarChartCore"
      pattern: "import.*RadarChartCore.*ev-ui"
---

<objective>
Publish the updated ev-ui package and update CompassV2 to consume the new version with fixed radar chart labels.

Purpose: The label fixes from Plan 01 live in ev-ui, which is a published npm package consumed by CompassV2. This plan publishes the new version and verifies the fixes render correctly in the actual application.

Output: ev-ui 0.1.22 published to GitHub npm registry, CompassV2 updated and verified.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-radar-label-fixes/22-CONTEXT.md
@.planning/phases/22-radar-label-fixes/22-01-SUMMARY.md
@ev-ui/package.json
@CompassV2/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, version bump, and publish ev-ui</name>
  <files>ev-ui/package.json</files>
  <action>
1. Build ev-ui to verify the updated RadarChartCore compiles:
   ```
   cd ev-ui && npm run build
   ```
   Confirm no errors in build output.

2. Bump the version in `ev-ui/package.json` from `0.1.21` to `0.1.22` (patch bump).

3. Publish to GitHub npm registry:
   ```
   cd ev-ui && npm publish
   ```
   This publishes `@chrisandrewsedu/ev-ui@0.1.22` to `https://npm.pkg.github.com`.

4. Verify the published version:
   ```
   npm view @chrisandrewsedu/ev-ui version --registry=https://npm.pkg.github.com
   ```
   Should return `0.1.22`.
  </action>
  <verify>
`npm run build` exits 0. `npm publish` succeeds. `npm view` returns 0.1.22.
  </verify>
  <done>
ev-ui 0.1.22 is published to GitHub npm registry with the RadarChartCore label fixes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CompassV2 to consume new ev-ui version</name>
  <files>CompassV2/package.json</files>
  <action>
1. Update the `@chrisandrewsedu/ev-ui` dependency in `CompassV2/package.json` from `^0.1.21` to `^0.1.22`.

2. Install the new version:
   ```
   cd CompassV2 && npm install
   ```

3. Build CompassV2 to confirm no breaking changes:
   ```
   cd CompassV2 && npm run build
   ```
   Should complete without errors.

4. Start the dev server for verification:
   ```
   cd CompassV2 && npm run dev
   ```
   Note the local URL (typically http://localhost:5173).
  </action>
  <verify>
`npm install` resolves `@chrisandrewsedu/ev-ui@0.1.22`. `npm run build` exits 0.
  </verify>
  <done>
CompassV2 consumes ev-ui 0.1.22 and builds without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify radar chart labels visually</name>
  <files>CompassV2/src/components/RadarChart.jsx</files>
  <action>
Human verification checkpoint. The following was built:
- Dynamic horizontal padding so labels never clip at edges
- 10px minimum font size for all labels
- Multi-word labels wrap naturally (max 2 lines, no truncation)
- "Medicare/Medicaid" splits at "/" onto two lines
- Inside-aligned text (left labels right-align, right labels left-align)

Steps to verify:
1. Open CompassV2 dev server (http://localhost:5173 or whatever port is shown)
2. Navigate to the compass page with topics selected
3. Check these specific cases:
   a. Left/right edge labels — no text cut off at the SVG boundary
   b. Short labels (e.g., "Immigration") — readable, not tiny
   c. Long single-word labels (e.g., "Misinformation") — readable, at least 10px
   d. Multi-word labels (e.g., "AI Regulation") — both words visible on separate lines
   e. "Medicare/Medicaid" — if this topic is selected, verify it splits at "/" onto two lines
4. Try the Compare view (select a politician) — verify same label behavior
5. Test with different topic counts (3, 5, 8) if possible — labels should not overlap
  </action>
  <verify>User confirms labels render correctly in both personal compass and Compare views</verify>
  <done>Human approved: all three label requirements (LABEL-01, LABEL-02, LABEL-03) satisfied visually</done>
</task>

</tasks>

<verification>
After all tasks and human verification:
1. ev-ui 0.1.22 is live on GitHub npm registry
2. CompassV2 builds and runs with the new version
3. Human confirmed: labels not clipped, readable, properly wrapped
4. Both personal compass and Compare views work correctly
</verification>

<success_criteria>
- ev-ui 0.1.22 published
- CompassV2 updated and building
- Human verified: all three label requirements (LABEL-01, LABEL-02, LABEL-03) satisfied visually
</success_criteria>

<output>
After completion, create `.planning/phases/22-radar-label-fixes/22-02-SUMMARY.md`
</output>
