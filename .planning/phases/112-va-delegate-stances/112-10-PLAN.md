---
phase: 112-va-delegate-stances
plan: 10
type: execute
wave: 2
depends_on: ["112-09"]
files_modified:
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv
  - supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql
autonomous: true
requirements: [VAST-03, VAST-05]
tags: [stance-research, virginia, house-of-delegates, phase-gate]

must_haves:
  truths:
    - "Each of the 6 Wave 10 delegates (HD-11 through HD-16, NoVA Fairfax/Prince William) either appears in inform.politician_answers with >= 1 row OR is documented as an honest-skip (D-07)"
    - "Every Wave 10 politician_answers row has a paired politician_context row with sources array containing >= 1 non-empty URL (VAST-05 / D-13)"
    - "Migration 340 applies cleanly via psql with the DO $$ block reporting unsourced_count = 0 for external_id BETWEEN -5120016 AND -5120011"
    - "PHASE GATE: across ALL VA delegates (external_id BETWEEN -5120100 AND -5120001) total stances > 0 AND unsourced stances = 0 — VAST-03 and VAST-05 closed"
  artifacts:
    - path: "backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv"
      provides: "Source-of-truth CSV with Wave 10 delegate stances (VAST-03)"
      contains: "header row + >= 1 data row per delegate with documentable stances"
    - path: "supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql"
      provides: "Final atomic BEGIN/COMMIT migration upserting Wave 10 stances + paired context rows"
      contains: "INSERT INTO inform.politician_answers, INSERT INTO inform.politician_context, DO $$ verification block"
  key_links:
    - from: "backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv"
      to: "supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql"
      via: "CSV reasoning + source URL columns map 1:1 to politician_context rows (VAST-05)"
      pattern: "every CSV row -> matching politician_answers + politician_context insert"
    - from: "supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql"
      to: "inform.politician_answers + inform.politician_context (full -5120100..-5120001 range)"
      via: "phase gate SQL after migration 340 applied"
      pattern: "SELECT COUNT(*) unsourced across all VA delegates returns 0"
---

<objective>
Research and ingest sourced compass stances for the final 6 VA House delegates in Wave 10 (HD-11 through HD-16, NoVA Fairfax/Prince William): Gretchen M. Bulova, Holly M. Seibold, Marcus B. Simon, Vivian E. Watts, Laura Jane Cohen, and Paul E. Krizek. Then run the PHASE GATE verifying VAST-03 and VAST-05 are closed across all 100 VA delegate records.

Purpose: Closes VAST-03 (sourced delegate stances, honest-skip applied) and VAST-05 (every stance paired with politician_context + real source URL) for the entire VA House. Simon (HD-13) and Watts (HD-14) are long-serving, well-documented delegates. Expected yield 12-22 stances per delegate. Wave 10 is a CONTIGUOUS external_id range (BETWEEN -5120016 AND -5120011). This is the FINAL plan and carries the phase gate.

Output: One pre-flight JSON, one CSV, one numbered migration (340) applied, the inline DO $$ block reporting 0 unsourced rows, and the 2-query phase gate confirming total stances > 0 and unsourced = 0 across external_id -5120100..-5120001.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-va-delegate-stances/112-RESEARCH.md
@.planning/phases/112-va-delegate-stances/112-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 10 pre-flight (migration number + topics snapshot + UUID confirmation)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Patterns 2/3; Wave 10 composition + phase gate)
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Pre-flight inline node scripts — Query A/B/C; Phase Gate SQL)
    - .claude/skills/research-stances/SKILL.md (Topic Resolution section)
    - backend/.env (DATABASE_URL — session pooler)
    - backend/src/lib/db.ts (pool export)
  </read_first>
  <action>
    Run four pre-flight checks; save to backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json with keys {max_migration, highest_file_on_disk, next_free_migration, topics, delegates, existing_rows}.

    Query A — migration number: SELECT MAX(version) FROM supabase_migrations.schema_migrations (expected 325). ALSO `ls supabase/migrations/ | sort | tail -1` — expected highest file 20260610000009_339_va_delegates_wave9_stances.sql. next_free_migration = 340. If highest disk file is not 339, halt and notify orchestrator.

    Query B — live topics snapshot (SKILL.md query). Record actual count; do not hardcode 44.

    Query C — UUID fetch (CONTIGUOUS): SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -5120016 AND -5120011 ORDER BY external_id DESC. Must return exactly 6 rows. Cross-check against Wave 10 roster: HD-11 Gretchen M. Bulova 1a1d7fb4-8bf0-4aaf-8a1d-7a4ccc00e5f0, HD-12 Holly M. Seibold 4a5090f7-8d76-40c1-b2f4-c2ed038e6687, HD-13 Marcus B. Simon c490eece-71f4-4051-975d-8fa5ed5f652b, HD-14 Vivian E. Watts b6e0f927-9ce6-41ba-b1b7-43c8c4ae2264, HD-15 Laura Jane Cohen a1a470c9-1b16-4e98-999c-4106422cc52c, HD-16 Paul E. Krizek cd70f416-c844-41db-9ff4-c528e04a72be. Any mismatch is a halt condition.

    Query D — 0-existing-rows: SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120016 AND -5120011. Must return 0. Record as existing_rows.

    Use the standard `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."` Bash pattern. No PostgREST (D-14).
  </action>
  <verify>
    <automated>node -e "const j=require('./backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json'); if(j.next_free_migration!==340){console.error('Expected 340, got '+j.next_free_migration); process.exit(1)}; if(!Array.isArray(j.topics)||j.topics.length<25){console.error('Topics invalid'); process.exit(1)}; if(!Array.isArray(j.delegates)||j.delegates.length!==6){console.error('Expected 6, got '+(j.delegates||[]).length); process.exit(1)}; const ids=j.delegates.map(d=>d.id).sort(); const expected=['1a1d7fb4-8bf0-4aaf-8a1d-7a4ccc00e5f0','4a5090f7-8d76-40c1-b2f4-c2ed038e6687','c490eece-71f4-4051-975d-8fa5ed5f652b','b6e0f927-9ce6-41ba-b1b7-43c8c4ae2264','a1a470c9-1b16-4e98-999c-4106422cc52c','cd70f416-c844-41db-9ff4-c528e04a72be'].sort(); if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('UUID mismatch'); process.exit(1)}; if(j.existing_rows!==0){console.error('Expected 0 existing rows, got '+j.existing_rows); process.exit(1)}; console.log('preflight OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json exists, valid JSON
    - next_free_migration equals 340; highest_file_on_disk equals 339
    - topics array >= 25 objects, each with stances sub-array length 5
    - delegates is exactly 6 objects whose ids match the Wave 10 UUIDs
    - existing_rows equals 0
    - Returned full_name values include Bulova, Seibold, Simon, Watts, Cohen, Krizek
  </acceptance_criteria>
  <done>
    Pre-flight JSON written; migration 340 confirmed; 6 Wave 10 UUIDs verified; 0 existing rows; topics captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential research dispatch for 6 Wave 10 delegates (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 1 — dispatch rules, prompt template, FIVE-CHAIRS, --output-file)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Source Strategy — NoVA section + Agent URL Fetch Pattern; Simon/Watts long records)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_research_one_at_a_time.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_no_assumption.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_scale_embed_texts.md
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json (UUID list + topic JSON)
  </read_first>
  <action>
    Dispatch politician-stance-researcher EXACTLY ONCE PER DELEGATE, SEQUENTIALLY (D-11). Never parallel. Wait for each CSV append before next.

    Dispatch order (HD-11 through HD-16):
    1. Gretchen M. Bulova (VA HD-11, Democrat) — 1a1d7fb4-8bf0-4aaf-8a1d-7a4ccc00e5f0
    2. Holly M. Seibold (VA HD-12, Democrat) — 4a5090f7-8d76-40c1-b2f4-c2ed038e6687
    3. Marcus B. Simon (VA HD-13, Democrat, long-serving, well-documented) — c490eece-71f4-4051-975d-8fa5ed5f652b
    4. Vivian E. Watts (VA HD-14, Democrat, long-serving, well-documented) — b6e0f927-9ce6-41ba-b1b7-43c8c4ae2264
    5. Laura Jane Cohen (VA HD-15, Democrat) — a1a470c9-1b16-4e98-999c-4106422cc52c
    6. Paul E. Krizek (VA HD-16, Democrat) — cd70f416-c844-41db-9ff4-c528e04a72be

    Per-agent prompt requirements (SKILL.md STEP 1 template): full_name + "VA House of Delegates" + HD-NN; embed FULL topic JSON from preflight (D-15); attempt all live topics (D-06, city-level via honest-skip); verbatim honest-skip + no-party-inference instruction (D-07/08/09); --output-file C:/EV-Accounts/backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv (all 6 append to same CSV); source fetch order NoVA (Tier 1 reliable): ballotpedia.org/[First_Last] -> house.virginia.gov/member/[district] -> lis.virginia.gov (H[district]) -> vpap.org/officials/[slug] -> Wikipedia (Simon, Watts) -> Fairfax Times, InsideNOVA.com, Virginia Mercury, Washington Post VA desk, votesmart.org VIF responses; WebFetch ONLY (D-12); CSV columns full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3 (+ quotes if produced).

    Between dispatches: verify CSV gained rows; flag < 2 min completions (Pitfall 1) and retry. No value=3 padding (Pitfall 5). 0 rows for any topic with no evidence is acceptable (D-10).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {parse}=require('csv-parse/sync'); if(!fs.existsSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv')){console.error('CSV missing'); process.exit(1)}; const rows=parse(fs.readFileSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv','utf8'),{columns:true,skip_empty_lines:true,relax_column_count:true}); const allowed=new Set(['Gretchen M. Bulova','Holly M. Seibold','Marcus B. Simon','Vivian E. Watts','Laura Jane Cohen','Paul E. Krizek']); const bad=rows.filter(r=>!allowed.has(r.full_name)); if(bad.length){console.error('Unexpected full_name: '+[...new Set(bad.map(b=>b.full_name))].join('|')); process.exit(1)}; const uns=rows.filter(r=>!r.source_url_1||r.source_url_1.trim()===''); if(uns.length){console.error(uns.length+' rows missing source_url_1'); process.exit(1)}; console.log('wave10 CSV OK: '+rows.length+' rows across '+new Set(rows.map(r=>r.full_name)).size+' delegates')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv exists (header present even if 0 data rows)
    - Every full_name is one of the 6 Wave 10 delegate names
    - Every CSV data row has non-empty source_url_1 (VAST-05)
    - No topic_key outside the preflight topics array
    - No two agents dispatched in parallel
    - Any 0-row delegate noted as documented honest-skip
  </acceptance_criteria>
  <done>
    Wave 10 CSV holds research for the final NoVA Fairfax/PW delegates; source URLs present; honest-skips noted; ready for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author migration 340 SQL (Wave 10 — contiguous BETWEEN -5120016 AND -5120011)</name>
  <files>supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Migration SQL files section — header, BEGIN/COMMIT, DO $$ block, ARRAY() sources filter, single-quote escaping)
    - supabase/migrations/20260609000005_330_va_senators_wave5_stances.sql (canonical analog)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave10.csv
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave10-preflight.json
  </read_first>
  <action>
    Author supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql translating every Wave 10 CSV data row into a paired INSERT (politician_answers + politician_context). Read CSV with csv-parse/sync (Pitfall 6).

    Header (mirror 330): "Phase 112-10: VA House Delegate Stances — Wave 10 (HD-11 through HD-16, NoVA Fairfax/Prince William, FINAL WAVE)"; "Requirements covered: VAST-03, VAST-05"; source CSV path; cross-check counts (equal); "max_migration at authoring: 325"; honest-skip list; 6-delegate UUID table; "Migration number: 340"; "Timestamp: 20260610000010"; "Applied: NOT YET (write-only)".

    Single BEGIN; ... COMMIT;. For each row emit paired INSERTs exactly as in analog (ON CONFLICT DO UPDATE on both; sources via ARRAY(SELECT u FROM unnest(ARRAY[url1,url2,url3]) AS u WHERE u IS NOT NULL AND trim(u) != '')). Escape every ' as ''. Skip empty-source rows (log in header).

    DO $$ verification — Wave 10 is CONTIGUOUS, use BETWEEN -5120016 AND -5120011 for both delegate_count and unsourced_count queries. Use pc.politician_id IS NULL (NOT pc.id IS NULL — Pitfall 2). ASSERT unsourced_count = 0.

    Do NOT apply yet (Task 4). If 0 CSV rows, still create the file with header skip note, empty body, and DO $$ block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql','utf8'); const ans=(sql.match(/INSERT INTO inform\.politician_answers/g)||[]).length; const ctx=(sql.match(/INSERT INTO inform\.politician_context/g)||[]).length; if(ans!==ctx){console.error('Pair mismatch: '+ans+' vs '+ctx); process.exit(1)}; if(!/BEGIN;[\s\S]+COMMIT;/.test(sql)){console.error('Missing BEGIN/COMMIT'); process.exit(1)}; if(!/ASSERT unsourced_count = 0/.test(sql)){console.error('Missing ASSERT'); process.exit(1)}; if(!/BETWEEN -5120016 AND -5120011/.test(sql)){console.error('DO \$\$ must scope to BETWEEN -5120016 AND -5120011'); process.exit(1)}; if(/pc\.id IS NULL/.test(sql)){console.error('Use pc.politician_id IS NULL'); process.exit(1)}; console.log('Migration 340 SQL OK: '+ans+' paired blocks')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql exists
    - Header lists all 6 Wave 10 UUIDs + honest-skips
    - answers INSERT count equals context INSERT count, both equal CSV rows with non-empty source_url_1
    - Exactly one BEGIN; and one COMMIT;
    - DO $$ block with ASSERT unsourced_count = 0 and external_id BETWEEN -5120016 AND -5120011
    - LEFT JOIN uses pc.politician_id IS NULL
    - No unescaped ' in any reasoning literal
  </acceptance_criteria>
  <done>
    Migration 340 SQL drafted; paired-insert invariant holds; verification block scoped to Wave 10 contiguous range with BETWEEN; ready for psql apply.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration 340 via psql, confirm DO $$ verification, then run PHASE GATE across all VA delegates</name>
  <files>supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql</files>
  <read_first>
    - supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql
    - backend/.env (DATABASE_URL — session pooler aws-0-*.pooler.supabase.com:5432)
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Phase Gate SQL (Wave 10 only) section)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Pattern 5 — Phase Gate SQL; Environment Availability)
  </read_first>
  <action>
    Apply Wave 10 migration via psql session pooler, then run the PHASE GATE.

    Step 1: set -a && source backend/.env && set +a. Confirm $DATABASE_URL points at aws-0-*.pooler.supabase.com:5432.
    Step 2: psql "$DATABASE_URL" -f supabase/migrations/20260610000010_340_va_delegates_wave10_stances.sql
    Step 3: Capture output. DO $$ emits "VA delegates with stances (Wave 10): <N>" (0..6) and "Unsourced VA delegate stances (Wave 10): 0". ASSERT rolls back if unsourced != 0.
    Step 4: Wave 10 follow-up: psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS delegates, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120016 AND -5120011;"

    Step 5 — PHASE GATE (run after migration 340 applied, scoped to the FULL VA delegate range -5120100..-5120001):
    Query 1 (total coverage): psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS delegates_with_stances, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120100 AND -5120001;" — total_stances MUST be > 0 (honest-skip means delegates_with_stances will be < 99; that is acceptable).
    Query 2 (VAST-05 invariant): psql "$DATABASE_URL" -c "SELECT COUNT(*) AS unsourced FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5120100 AND -5120001 AND (pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);" — MUST return 0.

    Record both phase-gate results in the SUMMARY. If Query 1 total_stances is 0 or Query 2 unsourced > 0, the phase is NOT complete — report the failure and halt.

    Do NOT use supabase db push or the JS client. If duplicate-version conflict, rename file to (highest disk + 1) and notify orchestrator; never edit schema_migrations. On success update header "Applied: NOT YET" -> "Applied: 2026-06-10".
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -c "SELECT (SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120100 AND -5120001) AS total, (SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5120100 AND -5120001 AND (pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0)) AS unsourced;" | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const m=s.replace(/[|\s]+/g,' ').trim().split(' ').filter(Boolean); const total=parseInt(m[0],10),uns=parseInt(m[1],10); if(!(total>0)){console.error('PHASE GATE FAIL: total stances = '+total+' (must be > 0)');process.exit(1)} if(uns!==0){console.error('PHASE GATE FAIL: unsourced = '+uns+' (must be 0)');process.exit(1)} console.log('PHASE GATE PASS: '+total+' total VA delegate stances, 0 unsourced (VAST-03 + VAST-05 closed)')})"</automated>
  </verify>
  <acceptance_criteria>
    - psql apply of migration 340 exits 0 (no rollback)
    - DO $$ prints "Unsourced VA delegate stances (Wave 10): 0"
    - Wave 10 follow-up: COUNT(DISTINCT pa.politician_id) for BETWEEN -5120016 AND -5120011 is 0..6
    - PHASE GATE Query 1: total_stances across external_id BETWEEN -5120100 AND -5120001 is > 0
    - PHASE GATE Query 2: unsourced count across the full VA delegate range returns 0 (VAST-05 invariant)
    - Migration file header updated to "Applied: 2026-06-10"
  </acceptance_criteria>
  <done>
    Migration 340 applied; Wave 10 NoVA Fairfax/PW stances live; PHASE GATE passed (total stances > 0, 0 unsourced across all VA delegates); VAST-03 and VAST-05 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | Untrusted scraped content as plain text; ingested via psql, never executed |
| CSV → SQL migration | Single-quote-escaped literals in a static SQL file |
| psql client → Supabase DB | TLS session pooler; RLS enforces public-read/admin-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Party-inferred stance with no source | mitigate | D-09 in agent prompt; Task 2 verify rejects rows missing source_url_1; DO $$ ASSERT; phase gate Query 2 confirms 0 unsourced across all delegates |
| T-112-02 | Tampering | FK violation | mitigate | Task 1 pre-flight confirms all 6 FK targets exist |
| T-112-03 | Tampering | Duplicate / over-count rows | mitigate | DO $$ block + ON CONFLICT upsert; Task 3 verify asserts equal insert counts |
| T-112-04 | Tampering | CSV reasoning contains SQL meta-chars | mitigate | Task 3 escapes ' as '' |
| T-112-05 | Denial of Service | Parallel agents exhaust WebFetch quota | mitigate | Task 2 sequential dispatch (D-11) |
| T-112-06 | Information Disclosure | Public-record research data | accept | Public web only; no PII |

No high-severity threats. No auth, no endpoints, no PII. ASVS not applicable.
</threat_model>

<verification>
- Wave 10 CSV passes csv-parse/sync; every full_name is a Wave 10 delegate; source_url_1 on every data row
- Migration 340: paired INSERTs (equal counts), single BEGIN/COMMIT, DO $$ ASSERT unsourced_count = 0 scoped to BETWEEN -5120016 AND -5120011, pc.politician_id IS NULL
- psql apply prints "Unsourced VA delegate stances (Wave 10): 0"
- PHASE GATE Query 1: total stances across -5120100..-5120001 is > 0
- PHASE GATE Query 2: unsourced count across -5120100..-5120001 returns 0 (VAST-05 invariant)
</verification>

<success_criteria>
- Each of the 6 Wave 10 delegates has >= 1 stance row OR a documented honest-skip
- 100% of inserted answers rows have a paired context row with sources length >= 1
- Migration 340 applied via psql (exit 0, ASSERT passed)
- PHASE GATE PASS: total VA delegate stances > 0, 0 unsourced across external_id -5120100..-5120001
- VAST-03 closed (sourced delegate stances, honest-skip applied); VAST-05 closed (every stance paired with context + real source URL)
- All 10 wave CSVs preserved as forensic artifacts
</success_criteria>

<output>
Create `.planning/phases/112-va-delegate-stances/112-10-SUMMARY.md` when done, including:
- Total Wave 10 stances + per-delegate breakdown + honest-skips
- Migration number used (340 or shifted)
- PHASE GATE results: total VA delegate stances (full range) and unsourced count (must be 0)
- Confirmation that VAST-03 and VAST-05 are closed for the entire VA House of Delegates
- A phase-level rollup: total stances ingested across all 10 waves, count of delegates with >= 1 stance, count of fully honest-skipped delegates
</output>
