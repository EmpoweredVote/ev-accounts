---
phase: 112-va-delegate-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv
  - supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql
autonomous: true
requirements: [VAST-03, VAST-05]
tags: [stance-research, virginia, house-of-delegates]

must_haves:
  truths:
    - "Each of the 10 Wave 1 delegates (HD-43 through HD-52) either appears in inform.politician_answers with >= 1 row OR is documented as an honest-skip in the migration header (per D-07, 0 rows is valid)"
    - "Every politician_answers row inserted in Wave 1 has a paired politician_context row with sources array containing >= 1 non-empty URL (VAST-05 / D-13)"
    - "Migration 331 applies cleanly via psql with the DO $$ verification block reporting unsourced_count = 0 for external_id BETWEEN -5120052 AND -5120043"
  artifacts:
    - path: "backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv"
      provides: "Source-of-truth CSV with all Wave 1 delegate stances (per VAST-03)"
      contains: "header row + >= 1 data row per delegate that had documentable stances"
    - path: "supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql"
      provides: "Atomic BEGIN/COMMIT migration upserting Wave 1 stances + paired context rows"
      contains: "INSERT INTO inform.politician_answers, INSERT INTO inform.politician_context, DO $$ verification block"
  key_links:
    - from: "backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv"
      to: "supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql"
      via: "CSV reasoning + source URL columns map 1:1 to politician_context rows (VAST-05)"
      pattern: "every CSV row -> matching politician_answers + politician_context insert"
    - from: "supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "psql session pooler apply"
      pattern: "psql \"\\$DATABASE_URL\" -f supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql"
---

<objective>
Research and ingest sourced compass stances for the 10 VA House delegates in Wave 1 (HD-43 through HD-52, Southwest VA, Republican-heavy rural): James W. Morefield, Israel D. O'Quinn, Terry G. Kilgore, Mitchell Cornett, Wren M. Williams, Eric J. Phillips, Madison Whittle, Thomas C. Wright Jr., Eric Zehr, and Wendell S. Walker.

Purpose: Closes the first 10/99 of VAST-03 (sourced stances for VA House delegates, honest-skip where no documentable evidence) and contributes to VAST-05 (every new stance paired with politician_context containing >= 1 real source URL). Establishes the delegate pre-flight + sequential-agent + migration pattern that Waves 2-10 replicate. Southwest VA delegates have thin web presence — expected yield 0-6 stances per delegate; honest-skip is expected to dominate (per D-10).

Output: One pre-flight JSON, one CSV file with Wave 1 stance research, one numbered migration (331) applied to the live DB, verified by the inline DO $$ block reporting 0 unsourced rows for the Wave 1 external_id range.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-va-delegate-stances/112-RESEARCH.md
@.planning/phases/112-va-delegate-stances/112-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 1 pre-flight (migration number + topics snapshot + UUID confirmation + 0-existing-rows check)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Patterns 2 and 3 — pre-flight queries; DB State table — confirms MAX(version)=325, highest file on disk=330, next free=331)
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Pre-flight inline node scripts section — exact Query A/B/C incantations and pre-flight JSON output structure)
    - .claude/skills/research-stances/SKILL.md (Topic Resolution section — exact pre-flight node --import tsx query for live topics + stance texts)
    - backend/.env (must contain DATABASE_URL pointing at Supabase session pooler aws-0-*.pooler.supabase.com:5432)
    - backend/src/lib/db.ts (confirm pool export signature used by inline node scripts)
  </read_first>
  <action>
    Run four pre-flight checks against the live Supabase DB and save consolidated output to backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json with top-level keys {max_migration, highest_file_on_disk, next_free_migration, topics, delegates}.

    Query A — migration number: SELECT MAX(version) FROM supabase_migrations.schema_migrations — expected 325 (psql-applied waves 326-330 are NOT tracked in schema_migrations). ALSO run `ls supabase/migrations/ | sort | tail -1` — expected highest file is 20260609000005_330_va_senators_wave5_stances.sql. next_free_migration = 331. If the highest file on disk is NOT 330, halt and notify the orchestrator so all 10 wave migration numbers shift.

    Query B — live topics snapshot: run the SKILL.md Topic Resolution query (SELECT t.id, t.topic_key, t.title, t.question_text, json_agg(json_build_object('value', s.value, 'text', s.text) ORDER BY s.value) AS stances FROM inform.compass_topics t JOIN inform.compass_stances s ON s.topic_id = t.id WHERE t.is_live = true GROUP BY ... ORDER BY t.topic_key). Result is a JSON array of live topics (expected ~44 per RESEARCH.md — do NOT hardcode 44; record actual count and proceed with whatever is returned).

    Query C — UUID fetch (contiguous range): SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -5120052 AND -5120043 ORDER BY external_id DESC. Must return exactly 10 rows. Cross-check each returned (full_name, UUID) pair against this Wave 1 roster: HD-43 James W. Morefield df51bc00-8a69-4bd0-9418-e61a3cfe248b, HD-44 Israel D. O'Quinn 36673ec0-1045-4a98-8074-12d6deed5cc8, HD-45 Terry G. Kilgore 84257075-047c-46d3-ab7b-ed0e0ae0dd56, HD-46 Mitchell Cornett 3b10a611-77d2-48e2-bf45-40b7bdacdd51, HD-47 Wren M. Williams 38cb6796-1539-48ac-92e6-00068aa5e339, HD-48 Eric J. Phillips 7da511ee-1e98-4620-8852-a0f32dd45078, HD-49 Madison Whittle 4b4e3a27-dbf6-4984-a8a8-eeda9f972612, HD-50 Thomas C. Wright, Jr. ff50eaf3-0f12-455e-85b2-bc37f1e542db, HD-51 Eric Zehr e5da439f-17bd-4337-ade5-eb7e5c9d48d4, HD-52 Wendell S. Walker f830ca33-179a-4981-8b89-0b02155e374b. Any mismatch is a halt condition.

    Query D — 0-existing-rows check: SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120052 AND -5120043. Must return 0 (clean greenfield per RESEARCH.md DB State). Record this count in the JSON as existing_rows.

    Use the `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."` Bash pattern shown in 112-PATTERNS.md. Do NOT use PostgREST — inform schema is not in the PostgREST exposed list (D-14).
  </action>
  <verify>
    <automated>node -e "const j=require('./backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json'); if(j.next_free_migration!==331){console.error('Expected next_free_migration=331, got '+j.next_free_migration); process.exit(1)}; if(!Array.isArray(j.topics)||j.topics.length<25){console.error('Topics array invalid/too small'); process.exit(1)}; if(!Array.isArray(j.delegates)||j.delegates.length!==10){console.error('Expected 10 Wave 1 delegates, got '+(j.delegates||[]).length); process.exit(1)}; const ids=j.delegates.map(d=>d.id).sort(); const expected=['df51bc00-8a69-4bd0-9418-e61a3cfe248b','36673ec0-1045-4a98-8074-12d6deed5cc8','84257075-047c-46d3-ab7b-ed0e0ae0dd56','3b10a611-77d2-48e2-bf45-40b7bdacdd51','38cb6796-1539-48ac-92e6-00068aa5e339','7da511ee-1e98-4620-8852-a0f32dd45078','4b4e3a27-dbf6-4984-a8a8-eeda9f972612','ff50eaf3-0f12-455e-85b2-bc37f1e542db','e5da439f-17bd-4337-ade5-eb7e5c9d48d4','f830ca33-179a-4981-8b89-0b02155e374b'].sort(); if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('UUID mismatch'); process.exit(1)}; if(j.existing_rows!==0){console.error('Expected 0 existing rows, got '+j.existing_rows); process.exit(1)}; console.log('preflight OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json exists and is valid JSON
    - Key next_free_migration equals 331; highest_file_on_disk equals 330 (halt + notify orchestrator if different)
    - Key topics is an array of >= 25 topic objects, each with id, topic_key, question_text, and a stances sub-array of length 5
    - Key delegates is an array of exactly 10 objects whose id values match the 10 Wave 1 UUIDs in the action
    - Key existing_rows equals 0 (clean greenfield for this external_id range)
    - The 10 returned full_name values include "James W. Morefield", "Israel D. O'Quinn", "Terry G. Kilgore", "Mitchell Cornett", "Wren M. Williams", "Eric J. Phillips", "Madison Whittle", "Thomas C. Wright, Jr.", "Eric Zehr", and "Wendell S. Walker" (substring match acceptable for initials/suffixes)
  </acceptance_criteria>
  <done>
    Pre-flight JSON written, migration number confirmed as 331, Wave 1 delegate UUIDs verified, 0 existing rows confirmed, live topic list captured for embedding in research agent prompts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential research dispatch for 10 Wave 1 delegates (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 1 — dispatch rules, agent prompt template, FIVE-CHAIRS framing, city-level topic skip list, --output-file convention)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Source Strategy for VA House Delegates — Southwest VA section + Agent URL Fetch Pattern; Notes on O'Quinn HD-44 and Kilgore HD-45)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_research_one_at_a_time.md (mass-launch hazard reminder)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_no_assumption.md (party-inference is forbidden)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_scale_embed_texts.md (stance-text-embed requirement)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json (UUID list + topic JSON to embed in prompts)
  </read_first>
  <action>
    Dispatch the politician-stance-researcher subagent EXACTLY ONCE PER DELEGATE, SEQUENTIALLY (subagent_type: "politician-stance-researcher"). Never dispatch two agents in parallel — confirmed by SKILL.md STEP 1 and user memory feedback_stance_research_one_at_a_time.md (D-11). Wait for the prior CSV append to complete before triggering the next dispatch.

    Dispatch order (HD-43 through HD-52):
    1. James W. Morefield (VA HD-43, Republican) — df51bc00-8a69-4bd0-9418-e61a3cfe248b
    2. Israel D. O'Quinn (VA HD-44, Republican) — 36673ec0-1045-4a98-8074-12d6deed5cc8
    3. Terry G. Kilgore (VA HD-45, Republican, senior delegate / former Speaker pro tempore — expect more Ballotpedia depth) — 84257075-047c-46d3-ab7b-ed0e0ae0dd56
    4. Mitchell Cornett (VA HD-46, Republican) — 3b10a611-77d2-48e2-bf45-40b7bdacdd51
    5. Wren M. Williams (VA HD-47, Republican) — 38cb6796-1539-48ac-92e6-00068aa5e339
    6. Eric J. Phillips (VA HD-48, Republican) — 7da511ee-1e98-4620-8852-a0f32dd45078
    7. Madison Whittle (VA HD-49, Republican) — 4b4e3a27-dbf6-4984-a8a8-eeda9f972612
    8. Thomas C. Wright, Jr. (VA HD-50, Republican) — ff50eaf3-0f12-455e-85b2-bc37f1e542db
    9. Eric Zehr (VA HD-51, Republican) — e5da439f-17bd-4337-ade5-eb7e5c9d48d4
    10. Wendell S. Walker (VA HD-52, Republican) — f830ca33-179a-4981-8b89-0b02155e374b

    Per-agent prompt requirements (build by following SKILL.md STEP 1 template):
    - Include the delegate's full_name AND "VA House of Delegates" office title AND district number (HD-NN)
    - Embed the FULL topic JSON from preflight (id, topic_key, question_text, stances array with text per value) — never hardcode topic lists (D-15). Attempt all live topics (D-06); city-level topics are skipped by the agent via honest-skip (no pre-filtering needed)
    - Include explicit honest-skip + no-party-inference instruction verbatim: "Skip any topic where you cannot find sufficient evidence. Do NOT infer from party affiliation. Zero rows for a delegate is a valid and acceptable outcome." (D-07, D-08, D-09)
    - --output-file must be the absolute path C:/EV-Accounts/backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv (all 10 delegates append to the SAME wave CSV)
    - Source URL fetch order (Southwest VA — limited coverage, per RESEARCH.md): ballotpedia.org/[First_Last] -> lis.virginia.gov floor votes (legp604.exe mbr query for H[district]) -> house.virginia.gov/member/[district] -> vpap.org/officials/[slug] -> Wikipedia -> regional press (Southwest Times, Bristol Herald Courier, Roanoke Times). Try web.archive.org if a current campaign site is down.
    - Tool rule: WebFetch ONLY — no WebSearch, no Playwright (D-12, shared rate-limited quota pool)
    - CSV column order: full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3 (plus quote_text,quote_deidentified if agent produces them)

    For Thomas C. Wright, Jr.: the full_name passed to the agent and recorded in the CSV must be exactly "Thomas C. Wright, Jr." (matching essentials.politicians) — the comma must be preserved via RFC-4180 quoting since the migration's csv-parse step depends on it.

    Between dispatches: verify the CSV gained new rows for the prior delegate (Read or csv-parse check), confirm the agent did not finish suspiciously fast (< 2 min suggests rate-limit per Pitfall 1) — if either signal fails, retry that delegate before moving on. Southwest delegates yielding 0 rows is acceptable and expected (D-10); do NOT fabricate value=3 neutral rows to inflate counts (Pitfall 5).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {parse}=require('csv-parse/sync'); if(!fs.existsSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv')){console.error('CSV missing'); process.exit(1)}; const csv=fs.readFileSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv','utf8'); const rows=parse(csv,{columns:true,skip_empty_lines:true,relax_column_count:true}); const allowed=new Set(['James W. Morefield','Israel D. O'Quinn','Terry G. Kilgore','Mitchell Cornett','Wren M. Williams','Eric J. Phillips','Madison Whittle','Thomas C. Wright, Jr.','Eric Zehr','Wendell S. Walker']); const bad=rows.filter(r=>!allowed.has(r.full_name)); if(bad.length){console.error('Unexpected full_name values: '+[...new Set(bad.map(b=>b.full_name))].join('|')); process.exit(1)}; const unsourced=rows.filter(r=>!r.source_url_1||r.source_url_1.trim()===''); if(unsourced.length){console.error(unsourced.length+' rows missing source_url_1 (VAST-05 violation)'); process.exit(1)}; console.log('wave1 CSV OK: '+rows.length+' rows across '+new Set(rows.map(r=>r.full_name)).size+' delegates (0 rows acceptable for honest-skip)')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv exists (header present even if 0 data rows — honest-skip is valid per D-07)
    - Every full_name value in the CSV is one of the 10 Wave 1 delegate names (no foreign/hallucinated names)
    - Every CSV data row has source_url_1 populated with a non-empty URL string (VAST-05)
    - No topic_key value appears that is absent from the preflight topics array
    - No two research agents were dispatched in parallel (confirmable by ordering of tool_use timestamps)
    - Any delegate with 0 rows is noted in the executor response as a documented honest-skip
  </acceptance_criteria>
  <done>
    Wave 1 CSV contains research output for the Southwest VA delegates that had documentable evidence; per-row source URLs present; honest-skips noted; CSV ready for migration translation in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author migration 331 SQL (Wave 1 stances + paired context, inline DO $$ verification — BETWEEN)</name>
  <files>supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Migration SQL files section — header comment block, BEGIN/COMMIT wrapper, DO $$ verification block, ARRAY() sources filter, single-quote escaping rule)
    - supabase/migrations/20260609000005_330_va_senators_wave5_stances.sql (canonical analog — mirror header style, per-row INSERT pattern, ARRAY(SELECT u FROM unnest(...) WHERE ...) filter)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv (source CSV — every data row maps to one politician_answers INSERT + one politician_context INSERT)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave1-preflight.json (UUID truth source)
  </read_first>
  <action>
    Author supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql by translating every data row of the Wave 1 CSV into a paired INSERT (politician_answers + politician_context). Read the CSV with csv-parse/sync — never split on commas (Pitfall 6; "Thomas C. Wright, Jr." contains a comma).

    Header comment block (mirror 330 analog style): title line "Phase 112-01: VA House Delegate Stances — Wave 1 (HD-43 through HD-52, Southwest VA)"; "Requirements covered: VAST-03, VAST-05"; source CSV path; pre-write cross-check counts (CSV data rows, politician_answers INSERT count, politician_context INSERT count — all three must be equal); "max_migration at authoring: 325 (psql-applied waves 326-330 not tracked in schema_migrations)"; honest-skip list (delegates with 0 rows + reason "thin Southwest VA web presence — no documentable evidence found", or "None"); the 10-delegate UUID table (ext_id -> UUID); "Migration number: 331"; "Timestamp: 20260610000001"; "Applied: NOT YET (write-only)".

    Wrap all INSERTs in a single BEGIN; ... COMMIT; block. For every CSV data row emit:
    1. INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES ('<uuid>', (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), <value>) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;
    2. INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES ('<uuid>', (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), '<reasoning, single-quote-escaped>', ARRAY(SELECT u FROM unnest(ARRAY['<source_url_1>','<source_url_2>','<source_url_3>']) AS u WHERE u IS NOT NULL AND trim(u) != '')) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;

    Single-quote escaping: replace every ' in reasoning with '' (never \'). Skip CSV rows where source_url_1 is empty (would violate VAST-05) and log them in the header.

    Append the DO $$ verification block. Wave 1 is a CONTIGUOUS range — use BETWEEN -5120052 AND -5120043. Declare delegate_count and unsourced_count; RAISE NOTICE both; ASSERT unsourced_count = 0. CRITICAL: use pc.politician_id IS NULL in the LEFT JOIN check, NOT pc.id IS NULL — inform.politician_context has a composite PK (politician_id, topic_id) with no standalone id column (Pitfall 2).

    Do NOT apply the migration yet — that is Task 4. If the CSV had 0 rows (full honest-skip wave), still create the migration file with header documenting the skip, an empty BEGIN/COMMIT body, and the DO $$ block (which will report delegate_count=0, unsourced_count=0, ASSERT passes).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql','utf8'); const ans=(sql.match(/INSERT INTO inform\.politician_answers/g)||[]).length; const ctx=(sql.match(/INSERT INTO inform\.politician_context/g)||[]).length; if(ans!==ctx){console.error('Pair mismatch: '+ans+' answers vs '+ctx+' context'); process.exit(1)}; if(!/BEGIN;[\s\S]+COMMIT;/.test(sql)){console.error('Missing BEGIN/COMMIT wrapper'); process.exit(1)}; if(!/ASSERT unsourced_count = 0/.test(sql)){console.error('Missing ASSERT unsourced_count = 0'); process.exit(1)}; if(!/BETWEEN -5120052 AND -5120043/.test(sql)){console.error('Verification block must scope to Wave 1 range BETWEEN -5120052 AND -5120043'); process.exit(1)}; if(/pc\.id IS NULL/.test(sql)){console.error('Must use pc.politician_id IS NULL not pc.id IS NULL'); process.exit(1)}; console.log('Migration 331 SQL OK: '+ans+' paired insert blocks')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql exists
    - Header lists all 10 Wave 1 delegate UUIDs and any honest-skipped delegates with reason
    - Count of "INSERT INTO inform.politician_answers" equals count of "INSERT INTO inform.politician_context"
    - Both counts equal the number of CSV data rows with non-empty source_url_1
    - Exactly one BEGIN; and one COMMIT; wrapping all INSERTs
    - DO $$ block present with ASSERT unsourced_count = 0 and external_id BETWEEN -5120052 AND -5120043
    - LEFT JOIN check uses pc.politician_id IS NULL (NOT pc.id IS NULL)
    - No unescaped ' inside any string literal (every apostrophe in reasoning written as '')
  </acceptance_criteria>
  <done>
    Migration 331 SQL drafted, paired-insert invariant holds, verification block scoped to Wave 1 contiguous range with BETWEEN, ready for psql apply in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration 331 via psql session pooler and confirm DO $$ verification passes</name>
  <files>supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql</files>
  <read_first>
    - supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql (the migration to apply)
    - backend/.env (DATABASE_URL — must be the Supabase session pooler URL aws-0-*.pooler.supabase.com:5432, NOT the IPv6 direct host)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Environment Availability table + psql apply pattern)
  </read_first>
  <action>
    Apply the Wave 1 migration to the live Supabase DB using psql via the session pooler.

    Step 1: Source backend/.env. Confirm $DATABASE_URL is set and points at aws-0-*.pooler.supabase.com:5432 (NOT IPv6 direct host, which fails on Windows). Use Bash: set -a && source backend/.env && set +a.

    Step 2: Run exactly: psql "$DATABASE_URL" -f supabase/migrations/20260610000001_331_va_delegates_wave1_stances.sql

    Step 3: Capture stdout/stderr. The DO $$ block emits two NOTICE lines: "VA delegates with stances (Wave 1): <N>" (0..10 — honest-skip means N may be well under 10) and "Unsourced VA delegate stances (Wave 1): 0". The ASSERT raises an exception and rolls back if unsourced_count != 0.

    Step 4: On success, run a follow-up confirmation query: psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS delegates, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120052 AND -5120043;"

    Do NOT use `supabase db push` — this project uses direct psql for stance migrations (psql-applied wave migrations do not register in schema_migrations, matching Phase 111 waves 326-330). Do NOT apply via the Supabase JS client — inform schema is not exposed via PostgREST.

    If apply fails due to a duplicate version/file conflict, do NOT manually edit supabase_migrations.schema_migrations. Rename the file to (current highest disk number + 1), update Task verification SQL accordingly, and notify the orchestrator so subsequent waves shift.

    On success, update the migration header line "Applied: NOT YET (write-only)" to "Applied: 2026-06-10".
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5120052 AND -5120043 AND (pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);" | tr -d ' \n' | grep -qx '0' && echo "VAST-05 OK: 0 unsourced Wave 1 delegate stances" || (echo "VAST-05 VIOLATION: unsourced Wave 1 stances found"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - psql apply completes with exit code 0 (no rollback)
    - DO $$ block prints "Unsourced VA delegate stances (Wave 1): 0" NOTICE
    - Follow-up SQL: COUNT(DISTINCT pa.politician_id) for external_id BETWEEN -5120052 AND -5120043 is between 0 and 10 (honest-skip allowed)
    - Follow-up SQL: unsourced count (context missing OR sources NULL OR sources array empty) for the same range returns 0
    - Migration file header updated from "Applied: NOT YET" to "Applied: 2026-06-10"
  </acceptance_criteria>
  <done>
    Migration 331 applied; Wave 1 Southwest VA delegate stances live in inform.politician_answers + inform.politician_context; VAST-05 invariant holds for the Wave 1 range; next free migration number is 332.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | Untrusted scraped content lands as plain text in CSV reasoning + URL columns; only ingested via psql, never executed |
| CSV → SQL migration | Single-quote-escaped string literals in a static SQL file (no dynamic concatenation at runtime) |
| psql client → Supabase DB | TLS session pooler; admin-write via DATABASE_URL; RLS on inform tables enforces public-read/admin-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Party-inferred stance value with no real source | mitigate | D-09 rule embedded in every agent prompt; Task 2 verify rejects rows missing source_url_1; DO $$ ASSERT unsourced_count = 0 |
| T-112-02 | Tampering | FK violation (politician_id not present) | mitigate | Task 1 pre-flight confirms all 10 FK targets exist before migration authoring |
| T-112-03 | Tampering | Duplicate / over-count rows | mitigate | DO $$ block + ON CONFLICT upsert; Task 3 verify asserts answers/context insert counts are equal |
| T-112-04 | Tampering | CSV reasoning contains SQL meta-chars | mitigate | Task 3 escapes every ' as '' before SQL emit; psql parses each INSERT as a literal string |
| T-112-05 | Denial of Service | Parallel research agents exhaust WebFetch quota | mitigate | Task 2 mandates sequential dispatch (D-11, user memory) |
| T-112-06 | Information Disclosure | Source URLs + reasoning are public-record research | accept | All data from public web; no PII, no internal endpoints, no credentials |

No high-severity threats. No auth changes, no new endpoints, no PII. ASVS not applicable.
</threat_model>

<verification>
- Wave 1 CSV passes csv-parse/sync read; every full_name is a Wave 1 delegate; source_url_1 populated on every data row
- Migration 331 file: paired politician_answers + politician_context INSERTs (counts equal), single BEGIN/COMMIT, DO $$ ASSERT unsourced_count = 0 scoped to BETWEEN -5120052 AND -5120043, uses pc.politician_id IS NULL
- psql apply prints "Unsourced VA delegate stances (Wave 1): 0" NOTICE
- Post-apply SQL: COUNT(DISTINCT pa.politician_id) for the range is 0..10 (honest-skip allowed)
- Post-apply SQL: unsourced count for the same range returns 0 (VAST-05)
</verification>

<success_criteria>
- Each of the 10 Wave 1 delegates has >= 1 stance row OR a documented honest-skip in the migration header
- 100% of inserted politician_answers rows have a paired politician_context row with sources array length >= 1
- Migration 331 applied via psql (exit 0, DO $$ ASSERT passed)
- CSV preserved at backend/data/stance-research/2026-06-10-112-va-delegates-wave1.csv as forensic artifact
- Plan 112-02 unblocked: next migration number is 332
</success_criteria>

<output>
Create `.planning/phases/112-va-delegate-stances/112-01-SUMMARY.md` when done, including:
- Total stances ingested for Wave 1
- Per-delegate breakdown (topic count per delegate)
- Any honest-skipped delegates with rationale
- Migration number actually used (331 or shifted)
- Confirmation that VAST-05 invariant holds (unsourced count = 0 for the Wave 1 range)
</output>
