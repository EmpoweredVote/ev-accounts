---
phase: 112-va-delegate-stances
plan: 09
type: execute
wave: 2
depends_on: ["112-08"]
files_modified:
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json
  - backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv
  - supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql
autonomous: true
requirements: [VAST-03, VAST-05]
tags: [stance-research, virginia, house-of-delegates]

must_haves:
  truths:
    - "Each of the 10 Wave 9 delegates (HD-1 through HD-10, NoVA Core) either appears in inform.politician_answers with >= 1 row OR is documented as an honest-skip (D-07)"
    - "Every Wave 9 politician_answers row has a paired politician_context row with sources array containing >= 1 non-empty URL (VAST-05 / D-13)"
    - "Migration 339 applies cleanly via psql with the DO $$ block reporting unsourced_count = 0 for external_id BETWEEN -5120010 AND -5120001"
  artifacts:
    - path: "backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv"
      provides: "Source-of-truth CSV with Wave 9 delegate stances (VAST-03)"
      contains: "header row + >= 1 data row per delegate with documentable stances"
    - path: "supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql"
      provides: "Atomic BEGIN/COMMIT migration upserting Wave 9 stances + paired context rows"
      contains: "INSERT INTO inform.politician_answers, INSERT INTO inform.politician_context, DO $$ verification block"
  key_links:
    - from: "backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv"
      to: "supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql"
      via: "CSV reasoning + source URL columns map 1:1 to politician_context rows (VAST-05)"
      pattern: "every CSV row -> matching politician_answers + politician_context insert"
    - from: "supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "psql session pooler apply"
      pattern: "psql \"\\$DATABASE_URL\" -f supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql"
---

<objective>
Research and ingest sourced compass stances for the 10 VA House delegates in Wave 9 (HD-1 through HD-10, NoVA Core — most prominent Northern Virginia delegates): Patrick A. Hope, Adele Y. McClure, Alfonso H. Lopez, Charniele L. Herring, R. Kirk McPike, Richard C. Sullivan Jr., Karen Keys-Gamarra, Irene Shin, Karrie K. Delaney, and Dan Helmer.

Purpose: Continues VAST-03 and contributes to VAST-05. These are the highest-yield delegates: Hope (long-serving Arlington), Herring (House Minority Leader emeritus), Lopez — extensive press records. Expected yield 12-22 stances per delegate. Wave 9 is a CONTIGUOUS external_id range (BETWEEN -5120010 AND -5120001).

Output: One pre-flight JSON, one CSV, one numbered migration (339) applied, verified by the inline DO $$ block reporting 0 unsourced rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-va-delegate-stances/112-RESEARCH.md
@.planning/phases/112-va-delegate-stances/112-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 9 pre-flight (migration number + topics snapshot + UUID confirmation)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Patterns 2/3; Wave 9 composition)
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Pre-flight inline node scripts — Query A/B/C)
    - .claude/skills/research-stances/SKILL.md (Topic Resolution section)
    - backend/.env (DATABASE_URL — session pooler)
    - backend/src/lib/db.ts (pool export)
  </read_first>
  <action>
    Run four pre-flight checks; save to backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json with keys {max_migration, highest_file_on_disk, next_free_migration, topics, delegates, existing_rows}.

    Query A — migration number: SELECT MAX(version) FROM supabase_migrations.schema_migrations (expected 325). ALSO `ls supabase/migrations/ | sort | tail -1` — expected highest file 20260610000008_338_va_delegates_wave8_stances.sql. next_free_migration = 339. If highest disk file is not 338, halt and notify orchestrator.

    Query B — live topics snapshot (SKILL.md query). Record actual count; do not hardcode 44.

    Query C — UUID fetch (CONTIGUOUS): SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -5120010 AND -5120001 ORDER BY external_id DESC. Must return exactly 10 rows. Cross-check against Wave 9 roster: HD-1 Patrick A. Hope af6e165b-4668-449b-97a5-6b25c01c572a, HD-2 Adele Y. McClure 8461412e-6413-4f44-ae5e-b7c8e7f736a5, HD-3 Alfonso H. Lopez 5b7f3c42-b1a2-4dbc-870b-7f1a96a7f4ba, HD-4 Charniele L. Herring 51f5dd85-abb6-4f50-bcb9-e5e6b82376b7, HD-5 R. Kirk McPike b85d17af-a823-413c-b79c-aa4e7cfab730, HD-6 Richard C. Sullivan, Jr. 1964984f-1751-4ac7-ae94-69e50b0c2968, HD-7 Karen Keys-Gamarra c0baa6ca-b02d-4bbe-8d90-5f36d31cba1e, HD-8 Irene Shin 98023fc8-d83b-43ba-b7e1-5bd132122bbe, HD-9 Karrie K. Delaney b17426e3-d363-44fd-a2b7-a04f54f6d7cb, HD-10 Dan Helmer 090cebbd-19b5-41ed-9c9a-5f537cdb5470. Any mismatch is a halt condition.

    Query D — 0-existing-rows: SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120010 AND -5120001. Must return 0. Record as existing_rows.

    Use the standard `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."` Bash pattern. No PostgREST (D-14).
  </action>
  <verify>
    <automated>node -e "const j=require('./backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json'); if(j.next_free_migration!==339){console.error('Expected 339, got '+j.next_free_migration); process.exit(1)}; if(!Array.isArray(j.topics)||j.topics.length<25){console.error('Topics invalid'); process.exit(1)}; if(!Array.isArray(j.delegates)||j.delegates.length!==10){console.error('Expected 10, got '+(j.delegates||[]).length); process.exit(1)}; const ids=j.delegates.map(d=>d.id).sort(); const expected=['af6e165b-4668-449b-97a5-6b25c01c572a','8461412e-6413-4f44-ae5e-b7c8e7f736a5','5b7f3c42-b1a2-4dbc-870b-7f1a96a7f4ba','51f5dd85-abb6-4f50-bcb9-e5e6b82376b7','b85d17af-a823-413c-b79c-aa4e7cfab730','1964984f-1751-4ac7-ae94-69e50b0c2968','c0baa6ca-b02d-4bbe-8d90-5f36d31cba1e','98023fc8-d83b-43ba-b7e1-5bd132122bbe','b17426e3-d363-44fd-a2b7-a04f54f6d7cb','090cebbd-19b5-41ed-9c9a-5f537cdb5470'].sort(); if(JSON.stringify(ids)!==JSON.stringify(expected)){console.error('UUID mismatch'); process.exit(1)}; if(j.existing_rows!==0){console.error('Expected 0 existing rows, got '+j.existing_rows); process.exit(1)}; console.log('preflight OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json exists, valid JSON
    - next_free_migration equals 339; highest_file_on_disk equals 338
    - topics array >= 25 objects, each with stances sub-array length 5
    - delegates is exactly 10 objects whose ids match the Wave 9 UUIDs
    - existing_rows equals 0
    - Returned full_name values include Hope, McClure, Lopez, Herring, McPike, Sullivan Jr., Keys-Gamarra, Shin, Delaney, Helmer
  </acceptance_criteria>
  <done>
    Pre-flight JSON written; migration 339 confirmed; 10 Wave 9 UUIDs verified; 0 existing rows; topics captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sequential research dispatch for 10 Wave 9 delegates (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 1 — dispatch rules, prompt template, FIVE-CHAIRS, --output-file)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Source Strategy — NoVA section + Agent URL Fetch Pattern; Notes on Hope HD-1, Herring HD-4, Keys-Gamarra HD-7)
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_research_one_at_a_time.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_no_assumption.md
    - C:/Users/Chris/.claude/projects/C--EV-Accounts/memory/feedback_stance_scale_embed_texts.md
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json (UUID list + topic JSON)
  </read_first>
  <action>
    Dispatch politician-stance-researcher EXACTLY ONCE PER DELEGATE, SEQUENTIALLY (D-11). Never parallel. Wait for each CSV append before next.

    Dispatch order (HD-1 through HD-10):
    1. Patrick A. Hope (VA HD-1, Democrat, long-serving Arlington — extensive press record) — af6e165b-4668-449b-97a5-6b25c01c572a
    2. Adele Y. McClure (VA HD-2, Democrat) — 8461412e-6413-4f44-ae5e-b7c8e7f736a5
    3. Alfonso H. Lopez (VA HD-3, Democrat) — 5b7f3c42-b1a2-4dbc-870b-7f1a96a7f4ba
    4. Charniele L. Herring (VA HD-4, Democrat, House Minority Leader emeritus — well-documented) — 51f5dd85-abb6-4f50-bcb9-e5e6b82376b7
    5. R. Kirk McPike (VA HD-5, Democrat) — b85d17af-a823-413c-b79c-aa4e7cfab730
    6. Richard C. Sullivan, Jr. (VA HD-6, Democrat) — 1964984f-1751-4ac7-ae94-69e50b0c2968
    7. Karen Keys-Gamarra (VA HD-7, Democrat, former Fairfax County school board at-large) — c0baa6ca-b02d-4bbe-8d90-5f36d31cba1e
    8. Irene Shin (VA HD-8, Democrat) — 98023fc8-d83b-43ba-b7e1-5bd132122bbe
    9. Karrie K. Delaney (VA HD-9, Democrat) — b17426e3-d363-44fd-a2b7-a04f54f6d7cb
    10. Dan Helmer (VA HD-10, Democrat) — 090cebbd-19b5-41ed-9c9a-5f537cdb5470

    Per-agent prompt requirements (SKILL.md STEP 1 template): full_name + "VA House of Delegates" + HD-NN; embed FULL topic JSON from preflight (D-15); attempt all live topics (D-06, city-level via honest-skip); verbatim honest-skip + no-party-inference instruction (D-07/08/09); --output-file C:/EV-Accounts/backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv (all 10 append to same CSV); source fetch order NoVA (Tier 1 reliable): ballotpedia.org/[First_Last] -> house.virginia.gov/member/[district] -> lis.virginia.gov (H[district]) -> vpap.org/officials/[slug] -> Wikipedia (Hope, Herring, Lopez) -> Fairfax Times, InsideNOVA.com, Virginia Mercury, Washington Post VA desk, votesmart.org VIF responses; WebFetch ONLY (D-12); CSV columns full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3 (+ quotes if produced).

    For Richard C. Sullivan, Jr.: preserve exact full_name with comma via RFC-4180 quoting.

    Between dispatches: verify CSV gained rows; flag < 2 min completions (Pitfall 1) and retry. No value=3 padding (Pitfall 5). NoVA delegates are expected to yield high counts but 0 rows for any specific topic with no evidence is still acceptable (D-10).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {parse}=require('csv-parse/sync'); if(!fs.existsSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv')){console.error('CSV missing'); process.exit(1)}; const rows=parse(fs.readFileSync('backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv','utf8'),{columns:true,skip_empty_lines:true,relax_column_count:true}); const allowed=new Set(['Patrick A. Hope','Adele Y. McClure','Alfonso H. Lopez','Charniele L. Herring','R. Kirk McPike','Richard C. Sullivan, Jr.','Karen Keys-Gamarra','Irene Shin','Karrie K. Delaney','Dan Helmer']); const bad=rows.filter(r=>!allowed.has(r.full_name)); if(bad.length){console.error('Unexpected full_name: '+[...new Set(bad.map(b=>b.full_name))].join('|')); process.exit(1)}; const uns=rows.filter(r=>!r.source_url_1||r.source_url_1.trim()===''); if(uns.length){console.error(uns.length+' rows missing source_url_1'); process.exit(1)}; console.log('wave9 CSV OK: '+rows.length+' rows across '+new Set(rows.map(r=>r.full_name)).size+' delegates')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv exists (header present even if 0 data rows)
    - Every full_name is one of the 10 Wave 9 delegate names
    - Every CSV data row has non-empty source_url_1 (VAST-05)
    - No topic_key outside the preflight topics array
    - No two agents dispatched in parallel
    - Any 0-row delegate noted as documented honest-skip
  </acceptance_criteria>
  <done>
    Wave 9 CSV holds research for the NoVA Core delegates; source URLs present; honest-skips noted; ready for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author migration 339 SQL (Wave 9 — contiguous BETWEEN -5120010 AND -5120001)</name>
  <files>supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql</files>
  <read_first>
    - .planning/phases/112-va-delegate-stances/112-PATTERNS.md (Migration SQL files section — header, BEGIN/COMMIT, DO $$ block, ARRAY() sources filter, single-quote escaping)
    - supabase/migrations/20260609000005_330_va_senators_wave5_stances.sql (canonical analog)
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave9.csv
    - backend/data/stance-research/2026-06-10-112-va-delegates-wave9-preflight.json
  </read_first>
  <action>
    Author supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql translating every Wave 9 CSV data row into a paired INSERT (politician_answers + politician_context). Read CSV with csv-parse/sync (Pitfall 6).

    Header (mirror 330): "Phase 112-09: VA House Delegate Stances — Wave 9 (HD-1 through HD-10, NoVA Core)"; "Requirements covered: VAST-03, VAST-05"; source CSV path; cross-check counts (equal); "max_migration at authoring: 325"; honest-skip list; 10-delegate UUID table; "Migration number: 339"; "Timestamp: 20260610000009"; "Applied: NOT YET (write-only)".

    Single BEGIN; ... COMMIT;. For each row emit paired INSERTs exactly as in analog (ON CONFLICT DO UPDATE on both; sources via ARRAY(SELECT u FROM unnest(ARRAY[url1,url2,url3]) AS u WHERE u IS NOT NULL AND trim(u) != '')). Escape every ' as ''. Skip empty-source rows (log in header).

    DO $$ verification — Wave 9 is CONTIGUOUS, use BETWEEN -5120010 AND -5120001 for both delegate_count and unsourced_count queries. Use pc.politician_id IS NULL (NOT pc.id IS NULL — Pitfall 2). ASSERT unsourced_count = 0.

    Do NOT apply yet (Task 4). If 0 CSV rows, still create the file with header skip note, empty body, and DO $$ block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql','utf8'); const ans=(sql.match(/INSERT INTO inform\.politician_answers/g)||[]).length; const ctx=(sql.match(/INSERT INTO inform\.politician_context/g)||[]).length; if(ans!==ctx){console.error('Pair mismatch: '+ans+' vs '+ctx); process.exit(1)}; if(!/BEGIN;[\s\S]+COMMIT;/.test(sql)){console.error('Missing BEGIN/COMMIT'); process.exit(1)}; if(!/ASSERT unsourced_count = 0/.test(sql)){console.error('Missing ASSERT'); process.exit(1)}; if(!/BETWEEN -5120010 AND -5120001/.test(sql)){console.error('DO \$\$ must scope to BETWEEN -5120010 AND -5120001'); process.exit(1)}; if(/pc\.id IS NULL/.test(sql)){console.error('Use pc.politician_id IS NULL'); process.exit(1)}; console.log('Migration 339 SQL OK: '+ans+' paired blocks')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql exists
    - Header lists all 10 Wave 9 UUIDs + honest-skips
    - answers INSERT count equals context INSERT count, both equal CSV rows with non-empty source_url_1
    - Exactly one BEGIN; and one COMMIT;
    - DO $$ block with ASSERT unsourced_count = 0 and external_id BETWEEN -5120010 AND -5120001
    - LEFT JOIN uses pc.politician_id IS NULL
    - No unescaped ' in any reasoning literal
  </acceptance_criteria>
  <done>
    Migration 339 SQL drafted; paired-insert invariant holds; verification block scoped to Wave 9 contiguous range with BETWEEN; ready for psql apply.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration 339 via psql and confirm DO $$ verification passes</name>
  <files>supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql</files>
  <read_first>
    - supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql
    - backend/.env (DATABASE_URL — session pooler aws-0-*.pooler.supabase.com:5432)
    - .planning/phases/112-va-delegate-stances/112-RESEARCH.md (Environment Availability + psql apply pattern)
  </read_first>
  <action>
    Apply Wave 9 migration via psql session pooler.

    Step 1: set -a && source backend/.env && set +a. Confirm $DATABASE_URL points at aws-0-*.pooler.supabase.com:5432.
    Step 2: psql "$DATABASE_URL" -f supabase/migrations/20260610000009_339_va_delegates_wave9_stances.sql
    Step 3: Capture output. DO $$ emits "VA delegates with stances (Wave 9): <N>" (0..10) and "Unsourced VA delegate stances (Wave 9): 0". ASSERT rolls back if unsourced != 0.
    Step 4: Follow-up confirm: psql "$DATABASE_URL" -c "SELECT COUNT(DISTINCT pa.politician_id) AS delegates, COUNT(*) AS total_stances FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -5120010 AND -5120001;"

    Do NOT use supabase db push or the JS client. If duplicate-version conflict, rename file to (highest disk + 1) and notify orchestrator; never edit schema_migrations. On success update header "Applied: NOT YET" -> "Applied: 2026-06-10".
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id BETWEEN -5120010 AND -5120001 AND (pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);" | tr -d ' \n' | grep -qx '0' && echo "VAST-05 OK: 0 unsourced Wave 9 stances" || (echo "VAST-05 VIOLATION"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - psql apply exits 0 (no rollback)
    - DO $$ prints "Unsourced VA delegate stances (Wave 9): 0"
    - Follow-up SQL: COUNT(DISTINCT pa.politician_id) for BETWEEN -5120010 AND -5120001 is 0..10
    - Follow-up SQL: unsourced count for the same range returns 0
    - Header updated to "Applied: 2026-06-10"
  </acceptance_criteria>
  <done>
    Migration 339 applied; Wave 9 NoVA Core stances live; VAST-05 holds for the Wave 9 range; next free migration is 340.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | Untrusted scraped content as plain text; ingested via psql, never executed |
| CSV → SQL migration | Single-quote-escaped literals in a static SQL file |
| psql client → Supabase DB | TLS session pooler; RLS enforces public-read/admin-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Party-inferred stance with no source | mitigate | D-09 in agent prompt; Task 2 verify rejects rows missing source_url_1; DO $$ ASSERT |
| T-112-02 | Tampering | FK violation | mitigate | Task 1 pre-flight confirms all 10 FK targets exist |
| T-112-03 | Tampering | Duplicate / over-count rows | mitigate | DO $$ block + ON CONFLICT upsert; Task 3 verify asserts equal insert counts |
| T-112-04 | Tampering | CSV reasoning contains SQL meta-chars | mitigate | Task 3 escapes ' as '' |
| T-112-05 | Denial of Service | Parallel agents exhaust WebFetch quota | mitigate | Task 2 sequential dispatch (D-11) |
| T-112-06 | Information Disclosure | Public-record research data | accept | Public web only; no PII |

No high-severity threats. No auth, no endpoints, no PII. ASVS not applicable.
</threat_model>

<verification>
- Wave 9 CSV passes csv-parse/sync; every full_name is a Wave 9 delegate; source_url_1 on every data row
- Migration 339: paired INSERTs (equal counts), single BEGIN/COMMIT, DO $$ ASSERT unsourced_count = 0 scoped to BETWEEN -5120010 AND -5120001, pc.politician_id IS NULL
- psql apply prints "Unsourced VA delegate stances (Wave 9): 0"
- Post-apply SQL: COUNT(DISTINCT pa.politician_id) for the range is 0..10; unsourced count returns 0
</verification>

<success_criteria>
- Each of the 10 Wave 9 delegates has >= 1 stance row OR a documented honest-skip
- 100% of inserted answers rows have a paired context row with sources length >= 1
- Migration 339 applied via psql (exit 0, ASSERT passed)
- CSV preserved as forensic artifact
- Plan 112-10 unblocked: next migration number is 340
</success_criteria>

<output>
Create `.planning/phases/112-va-delegate-stances/112-09-SUMMARY.md` when done, including total stances, per-delegate breakdown, honest-skips, migration number used, and VAST-05 confirmation (unsourced = 0 for the Wave 9 range).
</output>
