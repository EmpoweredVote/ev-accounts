---
phase: 31-referral-dashboard-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Connected user at level 2+ sees referral code with one-click copy button"
    - "Connected user at level < 2 sees locked card explaining level 2 requirement"
    - "Connected user whose invitee has not yet reached level 2 sees waiting state"
    - "Card state driven entirely by GET /api/referral — no client-side guessing"
  artifacts:
    - path: "app/src/pages/DashboardPage.tsx"
      provides: "Referral card with locked/waiting/active states"
      contains: "ReferralState"
  key_links:
    - from: "app/src/pages/DashboardPage.tsx"
      to: "/api/referral"
      via: "apiFetch in useEffect gated on connected_profile"
      pattern: "apiFetch.*referral"
---

<objective>
Verify that the referral dashboard card (already shipped in commit 97b2dab) satisfies all four Phase 31 success criteria. No new code is expected — this plan reads the existing implementation and confirms each requirement against the live code.

Purpose: Phase 31 was implemented ahead-of-schedule during v1.4 work. This plan formally verifies all requirements are met so the phase can be marked complete.
Output: Verification confirmation — no new files created.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all four referral card requirements against existing code</name>
  <files></files>
  <action>
Read `app/src/pages/DashboardPage.tsx` and verify each requirement:

**REF-01 (Active state):** Confirm that when `referral.unlocked === true` and either `inviteeJoined === false` OR `inviteeLevel >= 2`, the card shows:
- The referral code in a visible element
- A copy button that calls `navigator.clipboard.writeText(referral.code)`
- Visual feedback ("Copied!") after clicking

**REF-02 (Locked state):** Confirm that when `referral.unlocked === false`:
- A lock icon is rendered
- Text explains the level 2 requirement ("Reach level 2 to unlock")
- No referral code is visible anywhere in this branch

**REF-03 (Waiting state):** Confirm that when `referral.inviteeJoined === true` AND `inviteeLevel < 2`:
- A distinct visual state is shown (pulsing dot, "Friend joined!")
- The invitee's current level is displayed
- Text explains the user will get a new code when invitee reaches level 2

**REF-04 (Backend-driven):** Confirm that:
- `ReferralState` interface matches the `GET /api/referral` contract: `{ unlocked, code, inviteeJoined, inviteeLevel }`
- The fetch is `apiFetch<ReferralState>('/referral')` — no hardcoded state
- The fetch is gated on `me?.connected_profile` (Inform-tier users never trigger it)
- The render guard is `{cp && referral && (...)}` — card only appears when data is loaded

If ALL four requirements are confirmed, no further action is needed. If any gap exists, document it precisely for a gap-closure plan.
  </action>
  <verify>
Run `grep -n "referral" app/src/pages/DashboardPage.tsx` to confirm the referral card code is present and unmodified. All four REF requirements must map to specific line ranges in the file.
  </verify>
  <done>All four requirements (REF-01 through REF-04) confirmed present in the existing codebase with no gaps identified.</done>
</task>

</tasks>

<verification>
- REF-01: Lines 312-329 show active state with code display and copy button using navigator.clipboard.writeText
- REF-02: Lines 282-294 show locked state with lock icon and "Reach level 2 to unlock" text, no code visible
- REF-03: Lines 295-311 show waiting state with pulsing dot, "Friend joined!" text, and invitee level display
- REF-04: Line 152 fetches from /api/referral gated on connected_profile; ReferralState interface at lines 28-33 matches backend contract; render guard at line 278 requires both cp and referral
</verification>

<success_criteria>
All four Phase 31 success criteria confirmed satisfied by existing code. No new files created, no code changes needed. Phase 31 can be marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/31-referral-dashboard-card/31-01-SUMMARY.md`
</output>
