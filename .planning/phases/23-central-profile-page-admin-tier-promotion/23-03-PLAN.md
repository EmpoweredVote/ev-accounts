---
phase: 23-central-profile-page-admin-tier-promotion
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - admin/src/pages/admin/AccountsPage.tsx
  - admin/src/pages/admin/PromotionsPage.tsx
  - admin/src/pages/admin/AdminLayout.tsx
  - admin/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can search users by email or username with search-as-you-type dropdown"
    - "Search dropdown shows results inline below the search field and navigates on click"
    - "Global promotions page at /admin/promotions shows all promotions across all users"
    - "Promotions nav item appears in the admin sidebar"
  artifacts:
    - path: "admin/src/pages/admin/AccountsPage.tsx"
      provides: "Search-as-you-type dropdown with inline results"
      contains: "dropdown"
    - path: "admin/src/pages/admin/PromotionsPage.tsx"
      provides: "Global promotions log page"
      exports: ["PromotionsPage"]
    - path: "admin/src/pages/admin/AdminLayout.tsx"
      provides: "Updated nav with Promotions item"
      contains: "Promotions"
    - path: "admin/src/App.tsx"
      provides: "Route registration for /admin/promotions"
      contains: "promotions"
  key_links:
    - from: "admin/src/pages/admin/AccountsPage.tsx"
      to: "/api/admin/accounts"
      via: "apiFetch with search param"
      pattern: "apiFetch.*admin/accounts"
    - from: "admin/src/pages/admin/PromotionsPage.tsx"
      to: "/api/admin/promotions"
      via: "apiFetch GET call"
      pattern: "apiFetch.*promotions"
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/admin/PromotionsPage.tsx"
      via: "Route component registration"
      pattern: "PromotionsPage"
---

<objective>
Add search-as-you-type dropdown to the accounts list page and create a global promotions log page with navigation.

Purpose: Completes PROMO-01 (search + promote flow) and adds the global promotion audit view for admin oversight.
Output: Enhanced AccountsPage.tsx with inline search dropdown, new PromotionsPage.tsx, updated AdminLayout.tsx and App.tsx.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-central-profile-page-admin-tier-promotion/23-CONTEXT.md
@.planning/phases/23-central-profile-page-admin-tier-promotion/23-RESEARCH.md
@.planning/phases/23-central-profile-page-admin-tier-promotion/23-01-SUMMARY.md
@admin/src/pages/admin/AccountsPage.tsx
@admin/src/pages/admin/AdminLayout.tsx
@admin/src/App.tsx
@admin/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search-as-you-type Dropdown on Accounts List</name>
  <files>admin/src/pages/admin/AccountsPage.tsx</files>
  <action>
Read the existing AccountsPage.tsx. It already has a search input with `useDebounce(search, 300)` that triggers a table re-fetch. The context decision says: "Inline dropdown below search field, search-as-you-type. Admin clicks result to navigate to their profile."

Enhance the search UX with an inline dropdown that appears below the search field:

1. Add state: `showSearchDropdown: boolean` (true when search field is focused AND debouncedSearch has a value), `searchResults: Account[]` (separate from the main table data — the dropdown shows a compact list).

2. When `debouncedSearch` changes AND has length >= 2:
   - Fetch `GET /api/admin/accounts?search=${debouncedSearch}&page=1` (reuse existing API — the search param already works with ILIKE on both email and username).
   - Store results in `searchResults` (take first 8 results max for dropdown).
   - Set `showSearchDropdown = true`.

3. When search field is empty or blurred: hide dropdown. Use `onFocus` to re-show if search has value, `onBlur` with a small delay (150ms setTimeout) to allow click events on dropdown items to fire before hiding.

4. **Dropdown UI** — positioned absolutely below the search input:
   - Wrapper: `relative` on the search input container.
   - Dropdown: `absolute top-full left-0 right-0 mt-1 bg-white border border-gray-200 rounded-lg shadow-lg z-10 max-h-64 overflow-y-auto`.
   - Each result item: clickable row showing `display_name` (bold), `email` (gray, smaller), tier badge (inline, using TIER_BADGE colors). On click: `navigate('/admin/accounts/${account.id}')`.
   - Hover state: `hover:bg-gray-50`.
   - No results state: "No users found for '{debouncedSearch}'" in gray text, padded.

5. The existing table still works as before — the dropdown is an ADDITIONAL navigation shortcut, not a replacement for the filtered table.

6. Keep the existing `useDebounce` hook (already at 300ms — matches the context research finding).
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` — zero errors.
Run `grep -n "searchDropdown\|showSearchDropdown" admin/src/pages/admin/AccountsPage.tsx` — confirms dropdown state exists.
  </verify>
  <done>
AccountsPage has an inline search-as-you-type dropdown below the search field. Typing 2+ chars shows matching users with name, email, and tier badge. Clicking a result navigates to /admin/accounts/:userId. Dropdown hides on blur or empty search.
  </done>
</task>

<task type="auto">
  <name>Task 2: Global Promotions Page + Nav + Route Registration</name>
  <files>
    admin/src/pages/admin/PromotionsPage.tsx
    admin/src/pages/admin/AdminLayout.tsx
    admin/src/App.tsx
  </files>
  <action>
**PromotionsPage.tsx (NEW FILE):**

Create a new page component following the pattern of existing admin pages (AccountsPage, CronLogPage).

Interface `PromotionEntry`:
- `id: string, admin_email: string, target_user_id: string, target_display_name: string, previous_tier: string, new_tier: string, note: string | null, created_at: string`

Interface `PromotionsResponse`:
- `entries: PromotionEntry[]; total: number; page: number; pages: number`

Component `PromotionsPage`:
- State: `data: PromotionsResponse | null`, `loading: boolean`, `error: string | null`, `page: number`.
- Fetch `GET /api/admin/promotions?page=${page}` on mount and page change.
- Render:
  - Page title: "Tier Promotions" (h1).
  - Subtitle: "All tier promotion events across all users." (gray text).
  - Table with columns: Date, Target User, Admin, Previous Tier, New Tier, Note.
    - Date: formatted `created_at` via `new Date().toLocaleString()`.
    - Target User: `target_display_name` as a Link to `/admin/accounts/${target_user_id}`.
    - Admin: `admin_email`.
    - Previous/New Tier: with tier badge styling (TIER_BADGE map — copy from AccountsPage or import).
    - Note: text or dash if null.
  - Pagination: same Previous/Next button pattern as AccountsPage/XP History.
  - Loading state: skeleton rows (same pattern as other pages).
  - Error state: red banner.
  - Empty state: "No promotions recorded yet."

**AdminLayout.tsx update:**

Read existing file. Add to `navItems` array:
```typescript
{ label: 'Promotions', to: '/admin/promotions' },
```
Place it after "Accounts" in the nav order (it's a closely related admin function).

**App.tsx update:**

Read existing file. Add import:
```typescript
import { PromotionsPage } from './pages/admin/PromotionsPage';
```

Add route inside the `<Route path="/admin" element={<AdminLayout />}>` block:
```tsx
<Route path="promotions" element={<PromotionsPage />} />
```
Place after the `accounts/:userId` route.
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` — zero errors.
Run `grep -n "PromotionsPage" admin/src/App.tsx` — confirms import and route.
Run `grep -n "Promotions" admin/src/pages/admin/AdminLayout.tsx` — confirms nav item.
  </verify>
  <done>
PromotionsPage.tsx renders a paginated global promotions table at /admin/promotions. AdminLayout shows "Promotions" in sidebar nav. App.tsx registers the route. Table displays date, target user (linked), admin email, tier transition, and notes.
  </done>
</task>

</tasks>

<verification>
1. `cd admin && npx tsc --noEmit` passes with zero errors.
2. AccountsPage search dropdown appears with inline results and navigates on click.
3. PromotionsPage renders at /admin/promotions with paginated log table.
4. AdminLayout sidebar includes "Promotions" nav item.
5. App.tsx has route for /admin/promotions.
</verification>

<success_criteria>
- Search-as-you-type dropdown works on AccountsPage with 300ms debounce
- Dropdown shows name, email, tier badge for matching users
- Global promotions page shows all promotion events with pagination
- Promotions nav item in admin sidebar
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-central-profile-page-admin-tier-promotion/23-03-SUMMARY.md`
</output>
