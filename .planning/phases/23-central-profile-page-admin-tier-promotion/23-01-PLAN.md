---
phase: 23-central-profile-page-admin-tier-promotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260314000035_phase23_tier_promotion.sql
  - backend/src/routes/profile.ts
  - backend/src/lib/profileService.ts
  - backend/src/lib/adminService.ts
  - backend/src/routes/admin.ts
  - backend/src/index.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/account/profile/:userId returns public profile without auth"
    - "GET /api/account/profile/me returns owner profile with gems, location_consent, email"
    - "POST /api/admin/accounts/:userId/promote creates connected_profiles row and log entry atomically"
    - "Promoting an already-Connected or Empowered user returns 409 without writing a log row"
    - "GET /api/admin/accounts/:userId/promotion-history returns paginated promotion log"
    - "GET /api/admin/promotions returns global paginated promotion log"
    - "Empowered profile block includes politician fields from inform.politicians"
  artifacts:
    - path: "supabase/migrations/20260314000035_phase23_tier_promotion.sql"
      provides: "empowered_profiles.politician_id FK column + tier_promotion_log table + promote_to_connected RPC"
      contains: "CREATE TABLE"
    - path: "backend/src/routes/profile.ts"
      provides: "Profile route handlers"
      exports: ["default"]
    - path: "backend/src/lib/profileService.ts"
      provides: "Profile data aggregation logic"
      exports: ["getPublicProfile", "getOwnerProfile"]
    - path: "backend/src/lib/adminService.ts"
      provides: "promoteToConnected, getPromotionHistory, getGlobalPromotionLog"
  key_links:
    - from: "backend/src/routes/profile.ts"
      to: "backend/src/lib/profileService.ts"
      via: "import getPublicProfile, getOwnerProfile"
      pattern: "import.*profileService"
    - from: "backend/src/routes/admin.ts"
      to: "backend/src/lib/adminService.ts"
      via: "import promoteToConnected"
      pattern: "promoteToConnected"
    - from: "backend/src/lib/adminService.ts"
      to: "connect.promote_to_connected RPC"
      via: "adminRpc call"
      pattern: "adminRpc.*promote_to_connected"
    - from: "backend/src/index.ts"
      to: "backend/src/routes/profile.ts"
      via: "app.use('/api/account/profile')"
      pattern: "profileRouter"
---

<objective>
Create the backend foundation for Phase 23: migration for tier_promotion_log table and promote_to_connected RPC, profile service for public/owner data aggregation, profile route handlers, and admin promotion endpoint with audit logging.

Purpose: Establishes all backend APIs that the admin UI (Plans 02 and 03) will consume.
Output: Migration SQL, profileService.ts, profile.ts routes, admin promote/promotion-log endpoints.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-central-profile-page-admin-tier-promotion/23-CONTEXT.md
@.planning/phases/23-central-profile-page-admin-tier-promotion/23-RESEARCH.md
@backend/src/routes/account.ts
@backend/src/routes/admin.ts
@backend/src/routes/candidates.ts
@backend/src/lib/adminService.ts
@backend/src/lib/candidateService.ts
@backend/src/lib/supabase.ts
@backend/src/middleware/auth.ts
@backend/src/index.ts
@supabase/migrations/20260224000006_empower_empowered_profiles.sql
@supabase/migrations/20260227000018_empower_phase5.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Profile Service + Profile Routes</name>
  <files>
    supabase/migrations/20260314000035_phase23_tier_promotion.sql
    backend/src/lib/profileService.ts
    backend/src/routes/profile.ts
    backend/src/index.ts
  </files>
  <action>
**Migration (20260314000035_phase23_tier_promotion.sql):**

Create in a BEGIN/COMMIT block:

1. `CREATE TABLE IF NOT EXISTS connect.tier_promotion_log` with columns:
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `admin_id UUID NOT NULL REFERENCES public.users(id)`
   - `admin_email TEXT NOT NULL` (denormalized — avoids auth.users join at read time)
   - `target_user_id UUID NOT NULL REFERENCES public.users(id)`
   - `previous_tier TEXT NOT NULL`
   - `new_tier TEXT NOT NULL`
   - `note TEXT` (nullable)
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
   Indexes on `target_user_id` and `admin_id`.

2. `ALTER TABLE empower.empowered_profiles ADD COLUMN IF NOT EXISTS politician_id UUID REFERENCES inform.politicians(id)`:
   - Adds the FK column that links an Empowered user to their politician record in inform.politicians.
   - Nullable — not every Empowered user may have a linked politician record yet.
   - No backfill needed in this migration (the seed script in Plan 03 handles this for existing Empowered accounts).

3. `CREATE OR REPLACE FUNCTION connect.promote_to_connected(...)` — SECURITY DEFINER, SET search_path = '':
   Parameters: `p_admin_id UUID, p_admin_email TEXT, p_target_user_id UUID, p_note TEXT DEFAULT NULL`
   Returns JSONB.
   Logic:
   - Look up `display_name` from `public.users` WHERE `id = p_target_user_id`. RAISE EXCEPTION 'USER_NOT_FOUND' if not found.
   - Check for existing `connect.connected_profiles` row WHERE `user_id = p_target_user_id`. RAISE EXCEPTION 'ALREADY_CONNECTED_OR_HIGHER' if found.
   - INSERT into `connect.connected_profiles`: user_id, display_name, account_standing='active', verification_status='pending', verification_method='admin_promotion', total_xp=0, gem_balance_yellow=0, gem_balance_blue=0, gem_balance_red=0, completed_onboarding=false. Omit gem_balance from the INSERT column list entirely — it has DEFAULT 0 and will be populated automatically.
   - INSERT into `connect.tier_promotion_log`: admin_id=p_admin_id, admin_email=p_admin_email, target_user_id=p_target_user_id, previous_tier='inform', new_tier='connected', note=p_note.
   - Return `jsonb_build_object('ok', true, 'display_name', v_display_name)`.

4. GRANT EXECUTE on the function to service_role.

**profileService.ts (NEW FILE):**

Service using `supabaseAdmin` for all reads (unauthenticated callers have no JWT).

`getPublicProfile(userId: string)`:
- Fetch `public.users` row: `id, display_name, created_at` via supabaseAdmin. Return null if not found.
- Fetch `connect.connected_profiles` row: `user_id, display_name, total_xp, selected_topic_ids` via supabaseAdmin.schema('connect'). Use `.maybeSingle()`.
- Fetch `empower.empowered_profiles` row: `user_id, legal_name, candidate_page_slug, is_active, empowered_at, demoted_at` via supabaseAdmin.schema('empower'). Filter `.is('deleted_at', null)`. Use `.maybeSingle()`.
- Derive tier: `(empowered && empowered.is_active) ? 'empowered' : connected ? 'connected' : 'inform'`
- For Connected/Empowered: call `adminRpc('calculate_level', { p_total_xp: connected.total_xp })` to get level. Use `total_xp` (NEVER `xp`).
- Build base response: `{ username, tier, level, total_xp }` where `username` = empowered?.candidate_page_slug ?? connected?.display_name ?? user.display_name.
- If Connected or Empowered: add `selected_topic_ids` (from connected_profiles, default to []).
- If Empowered:
  - Fetch ALL compass answers via supabaseAdmin.schema('inform').from('compass_responses').select('topic_id, value, write_in_text, inverted, updated_at').eq('user_id', userId).is('deleted_at', null). Add as `compass_answers`.
  - Fetch the politician record from `inform.politicians` using the `politician_id` FK added to `empower.empowered_profiles` in migration 035: `supabaseAdmin.schema('inform').from('politicians').select('*').eq('id', empowered.politician_id).maybeSingle()`. (There is no join via candidate_page_slug — `inform.politicians` has no slug column. The `politician_id` FK is the only join path.)
  - NOTE: The RESEARCH.md doc found no existing join between these tables — that was accurate as of migration 034. Migration 035 adds the `politician_id` FK column. Do not attempt to find a join in candidates.ts or candidateService.ts — it does not exist there.
  - Build `empowered_profile` by merging empowered_profiles fields (`legal_name, candidate_page_slug, is_active, empowered_at, demoted_at`) WITH politician fields from inform.politicians (all columns: `office_title, district_label, district_id, district_type, representing_city, representing_state, photo_origin_url, full_name, preferred_name, first_name, last_name, chamber_name, government_name, is_candidate, is_vacant`). If `politician_id` is null (no linked politician record), return `empowered_profile` with only the empowered_profiles metadata and no politician fields. The CONTEXT.md locked decision requires empowered_profile to include the "full politician record" — include all politician columns when present.
- EXCLUDE from response: gems, tolerance_rating, legal_name (at root), email, location_consent.

`getOwnerProfile(userId: string, email: string)`:
- Call `getPublicProfile(userId)` for the base shape.
- Add owner-only fields: `gem_balances: { yellow, blue, red }` (from connected_profiles `gem_balance_yellow/blue/red` — default 0 for Inform users), `location_consent` (from connected_profiles via `getLocationConsent` helper from connectService.ts or direct read), `email`.
- Return the combined shape.

**profile.ts (NEW ROUTE FILE):**

Create an Express Router.

CRITICAL ORDERING: Register `/me` BEFORE `/:userId`.

`GET /me` — `requireAuth` middleware. Extract userId and email from AuthenticatedRequest. Call `getOwnerProfile(userId, email)`. Return 404 if null.

`GET /:userId` — No auth middleware (public endpoint). Validate userId is UUID format. Call `getPublicProfile(userId)`. Return 404 if null.

Export default router.

**index.ts update:**

Import profileRouter from `./routes/profile.js`.
Mount at: `app.use('/api/account/profile', profileRouter)` — BEFORE the `/api/account` mount to prevent Express route conflicts. Insert the line right before `app.use('/api/account', accountRouter)`.
  </action>
  <verify>
Run `npx tsc --noEmit` from backend/ — zero errors.
Run `grep -n "profileRouter" backend/src/index.ts` — confirms route registration.
Run `grep -n "/me" backend/src/routes/profile.ts` — confirm /me is registered before /:userId.
Run `grep -n "inform.*politicians\|politicians" backend/src/lib/profileService.ts` — confirms inform.politicians fetch is present for Empowered tier.
  </verify>
  <done>
profileService.ts exports getPublicProfile and getOwnerProfile. getPublicProfile fetches inform.politicians for Empowered users and merges politician fields into empowered_profile block. profile.ts registers GET /me (auth) and GET /:userId (public) in correct order. Migration creates tier_promotion_log table and promote_to_connected RPC. index.ts mounts profile routes at /api/account/profile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin Promotion Endpoint + Promotion Log Endpoints</name>
  <files>
    backend/src/lib/adminService.ts
    backend/src/routes/admin.ts
  </files>
  <action>
**adminService.ts additions:**

Add three new exported functions:

NOTE: The RESEARCH.md Patterns 3 and 4 show an older RPC signature without `p_admin_email`. Those examples are superseded. Use the 4-param signature with `p_admin_email TEXT` as specified in Task 1's migration above.

1. `promoteToConnected(adminId: string, adminEmail: string, targetUserId: string, note?: string): Promise<{ display_name: string }>`:
   - Call `adminRpc('promote_to_connected', { p_admin_id: adminId, p_admin_email: adminEmail, p_target_user_id: targetUserId, p_note: note ?? null }, 'connect')`.
   - Map RPC exceptions: 'USER_NOT_FOUND' -> throw with code 'NOT_FOUND'. 'ALREADY_CONNECTED_OR_HIGHER' -> throw with code 'ALREADY_CONNECTED'.
   - Return `{ display_name }` from RPC result for toast message.

2. `getPromotionHistory(targetUserId: string, page: number = 1): Promise<{ entries: unknown[]; total: number; page: number; pages: number }>`:
   - PAGE_SIZE = 25.
   - Query `supabaseAdmin.schema('connect').from('tier_promotion_log').select('*', { count: 'exact' }).eq('target_user_id', targetUserId).order('created_at', { ascending: false }).range(from, from + PAGE_SIZE - 1)`.
   - admin_email is already denormalized in the log row (no join needed).
   - Return paginated shape matching XP history pattern.

3. `getGlobalPromotionLog(page: number = 1): Promise<{ entries: unknown[]; total: number; page: number; pages: number }>`:
   - Same as above but without the `target_user_id` filter.
   - Join target user display_name: use separate query to `public.users` for the target_user_ids in the page, or use a PostgREST foreign key embed if available. Simplest: fetch the log page, then batch-fetch display_names for all unique target_user_ids. Build a map and attach `target_display_name` to each entry.

Add imports: add `promoteToConnected, getPromotionHistory, getGlobalPromotionLog` to the exports.

**admin.ts additions:**

Add to imports from adminService: `promoteToConnected, getPromotionHistory, getGlobalPromotionLog`.

1. `POST /api/admin/accounts/:userId/promote`:
   - Zod schema: `{ note: z.string().max(500).optional() }`.
   - Extract adminId via `actorId(req)`.
   - Resolve admin email server-side: call `supabaseAdmin.auth.admin.getUserById(actorId(req))` to get the admin's email. This is the ONLY acceptable method — do not use a placeholder, do not read from the request body, do not skip this step. Extract the email from the returned user object (`adminUser.data.user.email`). If the call fails or email is missing, return 500 with "Failed to resolve admin identity".
   - Call `promoteToConnected(adminId, adminEmail, userId, parsed.data.note)`.
   - Call `logAdminAction(actorId(req), 'promote_to_connected', userId, { previous_tier: 'inform', new_tier: 'connected', note: parsed.data.note ?? null })`.
   - Return `{ ok: true }`.
   - Error handling: code 'NOT_FOUND' -> 404. code 'ALREADY_CONNECTED' -> 409 with user-facing message "User is already Connected or Empowered".

2. `GET /api/admin/accounts/:userId/promotion-history`:
   - Zod query schema: `{ page: z.coerce.number().int().min(1).default(1) }` (same pattern as XpHistoryQuerySchema).
   - Call `getPromotionHistory(userId, parsed.data.page)`.
   - Return the paginated result.

3. `GET /api/admin/promotions`:
   - Zod query schema: same page pattern.
   - Call `getGlobalPromotionLog(parsed.data.page)`.
   - Return the paginated result.

Place the promote route NEAR the existing demote route (after line ~210). Place promotion-history after xp-history. Place global promotions route near the end of admin routes.
  </action>
  <verify>
Run `npx tsc --noEmit` from backend/ — zero errors.
Run `grep -n "promote" backend/src/routes/admin.ts` — confirms promote route registered.
Run `grep -n "promoteToConnected\|getPromotionHistory\|getGlobalPromotionLog" backend/src/lib/adminService.ts` — confirms all three functions exported.
Run `grep -n "getUserById" backend/src/routes/admin.ts` — confirms admin email is resolved via supabaseAdmin.auth.admin.getUserById.
  </verify>
  <done>
POST /api/admin/accounts/:userId/promote resolves admin email via supabaseAdmin.auth.admin.getUserById (no placeholder, no alternatives), then calls promote_to_connected RPC with error mapping for NOT_FOUND (404) and ALREADY_CONNECTED (409). GET /api/admin/accounts/:userId/promotion-history and GET /api/admin/promotions return paginated promotion logs with admin_email denormalized. All mutations logged via logAdminAction.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors from backend/.
2. Migration file exists at supabase/migrations/20260314000035_phase23_tier_promotion.sql with BEGIN/COMMIT, ALTER TABLE (politician_id FK), CREATE TABLE, CREATE FUNCTION, GRANT.
3. profileService.ts uses supabaseAdmin (NOT createUserClient) for all reads.
4. profileService.ts fetches inform.politicians via empowered.politician_id FK for Empowered tier and includes politician fields in empowered_profile block.
5. profile.ts registers /me BEFORE /:userId.
6. index.ts mounts /api/account/profile BEFORE /api/account.
7. admin.ts promote route resolves admin email via supabaseAdmin.auth.admin.getUserById — no placeholder.
8. admin.ts promote route returns 409 for already-Connected users.
9. No sensitive fields (tolerance_rating, legal_name, location, email) appear in getPublicProfile return.
</verification>

<success_criteria>
- TypeScript compiles cleanly
- Migration creates tier_promotion_log + promote_to_connected RPC
- Profile routes serve tier-conditional public/owner data
- Empowered profile block includes politician fields from inform.politicians
- Admin promote endpoint resolves admin email server-side and handles error cases
- Promotion log endpoints with pagination
- All functions use explicit whitelist serialization (no spread)
</success_criteria>

<output>
After completion, create `.planning/phases/23-central-profile-page-admin-tier-promotion/23-01-SUMMARY.md`
</output>
