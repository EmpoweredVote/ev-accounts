---
phase: 15-help-page-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/assets/help/
autonomous: true
requirements: []

must_haves:
  truths:
    - "Screenshot files exist for each help slide at both mobile and desktop sizes"
    - "Screenshots show real app state (not empty or placeholder data)"
  artifacts:
    - path: "CompassV2/src/assets/help/"
      provides: "Static screenshot images for help page slides"
  key_links: []
---

<objective>
Capture real UI screenshots for the updated /help page slides at both mobile and desktop viewport sizes.

Purpose: The user-decided replacement for the obsolete onboarding GIFs — static screenshots of the actual UI that will be displayed responsively on the /help page.
Output: 8-10 PNG screenshot files (4-5 slides x 2 sizes) saved in `CompassV2/src/assets/help/`.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-help-page-update/15-CONTEXT.md
@CompassV2/src/pages/Compass.jsx
@CompassV2/src/components/CalibrationOverlay.jsx
@CompassV2/src/pages/Library.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture responsive screenshots of each help page slide state</name>
  <files>CompassV2/src/assets/help/</files>
  <action>
Use Playwright (browser MCP tools) to capture real screenshots of the CompassV2 app for each help page slide. The dev server should be started at localhost:5173 (or whichever port Vite uses).

**Pre-requisite:** Start the CompassV2 dev server with `cd CompassV2 && npm run dev` in background. Wait for it to be ready.

**Screenshots to capture (5 slides):**

1. **Welcome / Calibration Overlay** — Navigate to /results (compass page). If calibration overlay is not visible (user has prior state), clear localStorage first to reset state, then reload. Capture the CalibrationOverlay welcome screen. This is the "Calibrate your Compass" entry point.
   - Desktop: 1280x800 viewport → `help_1_welcome_desktop.png`
   - Mobile: 390x844 viewport → `help_1_welcome_mobile.png`

2. **Topic Selection (Calibration pick step)** — Click "Get Started" on the welcome screen to advance to the topic picker step. Capture with some topics toggled on (select 4-5 topics).
   - Desktop: 1280x800 → `help_2_calibrate_desktop.png`
   - Mobile: 390x844 → `help_2_calibrate_mobile.png`

3. **Library Exploration** — Navigate to /library. Capture the Library page showing topic cards with the drawer open on one topic (click a topic card to open the LibraryDrawer). This shows the "explore topics" flow.
   - Desktop: 1280x800 → `help_3_library_desktop.png`
   - Mobile: 390x844 → `help_3_library_mobile.png`

4. **Compare with Candidates** — Navigate to /results (compass page). The compass should have data from the calibration. Click "Compare" button and capture the compare modal or compare panel with a politician selected.
   - Desktop: 1280x800 → `help_4_compare_desktop.png`
   - Mobile: 390x844 → `help_4_compare_mobile.png`

5. **Your Compass (final state)** — Navigate to /results showing the completed compass with spokes and data filled in. No overlays, no modals — just the clean compass view.
   - Desktop: 1280x800 → `help_5_compass_desktop.png`
   - Mobile: 390x844 → `help_5_compass_mobile.png`

**Important notes:**
- Save all files to `CompassV2/src/assets/help/` directory (create it if needed)
- Use PNG format for crisp screenshots
- If the app needs real topic/answer data to look good, use the calibration overlay flow to answer a few topics first, then capture the later slides
- For the compare screenshot, if no politicians are available to compare, capture the compare modal showing the search/selection interface instead
- Screenshots should capture the viewport content (not full page scrolls)

If Playwright browser tools are unable to interact with the running dev server effectively (e.g., no topics load because the backend API is not running), fall back to:
- Capturing whatever states are reachable
- For states that require backend data, capture the UI in its loading/empty state — this is still better than outdated GIFs
- Note in the summary which screenshots show real data vs empty states
  </action>
  <verify>
All 10 screenshot files exist in `CompassV2/src/assets/help/`:
- `ls CompassV2/src/assets/help/` shows 10 PNG files
- Files are non-zero size (actual screenshots, not empty files)
  </verify>
  <done>
10 PNG screenshot files (5 slides x 2 viewport sizes) exist in CompassV2/src/assets/help/ showing real app UI states for each help page slide.
  </done>
</task>

</tasks>

<verification>
- `ls -la CompassV2/src/assets/help/*.png` shows 10 files with reasonable sizes (>10KB each)
- Screenshot filenames follow the naming convention: help_{N}_{name}_{desktop|mobile}.png
</verification>

<success_criteria>
Screenshot assets are captured and ready for Plan 02 to import into the rewritten Onboarding.jsx component.
</success_criteria>

<output>
After completion, create `.planning/phases/15-help-page-update/15-01-SUMMARY.md`
</output>
