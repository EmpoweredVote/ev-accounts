---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/155-verify.sql
autonomous: true
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05]
must_haves:
  truths:
    - "A standalone, write-free 155-verify.sql exists asserting every USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 success criterion, scoped per-state to the 17 PA + 17 IL House races"
    - "Every assertion is scoped via NATIONAL_LOWER + substr(geo_id,1,2) IN ('42','17') within the PA/IL 2026 Statewide General elections — no cross-state contamination"
    - "Lost/retired/open incumbents (PA-3 Evans, IL-2 Kelly, IL-4 García, IL-7 Davis, IL-8 Krishnamoorthi, IL-9 Schakowsky) are asserted ABSENT from the active field; their certified general nominees present"
    - "Any whole-record stance honest-skip and any headshot honest-skip is pinned by exact UUID/external_id with the query's exact ORDER BY (143 lesson)"
  artifacts:
    - path: "backend/scripts/155-verify.sql"
      provides: "Read-only labeled-assertion gate for Phase 155 (USHC2-02/03/04/05 + decisions), per-state PA/IL-scoped, honest-skip-pinnable"
      contains: "NATIONAL_LOWER"
      min_lines: 120
  key_links:
    - from: "155-verify.sql assertions"
      to: "the 17 PA + 17 IL House races within the PA/IL 2026 Statewide General elections"
      via: "d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('42','17') predicate"
      pattern: "NATIONAL_LOWER"
---

<objective>
Author the read-only `backend/scripts/155-verify.sql` gate (Wave-0 test infrastructure, nyquist) that every Phase 155 wave runs to self-check and that 155-09 runs as the final consolidated proof. It asserts elections/races scaffold (USHC2-03), records + dedup (USHC2-02), race wiring, headshots (USHC2-04), stances (USHC2-05), the seed-all-minor-party invariant (D-02), the lost/retired-incumbent-absent invariant (D-04), and zero-duplicate-full_name-per-state (D-03) — scoped per-state to the 17 PA + 17 IL House races. This is the PER-PHASE gate, NOT the milestone gate (Phase 158).

Purpose: This gate must exist before later waves so each can verify its slice. Trap-prone parts: (a) per-state scoping (PA and IL are separate elections — never cross-count), (b) lost/retired-incumbent absence (PA-3 + 5 IL retirements), (c) the D-01 symmetry — BOTH PA and IL incumbents are all-partial and DELIBERATELY untouched this phase (like NY in Phase 150), so they must NOT be required to reach federal-24, only the ~48 new candidates are in the stance scope.
Output: A standalone, write-free .sql gate (the 150-verify.sql precedent, adapted for PA/IL with all-partial-incumbent exclusion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@backend/scripts/150-verify.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 155-verify.sql — per-state PA/IL-scoped labeled assertions for USHC2-02/03/04/05 + decisions</name>
  <read_first>
    - backend/scripts/150-verify.sql (the validated TWO-STATE precedent: read-only DO-block labeled assertions; CREATE TEMP TABLE … ON COMMIT DROP working sets; reuse_pid / lost-incumbent / new-cands / stance-skip / fed24 pin-table patterns; per-state scoping; NY-partial-exclusion encoding; the 143 honest-skip-ORDER-BY lesson)
    - backend/scripts/149-verify.sql (the original single-state precedent + the in-scope-set TEMP-table convention so mid-wave plans can run the gate)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md (non-incumbent-nominee flag list: PA-3 + IL-2/4/7/8/9 retirements/Senate runs; the IL-4 7-candidate + IL-2 3-candidate + IL minor-line rows for D-02; stance-gap summary = PA/IL ALL-partial, no zero-stance incumbents)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (PA/IL geo_id → incumbent_pid + external_id, for the reuse-pid pin set and the lost/retired-incumbent-absent pins: PA-3 Evans 3c962502…/-42003, IL-2 Kelly 9d9ce75d…/-17002, IL-4 García 2bc2ecee…/-17004, IL-7 Davis 0e65d006…/-17007, IL-8 Krishnamoorthi ee1a8680…/-17008, IL-9 Schakowsky 4fbdecd3…/-17009)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md (D-01 symmetry — BOTH PA and IL incumbents untouched; D-02 seed-all-minor; D-03 zero-dup-full_name-per-state + certified-general re-confirm; D-04 lost/retired no-active-row)
  </read_first>
  <action>
    Write `backend/scripts/155-verify.sql` as a write-free, labeled-assertion gate following 150-verify.sql exactly, parameterized for the two states PA + IL. Resolve both election ids at the top (SELECT id FROM essentials.elections WHERE name='PA 2026 Statewide General' / '…IL…'). Build per-state House working sets via the join chain races JOIN offices ON office_id JOIN districts ON district_id filtered r.election_id IN (pa_eid, il_eid) AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('42','17'). NEVER write a cross-state or election-wide count.

    Labeled assertions (each DO $$ … IF NOT (…) THEN RAISE EXCEPTION 'FAIL <label>: %' … ELSE RAISE NOTICE 'PASS <label>'; END $$; style):
    - USHC2-03a-PA / USHC2-03a-IL — all 17 PA races (resp. 17 IL) have ≥1 active race_candidate; additionally assert the COUNT of races = 17 PA / 17 IL so no district is missing. Most have ≥2; default to ≥2 with a documented allowance note for single-candidate generals (PA-3 Chris Rabb is unopposed-D per 154 field — assert ≥1 for that pinned district).
    - USHC2-03b — 0 active race_candidates rows with NULL politician_id (per-state, House-scoped).
    - USHC2-02a-PA / USHC2-02a-IL — 0 duplicate full_name among ACTIVE candidates WITHIN each state (GROUP BY lower(full_name) HAVING count(*)>1 over that state's races returns 0) — D-03 zero-duplicate-full_name-per-state.
    - USHC2-02c — every renominated PA & IL incumbent reuses its 154 incumbent_pid (pin a reuse_pid TEMP table of (geo_id, expected_pid) from 154-incumbent-map.csv for every renominated incumbent: PA all 17 except PA-3; IL all except IL-2/4/7/8/9; assert each pinned pid appears as an ACTIVE is_incumbent=true House candidate). Document in a header comment which incumbents are intentionally NOT in the set (the 6 lost/retired).
    - D-04-lost-incumbent — the lost/retired/open incumbent pids are ABSENT from the active field: assert PA-3 Evans (3c962502-99c7-46ec-ae6b-0c69143f571c), IL-2 Kelly (9d9ce75d-…), IL-4 García (2bc2ecee-…), IL-7 Davis (0e65d006-…), IL-8 Krishnamoorthi (ee1a8680-…), IL-9 Schakowsky (4fbdecd3-…) each appear 0 times as an active candidate; assert their certified general nominees ARE active (PA-3 Chris Rabb; IL-2 Donna Miller; IL-4 the re-confirmed D nominee; IL-7 La Shawn Ford; IL-8 Melissa Bean; IL-9 Daniel Biss — pin the winner pids from the 155-04 reconciliation, fallback name match within the district).
    - D-02-minor — named minor-line / multi-candidate fields are seeded (party-agnostic, assert presence): IL-2 Ashley Banks (Independent); IL-4 the certified third-line candidate(s); PA-10 Isabelle Harman + Steven Long (Independent); PA-13 Cody Thomas (Independent). Confirm the actual certified set against the 155-03/04 reconciliations (D-03 re-confirm may trim the IL-4 primary field).
    - USHC2-04 — every newly-seeded PA/IL candidate (politician_id in the new-candidate external_id band assigned by 155-03/04) has an essentials.politician_images row; documented headshot honest-skips pinned by exact external_id with explicit ORDER BY.
    - USHC2-05a — 0 unsourced stance rows for the in-scope set: 0 = count over inform.politician_answers for the in-scope pids WHERE NOT EXISTS a matching inform.politician_context with a non-empty sources array. In-scope set (D-01 symmetry): ONLY the new PA/IL challengers + open-seat nominees. EXCLUDE ALL PA + IL partial incumbents (left as-is this phase) so their pre-existing partial coverage cannot false-fail a 0-unsourced or coverage check.
    - USHC2-05b — each in-scope candidate (new challenger / open-seat nominee) has ≥1 sourced federal stance OR is a pinned whole-record honest-skip (per the project-149-stance-gate-standard: ≥1-sourced-OR-pinned, full-24 NOT required; chairs-not-polarity forbids party-inference inflation). Whole-record skips pinned by UUID with exact ORDER BY. PA/IL partial incumbents are NOT in this set (D-01).

    Define the in-scope stance/headshot set + honest-skip pins as TEMP tables at the top (the 149/150 pattern) so 155-05..08 can run the gate mid-wave. Header comment documents: (a) per-state scoping rule; (b) the D-01 PA+IL all-partial-incumbent exclusion from the stance scope; (c) data-dependent assertions legitimately FAIL pre-seed and go green only after the waves complete; (d) the 143 honest-skip-ORDER-BY lesson.

    Self-test that the SQL parses + runs read-only against prod NOW (pre-seed). The races scaffold (155-01) may already be live in this wave; USHC2-03a may already pass for the races count while candidate/headshot/stance assertions FAIL pre-seed (expected).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^[[:space:]]*--' scripts/155-verify.sql > /tmp/155v.nc && grep -qi 'NATIONAL_LOWER' /tmp/155v.nc && grep -q "'42'" /tmp/155v.nc && grep -q "'17'" /tmp/155v.nc && ! grep -qiE 'DELETE |UPDATE |INSERT[[:space:]]+INTO[[:space:]]+(essentials|inform)' /tmp/155v.nc && echo PASS-scoped-and-write-free</automated>
  </verify>
  <acceptance_criteria>
    - 155-verify.sql contains all labeled assertions above; every data query is per-state House-scoped (NATIONAL_LOWER + geo_id prefix '42'/'17' within the PA/IL 2026 Statewide General elections).
    - The file is write-free: no DELETE/UPDATE/INSERT INTO essentials|inform (TEMP-table CREATE … ON COMMIT DROP is the only allowed write, matching 150-verify.sql).
    - D-01 symmetry encoded: ALL PA + IL partial incumbents are EXCLUDED from the USHC2-05a/05b in-scope set (header-documented).
    - Lost/retired-incumbent-absent assertions present for PA-3 + IL-2/4/7/8/9 (pids absent, certified nominees present); D-02 minor-line/multi-candidate presence assertions present.
    - Honest-skip pins use explicit ORDER BY (143 lesson, header comment + pinned-set query).
    - The script parses + runs read-only against prod NOW; data-dependent assertions may FAIL pre-seed (documented as expected).
  </acceptance_criteria>
  <done>155-verify.sql authored, per-state PA/IL-scoped, write-free, all assertions present incl. D-01 all-partial-incumbent exclusion + lost/retired-incumbent-absent + D-02 minor-line; parses + runs read-only against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → production DB (read) | 155-verify.sql runs SELECT-only against live prod; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-04 | Tampering | gate accidentally mutates prod | mitigate | Write-free enforced by verify check; only TEMP-table writes allowed |
| T-155-05 | Repudiation | gate false-fails on PA/IL partial-incumbent pre-existing data | mitigate | ALL PA+IL partial incumbents EXCLUDED from in-scope stance set (D-01); header-documented |
| T-155-06 | Repudiation | cross-state contamination false-passes a count | mitigate | Every assertion per-state scoped (geo_id prefix '42'/'17' within the named election) |
| T-155-07 | Repudiation | lost/retired incumbent silently surfaced as active | mitigate | D-04-lost-incumbent asserts PA-3 + IL-2/4/7/8/9 pids absent + certified nominees present |
| T-155-08 | Repudiation | honest-skip ordering mismatch false-fails (143 lesson) | mitigate | Honest-skip pins use explicit ORDER BY matching the query |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- The verify automated check confirms per-state scoping + write-free.
- Manual: run the gate against prod pre-seed; confirm it executes (data assertions may FAIL pre-seed, expected).
</verification>

<success_criteria>
- 155-verify.sql exists, write-free, per-state PA/IL-scoped, with all USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 labeled assertions; runs read-only against prod without syntax/permission error.
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-02-SUMMARY.md` when done (record: assertion labels, the in-scope-set TEMP-table convention, the PA+IL all-partial-incumbent exclusion, and how 155-05..08 should invoke it mid-wave).
</output>
