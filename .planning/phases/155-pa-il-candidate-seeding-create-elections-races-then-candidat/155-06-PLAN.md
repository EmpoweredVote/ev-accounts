---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 06
type: execute
wave: 3
depends_on: ["155-04", "155-05"]
files_modified:
  - backend/data/seed-il-2026-house/155-06-il-headshots.manual.txt
autonomous: true
requirements: [USHC2-04]
must_haves:
  truths:
    - "Every newly-seeded IL candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row + photo_origin_url, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/data/seed-il-2026-house/155-06-il-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source IL portraits where Wikipedia has none"
  key_links:
    - from: "new IL candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-pa-il-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded IL candidate a headshot using the shared seed-pa-il-house-headshots.py pipeline authored in 155-05 (no new script — reuse + retarget to the IL band).

Purpose: USHC2-04 (IL slice). Reused incumbents already have headshots — only the 155-04 new IL candidates need them. IL has the larger new-record count (~28) including the open-seat nominees in IL-2/4/7/8/9.
Output: Every new IL candidate has an image row (or documented honest-skip) + an IL manual-source list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-SUMMARY.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-05-SUMMARY.md
@.claude/skills/find-headshots/SKILL.md
@backend/scripts/seed-pa-il-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the shared headshot pipeline for the IL new-candidate band</name>
  <read_first>
    - backend/scripts/seed-pa-il-house-headshots.py (the shared two-state pipeline authored in 155-05 — invoke with the IL external_id band; do NOT modify the guards)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-05-SUMMARY.md (the shared script path + invocation convention + the auto-pass-vs-manual ratio observed for PA)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-SUMMARY.md (the authoritative new IL-candidate external_id list, incl. the IL-2/4/7/8/9 open-seat nominees)
    - .claude/skills/find-headshots/SKILL.md (find-headshots conventions: 600×750 Storage-mirrored + politician_images row + photo_origin_url; free-license; wrong-person guard)
  </read_first>
  <action>
    Run the automatic Wikipedia pass for the IL band: `python backend/scripts/seed-pa-il-house-headshots.py` with the IL external_id list from 155-04-SUMMARY. For IL candidates with no free Wikipedia portrait, build backend/data/seed-il-2026-house/155-06-il-headshots.manual.txt with external_id|image_url_or_path|license lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL), then run with --manual.

    Pay particular attention to the open-seat nominees (IL-2 Donna Miller, IL-4 certified D nominee, IL-7 La Shawn Ford, IL-8 Melissa Bean, IL-9 Daniel Biss) — several are sitting state/local officials with .gov portraits. For any IL candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 155-verify.sql can pin it — NEVER a placeholder or wrong-person image. Keep all pipeline guards intact (free-license, wrong-person, 4:5 → 600x750, x-upsert, idempotent).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-04-SUMMARY.md','utf8'); const ids=[...sum.matchAll(/-17[0-9]{4}/g)].map(m=>+m[0]).filter(x=>x<-170000); if(!ids.length){console.error('no new IL external_ids found in 155-04-SUMMARY (check band)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new IL candidates still missing headshot:', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - Every new IL candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed, photo_origin_url set; wrong-person guard intact.
    - No modification to the shared script's guards; re-running inserts 0 additional image rows (idempotent).
  </acceptance_criteria>
  <done>IL headshots run via the shared script; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) → Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-22 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained from the shared 155-05 pipeline; manual sources human-verified; spot-check in 155-09 |
| T-155-23 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-155-24 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-155-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (155-09): sample new IL headshots against source pages for identity.
</verification>

<success_criteria>
- Every new IL candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact.
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-06-SUMMARY.md` when done (record: IL candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs).
</output>
