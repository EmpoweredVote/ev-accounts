---
phase: 155-pa-il-candidate-seeding-create-elections-races-then-candidat
plan: 03
type: execute
wave: 2
depends_on: ["155-01"]
files_modified:
  - backend/migrations/NNNN_seed_pa_2026_house_candidates.sql
  - backend/data/seed-pa-2026-house/155-03-pa-reconciliation.csv
autonomous: true
requirements: [USHC2-02, USHC2-03]
must_haves:
  truths:
    - "Every one of the 17 PA US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'"
    - "Every renominated PA incumbent (all 17 except PA-3 Evans) reuses its 154 incumbent_pid as the active is_incumbent=true candidate; the challenger(s) are new records"
    - "PA-3 Dwight Evans (3c962502…/-42003) gets NO active race_candidates row; Chris Rabb (D) is the active candidate (re-confirmed certified general nominee)"
    - "Every 'new' PA candidate is live-DB dedup-checked before insert; zero duplicate full_name within PA; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_pa_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT new PA politicians + INSERT race_candidates onto the 17 PA races authored in 155-01"
      contains: "race_candidates"
    - path: "backend/data/seed-pa-2026-house/155-03-pa-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 17 PA districts"
      min_lines: 35
  key_links:
    - from: "race_candidates.race_id"
      to: "the 17 PA House races (155-01 scaffold)"
      via: "office_id → districts.geo_id 42NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (154-incumbent-map.csv) or newly-inserted politician id (live-confirmed)"
      via: "155-03-pa-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 17 PA US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every incumbent / previously-seeded figure — after a MANDATORY per-candidate live-DB dedup check (D-03).

Purpose: USHC2-02 + USHC2-03 for PA. PA is all-partial-incumbent: 16 of 17 incumbents are renominated and reused as is_incumbent=true actives; only PA-3 (Evans retired → Chris Rabb) has a non-incumbent nominee. The ~20 new records are the challengers + Rabb.
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every PA House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates schema (verified in 150-03):
     id uuid PK; race_id uuid NOT NULL FK->races; politician_id uuid (D-06: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL -> feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite source_url); external_id text optional. NO unique constraint -> idempotency app-enforced:
     check (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool.
     NO updated_at column (149-01 Rule-3 fix). -->
<!-- PA renominated incumbents (reuse incumbent_pid from 154-incumbent-map.csv): PA-1..2, PA-4..17 (all except PA-3).
     PA-8 incumbent "Robert P. Bresnahan, Jr." (ac16b65b…/-42008) IS renominated despite the 154-field-table.csv comma
     artifact; challenger Paige Cognetti (D) is the new record. -->
<!-- PA-3 retired: Dwight Evans (3c962502…/-42003) keeps record, NO active row; Chris Rabb (D) is the active nominee
     (per 154 field, currently listed unopposed -> assert >=1 active for PA-3, re-confirm certified general). -->
<!-- external_id band for new PA challengers (D-06): -(42*10000 + cd*100 + seq) -> e.g. PA-1 seq1 = -420101.
     Verify the -420000..-429999 band is collision-free vs live external_id<0 before insert. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation — confirm reuse-vs-new per PA candidate + re-confirm certified generals (D-03 mandatory)</name>
  <read_first>
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (PA rows geo_id 42NN: general_candidates, new_records_needed, nominee_status, incumbent_pid; NOTE the PA-8 "Robert P. Bresnahan, Jr." comma artifact — relax-parse, don't trust raw column splits)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (PA geo_id → incumbent_pid + external_id, all 17 incumbents)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-PLAN.md (the reuse-vs-new reconciliation pattern + external_id-band collision check + lost-incumbent absence + live-name-query dedup mechanics)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md (the NY all-partial-incumbent reuse pattern — PA mirrors NY, not TX)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md (D-03 dedup + certified-general re-confirm; D-04 PA-3 Evans no-active-row; PA fips=42)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-pa-2026-house/155-03-pa-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) — one row per general candidate across all 17 PA districts (~35 rows). Resolve race_id by joining the 155-01 PA races to districts.geo_id.

    Re-confirm certified Nov-3 general nominees per district from the PA Department of State certified ballot list (D-03) before deciding rows — the 154 general_candidates column reflects the field as of diagnostic time; seed only ballot-qualified general candidates. Parse the party in parens for context only, NEVER store it (D-06).

    For EACH candidate:
    1. If the name matches the district's renominated PA incumbent (PA-1..2, PA-4..17) → REUSE, target = the 154 incumbent_pid, is_incumbent=true. PA-8 Bresnahan IS renominated (comma artifact) — REUSE -42008.
    2. PA-3 Evans → NO row (retired); Chris Rabb (D) → run a LIVE name query against essentials.politicians; if a real matching record exists → REUSE that UUID, else NEW.
    3. For every challenger / new name → run a LIVE name query (full_name ILIKE + sanity on external_id/office context). If a real matching record exists → flip to REUSE with that UUID. If 0 matches → decision=NEW, assign a collision-free negative external_id.
    4. Confirm PA-3 Evans (-42003) appears 0 times as an active candidate.

    For new external_ids: query SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id. PA incumbents occupy -42001..-42017. Use the scheme -(42*10000 + cd*100 + seq) (e.g. PA-1 seq1 = -420101) and VERIFY the -420000..-429999 band (excluding the 17 incumbent ids) is empty before use; if any collision, pick another verified-empty band and document it (149-01 collision lesson).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-pa-2026-house/155-03-pa-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<30){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; console.log('PA reconciliation OK rows',body.length,'new',newids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 155-03-pa-reconciliation.csv has one row per PA general candidate across 17 districts (~35 rows), each with a resolved race_id, re-confirmed against the PA certified ballot list.
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred); all 16 renominated incumbents REUSE their 154 incumbent_pid; PA-8 Bresnahan REUSE -42008.
    - PA-3 Evans (-42003) has NO REUSE row in PA-3; Chris Rabb is the active candidate (REUSE-if-exists else NEW, live-confirmed).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live external_id<0 set; band verified empty.
  </acceptance_criteria>
  <done>pa-reconciliation.csv exists, all REUSE UUIDs live-confirmed, certified generals re-confirmed, PA-3 Evans absent, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent PA House seed migration (new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-pa-2026-house/155-03-pa-reconciliation.csv (Task 1 output — authoritative reuse/new + external_id + race_id map)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-PLAN.md (Task 2: the validated idempotent BEGIN/COMMIT seed-migration mechanics — INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at; no challenger offices)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-01-SUMMARY.md (the PA election UUID — confirm races resolve to it)
    - .planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-CONTEXT.md (D-06 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card)
  </read_first>
  <action>
    Pick the next free migration number (list backend/migrations, increment highest on disk). Name `NNNN_seed_pa_2026_house_candidates.sql`. All writes in one BEGIN; … COMMIT;. Header cites the field source (154-FIELD-TABLE.md PA section + per-district Wikipedia URLs) + the antipartisan invariant (D-06).

    Migration body, in order (per the 150-03 Task-2 mechanics):
    1. INSERT the new PA politicians (NOT EXISTS on external_id), canonical accents preserved, is_active=true. NO essentials.offices rows for challengers (a challenger office would pollute the reps feed / geofence search — 149-01 A2).
    2. INSERT the race_candidates onto the 17 PA races: for each reconciliation row, race_id=resolved PA race, politician_id=REUSE target or just-inserted new UUID (resolve new via SELECT id … WHERE external_id=…), full_name/first_name/last_name, is_incumbent per CSV, candidate_status='active', source=district source_url. Guard each with NOT EXISTS on (race_id, full_name).
    3. NEVER touch the races rows (155-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate.

    Post-COMMIT verification block (or run separately), scoped to PA NATIONAL_LOWER (geo_id prefix '42') within the PA 2026 Statewide General election: every PA race has ≥1 active candidate (most ≥2); 0 NULL politician_id; 0 duplicate full_name within PA; PA-3 Evans (-42003) has 0 active rows; all 16 renominated incumbents present as is_incumbent=true.

    Run dry first (review SQL), then apply against prod via psql -v ON_ERROR_STOP=1. Do not inline the SQL here — author per the 150-03 mechanics.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='PA 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='42'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='PA 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='42' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==17||+row.nullpid>0||+dup.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n);process.exit(1)}; console.log('PA OK: 17 races,',row.active_rc,'active rc, 0 null pid, 0 dup full_name'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 17 PA House races (NATIONAL_LOWER geo '42' within the PA 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within PA (D-03 zero-dup-per-state).
    - PA-3 Evans has no active row; all 16 renominated incumbents present as is_incumbent=true; Chris Rabb active in PA-3.
    - No new essentials.offices rows created; no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; PA 17-race assertion passes; dedup correct; idempotent on re-run. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes new PA politicians + race_candidates to live prod |
| 154 field artifacts → seed input | Field/identity locked + human-reviewed in Phase 154; reuse-vs-new + certified-general re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-09 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Live name-check per candidate; reuse 154 incumbent_pid; 0-dup-full_name-per-state gate |
| T-155-10 | Repudiation | retired PA-3 Evans surfaced as active candidate | mitigate | Evans gets NO active row (reconciliation); 155-verify.sql D-04 absence assertion |
| T-155-11 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-155-12 | DoS | write to wrong election / cross-state contamination | mitigate | All inserts scoped to PA 2026 Statewide General + NATIONAL_LOWER geo '42' |
| T-155-13 | Tampering | seeding the IL-style primary field instead of certified general | mitigate | D-03 certified-general re-confirm from PA SoS certified ballot list before insert |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 155-verify.sql USHC2-02/03 + D-03/D-04 PA assertions pass after this runs.
- Manual spot-check: cross-check ~5 PA districts against 154-FIELD-TABLE.md source_url citations.
</verification>

<success_criteria>
- 17 PA House races each carry their active field, all politician_id non-null; ~20 new politician rows; every incumbent/previously-seeded figure reused by id (0 duplicates); PA-3 Evans absent + Rabb active; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/155-pa-il-candidate-seeding-create-elections-races-then-candidat/155-03-SUMMARY.md` when done (record: migration number, final NEW count, the new-candidate external_id list for downstream headshot/stance plans, any 154-name flips/certified-general changes, PA-3 Evans absence confirmation).
</output>
