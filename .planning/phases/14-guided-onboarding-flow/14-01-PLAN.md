---
phase: 14-guided-onboarding-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/pages/Library.jsx
  - CompassV2/src/pages/Quiz.jsx
  - CompassV2/src/pages/BuildCompass.jsx
autonomous: true
requirements: [LIBR-02]

must_haves:
  truths:
    - "Library page has no 'Start Quiz' fixed bottom button"
    - "'Quiz' language replaced with 'Calibrate' across Library, Quiz page, and BuildCompass"
    - "Full Quiz CTA on Library page reads 'Take the Full Calibration' (or similar calibrate-branded text)"
    - "Quiz page exit/finish navigation still works correctly"
  artifacts:
    - path: "CompassV2/src/pages/Library.jsx"
      provides: "Library without Start Quiz button, calibrate branding"
      contains: "calibrat"
    - path: "CompassV2/src/pages/Quiz.jsx"
      provides: "Quiz page with calibrate branding"
      contains: "calibrat"
  key_links:
    - from: "CompassV2/src/pages/Library.jsx"
      to: "/quiz route"
      via: "navigate('/quiz')"
      pattern: "navigate.*quiz"
---

<objective>
Remove the "Start Quiz" fixed bottom button from the Library page and rebrand all "quiz" language to "calibrate" throughout the app.

Purpose: The calibration overlay on the compass page replaces the "Start Quiz" entry point (LIBR-02). Rebranding "quiz" to "calibrate" aligns with the new onboarding language per locked decisions.
Output: Library.jsx without the Start Quiz button, and Quiz/Library/BuildCompass with "calibrate" branding.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CompassV2/src/pages/Library.jsx
@CompassV2/src/pages/Quiz.jsx
@CompassV2/src/pages/BuildCompass.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Start Quiz button from Library</name>
  <files>CompassV2/src/pages/Library.jsx</files>
  <action>
Remove the "Start Quiz" fixed bottom button and its spacer from Library.jsx:

1. Delete the `{/* Bottom spacer for fixed button */}` div (`<div className="h-24" />`) near line ~703
2. Delete the entire `{/* Start Quiz button (curated flow) */}` block (the fixed bottom `<div>` with the "Start Quiz" button) near lines ~706-719

The bottom nav already provides navigation, and the calibration overlay on the compass page will serve as the new entry point.

Also rebrand the "Full Quiz CTA" section in Library.jsx:
- Change "Take the Full Quiz" to "Take the Full Calibration"
- Change "Answer every question, then choose which topics appear on your compass" — keep as-is (it's descriptive and still accurate)
- Change the navigate target from `/quiz?mode=full` to `/quiz?mode=full` (keep the route the same; the Quiz page itself will be rebranded)
  </action>
  <verify>Open Library.jsx and confirm: no "Start Quiz" button at bottom, no h-24 spacer div, "Full Quiz" text replaced with "Full Calibration"</verify>
  <done>"Start Quiz" fixed bottom button is gone from Library; "Full Quiz" CTA rebranded to "Full Calibration"</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand quiz to calibrate across Quiz and BuildCompass pages</name>
  <files>CompassV2/src/pages/Quiz.jsx, CompassV2/src/pages/BuildCompass.jsx</files>
  <action>
Rebrand "quiz" language to "calibrate" in user-facing text across Quiz.jsx and BuildCompass.jsx:

**Quiz.jsx:**
- Change the loading state text "Loading quiz..." to "Loading calibration..."
- Change the exit button aria-label from "Exit quiz" to "Exit calibration" (appears in both full and curated mode layouts)
- The `mode` query param and internal variable name `mode` stay as-is (implementation detail, not user-facing)
- The file export name `Quiz` and route `/quiz` stay as-is — renaming routes would break bookmarks and is unnecessary

**BuildCompass.jsx:**
- Change heading "Build Your Compass" — keep as-is (this is compass language, not quiz language)
- No other quiz language exists in BuildCompass, so no changes needed there

**Library.jsx (additional):**
- In the Full Quiz CTA section, change the `<p>` text from "Take the Full Quiz" to "Take the Full Calibration"
- Change the sub-text "Answer every question, then choose which topics appear on your compass" — keep as-is (descriptive, not quiz-branded)
  </action>
  <verify>Grep for case-insensitive "quiz" in Library.jsx, Quiz.jsx, BuildCompass.jsx — only internal variable names and route paths should remain, no user-facing "quiz" text</verify>
  <done>All user-facing "quiz" text replaced with "calibrate/calibration" equivalents; route paths and variable names preserved</done>
</task>

</tasks>

<verification>
1. `grep -i "start quiz" CompassV2/src/pages/Library.jsx` returns no results
2. `grep -i '".*quiz.*"' CompassV2/src/pages/Library.jsx` shows only route paths like `/quiz`, not user-facing text
3. `grep -i "Exit quiz" CompassV2/src/pages/Quiz.jsx` returns no results
4. `grep -i "Loading quiz" CompassV2/src/pages/Quiz.jsx` returns no results
5. The Library page renders without the fixed bottom button
</verification>

<success_criteria>
- Library page has no "Start Quiz" fixed bottom button or its spacer
- All user-facing "quiz" text replaced with "calibrate" equivalents across Library, Quiz, and BuildCompass
- Route paths (/quiz) and internal variable names preserved unchanged
- Navigation still works: Library Full Calibration CTA navigates to /quiz?mode=full
</success_criteria>

<output>
After completion, create `.planning/phases/14-guided-onboarding-flow/14-01-SUMMARY.md`
</output>
