---
phase: 133-ga-mi-house-rep-stances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-19-ga-house-batch-a.csv
  - backend/data/stance-research/ga-house-a/_TOPIC_SCALE.txt
  - backend/data/stance-research/ga-house-a/_merge.ts
  - backend/data/stance-research/ga-house-a/_push.ts
autonomous: false
requirements: [USHS-07]

must_haves:
  truths:
    - "Each of the 7 GA House reps in batch A (external_id -13001..-13007) has >=1 row in inform.politician_answers OR a documented honest-skip"
    - "Every politician_answers row written has a paired inform.politician_context row with a non-empty sources array of REAL fetched URLs"
    - "No stance inferred from party affiliation — every value matches the exact stance text and traces to a fetched URL"
    - "No row sourced ONLY by isidewith.com (below evidence bar) is kept"
    - "Live topics fetched fresh from inform.compass_topics; city + judicial-* topics skipped (25 federal topics in scope)"
    - "Stance counts for all politicians OTHER than the 7 batch-A reps are unchanged"
  artifacts:
    - path: "backend/data/stance-research/2026-06-19-ga-house-batch-a.csv"
      provides: "Reviewable stance research CSV for GA House reps batch A (7 reps, GA-1..GA-7)"
      contains: "topic_key,value,reasoning,source_url"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context"
      via: "politician_id + topic_id composite key"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE on both tables"
---

<objective>
Research and ingest sourced compass stances for the first 7 Georgia US House reps
(GA-1..GA-7, external_id -13001..-13007), using the research-stances skill, following the
validated Phase 127-132 playbook. Concurrency cap = 3 (validated on premium tier). WebFetch-only.

Purpose: Second wave of v2.17 (Tier 2 continuation), opening USHS-07 (GA + MI). Each rep
currently has 0 stances; pure scale-out of the v2.16 pipeline — no new architecture.
Output: CSV of record committed; stances + context + quotes pushed to production.
</objective>

<execution_context>
@C:\EV-Accounts\.planning\REQUIREMENTS.md
@C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
@C:\EV-Accounts\.planning\phases\132-oh-nc-house-rep-stances\132-04-SUMMARY.md
</execution_context>

<context>
Production DB = Supabase project kxsdzaojfaibhuzmclfq. In-scope reps (GA House, batch A):

| external_id | name | district |
|-------------|------|----------|
| -13001 | Earl Carter | GA-1 |
| -13002 | Sanford Bishop | GA-2 |
| -13003 | Brian Jack | GA-3 |
| -13004 | Henry Johnson | GA-4 |
| -13005 | Nikema Williams | GA-5 |
| -13006 | Lucy McBath | GA-6 |
| -13007 | Rich McCormick | GA-7 |

All currently have 0 stances. Resolve each politician_id by external_id->UUID at run time
(names above are for source-finding only).

KEY RULES (validated Phase 127-132 playbook):
- **25 federal topics in scope** = 44 live topics minus 11 city-level + all judicial-* topics.
  Fetch live topics FRESH; write the formatted scale to
  `backend/data/stance-research/ga-house-a/_TOPIC_SCALE.txt`; each agent Reads it.
- **Concurrency: up to 3 agents at a time** (validated premium tier). WebFetch ONLY — never
  WebSearch or Playwright (shared rate-limited quota).
- **Per-rep output files -> merge + RFC-4180-validate** (csv-parse/sync) into the batch CSV.
- **ADD the explicit RFC-4180 CSV-escaping instruction to every agent prompt.** Apply the canonical
  re-parse(relax_column_count)/re-stringify repair before merge. CSV quote-repair: collapse
  `""""`->`"""` ONLY (4->3); NEVER `"""`->`""` (3->2) — agents emit valid `"""quote"""` and the
  3->2 collapse corrupts it. Use `replace(/"""""+/g,'"""').replace(/""""/g,'"""')` + csv-parse
  `relax_quotes` (see nc-house-b/_merge.ts).
- **isidewith.com is BELOW the evidence bar** — DROP any row sourced ONLY by isidewith (no
  ballotpedia/wikipedia/ontheissues/lcv/votesmart/.gov backing); thin reps become honest-partials.
- **Resolve politician_id by external_id->UUID map**, not by name.
- **Five-chairs framing**: match documented record to exact stance text; never infer from party;
  honest-skip topics with no evidence. house.gov/congress.gov/govtrack/clerk.house.gov 403 to
  WebFetch — lean on Ballotpedia, OnTheIssues (GA/ pages), Wikipedia, LCV scorecard.
</context>

<task id="1" type="execute">
<title>Fetch live topics, then research batch A (3 concurrent, WebFetch-only)</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md
- C:\EV-Accounts\.planning\phases\132-oh-nc-house-rep-stances\132-04-SUMMARY.md (latest validated playbook + isidewith/CSV-escaping lessons)
- backend/data/stance-research/nc-house-b/_merge.ts (latest merge+repair: 4->3 collapse, relax_quotes)
- backend/data/stance-research/pa-house-a/_TOPIC_SCALE.txt (scale-file format example)
</read_first>
<action>
1. Run the SKILL's topic-resolution query fresh against production (kxsdzaojfaibhuzmclfq); drop the 11 city-level topics + all judicial-* topics so 25 federal topics remain. Write the formatted scale (topic_key, question_text, and the five value->text lines per topic) to `backend/data/stance-research/ga-house-a/_TOPIC_SCALE.txt`.
2. Dispatch `politician-stance-researcher` agents for the 7 batch-A reps (external_id -13001..-13007), up to 3 concurrent. Each prompt: five-chairs framing, instruct the agent to Read `_TOPIC_SCALE.txt`, WebFetch ONLY, explicit RFC-4180 escaping rule, drop isidewith-only rows, write to `backend/data/stance-research/ga-house-a/<surname>.csv`. Include the external_id in each agent's row output so the push resolves by external_id->UUID.
3. Verify each agent's rows before dispatching the next wave; retry empty-output agents solo.
4. Merge the 7 per-rep CSVs into `backend/data/stance-research/2026-06-19-ga-house-batch-a.csv` with a real RFC-4180 parser (csv-parse/sync) — copy/adapt `nc-house-b/_merge.ts`; run the canonical re-parse/re-stringify repair (4->3 collapse only); validate every row (valid topic_key, value 1-5, non-empty reasoning, >=1 http source).
</action>
<acceptance_criteria>
- 2026-06-19-ga-house-batch-a.csv exists with rows for the 7 reps (or documented honest-skips); merge parser reports 0 problems
- Every row has value (1-5) + reasoning + >=1 real http source_url; topic_key is one of the 25 fetched federal topics; no isidewith-only rows
- Each of the 7 reps (GA-1..GA-7) accounted for: stances OR an honest-skip noted (no party-inferred values)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Review + push batch A to production</title>
<read_first>
- C:\EV-Accounts\.claude\skills\research-stances\SKILL.md (STEP 3 approval, STEP 4 DB push)
- backend/data/stance-research/pa-house-a/_push.ts (reusable external_id-keyed push)
- backend/data/stance-research/2026-06-19-ga-house-batch-a.csv
</read_first>
<action>
Present the STEP 3 approval summary (per-rep topics found/skipped, stance overview, Read & Rank quote table). On approval, push via the external_id-keyed `_push.ts` pattern: copy/adapt `backend/data/stance-research/pa-house-a/_push.ts` to `backend/data/stance-research/ga-house-a/_push.ts`, resolving politician_id by external_id->UUID + topic_id by topic_key, upserting inform.politician_answers + inform.politician_context (ON CONFLICT (politician_id, topic_id) DO UPDATE on both), pushing quotes to essentials.quotes with Read & Rank selection + suffix-aware surname leak-check. Commit the CSV (force-add past the stance-research gitignore).
</action>
<acceptance_criteria>
- Each batch-A rep has >=1 inform.politician_answers row OR a documented honest-skip
- Every new answer has a paired sourced context row (0 unsourced)
- Push reported counts (answers/contexts/quotesIns/selected/leaks) with zero rollbacks; stance counts for non-batch-A politicians unchanged
- CSV committed (force-added past gitignore)
</acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agent -> CSV | untrusted web content lands in a reviewable file (human STEP 3 gate before DB) |
| _push.ts -> production DB | service-role write to inform.politician_answers/context + essentials.quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | _push.ts service-role upsert | mitigate | idempotent ON CONFLICT (politician_id, topic_id) DO UPDATE; re-runs are safe; single BEGIN/COMMIT with ROLLBACK on error |
| T-133-02 | Information disclosure | essentials.quotes Read & Rank pick | mitigate | suffix-aware surname leak-check; de-id-less or surname-leaking quotes never selected |
| T-133-03 | Integrity (scope creep) | non-in-scope reps | mitigate | push keyed strictly to batch-A external_ids; invariant: stance counts for all other politicians unchanged |

No high-severity threats: read-only web research + an idempotent service-role upsert. No package installs.
</threat_model>

<verification>
- 7 batch-A GA reps each have sourced stances or a documented skip; every answer has paired sourced context
- No party-inferred stances; no isidewith-only rows; live topics fresh; city + judicial-* skipped (25 federal topics); concurrency held at 3
- Non-batch-A politician stance counts unchanged
</verification>

<success_criteria>
USHS-07 advanced: GA-1..GA-7 (7 reps) covered with sourced stances or honest-skips, all answers paired with sourced context, CSV committed.
</success_criteria>

<output>
Create `.planning/phases/133-ga-mi-house-rep-stances/133-01-SUMMARY.md` when done.
</output>
