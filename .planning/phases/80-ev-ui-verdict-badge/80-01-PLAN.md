---
phase: 80-ev-ui-verdict-badge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ev-ui/src/StanceAccordion.jsx
  - ev-ui/src/Favicon.jsx
  - ev-ui/src/index.js
  - ev-ui/package.json
  - essentials/src/components/CompassCard.jsx
autonomous: true
requirements:
  - PROF-03

must_haves:
  truths:
    - "ev-ui v0.1.42 is published to the GitHub npm registry with StanceAccordion exported"
    - "When verdictsByTopic is passed with a matching topic ID, an agree or disagree badge renders inline in that topic's row"
    - "When verdictsByTopic is omitted entirely, all topic rows render identically to the current behavior in essentials"
    - "essentials CompassCard imports StanceAccordion from @chrisandrewsedu/ev-ui and passes apiUrl prop"
  artifacts:
    - path: "ev-ui/src/StanceAccordion.jsx"
      provides: "StanceAccordion component with verdictsByTopic prop and inline verdict badges"
      contains: "verdictsByTopic"
    - path: "ev-ui/src/Favicon.jsx"
      provides: "Favicon sub-component (private to ev-ui)"
    - path: "ev-ui/src/index.js"
      provides: "StanceAccordion export"
      contains: "export.*StanceAccordion"
    - path: "ev-ui/package.json"
      provides: "Version 0.1.42"
      contains: '"version": "0.1.42"'
    - path: "essentials/src/components/CompassCard.jsx"
      provides: "Updated caller importing from ev-ui"
      contains: "@chrisandrewsedu/ev-ui"
  key_links:
    - from: "essentials/src/components/CompassCard.jsx"
      to: "ev-ui dist (published)"
      via: "import { StanceAccordion } from '@chrisandrewsedu/ev-ui'"
      pattern: "from '@chrisandrewsedu/ev-ui'"
    - from: "ev-ui/src/StanceAccordion.jsx"
      to: "API (compass context endpoint)"
      via: "apiUrl prop passed by caller"
      pattern: "apiUrl"
---

<objective>
Migrate StanceAccordion from the essentials app into the ev-ui shared component library, add a `verdictsByTopic` prop that renders inline agree/disagree badges per topic row, publish ev-ui v0.1.42, and update the essentials CompassCard caller to import from the library.

Purpose: Phase 81 (Profile Integration) requires StanceAccordion to be in ev-ui with the verdictsByTopic prop so it can receive verdict data from CompassContext cleanly. This plan creates that contract and publishes it.
Output: ev-ui v0.1.42 on GitHub npm registry; essentials CompassCard importing from ev-ui.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

Current ev-ui/src/index.js exports (add StanceAccordion after existing entries):
```js
export { default as RadarChartCore } from "./RadarChartCore.jsx";
export { default as Header } from "./Header.jsx";
export { default as SiteHeader, defaultNavItems, defaultCtaButton } from "./SiteHeader.jsx";
export { default as FilterSidebar } from "./FilterSidebar.jsx";
export { default as PoliticianCard } from "./PoliticianCard.jsx";
export { default as CategorySection } from "./CategorySection.jsx";
export { default as SocialLinks } from "./SocialLinks.jsx";
export { default as IssueTags } from "./IssueTags.jsx";
export { default as CommitteeTable } from "./CommitteeTable.jsx";
export { default as PoliticianProfile } from "./PoliticianProfile.jsx";
export { default as LegislativeInlineSummary } from "./LegislativeInlineSummary.jsx";
export { default as LegislativeRecord } from "./LegislativeRecord.jsx";
export { default as AuthForm } from "./AuthForm.jsx";
export { default as useMediaQuery } from "./useMediaQuery.js";
export * from "./tokens.js";
```

Current essentials CompassCard caller signature (lines 313-319 and 396-403):
```jsx
<StanceAccordion
  topics={topicsFiltered.length > 0 ? topicsFiltered : allPolTopics}
  polAnswers={polAnswers}
  politicianId={politicianId}
  allTopics={allTopics}
  expandedTopics={topicsFiltered.length > 0 ? allPolTopics : undefined}
/>
```

Target ev-ui StanceAccordion prop signature (Phase 81 will add verdictsByTopic; Phase 80 makes it available):
```jsx
export default function StanceAccordion({
  topics,
  polAnswers,
  politicianId,
  allTopics,
  expandedTopics,
  verdictsByTopic,   // Record<string, 'agreed' | 'disagreed'> | undefined
  apiUrl = 'https://api.empowered.vote'
}) { ... }
```

Verdict badge inline style constants (from Phase 78 locked design — cyan=agreed, amber=disagreed):
```jsx
// agreed pill
style={{ display: 'inline-flex', alignItems: 'center', gap: '4px',
  backgroundColor: '#ecfeff', color: '#0e7490',
  padding: '2px 8px', borderRadius: '9999px',
  fontSize: '11px', fontWeight: 500, marginLeft: '6px', flexShrink: 0 }}

// disagreed pill
style={{ display: 'inline-flex', alignItems: 'center', gap: '4px',
  backgroundColor: '#fffbeb', color: '#b45309',
  padding: '2px 8px', borderRadius: '9999px',
  fontSize: '11px', fontWeight: 500, marginLeft: '6px', flexShrink: 0 }}
```

Checkmark SVG (agreed):
```jsx
<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round">
  <path d="M5 13l4 4L19 7" />
</svg>
```

X SVG (disagreed):
```jsx
<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round">
  <path d="M6 18L18 6M6 6l12 12" />
</svg>
```

Favicon.jsx (copy verbatim from essentials/src/components/Favicon.jsx — no changes needed):
- Pure React component, no deps, uses Google favicon service
- Keep as private impl detail — do NOT export from ev-ui index.js
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create StanceAccordion.jsx and Favicon.jsx in ev-ui</name>
  <files>ev-ui/src/StanceAccordion.jsx, ev-ui/src/Favicon.jsx</files>
  <action>
Copy essentials/src/components/Favicon.jsx to ev-ui/src/Favicon.jsx verbatim — no changes needed.

Create ev-ui/src/StanceAccordion.jsx by adapting essentials/src/components/StanceAccordion.jsx with these three changes only:

1. Replace the VITE env var reference:
   - Remove: `const API = import.meta.env.VITE_API_URL || '/api';`
   - Add prop: `apiUrl = 'https://api.empowered.vote'` in the destructured props
   - Replace all `${API}/` references with `${apiUrl}/`

2. Add verdictsByTopic to the destructured props (after expandedTopics):
   ```
   verdictsByTopic,  // Record<string, 'agreed' | 'disagreed'> | undefined
   ```
   Inside the map over visibleTopics, after computing `topicId`, add:
   ```jsx
   const verdict = verdictsByTopic ? verdictsByTopic[topicId] : undefined;
   ```

3. Render verdict badge inline in the collapsed row button, after the `<span className="text-xs text-neutral-500 mt-0.5">{label}</span>` and before the closing `</div>` of the left flex column. Use inline styles (not Tailwind) for the badges per the interface context above — this ensures no Tailwind dependency in ev-ui:
   ```jsx
   {verdict === 'agreed' && (
     <span style={{ display: 'inline-flex', alignItems: 'center', gap: '4px',
       backgroundColor: '#ecfeff', color: '#0e7490', padding: '2px 8px',
       borderRadius: '9999px', fontSize: '11px', fontWeight: 500,
       marginTop: '3px', flexShrink: 0, alignSelf: 'flex-start' }}>
       <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round">
         <path d="M5 13l4 4L19 7" />
       </svg>
       Agreed
     </span>
   )}
   {verdict === 'disagreed' && (
     <span style={{ display: 'inline-flex', alignItems: 'center', gap: '4px',
       backgroundColor: '#fffbeb', color: '#b45309', padding: '2px 8px',
       borderRadius: '9999px', fontSize: '11px', fontWeight: 500,
       marginTop: '3px', flexShrink: 0, alignSelf: 'flex-start' }}>
       <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round">
         <path d="M6 18L18 6M6 6l12 12" />
       </svg>
       Disagreed
     </span>
   )}
   ```

Do NOT use Tailwind class names for the badge spans — use inline styles only. The existing Tailwind classes already in the component body (e.g., on the button, divs, spans) can remain as-is; tsup bundles them as plain strings and the essentials consumer provides the Tailwind runtime.

Import path for Favicon stays `import Favicon from './Favicon';` — works because both files are in ev-ui/src/.
  </action>
  <verify>
    <automated>grep -n 'verdictsByTopic\|apiUrl\|VITE_API_URL' /Users/chrisandrews/Documents/GitHub/ev-ui/src/StanceAccordion.jsx</automated>
  </verify>
  <done>ev-ui/src/StanceAccordion.jsx exists with verdictsByTopic and apiUrl in the signature; VITE_API_URL is absent. ev-ui/src/Favicon.jsx exists.</done>
</task>

<task type="auto">
  <name>Task 2: Export StanceAccordion from ev-ui index.js, bump version to 0.1.42, build and publish</name>
  <files>ev-ui/src/index.js, ev-ui/package.json</files>
  <action>
Step 1 — Add export to ev-ui/src/index.js. Append after the last existing export line:
```js
export { default as StanceAccordion } from "./StanceAccordion.jsx";
```
Do NOT export Favicon — it is a private implementation detail.

Step 2 — Bump version in ev-ui/package.json: change `"version": "0.1.41"` to `"version": "0.1.42"`. No other changes to package.json.

Step 3 — Build and publish:
```bash
cd /Users/chrisandrews/Documents/GitHub/ev-ui && npm run build
npm publish
```

The global ~/.npmrc has the GitHub npm token. Publish target is https://npm.pkg.github.com. If publish fails with "cannot publish over the existing version", the version was not bumped — check package.json and retry.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/ev-ui && npm run build && echo "BUILD_OK" && npm view @chrisandrewsedu/ev-ui version</automated>
  </verify>
  <done>npm run build exits 0. `npm view @chrisandrewsedu/ev-ui version` returns `0.1.42`.</done>
</task>

<task type="auto">
  <name>Task 3: Update essentials CompassCard to import StanceAccordion from ev-ui</name>
  <files>essentials/src/components/CompassCard.jsx</files>
  <action>
In essentials/src/components/CompassCard.jsx:

1. Change the import on line 6:
   - Remove: `import StanceAccordion from './StanceAccordion';`
   - Add to the existing ev-ui import on line 3:
     ```js
     import { RadarChartCore, StanceAccordion } from '@chrisandrewsedu/ev-ui';
     ```

2. Add `apiUrl={import.meta.env.VITE_API_URL}` to all StanceAccordion usages. There are two call sites (lines ~313-319 and ~396-403). Update both:
   ```jsx
   <StanceAccordion
     topics={...}
     polAnswers={polAnswers}
     politicianId={politicianId}
     allTopics={allTopics}
     expandedTopics={...}
     apiUrl={import.meta.env.VITE_API_URL}
   />
   ```
   Leave verdictsByTopic absent from the call sites — Phase 81 wires that prop from CompassContext.

3. Delete the local source files that are now provided by ev-ui:
   - Delete essentials/src/components/StanceAccordion.jsx
   - Delete essentials/src/components/Favicon.jsx

4. Run npm install in essentials to pull the published ev-ui 0.1.42:
   ```bash
   cd /Users/chrisandrews/Documents/GitHub/essentials && npm install @chrisandrewsedu/ev-ui@0.1.42
   ```

5. Verify the essentials dev build compiles without errors:
   ```bash
   cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build
   ```
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>essentials npm run build exits 0. CompassCard.jsx imports StanceAccordion from @chrisandrewsedu/ev-ui. Local StanceAccordion.jsx and Favicon.jsx are deleted from essentials/src/components/.</done>
</task>

</tasks>

<verification>
1. `grep -n 'verdictsByTopic\|apiUrl' /Users/chrisandrews/Documents/GitHub/ev-ui/src/StanceAccordion.jsx` — both props present
2. `npm view @chrisandrewsedu/ev-ui version` — returns `0.1.42`
3. `grep 'StanceAccordion' /Users/chrisandrews/Documents/GitHub/ev-ui/src/index.js` — export line present
4. `grep 'StanceAccordion' /Users/chrisandrews/Documents/GitHub/essentials/src/components/CompassCard.jsx` — shows import from @chrisandrewsedu/ev-ui
5. `ls /Users/chrisandrews/Documents/GitHub/essentials/src/components/StanceAccordion.jsx 2>&1` — file not found (deleted)
6. `cd /Users/chrisandrews/Documents/GitHub/essentials && npm run build` — exits 0
</verification>

<success_criteria>
- ev-ui v0.1.42 published to GitHub npm registry with StanceAccordion exported
- StanceAccordion accepts verdictsByTopic prop and renders cyan pill for 'agreed', amber pill for 'disagreed', nothing when prop is absent or topic ID not in map
- apiUrl prop replaces VITE_API_URL; defaults to 'https://api.empowered.vote'
- essentials imports StanceAccordion from @chrisandrewsedu/ev-ui; local copy deleted
- essentials npm run build exits 0 after the update
</success_criteria>

<output>
After completion, create `.planning/phases/80-ev-ui-verdict-badge/80-01-SUMMARY.md` following the summary template.
</output>
