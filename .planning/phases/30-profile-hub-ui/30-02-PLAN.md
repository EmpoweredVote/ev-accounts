---
phase: 30-profile-hub-ui
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - admin/src/pages/ProfilePage.tsx
autonomous: false

must_haves:
  truths:
    - "Profile page shows a feature hub with cards for CTC, VQ, Essentials, Read & Rank, and Treasury Tracker"
    - "Each card has a short description and a link"
    - "Cards communicate 'explore freely, connect to save' — user understands what is available without a Connected account"
  artifacts:
    - path: "admin/src/pages/ProfilePage.tsx"
      provides: "Feature hub card grid"
      contains: "feature-hub"
  key_links:
    - from: "admin/src/pages/ProfilePage.tsx"
      to: "external app URLs"
      via: "anchor tags with href"
      pattern: "href=.*https?://"
---

<objective>
Add a feature hub section to the profile page with cards for all five live Empowered Vote features (CTC, VQ, Essentials, Read & Rank, Treasury Tracker). Each card has a description, link, and messaging that communicates "explore freely, connect to save."

Purpose: PROFILE-03 (feature hub) and PROFILE-04 (explore freely messaging).
Output: Updated `ProfilePage.tsx` with feature hub grid below the profile card.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-profile-hub-ui/30-01-SUMMARY.md
@admin/src/pages/ProfilePage.tsx
@admin/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feature hub card grid</name>
  <files>admin/src/pages/ProfilePage.tsx</files>
  <action>
Add a feature hub section below the existing profile card (outside the white card div, still inside `max-w-lg mx-auto px-4`). Widen the container to `max-w-2xl` to accommodate the grid.

Feature data array (define as a const above the component or at file top):

```typescript
const FEATURES = [
  {
    name: 'Choose the Correct',
    abbr: 'CTC',
    description: 'Test your knowledge of where politicians really stand on the issues.',
    color: 'bg-ev-teal',
    href: 'https://choose-the-correct.empoweredvote.com',
  },
  {
    name: 'Validation Quests',
    abbr: 'VQ',
    description: 'Help verify politician stances and earn Red Gems for accuracy.',
    color: 'bg-ev-red',
    href: 'https://validation-quests.empoweredvote.com',
  },
  {
    name: 'Essentials',
    abbr: 'ESS',
    description: 'See where your representatives stand on the issues that matter to you.',
    color: 'bg-ev-yellow',
    href: 'https://essentials.empoweredvote.com',
  },
  {
    name: 'Read & Rank',
    abbr: 'R&R',
    description: 'Read real legislation and rank it — your opinion shapes the civic conversation.',
    color: 'bg-ev-teal-light',
    href: 'https://read-and-rank.empoweredvote.com',
  },
  {
    name: 'Treasury Tracker',
    abbr: 'TT',
    description: 'Follow the money — see how public funds are allocated and spent.',
    color: 'bg-gray-700',
    href: 'https://treasury-tracker.empoweredvote.com',
  },
] as const;
```

Note: These URLs are placeholders. Add a `{/* TODO: Replace with actual production URLs */}` comment above the array.

Feature hub section markup:
- Section heading: `text-lg font-semibold text-gray-900 mb-1` — "Empowered Vote Features"
- Subheading: `text-sm text-gray-500 mb-4` — "Explore freely. Connect to save your progress."
- Grid: `grid grid-cols-1 sm:grid-cols-2 gap-3`
- Each card: `<a>` tag wrapping the card (entire card is clickable), `target="_blank" rel="noopener noreferrer"`:
  - Container: `bg-white rounded-lg shadow-sm border border-gray-200 p-4 hover:shadow-md transition-shadow`
  - Top row: colored dot (`w-2.5 h-2.5 rounded-full {feature.color}`) + feature name in `text-sm font-semibold text-gray-900`
  - Description: `text-xs text-gray-500 mt-1.5 leading-relaxed`
  - Bottom: `text-xs text-ev-teal font-medium mt-2` — "Explore" with a right arrow character

The 5 cards in a 2-col grid will produce 3 rows (2+2+1). The last card spans normally (single card on left).

This section is visible to ALL tiers (Inform, Connected, Empowered) — everyone can explore features.
  </action>
  <verify>Run `cd admin && npx tsc --noEmit` — no type errors. Run `cd admin && npm run build` — succeeds. Visually confirm 5 cards render in grid layout.</verify>
  <done>Feature hub with 5 cards renders below profile card. Each card has name, description, colored indicator, and clickable link. "Explore freely, connect to save" messaging is present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete profile page redesign: stats from /account/me (tier, level, XP, gems, VR), location form, and feature hub with 5 app cards.</what-built>
  <how-to-verify>
    1. Log in at /login with a Connected account
    2. Navigate to /profile
    3. Confirm: tier badge, level, XP, gem balances (yellow/blue/red), and Verification Rating (X / 150) all display
    4. Enter an address in the location form, submit — confirm success message appears (or appropriate error if address invalid)
    5. Scroll down to feature hub — confirm 5 cards visible: CTC, VQ, Essentials, Read & Rank, Treasury Tracker
    6. Confirm "Explore freely. Connect to save your progress." subheading is present
    7. Click a card — confirm it opens in new tab
    8. (Optional) Log in as Inform user — confirm only email, tier badge, and feature hub show (no gems, VR, or location form)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd admin && npx tsc --noEmit` passes
2. `cd admin && npm run build` succeeds
3. Feature hub renders 5 cards in responsive grid
4. All tiers see the feature hub (not gated to Connected)
5. Cards are clickable links opening in new tab
</verification>

<success_criteria>
- PROFILE-03: Feature hub with CTC, VQ, Essentials, Read & Rank, Treasury Tracker cards — each with description and link
- PROFILE-04: "Explore freely, connect to save" messaging present; all tiers see the hub
- Visual quality approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/30-profile-hub-ui/30-02-SUMMARY.md`
</output>
