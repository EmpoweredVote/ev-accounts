---
context: phase
phase: 117-ma-city-official-stances
task: 0
total_tasks: 4
status: not_started
last_updated: 2026-06-12T18:50:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: ONE-AGENT-AT-A-TIME — Never dispatch more than 1 (max 2) politician-stance-researcher agents in parallel. Mass launches cause rate limit hits with empty output. Each agent must confirm its CSV before the next is dispatched.
- [ ] CONSTRAINT: NO-PARTY-INFERENCE — NEVER infer a stance from party affiliation. If no documentable evidence exists for a topic, honest-skip it (write zero rows for that topic). Every stance row must trace to a real fetched URL.
- [ ] CONSTRAINT: LIVE-MIGRATION-NUMBER — Do NOT use the migration number from STATE.md or planning docs (515 at plan time). Run `SELECT MAX(version) FROM supabase_migrations.schema_migrations` live before writing any migration file.
- [ ] CONSTRAINT: CHAIRS-CALIBRATION — Every stance value must be calibrated against the exact stance text for that Chair position (fetched fresh from DB). Never write "5=progressive" — direction varies by topic.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Party-inference stances | Assigning a stance value based on "Democrat would likely support X" — no evidence fetched | blocking | Human review gate at STEP 3 before any DB write; zero tolerance |
| Parallel agent launches | Dispatching 3+ stance agents simultaneously causes rate limit hits, empty CSV output | blocking | Serialize all agent dispatches; wait for CSV confirmation before next |
| Stale migration number | Using cached MAX(version) from planning docs instead of live DB query | blocking | Query `SELECT MAX(version) FROM supabase_migrations.schema_migrations` inline before each migration write |
| Hardcoded topic stances | Writing scale text like "1=strongly oppose, 5=strongly support" instead of fetching actual stance texts from DB | advisory | SKILL.md STEP 0 fetches live topic+stance JSON — follow it exactly |

<current_state>
Phase 117 planning is **complete** (committed f515adf3). No execution has started.

Three wave plans exist:
- `117-01-PLAN.md` — Wave 1: Boston (14 officials) + Cambridge (~15 officials)
- `117-02-PLAN.md` — Wave 2: Worcester (11) + Springfield (14)
- `117-03-PLAN.md` — Wave 3: Lowell (12) + Brockton (12) + Quincy (10)

Ready to begin Wave 1, Task 1.
</current_state>

<completed_work>
- Phase 117 CONTEXT.md created with all locked decisions (D-01 through D-15)
- All 3 wave PLAN.md files created and committed
- ROADMAP.md updated with Phase 117 row
</completed_work>

<remaining_work>
**Wave 1 (117-01-PLAN.md):**
- Task 1: Research Boston stances — Michelle Wu + 4 at-large councillors
- Task 2: Research Boston stances — 9 district councillors + write/apply Boston migration
- Task 3: Human checkpoint — verify Boston migration gate
- Task 4: Research and ingest Cambridge stances

**Wave 2 (117-02-PLAN.md):** Worcester + Springfield — not started
**Wave 3 (117-03-PLAN.md):** Lowell + Brockton + Quincy — not started
</remaining_work>

<decisions_made>
- D-01: Attempt ALL 44 compass topics per official (not just local-scope). User instruction: "try for all 44, even if we only land at 10-14 answered"
- D-02: Expected yield 10–14 stances per official. Honest-skip is correct behavior, not a gap.
- D-03: Chairs methodology — calibrate against exact stance text for the Chair position, not directional lean
- D-04: Every stance requires a real fetched source URL in inform.politician_context.sources[]
- D-05: NEVER infer from party affiliation — if no evidence, write zero rows
- D-06: ONE agent at a time — never >2 parallel (rate-limit protection)
- D-09: Each city gets its own migration. ON CONFLICT DO UPDATE for both politician_answers and politician_context
- D-11: Migration numbering — check MAX(version) live before each wave. MAX was 515 at plan time (June 12); subsequent stance migrations for MA House reps have incremented it — **must check live**
- D-12: Wave structure: W1=Boston+Cambridge, W2=Worcester+Springfield, W3=Lowell+Brockton+Quincy
</decisions_made>

<blockers>
None currently.
</blockers>

## Required Reading (in order)

1. `.claude/skills/research-stances/SKILL.md` — orchestrator protocol (STEP 0 topic fetch, Chairs methodology, STEP 3 approval gate, agent rate limiting). MUST read before dispatching any agent.
2. `backend/migrations/347_boston_government.sql` — Boston roster with external_ids (-2507000001 to -2507000014, Mayor Wu through District 9 Breadon)
3. `backend/migrations/159_cambridge_incumbents.sql` — Cambridge roster (9 councillors + Mayor Siddiqui + City Manager Yi-An Huang + 6 School Committee)
4. `backend/migrations/515_richard_wells_stances.sql` — most recent stance migration (pattern reference for BEGIN/COMMIT, ON CONFLICT, verification comment block)
5. `.planning/phases/117-ma-city-official-stances/117-01-PLAN.md` — Wave 1 task details, interfaces, verification queries

## Critical Anti-Patterns (do NOT repeat these)

- PARTY-INFERENCE: Never assign a stance from "Democrat/Republican would likely..." → structural mitigation: human approval gate at STEP 3
- PARALLEL-AGENTS: Never launch 3+ agents simultaneously → serialize; each agent confirms CSV before next dispatches
- STALE-MIGRATION-NUM: Don't use 515 from planning notes → run `SELECT MAX(version) FROM supabase_migrations.schema_migrations` live

## Infrastructure State

- **DB migration MAX at plan time**: 515 (`515_richard_wells_stances.sql`)
- **Migration numbering note**: MA House/Senate stances (migrations 372–517) were applied during v2.10/v2.11. Several more may have been applied since; always query live.
- **Boston external_id range**: -2507000001 (Wu) through -2507000014 (Breadon D9)
- **Cambridge**: query live via `WHERE g.city = 'Cambridge' AND g.state = 'MA'`
- **Supabase MCP**: available for `execute_sql` to apply migrations and run verification queries

<context>
Phase 117 is stances-only — all politician records, governments, chambers, districts, and geofences for all 7 MA cities are already in the DB. The only work is researching and ingesting stance rows.

The user's explicit instruction: "try for all 44 questions, even if we only land at 10-14 answered. (Use chairs and sources, no guessing)."

Boston Mayor Michelle Wu is the highest-profile official — her positions on housing, climate, and local policy are extensively documented. Start with her, then the 4 at-large councillors, then the 9 district councillors. Use Boston Globe, WBUR, boston.gov official pages, BostonIndicators.org, and official social media as primary sources.
</context>

<next_action>
Start with: Read `.claude/skills/research-stances/SKILL.md` in full. Then fetch live topics from DB (SKILL.md STEP 0 query). Then confirm scope with user (5 officials × 44 topics = 220 assessments for Task 1). Then dispatch the first politician-stance-researcher agent for Michelle Wu.
</next_action>
