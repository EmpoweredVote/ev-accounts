---
phase: 40-frontend-auth-updates
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - "[essentials] src/lib/auth.js (NEW)"
  - "[essentials] src/lib/api.jsx"
  - "[essentials] src/lib/compass.js"
  - "[essentials] src/lib/adminApi.js"
  - "[essentials] src/contexts/CompassContext.jsx"
  - "[essentials] src/components/AuthIndicator.jsx"
  - "[essentials] netlify.toml"
autonomous: false

must_haves:
  truths:
    - "Essentials attaches Bearer token to all API requests via apiFetch()"
    - "Essentials extracts hash fragment token on app init before auth check"
    - "Essentials has a Sign In link that redirects to Auth Hub when user is not logged in"
    - "Essentials logout clears localStorage token and redirects to Auth Hub"
    - "Essentials Netlify proxy points to accounts.empowered.vote"
  artifacts:
    - path: "[essentials] src/lib/auth.js"
      provides: "Shared apiFetch wrapper with Bearer token"
      exports: ["apiFetch", "getToken", "clearToken", "setToken", "extractHashToken", "redirectToLogin"]
    - path: "[essentials] netlify.toml"
      provides: "Proxy target pointing to ev-accounts"
      contains: "accounts.empowered.vote"
  key_links:
    - from: "[essentials] src/lib/api.jsx"
      to: "src/lib/auth.js"
      via: "import { apiFetch }"
      pattern: "apiFetch"
    - from: "[essentials] src/contexts/CompassContext.jsx"
      to: "src/lib/auth.js"
      via: "import { extractHashToken, apiFetch }"
      pattern: "extractHashToken"
---

<objective>
Migrate Essentials app from cookie-based Go server auth to Bearer token auth against ev-accounts.

Purpose: Essentials has no login UI — it relies entirely on session cookies from the Go server. After migration, it needs a Sign In link to the Auth Hub and hash fragment token extraction for the return flow.

Output: Essentials fully migrated to Bearer token auth with all fetch calls going through `apiFetch()`, Sign In link added, auth flow working via Auth Hub.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-frontend-auth-updates/40-CONTEXT.md
@.planning/phases/40-frontend-auth-updates/40-RESEARCH.md
@.planning/phases/40-frontend-auth-updates/40-01-SUMMARY.md

IMPORTANT: Essentials is a SEPARATE REPO. Local path from research: C:\Transparent Motivations\essentials (or similar).
The user must confirm the local path before execution begins. If user provides a different path, use that.
GitHub: https://github.com/EmpoweredVote/essentials
Language: React 19 + JavaScript (JSX, no TypeScript)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth.js wrapper, migrate all fetch calls, add Sign In link</name>
  <files>
    src/lib/auth.js (NEW)
    src/lib/api.jsx
    src/lib/compass.js
    src/lib/adminApi.js
    src/contexts/CompassContext.jsx
    src/components/AuthIndicator.jsx (or Layout.jsx — read first to determine)
    netlify.toml
  </files>
  <action>
**Step 1: Create `src/lib/auth.js`**

Create the shared auth module — identical pattern to CompassV2's auth.js (see RESEARCH.md "Code Examples" section). Exports: `apiFetch`, `getToken`, `clearToken`, `setToken`, `extractHashToken`, `redirectToLogin`, `API_BASE`, `AUTH_HUB_URL`, `TOKEN_KEY`.

**Step 2: Update `src/lib/api.jsx`**

- Read file first to understand existing pattern
- Replace all `credentials: "include"` fetch calls with `apiFetch()` from `../auth`
- If `api.jsx` exports wrapper functions that other files import, update those functions to use `apiFetch` internally
- Remove the raw `API` base URL construction — `apiFetch` handles this
- If `api.jsx` re-exports functions used by other files, keep the export signatures stable — just change the internal implementation

**Step 3: Update `src/lib/compass.js`**

- Read file first
- Replace all `credentials: "include"` fetch calls with `apiFetch()` from `./auth`
- Adjust import paths as needed

**Step 4: Update `src/lib/adminApi.js`**

- Read file first
- Replace all `credentials: "include"` fetch calls with `apiFetch()` from `./auth`

**Step 5: Update `src/contexts/CompassContext.jsx`**

- Import `{ extractHashToken, getToken, apiFetch, clearToken }` from `../lib/auth`
- In the auth-check useEffect:
  1. Call `extractHashToken()` first
  2. Check `getToken()` — if null, set logged-out state and return
  3. Call `apiFetch('/auth/me')` — if null (401), return
  4. Parse response: use `data.display_name` if the code currently reads `data.username` or `data.userName`
- Update logout: `apiFetch('/auth/logout', { method: 'POST' })` then `clearToken()`
- Remove ALL `credentials: "include"`

**Step 6: Add Sign In link**

Read `AuthIndicator.jsx` (and `Layout.jsx` if exists) to find where logged-out state is handled:
- When user is NOT logged in, show a "Sign in" link/button that calls `redirectToLogin()` from auth.js
- This is a small but critical addition — without it, Essentials has no login path after migration
- Style consistently with existing UI (read the component to match existing patterns)
- Import `{ redirectToLogin }` from the auth module

**Step 7: Update netlify.toml**

Change proxy target:
```toml
[[redirects]]
  from = "/api/*"
  to = "https://accounts.empowered.vote/api/:splat"
  status = 200
  force = true
```

**CRITICAL CHECKS:**
- `data.username` or `data.userName` → `data.display_name` in EVERY file
- Zero remaining `credentials: "include"` in src/
- Zero remaining hardcoded Go server URLs
  </action>
  <verify>
After all changes:
1. `grep -r 'credentials.*include' src/` returns ZERO results
2. `grep -r 'ev-backend-h3n8' .` returns ZERO results
3. `grep -r 'api\.empowered\.vote' .` returns ZERO results
4. `src/lib/auth.js` exists with all required exports
5. Sign in link exists somewhere in the component tree for unauthenticated users
6. `netlify.toml` proxy points to `accounts.empowered.vote`
  </verify>
  <done>
All Essentials fetch calls use apiFetch() with Bearer token. Hash fragment extraction on init. Sign In link redirects to Auth Hub. Netlify proxy points to ev-accounts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Essentials app fully migrated from cookie auth to Bearer token auth against ev-accounts API. Sign In link added for unauthenticated users. All fetch calls go through apiFetch() wrapper.</what-built>
  <how-to-verify>
1. Review git diff — confirm no remaining `credentials: "include"`, no Go server URLs
2. Confirm `src/lib/auth.js` exists with apiFetch, extractHashToken, redirectToLogin exports
3. Confirm Sign In link exists for unauthenticated users (in AuthIndicator or Layout)
4. Confirm `netlify.toml` proxy points to `accounts.empowered.vote`
5. Confirm CompassContext.jsx uses `data.display_name` (not `data.username`)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Zero `credentials: "include"` in Essentials src/
- Zero hardcoded Go server URLs
- Sign In link present for unauthenticated users
- `src/lib/auth.js` provides shared Bearer token wrapper
- `netlify.toml` points to ev-accounts
</verification>

<success_criteria>
- Essentials app is ready for deployment with Bearer token auth against ev-accounts
- Unauthenticated users can reach Auth Hub login via Sign In link
- No cookie-based auth patterns remain
</success_criteria>

<output>
After completion, create `.planning/phases/40-frontend-auth-updates/40-03-SUMMARY.md`
</output>
