---
phase: 41-vq-and-trivia-migration
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - .planning/phases/41-vq-and-trivia-migration/41-04-SUMMARY.md
autonomous: false
user_setup:
  - service: render
    why: "Update VQ service DATABASE_URL env var"
    env_vars:
      - name: DATABASE_URL
        source: "Generated in Plan 02 (vq_service connection string)"
    dashboard_config:
      - task: "Update DATABASE_URL on VQ Render service"
        location: "Render Dashboard -> VQ service -> Environment"

must_haves:
  truths:
    - "VQ application starts successfully with the new DATABASE_URL pointing to ev-accounts"
    - "VQ can read/write its own validation_quests tables through the vq_service role"
    - "POST /api/vq/confirm-stance completes end-to-end with correct VR adjustment and gem award"
    - "Trivia's database access works correctly after migration (either via updated DATABASE_URL or existing API)"
  artifacts:
    - path: ".planning/phases/41-vq-and-trivia-migration/41-04-SUMMARY.md"
      provides: "Cutover confirmation: VQ startup verified, smoke test passed, CONS-18 and CONS-19 fulfilled"
  key_links:
    - from: "VQ Render service"
      to: "ev-accounts database"
      via: "DATABASE_URL with vq_service credentials"
      pattern: "postgresql://vq_service:.*supabase\\.co:5432"
    - from: "POST /api/vq/confirm-stance"
      to: "connect.confirm_vq_stance RPC"
      via: "ev-accounts Express API (HTTP)"
      pattern: "confirm.stance.*200"
---

<objective>
Execute the maintenance window cutover: update VQ's DATABASE_URL on Render, restart services, verify VQ connects successfully, and run the end-to-end smoke test.

Purpose: This is the final step that makes the migration live. All preparatory work (restore, roles, RLS) is done. This plan coordinates the brief downtime window and confirms everything works.

Output: VQ running against ev-accounts database. Confirmation flow verified. CONS-18 and CONS-19 fulfilled.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-vq-and-trivia-migration/41-CONTEXT.md
@.planning/phases/41-vq-and-trivia-migration/41-01-SUMMARY.md
@.planning/phases/41-vq-and-trivia-migration/41-02-SUMMARY.md
@.planning/phases/41-vq-and-trivia-migration/41-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Update VQ DATABASE_URL on Render</action>
  <instructions>
The user must update the DATABASE_URL environment variable on the VQ Render service.

**New DATABASE_URL value:** See 41-02-SUMMARY.md for the vq_service connection string generated in Plan 02.

Steps:
1. Go to Render Dashboard -> VQ service -> Environment
2. Find the `DATABASE_URL` env var
3. Replace the current value with the new vq_service connection string from Plan 02
4. Save and trigger a manual deploy / restart

**If Trivia also uses DATABASE_URL (determined in Plan 01):**
Also update Trivia's `DATABASE_URL` env var on its Render service with the trivia_service connection string from Plan 02.

**Timing:** This is the maintenance window. VQ will be briefly unavailable (< 5 min) between the env var update and successful restart.
  </instructions>
  <resume-signal>Type "deployed" once VQ (and Trivia if applicable) has restarted with the new DATABASE_URL</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify VQ startup and run end-to-end smoke test</name>
  <files>.planning/phases/41-vq-and-trivia-migration/41-04-SUMMARY.md</files>
  <action>
After the user confirms VQ has restarted:

**Step A: Verify VQ startup**
Check VQ's Render logs for:
- Successful database connection message (no connection errors)
- No "relation does not exist" errors
- No "permission denied for schema" errors

If the user can share VQ's health endpoint URL, hit it:
```bash
curl -s https://<vq-render-url>/health
```

**Step B: Run confirm-stance smoke test**

Use the smoke test from 41-RESEARCH.md. The confirm-stance endpoint is on ev-accounts (NOT on VQ):

```bash
curl -X POST https://ev-accounts-api.onrender.com/api/vq/confirm-stance \
  -H "X-Gems-Service-Key: $VQ_GEMS_SERVICE_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "politician_id": "<valid-essentials-uuid>",
    "topic_id": "<valid-compass-topic-uuid>",
    "confirmed_value": 3,
    "correct_user_ids": [],
    "incorrect_user_ids": [],
    "idempotency_key": "smoke-test-phase41-cutover-001",
    "gems_amount": 1
  }'
```

Expected: HTTP 200 with `{ politician_id, topic_id, confirmed_value }` in response.

If you don't have the service key or valid UUIDs, ask the user to run this test or provide the values.

**Step C: Verify row counts post-cutover**

Run the same row count queries from Plan 02 verification against ev-accounts to confirm no data was lost or corrupted during the cutover window.

**Step D: If Trivia was also migrated (DATABASE_URL updated):**
Verify Trivia's startup logs show successful connection. If Trivia has a health endpoint, hit it.

**Step E: Document CONS-18 and CONS-19 fulfillment**

In the summary:
- CONS-18: `validation_quests` schema in ev-accounts, VQ DATABASE_URL updated, VQ connects successfully, confirm-stance flow works end-to-end
- CONS-19: `trivia` schema in ev-accounts, politician FKs resolve against essentials.politicians, no FK violation errors

Record the smoke test results, any issues encountered, and the final state.
  </action>
  <verify>
1. VQ startup logs show no errors
2. confirm-stance smoke test returns 200
3. Row counts unchanged from Plan 02 baseline
4. No FK violation errors in any log
  </verify>
  <done>VQ running on ev-accounts database. Confirm-stance flow verified end-to-end. CONS-18 and CONS-19 fulfilled. Phase 41 complete.</done>
</task>

</tasks>

<verification>
- VQ Render service running with new DATABASE_URL (no startup errors)
- VQ can read/write validation_quests tables (verified by application operation or direct query)
- POST /api/vq/confirm-stance returns 200 with expected response
- Row counts stable post-cutover
- If Trivia migrated: Trivia service running with new DATABASE_URL
- CONS-18 requirements met (schema exists, DATABASE_URL updated, VQ connects, confirmation flow works)
- CONS-19 requirements met (trivia schema exists, FKs resolve, no violations)
</verification>

<success_criteria>
Maintenance window complete. VQ and Trivia (if applicable) running against ev-accounts database. End-to-end confirmation flow verified. CONS-18 and CONS-19 fulfilled. Phase 41 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/41-vq-and-trivia-migration/41-04-SUMMARY.md`
</output>
