---
phase: 72-db-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-db-audit/72-FINDINGS.md
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "A confirmed list of distinct chamber_name and chamber_name_formal values exists for all Monroe County and Bloomington officials"
    - "It is known whether Monroe County Commissioners and Council currently produce distinct group keys or collide in classify.js"
    - "The TIGER GEO_ID for Monroe County (18105) is verified present or confirmed absent in geofence_boundaries"
    - "A regression test mapping of politician-to-expected-group is documented for Phase 73 verification"
  artifacts:
    - path: ".planning/phases/72-db-audit/72-FINDINGS.md"
      provides: "Complete DB audit results with regression mapping table"
      contains: "## Q1"
  key_links:
    - from: "72-FINDINGS.md"
      to: "Phase 73 planning"
      via: "Documented facts gate Phase 73 approach (data migration vs feature-only)"
      pattern: "Phase 73 Branch Decision"
---

<objective>
Run 5 SQL audit queries against the live Supabase database to document current chamber_name_formal values, classify.js group collisions, geofence presence, and a regression mapping table for Indiana officials.

Purpose: Phase 72 gates all subsequent phases. Before writing any classification or display code, we must know what is actually in the database. The key risk is whether Monroe County Commissioners and Council members collide into the same "County Legislators" group in classify.js.

Output: `.planning/phases/72-db-audit/72-FINDINGS.md` containing verbatim query results and a Phase 73 branch decision.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-db-audit/72-RESEARCH.md

@essentials/src/lib/classify.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute DB audit queries and capture raw output</name>
  <files>.planning/phases/72-db-audit/72-FINDINGS.md</files>
  <action>
Source the DATABASE_URL from EV-Backend/.env.local (parse the line starting with DATABASE_URL=). Run all 5 SQL queries from 72-RESEARCH.md against the live database using psql. Capture output in a structured format.

The 5 queries to run (all read-only SELECT statements):

1. **Distinct chamber values** for Monroe County and Bloomington officials — Query 1 from RESEARCH.md. Groups by chamber_name, chamber_name_formal, district_type, geo_id with politician count. Use filter: `d.geo_id LIKE '18105%' OR (d.state = '18' AND (d.city ILIKE '%bloomington%' OR ch.name ILIKE '%bloomington%' OR ch.name ILIKE '%common council%'))`.

2. **classify.js group simulation** — Query 2 from RESEARCH.md. Shows full_name, office_title, district_type, chamber_name_formal, chamber_name, geo_id, and simulated_group using CASE logic matching classify.js. Filter: `d.geo_id LIKE '18105%' OR d.state = '18'`. This captures all Indiana officials to give full picture.

3. **Monroe County TIGER geofence check** — Query 3 from RESEARCH.md. Check `essentials.geofence_boundaries WHERE geo_id LIKE '18105%'` for geo_id, mtfcc, name, state, source, imported_at.

4. **Indiana geofences coverage** — Query 4 from RESEARCH.md. Group by mtfcc showing count and sample_geo_id for all `state = '18'` geofences.

5. **All active Indiana officials** — Query 5 from RESEARCH.md. Full picture: full_name, is_active, office_title, district_type, geo_id, district_label, state, city, chamber_name, chamber_name_formal, government_name. Filter: `d.state = '18' OR d.geo_id LIKE '18%'`.

IMPORTANT: Use `psql` with `\pset format wrapped` or pipe through column formatting. For each query, capture the full output. If psql is not available, use the DATABASE_URL with any available PostgreSQL client.

CAUTION per RESEARCH.md pitfalls:
- Use `WHERE state = '18'` for Indiana (not 'IN') — geofence_boundaries uses numeric FIPS codes
- Use `COALESCE(name_formal, '')` to catch both NULL and empty string cases
- Use `geo_id LIKE '18105%'` not exact match (format may vary)
  </action>
  <verify>
    <automated>grep -c "## Q" .planning/phases/72-db-audit/72-FINDINGS.md | grep -q "[4-5]" && echo "PASS: All query sections present" || echo "FAIL: Missing query sections"</automated>
  </verify>
  <done>72-FINDINGS.md exists with raw output from all 5 queries pasted verbatim under sections Q1-Q5</done>
</task>

<task type="auto">
  <name>Task 2: Analyze results and document Phase 73 branch decision</name>
  <files>.planning/phases/72-db-audit/72-FINDINGS.md</files>
  <action>
Read the raw query output captured in Task 1. Add analysis sections to 72-FINDINGS.md:

1. **Q1 Analysis — Chamber Values:** List every distinct (chamber_name, chamber_name_formal) pair found. Flag any empty/NULL chamber_name_formal values. Note if BallotReady used state-qualified names like "Monroe County Council, Indiana".

2. **Q2 Analysis — Group Collision:** Confirm YES or NO whether Monroe County Commissioners and Council members both land in "County Legislators". List exactly which officials collide. This is the critical finding for Phase 73.

3. **Q3 Analysis — Geofence Presence:** Confirm YES or NO whether geo_id 18105 (or variant) exists. If NO, note that Phase 73 must add a geofence import step.

4. **Q4 Analysis — Indiana Geofence Coverage:** Summarize what MTFCC types exist for Indiana. Note any gaps relevant to county-level matching.

5. **Regression Mapping Table:** From Query 5 output, create a table with columns: Full Name | office_title | district_type | chamber_name | Current Group (from classify.js logic) | Expected Group (what Phase 73 should produce). Annotate the Expected Group column by tracing each official through classify.js logic in classify.js. For officials where Current Group != Expected Group, add a note explaining what Phase 73 must change.

6. **Phase 73 Branch Decision:** Based on findings, document:
   - If chamber_name_formal IS populated: Phase 73 is a pure feature phase (add GovernmentBody table + classify.go)
   - If chamber_name_formal IS NOT populated: Phase 73 must include a data migration step to populate chamber_name_formal before the feature work
   - Whether Bloomington Common Council district_type is LOCAL or COUNTY (affects classify.go logic design)

Read essentials/src/lib/classify.js to trace the exact logic for the classify.js group assignment. The key function is classifyCategory(). Pay attention to:
- Line ~196: COUNTY branch with hasAny(title, ["commissioner", "supervisor", "council"])
- Line ~210+: LOCAL branch with council/township/board checks
- The priority order of checks (first match wins)
  </action>
  <verify>
    <automated>grep -c "Phase 73 Branch Decision" .planning/phases/72-db-audit/72-FINDINGS.md | grep -q "1" && grep -c "Regression Mapping" .planning/phases/72-db-audit/72-FINDINGS.md | grep -q "1" && echo "PASS: Analysis sections present" || echo "FAIL: Missing analysis sections"</automated>
  </verify>
  <done>72-FINDINGS.md contains: (1) analysis for all 5 queries, (2) collision YES/NO determination, (3) geofence presence YES/NO, (4) regression mapping table with Expected Group annotations, (5) Phase 73 branch decision clearly stated</done>
</task>

</tasks>

<verification>
Phase 72 is complete when all four success criteria from the ROADMAP are met:

1. `grep "chamber_name" 72-FINDINGS.md` shows distinct chamber values documented
2. `grep "County Legislators" 72-FINDINGS.md` shows collision analysis (YES or NO)
3. `grep "18105" 72-FINDINGS.md` shows geofence verification result
4. `grep "Regression Mapping" 72-FINDINGS.md` shows regression table exists with Expected Group column
</verification>

<success_criteria>
- 72-FINDINGS.md exists with verbatim query output for all 5 queries
- Collision between Commissioners and Council in classify.js is confirmed or denied with specific evidence
- Monroe County geofence (18105) presence is confirmed or absence documented
- Regression mapping table has at least 5 rows with Expected Group annotations
- Phase 73 branch decision is clearly stated (data migration needed vs. feature-only)
</success_criteria>

<output>
After completion, create `.planning/phases/72-db-audit/72-01-SUMMARY.md`
</output>
