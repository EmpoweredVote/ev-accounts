---
phase: 18-title-display-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/util/topic.js
  - CompassV2/src/pages/Library.jsx
  - CompassV2/src/components/CalibrationOverlay.jsx
autonomous: true
requirements: [TITLE-02, TITLE-03]

must_haves:
  truths:
    - "Library cards display the tension title in two-line format: topic name on line 1, poles on line 2, no colon visible"
    - "Calibration pick-step cards display the same two-line tension title layout as Library cards"
    - "No 'Where do you stand on...' prefix is visible anywhere in Library or calibration pick step"
    - "getQuestionText helper no longer generates 'Where do you stand on' fallback"
  artifacts:
    - path: "CompassV2/src/util/topic.js"
      provides: "parseTensionTitle helper + cleaned getQuestionText"
      exports: ["getQuestionText", "parseTensionTitle"]
    - path: "CompassV2/src/pages/Library.jsx"
      provides: "Two-line tension title layout on Library cards"
      contains: "parseTensionTitle"
    - path: "CompassV2/src/components/CalibrationOverlay.jsx"
      provides: "Two-line tension title on calibration pick-step cards"
      contains: "parseTensionTitle"
  key_links:
    - from: "CompassV2/src/pages/Library.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { parseTensionTitle }"
      pattern: "parseTensionTitle"
    - from: "CompassV2/src/components/CalibrationOverlay.jsx"
      to: "CompassV2/src/util/topic.js"
      via: "import { parseTensionTitle }"
      pattern: "parseTensionTitle"
---

<objective>
Create the tension title helper, clean up the getQuestionText fallback, and update Library cards and calibration pick-step cards to display the two-line tension title layout.

Purpose: Library and calibration surfaces currently show question_text (or the old "Where do you stand on..." prefix). Phase 17 made server data canonical with tension titles in the format "Topic: Pole A — Pole B". This plan makes those surfaces display the clean two-line tension title format and removes the prohibited prefix fallback.

Output: Library cards and calibration pick cards both render tension titles as two lines (topic name + poles), getQuestionText no longer generates prefix fallback.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-title-display-frontend/18-CONTEXT.md
@.planning/phases/17-title-standardization-backend/17-02-SUMMARY.md
@CompassV2/src/util/topic.js
@CompassV2/src/pages/Library.jsx
@CompassV2/src/components/CalibrationOverlay.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parseTensionTitle helper and clean getQuestionText fallback</name>
  <files>CompassV2/src/util/topic.js</files>
  <action>
Update `CompassV2/src/util/topic.js` with two exports:

1. **`parseTensionTitle(topic)`** — New helper that splits `topic.title` at the first colon into `{ name, poles }`:
   - If `topic.title` contains a colon: `name` = text before colon (trimmed), `poles` = text after colon (trimmed)
   - If no colon or no title: `name` = `topic.short_title` or empty string, `poles` = null
   - Return object: `{ name: string, poles: string|null }`
   - Example: `"Healthcare: Universal Coverage — Market-Driven"` → `{ name: "Healthcare", poles: "Universal Coverage — Market-Driven" }`

2. **`getQuestionText(topic)`** — Keep existing but remove the "Where do you stand on" fallback:
   - Return `topic.question_text || ""` (empty string if missing, not the old prefix)
   - Phase 17 made server data canonical — all topics have question_text, so this fallback should never fire, but if it does, showing nothing is better than showing the prohibited prefix

Both functions are named exports. No default export.
  </action>
  <verify>
Run: `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && grep -n "Where do you stand" src/util/topic.js` — should return nothing.
Run: `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && grep -n "parseTensionTitle" src/util/topic.js` — should show the export.
  </verify>
  <done>
`parseTensionTitle` returns `{ name, poles }` from topic.title; `getQuestionText` returns `topic.question_text || ""` with no "Where do you stand on" fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Library cards and Calibration pick step with two-line tension title</name>
  <files>
    CompassV2/src/pages/Library.jsx
    CompassV2/src/components/CalibrationOverlay.jsx
  </files>
  <action>
**Library.jsx — Topic cards (the grid cards in the category sections):**

1. Import `parseTensionTitle` from `"../util/topic"` (add to existing import line that already imports `getQuestionText`).

2. In the topic card button (around line 595, inside `visible.map((topic) => ...)`), replace the current single-line text `{getQuestion(topic)}` with a two-line tension title layout:
   - Call `const { name, poles } = parseTensionTitle(topic)` inside the map callback (before the return, or inline).
   - Line 1: Topic name in current font weight/size (`text-sm md:text-base font-medium leading-snug`).
   - Line 2: Poles text, visually subordinate — use `text-xs text-gray-500 font-normal mt-0.5` (smaller, muted, lighter weight). Only render line 2 if `poles` is not null.
   - Remove the colon — the line break replaces it.
   - Keep the checkmark icon and level badges as they are.

3. The search filter (line 216-218) currently searches by `short_title` and `question_text`. Update it to also search by `topic.title` so tension titles are searchable:
   - Add `|| (t.title && t.title.toLowerCase().includes(search.toLowerCase()))` to the filter chain.

4. Remove the `const getQuestion = getQuestionText;` alias on line 44 — it's no longer needed since cards use parseTensionTitle instead. If `getQuestionText` is still imported for other uses in the file, keep the import but remove the alias.

**CalibrationOverlay.jsx — Pick step topic cards (around line 370):**

1. Import `parseTensionTitle` from `"../util/topic"` (add to existing import).

2. In the pick step's category.topics.map (around line 353-379), replace `{getQuestionText(fullTopic)}` with the same two-line layout:
   - `const { name, poles } = parseTensionTitle(fullTopic)`
   - Line 1: `name` in `text-sm font-medium leading-snug`
   - Line 2: `poles` in `text-xs text-gray-500 font-normal mt-0.5` (only if poles is not null)
   - Wrap both lines in a `<div>` (replacing the current `<span>`)

Per user decision: Library cards and calibration pick cards use IDENTICAL tension title layout. The same classes, same structure.

Poles line styling (Claude's discretion): Use smaller size + muted color (`text-xs text-gray-500`) to make the topic name the primary read and poles the supporting context. This follows the user's guidance: "topic name is the primary read, poles are supporting context."
  </action>
  <verify>
Run: `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build 2>&1 | tail -5` — build should succeed with no errors.
Run: `grep -n "getQuestion\b" CompassV2/src/pages/Library.jsx` — the alias should be gone.
Run: `grep -n "parseTensionTitle" CompassV2/src/pages/Library.jsx CompassV2/src/components/CalibrationOverlay.jsx` — both files should import and use it.
Run: `grep -rn "Where do you stand" CompassV2/src/` — should return zero results across entire frontend.
  </verify>
  <done>
Library topic cards show two-line tension title (topic name + poles). Calibration pick cards show the identical layout. No "Where do you stand on..." prefix appears anywhere. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build` — succeeds with zero errors
2. `grep -rn "Where do you stand" CompassV2/src/` — returns zero matches (prefix completely removed)
3. `grep -n "parseTensionTitle" CompassV2/src/util/topic.js CompassV2/src/pages/Library.jsx CompassV2/src/components/CalibrationOverlay.jsx` — all three files reference the helper
4. `grep -n "getQuestion = getQuestionText" CompassV2/src/pages/Library.jsx` — alias removed
</verification>

<success_criteria>
- Library topic cards display "Healthcare" on line 1 and "Universal Coverage — Market-Driven" on line 2 (two-line tension title)
- Calibration pick-step cards display the same two-line layout
- No "Where do you stand on..." prefix visible in Library cards, calibration pick cards, or generated by getQuestionText helper
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-title-display-frontend/18-01-SUMMARY.md`
</output>
