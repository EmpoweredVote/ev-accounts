---
phase: 48-compliance-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/src/pages/PrivacyPage.tsx
  - admin/src/App.tsx
  - admin/src/pages/Login.tsx
  - admin/src/pages/Signup.tsx
autonomous: false

must_haves:
  truths:
    - "accounts.empowered.vote/privacy is publicly accessible without authentication"
    - "Privacy page contains a cookie disclosure table naming ev_session with purpose, domain, duration, type, and classification"
    - "ev_session is classified as strictly necessary — no consent banner exists"
    - "Login and Signup pages link to /privacy in a footer area"
  artifacts:
    - path: "admin/src/pages/PrivacyPage.tsx"
      provides: "Full privacy policy page with cookie disclosure section"
      min_lines: 80
    - path: "admin/src/App.tsx"
      provides: "Route registration for /privacy before catch-all"
      contains: "PrivacyPage"
  key_links:
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/PrivacyPage.tsx"
      via: "Route element import"
      pattern: "path=\"/privacy\""
    - from: "admin/src/pages/Login.tsx"
      to: "/privacy"
      via: "Link component"
      pattern: "to=\"/privacy\""
    - from: "admin/src/pages/Signup.tsx"
      to: "/privacy"
      via: "Link component"
      pattern: "to=\"/privacy\""
---

<objective>
Create a publicly accessible privacy policy page at `/privacy` on accounts.empowered.vote with full privacy policy content including a cookie disclosure table for `ev_session`. Add footer links from Login and Signup pages.

Purpose: GDPR/ePrivacy compliance — document the ev_session cookie as strictly necessary so no consent banner is needed, and give users a clear data rights reference.
Output: PrivacyPage.tsx component, updated App.tsx routing, updated Login.tsx and Signup.tsx with privacy footer links.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@admin/src/App.tsx
@admin/src/pages/Login.tsx
@admin/src/pages/Signup.tsx
@admin/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyPage component and register route</name>
  <files>admin/src/pages/PrivacyPage.tsx, admin/src/App.tsx</files>
  <action>
Create `admin/src/pages/PrivacyPage.tsx` — a standalone public page (no auth required) with the full privacy policy for Empowered Vote.

Visual style — match Login.tsx patterns:
- Outer: `min-h-screen flex flex-col items-center bg-gray-50 dark:bg-ev-black px-4 py-12`
- Wordmark at top: `text-3xl font-bold text-ev-teal dark:text-ev-teal-light tracking-tight` linking to `/login`
- Content card: `bg-white dark:bg-gray-900 rounded-2xl border border-gray-200 dark:border-gray-800 shadow-sm p-8 w-full max-w-2xl`
- Headings: `text-xl font-semibold text-gray-900 dark:text-white` for h2, `text-lg font-semibold` for h3
- Body text: `text-sm text-gray-700 dark:text-gray-300 leading-relaxed`
- Use `prose`-like spacing via Tailwind utility classes (no @tailwindcss/typography dependency needed)

Page sections (in order):
1. **Privacy Policy** — title, effective date (2026-03-24), brief intro ("Empowered Vote operates civic engagement tools...")
2. **Information We Collect** — account info (email, legal name), usage data, compass responses
3. **How We Use Your Information** — account management, civic engagement features, platform security
4. **Data Sharing** — we do not sell data; limited sharing with Supabase (infrastructure), no ad networks
5. **Cookies and Local Storage** — intro paragraph explaining strictly necessary classification under GDPR/ePrivacy Directive, then the cookie disclosure table:

| Name | Purpose | Domain | Duration | Type | Classification |
|------|---------|--------|----------|------|----------------|
| ev_session | Session continuity across Empowered Vote apps | .empowered.vote | Session (until logout or browser close) | First-party, HttpOnly, Secure, SameSite=Lax | Strictly necessary |

Explicitly state: "Because this cookie is strictly necessary for authentication, no opt-in consent banner is required under the ePrivacy Directive."

6. **Your Rights** — right to access, correct, delete your data. Link to `/profile` for self-service (profile edits). Contact `privacy@empowered.vote` for data export, access requests, or questions.
7. **Data Retention** — account data retained while account is active; deleted upon account deletion request
8. **Security** — encryption in transit (HTTPS), database-level RLS, HttpOnly cookies
9. **Changes to This Policy** — we may update; effective date at top reflects latest version
10. **Contact** — `privacy@empowered.vote`

Render the cookie table as an actual HTML table with Tailwind classes:
- `w-full text-sm text-left` on table
- `bg-gray-100 dark:bg-gray-800 font-medium` on thead row
- `border-b border-gray-200 dark:border-gray-700` on tbody rows
- `px-3 py-2` on all cells

In App.tsx:
- Import PrivacyPage: `import PrivacyPage from './pages/PrivacyPage';`
- Add route BEFORE the catch-all `<Route path="*">` and OUTSIDE any guard wrappers: `<Route path="/privacy" element={<PrivacyPage />} />`
- Place it alongside `/login` and `/signup` routes (lines 65-66 area)

CRITICAL: The `/privacy` route MUST appear BEFORE `<Route path="*" element={<Navigate to="/login" replace />}>` or it will silently redirect to login.
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` — no type errors. Visually inspect that PrivacyPage.tsx exports a default function component. Confirm App.tsx has `/privacy` route before the catch-all `path="*"` route.
  </verify>
  <done>PrivacyPage.tsx exists with all 10 sections including cookie disclosure table. App.tsx registers /privacy route before catch-all. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add privacy footer links to Login and Signup pages</name>
  <files>admin/src/pages/Login.tsx, admin/src/pages/Signup.tsx</files>
  <action>
In Login.tsx — after the existing "Don't have an account? Create one" paragraph (around line 143-148), add a new paragraph below it with smaller text:

```tsx
<p className="text-center text-xs text-gray-400 dark:text-gray-600 mt-2">
  <Link to="/privacy" className="hover:underline">Privacy Policy</Link>
</p>
```

This goes inside the card div (before its closing `</div>`), after the existing sign-up link paragraph. Import `Link` from react-router-dom if not already imported (it already is in Login.tsx).

In Signup.tsx — after the existing footer links div (around line 254-271, the `<div className="mt-4 space-y-2 text-center">` block), add a similar privacy link. Place it as a new paragraph INSIDE that same div:

```tsx
<p className="text-sm text-gray-400 dark:text-gray-500">
  <Link to="/privacy" className="hover:underline">Privacy Policy</Link>
</p>
```

Link is already imported in Signup.tsx. Keep the styling subtle — these are secondary navigation links, not primary CTAs. Use `text-xs` on Login (which has a tighter card) and `text-sm` on Signup (which has more space).
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` — no type errors. Grep both files for `to="/privacy"` to confirm the links exist.
  </verify>
  <done>Login.tsx and Signup.tsx both contain a "Privacy Policy" link pointing to /privacy. Links are visually subtle (small text, gray color).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Privacy policy page at /privacy with cookie disclosure table, footer links on Login and Signup pages</what-built>
  <how-to-verify>
1. Visit https://accounts.empowered.vote/privacy (no login required) — confirm page loads with full privacy policy
2. Scroll to "Cookies and Local Storage" section — confirm ev_session cookie table is visible with all 6 columns
3. Confirm "strictly necessary" classification is explicitly stated
4. Visit https://accounts.empowered.vote/login — confirm "Privacy Policy" link appears below "Create one" link
5. Visit https://accounts.empowered.vote/signup — confirm "Privacy Policy" link appears in footer area
6. Click the Privacy Policy link from login page — confirm it navigates to /privacy
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` in admin/ passes
- `/privacy` route exists before catch-all in App.tsx
- PrivacyPage.tsx contains cookie disclosure table with ev_session row
- Login.tsx contains `to="/privacy"` link
- Signup.tsx contains `to="/privacy"` link
</verification>

<success_criteria>
- accounts.empowered.vote/privacy is publicly accessible without authentication
- Page contains complete privacy policy with cookie disclosure table
- ev_session cookie is documented with purpose, domain, duration, type, and "strictly necessary" classification
- Login and Signup pages both link to /privacy
</success_criteria>

<output>
After completion, create `.planning/phases/48-compliance-e2e-verification/48-01-SUMMARY.md`
</output>
