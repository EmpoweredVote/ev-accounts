---
phase: 119-ma-city-council-district-geofencing
plan: 04
type: execute
wave: 3
depends_on:
  - "119-02"
  - "119-03"
files_modified:
  - backend/scripts/verify-phase-119.sql
autonomous: false
requirements:
  - MAGE-10
  - MAGE-11
  - MAGE-12
  - MAGE-13
  - MAGE-14
  - MAGE-15

must_haves:
  truths:
    - "All 6 cities have per-ward/district X0013/X0014 geofence rows with correct counts"
    - "All per-ward district rows have tiger_geoid = geo_id"
    - "No ward/district councillors remain linked to a citywide LOCAL district"
    - "Path 0 join returns the correct ward councillor for a test address in each of the 6 cities"
    - "At-large councillors and Mayors still resolve via citywide rows"
  artifacts:
    - path: "backend/scripts/verify-phase-119.sql"
      provides: "8-assertion phase gate verification script for all 6 cities"
      contains: "MAGE-10"
  key_links:
    - from: "essentials.districts (per-ward rows)"
      to: "essentials.geofence_boundaries (X0013/X0014 polygons)"
      via: "d.tiger_geoid = gb.geo_id AND gb.mtfcc = d.mtfcc"
      pattern: "ST_Contains.*ST_Point"
---

<objective>
Write and run a phase gate verification SQL script that asserts all 6 MAGE requirements are satisfied. Run Path 0 spot checks for one test address in each city to confirm end-to-end geofencing works. Human verifies the spot-check outputs are geographically correct.

Purpose: Phase 119 gate — confirms the full Tier 2 → Tier 3 upgrade is correct before closing the phase. Catches any migration that was skipped, any re-link that didn't apply, or any tiger_geoid backfill that was missed.

Output: verify-phase-119.sql script, all 8 assertions pass, human-verified Path 0 results for all 6 cities.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-ma-city-council-district-geofencing/119-RESEARCH.md
@.planning/phases/119-ma-city-council-district-geofencing/119-01-SUMMARY.md
@.planning/phases/119-ma-city-council-district-geofencing/119-02-SUMMARY.md
@.planning/phases/119-ma-city-council-district-geofencing/119-03-SUMMARY.md

<interfaces>
<!-- Phase gate pattern from prior phases -->

From backend/scripts/verify-la-county-108.sql (pattern):
  -- Assertion N: [MAGE-XX] description
  DO $$ DECLARE v_count INTEGER; BEGIN
    SELECT COUNT(*) INTO v_count FROM ... WHERE ...;
    IF v_count <> expected THEN RAISE EXCEPTION 'ASSERTION N FAILED: expected %, got %', expected, v_count; END IF;
    RAISE NOTICE 'ASSERTION N PASSED: %', v_count;
  END $$;

Path 0 join query pattern (from essentialsService):
  SELECT d.geo_id, d.label, d.district_type, p.full_name
  FROM essentials.districts d
  JOIN essentials.geofence_boundaries gb ON d.tiger_geoid = gb.geo_id AND gb.mtfcc = d.mtfcc
  JOIN essentials.offices o ON o.district_id = d.id
  JOIN essentials.politicians p ON p.id = o.politician_id
  WHERE public.ST_Contains(gb.geometry, public.ST_SetSRID(public.ST_Point($lon, $lat), 4326))
    AND d.state = 'ma'
    AND d.district_type = 'LOCAL'
  ORDER BY d.geo_id;

Test addresses from 119-RESEARCH.md Section "Test addresses for Path 0 validation":
  Boston:     (-71.056, 42.360) — Faneuil Hall area — expect a 'boston-ma-council-district-N' row
  Worcester:  (-71.803, 42.262) — City Hall area — expect 'worcester-ma-council-district-N' row
  Springfield:(-72.589, 42.102) — City Hall area — expect 'springfield-ma-council-ward-N' row
  Lowell:     (-71.310, 42.634) — City Hall area — expect 'lowell-ma-council-district-N' row
  Brockton:   (-71.018, 42.082) — City Hall area — expect 'brockton-ma-council-ward-N' row
  Quincy:     (-71.003, 42.251) — City Hall area — expect 'quincy-ma-council-ward-N' row
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run verify-phase-119.sql — 8 assertion phase gate</name>
  <files>backend/scripts/verify-phase-119.sql</files>
  <read_first>
    - backend/scripts/verify-ma-tiger-import.sql — prior phase gate pattern for MA geofencing
    - .planning/phases/119-ma-city-council-district-geofencing/119-RESEARCH.md — Section "Phase Gate Verification Queries" and "Test addresses for Path 0 validation"
  </read_first>
  <action>
    Create backend/scripts/verify-phase-119.sql with 8 assertion blocks plus 6 Path 0 spot-check queries (non-failing — for human review).

    Header comment: "Phase 119: MA City Council District Geofencing — Phase Gate Verification. Run via Supabase MCP execute_sql. All 8 assertions must pass (RAISE EXCEPTION on failure). 6 Path 0 spot checks follow — review output for geographic correctness."

    ASSERTION 1 — MAGE-10 (Boston X0013 tiger_geoid count):
      SELECT COUNT(*) WHERE state='ma' AND geo_id LIKE 'boston-ma-council-district-%' AND mtfcc='X0013' AND tiger_geoid IS NOT NULL.
      Expected: 9. RAISE EXCEPTION 'ASSERTION 1 FAILED [MAGE-10]: expected 9 Boston X0013 rows with tiger_geoid, found %' on mismatch.

    ASSERTION 2 — MAGE-10 (Boston citywide rows):
      SELECT COUNT(*) WHERE state='ma' AND geo_id='2507000' AND tiger_geoid IS NOT NULL.
      Expected: 2. RAISE EXCEPTION 'ASSERTION 2 FAILED [MAGE-10]: expected 2 Boston citywide rows with tiger_geoid (LOCAL + LOCAL_EXEC), found %' on mismatch.

    ASSERTION 3 — MAGE-11 (Worcester X0014 geofence_boundaries):
      SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE geo_id LIKE 'worcester-ma-council-district-%' AND mtfcc='X0014'.
      Expected: 5.

    ASSERTION 4 — MAGE-11 (Worcester district re-link — no councillors at citywide LOCAL):
      SELECT COUNT(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=o.politician_id WHERE p.external_id BETWEEN -258200011 AND -258200007 AND d.geo_id='2582000' AND d.district_type='LOCAL'.
      Expected: 0.

    ASSERTION 5 — MAGE-12+13+14+15 (Total X0014 ward rows across 4 cities in geofence_boundaries):
      SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0014' AND (geo_id LIKE 'springfield-ma-council-ward-%' OR geo_id LIKE 'lowell-ma-council-district-%' OR geo_id LIKE 'brockton-ma-council-ward-%' OR geo_id LIKE 'quincy-ma-council-ward-%').
      Expected: 29 (8+8+7+6). RAISE EXCEPTION on mismatch.

    ASSERTION 6 — MAGE-12 (Springfield re-link):
      SELECT COUNT(*) FROM offices o JOIN districts d ON d.id=o.district_id JOIN politicians p ON p.id=o.politician_id WHERE p.external_id BETWEEN -256700009 AND -256700002 AND d.geo_id='2567000' AND d.district_type='LOCAL'.
      Expected: 0.

    ASSERTION 7 — MAGE-13 (Lowell re-link):
      SELECT COUNT(*) FROM offices o JOIN districts d ON d.id=o.district_id JOIN politicians p ON p.id=o.politician_id WHERE p.external_id BETWEEN -253700012 AND -253700005 AND d.geo_id='2537000' AND d.district_type='LOCAL'.
      Expected: 0.

    ASSERTION 8 — MAGE-14+15 (Brockton + Quincy re-link, combined):
      Brockton: SELECT COUNT(*) ... WHERE p.external_id BETWEEN -250900008 AND -250900002 AND d.geo_id='2509000' AND d.district_type='LOCAL'. Expected: 0.
      Quincy: SELECT COUNT(*) ... WHERE p.external_id BETWEEN -255574507 AND -255574502 AND d.geo_id='2555745' AND d.district_type='LOCAL'. Expected: 0.
      Use two separate DO $$ blocks: one for Brockton (geo_id='2509000') and one for Quincy (geo_id='2555745'), each with a city-named EXCEPTION message (e.g., 'ASSERTION 8a FAILED [MAGE-14]: Brockton ward councillors still linked to citywide' and 'ASSERTION 8b FAILED [MAGE-15]: Quincy ward councillors still linked to citywide').

    PATH 0 SPOT CHECKS (6 queries, SELECT only — no RAISE EXCEPTION; for human review):
    Add a RAISE NOTICE '=== PATH 0 SPOT CHECKS ===' separator.
    For each city, run:
      SELECT d.geo_id, d.label, p.full_name
      FROM essentials.districts d
      JOIN essentials.geofence_boundaries gb ON d.tiger_geoid = gb.geo_id AND gb.mtfcc = d.mtfcc
      JOIN essentials.offices o ON o.district_id = d.id
      JOIN essentials.politicians p ON p.id = o.politician_id
      WHERE public.ST_Contains(gb.geometry, public.ST_SetSRID(public.ST_Point({lon}, {lat}), 4326))
        AND d.state = 'ma'
        AND d.district_type = 'LOCAL'
      ORDER BY d.geo_id;
    Coordinates: Boston(-71.056, 42.360), Worcester(-71.803, 42.262), Springfield(-72.589, 42.102), Lowell(-71.310, 42.634), Brockton(-71.018, 42.082), Quincy(-71.003, 42.251).

    Add RAISE NOTICE 'Phase 119 gate PASSED: all 8 assertions passed.' at the end.

    Run the script via Supabase MCP execute_sql. Capture all RAISE NOTICE output for the SUMMARY.
  </action>
  <verify>
    <automated>
      All 8 DO $$ assertion blocks complete without RAISE EXCEPTION. Script prints 'Phase 119 gate PASSED' notice.
    </automated>
  </verify>
  <acceptance_criteria>
    - Script file exists at backend/scripts/verify-phase-119.sql
    - All 8 assertions run without raising EXCEPTION (all counts match expected values)
    - Script produces 'Phase 119 gate PASSED' RAISE NOTICE
    - 6 Path 0 spot-check queries execute and return results for human review
    - Each spot-check returns at least 1 row (a LOCAL district councillor for that city)
    - MAGE-10 through MAGE-15 labels present in assertion comments
  </acceptance_criteria>
  <done>verify-phase-119.sql runs cleanly — all 8 assertions pass. Path 0 spot-check results logged for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 119 gate SQL assertions (all 8 pass) plus Path 0 spot checks for all 6 MA cities. The Path 0 queries return the specific ward councillor for each city hall test coordinate.
  </what-built>
  <how-to-verify>
    Review the Path 0 spot-check output from Task 1. Confirm the returned councillors are geographically plausible for each city hall location:

    1. Boston (-71.056, 42.360 — Faneuil Hall): Should return a 'boston-ma-council-district-N' row. District 1 covers the downtown/Waterfront area — verify the returned councillor matches that district.

    2. Worcester (-71.803, 42.262 — City Hall Plaza): Should return a 'worcester-ma-council-district-N' row. Worcester City Hall is in the downtown core — verify the returned district is the correct downtown district (likely District 4 per geographic layout).

    3. Springfield (-72.589, 42.102 — City Hall): Should return a 'springfield-ma-council-ward-N' row. Springfield City Hall is in downtown — verify a ward councillor is returned (not an at-large councillor).

    4. Lowell (-71.310, 42.634 — City Hall): Should return a 'lowell-ma-council-district-N' row. Verify a district councillor is returned.

    5. Brockton (-71.018, 42.082 — Brockton City Hall): Should return a 'brockton-ma-council-ward-N' row. Verify a ward councillor is returned (not at-large).

    6. Quincy (-71.003, 42.251 — Quincy City Hall): Should return a 'quincy-ma-council-ward-N' row. Verify a ward councillor is returned.

    For any city that returns 0 rows from the per-ward Path 0 query, that is a bug — report the city and the geo_id prefix to diagnose.

    Optional sanity check: Run the citywide query for at-large officials in one city:
    SELECT p.full_name, d.geo_id, d.district_type FROM essentials.districts d JOIN essentials.geofence_boundaries gb ON d.tiger_geoid = gb.geo_id AND gb.mtfcc = 'G4110' JOIN essentials.offices o ON o.district_id = d.id JOIN essentials.politicians p ON p.id = o.politician_id WHERE public.ST_Contains(gb.geometry, public.ST_SetSRID(public.ST_Point(-71.803, 42.262), 4326)) AND d.state = 'ma' AND d.geo_id = '2582000';
    Should return Worcester at-large councillors (King, Mitra, Toomey, Bergman, Rosen) via the G4110 citywide polygon.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 Path 0 spot checks return geographically plausible ward councillors, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification SQL → Supabase | Read-only assertions; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-V1 | Tampering | ST_Contains Path 0 assertions | accept | Read-only queries; coordinates are hardcoded constants; no user input |
| T-119-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
All 8 DO $$ assertion blocks complete with RAISE NOTICE (not EXCEPTION). Final script output includes 'Phase 119 gate PASSED'. 6 Path 0 spot checks each return at least 1 per-ward councillor row. Human approved.
</verification>

<success_criteria>
Phase 119 gate fully satisfied:
- MAGE-10 ✅: Boston 9 X0013 rows + 2 citywide rows have tiger_geoid
- MAGE-11 ✅: Worcester 5 X0014 boundaries + 5 district rows + 5 councillors re-linked
- MAGE-12 ✅: Springfield 8 X0014 boundaries + 8 ward rows + 8 councillors re-linked
- MAGE-13 ✅: Lowell 8 X0014 boundaries + 8 district rows + 8 councillors re-linked
- MAGE-14 ✅: Brockton 7 X0014 boundaries + 7 ward rows + 7 councillors re-linked
- MAGE-15 ✅: Quincy 6 X0014 boundaries + 6 ward rows + 6 councillors re-linked
Path 0 geofencing works end-to-end for all 6 cities — human approved.
</success_criteria>

<output>
Create .planning/phases/119-ma-city-council-district-geofencing/119-04-SUMMARY.md when done.
</output>
