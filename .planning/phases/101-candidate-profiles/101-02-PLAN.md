---
phase: 101-candidate-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - backend/data/stance-research/2026-06-06-senator-remediation.csv
  - supabase/migrations/20260606000001_128_senator_source_remediation.sql
  - .planning/phases/101-candidate-profiles/101-DELETION-LOG.md
  - .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md
  - .planning/phases/101-candidate-profiles/101-VERIFICATION.md
autonomous: false
requirements:
  - FEDX-01
  - QUAL-01
  - QUAL-02
tags:
  - inform
  - sources
  - senate
  - research-stances
  - migration
  - deletion-log

must_haves:
  truths:
    - "Every senator listed in 101-SENATOR-TARGETS.csv has been re-researched with the research-stances skill in a single full pass (per D-02 — full re-research, not URL-patch-only)"
    - "Every retained or updated stance for a flagged senator has a paired inform.politician_context row with sources containing at least one specific (non-homepage) URL after the migration applies"
    - "Every flagged stance for which research-stances found no real specific URL is DELETED from inform.politician_answers in the migration (per D-04 — no middle ground, delete if no real URL found, regardless of value)"
    - "A deletion log markdown file 101-DELETION-LOG.md exists listing every deleted stance with politician full_name, topic_key, former value, and reason — QUAL-02 format"
    - "The migration file supabase/migrations/20260606000001_128_senator_source_remediation.sql has been applied to the live DB via psql using the session pooler DATABASE_URL"
    - "Post-migration verification SQL returns 0 unsourced senator stances (the four-rule sourced definition from 100-AUDIT-REPORT.md applied to the NATIONAL_UPPER senator population)"
    - "Post-migration verification SQL returns 0 senator stances with only homepage-only sources (matches FEDX-01 success criterion 4 — no placeholder, no non-specific landing page)"
    - "Stance scale for the research run was fetched fresh from inform.compass_stances via the SKILL.md Step 0 query — no hardcoded scale was used (per D-02)"
    - "research-stances dispatched ONE agent at a time, never in parallel (per D-02 rate-limit rule and the memory.md user-feedback note)"
    - "Every value retained or updated was verified against the specific Chair text for that topic — QUAL-01 applied (no party-inference; no directional keep per D-04)"
  artifacts:
    - path: "backend/data/stance-research/2026-06-06-senator-remediation.csv"
      provides: "research-stances CSV output covering every senator in 101-SENATOR-TARGETS.csv — Chair-methodology re-research per D-02"
      contains: "full_name"
    - path: "supabase/migrations/20260606000001_128_senator_source_remediation.sql"
      provides: "SQL migration that UPSERTs inform.politician_answers and politician_context for retained-or-corrected stances AND DELETEs stances flagged for removal (D-04 deletion threshold)"
      contains: "inform.politician_context"
    - path: ".planning/phases/101-candidate-profiles/101-DELETION-LOG.md"
      provides: "QUAL-02 deletion log for Phase 101"
      contains: "former value"
    - path: ".planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md"
      provides: "Research batch log: which senators were researched, which topics changed value vs same value with better source, which topics were flagged for deletion"
      contains: "senator"
    - path: ".planning/phases/101-candidate-profiles/101-VERIFICATION.md"
      provides: "Post-migration verification record — captures the success-criteria SQL outputs proving FEDX-01"
      contains: "VERIFICATION"
  key_links:
    - from: "backend/data/stance-research/2026-06-06-senator-remediation.csv"
      to: "supabase/migrations/20260606000001_128_senator_source_remediation.sql"
      via: "CSV rows become UPSERT statements; flagged (senator, topic) pairs absent from CSV become DELETE statements"
      pattern: "INSERT INTO inform\\.politician_(answers|context)"
    - from: "supabase/migrations/20260606000001_128_senator_source_remediation.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "UPSERT (ON CONFLICT DO UPDATE) and DELETE WHERE composite key matches flagged rows"
      pattern: "ON CONFLICT \\(politician_id, topic_id\\) DO UPDATE"
    - from: "supabase/migrations/20260606000001_128_senator_source_remediation.sql"
      to: ".planning/phases/101-candidate-profiles/101-DELETION-LOG.md"
      via: "every DELETE pair in the migration corresponds to exactly one row in the deletion log"
      pattern: "DELETE FROM inform\\.politician_answers"
    - from: ".planning/phases/101-candidate-profiles/101-DELETION-LOG.md"
      to: "QUAL-02 deletion-log requirement"
      via: "table with columns politician full_name, topic_key, former value, reason"
      pattern: "former value"
---

<objective>
Re-research every senator flagged by Plan 01's triage (in 101-SENATOR-TARGETS.csv) using the research-stances skill under the Chair methodology, then write and apply a single SQL migration that UPSERTs every retained or corrected stance into inform.politician_answers and inform.politician_context with at least one specific (non-homepage) source URL, and DELETEs every flagged stance for which no real URL was found. Commit the QUAL-02 deletion log alongside the migration.

Purpose: Phase 101's only outcome is that every US Senator stance is either backed by a real primary-source URL OR has been deleted. Plan 01 produced the precise scope; Plan 02 does the actual remediation. After this plan applies, the success-criteria SQL from ROADMAP Phase 101 returns 0 unsourced senator stances and 0 homepage-only senator sources.

Implements: FEDX-01 (the remediation itself), QUAL-01 (Chair-methodology value verification), QUAL-02 (deletion log).

Output:
- backend/data/stance-research/2026-06-06-senator-remediation.csv — research-stances CSV output
- supabase/migrations/20260606000001_128_senator_source_remediation.sql — migration with UPSERTs and DELETEs
- .planning/phases/101-candidate-profiles/101-DELETION-LOG.md — QUAL-02 deletion log
- .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md — research batch log
- .planning/phases/101-candidate-profiles/101-VERIFICATION.md — post-migration verification record
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-candidate-profiles/101-CONTEXT.md
@.planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv
@.planning/phases/101-candidate-profiles/101-TRIAGE-REPORT.md
@.planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — load target list, fetch live stance scale, decide batching</name>
  <files>.planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md</files>
  <read_first>
    - .planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv (Plan 01 output — authoritative scope)
    - .planning/phases/101-candidate-profiles/101-TRIAGE-REPORT.md (per-senator per-topic detail)
    - .claude/skills/research-stances/SKILL.md (Step 0 topic-resolution query — required fresh stance scale)
    - .planning/STATE.md section "v2.7 Source Integrity Patterns" (Chair methodology rules)
    - .planning/STATE.md section "v2.3 Senator Records Patterns" (DB full_name check before name-based SQL; example: Adam B. Schiff not Adam Schiff)
  </read_first>
  <action>
    Read .planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv into memory. For each row extract: full_name, politician_id, state, party, affected_topic_keys (split on pipe), classification.

    Verify every politician_id exists in essentials.politicians via a single pg.Pool query: SELECT id, full_name FROM essentials.politicians WHERE id = ANY of the CSV politician_ids AND is_active = true. Confirm row count matches CSV row count. If a name mismatch is detected (Phase 73 pattern — Adam B. Schiff vs Adam Schiff), record both names in the notes file so Task 3 uses the DB full_name for any name-based SQL.

    Fetch the live stance scale per SKILL.md Step 0: run the topic-resolution query that joins inform.compass_topics and inform.compass_stances with json_agg. Capture the JSON output verbatim. Task 2 must paste this entire payload into every research-stances agent prompt — no paraphrasing, no trimming.

    Apply D-03 batching using the CSV count (re-confirm against the report recommendation; CSV is source of truth):
      - count is at most 10 senators: one research batch (one CSV file)
      - count is between 11 and 25 senators: split alphabetically A through M and N through Z (still merged into one final CSV)
      - count is 26 or more senators: split into 3 alphabetical buckets

    Write .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md with these sections:
      - "Scope": total flagged senator count, classification breakdown (unsourced_only / weak_only / both)
      - "Senators in batch 1" (and 2, 3 as needed): table with full_name, politician_id (UUID), state, party, affected_topic_keys
      - "Live stance scale": embed the JSON payload from the SKILL.md Step 0 query, with a fetched-at timestamp header
      - "Batching decision": stated count, the D-03 threshold tier, the chosen batch plan (one of: "1 batch", "2 batches A-M and N-Z", "3 batches")

    Do NOT dispatch research-stances agents in this task. Task 1 is pre-flight only.
  </action>
  <verify>
    <automated>test -f .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md &amp;&amp; grep -q "Live stance scale" .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md &amp;&amp; grep -q "Batching decision" .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md exists
    - File contains the section header "Scope"
    - File contains the section header "Live stance scale"
    - File contains the section header "Batching decision"
    - Live stance scale section contains a JSON object with at least one entry whose key is "topic_key" (sanity check the SKILL.md Step 0 query ran)
    - The count of distinct politician_id UUIDs listed under the "Senators in batch" sections equals the data-row count in 101-SENATOR-TARGETS.csv (data rows = total lines minus 1 header line)
    - Batching decision section explicitly names one of: "1 batch", "2 batches A-M and N-Z", or "3 batches"
  </acceptance_criteria>
  <done>Pre-flight notes committed. The exact senator-to-batch mapping and the live stance scale are recorded. Task 2 can dispatch research-stances agents with deterministic input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run research-stances skill for every flagged senator (ONE agent at a time)</name>
  <files>backend/data/stance-research/2026-06-06-senator-remediation.csv</files>
  <read_first>
    - .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md (batch plan from Task 1)
    - .planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv (per-senator affected_topic_keys — agent --topics flag limited to these)
    - .claude/skills/research-stances/SKILL.md (full skill — Step 1 dispatch rules, Step 2 collection, Step 3 approval format)
    - .planning/STATE.md section "v2.7 Source Integrity Patterns" (Chair methodology — value must match exact stance text, not directional inference)
  </read_first>
  <what-built>
    Plan 02 Task 1 has produced 101-RESEARCH-NOTES.md with the batch plan and the live stance scale. This task dispatches the research-stances skill for each batch.
  </what-built>
  <action>
    Dispatch the research-stances skill for the senators in 101-SENATOR-TARGETS.csv. Rules locked by D-02 and the user-memory rate-limit feedback:
      - ONE senator (or one small group of 2-3 senators) per agent invocation. NEVER launch agents in parallel. Wait for the prior agent's CSV write to complete before dispatching the next.
      - Use the agent's --topics argument with each senator's affected_topic_keys ONLY (do not re-research topics that already have specific sources — per D-02 affected-senators-only scope and CONTEXT D-01 scope-is-dynamic).
      - Pass the full live stance scale (from 101-RESEARCH-NOTES.md "Live stance scale" section) into every agent prompt per SKILL.md Step 1. Do not paraphrase or trim.
      - Output filename for every agent: backend/data/stance-research/2026-06-06-senator-remediation.csv. If multiple batches, each agent appends to the same file (verify no duplicate headers after each batch).

    Required CSV columns produced by the agent: full_name, topic_key, value, reasoning, source_url_1, source_url_2, source_url_3. The CSV is the source of truth — any topic the agent skips (insufficient evidence) flows to deletion in Task 3.

    Single research pass per D-04: do NOT retry topics the agent skipped. Skipped topics flow directly to deletion in the migration.

    After every agent completes, append to .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md a per-batch log section: senator name, topics researched, topics returned with value, topics skipped (deletion candidates). Do not modify the DB in this task — Task 3 (the migration) is the only DB-write step.
  </action>
  <how-to-verify>
    1. backend/data/stance-research/2026-06-06-senator-remediation.csv exists with exactly one header row (no duplicate headers from multi-batch appends).
    2. Every data row has a non-blank source_url_1 (no placeholder rows in the CSV — placeholder candidates are reflected as skipped topics in 101-RESEARCH-NOTES.md, not as CSV rows).
    3. .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md has a "topics skipped" section per batch with every (senator, topic_key) pair NOT in the CSV listed explicitly.
    4. Manual scan: pick 3 random rows from the CSV. For each, click source_url_1 — confirm the URL resolves to a specific page (bill vote, press release, floor speech, official statement) and the content matches the senator's value assignment per QUAL-01.
  </how-to-verify>
  <verify>
    <automated>test -f backend/data/stance-research/2026-06-06-senator-remediation.csv &amp;&amp; head -1 backend/data/stance-research/2026-06-06-senator-remediation.csv | grep -q "topic_key" &amp;&amp; grep -q "source_url_1" backend/data/stance-research/2026-06-06-senator-remediation.csv</automated>
    <human-check>Spot-check 3 random source URLs from the CSV — each must resolve to a specific page (not a homepage, not a 404, not a disambiguation page) and the page content must match the Chair-text value the agent assigned.</human-check>
  </verify>
  <acceptance_criteria>
    - File backend/data/stance-research/2026-06-06-senator-remediation.csv exists
    - First line of the CSV contains the tokens full_name, topic_key, value, reasoning, source_url_1 (all on the header row)
    - The CSV contains exactly one header line — verification: `grep -c "^full_name,topic_key" backend/data/stance-research/2026-06-06-senator-remediation.csv` returns 1
    - Zero CSV data rows have a blank source_url_1 — verification: data rows starting with the first comma group ending in `,,` (empty value followed by empty url) must not exist
    - The set of distinct full_name values in the CSV is a subset of the full_name values in .planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv
    - .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md has a per-batch "topics skipped" section listing every flagged (senator, topic_key) pair NOT in the research CSV — these are the Task 3 deletion candidates
    - Human verification: 3 spot-checked source URLs each resolve to a specific page and the content matches the agent's value assignment per QUAL-01 (no homepage URLs, no 404s, no party-inference)
  </acceptance_criteria>
  <resume-signal>Reply "approved" once the CSV is reviewed and the 3 spot-check URLs pass QUAL-01. Reply with specific row indices if any value must be rejected before Task 3 builds the migration.</resume-signal>
  <done>The research-stances CSV is complete and self-consistent. Every flagged (senator, topic) pair is classified as either upgrade-and-keep (CSV row exists) or delete (CSV row absent + logged in 101-RESEARCH-NOTES.md "topics skipped").</done>
</task>

<task type="auto">
  <name>Task 3: Write migration SQL (UPSERTs from CSV + DELETEs from skipped list) and the deletion log</name>
  <files>
    supabase/migrations/20260606000001_128_senator_source_remediation.sql,
    .planning/phases/101-candidate-profiles/101-DELETION-LOG.md
  </files>
  <read_first>
    - backend/data/stance-research/2026-06-06-senator-remediation.csv (the upsert source — Task 2 output)
    - .planning/phases/101-candidate-profiles/101-RESEARCH-NOTES.md "topics skipped" sections (the deletion source — Task 2 output)
    - .planning/phases/101-candidate-profiles/101-SENATOR-TARGETS.csv (cross-check — every flagged stance must end up EITHER in upsert OR in deletion, never both, never neither)
    - supabase/migrations/20260603000012_127_derek_dooley_source_corrections.sql (most recent prior migration touching inform.politician_context.sources — template for UPDATE/UPSERT shape and file header style)
    - .planning/STATE.md "Migration number" line — last applied is 127; next available is 128
  </read_first>
  <action>
    Verify the next migration number against the live DB before writing: run `SELECT MAX(version) FROM supabase_migrations.schema_migrations` via psql using the session pooler DATABASE_URL. The next migration filename uses one greater than that result. Expected next value: 128. If the DB returns a higher number, abort and surface the conflict — another migration ran in between.

    Migration filename pattern: `supabase/migrations/20260606000001_128_senator_source_remediation.sql`. The date prefix YYYYMMDD000NNN matches the recent v2.6 migration convention (see 20260603000012_127_*.sql). Replace 20260606 with the actual run date if not 2026-06-06.

    Migration content structure (single transaction):
      1. Header comment block: phase identifier, requirements covered (FEDX-01, QUAL-01, QUAL-02), source CSV path, source deletion-log path, expected row counts (upserts and deletes).
      2. BEGIN.
      3. UPSERT block — for every row in 2026-06-06-senator-remediation.csv:
         - Resolve politician_id by JOIN against essentials.politicians on full_name; when full_name has middle-initial ambiguity (Phase 73 pattern — Adam B. Schiff), use the canonical UUID literal recorded in 101-RESEARCH-NOTES.md instead of name-based resolution.
         - Resolve topic_id by lookup against inform.compass_topics on topic_key.
         - INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES (...) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value.
         - INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES (...) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources.
         - The sources array MUST exclude blank or NULL elements. Either build the cleaned array in the migration generator script before emitting the SQL, or use the Postgres pattern `ARRAY(SELECT u FROM unnest(ARRAY[...]) AS u WHERE u IS NOT NULL AND trim(u) != '')` to filter inline.
      4. DELETE block — for every (senator, topic_key) pair listed in 101-RESEARCH-NOTES.md "topics skipped":
         - Each DELETE pair preceded by a SQL comment of the form `-- DELETED: {full_name} / {topic_key} / former value={v} / reason={r}` so the migration is self-documenting.
         - DELETE FROM inform.politician_context WHERE politician_id = uuid_literal AND topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = 'literal_key').
         - DELETE FROM inform.politician_answers WHERE politician_id = uuid_literal AND topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = 'literal_key').
         - Order: delete context FIRST, then answers (defensive — keeps intermediate state consistent if the transaction aborts).
      5. Post-state RAISE NOTICE block — embedded SELECTs that compute and RAISE NOTICE for the post-state unsourced senator count and the post-state homepage-only senator count. Do NOT use ROLLBACK or RAISE EXCEPTION based on these counts — verification is Task 4's responsibility.
      6. COMMIT.

    In parallel, write .planning/phases/101-candidate-profiles/101-DELETION-LOG.md in the QUAL-02 format. Required columns (locked, in this order): politician full_name | topic_key | former value | reason. Reason must be exactly one of these strings: "no evidence found" (research returned no real URL for the topic) OR "value incorrect and no correcting source found" (research returned a different value for the topic but no sourceable URL).

    Cross-check before finalizing both files:
      - Count of UPSERT (politician_id, topic_id) pairs in the migration equals the count of data rows in the research CSV
      - Count of DELETE comment markers in the migration equals the count of data rows in 101-DELETION-LOG.md
      - Union of UPSERT pairs and DELETE pairs equals the set of (politician_id, topic_key) flagged by Plan 01 (expanded from 101-SENATOR-TARGETS.csv affected_topic_keys)
      - Intersection of UPSERT pairs and DELETE pairs is empty (no row both upserted and deleted)
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260606000001_128_senator_source_remediation.sql &amp;&amp; test -f .planning/phases/101-candidate-profiles/101-DELETION-LOG.md &amp;&amp; grep -q "ON CONFLICT (politician_id, topic_id)" supabase/migrations/20260606000001_128_senator_source_remediation.sql &amp;&amp; grep -q "former value" .planning/phases/101-candidate-profiles/101-DELETION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260606000001_128_senator_source_remediation.sql exists
    - Migration begins with BEGIN; and ends with COMMIT;
    - Migration contains the exact string `ON CONFLICT (politician_id, topic_id) DO UPDATE` (upsert pattern)
    - Migration contains at least the string `INSERT INTO inform.politician_answers` (upsert section present)
    - Migration contains at least the string `INSERT INTO inform.politician_context` (upsert section present)
    - File .planning/phases/101-candidate-profiles/101-DELETION-LOG.md exists
    - Deletion log contains the column headers `politician full_name`, `topic_key`, `former value`, `reason` (QUAL-02 format)
    - Count of `-- DELETED:` comments in the migration equals the count of non-header data rows in 101-DELETION-LOG.md (verifiable with `grep -c '^-- DELETED:'`)
    - Count of distinct `INSERT INTO inform.politician_answers` row tuples in the migration equals the count of non-header data rows in backend/data/stance-research/2026-06-06-senator-remediation.csv
    - No (politician_id, topic_id) pair appears in both the upsert section and the delete section (cross-check via the migration generator's pre-write assertion, recorded as a comment in the migration header)
  </acceptance_criteria>
  <done>The migration file and the QUAL-02 deletion log exist, the row counts cross-check correctly against the research CSV and the skipped-topics list, and the migration is ready to be applied via psql in Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Apply migration via psql (session pooler) and run FEDX-01 success-criteria verification</name>
  <files>.planning/phases/101-candidate-profiles/101-VERIFICATION.md</files>
  <read_first>
    - supabase/migrations/20260606000001_128_senator_source_remediation.sql (the migration to apply — Task 3 output)
    - .planning/STATE.md section "v2.2 Geospatial Patterns" (Migration apply method 2026-05-09 — direct psql via session pooler URL, NOT supabase db push)
    - .planning/ROADMAP.md section "Phase 101: Federal Senate Remediation" — success criteria 1 through 4 (the verification SQL is criterion 1; criterion 4 is the no-placeholder rule)
  </read_first>
  <action>
    Apply the migration to the live Supabase DB using direct psql:
      `psql "$DATABASE_URL" -f supabase/migrations/20260606000001_128_senator_source_remediation.sql`

    DATABASE_URL must be the session pooler URL (aws-0-PROJECT.pooler.supabase.com:5432). Per the BLOCKING schema_push_requirement in the planning context, `supabase db push` does not work in this project — direct psql only.

    Capture the full psql output (notices, row counts, any errors). If the migration aborts with an error, do NOT proceed — surface the error, do not retry, fix the migration in a follow-up plan.

    After successful apply, run the FEDX-01 success-criteria verification SQL (queries derived from ROADMAP Phase 101 success criteria 1 and 4):

      Query V1 — Unsourced senator stances (must return 0):
        SELECT COUNT(*) AS unsourced_count
        FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc
          ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id IN (
          SELECT DISTINCT p.id FROM essentials.politicians p
          JOIN essentials.offices o ON o.politician_id = p.id AND o.is_vacant = false
          JOIN essentials.districts d ON d.id = o.district_id
          WHERE d.district_type = 'NATIONAL_UPPER' AND p.is_active = true
        )
        AND (
          pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL
          OR NOT EXISTS (SELECT 1 FROM unnest(pc.sources) s(u) WHERE u IS NOT NULL AND trim(u) != '')
        );

      Query V2 — Senator stances with only homepage-only sources (must return 0):
        SELECT COUNT(*) AS homepage_only_count
        FROM inform.politician_context pc
        WHERE pc.politician_id IN (
          SELECT DISTINCT p.id FROM essentials.politicians p
          JOIN essentials.offices o ON o.politician_id = p.id AND o.is_vacant = false
          JOIN essentials.districts d ON d.id = o.district_id
          WHERE d.district_type = 'NATIONAL_UPPER' AND p.is_active = true
        )
        AND pc.sources IS NOT NULL
        AND array_length(pc.sources, 1) IS NOT NULL
        AND NOT EXISTS (
          SELECT 1 FROM unnest(pc.sources) s(u)
          WHERE u IS NOT NULL AND trim(u) != '' AND trim(u) !~ '^https?://[^/]+/?$'
        )
        AND EXISTS (
          SELECT 1 FROM unnest(pc.sources) s(u) WHERE u IS NOT NULL AND trim(u) != ''
        );

      Query V3 — Deletion accounting (informational): SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (the senator id set) — record the post-delete senator stance total in the verification file.

    Write .planning/phases/101-candidate-profiles/101-VERIFICATION.md with:
      - Section "Migration apply": command used, exit code, timestamp, summary of the psql NOTICE output
      - Section "FEDX-01 verification": Query V1 result (must be 0), Query V2 result (must be 0), Query V3 total
      - Section "Cross-checks": confirmation that 101-DELETION-LOG.md row count + research CSV row count = original flagged-stance count from 101-SENATOR-TARGETS.csv
      - Section "QUAL-01 + QUAL-02 compliance": one-line confirmation that every retained value was Chair-text verified (link Task 2 spot-check) and the deletion log exists with the required columns
  </action>
  <verify>
    <automated>test -f .planning/phases/101-candidate-profiles/101-VERIFICATION.md &amp;&amp; grep -q "FEDX-01 verification" .planning/phases/101-candidate-profiles/101-VERIFICATION.md &amp;&amp; grep -E "unsourced_count[[:space:]]*[:|=][[:space:]]*0" .planning/phases/101-candidate-profiles/101-VERIFICATION.md &amp;&amp; grep -E "homepage_only_count[[:space:]]*[:|=][[:space:]]*0" .planning/phases/101-candidate-profiles/101-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied to live DB via `psql "$DATABASE_URL" -f supabase/migrations/20260606000001_128_senator_source_remediation.sql` with exit code 0
    - psql post-apply check via `SELECT MAX(version) FROM supabase_migrations.schema_migrations` returns 128 (or one greater than the prior MAX, whichever was used)
    - File .planning/phases/101-candidate-profiles/101-VERIFICATION.md exists
    - Verification file contains the section header "FEDX-01 verification"
    - Verification file records Query V1 result as 0 (unsourced senator count is zero post-migration)
    - Verification file records Query V2 result as 0 (homepage-only senator count is zero post-migration)
    - Verification file records Query V3 informational total (post-delete senator stance count) — value present and matches the arithmetic: original total minus deletion-log row count
    - Verification file confirms QUAL-02 deletion-log row count equals the migration's DELETE pair count
  </acceptance_criteria>
  <done>The migration has been applied to the live DB, FEDX-01 success criteria 1 and 4 are verified at 0, and QUAL-01 + QUAL-02 compliance is recorded. Phase 101 closes when this verification file is committed.</done>
</task>

</tasks>

<verification>
- Migration file exists and applied cleanly via psql (exit 0)
- FEDX-01 success-criteria queries return 0 unsourced and 0 homepage-only senator rows post-migration
- 101-DELETION-LOG.md committed with QUAL-02 columns
- 101-RESEARCH-NOTES.md and 101-VERIFICATION.md committed
- Research CSV row count + deletion log row count equals the original flagged-stance count from Plan 01
</verification>

<success_criteria>
1. supabase/migrations/20260606000001_128_senator_source_remediation.sql is present and applied to the live DB.
2. The post-migration FEDX-01 verification SQL returns 0 unsourced and 0 homepage-only senator stances.
3. .planning/phases/101-candidate-profiles/101-DELETION-LOG.md is committed in the locked QUAL-02 format.
4. Every flagged senator stance from Plan 01 has been accounted for in EITHER the migration UPSERT block OR the DELETE block — never both, never neither.
5. research-stances was run with one agent at a time, with the live stance scale embedded in every prompt, per D-02 and the user-memory rate-limit feedback.
</success_criteria>

<output>
Create `.planning/phases/101-candidate-profiles/101-02-SUMMARY.md` when done. The summary must record:
- Final stance counts: upserted, deleted, total senator stances post-migration
- Migration number applied (expected: 128)
- FEDX-01 verification query results (both must be 0)
- Link to 101-DELETION-LOG.md (QUAL-02 deliverable)
- Confirmation that all three requirements (FEDX-01, QUAL-01, QUAL-02) are satisfied
- Any senators where research found a value disagreement — note in summary so future audits can spot-check
</output>
