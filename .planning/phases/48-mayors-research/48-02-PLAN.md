---
phase: 48-mayors-research
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - EV-Backend/data/stance_research.csv
autonomous: true
requirements:
  - STANCE-10

must_haves:
  truths:
    - "Mayor Bass has integer stance values (1-5) with source URLs for applicable compass topics"
    - "Every Bass stance row has at least one source URL pointing to a specific article, vote record, or official statement"
    - "No row uses advocacy group ratings as a source"
    - "No fabricated URLs — all URLs use verifiable patterns (congress.gov, mayor.lacity.org, LA Times, or general fallback pages)"
    - "All researched politicians (23 total) are represented in the stance CSV with at least one source per stance"
  artifacts:
    - path: "EV-Backend/data/stance_research.csv"
      provides: "Complete stance research CSV with all Phase 46-48 politicians including Mayor Bass"
      contains: "Karen Bass"
  key_links:
    - from: "EV-Backend/data/stance_research.csv"
      to: "compass.topics.topic_key"
      via: "topic_key column values must match exactly"
      pattern: "healthcare|abortion|tariffs|taxes|same-sex-marriage"
---

<objective>
Research and populate sourced stance data for Los Angeles CA Mayor Karen Bass across all 21 compass topics, appending to the existing stance research CSV. Then perform final validation of the complete Phase 48 CSV.

Purpose: Captures LA mayor stance data. Bass has an extensive congressional record (US House 2011-2022) providing rich sourcing, plus her mayoral actions since December 2022. This completes the mayor research phase.
Output: Updated `EV-Backend/data/stance_research.csv` with Bass stance rows appended and full validation.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-mayors-research/48-CONTEXT.md

Key reference — the 21 compass topic_keys (from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`):
healthcare, abortion, tariffs, taxes, same-sex-marriage, religious-freedom, trans-athletes, ukraine-support, medicare, fossil-fuels, voting-rights, deportation, social-security, ai-regulation, climate-change, civil-rights, housing, campaign-finance, immigration, misinformation, redistricting

Stance scale reference (from the same CSV — each topic has 5 defined stances):
- 1 = Most progressive/expansive government role
- 5 = Most conservative/limited government role
- 3 = Moderate/centrist position

CSV schema (established in Phase 46):
```
full_name,external_id,topic_key,value,source_url_1,source_url_2,source_url_3
```

The CSV has Thomson rows from Plan 48-01 appended after the original 422 rows.

Karen Bass background:
- Current Mayor of Los Angeles (took office December 2022)
- US House of Representatives (2011-2022), represented CA-37 then CA-33 after redistricting
- Served as Speaker of the California State Assembly (2008-2010)
- Congress.gov bioguide ID: B001270
- Verified congress.gov member page: https://www.congress.gov/member/karen-bass/B001270
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research Mayor Karen Bass stances</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Research Los Angeles Mayor Karen Bass's (D) positions on all 21 compass topics using web search. Bass has extensive public records from multiple roles:

**Source hierarchy (prefer most recent):**
1. Mayoral actions (2022-present): executive directives, budget proposals, press conferences, LA Times coverage
2. Congressional record (2011-2022): House votes, bills sponsored/cosponsored, committee actions
3. State Assembly record (pre-2011): legislation, leadership decisions
4. Campaign positions and official statements

For each topic:

1. Search for Bass's documented positions across her various roles
2. Match her position to the closest of the 5 defined stances for that topic (read the stance descriptions from `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`)
3. Assign an integer value 1-5
4. Record 1-3 source URLs pointing to specific articles, vote records, bills, or official statements

**Research guidelines (from CONTEXT.md decisions — these are NON-NEGOTIABLE):**
- Integers only (1-5), no decimals
- When evidence conflicts across time, use the MOST RECENT position
- Only use the individual politician's documented positions — NO party platform fallbacks
- Valid sources: congress.gov (bills, votes, member page), mayor.lacity.org, LA Times, official statements, city council records, news from major outlets
- Advocacy group ratings are NOT valid sources
- Omit the row entirely if no documented position exists for a topic
- Minimum 1 source URL per row (required); up to 3 source URLs
- Sources should point to specific articles/votes/speeches — general "Issues" pages acceptable as fallback only

**URL AUTHENTICITY (CRITICAL — learned from Phase 47):**
- DO NOT fabricate or guess URLs. Phase 47 produced ~723 fabricated URLs requiring extensive cleanup.
- Verified fallback URLs for Bass:
  - Congress.gov member page: `https://www.congress.gov/member/karen-bass/B001270` (VERIFIED)
  - LA Mayor official site: `https://mayor.lacity.org/` (with real subpages if known)
- When citing congress.gov bill URLs, use the format `https://www.congress.gov/bill/{congress}th-congress/{chamber}-bill/{number}` — only cite bills you are confident Bass actually sponsored or voted on
- When citing LA Times articles, only include URLs with real, specific slugs you are confident about
- It is better to use the verified congress.gov member page as a general source than to fabricate a specific bill URL

**Coverage expectations:** Bass should have good coverage across most or all 21 topics due to her extensive congressional record. She is expected to have more documented stances than Thomson.

Bass's BallotReady external_id: look up via web search or leave blank if not findable.

Append all Bass rows to the CSV after the Thomson rows from Plan 48-01.
  </action>
  <verify>
    <automated>grep -c "Karen Bass" EV-Backend/data/stance_research.csv && echo "---" && wc -l EV-Backend/data/stance_research.csv && echo "---" && awk -F',' '{print $1}' EV-Backend/data/stance_research.csv | sort | uniq -c | sort -rn | head -25</automated>
  </verify>
  <done>Bass has stance rows appended to the CSV with integer values 1-5 and at least one source URL per row. Topics without documented positions are omitted (no empty/placeholder rows). All URLs are verifiable real pages. Bass expected to cover most or all 21 topics.</done>
</task>

<task type="auto">
  <name>Task 2: Final Phase 48 CSV validation</name>
  <files>EV-Backend/data/stance_research.csv</files>
  <action>
Run comprehensive validation of the entire CSV after adding both Thomson and Bass rows.

**Validation checks (all must pass):**

1. **Column count:** Every row has exactly 7 columns
2. **Politician count:** At least 23 unique politician names (21 from Phase 47 + Thomson + Bass)
3. **Topic key validation:** All topic_key values match one of the 21 defined keys
4. **Value range:** All stance values are integers 1-5
5. **No duplicates:** No duplicate (full_name, topic_key) pairs
6. **Source URL coverage:** Every row has non-empty source_url_1
7. **URL format:** All URLs start with `https://` or `http://`
8. **No empty rows:** No rows with empty full_name or topic_key
9. **Thomson present:** Kerry Thomson has at least 1 row
10. **Bass present:** Karen Bass has at least 1 row
11. **Prior data preserved:** Original 21 politicians still present with correct row counts

**Per-politician summary for Phase 48 mayors:** For Thomson and Bass, report:
- Number of rows (topics covered)
- Sample of source URL domains used
- Any topics omitted and why

**URL authenticity spot-check for Bass rows:**
- Pick 5 Bass URLs and verify they look like real, specific pages (not generic slugs)
- For any congress.gov bill URLs, verify the bill number and congress session look plausible
- If any URL looks suspicious, replace with verified congress.gov member page fallback or remove

Report validation results as a structured summary.
  </action>
  <verify>
    <automated>python3 -c "
import csv, re
from collections import Counter

with open('EV-Backend/data/stance_research.csv') as f:
    reader = csv.reader(f)
    header = next(reader)
    rows = list(reader)

valid_topics = {'healthcare','abortion','tariffs','taxes','same-sex-marriage','religious-freedom','trans-athletes','ukraine-support','medicare','fossil-fuels','voting-rights','deportation','social-security','ai-regulation','climate-change','civil-rights','housing','campaign-finance','immigration','misinformation','redistricting'}

errors = []

# Check 1: Column count
bad_cols = [i+2 for i, r in enumerate(rows) if len(r) != 7]
if bad_cols:
    errors.append(f'Bad column count on lines: {bad_cols[:5]}')

# Check 2: Politician count
politicians = set(r[0] for r in rows)
if len(politicians) < 23:
    errors.append(f'Politician count: {len(politicians)} (expected >= 23)')

# Check 3: Topic keys
bad_topics = [r[2] for r in rows if r[2] not in valid_topics]
if bad_topics:
    errors.append(f'Invalid topic_keys: {set(bad_topics)}')

# Check 4: Value range
bad_values = [(r[0], r[2], r[3]) for r in rows if not r[3].strip().isdigit() or int(r[3]) < 1 or int(r[3]) > 5]
if bad_values:
    errors.append(f'Invalid values: {bad_values[:5]}')

# Check 5: Duplicates
pairs = [(r[0], r[2]) for r in rows]
dupes = [p for p, c in Counter(pairs).items() if c > 1]
if dupes:
    errors.append(f'Duplicate pairs: {dupes[:5]}')

# Check 6: Source URL coverage
missing_url1 = [(r[0], r[2]) for r in rows if not r[4].strip()]
if missing_url1:
    errors.append(f'Missing source_url_1: {missing_url1[:5]}')

# Check 7: URL format
bad_urls = []
for r in rows:
    for url in r[4:7]:
        if url.strip() and not url.strip().startswith('http'):
            bad_urls.append(url[:50])
if bad_urls:
    errors.append(f'Bad URL format: {bad_urls[:5]}')

# Check 8: No empty rows
empty_rows = [i+2 for i, r in enumerate(rows) if not r[0].strip() or not r[2].strip()]
if empty_rows:
    errors.append(f'Empty name/topic on lines: {empty_rows[:5]}')

# Check 9-10: Thomson and Bass present
thomson_rows = [r for r in rows if r[0] == 'Kerry Thomson']
bass_rows = [r for r in rows if r[0] == 'Karen Bass']
if len(thomson_rows) == 0:
    errors.append('No Kerry Thomson rows found')
if len(bass_rows) == 0:
    errors.append('No Karen Bass rows found')

if errors:
    for e in errors:
        print(f'FAIL: {e}')
    assert False, f'{len(errors)} validation checks failed'
else:
    print(f'Total rows: {len(rows)}')
    print(f'Politicians: {len(politicians)}')
    print(f'Thomson rows: {len(thomson_rows)}')
    print(f'Bass rows: {len(bass_rows)}')
    print(f'All rows have valid structure')
    print(f'All rows have source_url_1')
    print('PASS: Full Phase 48 CSV validation complete')
"</automated>
  </verify>
  <done>Full CSV validation passes all 11 checks. Both Thomson and Bass are present with valid stance data. All URLs properly formatted and spot-checked. CSV is ready for Phase 50 data import.</done>
</task>

</tasks>

<verification>
1. CSV file at `EV-Backend/data/stance_research.csv` contains rows for both Thomson and Bass
2. All `topic_key` values match one of the 21 defined keys exactly
3. All `value` entries are integers 1-5
4. Every row has at least one non-empty source URL
5. No advocacy group rating URLs appear as sources
6. All URLs are verifiable real pages (no fabricated slugs)
7. Bass has rows for most or all of the 21 topics
8. Thomson has rows for topics where documented positions were found
9. The existing rows for the prior 21 politicians remain unchanged
10. The CSV remains valid (no malformed rows, proper comma separation)
11. Total politician count is at least 23
</verification>

<success_criteria>
- Mayor Bass has sourced stance data for applicable compass topics
- Both Thomson and Bass are represented in the CSV with at least one source per stance
- All stance values are integers 1-5 matching the defined stance descriptions
- Every row has at least one source URL to a specific article, vote record, or official page
- All URLs are real and verifiable
- Full CSV validation passes with zero errors
- CSV confirmed ready for Phase 50 import
</success_criteria>

<output>
After completion, create `.planning/phases/48-mayors-research/48-02-SUMMARY.md`
</output>
