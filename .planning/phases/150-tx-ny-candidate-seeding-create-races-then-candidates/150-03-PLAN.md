---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 03
type: execute
wave: 2
depends_on: ["150-01"]
files_modified:
  - backend/migrations/NNNN_seed_tx_2026_house_candidates.sql
  - backend/data/seed-tx-2026-house/150-03-tx-reconciliation.csv
autonomous: true
requirements: [USHC-02, USHC-03]
must_haves:
  truths:
    - "Every one of the 38 TX US House races carries its full Nov-3 active race_candidates field, each row non-null politician_id, candidate_status='active'"
    - "Every renominated TX incumbent reuses its 148 incumbent_pid; Greg Casar's existing TX-35 record (-100335) is reused as the active TX-37 candidate; TX-35's general is the two new candidates"
    - "Colin Allred (TX-33) is created NEW (live DB confirmed 0 existing record); Steve Toth (TX-2) reuses existing -100515; all renominated incumbents reuse existing pids"
    - "Lost/retired/redistricted-away TX incumbents get NO active race_candidates row in their old seat"
    - "Zero duplicate full_name within TX; no party stored on any candidate card"
  artifacts:
    - path: "backend/migrations/NNNN_seed_tx_2026_house_candidates.sql"
      provides: "Idempotent BEGIN/COMMIT: INSERT new TX politicians + INSERT race_candidates onto the 38 TX races authored in 150-01"
      contains: "race_candidates"
    - path: "backend/data/seed-tx-2026-house/150-03-tx-reconciliation.csv"
      provides: "Per-candidate live-confirmed reuse-vs-new decision with target politician_id / assigned external_id for all 38 TX districts"
      min_lines: 76
  key_links:
    - from: "race_candidates.race_id"
      to: "the 38 TX House races (150-01 scaffold)"
      via: "office_id -> districts.geo_id 48NN join to resolve race_id"
      pattern: "race_candidates"
    - from: "race_candidates.politician_id"
      to: "incumbent_pid (148-incumbent-map.csv) or newly-inserted politician id (live-confirmed)"
      via: "150-03-tx-reconciliation.csv decision"
      pattern: "politician_id"
---

<objective>
Wire the full Nov-3 general field onto all 38 TX US House races as `essentials.race_candidates`, creating new `essentials.politicians` records ONLY for genuinely-new candidates and reusing existing records for every incumbent / previously-seeded figure — after a MANDATORY per-candidate live-DB dedup check (D-03).

Purpose: USHC-02 + USHC-03 for TX. TX is the all-zero-incumbent state (like CA) with the highest cross-district dedup risk: Greg Casar (TX-35 incumbent) is the active TX-37 candidate, and several "new" names per 148 must be live-confirmed before insert.
Output: One idempotent seed migration + a live-confirmed reconciliation CSV; after it runs, every TX House race surfaces its field on `/elections`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-FIELD-TABLE.md
@.planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md

<interfaces>
<!-- essentials.race_candidates VERIFIED LIVE SCHEMA:
     id uuid PK; race_id uuid NOT NULL FK->races; politician_id uuid (D-05: MUST be non-null for House);
     full_name text NOT NULL; first_name/last_name text; photo_url text (leave NULL -> feed resolves via politician_images);
     is_incumbent boolean default false; candidate_status text default 'active' CHECK IN ('active','withdrawn','filed');
     source text (cite source_url); external_id text optional. NO unique constraint -> idempotency is application-enforced:
     check (race_id, full_name) NOT EXISTS before insert. -->
<!-- essentials.politicians: id uuid, external_id int (negative for seeded), full_name, first_name, last_name, is_active bool.
     NO updated_at column (149-01 Rule-3 fix). -->
<!-- D-03 LIVE PRE-CHECK ALREADY DONE for high-risk names (planner-confirmed live 2026-06-29):
     - Greg Casar EXISTS ext -100335 (TX-35 incumbent) -> REUSE as active TX-37 candidate, is_incumbent=false; TX-35 incumbent gets NO active row.
     - Colin Allred -> 0 existing records live -> NEW in TX-33 (CONTEXT's "likely 2024 Senate record" is FALSE per live DB; confirms D-03 mandate).
     - Steve Toth EXISTS ext -100515 -> REUSE as active TX-2 candidate (Crenshaw lost primary, gets no active row).
     - Sylvia Garcia EXISTS ext -100329 (TX-29 incumbent) -> REUSE, renominated.
     RE-RUN the live check per candidate anyway; these are the confirmed-as-of-planning anchors. -->
<!-- TX redistricted incumbents keep their record, get NO active row in their OLD seat:
     TX-9 Al Green (-100309), TX-30 Crockett (-100330), TX-32 Julie Johnson (-100332), TX-33 Veasey (-100333),
     TX-35 Casar (-100335 -- but Casar IS active in TX-37). TX-23 (4823) open-seat vacancy: both candidates NEW. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live reconciliation -- confirm reuse-vs-new per TX candidate (D-03 mandatory)</name>
  <read_first>
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv (TX rows: general_candidates, new_records_needed, nominee_status, incumbent_pid per geo_id 48NN)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv (TX geo_id -> incumbent_pid + external_id, all 37 incumbents + TX-23 VACANT)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-01-SUMMARY.md (the reuse-vs-new reconciliation pattern + external_id-scheme-collision lesson)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md (D-03 cross-district dedup; the Casar/Allred/Toth anchors)
    - backend/src/lib/db.js (pg pool; run via `cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "..."`)
  </read_first>
  <action>
    Produce `backend/data/seed-tx-2026-house/150-03-tx-reconciliation.csv` (columns: cd, geo_id, race_id, full_name, party_from_field, decision[REUSE|NEW], target_politician_id, assign_external_id, is_incumbent, source_url) -- one row per general candidate across all 38 TX districts (~76 rows). Resolve race_id by joining the 150-01 TX races to districts.geo_id.

    For EACH candidate parsed from the 148 `general_candidates` field (party in parens read for context only, NEVER stored -- D-05):
    1. If the name matches the district's renominated TX incumbent -> REUSE, target = the 148 incumbent_pid, is_incumbent=true.
    2. CROSS-DISTRICT REUSE (D-03): Greg Casar (active TX-37) -> REUSE his existing -100335 record, is_incumbent=false (he is the redistricted incumbent of TX-35 running in TX-37). TX-35's own field (Johnny Garcia / Carlos De La Cruz) is all-new; TX-35 incumbent Casar gets NO active row in TX-35.
    3. For every name in `new_records_needed` AND every name not matching the home incumbent -> run a LIVE name query against essentials.politicians (`full_name ILIKE` + sanity on external_id/office context). If a real matching record exists -> flip to REUSE with that UUID (e.g. Steve Toth -100515). If 0 matching records (e.g. Colin Allred returned 0 live) -> decision=NEW, assign a collision-free negative external_id.
    4. Lost/retired/redistricted-away incumbents (TX-2 Crenshaw; TX-8/10/19/21/37/38 retired; TX-9/30/32/33 redistricted-away; TX-35 Casar-runs-TX-37) get NO active race_candidates row in their old seat -- confirm their new field is the listed candidates.

    For new external_ids: query `SELECT external_id FROM essentials.politicians WHERE external_id < 0 ORDER BY external_id`. TX incumbents occupy -100301..-100338; the city band uses -100515 etc. Propose `-(4810000 + cd*100 + seq)` (e.g. TX-1 Yolanda Prince -> -4810101) and VERIFY the -4810000..-4819999 band is empty (0 rows) before use; if occupied, pick another verified-empty band and document it (149-01 collision lesson).

    Re-confirm: every TX district's active field matches 148 (lost incumbents absent, winners present).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; const l=readFileSync('data/seed-tx-2026-house/150-03-tx-reconciliation.csv','utf8').trim().split('\n'); const body=l.slice(1); if(body.length<70){console.error('FAIL row count',body.length);process.exit(1)}; const newids=body.map(r=>r.split(',')).filter(c=>c[5]==='NEW').map(c=>c[7]); const dup=newids.filter((v,i)=>newids.indexOf(v)!==i); if(dup.length){console.error('FAIL dup external_id',dup);process.exit(1)}; console.log('TX reconciliation OK rows',body.length,'new',newids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 150-03-tx-reconciliation.csv has one row per TX general candidate across 38 districts (~76 rows), each with a resolved race_id.
    - Every REUSE row's target_politician_id is a real live essentials.politicians UUID (confirmed via query, not inferred).
    - Greg Casar REUSE in TX-37 (-100335, is_incumbent=false); Steve Toth REUSE in TX-2 (-100515); Colin Allred NEW in TX-33 (live 0-match confirmed); every renominated incumbent REUSE.
    - Lost/retired/redistricted-away incumbents have NO REUSE row in their old seat (TX-2/8/9/10/19/21/30/32/33/35-old/37-old/38).
    - Assigned NEW external_ids are unique within the CSV and collision-free vs the live `external_id < 0` set; band verified empty.
  </acceptance_criteria>
  <done>tx-reconciliation.csv exists, all REUSE UUIDs live-confirmed, Casar/Allred/Toth resolved, lost incumbents absent, external_id band verified empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author + run the idempotent TX House seed migration (new politicians + race_candidates)</name>
  <read_first>
    - backend/data/seed-tx-2026-house/150-03-tx-reconciliation.csv (Task 1 output -- authoritative reuse/new + external_id + race_id map)
    - backend/migrations/1091_seed_ca_2026_house_candidates.sql (validated idempotent BEGIN/COMMIT precedent: INSERT politicians WHERE NOT EXISTS (external_id) + INSERT race_candidates WHERE NOT EXISTS (race_id, full_name); canonical accents; no updated_at)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-01-SUMMARY.md (the TX election UUID -- confirm races resolve to it)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md (D-05 invariants: candidate_status='active', is_incumbent flag, never office_id NULL, never party on card)
  </read_first>
  <action>
    Pick the next free migration number (`ls backend/migrations | sort -t_ -k1 -n | tail -1`, increment; disk wins). Name `NNNN_seed_tx_2026_house_candidates.sql`. All writes in one `BEGIN; ... COMMIT;`. Header cites the field source (148-FIELD-TABLE.md TX section + Wikipedia per-district URLs) + the antipartisan invariant (D-05).

    Migration body, in order:
    1. INSERT the new TX politicians: `INSERT INTO essentials.politicians (id, external_id, full_name, first_name, last_name, is_active) SELECT gen_random_uuid(), v.ext::int, v.full_name, v.first, v.last, true FROM (VALUES ...) v(...) WHERE NOT EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id=v.ext::int)`. Canonical accents preserved. NO essentials.offices rows for challengers (feed resolves via race_candidates.politician_id; an office row would pollute the reps feed / geofence search -- 149-01 A2).
    2. INSERT the race_candidates onto the 38 TX races: for each reconciliation row, `race_id`=resolved TX race, `politician_id`=REUSE target or just-inserted new UUID (resolve new via `(SELECT id FROM essentials.politicians WHERE external_id=...)`), `full_name`, `first_name`, `last_name`, `is_incumbent` per CSV, `candidate_status='active'`, `source`=district source_url. Guard each `WHERE NOT EXISTS (SELECT 1 FROM essentials.race_candidates rc WHERE rc.race_id=v.race_id::uuid AND rc.full_name=v.full_name)`.
    3. NEVER touch the `races` rows (150-01 owns them); NEVER set office_id NULL; NEVER store party on a candidate.

    Post-COMMIT verification block (or run separately), scoped to TX NATIONAL_LOWER (geo_id prefix '48') within the TX 2026 Statewide General election: every TX race has >=1 active candidate (most >=2); 0 NULL politician_id; 0 duplicate full_name within TX; Greg Casar (-100335) active in TX-37; lost incumbents (Crenshaw -100302 etc.) have 0 active rows.

    Run dry first (review SQL), then apply against prod via `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/NNNN_...sql`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; const r=await pool.query(\`SELECT count(DISTINCT r.id) races, count(*) FILTER (WHERE rc.candidate_status='active') active_rc, count(*) FILTER (WHERE rc.candidate_status='active' AND rc.politician_id IS NULL) nullpid FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id LEFT JOIN essentials.race_candidates rc ON rc.race_id=r.id WHERE el.name='TX 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='48'\`); const row=r.rows[0]; const dup=await pool.query(\`SELECT count(*) n FROM (SELECT lower(rc.full_name) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections el ON el.id=r.election_id WHERE el.name='TX 2026 Statewide General' AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='48' AND rc.candidate_status='active' GROUP BY lower(rc.full_name) HAVING count(*)>1) q\`); if(+row.races!==38||+row.nullpid>0||+dup.rows[0].n>0){console.error('FAIL races',row.races,'nullpid',row.nullpid,'dup',dup.rows[0].n);process.exit(1)}; console.log('TX OK: 38 races,',row.active_rc,'active rc, 0 null pid, 0 dup full_name'); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - All 38 TX House races (NATIONAL_LOWER geo '48' within the TX 2026 Statewide General) carry their active field; 0 active rows with NULL politician_id.
    - 0 duplicate full_name within TX (D-03 zero-dup-per-state).
    - Greg Casar (-100335) is an active TX-37 candidate; TX-35 incumbent has no active row in TX-35; Steve Toth active TX-2; Crenshaw/other lost incumbents have 0 active rows.
    - No new essentials.offices rows created; no party stored on any candidate.
    - Re-running the migration inserts 0 additional rows (NOT EXISTS idempotency).
  </acceptance_criteria>
  <done>Migration applied; TX 38-race assertion passes; dedup correct; idempotent on re-run. Record new-candidate external_id list in SUMMARY for downstream headshot/stance plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor -> production DB (`kxsdzaojfaibhuzmclfq`) | Reviewed migration writes new TX politicians + race_candidates to live prod |
| 148 field artifacts -> seed input | Field/identity locked + human-reviewed in Phase 148; reuse-vs-new re-confirmed live (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-09 | Tampering | duplicate incumbent record (v2.4 / mig-1074 vector) | mitigate | Live name-check per candidate; reuse 148 incumbent_pid; Casar cross-district reuse; 0-dup-full_name-per-state gate |
| T-150-10 | Repudiation | lost/redistricted incumbent surfaced as active candidate | mitigate | Lost incumbents get NO active row (reconciliation); 150-verify.sql lost-incumbent-absent assertion |
| T-150-11 | Tampering | race_candidates re-run duplicates | mitigate | (race_id, full_name) NOT EXISTS guard; re-run inserts 0 |
| T-150-12 | DoS | write to wrong election / Governor-style contamination | mitigate | All inserts scoped to TX 2026 Statewide General + NATIONAL_LOWER geo '48' |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure SQL migration) |
</threat_model>

<verification>
- 150-verify.sql USHC-02/03 + D-03/D-05 TX assertions pass after this runs.
- Manual spot-check: cross-check ~5 TX districts against 148-FIELD-TABLE.md source_url citations.
</verification>

<success_criteria>
- 38 TX House races each carry their active field, all politician_id non-null; <=~46 new politician rows; every incumbent/previously-seeded figure reused by id (0 duplicates); Casar/Toth reused, Allred new; lost incumbents absent; migration idempotent.
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md` when done (record: migration number, final NEW count, the Casar/Allred/Toth resolutions, any 148-name flips, the new-candidate external_id list for downstream headshot/stance plans, the 37 zero-stance TX incumbent pids for the stance scope).
</output>
