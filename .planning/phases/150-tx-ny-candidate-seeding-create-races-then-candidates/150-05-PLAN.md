---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 05
type: execute
wave: 3
depends_on: ["150-03"]
files_modified:
  - backend/scripts/seed-tx-ny-house-headshots.py
  - backend/data/seed-tx-2026-house/150-05-tx-headshots.manual.txt
autonomous: true
requirements: [USHC-04]
must_haves:
  truths:
    - "Every newly-seeded TX candidate has a 600x750 headshot mirrored to the politician_photos bucket + a politician_images row, or is a documented honest-skip"
    - "No headshot is the wrong person or a non-free-licensed image (pipeline guards enforced)"
  artifacts:
    - path: "backend/scripts/seed-tx-ny-house-headshots.py"
      provides: "Headshot pipeline targeting the new TX/NY candidate external_id band (clone of seed-ca-house-headshots.py)"
    - path: "backend/data/seed-tx-2026-house/150-05-tx-headshots.manual.txt"
      provides: "external_id|image_url|license lines for 2nd-source TX portraits where Wikipedia has none"
  key_links:
    - from: "new TX candidate negative external_id"
      to: "essentials.politician_images.url + politician_photos/{uuid}-headshot.jpg"
      via: "seed-tx-ny-house-headshots.py (Wikipedia pageimages or --manual)"
      pattern: "politician_images"
---

<objective>
Give every newly-seeded TX candidate a headshot following the established find-headshots conventions (600x750, free-license, Storage-mirrored + politician_images row), reusing the validated Phase 149 headshot pipeline.

Purpose: USHC-04 (TX slice). race_candidates resolve photos via `COALESCE(rc.photo_url, pi.url)` lateral join on politician_id, so a politician_images row makes the candidate's headshot appear on `/elections`. Reused incumbents already have headshots -- only the 150-03 new TX candidates need them.
Output: The TX/NY headshot script (created here, reused by 150-06 for NY) + a TX manual-source list; every new TX candidate has an image row or a documented honest-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-CONTEXT.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md
@backend/scripts/seed-ca-house-headshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the headshot pipeline to target the new TX/NY candidate band + run it for TX</name>
  <read_first>
    - backend/scripts/seed-ca-house-headshots.py (the validated Phase-149 pipeline: Wikipedia pageimages -> free-license check -> hardened wrong-person guard -> 4:5 crop -> 600x750 LANCZOS q90 -> PUT politician_photos/{uuid}-headshot.jpg x-upsert -> INSERT politician_images WHERE NOT EXISTS; targets by explicit negative external_id list; `--manual` reads external_id|image_url|license)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md (the authoritative new TX-candidate external_id list)
    - .planning/phases/149-ca-candidate-seeding-race-candidates-only-turnkey/149-03-SUMMARY.md (the 149 headshot run: auto-pass rate ~4/36, the rest manual/honest-skip; the wrong-person guard hardening)
  </read_first>
  <action>
    Clone `seed-ca-house-headshots.py` to `backend/scripts/seed-tx-ny-house-headshots.py`. Swap ONLY the target-selection: parameterize it to accept a state/band argument (or two explicit external_id lists -- TX from 150-03-SUMMARY, NY from 150-04-SUMMARY) so the SAME script serves both 150-05 (TX) and 150-06 (NY). Because new challengers have NO essentials.offices row, the target query selects by explicit negative external_id list `AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id = p.id)`. Keep EVERY guard intact: free-license-only, hardened wrong-person guard (title must contain candidate first+surname; reject election/place/event/non-political disambiguators), 4:5 crop -> 600x750, x-upsert, idempotent INSERT.

    Run the automatic Wikipedia pass for the TX band: `python backend/scripts/seed-tx-ny-house-headshots.py` with the TX external_id list. For TX candidates with no free Wikipedia portrait, build `backend/data/seed-tx-2026-house/150-05-tx-headshots.manual.txt` with `external_id|image_url_or_path|license` lines sourced from .gov / campaign / Ballotpedia free-license portraits (one fetch attempt per URL -- MEMORY one-try rule), then run with `--manual backend/data/seed-tx-2026-house/150-05-tx-headshots.manual.txt`.

    For any TX candidate with no free-license image anywhere after the manual pass, record an honest-skip (name + external_id + reason) in the SUMMARY so 150-verify.sql can pin it -- NEVER a placeholder or wrong-person image.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {pool} from './src/lib/db.js'; import {readFileSync} from 'fs'; const sum=readFileSync('../.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md','utf8'); const ids=[...sum.matchAll(/-481[0-9]{4}/g)].map(m=>+m[0]); if(!ids.length){console.error('no new TX external_ids found in 150-03-SUMMARY (check band)');process.exit(1)}; const r=await pool.query('SELECT p.external_id FROM essentials.politicians p WHERE p.external_id = ANY(\$1) AND NOT EXISTS (SELECT 1 FROM essentials.politician_images pi WHERE pi.politician_id=p.id)',[ids]); console.log('new TX candidates still missing headshot:', r.rows.map(x=>x.external_id)); await pool.end()"</automated>
  </verify>
  <acceptance_criteria>
    - seed-tx-ny-house-headshots.py targets the new-candidate set by explicit external_id list (not the offices-join query) and keeps all original guards; parameterized so 150-06 reuses it for NY.
    - Every new TX candidate has an essentials.politician_images row EXCEPT explicitly documented honest-skips (name + external_id + no-free-image reason) in the SUMMARY.
    - Each image is 600x750, mirrored to politician_photos/{uuid}-headshot.jpg, free-licensed; wrong-person guard intact.
    - Re-running inserts 0 additional image rows (idempotent WHERE NOT EXISTS).
  </acceptance_criteria>
  <done>Headshot script created + run for TX; the missing-headshot query returns only documented honest-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image source (Wikipedia / .gov / Ballotpedia) -> Storage + DB | Untrusted external images fetched, cropped, uploaded to the politician_photos bucket via service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-17 | Spoofing | wrong-person image attached to a candidate | mitigate | Hardened wrong-person guard retained from seed-ca-house-headshots.py; manual sources human-verified; spot-check in 150-12 |
| T-150-18 | Tampering | non-free-licensed image ingested | mitigate | Free-license-only guard retained |
| T-150-19 | Information Disclosure | wrong bucket/path overwrite | mitigate | x-upsert to deterministic politician_photos/{uuid}-headshot.jpg keyed on candidate UUID |
| T-150-SC | Tampering | pip installs | accept | No new packages (Pillow/requests/psycopg2 already in repo) |
</threat_model>

<verification>
- Missing-headshot query returns only documented honest-skips.
- Manual spot-check (150-12): sample new TX headshots against source pages for identity.
</verification>

<success_criteria>
- Every new TX candidate has a 600x750 politician_images row (or documented honest-skip); idempotent; all pipeline guards intact; the shared script is ready for NY (150-06).
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md` when done (record: TX candidates imaged, honest-skips with reasons + external_ids for gate pinning, manual-source URLs, the shared script path for 150-06).
</output>
