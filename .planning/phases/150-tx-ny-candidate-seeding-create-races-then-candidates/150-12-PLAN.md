---
phase: 150-tx-ny-candidate-seeding-create-races-then-candidates
plan: 12
type: execute
wave: 4
depends_on: ["150-01", "150-02", "150-03", "150-04", "150-05", "150-06", "150-07", "150-08", "150-09", "150-10", "150-11"]
files_modified:
  - backend/scripts/150-coordinate-smoke.ts
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
autonomous: true
must_haves:
  truths:
    - "150-verify.sql runs read-only against prod and EVERY labeled assertion (USHC-02/03/04/05 + D-01/D-02/D-03/D-05) PASSES with exit 0"
    - "For >=3 in-district TX + >=3 in-district NY House test coordinates, the surfacing join returns the resident's House race with its full candidate field (CHALLENGER present, not just incumbent)"
    - "0 unsourced stance rows for the in-scope set + 0 duplicate full_name per state across all 38 TX + 26 NY districts; honest-skips pinned by UUID; lost incumbents absent"
  artifacts:
    - path: "backend/scripts/150-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers a TX + NY House geofence centroid -> assert race + field surfaces with challenger"
  key_links:
    - from: "150-verify.sql + 150-coordinate-smoke.ts"
      to: "the 38 TX + 26 NY House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 150 end-to-end: run the per-state TX/NY-scoped `150-verify.sql` gate green and add a coordinate-surfacing smoke that confirms >=3 TX + >=3 NY House races actually surface for an in-district address.

Purpose: USHC-02/03/04/05 consolidated verification for TX + NY -- the second of three seeding-phase proofs. This gate + smoke feed Phase 152's full-144 gate.
Output: A passing gate + coordinate smoke; Phase 150 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/scripts/150-verify.sql
@backend/scripts/149-coordinate-smoke.ts
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-03-SUMMARY.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-04-SUMMARY.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-05-SUMMARY.md
@.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the TX+NY coordinate-surfacing smoke + finalize honest-skip pins in 150-verify.sql</name>
  <read_first>
    - backend/scripts/149-coordinate-smoke.ts (the validated read-only ST_Covers surfacing smoke; mirror it for two states -- sample TX geo_ids 48NN + NY geo_ids 36NN)
    - backend/scripts/150-verify.sql (the gate authored in 150-02; its honest-skip + in-scope TEMP-table pins)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers -> district geo_id -> office -> race -> race_candidates; the join discriminator d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc))
    - the 150-05/06 SUMMARYs (headshot honest-skips) + 150-07..11 SUMMARYs (whole-record-stance honest-skips) -- the pids to pin by UUID
  </read_first>
  <action>
    Author `backend/scripts/150-coordinate-smoke.ts` (read-only) by cloning 149-coordinate-smoke.ts and parameterizing for two states: pick >=3 spread TX geo_ids (e.g. 4801/4823/4838 -- include the TX-23 open seat) + >=3 spread NY geo_ids (e.g. 3601/3610/3621 -- include NY-10 lost-primary + NY-21 3-candidate). For each, resolve the election by name ('TX 2026 Statewide General' / 'NY 2026 Statewide General'), compute an interior centroid via `ST_PointOnSurface(gb.geometry)`, run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point -> district -> office -> race within that election, NATIONAL_LOWER, geo prefix '48'/'36' -> race_candidates active count + linked count + challenger count). Assert each sample race returns >=2 active candidates (>=3 for NY-13/NY-21), non-null politician_id, AND >=1 challenger (is_incumbent=false) -- the Pitfall-5 two-path guard. For NY-10/NY-13 also assert the active candidate is the WINNER not the lost incumbent (by full_name match).

    Gather the documented honest-skips (headshots from 150-05/06; whole-record stances from 150-07..11 SUMMARYs) and confirm they are pinned by exact UUID/external_id with explicit ORDER BY inside 150-verify.sql's pin sets; if any are missing, add them (this pin-list update is the only edit allowed to 150-verify.sql here -- not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/150-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 150-coordinate-smoke.ts is read-only and asserts >=3 TX + >=3 NY House races surface with >=2 active candidates incl. >=1 challenger for an in-district centroid.
    - NY-10 + NY-13 sample assert the primary WINNER is active (lost incumbent absent).
    - All documented headshot + whole-record-stance honest-skips are pinned by exact UUID/external_id with explicit ORDER BY in 150-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for >=3 TX + >=3 NY districts; honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 150-verify.sql gate green</name>
  <read_first>
    - backend/scripts/150-verify.sql (the full gate)
    - .planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-02-SUMMARY.md (the assertion labels + in-scope-set convention + NY-partial exclusion)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/150-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (elections/races -> 150-01; TX records -> 150-03; NY records -> 150-04; headshots -> 150-05/06; stances -> 150-07..11) and re-run after fix -- do NOT relax an assertion to make it pass (149 T-149-18). Confirm the gate stays per-state-scoped (no cross-state contamination), honors the D-01 NY-partial exclusion, and is write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/150-verify.sql >/tmp/150gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -25 /tmp/150gate.log; exit 1; fi; grep -c 'PASS' /tmp/150gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 150-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 38 TX + 26 NY House races each carry their active field, 0 null politician_id; 0 duplicate full_name per state; lost incumbents (TX-2/NY-10/NY-13) absent + winners present; D-02 minor-line candidates present; every new candidate has a headshot (or pinned skip); 0 unsourced stances for the in-scope set; federal-24 coverage or pinned whole-record skip; NY partials untouched.
    - Coordinate smoke (Task 1) passes for >=3 TX + >=3 NY districts.
  </acceptance_criteria>
  <done>150-verify.sql green (exit 0); coordinate smoke green; Phase 150 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke -> production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-32 | Repudiation | gate passes while a district silently has no challenger | mitigate | Coordinate smoke asserts >=1 is_incumbent=false candidate per sample race (Pitfall-5) |
| T-150-33 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-150-34 | Repudiation | cross-state contamination false-passes a count | mitigate | Gate per-state scoped (geo '48'/'36' within named elections); re-confirmed write-free |
| T-150-35 | Repudiation | NY-10/NY-13 lost incumbent silently surfaced | mitigate | Smoke + gate assert winner active, lost incumbent absent |
| T-150-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 150-verify.sql exits 0, all PASS.
- 150-coordinate-smoke.ts surfaces >=3 TX + >=3 NY House races with challenger present + lost-incumbent winner.
</verification>

<success_criteria>
- All USHC-02/03/04/05 + D-01/D-02/D-03/D-05 assertions PASS; coordinate smoke green; TX + NY Wave-1 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/150-tx-ny-candidate-seeding-create-races-then-candidates/150-12-SUMMARY.md` when done (record: final per-state per-district candidate counts, total new records TX + NY, total stanced candidates, all honest-skips pinned, gate PASS log, smoke results -- this becomes the TX+NY half of the Phase 152 full-144 gate input).
</output>
