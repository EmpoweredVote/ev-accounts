---
phase: 123-ward-geofencing-all-7-cities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/load-ma-ward-boundaries.ts
autonomous: true
requirements:
  - MAGE-16
  - MAGE-17
  - MAGE-18
  - MAGE-19
  - MAGE-20
  - MAGE-21
  - MAGE-22

must_haves:
  truths:
    - "load-ma-ward-boundaries.ts CITY_CONFIGS map has 7 new entries: NEWTON, SOMERVILLE, LYNN, FALL RIVER, WALTHAM, MEDFORD, NEW BEDFORD"
    - "Script parseArgs() handles city names with spaces ('FALL RIVER', 'NEW BEDFORD') without splitting on space"
    - "8 newton-ma-council-ward-N rows (N=1..8) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "7 somerville-ma-council-ward-N rows (N=1..7) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "7 lynn-ma-council-ward-N rows (N=1..7) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "9 fall-river-ma-council-ward-N rows (N=1..9) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "9 waltham-ma-council-ward-N rows (N=1..9) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "8 medford-ma-council-ward-N rows (N=1..8) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "6 new-bedford-ma-council-ward-N rows (N=1..6) in essentials.geofence_boundaries, mtfcc='X0014'"
    - "All 54 new rows are dissolved ward polygons (not precinct polygons) — row count per city matches ward count, not precinct count"
  artifacts:
    - path: "backend/scripts/load-ma-ward-boundaries.ts"
      provides: "Extended script with 7 new CITY_CONFIGS entries + space-safe parseArgs"
      min_lines: 110
    - path: "essentials.geofence_boundaries"
      provides: "54 new X0014 ward polygons for all 7 Phase 123 cities"
      contains: "newton-ma-council-ward-, somerville-ma-council-ward-, lynn-ma-council-ward-, fall-river-ma-council-ward-, waltham-ma-council-ward-, medford-ma-council-ward-, new-bedford-ma-council-ward-"
  key_links:
    - from: "MassGIS WARDSPRECINCTS2022_POLY FeatureServer (TOWN='NEWTON'|'SOMERVILLE'|...)"
      to: "essentials.geofence_boundaries (geo_id='city-ma-council-ward-N', mtfcc='X0014')"
      via: "load-ma-ward-boundaries.ts ST_Union dissolution + pool.query INSERT"
      pattern: "ON CONFLICT.*geo_id.*mtfcc.*DO NOTHING"
---

<objective>
Extend load-ma-ward-boundaries.ts with 7 new CITY_CONFIGS entries (Newton, Somerville, Lynn, Fall River, Waltham, Medford, New Bedford) and fix parseArgs() to handle city names with spaces. Then run the script for all 7 cities to load dissolved ward polygons into essentials.geofence_boundaries.

Purpose: All subsequent migrations (706–712) require ward polygons to be in geofence_boundaries before they run — each migration's pre-flight DO $$ block raises EXCEPTION if the polygon rows are missing. This plan loads those rows.

Output: 54 new X0014 geofence_boundary rows across 7 cities (8+7+7+9+9+8+6), script extended and committed.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-ward-geofencing-all-7-cities/123-RESEARCH.md

<interfaces>
<!-- Existing script structure — extend, do not rewrite -->

From backend/scripts/load-ma-ward-boundaries.ts (CURRENT STATE — 4 cities):

CITY_CONFIGS entries to ADD (append to existing map, do not remove existing keys):
  NEWTON:       { geoIdPrefix: 'newton-ma-council-ward-',      wardLabel: 'Ward' }
  SOMERVILLE:   { geoIdPrefix: 'somerville-ma-council-ward-',  wardLabel: 'Ward' }
  LYNN:         { geoIdPrefix: 'lynn-ma-council-ward-',        wardLabel: 'Ward' }
  'FALL RIVER': { geoIdPrefix: 'fall-river-ma-council-ward-',  wardLabel: 'Ward' }
  WALTHAM:      { geoIdPrefix: 'waltham-ma-council-ward-',     wardLabel: 'Ward' }
  MEDFORD:      { geoIdPrefix: 'medford-ma-council-ward-',     wardLabel: 'Ward' }
  'NEW BEDFORD':{ geoIdPrefix: 'new-bedford-ma-council-ward-', wardLabel: 'Ward' }

parseArgs() BUG TO FIX — city names with spaces:
  Current code: const city = (args[cityIdx + 1] ?? '').toUpperCase()
  This works for single-word cities. For "FALL RIVER" passed as --city "FALL RIVER",
  args[cityIdx + 1] correctly gets 'FALL RIVER' (the shell quotes ensure it arrives
  as a single element). The real issue is the ALLOWED_CITIES check uses
  Object.keys(CITY_CONFIGS) which will include 'FALL RIVER' and 'NEW BEDFORD' once
  added. However, the error message currently lists only 4 cities — update it.
  Also: the ALLOWED_CITIES constant is derived from Object.keys(CITY_CONFIGS), so
  adding the new keys automatically extends the allowlist.

Usage comment to update (add 7 new example invocations):
  npx tsx scripts/load-ma-ward-boundaries.ts --city NEWTON --ward-count 8
  npx tsx scripts/load-ma-ward-boundaries.ts --city SOMERVILLE --ward-count 7
  npx tsx scripts/load-ma-ward-boundaries.ts --city LYNN --ward-count 7
  npx tsx scripts/load-ma-ward-boundaries.ts --city "FALL RIVER" --ward-count 9
  npx tsx scripts/load-ma-ward-boundaries.ts --city WALTHAM --ward-count 9
  npx tsx scripts/load-ma-ward-boundaries.ts --city MEDFORD --ward-count 8
  npx tsx scripts/load-ma-ward-boundaries.ts --city "NEW BEDFORD" --ward-count 6

Ward counts per city (verified from MassGIS live API, RESEARCH.md):
  NEWTON:       8 wards (32 precincts = 4/ward)
  SOMERVILLE:   7 wards (28 precincts = 4/ward)
  LYNN:         7 wards (28 precincts = 4/ward)
  FALL RIVER:   9 wards (27 precincts = 3/ward)
  WALTHAM:      9 wards (18 precincts = 2/ward)
  MEDFORD:      8 wards (16 precincts = 2/ward)
  NEW BEDFORD:  6 wards (36 precincts = 6/ward)
  Total expected: 8+7+7+9+9+8+6 = 54 dissolved ward polygons

CRITICAL — TOWN filter values for MassGIS query (must be uppercase, spaces preserved):
  Newton:      TOWN='NEWTON'
  Somerville:  TOWN='SOMERVILLE'
  Lynn:        TOWN='LYNN'
  Fall River:  TOWN='FALL RIVER'    ← space in name, URL-encode as FALL%20RIVER
  Waltham:     TOWN='WALTHAM'
  Medford:     TOWN='MEDFORD'
  New Bedford: TOWN='NEW BEDFORD'   ← space in name, URL-encode as NEW%20BEDFORD
  (encodeURIComponent(city) handles the space correctly)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend load-ma-ward-boundaries.ts with 7 new city configs</name>
  <files>backend/scripts/load-ma-ward-boundaries.ts</files>
  <read_first>
    - backend/scripts/load-ma-ward-boundaries.ts — read entire file before editing; understand current CITY_CONFIGS map structure, parseArgs(), usage comment, and ward counts comment block at top
    - .planning/phases/123-ward-geofencing-all-7-cities/123-RESEARCH.md — Section "Script Strategy" for exact CITY_CONFIGS entries and space-handling note; Section "GIS Data Sources" for verified ward counts
  </read_first>
  <action>
    Edit backend/scripts/load-ma-ward-boundaries.ts. Make ONLY the following targeted changes — do not restructure or rewrite other logic:

    1. UPDATE the file-level ward counts comment block (lines ~25-30) to add 7 new cities:
       Newton:      8 wards, 32 precincts (4 per ward)
       Somerville:  7 wards, 28 precincts (4 per ward)
       Lynn:        7 wards, 28 precincts (4 per ward)
       Fall River:  9 wards, 27 precincts (3 per ward)
       Waltham:     9 wards, 18 precincts (2 per ward)
       Medford:     8 wards, 16 precincts (2 per ward)
       New Bedford: 6 wards, 36 precincts (6 per ward)

    2. ADD 7 new entries to CITY_CONFIGS (append after QUINCY entry, before the closing brace):
       NEWTON:        { geoIdPrefix: 'newton-ma-council-ward-',      wardLabel: 'Ward' }
       SOMERVILLE:    { geoIdPrefix: 'somerville-ma-council-ward-',  wardLabel: 'Ward' }
       LYNN:          { geoIdPrefix: 'lynn-ma-council-ward-',        wardLabel: 'Ward' }
       'FALL RIVER':  { geoIdPrefix: 'fall-river-ma-council-ward-',  wardLabel: 'Ward' }
       WALTHAM:       { geoIdPrefix: 'waltham-ma-council-ward-',     wardLabel: 'Ward' }
       MEDFORD:       { geoIdPrefix: 'medford-ma-council-ward-',     wardLabel: 'Ward' }
       'NEW BEDFORD': { geoIdPrefix: 'new-bedford-ma-council-ward-', wardLabel: 'Ward' }

    3. UPDATE the usage comment block at the top (lines ~38-44) to add 7 new invocation examples for the Phase 123 cities (after the existing 4 Quincy/Brockton/Lowell/Springfield lines).

    4. UPDATE the error message in parseArgs() that currently lists "SPRINGFIELD, LOWELL, BROCKTON, QUINCY" to say "use --city CITY where CITY is a key in CITY_CONFIGS (e.g., SPRINGFIELD, NEWTON, 'FALL RIVER')" so it self-describes the multi-word key pattern.

    DO NOT change: MASSGIS_BASE_URL, MTFCC, STATE, SOURCE constants; fetchJson(); Pool setup; dissolution INSERT SQL; post-insert verification logic; ALLOWED_CITIES (it auto-updates because it uses Object.keys(CITY_CONFIGS)).

    CRITICAL (pitfall 2 from RESEARCH.md): 'FALL RIVER' and 'NEW BEDFORD' use quoted string keys in the CITY_CONFIGS object literal. TypeScript supports quoted string keys in Record<string, CityConfig>. The ALLOWED_CITIES constant derived from Object.keys() will include these keys with spaces. The shell quote approach (--city "FALL RIVER") ensures args[cityIdx + 1] gets the full string with space as a single element. encodeURIComponent('FALL RIVER') correctly produces 'FALL%20RIVER' for the MassGIS URL. No further parsing changes are required.

    After editing, verify the TypeScript compiles without errors before running:
      cd C:/EV-Accounts/backend && npx tsc --noEmit scripts/load-ma-ward-boundaries.ts 2>&1 | head -20
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CITY_CONFIGS has 11 entries total (4 existing + 7 new); keys are exact uppercase: SPRINGFIELD, LOWELL, BROCKTON, QUINCY, NEWTON, SOMERVILLE, LYNN, 'FALL RIVER', WALTHAM, MEDFORD, 'NEW BEDFORD'
    - ALLOWED_CITIES (Object.keys(CITY_CONFIGS)) therefore has 11 entries
    - Ward counts comment block at file top mentions all 7 new cities with correct counts
    - Usage comment includes --city "FALL RIVER" --ward-count 9 and --city "NEW BEDFORD" --ward-count 6 examples
    - TypeScript compiles without errors (npx tsc --noEmit exits 0)
    - No changes to dissolution logic, fetchJson, Pool setup, INSERT SQL, or post-insert verification
  </acceptance_criteria>
  <done>load-ma-ward-boundaries.ts extended with 7 new CITY_CONFIGS entries. TypeScript compiles. Ready to run imports.</done>
</task>

<task type="auto">
  <name>Task 2: Run polygon imports for all 7 cities</name>
  <files>backend/scripts/load-ma-ward-boundaries.ts</files>
  <read_first>
    - backend/scripts/load-ma-ward-boundaries.ts — confirm Task 1 edits are present before running
    - .planning/phases/123-ward-geofencing-all-7-cities/123-RESEARCH.md — Section "GIS Data Sources" (TOWN filter values + ward counts), Section "Common Pitfalls" (pitfalls 2, 5, 6)
  </read_first>
  <action>
    Run load-ma-ward-boundaries.ts for all 7 cities in sequence. All commands run from C:/EV-Accounts/backend.

    First, dry-run NEWTON to confirm script works and fetches 32 precincts grouped into 8 wards:
      cd C:/EV-Accounts/backend && npx tsx scripts/load-ma-ward-boundaries.ts --city NEWTON --ward-count 8 --dry-run

    If dry-run shows 8 ward groups with 4 precincts each: proceed with live inserts.
    If dry-run fails: diagnose before continuing.

    Run live imports in this exact order (ORDER MATTERS — each is independent but sequential to avoid pool conflicts):
      1. npx tsx scripts/load-ma-ward-boundaries.ts --city NEWTON --ward-count 8
      2. npx tsx scripts/load-ma-ward-boundaries.ts --city SOMERVILLE --ward-count 7
      3. npx tsx scripts/load-ma-ward-boundaries.ts --city LYNN --ward-count 7
      4. npx tsx scripts/load-ma-ward-boundaries.ts --city "FALL RIVER" --ward-count 9
      5. npx tsx scripts/load-ma-ward-boundaries.ts --city WALTHAM --ward-count 9
      6. npx tsx scripts/load-ma-ward-boundaries.ts --city MEDFORD --ward-count 8
      7. npx tsx scripts/load-ma-ward-boundaries.ts --city "NEW BEDFORD" --ward-count 6

    Each run should end with "All N {CITY} ward polygons loaded successfully." and zero warnings.

    CRITICAL (pitfall 5): MassGIS TOWN field is uppercase. The script sends the city arg as-is (already uppercase from CITY_CONFIGS key). encodeURIComponent handles spaces. No further normalization needed.

    CRITICAL (pitfall 6): The script's INSERT SQL already includes outSR=4326 in MASSGIS_BASE_URL construction. Confirm the URL logged by the script contains 'outSR=4326' before each city inserts.

    After all 7 runs, verify total counts via Supabase execute_sql:
      SELECT geo_id, COUNT(*) as ct
      FROM essentials.geofence_boundaries
      WHERE mtfcc = 'X0014'
        AND (
          geo_id LIKE 'newton-ma-council-ward-%'
          OR geo_id LIKE 'somerville-ma-council-ward-%'
          OR geo_id LIKE 'lynn-ma-council-ward-%'
          OR geo_id LIKE 'fall-river-ma-council-ward-%'
          OR geo_id LIKE 'waltham-ma-council-ward-%'
          OR geo_id LIKE 'medford-ma-council-ward-%'
          OR geo_id LIKE 'new-bedford-ma-council-ward-%'
        )
      GROUP BY geo_id
      ORDER BY geo_id;
    Expected: 54 rows (8 newton + 7 somerville + 7 lynn + 9 fall-river + 9 waltham + 8 medford + 6 new-bedford), each with ct=1.
  </action>
  <verify>
    <automated>
      SELECT
        SUM(CASE WHEN geo_id LIKE 'newton-ma-council-ward-%' THEN 1 ELSE 0 END) AS newton,
        SUM(CASE WHEN geo_id LIKE 'somerville-ma-council-ward-%' THEN 1 ELSE 0 END) AS somerville,
        SUM(CASE WHEN geo_id LIKE 'lynn-ma-council-ward-%' THEN 1 ELSE 0 END) AS lynn,
        SUM(CASE WHEN geo_id LIKE 'fall-river-ma-council-ward-%' THEN 1 ELSE 0 END) AS fall_river,
        SUM(CASE WHEN geo_id LIKE 'waltham-ma-council-ward-%' THEN 1 ELSE 0 END) AS waltham,
        SUM(CASE WHEN geo_id LIKE 'medford-ma-council-ward-%' THEN 1 ELSE 0 END) AS medford,
        SUM(CASE WHEN geo_id LIKE 'new-bedford-ma-council-ward-%' THEN 1 ELSE 0 END) AS new_bedford,
        COUNT(*) AS total
      FROM essentials.geofence_boundaries
      WHERE mtfcc = 'X0014'
        AND (
          geo_id LIKE 'newton-ma-council-ward-%'
          OR geo_id LIKE 'somerville-ma-council-ward-%'
          OR geo_id LIKE 'lynn-ma-council-ward-%'
          OR geo_id LIKE 'fall-river-ma-council-ward-%'
          OR geo_id LIKE 'waltham-ma-council-ward-%'
          OR geo_id LIKE 'medford-ma-council-ward-%'
          OR geo_id LIKE 'new-bedford-ma-council-ward-%'
        );
      -- Expected: newton=8, somerville=7, lynn=7, fall_river=9, waltham=9, medford=8, new_bedford=6, total=54
    </automated>
  </verify>
  <acceptance_criteria>
    - Dry-run for NEWTON reports exactly 8 ward groups (not 32 precincts individually)
    - All 7 cities run to completion with "loaded successfully" message — no ERROR or WARNING lines
    - Query result: newton=8, somerville=7, lynn=7, fall_river=9, waltham=9, medford=8, new_bedford=6, total=54
    - All 54 rows have mtfcc='X0014' and state='25' (Massachusetts FIPS state code)
    - Geometry column is non-null for all 54 rows (dissolved ward polygons, not NULL)
    - Script is idempotent: re-running any city invocation produces "Skipped (already exists)" for all wards (ON CONFLICT DO NOTHING)
  </acceptance_criteria>
  <done>54 X0014 ward polygon rows loaded for all 7 Phase 123 cities. Counts verified via SQL. Migrations 706-712 are now unblocked (their pre-flight guards will pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MassGIS FeatureServer → script | External HTTP response; WARD field values used to construct geo_ids |
| Script → Supabase pool | Pool.query with $N parameterized values; geometry array passed as $7::text[] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-M1 | Tampering | WARD field value from MassGIS | mitigate | Existing parseInt + isNaN + range 1..wardCount validation in script (T-119-M1 inherited); no change needed |
| T-123-M2 | Tampering | TOWN field case + space | mitigate | Script keys are uppercase in CITY_CONFIGS; encodeURIComponent handles space in FALL RIVER / NEW BEDFORD correctly; ALLOWED_CITIES check prevents injection |
| T-123-M3 | Tampering | Precinct vs ward confusion | mitigate | Script groups by WARD and runs ST_Union; post-insert count check confirms = wardCount (not precinct count) |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No new packages; script uses existing pg, https, dotenv, tsx |
</threat_model>

<verification>
After Task 2 completes:
- SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0014' AND geo_id LIKE 'newton-ma-council-ward-%'; → 8
- SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0014' AND geo_id LIKE 'fall-river-ma-council-ward-%'; → 9
- SELECT COUNT(*) FROM essentials.geofence_boundaries WHERE mtfcc='X0014' AND geo_id LIKE 'new-bedford-ma-council-ward-%'; → 6
- Total new X0014 rows across all 7 cities: 54
</verification>

<success_criteria>
54 dissolved ward polygon rows loaded across all 7 Phase 123 cities. All polygons are in WGS-84 (SRID 4326). load-ma-ward-boundaries.ts is extended with 11 CITY_CONFIGS entries. Migrations 706-712 are unblocked.
</success_criteria>

<output>
Create .planning/phases/123-ward-geofencing-all-7-cities/123-01-SUMMARY.md when done.
</output>
