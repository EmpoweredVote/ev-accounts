---
phase: 17-live-alpha-deployment
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Migrations 026–029 are confirmed applied to the production Supabase instance with no failures reported"
    - "The smoke test suite exits 0 against the production URL, confirming all automated checks pass"
    - "The admin UI loads at the production URL without JavaScript console errors"
    - "Alpha users can reach the live deployment"
  artifacts: []
  key_links: []
---

<objective>
Gate Phase 17 completion on human-confirmed production execution. Plans 01 and 02 created the tooling; this plan ensures the tooling was actually run against production and that the results pass.

Purpose: Success criterion 4 ("demonstrably accessible to Alpha users") cannot be satisfied by artifact creation alone. A human must run the migration script against production, run the smoke test against the live URL, and visually confirm the admin UI loads. This plan captures those confirmations as blocking checkpoints before Phase 17 is marked complete.

Output: Human-confirmed production state — migrations applied, smoke test passing, admin UI reachable.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-live-alpha-deployment/17-01-SUMMARY.md
@.planning/phases/17-live-alpha-deployment/17-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Apply migrations to production</name>
  <what-built>applyMigrations.ts (Plan 01) and DEPLOY.md runbook</what-built>
  <how-to-verify>
Run the migration apply script against the production DATABASE_URL (direct connection, port 5432):

```bash
DATABASE_URL="postgresql://postgres.<ref>:<pwd>@db.<ref>.supabase.co:5432/postgres" \
  npx tsx backend/scripts/applyMigrations.ts
```

Expected output — each migration should show either `[026] OK` or `[026] SKIP — already applied`. No migration should show `[026] FAIL`.

Final line must be: `All migrations applied successfully.`

Then run the post-apply verification queries in Supabase Dashboard → SQL Editor (listed in DEPLOY.md Step 2) to confirm the is_candidate column and all four RPCs exist.
  </how-to-verify>
  <resume-signal>
Paste the script output here, or type "applied" if all migrations show OK or SKIP with no failures. If a migration failed, describe which one and what the error was.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 2: Smoke test against production URL</name>
  <what-built>smokeTest.ts (Plan 02)</what-built>
  <how-to-verify>
Run the smoke test suite against the production backend URL:

```bash
SMOKE_TEST_URL=https://<backend-domain> npx tsx backend/scripts/smokeTest.ts
```

Expected output: all four automated checks show `[PASS]`, the admin UI check shows `[SKIP]`, and the final line reads `SMOKE TEST PASSED`.

If any check shows `[FAIL]`, do not proceed — investigate and fix the underlying issue before marking this checkpoint complete.
  </how-to-verify>
  <resume-signal>
Paste the smoke test output here, or type "passed" if the final line reads "SMOKE TEST PASSED". If a check failed, describe which one and what the output showed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Admin UI visual verification</name>
  <what-built>Admin UI deployment (Step 4 of DEPLOY.md)</what-built>
  <how-to-verify>
1. Open the production admin UI URL in a browser.
2. Open the browser developer console (F12 → Console tab).
3. Confirm the page loads — you should see the login screen or dashboard.
4. Confirm there are no red JavaScript errors in the console. Network errors for missing assets or 401s from unauthenticated requests are expected and acceptable. Only JS runtime errors (TypeError, ReferenceError, etc.) are failures.
5. Log in with admin credentials and confirm the dashboard renders.
  </how-to-verify>
  <resume-signal>
Type "approved" if the admin UI loads without JS errors. Describe any issues if the UI fails to load or shows console errors.
  </resume-signal>
</task>

</tasks>

<verification>
All three checkpoints confirmed by human:
1. Migration script output shows OK or SKIP for all four migrations (026–029), no FAIL lines, final line is "All migrations applied successfully."
2. Smoke test output shows SMOKE TEST PASSED with all automated checks as [PASS].
3. Admin UI loads at production URL without JavaScript runtime errors.
</verification>

<success_criteria>
1. Migrations 026–029 confirmed applied to production — is_candidate column and all four RPCs verified to exist via post-apply queries.
2. `SMOKE_TEST_URL=<production-url> npx tsx backend/scripts/smokeTest.ts` exits 0 with SMOKE TEST PASSED.
3. Admin UI accessible and functional at production URL with no JS runtime errors.
4. Phase 17 complete — Live Alpha deployment confirmed operational.
</success_criteria>

<output>
After completion, create `.planning/phases/17-live-alpha-deployment/17-03-SUMMARY.md`
</output>
