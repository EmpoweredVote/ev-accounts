---
phase: 87-stance-accuracy-audit-agent-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/research-stances/SKILL.md
autonomous: true
requirements:
  - SACC-04
tags:
  - agent-config
  - skill-update
  - five-chairs

must_haves:
  truths:
    - "When a researcher agent reads SKILL.md, the SCALE RULE block no longer exists — it has been replaced by the five-chairs framing block"
    - "Future stance-research dispatches paste the five-chairs block (not the SCALE RULE block) into each agent prompt"
    - "The five-chairs block makes the agent's evaluative posture text-match-to-chair, not party-direction-inference"
    - "The defensibility claim ('they both support [specific written text]') is present in the SKILL.md block"
    - "The 'spoke has no correct end / direction varies by topic' rule is present"
    - "All surrounding SKILL.md structure (STEP 1 dispatch rules, TOPIC SCALE REFERENCE block, TOOL RULE — CRITICAL block) is preserved unchanged"
  artifacts:
    - path: ".claude/skills/research-stances/SKILL.md"
      provides: "Researcher agent system prompt with five-chairs framing"
      contains: "FIVE-CHAIRS FRAMING"
      contains_also:
        - "five named chairs"
        - "direction varies by topic"
        - "defensible"
        - "EXACT TEXT"
    - path: ".claude/skills/research-stances/SKILL.md"
      provides: "Preserved surrounding agent prompt structure"
      contains: "TOPIC SCALE REFERENCE"
      contains_also:
        - "TOOL RULE — CRITICAL"
        - "subagent_type: \"politician-stance-researcher\""
  key_links:
    - from: "STEP 1 agent prompt template"
      to: "five-chairs framing block"
      via: "inline text in the prompt rendered for every dispatch"
      pattern: "FIVE-CHAIRS FRAMING"
    - from: "politician-stance-researcher agent"
      to: "five-chairs evaluative posture"
      via: "system prompt at dispatch time"
      pattern: "named chairs"
---

<objective>
Replace the `SCALE RULE — CRITICAL — READ BEFORE ASSIGNING ANY VALUE:` block in `.claude/skills/research-stances/SKILL.md` (currently at lines ~101–106 inside the STEP 1 agent prompt template) with a new `FIVE-CHAIRS FRAMING — READ BEFORE ASSIGNING ANY VALUE:` block sourced from CONTEXT.md `<specifics>`. The replacement must (a) eliminate the original block entirely (per D-01), (b) carry the chair metaphor, defensibility claim, and direction-is-arbitrary rule (per D-02, D-03, D-04), and (c) preserve all surrounding SKILL.md structure unchanged. Implements SACC-04.

Purpose: The current SCALE RULE framing is too thin — it tells the researcher agent "don't use party as a shortcut" but does not give it a positive evaluative posture to replace the shortcut with. The five-chairs framing supplies that posture: the agent reads written stance text, matches documented record to chair text, and can defend the match with a specific position citation. This prevents the inversion pattern observed across the 8 confirmed cases in Phase 87.

Output: A single-section text replacement in `.claude/skills/research-stances/SKILL.md`. No other file is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-CONTEXT.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-RESEARCH.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SCALE RULE block with FIVE-CHAIRS FRAMING block in SKILL.md</name>
  <files>
    .claude/skills/research-stances/SKILL.md
  </files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (read the ENTIRE file once to locate the exact block bounds — the SCALE RULE block sits inside the STEP 1 agent prompt template, between the `Research all current policy topics.` line and the `TOPIC SCALE REFERENCE — assign values by matching to exact stance text:` line)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-CONTEXT.md (D-01, D-02, D-03, D-04 + the five-chairs philosophy text in `<specifics>` which is the canonical source for the replacement)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-RESEARCH.md (Pattern 5 — replacement block rendered verbatim; Pitfall 5 — warning about editing the wrong section since file is 518 lines)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-PATTERNS.md (target section lines and replacement block rendered verbatim; the "surrounding context to preserve" subsection)
  </read_first>
  <action>
    Use the Edit tool to perform a surgical text replacement in `.claude/skills/research-stances/SKILL.md`. Locate the unique 6-line block that starts with `SCALE RULE — CRITICAL — READ BEFORE ASSIGNING ANY VALUE:` and ends with `EXACT TEXT at this value?" If not, pick a different value.` — this block appears once in the file inside the STEP 1 agent prompt template (around lines 101–106 per 87-RESEARCH.md Pattern 5 and 87-PATTERNS.md target section).

    Replace it with the FIVE-CHAIRS FRAMING block specified verbatim in CONTEXT.md D-02 (and rendered verbatim in 87-RESEARCH.md Pattern 5 and 87-PATTERNS.md). The replacement block opens with `FIVE-CHAIRS FRAMING — READ BEFORE ASSIGNING ANY VALUE:`, contains the five-chairs metaphor paragraph (five distinct positions, named chairs in a room), the direction-is-arbitrary paragraph (spoke has no correct end, value=1 is not "conservative", value=5 is not "progressive", direction varies per topic, read written text + find sources + match documented record to chair text), the defensibility paragraph (when a voter matches a politician the system must say "they both support [specific written text]" — not just "they both scored a 2"), and the closing imperative paragraph (do NOT pick based on party expectation, do NOT assume direction, for every stance ask: does this politician's documented position match the EXACT TEXT at this value? — if not, pick a different value or skip the topic).

    Use the exact replacement text from 87-PATTERNS.md "Replacement block" subsection — do not paraphrase further. Per D-02, the text was already written naturally from the source document; the planner has finalized the wording. Preserve the existing line-break / wrapping style of the surrounding STEP 1 prompt template so the rendered prompt remains readable.

    Critical preservation rules (per 87-RESEARCH.md Pitfall 5 and 87-PATTERNS.md surrounding-context-to-preserve list):
    - Lines BEFORE the replaced block (the dispatch-rule prose, `Research the political stances of [POLITICIAN_NAME]...`, `[If --topics was specified:]`, `[If --topics was NOT specified:]`, `Research all current policy topics.`) MUST be left exactly as they are.
    - Lines AFTER the replaced block (`TOPIC SCALE REFERENCE — assign values by matching to exact stance text:` and everything downstream — the JSON paste instructions, the per-topic format template, the City-level topics skip list, the `--output-file` path line, the `TOOL RULE — CRITICAL` section, all of STEP 2 / STEP 3 / STEP 4, and the REWRITE RE-EVALUATION MODE section) MUST be left exactly as they are.
    - The REWRITE RE-EVALUATION MODE section near the bottom of the file does NOT contain the SCALE RULE block — do NOT edit anything in that section.
    - Do NOT add a second FIVE-CHAIRS block anywhere else; the replacement is one-for-one.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.claude/skills/research-stances/SKILL.md';const c=fs.readFileSync(p,'utf8');const errs=[];if(c.includes('SCALE RULE'))errs.push('SCALE RULE still present');if(!c.includes('FIVE-CHAIRS FRAMING'))errs.push('FIVE-CHAIRS FRAMING missing');if(!/five (named )?chairs/i.test(c))errs.push('chair metaphor missing');if(!c.includes('TOPIC SCALE REFERENCE'))errs.push('TOPIC SCALE REFERENCE preserved section missing');if(!c.includes('TOOL RULE — CRITICAL'))errs.push('TOOL RULE preserved section missing');if(!c.includes('subagent_type: \"politician-stance-researcher\"'))errs.push('STEP 1 dispatch line missing');if(!c.includes('REWRITE RE-EVALUATION MODE'))errs.push('rewrite-mode section missing');if((c.match(/FIVE-CHAIRS FRAMING/g)||[]).length!==1)errs.push('FIVE-CHAIRS FRAMING appears '+(c.match(/FIVE-CHAIRS FRAMING/g)||[]).length+' times, expected 1');if(errs.length){console.error(errs.join('; '));process.exit(1)};console.log('SKILL.md edit verified')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SCALE RULE" .claude/skills/research-stances/SKILL.md` returns ZERO matches (the old block is fully removed per D-01)
    - `grep "FIVE-CHAIRS FRAMING" .claude/skills/research-stances/SKILL.md` returns exactly ONE match (the replacement block appears exactly once)
    - `grep -E "five (named )?chairs" .claude/skills/research-stances/SKILL.md` returns at least one match (the chair metaphor is present per D-02)
    - The replacement block contains the substring `EXACT TEXT at this value?` (the imperative paragraph closer is intact per D-03)
    - The replacement block contains a defensibility-claim sentence — `grep -E "(defensible|specific written text|exactly why|both support)" .claude/skills/research-stances/SKILL.md` returns at least one match (per D-04)
    - The replacement block contains the direction-is-arbitrary rule — `grep -E "direction varies|no correct end" .claude/skills/research-stances/SKILL.md` returns at least one match (per D-03)
    - Surrounding structure preserved: file still contains `TOPIC SCALE REFERENCE`, `TOOL RULE — CRITICAL`, `subagent_type: "politician-stance-researcher"`, and `REWRITE RE-EVALUATION MODE` literal strings
    - No other file in the repository is modified
  </acceptance_criteria>
  <done>SKILL.md edit complete; SCALE RULE block removed; FIVE-CHAIRS FRAMING block in place inside STEP 1 agent prompt template; all surrounding structure intact; future research-stances runs will dispatch agents with the new evaluative posture. SACC-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor -> SKILL.md (agent config) | Local file edit; SKILL.md is read by the research-stances orchestrator and pasted into every researcher-agent dispatch — so changes to this file change every future stance research run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Tampering | SKILL.md agent prompt | mitigate | Edit is surgical (one block replacement); surrounding sections preserved verbatim; automated verify confirms both removal of old block and presence of new block, and that key surrounding markers (`TOPIC SCALE REFERENCE`, `TOOL RULE — CRITICAL`, `REWRITE RE-EVALUATION MODE`, dispatch line) are untouched |
| T-87-05 | Repudiation | Prompt-change traceability | mitigate | Edit is committed with a clear message linking to SACC-04; the new framing text is canonically sourced from 87-CONTEXT.md D-02 / `<specifics>`, both of which are in git |
| T-87-06 | Denial of Service | Researcher agent function | accept | Replacement is semantically additive (gives the agent more evaluative guidance, not less); cannot prevent the agent from running. If wording proves confusing in practice, a follow-up edit can refine it. |
</threat_model>

<verification>
- `grep "SCALE RULE" .claude/skills/research-stances/SKILL.md` returns no matches
- `grep -c "FIVE-CHAIRS FRAMING" .claude/skills/research-stances/SKILL.md` returns exactly 1
- `grep "TOPIC SCALE REFERENCE" .claude/skills/research-stances/SKILL.md` returns matches (preserved)
- Manual diff review: only the targeted block changed; line count delta is consistent with a 6-line block replaced by a ~22-line block (file grows by ~16 lines)
- Manual smell test: re-read the new block in context — does the dispatch template still flow naturally into `TOPIC SCALE REFERENCE — assign values by matching to exact stance text:`?
</verification>

<success_criteria>
- SACC-04 closed: the researcher agent's evaluative posture is now five-chairs framing — text-match-to-chair, defensibility-required, direction-agnostic
- Surrounding STEP 1 template, TOOL RULE, STEP 2/3/4, and REWRITE RE-EVALUATION MODE all unchanged
- Future `/research-stances` runs will paste the new block into every dispatched agent prompt without further code changes
</success_criteria>

<output>
Create `.planning/phases/87-stance-accuracy-audit-agent-update/87-02-SUMMARY.md` when done. Summary should record: line numbers actually edited (start/end), the exact wording of the new block (or a diff snippet), confirmation that the verification grep checks pass, and any wording deviations from CONTEXT.md D-02 / RESEARCH.md Pattern 5 (none expected).
</output>
