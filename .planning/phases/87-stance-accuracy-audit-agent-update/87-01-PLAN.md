---
phase: 87-stance-accuracy-audit-agent-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md
autonomous: true
requirements:
  - SACC-01
tags:
  - audit
  - stance-accuracy
  - postgres
  - reporting

must_haves:
  truths:
    - "An audit report file exists at .planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md"
    - "The report covers all ~1,049 politicians with stance data — none silently dropped"
    - "The 8 known confirmed inversions (Gonzalez, Niello, Nixon, Vindman, Grayson, Hinson, Dooley, Hinojosa) appear pre-seeded under the confirmed-inversion tier"
    - "Every flagged row has columns name, party, office, stance_count, flagged_count, priority_tier, flagged_topics"
    - "The exact audit SQL is embedded in a fenced ```sql block so a future agent can re-run it"
    - "Known-correct cases (Collins/Murkowski/Tillis/Young/Capito SSM=2; VanDeaver school-vouchers=2) are documented as reviewed-and-confirmed so Phase 88 does not re-research them"
    - "Phase 88 reading this report can produce a prioritized re-research work queue without re-running SQL"
  artifacts:
    - path: ".planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md"
      provides: "Prioritized correction list, audit SQL, methodology, and known-correct exclusions"
      contains: "confirmed-inversion"
      contains_also:
        - "borderline"
        - "likely-correct"
        - "Jeff Gonzalez"
        - "Roger Niello"
        - "Angie Nixon"
        - "Alex Vindman"
        - "Tim Grayson"
        - "Ashley Hinson"
        - "Derek Dooley"
        - "Adam Hinojosa"
        - "Collins"
        - "VanDeaver"
        - "```sql"
  key_links:
    - from: "Phase 88 planner"
      to: "87-AUDIT-REPORT.md"
      via: "file read"
      pattern: "priority_tier"
    - from: "Audit SQL"
      to: "inform.politician_answers + essentials.politicians + essentials.offices + inform.compass_topics"
      via: "pool.query() (direct postgres — inform.* not in PostgREST exposed schemas)"
      pattern: "FROM inform.politician_answers"
---

<objective>
Produce `87-AUDIT-REPORT.md` — a prioritized stance-accuracy correction list covering all ~1,049 politicians, derived from a 60%-dominant-value PostgreSQL audit query against `inform.politician_answers`, with the 8 known confirmed inversions pre-seeded into the `confirmed-inversion` tier. This is the canonical work queue Phase 88 reads to drive re-research order. Implements SACC-01 per D-05, D-06, D-07, D-08, D-09, D-10, D-11.

Purpose: The 2026-06-02 informal audit found ~204 flagged stances concentrated in a small number of politicians. Phase 88 needs a written, prioritized, source-cited target list — not a verbal handoff. The report makes the audit reproducible (SQL is embedded) and the priority tiers make Phase 88 sequencing deterministic.

Output: A single Markdown file at `.planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md` containing a YAML frontmatter block, narrative methodology section, three priority-tier tables, known-correct cases section, the audit SQL in a fenced code block, and summary statistics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-CONTEXT.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-RESEARCH.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-PATTERNS.md
@.planning/milestones/v2.2-MILESTONE-AUDIT.md
@backend/src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run audit SQL and capture results</name>
  <files>
    backend/scripts/run-stance-audit.ts (new, throwaway runner — may be deleted after run if preferred)
  </files>
  <read_first>
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-RESEARCH.md (Patterns 1, 2, 3 — the validated SQL, threshold tuning table, and Jeff Gonzalez multi-value note)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-PATTERNS.md (Shared Patterns — pool.query() invocation pattern and node --import tsx script form)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-CONTEXT.md (D-09 60% threshold, D-11 party-distribution-is-NOT-primary-signal)
    - backend/src/lib/db.ts (the `pool` export and connection settings)
    - .claude/skills/research-stances/SKILL.md (lines 27–40 and 200–219 — canonical pool.query() invocation pattern)
  </read_first>
  <action>
    Run the audit SQL from 87-RESEARCH.md Pattern 1 against live Supabase using `pool.query()` from `backend/src/lib/db.ts`. Use the recommended threshold per D-09 and RESEARCH.md Pattern 2: dominant-value ratio >= 0.60 AND total_stances >= 15 (yields ~247 flagged rows per research). Exclude half-values via `WHERE value = ROUND(value)` in the per_value_counts CTE — 63 rows have values 1.5/2.5/3.5/4.5 and are out of scope. JOIN office title via the `LIMIT 1` subquery pattern — `essentials.offices` has NO `is_current` column (per RESEARCH.md Pitfall 3). Capture per-politician rows including name, party (use `COALESCE(NULLIF(p.party, ''), 'Unknown')`), office, stance_count, dominant_count (= flagged_count), dominant_value, flag_ratio_pct, and a comma-separated `flagged_topics` string built from `string_agg(t.topic_key, ', ' ORDER BY t.topic_key)` joined through `inform.compass_topics`.

    Additionally run TWO companion queries and capture their output for the report:
      (1) Total politician count with stance data: `SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers` — must return 1049 per RESEARCH.md verified value.
      (2) Party-distribution-by-flag breakdown: count of flagged rows grouped by `party` — informational summary statistic only, NOT used to assign tiers (per D-11).

    Run the audit via a Node.js script using `node --import tsx` per the SKILL.md pattern, importing `pool` from `./src/lib/db.js`. Working directory: `C:/EV-Accounts/backend`. Load `.env` with `set -a && source .env && set +a`. Output all three result sets to stdout as JSON (or to a temp file) — Task 2 will consume the output. After running, call `await pool.end()` so the process exits cleanly.

    If the resulting flagged count exceeds 100 beyond the 8 pre-seeded inversions, raise the threshold to 70% per the planner-tuning discretion in CONTEXT.md (RESEARCH.md Pattern 2 verified counts: 70% + 15+ stances = 127 flagged). If below 30, lower to 50%. Record the chosen threshold in the script output so Task 2 can document it in the report.

    Do NOT write any data back to the database. This is read-only audit work. Do NOT use `supabaseAdmin.schema('inform')` — `inform.*` is not in the PostgREST exposed schemas list (RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>cd "C:/EV-Accounts" && node -e "(async () => { const { pool } = await import('./backend/src/lib/db.js'); const r = await pool.query('SELECT COUNT(DISTINCT politician_id)::int AS n FROM inform.politician_answers'); console.log('distinct_politicians=' + r.rows[0].n); await pool.end(); })().catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - Audit SQL ran successfully against live Supabase; rows returned are captured (stdout, JSON file, or in-memory) and available to Task 2
    - The query returned the threshold-selected flagged set (target: 30–100 SQL-derived flags beyond the 8 pre-seeded inversions; chose 60%+15-stance per RESEARCH.md recommendation unless tuned)
    - The threshold actually used (e.g., "60% + min 15 stances = 247 flagged") is recorded so it can be quoted in the report
    - The total distinct politician count with stance data was confirmed as 1049 (matches RESEARCH.md verified value)
    - No row was inserted, updated, or deleted in any table — audit is read-only
    - Every audit query went through `pool.query()` from `backend/src/lib/db.ts`; zero calls to `supabaseAdmin.schema('inform')`
    - The companion party-distribution-of-flags query ran and its output is captured for the report's optional summary stats section
  </acceptance_criteria>
  <done>Audit SQL has been executed against live DB; the chosen threshold, the flagged-row payload, and the distinct-politician baseline (1049) are captured and ready to be rendered as Markdown by Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write 87-AUDIT-REPORT.md with pre-seeded inversions, SQL-derived tiers, and known-correct exclusions</name>
  <files>
    .planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md
  </files>
  <read_first>
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-CONTEXT.md (D-05 path, D-06 columns, D-07 three tiers, D-08 embed SQL, decisions and specifics sections)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-RESEARCH.md (Pattern 1 SQL block, Pattern 4 tier-assignment logic, Data Findings — Ukraine and party-string findings, Known-Correct Cases)
    - .planning/phases/87-stance-accuracy-audit-agent-update/87-PATTERNS.md (Report structure pattern, required table columns, pre-seeded confirmed-inversion rows table, three-tier section structure)
    - .planning/milestones/v2.2-MILESTONE-AUDIT.md (lines 1–18 — frontmatter / H1 / metadata pattern to mirror)
    - .planning/STATE.md (v2.6 Stance Accuracy Retro section — verbatim confirmed-inversion list and known-correct cases for citation)
    - Output captured by Task 1 — required input for the borderline / likely-correct tables and summary stats
  </read_first>
  <action>
    Write the audit report at `.planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md` using the Write tool. The report has these sections in order:

    1. YAML frontmatter — mirror the `v2.2-MILESTONE-AUDIT.md` shape: `phase: 87`, `audited: 2026-06-02`, `status: completed`, `requirements_covered: [SACC-01]`, `total_politicians: 1049`, `total_stance_rows: 13732`, `flagged_count: <SQL count + 8 pre-seeded>`, `threshold: "<chosen by Task 1 — e.g. 60% dominant-value with min 15 stances>"`.

    2. H1 title: `# Phase 87: Stance Accuracy Audit Report` followed by **Audited**, **Phase**, **Requirements**, **Status** bold-metadata lines per the v2.2 audit pattern.

    3. H2 `## Methodology` — narrative explanation in plain prose. Cover: (a) two-source approach (pre-seeded confirmed inversions from informal 2026-06-02 audit + SQL low-variance flag); (b) why party-distribution comparison is NOT the primary signal (per D-11 — encoding partisan direction conflicts with five-chairs framing); (c) the 60%-dominant-value rule (D-09) and chosen minimum-stance filter; (d) the three priority tiers and what they mean; (e) note that the Jeff Gonzalez multi-value-inversion pattern is invisible to the SQL and is only caught by manual pre-seeding (per RESEARCH.md Pattern 3).

    4. H2 `## Priority Tier 1 — Confirmed Inversions` — Markdown table with these EXACT column headers in this order: `name`, `party`, `office`, `stance_count`, `flagged_count`, `priority_tier`, `flagged_topics`. Pre-seed these 8 rows (data verbatim from 87-CONTEXT.md `<specifics>` and 87-PATTERNS.md):
       - Jeff Gonzalez | R | CA Assembly | 21 | multi-value (43% @1 + 38% @2) | confirmed-inversion | all topics at values 1-2
       - Roger Niello | R | CA State Sen. | 14 | 9 | confirmed-inversion | (list flagged_topics from Task 1's per-politician topic dump, or note "see source DB")
       - Angie Nixon | D | FL Senate cand. | 11 | 10 | confirmed-inversion | every topic at value=4
       - Alex Vindman | D | FL Senate cand. | 11 | 9 | confirmed-inversion | abortion, religious-freedom, and others
       - Tim Grayson | D | CA State Sen. | 13 | 9 | confirmed-inversion | civil-rights, climate, same-sex-marriage, and others
       - Ashley Hinson | R | IA Senate cand. | 11 | 9 | confirmed-inversion | abortion, climate, immigration, and others
       - Derek Dooley | R | GA Senate cand. | 10 | 9 | confirmed-inversion | (dominant value=2 pattern across most topics)
       - Adam Hinojosa | D (party tag may be wrong) | TX | 6 | 6 | confirmed-inversion | values look Republican
       Below the table, add a short note that Adam Hinojosa has only 6 stances and would have been dropped by any minimum-stance filter — hence pre-seeding is required (per RESEARCH.md Pattern 3).

    5. H2 `## Priority Tier 2 — Borderline (SQL-Flagged, Cross-Party Pattern)` — Markdown table with the same 7 columns. Populate from Task 1 output, filtered to: rows where the dominant value points across party lines (R with dominant_value <= 2, OR D/Democratic with dominant_value >= 4) OR uniform-neutral pattern (dominant_value = 3 at >= 80% ratio) OR any single-value lock at >= 90% ratio regardless of party. Sort by flag_ratio_pct DESC, then stance_count DESC. Use party value as it appears in `essentials.politicians` (do NOT normalize "Democrat" vs "Democratic" — that is Phase 88 / SACC-03 scope per D-11 deferred list). The `flagged_topics` cell is the comma-separated `topic_key` list captured by Task 1.

    6. H2 `## Priority Tier 3 — Likely Correct (SQL-Flagged, Same-Party Pattern)` — same column set. Populate from Task 1 output, filtered to: D/Democratic dominant_value 1 or 2, OR R/Republican dominant_value 4 or 5 — these follow expected partisan distributions and likely reflect natural alignment rather than research error. Phase 88 may still re-research selectively but these are lowest priority.

    7. H2 `## Reviewed and Confirmed — Do NOT Re-Research` — narrative + bulleted list of known-correct exclusions verbatim from STATE.md and 87-CONTEXT.md `<specifics>`:
       - Susan M. Collins, Lisa Murkowski, Thom Tillis, Todd Young, Shelley Moore Capito: `same-sex-marriage=2` (all voted for the Respect for Marriage Act)
       - Gary VanDeaver (R-TX): `school-vouchers=2` (voted against TX voucher bills)
       Explicit instruction to Phase 88: these stances are reviewed-and-confirmed correct; do not include in re-research queue.

    8. H2 `## Deferred to Phase 88` — short bulleted list of items Phase 87 noted but did not act on (per 87-CONTEXT.md `<deferred>`):
       - Ukraine-support: 25 Republicans at value=2 on the ukraine-support topic — individual verification deferred to Phase 88 (cite RESEARCH.md Data Findings number).
       - Party string normalization — "Democrat" (496 rows) vs "Democratic" (280 rows) in `essentials.politicians.party` — SACC-03 / Phase 88 scope.

    9. H2 `## Audit SQL` — embed the EXACT SQL from `87-RESEARCH.md` Pattern 1 (and `87-PATTERNS.md` core audit SQL pattern) inside a fenced ```sql block, including the comment lines at the top so a future agent can re-run it without consulting other files (per D-08). Include a one-line note about how to invoke it (cd to backend, source .env, node --import tsx) with a pointer to `backend/src/lib/db.ts` for the `pool` export.

    10. H2 `## Summary Statistics` (optional per CONTEXT.md Claude's Discretion) — short bulleted list: total politicians with stance data, total stance rows, threshold used, count in each tier (confirmed-inversion / borderline / likely-correct), party-distribution breakdown of flags from Task 1's companion query.

    Rules:
    - Use the Write tool. Do NOT use `cat << EOF` or any heredoc.
    - Every politician in tiers 1–3 has exactly the 7 columns in the order specified by D-06.
    - The 8 pre-seeded inversions appear at the TOP of the priority hierarchy (per D-07) — they are listed regardless of whether the SQL would have caught them (Adam Hinojosa would not; Jeff Gonzalez would not).
    - The SQL block in section 9 is verbatim from RESEARCH.md Pattern 1 — do NOT paraphrase or simplify it.
    - Do NOT include the optional party-distribution-comparison query as primary methodology — only as a documented reference in the Methodology section if mentioned at all (per D-11).
    - Do NOT introduce party-affiliation-based tier assignment for any politician beyond the documented cross-party-vs-same-party heuristic in section 5/6 — that heuristic is the only place party direction informs prioritization, and even there it just controls section placement, not whether a politician needs re-research.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md';if(!fs.existsSync(p)){console.error('missing');process.exit(1)};const c=fs.readFileSync(p,'utf8');const required=['confirmed-inversion','borderline','likely-correct','Jeff Gonzalez','Roger Niello','Angie Nixon','Alex Vindman','Tim Grayson','Ashley Hinson','Derek Dooley','Adam Hinojosa','Collins','VanDeaver','SACC-01','flagged_topics','priority_tier','stance_count','flagged_count','```sql','FROM inform.politician_answers'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('missing tokens:',missing);process.exit(1)};console.log('all required tokens present');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md` exists
    - File contains YAML frontmatter with `phase: 87` and `requirements_covered:` (or equivalent) listing `SACC-01`
    - File contains the literal strings `confirmed-inversion`, `borderline`, `likely-correct` (the three tier labels per D-07)
    - File contains all 8 confirmed-inversion politician names: `Jeff Gonzalez`, `Roger Niello`, `Angie Nixon`, `Alex Vindman`, `Tim Grayson`, `Ashley Hinson`, `Derek Dooley`, `Adam Hinojosa`
    - File contains known-correct citation tokens: `Collins` and `VanDeaver`
    - File contains the seven required column headers in a Markdown table: `name`, `party`, `office`, `stance_count`, `flagged_count`, `priority_tier`, `flagged_topics` (verifiable by string search for header tokens)
    - File contains the literal `` ```sql `` fence and a `FROM inform.politician_answers` line inside it (the embedded re-runnable SQL per D-08)
    - File mentions the deferred items: ukraine-support and Democrat/Democratic party-string variants
    - File does NOT advocate party-distribution-comparison as the primary flag methodology (per D-11)
    - Every borderline/likely-correct row is sourced from Task 1's SQL output — the report does not invent politician rows
    - Heuristic check that Phase 88 can act: `grep -c '^| ' 87-AUDIT-REPORT.md` returns at least 10 (8 pre-seeded + Markdown table headers across multiple sections)
  </acceptance_criteria>
  <done>The audit report is written, contains all three priority tiers, embeds the audit SQL, lists all 8 pre-seeded inversions, documents known-correct cases, and is ready for Phase 88's planner to consume as its work queue. SACC-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script -> Supabase | Read-only `pool.query()` against `inform.*` and `essentials.*` using `DATABASE_URL` from backend `.env`; service role credentials never appear in the report file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure | `87-AUDIT-REPORT.md` (committed planning artifact) | mitigate | Report includes only public politician names + party + office + stance counts and a topic-key list — no auth tokens, no env vars, no service-role keys, no DATABASE_URL. Verify before commit that no secret strings appear in the file. |
| T-87-02 | Tampering | Audit SQL | mitigate | SQL is read-only (SELECTs and CTEs only); no INSERT/UPDATE/DELETE statements exist in Pattern 1. Reviewer/executor must confirm the script never opens a write transaction. |
| T-87-03 | Repudiation | Reproducibility of the audit | mitigate | The exact SQL is embedded in the report (per D-08) so a future agent can re-run and reach the same result independent of code state. |
</threat_model>

<verification>
- File exists: `ls .planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md`
- All required tokens present (combined script from Task 2 `<automated>`)
- Manual review: open the report and confirm the three tier sections, methodology narrative, and known-correct exclusions read clearly to a fresh planner
- No write side-effects: `git diff` on backend DB-touching files shows no new write paths
</verification>

<success_criteria>
- SACC-01 closed: a complete accuracy report exists for all ~1,049 politicians, with prioritized correction list and pre-seeded confirmed-inversion tier
- Phase 88's planner can read this file alone and produce its plan list without re-running the audit query (audit SQL is embedded for re-runs but not required to interpret the report)
- The methodology section accurately reflects D-09 (60% dominant-value), D-10 (8 pre-seeded), D-11 (party-distribution is reference only — NOT primary signal)
- Known-correct cases (Collins SSM, VanDeaver vouchers) are documented as reviewed so Phase 88 does not re-research them
- Deferred items (ukraine-support, Democrat/Democratic) are noted with pointer to Phase 88 / SACC-03
</success_criteria>

<output>
Create `.planning/phases/87-stance-accuracy-audit-agent-update/87-01-SUMMARY.md` when done. Summary should record: chosen threshold, total flagged count from SQL, count placed in each tier, any politicians where the office subquery returned NULL (potential data-quality follow-up), and any deviation from CONTEXT.md decisions.
</output>
