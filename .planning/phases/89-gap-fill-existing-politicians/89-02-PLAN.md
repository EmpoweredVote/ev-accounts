---
phase: 89-gap-fill-existing-politicians
plan: 02
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - inform.politician_answers
  - inform.politician_context
  - backend/data/stance-research/2026-06-03-gap-fill-federal.csv
  - backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv
  - backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv
  - backend/data/stance-research/2026-06-03-gap-fill-ma-legislators.csv
  - .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
autonomous: true
requirements:
  - GAPF-02

must_haves:
  truths:
    - "Every Tier 1 politician from 89-GAP-FILL-AUDIT.md either has >= 10 stances in inform.politician_answers OR has a documented 'no additional evidence available' entry in the audit artifact"
    - "Every new stance row inserted by this plan has a paired inform.politician_context row with sources array of length >= 1"
    - "No stance row was written based on party-affiliation inference — every reasoning is grounded in a fetched URL"
    - "James Byrd and Derek Dooley were both re-attempted; their post-plan stance counts are recorded in the audit artifact"
  artifacts:
    - path: "backend/data/stance-research/2026-06-03-gap-fill-federal.csv"
      provides: "Per-politician stance research output for federal Tier 1 politicians (Byrd, Dooley)"
      contains: "topic_key"
    - path: "backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv"
      provides: "Per-politician stance research output for STATE_EXEC Tier 1 politicians"
      contains: "topic_key"
    - path: "backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv"
      provides: "Per-politician stance research output for CA Assembly + CA State Senate Tier 1 politicians"
      contains: "topic_key"
    - path: "backend/data/stance-research/2026-06-03-gap-fill-ma-legislators.csv"
      provides: "Per-politician stance research output for MA Tier 1 politicians"
      contains: "topic_key"
  key_links:
    - from: ".planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md"
      to: "Wave 2 work order"
      via: "Executor reads Tier 1 work order section to determine processing order"
      pattern: "Wave 2 Work Order"
    - from: "research-stances skill (one-agent-at-a-time)"
      to: "inform.politician_answers + inform.politician_context"
      via: "pool.query() ON CONFLICT DO UPDATE upserts per SKILL.md Step 4b"
      pattern: "INSERT INTO inform.politician_answers.*ON CONFLICT.*DO UPDATE"
    - from: "Per-group CSV files"
      to: "Audit artifact final state column"
      via: "After each group completes, executor annotates audit artifact with per-politician post-fill stance count"
      pattern: "post_fill_count"
---

<objective>
Per GAPF-02: research and ingest missing stances for every Tier 1 politician listed in 89-GAP-FILL-AUDIT.md so that, by the end of this plan, every Tier 1 politician either has >= 10 stances in `inform.politician_answers` OR is documented as "no additional evidence available" in the audit artifact. Every new stance row has a paired `inform.politician_context` row with at least one source URL. Tier 2 politicians are attempted opportunistically per the audit's work order; Tier 3 politicians are NOT researched (already documented as evidence floor by Plan 89-01).

Purpose: This is the work the rest of v2.6 depends on for stance data quality. Without it, GAPF-02 stays open and the platform continues to ship with ~51 politicians at sparse coverage levels that look like data bugs to users hitting the compass compare view.

Output:
- One CSV per politician group in `backend/data/stance-research/` capturing each agent's full research output (preserves the audit trail per 89-RESEARCH.md "Don't Hand-Roll" — CSV is source of truth, DB push is additive).
- New rows in `inform.politician_answers` + `inform.politician_context` via direct pool.query() upserts (per 89-RESEARCH.md "Migration Numbering" — direct upserts, not numbered migrations).
- Updated 89-GAP-FILL-AUDIT.md with a `post_fill_count` annotation on every Tier 1 politician row so verification can prove the work happened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
@.planning/phases/89-gap-fill-existing-politicians/89-VALIDATION.md
@.planning/phases/89-gap-fill-existing-politicians/89-01-SUMMARY.md
@.planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier 1 Federal group — re-attempt James Byrd (WY-D) + gap-fill Derek Dooley (GA-R) to >= 10 stances or document evidence floor</name>
  <files>
    backend/data/stance-research/2026-06-03-gap-fill-federal.csv
    inform.politician_answers (DB writes)
    inform.politician_context (DB writes)
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .claude/skills/research-stances/SKILL.md
  </read_first>
  <action>
    Resolve the federal Tier 1 group from 89-GAP-FILL-AUDIT.md — per 89-RESEARCH.md "Known Special Cases" this is exactly two politicians: James Byrd (WY-D, NATIONAL_UPPER, currently 5 stances) and Derek Dooley (GA-R, NATIONAL_UPPER, currently 7 stances after Phase 88 / migration 127).

    Execute SERIALLY (research-stances SKILL.md dispatch rule — "Always dispatch ONE agent at a time. Never run agents in parallel"). Do not start Dooley until Byrd is complete and its CSV is written.

    Pre-flight for each politician:
      1. Confirm current stance count via mcp__supabase-local__execute_sql:
         SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE full_name = '<NAME>' LIMIT 1);
      2. List EXISTING topic_keys already stanced for this politician so the researcher knows what NOT to re-research:
         SELECT t.topic_key FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE pa.politician_id = '<UUID>' ORDER BY t.topic_key;
      3. Fetch live topics + 5-stance text scale per SKILL.md Step 0 "Topic Resolution" query (returns 44 live topics as of 2026-06-03).
      4. Compute the topic-keys-to-research delta = (federal applicable topics, ~30 per 89-RESEARCH.md) MINUS (existing topic_keys). City-level topics to SKIP per SKILL.md: transportation-priorities, economic-development, homelessness-response, residential-zoning, city-sanitation, local-immigration, rent-regulation, growth-and-development, local-environment, public-safety-approach, jail-capacity, and any topic_key matching the prefix `judicial-`.

    Dispatch a politician-stance-researcher agent per SKILL.md "Agent prompt template" with:
      - Politician name + party + office (e.g., "James Byrd, Democratic, US Senate candidate WY")
      - --topics flag set to ONLY the delta computed above (do NOT re-research existing stances)
      - --output-file absolute path: C:/EV-Accounts/backend/data/stance-research/2026-06-03-gap-fill-federal.csv
      - FIVE-CHAIRS FRAMING block verbatim from SKILL.md
      - The full JSON output of the topic-resolution query formatted per SKILL.md "Orchestrator note" (id, topic_key, question_text, and the 5 stance value+text lines per topic)
      - TOOL RULE block from SKILL.md: WebFetch ONLY, never WebSearch or Playwright
      - For Byrd specifically: include the 89-RESEARCH.md "Known Special Cases" note that prior research (Phase 76) only found 5 stances from a single WyoFile article — instruct the agent to attempt fresh 2026 campaign sources (campaign site, recent local coverage post-2026 announcement), and to skip rather than fabricate if evidence is still single-source after a reasonable search.
      - For Dooley specifically: include the 89-RESEARCH.md note that Phase 88 corrected his 7 stances and migration 127 fixed source URLs — instruct the agent to add stances on additional topics, NOT to revisit the 7 already-corrected stances.

    After each agent completes, BEFORE dispatching the next:
      1. Read the CSV the agent wrote and verify it has at least the header + the agent's reported number of rows (per SKILL.md "Collect and Merge Results").
      2. Run a per-row sanity check: reject any row where `reasoning` contains "as a [Republican|Democrat]", "likely supports", "given their party", "expected to" (per 89-RESEARCH.md Pitfall 2 — party-inference fallback). If any rows fail, do NOT upsert them; document the rejection in the audit artifact.
      3. Upsert non-rejected rows to DB via the SKILL.md Step 4b transaction-wrapped pool.query() pattern (INSERT INTO inform.politician_answers ... ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value; INSERT INTO inform.politician_context ... ON CONFLICT DO UPDATE). sources array must be `[s.source_url_1, s.source_url_2, s.source_url_3].filter(Boolean)`.
      4. Re-query post-fill stance count for THIS politician:
         SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = '<UUID>';
      5. Update 89-GAP-FILL-AUDIT.md: add or update a `post_fill_count` column entry for this politician in the Tier 1 Federal table. If count is still < 10, append a `final_status` note: "evidence floor — no additional sources found" (Byrd's likely outcome) or the actual count if successful.

    After BOTH politicians complete, run the final orphan-context check from 89-VALIDATION.md:
      SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.politician_id IS NULL AND pa.politician_id IN (Byrd_id, Dooley_id);
    Must return 0.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql query: SELECT p.full_name, COUNT(pa.topic_id) AS stance_count, COUNT(pc.id) AS context_count FROM essentials.politicians p JOIN inform.politician_answers pa ON pa.politician_id = p.id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.full_name IN ('James Byrd','Derek Dooley') GROUP BY p.full_name ORDER BY p.full_name; — every returned row MUST have context_count = stance_count (no orphans). At least one of the two politicians MUST have stance_count >= 10 OR the politician's row in 89-GAP-FILL-AUDIT.md MUST contain the phrase "evidence floor" in its final_status note.</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-03-gap-fill-federal.csv` exists with header row containing: full_name, topic_key, value, reasoning, source_url_1, source_url_2, source_url_3.
    - For Derek Dooley: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE full_name = 'Derek Dooley') returns >= 10 — Dooley is documented in 89-RESEARCH.md as having plausibly-available evidence (Phase 88 already established 7 sourced stances).
    - For James Byrd: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = (SELECT id FROM essentials.politicians WHERE full_name = 'James Byrd') returns >= 5 (original baseline) AND either >= 10 OR Byrd's audit row has `final_status` text containing "evidence floor" (the 89-RESEARCH.md A1 documented outcome).
    - SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id IN (Byrd id, Dooley id) AND pc.politician_id IS NULL returns 0.
    - SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN (Byrd id, Dooley id) AND (sources IS NULL OR array_length(sources, 1) = 0) returns 0.
    - Sample-grep the CSV: no row's `reasoning` column matches the regex `(as a (Republican|Democrat)|likely supports|given (his|her|their) party|expected to (support|oppose))` (89-RESEARCH.md Pitfall 2 enforcement).
    - 89-GAP-FILL-AUDIT.md Federal section has been updated — every Byrd/Dooley row has either a numeric `post_fill_count` >= 10 OR a `final_status` cell containing "evidence floor".
    - For Dooley specifically: the 7 pre-existing stance rows (per 89-RESEARCH.md "Known Special Cases" — Phase 88 corrected to value=4) have NOT had their `value` changed by this task (run: SELECT topic_id, value FROM inform.politician_answers WHERE politician_id = (Dooley id) — at least 7 rows still have value=4; the gap-fill only ADDS rows, does not modify Phase 88's corrections).
  </acceptance_criteria>
  <done>Federal Tier 1 group is closed: Byrd and Dooley each have either >= 10 stances or a documented "evidence floor" status in the audit artifact, with zero orphan context rows.</done>
</task>

<task type="auto">
  <name>Task 2: Tier 1 State Executive group — research + ingest all 7 STATE_EXEC politicians from the audit to >= 10 stances or documented floor</name>
  <files>
    backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv
    inform.politician_answers (DB writes)
    inform.politician_context (DB writes)
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .claude/skills/research-stances/SKILL.md
    backend/data/stance-research/2026-06-03-gap-fill-federal.csv
  </read_first>
  <action>
    Read the "Tier 1 — State Executive" subsection from 89-GAP-FILL-AUDIT.md. Per 89-RESEARCH.md "Distribution by District Type" there are 7 STATE_EXEC politicians with < 10 stances. The actual list comes from the audit artifact — process every politician in that subsection serially in the order listed.

    For EACH politician in the State Exec subsection of the audit:

    1. Pre-flight (identical to Task 1 Pre-flight):
       - Resolve politician_id, current stance count, existing topic_keys
       - Compute topic-keys-to-research delta = (state applicable topics, ~35 per 89-RESEARCH.md "Research-Stances Skill Usage") MINUS (existing topic_keys), excluding the SKILL.md city-level skip list
       - Fetch live topic JSON via SKILL.md Step 0 query

    2. Dispatch ONE politician-stance-researcher agent per SKILL.md "Agent prompt template" with:
       - --output-file: C:/EV-Accounts/backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv (all 7 politicians append to the SAME file)
       - --topics: delta topic_keys only
       - FIVE-CHAIRS FRAMING block verbatim
       - Full topic JSON formatted per SKILL.md "Orchestrator note"
       - TOOL RULE: WebFetch ONLY
       - State executives have substantial public records — Ballotpedia, official state government pages, Politico state coverage, AP state wire, state-specific outlets are primary sources

    3. After agent completes, BEFORE dispatching the next politician:
       - Verify CSV has the agent's new rows (count delta)
       - Apply party-inference sanity check (89-RESEARCH.md Pitfall 2): reject rows matching `(as a (Republican|Democrat)|likely supports|given (his|her|their) party|expected to)`
       - Upsert non-rejected rows via SKILL.md Step 4b transaction-wrapped pattern
       - Re-query stance count for this politician
       - Update audit artifact: add `post_fill_count` for this politician's row. If still < 10, add `final_status: "evidence floor"`.
       - Run orphan check for this politician_id only: SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.politician_id IS NULL AND pa.politician_id = '<UUID>'; must be 0.

    4. ONLY THEN move to the next politician.

    Total work item count: 7 sequential agent dispatches. Each agent produces ~10-25 new stance rows. Expected total CSV row growth: ~70-150 rows. Expected DB upsert count: same (minus rejected party-inference rows).

    After all 7 politicians complete, run the group-level verification SQL in the verify block.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql query: SELECT p.full_name, COUNT(pa.topic_id) AS stance_count, COUNT(pc.id) AS context_count FROM essentials.politicians p JOIN inform.politician_answers pa ON pa.politician_id = p.id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE d.district_type = 'STATE_EXEC' AND p.id IN (SELECT politician_id FROM (SELECT politician_id, COUNT(*) AS c FROM inform.politician_answers GROUP BY politician_id HAVING COUNT(*) > 0) sub) GROUP BY p.full_name ORDER BY stance_count DESC; — every row MUST have context_count = stance_count. For every politician originally in the Tier 1 State Exec list (per 89-GAP-FILL-AUDIT.md), stance_count >= 10 OR audit artifact has `final_status: evidence floor` for that politician.</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv` exists and has at least one data row per Tier 1 State Exec politician listed in 89-GAP-FILL-AUDIT.md (use the audit's count — research says 7).
    - For every Tier 1 State Exec politician listed in 89-GAP-FILL-AUDIT.md: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = <id> returns >= 10 OR that politician's row in 89-GAP-FILL-AUDIT.md has a `final_status` cell containing "evidence floor".
    - SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id IN (Tier 1 State Exec ids from audit) AND pc.politician_id IS NULL returns 0 — no orphan context rows.
    - SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN (Tier 1 State Exec ids) AND (sources IS NULL OR array_length(sources, 1) = 0) returns 0 — every context row has at least one source URL.
    - Grep the CSV for party-inference reasoning: zero rows match `(as a (Republican|Democrat)|likely supports|given (his|her|their) party|expected to (support|oppose))`.
    - Audit artifact 89-GAP-FILL-AUDIT.md State Exec subsection has every politician row updated with `post_fill_count` AND either a numeric value >= 10 OR a `final_status` cell containing "evidence floor".
  </acceptance_criteria>
  <done>State Executive Tier 1 group is closed: every politician in the audit subsection has >= 10 stances or a documented evidence floor, zero orphan context rows, zero party-inference reasoning.</done>
</task>

<task type="auto">
  <name>Task 3: Tier 1 CA Legislators group — research + ingest all CA Assembly + CA State Senate politicians from the audit to >= 10 stances or documented floor</name>
  <files>
    backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv
    inform.politician_answers (DB writes)
    inform.politician_context (DB writes)
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .claude/skills/research-stances/SKILL.md
    backend/data/stance-research/2026-06-03-gap-fill-state-exec.csv
  </read_first>
  <action>
    Read the "Tier 1 — CA State Legislators" subsection from 89-GAP-FILL-AUDIT.md. Per 89-RESEARCH.md "State Distribution" there are 22 CA STATE_LOWER/STATE_UPPER politicians with < 10 stances. The actual list comes from the audit artifact — process every politician in that subsection serially in the order listed.

    Per-politician procedure: IDENTICAL to Task 2 except:
      - --output-file: C:/EV-Accounts/backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv (all CA legislators append to the SAME file)
      - Source guidance for the agent prompt: CA Assembly + CA Senate politicians have strong source availability — CalMatters, Sacramento Bee, LA Times, LA Daily News, official CA Assembly/Senate pages, Ballotpedia, ontheissues.org, and the politician's official .ca.gov page. Vote records are publicly available at leginfo.legislature.ca.gov. Per MEMORY.md "CA Assembly + CA State Senate stance research COMPLETE (2026-05-31)" prior batches established that ALL 44 CA Assembly + 13 CA State Senators have at least some sourceable evidence — these are NOT evidence-floor candidates by default. Expect stance_count >= 15 achievable for most.
      - Run agent SERIALLY (never parallel — research-stances SKILL.md dispatch rule). Wait for each to complete CSV write and DB upsert + audit annotation BEFORE dispatching the next.

    Pause-and-resume protocol: This group has ~22 politicians. If the executor's context utilization exceeds 60% before all 22 complete, the executor MUST:
      1. Stop dispatching new agents.
      2. Write a partial-progress note in 89-GAP-FILL-AUDIT.md: "## Plan 89-02 Task 3 Progress — Paused at N/22" with the list of completed vs remaining politicians.
      3. Return control to the orchestrator with a structured handoff so a fresh executor can resume from the remaining list.
    This is not optional — context exhaustion mid-loop produces inconsistent state (some politicians researched, audit not updated, etc.).

    Per-politician pre-flight is IDENTICAL to Task 2 Step 1 (resolve id, current stance count, existing topic_keys, compute delta, fetch topic JSON).

    Per-politician post-agent verification IDENTICAL to Task 2 Step 3 (CSV verify, party-inference rejection, upsert via SKILL.md Step 4b, stance count re-query, audit annotation, per-politician orphan check).

    After all CA politicians complete (or the executor hands off mid-group), run the group-level verification SQL in the verify block.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql query: WITH ca_tier1 AS (SELECT p.id FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE d.state = 'CA' AND d.district_type IN ('STATE_LOWER','STATE_UPPER')) SELECT p.full_name, COUNT(pa.topic_id) AS stance_count, COUNT(pc.id) AS context_count FROM essentials.politicians p JOIN ca_tier1 ON ca_tier1.id = p.id JOIN inform.politician_answers pa ON pa.politician_id = p.id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id GROUP BY p.full_name HAVING COUNT(pa.topic_id) < 10 ORDER BY stance_count ASC; — every returned row MUST be listed in 89-GAP-FILL-AUDIT.md with `final_status: evidence floor` OR be listed in a "## Plan 89-02 Task 3 Progress — Paused" section (handoff state).</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv` exists with header + at least one data row per CA Tier 1 politician processed.
    - For every CA Tier 1 politician listed in 89-GAP-FILL-AUDIT.md AND marked complete in the audit: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = <id> returns >= 10 OR audit row has `final_status: evidence floor`.
    - Zero orphan context rows for any processed CA politician: SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id IN (processed CA ids) AND pc.politician_id IS NULL returns 0.
    - Zero context rows missing sources for any processed CA politician: SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN (processed CA ids) AND (sources IS NULL OR array_length(sources, 1) = 0) returns 0.
    - CSV grep for party-inference: zero rows match `(as a (Republican|Democrat)|likely supports|given (his|her|their) party|expected to (support|oppose))`.
    - If the executor paused mid-group: 89-GAP-FILL-AUDIT.md contains a `## Plan 89-02 Task 3 Progress — Paused` section listing the exact politicians completed vs remaining, and the remaining politicians' audit rows are NOT marked with `post_fill_count` (so the resuming executor knows where to pick up).
    - If the executor completed the full group: every CA Tier 1 politician in the audit has a `post_fill_count` annotation.
  </acceptance_criteria>
  <done>CA Tier 1 group is closed (every politician >= 10 stances or documented evidence floor) OR the work is paused with a clear resumption record in the audit artifact.</done>
</task>

<task type="auto">
  <name>Task 4: Tier 1 MA Legislators group — research + ingest all MA STATE_LOWER/STATE_UPPER politicians from the audit to >= 10 stances or documented floor</name>
  <files>
    backend/data/stance-research/2026-06-03-gap-fill-ma-legislators.csv
    inform.politician_answers (DB writes)
    inform.politician_context (DB writes)
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .claude/skills/research-stances/SKILL.md
    backend/data/stance-research/2026-06-03-gap-fill-ca-legislators.csv
  </read_first>
  <action>
    Read the "Tier 1 — MA State Legislators" subsection from 89-GAP-FILL-AUDIT.md. Per 89-RESEARCH.md "State Distribution" there are 20 MA STATE_LOWER/STATE_UPPER politicians with < 10 stances.

    IMPORTANT context from 89-RESEARCH.md Tier 1 definition: "MA state legislators with < 10 stances (20 politicians) — notably the ones outside the Phase 88 MA cluster investigation (those were already confirmed centrist). Check whether the remaining MA politicians with < 10 have evidence beyond what Phase 88 found." MA legislators MAY have higher "no evidence" rates than CA because of MA's smaller local political press landscape. Use the same Tier 1 procedure but expect more "evidence floor" outcomes here.

    Per-politician procedure: IDENTICAL to Task 2 except:
      - --output-file: C:/EV-Accounts/backend/data/stance-research/2026-06-03-gap-fill-ma-legislators.csv (all MA legislators append to the SAME file)
      - Source guidance for the agent prompt: MA Legislature page (malegislature.gov), Boston Globe, Boston Herald, CommonWealth Beacon, MassLive, WBUR, Ballotpedia, ontheissues.org. Vote records at malegislature.gov.
      - Run agent SERIALLY. Wait for each to complete CSV write and DB upsert + audit annotation BEFORE dispatching the next.
      - Pause-and-resume protocol from Task 3 applies — pause at >60% context utilization and write `## Plan 89-02 Task 4 Progress — Paused` to the audit.
      - When the agent's research returns only 2-3 new stances for an MA politician, that's acceptable — mark `final_status: evidence floor` and move on. Do NOT push the agent to fabricate more.

    Per-politician pre-flight and post-agent verification IDENTICAL to Task 2.

    After all MA politicians complete (or handoff is written), run the group-level verification SQL.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql query: WITH ma_tier1 AS (SELECT p.id FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE d.state = 'MA' AND d.district_type IN ('STATE_LOWER','STATE_UPPER')) SELECT p.full_name, COUNT(pa.topic_id) AS stance_count, COUNT(pc.id) AS context_count FROM essentials.politicians p JOIN ma_tier1 ON ma_tier1.id = p.id JOIN inform.politician_answers pa ON pa.politician_id = p.id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id GROUP BY p.full_name HAVING COUNT(pa.topic_id) < 10 ORDER BY stance_count ASC; — every returned row MUST be listed in 89-GAP-FILL-AUDIT.md with `final_status: evidence floor` OR in a `## Plan 89-02 Task 4 Progress — Paused` section.</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-03-gap-fill-ma-legislators.csv` exists with header + at least one data row per MA Tier 1 politician processed.
    - For every MA Tier 1 politician listed in 89-GAP-FILL-AUDIT.md AND marked complete: SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id = <id> returns >= 10 OR audit row has `final_status: evidence floor`.
    - Zero orphan context rows for any processed MA politician.
    - Zero context rows missing sources for any processed MA politician.
    - CSV grep for party-inference: zero rows match `(as a (Republican|Democrat)|likely supports|given (his|her|their) party|expected to (support|oppose))`.
    - If executor paused: handoff section exists in audit; if completed: every MA Tier 1 politician has `post_fill_count` annotation.
    - Final Plan 89-02 summary section in 89-GAP-FILL-AUDIT.md: "## Plan 89-02 Final Tier 1 Status" with counts of (completed >= 10) vs (documented evidence floor) vs (not yet processed). This section is required even if all groups completed — it provides the GAPF-02 completion proof for /gsd-verify-work.
  </acceptance_criteria>
  <done>MA Tier 1 group is closed (every politician >= 10 stances or documented evidence floor) OR work is paused with clear resumption record. Final tier 1 status summary written to audit.</done>
</task>

</tasks>

<verification>
**Plan-level checks for 89-02 (Tier 1 closure):**

1. **Zero high-priority politicians still under-stanced without documented justification:**

   ```sql
   WITH tier1_ids AS (
     -- Federal: NATIONAL_UPPER, NATIONAL_LOWER
     SELECT p.id FROM essentials.politicians p
     JOIN essentials.offices o ON o.politician_id = p.id
     JOIN essentials.districts d ON d.id = o.district_id
     WHERE d.district_type IN ('NATIONAL_UPPER','NATIONAL_LOWER')
     UNION
     -- State Exec
     SELECT p.id FROM essentials.politicians p
     JOIN essentials.offices o ON o.politician_id = p.id
     JOIN essentials.districts d ON d.id = o.district_id
     WHERE d.district_type = 'STATE_EXEC'
     UNION
     -- CA + MA state legislators
     SELECT p.id FROM essentials.politicians p
     JOIN essentials.offices o ON o.politician_id = p.id
     JOIN essentials.districts d ON d.id = o.district_id
     WHERE d.state IN ('CA','MA') AND d.district_type IN ('STATE_LOWER','STATE_UPPER')
   )
   SELECT p.full_name, COUNT(pa.topic_id) AS stance_count
   FROM essentials.politicians p
   JOIN tier1_ids ON tier1_ids.id = p.id
   JOIN inform.politician_answers pa ON pa.politician_id = p.id
   GROUP BY p.full_name
   HAVING COUNT(pa.topic_id) < 10
   ORDER BY stance_count ASC;
   ```

   Every returned row MUST be listed in 89-GAP-FILL-AUDIT.md with `final_status: evidence floor` (or equivalent documented status).

2. **Zero orphan context rows across all new stances:**

   ```sql
   SELECT COUNT(*) FROM inform.politician_answers pa
   LEFT JOIN inform.politician_context pc
     ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
   WHERE pc.politician_id IS NULL;
   ```

   Must return 0 (89-VALIDATION.md sign-off criterion).

3. **Zero context rows without sources for newly-touched politicians:**

   ```sql
   SELECT COUNT(*) FROM inform.politician_context
   WHERE politician_id IN (Tier 1 politician ids from audit)
     AND (sources IS NULL OR array_length(sources, 1) = 0);
   ```

   Must return 0.

4. **Audit artifact contains required summary section** — "## Plan 89-02 Final Tier 1 Status" exists with counts.
</verification>

<success_criteria>
- All 4 CSV files exist with researched stance data: federal, state-exec, ca-legislators, ma-legislators.
- Every Tier 1 politician from 89-GAP-FILL-AUDIT.md has either stance_count >= 10 OR documented `final_status: evidence floor` in the audit.
- Zero orphan context rows globally (89-VALIDATION.md sign-off check).
- Zero context rows missing sources for any newly-touched politician.
- Zero party-inference reasoning in any of the 4 CSVs (89-RESEARCH.md Pitfall 2).
- 89-GAP-FILL-AUDIT.md updated with `post_fill_count` annotations and a "## Plan 89-02 Final Tier 1 Status" summary section.
- Phase 88's Dooley stance corrections (the 7 value=4 rows + migration 127 source URLs) are preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/89-gap-fill-existing-politicians/89-02-SUMMARY.md` when done.
</output>
