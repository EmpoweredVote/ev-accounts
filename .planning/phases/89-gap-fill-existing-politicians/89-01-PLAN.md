---
phase: 89-gap-fill-existing-politicians
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
  - .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.csv
autonomous: true
requirements:
  - GAPF-01
  - GAPF-02

must_haves:
  truths:
    - "Every politician currently in inform.politician_answers with < 10 stances appears in 89-GAP-FILL-AUDIT.md"
    - "Every row in 89-GAP-FILL-AUDIT.md has a Tier (1/2/3) and Evidence (plausible/no_evidence/partial) assignment"
    - "All 4 orphan-context politicians (Niello, Schiavo, Zbur, Elhawary) have paired context rows for every stance after this plan runs"
    - "Wave 2 has a deterministic, prioritized work list — no executor judgment needed about who to research"
  artifacts:
    - path: ".planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md"
      provides: "Prioritized gap-fill target list — every politician with < 10 stances, tier-classified, evidence-classified"
      contains: "Tier 1"
    - path: ".planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.csv"
      provides: "Machine-readable audit data backing the .md report — same rows, raw fields"
      contains: "politician_id"
  key_links:
    - from: ".planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md"
      to: ".planning/phases/89-gap-fill-existing-politicians/89-02-PLAN.md"
      via: "Wave 2 executor reads Tier 1 list from this artifact to determine work order"
      pattern: "Tier 1"
    - from: "89-01 orphan-context task"
      to: "inform.politician_context"
      via: "pool.query() INSERT for 4 politicians × N orphan topics"
      pattern: "INSERT INTO inform.politician_context"
---

<objective>
Run the GAPF-01 audit to produce the prioritized gap-fill target list, and fix the 4 known orphan-context politicians (Niello, Schiavo, Zbur, Elhawary) so the Wave 2 executor has a clean, deterministic work list.

Purpose: Wave 2 cannot proceed efficiently without a hard answer to "which politicians get research, in what order, and which are documented as evidence-floor?" This plan produces that artifact and closes one side-task (orphan context rows for already-stanced politicians) that would otherwise leak past GAPF-02.

Output:
- `89-GAP-FILL-AUDIT.md` — human-readable prioritized list (Tier 1 first, then 2, then 3 documented as "no_evidence")
- `89-GAP-FILL-AUDIT.csv` — raw rows backing the .md (so Wave 2 can grep/filter without re-querying)
- 9 new `inform.politician_context` rows for Niello (6), Schiavo (1), Zbur (1), Elhawary (1)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
@.planning/phases/89-gap-fill-existing-politicians/89-VALIDATION.md
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gap-fill audit SQL and produce 89-GAP-FILL-AUDIT.md + .csv with tier + evidence classification</name>
  <files>
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md
    .planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.csv
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .planning/phases/89-gap-fill-existing-politicians/89-VALIDATION.md
  </read_first>
  <action>
    Per GAPF-01, execute the audit SQL via mcp__supabase-local__execute_sql against the live DB and emit the result to TWO artifacts. The SQL is the verified-working query in 89-RESEARCH.md "The Core Query" section (verbatim):

      SELECT p.id, p.full_name, COALESCE(NULLIF(p.party,''),'Unknown') AS party,
        sc.stance_count,
        (SELECT o.title FROM essentials.offices o WHERE o.politician_id=p.id LIMIT 1) AS office_title,
        (SELECT d.district_type FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.politician_id=p.id LIMIT 1) AS district_type,
        (SELECT d.state FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE o.politician_id=p.id LIMIT 1) AS state
      FROM (SELECT politician_id, COUNT(*) AS stance_count FROM inform.politician_answers GROUP BY politician_id HAVING COUNT(*) < 10) sc
      JOIN essentials.politicians p ON p.id=sc.politician_id
      ORDER BY sc.stance_count ASC, p.full_name ASC;

    Expected row count is ~440 (per 89-RESEARCH.md "Current State" section verified 2026-06-03). If the actual count differs by more than 20 in either direction, note the drift in the audit artifact preamble but still produce the artifact from the live numbers (DB is the source of truth).

    For each returned row, apply the deterministic tier + evidence classification rules from 89-RESEARCH.md "Priority Tier Definitions":

    Tier 1 (research immediately) when ANY of these hold:
      - district_type IN ('NATIONAL_UPPER', 'NATIONAL_LOWER') → evidence=plausible
      - district_type IN ('STATE_LOWER','STATE_UPPER') AND state IN ('CA','MA') → evidence=plausible
      - district_type='STATE_EXEC' (any state) → evidence=plausible
      - Explicit named special cases from 89-RESEARCH.md "Known Special Cases" — James Byrd (WY-D, NATIONAL_UPPER, 5 stances): Tier 1, evidence=partial (note "single-source evidence floor — re-attempt with 2026 campaign sources"); Derek Dooley (GA-R, NATIONAL_UPPER, 7 stances): Tier 1, evidence=plausible (note "post-Phase 88; migration 127 fixed source URLs — gap-fill from 7 to >=10")

    Tier 2 (research if time allows; expect ~40-50% no-evidence rate per 89-RESEARCH.md A3):
      - district_type IN ('STATE_LOWER','STATE_UPPER') AND state IN ('OR','TX','UT') → evidence=partial
      - district_type IN ('LOCAL','LOCAL_EXEC','COUNTY') AND state IN ('CA') → evidence=partial (major-city local with active local journalism)

    Tier 3 (document as no_evidence; do NOT research):
      - district_type IS NULL (the no_office bucket — 62 politicians per 89-RESEARCH.md "Distribution by District Type") → evidence=no_evidence
      - district_type IN ('LOCAL','LOCAL_EXEC','COUNTY') AND state NOT IN ('CA') → evidence=no_evidence (matches the Pitfall 1 warning about TX small-city councils)
      - party='Unknown' AND district_type IN ('LOCAL','LOCAL_EXEC') → evidence=no_evidence
      - Any remaining row not classified Tier 1 or Tier 2 by the above rules → evidence=no_evidence

    Write 89-GAP-FILL-AUDIT.csv (raw data, all rows from the SQL query) with columns in this order:
      politician_id,full_name,party,stance_count,office_title,district_type,state,tier,evidence,notes

    Write 89-GAP-FILL-AUDIT.md (human-readable report) with these sections in order:
      # Phase 89 — Gap-Fill Audit
      ## Metadata
        - Run date, total politicians with < 10 stances (actual count from SQL), and a one-line description.
      ## Tier 1 — Research Immediately
        Subsections grouped by district_type+state for executor convenience:
          ### Federal (NATIONAL_UPPER, NATIONAL_LOWER) — N politicians
          ### State Executive — N politicians
          ### CA State Legislators (STATE_LOWER + STATE_UPPER) — N politicians
          ### MA State Legislators — N politicians
        Each subsection: markdown table with columns | full_name | party | stance_count | office_title | notes |
      ## Tier 2 — Research If Time Allows
        Subsections:
          ### OR State Legislators — N politicians
          ### TX State Legislators — N politicians
          ### UT State Legislators — N politicians
          ### CA Local Officials — N politicians
        Same table columns; preamble must include the 89-RESEARCH.md A3 warning verbatim: "Expect ~40–50% no-evidence rate for OR state legislators based on prior Phase 76 experience."
      ## Tier 3 — Documented Evidence Floor (No Research)
        Single summary count by category (no per-politician table needed; the .csv has the detail). Include explicit category counts: no_office (~62), LOCAL non-CA, party=Unknown LOCAL.
      ## Wave 2 Work Order
        Numbered list, Tier 1 sections in this exact order: Federal → State Exec → CA → MA. The Wave 2 executor will work the list top-to-bottom.

    Counts in each section heading MUST be derived from the actual classified rows, not the RESEARCH.md estimates — RESEARCH.md numbers are 2026-06-03 estimates; the audit must reflect the live SQL result.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const md=fs.readFileSync('.planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md','utf8');const csv=fs.readFileSync('.planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.csv','utf8');const csvLines=csv.trim().split(/\r?\n/);if(csvLines.length<2){console.error('CSV empty');process.exit(1);}const header=csvLines[0].split(',');for(const col of ['politician_id','full_name','party','stance_count','office_title','district_type','state','tier','evidence']){if(!header.includes(col)){console.error('Missing CSV column:',col);process.exit(1);}}const dataRows=csvLines.slice(1);const tierIdx=header.indexOf('tier');const evidenceIdx=header.indexOf('evidence');const tierCounts={'1':0,'2':0,'3':0};const evidenceCounts={plausible:0,partial:0,no_evidence:0};for(const row of dataRows){const fields=row.split(',');const t=fields[tierIdx];const e=fields[evidenceIdx];if(!['1','2','3'].includes(t)){console.error('Bad tier value:',t,'in row:',row);process.exit(1);}if(!['plausible','partial','no_evidence'].includes(e)){console.error('Bad evidence value:',e,'in row:',row);process.exit(1);}tierCounts[t]++;evidenceCounts[e]++;}for(const heading of ['# Phase 89','## Tier 1','## Tier 2','## Tier 3','## Wave 2 Work Order']){if(!md.includes(heading)){console.error('MD missing heading:',heading);process.exit(1);}}console.log('CSV rows:',dataRows.length,'tier:',JSON.stringify(tierCounts),'evidence:',JSON.stringify(evidenceCounts));"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.csv` exists with header row containing exactly these columns (in any order): politician_id, full_name, party, stance_count, office_title, district_type, state, tier, evidence, notes.
    - CSV has between 420 and 460 data rows (live result expected ~440 per 89-RESEARCH.md; ±20 tolerance for natural DB drift).
    - Every CSV data row has a tier value in {1, 2, 3} — zero rows with NULL/empty/other.
    - Every CSV data row has an evidence value in {plausible, partial, no_evidence} — zero rows with NULL/empty/other.
    - Both NATIONAL_UPPER politicians (James Byrd and Derek Dooley per 89-RESEARCH.md) appear in the CSV with tier=1.
    - James Byrd's row has evidence=partial AND a `notes` value mentioning "single-source" or "evidence floor".
    - Derek Dooley's row has evidence=plausible AND a `notes` value mentioning "Phase 88" or "migration 127".
    - File `.planning/phases/89-gap-fill-existing-politicians/89-GAP-FILL-AUDIT.md` exists and contains the exact headings: `# Phase 89`, `## Tier 1`, `## Tier 2`, `## Tier 3`, `## Wave 2 Work Order`.
    - The .md "Wave 2 Work Order" section lists Tier 1 sub-groups in the exact order: Federal → State Exec → CA → MA.
    - Tier 3 row count in CSV is at least 62 (no_office bucket alone is ~62 per 89-RESEARCH.md).
    - Tier 1 row count in CSV is between 40 and 80 (research estimate: CA 22 + MA 20 + federal 2 + state_exec 7 ≈ 51, with reasonable tolerance for live DB drift and edge cases).
  </acceptance_criteria>
  <done>The audit artifact and CSV are both committed and the Wave 2 executor can read 89-GAP-FILL-AUDIT.md "Wave 2 Work Order" to determine work order without any further judgment calls.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 4 known orphan-context politicians (Niello, Schiavo, Zbur, Elhawary) by adding paired context rows for every orphan stance</name>
  <files>
    inform.politician_context (DB writes via pool.query())
    backend/data/stance-research/2026-06-03-orphan-context-fix.csv
  </files>
  <read_first>
    .planning/phases/89-gap-fill-existing-politicians/89-RESEARCH.md
    .claude/skills/research-stances/SKILL.md
  </read_first>
  <action>
    Per GAPF-02's requirement that "every stance row paired with a context row containing at least one source URL", close the 4 orphan-context cases identified in 89-RESEARCH.md "Current State — Orphan Stances": Roger Niello (Republican, CA State Senate, 6 orphan topics), Pilar Schiavo (Democratic, CA Assembly, 1 orphan), Rick Chavez Zbur (Democratic, CA Assembly, 1 orphan), Sade Elhawary (Democratic, CA Assembly, 1 orphan). Total: 9 missing context rows.

    Procedure for each of the 4 politicians (work them SERIALLY — do not parallelize):

    Step A — Identify the orphan topics for THIS politician via mcp__supabase-local__execute_sql:
      SELECT pa.topic_id, t.topic_key, t.title, t.question_text, pa.value
      FROM inform.politician_answers pa
      JOIN inform.compass_topics t ON t.id = pa.topic_id
      LEFT JOIN inform.politician_context pc
        ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
      WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE full_name = '<NAME>' LIMIT 1)
        AND pc.id IS NULL
      ORDER BY t.topic_key;

    Step B — For each orphan topic returned, dispatch a politician-stance-researcher agent (one at a time, never parallel — research-stances SKILL.md "Dispatch rules" line: "Always dispatch ONE agent at a time"). The agent's job is reduced from "find a value" to "find sources backing the existing value". Pass the agent:
      - Politician name + party + office
      - Topic key + question_text + the full 5-stance text scale (fetch from inform.compass_stances)
      - The EXISTING stance value already in pa.value (so the agent is anchoring, not re-scoring)
      - FIVE-CHAIRS FRAMING block verbatim from SKILL.md "Agent prompt template"
      - TOOL RULE: WebFetch ONLY (no WebSearch, no Playwright) per SKILL.md
      - Instruction: produce `reasoning` text (1-3 sentences explaining why this politician holds this stance) + `sources` array with at least 1 real fetched URL (Ballotpedia, ontheissues.org, official campaign/legislator site, CalMatters, LA Times, Sacramento Bee). If after a reasonable search no source can be located that defensibly supports the existing value, the agent must output value=null with a `null_reason` field; do NOT fabricate sources.

    Step C — Append the agent's output to backend/data/stance-research/2026-06-03-orphan-context-fix.csv with these columns: full_name, politician_id, topic_key, topic_id, value, reasoning, source_url_1, source_url_2, source_url_3, null_reason. If a topic returned null, log it in the CSV with empty source columns and a non-empty null_reason — do NOT upsert to DB.

    Step D — After ALL 4 politicians complete, upsert non-null rows to inform.politician_context via pool.query() using the exact SKILL.md Step 4b INSERT ... ON CONFLICT DO UPDATE pattern (transaction-wrapped with BEGIN/COMMIT/ROLLBACK). Use sources arrays filtered for non-empty strings. Do NOT touch inform.politician_answers — values are pre-existing and must not change.

    Step E — Re-run the orphan check from 89-VALIDATION.md Quick run command to confirm zero remaining orphans for these 4 politicians:
      SELECT p.full_name, COUNT(pa.topic_id) AS orphans
      FROM essentials.politicians p
      JOIN inform.politician_answers pa ON pa.politician_id = p.id
      LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
      WHERE p.full_name IN ('Roger Niello','Pilar Schiavo','Rick Chavez Zbur','Sade Elhawary')
        AND pc.id IS NULL
      GROUP BY p.full_name;

    If any of the 4 politicians still have orphans (because the agent legitimately could not find sources for some topics), document those specific (politician, topic) pairs in 89-GAP-FILL-AUDIT.md under a new subsection "## Unfixable Orphans (No Sources Found)" so they are auditable.
  </action>
  <verify>
    <automated>mcp__supabase-local__execute_sql query: SELECT p.full_name, COUNT(pa.topic_id) AS remaining_orphans FROM essentials.politicians p JOIN inform.politician_answers pa ON pa.politician_id = p.id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.full_name IN ('Roger Niello','Pilar Schiavo','Rick Chavez Zbur','Sade Elhawary') AND pc.id IS NULL GROUP BY p.full_name ORDER BY p.full_name; — result MUST be 0 rows OR every returned row is also listed in 89-GAP-FILL-AUDIT.md "## Unfixable Orphans (No Sources Found)".</automated>
  </verify>
  <acceptance_criteria>
    - For each politician in {Roger Niello, Pilar Schiavo, Rick Chavez Zbur, Sade Elhawary}, SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id=pa.politician_id AND pc.topic_id=pa.topic_id WHERE pa.politician_id = <id> AND pc.id IS NULL returns 0 — OR each remaining orphan (politician_id, topic_id) pair is enumerated in 89-GAP-FILL-AUDIT.md under "## Unfixable Orphans (No Sources Found)" with a one-line reason.
    - File `backend/data/stance-research/2026-06-03-orphan-context-fix.csv` exists with at least 9 rows (the 9 orphan topics across the 4 politicians) with columns: full_name, politician_id, topic_key, topic_id, value, reasoning, source_url_1, source_url_2, source_url_3, null_reason.
    - For every CSV row with a non-empty `value` (i.e., not a null_reason row), the row's source_url_1 is non-empty AND the corresponding `inform.politician_context` row in the DB has `array_length(sources, 1) >= 1`.
    - For every CSV row, `inform.politician_answers.value` for (politician_id, topic_id) MATCHES the CSV `value` field — confirming Task 2 did NOT modify any stance values (the audit ran against existing values only).
    - SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN (Niello, Schiavo, Zbur, Elhawary ids) AND (sources IS NULL OR array_length(sources, 1) = 0) returns 0.
  </acceptance_criteria>
  <done>The 4 orphan-context politicians have paired context rows with sources for every stance that has an available source, and any topic with no findable source is documented in the audit artifact.</done>
</task>

</tasks>

<verification>
**Phase-level checks for Plan 89-01:**

1. Audit artifact exists and is complete:
   - `89-GAP-FILL-AUDIT.csv` row count is within ±20 of 440 (the verified-as-of-2026-06-03 baseline in 89-RESEARCH.md).
   - Every CSV row has a tier (1/2/3) and evidence (plausible/partial/no_evidence) value.
   - `89-GAP-FILL-AUDIT.md` "Wave 2 Work Order" section provides deterministic order: Federal → State Exec → CA → MA.

2. Orphan context fix verified:
   - SQL orphan check for {Niello, Schiavo, Zbur, Elhawary} returns 0 rows, OR every remaining row is listed in "## Unfixable Orphans" in 89-GAP-FILL-AUDIT.md.
   - `backend/data/stance-research/2026-06-03-orphan-context-fix.csv` has at least 9 rows.

3. No stance values modified (Task 2 invariant):
   - Sample-check 3 random (politician_id, topic_id) pairs from the orphan CSV — `inform.politician_answers.value` matches the CSV value column verbatim.
</verification>

<success_criteria>
- 89-GAP-FILL-AUDIT.md exists with all 5 required headings and per-tier subsections.
- 89-GAP-FILL-AUDIT.csv exists with classified rows for every politician returned by the gap-fill SQL.
- All 9 orphan context rows are resolved (added to DB) OR documented as "Unfixable" in the audit.
- Wave 2 plan (89-02) can read 89-GAP-FILL-AUDIT.md and proceed without re-running any audit logic.
</success_criteria>

<output>
Create `.planning/phases/89-gap-fill-existing-politicians/89-01-SUMMARY.md` when done.
</output>
