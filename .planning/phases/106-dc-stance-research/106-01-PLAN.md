---
phase: 106-dc-stance-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql
  - backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv
autonomous: true
requirements:
  - DCST-01

must_haves:
  truths:
    - "Mayor Muriel Bowser has stance rows in inform.politician_answers for at least the 8 city-scope topics (housing, homelessness, climate, civil rights, childcare, immigration, taxes, voting)"
    - "Each of the 13 DC Council members has stance rows for at least the 8 city-scope topics for which documented evidence exists; topics without evidence are honestly skipped"
    - "Attorney General Brian Schwalb has stance rows for applicable city-scope topics for which documented evidence exists"
    - "Every stance row written by this plan has a paired inform.politician_context row whose sources TEXT[] contains at least one real, non-placeholder URL"
    - "No stance value was assigned by party-affiliation inference — every value matches the exact text of one of the five chairs for that topic per D-11"
    - "Topics without real fetched evidence were skipped, not invented (D-06)"
    - "All 15 politicians were researched in a single sequential pass (one agent at a time) before any migration was written — research-all-then-write-migrations order per D-03"
    - "All 15 Plan 1 politicians received the same live-topic scope with no differentiation by seniority or role per D-05"
  artifacts:
    - path: "backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv"
      provides: "Reviewable CSV of all stance research output for 15 DC officials (Mayor + 13 Council + AG)"
      contains: "full_name,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3"
    - path: "supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql"
      provides: "SQL migration upserting all approved stances + context rows for Mayor Bowser, 13 Council members, AG Schwalb"
      contains: "INSERT INTO inform.politician_answers"
  key_links:
    - from: "inform.politician_answers rows added by migration 289"
      to: "essentials.politicians records (external_id range -600001 to -600015)"
      via: "politician_id FK resolved via (SELECT id FROM essentials.politicians WHERE external_id = ...)"
      pattern: "SELECT id FROM essentials.politicians WHERE external_id ="
    - from: "inform.politician_answers rows added by migration 289"
      to: "inform.compass_topics rows"
      via: "topic_id FK resolved via (SELECT id FROM inform.compass_topics WHERE topic_key = ...)"
      pattern: "SELECT id FROM inform.compass_topics WHERE topic_key ="
    - from: "inform.politician_context rows added by migration 289"
      to: "real source URLs"
      via: "sources TEXT[] column"
      pattern: "ARRAY\\["
---

<objective>
Research and ingest sourced stances for 15 DC officials covering DCST-01: Mayor Muriel Bowser, all 13 DC Council members, and AG Brian Schwalb. Each stance must be backed by at least one real source URL; topics without documented evidence are honestly skipped (per D-06).

Purpose: Close DCST-01 — DC users using the compass compare view must be able to see Mayor / Council / AG positions on city-scope topics with verifiable citations. This is the largest of the three Phase 106 plans (15 of 27 DC officials).

Output:
- Reviewable CSV at `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv`
- Single SQL migration `289_dc_mayor_council_ag_stances.sql` that upserts approved stances into `inform.politician_answers` + `inform.politician_context`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-dc-stance-research/106-CONTEXT.md
@.claude/skills/research-stances/SKILL.md
@supabase/migrations/20260606000004_279_md_officials_stances.sql
@supabase/migrations/20260607000004_286_dc_official_records_council_mayor.sql
@supabase/migrations/20260607000005_287_dc_official_records_ag_shadow_ehn.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch live topics + resolve 15 politician UUIDs (pre-flight)</name>
  <files>backend/data/stance-research/2026-06-08-106-dc-topics-snapshot.json, backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json</files>
  <read_first>
    .planning/phases/106-dc-stance-research/106-CONTEXT.md
    .claude/skills/research-stances/SKILL.md
    supabase/migrations/20260607000004_286_dc_official_records_council_mayor.sql
    supabase/migrations/20260607000005_287_dc_official_records_ag_shadow_ehn.sql
  </read_first>
  <action>
    Per D-12, fetch ALL live compass topics fresh from DB. Run the topic resolution query exactly as documented in SKILL.md `Topic Resolution` section (`SELECT t.id, t.topic_key, t.title, t.question_text, json_agg(stances) FROM inform.compass_topics t JOIN inform.compass_stances s ON s.topic_id = t.id WHERE t.is_live = true GROUP BY ... ORDER BY t.topic_key`). Save raw JSON output to `backend/data/stance-research/2026-06-08-106-dc-topics-snapshot.json`.

    Per CONTEXT.md canonical_refs, resolve the 15 politician UUIDs from `essentials.politicians` using `external_id` IN range -600001 to -600015. Query: `SELECT id, external_id, full_name FROM essentials.politicians WHERE external_id BETWEEN -600015 AND -600001 ORDER BY external_id`. Save JSON output (array of {id, external_id, full_name}) to `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json`.

    Verify count = 15 (Bowser -600001, Council -600002 to -600014, Schwalb -600015). If count != 15, stop and report the missing external_ids — do not proceed to Task 2.

    Note topic count from snapshot (CONTEXT.md D-04 says ~44 live as of 2026-06-02; verify actual count from DB). This snapshot will be embedded into every agent prompt in Task 2.
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync } from 'fs'; const t = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-dc-topics-snapshot.json','utf8')); const p = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json','utf8')); if (p.length !== 15) { console.error('FAIL: expected 15 politicians, got ' + p.length); process.exit(1); } if (t.length < 20) { console.error('FAIL: expected at least 20 live topics, got ' + t.length); process.exit(1); } console.log('OK: ' + p.length + ' politicians, ' + t.length + ' topics');"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-08-106-dc-topics-snapshot.json` exists and is a JSON array; each element has keys `id`, `topic_key`, `title`, `question_text`, `stances` (a 5-element array with `value` 1..5 + `text`).
    - File `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json` exists, contains exactly 15 elements, every element has `id` (UUID), `external_id` (integer in -600015..-600001), `full_name`.
    - All 15 external_ids from -600001 through -600015 are present (no gaps).
    - Live topic count >= 44 (per CONTEXT.md D-04 baseline; number may have grown).
    - SQL query for politicians filters strictly by `external_id BETWEEN -600015 AND -600001` — no name-based matching anywhere in this task.
  </acceptance_criteria>
  <done>Topic JSON + politician UUID JSON both written and verified; counts match expectation. Task 2 can now embed the topic JSON into agent prompts and dispatch researchers using the resolved UUIDs.</done>
</task>

<task type="auto">
  <name>Task 2: Run research-stances skill for 15 DC officials (sequential agent dispatch)</name>
  <files>backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv</files>
  <read_first>
    .claude/skills/research-stances/SKILL.md
    backend/data/stance-research/2026-06-08-106-dc-topics-snapshot.json
    backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json
    .planning/phases/106-dc-stance-research/106-CONTEXT.md
  </read_first>
  <action>
    For each of the 15 politicians from the UUID snapshot, dispatch ONE `politician-stance-researcher` agent at a time per D-10 (never parallel; never batch). The 15 politicians in scope (per CONTEXT.md D-01 + canonical_refs):

      external_id -600001  Mayor Muriel Bowser
      external_id -600002 through -600014  the 13 DC Council members (Chairman Phil Mendelson + 8 ward + 4 at-large) — exact names come from migration 286
      external_id -600015  AG Brian Schwalb

    Embed the FULL JSON from the topics snapshot into each agent prompt (per D-12 — never hardcode). Format the topics inside the prompt per SKILL.md's orchestrator note: each entry as `[topic_key] (id: [uuid])` + `Question: "..."` + `1 = "..."` through `5 = "..."`.

    Agent prompt body must follow SKILL.md STEP 1 template verbatim:
      - Politician name + office title
      - "Research all current policy topics" (no `--topics` filter — per D-04, all 44+ live topics in scope; minimum required topics housing, homelessness, climate, civil rights, childcare, immigration, taxes, voting must be researched first if time-constrained)
      - FIVE-CHAIRS framing block (D-11)
      - Topic scale reference (embedded JSON)
      - Tool rule: WebFetch ONLY, never WebSearch or Playwright
      - Skip topics without sufficient evidence per D-06 — "Skip > infer"
      - Output file: `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv` (all 15 agents append to the same CSV)

    Wait for each agent to confirm it wrote rows to the CSV before dispatching the next. If an agent fails or times out, log the failure and continue with the next politician — do NOT retry in parallel.

    After all 15 agents complete, deduplicate CSV header rows (only one header at top) and verify each row has: full_name, topic_key, value, reasoning, source_url_1 (+ optional source_url_2, source_url_3). Reject any row where source_url_1 is empty or a placeholder string like "TBD", "n/a", or a bare domain root.
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync } from 'fs'; const lines = readFileSync('backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv','utf8').split('\n').filter(l => l.trim()); const dataRows = lines.slice(1); const noUrl = dataRows.filter(l => { const cols = l.split(','); return !cols[4] || cols[4].trim() === '' || ['TBD','n/a','N/A'].includes(cols[4].trim()); }); if (noUrl.length > 0) { console.error('FAIL: ' + noUrl.length + ' rows missing source_url_1'); process.exit(1); } if (dataRows.length < 30) { console.error('FAIL: only ' + dataRows.length + ' rows — expected at least 30 (15 politicians x 2 documented topics minimum)'); process.exit(1); } console.log('OK: ' + dataRows.length + ' rows, all with source_url_1');"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv` exists with exactly one header row at line 1.
    - Every data row has a non-empty, non-placeholder `source_url_1` value.
    - `topic_key` column values in the CSV are all present in the Task 1 topic snapshot (no invented topic keys).
    - `full_name` column values match (case-insensitive) one of the 15 names from the politician UUID snapshot.
    - Total data row count >= 30 (floor: 15 politicians x 2 documented topics; realistic expectation is 100+ rows given the 8 minimum city-scope topics across 15 officials).
    - No CSV row has `value` outside the range 1..5.
    - Agents were dispatched ONE AT A TIME (verifiable from conversation transcript — no two `politician-stance-researcher` agents running concurrently).
  </acceptance_criteria>
  <done>CSV contains rows for all 15 officials (or documented "skipped — no evidence" notes for any politician with zero researchable stances per D-06). Every retained row has at least one real source URL.</done>
</task>

<task type="auto">
  <name>Task 3: Write migration 289 upserting all CSV-approved stances</name>
  <files>supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql</files>
  <read_first>
    backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv
    backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json
    supabase/migrations/20260606000004_279_md_officials_stances.sql
    supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql
  </read_first>
  <action>
    Write a single SQL migration at `supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql` upserting every row from the Task 2 CSV. Migration number 289 (per CONTEXT.md phase_specific_guidance — last applied is 288). Timestamp prefix `20260608000001`.

    Follow the exact SQL pattern from migration 279 (MD officials — also pure INSERT/no DELETE):
      - `BEGIN;` ... `COMMIT;` transaction wrapper
      - Header comment block: phase, requirements covered (DCST-01), source CSV path, pre-write cross-check (CSV row count == INSERT count == context INSERT count), migration number derivation, list of 15 politician UUIDs (use UUID literals from Task 1 JSON snapshot — do NOT use `external_id` lookups in the INSERT body)
      - One pair of statements per CSV row:
        1. `INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES (<uuid_literal>, (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), <value>) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;`
        2. `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES (<uuid_literal>, (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), <reasoning_with_doubled_single_quotes>, ARRAY(SELECT u FROM unnest(ARRAY[<urls>]) AS u WHERE u IS NOT NULL AND trim(u) != '')) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;`
      - Use plain overwrite ON CONFLICT (NOT `ARRAY_CAT`) because these are first-time DC official stances — no prior context to preserve. This matches migration 279's MD officials pattern.
      - Escape single quotes in reasoning text by doubling them (SQL standard).
      - Group rows by politician with a `-- ---- <full_name> / <topic_key> / value=<n> ----` divider comment before each pair.
      - End with a `DO $$ BEGIN RAISE NOTICE ... END $$;` block reporting post-state counts: total stances added, total context rows added, politicians covered.

    Do NOT apply the migration in this task — application is a manual deploy step. Plan deliverable is the migration file written to disk and ready to apply.

    UUID resolution: for each CSV row's `full_name`, look up the UUID in `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag-uuids.json` (case-insensitive name match). If a CSV row's full_name does not resolve to a UUID in the snapshot, abort migration write and report the mismatch — do NOT silently skip the row.
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync } from 'fs'; const sql = readFileSync('supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql','utf8'); const csvLines = readFileSync('backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv','utf8').split('\n').filter(l => l.trim()); const expectedRows = csvLines.length - 1; const answerInserts = (sql.match(/INSERT INTO inform\.politician_answers/g) || []).length; const contextInserts = (sql.match(/INSERT INTO inform\.politician_context/g) || []).length; if (answerInserts !== expectedRows) { console.error('FAIL: expected ' + expectedRows + ' answer INSERTs, got ' + answerInserts); process.exit(1); } if (contextInserts !== expectedRows) { console.error('FAIL: expected ' + expectedRows + ' context INSERTs, got ' + contextInserts); process.exit(1); } if (!sql.includes('BEGIN;') || !sql.includes('COMMIT;')) { console.error('FAIL: missing BEGIN/COMMIT'); process.exit(1); } if (!sql.includes('ON CONFLICT (politician_id, topic_id) DO UPDATE')) { console.error('FAIL: missing ON CONFLICT clause'); process.exit(1); } console.log('OK: ' + answerInserts + ' answer + ' + contextInserts + ' context INSERTs');"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql` exists.
    - Migration contains exactly one `BEGIN;` and exactly one `COMMIT;`.
    - Migration contains `INSERT INTO inform.politician_answers` count equal to CSV data-row count.
    - Migration contains `INSERT INTO inform.politician_context` count equal to CSV data-row count.
    - Every `INSERT INTO inform.politician_answers` includes `ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`.
    - Every `INSERT INTO inform.politician_context` includes `sources` array containing at least one URL string.
    - All `politician_id` values in INSERT statements are UUID literals (string format `'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx'`); zero usages of `external_id =` inside the INSERT body.
    - Header comment block names Phase 106, requirements `DCST-01`, source CSV path, migration number derivation, and lists all 15 UUIDs.
    - Migration file size is reasonable: not zero bytes, no truncation marker.
  </acceptance_criteria>
  <done>Migration 289 written, syntactically valid, INSERT counts match CSV row count, ready for manual apply via psql.</done>
</task>

</tasks>

<verification>
After all three tasks complete, the executor agent must produce a SUMMARY.md in `.planning/phases/106-dc-stance-research/106-01-SUMMARY.md` that records:
- Total CSV rows researched
- Per-politician stance count (out of ~44 topics)
- Politicians that produced zero stances (D-06/D-07 honest skip — list them)
- Migration filename + apply status (written, applied, or written-pending-apply)
- Any researcher agent failures + which politicians were affected

Verification queries (to run manually post-apply, NOT in this plan):
  SELECT COUNT(*) FROM inform.politician_answers pa
   WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -600015 AND -600001);
  -- expect: matches Task 3 INSERT count

  SELECT COUNT(*) FROM inform.politician_answers pa
   LEFT JOIN inform.politician_context pc
     ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
   WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -600015 AND -600001)
     AND (pc.id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);
  -- expect: 0 (every stance has a sourced context row)
</verification>

<success_criteria>
1. CSV at `backend/data/stance-research/2026-06-08-106-dc-mayor-council-ag.csv` exists, every data row has at least one real source URL.
2. Migration `supabase/migrations/20260608000001_289_dc_mayor_council_ag_stances.sql` exists; INSERT count matches CSV row count exactly; uses ON CONFLICT upsert pattern; uses UUID literals (not external_id lookups).
3. Mayor Bowser (-600001), all 13 Council members (-600002..-600014), AG Schwalb (-600015) are each represented in the CSV with at least their documented city-scope topics, OR are documented as "zero documented stances found" honest-skip per D-06.
4. Zero stance values were assigned via party-affiliation inference (every value matches exact chair text per D-11 — verifiable from reasoning column in CSV).
5. All 15 researcher agents were dispatched serially, one at a time (D-10).
</success_criteria>

<output>
Create `.planning/phases/106-dc-stance-research/106-01-SUMMARY.md` when done with: per-politician stance counts, honest-skip list, migration number applied, agent failure log (if any).
</output>
