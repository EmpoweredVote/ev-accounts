---
phase: 106-dc-stance-research
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql
  - backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv
  - backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv
  - backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv
  - backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json
autonomous: true
requirements:
  - DCST-03

must_haves:
  truths:
    - "Paul Strauss (external_id -600016) has stance rows for every topic on which a real source URL was found, with DC statehood / voting rights focus"
    - "Ankit Jain (external_id -600017) has stance rows for every topic on which a real source URL was found"
    - "Eleanor Holmes Norton (external_id -600030) has stance rows for ALL applicable federal topics — pre-existing sourced stances retained, missing topics filled in with sourced research"
    - "EHN's existing sourced stances were NOT overwritten — gap-fill only per D-08"
    - "Every stance row written by this plan has a paired inform.politician_context row with sources[] containing at least one real URL"
    - "Shadow senators with low public records get honest skips per D-09 (acceptable to have low stance counts)"
    - "All 3 politicians were researched sequentially (one agent at a time) before migration 291 was written per D-03"
  artifacts:
    - path: "backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json"
      provides: "Pre-flight snapshot of EHN's existing inform.politician_answers + politician_context — used to scope gap-fill (D-08)"
      contains: "[ { topic_id, topic_key, value, has_sourced_context } ]"
    - path: "backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv"
      provides: "Stance research output for Paul Strauss"
    - path: "backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv"
      provides: "Stance research output for Ankit Jain"
    - path: "backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv"
      provides: "Stance research output for EHN, scoped to ONLY topics with no existing sourced stance"
    - path: "supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql"
      provides: "Migration upserting Strauss, Jain, and EHN gap-fill stances + context"
      contains: "INSERT INTO inform.politician_answers"
  key_links:
    - from: "EHN gap-fill CSV"
      to: "EHN's pre-flight existing-stances snapshot"
      via: "set-difference: only topics NOT in existing-stances with sourced context appear in gap-fill"
      pattern: "topic_key NOT IN existing_sourced_topic_keys"
    - from: "migration 291 INSERTs for EHN"
      to: "EHN's existing politician_answers rows"
      via: "ON CONFLICT DO UPDATE — but gap-fill scope means only previously-missing topics receive INSERTs"
      pattern: "ON CONFLICT (politician_id, topic_id) DO UPDATE"
---

<objective>
Research and ingest sourced stances for 3 politicians covering DCST-03: Paul Strauss (Shadow Senator, -600016), Ankit Jain (Shadow Senator, -600017), and Eleanor Holmes Norton (DC Delegate to the US House, -600030). Strauss and Jain are full 44-topic research passes with DC statehood / voting rights focus per D-09; EHN is a GAP-FILL ONLY per D-08 — pre-existing sourced stances must NOT be overwritten.

Purpose: Close DCST-03. DC users using the compass compare view will see the only members of Congress who represent them (EHN as Delegate + 2 Shadow Senators as non-voting advocates) with positions backed by real sources. EHN already has stances from prior phases; this plan fills only the topic gaps in her record.

Output:
- 3 reviewable CSVs (one per politician for clarity)
- 1 EHN existing-stances JSON snapshot (pre-flight evidence for gap-fill scope)
- 1 SQL migration `291_dc_shadow_senators_ehn_stances.sql`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-dc-stance-research/106-CONTEXT.md
@.claude/skills/research-stances/SKILL.md
@supabase/migrations/20260606000004_279_md_officials_stances.sql
@supabase/migrations/20260607000005_287_dc_official_records_ag_shadow_ehn.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — fetch topics, resolve UUIDs, snapshot EHN's existing stances (D-08)</name>
  <files>backend/data/stance-research/2026-06-08-106-dcst03-topics-snapshot.json, backend/data/stance-research/2026-06-08-106-dcst03-uuids.json, backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json</files>
  <read_first>
    .planning/phases/106-dc-stance-research/106-CONTEXT.md
    .claude/skills/research-stances/SKILL.md
    supabase/migrations/20260607000005_287_dc_official_records_ag_shadow_ehn.sql
  </read_first>
  <action>
    Three pre-flight queries, all required before Task 2:

    1. Fetch live topics fresh per D-12 using the SKILL.md `Topic Resolution` query. Save to `backend/data/stance-research/2026-06-08-106-dcst03-topics-snapshot.json`.

    2. Resolve the 3 politician UUIDs. Query `SELECT id, external_id, full_name FROM essentials.politicians WHERE external_id IN (-600016, -600017, -600030) ORDER BY external_id`. Save to `backend/data/stance-research/2026-06-08-106-dcst03-uuids.json`. Verify exactly 3 rows, names matching Paul Strauss / Ankit Jain / Eleanor Holmes Norton.

    3. EHN gap-fill pre-flight per D-08 and code_context. Run the exact query from CONTEXT.md `Established Patterns`:
       ```
       SELECT pa.topic_id, t.topic_key, pa.value, pc.sources,
              (pc.id IS NOT NULL AND pc.sources IS NOT NULL AND array_length(pc.sources, 1) > 0) AS has_sourced_context
         FROM inform.politician_answers pa
         JOIN inform.compass_topics t ON t.id = pa.topic_id
         LEFT JOIN inform.politician_context pc
           ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -600030)
        ORDER BY t.topic_key
       ```
       Save full JSON output (array of {topic_id, topic_key, value, sources, has_sourced_context}) to `backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json`. This is the evidence base for Task 2's EHN gap-fill scope.

    Per D-08: the EHN gap-fill must address topics where EITHER:
      (a) EHN has no row in politician_answers for that topic at all, OR
      (b) EHN has a row in politician_answers but its paired politician_context is missing OR has no sources URL.
    Topics where EHN already has a sourced stance must NOT be re-researched and must NOT appear in migration 291.

    If EHN's existing-stances snapshot returns zero rows (no prior data), the gap-fill becomes a full 44-topic research pass for her — note this in the output for Task 2.
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync } from 'fs'; const t = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-dcst03-topics-snapshot.json','utf8')); const p = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-dcst03-uuids.json','utf8')); const ehn = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json','utf8')); if (p.length !== 3) { console.error('FAIL: expected 3 politicians, got ' + p.length); process.exit(1); } const ids = p.map(x => x.external_id).sort((a,b) => a-b); if (ids[0] !== -600030 || ids[1] !== -600017 || ids[2] !== -600016) { console.error('FAIL: external_ids mismatch: ' + JSON.stringify(ids)); process.exit(1); } if (!Array.isArray(ehn)) { console.error('FAIL: ehn snapshot must be an array'); process.exit(1); } for (const row of ehn) { if (typeof row.has_sourced_context !== 'boolean') { console.error('FAIL: has_sourced_context missing on ehn row'); process.exit(1); } } console.log('OK: ' + t.length + ' topics, 3 politicians, ' + ehn.length + ' EHN existing stances (' + ehn.filter(r => r.has_sourced_context).length + ' sourced, ' + ehn.filter(r => !r.has_sourced_context).length + ' gap-fill candidates)');"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/2026-06-08-106-dcst03-topics-snapshot.json` exists with >=20 entries.
    - File `backend/data/stance-research/2026-06-08-106-dcst03-uuids.json` has exactly 3 entries with external_ids -600016, -600017, -600030 (any order).
    - The Strauss row's `full_name` matches /Paul.*Strauss/i; Jain's matches /Ankit.*Jain/i; EHN's matches /Eleanor.*Norton/i.
    - File `backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json` is a JSON array; every element has keys `topic_id`, `topic_key`, `value`, `sources` (nullable), `has_sourced_context` (boolean).
    - EHN snapshot was generated by a query filtering on `essentials.politicians.external_id = -600030` — NOT name-based.
  </acceptance_criteria>
  <done>Topics snapshot, 3 UUIDs, and EHN existing-stances snapshot all written and verified. Task 2 has the data it needs to scope agent prompts correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch 3 researcher agents sequentially — Strauss (full), Jain (full), EHN (gap-fill only per D-08)</name>
  <files>backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv, backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv, backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv</files>
  <read_first>
    .claude/skills/research-stances/SKILL.md
    backend/data/stance-research/2026-06-08-106-dcst03-topics-snapshot.json
    backend/data/stance-research/2026-06-08-106-dcst03-uuids.json
    backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json
    .planning/phases/106-dc-stance-research/106-CONTEXT.md
  </read_first>
  <action>
    Dispatch 3 `politician-stance-researcher` agents sequentially per D-10 — ONE AT A TIME. Wait for each to confirm CSV write before next.

    **Agent A — Paul Strauss (-600016), full pass per D-09:**
      - Title: "DC Shadow Senator (non-voting US Senate advocate for DC statehood)"
      - Research scope: ALL 44+ live topics from the topics snapshot. DC statehood / voting rights topics are the highest priority but every topic is in scope.
      - FIVE-CHAIRS framing (D-11)
      - Topic scale reference: embed full JSON from topics snapshot (D-12)
      - Tool rule: WebFetch ONLY
      - Per D-06/D-09: low public record is expected for shadow senators — skip topics without real evidence; do NOT infer from party
      - Output: `backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv`

    **Agent B — Ankit Jain (-600017), full pass per D-09:**
      - Same prompt structure as Agent A but for Ankit Jain
      - Title: "DC Shadow Senator (non-voting US Senate advocate for DC statehood)"
      - Output: `backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv`

    **Agent C — Eleanor Holmes Norton (-600030), GAP-FILL ONLY per D-08:**
      - Title: "DC Delegate to the US House (non-voting, longest-serving DC representative)"
      - Compute gap-fill scope BEFORE dispatching: build a list `ehn_gap_topic_keys` = (all topic_keys from topics snapshot) MINUS (topic_keys in EHN existing-stances snapshot where `has_sourced_context = true`). Topics in EHN's existing snapshot where `has_sourced_context = false` ARE in scope (existing row without sources counts as a gap per D-08). Topics with no row at all are in scope.
      - Agent prompt includes the EXPLICIT instruction: "Research ONLY these topic_keys: [ehn_gap_topic_keys]. Other topics are already sourced and must not be touched."
      - Embed only the gap-scope topic entries from the topics snapshot in the agent prompt (or embed the full snapshot but include the explicit allow-list for clarity)
      - Per D-06: skip any in-scope topic without real evidence
      - Output: `backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv`
      - Per D-08: agent prompt must explicitly forbid researching the EHN topics already sourced — list them in a "DO NOT RESEARCH THESE" block in the prompt.

    After all 3 agents complete:
      - Each CSV has at most one header row
      - Verify every data row has source_url_1 (real URL, not placeholder)
      - Verify EHN gap-fill CSV contains ONLY topic_keys in the gap-scope set (no topic_keys where EHN already has sourced context)
      - Track honest-skips per agent (politicians with low total stance counts are expected per D-09 for shadow senators)
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync, existsSync } from 'fs'; const files = ['backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv','backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv','backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv']; for (const f of files) { if (!existsSync(f)) { console.error('FAIL: missing ' + f); process.exit(1); } } const ehnSourced = new Set(JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json','utf8')).filter(r => r.has_sourced_context).map(r => r.topic_key)); const ehnLines = readFileSync('backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv','utf8').split('\n').filter(l => l.trim()).slice(1); const overwrites = ehnLines.filter(l => { const cols = l.split(','); return ehnSourced.has(cols[1] && cols[1].trim()); }); if (overwrites.length > 0) { console.error('FAIL: ' + overwrites.length + ' EHN gap-fill rows touch already-sourced topics (D-08 violation)'); process.exit(1); } let totalRows = 0; for (const f of files) { const lines = readFileSync(f, 'utf8').split('\n').filter(l => l.trim()).slice(1); totalRows += lines.length; for (const l of lines) { const cols = l.split(','); if (!cols[4] || cols[4].trim() === '') { console.error('FAIL: row in ' + f + ' missing source_url_1'); process.exit(1); } } } console.log('OK: ' + totalRows + ' total rows, no D-08 violations, all sourced');"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 CSV files exist with exactly one header row each.
    - Every data row across all 3 CSVs has a non-empty `source_url_1`.
    - Zero rows in the EHN gap-fill CSV reference a topic_key where EHN already has sourced context (D-08 enforced).
    - Every `topic_key` value is present in the Task 1 topics snapshot (no invented keys).
    - Every Strauss/Jain row has `full_name` matching the respective politician (case-insensitive).
    - Every EHN row has `full_name` matching /Eleanor.*Norton/i.
    - Agents dispatched serially, one at a time (D-10), verifiable from conversation transcript.
    - Strauss/Jain low stance counts are acceptable per D-09 — not a verification failure.
  </acceptance_criteria>
  <done>3 CSVs produced; EHN gap-fill correctly scoped (no overwrites of existing sourced stances); shadow senators researched per D-09 honest-record-or-skip rules.</done>
</task>

<task type="auto">
  <name>Task 3: Write migration 291 upserting Strauss + Jain + EHN gap-fill stances</name>
  <files>supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql</files>
  <read_first>
    backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv
    backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv
    backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv
    backend/data/stance-research/2026-06-08-106-dcst03-uuids.json
    backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json
    supabase/migrations/20260606000004_279_md_officials_stances.sql
  </read_first>
  <action>
    Write a single SQL migration `supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql` upserting every row from the 3 Task 2 CSVs. Migration number 291 (after Plan 02's 290). Timestamp prefix `20260608000003`.

    Follow the migration 279 pattern (plain INSERT, plain overwrite ON CONFLICT):
      - `BEGIN;` ... `COMMIT;`
      - Header comment block: phase 106, requirements DCST-03, source CSV paths (all 3), pre-write cross-check (sum of 3 CSVs' data-row count == answer INSERT count == context INSERT count), migration number 291 derivation, list of 3 UUIDs, explicit D-08 gap-fill note for EHN listing the topic_keys that were INTENTIONALLY NOT touched (her already-sourced topics from existing-stances snapshot)
      - Group sections within the migration:
        1. `-- ============ Paul Strauss (-600016) — full pass ============`
        2. `-- ============ Ankit Jain (-600017) — full pass ============`
        3. `-- ============ Eleanor Holmes Norton (-600030) — GAP-FILL ONLY per D-08 ============`
      - Per-row pair pattern identical to Plan 01 Task 3 and Plan 02 Task 3:
        - `INSERT INTO inform.politician_answers ... ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value`
        - `INSERT INTO inform.politician_context ... ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources`
      - Use UUID literals; resolve UUIDs from the dcst03-uuids snapshot
      - Sources array uses `ARRAY(SELECT u FROM unnest(ARRAY[<urls>]) AS u WHERE u IS NOT NULL AND trim(u) != '')` pattern
      - Plain overwrite (not array_cat) — these are new gap-fill rows; no prior context preserved on EHN's gap topics by definition
      - End with `DO $$ BEGIN RAISE NOTICE ... END $$;` reporting: Strauss stance count, Jain stance count, EHN gap-fill stance count, EHN previously-sourced topics retained (untouched), total rows added by migration

    Critical D-08 invariant: the migration body must contain ZERO INSERT statements for EHN against any topic_key where her existing-stances snapshot showed `has_sourced_context = true`. If Task 2 produced a CSV row that violates this, abort migration write and report the violation — do NOT silently filter it. (Task 2's verify gate should already prevent this, but Task 3 must defend against it.)

    Handle empty CSVs: if Strauss or Jain produced zero rows (per D-09 — shadow senators may have very low public records), include that politician's section header in the migration with a comment "-- No researchable stances found per D-09" and zero INSERTs in that section. Migration still gets written.

    Do NOT apply the migration in this task.
  </action>
  <verify>
    <automated>node --import tsx -e "import { readFileSync } from 'fs'; const sql = readFileSync('supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql','utf8'); const csv1 = readFileSync('backend/data/stance-research/2026-06-08-106-dc-shadow-senators-strauss.csv','utf8').split('\n').filter(l => l.trim()).length - 1; const csv2 = readFileSync('backend/data/stance-research/2026-06-08-106-dc-shadow-senators-jain.csv','utf8').split('\n').filter(l => l.trim()).length - 1; const csv3 = readFileSync('backend/data/stance-research/2026-06-08-106-ehn-gap-fill.csv','utf8').split('\n').filter(l => l.trim()).length - 1; const total = csv1 + csv2 + csv3; const answerInserts = (sql.match(/INSERT INTO inform\.politician_answers/g) || []).length; const contextInserts = (sql.match(/INSERT INTO inform\.politician_context/g) || []).length; if (answerInserts !== total) { console.error('FAIL: expected ' + total + ' answer INSERTs, got ' + answerInserts); process.exit(1); } if (contextInserts !== total) { console.error('FAIL: expected ' + total + ' context INSERTs, got ' + contextInserts); process.exit(1); } if (!sql.includes('BEGIN;') || !sql.includes('COMMIT;')) { console.error('FAIL: missing BEGIN/COMMIT'); process.exit(1); } const ehnSourced = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-ehn-existing-stances.json','utf8')).filter(r => r.has_sourced_context).map(r => r.topic_key); const ehnUuid = JSON.parse(readFileSync('backend/data/stance-research/2026-06-08-106-dcst03-uuids.json','utf8')).find(x => x.external_id === -600030).id; for (const tk of ehnSourced) { const pat = new RegExp(ehnUuid + \"[\\\\s\\\\S]*?topic_key = '\" + tk + \"'\", 'g'); if (pat.test(sql)) { console.error('FAIL: migration touches EHN already-sourced topic_key ' + tk + ' (D-08 violation)'); process.exit(1); } } console.log('OK: ' + total + ' total INSERTs (S=' + csv1 + ' J=' + csv2 + ' EHN=' + csv3 + '); no D-08 violations');"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260608000003_291_dc_shadow_senators_ehn_stances.sql` exists.
    - Migration contains exactly one `BEGIN;` and exactly one `COMMIT;`.
    - `INSERT INTO inform.politician_answers` count == sum of data-row counts across all 3 CSVs.
    - `INSERT INTO inform.politician_context` count == sum of data-row counts across all 3 CSVs.
    - Zero INSERT pairs target EHN's UUID with a topic_key that has `has_sourced_context = true` in the existing-stances snapshot (D-08 hard invariant).
    - Migration has three section divider comments for Strauss / Jain / EHN.
    - All `politician_id` values are UUID literals (no `external_id =` lookups inside INSERT body).
    - Header lists EHN's preserved (untouched) topic_keys explicitly for traceability.
    - Header includes `-- Requirements covered: DCST-03` and `-- Phase 106`.
  </acceptance_criteria>
  <done>Migration 291 written with correct INSERT counts; D-08 invariant verified (no EHN already-sourced topics touched); Strauss/Jain sections present even if rows are zero per D-09.</done>
</task>

</tasks>

<verification>
After all three tasks complete, the executor agent produces `.planning/phases/106-dc-stance-research/106-03-SUMMARY.md` with:
- Strauss stance count + skipped topics
- Jain stance count + skipped topics
- EHN gap-fill: topics filled in, topics preserved (untouched)
- D-08 invariant assertion: confirmation that no previously-sourced EHN stance was overwritten
- Migration filename + apply status

Verification queries (manual, post-apply):
  -- Strauss + Jain coverage:
  SELECT p.full_name, COUNT(pa.topic_id) AS stance_count
    FROM essentials.politicians p
    LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
   WHERE p.external_id IN (-600016, -600017)
   GROUP BY p.full_name;
  -- expect: matches CSV row counts

  -- EHN gap-fill: every EHN stance now has sourced context
  SELECT COUNT(*) FROM inform.politician_answers pa
   LEFT JOIN inform.politician_context pc
     ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
   WHERE pa.politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -600030)
     AND (pc.id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) = 0);
  -- expect: 0 (EHN has no unsourced stances after gap-fill)

  -- D-08 invariant post-apply: any of EHN's previously-sourced topics' sources arrays still contain
  -- at least one URL (sanity check that ON CONFLICT path on a previously-sourced topic wasn't accidentally
  -- triggered — should be impossible since D-08 invariant was enforced at write time)
</verification>

<success_criteria>
1. All 3 CSVs exist; every data row has at least one real source URL.
2. Migration 291 INSERT count == sum of 3 CSV data-row counts.
3. EHN gap-fill scope strictly excludes topics where she already has sourced context (D-08 hard invariant — verified at both Task 2 and Task 3).
4. Strauss and Jain low stance counts are acceptable per D-09 — honest record, no inference.
5. All 3 researcher agents dispatched serially (D-10).
6. After this plan applies (manual psql apply post-plan), EHN has zero unsourced stances across all topics in her record.
</success_criteria>

<output>
Create `.planning/phases/106-dc-stance-research/106-03-SUMMARY.md` when done with: per-politician stance counts, EHN gap-fill scope breakdown (filled vs. preserved), D-08 invariant confirmation, migration filename.
</output>
