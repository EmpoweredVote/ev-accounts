---
phase: 25-onboarding-to-calibration-redirect-topic-display-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/pages/Library.jsx
  - CompassV2/src/components/CalibrationOverlay.jsx
  - CompassV2/src/pages/Quiz.jsx
  - CompassV2/src/components/CompassContext.jsx
autonomous: true
requirements: [ONBOARD-01]

must_haves:
  truths:
    - "Topic cards are visible on the Library page when categories load"
    - "Topic cards are visible on the CalibrationOverlay pick step"
    - "Topic cards are visible on the Quiz page in both full and curated modes"
    - "Cards display topic name, question text subtitle, level badges, and answered checkmarks as before"
  artifacts:
    - path: "CompassV2/src/pages/Library.jsx"
      provides: "Topic card grid rendering within categories"
      contains: "categories.map"
    - path: "CompassV2/src/components/CalibrationOverlay.jsx"
      provides: "Topic pick cards in calibration"
      contains: "categories.map"
  key_links:
    - from: "CompassV2/src/components/CompassContext.jsx"
      to: "Library.jsx, CalibrationOverlay.jsx"
      via: "topics and categories arrays from context"
      pattern: "categories\\.map"
---

<objective>
Diagnose and fix the topic card regression — cards are not displaying anywhere on the site.

Purpose: Topic/issue cards are the core content of the Library page, calibration pick step, and quiz. Without visible cards, the entire compass experience is broken. The CONTEXT.md states this is a recent regression, cards were previously working, and the fix should restore them to their previous working appearance with no visual changes needed.

Output: Topic cards render correctly across all compass views where they appeared before.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-onboarding-to-calibration-redirect-topic-display-fix/25-CONTEXT.md

Key files to investigate:
@CompassV2/src/components/CompassContext.jsx (topics/categories loading)
@CompassV2/src/pages/Library.jsx (topic card rendering)
@CompassV2/src/components/CalibrationOverlay.jsx (pick step cards)
@CompassV2/src/pages/Quiz.jsx (quiz topic rendering)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix topic card display regression</name>
  <files>
    CompassV2/src/pages/Library.jsx
    CompassV2/src/components/CalibrationOverlay.jsx
    CompassV2/src/pages/Quiz.jsx
    CompassV2/src/components/CompassContext.jsx
  </files>
  <action>
Investigate why topic/issue cards are not displaying anywhere on the site. This is a regression — cards were working before.

**Investigation approach:**

1. Start the CompassV2 dev server (`cd CompassV2 && npm run dev`) and check the browser for errors.

2. Check the data pipeline:
   - Does `GET /compass/topics` return topics? (Use curl or check browser network tab)
   - Does `GET /compass/categories` return categories with embedded topics?
   - Are `topics` and `categories` arrays populated in CompassContext state?

3. Check rendering pipeline:
   - Library.jsx: `categories.map()` at line ~547 renders topic cards. Is `answeredLoaded` true? Is `categories` empty? Does `getVisibleTopics(category)` return results?
   - CalibrationOverlay.jsx: `categories.map()` at line ~424 renders pick cards. Same check.

4. Check for recent regressions:
   - Did any Phase 23 or Phase 24 change break the categories endpoint or topic data shape?
   - Check if `short_name` removal from admin TopicEditor/TopicAccordion (Phase 24-02) somehow affected topic data fetching or the PATCH handler response format.
   - Check if the backend compass/handlers.go CategoryHandler is still returning topics with stances correctly.
   - Check if `categories` response includes `.topics` arrays (nested topics under each category).

5. Fix the root cause:
   - If data is missing from API: fix the backend handler or query
   - If data arrives but doesn't render: fix the rendering condition
   - If a state variable gates rendering incorrectly: fix the gate condition
   - The fix should restore cards to their previous working state — no visual changes needed

**Key patterns to check:**
- Library.jsx conditionally renders cards only when `answeredLoaded` is true (line ~546). If the answer fetch fails silently, cards may never appear.
- Library.jsx requires `categories` from context — if the categories endpoint returns empty or malformed data, no cards render.
- CalibrationOverlay renders only when `categories` has items with non-empty `.topics` arrays.
  </action>
  <verify>
1. `cd CompassV2 && npm run dev` — dev server starts without errors
2. Navigate to the Library page (/) — topic cards should be visible in category groups
3. Navigate to /results — if uncalibrated, the calibration overlay should show topic pick cards
4. Check browser console for any data-related errors or warnings
  </verify>
  <done>
Topic/issue cards are visible and functional on the Library page, CalibrationOverlay pick step, and Quiz page, matching their previous working appearance.
  </done>
</task>

</tasks>

<verification>
1. Library page shows topic cards grouped by category with topic name, question text, level badges
2. CalibrationOverlay pick step shows topic selection cards when entering calibration
3. Quiz page renders topic cards/questions in both full and curated modes
4. No JavaScript console errors related to missing data or rendering failures
5. `npm run build` completes without errors
</verification>

<success_criteria>
Topic cards display correctly across all compass views where they previously appeared. The regression is identified, fixed, and the application builds cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-onboarding-to-calibration-redirect-topic-display-fix/25-01-SUMMARY.md`
</output>
