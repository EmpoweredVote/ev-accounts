---
phase: 114-ux-walkthrough
plan: 06
type: execute
wave: 3
depends_on: [114-02, 114-03, 114-04, 114-05]
files_modified:
  - .planning/research/ux-walkthrough/cross-app.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/screenshots/cross-app/
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Cross-app stitching links (SiteHeader nav, profile→CompassCard, profile→Read&Rank badges, Compass picker→candidate, Essentials→Treasury) are walked after per-app context is built"
    - "Hand-off friction between apps is logged as its own gap class (app=cross-app)"
    - "Does NOT merge apps into a single continuous journey — per D-09, each app is still evaluated independently first"
  artifacts:
    - path: ".planning/research/ux-walkthrough/cross-app.md"
      provides: "Integration pass documenting every stitching link"
      min_lines: 50
    - path: ".planning/research/ux-walkthrough/screenshots/cross-app/"
      provides: "Screenshot evidence for each stitching link"
  key_links:
    - from: "cross-app.md gap entry"
      to: "GAPS.md"
      via: "appended monotonic G-114-NNN entry"
      pattern: "G-114-[0-9]{3}"
---

<objective>
Run the CONTEXT D-09 cross-app integration pass AFTER per-app walks (114-02 through 114-05) have completed, so the voter has context for the stitching links. Walk every cross-app connection (SiteHeader nav, politician profile → CompassCard, politician profile → Read & Rank verdict badges under StanceAccordion, Compass politician picker → candidate records, any Essentials → Treasury hand-off) and log stitching-level friction as `app=cross-app` gap entries.

Purpose: Per-app walks answer "is each app useful?". Cross-app pass answers "does the voter flow between apps, or do they get stuck at boundaries?"

Output: cross-app.md, screenshots in screenshots/cross-app/, appended cross-app gap entries in GAPS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/ux-walkthrough/essentials.md
@.planning/research/ux-walkthrough/compass.md
@.planning/research/ux-walkthrough/read-rank.md
@.planning/research/ux-walkthrough/treasury.md
@.planning/research/ux-walkthrough/GAPS.md
@ev-ui/src/PoliticianProfile.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk cross-app stitching links and capture screens</name>
  <files>.planning/research/ux-walkthrough/screenshots/cross-app/</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-09 scope — explicit stitching list)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (schema, antipartisan, severity/type enums)
    - .planning/research/ux-walkthrough/essentials.md (already-documented Essentials flow)
    - .planning/research/ux-walkthrough/compass.md (already-documented Compass flow)
    - .planning/research/ux-walkthrough/read-rank.md (already-documented Read & Rank flow)
    - .planning/research/ux-walkthrough/treasury.md (already-documented Treasury flow or relevance-gate result)
  </read_first>
  <action>
    Drive cross-app navigation starting from Essentials at https://essentials.empowered.vote/ with address `200 W Kirkwood Ave, Bloomington, IN 47404`. Use Playwright MCP ONLY:
    - `mcp__plugin_playwright_playwright__browser_navigate`
    - `mcp__plugin_playwright_playwright__browser_snapshot`
    - `mcp__plugin_playwright_playwright__browser_take_screenshot`
    - `mcp__plugin_playwright_playwright__browser_click`

    Walk EVERY one of these stitching links per D-09. Each link gets its own screenshot(s):

    1. **SiteHeader cross-app nav (guest)** — from Essentials landing, click each cross-app destination in the SiteHeader (Compass, Read & Rank, Treasury). Verify auth state persists (or fails to persist). Screenshot each destination as it loads.
       - `screenshots/cross-app/01-siteheader-essentials-to-compass.png`
       - `screenshots/cross-app/02-siteheader-essentials-to-readrank.png`
       - `screenshots/cross-app/03-siteheader-essentials-to-treasury.png`

    2. **Profile → CompassCard** — enter address in Essentials, open a politician profile, scroll to the CompassCard section. Screenshot:
       - `screenshots/cross-app/04-profile-compasscard.png`
       - Click through to Compass if a link is present; screenshot the Compass landing state.

    3. **Profile → Read & Rank verdict badges (StanceAccordion)** — on the same politician profile, scroll to StanceAccordion, find Read & Rank verdict badges (if any render). Screenshot:
       - `screenshots/cross-app/05-profile-stanceaccordion-readrank-badges.png`
       - Click a badge to see if it flows to Read & Rank. Screenshot the destination.

    4. **Compass politician picker → candidate record** — from Compass compare page, open the politician picker, click a Monroe candidate (or any candidate if Monroe is absent). Does it deep-link back to an Essentials profile or show inline data? Screenshot:
       - `screenshots/cross-app/06-compass-picker-to-candidate.png`

    5. **Essentials → Treasury hand-off (if any exists)** — look for any link from Essentials results / Election Central / profile that leads to Treasury Tracker. If none exists, note that as a gap (feature type, cross-app). Screenshot whatever exists or the negative finding:
       - `screenshots/cross-app/07-essentials-to-treasury-handoff.png`

    6. **Read & Rank → Essentials verdict flow-back (guest URL fragment)** — from Read & Rank, complete a verdict and follow the URL-fragment hand-off back to Essentials. Screenshot the Essentials landing that receives the fragment:
       - `screenshots/cross-app/08-readrank-to-essentials-verdict.png`

    Naming: `screenshots/cross-app/NN-<short-label>.png`, monotonic from 01.

    Antipartisan rule still applies: do not log party/endorsement/rating absences as gaps.

    Per D-09: this does NOT merge the apps into a single continuous journey. Each link is walked as a discrete stitching test.
  </action>
  <verify>
    <automated>ls .planning/research/ux-walkthrough/screenshots/cross-app/ | grep -cE "\.png$" | awk '$1 &gt;= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - At least 6 PNG screenshots under `screenshots/cross-app/` (one per stitching link walked)
    - All 6+ stitching links from D-09 either have a screenshot OR a scratchpad note recording "no such link exists — logged as feature gap"
  </acceptance_criteria>
  <done>All D-09 cross-app stitching links are walked and screenshotted (or recorded as absent). Scratchpad ready for Task 2 narrative.</done>
</task>

<task type="auto">
  <name>Task 2: Write cross-app.md and append cross-app gap entries</name>
  <files>.planning/research/ux-walkthrough/cross-app.md, .planning/research/ux-walkthrough/GAPS.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-09, D-10, D-11)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (schema, enums)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — find next monotonic G-114-NNN, continue from max+1)
  </read_first>
  <action>
    1. Create `.planning/research/ux-walkthrough/cross-app.md` with these required sections:
       - `# Cross-App Integration Pass (per CONTEXT D-09)`
       - `**Run date:** 2026-04-13`
       - `**Framing:** Per D-09, this is an integration pass layered on top of the per-app walks (114-02..05). Each stitching link is walked discretely — this is NOT a merged single-session journey.`
       - `## 1. SiteHeader Cross-App Nav (guest)`
       - `## 2. Politician Profile → CompassCard`
       - `## 3. Politician Profile → Read & Rank Verdict Badges (StanceAccordion)`
       - `## 4. Compass Politician Picker → Candidate Record`
       - `## 5. Essentials → Treasury Hand-Off`
       - `## 6. Read & Rank → Essentials Verdict Flow-Back (guest URL fragment)`
       - `## 7. Gap Summary (IDs only)`

       Each §1–§6 contains 2–4 sentences describing what happened, screenshot reference(s), and inline gap citations `(→ G-114-NNN)`.

    2. Append cross-app gap entries to GAPS.md with MONOTONIC IDs from `max(existing G-114-NNN) + 1`. Each entry uses:
       - `**app:** cross-app`
       - Correct severity/type/evidence/baseline_ref per schema

    3. Hard rules:
       - Antipartisan omissions NOT logged
       - `type: data` entries need baseline_ref (rare in cross-app; use "n/a" for pure ux-friction or feature gaps)
       - Every gap has evidence (screenshot or excerpt)

    4. Minimum floor: ≥3 cross-app gap entries (SiteHeader + at least one profile→other-app link + at least one hand-off).
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/cross-app.md &amp;&amp; grep -cE "^## " .planning/research/ux-walkthrough/cross-app.md | awk '$1 &gt;= 7 {exit 0} {exit 1}' &amp;&amp; grep -c "app:\*\* cross-app" .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 3 {exit 0} {exit 1}' &amp;&amp; grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 17 {exit 0} {exit 1}' &amp;&amp; test "$(wc -l &lt; .planning/research/ux-walkthrough/cross-app.md)" -ge 50</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/cross-app.md` succeeds
    - `grep -cE "^## " .planning/research/ux-walkthrough/cross-app.md` ≥ 7
    - At least 3 entries in GAPS.md with `**app:** cross-app`
    - Combined total gap count ≥ 17 (essentials≥6 + compass≥4 + read-rank≥3 + treasury≥1 + cross-app≥3)
    - All IDs monotonic — no duplicates (`grep -oE "G-114-[0-9]{3}" GAPS.md | sort | uniq -d | wc -l` == 0)
    - No antipartisan-omission entries
  </acceptance_criteria>
  <done>cross-app.md covers all D-09 stitching links with screenshots, and GAPS.md has ≥3 monotonic cross-app entries.</done>
</task>

</tasks>

<verification>
- cross-app.md has 7 sections covering all D-09 stitching links
- screenshots/cross-app/ has ≥6 PNGs
- GAPS.md has ≥3 cross-app entries, all monotonic
</verification>

<success_criteria>
- Cross-app stitching friction is documented independently of per-app walks, giving Phase 115 a clean "boundary friction" bucket to synthesize
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-06-SUMMARY.md` per execute-plan template.
</output>
