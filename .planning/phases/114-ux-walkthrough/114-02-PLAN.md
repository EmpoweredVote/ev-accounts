---
phase: 114-ux-walkthrough
plan: 02
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - .planning/research/ux-walkthrough/essentials.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/screenshots/essentials/
autonomous: false
requirements: [UX-01]
must_haves:
  truths:
    - "A first-time Monroe County voter's journey through Essentials at 200 W Kirkwood Ave is documented screen-by-screen against production"
    - "Every voter-facing gap is logged with monotonic G-114-NNN IDs, severity, type, evidence, and baseline_ref (when type=data)"
    - "Antipartisan omissions (party, endorsements, ratings) are NOT logged as gaps"
    - "Voter-facing gaps are distinguishable from already-audited DB gaps (Phase 112)"
  artifacts:
    - path: ".planning/research/ux-walkthrough/essentials.md"
      provides: "Full screen-by-screen narrative from address entry through candidate profiles"
      min_lines: 100
    - path: ".planning/research/ux-walkthrough/screenshots/essentials/"
      provides: "Screenshot evidence for cited gaps"
  key_links:
    - from: "essentials.md gap entries"
      to: "GAPS.md"
      via: "appended G-114-NNN entries"
      pattern: "G-114-[0-9]{3}"
    - from: "data-type gap entries"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "baseline_ref field"
      pattern: "baseline_ref"
---

<objective>
Walk the live Essentials production app (https://essentials.empowered.vote/) as a first-time Monroe County voter using the address `200 W Kirkwood Ave, Bloomington, IN 47404`, document every reachable screen, and log every voter-facing gap as a structured G-114-NNN entry in GAPS.md. Produces `essentials.md` narrative + screenshots + appended gap entries. This satisfies UX-01.

Purpose: A voter-side companion to Phase 112's DB-side audit. The goal is to catch friction, empty states, and classification errors a DB query can't see.

Output: essentials.md, screenshots in screenshots/essentials/, appended G-114-NNN gap entries in GAPS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
@.planning/research/AUDIT-REPORT-112.md
@.planning/research/ux-walkthrough/GAPS.md
@essentials/src/pages/Results.jsx
@essentials/src/lib/classify.js
@ev-ui/src/PoliticianProfile.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive Essentials walkthrough against production and capture screens</name>
  <files>.planning/research/ux-walkthrough/screenshots/essentials/</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-05 scope: full walkthrough)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (severity/type enums, antipartisan omissions, "done" criteria for Essentials)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (denominator — every race/candidate that SHOULD appear for 200 W Kirkwood)
    - .planning/research/AUDIT-REPORT-112.md (to mark a gap as "already-known DB gap" vs "voter-facing")
  </read_first>
  <action>
    Drive Essentials at https://essentials.empowered.vote/ using these Playwright MCP tools ONLY:
    - `mcp__plugin_playwright_playwright__browser_navigate`
    - `mcp__plugin_playwright_playwright__browser_snapshot`
    - `mcp__plugin_playwright_playwright__browser_take_screenshot`
    - `mcp__plugin_playwright_playwright__browser_click`
    - `mcp__plugin_playwright_playwright__browser_fill_form`

    Primary address (hardcoded, do not alter): `200 W Kirkwood Ave, Bloomington, IN 47404`

    Walk every screen the voter can reach — take the CONTEXT D-05 list LITERALLY:
    1. Landing page (screenshot)
    2. Address entry — if Google Maps Places picker blocks Playwright, write a "human leg" with exact URL, form field, exact address string to paste, and expected screenshot filename, then PAUSE for human paste-back
    3. Results page — capture the hierarchy (Federal / State / Local tiers). Screenshot each tier.
    4. Election Central view (if a separate route/section — screenshot)
    5. Every representative card opened (screenshot each card)
    6. At least one incumbent candidate profile — full legislative activity sections (committees, bills, votes, CompassCard, Read & Rank verdict badges under StanceAccordion) — screenshot each section
    7. At least one challenger candidate profile (minimal-view branch per ev-ui PoliticianProfile.jsx) — screenshot
    8. Empty states, loading states, error states encountered — screenshot each
    9. Back-nav behavior — screenshot the state after back-button use
    10. If the Profile → CompassCard or Profile → Read & Rank badge is visible, screenshot it here BUT log cross-app friction in Plan 114-06, not here (per D-09)

    Screenshot naming convention: `screenshots/essentials/NN-<short-label>.png`, NN monotonic starting at 01. Store every screenshot under `.planning/research/ux-walkthrough/screenshots/essentials/`.

    Do NOT log gaps yet — Task 2 writes the narrative and appends gap entries. This task is capture-only. While walking, keep a running plaintext notes scratchpad in session memory (screen label, URL at time of capture, observed issues, baseline cross-refs) for Task 2.

    Antipartisan rule (METHODOLOGY.md §8): if you notice that party labels / endorsements / interest-group ratings are absent, DO NOT note these as issues. They are intentional omissions.

    Voter-facing vs DB gap rule (CONTEXT D-05): if a missing field matches an entry in AUDIT-REPORT-112.md, mark the note as "DB gap already audited — 112 ref: ..." so Task 2 can label it accordingly rather than double-counting.
  </action>
  <verify>
    <automated>ls .planning/research/ux-walkthrough/screenshots/essentials/ | grep -cE "\.png$" | awk '$1 &gt;= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - At least 8 PNG screenshots exist under `.planning/research/ux-walkthrough/screenshots/essentials/` (landing + results + at least one rep card + at least one incumbent profile + at least one challenger profile + at least one state/error/empty screen)
    - `ls .planning/research/ux-walkthrough/screenshots/essentials/ | grep -cE "^[0-9]{2}-.+\.png$"` ≥ 8 (all files follow `NN-label.png` convention)
    - Human-leg fallback used at most once (address entry) and resumed; if used, a note in the session log records "human-leg: address entry"
  </acceptance_criteria>
  <done>All reachable Essentials screens for the primary address are captured as screenshots with NN-label naming, and a running notes scratchpad is ready for Task 2 to convert into narrative + gap entries.</done>
</task>

<task type="auto">
  <name>Task 2: Write essentials.md narrative and append G-114-NNN gap entries</name>
  <files>.planning/research/ux-walkthrough/essentials.md, .planning/research/ux-walkthrough/GAPS.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-05, D-10, D-11)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (schema, enums, antipartisan omissions)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — must read to find the next monotonic G-114-NNN number; this plan is first walkthrough so expected starting ID is G-114-001)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (resolve `baseline_ref` values for every data-type gap)
    - .planning/research/AUDIT-REPORT-112.md (flag already-known DB gaps)
  </read_first>
  <action>
    1. Create `.planning/research/ux-walkthrough/essentials.md` with these required section headings:
       - `# Essentials — UX Walkthrough (UX-01)`
       - `**App:** https://essentials.empowered.vote/`
       - `**Address:** 200 W Kirkwood Ave, Bloomington, IN 47404`
       - `**Run date:** 2026-04-13`
       - `## 1. Landing`
       - `## 2. Address Entry`
       - `## 3. Results — Federal Tier`
       - `## 4. Results — State Tier`
       - `## 5. Results — Local Tier`
       - `## 6. Election Central`
       - `## 7. Representative Cards`
       - `## 8. Candidate Profile — Incumbent`
       - `## 9. Candidate Profile — Challenger`
       - `## 10. Legislative Activity (committees / bills / votes)`
       - `## 11. Empty / Error / Loading States`
       - `## 12. Back-Nav Behavior`
       - `## 13. Gap Summary (IDs only)` — just a bullet list `- G-114-001, G-114-002, ...` referencing the IDs appended to GAPS.md

       For each section, write 2–6 sentences describing what the voter saw, embed the screenshot path (relative: `screenshots/essentials/NN-label.png`), and inline-cite the gap IDs discovered on that screen like `(→ G-114-001)`.

    2. Append gap entries to `.planning/research/ux-walkthrough/GAPS.md` under the `## Gaps` heading. Use MONOTONIC IDs starting at `G-114-001` (this is the first walkthrough plan to run — verify by reading GAPS.md and confirming no existing G-114-NNN entries; if any exist from a re-run, continue from max+1).

       Each entry uses EXACTLY this template:

       ```markdown
       ### G-114-NNN — <one-line description>
       - **app:** essentials
       - **screen:** <label> — <prod URL at time of walk>
       - **severity:** blocker | confusing | minor
       - **type:** data | feature | content | ux-friction
       - **evidence:** screenshots/essentials/NN-label.png
       - **baseline_ref:** <row in BALLOT-BASELINE-2026-05-05.md — REQUIRED if type=data>
       - **notes:** <optional rationale, or "DB gap already audited — see AUDIT-REPORT-112 §X" when applicable>
       ```

    3. Hard rules (METHODOLOGY.md §8):
       - DO NOT log missing party labels as gaps
       - DO NOT log missing endorsement lists as gaps
       - DO NOT log missing interest-group ratings as gaps
       - Every `type: data` entry MUST have a non-empty `baseline_ref` pointing to a specific BALLOT-BASELINE-2026-05-05.md row
       - Every gap MUST have evidence (screenshot path OR verbatim excerpt)

    4. Minimum floor (your-judgment guardrail): Essentials at 200 W Kirkwood is the richest surface — expect to log at least 6 voter-facing gap entries. If the walkthrough legitimately produces fewer than 6, document that in §13 with a one-line rationale; otherwise 6 is the floor.
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/essentials.md &amp;&amp; grep -q "200 W Kirkwood Ave" .planning/research/ux-walkthrough/essentials.md &amp;&amp; grep -cE "^## " .planning/research/ux-walkthrough/essentials.md | awk '$1 &gt;= 10 {exit 0} {exit 1}' &amp;&amp; grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 6 {exit 0} {exit 1}' &amp;&amp; ! grep -qi "party label\|endorsement list\|interest group rating" .planning/research/ux-walkthrough/GAPS.md &amp;&amp; test "$(wc -l &lt; .planning/research/ux-walkthrough/essentials.md)" -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/essentials.md` succeeds
    - `grep -q "200 W Kirkwood Ave" .planning/research/ux-walkthrough/essentials.md` succeeds
    - `grep -cE "^## " .planning/research/ux-walkthrough/essentials.md` ≥ 10 (at least 10 top-level section headings present)
    - `grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md` ≥ 6 (floor per D-05 scope)
    - Every essentials gap entry in GAPS.md contains `**app:** essentials`, `**severity:**`, `**type:**`, `**evidence:**` lines
    - Every `type: data` entry in the essentials block has a non-empty `**baseline_ref:**` pointing to BALLOT-BASELINE-2026-05-05.md (verified by reading each data entry manually; grep: `grep -A7 "type:\*\* data" GAPS.md | grep "baseline_ref" | grep -v "n/a"`)
    - No entry contains "party label", "endorsement list", or "interest group rating" (antipartisan rule)
    - Gap IDs in essentials.md §13 match IDs appended to GAPS.md (same set)
  </acceptance_criteria>
  <done>essentials.md is a complete voter-side narrative with screenshot refs, GAPS.md has ≥6 monotonic G-114-NNN entries for Essentials with correct schema, and antipartisan rule is respected.</done>
</task>

</tasks>

<verification>
- essentials.md has all 13 required sections and references screenshot files
- GAPS.md has appended essentials gap entries starting at G-114-001 with monotonic IDs
- Every data-type gap has a non-empty baseline_ref
- Antipartisan omissions are not logged
- Screenshots directory has ≥8 PNGs
</verification>

<success_criteria>
- A Phase 115 reader can reproduce the walkthrough from essentials.md + screenshots without re-running Playwright
- UX-01 is satisfied: the Essentials voter journey is documented with specific friction points and gaps identified
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-02-SUMMARY.md` per execute-plan template.
</output>
