---
phase: 114-ux-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/ux-walkthrough/METHODOLOGY.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/gaps.csv
  - .planning/research/ux-walkthrough/screenshots/essentials/.gitkeep
  - .planning/research/ux-walkthrough/screenshots/compass/.gitkeep
  - .planning/research/ux-walkthrough/screenshots/read-rank/.gitkeep
  - .planning/research/ux-walkthrough/screenshots/treasury/.gitkeep
  - .planning/research/ux-walkthrough/screenshots/cross-app/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "METHODOLOGY.md exists and locks persona, address, environment, date, severity/type definitions, and antipartisan intentional-omissions BEFORE any walkthrough runs"
    - "Empty GAPS.md and gaps.csv skeletons exist so walkthrough plans can append G-114-NNN entries monotonically"
    - "All five screenshots/ subdirectories exist so walkthroughs have a drop location"
  artifacts:
    - path: ".planning/research/ux-walkthrough/METHODOLOGY.md"
      provides: "Locked persona, address, environment, severity/type definitions, antipartisan omissions"
    - path: ".planning/research/ux-walkthrough/GAPS.md"
      provides: "Empty gap register with schema header"
    - path: ".planning/research/ux-walkthrough/gaps.csv"
      provides: "Empty CSV mirror with header row"
  key_links:
    - from: "METHODOLOGY.md"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "referenced as denominator for data-type gaps"
      pattern: "BALLOT-BASELINE-2026-05-05"
---

<objective>
Lock the framing for the Phase 114 UX walkthrough BEFORE any Playwright session runs. Write METHODOLOGY.md (persona, address, environment, date, severity/type definitions with worked examples, antipartisan intentional-omissions section, per-app "done" criteria) and initialize the empty output artifacts (GAPS.md, gaps.csv, screenshots/ subdirs) so plans 02–06 can append gap entries with monotonic G-114-NNN IDs.

Purpose: Establish the fairness ruler for the walkthrough the same way Phase 113's METHODOLOGY.md did for benchmarking. Without this, per-app walks drift in severity/type assignment and data-type gaps can't be tied back to the ballot baseline.

Output: METHODOLOGY.md + empty GAPS.md + empty gaps.csv + five screenshots/ subdirs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write METHODOLOGY.md for Phase 114 UX walkthrough</name>
  <files>.planning/research/ux-walkthrough/METHODOLOGY.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-01..D-14 — contract)
    - .planning/research/benchmark/METHODOLOGY.md (pattern template from Phase 113 per CONTEXT D-14)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (denominator referenced for data-type gaps)
    - .planning/PROJECT.md (antipartisan principle — drives intentional omissions section)
    - CLAUDE.md (workspace root — confirm live prod URLs for four apps)
  </read_first>
  <action>
    Create `.planning/research/ux-walkthrough/METHODOLOGY.md` with the following REQUIRED section headings, in order. Hardcode the exact values listed — do not paraphrase or generalize.

    Required sections:

    1. `# Phase 114 — UX Walkthrough Methodology`
       - Run date: 2026-04-13
       - Phase: 114-ux-walkthrough
       - Purpose paragraph: Lock persona, address, environment, severity/type definitions, and antipartisan omissions BEFORE any walkthrough runs. This is the fairness mechanism — there is no separate peer-review checkpoint. Every gap entry in plans 02–06 must trace back to the definitions here.

    2. `## 1. Persona`
       - Single primary persona: "A naive first-time Monroe County, IN voter with no prior knowledge of Empowered Vote or any competitor, entering each app cold."
       - State verbatim per D-01: "No student/rural personas in this phase — deferred."

    3. `## 2. Address`
       - Verbatim: `200 W Kirkwood Ave, Bloomington, IN 47404`
       - Rationale: same address as Phase 112 geofence smoke test and Phase 113 benchmark primary. Any experience divergence is attributable to the product, not input variance.

    4. `## 3. Environment`
       - Verbatim per D-03: **production**, not local dev.
       - List the four live prod URLs:
         - Essentials — https://essentials.empowered.vote/
         - Compass — https://compass.empowered.vote/
         - Read & Rank — https://readrank.empowered.vote/
         - Treasury Tracker — https://treasurytracker.empowered.vote/
       - State: "Unreleased-but-merged work is deliberately NOT credited. Phase 115's tiered report is about what must ship before May 5, and prod is the honest baseline."

    5. `## 4. Run Date`
       - 2026-04-13

    6. `## 5. Severity Definitions (per CONTEXT D-10)`
       - `blocker` — voter cannot complete the flow or the flow returns wrong info
       - `confusing` — voter can complete it but the experience is unclear, mislabeled, or unexpectedly empty
       - `minor` — cosmetic, copy polish, nice-to-have
       - Include one worked example per severity level on a hypothetical Essentials screen (e.g. "Address input accepts input but Results page shows zero federal-tier politicians for 200 W Kirkwood → blocker (voter gets wrong info)").

    7. `## 6. Type Definitions (per CONTEXT D-10)`
       - `data` — the field exists in the UI but the DB row is missing or wrong
       - `feature` — the UI itself is missing a capability a voter would expect
       - `content` — copy, framing, or labeling problem
       - `ux-friction` — flow, nav, state, or interaction friction
       - Include one worked example per type.

    8. `## 7. Gap Entry Schema (per CONTEXT D-11)`
       - List all 8 required fields verbatim: `id`, `app`, `screen`, `description`, `severity`, `type`, `evidence`, `baseline_ref`
       - id format: `G-114-NNN` zero-padded, monotonic across the whole phase (not per-app)
       - app enum: `essentials | compass | read-rank | treasury | cross-app`
       - severity enum: `blocker | confusing | minor`
       - type enum: `data | feature | content | ux-friction`
       - baseline_ref: REQUIRED when type=data, points to a row in BALLOT-BASELINE-2026-05-05.md

    9. `## 8. Intentional Omissions — Antipartisan (per CONTEXT D-14)`
       - State verbatim: "The following absences are INTENTIONAL per EV's antipartisan principle and MUST NOT be logged as gaps in any walkthrough:"
       - Missing party labels (D/R/I) on candidate cards or profiles
       - Missing endorsement lists
       - Missing interest-group ratings (NRA, Sierra Club, etc.)
       - State: "If a walkthrough task logs one of these as a gap, it is a methodology violation and must be removed during Plan 114-07 aggregation."

    10. `## 9. "Done" Criteria Per App`
        - Essentials (UX-01): address entered → results hierarchy reviewed (federal/state/local) → Election Central viewed → at least one representative card opened → at least one candidate profile opened (incumbent AND challenger if both present) → legislative activity sections surveyed → empty/error/back-nav states probed.
        - Compass (UX-02): guest entry → onboarding → topic selection → full quiz run → compare page → politician picker filtered to Monroe County candidates → dual-overlay radar vs at least one Monroe candidate.
        - Read & Rank (UX-03): landing → candidate filter by Monroe County / by race → at least one quote evaluation → verdict flow-back to Essentials attempted (guest path via URL fragment).
        - Treasury (UX-04): relevance check FIRST — is there ANY Monroe/Bloomington data present? If yes, full walkthrough. If no, log one top-level gap and STOP per D-08.
        - Cross-app (D-09): SiteHeader nav between apps (auth state + destinations), profile → CompassCard, profile → Read & Rank verdict badges under StanceAccordion, Compass politician picker → candidate records, any Essentials → Treasury hand-off.

    11. `## 10. Execution Method (per CONTEXT D-04)`
        - Primary: `plugin_playwright` MCP tools — `mcp__plugin_playwright_playwright__browser_navigate`, `browser_snapshot`, `browser_take_screenshot`, `browser_click`, `browser_fill_form`
        - Fallback: short "human leg" with exact URL, form values, expected screenshot — paused and resumed on paste-back (same rule as Phase 113 D-01).

    12. `## 11. Output Artifacts (per CONTEXT D-12, D-13)`
        - List the file layout from D-13 verbatim (METHODOLOGY.md, essentials.md, compass.md, read-rank.md, treasury.md, cross-app.md, GAPS.md, gaps.csv, screenshots/{app}/).
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "200 W Kirkwood Ave, Bloomington, IN 47404" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "Antipartisan" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "G-114-NNN" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "baseline_ref" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "blocker" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "confusing" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "ux-friction" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "essentials.empowered.vote" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "compass.empowered.vote" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "readrank.empowered.vote" .planning/research/ux-walkthrough/METHODOLOGY.md &amp;&amp; grep -q "treasurytracker.empowered.vote" .planning/research/ux-walkthrough/METHODOLOGY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - `grep -q "200 W Kirkwood Ave, Bloomington, IN 47404" .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - `grep -q "production" .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - `grep -q "Antipartisan" .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - `grep -q "G-114-NNN" .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - `grep -q "baseline_ref" .planning/research/ux-walkthrough/METHODOLOGY.md` succeeds
    - All three severity tokens (`blocker`, `confusing`, `minor`) and all four type tokens (`data`, `feature`, `content`, `ux-friction`) appear via grep
    - All four prod URLs (essentials/compass/readrank/treasurytracker.empowered.vote) appear via grep
    - Contains section headings: `## 1. Persona`, `## 2. Address`, `## 3. Environment`, `## 5. Severity Definitions`, `## 6. Type Definitions`, `## 7. Gap Entry Schema`, `## 8. Intentional Omissions`, `## 9. "Done" Criteria Per App`
  </acceptance_criteria>
  <done>METHODOLOGY.md is committed with all 11 required sections, hardcoded address, hardcoded prod URLs, severity/type enums with worked examples, and explicit antipartisan intentional-omissions block.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize empty GAPS.md, gaps.csv, and screenshots/ subdirs</name>
  <files>.planning/research/ux-walkthrough/GAPS.md, .planning/research/ux-walkthrough/gaps.csv, .planning/research/ux-walkthrough/screenshots/essentials/.gitkeep, .planning/research/ux-walkthrough/screenshots/compass/.gitkeep, .planning/research/ux-walkthrough/screenshots/read-rank/.gitkeep, .planning/research/ux-walkthrough/screenshots/treasury/.gitkeep, .planning/research/ux-walkthrough/screenshots/cross-app/.gitkeep</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-11, D-13)
    - .planning/research/benchmark/MATRIX.md (CSV header pattern reference per D-12)
  </read_first>
  <action>
    1. Create `.planning/research/ux-walkthrough/GAPS.md` with this exact skeleton:

       ```markdown
       # Phase 114 — Gap Register

       **Phase:** 114-ux-walkthrough
       **Generated:** 2026-04-13
       **Schema:** per `METHODOLOGY.md` §7 (id, app, screen, description, severity, type, evidence, baseline_ref)
       **Note:** IDs are `G-114-NNN` zero-padded and MUST be monotonic across the whole phase (not per-app). Antipartisan omissions (party labels, endorsements, interest-group ratings) are intentional per METHODOLOGY.md §8 and MUST NOT appear here.

       ## Summary Counts
       _Populated by Plan 114-07 aggregation. Left empty here._

       ## Gaps

       <!-- Walkthrough plans (114-02 .. 114-06) append their gap entries below. Each entry follows the template: -->
       <!--
       ### G-114-NNN — <one-line description>
       - **app:** essentials | compass | read-rank | treasury | cross-app
       - **screen:** <label> — <prod URL>
       - **severity:** blocker | confusing | minor
       - **type:** data | feature | content | ux-friction
       - **evidence:** screenshots/<app>/<file>.png OR "<verbatim excerpt>"
       - **baseline_ref:** <row in BALLOT-BASELINE-2026-05-05.md — REQUIRED if type=data, else "n/a">
       - **notes:** <optional severity-call rationale>
       -->
       ```

    2. Create `.planning/research/ux-walkthrough/gaps.csv` with ONLY this header row (no data rows):

       ```
       id,app,screen,description,severity,type,evidence,baseline_ref
       ```

       (Plan 114-07 will populate rows by parsing GAPS.md.)

    3. Create the five screenshot subdirectories with `.gitkeep` files so they exist in git:
       - `.planning/research/ux-walkthrough/screenshots/essentials/.gitkeep`
       - `.planning/research/ux-walkthrough/screenshots/compass/.gitkeep`
       - `.planning/research/ux-walkthrough/screenshots/read-rank/.gitkeep`
       - `.planning/research/ux-walkthrough/screenshots/treasury/.gitkeep`
       - `.planning/research/ux-walkthrough/screenshots/cross-app/.gitkeep`

       Each `.gitkeep` can be an empty file.
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/GAPS.md &amp;&amp; test -f .planning/research/ux-walkthrough/gaps.csv &amp;&amp; head -1 .planning/research/ux-walkthrough/gaps.csv | grep -q "^id,app,screen,description,severity,type,evidence,baseline_ref$" &amp;&amp; test -f .planning/research/ux-walkthrough/screenshots/essentials/.gitkeep &amp;&amp; test -f .planning/research/ux-walkthrough/screenshots/compass/.gitkeep &amp;&amp; test -f .planning/research/ux-walkthrough/screenshots/read-rank/.gitkeep &amp;&amp; test -f .planning/research/ux-walkthrough/screenshots/treasury/.gitkeep &amp;&amp; test -f .planning/research/ux-walkthrough/screenshots/cross-app/.gitkeep &amp;&amp; grep -q "G-114-NNN" .planning/research/ux-walkthrough/GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - GAPS.md exists and contains the exact 8-field schema note and monotonic-ID note
    - `grep -q "G-114-NNN" .planning/research/ux-walkthrough/GAPS.md` succeeds
    - gaps.csv exists with exactly this header as line 1: `id,app,screen,description,severity,type,evidence,baseline_ref`
    - `wc -l .planning/research/ux-walkthrough/gaps.csv` returns 1 (header only, no data rows)
    - All five `screenshots/{app}/.gitkeep` files exist
    - `grep -q "Antipartisan\|antipartisan\|intentional" .planning/research/ux-walkthrough/GAPS.md` succeeds (reminder present)
  </acceptance_criteria>
  <done>GAPS.md skeleton, gaps.csv header, and all five screenshots/ subdirs are in place and ready for plans 02–06 to append to.</done>
</task>

</tasks>

<verification>
- METHODOLOGY.md has all 11 required sections, hardcoded address, hardcoded prod URLs, severity/type enums with worked examples, antipartisan omissions block
- GAPS.md skeleton exists with schema header and monotonic ID reminder
- gaps.csv has exact 8-column header and zero data rows
- All 5 screenshots/{app}/.gitkeep files exist
</verification>

<success_criteria>
- Plans 114-02 through 114-06 can append gap entries to GAPS.md without having to invent schema
- Plan 114-07 aggregation can parse GAPS.md and produce gaps.csv rows deterministically
- Every walkthrough executor reads METHODOLOGY.md first to self-police severity/type calls and antipartisan omissions
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-01-SUMMARY.md` per execute-plan template.
</output>
