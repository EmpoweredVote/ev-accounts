---
phase: 114-ux-walkthrough
plan: 05
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - .planning/research/ux-walkthrough/treasury.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/screenshots/treasury/
autonomous: false
requirements: [UX-04]
must_haves:
  truths:
    - "Treasury Tracker relevance to Monroe County is explicitly determined via a relevance-check gate"
    - "If Monroe/Bloomington data is absent, ONE top-level gap is logged and the walkthrough stops (per D-08)"
    - "If Monroe/Bloomington data is present, a full walkthrough is documented"
    - "Antipartisan rule respected (not applicable here but still stated)"
  artifacts:
    - path: ".planning/research/ux-walkthrough/treasury.md"
      provides: "Relevance check result + conditional walkthrough narrative"
      min_lines: 30
    - path: ".planning/research/ux-walkthrough/screenshots/treasury/"
      provides: "Screenshot evidence (relevance check at minimum, walkthrough if triggered)"
  key_links:
    - from: "treasury.md gap entry"
      to: "GAPS.md"
      via: "appended monotonic G-114-NNN entry"
      pattern: "G-114-[0-9]{3}"
---

<objective>
Walk the live Treasury Tracker production app (https://treasurytracker.empowered.vote/) and perform the CONTEXT D-08 relevance-check-first gate: does Treasury have ANY Monroe County or Bloomington budget data? If NO → log one top-level gap and stop. If YES → full walkthrough. Satisfies UX-04.

Purpose: Absence is itself the finding. Don't pad treasury.md with empty-state narrative if the app is not relevant to the Monroe election context — just log it and move on.

Output: treasury.md, screenshots in screenshots/treasury/, 1 or more gap entries in GAPS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/ux-walkthrough/GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treasury relevance check + conditional walkthrough</name>
  <files>.planning/research/ux-walkthrough/treasury.md, .planning/research/ux-walkthrough/GAPS.md, .planning/research/ux-walkthrough/screenshots/treasury/</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-08 — relevance-check-first gate)
    - .planning/research/ux-walkthrough/METHODOLOGY.md ("done" criteria for Treasury, schema)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — find next monotonic G-114-NNN)
  </read_first>
  <action>
    Step 1 — Relevance check (MANDATORY FIRST):
    Drive https://treasurytracker.empowered.vote/ using Playwright MCP tools:
    - `mcp__plugin_playwright_playwright__browser_navigate`
    - `mcp__plugin_playwright_playwright__browser_snapshot`
    - `mcp__plugin_playwright_playwright__browser_take_screenshot`
    - `mcp__plugin_playwright_playwright__browser_click`

    Explicitly look for any Monroe County, IN or Bloomington, IN budget data:
    - Landing page — look for city/county picker, state filter, or default data
    - If a picker exists, attempt to select "Bloomington" or "Monroe County" or "Indiana"
    - Screenshot: `screenshots/treasury/01-landing.png`
    - Screenshot: `screenshots/treasury/02-relevance-check.png` (whatever the result of the picker / default view is)

    Step 2a — If Monroe/Bloomington data is ABSENT (expected outcome per PROJECT context — Bloomington import is still pending):
    - STOP walking. Do NOT walk the budget drill-down for Indianapolis/other cities.
    - Create `treasury.md` with:
      - `# Treasury Tracker — UX Walkthrough (UX-04)`
      - `**App:** https://treasurytracker.empowered.vote/`
      - `**Run date:** 2026-04-13`
      - `## 1. Relevance Check (per CONTEXT D-08)`
      - A paragraph stating the gate result: "Monroe County / Bloomington data is ABSENT. Per D-08, walkthrough is halted after the relevance check and one top-level gap is logged."
      - Screenshot references to 01-landing.png and 02-relevance-check.png
      - `## 2. Top-Level Gap`
      - Cite the single gap ID logged in GAPS.md
    - Append ONE gap entry to GAPS.md with MONOTONIC next ID:
      ```
      ### G-114-NNN — Treasury Tracker has no Monroe County / Bloomington budget data
      - **app:** treasury
      - **screen:** Landing — https://treasurytracker.empowered.vote/
      - **severity:** blocker
      - **type:** data
      - **evidence:** screenshots/treasury/02-relevance-check.png
      - **baseline_ref:** n/a (relevance gate — Treasury is out-of-ballot-scope until Monroe data is imported)
      - **notes:** Per CONTEXT D-08, absence itself is the finding. Treasury is not useful in a Monroe election context until data is imported. Full walkthrough deliberately skipped.
      ```

    Step 2b — If Monroe/Bloomington data is PRESENT:
    - Run a full walkthrough: category drill-down, line-item detail, budget-vs-actual comparison, any cross-app or time-range controls. Screenshot each screen (`NN-label.png`).
    - Create `treasury.md` with full screen-by-screen narrative sections (§1 Landing, §2 City picker, §3 Budget overview, §4 Category drill-down, §5 Line-item detail, §6 Gap Summary).
    - Append ≥2 gap entries with MONOTONIC IDs, each with correct schema.

    Hard rules:
    - Monotonic ID: read GAPS.md first, find `max(G-114-NNN)`, use `max+1` for the first new entry.
    - Every `type: data` entry needs `baseline_ref` (use "n/a" ONLY for the relevance-gate top-level entry, since baseline is a ballot baseline, not a treasury baseline).
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/treasury.md &amp;&amp; ls .planning/research/ux-walkthrough/screenshots/treasury/ | grep -cE "\.png$" | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; grep -q "Relevance Check" .planning/research/ux-walkthrough/treasury.md &amp;&amp; grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 14 {exit 0} {exit 1}' &amp;&amp; grep -A6 "Treasury Tracker has no\|treasury" .planning/research/ux-walkthrough/GAPS.md | grep -q "app:\*\* treasury" &amp;&amp; test "$(wc -l &lt; .planning/research/ux-walkthrough/treasury.md)" -ge 30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/treasury.md` succeeds
    - `grep -q "Relevance Check" .planning/research/ux-walkthrough/treasury.md` succeeds
    - At least 2 PNG screenshots in `screenshots/treasury/` (landing + relevance check result at minimum)
    - Combined essentials+compass+read-rank+treasury gap count ≥ 14 (`grep -cE "^### G-114-[0-9]{3} " GAPS.md` ≥ 14)
    - At least one gap entry in GAPS.md has `**app:** treasury`
    - If relevance gate fails (ABSENT path): treasury.md is short (relevance + gap only), not a padded empty-state narrative
    - If relevance gate passes (PRESENT path): treasury.md has ≥6 top-level sections and ≥2 treasury gap entries
    - Monotonic IDs: no duplicate G-114-NNN across whole GAPS.md
  </acceptance_criteria>
  <done>treasury.md records the relevance check result and either (a) stops after one top-level gap or (b) continues to a full walkthrough with ≥2 gap entries. GAPS.md has at least one treasury entry with correct schema.</done>
</task>

</tasks>

<verification>
- treasury.md exists with explicit §1 Relevance Check
- screenshots/treasury/ has ≥2 PNGs
- GAPS.md has ≥1 treasury gap entry with monotonic ID
- If relevance gate fails: walkthrough stopped after gate (per D-08)
</verification>

<success_criteria>
- UX-04 satisfied: Treasury relevance to Monroe election context is explicitly documented; walkthrough conducted iff relevant
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-05-SUMMARY.md` per execute-plan template.
</output>
