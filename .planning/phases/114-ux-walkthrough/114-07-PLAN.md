---
phase: 114-ux-walkthrough
plan: 07
type: execute
wave: 4
depends_on: [114-02, 114-03, 114-04, 114-05, 114-06]
files_modified:
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/gaps.csv
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Every G-114-NNN ID is monotonic and unique across the entire phase (no duplicates, no gaps in numbering)"
    - "Every data-type gap has a non-empty baseline_ref pointing to BALLOT-BASELINE-2026-05-05.md"
    - "gaps.csv mirrors GAPS.md exactly, one row per gap entry"
    - "GAPS.md §'Summary Counts' is populated with totals by severity × type × app"
    - "No antipartisan-omission entries (party/endorsement/rating) exist anywhere in GAPS.md"
  artifacts:
    - path: ".planning/research/ux-walkthrough/gaps.csv"
      provides: "Machine-readable gap table for Phase 115 ingestion"
    - path: ".planning/research/ux-walkthrough/GAPS.md"
      provides: "Same as before, now with populated summary counts at top"
  key_links:
    - from: "gaps.csv rows"
      to: "GAPS.md entries"
      via: "1:1 mirror"
      pattern: "G-114-[0-9]{3}"
    - from: "data-type entries"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "baseline_ref column"
      pattern: "baseline_ref"
---

<objective>
Reconcile GAPS.md, validate the cross-phase invariants (monotonic IDs, baseline_ref presence for data-type gaps, no antipartisan omissions), generate `gaps.csv` as a 1:1 machine-readable mirror of GAPS.md, and populate GAPS.md's "Summary Counts" section with totals by severity × type × app. This is the Phase 115 ingestion handoff.

Purpose: Individual walkthrough plans append entries independently. This plan is the reconciliation pass that proves the whole register is consistent and produces the CSV artifact Phase 115 reads.

Output: Reconciled GAPS.md (with summary counts), populated gaps.csv.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/ux-walkthrough/GAPS.md
@.planning/research/ux-walkthrough/gaps.csv
@.planning/research/BALLOT-BASELINE-2026-05-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile GAPS.md — fix monotonic IDs and populate summary counts</name>
  <files>.planning/research/ux-walkthrough/GAPS.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-10, D-11 — schema and invariants)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (§7 schema, §8 antipartisan rule)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — full register from plans 02–06)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (for validating baseline_ref pointers)
  </read_first>
  <action>
    1. Read GAPS.md end-to-end. Extract every `### G-114-NNN —` entry.

    2. **Validate monotonicity:**
       - Collect all IDs in file order. If any IDs are out of order, duplicated, or have gaps in numbering (e.g. 001, 002, 004 skipping 003), RENUMBER every entry sequentially starting at G-114-001 in file order.
       - Renumbering rule: preserve file order (which reflects walkthrough execution order: essentials → compass → read-rank → treasury → cross-app). Reassign IDs as G-114-001, G-114-002, ... G-114-NNN.
       - If renumbering happens, ALSO update the `§13/§10/§8/§2/§7 Gap Summary (IDs only)` section in each per-app markdown file (essentials.md, compass.md, read-rank.md, treasury.md, cross-app.md) to match the new IDs. Find-and-replace by original ID.

    3. **Validate antipartisan rule:** Grep for entries containing "party label", "endorsement", or "interest group rating" (case-insensitive). If any match, that is a methodology violation per METHODOLOGY.md §8 — REMOVE those entries entirely and renumber again.

    4. **Validate baseline_ref:** For every entry with `**type:** data`, confirm `**baseline_ref:**` is non-empty and is NOT "n/a" (except for the treasury relevance-gate entry, which is explicitly allowed to use n/a per plan 114-05). If a data-type entry has missing/empty/"n/a" baseline_ref AND it is not the treasury relevance gate, FAIL the task and report which entry needs fixing (do not invent a baseline_ref).

    5. **Populate Summary Counts** in GAPS.md §"Summary Counts" with these tables:

       ```markdown
       ## Summary Counts

       **Total gaps:** N

       ### By App
       | App | Count |
       |-----|-------|
       | essentials | X |
       | compass | X |
       | read-rank | X |
       | treasury | X |
       | cross-app | X |

       ### By Severity
       | Severity | Count |
       |----------|-------|
       | blocker | X |
       | confusing | X |
       | minor | X |

       ### By Type
       | Type | Count |
       |------|-------|
       | data | X |
       | feature | X |
       | content | X |
       | ux-friction | X |

       ### By Severity × Type (cross-tab)
       | | data | feature | content | ux-friction |
       |---|------|---------|---------|-------------|
       | blocker | X | X | X | X |
       | confusing | X | X | X | X |
       | minor | X | X | X | X |
       ```

       Counts are derived by parsing GAPS.md entries; do not invent numbers.
  </action>
  <verify>
    <automated>test $(grep -oE "G-114-[0-9]{3}" .planning/research/ux-walkthrough/GAPS.md | sort | uniq -d | wc -l) -eq 0 &amp;&amp; grep -q "^## Summary Counts$" .planning/research/ux-walkthrough/GAPS.md &amp;&amp; grep -q "Total gaps:" .planning/research/ux-walkthrough/GAPS.md &amp;&amp; ! grep -qi "party label\|endorsement list\|interest group rating" .planning/research/ux-walkthrough/GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - No duplicate G-114-NNN IDs: `grep -oE "G-114-[0-9]{3}" GAPS.md | sort | uniq -d | wc -l` == 0
    - IDs are contiguous starting at G-114-001: first ID = `G-114-001`, max ID = `G-114-0NN` where NN = total count
    - `grep -q "^## Summary Counts$" GAPS.md` succeeds and counts are populated (not the placeholder text)
    - `grep -q "Total gaps:" GAPS.md` succeeds
    - No antipartisan-violation strings in GAPS.md: `grep -qi "party label\|endorsement list\|interest group rating" GAPS.md` returns non-zero (no match)
    - Every data-type entry has non-empty baseline_ref OR is the single treasury relevance-gate entry with documented "n/a (relevance gate)"
    - If renumbering occurred, per-app markdown files' Gap Summary sections match the new IDs
  </acceptance_criteria>
  <done>GAPS.md has monotonic contiguous IDs, populated summary count tables, validated baseline_refs, and zero antipartisan-omission entries.</done>
</task>

<task type="auto">
  <name>Task 2: Generate gaps.csv from GAPS.md and validate 1:1 mirror</name>
  <files>.planning/research/ux-walkthrough/gaps.csv</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-11 schema, D-12 CSV pairing pattern)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (§7 schema)
    - .planning/research/ux-walkthrough/GAPS.md (POST-Task-1 reconciled state)
    - .planning/research/benchmark/MATRIX.md (113's CSV pairing reference)
  </read_first>
  <action>
    1. Parse GAPS.md — for every `### G-114-NNN —` entry, extract the 8 schema fields (id, app, screen, description, severity, type, evidence, baseline_ref).

    2. Write `.planning/research/ux-walkthrough/gaps.csv` with:
       - Line 1 (header, already exists): `id,app,screen,description,severity,type,evidence,baseline_ref`
       - One data row per gap entry, in the same order as GAPS.md

    3. CSV quoting rules:
       - Fields containing commas, double-quotes, or newlines must be double-quoted and internal double-quotes escaped as `""`
       - Empty `baseline_ref` for non-data entries: emit empty string (no quotes)
       - Treasury relevance-gate `baseline_ref: n/a` → emit `n/a` literal
       - `screen` field format: `<label> — <url>` (single string)
       - `description` field: take the text after `### G-114-NNN — ` on the entry heading line

    4. Validate 1:1 mirror:
       - Count of CSV data rows (excluding header) == count of `### G-114-NNN` headings in GAPS.md
       - For each row, `id` matches a heading in GAPS.md
       - For each row with `type == "data"`, `baseline_ref` is non-empty (except the one treasury relevance-gate row which may be `n/a`)

    5. Final cross-phase invariant check:
       - `awk -F, 'NR>1 && $6=="data" && ($8=="" || $8=="\"\"")' gaps.csv` MUST return zero lines (every data gap has baseline_ref)
       - Sort CSV IDs alphanumerically and confirm they are contiguous G-114-001..G-114-0NN
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/gaps.csv &amp;&amp; head -1 .planning/research/ux-walkthrough/gaps.csv | grep -q "^id,app,screen,description,severity,type,evidence,baseline_ref$" &amp;&amp; CSV_ROWS=$(tail -n +2 .planning/research/ux-walkthrough/gaps.csv | wc -l) &amp;&amp; MD_ROWS=$(grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md) &amp;&amp; test "$CSV_ROWS" -eq "$MD_ROWS" &amp;&amp; test $(awk -F, 'NR>1 &amp;&amp; $6=="data" &amp;&amp; ($8=="" || $8=="n/a") {if ($4 !~ /relevance/) print}' .planning/research/ux-walkthrough/gaps.csv | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/gaps.csv` succeeds
    - Header is exactly `id,app,screen,description,severity,type,evidence,baseline_ref`
    - `(wc -l gaps.csv) - 1 == grep -cE "^### G-114-[0-9]{3} " GAPS.md` (row count matches heading count)
    - Every CSV `id` appears exactly once as a heading in GAPS.md
    - `awk -F, 'NR>1 && $6=="data" && $8==""' gaps.csv | wc -l` == 0 (every data gap has a baseline_ref value)
    - CSV IDs sort to a contiguous G-114-001..G-114-0NN sequence (no gaps, no duplicates)
    - Total gap count in gaps.csv matches "Total gaps: N" in GAPS.md summary counts
  </acceptance_criteria>
  <done>gaps.csv is a validated 1:1 machine-readable mirror of the reconciled GAPS.md, suitable for Phase 115 ingestion.</done>
</task>

</tasks>

<verification>
- GAPS.md: monotonic contiguous IDs, populated summary counts, zero antipartisan entries, all data-type entries have baseline_ref
- gaps.csv: 1:1 mirror of GAPS.md, correct header, correct row count, all data-type rows have baseline_ref populated
- Per-app markdown files' Gap Summary sections still reference valid IDs (if renumbering occurred)
</verification>

<success_criteria>
- Phase 115 can ingest gaps.csv deterministically and trust that every `data` gap ties back to a specific baseline row
- No cross-phase invariants are violated (monotonic IDs, baseline_ref, antipartisan)
- The seven output files (METHODOLOGY.md, essentials.md, compass.md, read-rank.md, treasury.md, cross-app.md, GAPS.md + gaps.csv) are all complete
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-07-SUMMARY.md` per execute-plan template.
</output>
