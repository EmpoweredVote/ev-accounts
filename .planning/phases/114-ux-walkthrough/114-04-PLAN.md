---
phase: 114-ux-walkthrough
plan: 04
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - .planning/research/ux-walkthrough/read-rank.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/screenshots/read-rank/
autonomous: false
requirements: [UX-03]
must_haves:
  truths:
    - "A first-time Monroe County voter's journey through Read & Rank at production is documented"
    - "Sourced-quote coverage for Monroe County May 5 primary candidates is audited against BALLOT-BASELINE-2026-05-05.md"
    - "Candidate filter (by county / by race) is tested for whether it surfaces Monroe candidates"
    - "Verdict flow-back to Essentials (URL fragment for guest) is tested"
  artifacts:
    - path: ".planning/research/ux-walkthrough/read-rank.md"
      provides: "Landing → filter → quote eval → verdict flow-back walkthrough"
      min_lines: 70
    - path: ".planning/research/ux-walkthrough/screenshots/read-rank/"
      provides: "Screenshot evidence"
  key_links:
    - from: "read-rank.md gap entries"
      to: "GAPS.md"
      via: "appended monotonic G-114-NNN entries"
      pattern: "G-114-[0-9]{3}"
---

<objective>
Walk the live Read & Rank production app (https://readrank.empowered.vote/) as a first-time Monroe County voter and answer the CONTEXT D-07 framing question: "Are there enough sourced quotes for Monroe County primary candidates for the tool to be useful, and does candidate filtering actually surface them?" Produces `read-rank.md` + screenshots + appended gap entries. Satisfies UX-03.

Purpose: Read & Rank is useless in an election context without sourced quotes for the candidates on the ballot. Coverage is measured against the baseline list, not against the full universe.

Output: read-rank.md, screenshots in screenshots/read-rank/, appended gap entries in GAPS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
@.planning/research/ux-walkthrough/GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive Read & Rank walkthrough and capture screens</name>
  <files>.planning/research/ux-walkthrough/screenshots/read-rank/</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-07 scope)
    - .planning/research/ux-walkthrough/METHODOLOGY.md ("done" criteria for Read & Rank, antipartisan rule)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (Monroe County primary candidate list — denominator for coverage)
  </read_first>
  <action>
    Drive Read & Rank at https://readrank.empowered.vote/ using Playwright MCP ONLY:
    - `mcp__plugin_playwright_playwright__browser_navigate`
    - `mcp__plugin_playwright_playwright__browser_snapshot`
    - `mcp__plugin_playwright_playwright__browser_take_screenshot`
    - `mcp__plugin_playwright_playwright__browser_click`
    - `mcp__plugin_playwright_playwright__browser_fill_form`

    Walk in this order per METHODOLOGY.md §9 Read & Rank done criteria:
    1. Landing — screenshot
    2. Candidate filter — apply any available filter that narrows to Monroe County or a specific Monroe race. Screenshot the filter UI and the filtered result. Record which baseline candidates appear.
    3. Quote evaluation — open at least one quote that surfaced (for a Monroe candidate if possible; otherwise for any candidate, and log the inability to find Monroe candidates as a gap). Screenshot the evaluation UI (swipe / rank / classify interaction).
    4. Verdict flow — complete at least one quote verdict. Screenshot the verdict result.
    5. Verdict flow-back to Essentials (guest path) — attempt the URL-fragment hand-off described in CLAUDE.md. Screenshot the target Essentials page and note whether the verdict visibly appears.

    If a signup/login wall blocks any step, write a "human leg" with exact URL, form values, expected screenshot, pause, resume on paste-back.

    Screenshot convention: `screenshots/read-rank/NN-<short-label>.png`, monotonic from 01.

    Coverage audit (CRITICAL per D-07): For every Monroe County primary candidate in BALLOT-BASELINE-2026-05-05.md, note in your scratchpad whether that candidate has ANY quote in Read & Rank. This per-candidate coverage feeds Task 2's §6 coverage table.

    Antipartisan rule: DO NOT log missing party/endorsement/rating as gaps.
  </action>
  <verify>
    <automated>ls .planning/research/ux-walkthrough/screenshots/read-rank/ | grep -cE "\.png$" | awk '$1 &gt;= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - At least 5 PNG screenshots under `.planning/research/ux-walkthrough/screenshots/read-rank/` (landing, filter, quote eval, verdict, verdict flow-back target)
    - `ls .planning/research/ux-walkthrough/screenshots/read-rank/ | grep -cE "^[0-9]{2}-.+\.png$"` ≥ 5
    - Per-candidate coverage audit recorded in session notes
  </acceptance_criteria>
  <done>Read & Rank screens captured through landing, filter, evaluation, verdict, and flow-back. Coverage audit ready for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write read-rank.md and append G-114-NNN gap entries</name>
  <files>.planning/research/ux-walkthrough/read-rank.md, .planning/research/ux-walkthrough/GAPS.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-07, D-10, D-11)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (schema, enums, antipartisan)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — read to find next monotonic G-114-NNN, continue from max+1)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (baseline_ref for data-type gaps)
  </read_first>
  <action>
    1. Create `.planning/research/ux-walkthrough/read-rank.md` with these required sections:
       - `# Read & Rank — UX Walkthrough (UX-03)`
       - `**App:** https://readrank.empowered.vote/`
       - `**Framing question (per CONTEXT D-07):** Are there enough sourced quotes for Monroe County primary candidates for the tool to be useful, and does candidate filtering surface them?`
       - `**Run date:** 2026-04-13`
       - `## 1. Landing`
       - `## 2. Candidate Filter`
       - `## 3. Quote Evaluation Flow`
       - `## 4. Verdict Flow`
       - `## 5. Verdict Flow-Back to Essentials (Guest Path)`
       - `## 6. Per-Candidate Quote Coverage — Monroe May 5 Primary`
         - Table: `Candidate | Race | Quotes in Read & Rank (count) | Notes`
         - One row per Monroe County primary candidate from BALLOT-BASELINE-2026-05-05.md
       - `## 7. Answering the Framing Question`
         - 1–2 paragraph direct answer
       - `## 8. Gap Summary (IDs only)`

    2. Append gap entries to GAPS.md with MONOTONIC IDs from `max(existing G-114-NNN) + 1`. Each entry uses the METHODOLOGY.md §7 schema with `**app:** read-rank`.

    3. Hard rules:
       - Antipartisan omissions NOT logged
       - Every `type: data` entry MUST have `baseline_ref` (e.g. specific candidate row in baseline)
       - Every gap MUST have evidence

    4. Minimum floor: ≥3 voter-facing Read & Rank gap entries. Coverage gaps (candidates with 0 quotes) should each produce a data-type entry with baseline_ref = that candidate's baseline row.
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/read-rank.md &amp;&amp; grep -cE "^## " .planning/research/ux-walkthrough/read-rank.md | awk '$1 &gt;= 7 {exit 0} {exit 1}' &amp;&amp; grep -q "Per-Candidate Quote Coverage" .planning/research/ux-walkthrough/read-rank.md &amp;&amp; grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 13 {exit 0} {exit 1}' &amp;&amp; test "$(wc -l &lt; .planning/research/ux-walkthrough/read-rank.md)" -ge 70</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/read-rank.md` succeeds
    - `grep -cE "^## " .planning/research/ux-walkthrough/read-rank.md` ≥ 7
    - `grep -q "Per-Candidate Quote Coverage" .planning/research/ux-walkthrough/read-rank.md` succeeds
    - `grep -q "framing question\|Framing question" .planning/research/ux-walkthrough/read-rank.md` succeeds
    - Combined essentials+compass+read-rank gap count ≥ 13 (`grep -cE "^### G-114-[0-9]{3} " GAPS.md` ≥ 13)
    - Every read-rank gap entry contains `**app:** read-rank`
    - Every `type: data` read-rank entry has a non-empty `**baseline_ref:**`
    - No antipartisan-omission entries logged
  </acceptance_criteria>
  <done>read-rank.md answers the D-07 framing question with a per-candidate coverage table, and GAPS.md has ≥3 monotonic read-rank entries.</done>
</task>

</tasks>

<verification>
- read-rank.md has all 8 sections including §6 per-candidate coverage and §7 framing-question answer
- GAPS.md has read-rank entries with monotonic IDs
- Every data-type entry has baseline_ref
</verification>

<success_criteria>
- UX-03 satisfied: Read & Rank voter journey documented, per-candidate quote coverage audited against baseline, framing question directly answered
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-04-SUMMARY.md` per execute-plan template.
</output>
