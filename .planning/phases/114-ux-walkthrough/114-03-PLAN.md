---
phase: 114-ux-walkthrough
plan: 03
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - .planning/research/ux-walkthrough/compass.md
  - .planning/research/ux-walkthrough/GAPS.md
  - .planning/research/ux-walkthrough/screenshots/compass/
autonomous: false
requirements: [UX-02]
must_haves:
  truths:
    - "A first-time Monroe County voter's journey through Compass at production is documented screen-by-screen"
    - "Compass usability for contested Monroe County races is explicitly answered (can the voter meaningfully compare candidates?)"
    - "Stance data availability is audited per race against BALLOT-BASELINE-2026-05-05.md"
    - "Antipartisan omissions are not logged as gaps"
  artifacts:
    - path: ".planning/research/ux-walkthrough/compass.md"
      provides: "Guest entry → onboarding → quiz → compare → picker → radar walkthrough"
      min_lines: 80
    - path: ".planning/research/ux-walkthrough/screenshots/compass/"
      provides: "Screenshot evidence"
  key_links:
    - from: "compass.md gap entries"
      to: "GAPS.md"
      via: "appended monotonic G-114-NNN entries"
      pattern: "G-114-[0-9]{3}"
---

<objective>
Walk the live Compass production app (https://compass.empowered.vote/) as a first-time Monroe County voter and answer the framing question from CONTEXT D-06: "Can a Monroe County voter meaningfully use Compass to compare candidates in the May 5 contested races?" Produces `compass.md` + screenshots + appended G-114-NNN gap entries. Satisfies UX-02.

Purpose: Stance data availability for Monroe candidates is the critical success metric. Compass is useless in an election context if the voter can't overlay on a real Monroe candidate.

Output: compass.md, screenshots in screenshots/compass/, appended gap entries in GAPS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ux-walkthrough/114-CONTEXT.md
@.planning/research/ux-walkthrough/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
@.planning/research/ux-walkthrough/GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive Compass walkthrough and capture screens</name>
  <files>.planning/research/ux-walkthrough/screenshots/compass/</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-06 scope)
    - .planning/research/ux-walkthrough/METHODOLOGY.md ("done" criteria for Compass, antipartisan rule, severity/type enums)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (Monroe County contested races + candidates — this is what the voter is trying to map Compass onto)
  </read_first>
  <action>
    Drive Compass at https://compass.empowered.vote/ using Playwright MCP ONLY:
    - `mcp__plugin_playwright_playwright__browser_navigate`
    - `mcp__plugin_playwright_playwright__browser_snapshot`
    - `mcp__plugin_playwright_playwright__browser_take_screenshot`
    - `mcp__plugin_playwright_playwright__browser_click`
    - `mcp__plugin_playwright_playwright__browser_fill_form`

    Enter as GUEST (no login). Walk in this exact order per METHODOLOGY.md §9 Compass done criteria:
    1. Landing / guest entry — screenshot
    2. Onboarding (if any) — screenshot every onboarding step
    3. Topic selection — screenshot; note how many topics, any categorization
    4. Full quiz run — answer every question; screenshot at least landing, a mid-quiz state, and the completion screen
    5. Compare page — screenshot
    6. Politician picker — attempt to filter to Monroe County candidates from BALLOT-BASELINE-2026-05-05.md. Screenshot the picker UI and the filtered result. Note which baseline candidates appear and which do not.
    7. Dual-overlay radar vs at least one Monroe candidate — if a Monroe candidate is available, complete the overlay and screenshot. If NO Monroe candidates are available, screenshot the picker empty/filtered state.

    If signup/login wall blocks any step, write a "human leg" with exact URL, form values, and expected screenshot name, pause, and resume on paste-back.

    Screenshot convention: `screenshots/compass/NN-<short-label>.png`, NN monotonic starting at 01.

    Antipartisan rule: DO NOT log missing party/endorsement/rating fields as gaps.

    Stance data cross-check (CRITICAL per D-06): For every contested May 5 Monroe race in BALLOT-BASELINE-2026-05-05.md, note in your scratchpad whether Compass has ANY candidate in that race, whether that candidate has stance data filled, and whether the radar overlay succeeds. This feeds the narrative in Task 2.
  </action>
  <verify>
    <automated>ls .planning/research/ux-walkthrough/screenshots/compass/ | grep -cE "\.png$" | awk '$1 &gt;= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - At least 6 PNG screenshots under `.planning/research/ux-walkthrough/screenshots/compass/` (landing, topic select, quiz, compare, picker, radar OR picker-empty state)
    - `ls .planning/research/ux-walkthrough/screenshots/compass/ | grep -cE "^[0-9]{2}-.+\.png$"` ≥ 6
    - Running scratchpad records stance-data availability per Monroe contested race from baseline
  </acceptance_criteria>
  <done>Compass screens captured through quiz, compare, picker, and radar overlay (or documented picker-empty state). Stance-availability-per-race audit recorded in session notes for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write compass.md and append G-114-NNN gap entries</name>
  <files>.planning/research/ux-walkthrough/compass.md, .planning/research/ux-walkthrough/GAPS.md</files>
  <read_first>
    - .planning/phases/114-ux-walkthrough/114-CONTEXT.md (D-06, D-10, D-11)
    - .planning/research/ux-walkthrough/METHODOLOGY.md (schema, enums, antipartisan rule)
    - .planning/research/ux-walkthrough/GAPS.md (CURRENT state — read to find next monotonic G-114-NNN number; continue from max existing +1)
    - .planning/research/BALLOT-BASELINE-2026-05-05.md (resolve baseline_ref for every data-type gap)
  </read_first>
  <action>
    1. Create `.planning/research/ux-walkthrough/compass.md` with these required sections:
       - `# Compass — UX Walkthrough (UX-02)`
       - `**App:** https://compass.empowered.vote/`
       - `**Framing question (per CONTEXT D-06):** Can a Monroe County voter meaningfully use Compass to compare candidates in the May 5 contested races?`
       - `**Run date:** 2026-04-13`
       - `## 1. Landing / Guest Entry`
       - `## 2. Onboarding`
       - `## 3. Topic Selection`
       - `## 4. Quiz`
       - `## 5. Compare Page`
       - `## 6. Politician Picker — Monroe County Filter`
       - `## 7. Dual-Overlay Radar vs Monroe Candidate`
       - `## 8. Stance Data Availability — Per Contested Race`
         - Table with columns: `Race | Baseline candidates | Present in Compass | Stance data filled | Radar overlay works`
         - One row per contested Monroe May 5 race from BALLOT-BASELINE-2026-05-05.md
       - `## 9. Answering the Framing Question`
         - 1–2 paragraph direct answer: yes / partially / no, with rationale tied to §8
       - `## 10. Gap Summary (IDs only)`

    2. Append gap entries to GAPS.md using MONOTONIC IDs starting from `max(existing G-114-NNN) + 1`. Read GAPS.md first to find the highest existing ID (if Plan 114-02 ran first and logged up through G-114-00N, start at G-114-00(N+1)).

       Each entry uses the exact schema template from METHODOLOGY.md §7 with:
       - `**app:** compass`
       - Populated `severity`, `type`, `evidence`, and (for data type) `baseline_ref`

    3. Hard rules:
       - Antipartisan omissions NOT logged
       - Every `type: data` entry MUST have a `baseline_ref`
       - Every gap MUST have evidence (screenshot or verbatim excerpt)

    4. Minimum floor: ≥4 voter-facing Compass gap entries expected. If the stance-availability audit in §8 shows wide gaps, most will be type=data entries.
  </action>
  <verify>
    <automated>test -f .planning/research/ux-walkthrough/compass.md &amp;&amp; grep -q "framing question" .planning/research/ux-walkthrough/compass.md -i &amp;&amp; grep -cE "^## " .planning/research/ux-walkthrough/compass.md | awk '$1 &gt;= 9 {exit 0} {exit 1}' &amp;&amp; grep -cE "^### G-114-[0-9]{3} " .planning/research/ux-walkthrough/GAPS.md | awk '$1 &gt;= 10 {exit 0} {exit 1}' &amp;&amp; test "$(wc -l &lt; .planning/research/ux-walkthrough/compass.md)" -ge 80</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/ux-walkthrough/compass.md` succeeds
    - `grep -cE "^## " .planning/research/ux-walkthrough/compass.md` ≥ 9 (at least 9 top-level sections)
    - `grep -q "Stance Data Availability" .planning/research/ux-walkthrough/compass.md` succeeds
    - `grep -q "framing question\|Framing question\|framing-question" .planning/research/ux-walkthrough/compass.md` succeeds
    - Combined essentials+compass gap count ≥ 10 (`grep -cE "^### G-114-[0-9]{3} " GAPS.md` ≥ 10)
    - Every compass gap entry in GAPS.md contains `**app:** compass`
    - Every `type: data` compass entry has a non-empty `**baseline_ref:**`
    - No "party label", "endorsement", or "interest group rating" entries
    - Gap IDs in compass.md §10 match IDs appended to GAPS.md (compass block only)
  </acceptance_criteria>
  <done>compass.md answers the D-06 framing question with a §8 per-race stance-availability audit, screenshots referenced, and ≥4 monotonic compass gap entries appended to GAPS.md with correct schema.</done>
</task>

</tasks>

<verification>
- compass.md has all 10 sections including §8 per-race stance availability table and §9 framing-question answer
- GAPS.md has compass gap entries with monotonic IDs following the essentials block
- Every data-type compass gap has baseline_ref
- No antipartisan-omission gaps logged
</verification>

<success_criteria>
- UX-02 satisfied: Compass voter journey documented, stance data availability per contested race audited, framing question directly answered
</success_criteria>

<output>
After completion, create `.planning/phases/114-ux-walkthrough/114-03-SUMMARY.md` per execute-plan template.
</output>
