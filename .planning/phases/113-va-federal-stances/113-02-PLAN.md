---
phase: 113-va-federal-stances
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - backend/scripts/run-fec-finance-summary.ts
autonomous: true
requirements: [VAFI-01, VAFI-02]
tags: [campaign-finance, fec, vpap, va-federal, finance-summary]

must_haves:
  truths:
    - "FEC finance_summary is populated for every VA federal House rep that has a resolvable FEC candidate ID"
    - "Every populated finance_summary has source='FEC', total_raised (numeric >= 0), and cycle='2026'"
    - "Reps where FEC ID cannot be resolved (Vindman/McGuire/Subramanyam/Walkinshaw — newest members) are documented as skipped in the SUMMARY"
    - "VPAP has been assessed at vpap.org for VA Governor, Lt. Governor, AG, and a sample of state senators; findings documented in SUMMARY"
    - "If VPAP provides machine-readable structured data, it is ingested; if not, the SUMMARY records the finding and closes VAFI-02 with that documentation"
    - "Phase gate verify-va-federal-113.sql ASSERTION 5 reports reps_with_summary >= 7 and ASSERTION 6 reports bad_shape = 0"
  artifacts:
    - path: "backend/scripts/run-fec-finance-summary.ts"
      provides: "Existing FEC ingestion script — already handles all federal politicians including VA House reps; no new script needed"
      contains: "getFederalPoliticiansFromDb"
  key_links:
    - from: "backend/scripts/run-fec-finance-summary.ts"
      to: "essentials.politicians.finance_summary"
      via: "pool.query UPDATE essentials.politicians SET finance_summary = $1::jsonb WHERE id = $2"
      pattern: "finance_summary = \\$1::jsonb"
    - from: "backend/scripts/verify-va-federal-113.sql"
      to: "essentials.politicians.finance_summary"
      via: "ASSERTION 5 + 6 — COUNT(finance_summary) scoped to BETWEEN -5102011 AND -5102001"
      pattern: "BETWEEN -5102011 AND -5102001"
---

<objective>
Close VAFI-01 and VAFI-02. VAFI-01: run the existing `run-fec-finance-summary.ts` script — it already fetches all active federal politicians and writes finance_summary JSONB via FEC API. The 11 VA House reps (external_id BETWEEN -5102011 AND -5102001) will be picked up automatically since they are district_type = NATIONAL_LOWER in the DB. VAFI-02: assess VPAP (vpap.org) for VA state officials (Governor, Lt. Governor, AG, sample of state senators) — ingest if machine-readable structured data is available; document findings and close the requirement regardless of outcome (same pattern as DC OCF in phase 107).

Purpose: Fully close the Phase 113 requirements. VAST-04 and VAST-05 are already satisfied by migration 341. This plan handles the finance work.

Output:
- essentials.politicians.finance_summary populated for at least 7 of the 11 VA federal House reps (4 newly sworn-in reps may lack FEC IDs)
- VPAP assessment findings documented in SUMMARY (machine-readable ingest OR documented honest-skip per REQUIREMENTS.md "Out of Scope: VPAP scraping / unofficial data")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-va-federal-stances/113-PREFLIGHT.md
@backend/scripts/run-fec-finance-summary.ts
@backend/scripts/verify-va-federal-113.sql
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Key facts the executor needs to run the FEC script correctly. -->

FEC script usage:
  cd backend && npx tsx scripts/run-fec-finance-summary.ts
  Requires: DATABASE_URL (in backend/.env), FEC_API_KEY (register at api.data.gov/signup/ for 1000 req/hr)
  Runtime: ~10 min for all federal politicians (1500ms sleep between each FEC API call)
  The script queries: essentials.politicians WHERE district_type IN ('NATIONAL_UPPER','NATIONAL_LOWER')
  VA House reps are NATIONAL_LOWER — they will be included automatically

FEC crosswalk paths (from script header):
  Path 2 (primary): transparent_motivations.politician_sources WHERE source_system LIKE 'fec%' AND research_status = 'confirmed'
  Path 1a (fallback): bioguide_id -> congress-legislators YAML -> id.fec[] filtered to H-prefix
  Path 1b (fallback): full_name -> congress-legislators nameMap (for politicians without bioguide_id in DB)

Newly sworn-in reps (Jan 2025 / Sept 2025) — may not appear in congress-legislators YAML or may lack a 2026 FEC cycle committee:
  Eugene Vindman (VA-07), John McGuire (VA-09), Suhas Subramanyam (VA-10), James Walkinshaw (VA-11)
  Script handles these gracefully: logs [SKIP] No FEC ID found and continues — non-blocking.

VPAP assessment scope (VAFI-02):
  VA Governor: Glenn Youngkin (not running again; may have VPAP historical data)
  VA Lt. Governor: Winsome Earle-Sears (2025 gubernatorial candidate — active VPAP data expected)
  VA AG: Jason Miyares (2025 gubernatorial candidate — active VPAP data expected)
  VA state senators: sample 3-5 senators to determine if VPAP exposes machine-readable structured data
  VPAP URL pattern: https://www.vpap.org/candidates/{candidate-id}/
  Key question: Does VPAP expose a downloadable CSV, JSON API, or only HTML tables?
  Per REQUIREMENTS.md: "Only machine-readable official sources; honest-skip if inaccessible"
  Per REQUIREMENTS.md Out of Scope: "VPAP scraping / unofficial data — Only machine-readable official sources"
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run run-fec-finance-summary.ts for all federal politicians (VAFI-01)</name>
  <files>backend/scripts/run-fec-finance-summary.ts</files>
  <read_first>
    - backend/scripts/run-fec-finance-summary.ts (full file — understand the three crosswalk paths, DB query, and skip conditions before running)
    - backend/.env (confirm DATABASE_URL is set; confirm FEC_API_KEY is set or note it must be obtained)
    - .planning/phases/113-va-federal-stances/113-PREFLIGHT.md (VAFI-01 section — notes on FEC script, script location)
  </read_first>
  <action>
    This is primarily an execution task. No code changes are needed — the existing script already queries all NATIONAL_LOWER politicians and will include all 11 VA House reps.

    Pre-flight steps:
    1. Confirm backend/.env contains DATABASE_URL. If absent, psql "$DATABASE_URL" will fail — surface the gap and stop.
    2. Confirm backend/.env contains FEC_API_KEY. If absent, register at https://api.data.gov/signup/ to obtain a key (1000 req/hr limit). The script will exit 1 with a clear error message if the key is missing.
    3. Confirm the 11 VA reps are in the DB and have district_type = NATIONAL_LOWER: run psql "$DATABASE_URL" -c "SELECT p.full_name, p.external_id FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE p.external_id BETWEEN -5102011 AND -5102001 AND d.district_type = 'NATIONAL_LOWER' ORDER BY p.external_id DESC;" — expect 11 rows.

    Run the script and capture output:
    cd backend && npx tsx scripts/run-fec-finance-summary.ts 2>&1 | tee /tmp/113-02-fec-run.log

    Expected script behavior for the 11 VA reps:
    - Wittman, Kiggans, Scott, McClellan, Cline, Griffith, Beyer: long-serving members — bioguide_id and congress-legislators crosswalk should resolve. Expect [OK] finance_summary written.
    - Vindman, McGuire, Subramanyam, Walkinshaw: sworn in Jan 2025 or Sept 2025 — may not be in congress-legislators YAML or may lack a 2026 cycle committee. Expect [SKIP] No FEC ID found. This is acceptable and non-blocking.

    After the run completes:
    Run psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql to check ASSERTION 5 (reps_with_summary) and ASSERTION 6 (bad_shape = 0). Capture the output in the run log.

    If any long-serving rep (Wittman/Kiggans/Scott/McClellan/Cline/Griffith/Beyer) shows [SKIP], investigate:
    - Check whether their bioguide_id is populated in the DB: SELECT bioguide_id FROM essentials.politicians WHERE external_id = <ext_id>
    - If bioguide_id is NULL, the name-based fallback (Path 1b) should still work via congress-legislators YAML
    - If Path 1b also fails, document the specific failure in the SUMMARY — do NOT re-write the script to paper over it

    Do NOT modify the script logic. Do NOT add VA-specific hardcoding. The existing three-path crosswalk is the correct approach; skip failures for newest members are acceptable and documented.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT COUNT(*) AS reps_with_summary FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001 AND finance_summary IS NOT NULL;"</automated>
  </verify>
  <acceptance_criteria>
    - psql query above returns reps_with_summary >= 7 (7 long-serving reps; up to 4 newly sworn-in reps may be skipped)
    - psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql ASSERTION 6 output shows bad_shape = 0
    - For every populated finance_summary: finance_summary->>'source' = 'FEC', finance_summary->>'total_raised' is a non-null numeric string, finance_summary->>'cycle' = '2026'
    - Verify with: psql "$DATABASE_URL" -c "SELECT full_name, finance_summary->>'source' AS src, finance_summary->>'total_raised' AS raised, finance_summary->>'cycle' AS cycle FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001 ORDER BY external_id DESC;"
    - Run log /tmp/113-02-fec-run.log exists and shows the final JSON summary block (processed, succeeded, skipped_no_fec_id, errors)
    - Run log shows skipped_no_fec_id <= 4 (only the four newly sworn-in reps are acceptable skips; long-serving reps must all succeed)
    - Re-running the script produces the same reps_with_summary count (idempotency guaranteed by ON CONFLICT via UPDATE in script)
  </acceptance_criteria>
  <done>
    FEC finance_summary populated for all resolvable VA federal House reps. VAFI-01 is closed. Skipped reps documented with exact [SKIP] messages from the run log. ASSERTION 5 passes (reps_with_summary >= 7) and ASSERTION 6 passes (bad_shape = 0).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Assess VPAP for VA state officials — ingest if machine-readable (VAFI-02)</name>
  <what-built>
    Task 1 ran the FEC script and populated finance_summary for 7+ VA federal House reps (VAFI-01 closed).
    This task assesses VPAP (vpap.org) for VA state officials (Governor, Lt. Governor, AG, state senators) to determine whether machine-readable structured finance data is available.
  </what-built>
  <how-to-verify>
    Assessment steps (execute these before the human-verify checkpoint):

    1. Visit https://www.vpap.org/candidates/ and search for:
       a. Glenn Youngkin (VA Governor)
       b. Winsome Earle-Sears (VA Lt. Governor, 2025 gubernatorial candidate)
       c. Jason Miyares (VA AG, 2025 gubernatorial candidate)
       d. 2-3 VA state senators (e.g. search for "Virginia Senate")

    2. On each candidate page, look for:
       - A "Download" or "Export" button producing CSV or JSON
       - A documented public API at vpap.org/api/ or similar
       - An embedded data table that is structured (not image-based)
       - Any robots.txt or terms-of-service language prohibiting automated access

    3. If machine-readable API or CSV download is found:
       - Document the URL pattern and data structure in the SUMMARY
       - Write a targeted script (e.g. backend/scripts/run-vpap-finance-summary.ts) following the same pattern as run-fec-finance-summary.ts (env guards, pool import from '../src/lib/db.js', updateFinanceSummary with ::jsonb cast, non-aborting try/catch, pool.end on exit)
       - finance_summary shape for VPAP data: { total_raised: number, top_donors: [], cycle: string, source: 'VPAP' }
       - Run the script and verify bad_shape = 0 for any rows written
       - Add 'VPAP' to the valid sources check in verify-va-federal-113.sql ASSERTION 6 if rows are written

    4. If no machine-readable data is found (most likely outcome per REQUIREMENTS.md Out of Scope):
       - Document the finding in the SUMMARY: VPAP provides HTML-only candidate pages; no API, no CSV download confirmed on [date]
       - This documentation closes VAFI-02 per the DC OCF pattern (phase 107 precedent: "assess... ingest where machine-readable; document findings in SUMMARY if not machine-readable")
       - No ingestion script is needed; no migration needed

    Verification of closure:
    - If ingest was performed: psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql shows reps/officials with VPAP finance_summary populated and bad_shape = 0
    - If ingest was not performed: SUMMARY documents the assessment date, URLs visited, and conclusion (no machine-readable data)
    - Either outcome closes VAFI-02
  </how-to-verify>
  <resume-signal>
    Type "vpap-ingested" if machine-readable data was found and ingested, OR "vpap-documented" if VPAP provides HTML-only data and the finding is recorded in the SUMMARY. Include a one-line summary of what you found (e.g. "vpap-documented — HTML only, no API or CSV download found on 2026-06-11").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → FEC API | External HTTP calls to api.open.fec.gov; API key in env var |
| backend → PostgreSQL | pool.query writes to essentials.politicians.finance_summary |
| executor → vpap.org | Passive assessment only (no automated scraping per REQUIREMENTS.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | Information Disclosure | FEC_API_KEY in .env | mitigate | Key used only in backend process; never logged beyond first 8 chars (enforced in script line 347); never committed to git |
| T-113-03 | Tampering | finance_summary JSONB write | mitigate | Explicit ::jsonb cast in updateFinanceSummary prevents injection; script builds FinanceSummary object from typed fields only (never spreads raw FEC response — per script comment T-90-04) |
| T-113-04 | Denial of Service | FEC API rate limit | accept | 1500ms sleep between all FEC calls; well under 1000 req/hr limit; script self-limits |
| T-113-SC | Tampering | npm/pip installs | accept | No new package installs in this plan; run-fec-finance-summary.ts uses js-yaml already in package.json |
</threat_model>

<verification>
- psql "$DATABASE_URL" -f backend/scripts/verify-va-federal-113.sql — ASSERTION 5: reps_with_summary >= 7, ASSERTION 6: bad_shape = 0
- psql "$DATABASE_URL" -c "SELECT full_name, finance_summary->>'source', finance_summary->>'cycle' FROM essentials.politicians WHERE external_id BETWEEN -5102011 AND -5102001 AND finance_summary IS NOT NULL;" — all rows show source='FEC', cycle='2026'
- VAFI-02 closure confirmed in SUMMARY (either vpap-ingested or vpap-documented)
- All four phase 113 requirements appear in plan frontmatter and are closed: VAST-04 (migration 341 already committed), VAST-05 (migration 341 already committed), VAFI-01 (this plan Task 1), VAFI-02 (this plan Task 2)
</verification>

<success_criteria>
- VAFI-01: essentials.politicians.finance_summary populated for >= 7 of 11 VA federal House reps with source='FEC', cycle='2026', bad_shape = 0
- VAFI-02: VPAP assessed; either ingested (source='VPAP' rows with bad_shape=0) or documented (no machine-readable data found, finding in SUMMARY)
- verify-va-federal-113.sql ASSERTION 5 passes (reps_with_summary >= 7) and ASSERTION 6 passes (bad_shape = 0)
- Phase 113 SUMMARY captures FEC run log summary (processed/succeeded/skipped/errors counts) and VPAP assessment outcome
</success_criteria>

<output>
Create .planning/phases/113-va-federal-stances/113-02-SUMMARY.md when done — include:
1. FEC run log summary (processed, succeeded, skipped_no_fec_id counts; list of skipped rep names)
2. Per-rep finance_summary status table (full_name, external_id, populated y/n, source)
3. VPAP assessment outcome (URL visited, data format found or not found, disposition: ingested or documented-skip)
4. psql verify-va-federal-113.sql output snippet showing ASSERTION 5 + ASSERTION 6 values
5. Confirmation that VAFI-01 and VAFI-02 are closed
</output>
