---
phase: 67-login-hub-+-inform-signup-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/src/pages/Login.tsx
  - admin/src/components/InformConstraintsModal.tsx
autonomous: true

must_haves:
  truths:
    - "Unauthenticated visitor to login.empowered.vote sees the login form AND a visible 'Create an Account' CTA without scrolling"
    - "Clicking 'Create an Account' opens a modal that explains Inform Account capabilities (full Inform features, observe-only Connected/Empowered, yellow gems only)"
    - "The modal includes an 'I have an invite code' secondary link that routes to /signup (Connected path)"
    - "The modal's primary action navigates to /signup/inform"
    - "Login form behavior, layout, and existing 'Have an account? Log in' flow are not regressed"
  artifacts:
    - path: "admin/src/components/InformConstraintsModal.tsx"
      provides: "Reusable headlessui v2 modal explaining Inform Account constraints"
      contains: "DialogPanel"
      min_lines: 40
    - path: "admin/src/pages/Login.tsx"
      provides: "Login page with primary 'Create an Inform Account' CTA + secondary invite-code link"
      contains: "InformConstraintsModal"
  key_links:
    - from: "admin/src/pages/Login.tsx"
      to: "admin/src/components/InformConstraintsModal.tsx"
      via: "import + state-controlled open prop"
      pattern: "InformConstraintsModal"
    - from: "admin/src/components/InformConstraintsModal.tsx"
      to: "/signup/inform"
      via: "react-router Link or navigate"
      pattern: "/signup/inform"
    - from: "admin/src/components/InformConstraintsModal.tsx"
      to: "/signup"
      via: "react-router Link (preserves ?redirect=)"
      pattern: "/signup"
---

<objective>
Add a primary "Create an Inform Account" CTA to the Login page and a constraints modal that explains what an Inform Account can and cannot do before the user commits to signup. Preserve the existing invite-code (Connected) signup path as a secondary option.

Purpose: Phase 67 makes Inform a first-class entry point. The Login page is the gateway — the CTA must be visible without scrolling, and the modal must set accurate expectations so users self-select the right tier. This is the "anti-funnel" pattern in practice: invite into Inform, never pressure.

Output: A restyled Login.tsx with two clearly-distinguished signup paths and a new InformConstraintsModal component.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-login-hub-+-inform-signup-flow/67-RESEARCH.md

# Existing files to reference / modify
@admin/src/pages/Login.tsx
@admin/src/components/GrantRoleModal.tsx
@admin/src/lib/redirect.ts
@admin/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InformConstraintsModal component</name>
  <files>admin/src/components/InformConstraintsModal.tsx</files>
  <action>
Create a new reusable modal component using `@headlessui/react` v2 flat API (`Dialog`, `DialogPanel`, `DialogTitle`) — see `admin/src/components/GrantRoleModal.tsx` for the canonical pattern. DO NOT use the legacy v1 `Transition`/`Transition.Child` API (the old Signup.tsx uses it — that's legacy, not the standard).

Component contract:

```tsx
interface InformConstraintsModalProps {
  open: boolean;
  onClose: () => void;
  onContinue: () => void;       // navigates to /signup/inform (caller-controlled)
  onUseInviteCode: () => void;  // navigates to /signup (caller-controlled)
}
```

Layout (yellow Inform theming):
- Backdrop: `<div className="fixed inset-0 bg-black/40" aria-hidden="true" />`
- Panel: `max-w-md` centered, `bg-white dark:bg-gray-900`, `rounded-2xl`, `border border-ev-yellow/30`, shadow, padding `p-6`
- DialogTitle (h2): "What is an Inform Account?" — `text-lg font-semibold text-gray-900 dark:text-white`
- Body: brief paragraph + 3 bullet points using a checkmark/x icon set built from inline SVG or unicode `✓` / `✗` characters (NO new icon dependency). Bullets:
  - `✓` Full access to Inform features — Compass, Essentials, Read & Rank, Civics Test
  - `✓` Observe Connected and Empowered features (read-only — no participation in Validation Quests or Focused Communities)
  - `✓` Earn yellow gems for Inform activity (no red or teal gems)
- Use `text-ev-yellow` accents only on icons / heading underline; body copy in standard gray-700/gray-300.
- Primary action button: `bg-ev-yellow hover:bg-ev-yellow/90 text-ev-black font-semibold rounded-xl py-2.5 px-4 w-full text-sm`. Label: "Got it — Create my Inform Account". `onClick={onContinue}`.
- Secondary link below the button (centered, small text): "Have an invite code? Create a Connected Account →" — uses `text-ev-teal dark:text-ev-teal-light hover:underline`. `onClick={onUseInviteCode}`.
- Close-on-backdrop and Esc behavior is provided by `Dialog` automatically when `onClose` is wired.

Imports:
```tsx
import { Dialog, DialogPanel, DialogTitle } from '@headlessui/react';
```

DO NOT add any data fetching, state, or routing logic inside the modal. Caller (`Login.tsx`) decides where the buttons go — this keeps the modal pure and testable.
  </action>
  <verify>
1. File exists at `admin/src/components/InformConstraintsModal.tsx`.
2. `cd admin && npx tsc --noEmit` exits 0 (no TS errors).
3. Component exports a default React function component with the exact prop shape above.
4. Grep confirms v2 API: `grep -E "DialogPanel|DialogTitle" admin/src/components/InformConstraintsModal.tsx` returns matches; `grep "Transition.Child" admin/src/components/InformConstraintsModal.tsx` returns nothing.
  </verify>
  <done>
InformConstraintsModal.tsx renders a yellow-themed Inform constraints dialog with three checkmark bullets, a primary "Got it — Create my Inform Account" button (calls onContinue), and a secondary "Have an invite code?" link (calls onUseInviteCode). Built on headlessui v2 flat API. Type-checks clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CTA + modal into Login.tsx</name>
  <files>admin/src/pages/Login.tsx</files>
  <action>
Edit `admin/src/pages/Login.tsx` to add the modal-driven signup CTA. Preserve every existing element of the login form — only ADD new UI; do not change form behavior, redirect logic, error handling, or `useAuthStore` usage.

Changes:

1. **Add imports at the top of the file:**
   - `import { useState } from 'react';` — confirm `useState` is in the existing import line; if it already imports `useState, FormEvent`, leave alone.
   - `import { useNavigate } from 'react-router-dom';` — already imported as `Link, useNavigate`; confirm `useNavigate` is present (the file already imports `Link` only — ADD `useNavigate`).
   - `import InformConstraintsModal from '../components/InformConstraintsModal';`

2. **Inside the component, near the existing state hooks**, add:
   ```tsx
   const [signupModalOpen, setSignupModalOpen] = useState(false);
   ```
   The component already calls `useNavigate()` — reuse the existing `navigate`. (NOTE: the current file does call `useNavigate` but never uses the returned value — that's fine; we're going to use it.)

3. **Build a redirect-preserving inform signup href.** Add right after the existing `signupHref` const:
   ```tsx
   const informSignupHref = validRedirect
     ? `/signup/inform?redirect=${encodeURIComponent(validRedirect)}`
     : '/signup/inform';
   ```

4. **Replace the existing single "Don't have an account? Create one" paragraph** (currently at the bottom of the card, just above the Privacy Policy link) with a TWO-CTA block:

   ```tsx
   <div className="space-y-3 pt-2">
     <button
       type="button"
       onClick={() => setSignupModalOpen(true)}
       className="w-full py-3 px-4 bg-ev-yellow hover:bg-ev-yellow/90 text-ev-black font-semibold rounded-xl text-sm transition-colors"
     >
       Create an Account
     </button>
     <p className="text-center text-xs text-gray-500 dark:text-gray-500">
       Have an invite code?{' '}
       <Link to={signupHref} className="text-ev-teal dark:text-ev-teal-light hover:underline font-medium">
         Create a Connected Account
       </Link>
     </p>
   </div>
   ```

   Keep the Privacy Policy link below this block exactly as it is.

5. **Mount the modal at the bottom of the returned JSX** (just before the outermost closing `</div>` of the page):

   ```tsx
   <InformConstraintsModal
     open={signupModalOpen}
     onClose={() => setSignupModalOpen(false)}
     onContinue={() => {
       setSignupModalOpen(false);
       navigate(informSignupHref);
     }}
     onUseInviteCode={() => {
       setSignupModalOpen(false);
       navigate(signupHref);
     }}
   />
   ```

DO NOT remove or modify: form submission handler, `setAuth`, `getValidRedirect`, `appName` callout, error display, the heading "Log in", or password field. The CTA must be in the visible viewport at default desktop sizes (max-w-sm card, ~600px tall — verify the new buttons appear above the fold by visual check in Task 3).

CRITICAL: Do NOT change the `signupHref` variable — it stays pointing at `/signup` (Connected/invite path) for the secondary link.
  </action>
  <verify>
1. `cd admin && npx tsc --noEmit` exits 0.
2. `cd admin && npm run build` exits 0.
3. Grep confirms wiring:
   - `grep "InformConstraintsModal" admin/src/pages/Login.tsx` returns import + JSX usage.
   - `grep "/signup/inform" admin/src/pages/Login.tsx` returns the informSignupHref line.
   - `grep "bg-ev-yellow" admin/src/pages/Login.tsx` returns the new CTA button.
4. Login form's existing handler `handleSubmit` is unchanged (diff only adds; no logic edits).
  </verify>
  <done>
Login.tsx renders unchanged login form PLUS a yellow "Create an Account" button and a small "Have an invite code? Create a Connected Account" link. Clicking yellow button opens InformConstraintsModal. Modal's primary action navigates to /signup/inform; secondary link navigates to /signup. Both routes preserve any incoming ?redirect= param. Build and typecheck pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Login page now has a primary "Create an Account" CTA (yellow, prominent) and a secondary "Have an invite code?" link. Clicking the primary CTA opens the InformConstraintsModal which explains what an Inform Account is, then routes to /signup/inform on confirm. The modal also offers a path to /signup for users with invite codes.
  </what-built>
  <how-to-verify>
1. Run the admin dev server: `cd admin && npm run dev` and open http://localhost:5173/login (or whatever port Vite reports).
2. Visual check (LHUB-01): Login form is visible. A yellow "Create an Account" button appears below the password field. The "Have an invite code? Create a Connected Account" link is visible directly below it. Both should fit above the fold at 1280×720.
3. Click the yellow "Create an Account" button.
4. Modal check (LHUB-02): A dialog appears with title "What is an Inform Account?" and three checkmark bullets describing full Inform feature access, observe-only Connected/Empowered, and yellow gems only. The primary "Got it — Create my Inform Account" button is yellow. There's a secondary "Have an invite code? Create a Connected Account →" link.
5. Click "Got it — Create my Inform Account" — browser navigates to `/signup/inform`. (This route doesn't exist yet — Plan 67-02 creates it. Expect a 404 or fallback redirect to /login. That's expected at this point.)
6. Go back to /login, open modal again, click "Have an invite code? Create a Connected Account" — browser navigates to `/signup` and the existing Connected signup form (with invite code field) appears.
7. Test redirect preservation: Open `/login?redirect=https://app.empowered.vote/dashboard`. Open modal. Click primary CTA — URL should be `/signup/inform?redirect=https%3A%2F%2Fapp.empowered.vote%2Fdashboard`.
8. Press Esc with modal open — modal closes. Click backdrop — modal closes.

Report any visual issues (button size, contrast, dark-mode appearance), routing failures, or modal animation glitches.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
- TypeScript clean: `cd admin && npx tsc --noEmit`
- Production build succeeds: `cd admin && npm run build`
- Manual UAT per Task 3 checkpoint above (modal opens, both routes navigate, redirect preserved, dark mode looks right, no console errors)
- No regression in existing login flow (entering credentials and logging in still works exactly as before)
</verification>

<success_criteria>
1. `admin/src/components/InformConstraintsModal.tsx` exists, type-checks, uses headlessui v2 flat API.
2. `admin/src/pages/Login.tsx` shows the login form + yellow "Create an Account" button + "Have an invite code?" link without scrolling.
3. Clicking "Create an Account" opens a modal that accurately describes Inform constraints.
4. Modal primary CTA navigates to `/signup/inform`; secondary link navigates to `/signup`.
5. Both routes preserve `?redirect=` when present.
6. Existing login submission flow is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/67-login-hub-+-inform-signup-flow/67-01-SUMMARY.md` documenting:
- Frontmatter with `subsystem: admin-app-auth`, `tech-stack.added: []` (no new deps), affected files
- What changed in Login.tsx (additive only, no behavior changes)
- InformConstraintsModal.tsx component contract (props, behavior)
- Manual verification results from the human-verify checkpoint
- Any decisions made (e.g., copy tweaks, color adjustments)
</output>
