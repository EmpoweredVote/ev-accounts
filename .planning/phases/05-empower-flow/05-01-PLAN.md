---
phase: 05-empower-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260227000018_empower_phase5.sql
  - tests/integration/architecture.test.ts
  - backend/src/routes/account.ts
autonomous: true

must_haves:
  truths:
    - "empowered_profiles table has demoted_at and demotion_reason columns for demotion state tracking"
    - "connected_profiles table has candidate_role column with CHECK constraint limiting to valid role values"
    - "empower.consent_records table exists with user_id, consent_given_at, consent_version, consented_items, and created_at columns"
    - "execute_empowerment RPC accepts p_reserved_slug parameter, handles re-empowerment of demoted users via UPDATE (preserving original slug), and retries on unique_violation up to 5 times"
    - "execute_demotion RPC accepts p_demotion_reason JSONB parameter and sets demoted_at timestamp"
    - "GET /api/account/me returns tier:'connected', empowerment_status:'demoted', and demoted_at for demoted users (tier derivation uses is_active check)"
    - "Architecture test allowedFiles includes lib/empowerService.ts"
  artifacts:
    - path: "supabase/migrations/20260227000018_empower_phase5.sql"
      provides: "Phase 5 schema changes: candidate_role, demoted_at, demotion_reason, consent_records table, updated RPCs"
      contains: "CREATE TABLE IF NOT EXISTS empower.consent_records"
    - path: "tests/integration/architecture.test.ts"
      provides: "Updated allowedFiles list with empowerService.ts"
      contains: "empowerService.ts"
    - path: "backend/src/routes/account.ts"
      provides: "Demotion state in GET /me response"
      contains: "empowerment_status"
  key_links:
    - from: "supabase/migrations/20260227000018_empower_phase5.sql"
      to: "empower.execute_empowerment"
      via: "CREATE OR REPLACE FUNCTION with p_reserved_slug parameter"
      pattern: "p_reserved_slug"
    - from: "backend/src/routes/account.ts"
      to: "empower.empowered_profiles"
      via: "SELECT includes is_active and demoted_at for demotion state derivation"
      pattern: "empowerment_status"
---

<objective>
Phase 5 schema migration, architecture test update, and /account/me demotion state enrichment.

Purpose: Create the database foundation for empowerment and demotion lifecycle — new columns, consent table, and updated RPC functions that handle slug reservation, re-empowerment, and demotion reason tracking. Pre-approve empowerService.ts in the architecture test so Plan 02 can use supabaseAdmin without violations. Enrich GET /account/me to surface demotion state for demoted users.

Output: One migration file, updated architecture test, updated account route with demotion state.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-empower-flow/05-CONTEXT.md
@.planning/phases/05-empower-flow/05-RESEARCH.md
@supabase/migrations/20260224000006_empower_empowered_profiles.sql
@supabase/migrations/20260226000017_rpc_updates_phase4.sql
@supabase/migrations/20260224000004_connect_connected_profiles.sql
@tests/integration/architecture.test.ts
@backend/src/routes/account.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5 schema migration</name>
  <files>supabase/migrations/20260227000018_empower_phase5.sql</files>
  <action>
Create a single migration file wrapping all changes in BEGIN/COMMIT. Include these sections in order:

**Section 1: ALTER connect.connected_profiles**
- `ADD COLUMN IF NOT EXISTS candidate_role TEXT CHECK (candidate_role IN ('city_council', 'state_legislature', 'us_congress', 'president'))`
- This is the role-scope field the preflight reads to determine which compass topics to check for completeness.

**Section 2: ALTER empower.empowered_profiles**
- `ADD COLUMN IF NOT EXISTS demoted_at TIMESTAMPTZ` — set by execute_demotion, NULL for active users
- `ADD COLUMN IF NOT EXISTS demotion_reason JSONB` — stores `{ lapsed_topic_ids: string[], lapsed_at: string, triggered_by: 'cron' | 'admin' }`

**Section 3: Create empower.consent_records table**
```sql
CREATE TABLE IF NOT EXISTS empower.consent_records (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id          UUID        NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  consent_given_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  consent_version  TEXT        NOT NULL DEFAULT '1.0',
  consented_items  TEXT[]      NOT NULL,
  ip_address       TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_consent_records_user_id
  ON empower.consent_records(user_id);
```
- RLS: Enable row level security. Add SELECT policy for owner only (`auth.uid() = user_id`).
- GRANT: `GRANT SELECT ON empower.consent_records TO authenticated;`
- No INSERT/UPDATE/DELETE policies — writes go through service layer (pg pool) only.

**Section 4: CREATE OR REPLACE empower.execute_empowerment**

Updated signature: `(p_user_id UUID, p_legal_name TEXT, p_connected_profile_id UUID, p_reserved_slug TEXT DEFAULT NULL)`

Logic changes from the Phase 4 version (migration 017):
1. Check if a demoted empowered_profiles row already exists for this user (`is_active = false`). If so, UPDATE that row: set `is_active = true`, `empowered_at = now()`, `demoted_at = NULL`, `demotion_reason = NULL`, `updated_at = now()`. Preserve the original `candidate_page_slug` — do NOT generate a new slug. Store result in v_result with `SELECT * INTO v_result FROM empower.empowered_profiles WHERE user_id = p_user_id`.
2. If no existing row (fresh empowerment):
   - If `p_reserved_slug IS NOT NULL`, use it directly as `v_slug`. Otherwise, generate a slug the same way as before (kebab-case legal_name + 4-char random suffix).
   - INSERT the new row. On `unique_violation` for candidate_page_slug, retry up to 5 times with a newly generated random suffix each time. Use a `v_attempts INT := 0` counter and a LOOP/EXIT WHEN pattern. After 5 failures, RAISE the unique_violation.
3. After the INSERT or UPDATE, set all compass_responses to `visibility = 'public'` (same as Phase 4 version).
4. RETURN v_result.

Important: Keep SECURITY DEFINER, SET search_path = '', fully-qualified schema.table references, and EXCEPTION WHEN OTHERS THEN RAISE pattern.

**Section 5: CREATE OR REPLACE empower.execute_demotion**

Updated signature: `(p_user_id UUID, p_demotion_reason JSONB DEFAULT NULL)`

Logic changes from the Phase 4 version:
1. UPDATE empower.empowered_profiles SET is_active = false, `demoted_at = now()`, `demotion_reason = p_demotion_reason`, updated_at = now() WHERE user_id = p_user_id AND is_active = true.
2. UPDATE inform.compass_responses SET visibility = 'private' (same as Phase 4 version).

Keep same SECURITY DEFINER pattern and exception handler.

Comment headers should follow the same style as migration 017 (section separators, purpose descriptions).
  </action>
  <verify>
Read the migration file and verify:
1. BEGIN/COMMIT wrapper present
2. candidate_role CHECK constraint lists exactly 4 roles
3. consent_records has all required columns (id, user_id, consent_given_at, consent_version, consented_items, ip_address, created_at)
4. consent_records has RLS enabled with owner-only SELECT policy
5. execute_empowerment has p_reserved_slug parameter, re-empowerment UPDATE path, slug retry loop (up to 5 attempts)
6. execute_demotion has p_demotion_reason parameter, sets demoted_at
7. No syntax errors in SQL (all schema.table references fully qualified in RPC functions)
  </verify>
  <done>Migration file exists with all 5 sections complete. RPC functions handle re-empowerment (UPDATE preserving slug) and fresh empowerment (INSERT with retry). Demotion sets demoted_at and demotion_reason. Consent table has RLS. candidate_role column has CHECK constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Update architecture test and GET /account/me for demotion state</name>
  <files>tests/integration/architecture.test.ts, backend/src/routes/account.ts</files>
  <action>
**Architecture test update** (`tests/integration/architecture.test.ts`):

In the `allowedFiles` array of the "supabaseAdmin exists only in expected files" test, add:
```typescript
path.join(BACKEND_SRC, 'lib/empowerService.ts'),
```
Add it after the existing `lib/enrollService.ts` entry. This pre-approves the file so Plan 02 can create it without architecture test failures.

**GET /account/me update** (`backend/src/routes/account.ts`):

In the GET /me route handler, modify the empowered_profiles SELECT to also fetch `demoted_at`:
```typescript
.select('id, legal_name, is_active, candidate_page_slug, empowered_at, demoted_at')
```

In the `if (empowered)` block where the `empowered_profile` response object is built, add `demoted_at`:
```typescript
meResponse.empowered_profile = {
  legal_name: empowered.legal_name,
  is_active: empowered.is_active,
  candidate_page_slug: empowered.candidate_page_slug,
  empowered_at: empowered.empowered_at,
  demoted_at: empowered.demoted_at,
};
```

Add a new top-level field `empowerment_status` to the meResponse. Derive it as follows:
- If `empowered` exists AND `empowered.is_active === true` => `'empowered'`
- If `empowered` exists AND `empowered.is_active === false` => `'demoted'`
- Otherwise (no empowered_profiles row) => omit the field entirely (do NOT include it)

Place `empowerment_status` right after `tier` in the meResponse object.

Also update the PATCH /me response to include the same `empowerment_status` derivation and `demoted_at` field in the empowered_profile object. The PATCH route already fetches `updatedEmpowered` — update its SELECT to include `demoted_at` and add the same empowerment_status derivation logic.

Important: Update the `tier` derivation logic in BOTH GET /me and PATCH /me. The CONTEXT.md locked decision states demoted users must return `tier: 'connected'`. Change the tier derivation to:
```typescript
const tier = (empowered && empowered.is_active) ? 'empowered' : connected ? 'connected' : 'inform';
```
A demoted user has an empowered_profiles row with `is_active = false`, so they must fall through to the `connected` tier. The `empowerment_status` field provides additional active/demoted distinction.
  </action>
  <verify>
1. Run `cd C:/EV-Accounts && npx vitest run tests/integration/architecture.test.ts` — should pass (empowerService.ts doesn't exist yet, but it's only in the allowed list, which is fine).
2. Read architecture.test.ts and confirm `empowerService.ts` is in allowedFiles.
3. Read account.ts and confirm:
   - GET /me SELECT for empowered_profiles includes `demoted_at`
   - empowered_profile response object includes `demoted_at`
   - `empowerment_status` field present in meResponse (derived from empowered.is_active)
   - PATCH /me has the same updates
   - `tier` derivation updated to `(empowered && empowered.is_active) ? 'empowered' : connected ? 'connected' : 'inform'` — demoted users return tier:'connected'
  </verify>
  <done>Architecture test allows empowerService.ts for supabaseAdmin usage. GET /account/me and PATCH /account/me return `empowerment_status` ('empowered' or 'demoted') when an empowered_profiles row exists, and include `demoted_at` in the empowered_profile object. Tier derivation updated so demoted users return tier:'connected' per CONTEXT.md. Tests pass.</done>
</task>

</tasks>

<verification>
1. Migration file exists at `supabase/migrations/20260227000018_empower_phase5.sql` with all 5 sections
2. `npx vitest run tests/integration/architecture.test.ts` passes
3. `npx tsc --noEmit` (from backend/) passes — no TypeScript errors introduced
4. account.ts contains `empowerment_status` derivation and `demoted_at` in empowered_profile
</verification>

<success_criteria>
- Migration creates candidate_role column, demoted_at + demotion_reason columns, consent_records table, and updated RPC functions
- execute_empowerment handles fresh empowerment with slug reservation AND re-empowerment of demoted users preserving original slug
- execute_demotion stores demotion_reason and demoted_at
- Architecture test pre-approves empowerService.ts
- GET and PATCH /account/me surface demotion state to the caller
</success_criteria>

<output>
After completion, create `.planning/phases/05-empower-flow/05-01-SUMMARY.md`
</output>
