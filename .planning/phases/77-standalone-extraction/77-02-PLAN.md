---
phase: 77-standalone-extraction
plan: 02
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - EV-Backend/internal/middleware/middleware.go
autonomous: false
requirements: [EXTR-02, EXTR-03]

must_haves:
  truths:
    - "Visiting readrank.empowered.vote loads the Read & Rank hub page"
    - "All three routes are accessible: /, /candidate/:id/alignment, /animation-options"
    - "API calls from readrank.empowered.vote to api.empowered.vote return 200 (not CORS error)"
    - "Cloudflare Pages CI build passes including npm install of ev-ui"
  artifacts:
    - path: "EV-Backend/internal/middleware/middleware.go"
      provides: "CORS allowlist with readrank subdomain"
      contains: "readrank.empowered.vote"
  key_links:
    - from: "readrank.empowered.vote"
      to: "api.empowered.vote"
      via: "CORS preflight + credentialed fetch"
      pattern: "https://readrank.empowered.vote.*{} in allowed map"

user_setup:
  - service: cloudflare-pages
    why: "Deploy Read & Rank to readrank.empowered.vote"
    env_vars:
      - name: NPM_TOKEN
        source: "GitHub > Settings > Developer settings > Personal access tokens — use the same token value as in ~/.npmrc (the one that works for essentials/CompassV2 CI)"
    dashboard_config:
      - task: "Connect chrisandrewsedu/EV-readrank repo to Cloudflare Pages"
        location: "Cloudflare Dashboard > Workers & Pages > Create > Pages > Connect to Git"
        details: |
          Build settings:
            Framework preset: None
            Build command: npm run build
            Build output directory: dist
          Environment variables (Production AND Preview):
            NPM_TOKEN = <your GitHub token>
      - task: "Add custom domain readrank.empowered.vote"
        location: "Cloudflare Pages project > Custom domains > Set up a custom domain"
        details: "Enter readrank.empowered.vote — Cloudflare auto-provisions DNS since the domain is already on Cloudflare"
---

<objective>
Add readrank.empowered.vote to the backend CORS allowlist, coordinate Cloudflare Pages deployment via user setup, and verify all three routes and API calls work at the live URL.

Purpose: Without CORS, API calls from the new domain silently fail. Without the CF Pages deployment being triggered by the user (dashboard action), the site cannot go live.
Output: readrank.empowered.vote live with all routes working and API access confirmed.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/77-standalone-extraction/77-01-SUMMARY.md
</context>

<interfaces>
<!-- Current CORS allowlist in EV-Backend (add two entries) -->

From EV-Backend/internal/middleware/middleware.go (current allowed map — add after the last entry):
```go
var allowed = map[string]struct{}{
    "http://localhost:5173":                 {},
    // ... all existing entries ...
    "https://ev-prototypes.netlify.app":      {},
    // ADD THESE TWO:
    "https://readrank.empowered.vote":        {},
    "https://readrank-dev.empowered.vote":    {},
}
```

Note: CF Pages preview builds use random *.pages.dev URLs — those do NOT need CORS entries because the backend uses credentials and same-origin cookies. Only the production and staging custom subdomains need entries.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add readrank subdomain to backend CORS allowlist</name>
  <files>EV-Backend/internal/middleware/middleware.go</files>
  <action>
    In the `allowed` map in `EV-Backend/internal/middleware/middleware.go`, add two new entries after the existing `"https://ev-prototypes.netlify.app"` entry:

    ```go
    "https://readrank.empowered.vote":     {},
    "https://readrank-dev.empowered.vote": {},
    ```

    No other changes to this file. The CORSMiddleware function and all other middleware stay exactly the same.

    Then commit and push EV-Backend so the change deploys to the live backend on Render:
    ```bash
    cd /Users/chrisandrews/Documents/GitHub/EV-Backend
    git add internal/middleware/middleware.go
    git commit -m "feat(77): add readrank.empowered.vote to CORS allowlist

    EXTR-03"
    git push origin main
    ```
  </action>
  <verify>
    <automated>grep "readrank.empowered.vote" /Users/chrisandrews/Documents/GitHub/EV-Backend/internal/middleware/middleware.go</automated>
  </verify>
  <done>
    - Both readrank entries are in the allowed map
    - Commit pushed to EV-Backend main
    - Render will auto-deploy (check Render dashboard if needed)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure Cloudflare Pages deployment</name>
  <files>cloudflare-pages-dashboard (no files — external dashboard action)</files>
  <action>
    Claude cannot access the Cloudflare dashboard. You must configure and trigger the Cloudflare Pages deployment manually. Steps are in how-to-verify below.
  </action>
  <verify>
    <automated>MISSING — deployment requires Cloudflare dashboard access; verify manually via steps below</automated>
  </verify>
  <done>Cloudflare Pages build completes successfully and readrank.empowered.vote resolves to the deployed app</done>
  <what-needed>
    1. Add NPM_TOKEN environment variable to Cloudflare Pages BEFORE triggering any build (build fails with 401 if this is missing).
    2. Connect the chrisandrewsedu/EV-readrank repo to Cloudflare Pages with these build settings:
       - Build command: npm run build
       - Build output directory: dist
       - Framework preset: None
    3. Add the custom domain readrank.empowered.vote (Cloudflare auto-handles DNS since the domain is already on your Cloudflare account).

    The NPM_TOKEN value is the same GitHub Personal Access Token you use locally in ~/.npmrc — the one that lets essentials and CompassV2 install ev-ui in CI.
  </what-needed>
  <steps>
    1. Go to Cloudflare Dashboard > Workers & Pages > Create application > Pages > Connect to Git
    2. Select the chrisandrewsedu/EV-readrank repository
    3. Set build command: npm run build
    4. Set build output directory: dist
    5. BEFORE saving: go to Settings > Environment variables and add:
       - Variable name: NPM_TOKEN
       - Value: (your GitHub token — the same one in ~/.npmrc)
       - Apply to: Production AND Preview
    6. Save and trigger the first build
    7. Wait for build to complete successfully (check build log for npm install success)
    8. Go to Custom domains > Add readrank.empowered.vote
    9. Wait for DNS propagation (usually instant since domain is already on Cloudflare)
  </steps>
  <resume-signal>Type "deployed" once readrank.empowered.vote loads the app, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all routes and API access at live URL</name>
  <files>none</files>
  <action>
    Smoke test the live deployment to confirm zero behavior changes from prototype and CORS working from new domain.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -H "Origin: https://readrank.empowered.vote" https://api.empowered.vote/essentials/quotes</automated>
  </verify>
  <done>
    - curl returns 200 (CORS headers present for readrank.empowered.vote)
    - All three routes load in browser
    - localStorage key is ev_readrank
  </done>
  <what-built>
    - Read & Rank extracted repo deployed to Cloudflare Pages
    - Custom domain readrank.empowered.vote configured
    - Backend CORS updated for the new domain
  </what-built>
  <how-to-verify>
    1. Visit https://readrank.empowered.vote — confirm the hub page loads with EV logo and issue list
    2. Click into an issue — confirm the evaluation phase loads
    3. Visit https://readrank.empowered.vote/animation-options directly — confirm it loads without 404 (tests SPA _redirects)
    4. Open DevTools > Network tab — confirm any api.empowered.vote calls return 200, not CORS errors
    5. Open DevTools > Application > Local Storage > readrank.empowered.vote — confirm key ev_readrank exists after any interaction
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe what failed</resume-signal>
</task>

</tasks>

<verification>
Phase 77 complete when:
1. https://readrank.empowered.vote loads the hub (route /)
2. https://readrank.empowered.vote/animation-options loads directly without 404 (SPA routing working)
3. curl CORS check returns 200
4. localStorage key is ev_readrank (not readrank-storage)
5. Cloudflare Pages build log shows npm install succeeded with ev-ui 0.1.41
</verification>

<success_criteria>
- readrank.empowered.vote serves all three routes identically to the prototype behavior
- CORS preflight from readrank.empowered.vote succeeds against api.empowered.vote
- Zustand state persists to ev_readrank key in localStorage
- CI build passes including GitHub npm registry auth via NPM_TOKEN
</success_criteria>

<output>
After completion, create `.planning/phases/77-standalone-extraction/77-02-SUMMARY.md`
</output>
