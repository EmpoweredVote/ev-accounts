---
phase: 78-city-stance-research
plan: 03
type: execute
wave: 2
depends_on: [78-02]
files_modified: [backend/migrations/NNN_sd_stances.sql]
autonomous: false
requirements: [CSTA-02]
tags: [stance-research, san-diego, migration, csta-02]

must_haves:
  truths:
    - "All 11 San Diego officials (Mayor Todd Gloria + City Attorney Mara Elliott + 9 council members D1–D9) have stance research CSV output"
    - "Every San Diego official has at least 5 stance rows in inform.politician_answers (D-07 floor)"
    - "Every researched stance has a paired inform.politician_context row with at least one source URL"
    - "data-centers topic is NOT present in any San Diego politician_answers row (D-06 exclusion)"
    - "No San Jose research is concurrently in flight (D-05 — one city at a time)"
  artifacts:
    - path: "backend/data/stance-research/2026-MM-DD-san-diego-officials.csv"
      provides: "Reviewable CSV of all researched stances before migration generation"
    - path: "backend/migrations/NNN_sd_stances.sql"
      provides: "Idempotent stance migration for 11 SD officials"
  key_links:
    - from: "backend/migrations/NNN_sd_stances.sql"
      to: "essentials.politicians (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010)"
      via: "hard-coded politician UUIDs resolved from live DB before migration is written"
      pattern: "INSERT INTO inform.politician_answers .* VALUES \\('[a-f0-9-]+',"
---

<objective>
Research and ingest CompassV2 stance data for all 11 San Diego city officials (Mayor Todd Gloria, City Attorney Mara Elliott, plus 9 council members D1–D9) across all 42 applicable compass topics (43 live topics minus `data-centers`). Output one migration file `backend/migrations/NNN_sd_stances.sql` and apply it via psql to remote Supabase.

This is Wave 2 of the four-city sequence. Per D-05, Wave 1 (San Jose) must be applied and verified before this plan begins. No other research-stances dispatch may run concurrently.

Purpose: Close CSTA-02 — every San Diego official appears in the compass compare view with sourced positions. SD has the largest officials roster among the new cities (11 including a separately-elected City Attorney) and is the closest analog to the SF roster from migration 216.

Output:
1. `backend/data/stance-research/YYYY-MM-DD-san-diego-officials.csv` — researcher CSV
2. `backend/migrations/NNN_sd_stances.sql` — idempotent migration applied via psql
3. Updated SUMMARY recording row counts per politician and assigned migration number
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@.planning/phases/78-city-stance-research/78-02-SUMMARY.md
@backend/migrations/216_sf_officials_stances.sql
@backend/migrations/219_fremont_officials_stances.sql
@backend/migrations/208_sd_officials.sql
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch research-stances skill for San Diego officials and produce reviewable CSV</name>
  <files>backend/data/stance-research/YYYY-MM-DD-san-diego-officials.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (full skill flow, especially D-05 one-at-a-time rule on line 78-81)
    - backend/migrations/208_sd_officials.sql (defines the 11 SD politicians — confirms full_name spelling and current incumbency; SKILL warns SD D3–D9 names should be verified against current incumbents)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Politician Name Roster" (San Diego row)
    - .planning/phases/78-city-stance-research/78-CONTEXT.md §Research Execution (D-05, D-06, D-07)
    - backend/.claude/agent-memory/politician-stance-researcher/MEMORY.md (researcher persistent notes — any SD-specific quirks)
    - .planning/phases/78-city-stance-research/78-02-SUMMARY.md (confirm Wave 1 / SJ is closed before starting; if SUMMARY does not exist or shows incomplete state, ABORT)
  </read_first>
  <action>
PRECONDITION: Read `.planning/phases/78-city-stance-research/78-02-SUMMARY.md`. If it does not exist or its "All 11 officials have ≥ 5 stances" assertion is unmet, ABORT — Wave 1 must complete first per D-05.

Verify against live DB before dispatch — names may have changed since migration 208:
  `psql "$DATABASE_URL" -c "SELECT external_id, full_name FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010) ORDER BY external_id DESC;"`

If any of the 11 names from 78-PATTERNS.md don't match the DB, pass the DB name (the source of truth) to the researcher. The 11 expected by-name from PATTERNS.md are: Todd Gloria (Mayor), Mara Elliott (City Attorney), Joe LaCava (D1), Jennifer Campbell (D2), Stephen Whitburn (D3), Henry Foster III (D4), Marni von Wilpert (D5), Kent Lee (D6), Raul Campillo (D7), Vivian Moreno (D8), Sean Elo-Rivera (D9).

Invoke the `/research-stances` skill with the 11 names from the DB query. Normal mode, no --topics filter — all 42 applicable topics minus `data-centers` (per D-06).

Dispatch rules (D-05, MEMORY.md): ONE agent at a time, never parallel. CSV output: `backend/data/stance-research/$(date +%F)-san-diego-officials.csv`.

Per-agent prompt requirements (same as Wave 1):
- 42-topic UUID list (topic_key + UUID) from `inform.compass_topics WHERE is_live = true` minus the `data-centers` row
- WebFetch ONLY (no WebSearch, no Playwright)
- D-07 floor: ≥ 5 stances per politician
- Party stays NULL — antipartisan
- City Attorney Mara Elliott: include judicial-* topics since her role is legal/prosecutorial; she should get more judicial coverage than the council members
- Mayor Todd Gloria: full 42 topics applicable; he has the longest public record

Present the STEP 3 approval summary table to the user. Wait for explicit approval before Task 2.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/*-san-diego-officials.csv &amp;&amp; awk -F, 'NR>1 {names[$1]=1} END {n=0; for (k in names) n++; exit (n==11 ? 0 : 1)}' backend/data/stance-research/*-san-diego-officials.csv</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/YYYY-MM-DD-san-diego-officials.csv` exists with header row and at least 55 data rows (11 × 5 floor)
    - CSV `full_name` column contains all 11 distinct San Diego officials — verified by `awk -F, 'NR>1 {print $1}' ... | sort -u | wc -l` returning 11
    - CSV `topic_key` column never contains the string `data-centers` — `grep -c ',data-centers,' ...` returns 0
    - Every row has a non-empty `source_url_1` — `awk -F, 'NR>1 && $6 == "" {n++} END {exit (n>0 ? 1 : 0)}' ...`
    - Every politician has ≥ 5 rows — `awk -F, 'NR>1 {n[$1]++} END {for (k in n) if (n[k]<5) {print k": "n[k]; bad=1} exit bad}' ...` exits 0
    - User explicit approval recorded in conversation transcript
    - Wave 1 summary file `.planning/phases/78-city-stance-research/78-02-SUMMARY.md` exists and shows Wave 1 closed BEFORE this task began
  </acceptance_criteria>
  <done>
    Approved CSV with ≥ 55 sourced stances across all 11 San Diego officials; `data-centers` excluded; every row has at least one source URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate, apply, and verify migration NNN_sd_stances.sql from the approved CSV</name>
  <files>backend/migrations/NNN_sd_stances.sql</files>
  <read_first>
    - backend/migrations/219_fremont_officials_stances.sql (canonical city stance migration structure)
    - backend/migrations/216_sf_officials_stances.sql (the larger-roster analog — SD's 11-official count is closer to SF's 20 than to Fremont's 7; reference for section organization with both Mayor + City Attorney sections)
    - backend/migrations/208_sd_officials.sql (politician records — external_id ranges -650001, -650002, -650010 to -650018)
    - backend/data/stance-research/YYYY-MM-DD-san-diego-officials.csv (approved Task 1 output)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Core INSERT/ON CONFLICT Pattern" and §"Migration Header Pattern"
    - backend/migrations/NNN_sj_stances.sql (the just-completed Wave 1 migration — confirms the assigned NNN for SJ so SD can use NNN+1)
    - backend/.env (DATABASE_URL — read but do not echo)
  </read_first>
  <action>
Step 1 — Determine migration number. Run:
  `ls backend/migrations/ | grep -E '^[0-9]{3}_' | sort -r | head -5`
Take the highest numeric prefix and add 1. This will be the SJ migration number + 1 (since Wave 1 completed before this plan). Record the chosen NNN.

Step 2 — Resolve politician UUIDs from live DB:
  `psql "$DATABASE_URL" -c "SELECT id, full_name, external_id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010) ORDER BY external_id DESC;"`
11 rows expected. Capture (full_name, UUID) for the politician reference block.

Step 3 — Write `backend/migrations/NNN_sd_stances.sql` following the EXACT structure of `219_fremont_officials_stances.sql`:
  (a) Header: `-- Migration NNN: San Diego Officials Stances — 11 Politicians`. Include `-- Topic scope: 42 topics (all 43 live topics except data-centers)`.
  (b) Topic UUID reference block — 42-topic block from PATTERNS.md, data-centers excluded.
  (c) Politician UUID reference block — 11 lines, one per official (Mayor → City Attorney → D1–D9 order).
  (d) `BEGIN;`
  (e) Per-official sections in order: Todd Gloria (Mayor), Mara Elliott (City Attorney), then D1–D9 council members. Each section header uses the role label from PATTERNS.md (e.g., `-- Todd Gloria (Mayor)`, `-- Mara Elliott (City Attorney)`, `-- Joe LaCava (District 1)`). Within each section, emit paired INSERTs (politician_answers + politician_context) for every CSV row belonging to that politician, in topic_key alphabetical order or CSV file order — either is acceptable. `$$dollar-quoted$$` reasoning, `ARRAY[...]::text[]` sources, dual ON CONFLICT DO UPDATE.
  (f) `COMMIT;`

Step 4 — Apply via psql:
  `psql "$DATABASE_URL" -f backend/migrations/NNN_sd_stances.sql`
Must exit 0.

Step 5 — Spot-check row counts:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"`
Must equal the CSV row count.

Step 6 — Confirm data-centers exclusion:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"` returns 0.

Step 7 — Confirm context pairing:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"` returns 0.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));" returns "11"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/migrations/NNN_sd_stances.sql` exists where NNN = one greater than the max prefix in `backend/migrations/` at apply time
    - File begins with `-- Migration NNN: San Diego Officials Stances — 11 Politicians` and contains `-- Topic scope: 42 topics (all 43 live topics except data-centers)`
    - File contains exactly 11 lines in the "Politician UUID reference" comment block
    - File contains zero references to the topic UUID `4559b513-0fd8-4ed1-babd-f3b554162f40` — `grep -c "4559b513" backend/migrations/NNN_sd_stances.sql` returns 0
    - `psql "$DATABASE_URL" -f backend/migrations/NNN_sd_stances.sql` exits 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"` returns 11
    - `psql "$DATABASE_URL" -t -A -c "SELECT politician_id FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010)) GROUP BY politician_id HAVING COUNT(*) < 5;"` returns 0 rows
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"` returns 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-650001, -650002) OR external_id BETWEEN -650018 AND -650010));"` returns 0
  </acceptance_criteria>
  <done>
    San Diego stance migration applied; all 11 officials covered with ≥ 5 sourced stances each; data-centers excluded; context pairs complete. CSTA-02 closed pending the CSTA-05 audit in Wave 5.
  </done>
</task>

</tasks>

<verification>
1. CSV approved and contains all 11 SD officials with ≥ 5 stances each, sourced, no data-centers.
2. Migration file follows canonical pattern with 42-topic UUID block.
3. Applied via psql; transaction committed.
4. Spot-checks confirm count, exclusion, and context pairing.
</verification>

<success_criteria>
1. CSTA-02 closed: all 11 San Diego officials have stance research in DB with sourced context rows.
2. Wave 3 (Berkeley) is unblocked.
3. No parallel research dispatch occurred (D-05).
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-03-SUMMARY.md` when done. Record: assigned migration number, per-politician stance counts, total stance rows, and any thin-record officials hitting the 5-stance floor.
</output>
