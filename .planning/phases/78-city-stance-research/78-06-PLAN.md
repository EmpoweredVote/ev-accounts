---
phase: 78-city-stance-research
plan: 06
type: execute
wave: 5
depends_on: [78-02, 78-03, 78-04, 78-05]
files_modified: []
autonomous: true
requirements: [CSTA-05]
tags: [audit, verification, csta-05, context-rows, source-urls]

must_haves:
  truths:
    - "Across all 4 new cities (SJ, SD, Berkeley, Sacramento), zero politician_answers rows lack a paired politician_context row"
    - "Across all 4 new cities, zero politician_context rows have NULL/empty sources arrays"
    - "Every context row has at least one non-empty URL in its sources array"
    - "If any orphan or empty-source row is found, it is reported by (politician_id, full_name, topic_key) so it can be fixed before CSTA-05 is closed"
  artifacts:
    - path: ".planning/phases/78-city-stance-research/78-06-SUMMARY.md"
      provides: "Audit report listing per-city totals (answers count, context count, orphans found, empty-sources found) and overall pass/fail"
  key_links:
    - from: "inform.politician_answers (pa)"
      to: "inform.politician_context (pc)"
      via: "LEFT JOIN on (politician_id, topic_id) — pc.id IS NULL flags orphans"
      pattern: "LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id"
---

<objective>
Run a single inline SQL audit pass across the 4 new cities ingested in Waves 1–4 (San Jose, San Diego, Berkeley, Sacramento) to confirm every `inform.politician_answers` row has a paired `inform.politician_context` row, and every context row has at least one non-empty source URL. This closes CSTA-05.

The audit pattern mirrors Phase 74's SSTA-02 audit (74-03-PLAN.md lines 503-520) but scoped to the 41 new city officials added in Waves 1–4 (11 SJ + 11 SD + 10 Berkeley + 9 Sacramento). Fremont is explicitly excluded (D-04 — CSTA-04 already closed under migration 219 with a previously-verified audit).

No migration file is written in this wave. The output is a single SUMMARY recording the audit results.

Purpose: CSTA-05 requires that every stance row across the 4 new cities is paired with a sourced context row. This is the gate that confirms the Wave 1–4 migrations met the data-quality requirements set by D-06 (no silent skips, "no evidence" documented explicitly) and the broader CompassV2 contract that every politician position must be defensible with a citation.

Output:
1. `.planning/phases/78-city-stance-research/78-06-SUMMARY.md` — audit report with per-city totals and pass/fail verdict
2. If failures found: a detailed list of orphan/empty-source rows by (city, politician, topic) so the executor can return to the appropriate wave to fix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@.planning/phases/78-city-stance-research/78-02-SUMMARY.md
@.planning/phases/78-city-stance-research/78-03-SUMMARY.md
@.planning/phases/78-city-stance-research/78-04-SUMMARY.md
@.planning/phases/78-city-stance-research/78-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run orphan-answer and empty-sources audit queries; produce SUMMARY</name>
  <files>.planning/phases/78-city-stance-research/78-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"CSTA-05: Context Audit Query (Wave 5, no new file)" — the canonical orphan-check and empty-sources-check SQL
    - .planning/phases/78-city-stance-research/78-02-SUMMARY.md (SJ wave totals)
    - .planning/phases/78-city-stance-research/78-03-SUMMARY.md (SD wave totals)
    - .planning/phases/78-city-stance-research/78-04-SUMMARY.md (Berkeley wave totals)
    - .planning/phases/78-city-stance-research/78-05-SUMMARY.md (Sacramento wave totals — confirms Wave 4 is closed before this audit)
    - .planning/phases/78-city-stance-research/78-CONTEXT.md §Context Audit (D-09)
    - backend/.env (DATABASE_URL — read but do not echo)
  </read_first>
  <action>
PRECONDITION: All four Wave 1–4 SUMMARY files must exist (78-02-SUMMARY.md through 78-05-SUMMARY.md). If any is missing, ABORT — Waves 1–4 must complete first.

Step 1 — Pre-audit row totals (sanity check). Run:
  `psql "$DATABASE_URL" -c "SELECT CASE WHEN p.external_id BETWEEN -640019 AND -640001 THEN 'San Jose' WHEN p.external_id BETWEEN -650018 AND -650001 THEN 'San Diego' WHEN p.external_id BETWEEN -680017 AND -680001 THEN 'Berkeley' WHEN p.external_id BETWEEN -660017 AND -660001 THEN 'Sacramento' END AS city, COUNT(*) AS answers_count FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id WHERE (p.external_id BETWEEN -640019 AND -640001) OR (p.external_id BETWEEN -650018 AND -650001) OR (p.external_id BETWEEN -680017 AND -680001) OR (p.external_id BETWEEN -660017 AND -660001) GROUP BY city ORDER BY city;"`

Capture the 4 city counts (expected: SJ ≥ 55, SD ≥ 55, Berkeley ≥ 50, Sacramento ≥ 45 per D-07 floors). Cross-check against the wave SUMMARY totals.

Step 2 — Orphan-answer audit. Run the exact query from 78-PATTERNS.md §"CSTA-05: Context Audit Query":
  `psql "$DATABASE_URL" -c "SELECT pa.politician_id, p.full_name, t.topic_key FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id JOIN inform.compass_topics t ON t.id = pa.topic_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE p.external_id IN (SELECT external_id FROM essentials.politicians WHERE (external_id BETWEEN -640019 AND -640001) OR (external_id BETWEEN -650018 AND -650001) OR (external_id BETWEEN -680017 AND -680001) OR (external_id BETWEEN -660017 AND -660001)) AND pc.id IS NULL;"`

Expected: 0 rows. If rows are returned, capture each (politician_id, full_name, topic_key) tuple into the SUMMARY as a CSTA-05 failure, plus determine which city owns each row (look up external_id of full_name) so the executor knows which wave's migration to patch.

Step 3 — Empty-sources audit. Run the second query from PATTERNS.md:
  `psql "$DATABASE_URL" -c "SELECT pa.politician_id, p.full_name, t.topic_key FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id JOIN inform.compass_topics t ON t.id = pc.topic_id LEFT JOIN inform.politician_answers pa ON pa.politician_id = pc.politician_id AND pa.topic_id = pc.topic_id WHERE p.external_id IN (SELECT external_id FROM essentials.politicians WHERE (external_id BETWEEN -640019 AND -640001) OR (external_id BETWEEN -650018 AND -650001) OR (external_id BETWEEN -680017 AND -680001) OR (external_id BETWEEN -660017 AND -660001)) AND (pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL OR pc.sources = '{}');"`

Expected: 0 rows. Same fail-capture behavior as Step 2.

Step 4 — Single-URL minimum sanity check. Run:
  `psql "$DATABASE_URL" -c "SELECT pc.politician_id, p.full_name, t.topic_key, pc.sources FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id JOIN inform.compass_topics t ON t.id = pc.topic_id WHERE p.external_id IN (SELECT external_id FROM essentials.politicians WHERE (external_id BETWEEN -640019 AND -640001) OR (external_id BETWEEN -650018 AND -650001) OR (external_id BETWEEN -680017 AND -680001) OR (external_id BETWEEN -660017 AND -660001)) AND EXISTS (SELECT 1 FROM unnest(pc.sources) AS s WHERE s = '' OR s IS NULL);"`

Expected: 0 rows. Empty-string elements inside the `sources` array would fail the CSTA-05 "at least one source URL" requirement just as cleanly as a NULL array. Capture any failures.

Step 5 — Write `.planning/phases/78-city-stance-research/78-06-SUMMARY.md`. The SUMMARY must contain:
  (a) Date and audit invocation (which 4 cities are in scope; Fremont explicitly out of scope)
  (b) Pre-audit per-city totals from Step 1
  (c) Orphan-answer audit result (count + any failure tuples)
  (d) Empty-sources audit result (count + any failure tuples)
  (e) Empty-string-URL audit result (count + any failure tuples)
  (f) Overall pass/fail verdict for CSTA-05
  (g) If failures: a "Next steps" section listing which wave's migration must be patched (e.g., "Sacramento Mai Vang / housing → return to Wave 4 (plan 78-05) and add a context row with sources for this stance")
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND ((p.external_id BETWEEN -640019 AND -640001) OR (p.external_id BETWEEN -650018 AND -650001) OR (p.external_id BETWEEN -680017 AND -680001) OR (p.external_id BETWEEN -660017 AND -660001));" returns "0"</automated>
  </verify>
  <acceptance_criteria>
    - All four wave SUMMARY files (78-02-SUMMARY.md through 78-05-SUMMARY.md) exist before this task begins
    - Orphan-answer query returns 0 rows: `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id = pa.politician_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND ((p.external_id BETWEEN -640019 AND -640001) OR (p.external_id BETWEEN -650018 AND -650001) OR (p.external_id BETWEEN -680017 AND -680001) OR (p.external_id BETWEEN -660017 AND -660001));"` outputs `0`
    - Empty-sources query returns 0 rows: `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id WHERE (pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL OR pc.sources = '{}') AND ((p.external_id BETWEEN -640019 AND -640001) OR (p.external_id BETWEEN -650018 AND -650001) OR (p.external_id BETWEEN -680017 AND -680001) OR (p.external_id BETWEEN -660017 AND -660001));"` outputs `0`
    - Empty-string-URL query returns 0 rows: `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_context pc JOIN essentials.politicians p ON p.id = pc.politician_id WHERE EXISTS (SELECT 1 FROM unnest(pc.sources) AS s WHERE s = '' OR s IS NULL) AND ((p.external_id BETWEEN -640019 AND -640001) OR (p.external_id BETWEEN -650018 AND -650001) OR (p.external_id BETWEEN -680017 AND -680001) OR (p.external_id BETWEEN -660017 AND -660001));"` outputs `0`
    - File `.planning/phases/78-city-stance-research/78-06-SUMMARY.md` exists and includes per-city answer counts, the three audit query results, and an explicit "CSTA-05: PASS" or "CSTA-05: FAIL — see Next steps" verdict
    - If any audit returned non-zero rows, the SUMMARY's "Next steps" section names the affected (city, politician, topic) tuples and identifies which prior wave plan (78-02 through 78-05) owns the fix
    - Fremont (CSTA-04) is NOT in scope — verified by absence of Fremont external_ids (Fremont politicians sit outside the 4 ranges queried)
  </acceptance_criteria>
  <done>
    CSTA-05 audit complete. The SUMMARY records the verdict. If PASS, Phase 78 is ready for milestone close. If FAIL, the failure list points the executor at the specific prior wave plan(s) to revisit.
  </done>
</task>

</tasks>

<verification>
1. All four prior wave SUMMARYs exist.
2. Three audit queries (orphan answers, empty sources, empty-string URLs) all return 0 rows across the 4 new cities.
3. 78-06-SUMMARY.md records per-city totals, the three audit results, and a clear PASS verdict.
4. Fremont is not touched.
</verification>

<success_criteria>
1. CSTA-05 closed: every stance row across SJ, SD, Berkeley, Sacramento has a paired context row with at least one non-empty source URL.
2. The audit SUMMARY exists and is committable as the closing artifact for Phase 78.
3. Any anomalies are surfaced with enough specificity (city + politician + topic) to route the fix back to the correct prior wave plan.
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-06-SUMMARY.md`. On PASS, this is the terminal artifact of Phase 78 — the executor can update STATE.md/ROADMAP.md to mark Phase 78 complete and CSTA-01 through CSTA-05 closed (plus the informal Sacramento extension).
</output>
