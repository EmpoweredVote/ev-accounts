---
phase: 78-city-stance-research
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: []
tags: [stance-research, sacramento, headshots, verification, audit-only]

must_haves:
  truths:
    - "essentials.politician_images contains 9 headshot rows for Sacramento officials (external_ids -660001, -660010..-660017)"
    - "If headshots are missing, a numbered migration file is added to backend/migrations/ and applied via psql"
    - "All 9 Sacramento politician rows have a non-empty url in essentials.politician_images with type = 'default'"
  artifacts:
    - path: "backend/migrations/sac_headshots.sql"
      provides: "Verbatim source data for Sacramento headshot INSERTs (already applied via Supabase MCP on 2026-05-28 per file header)"
  key_links:
    - from: "essentials.politician_images.politician_id"
      to: "essentials.politicians.id"
      via: "FK lookup by external_id BETWEEN -660017 AND -660001"
      pattern: "WHERE external_id BETWEEN -660017 AND -660001"
---

<objective>
Verify Sacramento headshots are live in `essentials.politician_images` for all 9 Sacramento officials (Mayor Kevin McCarty plus 8 council members). The source file `backend/migrations/sac_headshots.sql` already states it was applied via Supabase MCP on 2026-05-28 ("AUDIT ONLY — already applied"); this plan is a verification wave that confirms reality matches that claim. If the claim is false (zero or partial rows found), promote the file to a numbered migration and apply it via psql.

Purpose: Phase 78 cannot meaningfully ship Sacramento stance research in Wave 4 unless Sacramento officials have working headshot URLs in the photo bucket — otherwise the compass compare view shows broken images for Sacramento officials even when their stance data is correct. Wave 0 closes this gap before downstream waves begin.

Output: One of two terminal states:
  (a) Verification confirms 9 rows already exist → no new migration file; Wave 0 closed with no DB writes.
  (b) Verification finds < 9 rows → planner-assigned migration file `backend/migrations/221_sac_headshots.sql` created (copy of `sac_headshots.sql` body with AUDIT-ONLY header stripped) and applied via psql.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@backend/migrations/sac_headshots.sql
@backend/migrations/220_sacramento_officials.sql
@backend/migrations/215_berkeley_headshots.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Sacramento headshots are live; if missing, apply as numbered migration</name>
  <files>backend/migrations/221_sac_headshots.sql (conditional — only create if verification shows < 9 rows)</files>
  <read_first>
    - backend/migrations/sac_headshots.sql (the audit-only source; provides 9 INSERT rows with hard-coded politician UUIDs and photo_origin_url values)
    - backend/migrations/220_sacramento_officials.sql (defines the 9 Sacramento politician UUIDs; cross-reference UUIDs in sac_headshots.sql against this file before any DB action)
    - backend/migrations/215_berkeley_headshots.sql (canonical headshots migration pattern from prior city — header format, ON CONFLICT clause, photo_license value)
    - backend/.env (DATABASE_URL — read but do not echo)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Sacramento Headshots Migration (Wave 0)" (the verification SQL and conditional-apply rules)
  </read_first>
  <action>
Run the verification SELECT against the live DB using `psql "$DATABASE_URL"` (DATABASE_URL from `backend/.env`):

  SELECT COUNT(*) FROM essentials.politician_images
  WHERE politician_id IN (
    SELECT id FROM essentials.politicians
    WHERE external_id BETWEEN -660017 AND -660001
  );

Three branches:

(A) Result = 9 → Wave 0 is a no-op. Record outcome in the SUMMARY: "Verified 9 Sacramento headshot rows already live; sac_headshots.sql AUDIT-ONLY header is accurate. No migration written." Do NOT create a new migration file. Stop.

(B) Result = 0 → Promote `sac_headshots.sql` to `backend/migrations/221_sac_headshots.sql`. The new file is a verbatim copy of the body (lines 10-30 of `sac_headshots.sql` — the INSERT statement and the ON CONFLICT DO NOTHING clause) wrapped with: (1) a new header matching the style of `215_berkeley_headshots.sql` (Migration 221 title, Sacramento officials, 9 rows, applied via psql); (2) `BEGIN;` before the INSERT; (3) `COMMIT;` after the ON CONFLICT line. Strip the original "AUDIT ONLY — already applied via Supabase MCP on 2026-05-28 / Do NOT apply via migration ledger" header lines. Apply via:

  psql "$DATABASE_URL" -f backend/migrations/221_sac_headshots.sql

Then re-run the COUNT query and confirm it returns 9.

(C) Result is between 1 and 8 → Stop and report the partial state. Do not write a migration. The 9 UUIDs in sac_headshots.sql may have already been partially inserted under different photo_license values or with manual edits; surface the row-by-row delta (which Sacramento politician_ids are missing from essentials.politician_images) and pause for human decision. Per D-02 the planner-assigned migration number is 221 (next available after 220_sacramento_officials.sql).

In all branches, never edit `sac_headshots.sql` in place. The new file (if created) is `backend/migrations/221_sac_headshots.sql`. Note: there are TWO unrelated files at migration 219 already (219_fremont_officials_stances.sql, 219_sacramento_government_structure.sql, 219_sj_headshots.sql) — this is acceptable per existing convention; do NOT renumber any existing file.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);" returns exactly "9"</automated>
  </verify>
  <acceptance_criteria>
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` outputs `9` (no quotes, no whitespace) on stdout
    - If branch (A) was taken: `backend/migrations/221_sac_headshots.sql` does NOT exist; `git status` shows zero files added in `backend/migrations/`
    - If branch (B) was taken: `backend/migrations/221_sac_headshots.sql` exists, begins with `-- Migration 221:` (not `-- AUDIT ONLY`), contains exactly 9 `(gen_random_uuid(),` tuples in the VALUES list, ends with `COMMIT;`, and `psql "$DATABASE_URL" -f backend/migrations/221_sac_headshots.sql` exits 0
    - If branch (C) was taken: no migration file is created; the SUMMARY records each missing politician_id and pauses for human decision
    - In ALL branches: `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM essentials.politician_images WHERE type = 'default' AND url LIKE '%storage.supabase.co%' AND politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` outputs `9` before proceeding to Wave 1
  </acceptance_criteria>
  <done>
    All 9 Sacramento officials (Mayor Kevin McCarty + 8 council members for D1–D8) have a `default`-type photo row in `essentials.politician_images` with a Supabase storage URL. Wave 1 (San Jose stance research) is unblocked.
  </done>
</task>

</tasks>

<verification>
1. `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM essentials.politician_images WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 9.
2. Cross-check: `psql "$DATABASE_URL" -c "SELECT p.full_name, pi.url FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id = p.id AND pi.type = 'default' WHERE p.external_id BETWEEN -660017 AND -660001 ORDER BY p.external_id DESC;"` shows all 9 Sacramento officials with non-null URLs.
3. The migration ledger now reflects truth: either `sac_headshots.sql` remains AUDIT-ONLY (branch A), or `221_sac_headshots.sql` is the recorded migration that placed those rows.
</verification>

<success_criteria>
1. Sacramento headshot row count in `essentials.politician_images` = 9 for politicians with `external_id BETWEEN -660017 AND -660001`.
2. No Sacramento officials block downstream waves due to missing photos.
3. Migration file state is consistent with DB state: either no new migration is needed (already applied) OR a numbered migration `221_sac_headshots.sql` exists and is recorded.
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-01-SUMMARY.md` when done. Record which branch (A/B/C) was taken, the row count before and after, and (if branch B) the migration number assigned.
</output>
