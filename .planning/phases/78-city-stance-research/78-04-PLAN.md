---
phase: 78-city-stance-research
plan: 04
type: execute
wave: 3
depends_on: [78-03]
files_modified: [backend/migrations/NNN_berkeley_stances.sql]
autonomous: false
requirements: [CSTA-03]
tags: [stance-research, berkeley, migration, csta-03]

must_haves:
  truths:
    - "All 10 Berkeley officials (Mayor Jesse Arreguin + City Auditor Jenny Wong + 8 council members D1–D8) have stance research CSV output"
    - "Every Berkeley official has at least 5 stance rows in inform.politician_answers (D-07 floor)"
    - "Every researched stance has a paired inform.politician_context row with at least one source URL"
    - "data-centers topic is NOT present in any Berkeley politician_answers row (D-06 exclusion)"
    - "No San Diego research is concurrently in flight (D-05)"
  artifacts:
    - path: "backend/data/stance-research/2026-MM-DD-berkeley-officials.csv"
      provides: "Reviewable CSV of all researched stances before migration generation"
    - path: "backend/migrations/NNN_berkeley_stances.sql"
      provides: "Idempotent stance migration for 10 Berkeley officials"
  key_links:
    - from: "backend/migrations/NNN_berkeley_stances.sql"
      to: "essentials.politicians (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010)"
      via: "hard-coded politician UUIDs resolved from live DB before migration is written"
      pattern: "INSERT INTO inform.politician_answers .* VALUES \\('[a-f0-9-]+',"
---

<objective>
Research and ingest CompassV2 stance data for all 10 Berkeley city officials (Mayor Jesse Arreguin, City Auditor Jenny Wong, plus 8 council members D1–D8) across all 42 applicable compass topics (43 live topics minus `data-centers`). Output one migration file `backend/migrations/NNN_berkeley_stances.sql` and apply it via psql.

This is Wave 3. Per D-05, Wave 2 (San Diego) must be applied and verified before this plan begins.

Purpose: Close CSTA-03. Berkeley has a notably progressive city council with deep public records on housing, rent-regulation, climate, and judicial-reform topics. City Auditor Jenny Wong's role is administrative/oversight; she will have a thinner direct-policy record and is the most likely candidate to hit the 5-stance floor (D-07) with adjacent-evidence reasoning.

Output:
1. `backend/data/stance-research/YYYY-MM-DD-berkeley-officials.csv` — researcher CSV
2. `backend/migrations/NNN_berkeley_stances.sql` — idempotent migration applied via psql
3. SUMMARY with per-politician counts and assigned migration number
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@.planning/phases/78-city-stance-research/78-03-SUMMARY.md
@backend/migrations/219_fremont_officials_stances.sql
@backend/migrations/214_berkeley_officials.sql
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch research-stances skill for Berkeley officials and produce reviewable CSV</name>
  <files>backend/data/stance-research/YYYY-MM-DD-berkeley-officials.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (full skill flow + D-05 one-at-a-time rule)
    - backend/migrations/214_berkeley_officials.sql (10 Berkeley politician records — confirms full_name spelling)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Politician Name Roster" (Berkeley row)
    - .planning/phases/78-city-stance-research/78-CONTEXT.md §Research Execution (D-05, D-06, D-07)
    - backend/.claude/agent-memory/politician-stance-researcher/MEMORY.md (researcher notes — any Berkeley quirks)
    - .planning/phases/78-city-stance-research/78-03-SUMMARY.md (confirm Wave 2 / SD is closed before starting; if missing/incomplete, ABORT)
  </read_first>
  <action>
PRECONDITION: Read `.planning/phases/78-city-stance-research/78-03-SUMMARY.md`. If missing or its closure assertions are unmet, ABORT — Wave 2 must complete first per D-05.

Verify against live DB:
  `psql "$DATABASE_URL" -c "SELECT external_id, full_name FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010) ORDER BY external_id DESC;"`

Expected 10 names from PATTERNS.md: Jesse Arreguin (Mayor), Jenny Wong (City Auditor), Rashi Kesarwani (D1), Terry Taplin (D2), Ben Bartlett (D3), Kate Harrison (D4), Cecilia Lunaparra (D5), Susan Wengraf (D6), Rigel Robinson (D7), Sophie Hahn (D8). Use DB names as source of truth.

Invoke `/research-stances` with the 10 names. Normal mode, no --topics filter — all 42 applicable topics minus `data-centers`.

Dispatch: ONE agent at a time (D-05). CSV output `backend/data/stance-research/$(date +%F)-berkeley-officials.csv`.

Per-agent prompt:
- 42-topic UUID list, data-centers excluded
- WebFetch ONLY
- D-07 floor: at least 5 stances per politician (City Auditor Jenny Wong is the most likely floor candidate — instruct researcher to use adjacent-evidence reasoning, e.g., public statements, endorsements, budget audit positions, rather than skipping)
- Party stays NULL
- Mayor Jesse Arreguin: long public record going back to early 2010s — expect 30+ topics with direct evidence
- Council members: prioritize housing, homelessness, rent-regulation, climate-change, local-environment, transportation-priorities, public-safety-approach

Present STEP 3 approval summary. Wait for explicit approval.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/*-berkeley-officials.csv &amp;&amp; awk -F, 'NR>1 {names[$1]=1} END {n=0; for (k in names) n++; exit (n==10 ? 0 : 1)}' backend/data/stance-research/*-berkeley-officials.csv</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/YYYY-MM-DD-berkeley-officials.csv` exists with header row and at least 50 data rows (10 × 5 floor)
    - CSV `full_name` column contains all 10 distinct Berkeley officials — `awk -F, 'NR>1 {print $1}' ... | sort -u | wc -l` returns 10
    - CSV `topic_key` column never contains the literal `data-centers` — `grep -c ',data-centers,' ...` returns 0
    - Every row has a non-empty `source_url_1` — `awk -F, 'NR>1 && $6 == "" {n++} END {exit (n>0 ? 1 : 0)}' ...`
    - Every politician has at least 5 rows — `awk -F, 'NR>1 {n[$1]++} END {for (k in n) if (n[k]<5) {print k": "n[k]; bad=1} exit bad}' ...` exits 0
    - User explicit approval recorded in conversation transcript
    - `.planning/phases/78-city-stance-research/78-03-SUMMARY.md` exists and shows Wave 2 closed BEFORE this task began
  </acceptance_criteria>
  <done>
    Approved CSV with at least 50 sourced stances across all 10 Berkeley officials; data-centers excluded; every row has a source URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate, apply, and verify migration NNN_berkeley_stances.sql from the approved CSV</name>
  <files>backend/migrations/NNN_berkeley_stances.sql</files>
  <read_first>
    - backend/migrations/219_fremont_officials_stances.sql (canonical pattern; closest analog by size to Berkeley's 10-official roster)
    - backend/migrations/214_berkeley_officials.sql (politician records; external_id ranges)
    - backend/data/stance-research/YYYY-MM-DD-berkeley-officials.csv (approved Task 1 output)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Core INSERT/ON CONFLICT Pattern"
    - backend/migrations/NNN_sd_stances.sql (Wave 2 migration; confirms migration number sequence — Berkeley should use the next number after SD)
    - backend/.env (DATABASE_URL — read but do not echo)
  </read_first>
  <action>
Step 1 — Determine migration number. Run:
  `ls backend/migrations/ | grep -E '^[0-9]{3}_' | sort -r | head -5`
Take max prefix + 1. Record the chosen NNN.

Step 2 — Resolve politician UUIDs from live DB:
  `psql "$DATABASE_URL" -c "SELECT id, full_name, external_id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010) ORDER BY external_id DESC;"`
10 rows expected. Capture (full_name, UUID) pairs.

Step 3 — Write `backend/migrations/NNN_berkeley_stances.sql` following the EXACT structure of `219_fremont_officials_stances.sql`:
  (a) Header: `-- Migration NNN: Berkeley Officials Stances — 10 Politicians`. Include `-- Topic scope: 42 topics (all 43 live topics except data-centers)`.
  (b) Full 42-topic UUID reference block from PATTERNS.md, data-centers excluded.
  (c) Politician UUID reference block — 10 lines (Mayor → City Auditor → D1–D8 council members).
  (d) `BEGIN;`
  (e) Per-official sections in order: Jesse Arreguin (Mayor), Jenny Wong (City Auditor), then D1–D8. Section headers: `-- Jesse Arreguin (Mayor)`, `-- Jenny Wong (City Auditor)`, `-- [Name] (District N)`. Paired INSERTs per stance (politician_answers + politician_context). `$$dollar-quoted$$` reasoning. `ARRAY[...]::text[]` sources. Dual ON CONFLICT DO UPDATE.
  (f) `COMMIT;`

Step 4 — Apply via psql:
  `psql "$DATABASE_URL" -f backend/migrations/NNN_berkeley_stances.sql`
Must exit 0.

Step 5 — Spot-check row counts:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"`
Must equal the CSV row count.

Step 6 — Confirm data-centers exclusion:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"` returns 0.

Step 7 — Confirm context pairing:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"` returns 0.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));" returns "10"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/migrations/NNN_berkeley_stances.sql` exists where NNN = one greater than the max prefix in `backend/migrations/` at apply time
    - File begins with `-- Migration NNN: Berkeley Officials Stances — 10 Politicians` and contains `-- Topic scope: 42 topics (all 43 live topics except data-centers)`
    - File contains exactly 10 lines in the "Politician UUID reference" comment block
    - File contains zero references to the topic UUID `4559b513-0fd8-4ed1-babd-f3b554162f40` — `grep -c "4559b513" backend/migrations/NNN_berkeley_stances.sql` returns 0
    - `psql "$DATABASE_URL" -f backend/migrations/NNN_berkeley_stances.sql` exits 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"` returns 10
    - `psql "$DATABASE_URL" -t -A -c "SELECT politician_id FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010)) GROUP BY politician_id HAVING COUNT(*) < 5;"` returns 0 rows
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"` returns 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE (external_id IN (-680001, -680002) OR external_id BETWEEN -680017 AND -680010));"` returns 0
  </acceptance_criteria>
  <done>
    Berkeley stance migration applied; all 10 officials covered with at least 5 sourced stances each; data-centers excluded; context pairs complete. CSTA-03 closed pending the CSTA-05 audit in Wave 5.
  </done>
</task>

</tasks>

<verification>
1. CSV approved with all 10 Berkeley officials, at least 5 stances each, sourced, no data-centers.
2. Migration file follows canonical pattern with 42-topic UUID block.
3. Applied via psql; transaction committed.
4. Spot-checks confirm count, exclusion, and context pairing.
</verification>

<success_criteria>
1. CSTA-03 closed: all 10 Berkeley officials have stance research in DB with sourced context rows.
2. Wave 4 (Sacramento) is unblocked.
3. No parallel research dispatch occurred (D-05).
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-04-SUMMARY.md` when done. Record: assigned migration number, per-politician stance counts, total stance rows, and any thin-record officials hitting the 5-stance floor (Jenny Wong specifically called out).
</output>
