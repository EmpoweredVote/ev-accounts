---
phase: 78-city-stance-research
plan: 02
type: execute
wave: 1
depends_on: [78-01]
files_modified: [backend/migrations/NNN_sj_stances.sql]
autonomous: false
requirements: [CSTA-01]
tags: [stance-research, san-jose, migration, csta-01]

must_haves:
  truths:
    - "All 11 San Jose officials (Mayor Matt Mahan + 10 council members D1–D10) have stance research CSV output"
    - "Every San Jose official has at least 5 stance rows in inform.politician_answers (D-07 floor)"
    - "Every researched stance has a paired inform.politician_context row with at least one source URL"
    - "data-centers topic is NOT present in any San Jose politician_answers row (D-06 exclusion)"
    - "No party value is assigned to any San Jose politician (antipartisan design)"
  artifacts:
    - path: "backend/data/stance-research/2026-MM-DD-san-jose-officials.csv"
      provides: "Reviewable CSV of all researched stances before migration generation"
    - path: "backend/migrations/NNN_sj_stances.sql"
      provides: "Idempotent stance migration for 11 SJ officials; pattern mirrors 219_fremont_officials_stances.sql"
  key_links:
    - from: "backend/migrations/NNN_sj_stances.sql"
      to: "essentials.politicians (external_id BETWEEN -640019 AND -640001)"
      via: "hard-coded politician UUIDs in INSERT statements; UUIDs resolved from live DB before migration is written"
      pattern: "INSERT INTO inform.politician_answers .* VALUES \\('[a-f0-9-]+',"
    - from: "backend/migrations/NNN_sj_stances.sql"
      to: "inform.compass_topics"
      via: "hard-coded topic UUIDs from the 42-topic reference block (data-centers excluded)"
      pattern: "-- Topic UUID reference"
---

<objective>
Research and ingest CompassV2 stance data for all 11 San Jose city officials (Mayor Matt Mahan + 10 council members D1–D10) across all 42 applicable compass topics (43 live topics minus `data-centers`). Output one migration file `backend/migrations/NNN_sj_stances.sql` (NNN assigned at apply time by checking the live migration table; expected 221 or 222 depending on Wave 0 outcome) and apply it via psql to remote Supabase.

This is the first of four city-research waves. Per D-05 (one city at a time), no other research-stances dispatch may run concurrently — Wave 2 (San Diego) does not begin until this plan's migration is applied and spot-checked.

Purpose: Close CSTA-01 — every San Jose official appears in the compass compare view with sourced positions. After this wave, users in San Jose see their mayor and council member positions on housing, homelessness, public safety, growth, and the full 42-topic set.

Output:
1. `backend/data/stance-research/YYYY-MM-DD-san-jose-officials.csv` — researcher CSV with politician_id, topic_key, value, reasoning, source_url_1..3
2. `backend/migrations/NNN_sj_stances.sql` — idempotent migration applied via psql
3. Updated SUMMARY recording row counts per politician and the assigned migration number
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@backend/migrations/216_sf_officials_stances.sql
@backend/migrations/219_fremont_officials_stances.sql
@backend/migrations/218_sj_officials.sql
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch research-stances skill for San Jose officials and produce reviewable CSV</name>
  <files>backend/data/stance-research/YYYY-MM-DD-san-jose-officials.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 0 → STEP 4 — exact dispatch sequence, one-at-a-time rule, CSV output path)
    - backend/migrations/218_sj_officials.sql (the 11 SJ politician records; provides full_name spelling for each — Matt Mahan, Rosemary Kamei, Pamela Campos, Anthony Tordillos, David Cohen, Peter Ortiz, Michael Mulcahy, Bien Doan, Domingo Candelas, Pam Foley, George Casey)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Politician Name Roster" (San Jose row) and §"One-at-a-Time Research Dispatch (D-05)"
    - .planning/phases/78-city-stance-research/78-CONTEXT.md §Research Execution (D-05, D-06, D-07)
    - backend/.claude/agent-memory/politician-stance-researcher/MEMORY.md (researcher agent persistent notes — read for any known San Jose quirks)
  </read_first>
  <action>
Invoke the `/research-stances` skill (or equivalent direct dispatch) with the politician name list:

  "Matt Mahan, Rosemary Kamei, Pamela Campos, Anthony Tordillos, David Cohen, Peter Ortiz, Michael Mulcahy, Bien Doan, Domingo Candelas, Pam Foley, George Casey"

Skill mode: **normal** (not --rewrite-id). No --topics filter — research all 42 applicable topics (the skill fetches live topics from `inform.compass_topics WHERE is_live = true` per its STEP 0; manually exclude `data-centers` from the topic list passed to each agent per D-06).

Dispatch rules (D-05, MEMORY.md feedback): ONE agent at a time. Maximum 1 in flight. Wait for each politician's CSV write to complete before launching the next. Do NOT batch 11 agents in parallel — this triggers WebSearch rate limit failures and produces empty CSVs.

CSV output file path: `backend/data/stance-research/$(date +%F)-san-jose-officials.csv` (single file, all 11 politicians appended). After each agent completes, verify the file gained rows for that politician before moving on.

Per-agent prompt MUST include:
- Politician name + office (Mayor / Council District N)
- The complete 42-topic JSON list (topic_key + UUID) fetched from `inform.compass_topics WHERE is_live = true` minus the `data-centers` row
- Tool rule: WebFetch ONLY — never WebSearch, never Playwright (rate-limit pool shared, burns out quickly per SKILL.md line 108-111)
- D-07 floor: every politician must yield at least 5 researched stances; for thin-record council members include adjacent-evidence reasoning rather than skipping
- Antipartisan rule: do not infer or assign party affiliations; party stays NULL on every row

After all 11 agents complete, generate the STEP 3 approval summary table (politicians × topic counts × stance previews). Present to the user. Do NOT proceed to migration generation until the user approves the CSV (per skill STEP 3, branch 1 "Approve all" or any explicit approval).
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/*-san-jose-officials.csv &amp;&amp; awk -F, 'NR>1 {names[$1]=1} END {n=0; for (k in names) n++; exit (n==11 ? 0 : 1)}' backend/data/stance-research/*-san-jose-officials.csv</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/YYYY-MM-DD-san-jose-officials.csv` exists with one header row and at least 55 data rows (11 politicians × 5 stance floor = 55 minimum)
    - The CSV `full_name` column contains all 11 distinct San Jose officials by name (Matt Mahan, Rosemary Kamei, Pamela Campos, Anthony Tordillos, David Cohen, Peter Ortiz, Michael Mulcahy, Bien Doan, Domingo Candelas, Pam Foley, George Casey) — verified by `awk -F, 'NR>1 {print $1}' ... | sort -u | wc -l` returning 11
    - The CSV `topic_key` column never contains the literal string `data-centers` — verified by `grep -c ',data-centers,' ... ` returning 0
    - Every row has a non-empty `source_url_1` value — verified by `awk -F, 'NR>1 && $6 == "" {n++} END {exit (n>0 ? 1 : 0)}' ...`
    - Every politician has at least 5 rows in the CSV — `awk -F, 'NR>1 {n[$1]++} END {for (k in n) if (n[k]<5) {print k": "n[k]; bad=1} exit bad}' ...` exits 0
    - User has explicitly approved the CSV via the skill's STEP 3 prompt (recorded in the conversation transcript before Task 2 begins)
  </acceptance_criteria>
  <done>
    The user has reviewed and approved a CSV file containing at least 55 sourced stances across all 11 San Jose officials, with `data-centers` excluded and every row carrying at least one source URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate, apply, and verify migration NNN_sj_stances.sql from the approved CSV</name>
  <files>backend/migrations/NNN_sj_stances.sql</files>
  <read_first>
    - backend/migrations/219_fremont_officials_stances.sql (canonical city stance migration pattern — header, topic UUID reference block, politician UUID reference block, BEGIN/COMMIT wrapper, dual-INSERT-with-ON-CONFLICT per stance)
    - backend/migrations/216_sf_officials_stances.sql (larger-city analog — confirms the full 42-topic UUID reference block format; data-centers shown excluded in header comment)
    - backend/migrations/218_sj_officials.sql (source of San Jose politician records — read to confirm external_id ranges -640001 and -640010 to -640019 match the live DB before resolving UUIDs)
    - backend/data/stance-research/YYYY-MM-DD-san-jose-officials.csv (the approved Task 1 output — every row becomes a pair of INSERT statements)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Core INSERT/ON CONFLICT Pattern" and §"Migration Header Pattern"
    - backend/.env (DATABASE_URL for psql apply — read but do not echo)
  </read_first>
  <action>
Step 1 — Determine the migration number. Run:
  `psql "$DATABASE_URL" -t -A -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;"`
If the table exists, use MAX(version) + 1. If the table doesn't exist or the project doesn't use it (likely — Supabase migrations on this project are applied directly via psql without a ledger), fall back to: count files in `backend/migrations/` matching `^[0-9]{3}_` and use one greater than the max numeric prefix. Last confirmed file-system max is 220 (`220_sacramento_officials.sql`); next is 221 if Wave 0 branch A was taken, 222 if Wave 0 branch B created `221_sac_headshots.sql`. Assign accordingly. Record the chosen NNN in the SUMMARY.

Step 2 — Resolve politician UUIDs from the live DB. Run:
  `psql "$DATABASE_URL" -c "SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001 ORDER BY external_id DESC;"`
This produces 11 rows. Capture each (full_name, UUID) pair for the politician reference block.

Step 3 — Write the migration file at `backend/migrations/NNN_sj_stances.sql` following the EXACT structure of `219_fremont_officials_stances.sql`:
  (a) Header block — `-- Migration NNN: San Jose Officials Stances — 11 Politicians` plus purpose/politicians/stance-count/idempotency/apply-via-psql lines. Header MUST include the literal comment `-- Topic scope: 42 topics (all 43 live topics except data-centers)` per the data-centers-exclusion convention.
  (b) Topic UUID reference comment block — copy the full 42-topic block from 78-PATTERNS.md §"Topic UUID Reference Block". DO NOT include the `data-centers` line.
  (c) Politician UUID reference comment block — 11 lines, one per official, with the UUID resolved in Step 2.
  (d) `BEGIN;`
  (e) Per-official section — for each of the 11 politicians, emit a section header (`-- ===... -- Full Name (Role) -- ===...`) followed by pairs of INSERT statements (one for `inform.politician_answers`, one for `inform.politician_context`) for every CSV row belonging to that politician. Use `$$dollar-quoted$$` reasoning strings. `sources` is `ARRAY['url1', 'url2']::text[]`. Both INSERTs have `ON CONFLICT (politician_id, topic_id) DO UPDATE`.
  (f) `COMMIT;`

Step 4 — Apply via psql:
  `psql "$DATABASE_URL" -f backend/migrations/NNN_sj_stances.sql`
This must exit 0. Any error aborts the transaction (BEGIN/COMMIT) and leaves the DB untouched.

Step 5 — Spot-check row counts against the CSV:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"`
This must equal the CSV row count (Task 1 produced ≥ 55).

Step 6 — Confirm data-centers exclusion in DB:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"`
Must return 0.

Step 7 — Confirm context pairing:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"`
Must return 0 (every answer has a paired context row — preview of CSTA-05 audit run in Wave 5).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);" returns "11"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/migrations/NNN_sj_stances.sql` exists where NNN is one greater than the highest-numbered migration file in `backend/migrations/` at apply time (expected 221 or 222)
    - The file begins with `-- Migration NNN: San Jose Officials Stances — 11 Politicians` and contains the literal comment `-- Topic scope: 42 topics (all 43 live topics except data-centers)`
    - File contains exactly 11 lines in the "Politician UUID reference" comment block (one per San Jose official)
    - File contains zero references to the topic UUID `4559b513-0fd8-4ed1-babd-f3b554162f40` (data-centers) — verified by `grep -c "4559b513" backend/migrations/NNN_sj_stances.sql` returning 0
    - `psql "$DATABASE_URL" -f backend/migrations/NNN_sj_stances.sql` exits with status 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"` returns 11
    - `psql "$DATABASE_URL" -t -A -c "SELECT politician_id FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001) GROUP BY politician_id HAVING COUNT(*) < 5;"` returns 0 rows (D-07 minimum 5 stances floor satisfied)
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"` returns 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -640019 AND -640001);"` returns 0
  </acceptance_criteria>
  <done>
    San Jose stance migration applied. All 11 officials have ≥ 5 stances; no data-centers rows; every answer has a paired context row with at least one source URL. CSTA-01 closed pending the CSTA-05 audit in Wave 5.
  </done>
</task>

</tasks>

<verification>
1. CSV exists and contains all 11 SJ officials with ≥ 5 stances each, sourced, no data-centers.
2. Migration file follows the canonical pattern from 219_fremont_officials_stances.sql with the SF-style 42-topic UUID block.
3. Migration applied via psql; transaction committed.
4. Spot-check queries confirm count, exclusion, and context pairing.
</verification>

<success_criteria>
1. CSTA-01 closed: all 11 San Jose officials have stance research in DB with sourced context rows.
2. Migration file is idempotent — re-applying it leaves the DB in the same state (ON CONFLICT DO UPDATE).
3. Phase 78 Wave 2 (San Diego) is unblocked — no parallel research dispatch occurred per D-05.
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-02-SUMMARY.md` when done. Record: assigned migration number, CSV row count per politician, total stance rows ingested, and any politicians who hit the 5-stance floor with adjacent-evidence reasoning rather than direct evidence.
</output>
