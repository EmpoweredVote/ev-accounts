---
phase: 78-city-stance-research
plan: 05
type: execute
wave: 4
depends_on: [78-04]
files_modified: [backend/migrations/NNN_sacramento_stances.sql]
autonomous: false
requirements: [CSTA-04]
tags: [stance-research, sacramento, migration, csta-04-extension, fifth-city]

must_haves:
  truths:
    - "All 9 Sacramento officials (Mayor Kevin McCarty + 8 council members D1–D8) have stance research CSV output"
    - "Every Sacramento official has at least 5 stance rows in inform.politician_answers (D-07 floor)"
    - "Every researched stance has a paired inform.politician_context row with at least one source URL"
    - "data-centers topic is NOT present in any Sacramento politician_answers row (D-06 exclusion)"
    - "Fremont stances (closed CSTA-04 via migration 219) remain untouched — no re-ingestion or re-verification of Fremont occurs in this wave (D-04)"
    - "No Berkeley research is concurrently in flight (D-05)"
  artifacts:
    - path: "backend/data/stance-research/2026-MM-DD-sacramento-officials.csv"
      provides: "Reviewable CSV of all researched stances before migration generation"
    - path: "backend/migrations/NNN_sacramento_stances.sql"
      provides: "Idempotent stance migration for 9 Sacramento officials"
  key_links:
    - from: "backend/migrations/NNN_sacramento_stances.sql"
      to: "essentials.politicians (external_id BETWEEN -660017 AND -660001)"
      via: "hard-coded politician UUIDs resolved from live DB before migration is written"
      pattern: "INSERT INTO inform.politician_answers .* VALUES \\('[a-f0-9-]+',"
---

<objective>
Research and ingest CompassV2 stance data for all 9 Sacramento city officials (Mayor Kevin McCarty + 8 council members D1–D8) across all 42 applicable compass topics (43 live topics minus `data-centers`). Output one migration file `backend/migrations/NNN_sacramento_stances.sql` and apply it via psql.

This is Wave 4, the 5th and final city stance batch (after SJ, SD, Berkeley). Sacramento is included as an informal extension of Phase 78 per D-01 — it is not formally covered by CSTA-01–05 (which names SJ/SD/Berkeley/Fremont), but Sacramento officials were seeded in migration 220 on 2026-05-23 alongside the other v2.5 cities and the user wants Sacramento covered before declaring Phase 78 done. The `requirements:` frontmatter cites CSTA-04 to keep the formal map intact; the SUMMARY will record this as a "CSTA-04 extension" rather than creating a new formal requirement (D-04 deferred — Sacramento formal CSTA number deferred to roadmap update after Phase 78).

Per D-04, Fremont (the originally-named CSTA-04 city) is already closed via migration 219 (56 stances). Do NOT re-ingest Fremont. Do NOT include Fremont external_ids in any query. This plan touches only Sacramento.

Output:
1. `backend/data/stance-research/YYYY-MM-DD-sacramento-officials.csv` — researcher CSV
2. `backend/migrations/NNN_sacramento_stances.sql` — idempotent migration applied via psql
3. SUMMARY with per-politician counts, assigned migration number, and explicit note that Sacramento is the informal 5th city extending CSTA-04
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/78-city-stance-research/78-CONTEXT.md
@.planning/phases/78-city-stance-research/78-PATTERNS.md
@.planning/phases/78-city-stance-research/78-04-SUMMARY.md
@.planning/phases/78-city-stance-research/78-01-SUMMARY.md
@backend/migrations/219_fremont_officials_stances.sql
@backend/migrations/220_sacramento_officials.sql
@.claude/skills/research-stances/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch research-stances skill for Sacramento officials and produce reviewable CSV</name>
  <files>backend/data/stance-research/YYYY-MM-DD-sacramento-officials.csv</files>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (full skill flow + D-05 one-at-a-time rule)
    - backend/migrations/220_sacramento_officials.sql (9 Sacramento politician records — confirms full_name spelling and external_id assignments)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Politician Name Roster" (Sacramento row)
    - .planning/phases/78-city-stance-research/78-CONTEXT.md §Sacramento Scope (D-01, D-02) and §Research Execution (D-05, D-06, D-07)
    - backend/.claude/agent-memory/politician-stance-researcher/MEMORY.md (researcher notes)
    - .planning/phases/78-city-stance-research/78-04-SUMMARY.md (confirm Wave 3 / Berkeley is closed before starting; if missing/incomplete, ABORT)
    - .planning/phases/78-city-stance-research/78-01-SUMMARY.md (confirm Sacramento headshots verified — sanity check that user-facing photos will work alongside the stance data being ingested)
  </read_first>
  <action>
PRECONDITION: Read `.planning/phases/78-city-stance-research/78-04-SUMMARY.md`. If missing or its closure assertions are unmet, ABORT — Wave 3 must complete first per D-05.

Verify against live DB:
  `psql "$DATABASE_URL" -c "SELECT external_id, full_name FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001 ORDER BY external_id DESC;"`

Expected 9 names from PATTERNS.md: Kevin McCarty (Mayor), Lisa Kaplan (D1), Roger Dickinson (D2), Karina Talamantes (D3), Phil Pluckebaum (D4), Caity Maple (D5), Eric Guerra (D6), Rick Jennings II (D7), Mai Vang (D8). Use DB names as source of truth.

Special context for the researcher:
- Kevin McCarty: former CA State Assembly member (multi-term, deep state-level record from 2014–2024). His state-legislative record is a rich source on housing, education, public-safety reform, and judicial topics. Sacramento mayoral election was November 2024; he's relatively new in the mayoral role but has years of public votes/statements to draw on. Expect 30+ topics with direct evidence.
- Roger Dickinson: former CA State Assembly member as well — multi-term state-level record similar to McCarty.
- Council members vary widely in record length; some are first-term and will likely hit the 5-stance floor with adjacent-evidence reasoning.

Invoke `/research-stances` with the 9 names. Normal mode, no --topics filter — all 42 applicable topics minus `data-centers`.

Dispatch: ONE agent at a time (D-05). CSV output `backend/data/stance-research/$(date +%F)-sacramento-officials.csv`.

Per-agent prompt:
- 42-topic UUID list, data-centers excluded
- WebFetch ONLY
- D-07 floor: at least 5 stances per politician
- Party stays NULL — antipartisan
- For McCarty and Dickinson: instruct researcher to leverage CA State Assembly vote records (leginfo.legislature.ca.gov, calmatters.org, capradio.org) in addition to Sacramento mayoral/council sources

Present STEP 3 approval summary. Wait for explicit approval before Task 2.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/*-sacramento-officials.csv &amp;&amp; awk -F, 'NR>1 {names[$1]=1} END {n=0; for (k in names) n++; exit (n==9 ? 0 : 1)}' backend/data/stance-research/*-sacramento-officials.csv</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/data/stance-research/YYYY-MM-DD-sacramento-officials.csv` exists with header row and at least 45 data rows (9 × 5 floor)
    - CSV `full_name` column contains all 9 distinct Sacramento officials — `awk -F, 'NR>1 {print $1}' ... | sort -u | wc -l` returns 9
    - CSV `topic_key` column never contains the literal `data-centers` — `grep -c ',data-centers,' ...` returns 0
    - Every row has a non-empty `source_url_1` — `awk -F, 'NR>1 && $6 == "" {n++} END {exit (n>0 ? 1 : 0)}' ...`
    - Every politician has at least 5 rows — `awk -F, 'NR>1 {n[$1]++} END {for (k in n) if (n[k]<5) {print k": "n[k]; bad=1} exit bad}' ...` exits 0
    - User explicit approval recorded in conversation transcript
    - `.planning/phases/78-city-stance-research/78-04-SUMMARY.md` exists and shows Wave 3 closed BEFORE this task began
    - NO Fremont officials appear in the CSV (Fremont external_ids are -670xxx range in PATTERNS.md context, but verifiable here by absence — the CSV must only contain the 9 Sacramento names listed above)
  </acceptance_criteria>
  <done>
    Approved CSV with at least 45 sourced stances across all 9 Sacramento officials; data-centers excluded; every row has a source URL; Fremont untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate, apply, and verify migration NNN_sacramento_stances.sql from the approved CSV</name>
  <files>backend/migrations/NNN_sacramento_stances.sql</files>
  <read_first>
    - backend/migrations/219_fremont_officials_stances.sql (canonical pattern; Fremont's 7-official roster is the closest analog by size to Sacramento's 9)
    - backend/migrations/220_sacramento_officials.sql (politician records; external_id range -660017 to -660001)
    - backend/data/stance-research/YYYY-MM-DD-sacramento-officials.csv (approved Task 1 output)
    - .planning/phases/78-city-stance-research/78-PATTERNS.md §"Core INSERT/ON CONFLICT Pattern"
    - backend/migrations/NNN_berkeley_stances.sql (Wave 3 migration; confirms migration number sequence — Sacramento should use the next number after Berkeley)
    - backend/.env (DATABASE_URL — read but do not echo)
  </read_first>
  <action>
Step 1 — Determine migration number:
  `ls backend/migrations/ | grep -E '^[0-9]{3}_' | sort -r | head -5`
Take max prefix + 1. Record the chosen NNN.

Step 2 — Resolve politician UUIDs from live DB:
  `psql "$DATABASE_URL" -c "SELECT id, full_name, external_id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001 ORDER BY external_id DESC;"`
9 rows expected.

Step 3 — Write `backend/migrations/NNN_sacramento_stances.sql` following the EXACT structure of `219_fremont_officials_stances.sql`:
  (a) Header: `-- Migration NNN: Sacramento Officials Stances — 9 Politicians`. Include `-- Topic scope: 42 topics (all 43 live topics except data-centers)`. Include a note line: `-- Phase 78 extension of CSTA-04 (formal CSTA requirement for Sacramento deferred per CONTEXT.md D-04)`.
  (b) Full 42-topic UUID reference block from PATTERNS.md, data-centers excluded.
  (c) Politician UUID reference block — 9 lines (Mayor → D1–D8 council members).
  (d) `BEGIN;`
  (e) Per-official sections in order: Kevin McCarty (Mayor), then D1–D8. Section headers: `-- Kevin McCarty (Mayor)`, `-- [Name] (District N)`. Paired INSERTs per stance. `$$dollar-quoted$$` reasoning. `ARRAY[...]::text[]` sources. Dual ON CONFLICT DO UPDATE.
  (f) `COMMIT;`

Step 4 — Apply: `psql "$DATABASE_URL" -f backend/migrations/NNN_sacramento_stances.sql` must exit 0.

Step 5 — Spot-check row counts:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"`
Must equal CSV row count.

Step 6 — data-centers exclusion:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 0.

Step 7 — Context pairing:
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 0.

Step 8 — Fremont untouched assertion (D-04 safeguard):
  `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE updated_at > (SELECT MAX(updated_at) - INTERVAL '1 hour' FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001)) AND politician_id IN (SELECT id FROM essentials.politicians WHERE full_name IN ('Raj Salwan','Desrie Campbell','Kathy Kimberlin','Raymond Liu','Teresa Keng','Yajing Zhang','Yang Shao'));"` returns 0 — no Fremont rows updated during this migration. If the `updated_at` column doesn't exist on `inform.politician_answers`, skip this check and rely on the row-count delta: total Fremont row count before and after this migration must be identical at 56.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);" returns "9"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/migrations/NNN_sacramento_stances.sql` exists where NNN = one greater than the max prefix in `backend/migrations/` at apply time
    - File begins with `-- Migration NNN: Sacramento Officials Stances — 9 Politicians` and contains both `-- Topic scope: 42 topics (all 43 live topics except data-centers)` and a comment line referencing the CSTA-04 extension status
    - File contains exactly 9 lines in the "Politician UUID reference" comment block
    - File contains zero references to the topic UUID `4559b513-0fd8-4ed1-babd-f3b554162f40` — `grep -c "4559b513" backend/migrations/NNN_sacramento_stances.sql` returns 0
    - File contains zero references to Fremont politician UUIDs (`71124b00-549d-460c-8f84-41a01d99e037` for Raj Salwan and the other 6 Fremont UUIDs listed in `219_fremont_officials_stances.sql` lines 32-38) — D-04 safeguard
    - `psql "$DATABASE_URL" -f backend/migrations/NNN_sacramento_stances.sql` exits 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(DISTINCT politician_id) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 9
    - `psql "$DATABASE_URL" -t -A -c "SELECT politician_id FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001) GROUP BY politician_id HAVING COUNT(*) < 5;"` returns 0 rows
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id WHERE t.topic_key = 'data-centers' AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 0
    - `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pc.id IS NULL AND pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -660017 AND -660001);"` returns 0
    - Fremont row count is unchanged at 56: `psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM inform.politician_answers WHERE politician_id IN (SELECT id FROM essentials.politicians WHERE full_name IN ('Raj Salwan','Desrie Campbell','Kathy Kimberlin','Raymond Liu','Teresa Keng','Yajing Zhang','Yang Shao'));"` returns 56
  </acceptance_criteria>
  <done>
    Sacramento stance migration applied; all 9 officials covered with at least 5 sourced stances each; data-centers excluded; context pairs complete; Fremont untouched. CSTA-04 extension closed pending the CSTA-05 audit in Wave 5.
  </done>
</task>

</tasks>

<verification>
1. CSV approved with all 9 Sacramento officials, at least 5 stances each, sourced, no data-centers.
2. Migration file follows canonical pattern with 42-topic UUID block and CSTA-04-extension header comment.
3. Applied via psql; transaction committed; Fremont row count unchanged at 56.
4. Spot-checks confirm count, exclusion, and context pairing.
</verification>

<success_criteria>
1. CSTA-04 extended: all 9 Sacramento officials have stance research in DB with sourced context rows.
2. Wave 5 (context audit) is unblocked.
3. Fremont stances are untouched (D-04 honored).
4. No parallel research dispatch occurred (D-05).
</success_criteria>

<output>
Create `.planning/phases/78-city-stance-research/78-05-SUMMARY.md` when done. Record: assigned migration number, per-politician stance counts, total stance rows, any thin-record officials hitting the 5-stance floor, and an explicit note that this closes the Sacramento extension of CSTA-04 (formal Sacramento CSTA requirement deferred to roadmap update).
</output>
