---
phase: 66-improve-onboarding-flow-with-guided-hints-and-ux-clarity
plan: 04
type: execute
wave: 3
depends_on: ["66-02", "66-03"]
files_modified:
  - CompassV2/src/components/Layout.jsx
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04, ONBOARD-05, ONBOARD-06]

must_haves:
  truths:
    - "All new onboarding localStorage flags are cleared when compass is reset"
    - "Complete onboarding flow works end-to-end from fresh state"
    - "All tours and hints display correctly on both mobile and desktop viewports"
  artifacts:
    - path: "CompassV2/src/components/Layout.jsx"
      provides: "All onboarding flag cleanup in handleClearCompass"
  key_links:
    - from: "CompassV2/src/components/Layout.jsx"
      to: "localStorage"
      via: "removeItem calls for all onboarding flags"
      pattern: "onboarding_"
---

<objective>
Consolidate onboarding flag cleanup in Layout.jsx and verify the complete onboarding flow.

Purpose: Ensures all new localStorage flags (onboarding_postCalTour, onboarding_compareTour, onboarding_libraryTour, onboarding_writeInHint) are cleared when the user resets their compass, and that the entire onboarding experience works end-to-end.

Output: Updated Layout.jsx, full flow verified
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-improve-onboarding-flow-with-guided-hints-and-ux-clarity/66-CONTEXT.md
@.planning/phases/66-improve-onboarding-flow-with-guided-hints-and-ux-clarity/66-01-SUMMARY.md
@.planning/phases/66-improve-onboarding-flow-with-guided-hints-and-ux-clarity/66-02-SUMMARY.md
@.planning/phases/66-improve-onboarding-flow-with-guided-hints-and-ux-clarity/66-03-SUMMARY.md

@CompassV2/src/components/Layout.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate onboarding flag cleanup in Layout.jsx</name>
  <files>CompassV2/src/components/Layout.jsx</files>
  <action>
Update the `handleClearCompass` function in Layout.jsx to remove all new onboarding localStorage flags alongside the existing ones. The function already removes `onboarding_spokeFlip`, `calibration_skipped`, `calibration_completed`, `calibration_progress`, and `quiz_progress`. Add the following removals:

```jsx
localStorage.removeItem("onboarding_postCalTour");
localStorage.removeItem("onboarding_compareTour");
localStorage.removeItem("onboarding_libraryTour");
localStorage.removeItem("onboarding_writeInHint");
```

Place them next to the existing `localStorage.removeItem("onboarding_spokeFlip");` line for logical grouping.

Note: Plans 02 and 03 may have already added some of these lines. This task ensures ALL flags are present and correctly placed. If any are already there, just verify completeness — do not duplicate.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>handleClearCompass in Layout.jsx removes all 5 onboarding localStorage flags (spokeFlip, postCalTour, compareTour, libraryTour, writeInHint) plus existing calibration/quiz flags. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete onboarding flow end-to-end</name>
  <files>CompassV2/src/components/Layout.jsx</files>
  <action>
Human verification of the complete onboarding system built across Plans 01-04:
1. Reusable CoachMark component with spotlight overlay
2. Simplified welcome screen (static SVG illustration, concise text)
3. Post-calibration 4-step guided tour (spoke, compare, library, help)
4. Write-in awareness hint on first calibration question
5. Library page 2-step coach mark tour (+ button, full calibration CTA)
6. Compare deep-dive 4-step tour (politician picker, topic selector, overlay, spoke inversion)
7. All flags cleared on compass reset

Verification steps:
1. Clear ALL localStorage: open browser DevTools > Application > Storage > Clear all
2. Visit CompassV2 dev server (npm run dev)

Welcome screen test:
3. Observe the simplified welcome screen — should show a static compass illustration (not a GIF) and 1-2 lines of text
4. Click "Get Started"

Calibration + write-in hint test:
5. Pick 3+ topics, proceed to answer step
6. On the FIRST question, verify a subtle hint appears below "Write your own..." button
7. Advance to question 2 — hint should disappear and never return

Post-calibration tour test:
8. Finish answering all topics, see "Your Compass is Ready" celebration
9. Click "View My Compass" — a 4-step tour should begin:
   - Step 1: Spotlight on compass chart ("Click any spoke to flip...")
   - Step 2: Spotlight on Compare button ("See how your views line up...")
   - Step 3: Spotlight on "Back to Library" link ("Add or change topics...")
   - Step 4: Spotlight on ? help button ("Need a refresher...")
10. Verify Next/Skip All buttons work on each step
11. Complete the tour — refresh page — tour should NOT re-appear

Library tour test:
12. Navigate to Library page (first visit)
13. Verify 2-step tour:
    - Step 1: Spotlight on a + button ("Tap + to add a topic...")
    - Step 2: Spotlight on Full Calibration banner ("Or take the full calibration...")
14. Complete or skip — refresh page — tour should NOT re-appear

Compare tour test:
15. Return to Compass, click Compare, select a politician
16. Verify 4-step tour:
    - Step 1: Politician picker ("Search for any politician...")
    - Step 2: Topic dropdown ("Pick a topic to see...")
    - Step 3: Radar chart ("The blue overlay shows...")
    - Step 4: Spoke inversion clarification ("Flipping a spoke changes...")
17. Complete or skip — deselect and reselect politician — tour should NOT re-appear

Reset test:
18. Use profile menu > Reset compass
19. Repeat steps 3-17 — all tours should fire again (flags were cleared)

Mobile responsiveness:
20. Repeat key flows on mobile viewport (375px wide) — verify coach marks position correctly
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>All onboarding flows verified: welcome screen simplified, write-in hint shows on first question only, post-cal tour fires after calibration, library tour fires on first visit, compare tour fires on first compare, compass reset clears all flags. Mobile and desktop display correctly.</done>
</task>

</tasks>

<verification>
- All 4 plans build successfully
- Fresh user sees: simplified welcome, write-in hint, post-cal tour, library tour, compare tour
- Returning user sees: none of the tours (all dismissed)
- Reset compass: all tours fire again
- Mobile and desktop: coach marks position correctly
</verification>

<success_criteria>
- The complete onboarding system provides a polished, guided discovery experience for first-time users
- Existing features (calibration, compare, library) are made discoverable without adding new features
- All interactions are one-time-only, non-intrusive, and respect user dismissal
- Compass reset cleanly resets the entire onboarding experience
</success_criteria>

<output>
After completion, create `.planning/phases/66-improve-onboarding-flow-with-guided-hints-and-ux-clarity/66-04-SUMMARY.md`
</output>
