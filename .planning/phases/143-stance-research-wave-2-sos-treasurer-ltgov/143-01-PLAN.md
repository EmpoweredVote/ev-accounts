---
phase: 143-stance-research-wave-2-sos-treasurer-ltgov
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/exec-w2-batch-a/
requirements: [SEXS-02]
must_haves:
  truths:
    - "All 11 batch-A execs (TX/FL/NY/PA LtGov+Treas + IL LtGov+SoS+Treas) have >=1 sourced inform.politician_answers row, OR are a documented honest-skip-of-whole-record (LtGov with no independent record)"
    - "Every batch-A answer row has a paired inform.politician_context row whose sources[1] starts with http (zero unsourced)"
    - "No previously-stanced exec is touched (push is external_id-keyed to the 11 batch-A ids only)"
    - "No stance inferred from party; no LtGov stance mirrors the same-state Governor without independent personal sourcing; topics without evidence honest-skipped"
    - "Treasurers scored from investment/divestment/fund actions; SoS from specific election-administration actions; LtGov honest-partial from independent record"
  artifacts:
    - path: "backend/data/stance-research/exec-w2-batch-a/"
      provides: "Per-exec research CSVs + _TOPIC_SCALE.txt + _merge.ts + _push.ts + dated merged CSV for batch A"
      contains: "_push.ts"
  key_links:
    - from: "backend/data/stance-research/exec-w2-batch-a/_push.ts"
      to: "inform.politician_answers + inform.politician_context (+ essentials.quotes)"
      via: "external_id->politician_id resolve, BEGIN/COMMIT upsert ON CONFLICT"
      pattern: "external_id|politician_answers|politician_context"
---

<objective>
Research and push sourced compass stances for **batch A — 11 execs**: the Lieutenant Governors, Secretaries of State, and Treasurers of **TX, FL, NY, PA, IL** (largest-population in-scope states for Wave 2). Reuse the v2.16/v2.17/142 stance pipeline VERBATIM — per-exec CSV from `politician-stance-researcher` at <=3 concurrency, merged + validated by `_merge.ts`, pushed external_id->UUID by `_push.ts`.

Purpose: SEXS-02 (completing). These execs currently lack any `inform.politician_answers` rows. Largest states first so calibration carries forward.
Output: a populated `backend/data/stance-research/exec-w2-batch-a/` dir + 11 execs' stances pushed to prod, zero unsourced.

**In-scope execs + EXACT external_ids (use literally — IDs are IRREGULAR, never derive from FIPS). Use the title shown in each dispatch prompt:**
- TX Lieutenant Governor (Dan Patrick) — `-100203`
- TX Comptroller [Treasurer] (Glenn Hegar) — `-100205`
- FL Lieutenant Governor (Jay Collins) — `-1200002`
- FL Chief Financial Officer [Treasurer] (Blaise Ingoglia) — `-1200004`
- NY Lieutenant Governor (Antonio Delgado) — `-3600002`
- NY Comptroller [Treasurer] (Thomas DiNapoli) — `-3600004`
- PA Lieutenant Governor (Austin Davis) — `-4200002`
- PA Treasurer (Stacy Garrity) — `-4200004`
- IL Lieutenant Governor (Juliana Stratton) — `-1700002`
- IL Secretary of State (Alexi Giannoulias) — `-1700004`
- IL Treasurer (Mike Frerichs) — `-1700005`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md

# Pipeline scripts to sed-clone (DO NOT split on commas; use these exact files as the template):
@backend/data/stance-research/gov-ag-batch-a/_merge.ts
@backend/data/stance-research/gov-ag-batch-a/_push.ts
# _TOPIC_SCALE.txt is the 25 federal-topic 1-5 scale text — copy as-is (unchanged for state execs):
#   backend/data/stance-research/gov-ag-batch-a/_TOPIC_SCALE.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up batch-a dir + research first 6 execs (<=3 concurrency)</name>
  <files>backend/data/stance-research/exec-w2-batch-a/</files>
  <read_first>
    - .planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md (office-type evidence rules; calibration; pipeline reuse; concurrency)
    - backend/data/stance-research/gov-ag-batch-a/_merge.ts (the OUT + IN_SCOPE edit points; repair()/esc()/FEDERAL reused untouched)
    - backend/data/stance-research/gov-ag-batch-a/_push.ts (external_id->UUID resolve, txn upsert — no edits needed)
  </read_first>
  <action>
    SET UP: `mkdir backend/data/stance-research/exec-w2-batch-a`. Copy `_TOPIC_SCALE.txt`, `_merge.ts`, `_push.ts` from `gov-ag-batch-a/`. In `_merge.ts` HAND-EDIT exactly two values (watch the substring-clobber sed trap — hand-edit, do not sed): (1) `OUT` -> `.../2026-06-21-exec-w2-batch-a.csv`; (2) `IN_SCOPE` -> the Set of these 11 external_ids: -100203,-100205,-1200002,-1200004,-3600002,-3600004,-4200002,-4200004,-1700002,-1700004,-1700005. Leave `DIR`, `FEDERAL`, `repair()`, `esc()` untouched.

    RESEARCH (<=3 concurrency per [[feedback_stance_research_one_at_a_time]], NEVER mass-launch): dispatch `politician-stance-researcher` agents in serial groups of <=3, waiting for each agent's CSV before launching the next group. This task covers the first 6 execs: TX LtGov Patrick (-100203), TX Comptroller Hegar (-100205), FL LtGov Collins (-1200002), FL CFO Ingoglia (-1200004), NY LtGov Delgado (-3600002), NY Comptroller DiNapoli (-3600004). Each dispatch prompt MUST include: politician name + the EXACT office/title shown above + state (e.g. "Glenn Hegar (Texas Comptroller)"); the absolute `--output-file` `C:/EV-Accounts/backend/data/stance-research/exec-w2-batch-a/{lastname}.csv`; the FULL `_TOPIC_SCALE.txt` contents pasted into the TOPIC SCALE REFERENCE placeholder (embed actual 1-5 texts per [[feedback_stance_scale_embed_texts]] — never "5=progressive"); the office-type evidence rule for that exec's office (Treasurer/Comptroller/CFO = investment/divestment/documented fund actions, NOT budget-overview text; SoS = specific election-administration actions, NOT role text; LtGov = honest-partial from independent personal record, NEVER mirror the Governor); the RFC-4180 escaping reminder (double-quote reasoning/quotes, escape embedded quotes by doubling, NO stray trailing double-quote on the empty final field); and the instruction to PUT THE EXACT external_id in the `external_id` column (merge is external_id-keyed and validates IN_SCOPE.has(ext)). CSV column order: full_name,external_id,topic_key,value,reasoning,source_url_1,source_url_2,source_url_3,quote_text,quote_deidentified. Validate the first triple completes with non-empty CSVs; if 429 empty-output reappears, drop to 1-2 concurrency.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && ls backend/data/stance-research/exec-w2-batch-a/*.csv 2>/dev/null | grep -v '/_' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - batch-a dir exists with _TOPIC_SCALE.txt + _merge.ts (OUT + IN_SCOPE edited to the 11 ids) + _push.ts.
    - At least 6 non-underscore CSVs exist, each with the exec's external_id in the external_id column.
    - No stance inferred from party; no LtGov mirrors the Governor; topics without evidence honest-skipped.
  </acceptance_criteria>
  <done>batch-a dir set up; 6 TX/FL/NY CSVs researched at <=3 concurrency with the exact external_ids.</done>
</task>

<task type="auto">
  <name>Task 2: Research PA + IL execs (5), merge, proxy-row review, push, assert coverage</name>
  <files>backend/data/stance-research/exec-w2-batch-a/</files>
  <read_first>
    - backend/data/stance-research/exec-w2-batch-a/_merge.ts (the in-progress batch dir from Task 1)
    - .planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-CONTEXT.md (proxy-row gate, isidewith below the bar, SSM=5 rule, LtGov honest-partial/whole-skip rule, no-scope-creep verification)
  </read_first>
  <action>
    RESEARCH the remaining 5 execs at <=3 concurrency: PA LtGov Davis (-4200002), PA Treasurer Garrity (-4200004), IL LtGov Stratton (-1700002), IL SoS Giannoulias (-1700004), IL Treasurer Frerichs (-1700005) — same dispatch-prompt requirements as Task 1.

    MERGE: `cd C:/EV-Accounts/backend && node --import tsx data/stance-research/exec-w2-batch-a/_merge.ts`. Fix per-file CSV artifacts BEFORE re-merge if the merge reports problems: stray lone trailing double-quote on the empty final field -> `sed -i 's/,"$/,/'` on that file; quad-quote is auto-repaired by `_merge.ts repair()` (collapse quad+ to triple ONLY — NEVER triple to double). Regenerate an offending exec's CSV rather than global quote-replace.

    PROXY-ROW REVIEW GATE: drop any row whose only evidence is caucus/coalition membership WITHOUT a published position on that topic; drop rows sourced ONLY by isidewith.com (no ballotpedia/wikipedia/ontheissues/lcv/votesmart/.gov/state-office press); drop same-sex-marriage=5 unless backed by a documented anti-recognition vote/action or explicit one-man-one-woman constitutional-amendment endorsement. For LtGovs: if NO independent sourceable record exists at all, honest-skip the whole record (zero rows) and note it for the gate — NEVER mirror the Governor to manufacture coverage.

    PUSH (only when merge reports 0 problems AND 0 unsourced — standing auto-push rule; still show the summary): `cd C:/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx data/stance-research/exec-w2-batch-a/_push.ts data/stance-research/2026-06-21-exec-w2-batch-a.csv` (the `set -a && source .env && set +a` prefix is MANDATORY — `_push.ts` does NOT load dotenv).

    ASSERT COVERAGE (no-scope-creep, per-scope external_id counts — NOT the global row counter): run the verify command below; it confirms every batch-A external_id that received stances is covered and zero of their answer rows lack a paired http-sourced context row. Note in the SUMMARY any LtGov honest-skip-of-whole-record (with its external_id) so the Phase-143 gate can pin it.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node scripts/verify-stance-coverage.mjs 11 -100203 -100205 -1200002 -1200004 -3600002 -3600004 -4200002 -4200004 -1700002 -1700004 -1700005</automated>
  </verify>
  <acceptance_criteria>
    - Every batch-A external_id with stances resolves and has >=1 inform.politician_answers row; any zero-record LtGov is documented as an honest-skip with its external_id.
    - Zero batch-A answer rows lack a paired inform.politician_context row with sources[1] LIKE 'http%'.
    - Proxy-only rows dropped per the review gate; no party-inferred stances; no Governor-mirrored LtGov stances.
    - Merge reported 0 problems AND 0 unsourced before push.
  </acceptance_criteria>
  <done>batch-A execs pushed; verify prints PASS; zero unsourced; any LtGov whole-skip recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live source (state .gov / ballotpedia / official press) -> stance data | Officeholder positions sourced externally; mis-sourcing, party-inference, or Governor-mirroring is the correctness risk |
| author -> prod DB (kxsdzaojfaibhuzmclfq) | _push.ts writes inform.politician_answers/context directly to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-A-01 | Spoofing | Stance inferred from party, or a LtGov stance mirrored from the Governor rather than the LtGov's own record | mitigate | [[feedback_stance_no_assumption]] strict mode + explicit no-mirror rule in every LtGov dispatch; _merge.ts rejects rows lacking an http source; honest-skip/honest-partial |
| T-143-A-02 | Tampering | Push writes to a previously-stanced or out-of-scope exec | mitigate | _push.ts is external_id-keyed; IN_SCOPE Set = exactly the 11 batch-A ids; per-scope coverage assertion |
| T-143-A-03 | Information Disclosure | Answer row with no paired source-bearing context row (unsourced) | mitigate | _merge.ts requires >=1 http source per row; verify asserts zero unsourced for the batch ids |
| T-143-A-04 | Tampering | Proxy-only / role-description row (SoS "administers elections"; Treasurer budget page; coalition w/o on-topic position; over-read SSM=5) inflates coverage | mitigate | Proxy-row review gate in Task 2 before push (ROADMAP SC#4) |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pipeline uses existing deps csv-parse/pg); no supply-chain surface |
</threat_model>

<verification>
Per-batch coverage assertion (Task 2 verify): every batch-A external_id with stances covered, unsourced=0. The consolidated Phase-143 gate (`verify-phase-143.sql`) re-asserts coverage + zero-unsourced for all Wave-2 roles keyed on (STATE_EXEC, role_canonical IN secretary_of_state/treasurer/lt_governor).
</verification>

<success_criteria>
- All 11 batch-A execs (TX/FL/NY/PA/IL LtGov+SoS+Treas) have >=1 sourced stance OR a documented LtGov whole-skip; zero unsourced.
- No previously-stanced exec touched; no party-inferred / Governor-mirrored stances; proxy gate applied.
- batch-a dir committed as the data record (CSVs + merged dated CSV + scripts).
</success_criteria>

<output>
Create `.planning/phases/143-stance-research-wave-2-sos-treasurer-ltgov/143-01-SUMMARY.md` when done.
</output>
