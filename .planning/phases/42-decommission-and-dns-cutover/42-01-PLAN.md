---
phase: 42-decommission-and-dns-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/.env.production
  - render.yaml
  - docs/SMOKE-TEST-INTEG.md
  - docs/ONBOARDING-VQ.md
  - docs/ONBOARDING-CTC.md
  - docs/DECOMMISSION-RUNBOOK.md
autonomous: true

must_haves:
  truths:
    - "A step-by-step runbook exists that Chris can follow to execute the full decommission sequence without needing additional context"
    - "No file in the ev-accounts repo references ev-accounts-api.onrender.com"
    - "The runbook includes a pre-flight checklist, DNS TTL steps, traffic gate criteria, DNS flip procedure, post-flip monitoring, rollback procedure, and Go server scale-down"
  artifacts:
    - path: "docs/DECOMMISSION-RUNBOOK.md"
      provides: "Complete operational runbook for DNS cutover and Go server decommission"
      min_lines: 100
    - path: "app/.env.production"
      provides: "Production env var pointing to canonical URL"
      contains: "accounts.empowered.vote"
    - path: "render.yaml"
      provides: "Render static site config with canonical URL"
      contains: "accounts.empowered.vote"
  key_links:
    - from: "docs/DECOMMISSION-RUNBOOK.md"
      to: "Phase 42 CONTEXT.md decisions"
      via: "Runbook sequence matches locked cutover decisions"
      pattern: "T-24h.*TTL.*300"
---

<objective>
Create the decommission runbook and clean up all stale internal Render URLs in the ev-accounts repo.

Purpose: The runbook is the primary deliverable of Phase 42 — a standalone document Chris follows to retire EV-Backend and flip DNS. The URL cleanup ensures no ev-accounts file references the internal `ev-accounts-api.onrender.com` URL after the cutover.

Output: `docs/DECOMMISSION-RUNBOOK.md` (runbook), updated `app/.env.production`, `render.yaml`, and 3 docs files.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-decommission-and-dns-cutover/42-CONTEXT.md
@.planning/phases/42-decommission-and-dns-cutover/42-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale internal URLs across repo</name>
  <files>
    app/.env.production
    render.yaml
    docs/SMOKE-TEST-INTEG.md
    docs/ONBOARDING-VQ.md
    docs/ONBOARDING-CTC.md
  </files>
  <action>
    Replace ALL occurrences of `https://ev-accounts-api.onrender.com` with `https://accounts.empowered.vote` in the following files:

    1. `app/.env.production` — line 1: `VITE_API_URL=https://accounts.empowered.vote`
    2. `render.yaml` — line 9 env var value: `https://accounts.empowered.vote`
    3. `docs/SMOKE-TEST-INTEG.md` — all curl examples and the "Production API URL" line
    4. `docs/ONBOARDING-VQ.md` — the header URL, all fetch examples, and the env var table row for `ACCOUNTS_URL`
    5. `docs/ONBOARDING-CTC.md` — the header URL, all fetch examples, and both env var table rows (`EMPOWERED_ACCOUNTS_URL` and `EMPOWERED_ACCOUNTS_API_URL`)

    After editing, run a repo-wide grep to confirm zero remaining occurrences of `ev-accounts-api.onrender.com` in tracked files (ignore .planning/ directory — those are historical records).
  </action>
  <verify>
    `grep -r "ev-accounts-api.onrender.com" app/ render.yaml docs/ backend/` returns zero results.
  </verify>
  <done>
    All 5 files updated. No tracked source or doc file outside `.planning/` references `ev-accounts-api.onrender.com`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the decommission runbook</name>
  <files>docs/DECOMMISSION-RUNBOOK.md</files>
  <action>
    Create `docs/DECOMMISSION-RUNBOOK.md` — a standalone operational runbook that Chris can follow without any other context. Structure it as follows:

    **Header:** Title, date prepared, executor (ED / Chris Cantrell), and a "Prerequisites" section listing what must be true before starting (Phase 40 complete, Phase 41 complete, all frontends on accounts.empowered.vote).

    **Section 1: Pre-Flight Checklist (T-48h to T-24h)**
    A checkbox list that must ALL be checked before proceeding:
    - [ ] Confirm Route 53 console access (ED logs in, can see `empowered.vote` hosted zone)
    - [ ] Confirm Render dashboard access to both `ev-backend` and `ev-accounts-api` services
    - [ ] Add `api.empowered.vote` as a custom domain on ev-accounts Render service (Settings > Custom Domains). Wait for Render to show "Verified" (SSL provisioned). If already added, confirm status is Verified.
    - [ ] Verify Netlify env vars for all 4 frontends — none reference `ev-backend-h3n8.onrender.com` or the Go server. Specific apps to check: CompassV2, Essentials (Transparent Motivations), Read & Rank (`VITE_API_URL` should be `https://accounts.empowered.vote`), Treasury Tracker.
    - [ ] Verify CTC and VQ Render services — note any stale `ev-accounts-api.onrender.com` URLs for post-cutover cleanup (not blocking).
    - [ ] Run `curl -s https://accounts.empowered.vote/api/health` — confirm `{"status":"ok","timestamp":"..."}` response.

    **Section 2: Lower DNS TTL (T-24h)**
    Step-by-step Route 53 instructions:
    1. Route 53 console > Hosted zones > `empowered.vote`
    2. Find `api.empowered.vote` record
    3. Edit > Change TTL from current value (likely 3600) to `300`
    4. Save. Note the timestamp. Wait 24 hours before proceeding to T-0.

    **Section 3: Traffic Gate Verification (T-0)**
    How to verify zero application traffic on Go server:
    1. Go to Render Dashboard > `ev-backend` service > Logs
    2. Review the last 24 hours of logs
    3. Filter out health check pings (path `/`, ~1/minute from Render itself)
    4. Zero application-level requests means paths like `/auth/*`, `/compass/*`, `/essentials/*`, `/api/*` — there should be NONE
    5. If traffic found: investigate source, fix stale URL, restart 24-hour clock
    6. If traffic persists after 48 hours and is unresolvable (bot/crawler): proceed anyway per CONTEXT.md decision

    **Section 4: DNS Flip (T-0, after traffic gate passes)**
    Step-by-step:
    1. Route 53 console > `empowered.vote` > `api.empowered.vote` record
    2. Edit the record value: point to ev-accounts Render service (same target as `accounts.empowered.vote` — likely a CNAME to the Render-provided hostname)
    3. Save. Note the timestamp.
    4. Wait 5 minutes (TTL is 300s)
    5. Verify: `curl -s https://api.empowered.vote/api/health` — expect `{"status":"ok","timestamp":"..."}`
    6. Verify CORS: `curl -I -X OPTIONS -H "Origin: https://compass.empowered.vote" -H "Access-Control-Request-Method: GET" -H "Access-Control-Request-Headers: Authorization" https://api.empowered.vote/api/auth/me` — expect `Access-Control-Allow-Origin: https://compass.empowered.vote`

    **Section 5: Post-Flip Monitoring (T-0 to T+4h)**
    - Monitor ev-accounts Render logs for 4 hours
    - Watch for any new 5xx errors not present before the flip
    - Rollback trigger: any new 5xx errors → flip DNS back to Go server (still running)
    - Rollback procedure: Route 53 > revert `api.empowered.vote` record to previous value > wait 5 min > verify Go server responds

    **Section 6: Scale Go Server to Zero (T+4h)**
    Only proceed if no new 5xx errors during monitoring window:
    1. Render Dashboard > `ev-backend` > Settings
    2. Suspend the service (or set min instances to 0)
    3. Verify: `curl -s https://ev-backend-h3n8.onrender.com` — should NOT respond (timeout or error)
    4. Note: after this point, rollback requires restarting the Go service on Render + DNS revert (more complex, avoid if possible)

    **Section 7: Raise DNS TTL (T+24h)**
    After 24 hours of stable operation:
    1. Route 53 > `api.empowered.vote` > Edit TTL back to `3600`
    2. Save

    **Section 8: Archive Go Repo (T+1 week)**
    After one week with no issues:
    1. GitHub > EV-Backend repository > Settings > Archive this repository
    2. This makes the repo read-only. It can be unarchived if needed.

    **Section 9: Post-Cutover Cleanup (non-blocking)**
    Recommended cleanup items (not blocking, do at leisure):
    - Update VQ Render service: `VITE_ACCOUNTS_API_URL` from `ev-accounts-api.onrender.com` to `accounts.empowered.vote`
    - Update CTC Render service: check for any stale `ev-accounts-api.onrender.com` env vars
    - Confirm `trivia_service` Supavisor registration (from Phase 41 open blocker)

    **Footer:** Phase 42 requirements satisfied (CONS-20, CONS-21, CONS-22), link to 42-CONTEXT.md for original decisions.
  </action>
  <verify>
    File exists at `docs/DECOMMISSION-RUNBOOK.md`. Contains all 9 sections. Contains at least one `curl` verification command per critical step. Contains checkbox-formatted pre-flight checklist.
  </verify>
  <done>
    Runbook is a standalone document. A person with Render dashboard and Route 53 console access can execute the full decommission sequence by following it top to bottom without needing any other document.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "ev-accounts-api.onrender.com" app/ render.yaml docs/ backend/` returns zero results
2. `docs/DECOMMISSION-RUNBOOK.md` exists and contains sections for: Pre-Flight, TTL, Traffic Gate, DNS Flip, Monitoring, Scale to Zero, TTL Restore, Archive, Cleanup
3. `app/.env.production` contains `https://accounts.empowered.vote`
4. `render.yaml` contains `https://accounts.empowered.vote`
</verification>

<success_criteria>
- All stale `ev-accounts-api.onrender.com` URLs replaced with `accounts.empowered.vote` in tracked files
- Decommission runbook is complete, standalone, and covers the full T-24h to T+1week sequence
- Runbook matches all locked decisions from 42-CONTEXT.md (hard flip, 300s TTL, 4-hour standby, zero-traffic gate)
</success_criteria>

<output>
After completion, create `.planning/phases/42-decommission-and-dns-cutover/42-01-SUMMARY.md`
</output>
