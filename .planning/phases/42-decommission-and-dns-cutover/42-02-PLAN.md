---
phase: 42-decommission-and-dns-cutover
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "api.empowered.vote resolves to the ev-accounts Express server and returns a healthy response"
    - "EV-Backend Go server is scaled to zero on Render and does not respond to HTTP requests"
    - "CORS headers on ev-accounts accept requests from all production frontend origins via api.empowered.vote"
  artifacts: []
  key_links:
    - from: "api.empowered.vote"
      to: "ev-accounts Express server"
      via: "Route 53 DNS CNAME record"
      pattern: "curl.*api\\.empowered\\.vote.*health"
---

<objective>
Execute the decommission cutover using the runbook created in Plan 01.

Purpose: This plan gates the actual infrastructure changes (DNS flip, Go server shutdown) behind human checkpoints since they require Render dashboard and Route 53 console access that Claude cannot automate.

Output: DNS flipped, Go server decommissioned, Phase 42 requirements (CONS-20, CONS-21, CONS-22) fulfilled.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-decommission-and-dns-cutover/42-CONTEXT.md
@.planning/phases/42-decommission-and-dns-cutover/42-01-SUMMARY.md
@docs/DECOMMISSION-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute decommission runbook</name>
  <action>
    Follow `docs/DECOMMISSION-RUNBOOK.md` from Section 1 (Pre-Flight Checklist) through Section 6 (Scale Go Server to Zero).

    This is a multi-day operation:
    - Day 0: Complete Pre-Flight Checklist (Section 1) + Lower DNS TTL (Section 2)
    - Day 1 (T-0): Traffic Gate Verification (Section 3) + DNS Flip (Section 4) + begin Post-Flip Monitoring (Section 5)
    - Day 1 (T+4h): Scale Go Server to Zero (Section 6) if monitoring passes
    - Day 2 (T+24h): Raise DNS TTL (Section 7)
    - Day 8 (T+1 week): Archive Go Repo (Section 8)

    Sections 7 and 8 can be done asynchronously after this plan is marked complete.
  </action>
  <how-to-verify>
    After completing through Section 6, verify all three success signals:

    1. Health check via new DNS:
       `curl -s https://api.empowered.vote/api/health`
       Expected: `{"status":"ok","timestamp":"..."}`

    2. CORS preflight via new DNS:
       `curl -I -X OPTIONS -H "Origin: https://compass.empowered.vote" -H "Access-Control-Request-Method: GET" -H "Access-Control-Request-Headers: Authorization" https://api.empowered.vote/api/auth/me`
       Expected: `Access-Control-Allow-Origin: https://compass.empowered.vote`

    3. Go server is down:
       `curl -s --max-time 10 https://ev-backend-h3n8.onrender.com`
       Expected: timeout or connection refused
  </how-to-verify>
  <resume-signal>
    Report results of the three verification commands above. Type "cutover complete" if all three pass, or describe any issues encountered.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record cutover completion</name>
  <files></files>
  <action>
    After Chris confirms cutover is complete, verify the three success signals by running the curl commands if possible from the current environment. Then create the plan summary documenting:
    - DNS flip confirmed (api.empowered.vote -> ev-accounts)
    - Go server scaled to zero
    - CORS verified
    - Any remaining items (TTL restore at T+24h, repo archive at T+1 week)
    - CONS-20, CONS-21, CONS-22 status
  </action>
  <verify>Summary file created with cutover results documented.</verify>
  <done>Phase 42 plan 02 summary recorded. CONS-20, CONS-21, CONS-22 fulfilled (pending TTL restore and repo archive which are deferred cleanup).</done>
</task>

</tasks>

<verification>
1. `curl -s https://api.empowered.vote/api/health` returns `{"status":"ok","timestamp":"..."}`
2. CORS preflight returns correct `Access-Control-Allow-Origin` header for all production frontend origins
3. Go server at `ev-backend-h3n8.onrender.com` does not respond to HTTP requests
4. No new 5xx errors observed during 4-hour post-flip monitoring window
</verification>

<success_criteria>
- api.empowered.vote resolves to ev-accounts Express server
- EV-Backend Go server is scaled to zero
- CORS headers allow all production frontend origins
- Phase 42 requirements CONS-20, CONS-21, CONS-22 are fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/42-decommission-and-dns-cutover/42-02-SUMMARY.md`
</output>
