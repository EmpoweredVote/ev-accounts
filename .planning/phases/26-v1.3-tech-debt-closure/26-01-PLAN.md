---
phase: 26-v1.3-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/account.ts
  - backend/src/types/database.types.ts
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "PATCH /api/account/me response includes location_consent at root level, matching GET /me shape"
    - "database.types.ts contains no gem_balance references (only gem_balance_yellow/blue/red)"
    - "REQUIREMENTS.md traceability table shows GEM-01/02/03 as Complete and HUB-01 through HUB-04 present with Phase 24 and Complete status"
  artifacts:
    - path: "backend/src/routes/account.ts"
      provides: "PATCH /me field parity with GET /me"
      contains: "location_consent"
    - path: "backend/src/types/database.types.ts"
      provides: "Clean generated types without legacy gem_balance"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability table"
      contains: "HUB-01"
  key_links:
    - from: "backend/src/routes/account.ts PATCH handler"
      to: "backend/src/routes/account.ts GET handler"
      via: "identical field set in response object"
      pattern: "location_consent.*connected.*location_consent"
---

<objective>
Close the three remaining v1.3 tech debt items: PATCH /me field parity with GET /me (add location_consent), remove legacy gem_balance from database.types.ts, and update REQUIREMENTS.md traceability.

NOTE: Research confirmed that migrations 031 and 032 were already fixed during Phase 19 execution (commit b6cbae3). Success criterion 1 (supabase db reset) is already satisfied. No migration edits needed.

Purpose: Ensure API consistency, clean types, and accurate project documentation before closing v1.3.
Output: Three surgically edited files — no new features, no refactoring beyond the named items.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-v1.3-tech-debt-closure/26-CONTEXT.md
@.planning/phases/26-v1.3-tech-debt-closure/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PATCH /me location_consent parity</name>
  <files>backend/src/routes/account.ts</files>
  <action>
Two surgical edits in the PATCH /api/account/me handler:

**Edit A — SELECT string (~line 337):** Add `location_consent` after `completed_onboarding` in the connected_profiles re-fetch SELECT. Current:
```
'id, display_name, account_standing, verification_status, tolerance_rating, total_xp, gem_balance_yellow, gem_balance_blue, gem_balance_red, completed_onboarding, created_at'
```
Change to:
```
'id, display_name, account_standing, verification_status, tolerance_rating, total_xp, gem_balance_yellow, gem_balance_blue, gem_balance_red, completed_onboarding, location_consent, created_at'
```

**Edit B — Response object (~line 384):** Add `location_consent` at root level after the `completed_onboarding` line. Insert:
```typescript
location_consent: updatedConnected?.location_consent ?? false,
```
This matches GET /me line ~120 which already has `location_consent: connected?.location_consent ?? false` at root.

Do NOT place location_consent inside the nested `connected_profile` object — it belongs at root only (so Inform-tier users without a connected_profile still have the field with a false default).
  </action>
  <verify>
- `grep 'location_consent' backend/src/routes/account.ts` shows the field in BOTH the GET handler SELECT (~line 61) AND the PATCH handler SELECT (~line 337), plus in both response objects
- `npx tsc --noEmit` from backend/ — 0 errors
  </verify>
  <done>PATCH /api/account/me response includes location_consent at root level, identical to GET /me shape. Both handlers select and return the same field set.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy gem_balance from database.types.ts</name>
  <files>backend/src/types/database.types.ts</files>
  <action>
Remove `gem_balance` from 6 locations in `backend/src/types/database.types.ts`. The `gem_balance_blue`, `gem_balance_red`, `gem_balance_yellow` siblings STAY — only the bare `gem_balance` line is removed.

Locations (all under `connect` schema):

1. Table `connected_profiles` > `Row` (~line 26): remove `gem_balance: number`
2. Table `connected_profiles` > `Insert` (~line 54): remove `gem_balance?: number`
3. Table `connected_profiles` > `Update` (~line 82): remove `gem_balance?: number`
4. View `connected_profiles_public` > `Row` (~line 369): remove `gem_balance: number | null`
5. View `connected_profiles_public` > `Insert` (~line 390): remove `gem_balance?: number | null`
6. View `connected_profiles_public` > `Update` (~line 411): remove `gem_balance?: number | null`

Do NOT run `supabase gen types` — that would overwrite manual fixes like `location_consent` added in Phase 20. This is a surgical manual edit only.
  </action>
  <verify>
- `grep 'gem_balance[^_]' backend/src/types/database.types.ts` returns ZERO matches (gem_balance_yellow etc. do NOT match because of the [^_] negative character class)
- `grep 'gem_balance_yellow' backend/src/types/database.types.ts` still returns matches (confirming siblings preserved)
- `npx tsc --noEmit` from backend/ — 0 errors
- `cd admin && npx tsc --noEmit` — 0 errors
  </verify>
  <done>database.types.ts contains zero references to the legacy gem_balance column. Only gem_balance_yellow, gem_balance_blue, gem_balance_red remain. TypeScript strict compilation passes in both backend and admin.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md traceability update</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Three targeted edits to `.planning/REQUIREMENTS.md`:

**Edit A — Mark GEM-01/02/03 complete:** In the traceability table at the bottom, change these three rows from `Pending` to `Complete`:
- `| GEM-01 | Phase 22 | Complete |`
- `| GEM-02 | Phase 22 | Complete |`
- `| GEM-03 | Phase 22 | Complete |`

**Edit B — Add HUB rows:** Insert four new rows in the traceability table after the PROFILE rows (before the closing coverage section):
```
| HUB-01 | Phase 24 | Complete |
| HUB-02 | Phase 24 | Complete |
| HUB-03 | Phase 24 | Complete |
| HUB-04 | Phase 24 | Complete |
```

Also add a `### HUB — Public Auth Hub` section to the requirements list (after the PROFILE section, before Future Requirements) with:
```
### HUB — Public Auth Hub

- [x] **HUB-01**: Login page at `accounts.empowered.vote/login` shows Connected Account-centric language with a visible signup link
- [x] **HUB-02**: Post-login tier-based routing — first-time to onboarding, Connected to dashboard, Empowered to empowered view, Admin to admin panel
- [x] **HUB-03**: `/signup` creates a Connected Account (email + password + profile fields) and redirects to onboarding
- [x] **HUB-04**: `?redirect=` parameter on login and signup routes to provided URL after success; untrusted domains ignored
```

**Edit C — Update coverage count:** Change the coverage summary from:
```
- v1.3 requirements: 29 total
- Mapped to phases: 29
```
to:
```
- v1.3 requirements: 33 total
- Mapped to phases: 33
```

Also update `Last updated` date to `2026-03-15 after Phase 26 tech debt closure`.
  </action>
  <verify>
- `grep 'GEM-01.*Complete' .planning/REQUIREMENTS.md` returns a match
- `grep 'HUB-01' .planning/REQUIREMENTS.md` returns a match
- `grep '33 total' .planning/REQUIREMENTS.md` returns a match
- Count of `| Complete |` in traceability table matches expected total
  </verify>
  <done>REQUIREMENTS.md traceability table is accurate: GEM-01/02/03 marked Complete, HUB-01 through HUB-04 added with Phase 24 and Complete status, coverage count updated to 33.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` from backend/ — 0 errors (covers Tasks 1 and 2)
2. `cd admin && npx tsc --noEmit` — 0 errors (covers Task 2 type changes)
3. `grep 'location_consent' backend/src/routes/account.ts` shows field in both GET and PATCH handlers
4. `grep 'gem_balance[^_]' backend/src/types/database.types.ts` returns zero matches
5. `grep -c '| Complete |' .planning/REQUIREMENTS.md` returns count including GEM-01/02/03 and HUB-01-04
</verification>

<success_criteria>
- PATCH /api/account/me returns location_consent at root level, matching GET /me
- database.types.ts has zero gem_balance references (only gem_balance_yellow/blue/red)
- REQUIREMENTS.md shows GEM-01/02/03 as Complete and HUB-01 through HUB-04 present
- TypeScript strict compilation passes in both backend and admin with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-v1.3-tech-debt-closure/26-01-SUMMARY.md`
</output>
