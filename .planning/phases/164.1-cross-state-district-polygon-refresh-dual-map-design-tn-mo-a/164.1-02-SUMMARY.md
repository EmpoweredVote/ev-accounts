---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 02
status: complete
completed: 2026-07-07
requirements: [D-04, D-10, D-11, DELIV-1, DELIV-3]
---

# 164.1-02 SUMMARY — D-10 verify harness (1641-verify.sql + 1641-coordinate-smoke.ts)

## What was built
Both harness artifacts, state-parameterized on `mtfcc='G5200V26'`, GREEN as no-ops today (0 V26 states). Per-state plans (03/04/05/07) only RE-RUN these — zero edits needed.

1. **`backend/scripts/1641-verify.sql`** (commit `0d3ddd9b`): `\set ON_ERROR_STOP on` DO-block gate.
   - **L1-TILING** per state (skip-NOTICE when 0 V26 rows; HARD FAIL on partial import `0 < n <> expected`): `ST_Area(ST_Difference(G4000 outline, ST_Union(V26)))` < **2%** of outline area (tolerance documented — absorbs shoreline/generalization slivers for coastal AL/LA; re-pin with dated comment if LA source shoreline treatment demands it).
   - **L1-RATIO**: each V26 district within [0.1, 10] of its G5200 counterpart's area.
   - **L2-ANCHOR**: every V26 geometry valid, non-empty, covers its own ST_PointOnSurface.
   - **D04-NOTOUCH** (md5 checksums, NOT counts), baselines captured live 2026-07-07:
     - offices (`id:district_id:representing_state` ORDER BY id, NATIONAL_LOWER, len(geo_id)=4): 47=`038e4c54…` (9), 29=`b0556389…` (8), 01=`0bd1a663…` (7), 22=`44909b82…` (6), 49=`4d8bbfb2…` (4)
     - geo_districts (`geoid:district_num` ORDER BY geoid, layer='us_house'): 47=`3f78d9ab…`, 29=`48427aab…`, 01=`5ccfa65c…`, 22=`02fb2976…`, 49=`10f51b4d…`
     - user_districts us_house DISTINCT (layer,geoid) set for the 5 FIPS: **EMPTY today** — pinned md5('')=`d41d8cd98f00b204e9800998ecf8427e`, cardinality 0. EXCEPTION on set change; NOTICE on row churn. **Degenerate-empty-set caveat documented in header**: first legitimate cron-resolved TN/MO/AL/LA/UT Connected user changes the set → re-pin with dated comment if no import is running.
   - V26 rows keyed by `substr(geo_id,1,2)` + `length=4`, NOT the `state` column (G5200 rows store FIPS there, G4000 rows store letters); G4000 outline keyed by `geo_id = fips`.

2. **`backend/scripts/1641-coordinate-smoke.ts`** (commit `7e0039b6`): data-driven, no hardcoded coordinates.
   - Per-state config with severe sets (TN 4704/4705/4706/4708/4709, MO 2902-2906, AL 0102, LA 2202/2206, UT none); skip without V26 rows; partial import hard-fails.
   - **L2**: every V26 anchor (ST_PointOnSurface) self-resolves under explicit G5200V26 resolution.
   - **L3 guaranteed differential** (non-severe NEW district A, OLD B, pairwise `ST_Intersection` > 1e-4 deg² with post-verified side-flip): live V26-preferring LATERAL (clone of the deployed electionService shape) surfaces NEW A's race; G5200 reps-feed query surfaces OLD B with a seated rep — same coordinate, both routes.
   - **L3 severe differential** (auto-conditional): withholding state detected from the DB (race → `{ST} 2026 Statewide General` = surfacing vs `…Polygon Pending` = withheld, a past-dated `special` the visibility window excludes). Post-flip → full differential; pre-flip → boundary differential + ZERO surfacing races.
   - **L3 Connected geo-id path (D-11)**: corrected `(NEW A,'G5200')` pair through a getElectionsByGeoIds-equivalent unnest join surfaces exactly 1 race.
   - **L3 D-11 RPC direct invocation**: dedicated `pool.connect()` client, ONE always-ROLLBACK'd txn: `INSERT auth.users(id)` sentinel `ffffffff-ffff-4fff-8fff-ffffffffffff` (a trigger auto-creates public.users — discovered live; ON CONFLICT guards both), `INSERT connect.connected_profiles(user_id)`, encrypt via `connect.upsert_user_location(sentinel, lat, lng)` (canonical Vault-backed encryptor), assert `connect.resolve_congressional_2026(sentinel)` = NEW A geo_id — hard fail on NULL/mismatch. ROLLBACK in `finally`; nothing persists (chain verified live this session inside BEGIN…ROLLBACK).
   - **UT nuance**: UT has no elections/races until Phase 165 — race-level asserts are conditional on `UT 2026 Statewide General` existing; boundary differential + RPC probe still run for UT.

## Verification
- `psql -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql` → GREEN (5 SKIP notices + 3 NOTOUCH PASSes).
- `npx tsc --noEmit` clean; `node --import tsx scripts/1641-coordinate-smoke.ts` → GREEN (5 skips).

## Deviations
- Differential discovery uses pairwise `ST_Intersection(NEW A, OLD B)` + post-verified side-flip rather than RESEARCH's ST_Difference formulation verbatim — same intent, but guarantees the sampled point actually lies in OLD B (the ST_Difference shape could report an old_geo_id that doesn't contain the specific sampled point).
- plpgsql RAISE printf-style specifiers (`%.3f`) replaced with rounded args (RAISE supports only plain `%`).

## Self-Check: PASSED
Both scripts GREEN as no-ops; all acceptance criteria met; no committed writes (sentinel probe rollback verified).
