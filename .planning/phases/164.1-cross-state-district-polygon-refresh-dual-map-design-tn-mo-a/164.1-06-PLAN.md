---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 06
type: execute
wave: 3
depends_on: [164.1-03, 164.1-04, 164.1-05]
files_modified:
  - .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-jan2027-boundary-promotion-spec.md
  - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md
autonomous: true
requirements: [D-03, D-04, D-10, D-11, DELIV-1, DELIV-3, DELIV-5]
must_haves:
  truths:
    - "The full 1641 verify bar (topology + anchors + D-04 NOTOUCH checksums) and coordinate smoke are GREEN across TN, AL, LA, UT"
    - "The flipped Phase-161 and Phase-163 gates re-run GREEN; the Phase-162 MO-SEVERE block still correctly asserts MO withheld (MO is date-gated, not yet flipped)"
    - "A Jan-2027 boundary-promotion phase spec exists (D-03) covering: promote G5200V26→canonical, retire old vintage, re-key essentials.offices per the UT wiring contract + 4-state correspondence, re-resolve connect.user_districts, refresh connected_profiles.congressional_geo_id, AND RETIRE the D-11 resolve_congressional_2026 read-path fallback once the cache reflects 2026 geometry"
    - "The D-11 Connected-tier fix (in-phase live fallback for differential-zone Connected users) is documented in the spec AND in STATE.md as SHIPPED in 164.1 — not an accepted gap — with the Jan-2027 phase noted as the point that retires the fallback"
    - "STATE.md records the MO re-entry date (≥ 2026-08-04) and the Jan-2027 promotion-phase re-entry date (≥ 2027-01-03)"
  artifacts:
    - path: ".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-jan2027-boundary-promotion-spec.md"
      provides: "Queued, date-gated Jan-2027 promotion-phase specification (D-03), including retirement of the D-11 fallback"
      contains: "2027-01-03"
    - path: ".planning/workstreams/2026-us-house-candidate-coverage/STATE.md"
      provides: "Operator next-steps + deferred items updated with re-entry dates + the shipped D-11 fix + its Jan-2027 retirement"
      contains: "2026-08-04"
  key_links:
    - from: "164.1-jan2027-boundary-promotion-spec.md"
      to: "essentials.offices re-key + connect.user_districts + connected_profiles re-resolve + resolve_congressional_2026 retirement"
      via: "documented promotion steps"
      pattern: "resolve_congressional_2026"
---

<objective>
Close the non-MO phase: run the consolidated D-10 verify bar across all four imported states (TN/AL/LA/UT), confirm the flipped Phase-161/163 gates are GREEN and Phase-162 still correctly withholds MO, then author the queued Jan-2027 boundary-promotion phase spec (D-03) and record in STATE.md the SHIPPED D-11 Connected-tier fix, the point at which Jan-2027 retires its read-path fallback, and all re-entry dates.

Purpose: Deliver DELIV-5 (Jan-2027 spec) and the phase-close verification, and formally record the two forward items: the D-11 fix shipped in-phase (with its Jan-2027 fallback retirement) and the MO date-gate. This runs after the four import/un-withhold plans; MO (Plan 07) is date-gated and completes independently.
Output: The Jan-2027 promotion-phase spec artifact + updated STATE.md, with the full verify bar confirmed GREEN across TN/AL/LA/UT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md

<interfaces>
<!-- The Jan-2027 promotion phase (D-03, deferred, executes ≥ 2027-01-03) must document these surfaces (from RESEARCH Open Questions + D-03/D-04 + the shipped D-11 fix): -->
1. Promote G5200V26 → canonical (retire the old G5200 vintage rows for the 5 states, or repoint consumers).
2. Re-key essentials.offices per the UT wiring contract (164.1-ut-wiring-contract.md) + the TN/AL/LA/MO correspondence.
3. Re-resolve connect.user_districts for TN/MO/AL/LA/UT (essentials.geo_districts is single-vintage; must be refreshed at promotion).
4. Refresh connected_profiles.congressional_geo_id (resolve_user_jurisdiction hardcodes mtfcc='G5200'; the weekly districtStalenessService cron will never pick up 2026 geometry on its own).
5. RETIRE the D-11 read-path fallback: once connected_profiles.congressional_geo_id reflects 2026 geometry (step 4), the `connect.resolve_congressional_2026` RPC + its Path-1/Path-1.5 route substitution (shipped in Plan 01) become redundant and should be removed so there is a single source of truth.

<!-- D-11 (SHIPPED in 164.1 per user decision 2026-07-06 — NOT an accepted gap): differential-zone Connected-tier users' /elections is corrected in-phase by a read-only live fallback (connect.resolve_congressional_2026, no cache mutation, 5 states only). Document it as delivered, and mark the Jan-2027 promotion phase as the point that retires the fallback (since the cache will then be authoritative). Do NOT describe this as an accepted limitation. -->

MO date-gate: ≥ 2026-08-04 (corrected from ~Jul-28 per RESEARCH; SOS Hoskins certification). Jan-2027 phase: ≥ 2027-01-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the consolidated verify bar across TN/AL/LA/UT + confirm gate states</name>
  <read_first>
    - backend/scripts/1641-verify.sql + backend/scripts/1641-coordinate-smoke.ts
    - backend/scripts/161-verify.sql, 163-verify.sql (flipped by Plans 04/05), 162-verify.sql (MO still withheld)
    - 164.1-RESEARCH.md "Sampling Rate" (phase-gate expectation)
  </read_first>
  <action>
    Run the full 1641-verify.sql + 1641-coordinate-smoke.ts against PROD — all Layer-1/2/3 blocks for TN(47)/AL(01)/LA(22)/UT(49) plus the D-04 NOTOUCH checksum block must be GREEN (including, for TN/AL/LA, the post-flip severe-district differentials proven by the data-driven smoke). Re-run 161-verify.sql + 161-coordinate-smoke.ts and 163-verify.sql + 163-coordinate-smoke.ts (flipped by Plans 04/05) — GREEN with TN/AL/LA surfacing. Re-run 162-verify.sql + 162-coordinate-smoke.ts and confirm the MO-SEVERE block STILL asserts MO (2902-2906) withheld (MO is date-gated; Plan 07 flips it later). This is the phase's consolidated acceptance across the four non-MO states.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql && node --import tsx scripts/1641-coordinate-smoke.ts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/161-verify.sql && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/163-verify.sql && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/162-verify.sql && echo CONSOLIDATED_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 1641-verify.sql + 1641-coordinate-smoke.ts exit 0 across TN/AL/LA/UT (topology + anchors + differential incl. post-flip severe + NOTOUCH checksums)
    - 161-verify.sql + 163-verify.sql exit 0 with TN/AL/LA surfacing (flipped assertions)
    - 162-verify.sql exits 0 with MO (2902-2906) STILL asserted withheld
    - D-04 NOTOUCH checksums GREEN for all 5 states' FIPS (offices + geo_districts + connect.user_districts unchanged)
  </acceptance_criteria>
  <done>Full verify bar GREEN across the four imported states; MO correctly still withheld; prior gates flipped and GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Jan-2027 boundary-promotion phase spec (D-03) incl. D-11 fallback retirement</name>
  <read_first>
    - .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-ut-wiring-contract.md (Plan 03 output — referenced by the promotion re-key)
    - .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-01-SUMMARY.md (the shipped D-11 fix: resolve_congressional_2026 RPC + route substitution)
    - 164.1-RESEARCH.md "Open Questions" #1 (connected_profiles) + "Common Pitfalls" Pitfall 2/3 + "D-02 Consumer Audit" (the surfaces the promotion must touch)
    - 164.1-CONTEXT.md D-03 + D-11 + Deferred Ideas
  </read_first>
  <action>
    Create .planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-jan2027-boundary-promotion-spec.md as a queued, date-gated (≥ 2027-01-03) phase specification. It MUST cover, as concrete steps: (1) promote G5200V26 → canonical (retire the old G5200 vintage for TN/MO/AL/LA/UT or repoint consumers); (2) re-key essentials.offices per the UT wiring contract + the TN/AL/LA/MO old→new correspondence; (3) re-resolve connect.user_districts for the 5 states (essentials.geo_districts is single-vintage, refreshed here — noting resolve_user_districts/cache_user_districts); (4) refresh connected_profiles.congressional_geo_id (resolve_user_jurisdiction hardcodes mtfcc='G5200'; the weekly districtStalenessService cron will not pick up 2026 geometry on its own); (5) RETIRE the D-11 read-path fallback — remove `connect.resolve_congressional_2026` + its Path-1/Path-1.5 route substitution (shipped in Plan 01) once step 4 makes the cache authoritative, so there is a single source of truth.
    It MUST also explicitly document that D-11 was FIXED IN-PHASE in 164.1 (differential-zone Connected-tier users already see the correct NEW district's race via the read-only live fallback) — describe it as SHIPPED, not an accepted gap — and that this Jan-2027 phase is the point that retires that temporary fallback. Follow the date-gated-phase template style used for the 153/159-05/167 deferrals.
  </action>
  <verify>
    <automated>f=".planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-jan2027-boundary-promotion-spec.md"; test -f "$f" && grep -q "2027-01-03" "$f" && grep -q "connected_profiles" "$f" && grep -q "essentials.offices" "$f" && grep -q "user_districts" "$f" && grep -q "resolve_congressional_2026" "$f" && echo SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - The spec file exists, is marked date-gated ≥ 2027-01-03
    - Covers all five promotion steps (promote canonical, re-key offices, re-resolve user_districts, refresh connected_profiles, retire the D-11 resolve_congressional_2026 fallback)
    - Documents the D-11 fix as SHIPPED in 164.1 (not an accepted gap) and this phase as the point that retires the read-path fallback
    - References the UT wiring contract
  </acceptance_criteria>
  <done>Jan-2027 promotion-phase spec exists with all promotion surfaces + the D-11 fallback retirement documented.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md operator next-steps + deferred items</name>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md ("Operator Next Steps" + "Deferred Items" sections)
  </read_first>
  <action>
    Update STATE.md: add to Operator Next Steps a "≥ 2026-08-04" entry to execute Plan 07 (MO date-gated import/un-withhold OR revert-branch per certification), and a "≥ 2027-01-03" entry to plan the Jan-2027 boundary-promotion phase (referencing 164.1-jan2027-boundary-promotion-spec.md, and noting it retires the D-11 resolve_congressional_2026 fallback). Record that D-11 SHIPPED in 164.1: differential-zone Connected-tier users' /elections is corrected in-phase by a read-only live fallback (connect.resolve_congressional_2026, no cache mutation, 5 states only) — this is DELIVERED, not an accepted limitation, and is retired by the Jan-2027 phase. Add to Deferred Items: the MO revert branch → Phase 167 if the referendum qualifies. Note that Phase 166's consolidated gate inherits the flipped TN/AL/LA severe assertions (now positive) + the 13 un-withheld districts joining the 178-district assertion set (minus MO's 5 until Plan 07 clears).
  </action>
  <verify>
    <automated>f=".planning/workstreams/2026-us-house-candidate-coverage/STATE.md"; grep -q "2026-08-04" "$f" && grep -q "2027-01-03" "$f" && grep -qi "resolve_congressional_2026" "$f" && echo STATE_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Operator Next Steps has a ≥ 2026-08-04 (MO) entry and a ≥ 2027-01-03 (Jan-2027 promotion) entry
    - STATE.md records the SHIPPED D-11 fix (resolve_congressional_2026 live fallback) and its Jan-2027 retirement — NOT as an accepted gap
    - STATE.md Deferred Items records the MO revert branch → Phase 167
    - The Phase-166 inheritance note (flipped assertions + 13 un-withheld districts) is present
  </acceptance_criteria>
  <done>STATE.md operator next-steps + deferred items updated with re-entry dates and the shipped D-11 fix + its retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate scripts → prod DB | Read-only verification; the only DB mutation is the 1641 smoke's D-11 RPC probe, always ROLLBACK'd (no committed writes) |
| docs → planning state | STATE.md edits could clobber concurrent notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-16 | Tampering (false gate) | Consolidated gate passing on incomplete data | mitigate | Re-run ALL of 1641/161/163 GREEN + confirm 162 STILL withheld (asserts MO not accidentally flipped) |
| T-1641-17 | Repudiation / lost-context | STATE.md edit races a concurrent session | mitigate | Append/edit only the named sections; read current file state first (Plan 07 also edits STATE.md but in a later wave) |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs; docs + read-only gates only |
</threat_model>

<verification>
- Full 1641 + 161 + 163 gates GREEN; 162 MO still withheld.
- Jan-2027 spec artifact present with all five promotion surfaces incl. the D-11 fallback retirement.
- STATE.md updated with both re-entry dates + the shipped D-11 fix + its retirement.
</verification>

<success_criteria>
The four-state (TN/AL/LA/UT) verify bar is GREEN, prior gates are flipped and GREEN, MO remains correctly withheld pending its date-gate, the Jan-2027 promotion-phase spec is authored with the D-11 fallback retirement documented, and STATE.md records all re-entry dates plus the shipped D-11 fix.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
