---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 05
type: execute
wave: 2
depends_on: [164.1-01, 164.1-02]
files_modified:
  - backend/scripts/1641-al-generate-polygon-import.mts
  - backend/scripts/1641-la-generate-polygon-import.mts
  - backend/migrations/1248_unwithhold_al_2026_house_races.sql
  - backend/migrations/1249_unwithhold_la_2026_house_races.sql
  - backend/scripts/163-verify.sql
  - backend/scripts/163-coordinate-smoke.ts
autonomous: false
requirements: [D-08, D-09, D-10, DELIV-1, DELIV-3]
must_haves:
  truths:
    - "Every AL (0101-0107) and LA (2201-2206) congressional district has a 2026-vintage G5200V26 polygon"
    - "AL-2 (0102) BVAP in the imported map is the ~39.9% severe-map figure (SCOTUS-reinstated 2023 legislature map), NOT the ~48.7% interim-map figure — confirming the correct map direction"
    - "AL and LA each pass the D-10 3-layer verify bar independently (per-state-atomic)"
    - "The AL severe race (0102) and LA severe races (2202,2206) surface on their state general election after the un-withhold flip"
    - "After the flip, 1641-coordinate-smoke.ts (re-run read-only) auto-proves a SEVERE-district differential for AL and for LA"
    - "Rick Edmonds (-220605) surfaces correctly as a LA-6 (2206) candidate after LA-6 un-withholds"
    - "163-verify.sql / 163-coordinate-smoke.ts assert AL-2, LA-4/LA-6-relevant districts as surfacing (positive)"
  artifacts:
    - path: "backend/scripts/1641-al-generate-polygon-import.mts"
      provides: "AL 2026 polygon import (SCOTUS-reinstated 2023 legislature map)"
      contains: "G5200V26"
    - path: "backend/scripts/1641-la-generate-polygon-import.mts"
      provides: "LA 2026 polygon import (SB121/Act 2)"
      contains: "G5200V26"
    - path: "backend/migrations/1248_unwithhold_al_2026_house_races.sql"
      provides: "Guarded un-withhold UPDATE for AL-2 (0102)"
      contains: "Polygon Pending"
    - path: "backend/migrations/1249_unwithhold_la_2026_house_races.sql"
      provides: "Guarded un-withhold UPDATE for LA-2 (2202) + LA-6 (2206)"
      contains: "Polygon Pending"
  key_links:
    - from: "backend/migrations/1248_unwithhold_al_2026_house_races.sql"
      to: "essentials.races.election_id → 'AL 2026 Statewide General'"
      via: "guarded UPDATE where election_id = AL Polygon Pending"
      pattern: "AL 2026 Statewide General"
    - from: "backend/migrations/1249_unwithhold_la_2026_house_races.sql"
      to: "essentials.races.election_id → 'LA 2026 Statewide General'"
      via: "guarded UPDATE where election_id = LA Polygon Pending"
      pattern: "LA 2026 Statewide General"
---

<objective>
Import Alabama's and Louisiana's enacted 2026 congressional polygons as `G5200V26`, confirm the correct AL map direction via a BVAP cross-check, pass the D-10 3-layer verify bar per state, then un-withhold AL-2 (0102) and LA-2/LA-6 (2202/2206), prove each state's severe-district differential via the re-run 1641 smoke, re-verify Edmonds surfaces in LA-6, spot-check the LA-4 borderline, and flip the shared Phase-163 gate assertions to positive. AL and LA share the Phase-163 gate files, so they are planned together to keep those edits atomic.

Purpose: Deliver AL + LA slices of D-09/D-10/DELIV-3. AL is the legally-tangled state — a SCOTUS stay (Milligan v. Allen, 2026-06-02) reinstated the 2023 legislature map (1 Black-opportunity district) over the interim court-ordered map, so a human-verify checkpoint confirms the imported map is the correct direction before un-withholding.
Output: AL + LA import scripts, 13 G5200V26 rows (AL 7 + LA 6), two un-withhold migrations, flipped Phase-163 gate scripts, Edmonds re-verified — all GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-PATTERNS.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-11-SUMMARY.md
@.planning/phases/163-wi-co-al-sc-la-candidate-seeding-create-elections-races-then/163-01-SUMMARY.md

<interfaces>
<!-- LA source (HIGH confidence, confirmed): -->
redist.legis.la.gov/2026_Files/2026CONGRESSACT2 — direct download "Shapefile - Act_2_(2026_RS).zip". Enactment: SB121 / Act 2 (2026 RS). Import: MTFCC='G5200V26', SOURCE='la_legis_2026', geo_ids 2201-2206, OMIT geo_districts.
LA severe set (163): 2202 (LA-2), 2206 (LA-6). LA-4 borderline (~20-22% moved, scored NOT-SEVERE) — cheap spot-check, not new scope. Edmonds -220605 was re-keyed LA-5→LA-6 in mig 1230 — re-verify he surfaces in refreshed LA-6 (2206).

<!-- AL source (MEDIUM confidence — confirm at execution time): -->
Alabama SoS "State District Maps" (sos.alabama.gov/alabama-votes/state-district-maps) — shapefile per a search snippet, direct fetch failed on TLS this session. RDH fallback per D-08. Enactment: SB5 (2023 legislature map) REINSTATED for 2026 by SCOTUS stay Milligan v. Allen 2026-06-02, OVERRIDING the interim 2-Black-opportunity map currently loaded via TIGER 2024. Import: MTFCC='G5200V26', SOURCE='al_sos_2026' or 'al_rdh_2026', geo_ids 0101-0107, OMIT geo_districts.
AL BVAP cross-check (Pitfall 4): AL-2 (0102) must be ~39.9% (severe/reinstated map) — if the sourced shapefile's AL-2 BVAP is ~48.7% (interim map), the WRONG map was downloaded.
AL severe set (163): 0102 (AL-2). Un-withhold targets: 'AL 2026 Statewide General', 'LA 2026 Statewide General'. Withheld markers: 'AL/LA 2026 Congressional Redistricting - Polygon Pending'.

Migration numbering: live high-water mark was 1245 as of 2026-07-07, BUT Plan 01 (Wave 1 RPC migration) and Plan 04 (TN un-withhold) each take numbers AHEAD of this plan, and parallel Phases 177/178 may take more. Re-run `ls backend/migrations | grep -E '^[0-9]' | sort | tail` immediately before authoring EACH migration and use the next two free numbers after Plan 04's; the 1248/1249 filenames in files_modified are PLACEHOLDERS. Since Plans 04 and 05 run inline/sequentially, this plan re-checks live so its numbers cannot collide with Plan 04's.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import AL and LA 2026 polygons (with AL source confirmation)</name>
  <read_first>
    - backend/scripts/load-national-house-districts.ts (import primitives; geo_districts insert to OMIT)
    - 164.1-RESEARCH.md "Import Pipeline" + "D-08" AL/LA rows + Pitfall 4 (AL map direction) + Assumption A3
    - 164.1-PATTERNS.md import-script section
  </read_first>
  <action>
    LA (confirmed source): clone load-national-house-districts.ts into backend/scripts/1641-la-generate-polygon-import.mts — download "Shapefile - Act_2_(2026_RS).zip" from redist.legis.la.gov, parse via adm-zip/shapefile, resolveColumn() → geo_ids 2201-2206, insert ONLY geofence_boundaries (MTFCC='G5200V26', SOURCE='la_legis_2026', ON CONFLICT DO NOTHING, ST_* normalization), header with provenance + geo_districts-omitted note, --dry-run first (expect 6).
    AL (confirm source first): attempt the AL SoS "State District Maps" shapefile; if the fetch fails (TLS as this session), use the RDH fallback (D-08). Clone into backend/scripts/1641-al-generate-polygon-import.mts with the same structure (geo_ids 0101-0107, SOURCE per actual source, MTFCC='G5200V26'). If NEITHER the AL SoS source NOR RDH yields a usable AL shapefile, STOP and raise the Task-2 checkpoint as a blocking decision rather than guessing.
    After importing, compute AL-2 (0102) BVAP from the imported geometry against block-level demographics (or record the sourced map's published AL-2 BVAP) for the Task-2 cross-check. Parameterize all inserts.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='01')||'/'||(SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='22')" | grep -qx "7/6" && echo AL7_LA6_ROWS</automated>
  </verify>
  <acceptance_criteria>
    - AL: 7 G5200V26 rows (state='01', geo_ids 0101-0107); LA: 6 G5200V26 rows (state='22', geo_ids 2201-2206)
    - `geo_districts` unchanged for FIPS 01 and 22 (imports omit geo_districts)
    - The AL-2 (0102) BVAP figure from the imported/sourced map is recorded for the Task-2 checkpoint
    - Both scripts parameterized; provenance in the `source` column
  </acceptance_criteria>
  <done>AL (7) and LA (6) G5200V26 polygons imported; AL-2 BVAP figure captured; geo_districts untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — confirm AL map direction + AL/LA topology before un-withholding</name>
  <files>backend/scripts/1641-al-generate-polygon-import.mts, backend/scripts/1641-verify.sql (read-only run)</files>
  <action>Pause for the operator after AL/LA import + Layer-1 topology. Automation (Task 1) has already imported the polygons and captured AL-2 BVAP; this checkpoint confirms the AL map DIRECTION (BVAP ~39.9% severe map, not ~48.7% interim) and AL/LA topology GREEN before any un-withhold flip proceeds. If AL source could not be obtained, the operator supplies the shapefile or defers AL here.</action>
  <what-built>
    AL (7) and LA (6) 2026 G5200V26 polygons imported. 1641-verify.sql Layer-1 topology for FIPS 01 + 22 run. AL-2 (0102) BVAP figure captured from the imported map.
  </what-built>
  <how-to-verify>
    1. Confirm the imported AL map is the SCOTUS-reinstated 2023 legislature map: AL-2 (0102) BVAP should read ~39.9% (severe). If it reads ~48.7%, the WRONG (interim) map was imported — reject and re-source before continuing.
    2. Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql` and confirm AL/FIPS-01 + LA/FIPS-22 Layer-1 tiling + area-ratio blocks PASS.
    3. Confirm the LA-4 borderline (~20-22% moved) spot-check looks sane in the area-ratio output (LA-4 = geo_id 2204).
    If AL source could not be confirmed at all (Task 1 could not obtain a usable AL shapefile from SoS or RDH), this checkpoint is where the operator decides: supply the shapefile manually into backend/data/, or defer AL to a follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" (AL map direction correct + AL/LA topology green) or describe the issue / provide the AL source.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Un-withhold AL-2 + LA-2/LA-6, prove severe differentials, re-verify Edmonds, flip the Phase-163 gate</name>
  <read_first>
    - backend/migrations/1230_fix_edmonds_la5_to_la6_rekey.sql (guarded-UPDATE + BEGIN/COMMIT + idempotency convention)
    - backend/scripts/163-verify.sql (AL-SEVERE + LA-SEVERE blocks) + backend/scripts/163-coordinate-smoke.ts (SEVERE_NEGATIVES: {AL,0102},{LA,2206})
    - backend/scripts/1641-coordinate-smoke.ts (re-run post-flip — auto-proves AL + LA severe differentials AND directly invokes connect.resolve_congressional_2026 via an always-ROLLBACK'd sentinel test row; makes no committed writes; do NOT edit it)
    - 164.1-RESEARCH.md "Un-Withhold Flip Surface" (Edmonds re-verify detail) + 164.1-PATTERNS.md migration + gate-edit sections
    - 163-11-SUMMARY.md (AL-SEVERE/LA-SEVERE assertions + Edmonds instruction)
  </read_first>
  <action>
    First run the full D-10 3-layer bar per state via 1641-coordinate-smoke.ts (AL + LA guaranteed non-severe differentials auto-discovered + asserted through the deployed live paths; severe differentials boundary-only pre-flip). AL and LA must EACH pass independently (per-state-atomic, D-09).
    Re-check live migration numbers FIRST (`ls backend/migrations | grep -E '^[0-9]' | sort | tail`) — Plan 01 and Plan 04 have taken numbers ahead of this plan and parallel Phases 177/178 may have too; the 1248/1249 filenames are provisional. Use the next two free numbers. Author two guarded, BEGIN/COMMIT-wrapped, idempotent migrations:
    - AL: set election_id to 'AL 2026 Statewide General' WHERE the race's district geo_id = '0102' AND election_id = 'AL 2026 Congressional Redistricting - Polygon Pending'.
    - LA: set election_id to 'LA 2026 Statewide General' WHERE district geo_id IN ('2202','2206') AND election_id = 'LA 2026 Congressional Redistricting - Polygon Pending'.
    Do NOT delete the 'Polygon Pending' marker elections. Apply both to PROD.
    RE-RUN 1641-coordinate-smoke.ts (unedited; makes no committed writes — its D-11 RPC probe inserts a sentinel row inside an always-ROLLBACK'd transaction): now that AL-2 and LA-2/LA-6 surface, the script auto-detects them and proves the AL and LA SEVERE-district differentials (NEW severe race on elections + OLD rep on reps feed). It must exit 0.
    Re-verify Edmonds: confirm politician -220605 has an active race_candidates row on the LA-6 (2206) race now that it surfaces (a race_candidates check — no stance/headshot work; seeded data is already keyed to the new map).
    Flip the Phase-163 gate: in 163-verify.sql, change the AL-SEVERE block (0102) and LA-SEVERE block (2202/2206) to assert those geo_ids point at their state general election (merge into the surfacing sets), update PASS text + header. In 163-coordinate-smoke.ts, remove {AL,0102} and {LA,2206} from SEVERE_NEGATIVES and add them (plus 2202) to the positive SAMPLES array with appropriate minActive; update the green summary. Re-run the 163 gate — GREEN.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections e ON e.id=r.election_id WHERE ((d.geo_id='0102' AND e.name='AL 2026 Statewide General') OR (d.geo_id IN ('2202','2206') AND e.name='LA 2026 Statewide General')) AND d.district_type='NATIONAL_LOWER'" | grep -qx 3 && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.politicians p ON p.id=rc.politician_id WHERE p.external_id=-220605 AND d.geo_id='2206' AND rc.candidate_status='active'" | grep -qx 1 && node --import tsx scripts/1641-coordinate-smoke.ts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/163-verify.sql && node --import tsx scripts/163-coordinate-smoke.ts && echo AL_LA_UNWITHHELD_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - AL-2 (0102) → 'AL 2026 Statewide General'; LA-2 (2202) + LA-6 (2206) → 'LA 2026 Statewide General' (3 races total re-pointed)
    - Both migrations BEGIN/COMMIT-wrapped, guarded on OLD election_id, idempotent; marker elections not deleted
    - 1641-coordinate-smoke.ts (re-run, unedited) exits 0 proving the AL and LA severe-district differentials now surface a race
    - Edmonds (-220605) has exactly 1 active race_candidates row on LA-6 (2206)
    - 163-verify.sql exits 0 with AL-2 + LA-2/LA-6 surfacing; 163-coordinate-smoke.ts exits 0 with those geo_ids now positive
    - AL and LA D-10 differential points each proven via the live paths
  </acceptance_criteria>
  <done>AL-2 + LA-2/LA-6 surfacing; AL/LA severe differentials proven by the re-run smoke; Edmonds re-verified in LA-6; Phase-163 gate flipped positive and GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AL/LA shapefile source → import | Untrusted geometry; AL has WRONG-MAP risk (multiple maps in play) |
| migration → prod races | Wrong-row UPDATE could surface the wrong districts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-12 | Tampering (wrong data) | AL wrong-direction map imported | mitigate | Task-2 blocking human-verify BVAP cross-check (~39.9% not ~48.7%) before any un-withhold |
| T-1641-13 | Tampering | Un-withhold UPDATE hits wrong races | mitigate | Guard on OLD election_id + explicit geo_id set; BEGIN/COMMIT; idempotent; verified by exact count = 3 |
| T-1641-14 | Tampering / integrity | Mis-imported AL/LA geometry | mitigate | D-10 Layer-1 tiling + area-ratio must pass (checkpoint) before Task 3; ST_* normalization |
| T-1641-15 | Injection (SQLi) | Shapefile-derived text | mitigate | Parameterized inserts throughout |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pg/shapefile/adm-zip already present |
</threat_model>

<verification>
- AL 7 + LA 6 G5200V26 rows; geo_districts/offices untouched.
- AL BVAP direction confirmed at the blocking checkpoint.
- D-10 3-layer bar GREEN for AL and LA independently; severe differentials proven post-flip via the re-run smoke.
- AL-2 + LA-2/LA-6 re-pointed to state general; Edmonds in LA-6; 163 gate GREEN.
</verification>

<success_criteria>
AL + LA 2026 polygons imported as G5200V26, AL map direction confirmed correct, per-state D-10 bars pass, AL-2/LA-2/LA-6 surface on their general elections, the AL/LA severe-district differentials are proven post-flip, Edmonds re-verified in LA-6, and the Phase-163 gate asserts them positive — all GREEN, per-state-atomic.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
