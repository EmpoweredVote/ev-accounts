---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 07
type: execute
# wave 4 is DATE-GATE-driven (≥ 2026-08-04 SOS Hoskins certification), NOT a data dependency.
# The only true data deps are the Wave-1 code + harness (01, 02); MO is sequenced last purely
# because it must not execute before certification. See objective + WARNING-4 note.
wave: 4
depends_on: [164.1-01, 164.1-02]
files_modified:
  - backend/scripts/1641-mo-generate-polygon-import.mts
  - backend/migrations/1250_unwithhold_mo_2026_house_races.sql
  - backend/scripts/162-verify.sql
  - backend/scripts/162-coordinate-smoke.ts
  - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md
autonomous: false
requirements: [D-05, D-06, D-08, D-09, D-10, DELIV-1, DELIV-3]
must_haves:
  truths:
    - "This plan executes ONLY on or after 2026-08-04 (SOS Hoskins certification decision) — never before; wave 4 encodes this date-gate, not a data dependency"
    - "If the 2026 map holds (frozen new): every MO district (2901-2908) has a G5200V26 polygon, MO passes the D-10 3-layer bar, and MO-2..MO-6 (2902-2906) un-withhold onto the MO general election"
    - "In the map-holds branch, after the flip 1641-coordinate-smoke.ts (re-run read-only) auto-proves the MO severe-district differential"
    - "If the referendum qualifies (revert to old map): MO does ZERO polygon work, stays withheld, and the divert to Phase 167's MO cluster is documented"
    - "The Phase-162 MO-SEVERE gate assertion is flipped to positive ONLY in the map-holds branch"
    - "essentials.offices and essentials.geo_districts for MO (FIPS 29) are untouched (D-04) in either branch"
  artifacts:
    - path: "backend/scripts/1641-mo-generate-polygon-import.mts"
      provides: "MO 2026 polygon import (map-holds branch only)"
      contains: "G5200V26"
    - path: "backend/migrations/1250_unwithhold_mo_2026_house_races.sql"
      provides: "Guarded un-withhold UPDATE for MO 2902-2906 (map-holds branch only)"
      contains: "Polygon Pending"
  key_links:
    - from: "backend/migrations/1250_unwithhold_mo_2026_house_races.sql"
      to: "essentials.races.election_id → 'MO 2026 Statewide General'"
      via: "guarded UPDATE where election_id = MO Polygon Pending"
      pattern: "MO 2026 Statewide General"
---

<objective>
Resolve Missouri's slice, DATE-GATED to ≥ 2026-08-04 (SOS Hoskins' certification decision on the People-Not-Politicians referendum). Two pre-committed branches (D-06): if the 2026 map HOLDS, import MO polygons + pass the D-10 bar + un-withhold MO-2..MO-6 (2902-2906) inside 164.1; if the referendum QUALIFIES (map reverts to old boundaries), MO exits 164.1 entirely with zero polygon work and the divert to Phase 167's MO cluster is documented.

Purpose: Deliver D-05 (date-gate the whole MO slice) + D-06 (pre-committed revert branch). MO must NOT proceed early — importing new MO polygons before certification would move the currently-surfacing MO-1/7/8 onto legally-unsettled boundaries. MO does NOT block Phase 165 or the phase's main gate (Plan 06 completes without it).

WAVE-4 NOTE (checker WARNING 4): this plan's data dependencies are ONLY the Wave-1 code + harness (Plans 01, 02) — reflected in `depends_on`. It is assigned wave 4 (after Plans 03/04/05/06) purely to encode the ≥ 2026-08-04 date-gate sequencing, NOT because it consumes anything from Plans 03-06. It may in fact execute after Plan 06 closes the four-state phase; the wave number is a scheduling marker, not a data-flow edge.
Output: Either (map-holds) MO import + un-withhold + flipped 162 gate, or (revert) a documented divert to Phase 167 — plus STATE.md updated in both cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-PATTERNS.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-01-SUMMARY.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-02-SUMMARY.md

<interfaces>
<!-- MO date-gate: ≥ 2026-08-04 (SOS Hoskins certification; corrected from CONTEXT's ~Jul-28 per RESEARCH — Jul-27 is the county sub-deadline, not the trigger). Re-verify the exact date at execution time (MO's legal timeline has shifted before). -->
MO severe set (162): geo_ids 2902,2903,2904,2905,2906 (MO-2..MO-6). Currently-surfacing (do NOT disturb until certified): 2901, 2907, 2908.
Withheld marker: 'MO 2026 Congressional Redistricting - Polygon Pending' (dated 2026-03-24, MO Supreme Court date). Un-withhold target: 'MO 2026 Statewide General'.
Import (map-holds branch): MTFCC='G5200V26', SOURCE='mo_*_2026', geo_ids 2901-2908, OMIT geo_districts. Source per D-08 (official MO GIS / court-filed / RDH fallback) — confirm at execution time.
Revert branch (D-06): MO exits 164.1; old polygons already correct (zero polygon work); field re-resolution + race_candidates re-wire + un-withhold-on-existing-polygons all divert to Phase 167's MO cluster; withholding stays in place until 167 re-wires the field.
Migration numbering: the `1250_...` filename is a PLACEHOLDER. Plans 01/04/05 each take numbers ahead of this plan, and by the ≥ 2026-08-04 execution date parallel Phases 177/178 (and any other sessions) will very likely have advanced the high-water mark further. Re-run `ls backend/migrations | grep -E '^[0-9]' | sort | tail` immediately before authoring and use the next free number — do NOT trust 1250.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Certification-outcome decision gate (≥ 2026-08-04)</name>
  <files>.planning/workstreams/2026-us-house-candidate-coverage/STATE.md (branch outcome recorded)</files>
  <action>Do NOT execute this plan before 2026-08-04. Re-verify the exact SOS Hoskins certification date/outcome, then pause for the operator to select the D-06 branch: map-holds (proceed to Task 2 import + un-withhold) or revert (proceed to Task 3 zero-work exit + Phase-167 divert). Record the chosen branch + confirmed date.</action>
  <decision>Which D-06 branch does MO take, based on SOS Hoskins' certification decision?</decision>
  <context>
    Do NOT run this plan before 2026-08-04. Confirm the actual certification outcome (re-verify the exact date first — MO's legal timeline has shifted before). The referendum submitted 300k+ signatures; Hoskins' statutory certification deadline is 2026-08-04.
  </context>
  <options>
    <option id="map-holds">
      <name>Map HOLDS (frozen new 2026 boundaries)</name>
      <pros>2026 map is legally settled; proceed with import + un-withhold inside 164.1 (Task 2)</pros>
      <cons>None — this is the base case</cons>
    </option>
    <option id="revert">
      <name>Referendum QUALIFIES (map reverts to OLD boundaries)</name>
      <pros>Zero polygon work — old MO polygons are already correct</pros>
      <cons>MO exits 164.1; field re-resolution + un-withhold diverts to Phase 167; withholding stays in place (Task 3)</cons>
    </option>
  </options>
  <resume-signal>Select: map-holds (→ Task 2) or revert (→ Task 3). Confirm the certification date/outcome in your response.</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (map-holds branch): Import MO, pass D-10 bar, un-withhold 2902-2906, prove severe differential, flip 162 gate</name>
  <read_first>
    - backend/scripts/load-national-house-districts.ts (import primitives; geo_districts to OMIT)
    - backend/scripts/1641-verify.sql + backend/scripts/1641-coordinate-smoke.ts (Plan 02)
    - backend/scripts/162-verify.sql (MO-SEVERE block) + backend/scripts/162-coordinate-smoke.ts (MO negatives)
    - backend/migrations/1230_fix_edmonds_la5_to_la6_rekey.sql (guarded-UPDATE convention)
    - 164.1-RESEARCH.md "Un-Withhold Flip Surface" + Pitfall 5 (date) + 162-01/162-02 SUMMARYs
  </read_first>
  <action>
    ONLY if Task 1 = map-holds. Confirm the MO source (official MO GIS / court-filed shapefile; RDH fallback per D-08). Author backend/scripts/1641-mo-generate-polygon-import.mts (clone of load-national-house-districts.ts): geo_ids 2901-2908, MTFCC='G5200V26', SOURCE per source, OMIT geo_districts, provenance header, --dry-run first (expect 8), then real. Run 1641-verify.sql (MO/FIPS-29 Layer-1 + NOTOUCH checksums) + 1641-coordinate-smoke.ts (MO Layer-2 anchors + MO guaranteed non-severe Layer-3 differential via live paths; severe differential boundary-only pre-flip) — all layers must pass for MO independently (D-09).
    Re-check the live migration number FIRST (`ls backend/migrations | grep -E '^[0-9]' | sort | tail`; 1250 is a placeholder — Plans 01/04/05 + 177/178 have advanced it), then author a BEGIN/COMMIT-wrapped, guarded, idempotent migration: set election_id to 'MO 2026 Statewide General' WHERE district geo_id IN (2902,2903,2904,2905,2906) AND election_id = 'MO 2026 Congressional Redistricting - Polygon Pending'. Do NOT touch the surfacing 2901/2907/2908 and do NOT delete the 'Polygon Pending' marker. Apply to PROD.
    RE-RUN 1641-coordinate-smoke.ts (unedited; makes no committed writes — its D-11 RPC probe inserts a sentinel row inside an always-ROLLBACK'd transaction): now that MO's severe races surface, the script auto-detects them and proves the MO SEVERE-district differential (NEW severe race on elections + OLD rep on reps feed). It must exit 0.
    Flip the Phase-162 gate: 162-verify.sql MO-SEVERE block → assert 2902-2906 surfacing on the MO general; move MO entries from SEVERE_NEGATIVES to positive SAMPLES in 162-coordinate-smoke.ts; update PASS text + summary. Re-run 162 gate — GREEN. Update STATE.md: MO complete inside 164.1.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.races r JOIN essentials.offices o ON o.id=r.office_id JOIN essentials.districts d ON d.id=o.district_id JOIN essentials.elections e ON e.id=r.election_id WHERE d.geo_id IN ('2902','2903','2904','2905','2906') AND d.district_type='NATIONAL_LOWER' AND e.name='MO 2026 Statewide General'" | grep -qx 5 && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='29'" | grep -qx 8 && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/1641-verify.sql && node --import tsx scripts/1641-coordinate-smoke.ts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/162-verify.sql && node --import tsx scripts/162-coordinate-smoke.ts && echo MO_UNWITHHELD_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - 8 MO G5200V26 rows (state='29', 2901-2908); geo_districts unchanged for FIPS 29
    - MO D-10 3-layer bar GREEN (1641-verify.sql + 1641-coordinate-smoke.ts MO blocks)
    - 1641-coordinate-smoke.ts (re-run, unedited) exits 0 proving the MO severe-district differential now surfaces a race
    - MO-2..MO-6 (2902-2906) → 'MO 2026 Statewide General'; 2901/2907/2908 untouched; marker not deleted
    - Migration guarded on OLD election_id, idempotent, using the live next-free number
    - 162-verify.sql + 162-coordinate-smoke.ts GREEN with MO now surfacing
  </acceptance_criteria>
  <done>MO polygons imported, D-10 bar passed, 2902-2906 surfacing, MO severe differential proven post-flip, 162 gate flipped positive and GREEN.</done>
</task>

<task type="auto">
  <name>Task 3 (revert branch): Document MO exit + Phase-167 divert</name>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/STATE.md (Deferred Items + Operator Next Steps)
    - .planning/workstreams/2026-us-house-candidate-coverage/ROADMAP.md §Phase 167 (MO cluster)
    - 164.1-CONTEXT.md D-06
  </read_first>
  <action>
    ONLY if Task 1 = revert. Perform ZERO polygon work (old MO polygons remain correct). Document in STATE.md (Deferred Items + Operator Next Steps) and a short note in this plan's SUMMARY that: MO exits 164.1; the MO withholding (2902-2906 on 'MO 2026 Congressional Redistricting - Polygon Pending') STAYS in place; the real work — MO field re-resolution vs OLD districts, race_candidates re-wire, then un-withhold on the existing polygons — diverts to Phase 167's MO cluster (already a mandatory re-pull). Do NOT flip the 162 gate (MO stays withheld). Confirm no MO G5200V26 rows were created.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.geofence_boundaries WHERE mtfcc='G5200V26' AND state='29'" | grep -qx 0 && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/162-verify.sql && grep -qi "Phase 167" .planning/workstreams/2026-us-house-candidate-coverage/STATE.md && echo MO_REVERT_DOCUMENTED</automated>
  </verify>
  <acceptance_criteria>
    - Zero MO G5200V26 rows exist (no polygon work performed)
    - 162-verify.sql exits 0 with MO (2902-2906) STILL withheld (gate NOT flipped)
    - STATE.md documents the MO exit + Phase-167 divert (field re-resolution + un-withhold)
  </acceptance_criteria>
  <done>MO revert branch executed: zero polygon work, withholding intact, Phase-167 divert documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| certification decision → branch | Executing early / wrong branch moves surfacing MO districts onto unsettled boundaries |
| MO shapefile source → import | Untrusted geometry (map-holds branch only) |
| migration → prod races | Wrong-row UPDATE could surface the wrong MO districts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-18 | Tampering (premature action) | Importing MO before certification | mitigate | Blocking decision checkpoint (Task 1) gated to ≥ 2026-08-04; date re-verified at execution |
| T-1641-19 | Tampering | Un-withhold UPDATE disturbs surfacing 2901/2907/2908 or hits wrong races | mitigate | Guard on OLD election_id + explicit geo_id set (2902-2906 only); BEGIN/COMMIT; idempotent; verified count = 5 |
| T-1641-20 | Tampering / integrity | Mis-imported MO geometry | mitigate | D-10 Layer-1 tiling + area-ratio must pass before flip; ST_* normalization |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pg/shapefile/adm-zip already present |
</threat_model>

<verification>
- Task 1 decision gated ≥ 2026-08-04.
- map-holds: 8 MO rows, D-10 bar GREEN, 2902-2906 surfacing, MO severe differential proven post-flip, 162 gate flipped GREEN.
- revert: 0 MO rows, 162 still withheld, Phase-167 divert documented.
</verification>

<success_criteria>
MO is resolved per the certified outcome: either fully imported + un-withheld inside 164.1 (map-holds) with the 162 gate flipped GREEN and the severe differential proven, or cleanly exited with zero polygon work and a documented Phase-167 divert (revert) — offices/geo_districts untouched, surfacing MO districts undisturbed, in both branches.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-07-SUMMARY.md` when done.
</output>
</content>
</invoke>
