---
phase: 164.1
slug: cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-06
---

# Phase 164.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | psql gate scripts (`-v ON_ERROR_STOP=1`) + coordinate-smoke curl scripts (161/162/163 harness pattern) + TypeScript build |
| **Config file** | none — gates are standalone re-runnable scripts per phase convention |
| **Quick run command** | `cd /c/EV-Accounts/backend && npx tsc --noEmit` |
| **Full suite command** | phase gate SQL (164.1-verify.sql) + coordinate smoke script against PROD |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd /c/EV-Accounts/backend && npx tsc --noEmit` (code tasks) or the relevant per-state verify SQL block (data tasks)
- **After every plan wave:** Run the phase gate SQL + coordinate smoke for states imported so far
- **Before `/gsd:verify-work`:** Full gate must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (planner fills per task) | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — the 161/162/163 gate + smoke harness is extended, not created.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Render deploy health | D-02 dual-map code live | Deploy hook is fire-and-forget | curl the deploy hook, poll /health, then run post-deploy differential-point smoke |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
