---
phase: 164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/geoIdGuard.ts
  - backend/src/lib/essentialsService.ts
  - backend/src/lib/electionService.ts
  - backend/src/routes/essentials.ts
  - backend/migrations/1246_resolve_congressional_2026_rpc.sql
autonomous: true
# NOTE: 4 tasks (borderline vs the 2-3 guideline) is intentional and orchestrator-approved (checker WARNING 3):
# the tasks are cohesive (the full dual-map opt-in code surface + its fail-safe deploy) and files_modified
# stays within range; splitting would divorce the deploy's fail-safe proof (Task 4) from the code it proves.
requirements: [D-01, D-02, D-11, DELIV-2]
must_haves:
  truths:
    - "With zero G5200V26 rows present, /elections coordinate resolution is byte-identical to pre-deploy (fail-safe default proven by existing 161/162/163 coordinate smokes staying GREEN)"
    - "The reps-feed MTFCC catch-all no longer silently matches a G5200V26 row (explicitly excluded in both catch-all lists)"
    - "getElectionsByCoordinate prefers a G5200V26 boundary for a geo_id when one exists, else falls back to the existing G5200 branch"
    - "Logged-in Connected-tier /elections (Path 1) re-resolves the congressional district LIVE against G5200V26 from the user's stored coordinate when the cached congressional_geo_id belongs to one of the 5 refreshed states — so a differential-zone Connected user sees their NEW district's race (D-11), with NO write to connected_profiles / user_districts / geo_districts"
    - "The dual-map code is deployed and the Render service /health responds 200"
  artifacts:
    - path: "backend/src/lib/geoIdGuard.ts"
      provides: "MTFCC_DISTRICT_TYPE_GUARD catch-all excluding G5200V26"
      contains: "G5200V26"
    - path: "backend/src/lib/essentialsService.ts"
      provides: "Inline reps-feed catch-all excluding G5200V26"
      contains: "G5200V26"
    - path: "backend/src/lib/electionService.ts"
      provides: "Vintage-preference LATERAL join in getElectionsByCoordinate"
      contains: "G5200V26"
    - path: "backend/migrations/1246_resolve_congressional_2026_rpc.sql"
      provides: "Read-only SECURITY DEFINER RPC that decrypts the stored coord and resolves the congressional geo_id against mtfcc='G5200V26' for the 5 states only (NULL otherwise)"
      contains: "G5200V26"
    - path: "backend/src/routes/essentials.ts"
      provides: "Path-1 Connected-tier live fallback: substitutes the G5200V26-resolved congressional geo_id when the cached one is in a refreshed state"
      contains: "resolve_congressional_2026"
  key_links:
    - from: "backend/src/lib/electionService.ts getElectionsByCoordinate"
      to: "essentials.geofence_boundaries (mtfcc='G5200V26' preferred)"
      via: "JOIN LATERAL vintage-preference subquery"
      pattern: "G5200V26"
    - from: "backend/src/lib/geoIdGuard.ts MTFCC_DISTRICT_TYPE_GUARD"
      to: "reps-feed district match"
      via: "NOT IN (...) exclusion list"
      pattern: "G5200V26"
    - from: "backend/src/routes/essentials.ts /elections/me Path 1"
      to: "resolve_congressional_2026 RPC (live G5200V26 resolution)"
      via: "corrected-geo_id substitution before getElectionsByGeoIds"
      pattern: "resolve_congressional_2026"
---

<objective>
Ship the dual-map opt-in backend code: a distinct `mtfcc` value (`'G5200V26'`) is the D-01 vintage discriminator, `/elections` coordinate resolution opts in to prefer it, the two fail-dangerous MTFCC catch-all clauses explicitly exclude it so the reps feed and every other consumer stay on the current (`'G5200'`) vintage, AND the logged-in Connected-tier `/elections` path (Path 1) re-resolves the congressional district live against `G5200V26` for the 5 refreshed states so it honors the dual-map promise for that tier too (D-11). Then deploy to Render.

Purpose: This is the milestone's first backend-code change and the foundation the entire phase depends on. Deploying it FIRST is fail-safe: with zero `G5200V26` rows in the DB yet, behavior is byte-identical to today; each state's elections resolution then flips automatically as its `G5200V26` rows land in later plans. D-11 (user-approved 2026-07-06) closes the Connected-tier gap in-phase rather than deferring it past the Nov-2026 general.
Output: Three modified `src/lib` files + one modified route + one new read-only RPC migration + a live Render deploy proven fail-safe by the existing coordinate smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-CONTEXT.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-RESEARCH.md
@.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-PATTERNS.md

<interfaces>
<!-- Exact current code targets (verified live this planning session, 2026-07-07). Line numbers may drift; grep the tokens below rather than trusting line numbers. -->

backend/src/lib/geoIdGuard.ts line 27 — end of MTFCC_DISTRICT_TYPE_GUARD:
  `OR (gp.mtfcc NOT IN ('G5210','G5220','G5200','G4020','G4040','G4110','G4120','G5400','G5410','G5420') AND gp.mtfcc NOT LIKE 'X%')`

backend/src/lib/essentialsService.ts line 652 — byte-for-byte inline duplicate of the same catch-all (reps feed, getRepresentativesByAddress):
  `OR (gb.mtfcc NOT IN ('G5210','G5220','G5200','G4020','G4040','G4110','G4120','G5400','G5410','G5420')`
  `    AND gb.mtfcc NOT LIKE 'X%')`

backend/src/lib/electionService.ts:
  - line ~284 `geofenceQueryText` — getElectionsByCoordinate Part A; line ~294 the plain `JOIN essentials.geofence_boundaries gb ON gb.geo_id = d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc = '' OR gb.mtfcc = d.mtfcc)` is the opt-in target. THIS is the only geometry consumer in electionService.
  - line ~61 `fetchDistrictRaceRows` — matches races by `gp.geo_id = d.geo_id AND ${MTFCC_DISTRICT_TYPE_GUARD}`. It has NO geofence/geometry join; it keys on district geo_id + mtfcc→district_type. It therefore needs NO vintage-preference join — feeding it the CORRECT congressional geo_id (Task 3) makes it surface the right race.
  - lines 24-27 `ELECTION_VISIBILITY_WINDOW` — the withholding lever; DO NOT touch (un-withholding is a pure data flip in later plans).

backend/src/routes/essentials.ts — GET /api/elections/me:
  - Path 1 (~line 778): `if (j && (j.congressional_geo_id || ...))` → builds geoPairs from stored slots via `electionGeoPairsFromSlots(j)` (which tags congressional_geo_id with mtfcc 'G5200') and calls `getElectionsByGeoIds(pairs, j.jurisdiction_state)`. THIS is where a differential-zone Connected user gets their OLD district's race today.
  - Path 1.5 (~line 793): coords present but no stored geo_ids → calls `resolve_user_jurisdiction` RPC (hardcoded mtfcc='G5200', so it also cannot see G5200V26) then getElectionsByGeoIds. The stored/refreshed slot query already selects `(encrypted_lat IS NOT NULL) AS has_coords` (~line 772).
  - `electionGeoPairsFromSlots` (~line 29): the [congressional_geo_id, 'G5200'] slot is the one to correct.

connect.connected_profiles: stores `encrypted_lat`/`encrypted_lng` (never plaintext to the app). Decryption happens only inside a SECURITY DEFINER RPC (`resolve_user_jurisdiction` decrypts internally; hardcodes mtfcc='G5200'). D-11's live fallback needs a NEW read-only SECURITY DEFINER RPC that decrypts the coord and ST_Covers against G5200V26 — it must NOT be resolve_user_jurisdiction (that stays G5200 for the reps feed per D-04) and must NOT write any table.

Affected-state FIPS: TN=47, MO=29, AL=01, LA=22, UT=49.

NOTE: backend/src/lib/essentialsService.ts is already `M` (modified, uncommitted) in git from concurrent work — READ its current state before editing; the catch-all at line 652 is confirmed still intact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude G5200V26 from both MTFCC catch-all clauses</name>
  <read_first>
    - backend/src/lib/geoIdGuard.ts (full file, 35 lines — read current state)
    - backend/src/lib/essentialsService.ts (read the region around the inline guard, grep `NOT IN ('G5210'`)
    - 164.1-RESEARCH.md "Common Pitfalls" Pitfall 1 + "D-01 Vintage Discriminator Mechanism" (why this exclusion is the actual fail-dangerous trap)
    - 164.1-PATTERNS.md sections for geoIdGuard.ts and essentialsService.ts inline duplicate
  </read_first>
  <action>
    In both catch-all clauses, add the literal `'G5200V26'` to the `NOT IN (...)` list so the new vintage marker is EXPLICITLY excluded from the "unknown MTFCC matches any district_type" fallback (making its exclusion a documented decision, not an accident of the string not colliding).
    - geoIdGuard.ts: append `,'G5200V26'` inside the `NOT IN ('G5210',...,'G5420')` list on the guard's last clause. Add one comment sentence above/beside it noting 2026-vintage congressional boundaries are intentionally excluded so only the elections opt-in join (electionService.ts) may reach them.
    - essentialsService.ts: apply the identical change to the byte-for-byte inline duplicate (reps feed, getRepresentativesByAddress) — this is an EXCLUSION patch, NOT an opt-in join; the reps feed must stay on G5200 per D-02.
    Do NOT change the `NOT LIKE 'X%'` portion or any other guard branch. Keep both lists identical to each other. (De-duplicating the inline copy by importing MTFCC_DISTRICT_TYPE_GUARD is optional per RESEARCH's Don't-Hand-Roll note; not required for this plan.)
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -c "G5200V26" src/lib/geoIdGuard.ts | grep -qx 1 && grep -c "G5200V26" src/lib/essentialsService.ts | grep -qx 1 && npx tsc --noEmit && echo GUARDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "G5200V26" backend/src/lib/geoIdGuard.ts` returns exactly the `NOT IN (...)` clause line
    - `grep "G5200V26" backend/src/lib/essentialsService.ts` returns exactly the inline catch-all line (reps feed unchanged otherwise)
    - `npx tsc --noEmit` exits 0
    - No other guard branch or the `NOT LIKE 'X%'` clause is altered (diff shows only the two `NOT IN` list additions + comments)
  </acceptance_criteria>
  <done>Both catch-all lists include `'G5200V26'`; tsc passes; reps-feed guard otherwise unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the vintage-preference opt-in join to getElectionsByCoordinate</name>
  <read_first>
    - backend/src/lib/electionService.ts (read getElectionsByCoordinate Part A `geofenceQueryText` ~lines 281-330 AND fetchDistrictRaceRows ~lines 55-82 — confirm fetchDistrictRaceRows has NO geometry join)
    - 164.1-RESEARCH.md "D-01 ... Required opt-in / exclusion patch" (the exact LATERAL shape) + "D-02 Consumer Audit" row for electionService
    - 164.1-PATTERNS.md "electionService.ts ... getElectionsByCoordinate Part A" (the exact before/after SQL)
  </read_first>
  <action>
    Replace the plain `JOIN essentials.geofence_boundaries gb ON gb.geo_id = d.geo_id AND (d.mtfcc IS NULL OR d.mtfcc = '' OR gb.mtfcc = d.mtfcc)` in `getElectionsByCoordinate` Part A (`geofenceQueryText`) with a `JOIN LATERAL` that prefers the `mtfcc = 'G5200V26'` geometry for that `geo_id` when `d.district_type = 'NATIONAL_LOWER'`, else falls back (via `NOT EXISTS` on a G5200V26 row) to the existing `gb.mtfcc = d.mtfcc` branch — use the exact LATERAL/UNION ALL/LIMIT 1 shape in RESEARCH.md's D-01 section and PATTERNS.md. Keep the `WHERE gb.geometry IS NOT NULL AND public.ST_Covers(...) AND ${ELECTION_VISIBILITY_WINDOW}` tail and the parameterized `$1::float8, $2::float8` coordinates EXACTLY (never string-interpolate coordinates — V5 input validation).
    Do NOT modify `fetchDistrictRaceRows`: it matches races by district geo_id + the MTFCC guard and has no geometry join, so there is no vintage-preference join to add there. Connected-tier correctness comes from Task 3 feeding it the CORRECT congressional geo_id — not from a change here. (Leave Part B's state-lookup query as-is; it only derives the state code.)
    Any of the 33 non-affected states (no G5200V26 row for their geo_ids) must fall straight through to the unchanged G5200 branch — zero blast radius.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -q "G5200V26" src/lib/electionService.ts && grep -q "JOIN LATERAL" src/lib/electionService.ts && grep -q "float8" src/lib/electionService.ts && npx tsc --noEmit && echo ELECTIONSVC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `getElectionsByCoordinate` uses a `JOIN LATERAL` with a `mtfcc = 'G5200V26'` branch UNION ALL a fallback guarded by `NOT EXISTS (... mtfcc = 'G5200V26')`
    - Parameterized `$1::float8, $2::float8` coordinates preserved (no string interpolation of lat/lng)
    - `ELECTION_VISIBILITY_WINDOW` and `RACE_SELECT` usage unchanged
    - `fetchDistrictRaceRows` is UNCHANGED (no geometry join exists there)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Elections coordinate path prefers G5200V26 when present; tsc passes; other states unaffected; fetchDistrictRaceRows untouched.</done>
</task>

<task type="auto">
  <name>Task 3: D-11 Connected-tier Path-1 live fallback (RPC + route substitution)</name>
  <read_first>
    - backend/src/routes/essentials.ts (GET /api/elections/me — Path 1 ~778-791, Path 1.5 ~793-843, the profile-slot SELECT ~768-775 with `has_coords`, and `electionGeoPairsFromSlots` ~29-45)
    - the definition of `resolve_user_jurisdiction` (fetch live via `psql "$DATABASE_URL" -c "\sf connect.resolve_user_jurisdiction"` or pg_get_functiondef) — clone its decryption preamble EXACTLY -- Vault key fetch ('location_encryption_key' from vault.decrypted_secrets), extensions.pgp_sym_decrypt_bytea(encrypted_lat, v_key) wrapped in convert_from(..., 'UTF8')::float8, then the point build via ST_SetSRID(ST_MakePoint(v_lng, v_lat), 4326), INCLUDING the exact schema-qualification the live RPC uses for PostGIS + pgcrypto under SET search_path='' (the analog qualifies BOTH as extensions.*, NOT public.* -- do NOT assume public.; read the live RPC and match whatever resolves). Note the analog also filters location_consent=true. Do NOT modify resolve_user_jurisdiction itself
    - backend/migrations/1230_fix_edmonds_la5_to_la6_rekey.sql (BEGIN/COMMIT + idempotent-guard migration convention; CREATE OR REPLACE FUNCTION is idempotent by nature)
    - 164.1-RESEARCH.md "Open Questions" #1 (the suggested fix shape) + "Common Pitfalls" Pitfall 3 + "D-02 Consumer Audit" Path-1 row
    - 164.1-CONTEXT.md D-11 (the user-approved in-phase fix) + D-04 (no cache mutation)
  </read_first>
  <action>
    Implement the D-11 read-path live fallback so a logged-in Connected-tier user in a differential zone of the 5 refreshed states sees their NEW congressional district's race — with NO mutation of connected_profiles / connect.user_districts / essentials.geo_districts (D-04 holds; this is a read fallback, not a cache re-resolution).

    (a) Re-check the live migration high-water mark (`ls backend/migrations | grep -E '^[0-9]' | sort | tail`) — this plan is Wave 1 and takes the FIRST free number; the `1246_...` filename is provisional. Author `backend/migrations/{next}_resolve_congressional_2026_rpc.sql`: a `CREATE OR REPLACE FUNCTION connect.resolve_congressional_2026(p_user_id uuid) RETURNS text` SECURITY DEFINER function, `SET search_path=''`, fully-qualified refs, that (1) decrypts the user's stored coordinate using the SAME decryption preamble as resolve_user_jurisdiction, (2) runs `ST_Covers` (schema-qualified exactly as the live analog qualifies its PostGIS calls under `search_path=''` -- read live, the analog uses `extensions.ST_*`) of the decrypted point against `essentials.geofence_boundaries` rows WHERE `mtfcc='G5200V26'` -- NOTE this is a DIFFERENT resolution table than the analog's `inform.district_boundaries`, so only the decrypt PREAMBLE is cloned, not the resolution body -- joined to `essentials.districts d ON d.geo_id = gb.geo_id AND d.district_type='NATIONAL_LOWER'`, and (3) returns the matching `geo_id` (the NEW congressional district) or NULL when the point is not covered by any G5200V26 boundary (i.e. the user is not in one of the 5 refreshed states — zero blast radius). Read-only: no INSERT/UPDATE/DELETE. Grant EXECUTE to the app role that resolve_user_jurisdiction is granted to. Apply to PROD.

    (b) In `backend/src/routes/essentials.ts` GET /api/elections/me Path 1 (and the analogous post-resolve block in Path 1.5): after building the slots but before calling `getElectionsByGeoIds`, when the profile has coords (`has_coords`) AND its `congressional_geo_id` belongs to one of the 5 refreshed states (test by state FIPS prefix of the geo_id: 47/29/01/22/49 → derive from the leading two digits of the 4-digit geo_id, i.e. TN=47xx, MO=29xx, AL=01xx, LA=22xx, UT=49xx), call `connect.resolve_congressional_2026(user_id)` via `adminRpc`. If it returns a non-null geo_id, SUBSTITUTE that value into the congressional slot (keep mtfcc 'G5200' when building the pair — fetchDistrictRaceRows keys on the corrected geo_id + the NATIONAL_LOWER guard branch, which is the explicit `gp.mtfcc = 'G5200'` branch) before calling getElectionsByGeoIds. If it returns null (or errors), fall through to the existing cached-geo_id behavior unchanged. Add a code comment referencing D-11 and that this is a read-only fallback closed by the Jan-2027 promotion phase.
    Never string-interpolate user input into SQL; use parameterized `adminRpc`/`pool.query`.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && grep -q "resolve_congressional_2026" src/routes/essentials.ts && ls migrations | grep -q "resolve_congressional_2026" && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='connect' AND p.proname='resolve_congressional_2026'" | grep -qx 1 && npx tsc --noEmit && echo CONNECTED_FALLBACK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `connect.resolve_congressional_2026(uuid)` exists on PROD: SECURITY DEFINER, `SET search_path=''`, read-only, returns the G5200V26-covering NATIONAL_LOWER geo_id or NULL
    - The RPC returns NULL for a coordinate not covered by any G5200V26 boundary (verified against the current DB where zero G5200V26 rows exist → returns NULL for every user; this is the fail-safe no-op today)
    - Path 1 (and Path 1.5) substitute the RPC-resolved geo_id into the congressional slot ONLY when the cached congressional_geo_id is in FIPS 47/29/01/22/49 and coords exist; other states/paths unchanged
    - No write to connected_profiles / user_districts / geo_districts introduced; resolve_user_jurisdiction unmodified
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Connected-tier Path 1 re-resolves the congressional district live against G5200V26 for the 5 states via a read-only RPC; no cache mutation; no-op today (returns NULL with zero G5200V26 rows).</done>
</task>

<task type="auto">
  <name>Task 4: Build, deploy to Render, and prove fail-safe (existing smokes stay GREEN)</name>
  <read_first>
    - $HOME/.claude/projects/C--EV-Accounts/memory/reference_render_deploy.md (deploy hook usage)
    - backend/scripts/161-coordinate-smoke.ts, backend/scripts/162-coordinate-smoke.ts, backend/scripts/163-coordinate-smoke.ts (the regression smokes to re-run)
    - 164.1-RESEARCH.md "Environment Availability" (RENDER_DEPLOY_HOOK presence unverified — fallback below)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && npx tsc --noEmit` to confirm a clean build. Trigger the Render deploy via `curl` on `$RENDER_DEPLOY_HOOK` (value in backend/.env or the operator's env per reference_render_deploy.md), then poll the service `/health` endpoint until it returns 200 (deploy complete). If `RENDER_DEPLOY_HOOK` is unset/unreachable, STOP and create a `checkpoint:human-action` asking the operator to trigger the deploy from the Render dashboard, then resume the /health poll.
    After the deploy is live, re-run all three existing coordinate smokes (161/162/163) against PROD. They MUST stay GREEN unchanged — with zero G5200V26 rows in the DB, the new LATERAL branch never matches AND `resolve_congressional_2026` always returns NULL, proving the deploy is fail-safe (no behavior change until polygons land). This is the deploy's acceptance proof. Also spot-confirm the Connected-tier fallback is a no-op today: `resolve_congressional_2026` returns NULL for a sample user with coords (or, if no Connected test user is handy, confirm the RPC returns NULL for an arbitrary uuid — no G5200V26 rows means no match).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && npx tsc --noEmit && set -a && source .env && set +a && node --import tsx scripts/161-coordinate-smoke.ts && node --import tsx scripts/163-coordinate-smoke.ts && echo DEPLOY_FAILSAFE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Render `/health` returns HTTP 200 after the deploy trigger
    - 161-coordinate-smoke.ts exits 0 (GREEN) — unchanged from pre-deploy
    - 163-coordinate-smoke.ts exits 0 (GREEN) — unchanged from pre-deploy (AL/LA severe still correctly withheld)
    - 162-coordinate-smoke.ts exits 0 (GREEN) if run — MO severe still correctly withheld
    - `resolve_congressional_2026` returns NULL today (no G5200V26 rows) — Connected-tier fallback is a proven no-op until polygons land
  </acceptance_criteria>
  <done>Code + RPC deployed to Render, /health 200, all three prior coordinate smokes GREEN, Connected-tier fallback a proven no-op — dual-map opt-in proven fail-safe with no polygons yet present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/elections | Untrusted lat/lng crosses into the new LATERAL geofence join |
| logged-in Connected user → /api/elections/me Path 1 | Cached (stale) congressional_geo_id vs live G5200V26 re-resolution |
| resolve_congressional_2026 (SECURITY DEFINER) → encrypted coordinate | Decrypts a user's stored location server-side |
| deploy → prod | Code + RPC change alters live elections resolution for all users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1641-01 | Elevation of Privilege / Tampering | MTFCC catch-all silently adopting G5200V26 in the reps feed | mitigate | Task 1 explicitly adds `'G5200V26'` to both `NOT IN` lists; reps feed can never match the new vintage |
| T-1641-02 | Injection (SQLi) | New LATERAL join in electionService + RPC | mitigate | Preserve parameterized `$1::float8, $2::float8`; RPC takes a typed uuid param; no string interpolation (V5) |
| T-1641-03 | Denial of Service / wrong-result | Deploy breaks live elections resolution | mitigate | Fail-safe default (no G5200V26 rows yet); Task 4 re-runs 161/162/163 smokes as regression proof; /health gate |
| T-1641-21 | Information Disclosure | resolve_congressional_2026 decrypts a user's coordinate | mitigate | Read-only SECURITY DEFINER with `SET search_path=''`, fully-qualified refs, returns ONLY a district geo_id (never the coordinate); EXECUTE granted only to the same app role as resolve_user_jurisdiction |
| T-1641-22 | Tampering (D-04 breach) | Fallback accidentally writes connected_profiles / user_districts | mitigate | RPC is read-only (no DML); route substitution mutates only the in-memory geoPairs; acceptance asserts no cache write |
| T-1641-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — dependency set unchanged (pg/shapefile/adm-zip already present); no supply-chain surface |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Render `/health` 200 post-deploy.
- 161/162/163 coordinate smokes GREEN post-deploy (fail-safe proof).
- `resolve_congressional_2026` exists, is read-only, returns NULL today.
- `grep G5200V26` present in geoIdGuard.ts / essentialsService.ts / electionService.ts / the RPC migration; `resolve_congressional_2026` present in routes/essentials.ts.
</verification>

<success_criteria>
Dual-map opt-in code deployed to PROD; with zero G5200V26 rows present, all existing coordinate smokes remain GREEN (byte-identical behavior); both catch-all clauses exclude G5200V26; electionService prefers G5200V26 when present; the Connected-tier Path-1 live fallback (D-11) is in place as a read-only, no-cache-mutation resolution that is a proven no-op until polygons land.
</success_criteria>

<output>
Create `.planning/phases/164.1-cross-state-district-polygon-refresh-dual-map-design-tn-mo-a/164.1-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
