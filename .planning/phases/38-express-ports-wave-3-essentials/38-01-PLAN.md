---
phase: 38-express-ports-wave-3-essentials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/geocodingService.ts
  - backend/src/lib/env.ts
  - backend/src/routes/connect.ts
autonomous: true

must_haves:
  truths:
    - "Census Geocoder returns lat/lng for a valid US address"
    - "Geocoder returns ADDRESS_NOT_FOUND for nonsense addresses"
    - "Geocoder returns GEOCODER_UNAVAILABLE on timeout"
    - "PO Box addresses are still rejected before any HTTP call"
    - "Server starts without GOOGLE_MAPS_API_KEY in environment"
    - "The exact column structure of essentials.geofence_boundaries, essentials.districts, and essentials.offices is documented in the SUMMARY"
  artifacts:
    - path: "backend/src/lib/geocodingService.ts"
      provides: "Census Geocoder with Redis cache, PO Box check, 5s timeout"
      exports: ["geocodeAddress", "GeocodingError", "GeocodingErrorCode"]
    - path: "backend/src/lib/env.ts"
      provides: "GOOGLE_MAPS_API_KEY as optional (not required)"
  key_links:
    - from: "backend/src/lib/geocodingService.ts"
      to: "https://geocoding.geo.census.gov/geocoder/locations/onelineaddress"
      via: "fetch with AbortController 5s timeout"
      pattern: "geocoding\\.geo\\.census\\.gov"
    - from: "backend/src/lib/geocodingService.ts"
      to: "backend/src/lib/cache.ts"
      via: "cache.get/set with 24hr TTL"
      pattern: "cache\\.(get|set)"
    - from: "backend/src/routes/connect.ts"
      to: "backend/src/lib/geocodingService.ts"
      via: "geocodeAddress import"
      pattern: "geocodeAddress"
---

<objective>
Investigate essentials schema join paths, rewrite geocodingService.ts to use Census Geocoder, and make GOOGLE_MAPS_API_KEY optional in env.ts.

Purpose: Unblocks all subsequent plans. The schema investigation reveals the PostGIS join path needed for address-search. The geocoder rewrite eliminates the Google Maps dependency. The env.ts change prevents server crash when GOOGLE_MAPS_API_KEY is absent.

Output: Rewritten geocodingService.ts (Census Geocoder + Redis cache), updated env.ts, documented schema column structure in SUMMARY.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-express-ports-wave-3-essentials/38-CONTEXT.md
@.planning/phases/38-express-ports-wave-3-essentials/38-RESEARCH.md
@backend/src/lib/geocodingService.ts
@backend/src/lib/env.ts
@backend/src/lib/cache.ts
@backend/src/routes/connect.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate essentials schema join paths</name>
  <files>No files modified — investigation only, results go in SUMMARY</files>
  <action>
Query the live database to discover the exact column structure needed for PostGIS address-search. Run these queries via pool.query() or Supabase MCP:

1. `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema = 'essentials' AND table_name = 'geofence_boundaries' ORDER BY ordinal_position`
2. `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema = 'essentials' AND table_name = 'districts' ORDER BY ordinal_position`
3. `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema = 'essentials' AND table_name = 'offices' ORDER BY ordinal_position`
4. `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema = 'essentials' AND table_name = 'politicians' ORDER BY ordinal_position`
5. Check FK relationships: `SELECT tc.table_name, kcu.column_name, ccu.table_name AS foreign_table, ccu.column_name AS foreign_column FROM information_schema.table_constraints tc JOIN information_schema.key_column_usage kcu ON tc.constraint_name = kcu.constraint_name JOIN information_schema.constraint_column_usage ccu ON tc.constraint_name = ccu.constraint_name WHERE tc.table_schema = 'essentials' AND tc.constraint_type = 'FOREIGN KEY' AND tc.table_name IN ('geofence_boundaries', 'districts', 'offices', 'politicians')`
6. Grab 2-3 sample rows from geofence_boundaries (just non-geom columns): `SELECT * FROM essentials.geofence_boundaries LIMIT 3` (will show column values)

Record ALL findings in the SUMMARY. This data is a hard prerequisite for Plan 02's address-search SQL.

Key questions to answer:
- What column in geofence_boundaries links to districts?
- What column in districts/offices links to politicians?
- What is the complete join path from geofence_boundaries -> politicians?
- Does geofence_boundaries have a district_type or mtfcc column?
  </action>
  <verify>All 6 queries return results. The join path from geofence_boundaries to politicians is documented.</verify>
  <done>Complete column inventory for geofence_boundaries, districts, offices, and politicians tables is captured in SUMMARY with the exact join path for PostGIS address-search.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite geocodingService.ts + update env.ts</name>
  <files>backend/src/lib/geocodingService.ts, backend/src/lib/env.ts, backend/src/routes/connect.ts</files>
  <action>
**geocodingService.ts — complete rewrite:**

1. Remove all Google Maps code (GoogleGeocodeResponse type, Google API URL, API key usage, LOW_CONFIDENCE error code).

2. Update GeocodingErrorCode type:
   - Keep: 'PO_BOX_REJECTED', 'ADDRESS_NOT_FOUND'
   - Add: 'GEOCODER_UNAVAILABLE' (replaces both 'GEOCODING_API_ERROR' and 'LOW_CONFIDENCE')
   - Remove: 'LOW_CONFIDENCE', 'GEOCODING_API_ERROR'

3. Keep the GeocodingError class and PO_BOX_PATTERN unchanged.

4. Rewrite geocodeAddress function:
   - Keep PO Box check as first step (before any network call)
   - Add Redis cache check: `cache.get<{ lat: number; lng: number }>(`geocode:v1:${address.toLowerCase().trim().replace(/\s+/g, ' ')}`)`. If cached, return immediately.
   - Build Census Geocoder URL: `https://geocoding.geo.census.gov/geocoder/locations/onelineaddress` with params: address, benchmark=Public_AR_Current, format=json
   - Use AbortController with 5-second timeout. On abort, throw GeocodingError('GEOCODER_UNAVAILABLE', 'Address lookup temporarily unavailable.')
   - Parse response: if `result.addressMatches` is empty array, throw GeocodingError('ADDRESS_NOT_FOUND', "We couldn't find that address. Please double-check and try again.")
   - Extract coordinates: `const lng = matches[0].coordinates.x; const lat = matches[0].coordinates.y;` — CRITICAL: x=longitude, y=latitude. Comment this clearly.
   - Cache result: `cache.set(cacheKey, { lat, lng }, 86400)` (24hr TTL)
   - Return { lat, lng }
   - Catch fetch errors (network, non-200 status): throw GeocodingError('GEOCODER_UNAVAILABLE', 'Address lookup temporarily unavailable.')

5. Import cache: `import { cache } from './cache.js';`

6. Do NOT import env (no longer needs GOOGLE_MAPS_API_KEY).

**env.ts — make Google Maps key optional:**

Change line 14 from:
`GOOGLE_MAPS_API_KEY: z.string().min(1),`
to:
`GOOGLE_MAPS_API_KEY: z.string().optional(),`

Update the comment above it to note it's deprecated (Census Geocoder replaced it in Phase 38).

**connect.ts — update error handling:**

The existing connect.ts `set-location` route handler catches `GeocodingError` and checks for `'LOW_CONFIDENCE'`. Update:
- Remove the `err.code === 'LOW_CONFIDENCE'` check (no longer exists)
- Add check for `err.code === 'GEOCODER_UNAVAILABLE'`: return 503 with `{ code: 'GEOCODER_UNAVAILABLE', message: 'Address lookup temporarily unavailable.' }`
- Keep PO_BOX_REJECTED and ADDRESS_NOT_FOUND handling unchanged

Do NOT change the function signature or return type of geocodeAddress — it still returns `Promise<{ lat: number; lng: number }>`.
  </action>
  <verify>
1. `grep -r "GOOGLE_MAPS_API_KEY" backend/src/` — should appear ONLY in env.ts (as optional) and nowhere else
2. `grep -r "googleapis.com" backend/src/` — should return zero results
3. `grep "census.gov" backend/src/lib/geocodingService.ts` — should show Census URL
4. `grep "GEOCODER_UNAVAILABLE" backend/src/lib/geocodingService.ts` — should exist
5. `grep "coordinates.x" backend/src/lib/geocodingService.ts` — should exist (longitude)
6. `grep "cache.get\|cache.set" backend/src/lib/geocodingService.ts` — should show Redis caching
7. `grep "LOW_CONFIDENCE" backend/src/` — should return zero results
  </verify>
  <done>geocodingService.ts uses Census Geocoder with Redis caching and 5s timeout. env.ts has GOOGLE_MAPS_API_KEY as optional. connect.ts error handling updated for new error codes. No Google Maps references remain in any service/route file.</done>
</task>

</tasks>

<verification>
1. `grep -r "googleapis.com" backend/src/` returns zero results
2. `grep "GOOGLE_MAPS_API_KEY" backend/src/lib/env.ts` shows `.optional()`
3. geocodingService.ts exports: geocodeAddress, GeocodingError, GeocodingErrorCode
4. Schema investigation results are complete in SUMMARY
</verification>

<success_criteria>
- Census Geocoder is the sole geocoding implementation
- Redis caching with 24hr TTL on successful geocode results
- 5-second timeout with GEOCODER_UNAVAILABLE on timeout
- PO Box rejection preserved
- Server starts without GOOGLE_MAPS_API_KEY
- Schema join path from geofence_boundaries to politicians is fully documented
</success_criteria>

<output>
After completion, create `.planning/phases/38-express-ports-wave-3-essentials/38-01-SUMMARY.md`

CRITICAL: The SUMMARY must include the complete schema investigation results — column names, data types, FK relationships, and the exact SQL join path from geofence_boundaries to politicians. Plan 02 depends on this.
</output>
