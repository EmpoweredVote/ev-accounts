---
phase: 102-federal-house-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/run-house-source-triage.ts
  - .planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md
  - .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv
autonomous: true
requirements:
  - FEDX-02
tags:
  - audit
  - inform
  - sources
  - data-quality
  - house
  - triage
  - candidates

must_haves:
  truths:
    - "A TypeScript triage script exists at backend/scripts/run-house-source-triage.ts and runs to exit 0 against the live DB"
    - "Running the script produces 102-TRIAGE-REPORT.md in the phase directory containing executive summary, NATIONAL_LOWER section, deferred-candidates section, and a combined target list"
    - "Running the script produces 102-HOUSE-TARGETS.csv in the phase directory with columns full_name, politician_id, scope, state, party, total_stances, unsourced_count, weak_count, affected_topic_keys, classification"
    - "Triage uses the same SOURCED_CASE / UNSOURCED_CASE four-rule definition as backend/scripts/run-senator-source-triage.ts (locked sourced definition from Phase 100 D-01)"
    - "Weak-source detection matches the homepage-only regex pattern ~ '^https?://[^/]+/?$' applied to every non-blank element of inform.politician_context.sources (per Phase 101 D-01)"
    - "House scope is enforced via essentials.offices JOIN essentials.districts WHERE district_type = 'NATIONAL_LOWER' using a DISTINCT ON (politician_id) subquery"
    - "Deferred-candidates scope queries district_type = 'NATIONAL_UPPER' AND p.is_incumbent = false (the 3 Senate candidates Dooley/Shoffner/Alme deferred from Phase 101) using DISTINCT ON (politician_id)"
    - "All inform.* queries go through pg.Pool — never PostgREST (inform schema not in PostgREST exposed list)"
    - "Plan 02 can size its work by reading 102-HOUSE-TARGETS.csv — both NATIONAL_LOWER row count (expected 0) and deferred-candidate row count (expected 3, 19 weak stances) are explicit"
  artifacts:
    - path: "backend/scripts/run-house-source-triage.ts"
      provides: "House + deferred-candidate weak-source + unsourced triage script (FEDX-02 scoping)"
      min_lines: 200
      contains: "pool.query"
    - path: ".planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md"
      provides: "Human-readable house triage report — Plan 02 scope input"
      contains: "House Triage"
    - path: ".planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv"
      provides: "Machine-readable target list with NATIONAL_LOWER and deferred-candidate rows; affected topic keys per politician"
      contains: "affected_topic_keys"
  key_links:
    - from: "backend/scripts/run-house-source-triage.ts"
      to: "inform.politician_answers + inform.politician_context"
      via: "LEFT JOIN on (politician_id, topic_id) inside pool.query() calls"
      pattern: "LEFT JOIN inform\\.politician_context"
    - from: "backend/scripts/run-house-source-triage.ts"
      to: "essentials.districts WHERE district_type = 'NATIONAL_LOWER'"
      via: "DISTINCT ON (politician_id) subquery joining offices and districts"
      pattern: "NATIONAL_LOWER"
    - from: "backend/scripts/run-house-source-triage.ts"
      to: "essentials.districts WHERE district_type = 'NATIONAL_UPPER' AND p.is_incumbent = false"
      via: "Second DISTINCT ON (politician_id) subquery for deferred Senate candidates"
      pattern: "is_incumbent = false"
    - from: "backend/scripts/run-house-source-triage.ts"
      to: "homepage-only URL detection"
      via: "regex ~ '^https?://[^/]+/?$' on every non-blank element of pc.sources"
      pattern: "\\^https\\?://"
    - from: "backend/scripts/run-house-source-triage.ts"
      to: ".planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md"
      via: "writeFileSync resolving phase dir via __dirname + path.resolve"
      pattern: "writeFileSync"
    - from: "backend/scripts/run-house-source-triage.ts"
      to: ".planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv"
      via: "writeFileSync of CSV string"
      pattern: "102-HOUSE-TARGETS\\.csv"
---

<objective>
Build a house-and-deferred-candidate source-triage script and run it against the live Supabase DB to produce the two Plan 02 scoping artifacts: `102-TRIAGE-REPORT.md` (human-readable) and `102-HOUSE-TARGETS.csv` (machine-readable). These artifacts size the Phase 102 research + migration work — Plan 02 needs them to know exactly which politicians and topics to research (per the Phase 101 D-03 pattern).

Purpose: 102-RESEARCH.md confirms via live DB queries that NATIONAL_LOWER incumbent representatives have 0 unsourced and 0 weak-sourced stances — V1 (FEDX-02 success criterion) will trivially pass. But Phase 101's V2 query returned 19 due to 3 deferred Senate candidates (Dooley, Shoffner, Alme) with homepage-only sources, which were explicitly assigned to Phase 102 in 101-VERIFICATION.md and deferred-items.md. This triage script must capture BOTH populations: NATIONAL_LOWER incumbents (confirm 0) and NATIONAL_UPPER non-incumbent candidates (confirm 3 politicians, 19 weak stances). The combined target list scopes Plan 02 (per Pitfall 1 of 102-RESEARCH.md — never let the deferred candidates fall through the cracks).

Output:
- `backend/scripts/run-house-source-triage.ts` — TypeScript triage script using `pg.Pool` direct queries (no PostgREST; inform schema not exposed)
- `.planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md` — human-readable report
- `.planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv` — machine-readable per-politician target list (covers both NATIONAL_LOWER and deferred-candidate rows)

Implements: FEDX-02 (scoping step — actual remediation in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-federal-house-remediation/102-RESEARCH.md
@.planning/phases/101-candidate-profiles/101-CONTEXT.md
@.planning/phases/101-candidate-profiles/101-VERIFICATION.md
@.planning/phases/101-candidate-profiles/deferred-items.md
@.planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md
@backend/scripts/run-senator-source-triage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build house-and-candidate triage script (run-house-source-triage.ts)</name>
  <files>backend/scripts/run-house-source-triage.ts</files>
  <read_first>
    - backend/scripts/run-senator-source-triage.ts (full script — direct template; copy SOURCED_CASE/UNSOURCED_CASE/HOMEPAGE_ONLY_REGEX constants verbatim, copy the dotenv/pool setup, copy the Query A/B/C structure and report builder, then adapt for two scopes)
    - .planning/phases/102-federal-house-remediation/102-RESEARCH.md (Live DB State section — confirms NATIONAL_LOWER returns 0 flagged; Pattern 1 House Triage CTE block; Pitfall 1 about scope confusion; Architecture Patterns "Recommended approach" two-query design)
    - .planning/phases/101-candidate-profiles/deferred-items.md (the 3 deferred candidates and their exact weak-topic lists)
    - .planning/phases/101-candidate-profiles/101-VERIFICATION.md (V2 = 19 finding that drives the second-query design)
    - .planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md (locked sourced definition + weak-source homepage-only pattern)
  </read_first>
  <action>
    Create backend/scripts/run-house-source-triage.ts as a standalone tsx-runnable script. Start by copying backend/scripts/run-senator-source-triage.ts verbatim, then adapt the following:

    KEEP VERBATIM (do not modify):
    - The dotenv + path + fileURLToPath + pg + node:fs import block and __dirname resolution
    - The DATABASE_URL guard and pool construction
    - The DRY_RUN argv check
    - The SOURCED_CASE and UNSOURCED_CASE multi-line SQL CASE expression constants
    - The HOMEPAGE_ONLY_REGEX constant string `'^https?://[^/]+/?$'`
    - The TypeScript interfaces SummaryRow / TargetRow / DetailRow shapes (extend TargetRow and DetailRow with a `scope` field — see below)

    REPLACE the single SENATE_POLITICIANS_CTE constant with TWO CTEs:

    1) HOUSE_POLITICIANS_CTE — same shape as the senator CTE, but:
       - `d.district_type = 'NATIONAL_LOWER'`
       - REMOVE the `AND p.is_incumbent = true` filter (per 102-RESEARCH.md — all 122 active NATIONAL_LOWER politicians are is_incumbent=true; removing the filter is robust to any future is_incumbent flips)
       - Resulting subquery name: `house_politicians`
       - Same DISTINCT ON (p.id) ORDER BY p.id pattern

    2) DEFERRED_CANDIDATES_CTE — Senate candidates from Phase 101:
       - `d.district_type = 'NATIONAL_UPPER'`
       - `AND p.is_incumbent = false`
       - Same WHERE p.is_active = true
       - Same DISTINCT ON (p.id) ORDER BY p.id pattern
       - Resulting subquery name: `deferred_candidates`

    EXTEND data types:
    - Add `scope: 'NATIONAL_LOWER' | 'NATIONAL_UPPER_DEFERRED'` to TargetRow and DetailRow interfaces.
    - The TargetRow scope column distinguishes the two populations in the combined CSV; the DetailRow scope distinguishes them in the markdown sections.

    REPLACE Query A (Summary) with two summary queries that return distinct objects, OR a single UNION ALL query that emits two rows tagged by scope. Recommended: keep the structure cleaner by running querySenatorSummary-equivalent TWICE — once with HOUSE_POLITICIANS_CTE producing the NATIONAL_LOWER summary, once with DEFERRED_CANDIDATES_CTE producing the NATIONAL_UPPER_DEFERRED summary. Function names: queryHouseSummary() and queryDeferredCandidateSummary(). Both return SummaryRow.

    REPLACE Query B (Per-target list) with two queries — queryHouseTargets() against house_politicians and queryDeferredCandidateTargets() against deferred_candidates. Each adds a `scope` literal column ('NATIONAL_LOWER' or 'NATIONAL_UPPER_DEFERRED') in the final SELECT so downstream code can tag rows. The HAVING (unsourced > 0 OR weak > 0) filter, the classification CASE, and the affected_topic_keys array_agg all stay identical to the senator script. Combine results in memory: `const combinedTargets = [...houseTargets, ...candidateTargets]`.

    REPLACE Query C (Topic detail rollup) similarly — queryHouseTopicDetail() and queryDeferredCandidateTopicDetail(). Each tags rows with a `scope` literal. Combine for the report sections.

    REPORT BUILDER changes (buildReport):
    - Two Executive Summary sub-tables — one for NATIONAL_LOWER, one for NATIONAL_UPPER_DEFERRED — each showing total politicians, total stances, unsourced count, weak count, flagged count.
    - Methodology block kept (same locked definition references) but mention the dual scope explicitly.
    - Replace the senator-only "Unsourced Senator Stances" and "Weak-Sourced Senator Stances" sections with FOUR sections:
        a) "NATIONAL_LOWER — Unsourced Stances" (expected: empty / `_No NATIONAL_LOWER representatives with unsourced stances found._`)
        b) "NATIONAL_LOWER — Weak-Sourced Stances" (expected: empty)
        c) "Deferred Candidates (NATIONAL_UPPER non-incumbent) — Unsourced Stances" (expected: empty unless data has drifted)
        d) "Deferred Candidates (NATIONAL_UPPER non-incumbent) — Weak-Sourced Stances" (expected: 3 politicians — Dooley/Shoffner/Alme — with 19 total weak topic rows)
    - Combined Target List table includes a `scope` column.
    - Plan 02 Scoping Note text: states the NATIONAL_LOWER flagged count, the deferred-candidate flagged count, and the chosen batch plan. Per 102-RESEARCH.md "Phase Plan Structure" — 3 candidates ≤ 10 threshold ⇒ 1 research plan for Plan 02. Embed this conclusion explicitly: `Recommended batching: 1 research plan (3 deferred candidates ≤ 10 threshold)`.

    CSV BUILDER changes (buildCsv):
    - Header row updates to: `full_name,politician_id,scope,state,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
    - The `scope` column is inserted between `politician_id` and `state`. Values: `NATIONAL_LOWER` or `NATIONAL_UPPER_DEFERRED`.
    - affected_topic_keys remains pipe-delimited inside double quotes.

    OUTPUT PATHS:
    - phaseDir = path.resolve(__dirname, '..', '..', '.planning', 'phases', '102-federal-house-remediation')
    - reportPath = path.resolve(phaseDir, '102-TRIAGE-REPORT.md')
    - csvPath = path.resolve(phaseDir, '102-HOUSE-TARGETS.csv')

    DRY-RUN behavior:
    - Print to stderr both summary objects so Task 2 / Plan 02 can verify expected counts without writing files. Specifically print four totals: NATIONAL_LOWER total_politicians, NATIONAL_LOWER unsourced_stance_count, NATIONAL_UPPER_DEFERRED total_politicians, NATIONAL_UPPER_DEFERRED weak_stance_count.

    STDOUT summary line (non-dry-run):
    - Print one line with the Plan 02 scope: `Plan 02 scope: {L} NATIONAL_LOWER + {C} deferred candidates flagged ({W} total weak stances) — recommended batching: 1 research plan (per 102-RESEARCH.md Phase Plan Structure)`.

    EXIT HANDLING: same as senator script — `await pool.end()` then `process.exit(0)` on success; on caught error, log to stderr and `process.exit(1)`.
  </action>
  <verify>
    <automated>cd backend && npx tsx scripts/run-house-source-triage.ts --dry-run 2>&1 | grep -E "(NATIONAL_LOWER|NATIONAL_UPPER_DEFERRED|total_politicians|weak_stance_count)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/scripts/run-house-source-triage.ts
    - `grep -v '^//' backend/scripts/run-house-source-triage.ts | grep -v '^ *\*' | grep -c 'pool.query'` returns a value >= 4 (Queries A, B, C run twice — once per scope)
    - Source contains the literal string `district_type = 'NATIONAL_LOWER'`
    - Source contains the literal string `district_type = 'NATIONAL_UPPER'`
    - Source contains the literal string `is_incumbent = false`
    - Source contains the SOURCED_CASE expression including the substring `array_length(pc.sources, 1) IS NOT NULL` (locked definition copied verbatim from run-senator-source-triage.ts)
    - Source contains the regex literal `^https?://[^/]+/?$`
    - Source contains at least two occurrences of `DISTINCT ON (p.id)` (one per CTE)
    - Source contains the path literals `102-TRIAGE-REPORT.md` and `102-HOUSE-TARGETS.csv`
    - Source contains the literal string `NATIONAL_UPPER_DEFERRED` (the scope tag)
    - Source contains `--dry-run` argv handling
    - Dry-run command `cd backend && npx tsx scripts/run-house-source-triage.ts --dry-run` exits with code 0
    - Dry-run stderr output includes a line containing `NATIONAL_LOWER` followed by a numeric `total_politicians` value
    - Dry-run stderr output includes a line containing `NATIONAL_UPPER_DEFERRED` followed by a numeric `total_politicians` value
    - Dry-run stderr output prints a NATIONAL_LOWER `unsourced_stance_count` of `0` (per 102-RESEARCH.md Live DB State — verified before research)
    - Dry-run stderr output prints a NATIONAL_UPPER_DEFERRED `weak_stance_count` of `19` (per 102-RESEARCH.md and deferred-items.md — Dooley 6 + Shoffner 5 + Alme 8 = 19)
  </acceptance_criteria>
  <done>The triage script exists, compiles under tsx, connects to the live DB, and prints both scope summaries (NATIONAL_LOWER and NATIONAL_UPPER_DEFERRED) when run with --dry-run. Ready to invoke without --dry-run in Task 2 to produce the artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Run triage script against live DB and commit artifacts</name>
  <files>
    .planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md,
    .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv
  </files>
  <read_first>
    - backend/scripts/run-house-source-triage.ts (the script created in Task 1)
    - .planning/phases/102-federal-house-remediation/102-RESEARCH.md (Live DB State — expected zero NATIONAL_LOWER flagged, three NATIONAL_UPPER_DEFERRED flagged: Dooley/Shoffner/Alme with 6/5/8 = 19 weak topics)
    - .planning/phases/101-candidate-profiles/deferred-items.md (authoritative list of 3 deferred candidates and their weak topic keys per candidate)
  </read_first>
  <action>
    Run the script against the live Supabase DB to produce the two phase artifacts.

    Command: `cd backend && npx tsx scripts/run-house-source-triage.ts` (no --dry-run flag — writes files).

    The script writes:
      - .planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md
      - .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv

    After the run, inspect both files. Confirm:
      - 102-TRIAGE-REPORT.md exists and contains TWO Executive Summary sub-tables (one per scope).
      - The NATIONAL_LOWER section shows total_politicians value (expected: a positive integer; the live-DB-verified count was 122 incumbents at research time but any positive number is acceptable — the assertion is "is_active=true is non-zero").
      - The NATIONAL_LOWER unsourced and weak counts are both 0.
      - The Deferred Candidates section lists Derek Dooley, Hallie Shoffner, and Kurt Alme with 6 / 5 / 8 weak topic counts respectively.
      - 102-HOUSE-TARGETS.csv has the exact header: `full_name,politician_id,scope,state,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
      - 102-HOUSE-TARGETS.csv contains exactly 3 data rows (one per deferred candidate) — no NATIONAL_LOWER rows because all 0 are flagged. Each row's `scope` column is `NATIONAL_UPPER_DEFERRED`.
      - Each candidate's affected_topic_keys pipe-delimited list matches the deferred-items.md expectations (Dooley: abortion|civil-rights|climate-change|healthcare|immigration|voting-rights; Shoffner: campaign-finance|climate-change|economic-development|housing|taxes; Alme: abortion|fossil-fuels|religious-freedom|same-sex-marriage|social-security|tariffs|trans-athletes|voting-rights — order may vary, set membership must match).

    If the NATIONAL_LOWER counts are non-zero (i.e., the live DB has drifted since research), record the discrepancy in the report's scoping note — Plan 02 must then be re-sized. If the deferred-candidate counts differ from 3 politicians / 19 stances, also flag the drift in the scoping note.

    The stdout single-line summary should print exactly the format: `Plan 02 scope: {L} NATIONAL_LOWER + {C} deferred candidates flagged ({W} total weak stances) — recommended batching: 1 research plan (per 102-RESEARCH.md Phase Plan Structure)`.

    No further DB writes in this task. The triage is read-only; it only writes to the phase directory.
  </action>
  <verify>
    <automated>test -f .planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md && test -f .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv && head -1 .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv | grep -q "full_name,politician_id,scope,state,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md exists
    - File .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv exists
    - First line of CSV is exactly: `full_name,politician_id,scope,state,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
    - Report contains a NATIONAL_LOWER section with `unsourced_stance_count` of `0`
    - Report contains a NATIONAL_LOWER section with `weak_stance_count` of `0`
    - Report contains the names `Derek Dooley`, `Hallie Shoffner`, and `Kurt Alme` in the Deferred Candidates section
    - Report records the Deferred Candidates weak counts as: Dooley = 6, Shoffner = 5, Alme = 8 (total 19)
    - CSV has exactly 4 lines (1 header + 3 data rows) — verifiable via `grep -v '^#' .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv | wc -l` returns 4
    - Every CSV data row has scope column value `NATIONAL_UPPER_DEFERRED` — `grep -c 'NATIONAL_UPPER_DEFERRED' .planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv` returns 3
    - CSV row for Derek Dooley contains affected_topic_keys including (case-insensitive substring match) `abortion`, `civil-rights`, `climate-change`, `healthcare`, `immigration`, `voting-rights`
    - CSV row for Hallie Shoffner contains affected_topic_keys including `campaign-finance`, `climate-change`, `economic-development`, `housing`, `taxes`
    - CSV row for Kurt Alme contains affected_topic_keys including `abortion`, `fossil-fuels`, `religious-freedom`, `same-sex-marriage`, `social-security`, `tariffs`, `trans-athletes`, `voting-rights`
    - Report contains a "Plan 02 Scoping" section explicitly stating `1 research plan` as the batching tier
  </acceptance_criteria>
  <done>Triage artifacts are committed to the phase directory. The NATIONAL_LOWER zero-flagged status is documented; the 3 deferred candidates and their 19 weak-topic affected_topic_keys are machine-readable for Plan 02. Plan 02 can read 102-HOUSE-TARGETS.csv to drive its research-stances dispatch (one agent per candidate, weak topics only).</done>
</task>

</tasks>

<verification>
- Both triage artifacts exist in .planning/phases/102-federal-house-remediation/
- CSV header matches the locked column shape including the new `scope` column
- NATIONAL_LOWER section is empty (0 unsourced, 0 weak) — confirms FEDX-02 V1 trivially passes
- Deferred Candidates section lists Dooley/Shoffner/Alme with 6/5/8 topic counts (total 19)
- Plan 02 batching tier is explicitly named: `1 research plan`
</verification>

<success_criteria>
1. `backend/scripts/run-house-source-triage.ts` exists and runs successfully against the live DB for both scopes (NATIONAL_LOWER and NATIONAL_UPPER non-incumbent).
2. `.planning/phases/102-federal-house-remediation/102-TRIAGE-REPORT.md` and `.planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv` are written.
3. The CSV gives Plan 02 a deterministic list of 3 deferred candidates with per-candidate affected_topic_keys — enough to scope the research-stances dispatch (one agent per candidate, weak topics only) without re-querying.
4. The "sourced" definition and homepage-only weak-source regex used here match Phase 100 / Phase 101 exactly (locked).
</success_criteria>

<output>
Create `.planning/phases/102-federal-house-remediation/102-01-SUMMARY.md` when done. The summary must include:
- Confirmation that NATIONAL_LOWER flagged count is 0 (FEDX-02 V1 will trivially pass)
- The 3 deferred-candidate target rows (Dooley/Shoffner/Alme) with their weak topic counts and politician_ids
- Total weak stance count (expected: 19)
- Recommended Plan 02 batching (1 research plan, 3 sequential agent dispatches)
- Path to the CSV that Plan 02 will read: `.planning/phases/102-federal-house-remediation/102-HOUSE-TARGETS.csv`
</output>
</content>
</invoke>