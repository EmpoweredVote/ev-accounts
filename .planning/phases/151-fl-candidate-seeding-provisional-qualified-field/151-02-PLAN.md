---
phase: 151-fl-candidate-seeding-provisional-qualified-field
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/151-verify.sql
autonomous: true
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
must_haves:
  truths:
    - "A standalone, write-free 151-verify.sql exists asserting every USHC-02/03/04/05 + D-01/D-02/D-03/D-04/D-05 success criterion, scoped to the 28 FL House races (NATIONAL_LOWER + geo prefix '12') within the FL 2026 Statewide General election"
    - "All 28 FL races assert description LIKE 'PROVISIONAL:%' (D-04); the FL-20 office is asserted present with politician_id NULL"
    - "The stance/headshot in-scope set is EXACTLY the 17 independent/NPA new candidates (NEW external_id band -12NNXX); the 27 partial-stance incumbents + AOC-equivalent are EXCLUDED (the NY false-fail trap)"
    - "No per-party cap / one-D-one-R assertion exists (FL is pre-primary crowded same-party by design — D-02); only full_name uniqueness + >=1 active per race"
    - "The 3 reuse pids (Frankel -12022, Moskowitz -12023, Wasserman Schultz -12025) are asserted active in their new districts; Cherfilus-McCormick is asserted NEW (no reuse); honest-skips pinned by exact UUID with explicit ORDER BY"
  artifacts:
    - path: "backend/scripts/151-verify.sql"
      provides: "Read-only labeled-assertion gate for Phase 151 (USHC-02/03/04/05 + decisions), single-state FL '12'-scoped, provisional-aware, honest-skip-pinnable"
      contains: "NATIONAL_LOWER"
      min_lines: 110
  key_links:
    - from: "151-verify.sql assertions"
      to: "the 28 FL House races within the FL 2026 Statewide General election"
      via: "d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='12' predicate"
      pattern: "NATIONAL_LOWER"
---

<objective>
Author the read-only `backend/scripts/151-verify.sql` gate (Wave-0 test infrastructure, nyquist) that every Phase 151 wave runs to self-check and that 151-06 runs as the final consolidated proof. It asserts elections/races scaffold + FL-20 office + provisional marking (USHC-03/D-04), records + dedup (USHC-02/D-03), race wiring (D-02/D-05), headshots (USHC-04), and stances (USHC-05) — scoped single-state to the 28 FL House races.

Purpose: This gate must exist before later waves so each can verify its slice. The trap-prone parts are (a) the D-01 asymmetry — the 27 FL partial-stance incumbents are deliberately untouched and must NOT be required to reach 24 or trip a 0-unsourced check (the exact NY false-fail trap); stance/headshot in-scope = the 17 independents ONLY; (b) FL is pre-primary crowded — multiple same-party candidates per district by design, so NO per-party cap / one-D-one-R assertion; (c) Cherfilus-McCormick is NEW not a reuse (RESEARCH Pitfall 3); (d) FEC-noise dedup guard (RESEARCH Pitfall 2).
Output: A standalone, write-free `.sql` gate (the 150-verify.sql precedent, adapted single-state + provisional-aware).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-field-table.csv
@.planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv
@backend/scripts/150-verify.sql
@backend/scripts/149-verify.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 151-verify.sql — single-state FL '12'-scoped labeled assertions for USHC-02/03/04/05 + decisions</name>
  <read_first>
    - backend/scripts/150-verify.sql (the validated two-state precedent: read-only DO-block labeled assertions; CREATE TEMP TABLE … ON COMMIT DROP working sets; _reuse_pid / _new_cands / _stance_skip / _fed24 pin-table patterns; the D-01 NY-partial-EXCLUSION ASYMMETRY note — FL mirrors NY's partial-incumbent exclusion exactly; the per-state scoping; the 143 honest-skip-ORDER-BY lesson)
    - backend/scripts/149-verify.sql (the single-state precedent shape this most closely resembles)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/RESEARCH.md (Q3 provisional description sentinel; Q4 the 17 independents list; Pitfall 1 no per-party cap; Pitfall 2 FEC-noise dedup; Pitfall 3 Cherfilus NEW; the 3 reuse pids w/ UUIDs; external_id band -12NNXX; Validation Architecture → Test Map)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-CONTEXT.md (D-01 in-scope=17 independents; D-02 seed-all-same-party; D-03 zero-dup-full_name + 3 reuse + Cherfilus-new; D-04 provisional; D-05 office_id-never-NULL)
    - .planning/phases/148-field-resolution-stance-gap-diagnostic/148-incumbent-map.csv (FL geo 12NN -> incumbent_pid + external_id -12NN, for the reuse-pid pin set)
  </read_first>
  <action>
    Write `backend/scripts/151-verify.sql` as a write-free, labeled-assertion gate following 150-verify.sql exactly, but for a SINGLE state (FL). Resolve the election id at the top (`SELECT id FROM essentials.elections WHERE name='FL 2026 Statewide General'`). Build the FL House working set via the join chain `races r JOIN offices o ON o.id=r.office_id JOIN districts d ON d.id=o.district_id` filtered `r.election_id = fl_eid AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='12'`. No cross-state scoping needed (drop the TX/NY two-state parameterization).

    Define TEMP tables at the top (149/150 pattern) so W2/W3 plans can run the gate mid-wave:
    - `_reuse_pid` (geo_id, expected_pid, external_id): the 3 reuse targets — Frankel `b4040115-b3ea-4500-89cf-1ddaecafc94e` (-12022) active in FL-23; Moskowitz `1cb8827c-6ae0-4fcf-884c-94ad2246f15d` (-12023) active in FL-25; Wasserman Schultz `097623b0-3063-4943-a13c-89d213ca5829` (-12025) active in FL-20.
    - `_new_cands`: the new-candidate external_id band filter `external_id BETWEEN -1229999 AND -1210000` (the -12NNXX band, verified empty pre-seed — RESEARCH external_id section).
    - `_indep_scope`: the 17 independent/NPA new candidates (the USHC-04/05 in-scope set). Resolve them as `_new_cands` rows that the W3 SUMMARYs flag as the 17 independents (pin by external_id or by full_name within the 17-list from RESEARCH Q4). Document that the 27 incumbents (external_id -12001..-12028) and ALL partisan-primary new candidates are EXCLUDED from USHC-04/05 (D-01 records-now/stances-at-153 + zero-only top-up rule).
    - `_stance_skip` / `_headshot_skip`: whole-record honest-skip pins by exact UUID/external_id with explicit ORDER BY (filled by W3 SUMMARYs; empty placeholder now).

    Labeled assertions (`DO $$ … IF NOT (…) THEN RAISE EXCEPTION 'FAIL <label>: %' … ELSE RAISE NOTICE 'PASS <label>'; END $$;`):
    - USHC-03a — COUNT of FL House races = 28 (no district missing); each race has >=1 active race_candidate. Use a `>=1` hard floor (NOT >=2: FL-10 Frost is uncontested in the 148 field — incumbent-only — so a >=2 cap would false-fail; emit a `<2` NOTICE for uncontested seats, not a failure). DO NOT add any per-party cap or one-D-one-R / <=2 upper bound (Pitfall 1 — FL-19 has 11 Republicans, FL-2 has 12, FL-24 has 10 by design).
    - USHC-03b — 0 active race_candidates rows with NULL politician_id (FL House-scoped).
    - USHC-03c (FL-20 office) — the FL-20 office (geo 1220 NATIONAL_LOWER) exists with politician_id NULL; the FL-20 race links to it (office_id non-null).
    - D-04-provisional — all 28 FL House races have `description LIKE 'PROVISIONAL:%'`.
    - USHC-02a — 0 duplicate full_name among ACTIVE FL candidates (`GROUP BY lower(full_name) HAVING count(*)>1` over FL House races returns 0) — D-03 zero-duplicate-full_name (FL-scoped).
    - USHC-02b-reuse — each `_reuse_pid` row appears as an ACTIVE FL House candidate in its expected district (Frankel FL-23, Moskowitz FL-25, Wasserman Schultz FL-20); pinned by exact UUID. Header comment documents these 3 are the ONLY reuse targets (matched by exact external_id, NOT name substring — Pitfall 2 FEC-noise guard).
    - USHC-02c-cherfilus — Sheila Cherfilus-McCormick appears as an ACTIVE FL-20 candidate whose external_id is in the `_new_cands` band (i.e. she is a NEW record, NOT one of the 3 reuse pids) — Pitfall 3.
    - D-05-lost-incumbent — the retired/redistricted incumbents get NO active row in the seat they LEFT: FL-2 Dunn (-12002), FL-16 Buchanan (-12016), FL-19 Donalds (-12019), FL-24 Wilson (-12024) [retired]; FL-22 Frankel (-12022), FL-23 Moskowitz (-12023), FL-25 Wasserman Schultz (-12025) [redistricted — assert each is ABSENT as an active candidate IN ITS OLD SEAT, while present in its NEW seat per USHC-02b-reuse]. FL-20 was vacant (no incumbent to exclude). Pin by external_id with explicit ORDER BY.
    - USHC-04 — every candidate in `_indep_scope` (the 17 independents) has an essentials.politician_images row, OR is pinned in `_headshot_skip` by exact external_id with explicit ORDER BY. The 27 incumbents + partisan new candidates are EXCLUDED (D-01).
    - USHC-05a — 0 unsourced stance rows for `_indep_scope` ONLY: 0 = count over inform.politician_answers for those pids WHERE NOT EXISTS a matching inform.politician_context with a non-empty sources array. EXCLUDE the 27 partial incumbents (their pre-existing partial coverage must NOT false-fail — the NY false-fail trap, documented in 150-verify.sql D-01 ASYMMETRY).
    - USHC-05b — each `_indep_scope` candidate has >=1 sourced federal stance OR is a pinned whole-record honest-skip in `_stance_skip` (per [[project-149-stance-gate-standard]]: >=1-sourced-OR-pinned, full-24 NOT required; chairs-not-polarity forbids party-inference inflation). Whole-record skips pinned by UUID with exact ORDER BY.

    Header comment documents: (a) single-state '12' scoping rule; (b) the D-01 17-independents-ONLY in-scope set + the 27-partial-incumbent EXCLUSION (NY-mirror asymmetry); (c) NO per-party cap (FL pre-primary crowded by design); (d) data-dependent assertions legitimately FAIL pre-seed and go green only after the waves complete; (e) the 143 honest-skip-ORDER-BY lesson; (f) Cherfilus-NEW + the FEC-noise dedup guard.

    Self-test that the SQL parses + runs read-only against prod NOW (pre-seed). The races scaffold (151-01) may already be live in this wave; USHC-03a race-count may already pass while candidate/headshot/stance assertions FAIL pre-seed (expected, documented).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^[[:space:]]*--' scripts/151-verify.sql > /tmp/151v.nc && grep -qi 'NATIONAL_LOWER' /tmp/151v.nc && grep -q "'12'" /tmp/151v.nc && grep -qi 'PROVISIONAL' /tmp/151v.nc && ! grep -qiE 'DELETE |UPDATE |INSERT[[:space:]]+INTO[[:space:]]+(essentials|inform)' /tmp/151v.nc && echo PASS-scoped-provisional-and-write-free</automated>
  </verify>
  <acceptance_criteria>
    - 151-verify.sql contains all labeled assertions above; every data query is single-state FL House-scoped (NATIONAL_LOWER + geo_id prefix '12' within the FL 2026 Statewide General election).
    - The file is write-free: no DELETE/UPDATE/INSERT INTO essentials|inform (TEMP-table CREATE … ON COMMIT DROP is the only allowed write, matching 150-verify.sql).
    - D-01 asymmetry encoded: stance/headshot in-scope = the 17 independents ONLY; the 27 partial incumbents + partisan new candidates EXCLUDED from USHC-04/05 (header-documented).
    - NO per-party cap / one-D-one-R / <=2 upper-bound assertion exists; USHC-03a uses >=1 with a <2 NOTICE allowance (FL-10 uncontested).
    - D-04 provisional-description assertion present (28/28 PROVISIONAL); FL-20-office assertion present.
    - 3 reuse pins (Frankel/Moskowitz/Wasserman Schultz) present; Cherfilus asserted NEW; lost-incumbent-absent for FL-2/16/19/24/22/23/25; honest-skip pins use explicit ORDER BY.
    - The script parses + runs read-only against prod NOW; data-dependent assertions may FAIL pre-seed (documented as expected).
  </acceptance_criteria>
  <done>151-verify.sql authored, single-state FL '12'-scoped, provisional-aware, write-free, all assertions present incl. D-01 17-independents-only in-scope + no-per-party-cap + FL-20-office + Cherfilus-NEW + lost-incumbent-absent; parses + runs read-only against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → production DB (read) | 151-verify.sql runs SELECT-only against live prod; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-05 | Tampering | gate accidentally mutates prod | mitigate | Write-free enforced by verify check; only TEMP-table writes allowed |
| T-151-06 | Repudiation | gate false-fails on the 27 FL partial-incumbent pre-existing data (NY trap) | mitigate | In-scope stance/headshot set = 17 independents ONLY; 27 partials EXCLUDED (D-01); header-documented |
| T-151-07 | Repudiation | per-party cap false-fails crowded pre-primary field (FL-19 11 R) | mitigate | No per-party / one-D-one-R / <=2 assertion; only full_name uniqueness + >=1 active |
| T-151-08 | Repudiation | Cherfilus treated as reuse / FEC-noise false dedup match | mitigate | Cherfilus asserted NEW (band); 3 reuse pins by exact UUID/external_id, not name substring |
| T-151-09 | Repudiation | honest-skip ordering mismatch false-fails (143 lesson) | mitigate | Honest-skip pins use explicit ORDER BY matching the query |
| T-151-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- The verify automated check confirms single-state scoping + provisional + write-free.
- Manual: run the gate against prod pre-seed; confirm it executes (data assertions may FAIL pre-seed, expected).
</verification>

<success_criteria>
- 151-verify.sql exists, write-free, single-state FL '12'-scoped, provisional-aware, with all USHC-02/03/04/05 + D-01/D-02/D-03/D-04/D-05 labeled assertions; runs read-only against prod without syntax/permission error.
</success_criteria>

<output>
Create `.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-02-SUMMARY.md` when done (record: assertion labels, the _indep_scope / _reuse_pid / _new_cands TEMP-table convention, the 27-partial-exclusion, the no-per-party-cap decision, and how 151-03..05 should invoke it mid-wave).
</output>
