---
phase: 151-fl-candidate-seeding-provisional-qualified-field
plan: 06
type: execute
wave: 4
depends_on: ["151-01", "151-02", "151-03", "151-04", "151-05"]
files_modified:
  - backend/scripts/151-coordinate-smoke.ts
requirements: [USHC-02, USHC-03, USHC-04, USHC-05]
autonomous: true
must_haves:
  truths:
    - "151-verify.sql runs read-only against prod and EVERY labeled assertion (USHC-02/03/04/05 + D-01/D-02/D-03/D-04/D-05) PASSES with exit 0"
    - "For >=3 in-district FL House test coordinates, the surfacing join returns the resident's House race with its full provisional candidate field (CHALLENGER present, not just incumbent); FL-10 (Frost uncontested) allowed minActive=1"
    - "0 unsourced stance rows for the 17 independents + 0 duplicate full_name within FL across all 28 districts; honest-skips pinned by UUID; retired/redistricted incumbents absent from old seats; all 28 races marked PROVISIONAL"
  artifacts:
    - path: "backend/scripts/151-coordinate-smoke.ts"
      provides: "Read-only node smoke: ST_Covers FL House geofence centroids -> assert race + provisional field surfaces with challenger; FL-10 uncontested allowance"
  key_links:
    - from: "151-verify.sql + 151-coordinate-smoke.ts"
      to: "the 28 FL House races + their geofence-resolved surfacing"
      via: "psql -f gate + node ST_Covers smoke"
      pattern: "getElectionsByCoordinate"
---

<objective>
Prove Phase 151 end-to-end: run the single-state FL-scoped `151-verify.sql` gate green and add a coordinate-surfacing smoke that confirms >=3 FL House races actually surface their full provisional field for an in-district address.

Purpose: USHC-02/03/04/05 consolidated verification for FL — the third of the seeding-phase proofs (after CA/149 + TX-NY/150). This gate + smoke feed Phase 152's full-144 gate (FL marked provisional) and the Phase 153 prune.
Output: A passing gate + coordinate smoke; Phase 151 closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/scripts/151-verify.sql
@backend/scripts/150-coordinate-smoke.ts
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-04-SUMMARY.md
@.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the FL coordinate-surfacing smoke + finalize honest-skip pins in 151-verify.sql</name>
  <read_first>
    - backend/scripts/150-coordinate-smoke.ts (the validated read-only ST_Covers surfacing smoke; adapt single-state — sample FL geo_ids 12NN; mirror the minActive override pattern for uncontested seats)
    - backend/scripts/151-verify.sql (the gate authored in 151-02; its _indep_scope / _reuse_pid / _headshot_skip / _stance_skip TEMP-table pins)
    - backend/src/lib/electionService.ts (getElectionsByCoordinate trace: geofence ST_Covers -> district geo_id -> office -> race -> race_candidates; the join discriminator d.geo_id=gb.geo_id AND (d.mtfcc IS NULL OR d.mtfcc='' OR gb.mtfcc=d.mtfcc))
    - the 151-04 SUMMARY (headshot honest-skips) + 151-05 SUMMARY (whole-record-stance honest-skips) — the pids/external_ids to pin by UUID
  </read_first>
  <action>
    Author `backend/scripts/151-coordinate-smoke.ts` (read-only) by cloning 150-coordinate-smoke.ts and parameterizing single-state: `st:'FL'`, `geoPrefix:'12'`, sample >=4 spread FL geo_ids covering the edge cases: 1201 (FL-1, crowded 5-candidate), 1210 (FL-10 Frost UNCONTESTED — set its `minActive` override = 1), 1219 (FL-19, crowded 14-candidate field), 1220 (FL-20 NEW open-seat office + the 3 reuse + Cherfilus). For each, resolve the election by name ('FL 2026 Statewide General'), compute an interior centroid via `ST_PointOnSurface(gb.geometry)`, run the getElectionsByCoordinate-equivalent join (geofence ST_Covers point -> district -> office -> race within that election, NATIONAL_LOWER, geo prefix '12' -> race_candidates active count + linked count + challenger count). Assert each sample race returns >= its minActive (default 2; FL-10 override 1) active candidates, non-null politician_id, AND (where minActive>=2) >=1 challenger (is_incumbent=false) — the Pitfall-5 two-path guard. For FL-20 assert the field includes the 3 reuse candidates + Cherfilus.

    Gather the documented honest-skips (headshots from 151-04; whole-record stances from 151-05 SUMMARY) and confirm they are pinned by exact UUID/external_id with explicit ORDER BY inside 151-verify.sql's _headshot_skip / _stance_skip pin sets; if any are missing, add them (this pin-list update is the only edit allowed to 151-verify.sql here — not a new assertion).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/151-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - 151-coordinate-smoke.ts is read-only and asserts >=3 FL House races surface with their provisional field (>=2 active incl. >=1 challenger for contested seats); FL-10 minActive override = 1 (uncontested).
    - FL-20 sample shows the open-seat field incl. the 3 reuse candidates + Cherfilus.
    - All documented headshot + whole-record-stance honest-skips are pinned by exact UUID/external_id with explicit ORDER BY in 151-verify.sql.
  </acceptance_criteria>
  <done>Coordinate smoke passes for >=3 FL districts (incl. FL-10 uncontested + FL-20 new office); honest-skip pins complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full 151-verify.sql gate green</name>
  <read_first>
    - backend/scripts/151-verify.sql (the full gate)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-02-SUMMARY.md (the assertion labels + in-scope-set convention + 27-partial-incumbent exclusion + no-per-party-cap)
  </read_first>
  <action>
    Run `cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/151-verify.sql`. Every labeled assertion must PASS (psql exit 0). If any FAILS, route the failure to the owning wave plan (election/races/FL-20-office/provisional -> 151-01; records/dedup/reuse -> 151-03; headshots -> 151-04; stances -> 151-05) and re-run after fix — do NOT relax an assertion to make it pass (149 T-149-18). Confirm the gate stays single-state FL-scoped, honors the D-01 17-independents-only in-scope set (27 partials excluded), asserts all 28 races PROVISIONAL, has NO per-party cap, and is write-free.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/151-verify.sql >/tmp/151gate.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL gate exit $rc"; tail -25 /tmp/151gate.log; exit 1; fi; grep -c 'PASS' /tmp/151gate.log; echo "GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - 151-verify.sql exits 0 with every labeled assertion emitting PASS.
    - 28 FL House races each carry their provisional active field, 0 null politician_id; 0 duplicate full_name within FL; all 28 marked PROVISIONAL; FL-20 office present (NULL pid).
    - 3 cross-district reuse pids active in new seats; Cherfilus NEW; retired/redistricted incumbents absent from old seats; multi-same-party fields present (no per-party cap false-fail).
    - The 17 independents each have a headshot (or pinned skip); 0 unsourced stances for the 17; federal-24 coverage or pinned whole-record skip; the 27 partial incumbents untouched.
    - Coordinate smoke (Task 1) passes for >=3 FL districts.
  </acceptance_criteria>
  <done>151-verify.sql green (exit 0); coordinate smoke green; Phase 151 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate + smoke -> production DB (read) | Both run SELECT-only against live prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-26 | Repudiation | gate passes while a district silently has no challenger | mitigate | Coordinate smoke asserts >=1 is_incumbent=false candidate per contested sample race (Pitfall 5); FL-10 uncontested allowance documented |
| T-151-27 | Tampering | assertion relaxed to force green | mitigate | Failures routed to owning wave plan + re-run; never weaken an assertion |
| T-151-28 | Repudiation | per-party cap false-passes/fails crowded field | mitigate | Gate uses full_name uniqueness + >=1 active only; no per-party cap (Pitfall 1) |
| T-151-29 | Repudiation | 27 partial incumbents false-fail USHC-05 (NY trap) | mitigate | In-scope = 17 independents only; gate header-documented exclusion |
| T-151-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- 151-verify.sql exits 0, all PASS.
- 151-coordinate-smoke.ts surfaces >=3 FL House races with challenger present (FL-10 uncontested allowance; FL-20 new-office field incl. reuse + Cherfilus).
</verification>

<success_criteria>
- All USHC-02/03/04/05 + D-01/D-02/D-03/D-04/D-05 assertions PASS; coordinate smoke green; FL provisional Wave-1 seeding proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-06-SUMMARY.md` when done (record: final per-district candidate counts, total new records, total stanced independents, all honest-skips pinned, gate PASS log, smoke results, the PROVISIONAL-sentinel carry-forward note for Phase 153 — this becomes the FL slice of the Phase 152 full-144 gate input).
</output>
