---
phase: 113-competitive-benchmarking
plan: 03
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - .planning/research/benchmark/vote411.md
  - .planning/research/benchmark/screenshots/vote411/
autonomous: false
requirements: [BENCH-02]
must_haves:
  truths:
    - "A live Vote411 session for 200 W Kirkwood Ave is documented"
    - "vote411.md contains field inventory, race/candidate count table, narrative, and precinct precision subsection"
    - "≥3 screenshots captured under screenshots/vote411/"
    - "Any blocker is logged with human-fallback instructions"
  artifacts:
    - path: ".planning/research/benchmark/vote411.md"
      provides: "Vote411 spot-check evidence file (D-03 four evidence types)"
    - path: ".planning/research/benchmark/screenshots/vote411/"
      provides: "PNG captures of address entry, race list, candidate Q&A"
  key_links:
    - from: "vote411.md"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "race/candidate count comparison table"
      pattern: "BALLOT-BASELINE-2026-05-05"
---

<objective>
Run a live Vote411 spot-check against the primary Bloomington address and produce all four D-03 evidence types. Vote411 is the League of Women Voters guide and is particularly strong on candidate Q&A — that dimension deserves special attention.

Purpose: Satisfies BENCH-02. Feeds plan 07's matrix scoring for the Vote411 column.
Output: `vote411.md` + ≥3 PNG screenshots in `screenshots/vote411/`.

Note: `autonomous: false` because Vote411 may require email entry to "build your ballot" and coverage may drop outside major races — blockers must be logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-competitive-benchmarking/113-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright-drive Vote411 for the primary address + capture screenshots</name>
  <files>.planning/research/benchmark/screenshots/vote411/</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
  </read_first>
  <action>
Use `mcp__plugin_playwright_playwright__browser_navigate` to open `https://www.vote411.org/`. Then:

1. Locate the address/ZIP entry. Enter `200 W Kirkwood Ave, Bloomington, IN 47404` (try full address first; if only ZIP accepted, use `47404`). Submit.
2. Screenshot the entry screen → `01-address-entry.png`.
3. Screenshot the resulting race list / ballot page → `02-race-list.png`.
4. Click into a contested race (prefer US Rep IN-9 or Monroe County Prosecutor / Clerk) → `03-race-detail.png`.
5. Click into a candidate profile — Vote411's distinctive feature is candidate-submitted Q&A. Capture the Q&A screen if present → `04-candidate-qa.png`.
6. Supplemental screenshots for unique features (voter registration links, polling place lookup) if present.

**Blocker handling (D-02):** Vote411 frequently shows "no races found" for secondary/primary races when LWV volunteers haven't covered them — this is NOT a blocker, it's a genuine coverage gap and MUST be scored as `0` or `1`, not `blocked`. `blocked` is only for signup/captcha/paywall/region gate. If the tool demands email to build a ballot, that IS a blocker — screenshot + fallback instructions.

Capture ≥3 screenshots or ≥1 blocker screenshot + fallback instructions in task 2.
  </action>
  <verify>
    <automated>test "$(ls .planning/research/benchmark/screenshots/vote411/*.png 2>/dev/null | wc -l)" -ge 3 || { test "$(ls .planning/research/benchmark/screenshots/vote411/blocker-*.png 2>/dev/null | wc -l)" -ge 1 && grep -qi "blocked\|fallback" .planning/research/benchmark/vote411.md; }</automated>
  </verify>
  <done>≥3 PNG screenshots capturing address entry, race list, and candidate profile — or a documented blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Write vote411.md with field inventory, count comparison, narrative, precinct precision</name>
  <files>.planning/research/benchmark/vote411.md</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/BALLOT-BASELINE-2026-05-05.md
    - .planning/research/benchmark/screenshots/vote411/
  </read_first>
  <action>
Create `.planning/research/benchmark/vote411.md` with the same 6-section structure as ballotready.md (headings verbatim so plan 07 can grep):

1. `# Vote411 — Spot Check` + date + primary address.
2. `## Access Status`
3. `## Field Inventory` — 10-core-dimension table + any extras. Pay particular attention to candidate Q&A depth (dimension 6 "Candidate quotes / Q&A") — Vote411 is the strongest player on this dimension in the competitor set, so score it honestly (may legitimately be 2 or 3).
4. `## Race / Candidate Count vs Baseline` — same structure as ballotready.md, referencing BALLOT-BASELINE-2026-05-05.md. Expect Vote411 to have lower race coverage than BallotReady for local races (LWV volunteer-staffed) — score honestly.
5. `## Narrative Notes` — special call-out for candidate Q&A format and response rate.
6. `## Precinct Precision Subsection` — re-run the 2 secondary addresses. Vote411 often uses ZIP-level resolution only, which MUST be noted (impacts dimension 8 geofence/address precision). Record for each secondary address.
7. `## Blockers`

Use METHODOLOGY.md 0-3 rubric. Do NOT score EV's column.
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/vote411.md && grep -q "^## Field Inventory" .planning/research/benchmark/vote411.md && grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/vote411.md && grep -q "^## Narrative Notes" .planning/research/benchmark/vote411.md && grep -q "^## Precinct Precision" .planning/research/benchmark/vote411.md && grep -q "^## Blockers" .planning/research/benchmark/vote411.md && grep -q "200 W Kirkwood" .planning/research/benchmark/vote411.md</automated>
  </verify>
  <done>vote411.md contains all 6 required sections with real data or documented blocker.</done>
</task>

</tasks>

<verification>
- All 6 sections present.
- ≥3 screenshots OR blocker case.
- Field inventory covers all 10 core dimensions.
- Baseline referenced in count table.
</verification>

<success_criteria>
- `test -f .planning/research/benchmark/vote411.md`
- Screenshots or blocker case met
- All 6 sections grep-present
</success_criteria>

<output>
After completion, create `.planning/phases/113-competitive-benchmarking/113-03-SUMMARY.md`: access status, screenshot count, baseline match rate, top 3 observations (esp. Q&A depth), any blockers.
</output>
