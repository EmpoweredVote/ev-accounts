---
phase: 113-competitive-benchmarking
plan: 06
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - .planning/research/benchmark/ev.md
  - .planning/research/benchmark/screenshots/ev/
autonomous: true
requirements: [BENCH-05, BENCH-06]
must_haves:
  truths:
    - "EV (essentials.empowered.vote) is spot-checked against the same 200 W Kirkwood Ave address using the same 10 core dimensions"
    - "ev.md contains the same 6 sections as competitor files, so plan 07 can merge uniformly"
    - "≥3 screenshots captured under screenshots/ev/"
    - "EV is scored honestly — not privileged (D-10.5)"
    - "EV's antipartisan omissions are noted, not listed as gaps"
  artifacts:
    - path: ".planning/research/benchmark/ev.md"
      provides: "EV self spot-check evidence file, applying same methodology as competitors"
    - path: ".planning/research/benchmark/screenshots/ev/"
      provides: "PNG captures of EV results page, candidate profile, election central"
  key_links:
    - from: "ev.md"
      to: "AUDIT-REPORT-112.md"
      via: "race/candidate counts sourced from Phase 112 audit for BENCH-06 accuracy"
      pattern: "AUDIT-REPORT-112"
---

<objective>
Apply the SAME benchmarking methodology to Empowered Vote itself so plan 07 can merge all 5 subjects into MATRIX.md uniformly. EV's column must be produced through the same ruler as competitors — D-10.5 forbids privileging EV.

Purpose: Satisfies BENCH-05 (matrix requires EV column) and feeds BENCH-06 (EV race/candidate count vs baseline). Because Phase 112 already measured EV's race and candidate coverage against the baseline, this plan reuses that data rather than re-counting from scratch — but still applies the 10-dimension scoring rubric through a live walkthrough of the UI.
Output: `ev.md` + ≥3 PNG screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-competitive-benchmarking/113-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
@.planning/research/AUDIT-REPORT-112.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright-drive essentials.empowered.vote for the primary address + capture screenshots</name>
  <files>.planning/research/benchmark/screenshots/ev/</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/AUDIT-REPORT-112.md
  </read_first>
  <action>
Use `mcp__plugin_playwright_playwright__browser_navigate` to open `https://essentials.empowered.vote/`. Then:

1. Enter `200 W Kirkwood Ave, Bloomington, IN 47404` in the address search (Google Places autocomplete). Submit.
2. Screenshot the landing/entry screen → `01-address-entry.png`.
3. Screenshot the results page showing federal/state/local tiers → `02-results-page.png`.
4. Click into a politician profile (prefer Erin Houchin US Rep IN-9, or a contested local official matching the baseline) → `03-politician-profile.png`.
5. Navigate to Election Central (if linked from results) → `04-election-central.png`.
6. If Compass/Read & Rank are linked from a profile, capture one screen → `05-stance-or-quote.png`.

If the production site is unavailable, fall back to localhost dev server (`http://localhost:5173` for essentials with ev-accounts backend on `localhost:3000`) and note the fallback in ev.md.

Do NOT score leniently. If a photo is missing, score 0. If a bio is missing, score 0. Per Phase 112 audit: bio coverage is 0/51 linked candidates — that is a 0 score for dimension 3.
  </action>
  <verify>
    <automated>test "$(ls .planning/research/benchmark/screenshots/ev/*.png 2>/dev/null | wc -l)" -ge 3</automated>
  </verify>
  <done>≥3 PNG screenshots capturing EV address entry, results, politician profile, and any additional unique screens.</done>
</task>

<task type="auto">
  <name>Task 2: Write ev.md applying same 6-section structure with honest self-scoring</name>
  <files>.planning/research/benchmark/ev.md</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/BALLOT-BASELINE-2026-05-05.md
    - .planning/research/AUDIT-REPORT-112.md
    - .planning/research/benchmark/screenshots/ev/
  </read_first>
  <action>
Create `.planning/research/benchmark/ev.md` with the EXACT same 6-section structure used for competitors (so plan 07 can merge uniformly):

1. `# Empowered Vote — Self Spot Check` + date + primary address + note that EV is scored on the same ruler as competitors per METHODOLOGY.md D-10.5.
2. `## Access Status` — `accessible` (or note dev-server fallback if used).
3. `## Field Inventory` — 10-core-dimension table. Source data for each dimension:
   - Dimension 1 (race coverage): pull from AUDIT-REPORT-112.md (DB has 46 races, baseline has ~43 — note that some DB races are NOT surfaced for the Kirkwood address specifically, score reflects address-filtered coverage not raw DB count).
   - Dimension 2 (photo): AUDIT-REPORT-112 says 19 cdn / 0 local / 62 none → roughly 23% headshot coverage → score honestly (likely 1).
   - Dimension 3 (bio): 0/51 linked candidates have bio → score 0.
   - Dimension 4 (contact): 1.2 avg contacts per linked candidate → score 1 or 2.
   - Dimension 5 (stance): 5/51 have any stances → score 1.
   - Dimension 6 (quotes/Q&A): 4/51 have any quotes → score 1.
   - Dimension 7 (legislative record): federal + state legislative data present via essentialsLegislativeService → score 2 (lazy-loaded but real).
   - Dimension 8 (geofence/address precision): Phase 112 geofence smoke test passed → score 2 or 3 (full-address PostGIS ST_Intersects is more precise than ZIP-only competitors).
   - Dimension 9 (data freshness): note how recently data was updated (check AUDIT-REPORT-112 for signals).
   - Dimension 10 (antipartisan framing): EV deliberately omits party labels, endorsements, interest-group ratings → score 3 per METHODOLOGY.md's framing.
4. `## Race / Candidate Count vs Baseline` — reuse Phase 112's measured counts from AUDIT-REPORT-112.md directly. Table columns same as competitor files (Race | Baseline count | EV count | Match? | Notes).
5. `## Narrative Notes` — honest self-critique: what competitors do better, where EV's UX beats them, antipartisan tradeoff trade-offs.
6. `## Precinct Precision Subsection` — test the 2 secondary addresses from METHODOLOGY.md. PostGIS should correctly change the State House district race between the two — verify and record.
7. `## Blockers` — likely empty; note "none" explicitly.

EV's column in MATRIX.md will be populated by plan 07 from this file's Field Inventory table. Score honestly — D-10.5 hard rule.
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/ev.md && grep -q "^## Field Inventory" .planning/research/benchmark/ev.md && grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/ev.md && grep -q "^## Narrative Notes" .planning/research/benchmark/ev.md && grep -q "^## Precinct Precision" .planning/research/benchmark/ev.md && grep -q "^## Blockers" .planning/research/benchmark/ev.md && grep -q "200 W Kirkwood" .planning/research/benchmark/ev.md && grep -q "AUDIT-REPORT-112" .planning/research/benchmark/ev.md</automated>
  </verify>
  <done>ev.md contains all 6 required sections, field inventory sourced from AUDIT-REPORT-112 + live walkthrough, honest scoring per rubric.</done>
</task>

</tasks>

<verification>
- All 6 sections present (matching competitor file structure).
- ≥3 screenshots.
- AUDIT-REPORT-112 referenced for count data.
- EV not privileged — low scores recorded where data is missing per Phase 112 audit.
</verification>

<success_criteria>
- `test -f .planning/research/benchmark/ev.md`
- ≥3 screenshots in screenshots/ev/
- All 6 sections grep-present
- AUDIT-REPORT-112 referenced
</success_criteria>

<output>
After completion, create `.planning/phases/113-competitive-benchmarking/113-06-SUMMARY.md`: top 3 EV strengths, top 3 EV weaknesses (per own 0-3 scoring), intentional-omission dimensions confirmed.
</output>
