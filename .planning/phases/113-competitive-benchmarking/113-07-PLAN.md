---
phase: 113-competitive-benchmarking
plan: 07
type: execute
wave: 3
depends_on: [113-01, 113-02, 113-03, 113-04, 113-05, 113-06]
files_modified:
  - .planning/research/benchmark/MATRIX.md
  - .planning/research/benchmark/matrix.csv
autonomous: true
requirements: [BENCH-05, BENCH-06]
must_haves:
  truths:
    - "MATRIX.md has a 0-3 or 'blocked' score in every cell across 10 core dimensions × 5 subjects (50 cells)"
    - "Every scored cell has a short evidence note citing the source per-competitor file"
    - "MATRIX.md contains a per-competitor race/candidate count comparison table vs BALLOT-BASELINE-2026-05-05.md"
    - "matrix.csv mirrors MATRIX.md scores in machine-readable form"
    - "Any derived-extra dimensions (beyond the core 10) are listed and scored for subjects that surfaced them"
  artifacts:
    - path: ".planning/research/benchmark/MATRIX.md"
      provides: "Fully scored benchmark matrix + race/candidate count comparison table"
    - path: ".planning/research/benchmark/matrix.csv"
      provides: "Machine-readable mirror of MATRIX.md for downstream phase 115 synthesis"
  key_links:
    - from: "MATRIX.md"
      to: "ballotready.md, vote411.md, votesmart.md, ballotpedia.md, ev.md"
      via: "cell evidence notes reference the corresponding subject file"
      pattern: "\\.md"
    - from: "MATRIX.md race count table"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "explicit denominator comparison"
      pattern: "BALLOT-BASELINE-2026-05-05"
---

<objective>
Merge the 5 per-subject spot-check files into the fully scored MATRIX.md + matrix.csv. Populate every cell with a 0-3 score (or `blocked`) plus a short evidence note. Add the per-competitor race/candidate count comparison table that directly satisfies BENCH-06. Add derived-extra dimensions if any competitor surfaced features beyond the core 10.

Purpose: Final synthesis step — converts 5 independent evidence files into one comparable scoreboard. Satisfies BENCH-05 (feature comparison matrix) and BENCH-06 (coverage depth comparison).
Output: Updated MATRIX.md (scored), updated matrix.csv (scored), no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-competitive-benchmarking/113-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
@.planning/research/benchmark/ballotready.md
@.planning/research/benchmark/vote411.md
@.planning/research/benchmark/votesmart.md
@.planning/research/benchmark/ballotpedia.md
@.planning/research/benchmark/ev.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate MATRIX.md cells with 0-3 scores + evidence notes from all 5 subject files</name>
  <files>.planning/research/benchmark/MATRIX.md</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/benchmark/ballotready.md
    - .planning/research/benchmark/vote411.md
    - .planning/research/benchmark/votesmart.md
    - .planning/research/benchmark/ballotpedia.md
    - .planning/research/benchmark/ev.md
    - .planning/research/benchmark/MATRIX.md
  </read_first>
  <action>
Read each per-subject file's `## Field Inventory` table. For each of the 10 core dimensions × 5 subjects = 50 cells, replace the `TBD` placeholder in MATRIX.md with:

- The 0-3 depth score (or `blocked`) from the subject file's Field Inventory
- A parenthetical evidence note in the form `(see ballotready.md#field-inventory row 3)` or similar short citation

Format: `3 (quotes: ballotready.md)` in the cell, or for compactness `3¹` with footnote. Choose whichever fits the table width — but EVERY cell must have both a score and a trace to the source file.

**Hard rules:**
- No cell may remain `TBD`.
- `blocked` and `0` are distinct — preserve the distinction from the subject files.
- EV's column is scored from ev.md's self-scoring, NOT adjusted upward.
- If a subject file shows a cell as `0` or `blocked`, MATRIX.md records it faithfully.

After the core 10 × 5 scoring table, append a `## Race / Candidate Count vs Baseline` section containing a table with columns: `Race (from baseline) | Baseline candidates | EV | BallotReady | Vote411 | VoteSmart | Ballotpedia`. Source row data from each per-subject file's `## Race / Candidate Count vs Baseline` section. At minimum include: US Rep IN-9, IN State Rep 61, County Prosecutor, County Clerk, County Recorder, County Sheriff, County Assessor, County Commissioner D-1, Judge Circuit Court Division 6, Judge Circuit Court Division 1. Add a `TOTAL` row with race-count totals and a `MATCH %` row showing what fraction of baseline races each competitor surfaced for the Kirkwood address.

After the count table, append a `## Derived-Extra Dimensions` section. For each extra dimension found in any subject file's Field Inventory that's NOT in the core 10 (e.g., interest-group ratings from VoteSmart, Candidate Connection survey from Ballotpedia, endorsements, campaign finance, donor tracking), add a row showing which subject has it and its 0-3 score. Up to 5 extras per D-04.

Also append a `## Score Summary` section with per-subject totals: sum of core-10 scores (max 30) and a one-line interpretation. EV's total is whatever it is — do not adjust.
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/MATRIX.md && ! grep -q "TBD" .planning/research/benchmark/MATRIX.md && grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/MATRIX.md && grep -q "^## Derived-Extra Dimensions" .planning/research/benchmark/MATRIX.md && grep -q "^## Score Summary" .planning/research/benchmark/MATRIX.md && grep -q "BALLOT-BASELINE-2026-05-05\|baseline" .planning/research/benchmark/MATRIX.md && test "$(grep -c '^|' .planning/research/benchmark/MATRIX.md)" -ge 25</automated>
  </verify>
  <done>MATRIX.md has no remaining TBD cells, has race count table, derived-extras section, and score summary. Every core-10 × 5 cell is a score (0-3 or `blocked`) with an evidence trace.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate matrix.csv from MATRIX.md scores in machine-readable form</name>
  <files>.planning/research/benchmark/matrix.csv</files>
  <read_first>
    - .planning/research/benchmark/MATRIX.md
    - .planning/research/benchmark/matrix.csv
  </read_first>
  <action>
Rewrite `.planning/research/benchmark/matrix.csv` to mirror MATRIX.md scores as a normalized long-form table (Claude's discretion per D-07). Format:

```
subject,dimension,score,evidence_file
ev,race_coverage_vs_baseline,2,ev.md
ev,candidate_photo,1,ev.md
...
ballotready,race_coverage_vs_baseline,3,ballotready.md
...
```

Columns: `subject` (one of ev|ballotready|vote411|votesmart|ballotpedia), `dimension` (snake_case, one of the 10 core + any derived extras), `score` (0|1|2|3|blocked), `evidence_file` (source .md filename).

Expected row count: 10 core dimensions × 5 subjects = 50 data rows + 1 header = 51 lines minimum. Plus derived-extras rows (subject × extra combinations actually scored).

Score values MUST match MATRIX.md exactly — this is a mirror, not a re-interpretation.
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/matrix.csv && test "$(wc -l < .planning/research/benchmark/matrix.csv)" -ge 51 && head -1 .planning/research/benchmark/matrix.csv | grep -q "subject,dimension,score" && grep -q "^ev," .planning/research/benchmark/matrix.csv && grep -q "^ballotready," .planning/research/benchmark/matrix.csv && grep -q "^vote411," .planning/research/benchmark/matrix.csv && grep -q "^votesmart," .planning/research/benchmark/matrix.csv && grep -q "^ballotpedia," .planning/research/benchmark/matrix.csv</automated>
  </verify>
  <done>matrix.csv exists with ≥51 lines (header + 50 core cells), contains rows for all 5 subjects, header has subject/dimension/score columns, values mirror MATRIX.md.</done>
</task>

</tasks>

<verification>
- MATRIX.md: no TBD remaining, 50 core cells scored, count-vs-baseline table present, derived-extras section present, score summary present.
- matrix.csv: ≥51 rows, all 5 subjects present, mirrors MATRIX.md.
- Every cell traces back to a subject file via evidence note.
- EV column reflects ev.md's honest self-scoring, not privileged.
</verification>

<success_criteria>
- `! grep -q "TBD" .planning/research/benchmark/MATRIX.md`
- `grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/MATRIX.md`
- `grep -q "^## Derived-Extra Dimensions" .planning/research/benchmark/MATRIX.md`
- `test $(wc -l < .planning/research/benchmark/matrix.csv) -ge 51`
- All 5 subjects present in matrix.csv
</success_criteria>

<output>
After completion, create `.planning/phases/113-competitive-benchmarking/113-07-SUMMARY.md` with: per-subject core-10 totals, top 3 dimensions where EV leads, top 3 where EV lags, baseline race-match rates per competitor, list of derived-extra dimensions added, any blocked cells summarized.
</output>
