---
phase: 113-competitive-benchmarking
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - .planning/research/benchmark/ballotready.md
  - .planning/research/benchmark/screenshots/ballotready/
autonomous: false
requirements: [BENCH-01]
must_haves:
  truths:
    - "A live BallotReady session for 200 W Kirkwood Ave is documented"
    - "ballotready.md contains field inventory, race/candidate count table, narrative, and precinct precision subsection"
    - "≥3 screenshots captured under screenshots/ballotready/"
    - "Any blocker (signup/captcha/paywall) is logged with human-fallback instructions, not silently treated as absent"
  artifacts:
    - path: ".planning/research/benchmark/ballotready.md"
      provides: "BallotReady spot-check evidence file (D-03 four evidence types)"
    - path: ".planning/research/benchmark/screenshots/ballotready/"
      provides: "PNG captures of address entry, race list, candidate profile"
  key_links:
    - from: "ballotready.md"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "race/candidate count comparison table"
      pattern: "BALLOT-BASELINE-2026-05-05"
---

<objective>
Run a live BallotReady spot-check against the primary Bloomington address and produce all four D-03 evidence types: screenshots, structured field inventory, race/candidate count vs baseline, and narrative notes. Also run the 2 secondary addresses for a precinct precision subsection.

Purpose: Satisfies BENCH-01. Feeds plan 07's matrix scoring for the BallotReady column.
Output: `ballotready.md` + ≥3 PNG screenshots in `screenshots/ballotready/`.

Note: `autonomous: false` because BallotReady is the competitor most likely to require signup or account creation. If Playwright hits a signup wall, the executor MUST log the blocker and write human-fallback instructions per D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-competitive-benchmarking/113-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright-drive BallotReady for the primary address + capture screenshots</name>
  <files>
    .planning/research/benchmark/screenshots/ballotready/
  </files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
  </read_first>
  <action>
Use `mcp__plugin_playwright_playwright__browser_navigate` to open `https://www.ballotready.org/`. Then:

1. Locate the address entry input (use `browser_snapshot` to identify). Enter `200 W Kirkwood Ave, Bloomington, IN 47404` using `browser_fill_form` or `browser_type`, submit.
2. `browser_take_screenshot` at the address-entry screen BEFORE submitting → save as `.planning/research/benchmark/screenshots/ballotready/01-address-entry.png`.
3. `browser_take_screenshot` after submission at the race list / ballot page → `02-race-list.png`.
4. Click through to one federal or state race (prefer US Rep IN-9 or State Rep IN-61 to match baseline's contested races) → `03-race-detail.png`.
5. Click through to one candidate profile (prefer a D candidate in US Rep IN-9 — e.g., James H. Graham, Brad Meyer, Tim Peck, or Keil Roark) → `04-candidate-profile.png`.
6. If additional fields are visible (contact info, quotes, endorsements) take supplemental screenshots up to `0N-*.png`.

**Blocker handling (D-02 — hard rule):** If at any step BallotReady demands signup, captcha, ZIP-only instead of full address, or returns no Monroe County data:
- Do NOT silently continue.
- Take a screenshot of the blocker screen → `blocker-{step}.png`.
- Write the exact blocker condition + human-fallback instructions into task 2's narrative file (step URL, form values to enter, what to screenshot, where to paste results back).
- Mark the task `checkpoint:human-action` style pause via the executor's blocker protocol so the user can complete manually.

Capture at least 3 non-blocker screenshots if access permits. If fully blocked, capture ≥1 blocker screenshot + write full fallback instructions.
  </action>
  <verify>
    <automated>test "$(ls .planning/research/benchmark/screenshots/ballotready/*.png 2>/dev/null | wc -l)" -ge 3 || { test "$(ls .planning/research/benchmark/screenshots/ballotready/blocker-*.png 2>/dev/null | wc -l)" -ge 1 && grep -qi "blocked\|fallback\|signup" .planning/research/benchmark/ballotready.md; }</automated>
  </verify>
  <done>≥3 PNG screenshots exist in screenshots/ballotready/ capturing address entry, race list, and at least one candidate profile — OR a blocker screenshot + fallback instructions exist if access was denied.</done>
</task>

<task type="auto">
  <name>Task 2: Write ballotready.md with field inventory, count comparison, narrative, precinct precision</name>
  <files>.planning/research/benchmark/ballotready.md</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/BALLOT-BASELINE-2026-05-05.md
    - .planning/research/benchmark/screenshots/ballotready/
  </read_first>
  <action>
Create `.planning/research/benchmark/ballotready.md` with these sections (exact heading names so plan 07 can grep):

1. `# BallotReady — Spot Check` + run date + primary address used.

2. `## Access Status` — `accessible` | `partially_blocked` | `blocked`. If blocked, include the human-fallback instructions (step-by-step: URL, form values, what to screenshot, paste-back format).

3. `## Field Inventory` — markdown table with columns: `Field | Present? (Y/N/blocked) | Depth (0-3) | Evidence (screenshot/URL)`. Rows MUST cover the 10 core dimensions from METHODOLOGY.md (race coverage, candidate photo, candidate bio, candidate contact, stance/issue, quotes/Q&A, legislative record, geofence/address precision, data freshness, antipartisan framing). Plus any extra fields BallotReady surfaces (endorsements, donors, ratings) as additional rows tagged `extra`.

4. `## Race / Candidate Count vs Baseline` — markdown table comparing BallotReady's counts for 200 W Kirkwood against BALLOT-BASELINE-2026-05-05.md. Columns: `Race (from baseline) | Baseline candidate count | BallotReady candidate count | Match? (Y/N/blocked) | Notes`. Include at minimum: US Rep IN-9, IN State Rep 61 (Kirkwood is in district 61), all county-wide races (Prosecutor, Clerk, Recorder, Sheriff, Assessor, Commissioner D-1), and judicial races. Total row at bottom: `TOTAL baseline races for this address | X | Y | match-rate%`.

5. `## Narrative Notes` — free-form observations: UX quirks, editorial quality, data freshness signals (last-updated timestamps visible?), partisanship signals (party labels, endorsements, interest-group ratings visible? — relevant to dimension 10), surprises.

6. `## Precinct Precision Subsection` — re-run BallotReady against the 2 secondary addresses from METHODOLOGY.md (rural Benton Township + opposite-side House District 61/62 address). Do NOT re-score the full matrix. Only answer: "Did the race list change correctly when the address moved across a district boundary?" Record for each secondary address: what changed, what stayed the same, whether State House district selection flipped as expected. 1 screenshot per secondary address is sufficient (saved as `05-secondary-benton.png`, `06-secondary-d62.png`).

7. `## Blockers` — bullet list of any auth/captcha/paywall/signup encountered with exact step and screenshot reference.

Use the METHODOLOGY.md 0-3 rubric for depth numbers. Do NOT score EV's column here — that's plan 06/07.
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/ballotready.md && grep -q "^## Field Inventory" .planning/research/benchmark/ballotready.md && grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/ballotready.md && grep -q "^## Narrative Notes" .planning/research/benchmark/ballotready.md && grep -q "^## Precinct Precision" .planning/research/benchmark/ballotready.md && grep -q "^## Blockers" .planning/research/benchmark/ballotready.md && grep -q "200 W Kirkwood" .planning/research/benchmark/ballotready.md</automated>
  </verify>
  <done>ballotready.md contains all 6 sections (Access Status, Field Inventory, Race/Candidate Count vs Baseline, Narrative Notes, Precinct Precision, Blockers) with real data or documented blocker.</done>
</task>

</tasks>

<verification>
- Evidence file exists with all required sections.
- ≥3 screenshots OR a documented blocker with fallback.
- Field inventory covers all 10 core dimensions.
- Count comparison references baseline file explicitly.
</verification>

<success_criteria>
- `test -f .planning/research/benchmark/ballotready.md`
- `test $(ls .planning/research/benchmark/screenshots/ballotready/*.png 2>/dev/null | wc -l) -ge 3` (or blocker case)
- All 6 sections grep-present in ballotready.md
</success_criteria>

<output>
After completion, create `.planning/phases/113-competitive-benchmarking/113-02-SUMMARY.md` summarizing: access status, screenshot count, baseline match rate (races found / baseline races expected), top 3 observations, any blockers encountered.
</output>
