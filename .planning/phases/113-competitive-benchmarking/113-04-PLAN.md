---
phase: 113-competitive-benchmarking
plan: 04
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - .planning/research/benchmark/votesmart.md
  - .planning/research/benchmark/screenshots/votesmart/
autonomous: false
requirements: [BENCH-03]
must_haves:
  truths:
    - "A live VoteSmart session for 200 W Kirkwood Ave is documented"
    - "votesmart.md contains field inventory, race/candidate count table, narrative, and precinct precision subsection"
    - "≥3 screenshots captured under screenshots/votesmart/"
    - "Any blocker is logged with human-fallback instructions"
  artifacts:
    - path: ".planning/research/benchmark/votesmart.md"
      provides: "VoteSmart spot-check evidence file (D-03 four evidence types)"
    - path: ".planning/research/benchmark/screenshots/votesmart/"
      provides: "PNG captures of address entry, race list, candidate profile with interest-group ratings"
  key_links:
    - from: "votesmart.md"
      to: "BALLOT-BASELINE-2026-05-05.md"
      via: "race/candidate count comparison table"
      pattern: "BALLOT-BASELINE-2026-05-05"
---

<objective>
Run a live VoteSmart spot-check. VoteSmart is distinctive for interest-group ratings and voting records on sitting officials — both of which are EV intentional omissions (dimension 10 antipartisan). Scoring must reflect this honestly: VoteSmart may score high on "legislative record" and low on antipartisan framing.

Purpose: Satisfies BENCH-03. Feeds plan 07's matrix scoring for the VoteSmart column.
Output: `votesmart.md` + ≥3 PNG screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-competitive-benchmarking/113-CONTEXT.md
@.planning/research/benchmark/METHODOLOGY.md
@.planning/research/BALLOT-BASELINE-2026-05-05.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright-drive VoteSmart for the primary address + capture screenshots</name>
  <files>.planning/research/benchmark/screenshots/votesmart/</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
  </read_first>
  <action>
Use `mcp__plugin_playwright_playwright__browser_navigate` to open `https://justfacts.votesmart.org/` (primary entry point from D-CONTEXT). If the justfacts subdomain redirects or fails, fall back to `https://votesmart.org/`. Then:

1. Locate address entry / "Find your candidates" input. Enter `200 W Kirkwood Ave, Bloomington, IN 47404`. If the tool only accepts ZIP, use `47404`.
2. Screenshot entry → `01-address-entry.png`.
3. Screenshot race/officials list → `02-officials-list.png`.
4. Click an incumbent (Erin Houchin — US Rep IN-9 R — is on the baseline and likely has a full VoteSmart profile) → `03-candidate-profile.png`.
5. Capture at least one of VoteSmart's distinctive screens: "Ratings" (interest-group ratings) or "Voting Record" tab → `04-ratings-or-votes.png`.
6. Supplemental screenshots for unique fields.

**Blocker handling (D-02):** VoteSmart generally allows public access without signup, so blockers are less likely. If hit, log per the rule.
  </action>
  <verify>
    <automated>test "$(ls .planning/research/benchmark/screenshots/votesmart/*.png 2>/dev/null | wc -l)" -ge 3 || { test "$(ls .planning/research/benchmark/screenshots/votesmart/blocker-*.png 2>/dev/null | wc -l)" -ge 1 && grep -qi "blocked\|fallback" .planning/research/benchmark/votesmart.md; }</automated>
  </verify>
  <done>≥3 PNG screenshots capturing entry, list, candidate profile, and ratings/voting-record screen — or blocker case.</done>
</task>

<task type="auto">
  <name>Task 2: Write votesmart.md with field inventory, count comparison, narrative, precinct precision</name>
  <files>.planning/research/benchmark/votesmart.md</files>
  <read_first>
    - .planning/phases/113-competitive-benchmarking/113-CONTEXT.md
    - .planning/research/benchmark/METHODOLOGY.md
    - .planning/research/BALLOT-BASELINE-2026-05-05.md
    - .planning/research/benchmark/screenshots/votesmart/
  </read_first>
  <action>
Create `.planning/research/benchmark/votesmart.md` with the same 6-section structure (headings verbatim):

1. `# VoteSmart — Spot Check` + date + primary address.
2. `## Access Status`
3. `## Field Inventory` — 10 core dimensions + VoteSmart extras (interest-group ratings, voting records, speeches, bio, public statements, campaign finance). These extras will be added to MATRIX.md derived-extras section by plan 07.
4. `## Race / Candidate Count vs Baseline` — VoteSmart is weak on primary races (typically lists sitting officials, not primary challengers) — this is expected and MUST be scored honestly on dimension 1 (race coverage).
5. `## Narrative Notes` — call out interest-group rating presence (affects dimension 10 antipartisan — VoteSmart likely scores low here) and legislative record depth (dimension 7 — likely scores high for incumbents like Houchin).
6. `## Precinct Precision Subsection` — re-run 2 secondary addresses. VoteSmart uses official → district lookup rather than address→precinct, so district precision depends on backend district data.
7. `## Blockers`
  </action>
  <verify>
    <automated>test -f .planning/research/benchmark/votesmart.md && grep -q "^## Field Inventory" .planning/research/benchmark/votesmart.md && grep -q "^## Race / Candidate Count vs Baseline" .planning/research/benchmark/votesmart.md && grep -q "^## Narrative Notes" .planning/research/benchmark/votesmart.md && grep -q "^## Precinct Precision" .planning/research/benchmark/votesmart.md && grep -q "^## Blockers" .planning/research/benchmark/votesmart.md && grep -q "200 W Kirkwood" .planning/research/benchmark/votesmart.md</automated>
  </verify>
  <done>votesmart.md contains all 6 required sections with real data or documented blocker.</done>
</task>

</tasks>

<verification>
- All 6 sections present.
- ≥3 screenshots OR blocker case.
- Interest-group ratings and legislative record both addressed in narrative.
</verification>

<success_criteria>
- `test -f .planning/research/benchmark/votesmart.md`
- Screenshots or blocker case met
- All 6 sections grep-present
</success_criteria>

<output>
After completion, create `.planning/phases/113-competitive-benchmarking/113-04-SUMMARY.md`: access status, screenshot count, baseline match rate, top 3 observations (esp. legislative record + ratings), any blockers.
</output>
