---
phase: 45-legacy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/internal/seeds/topics.go
  - EV-Backend/internal/seeds/categories.go
  - EV-Backend/internal/seeds/seeds.go
  - EV-Backend/internal/compass/data/topics.json
  - EV-Backend/cmd/seed/main.go
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "The old 50-topic topics.json file no longer exists in the repository"
    - "The seeds/topics.go seed function no longer exists"
    - "The seeds/categories.go hardcoded category map no longer exists"
    - "The entire internal/seeds/ directory is gone (no orphan package)"
    - "The 21-topic/5-stance CSV and compass_csv_seeder.go are present and unchanged"
    - "go build ./... compiles without errors after deletion"
  artifacts:
    - path: "EV-Backend/Empowered Compass Issues and Stances Top 20.csv"
      provides: "21-topic/5-stance source of truth CSV"
    - path: "EV-Backend/cmd/seed/compass_csv_seeder.go"
      provides: "CSV-based seeder (sole seeder)"
  key_links:
    - from: "EV-Backend/cmd/seed/main.go"
      to: "internal/seeds (deleted)"
      via: "commented-out import removed"
      pattern: "no reference to internal/seeds anywhere in codebase"
---

<objective>
Remove all deprecated 50-topic compass data files and old seed code from EV-Backend, leaving the 21-topic/5-stance CSV and compass_csv_seeder.go as the sole source of truth.

Purpose: The old 50-topic topics.json and internal/seeds/ package are dead code that was superseded by the CSV seeder. Removing them eliminates confusion about which seeder to use and prevents accidental use of the deprecated 50-topic data.

Output: Clean repository with only the CSV-based seeding path.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key facts established during planning:

1. `EV-Backend/internal/seeds/` contains 3 files: `topics.go`, `categories.go`, `seeds.go`. All are dead code — the main.go in `cmd/seed/main.go` has the seeds import fully commented out. The root `main.go` does NOT import seeds at all.

2. `EV-Backend/internal/compass/data/topics.json` is a 2351-line JSON file with 50 topics (6+ stances each). It is ONLY referenced by `internal/seeds/topics.go`. No other file reads it.

3. `EV-Backend/cmd/seed/main.go` currently has its entire `main()` function and imports commented out. After removing the seeds reference, the file should contain a valid but empty main package (since `compass_csv_seeder.go` in the same package already defines `func main()`).

4. `EV-Backend/cmd/seed/compass_csv_seeder.go` already defines `package main` and `func main()`. The `cmd/seed/main.go` file with its commented-out duplicate main is actually a build conflict — it can be deleted entirely since compass_csv_seeder.go owns the main function.

5. `EV-Backend/Empowered Compass Issues and Stances Top 20.csv` is the 21-topic/5-stance CSV (20 data rows + 1 header). This file and compass_csv_seeder.go must NOT be modified.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated seed files and old topics.json</name>
  <files>
    EV-Backend/internal/seeds/topics.go
    EV-Backend/internal/seeds/categories.go
    EV-Backend/internal/seeds/seeds.go
    EV-Backend/internal/compass/data/topics.json
    EV-Backend/cmd/seed/main.go
  </files>
  <action>
Delete the following files using `rm`:

1. `EV-Backend/internal/seeds/topics.go` — old SeedTopics() that reads topics.json
2. `EV-Backend/internal/seeds/categories.go` — hardcoded 50-topic category map and SeedCategories()/SeedTopicCategories()
3. `EV-Backend/internal/seeds/seeds.go` — SeedAll() orchestrator
4. `EV-Backend/internal/compass/data/topics.json` — the 2351-line 50-topic JSON data file
5. `EV-Backend/cmd/seed/main.go` — entirely commented-out stub that referenced seeds. This file must go because compass_csv_seeder.go in the same directory already defines `func main()`.

After deleting the 3 files in `internal/seeds/`, remove the now-empty `internal/seeds/` directory with `rmdir`.
Also remove the now-empty `internal/compass/data/` directory with `rmdir`.

Do NOT modify:
- `EV-Backend/cmd/seed/compass_csv_seeder.go`
- `EV-Backend/Empowered Compass Issues and Stances Top 20.csv`
  </action>
  <verify>
    <automated>cd EV-Backend && go build ./... && echo "BUILD OK"</automated>
  </verify>
  <done>
    - `internal/seeds/` directory does not exist
    - `internal/compass/data/topics.json` does not exist
    - `cmd/seed/main.go` does not exist
    - `go build ./...` compiles without errors
    - `cmd/seed/compass_csv_seeder.go` is unchanged (verify with `git diff cmd/seed/compass_csv_seeder.go` showing no changes)
    - `Empowered Compass Issues and Stances Top 20.csv` is unchanged (verify with `git diff "Empowered Compass Issues and Stances Top 20.csv"` showing no changes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no remaining references to deleted code</name>
  <files></files>
  <action>
Run a comprehensive search across the entire EV-Backend directory for any remaining references to the deleted code:

1. `grep -r "internal/seeds" EV-Backend/ --include="*.go"` — should return zero matches
2. `grep -r "topics\.json" EV-Backend/ --include="*.go"` — should return zero matches
3. `grep -r "SeedAll\|SeedTopics\|SeedCategories\|SeedTopicCategories" EV-Backend/ --include="*.go"` — should return zero matches

If any matches are found, remove or update the references.

Also confirm the preserved files are intact:
4. Verify `EV-Backend/cmd/seed/compass_csv_seeder.go` exists and contains `func main()`
5. Verify `EV-Backend/Empowered Compass Issues and Stances Top 20.csv` exists and has 21 lines (1 header + 20 topics)
  </action>
  <verify>
    <automated>cd EV-Backend && grep -r "internal/seeds" --include="*.go" . && echo "FAIL: stale references found" || echo "CLEAN: no stale references" && grep -r "topics\.json" --include="*.go" . && echo "FAIL: stale references found" || echo "CLEAN: no stale references" && test -f cmd/seed/compass_csv_seeder.go && echo "CSV seeder exists" && test -f "Empowered Compass Issues and Stances Top 20.csv" && echo "CSV data exists"</automated>
  </verify>
  <done>
    - Zero grep matches for "internal/seeds", "topics.json", "SeedAll", "SeedTopics", "SeedCategories", "SeedTopicCategories" across all .go files
    - compass_csv_seeder.go exists and is unmodified
    - The 21-topic CSV exists and is unmodified
  </done>
</task>

</tasks>

<verification>
1. `ls EV-Backend/internal/seeds/` returns "No such file or directory"
2. `ls EV-Backend/internal/compass/data/topics.json` returns "No such file or directory"
3. `ls EV-Backend/cmd/seed/main.go` returns "No such file or directory"
4. `cd EV-Backend && go build ./...` succeeds with exit code 0
5. `wc -l "EV-Backend/Empowered Compass Issues and Stances Top 20.csv"` returns 21
6. `head -1 EV-Backend/cmd/seed/compass_csv_seeder.go` returns "package main"
7. `grep -r "internal/seeds\|topics\.json" EV-Backend/ --include="*.go"` returns nothing
</verification>

<success_criteria>
- The old 50-topic topics.json no longer exists in the repository
- The entire internal/seeds/ package (topics.go, categories.go, seeds.go) no longer exists
- The cmd/seed/main.go stub no longer exists
- The Go project compiles cleanly with `go build ./...`
- The 21-topic CSV and compass_csv_seeder.go are present and completely unmodified
- No Go source files reference the deleted code
</success_criteria>

<output>
After completion, create `.planning/phases/45-legacy-cleanup/45-01-SUMMARY.md`
</output>
