---
phase: 99-election-central-page
plan: "06"
type: execute
wave: 4
depends_on:
  - "99-05"
gap_closure: true
files_modified:
  - "C:\\Transparent Motivations\\essentials\\src\\components\\ElectionsView.jsx"
  - "C:\\Transparent Motivations\\essentials\\src\\App.jsx"
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - ELEC-01
  - ELEC-02
  - ELEC-03
must_haves:
  truths:
    - "Race headers in the elections list display position_name only — no '— DEMOCRATIC PRIMARY' / '— REPUBLICAN PRIMARY' / any party name appears in the race title (antipartisan per D-02 reaffirmed by UAT test 6 failure)"
    - "Navigating directly to https://essentials.empowered.vote/elections with a stored address auto-loads UT 2026 Primary elections without requiring the user to manually re-submit the address form (UAT test 5 gap)"
    - "essentials repo `npm run build` passes with no errors after both source changes"
    - "essentials master branch has a fix commit that has been pushed (Render auto-deploys)"
    - "REQUIREMENTS.md traceability table (lines ~141-143) shows ELEC-01, ELEC-02, ELEC-03 status as `Complete` (not `Pending`) — checkboxes and table now agree"
  artifacts:
    - path: "C:\\Transparent Motivations\\essentials\\src\\components\\ElectionsView.jsx"
      provides: "Antipartisan race header rendering — subgroupLabel no longer references party"
      contains: "const subgroupLabel = subgroup"
    - path: "C:\\Transparent Motivations\\essentials\\src\\App.jsx"
      provides: "/elections route reads stored address from cross-app context bridge and redirects to /results with q= populated so auto-fetch triggers"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability table corrected — ELEC rows show Complete"
      contains: "ELEC-01 | Phase 99 | Complete"
  key_links:
    - from: "ElectionsView.jsx subgroupLabel"
      to: "race card header rendering"
      via: "label prop on race row"
      pattern: "subgroupLabel = subgroup"
    - from: "App.jsx /elections route"
      to: "/results?prefilled=true&view=elections&q=<encoded address>"
      via: "redirect component that reads ev-context / localStorage before navigating"
      pattern: "q=encodeURIComponent(storedAddress)"
    - from: "Results.jsx activeQuery (existing)"
      to: "GET /api/essentials/elections-by-address"
      via: "usePoliticianData + elections useEffect (line ~746)"
      pattern: "now satisfied because redirect supplies q="
---

<objective>
Close two gaps recorded in 99-VERIFICATION.md so Phase 99 actually satisfies ELEC-01 / ELEC-02 / ELEC-03 (currently marked [x] but the underlying behavior fails the UAT).

Gap 1 (BLOCKER): ElectionsView.jsx line 361 renders primary_party directly in every primary race header ("ASSESSOR — DEMOCRATIC PRIMARY"). This violates the antipartisan design requirement reaffirmed in 99-CONTEXT.md D-02 ("primary_party never rendered in UI"). Fix is a one-line change to drop party from the displayed label while keeping party available for the internal grouping subgroupKey (so React keys stay unique for Democratic vs Republican primaries of the same office).

Gap 2 (WARNING): App.jsx /elections route is a static `<Navigate to="/results?prefilled=true&view=elections" replace />` with no q= param. Results.jsx:434 derives activeQuery from queryFromUrl (which comes from searchParams.get('q') at line 366). With no q=, the elections fetch never fires. Fix is to replace the static Navigate with a small component that reads the stored address from the same cross-app context bridge already used by Results.jsx (loadUserAddressFromContext) and emits a redirect URL that includes q=<encoded address>.

Plus a small REQUIREMENTS.md cleanup: the checkboxes were flipped to [x] by Plan 05 but the traceability table at lines ~141-143 still shows the three ELEC rows as Pending. After the real fix lands, update the table to Complete.

Purpose: Stop the false ship. Get the page actually antipartisan and actually auto-loading before re-declaring ELEC-01/02/03 satisfied.

Output: 2 source changes in the essentials repo + 1 commit pushed + REQUIREMENTS.md traceability table corrected + smoke test confirming both behaviors now pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-election-central-page/99-CONTEXT.md
@.planning/phases/99-election-central-page/99-VERIFICATION.md
@.planning/phases/99-election-central-page/99-UAT.md
@.planning/phases/99-election-central-page/99-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix antipartisan race header in ElectionsView.jsx (Gap 1 — BLOCKER)</name>
  <files>C:\Transparent Motivations\essentials\src\components\ElectionsView.jsx</files>
  <read_first>
    - C:\Transparent Motivations\essentials\src\components\ElectionsView.jsx (lines 320–400 — deriveBodyAndSubGroup, the tier/body/subgroup loop at line 355, and how subgroupKey + subgroupLabel flow into tierMap[tier][body].races.push({ key, label, party, ... }) and downstream into the rendered race card header)
    - .planning/phases/99-election-central-page/99-VERIFICATION.md (Gap 1 specifics — line 361 is the offender)
    - .planning/phases/99-election-central-page/99-CONTEXT.md (D-02 — antipartisan is a defining design principle; primary_party must never be rendered to the user)
    - .planning/phases/99-election-central-page/99-UAT.md (test 6 — confirms the user-observable failure mode: "ASSESSOR — DEMOCRATIC PRIMARY")
  </read_first>
  <action>
Open C:\Transparent Motivations\essentials\src\components\ElectionsView.jsx and locate the loop that builds subgroupKey and subgroupLabel (around line 357–361). The exact current source lines are:

- Line 359: party assignment — `const party = isPrimary && race.primary_party ? race.primary_party : null;`
- Line 360: subgroupKey assignment — `const subgroupKey = party ? \`${subgroup}||${party}\` : subgroup;`
- Line 361: subgroupLabel assignment — `const subgroupLabel = party ? \`${subgroup} — ${party} Primary\` : subgroup;`

Change line 361 so that subgroupLabel is set to subgroup regardless of party. The new line must be exactly: const subgroupLabel = subgroup;

Keep lines 359 and 360 unchanged. The party variable is still used by subgroupKey to keep Democratic-vs-Republican primaries of the same position as separate React keys, and party is still pushed onto the race object (the existing party, field inside tierMap[tier][body].races.push({ ... })) where downstream rendering MAY use it for non-title purposes (a subtle pill, a filter, etc.). Do NOT delete the party variable or the party field on the pushed race object — only the user-visible title label is being de-partied.

After the change, grep the file for any other ${party} / primary_party / Democratic Primary / Republican Primary string concatenation that flows into JSX rendered to the user. Specifically look for:
- Any JSX node that renders {race.label} or {race.party} or {race.primary_party} directly as a heading or title
- Any other template literal that concatenates party into a user-facing string

If any such additional render path exists and renders the party name visibly as a title, also strip it. Do NOT remove uses of party that are pure internal grouping/sorting/keying or that gate a subtle non-title styling. Document any additional changes you make in the task summary.

If no other party-in-title render paths exist, the only edit is the one-line change at line 361.

Do not commit yet — Task 3 batches both source changes into one commit.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('C:/Transparent Motivations/essentials/src/components/ElectionsView.jsx','utf8');if(!/const subgroupLabel = subgroup;/.test(s)){process.exit(1);}if(/const subgroupLabel = party \?/.test(s)){process.exit(2);}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal source line: const subgroupLabel = subgroup; (semicolon required, exact match)
    - File does NOT contain the original `const subgroupLabel = party ?` ternary on any line
    - subgroupKey ternary using party is unchanged (party still keys the grouping)
    - The party, field is still pushed onto the race object inside tierMap[tier][body].races.push({ ... }) (downstream consumers untouched)
    - No other user-visible JSX in the file concatenates party / primary_party into a title or header heading (grep audit clean or documented exceptions only)
  </acceptance_criteria>
  <done>ElectionsView.jsx line 361 modified — race headers now render position_name only, no party name in title</done>
</task>

<task type="auto">
  <name>Task 2: Fix /elections auto-fetch by populating q= from stored address (Gap 2)</name>
  <files>C:\Transparent Motivations\essentials\src\App.jsx</files>
  <read_first>
    - C:\Transparent Motivations\essentials\src\App.jsx (current full file — note the static Navigate on line 72 routing /elections to /results?prefilled=true&view=elections, and existing imports of Navigate / useLocation from react-router-dom)
    - C:\Transparent Motivations\essentials\src\pages\Results.jsx (lines 360–410 — confirm queryFromUrl = searchParams.get('q') || '', activeQuery = queryFromUrl, and the existing loadUserAddressFromContext prefill effect; the redirect target must use the SAME stored-address source so behavior is consistent. Also note the exact import path of loadUserAddressFromContext in Results.jsx — reuse the identical path)
    - .planning/phases/99-election-central-page/99-VERIFICATION.md (Gap 2 specifics — App.jsx redirect missing q=)
    - .planning/phases/99-election-central-page/99-UAT.md (test 5 — observed failure mode: manual re-submit required)
  </read_first>
  <action>
The fix lives in App.jsx. The current /elections route uses a static Navigate element so it cannot read async stored address — replace it with a function component that performs the lookup asynchronously and falls back when the address does not resolve.

Concrete change:

1. At the top of App.jsx, add an import for loadUserAddressFromContext from the same module Results.jsx uses. Grep Results.jsx for the existing `import { loadUserAddressFromContext }` (or similar named import) and reuse the exact same module path so both call sites stay aligned.

2. Add useState to the existing react imports (useEffect is already imported on line 2). Final import line should read: import { useEffect, useState } from "react";

3. Define a new component named ElectionsRedirect above the App() function with the following behavior:
   - Holds a single piece of state: the resolved redirect URL string, initialized to null.
   - On mount (useEffect with empty deps), calls loadUserAddressFromContext(). If the resolved value has a truthy addr field, set the state to the string /results?prefilled=true&view=elections&q= followed by encodeURIComponent of the stored address. If no stored address is found, set the state to /results?prefilled=true&view=elections (no q= — equivalent to today's behavior; user will still need to type an address, which is acceptable for a logged-out / no-context visitor).
   - While the state is null, return null (renders nothing during the brief async read — matches the existing pattern in Results.jsx where the prefill effect runs without a spinner).
   - Once the state is set, return a Navigate component with to={resolved} and the replace prop set true.
   - Guard against the effect setting state after unmount using a cancelled flag in the effect cleanup (same pattern Results.jsx uses in its loadUserAddressFromContext effect — set cancelled = true in the cleanup function and bail inside the then handler when cancelled is truthy).

4. Replace the existing Route element for path="/elections" on line 72. Currently: `<Route path="/elections" element={<Navigate to="/results?prefilled=true&view=elections" replace />} />`. Change to: `<Route path="/elections" element={<ElectionsRedirect />} />`.

5. Confirm the Navigate import on line 3 stays (still used inside ElectionsRedirect).

Do NOT modify Results.jsx — the Gap 2 fix is intentionally on the App.jsx side per the recommended Option A in the gap directive. Reasons: keeps Results.jsx activeQuery derivation single-sourced from URL (?q=), avoids adding a divergent "trigger fetch from addressInput" branch, and makes the auto-fetch state visible/shareable via the URL bar.

After the edit, run npm run build from the essentials repo root to confirm the React + Vite build succeeds. If the import path for loadUserAddressFromContext is wrong the build will fail at the resolver step — fix the path to match Results.jsx exactly.

Do not commit yet — Task 3 batches both source changes into one commit and one push.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('C:/Transparent Motivations/essentials/src/App.jsx','utf8');if(!/function ElectionsRedirect|const ElectionsRedirect = /.test(s)){console.error('ElectionsRedirect component missing');process.exit(1);}if(!/loadUserAddressFromContext/.test(s)){console.error('stored-address helper not imported');process.exit(2);}if(!/encodeURIComponent/.test(s)){console.error('q= param not encoded');process.exit(3);}if(/path=\"\/elections\" element=\{<Navigate /.test(s)){console.error('static Navigate still wired to /elections');process.exit(4);}if(!/<ElectionsRedirect/.test(s)){console.error('Route element not switched to ElectionsRedirect');process.exit(5);}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx defines a component named ElectionsRedirect (function or const arrow component)
    - App.jsx imports loadUserAddressFromContext from the same module path Results.jsx imports it from (exact path match)
    - ElectionsRedirect body contains encodeURIComponent applied to the resolved stored address
    - The Route for path="/elections" renders <ElectionsRedirect /> — NOT a static Navigate element
    - When stored address resolves, the resulting redirect URL contains &q= followed by a percent-encoded address (asserted via presence of encodeURIComponent + the &q= literal in the source)
    - When no stored address is found, the fallback redirect URL is /results?prefilled=true&view=elections (no broken q= param)
    - Effect cleanup uses a cancelled flag so state is not set after unmount
    - `cd "C:\Transparent Motivations\essentials" && npm run build` exits 0 after both Task 1 and Task 2 edits
  </acceptance_criteria>
  <done>App.jsx /elections route resolves stored address before navigating to /results with q= populated; build passes</done>
</task>

<task type="auto">
  <name>Task 3: Build, commit, push, update REQUIREMENTS.md traceability table</name>
  <files>
    C:\Transparent Motivations\essentials (git commit + push, no new files)
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (read full file — specifically the checkbox lines 29–31 already showing [x] AND the traceability table at lines 130–151 where ELEC-01/02/03 still say "Pending")
    - .planning/phases/99-election-central-page/99-VERIFICATION.md (the source of truth for what was broken — referenced in the commit message)
  </read_first>
  <action>
1. Run the essentials build to confirm clean state after Task 1 + Task 2 edits:
   cd "C:\Transparent Motivations\essentials" && npm run build
   If the build fails, do not proceed — return to Task 1 or Task 2 and fix the source error.

2. Stage and commit the two source changes in the essentials repo. Stage exactly the two changed files (do not git add -A):
   cd "C:\Transparent Motivations\essentials" && git add src/components/ElectionsView.jsx src/App.jsx
   Then commit. Use a heredoc-style multi-line commit message via -m or git commit -F. The subject line must be: fix(elections): antipartisan race headers + /elections auto-fetch (Phase 99 gap closure). The body must reference the two specific gaps from 99-VERIFICATION.md:
   - Gap 1 (UAT test 6): drop primary_party from subgroupLabel in ElectionsView.jsx so race headers render position_name only.
   - Gap 2 (UAT test 5): /elections redirect now reads stored address via loadUserAddressFromContext and supplies q= so Results.jsx activeQuery is non-empty on landing, triggering auto-fetch.

3. Push to master (Render auto-deploys from master):
   cd "C:\Transparent Motivations\essentials" && git push origin master
   Capture the push output. If the remote rejects (non-fast-forward), STOP and report — do not force push.

4. In C:\EV-Accounts\.planning\REQUIREMENTS.md, locate the traceability table around lines 130–151 (the `| Requirement | Phase | Status |` table at the bottom of the file). Change exactly three rows:

   - Replace `| ELEC-01 | Phase 99 | Pending |` with `| ELEC-01 | Phase 99 | Complete |`
   - Replace `| ELEC-02 | Phase 99 | Pending |` with `| ELEC-02 | Phase 99 | Complete |`
   - Replace `| ELEC-03 | Phase 99 | Pending |` with `| ELEC-03 | Phase 99 | Complete |`

   Do not modify any other rows. The checkbox lines 29–31 already show [x] and remain unchanged (they were prematurely flipped by Plan 05 but now actually reflect reality after this gap closure).

5. Do NOT modify MILESTONES.md or STATE.md — those still reflect "Phase 99 complete" from Plan 05; they remain accurate once Tasks 1, 2, and 3 land. The smoke test in Task 4 is what re-validates that claim.
  </action>
  <verify>
    <automated>cd "C:\Transparent Motivations\essentials" && npm run build</automated>
    <automated>cd "C:\Transparent Motivations\essentials" && git log -1 --pretty=format:"%s"</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('C:/EV-Accounts/.planning/REQUIREMENTS.md','utf8');const ok=/\| ELEC-01 \| Phase 99 \| Complete \|/.test(s) && /\| ELEC-02 \| Phase 99 \| Complete \|/.test(s) && /\| ELEC-03 \| Phase 99 \| Complete \|/.test(s);const stillPending=/\| ELEC-0[123] \| Phase 99 \| Pending \|/.test(s);if(!ok||stillPending){console.error('table not updated');process.exit(1);}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 in the essentials repo after both source edits
    - git log -1 --pretty=format:"%s" in the essentials repo returns a subject line containing "antipartisan" AND "auto-fetch" (the canonical commit subject from this plan)
    - git push to origin master succeeds (no non-fast-forward rejection; no force push performed)
    - REQUIREMENTS.md traceability table contains exactly three rows reading `| ELEC-0N | Phase 99 | Complete |` for N in {1, 2, 3}
    - REQUIREMENTS.md contains zero rows reading `| ELEC-0N | Phase 99 | Pending |` for any N
    - MILESTONES.md and STATE.md untouched by this task
  </acceptance_criteria>
  <done>Both source fixes committed and pushed to essentials master; REQUIREMENTS.md traceability table aligned with checkboxes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke test deployed fix on https://essentials.empowered.vote/elections</name>
  <read_first>
    - .planning/phases/99-election-central-page/99-VERIFICATION.md (the two gaps this plan closes — confirm both behaviors in production)
    - .planning/phases/99-election-central-page/99-UAT.md (tests 5 and 6 — the failing scenarios that must now pass)
  </read_first>
  <what-built>
ElectionsView.jsx no longer renders primary_party in race header titles (subgroupLabel = subgroup). App.jsx /elections route now resolves stored address via loadUserAddressFromContext and redirects to /results?prefilled=true&view=elections&q=<encoded address> so Results.jsx activeQuery is non-empty on landing. Commit pushed to essentials master — Render auto-deploys (usually within 1–3 minutes). REQUIREMENTS.md traceability table aligned with checkboxes.
  </what-built>
  <how-to-verify>
Wait for Render auto-deploy to complete (check the Render dashboard for the essentials service if needed — wait until the deploy status reads "Live" with the new commit SHA).

Then perform the following checks against https://essentials.empowered.vote/elections in a real browser:

Pre-check: ensure you have a Salt Lake City UT address stored in the ev-context bridge (loadUserAddressFromContext source). If not, first visit https://essentials.empowered.vote/results, enter "123 Main St, Salt Lake City, UT 84101" in the address bar, submit, and confirm it persists. Then proceed.

1. Open a fresh browser tab (no URL params, no manual address typing). Navigate to https://essentials.empowered.vote/elections.
2. Expected: the page redirects to a URL of the form https://essentials.empowered.vote/results?prefilled=true&view=elections&q=123%20Main%20St%2C%20Salt%20Lake%20City%2C%20UT%2084101 (the exact percent-encoding may vary slightly — confirm q= is present and non-empty).
3. Expected: the elections list auto-loads WITHOUT requiring you to click into the address bar or hit submit. Race cards appear within a few seconds.
4. Spot-check race header text: open at least 3 race cards across the Local, State, and Federal tiers. Expected: every race title reads as a position name only (e.g. "ASSESSOR", "GOVERNOR", "U.S. SENATOR") — NEITHER "— DEMOCRATIC PRIMARY" NOR "— REPUBLICAN PRIMARY" NOR any other party name appears in any race title.
5. Open the browser devtools console. Confirm zero JS errors (the 401 on /api/auth/session for unauthenticated users is expected and not an error).
6. Optional mobile check at 375px viewport: same two behaviors hold (auto-fetch + no party in titles).

If any of the above fails, report the specific failure mode and recommend re-opening this plan. Do NOT update STATE.md or MILESTONES.md until this smoke test fully passes — those files were already updated by Plan 05 and remain accurate ONLY if this smoke test passes.
  </how-to-verify>
  <resume-signal>Type "verified" once both behaviors confirmed on production, or describe the specific failure mode (which test step failed, what was observed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored address (ev-context bridge) → URL query string (?q=) | Internal: address sourced from same cross-app context already trusted by Results.jsx prefill effect; passes through encodeURIComponent before becoming a URL param |
| Source commit → essentials master → Render auto-deploy | Internal: master push triggers Render build + deploy; build failure caught by npm run build pre-push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-06-01 | Information Disclosure | stored address in URL bar after /elections redirect | accept | Address was already exposed via the same Results.jsx flow whenever a user submits manually; visibility parity with existing UX; URL is the same-origin essentials.empowered.vote domain |
| T-99-06-02 | Tampering | App.jsx ElectionsRedirect resolved URL | mitigate | encodeURIComponent applied to stored address before concatenation prevents URL injection; fallback to view=elections-only redirect when address is absent prevents broken q= |
| T-99-06-03 | Availability | essentials Render auto-deploy | mitigate | npm run build runs locally before push; non-fast-forward push is rejected (no forced overwrite of master); git revert is the documented rollback path if production behavior regresses |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Task 1 verify regex confirms subgroupLabel = subgroup is present and the original ternary is absent
- Task 2 verify regex confirms ElectionsRedirect component defined, loadUserAddressFromContext imported, encodeURIComponent used, and the static Navigate for /elections is gone
- Task 3 verify confirms npm run build passes, the commit subject contains "antipartisan" and "auto-fetch", and REQUIREMENTS.md traceability table shows three Complete rows for ELEC-01/02/03 with zero Pending rows
- Task 4 human smoke test confirms both behaviors in production: auto-fetch on /elections landing + no party names in race titles
</verification>

<success_criteria>
- ElectionsView.jsx: subgroupLabel emits subgroup only (no party concatenation in user-visible title)
- App.jsx: /elections route uses ElectionsRedirect component that reads stored address and supplies q= in the redirect URL
- essentials repo: one commit pushed to master with subject "fix(elections): antipartisan race headers + /elections auto-fetch (Phase 99 gap closure)"
- npm run build: passes in essentials repo after both edits
- REQUIREMENTS.md traceability table: ELEC-01, ELEC-02, ELEC-03 all show Complete (zero Pending rows remain for these three IDs)
- Smoke test on https://essentials.empowered.vote/elections: race titles show position_name only AND elections auto-load when navigating with a stored address
</success_criteria>

<output>
Create `.planning/phases/99-election-central-page/99-06-SUMMARY.md` when done
</output>
