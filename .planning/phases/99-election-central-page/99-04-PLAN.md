---
phase: 99-election-central-page
plan: "04"
type: execute
wave: 2
depends_on:
  - "99-03"
files_modified:
  - "C:\\Transparent Motivations\\essentials\\src\\components\\ElectionsView.jsx"
  - "C:\\Transparent Motivations\\essentials\\src\\pages\\Results.jsx"
  - backend/migrations/268_ut_elections_fixes.sql
autonomous: false
requirements:
  - ELEC-02
must_haves:
  truths:
    - "Every issue recorded in 99-VERIFICATION-ISSUES.md has a corresponding fix"
    - "UI fixes committed to essentials repo master (auto-deploys to essentials.empowered.vote)"
    - "Data fixes applied via SQL migration(s) in backend/migrations/"
    - "If no issues were found, plan closes immediately with a 'no issues' summary"
  artifacts:
    - path: .planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md
      provides: "Source of all fixes — executor reads this first before touching any code"
  key_links:
    - from: "99-VERIFICATION-ISSUES.md data issues"
      to: backend/migrations/268_ut_elections_fixes.sql
      via: SQL migration apply
      pattern: "UPDATE / INSERT INTO essentials"
    - from: "99-VERIFICATION-ISSUES.md UI issues"
      to: "C:\\Transparent Motivations\\essentials\\src\\components\\ElectionsView.jsx"
      via: "git commit to essentials repo master"
      pattern: "auto-deploys on push"
---

<objective>
Fix every issue recorded in 99-VERIFICATION-ISSUES.md. This plan is driven entirely by what Wave 1 found — tasks are defined at execution time by reading the issues file first.

Purpose: Complete the fix loop before Wave 3 smoke test. If no issues were found, this plan terminates immediately with a "no issues to fix" summary.

Output: Zero open issues from 99-VERIFICATION-ISSUES.md. Fixes committed to their respective repos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-election-central-page/99-CONTEXT.md
@.planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md
@.planning/phases/99-election-central-page/99-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read issues file and triage — data vs UI vs no-op</name>
  <files>.planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md</files>
  <read_first>
    - .planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md (the definitive issue list)
    - .planning/phases/99-election-central-page/99-CONTEXT.md (D-07 = UI fixes to essentials repo; D-08 = data fixes to migration; D-09 = withdrawn-badge is UI fix)
  </read_first>
  <action>
Read 99-VERIFICATION-ISSUES.md in full. Triage every recorded issue into one of three buckets:

**Bucket A — Data fixes** (wrong dates, missing races, bad candidate records, wrong counts): Fix via SQL migration `backend/migrations/268_ut_elections_fixes.sql`. If the issues file records no data issues, skip this migration file entirely.

**Bucket B — UI fixes** (layout bugs, mobile scroll, wrong text, missing withdrawn-candidate badge, console errors): Fix directly in the essentials repo at `C:\Transparent Motivations\essentials\src\`. Per D-07, commit directly to master — Render auto-deploys. No PR needed.

**Bucket C — No issues**: If the issues file summary says "pass" with no items in any section, write the 99-04-SUMMARY.md immediately stating "No issues found in Wave 1 — no fixes required" and exit this plan.

Document the triage in your working notes before touching any files.
  </action>
  <verify>
    <automated>type .planning\phases\99-election-central-page\99-VERIFICATION-ISSUES.md</automated>
  </verify>
  <acceptance_criteria>
    - Every non-empty section in 99-VERIFICATION-ISSUES.md has been assigned to Bucket A, B, or C
    - If summary says "pass" with no items listed anywhere, Bucket C is used and plan exits
  </acceptance_criteria>
  <done>Triage complete — issue buckets defined</done>
</task>

<task type="auto">
  <name>Task 2: Apply data fixes (migration 268) if Bucket A has items</name>
  <files>backend/migrations/268_ut_elections_fixes.sql</files>
  <read_first>
    - .planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md (data issues section)
    - backend/migrations/267_ut_2026_primary.sql (what migration 267 inserted — know what to update)
    - backend/migrations/044_election_dedup_constraints.sql (ON CONFLICT clause rules per table)
    - backend/migrations/183_me_2026_elections_foundation.sql (UPDATE / correction pattern)
  </read_first>
  <action>
Skip this task entirely if triage found zero data issues.

Otherwise, write `backend/migrations/268_ut_elections_fixes.sql`. Header:
```sql
-- Migration 268: UT 2026 Primary data fixes — Phase 99 Plan 04
-- Corrects issues found during Wave 1 verification (99-VERIFICATION-ISSUES.md).
```

For each data issue in Bucket A, write an idempotent fix:
- Wrong election date: `UPDATE essentials.elections SET election_date = '<correct>' WHERE name = 'UT 2026 Primary' AND state = 'UT'`
- Wrong candidate name: `UPDATE essentials.race_candidates SET full_name = '<correct>', first_name = '<f>', last_name = '<l>' WHERE full_name = '<wrong>' AND race_id IN (SELECT r.id FROM essentials.races r JOIN essentials.elections e ON r.election_id = e.id WHERE e.name = 'UT 2026 Primary')`
- Missing race: INSERT using the races + race_candidates pattern from migration 267 / 183
- Extra/duplicate candidate: `DELETE FROM essentials.race_candidates WHERE full_name = '<name>' AND race_id = (SELECT id FROM essentials.races WHERE ...)`

After writing, apply:
```
psql $DATABASE_URL -f backend/migrations/268_ut_elections_fixes.sql
```
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.races r ON rc.race_id = r.id JOIN essentials.elections e ON r.election_id = e.id WHERE e.name = 'UT 2026 Primary'"</automated>
  </verify>
  <acceptance_criteria>
    - If Bucket A was empty, this task produces no file and exits cleanly
    - If Bucket A had items, migration 268 applies with no errors
    - Each specific data error from 99-VERIFICATION-ISSUES.md Data Issues section has a corresponding fix in 268
    - Re-running 268 produces no errors (idempotent)
  </acceptance_criteria>
  <done>All data fixes applied via migration 268 (or skipped if no data issues)</done>
</task>

<task type="auto">
  <name>Task 3: Apply UI fixes to essentials repo if Bucket B has items</name>
  <files>
    C:\Transparent Motivations\essentials\src\components\ElectionsView.jsx
    C:\Transparent Motivations\essentials\src\pages\Results.jsx
  </files>
  <read_first>
    - .planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md (UI issues section, mobile issues section, console errors section, withdrawn candidate behavior section)
    - C:\Transparent Motivations\essentials\src\components\ElectionsView.jsx (current 777-line component — read full file before making any changes)
    - C:\Transparent Motivations\essentials\src\pages\Results.jsx (if UI issue is in the host page)
    - .planning/phases/99-election-central-page/99-CONTEXT.md (D-09: withdrawn-badge is a UI fix; ESSENTIALS-NOTE describes desired behavior: show WITHDRAWN overlay/banner on photo)
  </read_first>
  <action>
Skip this task entirely if triage found zero UI issues.

Otherwise, for each UI issue in Bucket B:

**Mobile horizontal scroll**: If cards overflow at 375px, find the offending element in ElectionsView.jsx and add `overflow-x-hidden` or fix the flex/grid layout to `flex-col` or `min-w-0` on constrained children. Do not add arbitrary max-widths — find the overflow source.

**Console JS errors**: Read the exact error message from the issues file. Find the source line in ElectionsView.jsx or Results.jsx. Fix the root cause (null dereference, undefined property, bad import, etc.).

**Wrong text / display errors**: Find the exact string in ElectionsView.jsx (grep for it) and correct it. Do not rewrite surrounding logic.

**Withdrawn-candidate badge missing** (per D-09 + ESSENTIALS-NOTE): If the issues file confirms the badge is not implemented, add it to ElectionsView.jsx. The ESSENTIALS-NOTE specifies: show WITHDRAWN overlay/banner on the candidate card photo (not silently hide the candidate). Implementation: in the candidate card render, check `candidate.candidate_status === 'withdrawn'`. If true, add a relative-positioned overlay div with text "WITHDRAWN" over the photo. The backend `electionService.ts` filter `AND rc.candidate_status != 'withdrawn'` is only on `getCandidateDetail` — the elections listing does include withdrawn candidates per the service code. The badge should be a dark semi-transparent banner across the bottom of the photo with white "WITHDRAWN" text.

After making all changes, verify the essentials build still passes:
```
cd "C:\Transparent Motivations\essentials" && npm run build
```
Then commit to master:
```
cd "C:\Transparent Motivations\essentials" && git add src/ && git commit -m "fix(elections): resolve Wave 1 verification issues"
```
Render will auto-deploy on push to master. Push:
```
cd "C:\Transparent Motivations\essentials" && git push
```
  </action>
  <verify>
    <automated>cd "C:\Transparent Motivations\essentials" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - If Bucket B was empty, this task produces no commits and exits cleanly
    - If Bucket B had items, `npm run build` passes with no errors after changes
    - Each specific UI issue from 99-VERIFICATION-ISSUES.md UI Issues, Mobile Issues, Console Errors sections has a corresponding code change
    - Git commit exists in essentials repo with the fix
    - Push to master succeeds (triggers Render auto-deploy)
    - Withdrawn-badge decision: if ESSENTIALS-NOTE behavior was missing, badge is now implemented with "WITHDRAWN" overlay on candidate photo; if behavior was confirmed already working, no change made
  </acceptance_criteria>
  <done>All UI fixes committed and pushed to essentials repo (or skipped if no UI issues)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification findings → SQL fixes | Internal: developer writes corrections based on observed data errors |
| Code changes → Render auto-deploy | essentials repo master branch triggers Render deploy on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04-01 | Tampering | migration 268 | accept | Runs under developer credentials; idempotent patterns prevent double-apply damage |
| T-99-04-02 | Availability | Render auto-deploy | accept | Build failure is caught by `npm run build` before push; rollback via git revert |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- All items in 99-VERIFICATION-ISSUES.md are accounted for (fixed or confirmed N/A)
- If data issues existed: migration 268 applies without errors
- If UI issues existed: `npm run build` passes; git commit exists in essentials repo; pushed to master
- If no issues: plan summary explicitly states "no issues found, no fixes required"
</verification>

<success_criteria>
- Zero open issues from 99-VERIFICATION-ISSUES.md
- Data fixes (if any) applied via migration 268
- UI fixes (if any) committed and deployed to essentials.empowered.vote
</success_criteria>

<output>
Create `.planning/phases/99-election-central-page/99-04-SUMMARY.md` when done
</output>
