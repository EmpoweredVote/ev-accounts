---
phase: 99-election-central-page
plan: "05"
type: execute
wave: 3
depends_on:
  - "99-04"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ELEC-03
must_haves:
  truths:
    - "Smoke test passes: /elections loads for SLC address with no JS errors and elections list visible"
    - "ELEC-01, ELEC-02, ELEC-03 all marked [x] in REQUIREMENTS.md"
    - "MILESTONES.md has a new entry documenting elections feature shipped in v2.6"
    - "STATE.md reflects Phase 99 complete"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "ELEC-01, ELEC-02, ELEC-03 marked complete"
      contains: "[x] **ELEC-01**"
    - path: .planning/MILESTONES.md
      provides: "v2.6 elections milestone entry"
      contains: "Elections"
  key_links:
    - from: smoke test
      to: essentials.empowered.vote/elections
      via: Playwright or manual browser check
      pattern: "elections list renders for SLC address"
---

<objective>
Declare the elections feature shipped: re-run smoke test, mark ELEC-01/02/03 complete in REQUIREMENTS.md, write MILESTONES.md entry, and update STATE.md.

Purpose: Close the loop on Phase 99. Per D-10, no manual approval checkpoint is needed — agent writes the milestone entry directly once the smoke test passes.

Output: All three ELEC requirements marked complete; MILESTONES.md updated; STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/phases/99-election-central-page/99-CONTEXT.md
@.planning/phases/99-election-central-page/99-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test re-run</name>
  <files></files>
  <read_first>
    - .planning/phases/99-election-central-page/99-VERIFICATION-ISSUES.md (prior findings — confirm they were fixed)
    - .planning/phases/99-election-central-page/99-04-SUMMARY.md (what was fixed in Wave 2)
    - .planning/phases/99-election-central-page/99-CONTEXT.md (D-10: smoke test bar — page loads, elections list renders for UT address, no console errors)
  </read_first>
  <action>
Run a Playwright smoke test (or equivalent browser-drive) against https://essentials.empowered.vote/elections:

1. Navigate to https://essentials.empowered.vote/elections
2. Confirm the redirect completes without 404 or network error
3. Enter address `123 Main St, Salt Lake City, UT 84101`
4. Confirm elections list renders (at least one race card visible)
5. Open browser console — confirm zero JS errors

This is a go/no-go gate. If any of the three conditions fails (page loads / elections render / no console errors), STOP — do not update REQUIREMENTS.md or MILESTONES.md. Report the failure and recommend re-opening Wave 2.

If all three conditions pass, proceed to Tasks 2 and 3.
  </action>
  <verify>
    <automated>echo "Smoke test must be confirmed before proceeding — check Playwright output or manual browser run above"</automated>
  </verify>
  <acceptance_criteria>
    - https://essentials.empowered.vote/elections loads without 404 (redirect followed successfully)
    - Elections list renders at least one race card for `123 Main St, Salt Lake City, UT 84101`
    - Browser console shows zero JS errors during the test flow
  </acceptance_criteria>
  <done>Smoke test passed — page loads, elections render, no console errors</done>
</task>

<task type="auto">
  <name>Task 2: Mark ELEC-01, ELEC-02, ELEC-03 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the Elections section — find the exact lines for ELEC-01, ELEC-02, ELEC-03)
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, find the Elections (ELEC) section. Change each of the three requirement lines from `- [ ]` to `- [x]`:

- `- [ ] **ELEC-01**: Elections page at...` → `- [x] **ELEC-01**: Elections page at...`
- `- [ ] **ELEC-02**: All issues found...` → `- [x] **ELEC-02**: All issues found...`
- `- [ ] **ELEC-03**: Elections feature declared...` → `- [x] **ELEC-03**: Elections feature declared...`

Do not change any other lines in the file.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*ELEC-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*ELEC-0" .planning/REQUIREMENTS.md` returns `3`
    - No other requirements in the file were changed
    - The three ELEC lines now show `[x]` not `[ ]`
  </acceptance_criteria>
  <done>ELEC-01, ELEC-02, ELEC-03 marked [x] in REQUIREMENTS.md</done>
</task>

<task type="auto">
  <name>Task 3: Write MILESTONES.md entry and update STATE.md</name>
  <files>
    .planning/MILESTONES.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/MILESTONES.md (read the most recent milestone entry to match the format exactly)
    - .planning/STATE.md (find the current phase status and progress block)
    - .planning/phases/99-election-central-page/99-01-SUMMARY.md (backend deliverables)
    - .planning/phases/99-election-central-page/99-02-SUMMARY.md (frontend deliverables)
    - .planning/phases/99-election-central-page/99-03-SUMMARY.md (migration 267 + verification)
    - .planning/phases/99-election-central-page/99-04-SUMMARY.md (fixes applied)
  </read_first>
  <action>
**MILESTONES.md**: Prepend a new milestone entry at the top of the file (before the current most-recent entry). Use the same heading and field format as the existing entries. The entry should cover:

- Title: `v2.6 Elections Central (Shipped: 2026-06-XX)` (use today's date)
- Delivered: brief description — elections page at `/elections` backed by `GET /api/essentials/elections-by-address`; address → geocode → geofence-matched races + statewide races; UT 2026 Primary (June 23) seeded as first major test data set
- Phases completed: 99 (3 plans: backend, frontend, verification)
- Key accomplishments (bullet list):
  - `GET /api/essentials/elections-by-address` endpoint — geocodes address, runs PostGIS geofence join + statewide fallback, groups into elections → races → candidates hierarchy
  - `/elections` route in essentials app — redirects to `/results?prefilled=true&view=elections`; `fetchElectionsByAddress()` in api.jsx; `ElectionsView.jsx` (777 lines) renders tier-grouped (Federal/State/Local) race cards with candidate cards
  - Migration 267: UT 2026 Primary — 176 candidates across XX races; June 23, 2026
  - Any UI fixes from Wave 2 (read 99-04-SUMMARY.md for actual fixes applied)
- Requirements closed: ELEC-01, ELEC-02, ELEC-03

**STATE.md**: Update the phase status block:
- Change `Phase: 99` status from `Ready to plan` / `In progress` to `Complete`
- Update `progress.completed_phases` by incrementing by 1
- Update `last_activity` to today's date
- Update `last_updated` timestamp
- Add a note in Session Continuity: `Last session: [today's date] — Phase 99 complete, elections feature shipped`
  </action>
  <verify>
    <automated>grep -c "Elections" .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - MILESTONES.md has a new entry at the top containing "Elections" and today's date
    - The entry lists ELEC-01, ELEC-02, ELEC-03 as requirements closed
    - The entry describes the backend endpoint, frontend route, and migration 267
    - STATE.md `last_activity` matches today's date
    - STATE.md shows Phase 99 as complete
  </acceptance_criteria>
  <done>MILESTONES.md updated with elections feature entry; STATE.md shows Phase 99 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke test → ship decision | Internal gate; agent evaluates live site behavior before writing milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05-01 | Repudiation | MILESTONES.md | accept | Planning files are in git; entries are auditable and reversible |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Smoke test passes: page loads, elections render for SLC address, zero console errors
- `grep -c "\[x\] \*\*ELEC-0" .planning/REQUIREMENTS.md` returns 3
- `grep "Elections" .planning/MILESTONES.md` returns at least one match in a new entry at the top of the file
- STATE.md last_activity = today's date
</verification>

<success_criteria>
- Smoke test green
- ELEC-01, ELEC-02, ELEC-03 marked [x] in REQUIREMENTS.md
- MILESTONES.md has elections feature entry with today's date
- STATE.md updated — Phase 99 complete
</success_criteria>

<output>
Create `.planning/phases/99-election-central-page/99-05-SUMMARY.md` when done
</output>
