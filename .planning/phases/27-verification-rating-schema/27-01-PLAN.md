---
phase: 27-verification-rating-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260315000037_phase27_verification_rating.sql
  - backend/src/routes/account.ts
  - tests/integration/account.test.ts
autonomous: true

must_haves:
  truths:
    - "connected_profiles has verification_rating (integer, default 60, range 0-150) and vq_hold_until (timestamptz) columns"
    - "GET /api/account/me returns verification_rating, vq_hold_active, and red_gem_quests_unlocked at root level"
    - "A user with verification_rating >= 90 gets red_gem_quests_unlocked: true"
    - "A user with vq_hold_until in the future gets vq_hold_active: true"
    - "vq_hold_until does NOT appear in connected_profiles_public view"
    - "verification_rating DOES appear in connected_profiles_public view (public stat)"
  artifacts:
    - path: "supabase/migrations/20260315000037_phase27_verification_rating.sql"
      provides: "ALTER TABLE + CHECK constraint + partial index + updated public view"
      contains: "verification_rating"
    - path: "backend/src/routes/account.ts"
      provides: "Derived booleans vq_hold_active and red_gem_quests_unlocked on /me response"
      contains: "red_gem_quests_unlocked"
    - path: "tests/integration/account.test.ts"
      provides: "ALLOWED_ME_KEYS whitelist updated with new root-level fields"
      contains: "verification_rating"
  key_links:
    - from: "supabase/migrations/20260315000037_phase27_verification_rating.sql"
      to: "connect.connected_profiles"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ADD COLUMN.*verification_rating"
    - from: "backend/src/routes/account.ts"
      to: "connect.connected_profiles"
      via: "SELECT including new columns"
      pattern: "verification_rating.*vq_hold_until"
    - from: "backend/src/routes/account.ts"
      to: "/me response body"
      via: "derived boolean computation"
      pattern: "vq_hold_active|red_gem_quests_unlocked"
---

<objective>
Add Verification Rating columns to connected_profiles and expose them on GET /api/account/me with derived booleans for hold state and Red Gem quest eligibility.

Purpose: Phase 28 (VQ Confirmation Flow) needs these columns to adjust ratings. Phase 30 (Profile Hub) needs the /me response to display them. This is the foundation for the verification engine.
Output: One migration file, updated account.ts (both GET and PATCH handlers), updated test whitelist.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/account.ts
@tests/integration/account.test.ts
@supabase/migrations/20260304000029_phase9_xp_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 037 — verification_rating and vq_hold_until columns</name>
  <files>supabase/migrations/20260315000037_phase27_verification_rating.sql</files>
  <action>
Create migration 037 wrapped in BEGIN/COMMIT with these sections:

Section 1: ALTER TABLE connect.connected_profiles
- ADD COLUMN IF NOT EXISTS verification_rating INTEGER NOT NULL DEFAULT 60 CHECK (verification_rating >= 0 AND verification_rating <= 150)
- ADD COLUMN IF NOT EXISTS vq_hold_until TIMESTAMPTZ (nullable, NULL = no hold)

Section 2: Partial index for hold-state queries (Phase 28 needs to find users in hold)
- CREATE INDEX IF NOT EXISTS idx_connected_profiles_vq_hold ON connect.connected_profiles(vq_hold_until) WHERE vq_hold_until IS NOT NULL

Section 3: DROP and recreate connected_profiles_public view
- Must match the existing view from migration 029 (Phase 9) but add verification_rating
- Do NOT include vq_hold_until (internal enforcement state — same privacy pattern as tolerance_rating)
- Do NOT include tolerance_rating (already omitted in current view)
- After CREATE VIEW, re-GRANT SELECT to authenticated (DROP removes grants)

Use the exact column list from migration 029's view as baseline, inserting verification_rating after veracity_rating. The current view columns are: id, user_id, display_name, account_standing, verification_status, verification_method, verified_region, xp, total_xp, current_level, gem_balance, gem_balance_red, gem_balance_blue, gem_balance_yellow, gem_reserve_cap, veracity_rating, deleted_at, created_at, updated_at.

Add a comment block at the top explaining the migration purpose, CHECK constraint range (0-150), and why vq_hold_until is excluded from the public view.
  </action>
  <verify>Run: cd backend && npx tsc --noEmit (TypeScript still compiles). Visually inspect the migration file for correct SQL syntax, CHECK constraint, and view column list.</verify>
  <done>Migration file exists with correct ALTER TABLE, CHECK constraint (0-150), partial index, and updated public view that includes verification_rating but excludes vq_hold_until and tolerance_rating.</done>
</task>

<task type="auto">
  <name>Task 2: Update GET and PATCH /me handlers + test whitelist</name>
  <files>backend/src/routes/account.ts, tests/integration/account.test.ts</files>
  <action>
In backend/src/routes/account.ts, make these changes in BOTH the GET /me handler (line ~57-65 SELECT) and the PATCH /me handler (line ~333-340 SELECT):

1. Add verification_rating and vq_hold_until to both connected_profiles SELECT strings. Append after location_consent: ', verification_rating, vq_hold_until'

2. In both handlers, after the connected profile fetch and before building meResponse, compute derived booleans:
```typescript
const vqHoldActive: boolean = connected.vq_hold_until
  ? new Date(connected.vq_hold_until) > new Date()
  : false;
const redGemQuestsUnlocked: boolean = (connected.verification_rating ?? 60) >= 90;
```
(Use updatedConnected variable name in the PATCH handler instead of connected.)

3. Add three new root-level fields to BOTH meResponse objects (after location_consent line):
- verification_rating: connected?.verification_rating ?? 60
- vq_hold_active: vqHoldActive (computed above, false if no connected profile)
- red_gem_quests_unlocked: redGemQuestsUnlocked (computed above, false if no connected profile)

For inform-tier users (no connected profile), these three fields should still appear at root with defaults: verification_rating: 60, vq_hold_active: false, red_gem_quests_unlocked: false. Handle this by computing the booleans conditionally:
```typescript
const vqHoldActive = connected?.vq_hold_until
  ? new Date(connected.vq_hold_until) > new Date()
  : false;
const redGemQuestsUnlocked = (connected?.verification_rating ?? 60) >= 90;
```

4. Add verification_rating, vq_hold_active, and vq_hold_until to the connected_profile nested object (owner self-view). Place after completed_onboarding. Do NOT add red_gem_quests_unlocked to nested object — it's a root convenience field only.

5. In tests/integration/account.test.ts, add 'is_admin', 'verification_rating', 'vq_hold_active', and 'red_gem_quests_unlocked' to the ALLOWED_ME_KEYS Set. (Note: is_admin is already in the response but was missing from the whitelist — fix it now to prevent future test failures.)

IMPORTANT: The PATCH handler has its own separate SELECT and response-building block. Both must be updated identically. Search for both SELECT calls that reference connected_profiles and update both.
  </action>
  <verify>Run: cd backend && npx tsc --noEmit (TypeScript compiles). Run: cd backend && npm test (all existing tests pass, including the ALLOWED_ME_KEYS whitelist check).</verify>
  <done>Both GET and PATCH /me return verification_rating (integer), vq_hold_active (boolean), and red_gem_quests_unlocked (boolean) at root level. Connected_profile nested object includes verification_rating, vq_hold_active, and vq_hold_until. ALLOWED_ME_KEYS whitelist updated. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd backend && npm test` — all tests pass, including ALLOWED_ME_KEYS whitelist
3. Migration file contains CHECK (verification_rating >= 0 AND verification_rating <= 150)
4. Migration view does NOT contain vq_hold_until or tolerance_rating
5. Both GET and PATCH /me SELECT strings include verification_rating, vq_hold_until
6. Root response includes verification_rating, vq_hold_active, red_gem_quests_unlocked
7. connected_profile nested object includes verification_rating, vq_hold_active, vq_hold_until but NOT red_gem_quests_unlocked
</verification>

<success_criteria>
- Migration 037 exists and is valid SQL (BEGIN/COMMIT, ALTER TABLE, CHECK constraint, partial index, updated view)
- GET /api/account/me returns verification_rating (default 60), vq_hold_active (default false), red_gem_quests_unlocked (default false)
- A rating of 90+ would produce red_gem_quests_unlocked: true
- A future vq_hold_until would produce vq_hold_active: true
- vq_hold_until is excluded from connected_profiles_public view
- TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-verification-rating-schema/27-01-SUMMARY.md`
</output>
