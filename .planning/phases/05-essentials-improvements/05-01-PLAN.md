---
phase: 05-essentials-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/lib/classify.js
  - EV-Backend/internal/essentials/handlers.go
autonomous: true
requirements:
  - ESST-05
  - ESST-06

must_haves:
  truths:
    - "Federal section order is U.S. Senate > U.S. House > President/VP > Cabinet > Agencies > Executive (Other)"
    - "OfficialOut JSON includes term_start and term_end fields when data exists"
    - "Existing API consumers continue to work (all new fields use omitempty)"
  artifacts:
    - path: "essentials/src/lib/classify.js"
      provides: "Reordered FEDERAL_ORDER constant"
      contains: '["U.S. Senate", "U.S. House", "President / VP"'
    - path: "EV-Backend/internal/essentials/handlers.go"
      provides: "OfficialOut with TermStart and TermEnd fields"
      contains: "TermStart"
  key_links:
    - from: "EV-Backend/internal/essentials/handlers.go"
      to: "essentials.politicians.valid_from / valid_to"
      via: "SQL SELECT in fetchOfficialsFromDB and fetchFederalAndStateFromDBFiltered"
      pattern: "p\\.valid_from.*p\\.valid_to"
---

<objective>
Reorder the federal section so legislative branch (Senate, House) appears before executive branch, and expose term start/end dates in the backend API response.

Purpose: The federal category order must match user decision (legislative-first). Term dates (valid_from/valid_to) already exist in the database but are not surfaced in the API — this plan adds them to OfficialOut so the frontend can display them.

Output: Updated classify.js constant + OfficialOut DTO with term_start/term_end fields populated from all code paths.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@essentials/src/lib/classify.js
@EV-Backend/internal/essentials/handlers.go
@.planning/phases/05-essentials-improvements/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder FEDERAL_ORDER constant and expose term dates in OfficialOut</name>
  <files>
    essentials/src/lib/classify.js
    EV-Backend/internal/essentials/handlers.go
  </files>
  <action>
**Part A: Federal reorder (ESST-05)**

In `essentials/src/lib/classify.js` line 8, change the FEDERAL_ORDER array from:
```javascript
export const FEDERAL_ORDER = [
  "President / VP",
  "U.S. Senate",
  "U.S. House",
  "Cabinet",
  "Independent Agencies & Commissions",
  "Executive (Other)",
];
```
To:
```javascript
export const FEDERAL_ORDER = [
  "U.S. Senate",
  "U.S. House",
  "President / VP",
  "Cabinet",
  "Independent Agencies & Commissions",
  "Executive (Other)",
];
```

This is a one-line constant reorder. No other files reference this constant except Results.jsx which passes it to `orderedEntries()`.

**Part B: Term dates in OfficialOut (ESST-06 backend)**

In `EV-Backend/internal/essentials/handlers.go`:

1. Add two fields to the `OfficialOut` struct (after `IsContained`):
```go
TermStart string `json:"term_start,omitempty"`
TermEnd   string `json:"term_end,omitempty"`
```

2. In `fetchOfficialsFromDB` function (~line 2095): find the `type row struct` definition and add:
```go
ValidFrom string
ValidTo   string
```
Then add `p.valid_from, p.valid_to` to the SELECT query string, and populate `TermStart: r.ValidFrom, TermEnd: r.ValidTo` in the OfficialOut construction.

3. In `fetchFederalAndStateFromDBFiltered` function (~line 2379): same changes — add ValidFrom/ValidTo to the row struct, add to SELECT, populate in OfficialOut construction.

4. In `GetPoliticianByID` function (~line 3032): if it uses a row struct, add ValidFrom/ValidTo there too. If it uses GORM model scan, the fields may already be available via the Politician model.

5. In `normalizedToOfficialOut` function (~line 2672): populate TermStart/TermEnd from `o.ValidFrom` and `o.ValidTo` (these fields exist on `provider.NormalizedOfficial` at provider/types.go lines 25-26).

**IMPORTANT:** Use `omitempty` on both fields so existing API consumers are unaffected when dates are empty.
  </action>
  <verify>
1. Run `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build -o /dev/null .` — must compile without errors
2. Grep for `TermStart` in handlers.go — should appear in OfficialOut struct and in all construction sites
3. Check classify.js has "U.S. Senate" as the first element of FEDERAL_ORDER
  </verify>
  <done>
- FEDERAL_ORDER in classify.js starts with "U.S. Senate", "U.S. House" (legislative first)
- OfficialOut has TermStart and TermEnd fields with json:"term_start,omitempty" / json:"term_end,omitempty"
- All code paths that construct OfficialOut (fetchOfficialsFromDB, fetchFederalAndStateFromDBFiltered, GetPoliticianByID, normalizedToOfficialOut) populate TermStart/TermEnd
- Backend compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `go build` succeeds in EV-Backend
2. FEDERAL_ORDER in classify.js: first two elements are "U.S. Senate" and "U.S. House"
3. All OfficialOut construction sites include TermStart/TermEnd mapping
</verification>

<success_criteria>
- Federal section ordering matches user requirement: Senate > House > President/VP > Cabinet > Agencies
- Backend API response includes term_start and term_end when politician has valid_from/valid_to data
- No breaking changes to existing API consumers
</success_criteria>

<output>
After completion, create `.planning/phases/05-essentials-improvements/05-01-SUMMARY.md`
</output>
