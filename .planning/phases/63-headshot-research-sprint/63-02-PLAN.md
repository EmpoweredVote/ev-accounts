---
phase: 63-headshot-research-sprint
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - EV-Backend/scripts/headshot_research_manifest.csv
autonomous: false
requirements:
  - PHOTO-01
  - PHOTO-02

must_haves:
  truths:
    - "Every politician in Batch 1 cities has been researched — found_url recorded or research_status set to not_found/blocked"
    - "Source page URLs are recorded for every found headshot so images can be re-verified"
    - "Failed-status cities (Redondo Beach) have been researched through manual browser navigation"
  artifacts:
    - path: "EV-Backend/scripts/headshot_research_manifest.csv"
      provides: "Updated manifest with Batch 1 research results"
      contains: "found"
  key_links:
    - from: "EV-Backend/scripts/headshot_research_manifest.csv"
      to: "City council websites"
      via: "Playwright MCP browser navigation"
      pattern: "found_url.*https://"
---

<objective>
Research headshots for Batch 1: the 12 cities with the most missing headshots (~66 politicians).

Purpose: Start with the largest gaps to maximize coverage improvement per session. Includes 1 failed-status city (Redondo Beach) requiring deeper page navigation.

Output: ~66 rows in headshot_research_manifest.csv updated with found_url, source_page_url, research_status, and research_date.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-headshot-research-sprint/63-CONTEXT.md
@.planning/phases/63-headshot-research-sprint/63-01-SUMMARY.md
@EV-Backend/scripts/headshot_research_manifest.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research headshots for Batch 1 cities (12 cities, ~66 politicians)</name>
  <files>EV-Backend/scripts/headshot_research_manifest.csv</files>
  <action>
Research headshots for these 12 cities in order (largest gap first):

1. **City of Santa Monica** (~7 members) — https://www.santamonica.gov/city-council
2. **City of Pomona** (~6 members) — https://www.pomonaca.gov/government/city-council
3. **City of Lynwood** (~6 members) — https://www.lynwood.ca.us/government/city-council
4. **City of Artesia** (~6 members) — https://www.cityofartesia.us/government/city-council
5. **City of Redondo Beach** (~6 members, FAILED status) — https://www.redondo.org/government/elected-officials/city-council
6. **City of El Monte** (~6 members) — https://www.elmonteca.gov or similar
7. **City of Glendora** (~5 members) — council page URL from manifest
8. **City of Palos Verdes Estates** (~5 members) — council page URL from manifest
9. **City of Torrance** (~5 members) — council page URL from manifest
10. **City of Hermosa Beach** (~5 members) — council page URL from manifest
11. **City of Santa Fe Springs** (~5 members) — council page URL from manifest
12. **City of Maywood** (~4 members) — council page URL from manifest

**For each city:**
1. Use Playwright MCP to navigate to the council page URL from the manifest
2. Look for headshot images on the main council page — individual portrait photos (not group photos)
3. For each politician in the manifest for that city:
   a. If headshot found: record the direct image URL (.jpg/.png) in `found_url`, the page URL in `source_page_url`, set `research_status=found`, set `research_date` to today's ISO date (2026-03-05)
   b. If not on main page: try 2-3 alternative pages (About, individual member pages, city directory)
   c. If no photo found anywhere: set `research_status=not_found`, `research_date` to today
   d. If page cannot be accessed (Cloudflare/blocked): set `research_status=blocked`, `research_date` to today
4. Prefer official city pages. Accept government directories as fallback. News/LinkedIn as last resort only.
5. Minimum quality: recognizable face (even ~100px thumbnails are acceptable)
6. Individual headshots only — skip group photos

**For Redondo Beach (failed status):** The batch scraper's duplicate URL detection was too aggressive. Navigate manually and check sub-pages — the photos are likely on individual member profile pages rather than the main listing.

**CSV update approach:**
- Read the full CSV into memory
- For each researched politician, update their row in-place
- Write the complete CSV back
- Preserve all existing columns and rows for other batches

**Image URL extraction:**
- Right-click or inspect the image to get the direct src URL
- Ensure the URL is a direct image link (ends in .jpg, .png, .jpeg, or is a direct CDN URL)
- Do NOT record dynamically generated or JavaScript-dependent URLs
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-Backend/scripts && python3 -c "
import csv
with open('headshot_research_manifest.csv') as f:
    rows = list(csv.DictReader(f))
batch1_cities = ['santa_monica_city_council', 'pomona_city_council', 'lynwood_city_council', 'artesia_city_council', 'redondo_beach_city_council', 'el_monte_city_council', 'glendora_city_council', 'palos_verdes_estates_city_council', 'torrance_city_council', 'hermosa_beach_city_council', 'santa_fe_springs_city_council', 'maywood_city_council']
batch1_rows = [r for r in rows if r['city_id'] in batch1_cities]
researched = [r for r in batch1_rows if r['research_status'] in ('found', 'not_found', 'blocked')]
pending = [r for r in batch1_rows if r['research_status'] == 'pending']
found = [r for r in batch1_rows if r['research_status'] == 'found']
print(f'Batch 1: {len(batch1_rows)} total, {len(researched)} researched, {len(pending)} still pending')
print(f'Found headshots: {len(found)}')
assert len(pending) == 0, f'{len(pending)} politicians still pending in Batch 1'
# Check found rows have URLs
for r in found:
    assert r['found_url'].strip(), f'Found status but no URL for {r[\"member_name\"]}'
    assert r['source_page_url'].strip(), f'Found status but no source for {r[\"member_name\"]}'
print('All Batch 1 rows researched with valid data')
"</automated>
  </verify>
  <done>All ~66 Batch 1 politicians have research_status set to found/not_found/blocked, with URLs recorded for all found headshots</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Checkpoint: Spot-check research results</name>
  <action>
  <what-built>Headshot research for 12 cities (~66 politicians) — the largest gap cities including 1 failed-status city (Redondo Beach)</what-built>
  <how-to-verify>
    1. Open EV-Backend/scripts/headshot_research_manifest.csv
    2. Filter to Batch 1 cities — verify no rows have research_status=pending
    3. Spot-check 3-5 found_url entries by opening them in a browser — confirm they are real headshot images
    4. Spot-check 1-2 source_page_url entries — confirm the image appears on that page
    5. Review any not_found entries — do they seem reasonable? (small cities, newer officials)
    6. Check Redondo Beach specifically — were photos found despite the "failed" batch scraper status?
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  </action>
  <verify>User spot-check of found_url entries in browser</verify>
  <done>User has verified research quality for this batch</done>
</task>

</tasks>

<verification>
- Zero pending rows for Batch 1 cities in the manifest
- Every found row has both found_url and source_page_url populated
- research_date is set for all researched rows
- CSV structure intact (all 12 columns, no corrupted rows)
</verification>

<success_criteria>
All ~66 politicians in Batch 1 cities have been researched. Each has a found headshot URL, a not_found status, or a blocked status. The manifest CSV is valid and ready for the next batch.
</success_criteria>

<output>
After completion, create `.planning/phases/63-headshot-research-sprint/63-02-SUMMARY.md`
</output>
