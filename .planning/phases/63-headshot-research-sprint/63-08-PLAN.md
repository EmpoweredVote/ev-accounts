---
phase: 63-headshot-research-sprint
plan: 08
type: execute
wave: 8
depends_on: ["63-07"]
files_modified:
  - EV-Backend/scripts/headshot_research_manifest.csv
autonomous: false
requirements:
  - PHOTO-01
  - PHOTO-02

must_haves:
  truths:
    - "Every politician in the entire manifest has been researched — zero pending rows remain"
    - "Burbank (blocked/Cloudflare) has been researched through manual browser navigation"
    - "The manifest is a complete research output ready for Phase 64 upload pipeline"
  artifacts:
    - path: "EV-Backend/scripts/headshot_research_manifest.csv"
      provides: "Fully completed research manifest — every row has a research result"
      contains: "found"
  key_links:
    - from: "EV-Backend/scripts/headshot_research_manifest.csv"
      to: "Phase 64 upload pipeline"
      via: "politician_id + found_url columns"
      pattern: "politician_id.*found_url"
---

<objective>
Research headshots for Batch 7 (final): 10 cities with ~14 politicians, plus Burbank (blocked). Validate 100% manifest completion.

Purpose: Complete the final small batch and the single Cloudflare-blocked city (Burbank). After this plan, every politician in the manifest has been researched — the phase goal is met.

Output: Fully completed headshot_research_manifest.csv with zero pending rows, ready for Phase 64 upload.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-headshot-research-sprint/63-CONTEXT.md
@.planning/phases/63-headshot-research-sprint/63-07-SUMMARY.md
@EV-Backend/scripts/headshot_research_manifest.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research headshots for Batch 7 + Burbank (11 cities, ~14+ politicians)</name>
  <files>EV-Backend/scripts/headshot_research_manifest.csv</files>
  <action>
Research headshots for the final batch plus the blocked city:

**Batch 7 — remaining small cities (all scraped status):**
1. **City of La Mirada** (~2 members)
2. **City of Montebello** (~2 members)
3. **City of Culver City** (~2 members)
4. **City of Long Beach** (~2 members)
5. **City of La Canada Flintridge** (~1 member)
6. **City of Duarte** (~1 member)
7. **City of Covina** (~1 member)
8. **City of Huntington Park** (~1 member)
9. **City of Glendale** (~1 member)
10. **City of Compton** (~1 member)

**Cloudflare-blocked city:**
11. **City of Burbank** (BLOCKED status) — This city was excluded from the original manifest and included via `--include-blocked`. Research via manual Playwright navigation. Burbank's city website (burbank.gov) is Cloudflare-protected. Try navigating directly — if Cloudflare challenge appears, attempt to complete it. If still blocked, try alternative sources: Burbank city YouTube channel screenshots, local news articles with council photos, LinkedIn profiles.

**Research protocol:**
1. Read manifest CSV — process ALL remaining pending rows
2. Playwright MCP → navigate → extract headshot URLs
3. Sub-pages for 2-3 attempts, official sources preferred
4. Update rows with results

**Final validation after all research:**
After completing all cities, run a final check:
- Count total rows, found, not_found, blocked
- Verify zero pending rows
- Print summary statistics for the phase SUMMARY

**CSV update:** Read, update in-place, write back. Preserve all prior data.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-Backend/scripts && python3 -c "
import csv
from collections import Counter
with open('headshot_research_manifest.csv') as f:
    rows = list(csv.DictReader(f))
pending = [r for r in rows if r['research_status'] == 'pending']
assert len(pending) == 0, f'PHASE INCOMPLETE: {len(pending)} rows still pending'
statuses = Counter(r['research_status'] for r in rows)
print(f'PHASE 63 COMPLETE - All {len(rows)} politicians researched')
print(f'Status breakdown:')
for status, count in sorted(statuses.items(), key=lambda x: -x[1]):
    print(f'  {status}: {count} ({count*100//len(rows)}%)')
found_with_urls = [r for r in rows if r['research_status'] == 'found' and r['found_url'].strip()]
print(f'Found with valid URLs: {len(found_with_urls)}')
# Verify all found rows have both URLs
for r in rows:
    if r['research_status'] == 'found':
        assert r['found_url'].strip(), f'Missing found_url for {r[\"member_name\"]}'
        assert r['source_page_url'].strip(), f'Missing source_page_url for {r[\"member_name\"]}'
print('All found rows have valid URL data')
print(f'Ready for Phase 64 upload pipeline')
"</automated>
  </verify>
  <done>100% of manifest researched — zero pending rows, all found rows have valid URLs, ready for Phase 64</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Spot-check research results</name>
  <action>
  <what-built>Complete headshot research for all ~300 politicians across 82+ cities. This is the final checkpoint for the entire phase.</what-built>
  <how-to-verify>
    1. Open EV-Backend/scripts/headshot_research_manifest.csv
    2. Confirm zero rows with research_status=pending
    3. Review the status breakdown:
       - What percentage were "found"? (target: as high as possible, but success = 100% researched, not 100% found)
       - Are the "not_found" entries reasonable? (very small cities, newer officials)
       - Were any marked "blocked"? (only Burbank expected)
    4. Spot-check 5-10 found_url entries across different cities — open in browser to confirm real headshot images
    5. Check that source_page_url entries point to legitimate city government pages
    6. Verify the CSV is clean: no corrupted rows, all columns populated appropriately
    7. Confirm this output is sufficient for Phase 64 to proceed with uploads
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 63, or describe issues for a gap-closure plan</resume-signal>
  </action>
  <verify>User spot-check of found_url entries in browser</verify>
  <done>User has verified research quality for this batch</done>
</task>

</tasks>

<verification>
- Zero pending rows in entire manifest
- Every "found" row has both found_url and source_page_url
- Every row has a research_date
- CSV structure is clean (12 columns, no corruption)
- politician_id column has valid integer IDs for Phase 64
</verification>

<success_criteria>
Every politician in the headshot_research_manifest.csv has been reviewed. Each has either a sourced image URL (found), a confirmed-not-available status (not_found), or an access-blocked status (blocked). The CSV is ready for Phase 64 upload pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/63-headshot-research-sprint/63-08-SUMMARY.md`
</output>
