---
phase: 108.1-essentials-client-non-json-hardening
verified: 2026-04-11T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 108.1: Essentials Client Non-JSON Hardening — Verification Report

**Phase Goal:** `fetch_body_roster` returns a clean `EssentialsClientError` (instead of raw `JSONDecodeError`) when the server returns HTTP 200 with non-JSON content, so CLI and callers get a consistent error envelope.

**Verified:** 2026-04-11
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | When `fetch_body_roster` receives HTTP 200 with non-JSON body, callers see `EssentialsClientError` (not raw `JSONDecodeError`) | VERIFIED | `src/essentials_client.py` lines 114-121: `try: return resp.json()` / `except ValueError as exc: raise EssentialsClientError(..., status=resp.status_code) from exc`. Test `test_fetch_body_roster_non_json_200_wrapped` (test file line 127) asserts this behavior and passes. |
| 2 | The raised `EssentialsClientError` message contains a snippet of the body (first 200 chars) | VERIFIED | Line 117-119: `snippet = (resp.text or "")[:200]` then `f"Non-JSON response from {url}: {snippet}"`. Test asserts `"<!doctype html" in str(exc_info.value)`. |
| 3 | All previously passing essentials_client tests still pass (no regressions) | VERIFIED | Full pytest run: `49 passed in 0.09s`. Baseline was 48 (per SUMMARY), new total is 49 — exactly +1 as expected. |
| 4 | `refresh_roster.py` CLI continues to exit non-zero on this error with a clean stderr message | VERIFIED | `git diff feat/108-roster-client~2 feat/108-roster-client -- refresh_roster.py src/refresh_roster.py` returns empty. CLI already catches `EssentialsClientError` from Phase 108; wrapping at the client layer is sufficient. |
| 5 | No party/affiliation fields are read, logged, or persisted anywhere in essentials_client.py | VERIFIED (see note) | `grep -n "party" src/essentials_client.py` → 0 matches. `grep -n "affiliation" src/essentials_client.py` → 1 pre-existing match on line 4, which is the antipartisan *rule statement* in the module docstring ("Never reads, logs, or persists any affiliation fields"), not a data reference. Unchanged since Phase 108 commit `aada454`. No new affiliation references introduced by 108.1 — confirmed by the scoped diff showing only the try/except block added. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `CouncilScribe/src/essentials_client.py` | `fetch_body_roster` with try/except around `resp.json()` raising `EssentialsClientError` on parse failure; must contain reference to `JSONDecodeError` (comment acceptable) | VERIFIED | Lines 114-121 contain the try/except. Line 116 comment: `# includes requests.exceptions.JSONDecodeError`. `from exc` chaining on line 121. |
| `CouncilScribe/tests/test_essentials_client.py` | New test asserting `EssentialsClientError` on 200+text/html response; must contain `text/html` | VERIFIED | Test `test_fetch_body_roster_non_json_200_wrapped` at line 127. Line 139: `m.headers = {"Content-Type": "text/html; charset=utf-8"}`. Patches `json.side_effect` to `JSONDecodeError`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `fetch_body_roster resp.json()` call | `EssentialsClientError` | `try/except ValueError` (covers `JSONDecodeError` via inheritance) | WIRED | Lines 114-121; `from exc` chaining preserves traceback for debuggability. |
| New test | `fetch_body_roster` | Patched `requests.get` returning status 200 + `text/html` + `json.side_effect = JSONDecodeError` | WIRED | Test lines 126-151 exercise the exact production code path. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full pytest suite green | `python -m pytest tests/ -q` (in `.venv`) | `49 passed in 0.09s` | PASS |
| Antipartisan grep (party) | `grep -n "party" src/essentials_client.py` | 0 matches | PASS |
| Antipartisan grep (affiliation) | `grep -n "affiliation" src/essentials_client.py` | 1 pre-existing match (line 4 rule statement in docstring) | PASS (see truth #5 note) |
| Out-of-scope files untouched | `git diff feat/108-roster-client~2 feat/108-roster-client -- refresh_roster.py src/roster.py src/alias_gen.py src/refresh_roster.py` | empty | PASS |
| Diff scope | `git diff feat/108-roster-client~2 feat/108-roster-client --stat` | `src/essentials_client.py` +8/-1, `tests/test_essentials_client.py` +28 | PASS |
| Commits exist | `git log feat/108-roster-client -5` | `b920514` (RED test) and `2a60f2b` (GREEN fix) both present | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CSROSTER-03 (hardening extension) | 108.1-01-PLAN | Client error envelope must cover all "response unusable" cases, not just transport-layer failures (per CONTEXT.md). Note: CSROSTER-03 in REQUIREMENTS.md is the alias generation requirement scoped to Phase 108; 108.1 extends the same requirement ID to cover error envelope hardening per the phase context. | SATISFIED | `fetch_body_roster` now wraps both transport failures and HTTP 200 + non-JSON bodies in `EssentialsClientError`. Verified by `test_fetch_body_roster_non_json_200_wrapped` passing. |

### Anti-Patterns Found

None. Scanned `src/essentials_client.py` and `tests/test_essentials_client.py` for TODO/FIXME/placeholder/stub markers — zero matches. The fix is fully implemented with error chaining (`from exc`) preserving debuggability.

### Deviation Evaluation (from SUMMARY.md §Deviations)

SUMMARY.md flagged one minor interpretation of the antipartisan acceptance criterion: the file has a single pre-existing `affiliation` match on line 4, which is the module docstring stating the antipartisan rule itself, not a violation. This was present in Phase 108 (commit `aada454`) and was accepted by Phase 108's own antipartisan gate.

**Evaluation:** ACCEPTABLE. The plan's acceptance criterion was "zero matches," taken literally, but:
1. The only match is the rule-statement, not a data reference
2. `grep -n "party"` returns zero (strict literal interpretation of the string requested by the verifier task)
3. No new `party`/`affiliation` references were introduced by 108.1 — the diff confirms this
4. Phase 108 passed an identical gate with the same pre-existing match

No override entry needed; this is a documentation artifact, not a goal violation.

### Gaps Summary

No gaps. All five must-have truths are verified with direct evidence. The fix is minimal, focused, and correctly scoped:

- **Correctness:** `try/except ValueError` correctly catches `requests.exceptions.JSONDecodeError` (which inherits from `json.JSONDecodeError` → `ValueError`), consistent with the existing 404/422 branches in the same file.
- **Observability:** Error message includes URL and first 200 chars of response body — operators can diagnose root cause from stderr alone.
- **Chaining:** `from exc` preserves the original traceback for deep debugging.
- **Scope discipline:** Only the two intended files modified; `refresh_roster.py`, `roster.py`, `alias_gen.py` untouched (verified via git diff).
- **Test coverage:** Regression test pins the behavior with a realistic SPA catch-all HTML body, exactly matching the Phase 108 HUMAN-UAT G2 symptom.

Gap G2 from Phase 108 HUMAN-UAT is closed. CSROSTER-03 hardening extension is satisfied. Phase goal achieved.

---

*Verified: 2026-04-11*
*Verifier: Claude (gsd-verifier)*
