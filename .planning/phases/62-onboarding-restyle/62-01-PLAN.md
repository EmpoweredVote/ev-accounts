---
phase: 62-onboarding-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/onboarding/steps/LocationStep.tsx
autonomous: true

must_haves:
  truths:
    - "User sees AppNav at top of LocationStep when arriving via onboarding"
    - "User sees StepProgress bar showing 'Step 2 of 3' on LocationStep"
    - "User sees four address input fields (street, city, state, ZIP) immediately, with no 'I understand — show the input' reveal gate"
    - "User sees 'Find your civic community' heading with a pin icon"
    - "User sees the copy 'We use your location to connect you with your local civic space.' with NO 'Learn More' link"
    - "User can submit a complete address and reach the celebration step on success"
    - "User sees an inline error if address validation/geocoding fails"
    - "When LocationStep is rendered with isUpdate=true (UpdateLocationPage), AppNav and StepProgress are NOT rendered (update flow keeps its own wrapper)"
  artifacts:
    - path: "app/src/pages/onboarding/steps/LocationStep.tsx"
      provides: "Restyled LocationStep using v2.0 design components (AppNav, StepProgress, AuthCard, AuthInput, PrimaryButton)"
      exports: ["LocationStep", "LocationResult"]
      contains: "AuthInput"
  key_links:
    - from: "app/src/pages/onboarding/steps/LocationStep.tsx"
      to: "POST /connect/set-location"
      via: "apiFetch"
      pattern: "apiFetch.*connect/set-location"
    - from: "app/src/pages/onboarding/steps/LocationStep.tsx"
      to: "AppNav, StepProgress, AuthCard, AuthInput, PrimaryButton"
      via: "import from '../../../components/...'"
      pattern: "from '\\.\\./\\.\\./\\.\\./components/(AppNav|StepProgress|AuthCard|AuthInput|PrimaryButton)'"
---

<objective>
Restyle `LocationStep.tsx` with the v2.0 design language: dark navy background, AppNav, StepProgress (Step 2 of 3), AuthCard wrapper, AuthInput fields, PrimaryButton CTA. Remove the legacy "reveal" gate so the four address fields are immediately visible. Drop the "Learn More" link from the explanatory copy. Preserve the existing API contract — the component still exports `LocationStep` and `LocationResult`, still calls `POST /connect/set-location`, and still supports `isUpdate` for the settings flow.

Purpose: ONBD-01 + ONBD-03 — LocationStep must render the shared AppNav + StepProgress wrapper, show a pin icon and "Find your civic community" heading, and display four AuthInput fields (street, city, state, ZIP) with no skip option, all in the new design language.
Output: A single restyled file that drops in cleanly under OnboardingPage and continues to work inside UpdateLocationPage without the new chrome (AppNav/StepProgress) double-wrapping the update view.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-onboarding-restyle/62-CONTEXT.md
@.planning/phases/62-onboarding-restyle/62-RESEARCH.md

# v2.0 design components (Phase 60 — APIs locked)
@app/src/components/AppNav.tsx
@app/src/components/StepProgress.tsx
@app/src/components/AuthCard.tsx
@app/src/components/AuthInput.tsx
@app/src/components/PrimaryButton.tsx
@app/src/components/SecondaryButton.tsx

# Pattern reference (Phase 61 — same scaffold to follow)
@app/src/pages/SignupPage.tsx

# Files this plan modifies
@app/src/pages/onboarding/steps/LocationStep.tsx

# Consumers — must not break
@app/src/pages/onboarding/OnboardingPage.tsx
@app/src/pages/settings/UpdateLocationPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite LocationStep with v2.0 chrome and four-field address form</name>
  <files>app/src/pages/onboarding/steps/LocationStep.tsx</files>
  <action>
    Rewrite `app/src/pages/onboarding/steps/LocationStep.tsx`. Preserve the existing exported API exactly: `export interface LocationResult` (unchanged shape), `export function LocationStep({ isUpdate = false, onSuccess })`, props `{ isUpdate?: boolean; onSuccess: (result: LocationResult) => void }`, and the `POST /connect/set-location` call with `{ address, ...(isUpdate && { force: true }) }`.

    Behavioral changes:
    1. **Remove the `revealed` state and the "I understand — show the input" button entirely.** The four address fields (street, city, state, ZIP) are visible immediately on mount. Delete the `bg-ev-teal/5` context box that gated the form.
    2. **Remove the "Learn More" link.** The explanatory copy is just one line: `"We use your location to connect you with your local civic space."` No link, no expand-toggle, no longer copy.
    3. **No skip option.** Location is required. The submit button stays disabled until all four fields have non-empty trimmed values. On geocoding failure (ADDRESS_NOT_FOUND, PO_BOX_REJECTED, OUT_OF_COVERAGE, VALIDATION_ERROR, INTERNAL_ERROR) show the existing inline error message; do not offer a skip path.

    Layout when `isUpdate === false` (onboarding entry path) — match the SignupPage scaffold:
    ```tsx
    <div className="min-h-screen bg-ev-navy flex flex-col">
      <AppNav />
      <div className="flex-1 px-4 py-8">
        <div className="max-w-sm mx-auto space-y-6">
          <StepProgress currentStep={2} totalSteps={3} />
          <AuthCard>
            {/* pin icon + heading + copy + form */}
          </AuthCard>
        </div>
      </div>
    </div>
    ```

    Layout when `isUpdate === true` (settings path, used by `UpdateLocationPage`):
    - **Do NOT render AppNav or StepProgress** (UpdateLocationPage has its own back button and wraps the step itself).
    - Render only the AuthCard contents (no `bg-ev-navy`, no outer min-h-screen) so UpdateLocationPage's existing wrapper continues to work. Wrap the form in an AuthCard so the dark theme matches v2.0, but skip the page chrome.
    - Heading on update: "Update your location" with the existing one-line copy "Moved? We'll refresh your district connections." Keep the existing submit-button copy variant: `isUpdate ? 'Update location' : 'Find my representatives'`.

    Inside the AuthCard for the onboarding (non-update) path:
    1. **Pin icon** — a centered circular badge `w-16 h-16 rounded-full bg-ev-blue/10 flex items-center justify-center` containing an SVG location-pin icon (`text-ev-blue`, `w-8 h-8`). Use a Heroicons-style outline pin path (e.g. `M15 10.5a3 3 0 11-6 0 3 3 0 016 0z M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25s-7.5-4.108-7.5-11.25a7.5 7.5 0 1115 0z`).
    2. **Heading block** — `<h2 className="text-2xl font-bold text-white text-center">Find your civic community</h2>` followed by `<p className="text-sm text-gray-400 text-center leading-relaxed">We use your location to connect you with your local civic space.</p>`. **No "Learn More" link.**
    3. **Error banner** (if `error` set) — `<div className="bg-red-950/40 border border-red-800/60 rounded-xl px-4 py-3"><p className="text-sm text-ev-red">{error}</p></div>` matching SignupPage error pattern.
    4. **Form** — four AuthInput components in order:
       - `<AuthInput label="Street address" value={street} onChange={setStreet} placeholder="123 Main St" autoComplete="address-line1" autoFocus required />`
       - `<AuthInput label="City" value={city} onChange={setCity} placeholder="Indianapolis" autoComplete="address-level2" required />`
       - `<AuthInput label="State" value={state} onChange={(value) => setState(value.toUpperCase().slice(0, 2))} placeholder="IN" autoComplete="address-level1" required />` — keep the 2-char uppercase normalization
       - `<AuthInput label="ZIP code" value={zip} onChange={(value) => setZip(value.replace(/\D/g, '').slice(0, 5))} placeholder="46201" autoComplete="postal-code" inputMode="numeric" required />` — pass `inputMode` via `inputProps={{ inputMode: 'numeric' }}`
    5. **Submit** — `<PrimaryButton type="submit" disabled={loading || !isComplete}>{loading ? 'Finding your community…' : 'Find my representatives'}</PrimaryButton>`
    6. **Back button (onboarding path only)** — render a `<SecondaryButton onClick={() => navigate('/welcome')}>Back</SecondaryButton>` BELOW the PrimaryButton inside the form (still inside the AuthCard). Use `useNavigate` from `react-router-dom`. Do NOT render the Back button when `isUpdate` (UpdateLocationPage has its own back arrow).

    AuthInput note: AuthInput's `inputMode` is not a top-level prop, so for the ZIP field pass `inputProps={{ inputMode: 'numeric' }}`. AuthInput's onChange is `(value: string) => void`, so each setter must accept a string (not a synthetic event) — already true for `setStreet`/`setCity`, but the State and ZIP normalizers must take `(value)` and return the transformed string.

    Imports to add at the top:
    ```tsx
    import { useNavigate } from 'react-router-dom';
    import { AppNav } from '../../../components/AppNav';
    import { StepProgress } from '../../../components/StepProgress';
    import { AuthCard } from '../../../components/AuthCard';
    import { AuthInput } from '../../../components/AuthInput';
    import { PrimaryButton } from '../../../components/PrimaryButton';
    import { SecondaryButton } from '../../../components/SecondaryButton';
    ```
    Keep the existing import of `apiFetch` from `'../../../lib/api'`. Drop the implicit React `useState` import only if unused — it is still used for `street/city/state/zip/error/loading`.

    Preserve the existing `ERROR_MESSAGES` mapping and the existing `handleSubmit` logic that calls `apiFetch<LocationResult>('/connect/set-location', ...)` and invokes `onSuccess(result)`. Do NOT change the API contract or error-code mapping.

    Why the dual-mode layout: UpdateLocationPage already renders its own `← Back` button and its own outer wrapper (`bg-white dark:bg-ev-black min-h-screen`). If LocationStep unconditionally rendered AppNav+StepProgress+bg-ev-navy, the settings page would double-wrap and show a step counter where there is no step flow. Gating the chrome on `!isUpdate` is the simplest fix and preserves the settings flow exactly as it ships today.
  </action>
  <verify>
    1. `cd app && npx tsc --noEmit` passes (no type errors).
    2. `cd app && npm run build` succeeds.
    3. `grep -n "revealed\|setRevealed\|I understand" app/src/pages/onboarding/steps/LocationStep.tsx` returns nothing — reveal gate is gone.
    4. `grep -n "Learn More" app/src/pages/onboarding/steps/LocationStep.tsx` returns nothing — Learn More copy is gone.
    5. `grep -n "AppNav\|StepProgress\|AuthCard\|AuthInput\|PrimaryButton" app/src/pages/onboarding/steps/LocationStep.tsx` shows all five components imported and used.
    6. `grep -n "Step 2 of 3\|currentStep={2}" app/src/pages/onboarding/steps/LocationStep.tsx` shows the step counter is set to 2 of 3 (via `<StepProgress currentStep={2} totalSteps={3} />`).
    7. `grep -n "isUpdate" app/src/pages/onboarding/steps/LocationStep.tsx` shows the `isUpdate` prop is preserved and used to conditionally render chrome.
    8. `grep -n "/connect/set-location" app/src/pages/onboarding/steps/LocationStep.tsx` confirms the API call still exists.
    9. `grep -n "export interface LocationResult\|export function LocationStep" app/src/pages/onboarding/steps/LocationStep.tsx` confirms the public exports are unchanged.
  </verify>
  <done>
    LocationStep renders the v2.0 design language when reached via onboarding (AppNav + StepProgress "2 of 3" + dark AuthCard + four AuthInput fields + PrimaryButton + SecondaryButton "Back"). The reveal gate and "Learn More" copy are removed. The component still exports `LocationStep` and `LocationResult` with unchanged signatures so OnboardingPage and UpdateLocationPage keep compiling, and `isUpdate` is honored to suppress AppNav/StepProgress in the settings path.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles cleanly (`npx tsc --noEmit` in `app/`).
- Vite build succeeds (`npm run build` in `app/`).
- Visiting `/onboarding` (after a fresh signup, before location set) shows the new dark-themed location step with AppNav at the top, a "Step 2 of 3" StepProgress bar, a pin icon in a blue badge, the heading "Find your civic community", a single line of copy without any "Learn More" link, four address fields visible immediately, and "Find my representatives" + "Back" buttons.
- Visiting `/settings/location` (UpdateLocationPage) still shows the existing back-arrow header WITHOUT a duplicate AppNav and WITHOUT a step counter, and submitting a new address still updates the user's location.
- Submitting a complete valid address calls `POST /connect/set-location` and triggers `onSuccess` with a `LocationResult`.
- Submitting an invalid/PO-box address shows the inline error message and keeps the form mounted; no skip option is available.
</verification>

<success_criteria>
- `app/src/pages/onboarding/steps/LocationStep.tsx` is rewritten with all imports above and renders the v2.0 chrome conditional on `!isUpdate`.
- All four AuthInput fields are visible on mount; the submit button is disabled until all four have non-empty trimmed values.
- The exported `LocationResult` interface is byte-identical to the prior shape so OnboardingPage and UpdateLocationPage compile without changes.
- UpdateLocationPage continues to render correctly (no double AppNav, no stray "Step 2 of 3").
</success_criteria>

<output>
After completion, create `.planning/phases/62-onboarding-restyle/62-01-SUMMARY.md` documenting the file rewritten, the props/exports preserved, and any UpdateLocationPage compatibility notes.
</output>
