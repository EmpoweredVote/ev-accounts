---
phase: 161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca
plan: 03
type: execute
wave: 2
depends_on: ["161-02"]
files_modified:
  - backend/data/stance-research/az-2026-house/_merge.ts
  - backend/data/stance-research/az-2026-house/_push.ts
  - backend/data/stance-research/az-2026-house/_TOPIC_SCALE_FULL.txt
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "Every new AZ candidate lacking stances has sourced federal-24 chairs-not-polarity stances, each answer paired to an inform.politician_context row with a real fetched source URL"
    - "0 unsourced stance rows for AZ; thin candidates have per-topic or whole-record honest-skips with a written search trail"
    - "Already-stanced AZ incumbents are skipped (report-only); AZ stances are pushed as a self-contained state slice before the Jul-21 primary"
  artifacts:
    - path: "backend/data/stance-research/az-2026-house/_merge.ts"
      provides: "CSV validate+merge with IN_SCOPE = new AZ external_ids, federal-24 topic gate"
      contains: "IN_SCOPE"
  key_links:
    - from: "inform.politician_answers"
      to: "inform.politician_context (source URLs)"
      via: "paired rows per topic, 0 unsourced"
      pattern: "politician_context"
---

<objective>
Research and push federal-24 chairs-not-polarity stances for the 32 new AZ candidates seeded in 161-02 (D-04 standard uniform effort; D-03a majors/incumbents first, fringe last), completing AZ end-to-end before the Jul-21 primary. Already-stanced incumbents are skipped (report-only). 0-unsourced gate; honest-skip thin candidates only with a written search trail. Anchor for USHC3-05.

Output: az-2026-house stance directory with per-candidate CSVs, _merge.ts, _push.ts, filtered _TOPIC_SCALE_FULL.txt, and pushed answers/context/quotes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-RESEARCH.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-PATTERNS.md
@.claude/skills/research-stances/SKILL.md
@backend/data/stance-research/wa-house-a/_merge.ts
@backend/data/stance-research/mi-2026-house/_push.ts
@backend/data/seed-az-2026-house/161-02-az-reconciliation.csv

<interfaces>
IN_SCOPE = the 32 new AZ external_ids from 161-02 (band -(4*10000+cd*100+seq)); do NOT include incumbents (-4001..-4009 already stanced). Federal-24 topic set = live inform.compass_topics WHERE is_live minus 11 city-only, 8 judicial-*, and data-centers (must equal 24 — 161-RESEARCH Pitfall 5 + Code Examples query). CSV columns: external_id, full_name, topic_key, value(1-5), reasoning, source_url_1..3, quote_text, quote_deidentified. _push.ts resolves external_id -> politician_id, INSERTs politician_answers + politician_context (sources[]) + quotes with surname-leak guard. Use _push.ts (not _push_uuid.ts) since 161-02 migrations already created the politician rows with resolvable external_ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up the az-2026-house stance directory and federal-24 topic file</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (STEP 0 topic resolution query; push mechanics; 3-concurrency cap; 0-unsourced; honest-skip standard)
    - backend/data/stance-research/wa-house-a/_merge.ts (merge/validate shape to clone)
    - backend/data/stance-research/mi-2026-house/_push.ts (transactional push + surname-leak guard)
    - 161-RESEARCH.md (Code Examples: live federal-24 query, must equal 24)
  </read_first>
  <action>
    Create backend/data/stance-research/az-2026-house/. Run the live topic query and filter to federal-24 (exclude the 11 city-only topics, all judicial-* topics, and data-centers); confirm the count equals 24 before dispatching any agent; write the resulting topic keys + exact 1-5 scale texts to az-2026-house/_TOPIC_SCALE_FULL.txt. Clone _merge.ts from wa-house-a with IN_SCOPE set to the 32 new AZ external_ids and FEDERAL set to the 24 topic keys. Clone _push.ts from mi-2026-house verbatim (keeps the surname-leak word-boundary guard). Use the {state}-2026-house naming (avoid the pre-existing az-house-a incumbent dir).
  </action>
  <verify>
    <automated>cd backend && wc -l data/stance-research/az-2026-house/_TOPIC_SCALE_FULL.txt</automated>
  </verify>
  <acceptance_criteria>
    - az-2026-house/_TOPIC_SCALE_FULL.txt lists exactly 24 federal topics with 1-5 scale texts; no city/judicial/data-centers topic present
    - _merge.ts IN_SCOPE contains the 32 new AZ external_ids and no incumbent id
    - _push.ts retains the surname-leak guard regex
  </acceptance_criteria>
  <done>AZ stance directory ready with a verified federal-24 topic file and IN_SCOPE-scoped merge/push scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Research stances (majors/incumbents-first, fringe last) at 3-concurrency and validate</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (agent dispatch, chairs-not-polarity, evidence-only, honest-skip search-trail standard)
    - 161-CONTEXT.md (D-04 uniform effort; D-03a majors-first ordering; the 159 false-skip lesson)
  </read_first>
  <action>
    Dispatch politician-stance-researcher agents at 3-concurrency MAX (never more — rate-limit deaths), ordering evidenced majors first and fringe filers last (D-03a). Embed the exact 1-5 scale texts per topic in each agent prompt (never "5=progressive"). Each agent produces a per-candidate CSV in az-2026-house/. Chairs-not-polarity: value = the scale position the evidence supports; never infer from party. For thin candidates, honest-skip per-topic or whole-record ONLY with a written search trail documenting what was searched (a header-only CSV without a trail is NOT a valid skip — the 21/25 false-skip lesson). Validate the first wave of output before continuing. Run _merge.ts and fix any reported problems (bad external_id, bad topic_key, value out of 1-5, empty reasoning, no http source).
  </action>
  <verify>
    <automated>cd backend && node --import tsx data/stance-research/az-2026-house/_merge.ts</automated>
  </verify>
  <acceptance_criteria>
    - _merge.ts reports 0 problems; every answer row has value 1-5, non-empty reasoning, and at least one http source
    - Agents ran at <= 3 concurrency; whole-record and per-topic skips each carry a written search trail
    - No party-inferred values (spot-check reasoning cites fetched sources, not party)
  </acceptance_criteria>
  <done>Validated federal-24 AZ stance CSVs merged with 0 problems.</done>
</task>

<task type="auto">
  <name>Task 3: Primary-source verification pass, then push AZ stances</name>
  <read_first>
    - backend/data/stance-research/mi-2026-house/_push.ts (push + quote dedup + surname-leak guard)
    - 161-RESEARCH.md (mandatory primary-source verification before every push)
  </read_first>
  <action>
    Run the mandatory primary-source verification pass: re-fetch raw quotes for a sample and delete any polarity-inference rows before pushing. Then push AZ stances with _push.ts against prod (cd backend, source .env, run per-candidate). This is the AZ-only state slice push (D-03 per-state push) — do not batch with other states. If any quote is corrected after an initial push, wipe essentials.quotes for that pid before re-push. Record whole-record honest-skip pids (with search trails) for the 161-11 gate pin table.
  </action>
  <verify>
    <automated>cd backend && ls data/stance-research/az-2026-house</automated>
  </verify>
  <acceptance_criteria>
    - psql shows 0 AZ answer rows without a paired politician_context source URL (0-unsourced)
    - Every in-scope AZ candidate has >= 1 sourced stance OR a pinned whole-record honest-skip with a written trail
    - Quotes pass the surname-leak guard; no incumbent (already-stanced) was re-pushed
  </acceptance_criteria>
  <done>AZ federal-24 stances pushed to prod, 0 unsourced, honest-skips documented; AZ is fully live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-produced CSV -> inform tables | Researched stance text + sources written to prod |
| quotes -> Read and Rank surfacing | Deidentified quotes must not leak candidate surname |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-03-01 | Tampering | Unsourced / party-inferred stance rows reaching prod | mitigate | _merge.ts http-source gate + 0-unsourced psql check; chairs-not-polarity; primary-source verification pass before push |
| T-161-03-02 | Repudiation | Fake honest-skip (header-only CSV, no search trail) | mitigate | Written search trail required per skip (21/25 false-skip lesson); whole-record skips pinned in 161-11 gate |
| T-161-03-03 | Information disclosure | Surname leak in deidentified quotes | mitigate | Surname word-boundary guard in _push.ts retained verbatim |
| T-161-03-04 | Tampering | Scoring an already-stanced incumbent (double work / overwrite) | mitigate | IN_SCOPE excludes incumbent external_ids; only new candidates researched |
</threat_model>

<verification>
- 0 unsourced AZ answer rows (every answer paired to a politician_context source URL)
- Every in-scope AZ candidate has >= 1 sourced federal stance or a pinned whole-record skip with a trail
- Agents ran at <= 3 concurrency; no party inference; incumbents untouched
</verification>

<success_criteria>
All new AZ candidates have sourced federal-24 chairs-not-polarity stances (0 unsourced), pushed as a self-contained AZ slice before the Jul-21 primary, with documented honest-skips for the gate.
</success_criteria>

<output>
Create .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-03-SUMMARY.md recording per-candidate stance counts, whole-record honest-skip pids + trails, and any quote corrections.
</output>
