---
phase: 161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca
plan: 04
type: execute
wave: 2
depends_on: ["161-02"]
files_modified:
  - backend/scripts/161-wa-generate.mts
  - backend/migrations/1189_seed_wa_2026_house_elections_races.sql
  - backend/migrations/1190_seed_wa_2026_house_candidates.sql
  - backend/scripts/seed-wa-house-headshots.py
  - backend/data/seed-wa-2026-house/161-04-wa-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "Each WA district (geo_id 5301-5310) surfaces its full qualified top-two field on /elections for an in-district coordinate"
    - "Renominated WA incumbents reuse existing politician_id (0 duplicate full_name); retired Newhouse (WA-4) has no active race_candidates row"
    - "Every new WA candidate has a headshot or a documented honest-skip; no candidate card surfaces party"
  artifacts:
    - path: "backend/migrations/1190_seed_wa_2026_house_candidates.sql"
      provides: "~60 new WA politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
    - path: "backend/scripts/161-wa-generate.mts"
      provides: "WA field to migration-SQL generator"
      contains: "sqlStr"
  key_links:
    - from: "essentials.race_candidates"
      to: "essentials.races -> offices -> districts geo_id 53xx"
      via: "race_id FK, office_id never null"
      pattern: "race_candidates"
---

<objective>
Seed WA's full 2026 US House field (10 districts, ~60 new records — the heaviest seed slice of the phase). WA is top-two (ballot_system) but is seeded EXACTLY like any late-primary state: one race per district, all qualified candidates active from all parties — do NOT invent WA-specific races/candidates logic (161-RESEARCH Pitfall 4; the top-two cull is Phase 167's concern). Author one WA elections row + 10 races, create ~60 new records + race_candidates (reuse incumbent pids; retired Newhouse WA-4 gets no active row), then headshots. Continues USHC3-02/03/04.

Output: 161-wa-generate.mts, two WA migrations (run on prod), seed-wa-house-headshots.py, WA reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-RESEARCH.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@backend/scripts/161-az-generate.mts
@backend/migrations/1146_seed_mi_2026_house_elections_races.sql
@backend/scripts/seed-mi-house-headshots.py

<interfaces>
WA FIPS=53; geo_ids 5301-5310. Incumbents keep existing external_id (incumbent_external_id -53001..-53010; WA-4 Newhouse -53004 RETIRED). New challengers: external_id = -(53*10000 + cd*100 + seq), seq from 1 per CD (WA collision-free per 160-negative-id-audit; re-verify live). Election "WA 2026 Statewide General", date 2026-11-03, type general. Races description "PROVISIONAL: pre-primary qualified field, cull >= 2026-08-05" (Aug-4 top-two primary). race_candidates shape identical to AZ. NEVER office_id NULL; NEVER party on race_candidates. New candidates per district = new_records_needed column of the WA field-table rows (withdrawn candidates already excluded per VoteWA status). Names include apostrophes/quotes (Jacek "Jack" Kobiesa, Brian P. O'Gorman) — sqlStr-escape all.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 161-wa-generate.mts and emit the two WA migrations</name>
  <read_first>
    - backend/scripts/161-az-generate.mts (the just-authored AZ generator — clone it, swap FIPS/FIELD)
    - 161-PATTERNS.md (Pattern 1, Pattern 3)
    - 160-field-table-p161.csv (WA rows: incumbent_external_id, nominee_status, new_records_needed)
    - 160-negative-id-audit.csv (confirm WA absent from collision list)
  </read_first>
  <action>
    Clone 161-az-generate.mts to backend/scripts/161-wa-generate.mts. Set geo prefix 53, external_id base 53*10000. Build INC_EXT from -53001..-53010. Build FIELD from the WA field-table rows: one entry per incumbent (inc:true; set vacate:true for WA-4 Newhouse, nominee_status=retired, REUSE-NO-ROW) plus one entry per new_records_needed candidate (~60 total). Carry party only to pick the source label; NEVER write party to race_candidates. sqlStr-escape every free-text field (quoted nicknames, apostrophes). Emit migration 1 (elections + 10 races, PROVISIONAL cull >= 2026-08-05) and migration 2 (~60 politicians INSERT WHERE NOT EXISTS on external_id; race_candidates for REUSE + NEW with NOT EXISTS on race_id + lower(full_name)). Add the live collision re-check; print the self-check. Write backend/data/seed-wa-2026-house/161-04-wa-reconciliation.csv. Pick the next two free migration numbers via ls backend/migrations (after AZ's; a parallel session may hold higher).
  </action>
  <verify>
    <automated>cd backend && node --import tsx scripts/161-wa-generate.mts</automated>
  </verify>
  <acceptance_criteria>
    - Self-check prints 0 dup external_id, 0 dup full_name, per-district active counts matching the field table
    - Migration 2 has ~60 politicians INSERTs + race_candidates for every renominated incumbent + new candidate, none for Newhouse
    - Every INSERT uses WHERE NOT EXISTS; no party on race_candidates; no WA-specific top-two logic in the generator
  </acceptance_criteria>
  <done>Both WA migration files exist with idempotent guards; generator self-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run WA migrations on prod and verify surfacing</name>
  <read_first>
    - backend/migrations/1146_seed_mi_2026_house_elections_races.sql (run shape)
    - backend/src/lib/db.js (pool; DATABASE_URL)
  </read_first>
  <action>
    Run both WA migrations against prod with psql ON_ERROR_STOP, sourcing backend/.env in the same compound command. Run targeted SELECTs: 10 WA races under WA 2026 Statewide General with office_id NOT NULL; active race_candidates per district match the field table (incumbents present except vacated WA-4); 0 NULL politician_id on active rows; 0 duplicate full_name within WA; races.primary_party NULL on all 10. Re-run migrations once to confirm idempotency.
  </action>
  <verify>
    <automated>cd backend && grep -c INSERT migrations/1190_seed_wa_2026_house_candidates.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both migrations applied; re-run inserts 0 rows (idempotent)
    - psql shows 10 WA races with office_id NOT NULL; 0 NULL politician_id; 0 duplicate full_name within WA; primary_party NULL on all 10
  </acceptance_criteria>
  <done>WA elections + 10 races + ~60 candidate records live and idempotent on prod.</done>
</task>

<task type="auto">
  <name>Task 3: Headshots for the new WA candidates</name>
  <read_first>
    - backend/scripts/seed-mi-house-headshots.py (BANDS + wrong-person guard)
    - backend/data/seed-wa-2026-house/161-04-wa-reconciliation.csv (new WA external_id band)
  </read_first>
  <action>
    Clone seed-mi-house-headshots.py to backend/scripts/seed-wa-house-headshots.py. Add one BANDS entry for WA bracketing exactly the new WA external_id band (so incumbents -53001..-53010 are excluded), 'Washington', 'WA 2026 Statewide General'. Copy the wrong-person guard verbatim (trust first-name-mismatch rejection). Run with py. Any candidate with no free-license portrait is a documented honest-skip.
  </action>
  <verify>
    <automated>cd backend && ls scripts/seed-wa-house-headshots.py</automated>
  </verify>
  <acceptance_criteria>
    - seed-wa-house-headshots.py has one WA BANDS entry bracketing only the new band; incumbents excluded
    - Every new WA candidate with a free-license portrait has a politician_images row; the rest are documented honest-skips
    - No wrong-person image uploaded (guard intact)
  </acceptance_criteria>
  <done>All new WA candidates have a headshot or a documented honest-skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator free-text -> migration SQL | Names/sources become prod SQL literals |
| Wikipedia images -> politician_images | Third-party images bound to identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-04-01 | Tampering | SQL injection via unescaped names (quoted nicknames) | mitigate | sqlStr escaping on every free-text field |
| T-161-04-02 | Spoofing | Duplicate incumbent record | mitigate | Reuse -53001..-53010; politician INSERT guarded on external_id; verify 0 dup full_name |
| T-161-04-03 | Tampering | Party leakage onto candidate card | mitigate | No party on race_candidates; primary_party NULL |
| T-161-04-04 | Spoofing | Wrong-person headshot | mitigate | seed-mi guard copied verbatim |
| T-161-04-05 | Tampering | Migration-number collision (parallel 177/178 session, and same-wave with AZ) | mitigate | 161-04 depends_on 161-02 so it claims numbers after AZ (one migration-author per wave); re-check ls at author time; idempotent guards |
</threat_model>

<verification>
- 10 WA races under WA 2026 Statewide General, office_id NOT NULL, primary_party NULL
- ~60 new records + race_candidates for renominated incumbents + new candidates; none for Newhouse
- 0 NULL politician_id, 0 duplicate full_name within WA; migrations idempotent
- Headshots or documented honest-skips for all new WA candidates
</verification>

<success_criteria>
WA is fully seeded (elections + 10 races + ~60 records + race_candidates + headshots) on prod, idempotently, with no duplicate incumbents, no party leakage, and no top-two-specific logic — ready for stances in 161-05.
</success_criteria>

<output>
Create .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-04-SUMMARY.md recording the new WA external_id band, headshot honest-skips, migration numbers, and a breadcrumb that WA is top-two for Phase 167's cull.
</output>
