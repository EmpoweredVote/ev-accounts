---
phase: 161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca
plan: 11
type: execute
wave: 6
depends_on: ["161-01", "161-03", "161-05", "161-07", "161-08", "161-09", "161-10"]
files_modified:
  - backend/scripts/161-verify.sql
  - backend/scripts/161-coordinate-smoke.ts
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04, USHC3-05]
must_haves:
  truths:
    - "A read-only gate proves all 37 WA/AZ/TN/MA districts: correct race counts, 0 NULL office_id/politician_id, 0 duplicate incumbent, 0 unsourced stances, headshot coverage or pinned skip"
    - "A coordinate smoke proves a non-severe district in each state surfaces its challenger-inclusive field, AND a severe TN district surfaces ZERO races (the withholding works end-to-end)"
    - "Clark (MA-5) / Pressley (MA-7) each have exactly one race_candidates row"
  artifacts:
    - path: "backend/scripts/161-verify.sql"
      provides: "Consolidated read-only phase gate (per-state scoped) incl. TN severe non-surfacing assertion"
      contains: "Polygon Pending"
    - path: "backend/scripts/161-coordinate-smoke.ts"
      provides: "4-state positive samples + 1 severe-TN negative sample"
      contains: "severe"
  key_links:
    - from: "161-verify.sql severe assertion"
      to: "the 161-01 severe geo_id list + 161-06 withheld election"
      via: "assert severe races' parent election = Polygon Pending"
      pattern: "Polygon Pending"
---

<objective>
Author and run the phase mini-gate over all 37 WA/AZ/TN/MA districts (D-03 closing plan): a read-only 161-verify.sql (cloned from 156/158-verify.sql, per-state scoped) plus a 161-coordinate-smoke.ts (cloned from 158-coordinate-smoke.ts) extended to 4 states WITH a novel negative-result sample proving a severe TN district surfaces zero House races. Verifies USHC3-02/03/04/05 for the phase and pins documented honest-skips. This is the last plan before /gsd:verify-work.

Output: 161-verify.sql (green) + 161-coordinate-smoke.ts (green, incl. severe-TN negative sample).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-RESEARCH.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-PATTERNS.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-tn-correspondence-audit.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv
@backend/scripts/156-verify.sql
@backend/scripts/158-verify.sql
@backend/scripts/158-coordinate-smoke.ts
@backend/src/lib/electionService.ts

<interfaces>
Elections: AZ 2026 Statewide General, WA 2026 Statewide General, TN 2026 Statewide General, TN 2026 Congressional Redistricting - Polygon Pending (withheld), 2026 Massachusetts General Election (existing). Per-state scoping: assert by election id AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2) IN ('04','53','47','25') — never a cross-state count. Severe TN geo_ids come from the 161-01 audit "Severe geo_id list:" line (also recorded in 161-06-SUMMARY.md). Honest-skip pin tables (_stance_skip by pid, _img_skip by external_id) must be ORDER BY key (the Phase-143 ordering lesson). Coordinate query mirrors electionService.ts Part A (geofence ST_Covers point -> district -> office -> race). MA election-name string is '2026 Massachusetts General Election' (not 'MA 2026 Statewide General').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 161-verify.sql (per-state gate + TN severe non-surfacing assertion)</name>
  <read_first>
    - backend/scripts/156-verify.sql (3-state per-state-scoping discipline; _house temp table; _stance_skip/_img_skip pin tables; dup-name / NULLPID / no-image / unsourced assertions)
    - backend/scripts/158-verify.sql (multi-state gate structure)
    - 161-PATTERNS.md (161-verify.sql section incl. the NEW TN severe-race non-surfacing assertion block)
    - 161-tn-correspondence-audit.md and 161-06-SUMMARY.md (the severe geo_id set to pin)
    - the 161-03/05/07/09/10 SUMMARYs (whole-record honest-skip pids + trails to pin in _stance_skip; headshot honest-skip external_ids for _img_skip)
  </read_first>
  <action>
    Clone 156-verify.sql to backend/scripts/161-verify.sql. Resolve the four new election ids (AZ/WA/TN-general/TN-withheld) plus the MA existing election; build a _house temp table scoped to geo prefixes 04/53/47/25 and NATIONAL_LOWER. Assert, per state: expected race counts (AZ 9, WA 10, TN 9, MA 9); 0 NULL office_id on any race; 0 NULL politician_id on active race_candidates; 0 duplicate full_name per state; races.primary_party NULL; headshot coverage for every new candidate except pinned _img_skip entries; 0 unsourced stance rows and every in-scope new candidate having >=1 sourced stance except pinned _stance_skip entries. Add the MA exactly-one-row check for Clark and Pressley pids. Add the NEW assertion block: every severe TN race (geo_id in the audit severe set) has parent election name = 'TN 2026 Congressional Redistricting - Polygon Pending', and RAISE EXCEPTION if any severe race is wired to a surfacing election. Populate _stance_skip (by pid) and _img_skip (by external_id) from the stance/seed SUMMARYs, ORDER BY key. Run the gate green.
  </action>
  <verify>
    <automated>cd backend && grep -c "Polygon Pending" scripts/161-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - 161-verify.sql runs to completion with no RAISE EXCEPTION (all assertions pass)
    - Assertions are per-state scoped (geo prefixes 04/53/47/25); no cross-state count exists
    - The TN severe block asserts every severe geo_id's race parent = Polygon Pending; _stance_skip and _img_skip are ORDER BY key and match the SUMMARY-documented skips
  </acceptance_criteria>
  <done>161-verify.sql is green across all 37 districts incl. the TN severe non-surfacing invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Author 161-coordinate-smoke.ts (4 positive samples + 1 severe-TN negative sample) and run green</name>
  <read_first>
    - backend/scripts/158-coordinate-smoke.ts (full: STATE_CONFIG, SAMPLES, the geofence ST_Covers coordinate-surfacing query mirroring electionService.ts Part A, challenger-inclusive assertion)
    - backend/src/lib/electionService.ts (ELECTION_VISIBILITY_WINDOW — why the withheld TN election returns nothing)
    - 161-tn-correspondence-audit.md (a severe TN geo_id to use for the negative sample)
    - 161-PATTERNS.md (161-coordinate-smoke.ts section — the negative-result sample requirement)
  </read_first>
  <action>
    Clone 158-coordinate-smoke.ts to backend/scripts/161-coordinate-smoke.ts. Set STATE_CONFIG for AZ (04), WA (53), TN (47), MA (25 with election '2026 Massachusetts General Election'). Add one positive SAMPLE per state using a NON-severe, contested in-district geo_id (>= minActive active candidates AND >= 1 challenger, the Pitfall-5 two-path guard). Add a NEW negative-sample block: for a point inside a SEVERE TN district (from the audit), assert the surfacing query returns ZERO House races (proving the election_id withholding works end-to-end). Run green.
  </action>
  <verify>
    <automated>cd backend && node --import tsx scripts/161-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of AZ/WA/TN/MA positive samples returns its House race with >= 1 challenger (not just the incumbent)
    - The severe-TN negative sample returns ZERO House races; the script exits success (all assertions pass)
    - The TN positive sample uses a non-severe district; the negative sample uses a severe one
  </acceptance_criteria>
  <done>161-coordinate-smoke.ts is green: 4 states surface their field, and a severe TN district correctly surfaces nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script -> prod (read-only) | SELECT-only assertions; no writes |
| severe list -> gate assertion | The gate must pin the same severe set the seed used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-11-01 | Tampering | Cross-state contamination in a count | mitigate | Every assertion scoped by election id + geo prefix (156-verify discipline) |
| T-161-11-02 | Repudiation | A severe TN race silently leaking to /elections in future | mitigate | Gate asserts severe races -> Polygon Pending; coordinate smoke asserts ZERO races; note in SUMMARY that 166 and the future dual-map phase must re-run this assertion |
| T-161-11-03 | Tampering | Honest-skip pins ordered wrong -> false gate fail/pass | mitigate | _stance_skip / _img_skip ORDER BY key (Phase-143 lesson); pins sourced from the stance/seed SUMMARYs |
| T-161-11-04 | Repudiation | Unsourced stance slips past | mitigate | 0-unsourced assertion across all four states' new candidates |
</threat_model>

<verification>
- 161-verify.sql green: correct race counts (9/10/9/9), 0 NULL office_id/politician_id, 0 dup full_name, 0 unsourced, headshot coverage/pins, Clark/Pressley single-rowed, TN severe -> Polygon Pending
- 161-coordinate-smoke.ts green: 4 challenger-inclusive positive samples + 1 severe-TN zero-race negative sample
</verification>

<success_criteria>
The phase mini-gate proves all 37 WA/AZ/TN/MA districts satisfy USHC3-02/03/04/05 read-only, including the TN severe-district withholding end-to-end — ready for /gsd:verify-work and inheritable by the Phase 166 consolidated gate.
</success_criteria>

<output>
Create .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-11-SUMMARY.md recording gate results, the severe geo_id set asserted, the pinned honest-skips, and a breadcrumb that Phase 166 + the future dual-map phase must re-run the TN severe non-surfacing assertion.
</output>
