---
phase: 161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca
plan: 06
type: execute
wave: 3
depends_on: ["161-01", "161-04"]
files_modified:
  - backend/scripts/161-tn-generate.mts
  - backend/migrations/1191_seed_tn_2026_house_elections_races.sql
  - backend/migrations/1192_seed_tn_2026_house_candidates.sql
  - backend/scripts/seed-tn-house-headshots.py
  - backend/data/seed-tn-2026-house/161-06-tn-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "Non-severe TN districts surface their full qualified field on /elections; severe TN districts (per the 161-01 audit) are seeded but surface ZERO races"
    - "Every TN race has office_id NOT NULL (severe races differ ONLY in election_id, pointing at the withheld election); essentials.offices is never touched"
    - "Renominated TN incumbents reuse existing politician_id; retired Rose (TN-6) and redistricted Cohen (TN-9) have no active row; every new TN candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/1191_seed_tn_2026_house_elections_races.sql"
      provides: "TN general election + withheld 'Polygon Pending' election + 9 races wired by severity"
      contains: "Congressional Redistricting"
    - path: "backend/migrations/1192_seed_tn_2026_house_candidates.sql"
      provides: "73 new TN politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "severe TN races"
      to: "TN 2026 Congressional Redistricting - Polygon Pending election"
      via: "election_id substitution (visibility-window false)"
      pattern: "Polygon Pending"
---

<objective>
Seed TN's full 2026 US House field (9 districts, 73 new records — the heaviest slice) wired to the EXISTING old-CD district rows (D-01), with severe districts (from the 161-01 audit) seeded-but-withheld from /elections via a dedicated non-general election row (D-01b; 161-RESEARCH Pitfall 1) — NEVER office_id NULL, NEVER touching essentials.offices (Pitfall 2 keeps the reps feed on the true old-map incumbent). Author the TN general election + the withheld election + 9 races (severity-routed), create 73 new records + race_candidates (reuse incumbent pids; retired Rose TN-6 and redistricted Cohen TN-9 get no active row), then headshots. Continues USHC3-02/03/04.

Output: 161-tn-generate.mts, two TN migrations (run on prod), seed-tn-house-headshots.py, TN reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-RESEARCH.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-PATTERNS.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-tn-correspondence-audit.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@backend/scripts/161-az-generate.mts
@backend/src/lib/electionService.ts
@backend/scripts/seed-mi-house-headshots.py

<interfaces>
TN FIPS=47; geo_ids 4701-4709. Incumbents keep existing external_id (-47001..-47009; TN-6 Rose -47006 RETIRED, TN-9 Cohen -47009 REDISTRICTED). New challengers: external_id = -(47*10000 + cd*100 + seq) (TN collision-free; re-verify live). Two elections: "TN 2026 Statewide General" (election_type general, 2026-11-03) and "TN 2026 Congressional Redistricting - Polygon Pending" (election_type special, election_date 2026-05-07 — deliberately >30 days past so ELECTION_VISIBILITY_WINDOW evaluates false). Non-severe races wire election_id to the general; severe races (from 161-01 audit "Severe geo_id list:") wire election_id to the withheld election. ALL races: office_id = the district's existing old-CD NATIONAL_LOWER office (NEVER null). race_candidates shape identical to AZ. General-race description "PROVISIONAL: pre-primary qualified field, cull >= 2026-08-07" (Aug-6 primary). Names contain commas/quotes (David S. Kerr, Jr.; Wendell "Wells" Blankenship) — sqlStr-escape all.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the severe geo_id list and author 161-tn-generate.mts</name>
  <read_first>
    - .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-tn-correspondence-audit.md (the "Severe geo_id list:" line — the authoritative severe set)
    - backend/scripts/161-az-generate.mts (generator to clone)
    - backend/src/lib/electionService.ts (ELECTION_VISIBILITY_WINDOW lines 24-26 — why a past-dated non-general election is invisible)
    - 161-PATTERNS.md (TN severe-district withholding section; Pattern 1; Pattern 3)
    - 160-field-table-p161.csv (TN rows: incumbent_external_id, nominee_status, new_records_needed)
  </read_first>
  <action>
    Read the severe geo_id set from 161-tn-correspondence-audit.md. Clone 161-az-generate.mts to backend/scripts/161-tn-generate.mts. Set geo prefix 47, external_id base 47*10000, INC_EXT -47001..-47009. Build FIELD from the TN field-table rows: one entry per incumbent (inc:true; set vacate:true for TN-6 Rose [retired] and TN-9 Cohen [redistricted] -> REUSE-NO-ROW) plus one per new_records_needed candidate (73 total). Party carried only for the source label; never on race_candidates. sqlStr-escape all free text (commas, quoted nicknames, suffixes). Emit migration 1 that: (a) INSERTs the "TN 2026 Statewide General" election (general) WHERE NOT EXISTS; (b) INSERTs the "TN 2026 Congressional Redistricting - Polygon Pending" election (special, 2026-05-07) WHERE NOT EXISTS; (c) for each NON-severe geo_id, INSERTs a race wired to the general election (description PROVISIONAL cull >= 2026-08-07); (d) for each SEVERE geo_id, INSERTs an otherwise-identical race but with election_id = the withheld election (office_id still the normal old-CD office — NEVER null). Emit migration 2 (73 politicians + race_candidates, same guards as AZ). The generator must NOT emit any UPDATE/INSERT against essentials.offices. Add the live collision re-check + self-check (including a printed severe-vs-general race routing summary). Write backend/data/seed-tn-2026-house/161-06-tn-reconciliation.csv. Pick the next two free migration numbers via ls backend/migrations.
  </action>
  <verify>
    <automated>cd backend && node --import tsx scripts/161-tn-generate.mts</automated>
  </verify>
  <acceptance_criteria>
    - Generator reads the severe geo_id list from the audit file and prints a routing summary (which geo_ids -> general vs withheld election)
    - Migration 1 creates both elections and routes every severe geo_id's race to the "Polygon Pending" election, every non-severe race to the general; every race has office_id (none null)
    - Migration 2 has 73 politicians INSERTs + race_candidates for renominated incumbents + new candidates, none for Rose/Cohen; no INSERT/UPDATE against essentials.offices anywhere
    - Self-check: 0 dup external_id, 0 dup full_name, per-district active counts match the field table
  </acceptance_criteria>
  <done>Both TN migration files exist, severity-routed, with idempotent guards and offices untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run TN migrations on prod and verify withholding</name>
  <read_first>
    - backend/src/lib/electionService.ts (the three query paths; the visibility window)
    - backend/src/lib/db.js (pool; DATABASE_URL)
  </read_first>
  <action>
    Run both TN migrations against prod with psql ON_ERROR_STOP, sourcing backend/.env. Run targeted SELECTs: 9 TN races exist, all office_id NOT NULL; severe geo_ids' races point at the "Polygon Pending" election, non-severe at the general; 0 NULL politician_id on active rows; 0 duplicate full_name within TN; races.primary_party NULL; and a confirmation that every TN essentials.offices row's politician_id is unchanged (mirror the 159-03 check — reps feed untouched). Re-run migrations once for idempotency.
  </action>
  <verify>
    <automated>cd backend && grep -c INSERT migrations/1192_seed_tn_2026_house_candidates.sql</automated>
  </verify>
  <acceptance_criteria>
    - psql: 9 TN races, all office_id NOT NULL; severe races' parent election name = TN 2026 Congressional Redistricting - Polygon Pending; non-severe races' parent = TN 2026 Statewide General
    - 0 NULL politician_id on active rows; 0 duplicate full_name within TN; primary_party NULL
    - Every TN essentials.offices.politician_id is unchanged from before the migration (reps feed untouched); re-run inserts 0 rows
  </acceptance_criteria>
  <done>TN seeded on prod; severe races routed to the withheld election; offices/reps feed untouched; idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Headshots for the 73 new TN candidates</name>
  <read_first>
    - backend/scripts/seed-mi-house-headshots.py (BANDS + wrong-person guard)
    - backend/data/seed-tn-2026-house/161-06-tn-reconciliation.csv (new TN external_id band)
  </read_first>
  <action>
    Clone seed-mi-house-headshots.py to backend/scripts/seed-tn-house-headshots.py. Add one BANDS entry for TN bracketing exactly the new TN external_id band (excluding incumbents -47001..-47009), 'Tennessee', 'TN 2026 Statewide General'. Note: candidates in severe (withheld) districts STILL get headshots — seeding is complete regardless of surfacing; scope by external_id band, not by election. Copy the wrong-person guard verbatim. Run with py. No-portrait candidates are documented honest-skips.
  </action>
  <verify>
    <automated>cd backend && ls scripts/seed-tn-house-headshots.py</automated>
  </verify>
  <acceptance_criteria>
    - seed-tn-house-headshots.py has one TN BANDS entry over only the new band; incumbents excluded; severe-district candidates included
    - Every new TN candidate with a free-license portrait has a politician_images row; the rest are documented honest-skips
    - No wrong-person image uploaded
  </acceptance_criteria>
  <done>All 73 new TN candidates (incl. severe-district ones) have a headshot or documented honest-skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit severe list -> withholding | The severe set decides which races are hidden from voters |
| generator free-text -> migration SQL | Names/sources become prod SQL literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-06-01 | Tampering | A severe race accidentally wired to the general election (leaks wrong data to voters) | mitigate | election_id routed from the audit severe list; 161-06 psql check + 161-11 gate assert severe races -> withheld election; 161-11 coordinate smoke asserts ZERO races for a severe-district point |
| T-161-06-02 | Tampering | Reps feed broken by touching essentials.offices | mitigate | Generator emits nothing against offices; post-migration check confirms every TN offices.politician_id unchanged (159-03 lesson) |
| T-161-06-03 | Spoofing | Duplicate incumbent record | mitigate | Reuse -47001..-47009; INSERT guarded on external_id; 0 dup full_name |
| T-161-06-04 | Tampering | office_id NULL invariant violated to hide severe race | mitigate | Withholding uses election_id substitution, not office_id NULL; office_id always the old-CD office |
| T-161-06-05 | Tampering | SQL injection via unescaped names (commas/quotes/suffixes) | mitigate | sqlStr on every free-text field |
| T-161-06-06 | Tampering | Migration-number collision (parallel session; same-wave) | mitigate | depends_on 161-04 (one migration-author per wave); re-check ls; idempotent guards |
</threat_model>

<verification>
- 9 TN races, all office_id NOT NULL; severe races -> withheld election, non-severe -> general
- 73 new records + race_candidates for renominated incumbents + new candidates; none for Rose/Cohen
- essentials.offices untouched (all TN politician_id unchanged); 0 NULL politician_id; 0 dup full_name; primary_party NULL; idempotent
- Headshots or documented honest-skips for all 73 new TN candidates
</verification>

<success_criteria>
TN is fully seeded (both elections + 9 severity-routed races + 73 records + race_candidates + headshots) on prod, idempotently; severe districts are seeded-but-withheld via election_id (office_id never null, offices untouched); ready for stances in 161-07/161-09.
</success_criteria>

<output>
Create .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-06-SUMMARY.md recording the severe geo_id set used, the withheld election name/id, the new TN external_id band, headshot honest-skips, and migration numbers.
</output>
