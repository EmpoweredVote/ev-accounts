---
phase: 161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/161-az-generate.mts
  - backend/migrations/1187_seed_az_2026_house_elections_races.sql
  - backend/migrations/1188_seed_az_2026_house_candidates.sql
  - backend/scripts/seed-az-house-headshots.py
  - backend/data/seed-az-2026-house/161-02-az-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "Each AZ district (geo_id 0401-0409) surfaces its full qualified candidate field on /elections for an in-district coordinate"
    - "Renominated AZ incumbents reuse existing politician_id (0 duplicate full_name); retired Schweikert (AZ-1) and Biggs (AZ-5) have no active race_candidates row"
    - "Every new AZ candidate has a headshot or a documented honest-skip; no candidate card surfaces party"
  artifacts:
    - path: "backend/migrations/1188_seed_az_2026_house_candidates.sql"
      provides: "32 new AZ politicians + active race_candidates rows (idempotent NOT EXISTS)"
      contains: "essentials.race_candidates"
    - path: "backend/scripts/161-az-generate.mts"
      provides: "Deterministic AZ field to migration-SQL generator"
      contains: "sqlStr"
  key_links:
    - from: "essentials.race_candidates"
      to: "essentials.races -> offices -> districts geo_id 04xx"
      via: "race_id FK, office_id never null"
      pattern: "race_candidates"
---

<objective>
Seed AZ's full 2026 US House field END-TO-END and FIRST (D-02, hard target: AZ records + race_candidates + headshots live before the Jul-21 primary). Author one AZ elections row + 9 races (office_id to existing NATIONAL_LOWER offices), create 32 new candidate records + wire race_candidates (reuse incumbent politician_ids; retired Schweikert/Biggs get no active row), then run headshots. Stances follow in 161-03. Anchor for USHC3-02/03/04.

Output: 161-az-generate.mts, two AZ migrations (run on prod), seed-az-house-headshots.py, AZ reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-RESEARCH.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@backend/scripts/159-mi-generate.mts
@backend/migrations/1146_seed_mi_2026_house_elections_races.sql
@backend/migrations/1147_seed_mi_2026_house_candidates.sql
@backend/scripts/seed-mi-house-headshots.py

<interfaces>
AZ FIPS=04; geo_ids 0401-0409. Incumbents keep existing external_id (incumbent_external_id: -4001 Schweikert RETIRED, -4002 Crane, -4003 Ansari, -4004 Stanton, -4005 Biggs RETIRED, -4006 Ciscomani, -4007 Grijalva, -4008 Hamadeh, -4009 Gosar). New challengers: external_id = -(4*10000 + cd*100 + seq), seq from 1 per CD (AZ collision-free per 160-negative-id-audit; re-verify live). Election "AZ 2026 Statewide General", date 2026-11-03, type general. Races: position_name "U.S. Representative District N", primary_party NULL, seats 1, description "PROVISIONAL: pre-primary qualified field, cull >= 2026-07-22". race_candidates: race_id, politician_id NON-NULL, full_name, first/last, is_incumbent, candidate_status='active', source. NEVER office_id NULL; NEVER party on race_candidates. The 32 new candidates per district are the new_records_needed column of the AZ field-table rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 161-az-generate.mts and emit the two AZ migrations</name>
  <read_first>
    - backend/scripts/159-mi-generate.mts (full generator pattern: Cand type, INC_EXT, FIELD, seqByCd, sqlStr, migration templates, self-check)
    - 161-PATTERNS.md (Pattern 1, Pattern 3, generator section excerpts)
    - 160-field-table-p161.csv (AZ rows: incumbent_pid/external_id, nominee_status, new_records_needed)
    - 160-negative-id-audit.csv (confirm AZ absent from collision list)
  </read_first>
  <action>
    Clone 159-mi-generate.mts to backend/scripts/161-az-generate.mts. Set geo prefix 04 and external_id base 4*10000. Build INC_EXT from field-table incumbent_external_id (-4001..-4009). Build FIELD from AZ field-table rows: one entry per incumbent (inc:true; set vacate:true for AZ-1 Schweikert and AZ-5 Biggs whose nominee_status=retired so they emit REUSE-NO-ROW with NO active race_candidates row) plus one entry per new_records_needed candidate (32 total). Carry the party letter only to pick the source label; party is NEVER written to race_candidates. Use sqlStr on every free-text field (accented names like Galan-Woods). Emit migration 1 (elections + 9 races, description PROVISIONAL cull >= 2026-07-22) and migration 2 (32 politicians INSERT WHERE NOT EXISTS on external_id; race_candidates for all REUSE + NEW rows with NOT EXISTS on race_id + lower(full_name)). Add the live collision re-check per 161-RESEARCH Code Examples; print the self-check (dup external_id, dup full_name, per-district active counts). Write the AZ decision table to backend/data/seed-az-2026-house/161-02-az-reconciliation.csv. Pick the next two free migration numbers via ls backend/migrations (research max 1186; a parallel session may hold higher — use free numbers, rename files).
  </action>
  <verify>
    <automated>cd backend && node --import tsx scripts/161-az-generate.mts</automated>
  </verify>
  <acceptance_criteria>
    - Generator self-check prints 0 dup external_id, 0 dup full_name, and per-district active counts matching the field table
    - Migration 2 contains exactly 32 politicians INSERTs plus race_candidates INSERTs for every renominated incumbent + every new candidate, and none for Schweikert/Biggs
    - Every INSERT uses WHERE NOT EXISTS; no party value appears on any race_candidates INSERT (grep the migration finds no primary_party in race_candidates inserts)
  </acceptance_criteria>
  <done>Both AZ migration files exist with idempotent guards; generator self-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run the AZ migrations on prod and verify surfacing with targeted SELECTs</name>
  <read_first>
    - backend/migrations/1146_seed_mi_2026_house_elections_races.sql and 1147_seed_mi_2026_house_candidates.sql (run shape + idempotency)
    - backend/src/lib/db.js (pool; DATABASE_URL Session pooler)
  </read_first>
  <action>
    Run both AZ migrations against prod (ref kxsdzaojfaibhuzmclfq) using psql with ON_ERROR_STOP, sourcing backend/.env in the same compound command (cd backend, then source .env, then psql against DATABASE_URL). Then run targeted verification SELECTs (single-quoted SQL) confirming: 9 AZ races exist under AZ 2026 Statewide General with office_id NOT NULL; active race_candidates per district match the field table (incumbents present except vacated AZ-1/AZ-5); 0 NULL politician_id on active rows; 0 duplicate full_name within AZ; races.primary_party NULL on all 9. Re-run both migrations once to confirm idempotency (0 new rows).
  </action>
  <verify>
    <automated>cd backend && grep -c INSERT migrations/1188_seed_az_2026_house_candidates.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both migrations applied without error; a re-run inserts 0 additional rows (idempotent)
    - psql shows exactly 9 AZ races with office_id NOT NULL; 0 NULL politician_id on active race_candidates; 0 duplicate full_name within AZ
    - psql shows races.primary_party IS NULL for all 9 AZ races
  </acceptance_criteria>
  <done>AZ elections + 9 races + 32 candidate records are live and idempotent on prod.</done>
</task>

<task type="auto">
  <name>Task 3: Headshots for the 32 new AZ candidates</name>
  <read_first>
    - backend/scripts/seed-mi-house-headshots.py (full: BANDS dict, POLITICAL_KW / _NON_PERSON_TITLE / _BAD_DISAMBIG / _HISTORICAL_YEAR wrong-person guard, pageimages -> license -> crop 600x750 -> Storage -> politician_images pipeline)
    - 161-PATTERNS.md (seed-{state}-house-headshots.py section — BANDS is the only per-state parameter)
    - backend/data/seed-az-2026-house/161-02-az-reconciliation.csv (the new AZ external_id band from Task 1)
  </read_first>
  <action>
    Clone seed-mi-house-headshots.py to backend/scripts/seed-az-house-headshots.py. Add a single BANDS entry for AZ: (min_ext, max_ext, 'Arizona', 'AZ 2026 Statewide General') where min/max bracket exactly the new AZ external_id band (-40101 down to the lowest assigned, e.g. -40110 for AZ-1's 10 new candidates) so incumbents (-4001..-4009) are excluded. Copy the wrong-person guard constants verbatim — do NOT weaken the first-name-mismatch rejection (Bouchard lesson). Run with py (not python3). Any candidate with no free-license portrait becomes a documented honest-skip recorded in the reconciliation CSV / SUMMARY, not a wrong-person fill.
  </action>
  <verify>
    <automated>cd backend && ls scripts/seed-az-house-headshots.py</automated>
  </verify>
  <acceptance_criteria>
    - seed-az-house-headshots.py has exactly one AZ BANDS entry bracketing only the new AZ external_id band; incumbent ids are excluded
    - After running, essentials.politician_images has a row for every new AZ candidate that had a free-license portrait; candidates with none are listed as honest-skips with a reason
    - No wrong-person image is uploaded (guard intact; any first-name mismatch is rejected)
  </acceptance_criteria>
  <done>All new AZ candidates have a headshot or a documented honest-skip; guard intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator free-text -> migration SQL | Candidate names/sources become SQL literals executed on prod |
| Wikipedia images -> politician_images | Third-party images bound to a candidate identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-02-01 | Tampering | SQL injection via unescaped names in generated SQL | mitigate | sqlStr single-quote escaping on every free-text field (V5) |
| T-161-02-02 | Spoofing | Duplicate incumbent politician record | mitigate | Reuse incumbent external_id (-4001..-4009); politician INSERT guarded WHERE NOT EXISTS on external_id; verify 0 dup full_name |
| T-161-02-03 | Tampering | Party leakage onto candidate card | mitigate | Antipartisan invariant: no party on race_candidates; races.primary_party NULL; grep migration confirms |
| T-161-02-04 | Spoofing | Wrong-person headshot | mitigate | seed-mi headshot guard copied verbatim; first-name-mismatch rejection trusted |
| T-161-02-05 | Tampering | Migration-number collision with parallel 177/178 session | mitigate | Re-check ls backend/migrations at author time; idempotent NOT EXISTS guards make re-apply safe |
</threat_model>

<verification>
- 9 AZ races under AZ 2026 Statewide General, all office_id NOT NULL, primary_party NULL
- 32 new politicians + active race_candidates for all renominated incumbents and new candidates; none for vacated Schweikert/Biggs
- 0 NULL politician_id, 0 duplicate full_name within AZ; migrations idempotent
- Headshots present for new candidates or documented honest-skips
</verification>

<success_criteria>
AZ is fully seeded (elections + 9 races + 32 records + race_candidates + headshots) on prod, idempotently, with no duplicate incumbents and no party leakage — ready for stances in 161-03 and live before the Jul-21 primary.
</success_criteria>

<output>
Create .planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-02-SUMMARY.md recording the new AZ external_id band, headshot honest-skips, and the migration numbers used.
</output>
