---
phase: 104-local-remediation-city-officials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv
  - backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv
  - supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql
  - backend/scripts/_apply-migration-283.ts
  - .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md
  - .planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md
  - .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md
  - .planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md
autonomous: false
requirements:
  - STAX-03
  - QUAL-01
  - QUAL-02
tags:
  - inform
  - sources
  - city-officials
  - research-stances
  - migration
  - deletion-log
  - master-log

must_haves:
  truths:
    - "Bilal Mahmood (politician_id d3c5004c-9ca0-444e-96d9-107d4315abcb) abortion stance (former value=2.0) has been re-researched with the research-stances skill in a single pass and either UPGRADED with a real specific (non-homepage) source URL appended to sources via ARRAY_CAT (per D-04), or DELETED from inform.politician_answers AND inform.politician_context"
    - "Vivian Moreno (politician_id 0b16443e-fec4-4f33-abbc-eb1331e3b42d) city-sanitation stance (former value=3.0) has been re-researched with the research-stances skill in a single pass and either UPGRADED with a real specific (non-homepage) source URL appended to sources via ARRAY_CAT (per D-04), or DELETED from inform.politician_answers AND inform.politician_context"
    - "research-stances was dispatched ONE agent at a time — Mahmood first, then Moreno (MEMORY.md rate-limit rule + D-04)"
    - "Live stance scale for the abortion topic AND city-sanitation topic was fetched fresh from inform.compass_topics + inform.compass_stances via SKILL.md Step 0 query — never hardcoded — and embedded verbatim in each agent prompt"
    - "For UPGRADE outcomes, the migration uses `sources = politician_context.sources || EXCLUDED.sources` in the ON CONFLICT clause (ARRAY_CAT pattern per D-04) — never plain `sources = EXCLUDED.sources` — preserving the existing homepage URL alongside the new specific URL"
    - "For DELETE outcomes, the migration removes the inform.politician_context row FIRST then inform.politician_answers row (defensive ordering per Phase 103 pattern)"
    - "Migration filename uses the next available migration version verified via SELECT MAX(version) FROM supabase_migrations.schema_migrations BEFORE the file is written — expected next: 283 (Phase 103 applied 282); abort if a higher number is returned and use that"
    - "Migration applied to live DB via the backend/scripts/_apply-migration-283.ts script (tsx + pg.Pool reading DATABASE_URL) — never `supabase db push`"
    - "Post-migration V1 query (v2.5 city-official cohort unsourced stance count using external_id BETWEEN -689999 AND -630000 AND (external_id < -669999 OR external_id > -660000)) returns 0"
    - "Post-migration V2 query (v2.5 city-official cohort weak-source-only stance count — homepage-only URLs) returns 0"
    - "104-DELETION-LOG.md exists at .planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md listing every Phase-104 deleted stance (0–2 entries) with columns: politician full_name | topic_key | former value | reason"
    - "MASTER-DELETION-LOG.md exists at .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md merging all entries from 101-DELETION-LOG.md (1 entry: Deb Fischer / ai-regulation / 3) + 102-DELETION-LOG.md (12 entries: Dooley, Shoffner, Alme) + 103-DELETION-LOG.md (6 entries: Gómez Reyes, Stern, Bonta) + 104 entries (0–2) into one unified table"
    - "MASTER-DELETION-LOG.md has columns: phase | politician full_name | topic_key | former value | reason — preceded by a milestone summary row showing total deletions across v2.7 (19 + Phase 104 entries)"
    - "Every value retained or updated was verified against the specific Chair text for that topic — QUAL-01 applied (no party inference; no directional keep per D-04)"
    - "Out-of-scope city officials are NOT touched: Monica Rodriguez (external_id 695265), Chris Krupa Downs, Burt Thakur, Ryan Tubbs, Shun Thomas (null external_ids) — confirmed by D-02"
  artifacts:
    - path: "backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv"
      provides: "research-stances CSV output for Bilal Mahmood / abortion (Chair methodology, single research pass)"
      contains: "Bilal Mahmood"
    - path: "backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv"
      provides: "research-stances CSV output for Vivian Moreno / city-sanitation (Chair methodology, single research pass)"
      contains: "Vivian Moreno"
    - path: "supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql"
      provides: "SQL migration that UPSERTs (ARRAY_CAT) or DELETEs the two weak-source stances per research-stances outcome"
      contains: "inform.politician_context"
    - path: "backend/scripts/_apply-migration-283.ts"
      provides: "Apply script that runs the 283 migration via pg.Pool + DATABASE_URL with post-migration smoke checks"
      contains: "pool.query"
    - path: ".planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md"
      provides: "Research batch log — pre-flight DB queries, per-topic stance scale JSON embedded, agent dispatch order (Mahmood → Moreno), per-topic outcome (UPGRADE with URL / DELETE with reason), migration apply log"
      contains: "Live Stance Scales"
    - path: ".planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md"
      provides: "QUAL-02 phase-level deletion log for Phase 104 (0–2 entries depending on research outcomes)"
      contains: "former value"
    - path: ".planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md"
      provides: "QUAL-02 final milestone deletion log — unified table across all four remediation phases (101–104) covering the entire v2.7 milestone"
      contains: "phase | politician full_name | topic_key | former value | reason"
    - path: ".planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md"
      provides: "Post-migration verification record — V1 and V2 SQL outputs proving STAX-03"
      contains: "STAX-03"
  key_links:
    - from: "backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv"
      to: "supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql"
      via: "If CSV has a row with real URL → row becomes ARRAY_CAT UPSERT statement; if topic skipped → becomes DELETE statement"
      pattern: "politician_context\\.sources \\|\\| EXCLUDED\\.sources"
    - from: "supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "UPSERT with ARRAY_CAT for upgrade outcomes; DELETE pair for delete outcomes"
      pattern: "DELETE FROM inform\\.politician_(answers|context)"
    - from: ".planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md"
      to: ".planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md"
      via: "Phase 104 entries from 104-DELETION-LOG.md are merged with entries from 101/102/103-DELETION-LOG.md into one unified table — every deletion across v2.7 appears exactly once with a phase column added"
      pattern: "\\| 104 \\|"
    - from: ".planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md"
      to: "QUAL-02 final"
      via: "Single canonical file consolidates all v2.7 deletions; milestone summary row states total = 19 + Phase 104 entries"
      pattern: "Total deletions"
    - from: ".planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md"
      to: "STAX-03 success criteria"
      via: "V1 query (city-official cohort unsourced count) and V2 query (city-official cohort weak-source count) both return 0 post-migration"
      pattern: "city_(unsourced|weak)_count"
---

<objective>
Remediate the two weak-source-only city official stances identified in the Phase 104 discuss-phase triage (D-01): Bilal Mahmood / abortion (former value 2.0, weak source https://bilalmahmood.com/) and Vivian Moreno / city-sanitation (former value 3.0, weak source https://www.vivianmorenosd.com). Dispatch the research-stances skill ONE agent at a time (Mahmood first, then Moreno) per D-04 + MEMORY.md rate-limit rule. For each stance: if the agent finds a real specific (non-homepage) primary source URL, append it via the ARRAY_CAT pattern to the existing sources array; if not, DELETE the stance row + its paired context row. Write a single migration (number 283, verified at write time), apply via the apply-migration script, write 104-DELETION-LOG.md for the 0–2 phase deletions, then compile MASTER-DELETION-LOG.md merging all four phases' deletion logs (101: 1 entry, 102: 12 entries, 103: 6 entries, 104: 0–2 entries) into the single QUAL-02 canonical artifact for v2.7. Run the STAX-03 V1 query (city cohort unsourced count = 0) AND V2 query (city cohort weak-source count = 0).

Purpose: Implements STAX-03 (per D-02 — every city official stance in the v2.5 external_id cohort sourced or deleted) and finalizes QUAL-02 for the entire v2.7 Source Integrity milestone by producing the unified deletion log (per D-05). QUAL-01 is applied via the Chair-methodology pass executed by research-stances. Plan structure is one combined plan per D-03.

Implements: STAX-03 (city official remediation), QUAL-01 (Chair-methodology value verification — homepage-only URLs are not primary sources per D-04), QUAL-02 (phase deletion log + final milestone-wide deletion log per D-05).

Output:
- backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv — research-stances CSV for Mahmood / abortion
- backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv — research-stances CSV for Moreno / city-sanitation
- supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql — migration with ARRAY_CAT UPSERTs and/or DELETE pairs
- backend/scripts/_apply-migration-283.ts — apply script with post-apply smoke checks
- .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md — pre-flight + research outcomes + apply log
- .planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md — phase-level deletion log (0–2 entries)
- .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md — v2.7 unified deletion log (19+ entries)
- .planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md — STAX-03 V1+V2 verification record
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/104-local-remediation-city-officials/104-CONTEXT.md
@.planning/phases/101-candidate-profiles/101-DELETION-LOG.md
@.planning/phases/102-federal-house-remediation/102-DELETION-LOG.md
@.planning/phases/103-state-remediation-ca-md/103-DELETION-LOG.md
@.claude/skills/research-stances/SKILL.md
@backend/scripts/run-source-coverage-audit.ts
@supabase/migrations/20260606000005_282_ca_state_source_remediation.sql
@backend/scripts/_apply-migration-281.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight + sequential research-stances (Mahmood → Moreno)</name>
  <files>backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv, backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv, .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md</files>
  <read_first>
    - .planning/phases/104-local-remediation-city-officials/104-CONTEXT.md (D-01 triage table — politician_ids, topic_keys, former values, weak source URLs; D-02 scope boundary; D-04 deletion rule + ARRAY_CAT + one-agent-at-a-time)
    - .claude/skills/research-stances/SKILL.md (Step 0 — Topic Resolution query; Step 1 — agent prompt template; FIVE-CHAIRS framing; TOPIC SCALE REFERENCE format; TOOL RULE; single-agent dispatch rule)
    - MEMORY.md (rate-limit rule: ONE agent at a time, never parallel)
  </read_first>
  <action>
    PART A — PRE-FLIGHT (run before any agent dispatch). Execute a single Node/tsx script against DATABASE_URL that runs four read-only queries and writes their JSON outputs into a new file `.planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md` under a "Pre-Flight" section:

    1. Migration number: `SELECT MAX(version) FROM supabase_migrations.schema_migrations`. Expected = 282; next = 283. Record actual value; if not 282, use MAX+1 throughout (do NOT abort).
    2. Mahmood row: `SELECT pa.value, pc.sources, t.topic_key, t.id AS topic_id FROM inform.politician_answers pa JOIN inform.compass_topics t ON t.id = pa.topic_id LEFT JOIN inform.politician_context pc ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id WHERE pa.politician_id = 'd3c5004c-9ca0-444e-96d9-107d4315abcb' AND t.topic_key = 'abortion'`. Expected: value=2.0, sources=['https://bilalmahmood.com/'].
    3. Moreno row: same query with politician_id `0b16443e-fec4-4f33-abbc-eb1331e3b42d` and topic_key `city-sanitation`. Expected: value=3.0, sources=['https://www.vivianmorenosd.com'].
    4. Live stance scales (SKILL.md Step 0 query) filtered to `WHERE t.topic_key IN ('abortion','city-sanitation')`. Record full JSON for both topics under "Live Stance Scales — abortion" and "Live Stance Scales — city-sanitation" sections (topic id, question_text, all 5 stances with value+text). This JSON is REQUIRED verbatim in the agent prompts.

    If query 2 or 3 returns a value or sources different from the D-01 triage record, STOP and surface the discrepancy — the triage is stale and the user must re-confirm scope.

    PART B — DISPATCH RESEARCH AGENT #1 (Bilal Mahmood / abortion). Use the SKILL.md Step 1 Agent tool call with `subagent_type: "politician-stance-researcher"`. The prompt MUST include:
    - Politician identification: "Bilal Mahmood, San Francisco Board of Supervisors member" (SF District 5; see 104-CONTEXT.md "specifics")
    - `--topics abortion` (single-topic scope)
    - FIVE-CHAIRS framing block verbatim from SKILL.md Step 1
    - TOPIC SCALE REFERENCE block formatted per SKILL.md "Orchestrator note" — copy the abortion stance scale JSON from Part A's "Live Stance Scales — abortion" section into the prompt rendered as `abortion (id: <uuid>) / Question: "<text>" / 1 = "..." / 2 = "..." / 3 = "..." / 4 = "..." / 5 = "..."`
    - TOOL RULE block from SKILL.md (WebFetch only, never WebSearch or Playwright)
    - `--output-file C:/EV-Accounts/backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv` (absolute path)
    - Domain hint: "Look for SF Board of Supervisors resolutions on abortion access (post-Dobbs SF Sanctuary City resolutions), Mahmood-sponsored or co-sponsored resolutions, public statements at board meetings, campaign issue pages (NOT bare homepage https://bilalmahmood.com/), endorsements from Planned Parenthood / NARAL / SBA Pro-Life CA. The homepage URL https://bilalmahmood.com/ already exists in sources and does NOT count — find a specific page or vote." Cite D-04.
    - Deletion rule (D-04): "If you cannot find a real specific (non-homepage) primary source URL, output an empty CSV with header only — the orchestrator treats that as DELETE. Do NOT fall back to the homepage URL."

    Wait for the agent to complete. Read the CSV. Classify outcome = UPGRADE if exactly one row with non-empty `source_url_1` that is not `https://bilalmahmood.com/`; outcome = DELETE otherwise. Append "Mahmood / abortion — Research Outcome" section to 104-RESEARCH-NOTES.md recording outcome, new value (if UPGRADE), new specific URL (if UPGRADE), reasoning (if UPGRADE), reason (if DELETE — always "no evidence found" per D-04).

    PART C — DISPATCH RESEARCH AGENT #2 (Vivian Moreno / city-sanitation). ONLY after Part B agent has completed and the CSV is written. Sequential, never parallel. Same Agent tool shape with these substitutions:
    - Politician: "Vivian Moreno, San Diego City Council member, District 8"
    - `--topics city-sanitation`
    - TOPIC SCALE REFERENCE: city-sanitation stance scale JSON from Part A's "Live Stance Scales — city-sanitation" section
    - `--output-file C:/EV-Accounts/backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv`
    - Domain hint: "Look for San Diego City Council vote records on sanitation/trash/waste management/Environmental Services Department funding; Council resolutions on illegal dumping or homeless encampment cleanup; Moreno's official D8 council page issue statements (sandiego.gov/citycouncil/cd8 — NOT campaign homepage https://www.vivianmorenosd.com); San Diego Union-Tribune coverage. The campaign homepage URL https://www.vivianmorenosd.com already exists in sources and does NOT count — find a specific page or vote." Cite D-04.
    - Same deletion rule (D-04) as Mahmood agent.

    Wait for completion. Same outcome-classification + 104-RESEARCH-NOTES.md update pattern — append "Moreno / city-sanitation — Research Outcome" section.

    Present both outcomes to the user for approval. This task is checkpoint:human-verify because the outcomes determine what Task 2's migration writes.
  </action>
  <what-built>
    Pre-flight DB state recorded (next migration number, both target rows, live stance scales for both topics). Two research-stances agents dispatched sequentially (Mahmood → Moreno). Both CSVs written. Both outcomes (UPGRADE with specific URL, or DELETE — no evidence found) appended to 104-RESEARCH-NOTES.md.
  </what-built>
  <how-to-verify>
    1. Open 104-RESEARCH-NOTES.md — confirm sections: "Pre-Flight" (with MAX(version), both target rows, both stance scales) → "Mahmood / abortion — Research Outcome" → "Moreno / city-sanitation — Research Outcome". Order matters: Mahmood section MUST appear before Moreno section (sequential dispatch evidence).
    2. Open backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv — header-only (DELETE) OR exactly one Mahmood/abortion row with source_url_1 ≠ `https://bilalmahmood.com/`.
    3. Open backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv — header-only (DELETE) OR exactly one Moreno/city-sanitation row with source_url_1 ≠ `https://www.vivianmorenosd.com`.
    4. For any UPGRADE: verify the new URL resolves to a real specific page (vote record, press release, issue page) — not a bare homepage.
    5. Reply "approved" with the two outcomes summarized (e.g., "approved — Mahmood: UPGRADE to value 2 with sfbos.org/legislation/...; Moreno: DELETE no evidence found") or describe corrections needed.
  </how-to-verify>
  <acceptance_criteria>
    - 104-RESEARCH-NOTES.md exists with "Pre-Flight" section containing MAX(version) result, the resolved next migration number (default 283), Mahmood and Moreno pre-flight rows matching the D-01 triage record (value 2.0 + bilalmahmood.com for Mahmood; value 3.0 + vivianmorenosd.com for Moreno), and "Live Stance Scales — abortion" + "Live Stance Scales — city-sanitation" sections with topic id, question_text, and all 5 stances per topic
    - 104-RESEARCH-NOTES.md "Mahmood / abortion — Research Outcome" section appears BEFORE the "Moreno / city-sanitation — Research Outcome" section (sequential dispatch evidence)
    - Both research outcome sections record outcome = UPGRADE (with new value, new URL, reasoning) or DELETE (with reason = "no evidence found" citing D-04)
    - backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv exists; either header-only OR one row with source_url_1 ≠ `https://bilalmahmood.com/`
    - backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv exists; either header-only OR one row with source_url_1 ≠ `https://www.vivianmorenosd.com`
    - User has typed "approved" with both outcomes confirmed
  </acceptance_criteria>
  <resume-signal>Type "approved" with both outcomes confirmed to proceed to migration write/apply, or describe corrections needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write migration 283 + apply script, apply to DB, write 104-VERIFICATION.md</name>
  <files>supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql, backend/scripts/_apply-migration-283.ts, .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md, .planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md (Pre-Flight section + both Research Outcome sections — drives UPSERT vs DELETE selection per target)
    - backend/data/stance-research/2026-06-07-104-mahmood-abortion.csv (source rows for any UPGRADE outcome)
    - backend/data/stance-research/2026-06-07-104-moreno-city-sanitation.csv (source rows for any UPGRADE outcome)
    - supabase/migrations/20260606000005_282_ca_state_source_remediation.sql (template — copy header comment shape, BEGIN/COMMIT wrap, ARRAY_CAT UPSERT block, DELETE-context-before-answers ordering, and the post-state RAISE NOTICE DO-block at the bottom of file)
    - backend/scripts/_apply-migration-281.ts (template for apply script — pg.Pool + readFileSync + ssl.rejectUnauthorized:false; note: migrations live in `supabase/migrations/` not `backend/migrations/` — adjust path.join accordingly to `path.join(process.cwd(), '..', 'supabase', 'migrations', '20260607000001_283_phase104_city_official_remediation.sql')`)
    - backend/scripts/run-source-coverage-audit.ts (SOURCED_CASE and UNSOURCED_CASE expressions; v2.5 city-cohort external_id range pattern at lines 284–295)
    - .planning/phases/104-local-remediation-city-officials/104-CONTEXT.md (D-02 out-of-scope guard; D-04 ARRAY_CAT-not-replace; D-05 verification cohort scope)
  </read_first>
  <action>
    STEP A — WRITE MIGRATION FILE. Create `supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql` (substitute next-migration-number from Task 1 Pre-Flight if MAX(version) ≠ 282).

    Header comment block: Phase 104 / Plan 01 attribution; Requirements covered = STAX-03, QUAL-01, QUAL-02; source CSVs = both Mahmood and Moreno paths; source deletion log = .planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md; pre-write cross-check noting (UPSERTs + DELETEs = 2 = Mahmood + Moreno); the two in-scope UUIDs and topic_keys; migration number derivation note (MAX(version) was X at write time; next = X+1 = 283).

    Body wrapped in `BEGIN; ... COMMIT;`.

    For each target with UPGRADE outcome, emit an UPSERT block matching the migration 282 ARRAY_CAT shape exactly:
    - `INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES (<uuid>, (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), <value>) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;`
    - `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES (<uuid>, (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>'), '<reasoning>', ARRAY(SELECT u FROM unnest(ARRAY['<new_specific_url>']) AS u WHERE u IS NOT NULL AND trim(u) != '')) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = politician_context.sources || EXCLUDED.sources;`
    - Trailing comment: `-- ARRAY_CAT: append new URL to existing [<existing_homepage_url>] (preserves history per D-04)`

    For each target with DELETE outcome, emit a DELETE pair matching the migration 282 shape exactly (context FIRST, then answers):
    - `-- DELETED: <full_name> / <topic_key> / former value=<value> / reason=no evidence found`
    - `DELETE FROM inform.politician_context WHERE politician_id = '<uuid>' AND topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>');`
    - `DELETE FROM inform.politician_answers WHERE politician_id = '<uuid>' AND topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic_key>');`

    Add a post-state RAISE NOTICE DO-block before COMMIT modeled on migration 282's DO-block. The DO-block runs the V1 query (city-cohort unsourced count using `p.external_id BETWEEN -689999 AND -630000 AND (p.external_id < -669999 OR p.external_id > -660000)` and `p.is_active = true` with the UNSOURCED predicate from run-source-coverage-audit.ts) AND the V2 query (city-cohort weak-source count — sources non-empty but all URLs match a bare-homepage pattern `^https?://[^/]+/?$`). Both RAISE NOTICE values must equal 0 to satisfy STAX-03; RAISE NOTICE does not block COMMIT.

    Out-of-scope guard (D-02): the migration MUST contain only the two in-scope UUIDs (`d3c5004c-9ca0-444e-96d9-107d4315abcb`, `0b16443e-fec4-4f33-abbc-eb1331e3b42d`). It MUST NOT reference Monica Rodriguez, Chris Krupa Downs, Burt Thakur, Ryan Tubbs, Shun Thomas, or external_id 695265.

    STEP B — WRITE APPLY SCRIPT. Create `backend/scripts/_apply-migration-283.ts` modeled on `_apply-migration-281.ts`:
    - `import 'dotenv/config'; import { Pool } from 'pg'; import { readFileSync } from 'fs'; import path from 'path';`
    - `const pool = new Pool({ connectionString: process.env['DATABASE_URL'], ssl: { rejectUnauthorized: false } });`
    - `const sql = readFileSync(path.join(process.cwd(), '..', 'supabase', 'migrations', '20260607000001_283_phase104_city_official_remediation.sql'), 'utf8');`
    - Apply via `await pool.query(sql)` in try/finally with `await pool.end()`
    - Two post-apply smoke checks: (a) V1 city-cohort unsourced count, (b) V2 city-cohort weak-source count. Log both numeric results. If either ≠ 0, log "WARNING: STAX-03 not satisfied" but do NOT throw.

    STEP C — APPLY MIGRATION. Run `cd backend && npx tsx scripts/_apply-migration-283.ts` (PowerShell: `cd backend; npx tsx scripts/_apply-migration-283.ts`). Capture stdout/stderr verbatim. Append "Migration Apply Log" section to 104-RESEARCH-NOTES.md containing the command, full stdout (must include "Migration 283 applied successfully"), V1 result (target 0), V2 result (target 0), and timestamp. If V1 or V2 ≠ 0, STOP — surface the count and abort before Step D.

    STEP D — WRITE 104-VERIFICATION.md. Run two independent verification queries against the live DB (separate from the migration's embedded RAISE NOTICE):
    - V1: COUNT inform.politician_answers JOIN essentials.politicians p ON p.id = pa.politician_id WHERE p.external_id BETWEEN -689999 AND -630000 AND (p.external_id < -669999 OR p.external_id > -660000) AND p.is_active = true AND the UNSOURCED_CASE condition (from run-source-coverage-audit.ts) holds.
    - V2: same cohort + answers JOIN context where sources is non-empty AND every non-blank URL matches `^https?://[^/]+/?$` (bare-homepage regex).

    Write `.planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md` with sections:
    - Header: Phase 104, Plan 01, requirement = STAX-03, date
    - V1 Query: exact SQL run, result (must be 0), cohort scope `external_id BETWEEN -689999 AND -630000` citing D-02
    - V2 Query: exact SQL run, result (must be 0)
    - Cross-reference: V1/V2 values match the migration's embedded RAISE NOTICE output captured in 104-RESEARCH-NOTES.md "Migration Apply Log"
    - STAX-03 status: "SATISFIED" (both V1 and V2 = 0) or "NOT SATISFIED" (with offending rows enumerated)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const sql=fs.readFileSync('supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql','utf8');const apply=fs.readFileSync('backend/scripts/_apply-migration-283.ts','utf8');const notes=fs.readFileSync('.planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md','utf8');const ver=fs.readFileSync('.planning/phases/104-local-remediation-city-officials/104-VERIFICATION.md','utf8');const errs=[];if(!sql.includes('BEGIN;'))errs.push('migration missing BEGIN;');if(!sql.includes('COMMIT;'))errs.push('migration missing COMMIT;');if(!sql.includes('d3c5004c-9ca0-444e-96d9-107d4315abcb'))errs.push('missing Mahmood UUID');if(!sql.includes('0b16443e-fec4-4f33-abbc-eb1331e3b42d'))errs.push('missing Moreno UUID');if(!sql.includes('STAX-03'))errs.push('migration missing STAX-03');if(!sql.includes('QUAL-02'))errs.push('migration missing QUAL-02');if(!sql.includes('RAISE NOTICE'))errs.push('migration missing RAISE NOTICE');for(const f of ['Monica Rodriguez','Krupa Downs','Burt Thakur','Ryan Tubbs','Shun Thomas','695265'])if(sql.includes(f))errs.push('out-of-scope reference: '+f);const hasU=sql.includes('politician_context.sources || EXCLUDED.sources');const hasD=sql.includes('DELETE FROM inform.politician_context')&&sql.includes('DELETE FROM inform.politician_answers');if(!hasU && !hasD)errs.push('neither ARRAY_CAT UPSERT nor DELETE pair present');if(!apply.includes('283'))errs.push('apply script missing 283');if(!apply.includes('pool.query'))errs.push('apply script missing pool.query');if(!notes.includes('Migration Apply Log'))errs.push('notes missing Migration Apply Log');if(!notes.includes('Migration 283 applied'))errs.push('notes missing success log line');if(!ver.includes('STAX-03'))errs.push('verification missing STAX-03');if(!ver.includes('SATISFIED'))errs.push('verification missing SATISFIED');if(!ver.includes('external_id BETWEEN -689999'))errs.push('verification missing cohort range');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260607000001_283_phase104_city_official_remediation.sql exists, wrapped in BEGIN;/COMMIT;, with STAX-03 + QUAL-01 + QUAL-02 in header
    - Migration references only the two in-scope UUIDs (`d3c5004c-9ca0-444e-96d9-107d4315abcb`, `0b16443e-fec4-4f33-abbc-eb1331e3b42d`); zero references to Monica Rodriguez, Chris Krupa Downs, Burt Thakur, Ryan Tubbs, Shun Thomas, or external_id 695265
    - For each UPGRADE outcome from Task 1: migration contains an UPSERT block using `sources = politician_context.sources || EXCLUDED.sources` (ARRAY_CAT pattern per D-04)
    - For each DELETE outcome from Task 1: migration contains a DELETE pair with context DELETE FIRST then answers DELETE
    - Migration contains a `RAISE NOTICE` DO-block at the end running V1 (unsourced count) and V2 (weak-source count) queries against the v2.5 city-official external_id cohort
    - backend/scripts/_apply-migration-283.ts exists, reads the 283 SQL file via path.join with `'..', 'supabase', 'migrations'`, calls pool.query(sql), logs V1+V2 smoke-check results
    - Apply script was executed successfully; 104-RESEARCH-NOTES.md contains "Migration Apply Log" section with command, stdout including "Migration 283 applied", V1=0, V2=0, and timestamp
    - 104-VERIFICATION.md exists with V1 SQL + result=0, V2 SQL + result=0, cohort scope `external_id BETWEEN -689999 AND -630000` citing D-02, cross-reference to "Migration Apply Log" V1/V2, and STAX-03 status = SATISFIED
  </acceptance_criteria>
  <done>
    Migration 283 written, applied, and verified. STAX-03 V1 and V2 queries both return 0. QUAL-01 implicitly satisfied via Task 1 Chair-methodology research. Ready to finalize deletion logs in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 104-DELETION-LOG.md and MASTER-DELETION-LOG.md (QUAL-02 final, v2.7 milestone)</name>
  <files>.planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md, .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md</files>
  <read_first>
    - .planning/phases/101-candidate-profiles/101-DELETION-LOG.md (1 entry: Deb Fischer / ai-regulation / 3 / no evidence found)
    - .planning/phases/102-federal-house-remediation/102-DELETION-LOG.md (12 entries: Derek Dooley × 3, Hallie Shoffner × 3, Kurt Alme × 6 — copy verbatim into master)
    - .planning/phases/103-state-remediation-ca-md/103-DELETION-LOG.md (6 entries: Eloise Gómez Reyes × 2, Henry Stern × 3, Rob Bonta × 1 — copy verbatim including the ó character into master)
    - .planning/phases/104-local-remediation-city-officials/104-RESEARCH-NOTES.md (Research Outcome sections from Task 1 — determine 0, 1, or 2 Phase 104 entries)
    - .planning/phases/104-local-remediation-city-officials/104-CONTEXT.md (D-05 — MASTER-DELETION-LOG.md format, location, and canonical-artifact role)
  </read_first>
  <action>
    STEP A — Write 104-DELETION-LOG.md modeled on `.planning/phases/103-state-remediation-ca-md/103-DELETION-LOG.md`:
    - Header: Phase 104 — Deletion Log (QUAL-02); Phase = 104 — Local Remediation — City Officials; Plan = 01; Migration = 283 (or actual number from Task 1); Produced = YYYY-MM-DD
    - Notes section citing D-04 (delete-if-no-real-URL, no party inference, single-pass research) and the sequential dispatch order (Mahmood → Moreno per MEMORY.md rate-limit rule)
    - Deletions table with columns: `politician full_name | topic_key | former value | reason`
    - One row per DELETE outcome from Task 1 (0, 1, or 2 rows total) — `Bilal Mahmood | abortion | 2 | no evidence found` if Mahmood DELETE; `Vivian Moreno | city-sanitation | 3 | no evidence found` if Moreno DELETE
    - "Total deletions: N" line
    - Cross-check line: "Cross-check: K UPSERTs + N deletions = 2 = Phase 104 target count (Mahmood + Moreno) ✓"
    - If N = 0 (both UPGRADE): table is empty, notes section explicitly states "No deletions this phase — both targets upgraded with real specific source URLs"

    STEP B — Write MASTER-DELETION-LOG.md at `.planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md` per D-05:
    - Title: `# v2.7 Source Integrity — Master Deletion Log (QUAL-02 Final)`
    - Header section: milestone v2.7, phases covered = 101 + 102 + 103 + 104, date YYYY-MM-DD, scope = every stance deleted across the v2.7 milestone
    - Milestone summary line: `**Total deletions across v2.7: M**` where M = 1 + 12 + 6 + N(104) = 19 + N
    - Per-phase summary: `Phase 101: 1 | Phase 102: 12 | Phase 103: 6 | Phase 104: N | Total: M`
    - Single unified table with EXACTLY these columns in this order: `phase | politician full_name | topic_key | former value | reason`
    - Rows merged in this order:
      - Phase 101 (1 row): `101 | Deb Fischer | ai-regulation | 3 | no evidence found`
      - Phase 102 (12 rows): copy verbatim from 102-DELETION-LOG.md rows (Derek Dooley × 3: abortion=4, civil-rights=4, voting-rights=4; Hallie Shoffner × 3: campaign-finance=2, climate-change=2, housing=2; Kurt Alme × 6: religious-freedom=5, same-sex-marriage=5, social-security=4, tariffs=4, trans-athletes=4, voting-rights=4) — each row prefixed with `102 |`
      - Phase 103 (6 rows): copy verbatim from 103-DELETION-LOG.md rows (Eloise Gómez Reyes × 2: religious-freedom=3, ukraine-support=2; Henry Stern × 3: religious-freedom=3, social-security=2, ukraine-support=2; Rob Bonta × 1: ukraine-support=2) — preserve the ó character in Gómez exactly — each row prefixed with `103 |`
      - Phase 104 (N rows, 0–2): rows from Step A's 104-DELETION-LOG.md — each row prefixed with `104 |`
    - Footer citing source files (101-DELETION-LOG.md, 102-DELETION-LOG.md, 103-DELETION-LOG.md, 104-DELETION-LOG.md) as inputs and D-05 as the consolidation directive
    - Closing statement (verbatim): `This file is the QUAL-02 canonical artifact for the v2.7 Source Integrity milestone.`

    Verify row count: read each source log, count rows in its Deletions table, confirm 101→1, 102→12, 103→6, 104→N (matches Task 1 DELETE outcome count). MASTER unified table must contain exactly 19 + N rows.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const phase=fs.readFileSync('.planning/phases/104-local-remediation-city-officials/104-DELETION-LOG.md','utf8');const master=fs.readFileSync('.planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md','utf8');const errs=[];if(!phase.includes('Phase 104'))errs.push('phase log missing Phase 104');if(!phase.includes('Total deletions'))errs.push('phase log missing Total deletions');if(!phase.includes('QUAL-02'))errs.push('phase log missing QUAL-02');if(!master.includes('Master Deletion Log'))errs.push('master missing title');if(!master.includes('Total deletions across v2.7'))errs.push('master missing milestone summary');if(!master.includes('Deb Fischer'))errs.push('master missing Deb Fischer (101)');if(!master.includes('Derek Dooley'))errs.push('master missing Derek Dooley (102)');if(!master.includes('Hallie Shoffner'))errs.push('master missing Hallie Shoffner (102)');if(!master.includes('Kurt Alme'))errs.push('master missing Kurt Alme (102)');if(!master.includes('Gómez Reyes'))errs.push('master missing Gómez Reyes with proper accent (103)');if(!master.includes('Henry Stern'))errs.push('master missing Henry Stern (103)');if(!master.includes('Rob Bonta'))errs.push('master missing Rob Bonta (103)');if(!master.includes('QUAL-02'))errs.push('master missing QUAL-02 attribution');if(!master.includes('canonical artifact'))errs.push('master missing canonical artifact closer');const phaseRowRe=/\\|\\s*10[1-4]\\s*\\|/g;const phaseRows=(master.match(phaseRowRe)||[]).length;if(phaseRows<19)errs.push('master has only '+phaseRows+' phase-prefixed rows; expected 19 or more');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK rows='+phaseRows)"</automated>
  </verify>
  <acceptance_criteria>
    - 104-DELETION-LOG.md exists with header (Phase 104, Plan 01, Migration 283, date), notes citing D-04, deletions table with `politician full_name | topic_key | former value | reason`, total line, and cross-check line
    - 104-DELETION-LOG.md has N rows in its deletions table where N matches the DELETE-outcome count from Task 1 (0, 1, or 2)
    - MASTER-DELETION-LOG.md exists at .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md
    - MASTER-DELETION-LOG.md unified table has columns `phase | politician full_name | topic_key | former value | reason` in that exact order
    - MASTER-DELETION-LOG.md contains at least 19 phase-prefixed rows (regex `\| 10[1-4] \|`) — exactly 19 + N when N is known
    - MASTER-DELETION-LOG.md contains the entries Deb Fischer (101), Derek Dooley + Hallie Shoffner + Kurt Alme (102), Eloise Gómez Reyes (with the ó character preserved) + Henry Stern + Rob Bonta (103), plus N Phase-104 entries
    - MASTER-DELETION-LOG.md cites D-05 and includes the closing line "This file is the QUAL-02 canonical artifact for the v2.7 Source Integrity milestone."
    - MASTER-DELETION-LOG.md milestone summary line states total = 19 + N
  </acceptance_criteria>
  <done>
    QUAL-02 finalized for v2.7: Phase 104 deletion log committed (N entries) and master deletion log consolidates all 19+N deletions across phases 101–104 into the canonical milestone artifact.
  </done>
</task>

</tasks>

<verification>
- Migration 283 (or resolved next number) applied successfully; pool.query returned no error.
- Post-apply V1 query (unsourced count in v2.5 city cohort) = 0.
- Post-apply V2 query (weak-source-only count in v2.5 city cohort) = 0.
- 104-DELETION-LOG.md exists with N entries (0–2) matching the DELETE outcomes from Task 1.
- MASTER-DELETION-LOG.md exists with at least 19 phase-prefixed rows (exactly 19 + N when N is known) in its unified table — one row per deletion across phases 101–104.
- Both research-stances agents were dispatched SEQUENTIALLY (Mahmood completed before Moreno started) — verifiable from the ORDER of "Research Outcome" sections in 104-RESEARCH-NOTES.md (Mahmood section appears textually before Moreno section).
- No row in the migration references a politician other than Mahmood (d3c5004c-...) or Moreno (0b16443e-...) — D-02 out-of-scope guard.
- No reference to Monica Rodriguez, Chris Krupa Downs, Burt Thakur, Ryan Tubbs, or Shun Thomas anywhere in the migration or apply script.
</verification>

<success_criteria>
- STAX-03 closed: every city official stance (v2.5 cohort) is either sourced (specific non-homepage URL in inform.politician_context.sources) or deleted.
- QUAL-01 closed: every retained value was verified against the specific Chair text by the research-stances agent (FIVE-CHAIRS framing block was included in both agent prompts; no party inference used).
- QUAL-02 closed (final): MASTER-DELETION-LOG.md consolidates 19 + N deletions across the entire v2.7 milestone into one canonical artifact at .planning/phases/104-local-remediation-city-officials/MASTER-DELETION-LOG.md.
- v2.7 Source Integrity milestone deliverable scope is complete — all 9 requirements (SRCA-01/02, FEDX-01/02, STAX-01/02/03, QUAL-01/02) closed.
</success_criteria>

<output>
Create `.planning/phases/104-local-remediation-city-officials/104-01-SUMMARY.md` when done. The summary must record:
- Mahmood outcome (UPGRADE with URL | DELETE)
- Moreno outcome (UPGRADE with URL | DELETE)
- Migration number actually used (default 283; may differ if Task 1 Pre-Flight resolved otherwise)
- Total Phase-104 deletions (N, 0–2)
- Total v2.7 milestone deletions (19 + N) per MASTER-DELETION-LOG.md
- Confirmation that STAX-03 V1 and V2 both returned 0
- Note any deviations (migration number bumped, additional sources found beyond a single specific URL, etc.)
</output>
