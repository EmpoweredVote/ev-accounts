---
phase: 15-compass-admin-react-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/src/App.tsx
  - admin/src/pages/admin/AdminLayout.tsx
  - admin/src/pages/admin/TopicsPage.tsx
  - admin/src/pages/admin/PoliticiansPage.tsx
  - admin/src/pages/admin/CategoriesPage.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /admin/topics renders a page without crashing"
    - "Navigating to /admin/politicians renders a page without crashing"
    - "Navigating to /admin/categories renders a page without crashing"
    - "Topics, Politicians, and Categories appear as sidebar nav links"
    - "Active nav link is highlighted (bg-red-50 text-ev-red)"
  artifacts:
    - path: "admin/src/pages/admin/TopicsPage.tsx"
      provides: "Exported TopicsPage component (stub — loading state + empty state)"
    - path: "admin/src/pages/admin/PoliticiansPage.tsx"
      provides: "Exported PoliticiansPage component (stub)"
    - path: "admin/src/pages/admin/CategoriesPage.tsx"
      provides: "Exported CategoriesPage component (stub)"
    - path: "admin/src/App.tsx"
      provides: "Three new Route entries inside AdminLayout Route"
    - path: "admin/src/pages/admin/AdminLayout.tsx"
      provides: "Three new navItems entries (Topics, Politicians, Categories)"
  key_links:
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/admin/TopicsPage.tsx"
      via: "Route path='topics' element={<TopicsPage />}"
      pattern: "TopicsPage"
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/admin/PoliticiansPage.tsx"
      via: "Route path='politicians' element={<PoliticiansPage />}"
      pattern: "PoliticiansPage"
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/admin/CategoriesPage.tsx"
      via: "Route path='categories' element={<CategoriesPage />}"
      pattern: "CategoriesPage"
---

<objective>
Wire the three new compass admin pages into the existing navigation and routing scaffolding, with stub implementations that confirm routing is working before full feature builds begin.

Purpose: Plans 02, 03, and 04 each implement one compass page in full. They all depend on routes and sidebar links existing. Scaffolding here as a shared foundation eliminates repeated App.tsx and AdminLayout.tsx edits across parallel plans.

Output: Three new stub page files + updated App.tsx routes + updated AdminLayout.tsx navItems. All three /admin/topics, /admin/politicians, /admin/categories URLs load without error.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-compass-admin-react-ui/15-RESEARCH.md
@admin/src/App.tsx
@admin/src/pages/admin/AdminLayout.tsx
@admin/src/pages/admin/AccountsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub page files for TopicsPage, PoliticiansPage, CategoriesPage</name>
  <files>
    admin/src/pages/admin/TopicsPage.tsx
    admin/src/pages/admin/PoliticiansPage.tsx
    admin/src/pages/admin/CategoriesPage.tsx
  </files>
  <action>
    Create three stub page files. Each follows the existing page pattern from AccountsPage.tsx:
    - Named export function matching the filename (e.g., `export function TopicsPage()`)
    - Local state: `const [loading, setLoading] = useState(true)` and `const [error, setError] = useState<string | null>(null)`
    - useEffect that calls apiFetch and sets loading false (use the real endpoint but allow empty state)
    - Render: page title heading, loading skeleton (5 rows of animated-pulse divs), error block, empty state paragraph
    - DO NOT implement any actual features yet — stub only

    TopicsPage.tsx stub:
    - Fetch `apiFetch<{ topics: unknown[] }>('/admin/compass/topics')` in useEffect
    - Render `<h1>Topics</h1>` and a "No topics yet." empty state below loading skeleton
    - Import apiFetch from `../../lib/api`

    PoliticiansPage.tsx stub:
    - Fetch `apiFetch<{ politicians: unknown[] }>('/admin/compass/politicians')` in useEffect
    - Render `<h1>Politicians</h1>` and a "No politicians yet." empty state

    CategoriesPage.tsx stub:
    - Fetch `apiFetch<{ categories: unknown[] }>('/admin/compass/categories')` in useEffect
    - Render `<h1>Categories</h1>` and a "No categories yet." empty state

    Use the standard error display pattern from the research file:
    ```tsx
    {error && (
      <div className="mb-4 p-4 bg-red-50 border border-red-200 rounded text-red-700 text-sm">{error}</div>
    )}
    ```

    Tailwind classes on headings: `text-2xl font-bold text-gray-900 mb-6`
  </action>
  <verify>
    `cd C:/EV-Accounts && npx tsc --noEmit -p admin/tsconfig.json` exits 0 with no errors in the three new files.
  </verify>
  <done>
    Three .tsx files exist at the correct paths, export named functions, compile without TypeScript errors, and each correctly imports and calls apiFetch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register routes in App.tsx and add navItems to AdminLayout.tsx</name>
  <files>
    admin/src/App.tsx
    admin/src/pages/admin/AdminLayout.tsx
  </files>
  <action>
    Update App.tsx:
    - Add three import statements at the top (after the existing page imports):
      ```tsx
      import { TopicsPage } from './pages/admin/TopicsPage';
      import { PoliticiansPage } from './pages/admin/PoliticiansPage';
      import { CategoriesPage } from './pages/admin/CategoriesPage';
      ```
    - Inside the `<Route path="/admin" element={<AdminLayout />}>` block, after the existing `<Route path="roles" ...>` entry, add:
      ```tsx
      <Route path="topics" element={<TopicsPage />} />
      <Route path="politicians" element={<PoliticiansPage />} />
      <Route path="categories" element={<CategoriesPage />} />
      ```

    Update AdminLayout.tsx:
    - In the `navItems` array, after the `{ label: 'Roles', to: '/admin/roles' }` entry, append:
      ```tsx
      { label: 'Topics', to: '/admin/topics' },
      { label: 'Politicians', to: '/admin/politicians' },
      { label: 'Categories', to: '/admin/categories' },
      ```
    - Do NOT add an `exact` property — none of the three new routes need it (only Dashboard does).
    - Do NOT create a Compass section grouping — flat insertion matches the CONTEXT.md decision.

    After editing, run `npx tsc --noEmit -p admin/tsconfig.json` to confirm no errors.
  </action>
  <verify>
    1. `cd C:/EV-Accounts && npx tsc --noEmit -p admin/tsconfig.json` exits 0.
    2. `grep -c "TopicsPage\|PoliticiansPage\|CategoriesPage" admin/src/App.tsx` returns 6 (3 imports + 3 route elements).
    3. `grep -c "Topics\|Politicians\|Categories" admin/src/pages/admin/AdminLayout.tsx` returns 3.
  </verify>
  <done>
    App.tsx has 3 new routes; AdminLayout.tsx has 3 new navItems. TypeScript compiles clean. Running the dev server and visiting /admin/topics, /admin/politicians, /admin/categories each renders a page without a white screen or console error.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd C:/EV-Accounts && npx tsc --noEmit -p admin/tsconfig.json` exits 0 — no TypeScript errors anywhere in admin/src.
2. All five new/modified files exist at their exact paths.
3. `grep "TopicsPage\|PoliticiansPage\|CategoriesPage" admin/src/App.tsx` shows both imports and Route elements.
</verification>

<success_criteria>
Three new route paths (/admin/topics, /admin/politicians, /admin/categories) are reachable through the admin sidebar without a page crash. Stub pages render the page title and either a loading state or empty state. No TypeScript compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15-compass-admin-react-ui/15-01-SUMMARY.md` using the summary template.
</output>
