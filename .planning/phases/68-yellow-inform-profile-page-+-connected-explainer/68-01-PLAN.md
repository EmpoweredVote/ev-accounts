---
phase: 68-yellow-inform-profile-page-+-connected-explainer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/src/components/ConnectedExplainerModal.tsx
autonomous: true

must_haves:
  truths:
    - "An Inform user can open a modal that explains what Connected Accounts are."
    - "The modal explains identity verification works via invite codes during Alpha."
    - "The modal includes a clearly-labeled CTA leading to the existing Connected signup flow."
    - "The modal closes via Escape key, backdrop click, or explicit Close button."
  artifacts:
    - path: "admin/src/components/ConnectedExplainerModal.tsx"
      provides: "Focused Connected Account explainer dialog (CEXP-02, CEXP-03)"
      exports: ["default ConnectedExplainerModal"]
      contains: "Dialog"
      min_lines: 50
  key_links:
    - from: "admin/src/components/ConnectedExplainerModal.tsx"
      to: "@headlessui/react"
      via: "named imports of Dialog, DialogPanel, DialogTitle"
      pattern: "from '@headlessui/react'"
    - from: "admin/src/components/ConnectedExplainerModal.tsx"
      to: "react-router-dom Link"
      via: "I have an invite code CTA"
      pattern: "to=\"/signup\""
---

<objective>
Create a new focused modal component, `ConnectedExplainerModal.tsx`, that explains the Connected Account tier to Inform-tier users and provides a CTA to the existing Connected signup flow.

Purpose: Inform users on `/profile` need a clear, low-pressure explanation of what Connected Accounts are, how identity verification works during Alpha (invite codes), and how to proceed if they already have an invite code. The existing `AccountTypesModal.tsx` is a broad three-tier platform explainer and is NOT focused enough for this purpose. This phase requires a dedicated component covering CEXP-02 (explainer content) and CEXP-03 (invite-code CTA).

Output: A new file `admin/src/components/ConnectedExplainerModal.tsx` exporting a default `ConnectedExplainerModal` React component with `{ open, onClose }` props, using the canonical `@headlessui/react` v2 Dialog API and the teal (`ev-teal`) color system to match the Connected tier theme.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-yellow-inform-profile-page-+-connected-explainer/68-RESEARCH.md
@admin/src/components/InformConstraintsModal.tsx
@admin/src/components/AccountTypesModal.tsx
@admin/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConnectedExplainerModal.tsx with Dialog skeleton, props, and styling</name>
  <files>admin/src/components/ConnectedExplainerModal.tsx</files>
  <action>
    Create a new file `admin/src/components/ConnectedExplainerModal.tsx`.

    Use `InformConstraintsModal.tsx` as the canonical pattern reference — it is the most recently-built modal in this codebase and uses the correct flat headlessui v2 API (`Dialog`, `DialogPanel`, `DialogTitle` only). Do NOT use `Transition` or `Transition.Child` (those are legacy v1 API patterns still present in `Signup.tsx` but should NOT be used here).

    Component contract:
    - Default export: `ConnectedExplainerModal`
    - Props interface: `{ open: boolean; onClose: () => void }`
    - Imports: `Dialog`, `DialogPanel`, `DialogTitle` from `@headlessui/react`; `Link` from `react-router-dom`

    Dialog structure:
    - Outer `<Dialog open={open} onClose={onClose} className="relative z-50">`
    - Backdrop: `<div className="fixed inset-0 bg-black/40" aria-hidden="true" />`
    - Centered container: `<div className="fixed inset-0 flex items-center justify-center p-4">`
    - DialogPanel: `<DialogPanel className="w-full max-w-md bg-white dark:bg-gray-900 rounded-2xl border border-ev-teal/30 shadow-xl p-6 space-y-4">`
      - Use `border-ev-teal/30` (NOT yellow) — this modal explains the Connected tier, so it uses the teal color system per the brand color tokens defined in `admin/src/index.css`.

    Title: `<DialogTitle className="text-lg font-semibold text-gray-900 dark:text-white">Connected Accounts</DialogTitle>`

    Body content (CEXP-02 — what Connected is, identity verification, invite codes in Alpha):
    - One short intro paragraph: "Connected Accounts unlock the participatory side of Empowered Vote — contributing to Validation Quests, posting in Focused Communities, and voting with your values."
    - A `<ul className="space-y-3">` with TWO list items, each with `text-sm text-gray-700 dark:text-gray-300`:
      1. **Identity verified, privately.** "One real person, one voice. Your identity is verified but your participation is pseudonymous."
      2. **Alpha access via invite code.** "During Alpha, Connected Accounts require an invite code from an existing member."
    - Bold the leading phrase of each item using `<span className="font-semibold text-gray-900 dark:text-white">` for the lead-in.

    CTA (CEXP-03 — "I have an invite code" leading to Connected signup):
    - `<Link to="/signup" onClick={onClose} className="block w-full text-center py-2.5 px-4 bg-ev-teal hover:bg-ev-teal/90 text-white font-semibold rounded-xl text-sm transition-colors">I have an invite code →</Link>`
    - The `onClose` on click ensures the modal closes before navigation.
    - `to="/signup"` matches the existing Connected signup route in `App.tsx`.

    Close button (secondary, low-prominence):
    - `<button type="button" onClick={onClose} className="block w-full text-center py-2 text-sm text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 transition-colors">Close</button>`

    Anti-funnel copy rules (per project memory and CONTEXT in research):
    - NO "upgrade", "convert", "unlock now" pressure language.
    - Use invitational tone — "when you're ready" framing is appropriate elsewhere; here, a neutral factual description is the right voice.
    - DO NOT mention specific gem rewards or XP — keep it about the participation contract, not extrinsic motivators.

    Why these choices:
    - `border-ev-teal/30` matches the Connected tier color (per Brand Colors table — `ev-teal` = `#00657C`).
    - Flat v2 Dialog API matches all recent modals in this codebase (`InformConstraintsModal`, `AccountTypesModal`, `GrantRoleModal`).
    - `Link` (not `<a>`) preserves SPA navigation and avoids a full page reload.
    - `onClick={onClose}` on the Link ensures clean modal teardown before route change.
  </action>
  <verify>
    Run `cd admin && npx tsc --noEmit` — must complete with zero TypeScript errors related to the new file.

    Run `cd admin && npm run build` — must succeed.

    Open the file and confirm:
    - Default export is `ConnectedExplainerModal`.
    - Imports include `Dialog`, `DialogPanel`, `DialogTitle` from `@headlessui/react` and `Link` from `react-router-dom`.
    - File does NOT import `Transition` or `Transition.Child`.
    - Component renders a `Dialog` with backdrop, panel, title "Connected Accounts", two list items covering identity verification + invite codes, an "I have an invite code →" Link to `/signup`, and a Close button.
    - File length is at least 50 lines.

    grep verification:
    - `grep -E "from '@headlessui/react'" admin/src/components/ConnectedExplainerModal.tsx` returns the import.
    - `grep -E "to=\"/signup\"" admin/src/components/ConnectedExplainerModal.tsx` returns the Link CTA.
    - `grep -E "border-ev-teal" admin/src/components/ConnectedExplainerModal.tsx` returns at least one match.
    - `grep "Transition" admin/src/components/ConnectedExplainerModal.tsx` returns NO matches (anti-pattern guard).
  </verify>
  <done>
    `admin/src/components/ConnectedExplainerModal.tsx` exists, type-checks cleanly, builds successfully, exports a default React component matching the contract above, uses the flat headlessui v2 Dialog API, applies the teal color system, and includes a `Link to="/signup"` CTA. The file is ready to be imported by `ProfilePage.tsx` in Plan 02.
  </done>
</task>

</tasks>

<verification>
- TypeScript: `cd admin && npx tsc --noEmit` passes with no new errors.
- Build: `cd admin && npm run build` succeeds.
- File exists: `admin/src/components/ConnectedExplainerModal.tsx` is present.
- Anti-pattern guard: no `Transition` or `Transition.Child` imports.
- Color system: at least one `border-ev-teal` reference.
- CTA: at least one `to="/signup"` reference.
</verification>

<success_criteria>
1. New component file exists at `admin/src/components/ConnectedExplainerModal.tsx`.
2. Default export is a React functional component accepting `{ open, onClose }`.
3. Component renders a headlessui v2 Dialog with title "Connected Accounts", explainer content covering identity verification + Alpha invite codes (CEXP-02), an "I have an invite code →" `<Link to="/signup">` CTA (CEXP-03), and a Close button.
4. Uses `ev-teal` border accent (the Connected tier color), white/dark-gray-900 background, dark-mode aware text.
5. Closes via Escape, backdrop click, the Close button, AND the CTA click (which calls `onClose` before navigation).
6. TypeScript and build both pass clean.
</success_criteria>

<output>
After completion, create `.planning/phases/68-yellow-inform-profile-page-+-connected-explainer/68-01-SUMMARY.md` documenting:
- The component file created and its props contract.
- Color system used (ev-teal) and why.
- Confirmation that the flat headlessui v2 API was used (no Transition imports).
- Any deviations from the action spec and rationale.
</output>
