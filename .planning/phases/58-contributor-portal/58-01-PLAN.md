---
phase: 58-contributor-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/contributor.ts
  - backend/src/routes/compassContributor.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/contributor/me returns granted_at for each grant"
    - "GET /api/compass/contributors/politicians returns results for essentials_data_editor grants"
  artifacts:
    - path: "backend/src/routes/contributor.ts"
      provides: "granted_at in contributor/me response"
      contains: "granted_at"
    - path: "backend/src/routes/compassContributor.ts"
      provides: "essentials_data_editor access to contributor politicians endpoint"
      contains: "essentials_data_editor"
  key_links:
    - from: "backend/src/routes/contributor.ts"
      to: "roleService.UserRoleGrant"
      via: "g.granted_at mapping"
      pattern: "granted_at.*g\\.granted_at"
    - from: "backend/src/routes/compassContributor.ts"
      to: "requireRole middleware"
      via: "role slug array"
      pattern: "essentials_data_editor"
---

<objective>
Add two backend micro-fixes that unblock the Contributor Portal frontend: (1) include `granted_at` in the GET /api/contributor/me response, and (2) extend the contributor politicians endpoint to accept `essentials_data_editor` grants.

Purpose: The dashboard cards need grant dates, and the Essentials Editor page needs a jurisdiction-scoped politician list. Both are one-line changes.
Output: Updated contributor.ts and compassContributor.ts files.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-contributor-portal/58-CONTEXT.md
@.planning/phases/58-contributor-portal/58-RESEARCH.md
@backend/src/routes/contributor.ts
@backend/src/routes/compassContributor.ts
@backend/src/lib/roleService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add granted_at to contributor/me response</name>
  <files>backend/src/routes/contributor.ts</files>
  <action>
In `backend/src/routes/contributor.ts`, line 27-31 has the mapping object inside `grants.map()`:
```
const mapped = grants.map((g) => ({
  role_slug: g.slug,
  feature_scope: g.feature_scope,
  jurisdiction_geoid: g.jurisdiction_geoid,
  resource_id: g.resource_id,
}));
```

Add `granted_at: g.granted_at,` to this mapped object. The `UserRoleGrant` interface in `roleService.ts` already has `granted_at: string` — it's available on the `g` object, just not being passed through.

Also update the JSDoc comment at the top (line 16) to include `granted_at` in the documented response shape:
```
// Response: [{ role_slug, feature_scope, jurisdiction_geoid, resource_id, granted_at }]
```
  </action>
  <verify>Run `cd backend && npx tsc --noEmit` — no type errors. Grep for `granted_at` in contributor.ts to confirm it appears in the mapping.</verify>
  <done>GET /api/contributor/me response includes `granted_at` (ISO8601 string) for each grant object.</done>
</task>

<task type="auto">
  <name>Task 2: Extend contributor politicians endpoint for essentials_data_editor</name>
  <files>backend/src/routes/compassContributor.ts</files>
  <action>
In `backend/src/routes/compassContributor.ts`, make these changes to the `GET /contributors/politicians` handler:

1. **requireRole array** (line ~85): Change from:
   `requireRole(['compass_stance_editor', 'campaign_manager'])`
   to:
   `requireRole(['compass_stance_editor', 'campaign_manager', 'essentials_data_editor'])`

2. **contributorGrants filter** (line ~93-94): Change the `.filter()` from:
   `['compass_stance_editor', 'campaign_manager'].includes(g.slug)`
   to:
   `['compass_stance_editor', 'campaign_manager', 'essentials_data_editor'].includes(g.slug)`

3. **Update the file header comment** (line ~1-2): Update the description to mention essentials_data_editor alongside the other two roles. Change "compass_stance_editor and campaign_manager roles" to "compass_stance_editor, campaign_manager, and essentials_data_editor roles."

The `getContributorPoliticians` service function already handles jurisdiction-scoped grants correctly — `essentials_data_editor` uses identical jurisdiction semantics to `compass_stance_editor` (jurisdiction_geoid scoping). No service-layer changes needed.

Do NOT touch the PUT stance routes — those remain restricted to compass_stance_editor and campaign_manager only.
  </action>
  <verify>Run `cd backend && npx tsc --noEmit` — no type errors. Grep for `essentials_data_editor` in compassContributor.ts to confirm it appears in the requireRole array and the contributorGrants filter. Confirm the PUT routes still only reference compass_stance_editor and campaign_manager.</verify>
  <done>An essentials_data_editor user calling GET /api/compass/contributors/politicians receives their jurisdiction-scoped politician list (200) instead of 403.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` passes with zero errors
2. `cd backend && npm run build` succeeds (if build script exists)
3. `granted_at` appears in contributor.ts response mapping
4. `essentials_data_editor` appears in compassContributor.ts requireRole AND contributorGrants filter for the GET /contributors/politicians route
5. PUT /compass/stances routes still restricted to compass_stance_editor + campaign_manager only
</verification>

<success_criteria>
- Backend compiles clean with both changes
- contributor/me response now includes granted_at
- contributor politicians endpoint accepts essentials_data_editor role
- Stance write routes unchanged (no scope creep)
</success_criteria>

<output>
After completion, create `.planning/phases/58-contributor-portal/58-01-SUMMARY.md`
</output>
