---
phase: 78-visual-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - EV-ReadRank/src/components/ResultsPhase.tsx
  - EV-ReadRank/src/components/ProgressHeader.tsx
autonomous: false
requirements:
  - DSGN-03

must_haves:
  truths:
    - "ResultsPhase summary stats container uses ev-muted-blue/10 background (not ev-light-blue)"
    - "'View Your Alignment' CTA button in each QuoteResultCard uses ev-coral (not ev-light-blue)"
    - "Source link in QuoteResultCard uses ev-muted-blue instead of ev-light-blue/ev-teal"
    - "'Explore More Issues' button uses ev-coral gradient (not ev-light-blue gradient)"
    - "'Agreed'/'Disagreed' status badges in QuoteResultCard use the amber/cyan pair matching the swipe system"
    - "ProgressHeader back-button uses text-ev-muted-blue hover:text-ev-coral"
    - "npm run build completes with zero errors"
    - "Human confirms visual appearance matches EV brand at dev server"
  artifacts:
    - path: "EV-ReadRank/src/components/ResultsPhase.tsx"
      provides: "ev-coral CTAs, ev-muted-blue accents, amber/cyan agreed/disagreed badges"
      contains: "ev-coral"
    - path: "EV-ReadRank/src/components/ProgressHeader.tsx"
      provides: "ev-muted-blue back-button color"
      contains: "ev-muted-blue"
  key_links:
    - from: "EV-ReadRank/src/components/ResultsPhase.tsx"
      to: "EV-ReadRank/tailwind.config.js"
      via: "ev-coral Tailwind utility class on CTA button"
      pattern: "bg-ev-coral"
---

<objective>
Refresh the ResultsPhase layout with EV brand polish — ev-coral CTAs, ev-muted-blue accents, amber/cyan agreed/disagreed badges consistent with the swipe system, and update ProgressHeader back-button color.

Purpose: ResultsPhase is where users see the payoff — their verdicts revealed against candidate names. The current layout uses ev-light-blue for CTAs, ev-teal (undefined) for hover states, and red/green for agreed/disagreed badges — creating visual inconsistency with the swipe feedback system being updated in Plan 02. This plan brings ResultsPhase into full visual alignment with the EV platform and creates semantic coherence (amber/cyan verdict badges match the swipe color pair, reinforcing the connection between swipe actions and their results).

Output: ResultsPhase with ev-coral primary CTAs, ev-muted-blue accents, coherent amber/cyan verdict badge system, and a consistent ProgressHeader.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-visual-refresh/78-CONTEXT.md
@.planning/phases/78-visual-refresh/78-RESEARCH.md
@.planning/phases/78-visual-refresh/78-01-SUMMARY.md

<interfaces>
<!-- ResultsPhase.tsx — specific locations to change: -->

<!-- 1. QuoteResultCard getBorderColor() (lines 67-74) — border colors for result cards: -->
case 'diamond': return 'border-cyan-500';   /* KEEP — matches badge system */
case 'gold': return 'border-amber-500';     /* KEEP — matches badge system */
case 'agreed': return 'border-green-500';   → 'border-cyan-400'   /* align with swipe agree color */
case 'disagreed': return 'border-red-400';  → 'border-amber-400'  /* align with swipe disagree color */

<!-- 2. QuoteResultCard getStatusBadge() 'agreed' case (lines 94-100): -->
<!-- Before: bg-green-100 text-green-700 with green checkmark icon -->
<!-- After: bg-cyan-50 text-cyan-700 (matches agree/swipe-right color) -->

<!-- 3. QuoteResultCard getStatusBadge() 'disagreed' case (lines 101-111): -->
<!-- Before: bg-red-100 text-red-600 with red X icon -->
<!-- After: bg-amber-50 text-amber-700 (matches disagree/swipe-left color) -->

<!-- 4. ResultsPhase stats grid (lines 279-311): -->
<!-- Summary stats container: bg-ev-light-blue bg-opacity-10 → bg-ev-muted-blue/10 -->
<!-- Agreed stat value: text-green-600 → text-cyan-700 (matches badge) -->
<!-- Disagreed stat value: text-red-500 → text-amber-700 (matches badge) -->

<!-- 5. Source link in QuoteResultCard (line 158): -->
<!-- Before: text-ev-light-blue hover:text-ev-teal -->
<!-- After: text-ev-muted-blue hover:text-ev-coral -->

<!-- 6. "View Your Alignment" CTA button (line 171-179): -->
<!-- Before: bg-ev-light-blue hover:bg-ev-teal text-white font-manrope font-semibold rounded-lg -->
<!-- After: bg-ev-coral hover:bg-red-600 text-white font-manrope font-semibold rounded-xl -->

<!-- 7. "Explore More Issues" button (lines 334-353): -->
<!-- Before: from-ev-light-blue to-ev-muted-blue ... shadow-ev-light-blue/30 hover:shadow-ev-light-blue/40 -->
<!-- After: bg-ev-coral hover:bg-red-600 (remove gradient shimmer — simpler per CompassV2 primary button) -->
<!-- Keep the whileHover/whileTap/motion shimmer span IDENTICAL — only className changes -->
<!-- Per CONTEXT.md: "View on Essentials" CTA uses ev-coral — this is the equivalent back-to-issues CTA -->

<!-- ProgressHeader.tsx — current classes (need to audit): -->
<!-- back button likely uses text-ev-light-blue hover:text-ev-coral → update to text-ev-muted-blue hover:text-ev-coral -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ResultsPhase verdict badges, CTAs, and accent colors</name>
  <files>EV-ReadRank/src/components/ResultsPhase.tsx</files>
  <action>
Apply EV brand to ResultsPhase, working through the specific change locations documented in the `<interfaces>` block.

**QuoteResultCard — getBorderColor() function:**
- `case 'agreed': return 'border-cyan-400'` (was border-green-500)
- `case 'disagreed': return 'border-amber-400'` (was border-red-400)
- Keep `border-cyan-500` for diamond and `border-amber-500` for gold unchanged

**QuoteResultCard — getStatusBadge() function:**
- `agreed` case: `bg-cyan-50 text-cyan-700` (was bg-green-100 text-green-700). SVG icon color class: `stroke` is inherited via `stroke="currentColor"` so only the wrapper text color matters
- `disagreed` case: `bg-amber-50 text-amber-700` (was bg-red-100 text-red-600). Same SVG pattern

**QuoteResultCard — QuoteResultCard render, source link:**
- `text-ev-light-blue hover:text-ev-teal` → `text-ev-muted-blue hover:text-ev-coral`

**QuoteResultCard — "View Your Alignment" button:**
- `bg-ev-light-blue hover:bg-ev-teal text-white font-manrope font-semibold rounded-lg` → `bg-ev-coral hover:bg-red-600 text-white font-manrope font-semibold rounded-xl`
- DO NOT touch the `onClick` handler or the SVG arrow

**ResultsPhase — Summary Stats container:**
- `bg-ev-light-blue bg-opacity-10` → `bg-ev-muted-blue/10`

**ResultsPhase — Stats grid value colors:**
- Agreed count: `text-green-600` → `text-cyan-700`
- Disagreed count: `text-red-500` → `text-amber-700`
- Keep `text-cyan-600` for Diamond count and `text-amber-600` for Gold count (already correct badge metaphor colors)

**ResultsPhase — "Explore More Issues" button:**
Replace the gradient button className with a solid ev-coral button. Keep all Framer Motion props (whileHover, whileTap, the shimmer motion.span inside) byte-for-byte identical. Only the outer motion.button className changes:
```tsx
className="relative overflow-hidden bg-ev-coral hover:bg-red-600 text-white font-manrope font-bold text-base md:text-lg px-8 py-3 rounded-xl shadow-lg transition-all duration-300"
```
(Remove `from-ev-light-blue`, `to-ev-muted-blue`, `hover:from-*`, `hover:to-*`, `shadow-ev-light-blue/30`, `hover:shadow-xl`, `hover:shadow-ev-light-blue/40`)

**QuoteResultCard header (candidate info area, line 129):**
- `bg-gray-50 border-b border-gray-100` — keep as-is (Claude's discretion: gray-50 header is clean and lets candidate photo/name be primary focus)

**Candidate photo sizing (Claude's discretion):**
- Current: `w-12 h-12 rounded-full` — keep at this size; it's proportionate and the name/office text provides sufficient identity. No change needed.

**Stats header layout (Claude's discretion):**
- Keep current 4-column grid; it's clean and the updated colors (cyan/amber for agreed/disagreed matching the badge system) create sufficient hierarchy.

DO NOT modify any motion.div, motion.button `initial`, `animate`, `transition`, `whileHover`, `whileTap` props.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-ReadRank && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. ResultsPhase.tsx contains no ev-light-blue or ev-teal references. CTA button uses bg-ev-coral. Agreed badges use text-cyan-700, disagreed badges use text-amber-700. Stats container uses bg-ev-muted-blue/10.</done>
</task>

<task type="auto">
  <name>Task 2: Update ProgressHeader back-button color</name>
  <files>EV-ReadRank/src/components/ProgressHeader.tsx</files>
  <action>
Read the current ProgressHeader.tsx file to identify the back-button className. Find the back-button element (likely contains `text-ev-light-blue` or `text-ev-muted-blue` and an arrow icon).

Update the back-button color to: `text-ev-muted-blue hover:text-ev-coral`

If the back-button currently uses `text-ev-light-blue hover:text-ev-coral`, change `ev-light-blue` to `ev-muted-blue`. If it already uses `ev-muted-blue`, no change needed.

Also check ProgressHeader for any other `ev-light-blue` or `ev-teal` usages and update to `ev-muted-blue`.

DO NOT change layout, padding, font-size, or any structural class. Only color token references change.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-ReadRank && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. ProgressHeader.tsx contains no ev-light-blue references. Back-button uses text-ev-muted-blue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 78 visual refresh in browser</name>
  <action>Human verification of complete Phase 78 visual refresh across all three surfaces. Dev server must be running.</action>
  <what-built>
Complete Phase 78 visual refresh:
- Plan 01: Token infrastructure, .ev-heading/.ev-button-primary CSS definitions, IssueHub ev-muted-blue accents
- Plan 02: White QuoteCard with muted-blue top border, amber/cyan swipe system, stack shadows
- Plan 03: ResultsPhase ev-coral CTAs, amber/cyan verdict badges, ev-muted-blue accents, ProgressHeader update
  </what-built>
  <how-to-verify>
1. Run the dev server: `cd EV-ReadRank && npm run dev`
2. Open http://localhost:5173

**IssueHub checks:**
- [ ] Page title "Choose an Issue" renders in Manrope bold, dark color (not gray/no-style)
- [ ] Overall Progress bar fills with ev-muted-blue (#00657c dark teal), not light blue
- [ ] Issue cards show ev-muted-blue hover border (hover over a card)
- [ ] In-progress issues show ev-muted-blue icon container background (teal tint)

**EvaluationPhase / QuoteCard checks:**
- [ ] Quote card renders as WHITE with a dark teal top border (not solid teal block)
- [ ] Progress bar at top shows coral/red-orange fill (not nothing/invisible)
- [ ] Drag a card left: swipe zone shows amber/brown gradient (not red)
- [ ] Drag a card right: swipe zone shows cyan/teal gradient (not green)
- [ ] Arrow indicators (swipe instructions) use amber for left, cyan for right
- [ ] Card snaps back correctly on partial swipe (animation unchanged)
- [ ] Card animates off-screen on full swipe (animation unchanged)

**ResultsPhase checks (complete an issue to reach results):**
- [ ] "View Your Alignment" buttons on each result card are coral/orange (not light blue)
- [ ] "Explore More Issues" button is coral/orange (not blue gradient)
- [ ] Agreed verdict badges show cyan/teal background and text
- [ ] Disagreed verdict badges show amber/brown background and text
- [ ] Stats container uses dark-teal/muted-blue tint background (not light blue)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
After Task 1 and Task 2:
1. `npm run build` exits 0
2. `grep -n "ev-light-blue\|ev-teal" EV-ReadRank/src/components/ResultsPhase.tsx` returns empty
3. `grep -n "ev-light-blue" EV-ReadRank/src/components/ProgressHeader.tsx` returns empty
4. `grep -n "bg-ev-coral" EV-ReadRank/src/components/ResultsPhase.tsx` returns at least 2 results (CTA + back button)

After human checkpoint:
5. User confirms visual checks pass
</verification>

<success_criteria>
- ResultsPhase has zero ev-light-blue or ev-teal references
- All CTA buttons use ev-coral
- Agreed/disagreed verdict badges use cyan-700/amber-700 text colors
- ProgressHeader back-button uses ev-muted-blue
- npm run build passes
- Human visual inspection approved
</success_criteria>

<output>
After completion, create `.planning/phases/78-visual-refresh/78-03-SUMMARY.md` using the summary template.
</output>
