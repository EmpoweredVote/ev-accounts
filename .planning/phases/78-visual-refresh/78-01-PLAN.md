---
phase: 78-visual-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-ReadRank/tailwind.config.js
  - EV-ReadRank/src/index.css
  - EV-ReadRank/src/components/IssueHub.tsx
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "Hub page headings render in Manrope bold with ev-black color"
    - "Primary accent on the hub page (progress bar, hover borders, icon containers) uses ev-muted-blue (#00657c), not ev-light-blue (#59b0c4)"
    - "Custom utility classes .ev-heading, .ev-text-primary, .ev-text-secondary, and .ev-button-primary produce visible styling (no phantom classes)"
    - "ev-teal and ev-dark-blue resolve as Tailwind utility classes (alias to #00657c)"
    - "npm run build completes with zero TypeScript/Tailwind compile errors"
  artifacts:
    - path: "EV-ReadRank/tailwind.config.js"
      provides: "ev-teal and ev-dark-blue token aliases in theme.extend.colors"
      contains: "ev-teal"
    - path: "EV-ReadRank/src/index.css"
      provides: "Definitions for .ev-heading, .ev-text-primary, .ev-text-secondary, .ev-button-primary"
      contains: ".ev-button-primary"
    - path: "EV-ReadRank/src/components/IssueHub.tsx"
      provides: "ev-muted-blue accent replacing ev-light-blue and ev-teal references"
      contains: "ev-muted-blue"
  key_links:
    - from: "EV-ReadRank/src/components/IssueHub.tsx"
      to: "EV-ReadRank/tailwind.config.js"
      via: "Tailwind utility class generation"
      pattern: "ev-muted-blue"
    - from: "EV-ReadRank/src/components/IssueHub.tsx"
      to: "EV-ReadRank/src/index.css"
      via: "Custom class definitions"
      pattern: "\\.ev-heading"
---

<objective>
Lay the CSS/token foundation for the Phase 78 visual refresh and apply EV brand design to the IssueHub (hub/landing page).

Purpose: All three DSGN surfaces depend on the token infrastructure being correct. Phantom utility classes (.ev-heading, .ev-text-primary, etc.) currently produce no visible styling. Missing Tailwind tokens (ev-teal, ev-dark-blue) silently render as nothing. These must be fixed before the other plans touch components that consume them. IssueHub is the first surface users see — refreshing it to use ev-muted-blue and CompassV2 card patterns establishes the visual direction.

Output: Defined CSS utility classes, resolved Tailwind token aliases, and an IssueHub that matches EV brand with ev-muted-blue accents throughout.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-visual-refresh/78-CONTEXT.md
@.planning/phases/78-visual-refresh/78-RESEARCH.md

<interfaces>
<!-- Key CSS class inventory from EV-ReadRank/src/index.css (current state) -->
<!-- These exist and work: -->
.ev-quote-card { background-color: #00657c; color: white; ... }   /* needs overhaul in Plan 02 */
.ev-quote-card-dragging { background-color: rgba(0, 101, 124, 0.65); }
.swipe-arrow-left { color: #ef4444; }    /* needs change in Plan 02 */
.swipe-arrow-right { color: #22c55e; }   /* needs change in Plan 02 */
.swipe-peek-top-left { background: #ef4444; }   /* needs change in Plan 02 */
.swipe-peek-top-right { background: #22c55e; }  /* needs change in Plan 02 */
.swipe-zone-disagree { background: linear-gradient(to right, #ef4444, #f87171); }
.swipe-zone-agree { background: linear-gradient(to left, #22c55e, #4ade80); }
.action-button-disagree { background-color: #fef2f2; border-color: #fecaca; color: #dc2626; }
.action-button-agree { background-color: #f0fdf4; border-color: #bbf7d0; color: #16a34a; }
.question-banner { background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%); }

<!-- These DO NOT EXIST (phantom — zero styling produced): -->
.ev-heading       /* used in IssueHub, ResultsPhase, ProgressHeader, CollectionPhase */
.ev-text-primary  /* used in IssueHub, ResultsPhase, CollectionPhase, EvaluationPhase */
.ev-text-secondary /* used in EvaluationPhase, ProgressHeader, PhaseNavigation */
.ev-button-primary /* used in EvaluationPhase, PhaseNavigation, CandidateAlignmentPage */

<!-- Tailwind tokens defined in tailwind.config.js (ev-teal and ev-dark-blue are ABSENT): -->
'ev-black': '#1c1c1c'
'ev-white': '#ffffff'
'ev-coral': '#ff5740'
'ev-muted-blue': '#00657c'
'ev-light-blue': '#59b0c4'
'ev-yellow': '#fed12e'
/* ev-teal: MISSING — used as bg-ev-teal, hover:bg-ev-teal, to-ev-teal in JSX */
/* ev-dark-blue: MISSING — used as text-ev-dark-blue in question banners */

<!-- IssueHub.tsx current ev-light-blue usages to replace with ev-muted-blue: -->
line 127: "bg-ev-light-blue bg-opacity-10"  → "bg-ev-muted-blue/10"
line 133: "text-ev-light-blue"              → "text-ev-muted-blue"
line 139: "from-ev-light-blue to-ev-teal"  → "from-ev-muted-blue to-ev-muted-blue" (or solid ev-muted-blue)
line 157: "hover:border-ev-light-blue"      → "hover:border-ev-muted-blue"
line 171: "bg-ev-light-blue bg-opacity-20 text-ev-light-blue"  → "bg-ev-muted-blue/10 text-ev-muted-blue"
line 203: "bg-ev-light-blue" (progress bar fill)  → "bg-ev-muted-blue"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define missing CSS utility classes and add Tailwind token aliases</name>
  <files>EV-ReadRank/tailwind.config.js, EV-ReadRank/src/index.css</files>
  <action>
**tailwind.config.js** — add two color aliases under `theme.extend.colors` to resolve phantom Tailwind utilities:
```js
'ev-teal': '#00657c',       // alias — resolves to-ev-teal, hover:bg-ev-teal, bg-ev-teal usages
'ev-dark-blue': '#00657c',  // alias — resolves text-ev-dark-blue in question banners
```
Both point to the same value as ev-muted-blue. This is intentional: they are legacy names in JSX that couldn't be replaced because they were undefined.

**index.css** — add definitions for the four phantom utility classes AFTER the existing `.ev-quote-card` block (around line 101). Use CompassV2 visual language as the reference:

```css
.ev-heading {
  font-family: 'Manrope', sans-serif;
  font-weight: 700;
  color: #1c1c1c;
}

.ev-text-primary {
  font-family: 'Manrope', sans-serif;
  color: #374151; /* gray-700 */
}

.ev-text-secondary {
  font-family: 'Manrope', sans-serif;
  color: #6b7280; /* gray-500 */
}

.ev-button-primary {
  background-color: #ff5740; /* ev-coral */
  color: white;
  font-family: 'Manrope', sans-serif;
  font-weight: 700;
  border-radius: 0.75rem;
  padding: 0.625rem 1.5rem;
  transition: background-color 0.2s ease;
  border: none;
  cursor: pointer;
}

.ev-button-primary:hover {
  background-color: #e64a34;
}
```

Also fix the `.sidebar-quote-card` background (line 540 in current index.css) — it uses `background-color: #00657c` (solid muted-blue) which will be jarring white cards context. Change to `background-color: #f8fafc` (slate-50) with `color: #1c1c1c` and `border: 1px solid #e2e8f0` to match card patterns. This sidebar card is visible during evaluation and should be consistent with the new white-card direction.

Do NOT touch any Framer Motion keyframe definitions (`@keyframes gentle-pulse`, `@keyframes rank-badge-appear`, `@keyframes card-lift`), `.animate-gentle-pulse`, or any swipe/action-button CSS (those are Plan 02's scope).
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-ReadRank && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `bg-ev-teal`, `text-ev-dark-blue`, `.ev-heading`, `.ev-text-primary`, `.ev-text-secondary`, `.ev-button-primary` all have real definitions that will produce visible output when used.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh IssueHub with ev-muted-blue accents and CompassV2 card patterns</name>
  <files>EV-ReadRank/src/components/IssueHub.tsx</files>
  <action>
Apply ev-muted-blue token as the primary accent throughout IssueHub. Replace all ev-light-blue and ev-teal references. Preserve ALL Framer Motion props character-for-character — only className strings change.

**Progress summary container (around line 126-145):**
- `bg-ev-light-blue bg-opacity-10` → `bg-ev-muted-blue/10`
- `text-ev-light-blue` → `text-ev-muted-blue`
- Progress bar gradient `from-ev-light-blue to-ev-teal` → solid `bg-ev-muted-blue` (remove gradient — simpler, consistent)

**Issue card button (around line 154-219):**
- `hover:border-ev-light-blue` → `hover:border-ev-muted-blue`
- In-progress icon container: `bg-ev-light-blue bg-opacity-20 text-ev-light-blue` → `bg-ev-muted-blue/10 text-ev-muted-blue`
- In-progress bar fill: `bg-ev-light-blue` → `bg-ev-muted-blue`

**Completed issue icon container:** Currently `bg-green-100 text-green-600` — change to `bg-ev-muted-blue/10 text-ev-muted-blue` to match in-progress state (Claude's discretion: unified muted-blue treatment for both active states; completed is indicated by the status badge alone). Not-started stays `bg-gray-100 text-gray-500`.

**Status badge (completed state):** Currently `bg-green-100 text-green-700`. Change to `bg-ev-muted-blue/10 text-ev-muted-blue` (consistent with icon treatment). In-progress stays `bg-amber-100 text-amber-700` (amber is correct for in-progress — it's not partisan, and creates visual hierarchy).

**Candidate Profiles footer text (line ~232-236):** Update copy to: `"Rate quotes on each issue to see your alignment with candidates on their Essentials profile."` — clearer integration hint (Claude's discretion for copy adjustment).

DO NOT modify: `initial`, `animate`, `transition`, `whileHover`, `whileTap` props on any motion element.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-ReadRank && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. IssueHub.tsx contains no ev-light-blue or ev-teal references. All accent colors use ev-muted-blue. All Framer Motion props are identical to pre-edit state.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` exits 0 with no TypeScript/CSS errors
2. `grep -n "ev-light-blue\|ev-teal" EV-ReadRank/src/components/IssueHub.tsx` returns empty (no remaining old tokens)
3. `grep -n "\.ev-button-primary\|\.ev-heading\|\.ev-text-primary\|\.ev-text-secondary" EV-ReadRank/src/index.css` returns 4 rule blocks
4. `grep -n "ev-teal\|ev-dark-blue" EV-ReadRank/tailwind.config.js` returns 2 entries in theme.extend.colors
</verification>

<success_criteria>
- tailwind.config.js has ev-teal and ev-dark-blue entries in theme.extend.colors pointing to #00657c
- index.css has definitions for .ev-heading, .ev-text-primary, .ev-text-secondary, .ev-button-primary
- IssueHub.tsx uses ev-muted-blue as its accent token throughout (zero ev-light-blue or legacy ev-teal references)
- npm run build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/78-visual-refresh/78-01-SUMMARY.md` using the summary template.
</output>
