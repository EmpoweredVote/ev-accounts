---
phase: 141-roster-lock-seed-records-headshots
plan: 09
type: execute
wave: 2
depends_on: [141-04]
files_modified:
  - backend/migrations/954_state_exec_headshots_batch_b.sql
autonomous: true
requirements: [SEXR-04]
must_haves:
  truths:
    - "Every newly-seeded exec in batch B (AR, GA, HI, IA, MO, ND, OK, VT — 35) has a headshot OR a documented honest-skip"
    - "Headshots are stored in the politician_photos bucket and recorded in essentials.politician_images (column url)"
    - "Audit-only migration 954 records every politician_images INSERT with source URL + license"
  artifacts:
    - path: "backend/migrations/954_state_exec_headshots_batch_b.sql"
      provides: "Audit-only record of batch-B headshot politician_images INSERTs"
      contains: "politician_images"
  key_links:
    - from: "essentials.politician_images.url"
      to: "essentials.politicians (batch-B external_ids)"
      via: "INSERT ... SELECT politician id by external_id, WHERE NOT EXISTS"
      pattern: "politician_photos/.*-headshot.jpg"
---

<objective>
Source, process, store, and record a headshot for every newly-seeded exec in batch B (AR, GA, HI, IA, MO, ND, OK, VT — 35 execs) via the migration-271 storage-mirror pattern.

Purpose: SEXR-04 for the batch-B states seeded by plan 141-04. Honest-skip is the documented fallback (McDowell precedent).
Output: headshots in the politician_photos bucket + audit-only migration 954.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md
@backend/migrations/271_md_executive_headshots.sql
@.planning/phases/141-roster-lock-seed-records-headshots/141-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source + process + upload headshots (batch B, 35 execs)</name>
  <files>backend/migrations/954_state_exec_headshots_batch_b.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (source priority; PIL crop-4:5-then-resize-600x750-LANCZOS-q90; bucket path {politician_id}-headshot.jpg; column `url`)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Reusable-Asset Playbook" Headshot Pattern + source priority + find-headshots no-disk-skill note)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-04-SUMMARY.md (batch-B external_ids + officeholder names from the seed plan)
  </read_first>
  <action>
    For each of the 35 batch-B newly-seeded execs (external_id in the -(fips*100000+seq) range for AR/GA/HI/IA/MO/ND/OK/VT), resolve the politician UUID by external_id. Source a portrait in priority order (one fetch per source): (1) official state .gov bio, (2) Ballotpedia, (3) Wikimedia Commons (public-domain/CC preferred). Process with Python PIL (crop 4:5 from top, then resize 600x750 LANCZOS q90). Upload to `politician_photos` as `{uuid}-headshot.jpg`. Capture source URL + license per exec. No portrait found → HONEST-SKIP (document in SUMMARY).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -c "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state IN ('AR','GA','HI','IA','MO','ND','OK','VT');" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each non-skipped batch-B exec has a `{uuid}-headshot.jpg` object in the politician_photos bucket (600x750, processed).
    - SUMMARY lists every exec with external_id, source URL, license, OR honest-skip + reason.
  </acceptance_criteria>
  <done>Headshots sourced + processed + uploaded for all batch-B execs with a portrait; honest-skips documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author audit-only migration 954 + verify politician_images rows</name>
  <files>backend/migrations/954_state_exec_headshots_batch_b.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (audit-only INSERT pattern: column `url`, type 'default', photo_license, WHERE NOT EXISTS, storage URL base, per-exec source comment)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-09-SUMMARY.md (Task 1 per-exec UUID + source URL + license)
  </read_first>
  <action>
    Create backend/migrations/954_state_exec_headshots_batch_b.sql (confirm next disk number; bump if taken) as AUDIT-ONLY mirroring migration 271. One idempotent `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT ... WHERE NOT EXISTS (...)` per non-skipped exec, resolving politician by external_id, with a per-exec source-URL comment. Document honest-skips as comments. Apply to prod with `-v ON_ERROR_STOP=1`; VERIFY visible row count for batch-B states = (35 minus honest-skips).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/954_state_exec_headshots_batch_b.sql 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Migration 954 applies to prod without error.
    - Post-apply: politician_images rows for batch-B states = (35 minus documented honest-skips).
    - Every image row uses column `url` and points to politician_photos/{uuid}-headshot.jpg.
    - Re-running migration 954 affects 0 rows.
  </acceptance_criteria>
  <done>Migration 954 applied; politician_images rows present for all batch-B execs with a headshot; honest-skips documented; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image host → storage mirror | Untrusted binary downloaded, processed, re-hosted |
| author → prod DB + Supabase Storage | INSERT + object upload to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Spoofing | Wrong-person portrait mirrored | mitigate | Source from officeholder's own .gov bio first; record source URL per exec |
| T-141-12 | Information Disclosure | Copyrighted image mirrored without license | mitigate | Prefer public-domain .gov / CC; record photo_license; honest-skip over unlicensed mirror |
| T-141-13 | Denial of Service | Image-fetch loop hangs session | mitigate | One fetch per source; honest-skip on exhaustion |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | Python PIL already in existing stack (no new package) |
</threat_model>

<verification>
After apply, batch-B portion of `verify-phase-141.sql` SEXR-04 should pass minus documented honest-skips.
</verification>

<success_criteria>
- All batch-B newly-seeded execs have a headshot OR documented honest-skip.
- Migration 954 (audit-only) applied + idempotent; uses column `url`.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-09-SUMMARY.md` when done.
</output>
