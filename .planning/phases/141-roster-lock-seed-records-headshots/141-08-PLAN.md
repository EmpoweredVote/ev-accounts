---
phase: 141-roster-lock-seed-records-headshots
plan: 08
type: execute
wave: 2
depends_on: [141-03, 141-02]
files_modified:
  - backend/migrations/953_state_exec_headshots_batch_a.sql
autonomous: true
requirements: [SEXR-04]
must_haves:
  truths:
    - "Every newly-seeded exec in batch A (AK, AL, FL, IL, MS, NC, NY, SD — 35) has a headshot OR a documented honest-skip"
    - "IN's newly-linked SoS (Morales) and Treasurer (Elliott) have a headshot OR documented honest-skip"
    - "Headshots are stored in the politician_photos bucket and recorded in essentials.politician_images (column url)"
    - "Audit-only migration 953 records every politician_images INSERT with source URL + license"
  artifacts:
    - path: "backend/migrations/953_state_exec_headshots_batch_a.sql"
      provides: "Audit-only record of batch-A + IN headshot politician_images INSERTs"
      contains: "politician_images"
  key_links:
    - from: "essentials.politician_images.url"
      to: "essentials.politicians (batch-A external_ids + IN 642977/688298)"
      via: "INSERT ... SELECT politician id by external_id, WHERE NOT EXISTS"
      pattern: "politician_photos/.*-headshot.jpg"
---

<objective>
Source, process, and store a headshot for every newly-seeded exec in batch A (AK, AL, FL, IL, MS, NC, NY, SD — 35 execs) plus Indiana's two newly-linked Big-5 execs (Diego Morales SoS, Daniel Elliott Treasurer), then record each in `essentials.politician_images` via an audit-only migration.

Purpose: SEXR-04 — every newly-seeded exec needs a headshot (official .gov / Ballotpedia / Wikimedia portrait, storage-mirrored). The find-headshots pattern is documented inline in migration 271 (Python PIL → Supabase Storage → politician_images INSERT); there is no on-disk find-headshots skill. Honest-skip (no image row) is the fallback when no portrait is found (McDowell precedent).
Output: headshots in the politician_photos bucket + migration 953 (audit-only) committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md

# Headshot mechanism (CONFIRMED): essentials.politician_images table, column `url` (NOT photo_origin_url);
# AUDIT-ONLY migration + live Python PIL crop/resize + Supabase Storage upload. The cleanest precedent:
@backend/migrations/271_md_executive_headshots.sql
# Seed plans that produced these politicians (external_id ranges, names, offices):
@.planning/phases/141-roster-lock-seed-records-headshots/141-03-SUMMARY.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source + process + upload headshots (batch A + IN, 37 execs)</name>
  <files>backend/migrations/953_state_exec_headshots_batch_a.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (full headshot workflow: source priority, PIL crop-4:5-then-resize-600x750-LANCZOS-q90, bucket path {politician_id}-headshot.jpg, storage URL base, column `url`)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Reusable-Asset Playbook" item 2 Headshot Pattern + item 3 find-headshots note + source priority list)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-03-SUMMARY.md (batch-A external_ids + officeholder names)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-02-SUMMARY.md (IN Morales/Elliott UUIDs + external_ids 642977/688298)
  </read_first>
  <action>
    For each of the 37 execs (35 batch-A newly-seeded with external_id in the -(fips*100000+seq) range for AK/AL/FL/IL/MS/NC/NY/SD + IN Morales 642977 + IN Elliott 688298), resolve the politician UUID from prod by external_id. Source a portrait in priority order (one fetch attempt per source, then move on): (1) official state .gov biography page, (2) Ballotpedia individual politician page, (3) Wikimedia Commons (prefer public-domain/CC). Process with Python PIL: crop to 4:5 from top BEFORE resize, resize to 600x750 LANCZOS q90 (never stretch). Upload to the `politician_photos` Supabase Storage bucket as `{politician_uuid}-headshot.jpg`. Capture the source URL + license per exec for the audit migration + SUMMARY. If no portrait found after all sources → HONEST-SKIP (no upload, no image row); record the exec + reason in the SUMMARY (McDowell precedent).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -c "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id IN (642977,688298) OR p.external_id <= -100000;" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each non-skipped exec has a `{uuid}-headshot.jpg` object in the politician_photos bucket.
    - Each uploaded image is 600x750 (4:5), processed (not a raw stretch).
    - SUMMARY lists every exec with: external_id, source URL, license, OR honest-skip + reason.
    - Honest-skip count is documented (expected to be low; brand-new execs may lack portraits).
  </acceptance_criteria>
  <done>Headshots sourced + processed + uploaded for all batch-A + IN execs with a portrait; honest-skips documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author audit-only migration 953 + verify politician_images rows</name>
  <files>backend/migrations/953_state_exec_headshots_batch_a.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (audit-only INSERT pattern: column `url`, type 'default', photo_license, WHERE NOT EXISTS guard, storage URL base, per-exec source comment)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-08-SUMMARY.md (this plan's Task 1 output: per-exec UUID + source URL + license)
  </read_first>
  <action>
    Create backend/migrations/953_state_exec_headshots_batch_a.sql (confirm next disk number; bump if taken) as an AUDIT-ONLY migration mirroring migration 271: header documents it captures live politician_images INSERTs (actual writes happened live during Task 1 upload + the INSERTs run here are idempotent). For each non-skipped exec, `INSERT INTO essentials.politician_images (id, politician_id, url, type, photo_license) SELECT gen_random_uuid(), (SELECT id FROM essentials.politicians WHERE external_id = X), '<storage url>/{uuid}-headshot.jpg', 'default', '<license>' WHERE NOT EXISTS (...)`. Include a per-exec comment with the source URL. Document honest-skips as comments (no INSERT). Apply to prod with `-v ON_ERROR_STOP=1`. VERIFY visible rows: count politician_images for batch-A + IN execs equals (37 minus honest-skips).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/953_state_exec_headshots_batch_a.sql 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Migration 953 applies to prod without error.
    - Post-apply: count of essentials.politician_images rows for batch-A + IN execs = (37 minus documented honest-skips).
    - Every image row uses column `url` (not photo_origin_url) and points to politician_photos/{uuid}-headshot.jpg.
    - Re-running migration 953 affects 0 rows (WHERE NOT EXISTS guard).
  </acceptance_criteria>
  <done>Migration 953 applied; politician_images rows present for all batch-A + IN execs with a headshot; honest-skips documented; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image host (.gov/Ballotpedia/Wikimedia) → storage mirror | Untrusted binary downloaded, processed, re-hosted; wrong-person or copyrighted image is the risk |
| author → prod DB + Supabase Storage | INSERT + object upload to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Spoofing | Wrong-person portrait mirrored (mismatched name) | mitigate | Source from the officeholder's own .gov bio page first; record source URL per exec in audit migration for review |
| T-141-12 | Information Disclosure | Copyrighted image mirrored without license | mitigate | Prefer public-domain .gov / CC Wikimedia; record photo_license per row; honest-skip rather than mirror unlicensed |
| T-141-13 | Denial of Service | Image-fetch loop hangs the session | mitigate | One fetch attempt per source, then move on (efficiency rule); honest-skip on exhaustion |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | Python PIL/Pillow already used in prior headshot migrations (existing stack, no new package per RESEARCH §Environment) |
</threat_model>

<verification>
After apply, run the SEXR-04 portion of the gate (it covers all newly-seeded execs across batches; batch-A + IN should now pass minus documented honest-skips): `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql`.
</verification>

<success_criteria>
- All batch-A + IN newly-seeded execs have a headshot in politician_images OR a documented honest-skip.
- Migration 953 (audit-only) applied + idempotent; uses column `url`.
- Honest-skips documented with reasons.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-08-SUMMARY.md` when done.
</output>
