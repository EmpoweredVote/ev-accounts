---
phase: 141-roster-lock-seed-records-headshots
plan: 11
type: execute
wave: 2
depends_on: [141-06]
files_modified:
  - backend/migrations/956_state_exec_headshots_batch_d.sql
autonomous: true
requirements: [SEXR-04]
must_haves:
  truths:
    - "Every newly-seeded exec in batch D (CT, KY, MN, NH, NV, RI, TN, WI, WV — 35) has a headshot OR a documented honest-skip"
    - "Headshots are stored in the politician_photos bucket and recorded in essentials.politician_images (column url)"
    - "Audit-only migration 956 records every politician_images INSERT with source URL + license"
  artifacts:
    - path: "backend/migrations/956_state_exec_headshots_batch_d.sql"
      provides: "Audit-only record of batch-D headshot politician_images INSERTs"
      contains: "politician_images"
  key_links:
    - from: "essentials.politician_images.url"
      to: "essentials.politicians (batch-D external_ids)"
      via: "INSERT ... SELECT politician id by external_id, WHERE NOT EXISTS"
      pattern: "politician_photos/.*-headshot.jpg"
---

<objective>
Source, process, store, and record a headshot for every newly-seeded exec in batch D (CT, KY, MN, NH, NV, RI, TN, WI, WV — 35 execs) via the migration-271 storage-mirror pattern.

Purpose: SEXR-04 for the batch-D states seeded by plan 141-06. Honest-skip is the documented fallback.
Output: headshots in the politician_photos bucket + audit-only migration 956.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md
@backend/migrations/271_md_executive_headshots.sql
@.planning/phases/141-roster-lock-seed-records-headshots/141-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source + process + upload headshots (batch D, 35 execs)</name>
  <files>backend/migrations/956_state_exec_headshots_batch_d.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (source priority; PIL crop-4:5-then-resize-600x750-LANCZOS-q90; bucket path; column `url`)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Reusable-Asset Playbook" Headshot Pattern + source priority + find-headshots no-disk-skill note)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-06-SUMMARY.md (batch-D external_ids + officeholder names)
  </read_first>
  <action>
    For each of the 35 batch-D newly-seeded execs (external_id in the -(fips*100000+seq) range for CT/KY/MN/NH/NV/RI/TN/WI/WV), resolve the politician UUID by external_id. Source a portrait in priority order (one fetch per source): (1) state .gov bio, (2) Ballotpedia, (3) Wikimedia Commons (public-domain/CC). Process with Python PIL (crop 4:5 from top, resize 600x750 LANCZOS q90). Upload to `politician_photos` as `{uuid}-headshot.jpg`. Capture source URL + license per exec. No portrait → HONEST-SKIP (document in SUMMARY).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -c "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state IN ('CT','KY','MN','NH','NV','RI','TN','WI','WV');" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each non-skipped batch-D exec has a `{uuid}-headshot.jpg` object in the politician_photos bucket (600x750, processed).
    - SUMMARY lists every exec with external_id, source URL, license, OR honest-skip + reason.
  </acceptance_criteria>
  <done>Headshots sourced + processed + uploaded for all batch-D execs with a portrait; honest-skips documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author audit-only migration 956 + verify politician_images rows</name>
  <files>backend/migrations/956_state_exec_headshots_batch_d.sql</files>
  <read_first>
    - backend/migrations/271_md_executive_headshots.sql (audit-only INSERT pattern: column `url`, WHERE NOT EXISTS, storage URL base, per-exec source comment)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-11-SUMMARY.md (Task 1 per-exec UUID + source URL + license)
  </read_first>
  <action>
    Create backend/migrations/956_state_exec_headshots_batch_d.sql (confirm next disk number; bump if taken) as AUDIT-ONLY mirroring migration 271. One idempotent politician_images INSERT per non-skipped exec (resolve politician by external_id; WHERE NOT EXISTS; per-exec source-URL comment). Document honest-skips as comments. Apply to prod with `-v ON_ERROR_STOP=1`; VERIFY visible row count for batch-D states = (35 minus honest-skips).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/956_state_exec_headshots_batch_d.sql 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Migration 956 applies to prod without error.
    - Post-apply: politician_images rows for batch-D states = (35 minus documented honest-skips).
    - Every image row uses column `url` and points to politician_photos/{uuid}-headshot.jpg.
    - Re-running migration 956 affects 0 rows.
  </acceptance_criteria>
  <done>Migration 956 applied; politician_images rows present for all batch-D execs with a headshot; honest-skips documented; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image host → storage mirror | Untrusted binary downloaded, processed, re-hosted |
| author → prod DB + Supabase Storage | INSERT + object upload to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Spoofing | Wrong-person portrait mirrored | mitigate | Source from officeholder's own .gov bio first; record source URL per exec |
| T-141-12 | Information Disclosure | Copyrighted image mirrored without license | mitigate | Prefer public-domain .gov / CC; record photo_license; honest-skip over unlicensed mirror |
| T-141-13 | Denial of Service | Image-fetch loop hangs session | mitigate | One fetch per source; honest-skip on exhaustion |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | Python PIL already in existing stack (no new package) |
</threat_model>

<verification>
After apply, batch-D portion of `verify-phase-141.sql` SEXR-04 should pass minus documented honest-skips.
</verification>

<success_criteria>
- All batch-D newly-seeded execs have a headshot OR documented honest-skip.
- Migration 956 (audit-only) applied + idempotent; uses column `url`.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-11-SUMMARY.md` when done.
</output>
