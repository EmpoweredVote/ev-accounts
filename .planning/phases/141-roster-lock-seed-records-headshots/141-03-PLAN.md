---
phase: 141-roster-lock-seed-records-headshots
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/migrations/948_state_exec_seed_batch_a.sql
autonomous: true
requirements: [SEXR-01, SEXR-02, SEXR-03]
must_haves:
  truths:
    - "All 35 in-scope Big-5 offices for AK, AL, FL, IL, MS, NC, NY, SD are seeded (chambers + districts + politicians + offices)"
    - "Every newly-seeded district has uppercase state and non-empty geo_id (FIPS string)"
    - "Every newly-seeded office has role_canonical set (incl. FL CFO + NY Comptroller → treasurer)"
    - "Every officeholder name is verified current as-of 2026-06 from a live Wikipedia/.gov source"
    - "external_id is collision-free per the -(fips*100000+seq) scheme with preflight assertion"
  artifacts:
    - path: "backend/migrations/948_state_exec_seed_batch_a.sql"
      provides: "Seed of 35 Big-5 offices across AK, AL, FL, IL, MS, NC, NY, SD"
      contains: "STATE_EXEC"
  key_links:
    - from: "essentials.offices (batch A)"
      to: "essentials.districts (STATE_EXEC) + essentials.chambers + essentials.politicians"
      via: "CTE politician+office insert per office, idempotent"
      pattern: "role_canonical = '(governor|lt_governor|attorney_general|secretary_of_state|treasurer)'"
---

<objective>
Seed every missing elected Big-5 record (politician + office + chamber + district) for batch A: **AK, AL, FL, IL, MS, NC, NY, SD** — 35 in-scope offices — idempotently, with `role_canonical` set, uppercase state, non-empty geo_id, and collision-free external_ids.

Purpose: SEXR-02 core. These 8 states currently have zero STATE_EXEC records. The validated 50-state matrix (141-RESEARCH.md) is the authoritative roster — seed ONLY the in-scope offices it names (no phantom offices).
Output: migration 948 applied to prod with 35 rows verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md

# Seed pattern (chambers → per-office districts → CTE politician+office → office_id backfill → assertions):
@backend/migrations/269_md_government_chambers.sql
@backend/migrations/270_md_state_executives.sql
</context>

<interfaces>
Batch A in-scope offices (per the validated matrix in 141-RESEARCH.md §"Validated 50-State Elected Big-5 Matrix"):
- AK (FIPS 02): Governor, Lt Governor (TICKET) — 2 offices  [AG/SoS/Treasurer appointed/none → OUT]
- AL (FIPS 01): Governor, Lt Governor (sep), Attorney General, Secretary of State, Treasurer — 5
- FL (FIPS 12): Governor, Lt Governor (TICKET), Attorney General, CFO(=treasurer, D-01) — 4  [SoS appointed → OUT; CFO keeps title 'Chief Financial Officer', role_canonical='treasurer']
- IL (FIPS 17): Governor, Lt Governor, Attorney General, Secretary of State, Treasurer — 5
- MS (FIPS 28): Governor, Lt Governor (sep), Attorney General, Secretary of State, Treasurer — 5
- NC (FIPS 37): Governor, Lt Governor (sep), Attorney General, Secretary of State, Treasurer — 5
- NY (FIPS 36): Governor, Lt Governor (TICKET), Attorney General, Comptroller(=treasurer, D-01) — 4  [SoS appointed → OUT; Comptroller keeps title, role_canonical='treasurer']
- SD (FIPS 46): Governor, Lt Governor (TICKET), Attorney General, Secretary of State, Treasurer — 5
Total = 35 offices. external_id = -(fips*100000 + seq), seq per office 1..N within each state.
role_canonical values: governor, lt_governor, attorney_general, secretary_of_state, treasurer.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify current officeholders (AK, AL, FL, IL — 16 offices) + draft migration first half</name>
  <files>backend/migrations/948_state_exec_seed_batch_a.sql</files>
  <read_first>
    - backend/migrations/270_md_state_executives.sql (CTE pattern + per-office district + post-insert assertions)
    - backend/migrations/269_md_government_chambers.sql (chamber insert; slug GENERATED ALWAYS; government via name+state)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§matrix for the in-scope office list; §"Common Pitfalls" Pitfall 5 stale governors; §"external_id Collision Preflight Design")
    - .planning/phases/141-roster-lock-seed-records-headshots/141-CONTEXT.md (D-01 CFO/Comptroller→treasurer keep title; D-03/04 external_id scheme + preflight; D-10 uppercase state + non-empty geo_id; D-11 Wikipedia primary + .gov cross-check; D-12 as-of 2026-06)
  </read_first>
  <action>
    For each in-scope office in AK, AL, FL, IL (16 offices), confirm the CURRENT officeholder name + party as-of 2026-06 from a live source (D-11/D-12): WebFetch the relevant Wikipedia "List of current..." article or the state .gov biography page (Pitfall 5 — do NOT rely on training memory; 2024 gubernatorial winners inaugurated Jan 2025/2026). One fetch attempt per source, then move on (efficiency rule). Capture a source URL per office for the SUMMARY.

    Begin authoring backend/migrations/948_state_exec_seed_batch_a.sql (confirm next disk number; bump if taken). Wrap whole batch in one BEGIN/COMMIT. For AK, AL, FL, IL:
    - PREFLIGHT per state (D-04): assert each computed external_id -(fips*100000+seq) is < -56000 AND not already present (use the range-check from 141-RESEARCH.md §"Preflight SQL to embed").
    - Government preflight: assert the 'State of {Name}' government row exists for the state (these were seeded by migration 174 as 50-state stubs; use name+state lookup; if any state shows duplicates like IN, fall back to a canonical-UUID resolve — unlikely outside IN but assert COUNT >= 1 to be safe).
    - Chambers: one per in-scope office, names per the office title ('Governor','Lieutenant Governor','Attorney General','Secretary of State','Treasurer', and for FL 'Chief Financial Officer', for NY 'Comptroller'). slug excluded (GENERATED ALWAYS). Guard WHERE NOT EXISTS (name, government_id).
    - Districts: one labeled STATE_EXEC district per office, state=UPPERCASE postal, geo_id=FIPS string (AK='02', AL='01', FL='12', IL='17'), label='{State Name} {Office Title}', district_id='', mtfcc=''. Guard WHERE NOT EXISTS (district_type, state, label).
    - Politicians + offices: CTE per office (migration 270 pattern), external_id=-(fips*100000+seq), is_active=true, is_incumbent=true, is_appointed=false (all in-scope are elected), party from the verified source. Office row carries title (real title; FL CFO keeps 'Chief Financial Officer', NY Comptroller keeps 'Comptroller' per D-01/D-07), representing_state, is_appointed_position=false, role_canonical set to the canonical value (FL CFO + NY Comptroller → 'treasurer').
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && grep -v '^--' backend/migrations/948_state_exec_seed_batch_a.sql | grep -c "INSERT INTO essentials.offices"</automated>
  </verify>
  <acceptance_criteria>
    - Migration 948 contains office inserts for all 16 AK/AL/FL/IL in-scope offices.
    - Each office has an explicit role_canonical value (not NULL) in the INSERT.
    - FL Chief Financial Officer and (in Task 2) NY Comptroller offices have role_canonical='treasurer' with their real title preserved.
    - Each state has an external_id preflight assertion block.
    - SUMMARY captures a source URL per verified officeholder.
  </acceptance_criteria>
  <done>First-half of migration 948 drafted with AK/AL/FL/IL chambers+districts+politicians+offices; officeholders live-verified with source URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Verify officeholders (MS, NC, NY, SD — 19 offices) + finish migration + apply + verify rows</name>
  <files>backend/migrations/948_state_exec_seed_batch_a.sql</files>
  <read_first>
    - backend/migrations/948_state_exec_seed_batch_a.sql (the in-progress file from Task 1 — continue it)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§matrix; §"Architecture Patterns" Pattern 3 post-insert state/geo_id assertion)
  </read_first>
  <action>
    Live-verify current officeholders for MS, NC, NY, SD (19 offices) per D-11/D-12 (one fetch per source). Append their chambers + districts + politicians + offices to migration 948 using the same pattern as Task 1. geo_id: MS='28', NC='37', NY='36', SD='46'. NY Comptroller → role_canonical='treasurer', title 'Comptroller'. NC has separately-elected Lt Governor + full Big-5 (5 offices). SD Lt Gov is on TICKET.

    Add the office_id backfill UPDATE scoped to the batch's external_id ranges (migration 270 STEP 3 pattern), guarded WHERE office_id IS NULL.

    Add POST-INSERT ASSERTIONS before COMMIT (Pattern 3, scoped to this batch's 8 states): (a) zero STATE_EXEC districts in (AK,AL,FL,IL,MS,NC,NY,SD) with state != upper(state); (b) zero with geo_id IS NULL OR geo_id=''; (c) count of in-scope offices seeded for these 8 states = 35; (d) zero in-scope offices in these states with role_canonical IS NULL.

    APPLY to prod: run the migration with `-v ON_ERROR_STOP=1`. Then VERIFY rows are visible (a build/typecheck does NOT prove seeding): run `SELECT state, count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state IN ('AK','AL','FL','IL','MS','NC','NY','SD') AND o.role_canonical IS NOT NULL GROUP BY state ORDER BY state` and confirm per-state counts match the matrix (AK=2, AL=5, FL=4, IL=5, MS=5, NC=5, NY=4, SD=5).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/948_state_exec_seed_batch_a.sql 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Migration 948 applies to prod without error inside BEGIN/COMMIT.
    - Post-apply per-state office counts: AK=2, AL=5, FL=4, IL=5, MS=5, NC=5, NY=4, SD=5 (total 35), all with role_canonical NOT NULL.
    - Post-apply: zero batch-A STATE_EXEC districts with lowercase state or empty geo_id.
    - NY Comptroller + FL CFO offices have role_canonical='treasurer'.
    - Re-running migration 948 affects 0 rows (idempotent).
  </acceptance_criteria>
  <done>Migration 948 applied to prod; 35 batch-A offices seeded + role-tagged; uppercase/geo_id assertions pass; idempotent re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live source (Wikipedia/.gov) → seed data | Officeholder identity sourced externally; staleness is the correctness risk |
| author → prod DB | SQL migration applied to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-07 | Spoofing | Stale officeholder (2024-elected governor not in training data) seeded as outgoing incumbent | mitigate | D-11/D-12 mandatory live fetch per officeholder; source URL captured per office; Pitfall 5 |
| T-141-08 | Tampering | Phantom office seeded (e.g. FL SoS which is appointed) | mitigate | Seed ONLY the matrix-named in-scope offices; post-insert count assertion = 35 |
| T-141-09 | Information Disclosure | Lowercase state or empty geo_id makes execs invisible to feed (223a defect) | mitigate | Uppercase state + FIPS geo_id literal; Pattern 3 post-insert assertion |
| T-141-10 | Tampering | external_id collision corrupts an existing politician | mitigate | D-04 per-state preflight (< -56000 AND not present) before any insert |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure SQL) |
</threat_model>

<verification>
Per-batch quick count after apply: `SELECT state, COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state IN ('AK','AL','FL','IL','MS','NC','NY','SD') GROUP BY state ORDER BY state`. Full gate (SEXR-02/03 partial) runs after all seed batches: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql`.
</verification>

<success_criteria>
- 35 batch-A Big-5 offices seeded across AK, AL, FL, IL, MS, NC, NY, SD with role_canonical set.
- All districts uppercase state + non-empty geo_id; FL CFO + NY Comptroller mapped to treasurer.
- Officeholders live-verified with source URLs.
- Migration 948 idempotent.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-03-SUMMARY.md` when done.
</output>
