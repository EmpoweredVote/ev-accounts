---
phase: 141-roster-lock-seed-records-headshots
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/949_state_exec_seed_batch_b.sql
autonomous: true
requirements: [SEXR-01, SEXR-02, SEXR-03]
must_haves:
  truths:
    - "All 35 in-scope Big-5 offices for AR, GA, HI, IA, MO, ND, OK, VT are seeded (chambers + districts + politicians + offices)"
    - "Every newly-seeded district has uppercase state and non-empty geo_id"
    - "Every newly-seeded office has role_canonical set"
    - "Every officeholder name is verified current as-of 2026-06 from a live source"
    - "external_id is collision-free per the -(fips*100000+seq) scheme with preflight assertion"
  artifacts:
    - path: "backend/migrations/949_state_exec_seed_batch_b.sql"
      provides: "Seed of 35 Big-5 offices across AR, GA, HI, IA, MO, ND, OK, VT"
      contains: "STATE_EXEC"
  key_links:
    - from: "essentials.offices (batch B)"
      to: "essentials.districts + chambers + politicians"
      via: "CTE politician+office insert per office, idempotent"
      pattern: "role_canonical = '(governor|lt_governor|attorney_general|secretary_of_state|treasurer)'"
---

<objective>
Seed every missing elected Big-5 record for batch B: **AR, GA, HI, IA, MO, ND, OK, VT** — 35 in-scope offices — idempotently, with `role_canonical` set, uppercase state, non-empty geo_id, collision-free external_ids.

Purpose: SEXR-02 core. These 8 states currently have zero STATE_EXEC records. Seed ONLY the matrix-named in-scope offices.
Output: migration 949 applied to prod with 35 rows verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md
@backend/migrations/269_md_government_chambers.sql
@backend/migrations/270_md_state_executives.sql
</context>

<interfaces>
Batch B in-scope offices (per validated matrix in 141-RESEARCH.md):
- AR (FIPS 05): Governor, Lt Gov (TICKET), AG, SoS, Treasurer — 5
- GA (FIPS 13): Governor, Lt Gov (TICKET), AG, SoS — 4  [Treasurer appointed → OUT]
- HI (FIPS 15): Governor, Lt Gov (TICKET) — 2  [AG appointed; SoS none; Treasurer appointed → OUT]
- IA (FIPS 19): Governor, Lt Gov (TICKET), AG, SoS, Treasurer — 5
- MO (FIPS 29): Governor, Lt Gov (TICKET), AG, SoS, Treasurer — 5
- ND (FIPS 38): Governor, Lt Gov (TICKET), AG, SoS, Treasurer — 5
- OK (FIPS 40): Governor, Lt Gov (TICKET), AG, Treasurer — 4  [SoS appointed → OUT]
- VT (FIPS 50): Governor, Lt Gov (TICKET), AG, SoS, Treasurer — 5
Total = 35. external_id = -(fips*100000 + seq). role_canonical values: governor, lt_governor, attorney_general, secretary_of_state, treasurer.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify officeholders (AR, GA, HI, IA — 16 offices) + draft migration first half</name>
  <files>backend/migrations/949_state_exec_seed_batch_b.sql</files>
  <read_first>
    - backend/migrations/270_md_state_executives.sql
    - backend/migrations/269_md_government_chambers.sql
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§matrix; §"Common Pitfalls" Pitfall 5; §"external_id Collision Preflight Design")
    - .planning/phases/141-roster-lock-seed-records-headshots/141-CONTEXT.md (D-03/04 external_id+preflight; D-10 uppercase+geo_id; D-11/D-12 sourcing)
  </read_first>
  <action>
    Live-verify current officeholders for AR, GA, HI, IA (16 offices) per D-11/D-12 (one fetch per source; Pitfall 5). Begin authoring backend/migrations/949_state_exec_seed_batch_b.sql (confirm next disk number; bump if taken), wrapped in one BEGIN/COMMIT. For each state: external_id preflight (D-04, range check) → government-row preflight (COUNT >= 1 by name+state) → chambers (one per office, slug excluded) → labeled STATE_EXEC districts (uppercase state, geo_id FIPS string: AR='05', GA='13', HI='15', IA='19', label='{State} {Office}') → CTE politician+office (external_id=-(fips*100000+seq), elected flags, role_canonical set). Follow migration 270 idempotent guards exactly.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && grep -v '^--' backend/migrations/949_state_exec_seed_batch_b.sql | grep -c "INSERT INTO essentials.offices"</automated>
  </verify>
  <acceptance_criteria>
    - Migration 949 contains office inserts for all 16 AR/GA/HI/IA in-scope offices, each with explicit non-null role_canonical.
    - Each state has an external_id preflight assertion block.
    - SUMMARY captures a source URL per verified officeholder.
  </acceptance_criteria>
  <done>First-half of migration 949 drafted (AR/GA/HI/IA); officeholders live-verified with source URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Verify officeholders (MO, ND, OK, VT — 19 offices) + finish migration + apply + verify rows</name>
  <files>backend/migrations/949_state_exec_seed_batch_b.sql</files>
  <read_first>
    - backend/migrations/949_state_exec_seed_batch_b.sql (in-progress from Task 1)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§matrix; §"Architecture Patterns" Pattern 3 post-insert assertion)
  </read_first>
  <action>
    Live-verify officeholders for MO, ND, OK, VT (19 offices). Append chambers+districts+politicians+offices to migration 949. geo_id: MO='29', ND='38', OK='40', VT='50'. Add office_id backfill UPDATE scoped to the batch external_id ranges (guard office_id IS NULL). Add POST-INSERT ASSERTIONS scoped to (AR,GA,HI,IA,MO,ND,OK,VT): zero lowercase state, zero empty geo_id, in-scope office count = 35, zero role_canonical NULL. APPLY to prod with `-v ON_ERROR_STOP=1`, then VERIFY visible rows: `SELECT state, count(*) ... GROUP BY state` matches matrix (AR=5, GA=4, HI=2, IA=5, MO=5, ND=5, OK=4, VT=5).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/949_state_exec_seed_batch_b.sql 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Migration 949 applies to prod without error inside BEGIN/COMMIT.
    - Post-apply per-state counts: AR=5, GA=4, HI=2, IA=5, MO=5, ND=5, OK=4, VT=5 (total 35), all role_canonical NOT NULL.
    - Zero batch-B districts with lowercase state or empty geo_id.
    - Re-running migration 949 affects 0 rows.
  </acceptance_criteria>
  <done>Migration 949 applied to prod; 35 batch-B offices seeded + role-tagged; assertions pass; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live source → seed data | Officeholder identity sourced externally |
| author → prod DB | SQL migration applied to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-07 | Spoofing | Stale officeholder seeded | mitigate | D-11/D-12 live fetch per officeholder; source URL per office |
| T-141-08 | Tampering | Phantom office (GA Treasurer / HI AG / OK SoS appointed) seeded | mitigate | Seed only matrix-named offices; count assertion = 35 |
| T-141-09 | Information Disclosure | Lowercase state / empty geo_id hides execs | mitigate | Uppercase + FIPS geo_id; Pattern 3 assertion |
| T-141-10 | Tampering | external_id collision | mitigate | D-04 per-state preflight |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
Quick count after apply: `SELECT state, COUNT(*) FROM essentials.districts WHERE district_type='STATE_EXEC' AND state IN ('AR','GA','HI','IA','MO','ND','OK','VT') GROUP BY state ORDER BY state`.
</verification>

<success_criteria>
- 35 batch-B Big-5 offices seeded across AR, GA, HI, IA, MO, ND, OK, VT with role_canonical set.
- All districts uppercase state + non-empty geo_id.
- Officeholders live-verified with source URLs.
- Migration 949 idempotent.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-04-SUMMARY.md` when done.
</output>
