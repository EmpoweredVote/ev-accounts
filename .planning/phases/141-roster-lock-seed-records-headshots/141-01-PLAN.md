---
phase: 141-roster-lock-seed-records-headshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-phase-141.sql
  - backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql
autonomous: true
requirements: [SEXR-01, SEXR-03]
must_haves:
  truths:
    - "verify-phase-141.sql exists and asserts SEXR-01..04 + D-09/D-10 with labeled RAISE EXCEPTION/NOTICE blocks"
    - "All in-scope Big-5 offices in the 8 existing states (CA, MA, MD, ME, OR, TX, UT, VA) have non-null role_canonical"
    - "UT's 5 STATE_EXEC politicians have non-null external_id assigned via -(49*100000+seq)"
    - "TX Comptroller office has role_canonical='treasurer' (D-01)"
    - "No existing district/politician/office row is re-seeded or duplicated (D-09)"
  artifacts:
    - path: "backend/scripts/verify-phase-141.sql"
      provides: "Phase-141 read-only SQL gate covering SEXR-01..04 + D-09/D-10"
      contains: "RAISE EXCEPTION"
    - path: "backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql"
      provides: "role_canonical backfill (8 states) + UT NULL external_id fix"
      contains: "role_canonical"
  key_links:
    - from: "essentials.offices.role_canonical"
      to: "essentials.districts (STATE_EXEC) via district_id"
      via: "UPDATE join on district_type+state+chamber name"
      pattern: "role_canonical = '(governor|lt_governor|attorney_general|secretary_of_state|treasurer)'"
---

<objective>
Author the phase-local verification gate, then backfill `role_canonical` on the pre-existing Big-5 offices in 8 of the 9 already-seeded states (CA, MA, MD, ME, OR, TX, UT, VA — Indiana is owned by plan 141-02) and fix Utah's 5 NULL `external_id` values.

Purpose: SEXR-03 requires `role_canonical` populated on EVERY in-scope Big-5 office including pre-existing records; the cross-state dedup/query key (D-06). UT's NULL external_ids break idempotent `ON CONFLICT` keys (D-08, Pitfall 3) and must be fixed before any re-runnable seed. The gate (Wave 0) is the Nyquist proof for the whole phase.
Output: `backend/scripts/verify-phase-141.sql` + migration 946 applied to prod with rows verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md
@.planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md

# Authoritative source-of-truth for the role_canonical backfill set (existing external_ids per office):
#   141-RESEARCH.md §"Summary: What Phase 141 Must Do" item 2 lists every existing office's external_id.
# Gate analog (mirror its labeled-assertion structure exactly):
@backend/scripts/verify-phase-132-140.sql
# role_canonical backfill SQL patterns (Pattern 4) + post-insert assertions (Pattern 3):
#   141-RESEARCH.md §"Architecture Patterns"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author verify-phase-141.sql (Wave 0 gate)</name>
  <files>backend/scripts/verify-phase-141.sql, backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql</files>
  <read_first>
    - backend/scripts/verify-phase-132-140.sql (the labeled DO $$ ... RAISE EXCEPTION/NOTICE structure to mirror exactly)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-VALIDATION.md (§"Wave 0 Requirements" + Per-Task Verification Map — the exact assertions required)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Validated 50-State Elected Big-5 Matrix" — the 208-office denominator; §"Validation Architecture" Wave 0 Gaps list)
  </read_first>
  <action>
    Create backend/scripts/verify-phase-141.sql as a read-only labeled-assertion gate, mirroring verify-phase-132-140.sql (header comment block; each check is a `DO $$ DECLARE ... BEGIN ... IF NOT (...) THEN RAISE EXCEPTION ...; END IF; RAISE NOTICE '... PASS'; END $$;` block; final overall PASS NOTICE). Run header: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql`. Author these labeled assertions:
    - SEXR-01: count of distinct in-scope (state, role_canonical) STATE_EXEC office pairs with role_canonical IN ('governor','lt_governor','attorney_general','secretary_of_state','treasurer') equals 208 (document AZ LtGov as the deferred exclusion in a comment; the seeding denominator for AZ is 4).
    - SEXR-02: zero in-scope (state, role_canonical) pairs from the matrix are MISSING a politician+office. Encode the 208-pair expectation as a per-role count check: governor=50, lt_governor=43, attorney_general=43, secretary_of_state=35, treasurer=37 (each counts offices joined to STATE_EXEC districts). Note in comment that AZ LtGov absence is expected (43 not 44).
    - SEXR-03: COUNT of in-scope STATE_EXEC offices with role_canonical IS NULL = 0 (scope: offices joined to STATE_EXEC districts whose role is one of the 5 canonical values OR whose chamber is a Big-5 chamber).
    - SEXR-04: COUNT of newly-seeded execs (external_id <= -100000, i.e. the -(fips*100000+seq) range, AND UT range -4900001..-4900005) with NO essentials.politician_images row = 0, EXCEPT documented honest-skips (pin honest-skips to a NOTICE listing their external_ids; see find-headshots fallback in plans 141-08..12).
    - D-10a: COUNT of STATE_EXEC districts WHERE state != upper(state) = 0.
    - D-10b: COUNT of STATE_EXEC districts WHERE geo_id IS NULL OR geo_id = '' = 0 (NOTE: pre-existing CA shared "California" and OR "Oregon (Statewide)" + IN shared "Indiana" districts may have geo_id='' — scope this assertion to districts created by this phase, i.e. label LIKE state-qualified Big-5 labels, OR exclude the documented legacy shared-label districts by name; document the exclusion in a comment).
    - Headshot mechanism: assert against essentials.politician_images (column `url`), NOT photo_origin_url (confirmed via migration 271).
    Use the role_canonical column on essentials.offices as the canonical key per D-06. Confirm column/table names against 141-RESEARCH.md before writing (essentials.offices.role_canonical, essentials.districts.district_type='STATE_EXEC', essentials.politician_images.url).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File backend/scripts/verify-phase-141.sql exists.
    - File contains a labeled assertion (RAISE EXCEPTION) for each of: SEXR-01, SEXR-02, SEXR-03, SEXR-04, D-10a, D-10b.
    - Running the script with `-v ON_ERROR_STOP=1` executes without syntax error (it MAY RAISE EXCEPTION on as-yet-unmet assertions at this point in the phase — that is expected; syntactic validity + correct assertion logic is the acceptance bar for Task 1).
    - Headshot assertion references essentials.politician_images.url, not photo_origin_url.
  </acceptance_criteria>
  <done>verify-phase-141.sql committed; all 6 labeled assertions present; runs without SQL syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: role_canonical backfill (8 states) + UT NULL external_id fix — migration 946</name>
  <files>backend/scripts/verify-phase-141.sql, backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql</files>
  <read_first>
    - backend/migrations/270_md_state_executives.sql (CTE insert pattern, office lookup, post-insert assertion idioms)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-RESEARCH.md (§"Summary: What Phase 141 Must Do" item 2 — the exact existing external_id per office to backfill; §"Architecture Patterns" Pattern 4 backfill + Pattern 1 for CA/OR shared-label join-via-chamber)
    - .planning/phases/141-roster-lock-seed-records-headshots/141-CONTEXT.md (D-01 TX Comptroller→treasurer; D-05 do-not-retrofit; D-07 leave titles; D-08 UT fix; D-09 no-reseed)
  </read_first>
  <action>
    Create backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql (next available disk number is 946; confirm with `ls backend/migrations | sort` and bump if 946 is taken). Wrap in BEGIN/COMMIT. Two parts:

    PART A — UT NULL external_id fix (D-08): UPDATE essentials.politicians SET external_id = -(49*100000 + seq) for UT's 5 existing STATE_EXEC politicians, assigning seq deterministically by office: Cox (Gov)→-4900001, Henderson (LtGov)→-4900002, Brown (AG)→-4900003, Cannon (State Auditor, NOT Big-5)→-4900004, Oaks (Treasurer)→-4900005. Match each politician by current identity (full_name + the UT STATE_EXEC district/office join) since external_id is currently NULL — do NOT match on external_id. Guard idempotently with `WHERE external_id IS NULL`. PREFLIGHT before the UPDATE: assert none of -4900001..-4900005 already exist (D-04).

    PART B — role_canonical backfill on existing in-scope Big-5 offices for 8 states (CA, MA, MD, ME, OR, TX, UT, VA). EXCLUDE Indiana entirely (owned by plan 141-02). For each office, UPDATE essentials.offices SET role_canonical = <canonical> WHERE the office joins to a STATE_EXEC district for that state AND the office's chamber/title identifies the role. Per-state notes:
    - CA + OR use a SHARED district label ("California" / "Oregon (Statewide)") — disambiguate via chamber name (JOIN essentials.chambers ON chamber name LIKE '%Governor%' etc.), NOT district label (Pitfall 1). NEVER create new districts for CA/OR.
    - MD/MA/ME/VA/TX use per-office labeled districts — join via (state, label) or chamber name.
    - TX Comptroller (-100202..-100205 range; Hegar) → role_canonical='treasurer' (D-01), keep title 'Comptroller' (D-07).
    - MA Secretary of the Commonwealth → 'secretary_of_state'; MA Treasurer and Receiver-General → 'treasurer' (research notes these two are already set on MA — guard with `WHERE role_canonical IS NULL` so re-run is a no-op; backfill MA Gov/LtGov/AG).
    - Set role_canonical only where currently NULL (idempotent; D-05 no-retrofit of other columns).
    Use the existing external_ids from 141-RESEARCH.md item 2 to scope the UPDATEs precisely; do NOT insert any district/politician/office (D-09).

    POST-INSERT ASSERTIONS (in the same migration, before COMMIT): (a) every in-scope office for these 8 states has role_canonical NOT NULL; (b) no STATE_EXEC district for these states has lowercase state or empty geo_id was introduced (this migration creates none, so assert the count is unchanged — read-only sanity); (c) UT politicians all have non-null external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/migrations/946_state_exec_rolecanonical_backfill_utfix.sql 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Migration 946 applies to prod (kxsdzaojfaibhuzmclfq) inside BEGIN/COMMIT without error.
    - Post-apply query `SELECT count(*) FROM essentials.offices o JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state IN ('CA','MA','MD','ME','OR','TX','UT','VA') AND o.role_canonical IS NULL AND o.role_canonical_should_be_set` returns 0 in-scope NULLs (scope to Big-5 offices; non-Big-5 like CA Controller/MA Auditor/UT Auditor remain NULL by design).
    - Post-apply query `SELECT count(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND d.state='UT' AND p.external_id IS NULL` returns 0.
    - Post-apply query confirms TX Comptroller office role_canonical='treasurer'.
    - Re-running migration 946 affects 0 rows (idempotent).
  </acceptance_criteria>
  <done>Migration 946 applied to prod; role_canonical backfilled on all in-scope Big-5 offices in 8 states; UT 5 external_ids non-null; TX Comptroller=treasurer; re-run is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → prod DB (kxsdzaojfaibhuzmclfq) | SQL migration applied directly to production; the only attack/error surface is data correctness, not external untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering | role_canonical UPDATE over-matches and rewrites a non-Big-5 office (CA Controller, MA Auditor) | mitigate | Scope UPDATEs by existing external_id (from RESEARCH item 2) + chamber-name join; `WHERE role_canonical IS NULL` guard; post-insert assertion that non-Big-5 offices retain NULL |
| T-141-02 | Tampering | UT external_id assignment collides with an existing id | mitigate | D-04 preflight asserts -4900001..-4900005 absent before UPDATE; range is in confirmed-clear band |
| T-141-03 | Denial of Service | CA/OR shared-label join matches multiple offices, corrupting role mapping | mitigate | Join via chamber name (Pattern 4 / Pitfall 1), never district label; assert one-office-per-role post-update |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure SQL); no supply-chain surface |
</threat_model>

<verification>
Run the full gate after this plan (it will still RAISE on SEXR-02/04 until seeds + headshots land in later plans — that is expected; this plan's own assertions for the 8 backfilled states + UT must pass):
`psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-141.sql`
</verification>

<success_criteria>
- verify-phase-141.sql exists with all 6 labeled assertions and runs syntactically.
- Migration 946 applied to prod; role_canonical non-null on all in-scope Big-5 offices in CA, MA, MD, ME, OR, TX, UT, VA; TX Comptroller=treasurer.
- UT's 5 STATE_EXEC politicians have non-null external_ids in -4900001..-4900005.
- Idempotent: re-running migration 946 changes 0 rows.
</success_criteria>

<output>
Create `.planning/phases/141-roster-lock-seed-records-headshots/141-01-SUMMARY.md` when done.
</output>
