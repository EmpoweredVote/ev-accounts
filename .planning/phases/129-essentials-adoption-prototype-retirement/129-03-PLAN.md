---
phase: 129-essentials-adoption-prototype-retirement
plan: 03
type: execute
wave: 3
depends_on: [129-01, 129-02]
files_modified: []
autonomous: false
requirements: [ADOPT-01, ADOPT-02, ADOPT-04]
must_haves:
  truths:
    - "Representatives page (/results) renders CompassCardVertical without errors"
    - "Elections tab on /results renders incumbents and challengers without errors"
    - "/prototype URL no longer loads the old Prototype page"
    - "essentials production build is clean"
    - "Reps page sort, elected/appointed filter, and tier-band scroll-spy still function (ADOPT-01)"
    - "CompassV2 compare picker still renders against current ev-ui (ADOPT-04 consumer-side check)"
  artifacts:
    - path: ".planning/phases/129-essentials-adoption-prototype-retirement/129-03-SUMMARY.md"
      provides: "End-to-end verification record"
  key_links:
    - from: "essentials production build"
      to: "Results.jsx + ElectionsView.jsx + CompassCardVertical"
      via: "vite build output"
      pattern: "build succeeded with no errors"
---

<objective>
End-to-end verification that the prototype retirement and CompassPreview removal did not regress the live `CompassCardVertical` adoption on the Representatives and Elections pages. Per ADOPT-01, ADOPT-02, ADOPT-04: confirm both card flows still work and the production build is clean.

Purpose: Lock the win — Phase 129 cleanup did not break the production cards already shipped in earlier phases.

Output: Smoke-test record in 129-03-SUMMARY.md confirming all four ADOPT requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-CONTEXT.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-01-SUMMARY.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final regression grep + production build</name>
  <read_first>
    - essentials/src/App.jsx
    - essentials/src/pages/Results.jsx
    - essentials/src/components/ElectionsView.jsx
  </read_first>
  <files>(none — verification only)</files>
  <action>
    Run a final regression check across `essentials/src/`:

    1. Confirm no residual references to deleted symbols:
       - `grep -rn "CompassPreview\|previewPol\|setPreviewPol" essentials/src/` → must be empty
       - `grep -rn "Prototype\|CompassFirstCard\|mockCompassData" essentials/src/` → must be empty
    2. Confirm production card still wired:
       - `grep -n "CompassCardVertical" essentials/src/pages/Results.jsx` → at least one match
       - `grep -n "CompassCardVertical" essentials/src/components/ElectionsView.jsx` → at least one match
    3. Confirm SegmentedControl preserved (Reps/Elections tab toggle):
       - `grep -n "SegmentedControl" essentials/src/pages/Results.jsx` → at least one match
    4. Run the production build: `cd essentials && npm run build`. Must exit 0 with no warnings about missing modules.

    If any check fails, STOP and surface the failure for human review before the checkpoint.
  </action>
  <acceptance_criteria>
    - `! grep -rq "CompassPreview\|previewPol\|Prototype\|CompassFirstCard\|mockCompassData" essentials/src/`
    - `grep -q "CompassCardVertical" essentials/src/pages/Results.jsx`
    - `grep -q "CompassCardVertical" essentials/src/components/ElectionsView.jsx`
    - `grep -q "SegmentedControl" essentials/src/pages/Results.jsx`
    - `cd essentials && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials &amp;&amp; ! grep -rq "CompassPreview\|previewPol\|Prototype\|CompassFirstCard\|mockCompassData" src/ &amp;&amp; grep -q "CompassCardVertical" src/pages/Results.jsx &amp;&amp; grep -q "CompassCardVertical" src/components/ElectionsView.jsx &amp;&amp; grep -q "SegmentedControl" src/pages/Results.jsx &amp;&amp; npm run build</automated>
  </verify>
  <done>All grep checks pass; production build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke-test Reps + Elections + /prototype 404 + CompassV2 consumer</name>
  <what-built>
    Phase 129 cleanup is complete: `/prototype` route + page + harness deleted, `CompassPreview` popover removed from Results.jsx and component file deleted, classify.js doc comment cleaned. Representatives and Elections cards remain wired to `CompassCardVertical` (live since prior phases).
  </what-built>
  <how-to-verify>
    1. From `essentials/`, run `npm run dev` and open the dev URL (typically http://localhost:5173 or 5174).
    2. Enter a Bloomington, IN address (or any tested address) on the home page and submit.
    3. **Representatives tab — card render (ADOPT-01):** Verify each politician card shows portrait + name + position + radar (CompassCardVertical). No floating mini-radar popover should appear on hover.
    4. **Representatives tab — sort still works (ADOPT-01):** Open the sort control and apply at least one non-default sort option (e.g. by name or by district). Confirm the card order updates accordingly and no card disappears or errors.
    5. **Representatives tab — elected/appointed filter still works (ADOPT-01):** Toggle the elected/appointed filter. Confirm the visible card set changes (appointed officials hide/show as expected) and remaining cards still render correctly.
    6. **Representatives tab — tier-band scroll-spy still works (ADOPT-01):** Scroll the page from top to bottom. Confirm the active tier band indicator (Federal → State → Local) updates as the corresponding section enters the viewport.
    7. **Elections tab (ADOPT-02):** Switch to the Elections tab via SegmentedControl. Verify incumbents render the full compass card and challengers render the appropriate empty/minimal variant. Tab toggle must still work in both directions.
    8. **/prototype retired (ADOPT-03):** Manually navigate to `/prototype` in the address bar. Confirm it does NOT load the old Prototype page — it should hit the app's 404/fallback (or whatever the unmatched-route behavior is).
    9. **CompassV2 compare picker (ADOPT-04 consumer check):** Open https://compass.empowered.vote (or run CompassV2 locally with `cd CompassV2 && npm run dev`). Navigate to the Compare view and open the politician compare picker. Confirm the picker renders without errors and politicians are selectable — this verifies the current published `@empoweredvote/ev-ui` still works for the CompassV2 consumer. (The actual ev-ui auto-bump pipeline is owned by Phase 128 plan 04; this is the downstream-consumer smoke check only.)
    10. Confirm there are no console errors in the browser devtools across all views above (essentials /results Reps, /results Elections, /prototype, CompassV2 compare picker).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any regression seen.</resume-signal>
</task>

</tasks>

<verification>
- All ADOPT-01..04 requirements satisfied:
  - ADOPT-01: Reps page cards = CompassCardVertical, sort + elected/appointed filter + tier-band scroll-spy verified in human smoke-test
  - ADOPT-02: Elections page cards = CompassCardVertical with variants (verified inline + smoke)
  - ADOPT-03: /prototype retired (verified by file deletion + smoke 404)
  - ADOPT-04: ev-ui auto-bump pipeline owned by Phase 128 plan 04 (out of scope here); current ev-ui consumer verified working via essentials build pass + CompassV2 compare picker smoke check
</verification>

<success_criteria>
- Production build clean
- Reps + Elections smoke-tests pass (including sort, filter, scroll-spy)
- /prototype no longer loads
- CompassV2 compare picker still renders against current ev-ui
- No browser console errors on any verified view
</success_criteria>

<output>
After completion, create `.planning/phases/129-essentials-adoption-prototype-retirement/129-03-SUMMARY.md` recording smoke-test results.
</output>
