---
phase: 129-essentials-adoption-prototype-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/App.jsx
  - essentials/src/pages/Prototype.jsx
  - essentials/src/components/CompassFirstCard.jsx
  - essentials/src/data/mockCompassData.js
  - essentials/src/lib/classify.js
autonomous: true
requirements: [ADOPT-03]
must_haves:
  truths:
    - "Visiting /prototype no longer renders the Prototype page (route gone)"
    - "Prototype.jsx, CompassFirstCard.jsx, mockCompassData.js no longer exist"
    - "essentials build still passes after deletion"
  artifacts:
    - path: "essentials/src/App.jsx"
      provides: "Route table with no /prototype route and no Prototype import"
      contains_not: "Prototype"
  key_links:
    - from: "essentials/src/App.jsx"
      to: "(removed)"
      via: "no import of ./pages/Prototype"
      pattern: "Prototype"
---

<objective>
Retire the `/prototype` route and delete prototype-only files. Per D-03, D-04, D-05 in 129-CONTEXT.md: the route definition, the page file, and prototype-only deps (CompassFirstCard.jsx, mockCompassData.js) are removed entirely. No redirect is added — `/prototype` falls through to the app's normal 404/fallback.

Purpose: Prevent UI drift between the prototype harness and production cards, now that `CompassCardVertical` is live on Representatives and Elections pages.

Output: Smaller essentials bundle, no `/prototype` URL, no orphan prototype components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-CONTEXT.md

<interfaces>
Pre-scout (run by planner) confirmed callers — executor does NOT need to re-scout these:

- `essentials/src/components/CompassFirstCard.jsx` — only imported by `essentials/src/pages/Prototype.jsx`. Safe to delete.
- `essentials/src/data/mockCompassData.js` — only imported by `Prototype.jsx` and `CompassFirstCard.jsx`. Safe to delete.
- `essentials/src/components/SegmentedControl.jsx` — also imported by `LocalFilterSidebar.jsx` and `Results.jsx`. DO NOT delete.
- `essentials/src/lib/classify.js:300` has a JSDoc comment referencing `Prototype.jsx` — comment only, update to remove the Prototype.jsx reference (cosmetic, but keeps grep clean).

App.jsx structure (from pre-scout):
- Line 12: `import Prototype from "./pages/Prototype";`
- Line 60: `<Route path="/prototype" element={<Prototype />} />`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /prototype route from App.jsx</name>
  <read_first>
    - essentials/src/App.jsx
  </read_first>
  <files>essentials/src/App.jsx</files>
  <action>
    Delete two lines from `essentials/src/App.jsx`:
    1. The import statement: `import Prototype from "./pages/Prototype";` (currently line ~12)
    2. The route element: `<Route path="/prototype" element={<Prototype />} />` (currently line ~60)

    Do NOT add a redirect — per D-05, `/prototype` should fall through to the app's normal fallback. Do not add any other replacement.

    Leave all other imports and routes untouched. The remaining `<Route>` definitions (Home, Results, Profile, etc.) and other imports (SegmentedControl is NOT imported by App.jsx) must be preserved exactly.
  </action>
  <acceptance_criteria>
    - `! grep -q "Prototype" essentials/src/App.jsx`
    - `! grep -q "/prototype" essentials/src/App.jsx`
    - `grep -q "Routes" essentials/src/App.jsx` (Routes block still exists)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "Prototype" /Users/chrisandrews/Documents/GitHub/essentials/src/App.jsx &amp;&amp; ! grep -q "/prototype" /Users/chrisandrews/Documents/GitHub/essentials/src/App.jsx</automated>
  </verify>
  <done>App.jsx no longer references Prototype or /prototype. Other routes intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete prototype-only files</name>
  <read_first>
    - essentials/src/pages/Prototype.jsx
    - essentials/src/components/CompassFirstCard.jsx
    - essentials/src/data/mockCompassData.js
    - essentials/src/lib/classify.js
  </read_first>
  <files>
    essentials/src/pages/Prototype.jsx,
    essentials/src/components/CompassFirstCard.jsx,
    essentials/src/data/mockCompassData.js,
    essentials/src/lib/classify.js
  </files>
  <action>
    Per D-04, delete the three prototype-only files entirely:

    1. `rm essentials/src/pages/Prototype.jsx`
    2. `rm essentials/src/components/CompassFirstCard.jsx`
    3. `rm essentials/src/data/mockCompassData.js`

    Pre-scout confirmed these have no callers outside Prototype.jsx (which Task 1 unwired and which is itself deleted here).

    Then update `essentials/src/lib/classify.js`: locate the JSDoc comment near line 300 that says `Used by page-level components (Prototype.jsx, future Results.jsx).` and rewrite it to remove the `Prototype.jsx` reference. Replacement comment: `Used by page-level components (Results.jsx, ElectionsView.jsx).` Keep the surrounding `computeVariant` function and its other comments unchanged — this is a cosmetic doc edit only.
  </action>
  <acceptance_criteria>
    - `! test -f essentials/src/pages/Prototype.jsx`
    - `! test -f essentials/src/components/CompassFirstCard.jsx`
    - `! test -f essentials/src/data/mockCompassData.js`
    - `! grep -q "Prototype.jsx" essentials/src/lib/classify.js`
    - `grep -q "computeVariant" essentials/src/lib/classify.js` (function still exported)
  </acceptance_criteria>
  <verify>
    <automated>! test -f /Users/chrisandrews/Documents/GitHub/essentials/src/pages/Prototype.jsx &amp;&amp; ! test -f /Users/chrisandrews/Documents/GitHub/essentials/src/components/CompassFirstCard.jsx &amp;&amp; ! test -f /Users/chrisandrews/Documents/GitHub/essentials/src/data/mockCompassData.js &amp;&amp; ! grep -q "Prototype.jsx" /Users/chrisandrews/Documents/GitHub/essentials/src/lib/classify.js</automated>
  </verify>
  <done>All three prototype files deleted; classify.js doc comment cleaned of Prototype.jsx reference.</done>
</task>

</tasks>

<verification>
- `! grep -rn "Prototype\|CompassFirstCard\|mockCompassData" essentials/src/` (no residual references after deletion; expected to be silent)
- `cd essentials && npm run build` exits 0 (build passes with prototype removed)
</verification>

<success_criteria>
- /prototype route removed from App.jsx (import + Route element gone)
- Prototype.jsx, CompassFirstCard.jsx, mockCompassData.js deleted
- classify.js JSDoc no longer references Prototype.jsx
- essentials build passes
- No orphan imports anywhere in essentials/src/
</success_criteria>

<output>
After completion, create `.planning/phases/129-essentials-adoption-prototype-retirement/129-01-SUMMARY.md`
</output>
