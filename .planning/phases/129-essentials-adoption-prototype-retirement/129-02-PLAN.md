---
phase: 129-essentials-adoption-prototype-retirement
plan: 02
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - essentials/src/pages/Results.jsx
  - essentials/src/components/CompassPreview.jsx
autonomous: true
requirements: [ADOPT-01, ADOPT-02]
must_haves:
  truths:
    - "Results.jsx no longer imports or renders CompassPreview"
    - "CompassPreview.jsx component file is deleted"
    - "Representatives and Elections page card rendering is unaffected (CompassCardVertical still wired)"
    - "SegmentedControl import in Results.jsx is preserved (still used by Reps/Elections tab toggle)"
  artifacts:
    - path: "essentials/src/pages/Results.jsx"
      provides: "Results page with no CompassPreview popover; CompassCardVertical remains the production card"
      contains_not: "CompassPreview"
      contains: "CompassCardVertical"
  key_links:
    - from: "essentials/src/pages/Results.jsx"
      to: "@empoweredvote/ev-ui CompassCardVertical"
      via: "existing import preserved"
      pattern: "CompassCardVertical"
---

<objective>
Remove the redundant `CompassPreview` floating popover from `Results.jsx` and delete the component file. Per D-06, D-07: every politician card already shows the radar inline via `CompassCardVertical`, so the hover-popover is redundant and should be removed cleanly.

Purpose: Eliminate redundant UI and dead component files; simplify Results.jsx state management (drop `previewPol`).

Output: Cleaner Results.jsx, deleted CompassPreview component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-CONTEXT.md
@.planning/phases/129-essentials-adoption-prototype-retirement/129-01-SUMMARY.md

<interfaces>
Pre-scout (run by planner) confirmed:

- `CompassPreview` is imported only at `essentials/src/pages/Results.jsx:13` and rendered only at lines ~1345–1354. No other callers anywhere in essentials/src/.
- `previewPol` state variable in Results.jsx (~line 405) is the only state that drives `<CompassPreview>` — safe to delete.
- `SegmentedControl` is imported at `Results.jsx:12` and is ALSO used in `LocalFilterSidebar.jsx`. In Results.jsx it powers the Representatives/Elections tab toggle. DO NOT delete this import — it is still in use.
- `CompassCardVertical` is the production card already wired in Results.jsx (and ElectionsView.jsx). Do not touch its imports or usage.

Lines to remove in Results.jsx (per CONTEXT.md):
- Line 13: `import CompassPreview from '../components/CompassPreview';`
- Line ~405: the `previewPol` state declaration (and any related setter calls)
- Lines ~1345–1354: the `<CompassPreview ... />` JSX block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip CompassPreview from Results.jsx</name>
  <read_first>
    - essentials/src/pages/Results.jsx
    - essentials/src/components/CompassPreview.jsx
  </read_first>
  <files>essentials/src/pages/Results.jsx</files>
  <action>
    Edit `essentials/src/pages/Results.jsx` and remove all CompassPreview wiring:

    1. Delete the import line: `import CompassPreview from '../components/CompassPreview';` (currently line ~13).
    2. Find the `previewPol` state declaration (around line 405, e.g. `const [previewPol, setPreviewPol] = useState(...)`) and delete the entire line.
    3. Search for any remaining call sites of `setPreviewPol(` or `previewPol` elsewhere in the file (handlers passing setPreviewPol as a prop, mouseenter handlers, etc.) and delete the references. If `setPreviewPol` is passed as a prop to a child (e.g. politician card row), remove that prop and any handler that calls it. The card itself (`CompassCardVertical`) does not need a hover preview — the radar is already inline.
    4. Delete the `<CompassPreview ... />` JSX block (lines ~1345–1354).

    DO NOT touch:
    - The `import SegmentedControl from '../components/SegmentedControl';` line — still in use for the Reps/Elections tab toggle.
    - Any `CompassCardVertical` import or render — that is the production card.
    - Any other state, handlers, or imports unrelated to CompassPreview.

    After edits, the file must still compile (no orphan references to `previewPol`, `setPreviewPol`, or `CompassPreview`).
  </action>
  <acceptance_criteria>
    - `! grep -q "CompassPreview" essentials/src/pages/Results.jsx`
    - `! grep -q "previewPol" essentials/src/pages/Results.jsx`
    - `! grep -q "setPreviewPol" essentials/src/pages/Results.jsx`
    - `grep -q "SegmentedControl" essentials/src/pages/Results.jsx` (still used by tab toggle — must remain)
    - `grep -q "CompassCardVertical" essentials/src/pages/Results.jsx` (production card preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/essentials &amp;&amp; ! grep -q "CompassPreview\|previewPol" src/pages/Results.jsx &amp;&amp; grep -q "SegmentedControl" src/pages/Results.jsx &amp;&amp; grep -q "CompassCardVertical" src/pages/Results.jsx</automated>
  </verify>
  <done>Results.jsx has no CompassPreview/previewPol references; SegmentedControl and CompassCardVertical preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Delete CompassPreview.jsx and verify essentials build</name>
  <read_first>
    - essentials/src/components/CompassPreview.jsx
    - essentials/src/pages/Results.jsx
  </read_first>
  <files>essentials/src/components/CompassPreview.jsx</files>
  <action>
    Per D-07: pre-scout confirmed `CompassPreview` has no callers outside Results.jsx (and Task 1 just unwired Results.jsx). Run a final no-callers grep across `essentials/src/` to be safe, then delete the file.

    1. Confirm no remaining callers: `grep -rn "CompassPreview" essentials/src/` should return zero matches.
    2. Delete the component: `rm essentials/src/components/CompassPreview.jsx`.
    3. Run the production build to confirm no broken imports anywhere: `cd essentials &amp;&amp; npm run build`.

    If grep returns ANY remaining match (other than the file itself before deletion), STOP — do not delete. Surface the unexpected caller and ask the user before proceeding.
  </action>
  <acceptance_criteria>
    - `! test -f essentials/src/components/CompassPreview.jsx`
    - `! grep -rq "CompassPreview" essentials/src/`
    - `cd essentials && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! test -f /Users/chrisandrews/Documents/GitHub/essentials/src/components/CompassPreview.jsx &amp;&amp; ! grep -rq "CompassPreview" /Users/chrisandrews/Documents/GitHub/essentials/src/ &amp;&amp; cd /Users/chrisandrews/Documents/GitHub/essentials &amp;&amp; npm run build</automated>
  </verify>
  <done>CompassPreview.jsx deleted; no residual references in essentials/src/; production build passes.</done>
</task>

</tasks>

<verification>
- `! grep -rq "CompassPreview\|previewPol" essentials/src/`
- `! grep -rq "Prototype\|CompassFirstCard\|mockCompassData" essentials/src/` (cumulative cleanup from plan 01)
- `cd essentials && npm run build` exits 0
- Manual smoke (next plan): /results renders CompassCardVertical for both Representatives and Elections tabs.
</verification>

<success_criteria>
- CompassPreview import + state + JSX removed from Results.jsx
- CompassPreview.jsx file deleted
- SegmentedControl import preserved (Reps/Elections tab still works)
- CompassCardVertical still rendering as the production card
- essentials production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/129-essentials-adoption-prototype-retirement/129-02-SUMMARY.md`
</output>
