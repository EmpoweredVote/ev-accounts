---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/164-ks-generate.mts
  - backend/migrations/1231_seed_ks_2026_house_elections_races.sql
  - backend/migrations/1232_seed_ks_2026_house_candidates.sql
  - backend/scripts/seed-ks-house-headshots.py
  - backend/data/seed-ks-2026-house/164-01-ks-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "KS has 1 election ('KS 2026 Statewide General', general, 2026-11-03) and 4 races, each office_id → the existing NATIONAL_LOWER office (never null); full PROVISIONAL pre-primary field surfaces on /elections (Aug-4 primary, cull >= 2026-08-05)"
    - "22 new KS politicians created (collision-free -(20*10000+cd*100+seq) band; KS-1 seq starts at 3, KS-2 seq starts at 10 per D-04); renominated incumbents reuse existing pid (-20001..-20004); 0 duplicate full_name per state; every new candidate has a headshot or documented honest-skip"
    - "KS is NOT redistricted — vanilla single-election pattern, no withholding; heavy multi-filer fields (KS-4 has 11 candidates) all seeded PROVISIONAL"
  artifacts:
    - path: "backend/migrations/1231_seed_ks_2026_house_elections_races.sql"
      provides: "KS general election + 4 races wired to existing offices"
      contains: "KS 2026 Statewide General"
    - path: "backend/migrations/1232_seed_ks_2026_house_candidates.sql"
      provides: "22 new KS politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "KS races"
      to: "existing NATIONAL_LOWER offices (geo prefix 20)"
      via: "join essentials.districts → essentials.offices"
      pattern: "NATIONAL_LOWER"
---

<objective>
Seed KS's full 2026 US House field (4 districts, 22 new records) — vanilla single-election pattern (KS is NOT redistricted, no withholding). Author 1 election + 4 races wired to existing offices, create 22 new records + race_candidates (reuse incumbent pids -20001..-20004), then headshots. Full field marked PROVISIONAL: (Aug-4 late primary — EARLIEST civic deadline in the phase per D-06, plan/execute first). Continues USHC3-02/03/04.

Output: 164-ks-generate.mts, two KS migrations (run on prod), seed-ks-house-headshots.py, KS reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@backend/scripts/162-mn-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
KS FIPS=20; geo_ids 2001-2004; geo prefix '20'. Incumbents keep existing external_id (-20001 Mann KS-1, -20002 Schmidt KS-2, -20003 Davids KS-3, -20004 Estes KS-4) — reuse by (district_type='NATIONAL_LOWER', geo_id) join, never recompute. New challengers: external_id = -(20*10000 + cd*100 + seq); COLLISION SUB-BANDS PER D-04: KS-1 seq starts at 3 (seqs 1-2 occupied), KS-2 seq starts at 10 (seqs 1-9 occupied); KS-3/KS-4 standard seq start 1. Query band for the whole state = -200499..-200101. One election: "KS 2026 Statewide General" (election_type general, election_date 2026-11-03). All 4 races: office_id = the district's existing NATIONAL_LOWER office (join essentials.districts d ON substr(d.geo_id,1,2)='20' → essentials.offices o ON o.district_id=d.id). race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party NEVER on the card (races.primary_party NULL). Description: 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-05' (Aug-4 primary). Parties normalized (Democratic/Republican/Libertarian/United Kansas/Independent — never "Democrat"). sqlStr()-escape every free-text field.
Per-district new records (from 160-field-table-p164.csv, KS rows): KS-1=4 (Colin McRoberts D; Lauren Reinhold D; Steven Jacob Libertarian; Craig Musser United Kansas); KS-2=3 (Chad Young R; Don Coover D; Braeden Curwick D); KS-3=5 (Sarah Preu D; Eric Jenkins R; Chase LaPorte R; Gavin Solomon R; Blake Stanley R); KS-4=10 (Michael Gaynor R; Frank McCollum R; Chris Carmichael D; Katy Tyndell D; Cole Epley D; Ryan Gilbert D; Jordan Mitchell D; Daniel Schneider D; Drew Cranmer Libertarian; Paul Catanese Independent). Total = 22 new. All 4 incumbents renominated → reuse existing rows (is_incumbent=true). No open seats in KS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-20 collision check + confirm D-04 sub-bands free</name>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (KS-1 collision_count=2 colliding_seqs 1-2 → safe_start_seq=3; KS-2 collision_count=9 colliding_seqs 1-9 → safe_start_seq=10)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (KS incumbent pids/external_ids -20001..-20004; all renominated)
  </read_first>
  <action>
    Run the fresh scoped collision check for KS's new-challenger band against prod (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 UNPLANNED rows in -200499..-200101 excluding legacy incumbent ids -20001..-20004 (cross-check any hit against 160-field-table-p164.csv). Explicitly confirm the D-04 sub-band starts are still free: KS-1 seqs 3+ (i.e. -200103 downward) unoccupied, KS-2 seqs 10+ (-200210 downward) unoccupied. The 160 audit is the starting point; a fresh live check is mandatory (162 carried-forward standard). Record the confirmed per-district seq start values for Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -200499 AND -200101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -200499..-200101 (or every hit is confirmed a legacy incumbent id, not a real collision)
    - KS-1 seq-3-start and KS-2 seq-10-start sub-bands confirmed unoccupied against live prod
  </acceptance_criteria>
  <done>KS band confirmed collision-free (incl. D-04 sub-band starts) before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply KS elections/races + candidates migrations</name>
  <read_first>
    - backend/scripts/162-mn-generate.mts (the vanilla single-election analog to clone: INC_EXT, single-election migration template, candidate-insert template, sqlStr, NOT EXISTS guards)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (the 22 KS new records + 4 incumbent reuse rows, KS rows)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/staging/p164-KS.csv (per-candidate provenance/source URLs)
  </read_first>
  <action>
    Clone backend/scripts/162-mn-generate.mts → backend/scripts/164-ks-generate.mts. Change: FIPS 20, 4 districts, INC_EXT = -20001..-20004 (all renominated, all get is_incumbent=true rows); FIELD = the 22 KS new records from 160-field-table-p164.csv (parties normalized, never "Democrat"; "United Kansas" and "Libertarian" preserved as party strings on races.primary_party context only, never on the card); geo prefix '20'; election name 'KS 2026 Statewide General'; description 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-05'. Apply the D-04 per-district seq starts: KS-1 seq 3, KS-2 seq 10, KS-3/KS-4 seq 1. Keep the -(20*10000+cd*100+seq) formula, single-election migration template, NOT EXISTS idempotency guards on politicians/race_candidates, and sqlStr() on every free-text field. Emit two migration files — reserved pair is 1231 (elections/races) + 1232 (candidates); FIRST re-verify with `ls backend/migrations | sort -n | tail` that 1231/1232 are still free (a parallel OR-session on phases 177/178 may have landed migrations since HWM=1230). If taken, shift both KS migrations to the next free contiguous pair and record actual numbers in the SUMMARY. Run node --import tsx scripts/164-ks-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Write backend/data/seed-ks-2026-house/164-01-ks-reconciliation.csv listing every KS candidate with new/reuse disposition + external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='KS 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - Both KS migrations applied cleanly (psql exit 0); 4 races exist, all office_id NOT NULL (FILTER count is 0)
    - 22 new politicians in -200499..-200101 (KS-1 from seq 3, KS-2 from seq 10); incumbents reuse -20001..-20004 with is_incumbent=true; 0 duplicate full_name per state; description carries 'PROVISIONAL:'
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>KS races + candidates seeded on prod, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: KS headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-mo-house-headshots.py (the band-scoped analog to clone; wrong-person guards byte-for-byte)
    - .claude/skills/find-headshots/SKILL.md (trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-mo-house-headshots.py → backend/scripts/seed-ks-house-headshots.py. Change ONLY the module docstring, BANDS = {'KS': (-200499, -200101, 'Kansas')} (band-scoped), and RESULTS_JSON = '_ks-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes (_NON_PERSON_TITLE / _BAD_DISAMBIG / _HISTORICAL_YEAR). Run with py (NOT python3). Any candidate with no free-license portrait gets a documented honest-skip in the results JSON. Incumbents (-20001..-20004) fall outside the band (already imaged in prior phases).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -200499 AND -200101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-ks-house-headshots.py uses the band-scoped BANDS; wrong-person guards unchanged
    - Every new KS candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>KS new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| headshot fetch -> politician_images | Wrong-person image mis-attribution risk |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD, no runtime code. Genuine data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field before interpolation |
| T-164-01-02 | Repudiation | Duplicate incumbent record | mitigate | Incumbents reuse -20001..-20004 by geo_id join; full_name dedup guard; 0-duplicate assertion in 164-13 |
| T-164-01-03 | Tampering | Writing to wrong project / non-idempotent double-insert | mitigate | Single prod DATABASE_URL from .env; NOT EXISTS guards make re-apply a no-op |
| T-164-01-04 | Information disclosure | Party leaking onto candidate card | mitigate | race_candidates has no party column; party lives on races.primary_party only |
| T-164-01-05 | Spoofing | Wrong-person headshot | mitigate | find-headshots first-name-mismatch + historical-year guards unchanged |
| T-164-01-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted in 148-163 |
</threat_model>

<verification>
- Both KS migrations applied on prod (psql exit 0); 4 races, all office_id NOT NULL; description PROVISIONAL
- 22 new politicians in-band (KS-1 seq 3, KS-2 seq 10); incumbents reused; 0 duplicate full_name
- Every new KS candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
KS's full 2026 House field is seeded on prod (22 new + 4 incumbent reuse), PROVISIONAL-marked, surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (164-07).
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-01-SUMMARY.md when done, recording applied migration numbers, the KS new-candidate external_id range, and any headshot honest-skips (external_ids) for the 164-13 gate pin.
</output>
