---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/164-or-generate.mts
  - backend/migrations/1235_seed_or_2026_house_candidates.sql
  - backend/scripts/seed-or-house-headshots.py
  - backend/data/seed-or-2026-house/164-03-or-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "OR REUSES its 6 existing_race_id general races — NO new elections/races rows authored (D-03); all 6 incumbents + 7 new challengers get fresh race_candidates INSERTs onto the 6 existing races (exactly 1 row per race_id+politician_id)"
    - "7 new OR politicians created (collision-free -(41*10000+cd*100+seq) band; OR-1 seq starts at 14 per D-04; query -410699..-410101); 6 incumbents reuse existing external_id (-4102001..-4102006); 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
    - "Decided field (OR primary done) — NOT PROVISIONAL-marked; the OR Aug-25 unaffiliated/minor-party window is deferred to Phase 167 (D-03a), no within-phase re-pull gate"
  artifacts:
    - path: "backend/migrations/1235_seed_or_2026_house_candidates.sql"
      provides: "OR candidates-only insert onto 6 existing races (idempotent, NOT EXISTS on race_id+politician_id)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "OR race_candidates rows"
      to: "the 6 existing_race_id UUIDs (4101→8dfd6e35 ... 4106→dd25e913)"
      via: "candidates-only insert (no new races)"
      pattern: "race_candidates"
---

<objective>
Seed OR's full 2026 US House field (6 districts, 7 new records) as a CANDIDATES-ONLY insert onto OR's 6 pre-existing scaffolded races (D-03, the MD-162 / MA-161 reuse pattern) — NO new elections/races rows. The phase's cheap early win. Confirm 0 pre-existing OR race_candidates rows first (like MD in 162). Decided field → NOT PROVISIONAL-marked; OR's Aug-25 open window deferred to Phase 167 (D-03a). Then headshots. Continues USHC3-02/03/04.

Output: 164-or-generate.mts, one OR candidates migration (run on prod), seed-or-house-headshots.py, OR reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
@backend/scripts/162-md-generate.mts
@backend/scripts/seed-mo-house-headshots.py

<interfaces>
OR FIPS=41; geo_ids 4101-4106; geo prefix '41'. Reuse the 6 EXISTING_RACE_ID general races (do NOT author new races/elections — D-03): 4101→8dfd6e35-f91d-4d14-bcec-ae983035da51, 4102→504a156a-d4e6-4abe-9a85-a418c0135805, 4103→61297fac-c98d-4ad4-b93f-b2e36722bd6a, 4104→c5023da0-985e-4e1c-817c-a843849ef6e9, 4105→17a4b696-5c15-4f2b-9390-ce1864d57f37, 4106→dd25e913-2496-40fa-a735-c9b1d7f68df8. Incumbents keep existing external_id (-4102001 Bonamici, -4102002 Bentz, -4102003 Dexter, -4102004 Hoyle, -4102005 Bynum, -4102006 Salinas) — all renominated, each gets an is_incumbent=true reuse row. New challengers: external_id = -(41*10000 + cd*100 + seq); COLLISION SUB-BAND PER D-04: OR-1 seq starts at 14 (seqs 10-13 occupied); OR-2..6 standard seq start 1. Query band = -410699..-410101 (6-digit; distinct from 7-digit legacy incumbent ids -4102001..-4102006). Decided field → do NOT PROVISIONAL-mark; optionally set the migration comment to note 'Confirmed general field; OR unaffiliated/minor window open to 2026-08-25 → Phase 167'. race_candidates: non-null politician_id, candidate_status='active', incumbents is_incumbent=true, party NEVER on card. sqlStr()-escape every free-text field. The OR election name (for the headshot join) must be looked up LIVE from the 6 existing_race_id rows (field table shows 'OR 2026 General' — confirm live, do NOT guess).
Per-district new records (from 160-field-table-p164.csv OR rows): OR-1=1 (Barbara Kahl R) seq 14; OR-2=1 (Chris Beck D); OR-3=1 (Loran Ayles R); OR-4=2 (Monique DeSpain R; Justin Filip Pacific Green); OR-5=1 (Patti Adair R); OR-6=1 (David Russ R). Total = 7 new. NOTE: a parallel session works OR LOCAL offices (Hillsboro/Tigard, phases 177/178) — this plan touches OR US HOUSE (federal) only; do not edit that session's dirs, and if any OR headshot/politician row collides, coordinate before overwriting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIPS-41 collision check + confirm 0 pre-existing OR race_candidates + capture election name</name>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (OR-1 collision_count=4 colliding_seqs 10-13 → safe_start_seq=14)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (OR incumbent pids/external_ids + the 6 existing_race_id UUIDs)
    - backend/scripts/162-md-generate.mts (the candidates-only reuse analog; how it looks up + wires existing races)
  </read_first>
  <action>
    In one compound command (cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql): (1) confirm 0 UNPLANNED rows in -410699..-410101 excluding legacy incumbent ids -4102001..-4102006; explicitly confirm OR-1 seq-14-start (-410114 downward) is unoccupied per D-04. (2) Confirm 0 pre-existing race_candidates rows on the 6 OR existing_race_id UUIDs (SELECT count(*) FROM essentials.race_candidates WHERE race_id = ANY(ARRAY[...6 UUIDs...])) — expect 0 like MD-162; if any exist, do NOT duplicate them (MA-161 Clark/Pressley precedent). (3) Look up the election name for those 6 races (SELECT DISTINCT el.name FROM essentials.elections el JOIN essentials.races r ON r.election_id=el.id WHERE r.id = ANY(ARRAY[...])) for the headshot join. Record all three results.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates WHERE race_id = ANY(ARRAY['8dfd6e35-f91d-4d14-bcec-ae983035da51','504a156a-d4e6-4abe-9a85-a418c0135805','61297fac-c98d-4ad4-b93f-b2e36722bd6a','c5023da0-985e-4e1c-817c-a843849ef6e9','17a4b696-5c15-4f2b-9390-ce1864d57f37','dd25e913-2496-40fa-a735-c9b1d7f68df8']::uuid[])"</automated>
  </verify>
  <acceptance_criteria>
    - FIPS-41 collision query returns 0 in -410699..-410101 (or only legacy-incumbent hits); OR-1 seq-14 start confirmed free
    - Pre-existing race_candidates count on the 6 OR UUIDs recorded (expected 0; any existing row noted to avoid duplication)
    - OR election name captured for the headshot join
  </acceptance_criteria>
  <done>OR band collision-free, reuse-races checked for pre-existing rows, election name captured — ready to generate.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply OR candidates-only migration + headshots</name>
  <read_first>
    - backend/scripts/162-md-generate.mts (the candidates-only reuse analog to clone: EXISTING_RACE_ID map, incumbent REUSE rows, NO elections/races INSERT, NOT EXISTS (race_id, politician_id) guard, sqlStr)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p164.csv (the 7 OR new records + 6 incumbents)
    - backend/scripts/seed-mo-house-headshots.py (headshot analog)
  </read_first>
  <action>
    Clone backend/scripts/162-md-generate.mts → backend/scripts/164-or-generate.mts: EXISTING_RACE_ID = the 6 OR UUIDs above; INC_PID/INC_EXT = OR's 6 incumbents (-4102001..-4102006, all renominated, each a fresh REUSE row); FIELD = the 7 OR new records (parties normalized — "Pacific Green" preserved as a party string, never on the card); apply OR-1 seq start 14. Do NOT author any elections/races INSERT (candidates-only, D-03). Do NOT PROVISIONAL-mark (decided); optionally add the decided/open-window comment. Keep the NOT EXISTS (race_id, politician_id) idempotency guard, the -(41*10000+cd*100+seq) formula, and sqlStr(). Emit one migration file — reserved number 1235; FIRST re-verify with `ls backend/migrations | sort -n | tail` that 1235 is free; claim the next free number if taken and record it in the SUMMARY. Run node --import tsx scripts/164-or-generate.mts, apply to prod via psql -v ON_ERROR_STOP=1. Then clone seed-mo-house-headshots.py → seed-or-house-headshots.py with BANDS = {'OR': (-410699, -410101, 'Oregon')} and RESULTS_JSON '_or-house-headshot-results.json'; wrong-person guards unchanged; run with py. Write backend/data/seed-or-2026-house/164-03-or-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), max(c) FROM (SELECT race_id, politician_id, count(*) c FROM essentials.race_candidates WHERE race_id = ANY(ARRAY['8dfd6e35-f91d-4d14-bcec-ae983035da51','504a156a-d4e6-4abe-9a85-a418c0135805','61297fac-c98d-4ad4-b93f-b2e36722bd6a','c5023da0-985e-4e1c-817c-a843849ef6e9','17a4b696-5c15-4f2b-9390-ce1864d57f37','dd25e913-2496-40fa-a735-c9b1d7f68df8']::uuid[]) GROUP BY race_id, politician_id) s"</automated>
  </verify>
  <acceptance_criteria>
    - OR migration applied cleanly (psql exit 0); race_candidates rows on all 6 existing races; NO new elections/races rows created
    - Exactly 1 row per (race_id, politician_id) — max(c) in the verify query is 1 (no duplicate wiring)
    - 7 new politicians in -410699..-410101 (OR-1 from seq 14); 6 incumbents reuse -4102001..-4102006 with is_incumbent=true; 0 duplicate full_name; migration idempotent
    - Every new OR candidate has a headshot row or documented honest-skip
  </acceptance_criteria>
  <done>OR candidates wired onto the 6 existing races on prod, no duplicates, with headshots and a reconciliation CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| headshot fetch -> politician_images | Wrong-person mis-attribution (esp. shared Oregon rows with the 177/178 session) |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD. Genuine data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-03-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field |
| T-164-03-02 | Repudiation | Duplicate race_candidates wiring on an existing race | mitigate | NOT EXISTS (race_id, politician_id) guard; exactly-1-per-pair assertion in verify + 164-13 |
| T-164-03-03 | Tampering | Accidentally authoring new OR races (violates D-03 reuse) | mitigate | Candidates-only generator (no elections/races INSERT); Task 1 confirms the 6 existing races are the wiring targets |
| T-164-03-04 | Denial of service | Collision with the parallel 177/178 OR session on shared politician/headshot rows | mitigate | Federal US-House band only; coordinate before overwriting any shared OR row |
| T-164-03-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- OR migration applied; race_candidates on all 6 existing races; NO new races created; exactly 1 row per (race_id, politician_id)
- 7 new politicians in-band (OR-1 seq 14); 6 incumbents reused; 0 duplicate full_name
- Every new OR candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
OR's full decided 2026 House field is wired onto its 6 existing races on prod (7 new + 6 incumbent reuse, no duplicates), surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (164-09).
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-03-SUMMARY.md when done, recording the applied migration number, the OR election name (for downstream), the OR new-candidate external_id range, and any headshot honest-skips for the 164-13 gate pin.
</output>
