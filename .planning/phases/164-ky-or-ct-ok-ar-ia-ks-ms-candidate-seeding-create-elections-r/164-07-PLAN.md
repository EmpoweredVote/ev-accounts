---
phase: 164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r
plan: 07
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - backend/data/stance-research/ks-2026-house/_merge.ts
  - backend/data/stance-research/ks-2026-house/_push.ts
  - backend/data/stance-research/ks-2026-house/_push_uuid.ts
  - backend/data/stance-research/ks-2026-house/_TOPIC_SCALE_FULL.txt
  - backend/data/stance-research/ks-2026-house/164-07-ks-stances.csv
autonomous: true
requirements: [USHC3-05]
must_haves:
  truths:
    - "All 22 new KS candidates have sourced chairs-not-polarity federal-24 stances (or a gate-pinned whole-record skip with a written search trail), 0 unsourced, primary-source-verified, pushed to PROD"
    - "Every KS answer pairs to an inform.politician_context row with a real fetched source_url"
    - "KS's partial-tier incumbents (-20001..-20004) are skipped via the diagnostic (out of scope)"
  artifacts:
    - path: "backend/data/stance-research/ks-2026-house/164-07-ks-stances.csv"
      provides: "KS new-candidate stance rows"
      contains: "source_url"
  key_links:
    - from: "stance CSV rows"
      to: "inform.politician_answers + inform.politician_context + PROD"
      via: "_push_uuid.ts / _push.ts"
      pattern: "politician_context"
---

<objective>
Research + push KS stances — all 22 new KS candidates (heavy multi-filer field, incl. KS-4's 10 new records). KS's incumbents are all partial-tier → skipped via the diagnostic. Set up the ks-2026-house scaffold, regenerate _TOPIC_SCALE_FULL.txt LIVE, run agents at 3-concurrency (1 agent per district) with a primary-source verification pass, push to PROD (durable store — stance CSVs are gitignored). First stance plan (KS Aug-4 primary, earliest civic deadline per D-06). Continues USHC3-05.

Output: ks-2026-house scaffold + stance CSV pushed to PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-CONTEXT.md
@.planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-01-SUMMARY.md
@backend/data/stance-research/mn-2026-house/_push.ts
@backend/data/stance-research/mn-2026-house/_merge.ts

<interfaces>
Scaffold: copy _push.ts / _merge.ts / _push_uuid.ts byte-for-byte from an existing state dir (e.g. mn-2026-house/) into ks-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt LIVE (SELECT ... FROM inform.compass_topics WHERE is_active=true; filter to the federal-24 subset; re-verify the live active count, do NOT assume 44). KS targets = the 22 new KS candidates (external_id band -200499..-200101, from 164-01-SUMMARY.md). KS incumbents (-20001..-20004) are all partial-tier → OUT of scope. Standards (project standing): chairs-not-polarity, never party-inferred, honest-skip with written search trail (whole-record gate-pinned), 0-unsourced floor, mandatory primary-source verification pass, 3-concurrency cap, first-wave validation, embed exact 1-5 scale texts per agent prompt. Sources: KS Wikipedia 2026-House page + Ballotpedia via Playwright + candidate sites/socials; over-index thoroughness given the large fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ks-2026-house + regenerate the federal-24 topic scale live</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (scaffold + topic conventions, 3-concurrency, chairs-not-polarity)
    - backend/data/stance-research/mn-2026-house/_push.ts (pipeline scripts to copy verbatim)
  </read_first>
  <action>
    Create backend/data/stance-research/ks-2026-house/. Copy _push.ts, _merge.ts, _push_uuid.ts byte-for-byte from mn-2026-house/. Regenerate _TOPIC_SCALE_FULL.txt via the LIVE compass_topics query filtered to the federal-24 subset (re-verify the live active total before subtracting non-federal topics), writing the exact 1-5 scale texts per topic. Do NOT copy an old state's _TOPIC_SCALE_FULL.txt. Set _merge.ts IN_SCOPE to the KS new-candidate band.
  </action>
  <verify>
    <automated>test -f backend/data/stance-research/ks-2026-house/_push.ts && test -f backend/data/stance-research/ks-2026-house/_TOPIC_SCALE_FULL.txt && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - ks-2026-house/ contains _merge.ts, _push.ts, _push_uuid.ts (verbatim) and _TOPIC_SCALE_FULL.txt
    - _TOPIC_SCALE_FULL.txt regenerated from a live query, filtered to the federal-24 subset (count re-verified), with exact 1-5 texts
  </acceptance_criteria>
  <done>KS stance scaffold ready; topic scale reflects the current live federal-24 set.</done>
</task>

<task type="auto">
  <name>Task 2: Research KS stances (22 new candidates, majors first) and push to PROD</name>
  <read_first>
    - .claude/skills/research-stances/SKILL.md (3-concurrency cap, chairs-not-polarity, 0-unsourced, honest-skip with written trail)
    - backend/data/stance-research/ks-2026-house/_TOPIC_SCALE_FULL.txt (embed per agent prompt)
    - 164-01-SUMMARY.md (KS candidate external_ids)
  </read_first>
  <action>
    Target the 22 new KS candidates (band -200499..-200101). Order evidenced majors first, fringe filers last. Do NOT research KS's partial-tier incumbents (out of scope). Dispatch politician-stance-researcher agents at 3-concurrency MAX, 1 agent per district (KS-4's 10 records may need splitting into two waves); validate the first wave before dispatching more. Embed the exact 1-5 scale texts per agent prompt. Score chairs-not-polarity; never infer from party; honest-skip per-topic or whole-record ONLY with a written search trail (whole-record gate-pinnable). Every answer pairs to a real fetched source_url. Run a mandatory primary-source verification pass. Write 164-07-ks-stances.csv, merge + push to PROD via _push.ts / _push_uuid.ts as each wave completes (PROD is the durable store — CSVs are gitignored).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM inform.politician_answers pa LEFT JOIN inform.politician_context pc ON pc.answer_id = pa.id WHERE pa.politician_id IN (SELECT id FROM essentials.politicians WHERE external_id BETWEEN -200499 AND -200101) AND (pc.source_url IS NULL OR pc.source_url = '')"</automated>
  </verify>
  <acceptance_criteria>
    - 0 unsourced answers among pushed KS rows (query returns 0)
    - Every new KS candidate has ≥1 sourced federal-24 stance OR a gate-pinned whole-record skip with a written search trail
    - Stance agents ran at ≤3 concurrency; a primary-source verification pass was performed; no partial-tier KS incumbent was topped up
  </acceptance_criteria>
  <done>KS's full field is stance-complete on PROD, 0 unsourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web sources -> stance CSV -> PROD | Third-party evidence becomes durable stance data |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — offline research + data push, no runtime code. Genuine data-integrity controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-07-01 | Repudiation | Unsourced / party-inferred stance rows | mitigate | 0-unsourced gate query; chairs-not-polarity; honest-skip with written trail; primary-source verification pass |
| T-164-07-02 | Tampering | Surname-leak in selected quotes | mitigate | _push.ts surname-leak guard (verbatim) |
| T-164-07-03 | Denial of service | Stance-agent rate-limit death | mitigate | 3-concurrency cap; first-wave validation; run on Sonnet |
</threat_model>

<verification>
- 0 unsourced answers among pushed KS rows
- Every new KS candidate has sourced stances or a gate-pinned whole-record skip
- Scaffold present; topic scale live-regenerated
</verification>

<success_criteria>
KS's full 2026 House field is stance-complete on PROD, 0 unsourced, chairs-not-polarity, primary-source-verified.
</success_criteria>

<output>
Create .planning/phases/164-ky-or-ct-ok-ar-ia-ks-ms-candidate-seeding-create-elections-r/164-07-SUMMARY.md when done, recording targets covered, gate-pinned whole-record skips (external_ids) for 164-13, and the live federal-24 count.
</output>
