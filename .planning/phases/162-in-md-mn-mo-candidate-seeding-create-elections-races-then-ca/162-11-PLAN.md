---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 11
type: execute
wave: 10
depends_on: ["162-01", "162-02", "162-03", "162-04", "162-05", "162-06", "162-07", "162-08", "162-09", "162-10"]
files_modified:
  - backend/scripts/162-verify.sql
  - backend/scripts/162-coordinate-smoke.ts
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04, USHC3-05]
must_haves:
  truths:
    - "A read-only gate proves all 33 IN/MD/MN/MO districts: correct race counts (IN 9 + MD 8 + MN 8 + MO 8), 0 NULL office_id/politician_id, 0 duplicate full_name per state, exactly 1 race_candidates row per (race_id, politician_id) for MD, 0 unsourced stances, headshot coverage or pinned skip"
    - "A NEW MO-SEVERE block proves severe MO races' parent election is the withheld 'Polygon Pending' election (not the general); a NEW IN9-FLAG block proves race 7d3f0042-... has exactly 1 is_incumbent=true (Houchin) and 4 false"
    - "A coordinate smoke proves a non-severe district in each of IN/MD/MN/MO surfaces its challenger-inclusive field, AND a severe MO district surfaces ZERO races"
  artifacts:
    - path: "backend/scripts/162-verify.sql"
      provides: "Consolidated read-only 33-district phase gate incl. MO-SEVERE + IN9-FLAG assertion blocks"
      contains: "Polygon Pending"
    - path: "backend/scripts/162-coordinate-smoke.ts"
      provides: "4-state positive samples + 1 severe-MO negative sample"
      contains: "SEVERE_MO_GEO_ID"
  key_links:
    - from: "162-verify.sql MO-SEVERE assertion"
      to: "the 162-01 severe geo_id list + 162-02 withheld election"
      via: "assert severe races' parent election = Polygon Pending"
      pattern: "Polygon Pending"
    - from: "162-verify.sql IN9-FLAG assertion"
      to: "race 7d3f0042-eb15-462b-bf14-df15244c5d16"
      via: "assert exactly 1 is_incumbent=true (Houchin)"
      pattern: "7d3f0042"
---

<objective>
Author the phase-closing 33-district mini-gate: 162-verify.sql (clone 161-verify.sql structurally + NEW MO-SEVERE block + NEW IN9-FLAG block) and 162-coordinate-smoke.ts (clone 161-coordinate-smoke.ts, extended to IN/MD/MN/MO + a severe-MO negative sample). This is the phase gate proving USHC3-02/03/04/05 across all 33 districts before /gsd:verify-work. Read-only (no DB writes except CREATE TEMP TABLE ON COMMIT DROP). The MO-non-surfacing and IN-9-flag assertions become standing invariants Phase 166's consolidated gate inherits (security_note: they must not silently regress).

Output: 162-verify.sql (green) + 162-coordinate-smoke.ts (green: 4 positive + 1 severe-MO negative).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-VALIDATION.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-04-SUMMARY.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-06-SUMMARY.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-09-SUMMARY.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-10-SUMMARY.md
@backend/scripts/161-verify.sql
@backend/scripts/161-coordinate-smoke.ts

<interfaces>
162-verify.sql (clone 161-verify.sql): resolve in_eid, mn_eid, mo_gen_eid, mo_withheld_eid (+ MD's races via the 8 existing_race_id, since MD reuses pre-existing races — verify whether they share one election name live). Combined _house temp table scoped by election_id IN (...) AND district_type='NATIONAL_LOWER' AND substr(geo_id,1,2) IN ('18','24','27','29'). CRITERION 1 (race-count): IN 9 + MD 8 + MN 8 + MO 8 = 33. CRITERION party-null (races.primary_party NULL). 0 NULL office_id; 0 NULL politician_id on active rows. 0 duplicate full_name per state. MD dedup: GROUP BY race_id, politician_id HAVING COUNT(*)>1 → 0.
NEW MO-SEVERE block (clone the TN-SEVERE structure): for each severe MO geo_id (from 162-mo-correspondence-audit.md's "Severe geo_id list:"), assert its race's parent election_id = mo_withheld_eid (COUNT of severe geo_ids whose race_election_id <> mo_withheld_eid must be 0).
NEW IN9-FLAG block (novel): SELECT COUNT(*) FILTER (WHERE is_incumbent) on race 7d3f0042-eb15-462b-bf14-df15244c5d16 must = 1, and that row must be Houchin's race_candidates.id 9d2de2ae-...; RAISE EXCEPTION otherwise.
USHC3-04/05 blocks + honest-skip pin tables (_stance_skip ORDER BY resolved politician_id; _img_skip ORDER BY external_id) — populate pins from 162-04/06/09/10 SUMMARY gate-pinned whole-record skips; _new_cands band bounds = the 4 phase bands (-180999..-180101, -240899..-240101, -270899..-270101, -290899..-290101).
162-coordinate-smoke.ts (clone 161-coordinate-smoke.ts): STATE_CONFIG gets IN/MD/MN/MO (MO uses the surfacing 'MO 2026 Statewide General'); SAMPLES = one in-district contested coordinate per state (4 positive, MIN_DISTRICTS=4); NEW SEVERE_MO_GEO_ID + negative-sample block proving a coordinate inside a severe MO district surfaces ZERO races on the MO surfacing election. All PostGIS calls use the public. schema prefix (public.ST_Covers / public.ST_PointOnSurface / public.ST_SetSRID / public.ST_MakePoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 162-verify.sql (clone 161 + MO-SEVERE + IN9-FLAG blocks) and run it green</name>
  <read_first>
    - 162-PATTERNS.md (162-verify.sql section — structural clone; MO-SEVERE from TN-SEVERE template; IN9-FLAG novel block; pin-table ordering discipline)
    - backend/scripts/161-verify.sql (the full structural analog)
    - 162-mo-correspondence-audit.md (the severe geo_id list for the MO-SEVERE block)
    - 162-04/06/09/10 SUMMARYs (gate-pinned whole-record stance/image skips to load into pin tables)
  </read_first>
  <action>
    Clone backend/scripts/161-verify.sql → backend/scripts/162-verify.sql. Resolve the IN/MN/MO elections (+ MD's reuse races). Build the _house temp table scoped to geo prefixes 18/24/27/29. Substitute CRITERION 1 counts (IN 9 + MD 8 + MN 8 + MO 8 = 33). Keep the party-null, NULL-office_id, NULL-politician_id, and 0-duplicate-full_name criteria. Add an MD exactly-1-row-per-(race_id, politician_id) dedup criterion. Author the NEW MO-SEVERE block (clone TN-SEVERE structure): assert every severe MO geo_id's race parent election = mo_withheld_eid. Author the NEW IN9-FLAG block: assert race 7d3f0042-... has exactly 1 is_incumbent=true (Houchin's rc.id 9d2de2ae-...) and RAISE EXCEPTION otherwise. Populate the _stance_skip (ORDER BY resolved politician_id) and _img_skip (ORDER BY external_id) pin tables from the 162-04/06/09/10 SUMMARYs' gate-pinned whole-record skips; set _new_cands band bounds to the 4 phase bands. Keep every block as SELECT ... INTO v_x; IF v_x <> expected THEN RAISE EXCEPTION ...; RAISE NOTICE 'PASS ...'. WRITE-FREE except CREATE TEMP TABLE ... ON COMMIT DROP. Run it against prod and iterate until green.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/162-verify.sql</automated>
  </verify>
  <acceptance_criteria>
    - 162-verify.sql runs to completion with psql exit 0 (all criteria PASS, no RAISE EXCEPTION)
    - MO-SEVERE block asserts every severe geo_id's race election = the Polygon Pending election; IN9-FLAG block asserts exactly 1 is_incumbent=true (Houchin) on race 7d3f0042-...
    - 33 races counted; 0 NULL office_id; 0 NULL active politician_id; 0 duplicate full_name per state; MD exactly-1-per-pair; 0 unsourced stances; headshot coverage or pinned skip
  </acceptance_criteria>
  <done>The consolidated 33-district SQL gate passes green against prod, including the MO-SEVERE and IN9-FLAG invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Author 162-coordinate-smoke.ts (4-state positive + severe-MO negative) and run it green</name>
  <read_first>
    - 162-PATTERNS.md (162-coordinate-smoke.ts section — STATE_CONFIG/SAMPLES clone; SEVERE_MO_GEO_ID negative sample; public. PostGIS prefix)
    - backend/scripts/161-coordinate-smoke.ts (the full analog)
    - 162-mo-correspondence-audit.md (a severe MO geo_id for the negative sample)
  </read_first>
  <action>
    Clone backend/scripts/161-coordinate-smoke.ts → backend/scripts/162-coordinate-smoke.ts. Add IN/MD/MN/MO to STATE_CONFIG (MO uses the surfacing 'MO 2026 Statewide General' election, same "surfacing election only" comment as TN). Set SAMPLES = one contested in-district coordinate per state (4 positive samples, MIN_DISTRICTS=4). Add a SEVERE_MO_GEO_ID constant (a severe geo_id from the audit) + copy the negative-sample block proving a coordinate inside that severe MO district surfaces ZERO races on the MO surfacing election. Keep all PostGIS calls prefixed public. (public.ST_Covers / public.ST_PointOnSurface / public.ST_SetSRID / public.ST_MakePoint). Run against prod and iterate until green.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/162-coordinate-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Coordinate smoke passes: 4 positive samples (one per IN/MD/MN/MO) each surface a challenger-inclusive field with non-null politician_ids
    - The severe-MO negative sample surfaces ZERO races on the surfacing election (proving the withholding works end-to-end)
    - All PostGIS functions use the public. schema prefix
  </acceptance_criteria>
  <done>The 4-state positive + severe-MO negative coordinate smoke passes green against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded prod data -> read-only gate | The gate is the last automated line of defense before the phase is declared done |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-11-01 | Tampering | A severe MO race silently surfacing (regression) | mitigate | MO-SEVERE block asserts parent election = Polygon Pending; coordinate-smoke negative sample re-proves 0 races end-to-end; Phase 166 inherits both (standing invariant) |
| T-162-11-02 | Tampering | IN-9 flag fix silently reverted by a future migration | mitigate | IN9-FLAG block asserts exactly 1 is_incumbent=true (Houchin) on race 7d3f0042-...; Phase 166 inherits it |
| T-162-11-03 | Repudiation | False-fail from pin-table ordering mismatch | mitigate | _stance_skip ORDER BY resolved politician_id; _img_skip ORDER BY external_id (the 143 ordering lesson) |
</threat_model>

<verification>
- 162-verify.sql green (psql exit 0): 33 races, 0 NULL office_id/politician_id, 0 duplicate full_name, MD exactly-1-per-pair, MO-SEVERE + IN9-FLAG assertions pass, 0 unsourced stances, headshot coverage or pinned skip
- 162-coordinate-smoke.ts green: 4 positive + 1 severe-MO negative sample
</verification>

<success_criteria>
The consolidated 33-district read-only gate proves the phase end-to-end: correct race wiring, no duplicates, MO severe-district withholding verified, IN-9 flags corrected, 0 unsourced stances, headshots covered/pinned — the MO-SEVERE and IN9-FLAG invariants ready to be inherited by the Phase 166 gate.
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-11-SUMMARY.md when done, recording the gate PASS output, the severe MO geo_ids asserted withheld, and a note that Phase 166 + Phase 164.1 must inherit the MO-SEVERE + IN9-FLAG assertions.
</output>
