---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - backend/scripts/162-mo-generate.mts
  - backend/migrations/{N}_seed_mo_2026_house_elections_races.sql
  - backend/migrations/{N1}_seed_mo_2026_house_candidates.sql
  - backend/scripts/seed-mo-house-headshots.py
  - backend/data/seed-mo-2026-house/162-02-mo-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "MO has 2 elections: 'MO 2026 Statewide General' (general, 2026-11-03) and 'MO 2026 Congressional Redistricting - Polygon Pending' (special, dated 2026-03-24 so ELECTION_VISIBILITY_WINDOW is false); 8 races wired by severity"
    - "Non-severe MO districts surface their full PROVISIONAL field on /elections; severe MO districts (per the 162-01 audit) are seeded but surface ZERO races (election_id points at the withheld election); office_id is NEVER null; essentials.offices is never touched"
    - "58 new MO politicians created (collision-free -(29*10000+cd*100+seq) band); renominated incumbents reuse existing pid (-29001..-29008); Graves MO-6 (retired) has no active row; Cori Bush reused-if-exists else new (no duplicate full_name); every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/{N}_seed_mo_2026_house_elections_races.sql"
      provides: "MO general + withheld 'Polygon Pending' election + 8 severity-routed races"
      contains: "Congressional Redistricting"
    - path: "backend/migrations/{N1}_seed_mo_2026_house_candidates.sql"
      provides: "58 new MO politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "severe MO races"
      to: "MO 2026 Congressional Redistricting - Polygon Pending election"
      via: "election_id substitution (visibility-window false)"
      pattern: "Polygon Pending"
    - from: "race_candidates insert"
      to: "district old-CD NATIONAL_LOWER office"
      via: "join by geo_id (not election name)"
      pattern: "geo_id"
---

<objective>
Seed MO's full 2026 US House field (8 districts, 58 new records — this phase's heaviest slice, comparable to WA's 60 in 161) wired to the EXISTING old-CD district rows (D-01), with SEVERE districts (from the 162-01 audit) seeded-but-withheld from /elections via a dedicated non-general "Polygon Pending" election row (D-01b) — NEVER office_id NULL, NEVER touching essentials.offices (keeps the reps feed on the true old-map incumbent). MO is first end-to-end per D-03 (earliest civic moment: Aug-4 primary, Jul-27 petition deadline). Author the 2 elections + 8 severity-routed races, create 58 new records + race_candidates (reuse incumbent pids; Graves MO-6 retired gets no active row; Cori Bush reused-if-exists), then headshots. Full field marked PROVISIONAL: (late-primary). Continues USHC3-02/03/04.

Output: 162-mo-generate.mts, two MO migrations (run on prod), seed-mo-house-headshots.py (band-scoped, 2-election variant), MO reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-01-SUMMARY.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv
@backend/scripts/161-tn-generate.mts
@backend/src/lib/electionService.ts
@backend/scripts/seed-tn-house-headshots.py

<interfaces>
MO FIPS=29; geo_ids 2901-2908 (NO CD-9). Incumbents keep existing external_id (-29001..-29008); MO-6/2906 Graves RETIRED (Withdrawn — no active row). New challengers: external_id = -(29*10000 + cd*100 + seq). Two elections:
- "MO 2026 Statewide General" (election_type general, election_date 2026-11-03) — the surfacing election
- "MO 2026 Congressional Redistricting - Polygon Pending" (election_type special, election_date 2026-03-24 — >30 days past so ELECTION_VISIBILITY_WINDOW is false; use the MO Supreme Court 4-3 upholding date, NOT TN's 2026-05-07)
ELECTION_VISIBILITY_WINDOW (electionService.ts): (e.election_type != 'general' AND e.election_date >= CURRENT_DATE - INTERVAL '30 days') OR (e.election_type = 'general' AND e.election_date >= DATE_TRUNC('year', CURRENT_DATE::date)). Non-severe races wire election_id → general; severe races (from 162-01 "Severe geo_id list:") wire election_id → withheld. ALL races: office_id = the district's existing old-CD NATIONAL_LOWER office (NEVER null). race_candidates joined by geo_id (NOT election name) so one insert block wires severe + non-severe without branching. General-race description: 'PROVISIONAL: pre-primary qualified field, cull >= 2026-08-05' (Aug-4 primary). Per-district new-record counts: MO-1=7, MO-2=10, MO-3=6, MO-4=10, MO-5=7, MO-6=9, MO-7=4, MO-8=5 (=58). MO-1: Wesley Bell (incumbent D, -29001, inc:true) AND Cori Bush (separate D filer). sqlStr()-escape every free-text field (commas/quotes, e.g. "Robert F. Onder, Jr."; "Brandon Coulter Daugherty").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-29 collision check + Cori Bush record lookup</name>
  <read_first>
    - 162-RESEARCH.md (Must-Answer #3 — collision check mandatory, 160-negative-id-audit.csv does NOT cover MO; Common Pitfalls #4 — Bush/Bell)
    - 162-PATTERNS.md (162-mo-generate.mts section — MO-1 Bush/Bell handling)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (MO incumbent pids/external_ids; Graves retired)
  </read_first>
  <action>
    Before authoring any MO migration, run the fresh scoped external_id collision check for MO's new-challenger band against prod (in a single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql). Confirm 0 rows in -290899..-290101, excluding legacy incumbent ids -29001..-29008 (different formula, not collisions — cross-check any hit against 160-incumbent-map.csv). Then run the Cori Bush lookup: SELECT id, external_id FROM essentials.politicians WHERE lower(full_name) LIKE '%bush%' AND lower(full_name) LIKE '%cori%'. Record her disposition: if a prior record exists (2021-2025 MO-1 tenure), she is a REUSE entry pointing at that pid (never recompute external_id, never duplicate full_name); if not, she is a NEW FIELD row in the challenger band. Record both outcomes (collision result + Bush pid-or-none) in the reconciliation CSV header notes.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politicians WHERE external_id BETWEEN -290899 AND -290101"</automated>
  </verify>
  <acceptance_criteria>
    - Collision query returns 0 in -290899..-290101 (or every hit confirmed a legacy incumbent id via 160-incumbent-map.csv, not a real collision)
    - Cori Bush lookup executed; her disposition (REUSE existing pid OR NEW) recorded in the reconciliation CSV
    - Graves MO-6 confirmed retired (no active race_candidates row for him)
  </acceptance_criteria>
  <done>MO band confirmed collision-free and Bush's new-vs-reuse decision is locked before any SQL is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply MO elections/races + candidates migrations</name>
  <read_first>
    - 162-PATTERNS.md (162-mo-generate.mts section — clone 161-tn-generate.mts; SEVERE_GEO_IDS, INC_EXT, external-id block, migration templates, join-by-geo_id, office_id-never-null invariant)
    - backend/scripts/161-tn-generate.mts (the exact 2-election severity-routed analog to clone)
    - 162-mo-correspondence-audit.md (the "Severe geo_id list:" to load into SEVERE_GEO_IDS)
    - 160-field-table-p162.csv (the 58 MO new records + incumbent reuse rows)
  </read_first>
  <action>
    Clone backend/scripts/161-tn-generate.mts → backend/scripts/162-mo-generate.mts. Change: SEVERE_GEO_IDS = the geo_ids from 162-mo-correspondence-audit.md's "Severe geo_id list:" line; INC_EXT = MO's -29001..-29008 (Graves MO-6 flagged vacate:true, no active row); FIELD = the 58 MO new records from 160-field-table-p162.csv (parties normalized Democratic/Republican/Libertarian, never "Democrat"); geo prefix '29'; 8 districts. Wire Wesley Bell inc:true (reuse -29001); wire Cori Bush per Task 1 (REUSE existing pid as an inc-style reuse entry, OR a NEW challenger row). Keep the -(29*10000+cd*100+seq) formula and the severe?withheld:general election branch verbatim. Set the withheld election's election_date to 2026-03-24 (MO Supreme Court upholding date), NOT TN's date. Copy the "office_id is NEVER null; essentials.offices is NEVER touched" invariant into the migration header comment verbatim. Keep the race_candidates join-by-geo_id (not election name). Use sqlStr() on every free-text field. Emit two migration files with the next unused numbers — re-check live first (ls backend/migrations | grep -E '^[0-9]+_' | sort -t_ -k1 -n | tail -1; a parallel OR session is climbing past 1205; pick a free number pair, avoid colliding with any sibling seed plan). Run node --import tsx scripts/162-mo-generate.mts, then apply both migrations to prod via psql -v ON_ERROR_STOP=1. Idempotent NOT EXISTS guards on all inserts. Write backend/data/seed-mo-2026-house/162-02-mo-reconciliation.csv listing every MO candidate with new/reuse disposition + external_id.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.name LIKE 'MO 2026%' AND rc.candidate_status='active'"</automated>
  </verify>
  <acceptance_criteria>
    - Both MO migrations applied cleanly (psql exit 0); 8 races exist, each office_id NOT NULL; essentials.offices untouched
    - Severe districts' races have election_id = the 'MO 2026 Congressional Redistricting - Polygon Pending' election; non-severe = the general
    - 58 new politicians in -290899..-290101; incumbents reuse -29001..-29008; Graves has no active row; 0 duplicate full_name per state (verify: no full_name appears twice among active MO rows)
    - Re-running both migrations is a 0-row no-op (idempotency)
  </acceptance_criteria>
  <done>MO races + candidates seeded on prod, severity-routed, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: MO headshots (band-scoped, 2-election variant)</name>
  <read_first>
    - 162-PATTERNS.md (seed-mo-house-headshots.py section — band-scoped-only BANDS 3-tuple, no election-name join; wrong-person guards copied byte-for-byte)
    - backend/scripts/seed-tn-house-headshots.py (the exact 2-election analog to clone)
    - .claude/skills/ (find-headshots conventions — trust the first-name-mismatch guard)
  </read_first>
  <action>
    Clone backend/scripts/seed-tn-house-headshots.py → backend/scripts/seed-mo-house-headshots.py. Change ONLY the module docstring, BANDS = {'MO': (-290899, -290101, 'Missouri')} (the 3-tuple band-scoped-only shape — NO election-name join, because severe-district candidates on the withheld election still need headshots), and RESULTS_JSON = '_mo-house-headshot-results.json'. Do NOT modify the wrong-person guard regexes (_NON_PERSON_TITLE / _BAD_DISAMBIG / _HISTORICAL_YEAR) — they are hardened (Bouchard/Hancock lesson). Run with py (NOT python3). Any candidate with no free-license portrait found gets a documented honest-skip. Incumbents (-29001..-29008) fall outside the band and are excluded by construction (already imaged in prior phases).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -290899 AND -290101"</automated>
  </verify>
  <acceptance_criteria>
    - seed-mo-house-headshots.py uses the 3-tuple band-scoped BANDS (no election join); wrong-person guards unchanged
    - Headshot attempted for every new MO candidate including severe-district candidates (coverage > 0% for severe districts specifically — Pitfall 3)
    - Every new candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>MO new-candidate headshots ingested (or honest-skipped) via the band-scoped script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text candidate names/sources interpolated into SQL executed against prod |
| audit severe list -> election_id routing | The severity classification decides which races surface to MO voters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-02-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | Every free-text field passes sqlStr() ("'" + s.replace(/'/g,"''") + "'") before interpolation — never raw string interpolation |
| T-162-02-02 | Tampering | A severe MO race accidentally surfacing (wrong-map data to voters) | mitigate | Severe races routed to the non-general past-dated withheld election; office_id never null; 162-11 verify.sql MO-SEVERE block + coordinate-smoke negative sample re-prove non-surfacing |
| T-162-02-03 | Repudiation | Duplicate Cori Bush record (violates 0-duplicate invariant) | mitigate | Task 1 lookup + full_name dedup guard; reuse existing pid if found |
| T-162-02-SC | Tampering | npm/psql tooling — no new packages installed this plan | accept | Zero new package installs; all tooling (tsx, psql, py) pre-vetted in 148-161 |
</threat_model>

<verification>
- Both MO migrations applied on prod (psql exit 0); 8 races, all office_id NOT NULL; essentials.offices untouched
- Severe races point at the withheld election; non-severe at the general; 58 new politicians in-band; incumbents reused; 0 duplicate full_name
- Every new MO candidate has a headshot row or a documented honest-skip
</verification>

<success_criteria>
MO's full 2026 House field is seeded on prod (58 new + incumbent reuse), severity-routed so severe districts are withheld from /elections while office_id stays populated, PROVISIONAL-marked, with headshots and a reconciliation CSV — ready for stance research (162-03/04).
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-02-SUMMARY.md when done, recording the applied migration numbers, the severe geo_ids routed to the withheld election, and Cori Bush's final disposition.
</output>
