---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 07
type: execute
wave: 7
depends_on: ["162-06"]
files_modified:
  - backend/migrations/{N}_fix_in9_incumbent_flags.sql
  - backend/scripts/162-in-generate.mts
  - backend/migrations/{N1}_seed_in_2026_house_elections_races.sql
  - backend/migrations/{N2}_seed_in_2026_house_candidates.sql
  - backend/scripts/seed-in-house-headshots.py
  - backend/data/seed-in-2026-house/162-07-in-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "The IN-9 incumbent-flag bug is FIXED FIRST: on primary race 7d3f0042-eb15-462b-bf14-df15244c5d16, Houchin is is_incumbent=true and Graham/Roark/Peck/Meyer are is_incumbent=false (verified by re-query) — BEFORE any IN general race is authored"
    - "IN has 1 election ('IN 2026 Statewide General', general, 2026-11-03) and 9 races, each office_id → the existing NATIONAL_LOWER office (never null); decided field (NOT PROVISIONAL-marked, May-5 primary done)"
    - "12 new IN politicians created (collision-free band); incumbents reuse existing external_id (mixed schemes: -18nnn legacy AND positive SoS ids 499386/499408/499413/499417); Houchin (68568faf-...) reused for the general race, never re-inserted; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/{N}_fix_in9_incumbent_flags.sql"
      provides: "Idempotent 5-row is_incumbent correction on the existing IN-9 primary race"
      contains: "7d3f0042-eb15-462b-bf14-df15244c5d16"
    - path: "backend/migrations/{N2}_seed_in_2026_house_candidates.sql"
      provides: "12 new IN politicians + active race_candidates rows (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "IN-9 general race Houchin row"
      to: "existing politician 68568faf-1e0f-4ca2-89d9-bda625665712"
      via: "hardcoded REUSE entry (never derived from the formerly-buggy primary flags)"
      pattern: "68568faf"
---

<objective>
Fix the IN-9 incumbent-flag bug FIRST (D-02, Pitfall 2), then seed IN's full 2026 US House field (9 districts, 12 new records) — vanilla new-election pattern, decided field (May-5 primary done, NOT PROVISIONAL). IN runs last with MD per D-03 (small, decided, low-urgency). The flag fix (Task 1) corrects 5 mis-flagged rows on the existing primary race BEFORE any IN general race is authored, so no incumbency-derived logic inherits the error. Then author 1 election + 9 races, create 12 new records + race_candidates (reuse incumbent pids incl. Houchin), then headshots. Continues USHC3-02/03/04.

Output: fix_in9_incumbent_flags.sql, 162-in-generate.mts, two IN seed migrations (run on prod), seed-in-house-headshots.py, IN reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
@backend/scripts/161-az-generate.mts
@backend/scripts/seed-ma-house-headshots.py

<interfaces>
IN-9 flag fix (D-02): primary race id 7d3f0042-eb15-462b-bf14-df15244c5d16 ("2026 Indiana Primary", source=sos_excel). Set is_incumbent=true on Houchin's race_candidates row id 9d2de2ae-2fef-48b6-b3d4-2787166b78df; set is_incumbent=false on Graham 037ad94f-d379-4f9c-baf1-a75742a46eac, Roark 283b1fdd-3d89-4f82-a065-098324f2f967, Peck a9233775-b8af-423e-aea6-734c2855e5ac, Meyer 926943ad-ee64-4ddb-b9e7-6475a6a2d087. Guard each UPDATE with WHERE is_incumbent = {opposite value} → idempotent 0-row no-op on re-apply. Do NOT delete the primary race or rows (D-02a). RE-VERIFY both id sets (candidate_pid vs race_candidates.id) with a LIVE SELECT before authoring — the audit CSV's column semantics must be confirmed, not positionally inferred (Assumptions Log A2).

IN seed: FIPS=18; geo_ids 1801-1809. New challengers: external_id = -(18*10000+cd*100+seq). Incumbents keep existing external_id — MIXED schemes: legacy -18nnn for some, POSITIVE SoS ids for Baird 499386, Carson 499408, Messmer 499413, Houchin 499417 — confirm each incumbent's exact external_id per 160-incumbent-map.csv row-by-row (do NOT assume uniform -fips*1000+cd). Houchin's politician_id = 68568faf-1e0f-4ca2-89d9-bda625665712 → reuse as a hardcoded inc:true REUSE entry for the IN-9 general race; NEVER insert a new Houchin record; NEVER derive her incumbency from the (formerly-buggy) primary flags. One election: 'IN 2026 Statewide General' (general, 2026-11-03). All races office_id = existing NATIONAL_LOWER office (geo prefix 18). Decided field → description e.g. 'Confirmed nominee + declared-so-far minor-party field (primary decided May-5)' — do NOT use the PROVISIONAL wording. Per-district new-record counts: IN-1=1, IN-2=2, IN-3=1, IN-4=1, IN-5=1, IN-6=1, IN-7=2, IN-8=1, IN-9=2 (=12). sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the IN-9 incumbent flags (D-02) — BEFORE any IN seeding</name>
  <read_first>
    - 162-PATTERNS.md (fix_in9_incumbent_flags.sql section — the exact idempotent UPDATE SQL + ordering requirement)
    - 162-RESEARCH.md (Must-Answer #2 + Pattern 4 — exact rows/ids; Assumptions Log A2 — live-SELECT re-verify)
    - 160-race-preexistence-audit.csv (the 5 IN-9 primary rows)
  </read_first>
  <action>
    FIRST run a live SELECT against prod to re-verify the 5 target rows on race 7d3f0042-eb15-462b-bf14-df15244c5d16 — confirm Houchin's race_candidates.id (9d2de2ae-...) and the 4 opponents' ids, and their current (buggy) is_incumbent values — do NOT trust the audit CSV positional read alone. Then author backend/migrations/{N}_fix_in9_incumbent_flags.sql: an UPDATE setting Houchin's row is_incumbent=true (guarded WHERE is_incumbent=false) and a second UPDATE setting the 4 opponents' rows is_incumbent=false (guarded WHERE is_incumbent=true), inside BEGIN/COMMIT. Do NOT delete the primary race or its rows. Emit with the next unused migration number (re-check live). Apply to prod via psql -v ON_ERROR_STOP=1. This migration MUST be applied before the IN general-race migration in Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FILTER (WHERE is_incumbent) AS inc_true, count(*) FILTER (WHERE NOT is_incumbent) AS inc_false FROM essentials.race_candidates WHERE race_id = '7d3f0042-eb15-462b-bf14-df15244c5d16'"</automated>
  </verify>
  <acceptance_criteria>
    - Live re-verify of the 5 row ids performed before authoring the UPDATE (A2 mitigation)
    - On race 7d3f0042-...: exactly 1 is_incumbent=true row (Houchin, id 9d2de2ae-...) and the 4 opponents are is_incumbent=false
    - Re-applying the migration is a 0-row no-op; the primary race and its rows are NOT deleted
  </acceptance_criteria>
  <done>IN-9 primary flags corrected on prod and verified BEFORE any IN general race exists.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh FIPS-18 collision check + generate/apply IN seed migrations + headshots</name>
  <read_first>
    - 162-PATTERNS.md (162-in-generate.mts section — clone 161-az-generate.mts; IN mixed external_id schemes; Houchin hardcoded REUSE; decided-not-PROVISIONAL wording; seed-in-house-headshots.py section)
    - backend/scripts/161-az-generate.mts (the vanilla new-election analog)
    - 160-field-table-p162.csv (the 12 IN new records) + 160-incumbent-map.csv (IN incumbent external_ids, mixed schemes)
    - backend/scripts/seed-ma-house-headshots.py (1-election headshot analog)
  </read_first>
  <action>
    Run the fresh FIPS-18 collision check (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql) — confirm 0 rows in -180999..-180101 excluding legacy incumbent ids (cross-check any hit vs 160-incumbent-map.csv). Clone 161-az-generate.mts → 162-in-generate.mts: FIPS 18, 9 districts, INC_EXT per-incumbent from 160-incumbent-map.csv (mixed -18nnn AND positive SoS ids 499386/499408/499413; Houchin 499417 with politician_id 68568faf-... wired as a hardcoded inc:true REUSE); FIELD = the 12 IN new records; election 'IN 2026 Statewide General'; description the decided-field wording (NOT PROVISIONAL). Keep -(18*10000+cd*100+seq), single-election template, NOT EXISTS guards, sqlStr(). Emit two migration files with the next unused numbers AFTER the Task-1 flag-fix migration (re-check live HWM). Run node --import tsx scripts/162-in-generate.mts, apply both to prod via psql -v ON_ERROR_STOP=1. Then clone seed-ma-house-headshots.py → seed-in-house-headshots.py with BANDS = {'IN': (-180999, -180101, 'Indiana', 'IN 2026 Statewide General')} (verify band bounds against the emitted range) and RESULTS_JSON '_in-house-headshot-results.json'; wrong-person guards unchanged; run with py. Write backend/data/seed-in-2026-house/162-07-in-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='IN 2026 Statewide General'"</automated>
  </verify>
  <acceptance_criteria>
    - FIPS-18 collision query returned 0 (or only legacy-incumbent hits) before authoring
    - Both IN seed migrations applied cleanly (psql exit 0); 9 races, all office_id NOT NULL (FILTER count 0); description is the decided-field wording, NOT PROVISIONAL
    - 12 new politicians; incumbents reuse their exact existing external_ids (mixed schemes); Houchin reused via 68568faf-... on IN-9 general (no new Houchin record); 0 duplicate full_name
    - Every new IN candidate has a headshot row or documented honest-skip; migrations idempotent
  </acceptance_criteria>
  <done>IN races + candidates seeded on prod (post-flag-fix), decided-field, with headshots and a reconciliation CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |
| stale primary-race flags -> incumbency logic | The buggy is_incumbent flags could poison any incumbency-derived logic if not fixed first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07-01 | Tampering | IN general race authored before the flag fix → wrong incumbent inherited (Pitfall 2) | mitigate | Flag-fix migration is Task 1, applied + verified before Task 2's general race; Houchin's row wired from her hardcoded pid, never derived from primary flags |
| T-162-07-02 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field |
| T-162-07-03 | Repudiation | Duplicate Houchin record | mitigate | Reuse 68568faf-... as a hardcoded REUSE entry; 0-duplicate full_name assertion in 162-11 |
| T-162-07-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- IN-9 primary race: exactly 1 is_incumbent=true (Houchin), 4 false — verified before IN general race exists
- Both IN seed migrations applied; 9 races, all office_id NOT NULL; decided-field wording
- 12 new politicians in-band; incumbents reused (mixed schemes); Houchin reused; 0 duplicate full_name; headshots or honest-skips
</verification>

<success_criteria>
IN-9 flags corrected first, then IN's full decided 2026 House field seeded on prod (12 new + incumbent reuse) surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (162-09).
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-07-SUMMARY.md when done, recording the flag-fix migration number, the IN seed migration numbers, and the IN new-candidate external_id range.
</output>
