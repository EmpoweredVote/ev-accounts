---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md
autonomous: true
requirements: [USHC3-03]
must_haves:
  truths:
    - "Every MO district (geo_id 2901-2908) has an old-vs-new boundary-shift severity score derived from named sources"
    - "The set of MO geo_ids classified SEVERE (seeded-but-withheld per D-01b) is explicitly enumerated with rationale as a machine-consumable line"
    - "MO-5/2905 (Cleaver, Kansas City — the 2025 remap's primary target) is adjudicated; MO-4/2904 and MO-6/2906 adjacency reviewed explicitly; all 8 districts scored (no shortcut to 'just MO-5')"
  artifacts:
    - path: ".planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md"
      provides: "Per-district severity table + severe-geo_id list + rubric (cloned verbatim from TN) + source URLs"
      contains: "Severe geo_id list"
      min_lines: 40
  key_links:
    - from: "audit severe list"
      to: "162-02 withheld-election wiring + 162-11 verify.sql MO-SEVERE assertion + coordinate-smoke SEVERE_MO_GEO_ID"
      via: "severe geo_id enumeration consumed downstream"
      pattern: "Severe geo_id list"
---

<objective>
Produce the MO old-vs-new congressional map correspondence audit (locked decision D-01a) — the phase's FIRST artifact, front-loaded, on which all MO seeding depends. MO is redistricted for 2026 (2025 GOP-drawn map upheld by the MO Supreme Court 4-3 on 2026-03-24; the pending referendum does not affect this cycle). essentials.districts / essentials.geofence_boundaries still carry the OLD (pre-2025) MO polygons (they also drive the reps feed for members representing the old districts until Jan 2027). This audit scores each old-numbered district by boundary-shift severity so 162-02 (MO seed) knows which districts to seed-but-withhold from /elections (D-01b) rather than surfacing a new-map-correct slate against a stale polygon.

Purpose: Prevent serving wrong House races to MO voters while keeping the reps feed correct. Mirrors the TN audit (161-01) exactly.
Output: 162-mo-correspondence-audit.md with a per-district severity table, an enumerated severe-geo_id list, the (verbatim-cloned TN) rubric, and source URLs. No DB writes, no code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-CONTEXT.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/161-wa-az-tn-ma-candidate-seeding-create-elections-races-then-ca/161-tn-correspondence-audit.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-03-SUMMARY.md

<interfaces>
Pure-research artifact. The one downstream contract is the severe-geo_id set. MO geo_ids are 2901..2908 (FIPS 29 + zero-padded CD; MO has NO CD-9). The severe list is consumed by 162-02 (severe races wire election_id to a withheld election row) and 162-11 (verify.sql asserts each severe race's parent election is the withheld one, + a negative coordinate-smoke sample). Starting hypothesis (NOT pre-decided): MO-5/2905 (Cleaver, Kansas City) is the remap's primary target, redrawn more Republican by adding rural counties; MO-4/2904 and MO-6/2906 flagged for adjacency review. RESEARCH Pitfall 1: TN's own audit found 5/9 severe against a working assumption of ~2 — budget for a severe set larger than {2905}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather old-vs-new MO district composition from named sources</name>
  <read_first>
    - 162-RESEARCH.md (Must-Answer #1 — rubric + predicted severe districts; Common Pitfalls #1 — under-scoping risk; Sources)
    - 160-03-SUMMARY.md (MO redistricting provenance: 2025 GOP map upheld 4-3 on 2026-03-24; MO SoS ASPX fetch-wall → use Wikipedia raw wikitext)
    - 161-tn-correspondence-audit.md (the exact structure + evidence-basis discipline to mirror)
  </read_first>
  <action>
    For each of MO's 8 old-numbered districts (geo_id 2901-2908) gather the OLD-map and NEW-map county composition. Pull the NEW map from the Wikipedia "2026 United States House of Representatives elections in Missouri" article and any dedicated "2026 Missouri redistricting" article (raw wikitext via Special:Export if the rendered page is thin), cross-confirmed against MO Supreme Court 4-3 ruling coverage (2026-03-24) and local reporting on the Cleaver/MO-5 remap (Kansas City Star / Missouri Independent / AP). MO SoS's own ASPX pages render only nav boilerplate — do NOT rely on them; use Wikipedia/news per the fetch-wall intel. For the OLD map use the pre-2025 district-to-county composition (a qualitative county-level breakdown suffices — do NOT fetch/parse shapefiles or install any GIS library). Record which counties moved from each old CD to which new CD, paying explicit attention to MO-5 (Kansas City / Jackson County core) and any MO-4/MO-6 counties absorbed or shed. Re-confirm the 2025 map is still in effect (no referendum/litigation reversal) as of execution date.
  </action>
  <verify>
    <automated>echo research-step-verified-in-task-2</automated>
  </verify>
  <acceptance_criteria>
    - County-level old-to-new movement recorded for all 8 MO districts, each backed by a named source URL
    - MO-5 (Kansas City core) county reassignments documented explicitly; MO-4 and MO-6 adjacency moves documented
    - Current status of the 2025 map (upheld / not reversed) re-confirmed as of execution date
  </acceptance_criteria>
  <done>Old-vs-new composition data collected for all 8 MO districts with source citations, ready to score.</done>
</task>

<task type="auto">
  <name>Task 2: Score severity per district and write the audit artifact</name>
  <read_first>
    - 162-PATTERNS.md (the 162-mo-correspondence-audit.md section — verbatim rubric + table columns + "Notes for downstream consumers" pattern)
    - 161-tn-correspondence-audit.md (the "Severe geo_id list:" contract line format to reproduce exactly)
  </read_first>
  <action>
    Write .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md. Clone the TN rubric verbatim (a district is SEVERE if >25% of its population moved to a different CD OR its core anchor city/county changed; otherwise NOT-SEVERE; evidence basis = qualitative county-level reporting + a quantitative proxy from Dave's Redistricting App via Wikipedia + explicit "anchor changed" statements). The file MUST contain (1) the rubric definition; (2) a markdown table with columns geo_id, old_cd, incumbent, anchor_county_old, anchor_county_new, pct_population_moved (or qualitative low/med/high with a stated note if exact percent unavailable), severity, rationale — one row per MO district (8 rows); (3) a line beginning exactly "Severe geo_id list:" followed by the comma-separated severe geo_ids, or "Severe geo_id list: (none)" if empty — the machine-consumable contract for 162-02 and 162-11; (4) source URLs (all fetched during this audit; do NOT reuse any TN URL). Adjudicate all 8 districts explicitly — leave none unscored. Do NOT treat MO-5/4/6 as pre-decided; score them on the gathered evidence. In a closing "Notes for downstream consumers" section, state which districts confirmed vs expanded the pre-audit hypothesis and flag the under-scoping risk per RESEARCH Pitfall 1. Do NOT write to the database and do NOT touch essentials.offices.
  </action>
  <verify>
    <automated>test -f .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md && grep -q "Severe geo_id list:" .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-mo-correspondence-audit.md && echo FOUND</automated>
  </verify>
  <acceptance_criteria>
    - 162-mo-correspondence-audit.md exists with the verbatim TN rubric, an 8-row severity table, and a "Severe geo_id list:" line (comma-separated geo_ids or "(none)")
    - Every district row has a severity value and a rationale; no district is unscored
    - Each severe classification cites a named source URL; a closing note flags the under-scoping risk
  </acceptance_criteria>
  <done>The MO severity audit artifact is written and its severe geo_id list is consumable by 162-02 and 162-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external redistricting sources -> audit artifact | Third-party reporting is transcribed into a decision artifact that gates what MO voters see on /elections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01-01 | Tampering | Severity mis-classification (a severe district scored not-severe, so wrong-map data surfaces) | mitigate | Anchor-county-changed OR >25%-moved rubric applied per district with rationale; all 8 districts reviewed (no MO-5-only shortcut); 162-11 coordinate smoke re-proves severe districts surface zero races end-to-end |
| T-162-01-02 | Repudiation | Undocumented severity claims | mitigate | Every row cites a named source URL; rubric stated in-file so classifications are reproducible |
| T-162-01-03 | Information disclosure | Stale legal facts (map reversed by referendum/litigation since research) | accept-with-check | Re-verify the 2025 map is still in effect at execution (7-day freshness); low risk (upheld 2026-03-24) |
</threat_model>

<verification>
- 162-mo-correspondence-audit.md exists with rubric, an 8-row per-district table, and a machine-consumable "Severe geo_id list:" line
- All 8 MO districts scored from named sources; MO-5/4/6 adjudicated on evidence, not assumption
- No DB writes occurred and essentials.offices was not touched
</verification>

<success_criteria>
All 8 MO districts are severity-scored from named sources, the severe geo_id set is explicitly enumerated as a machine-consumable line, and the artifact is ready to gate MO seeding (162-02) and verification (162-11).
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-01-SUMMARY.md when done, recording the severe geo_id list verbatim so 162-02 and 162-11 inherit it.
</output>
