---
phase: 162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca
plan: 08
type: execute
wave: 7
depends_on: ["162-06"]
files_modified:
  - backend/scripts/162-md-generate.mts
  - backend/migrations/{N}_seed_md_2026_house_candidates.sql
  - backend/scripts/seed-md-house-headshots.py
  - backend/data/seed-md-2026-house/162-08-md-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "MD REUSES its 8 existing_race_id general races — NO new elections/races rows authored; all 8 incumbents + 13 new challengers get fresh race_candidates INSERTs onto the 8 existing races"
    - "13 new MD politicians created (collision-free -(24*10000+cd*100+seq) band); 8 incumbents reuse existing external_id (-2440001..-2440008); Hoyer MD-5 (retired) gets no active row; 0 duplicate full_name; exactly 1 race_candidates row per (race_id, politician_id)"
    - "Decided field (Jun-23 primary done, open unaffiliated/minor window to Aug-3); every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/{N}_seed_md_2026_house_candidates.sql"
      provides: "MD candidates-only insert onto 8 existing races (idempotent, NOT EXISTS on race_id+politician_id)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "MD race_candidates rows"
      to: "the 8 existing_race_id UUIDs (2401→cb9a70c8...  ...  2408→52874d42...)"
      via: "candidates-only insert (no new races)"
      pattern: "race_candidates"
---

<objective>
Seed MD's full 2026 US House field (8 districts, 13 new records) as a CANDIDATES-ONLY insert onto MD's 8 pre-existing scaffolded races (D-04, MA-reuse pattern) — NO new elections/races rows. MD runs last with IN per D-03 (small, decided, low-urgency). RESEARCH confirmed 0 pre-existing MD race_candidates rows (unlike MA's Clark/Pressley), so all 8 incumbents + 13 new challengers get fresh race_candidates INSERTs. Decided field (Jun-23 primary done; open unaffiliated/minor window to Aug-3 → seed nominees + declared minors now, Phase 167 catches late independents). Then headshots. Continues USHC3-02/03/04.

Output: 162-md-generate.mts, one MD candidates migration (run on prod), seed-md-house-headshots.py, MD reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-RESEARCH.md
@.planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p162.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
@backend/scripts/161-ma-generate.mts
@backend/scripts/seed-ma-house-headshots.py

<interfaces>
MD FIPS=24; geo_ids 2401-2408. Reuse the 8 EXISTING_RACE_ID general races (do NOT author new races): 2401→cb9a70c8-626b-43ed-9267-9531d2403535, 2402→01c39962-63fe-4f5d-ac56-a546e09374a6, 2403→34ae857f-a68e-40c6-a1bd-1bc9266dce5b, 2404→1df5607b-36f4-45f6-a94f-adabba5811da, 2405→b927bbd3-be7b-4ca1-a5f0-0fcd743a9997, 2406→d5d7f27a-e421-46d8-af73-da225ea625a0, 2407→a6b83f0d-bc99-4f29-9d39-a87025d55a01, 2408→52874d42-9b2b-47c9-a87b-c11801627eb2. Incumbents keep existing external_id (-2440001..-2440008); MD-5/2405 Hoyer RETIRED (Boafo won primary) → no active row. New challengers: external_id = -(24*10000+cd*100+seq). 0 pre-existing MD race_candidates rows (confirmed) → no Clark/Pressley-style skip branch; every incumbent gets a fresh REUSE row; keep the NOT EXISTS (race_id, politician_id) guard for idempotency. Decided field → do NOT PROVISIONAL-mark; optionally set description 'Confirmed nominees + declared-so-far minor-party field; unaffiliated window open to 2026-08-03'. Per-district new-record counts: MD-1=1, MD-2=1, MD-3=1, MD-4=1, MD-5=4 (open, Hoyer retired: Boafo-D, Chaffee-R, Burruss-Ind, Jordan-Ind), MD-6=2, MD-7=1, MD-8=2 (=13). MD election name for the headshot join must be looked up live from the 8 existing_race_id rows (do NOT guess). sqlStr()-escape every free-text field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh FIPS-24 collision check + confirm 0 pre-existing MD race_candidates</name>
  <read_first>
    - 162-RESEARCH.md (Must-Answer #3 — collision check; Must-Answer #4 — MD reuse + 0 pre-existing rows confirmation)
    - 160-race-preexistence-audit.csv (MD's 8 existing_race_id rows, all race_candidates columns blank)
    - 160-incumbent-map.csv (MD incumbent pids/external_ids; Hoyer retired)
  </read_first>
  <action>
    Run the fresh FIPS-24 collision check (single compound command: cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql) — confirm 0 rows in -240899..-240101 excluding legacy incumbent ids -2440001..-2440008 (cross-check any hit vs 160-incumbent-map.csv). Then confirm 0 pre-existing race_candidates rows on the 8 MD existing_race_id UUIDs (SELECT count(*) FROM essentials.race_candidates WHERE race_id = ANY(ARRAY[...8 UUIDs...])) — expect 0, matching RESEARCH Must-Answer #4. Also look up the election name(s) for those 8 races (SELECT DISTINCT el.name FROM essentials.elections el JOIN essentials.races r ON r.election_id=el.id WHERE r.id = ANY(ARRAY[...])) for the headshot join. Confirm Hoyer MD-5 retired (no active row).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.race_candidates WHERE race_id = ANY(ARRAY['cb9a70c8-626b-43ed-9267-9531d2403535','01c39962-63fe-4f5d-ac56-a546e09374a6','34ae857f-a68e-40c6-a1bd-1bc9266dce5b','1df5607b-36f4-45f6-a94f-adabba5811da','b927bbd3-be7b-4ca1-a5f0-0fcd743a9997','d5d7f27a-e421-46d8-af73-da225ea625a0','a6b83f0d-bc99-4f29-9d39-a87025d55a01','52874d42-9b2b-47c9-a87b-c11801627eb2']::uuid[])"</automated>
  </verify>
  <acceptance_criteria>
    - FIPS-24 collision query returns 0 in -240899..-240101 (or only legacy-incumbent hits)
    - 0 pre-existing race_candidates rows on the 8 MD existing_race_id UUIDs (the count query returns 0)
    - MD election name(s) for the 8 races recorded for the headshot join; Hoyer confirmed retired
  </acceptance_criteria>
  <done>MD band collision-free, reuse-races confirmed empty, election name captured — ready to generate.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply MD candidates-only migration + headshots</name>
  <read_first>
    - 162-PATTERNS.md (162-md-generate.mts section — clone 161-ma-generate.mts; EXISTING_RACE_ID map; delete Clark/Pressley skip branch; NO PROVISIONAL UPDATE; NOT EXISTS guard; seed-md-house-headshots.py section)
    - backend/scripts/161-ma-generate.mts (the races-only reuse analog)
    - 160-field-table-p162.csv (the 13 MD new records + 8 incumbents)
    - backend/scripts/seed-ma-house-headshots.py (headshot analog)
  </read_first>
  <action>
    Clone 161-ma-generate.mts → 162-md-generate.mts: EXISTING_RACE_ID = the 8 MD UUIDs above; INC_PID/INC_NAME = MD's 8 incumbents (Harris/Olszewski/Elfreth/Ivey/Hoyer[retired,no row]/McClain Delaney/Mfume/Raskin) with their existing external_ids -2440001..-2440008; FIELD = the 13 MD new records; parties normalized (Democratic/Republican/Green/etc, never "Democrat"). DELETE the Clark/Pressley "already-wired-skip" branch entirely (MD has 0 pre-wired rows) — every incumbent (except retired Hoyer) gets a fresh REUSE-ADD-ROW insert. Do NOT author any elections/races INSERT (candidates-only). Do NOT copy MA's PROVISIONAL-marking UPDATE; optionally set description to the decided/open-window wording. Keep the NOT EXISTS (race_id, politician_id) idempotency guard and -(24*10000+cd*100+seq) formula and sqlStr(). Emit one migration file with the next unused number (re-check live HWM; avoid colliding with sibling plan 162-07's IN migrations if run concurrently — claim a distinct free number). Run node --import tsx scripts/162-md-generate.mts, apply to prod via psql -v ON_ERROR_STOP=1. Then clone seed-ma-house-headshots.py → seed-md-house-headshots.py with BANDS = {'MD': (-240899, -240101, 'Maryland', <the live-looked-up MD election name from Task 1>)} and RESULTS_JSON '_md-house-headshot-results.json'; wrong-person guards unchanged; run with py. Write backend/data/seed-md-2026-house/162-08-md-reconciliation.csv.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*), max(c) FROM (SELECT race_id, politician_id, count(*) c FROM essentials.race_candidates WHERE race_id = ANY(ARRAY['cb9a70c8-626b-43ed-9267-9531d2403535','01c39962-63fe-4f5d-ac56-a546e09374a6','34ae857f-a68e-40c6-a1bd-1bc9266dce5b','1df5607b-36f4-45f6-a94f-adabba5811da','b927bbd3-be7b-4ca1-a5f0-0fcd743a9997','d5d7f27a-e421-46d8-af73-da225ea625a0','a6b83f0d-bc99-4f29-9d39-a87025d55a01','52874d42-9b2b-47c9-a87b-c11801627eb2']::uuid[]) GROUP BY race_id, politician_id) s"</automated>
  </verify>
  <acceptance_criteria>
    - MD migration applied cleanly (psql exit 0); race_candidates rows exist on all 8 existing races; NO new elections/races rows created
    - Exactly 1 row per (race_id, politician_id) — the max(c) in the verify query is 1 (no duplicate wiring)
    - 13 new politicians in -240899..-240101; 7 active incumbents reuse -2440001..-2440008 (Hoyer no row); 0 duplicate full_name
    - Every new MD candidate has a headshot row or documented honest-skip; migration idempotent (0-row no-op on re-apply)
  </acceptance_criteria>
  <done>MD candidates wired onto the 8 existing races on prod, no duplicates, with headshots and a reconciliation CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-08-01 | Tampering (SQLi) | Unescaped candidate names in generated SQL | mitigate | sqlStr() on every free-text field |
| T-162-08-02 | Repudiation | Duplicate race_candidates wiring on an existing race | mitigate | NOT EXISTS (race_id, politician_id) guard; exactly-1-per-pair assertion in verify + 162-11 |
| T-162-08-03 | Tampering | Accidentally authoring new MD races (violates D-04 reuse) | mitigate | Candidates-only generator (no elections/races INSERT); Task 1 confirms the 8 existing races are the wiring targets |
| T-162-08-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs |
</threat_model>

<verification>
- MD migration applied; race_candidates on all 8 existing races; no new races created; exactly 1 row per (race_id, politician_id)
- 13 new politicians in-band; 7 incumbents reused (Hoyer no row); 0 duplicate full_name
- Every new MD candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
MD's full decided 2026 House field is wired onto its 8 existing races on prod (13 new + 7 incumbent reuse, no duplicates), surfacing on /elections, with headshots and a reconciliation CSV — ready for stance research (162-10).
</success_criteria>

<output>
Create .planning/phases/162-in-md-mn-mo-candidate-seeding-create-elections-races-then-ca/162-08-SUMMARY.md when done, recording the applied migration number, the MD election name (for downstream), and the MD new-candidate external_id range.
</output>
