---
phase: 51-essentials-xp-source-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env.example
  - docs/ESSENTIALS-INTEGRATION.md
autonomous: false
user_setup:
  - service: render
    why: "Provision ESSENTIALS_SERVICE_KEY env var for ev-accounts-api"
    env_vars:
      - name: ESSENTIALS_SERVICE_KEY
        source: "Generate with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\". Then set in Render Dashboard -> ev-accounts-api -> Environment -> Add env var. Share the value with the Essentials team."
    dashboard_config:
      - task: "Add ESSENTIALS_SERVICE_KEY environment variable"
        location: "Render Dashboard -> ev-accounts-api -> Environment"

must_haves:
  truths:
    - "ESSENTIALS_SERVICE_KEY is set in the Render dashboard for ev-accounts-api"
    - ".env.example documents the key name and its authorized XP source"
    - "ESSENTIALS-INTEGRATION.md has a dedicated XP Service Key subsection with env var name, source string, and verification curl"
    - "A curl call to POST /api/xp/award with the provisioned key and source essentials-rep-lookup returns HTTP 200"
  artifacts:
    - path: "backend/.env.example"
      provides: "Descriptive comment for ESSENTIALS_SERVICE_KEY"
      contains: "essentials-rep-lookup"
    - path: "docs/ESSENTIALS-INTEGRATION.md"
      provides: "XP Service Key setup subsection in Section 8"
      contains: "ESSENTIALS_SERVICE_KEY"
  key_links:
    - from: "Render env var ESSENTIALS_SERVICE_KEY"
      to: "backend/src/middleware/serviceKeyAuth.ts"
      via: "process.env.ESSENTIALS_SERVICE_KEY read at module load"
      pattern: "ESSENTIALS_SERVICE_KEY"
    - from: "docs/ESSENTIALS-INTEGRATION.md"
      to: "backend/src/middleware/serviceKeyAuth.ts"
      via: "documented env var name matches code"
      pattern: "ESSENTIALS_SERVICE_KEY"
---

<objective>
Provision the ESSENTIALS_SERVICE_KEY environment variable and update documentation so the Essentials service can award XP through the accounts API.

Purpose: Close the last operational gap from v1.8 — the backend code already maps ESSENTIALS_SERVICE_KEY to the "essentials-rep-lookup" XP source, but the key has never been assigned a real value in production.

Output: Updated `.env.example` with descriptive comment, updated `ESSENTIALS-INTEGRATION.md` with dedicated XP Service Key subsection, and a verified live key in Render.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-essentials-xp-source-provisioning/51-RESEARCH.md
@backend/.env.example
@docs/ESSENTIALS-INTEGRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example and ESSENTIALS-INTEGRATION.md</name>
  <files>backend/.env.example, docs/ESSENTIALS-INTEGRATION.md</files>
  <action>
  Two file edits, no code changes:

  1. **backend/.env.example** — Replace the existing service key block (lines 9-13) with commented versions. Change:
  ```
  # XP service keys — one per feature repo
  QUEST_SERVICE_KEY=your-quest-service-key
  TRIVIA_SERVICE_KEY=your-trivia-service-key
  ADMIN_SERVICE_KEY=your-admin-service-key
  ESSENTIALS_SERVICE_KEY=your-essentials-service-key
  ```
  To:
  ```
  # XP service keys — one per feature repo; each key authorizes specific XP source(s)
  QUEST_SERVICE_KEY=your-quest-service-key           # source: "validation_quest_completion"
  TRIVIA_SERVICE_KEY=your-trivia-service-key         # source: "civic_trivia_championship_score"
  ADMIN_SERVICE_KEY=your-admin-service-key           # source: "admin_gift"
  ESSENTIALS_SERVICE_KEY=your-essentials-service-key # source: "essentials-rep-lookup"
  ```

  NOTE: Verify the actual source strings for QUEST, TRIVIA, and ADMIN keys by reading `backend/src/middleware/serviceKeyAuth.ts` before writing — the research doc lists them but confirm from source. If only ESSENTIALS source is verifiable, only add the ESSENTIALS comment; leave the others unchanged.

  2. **docs/ESSENTIALS-INTEGRATION.md** — Add a new subsection `### XP Service Key` inside Section 8, immediately after the existing "Coordination Required Before Going Live" block (after line 423). Content:

  ```markdown
  ### XP Service Key

  The Essentials backend uses `ESSENTIALS_SERVICE_KEY` to authorize XP awards via `POST /api/xp/award`.

  | Item | Value |
  |------|-------|
  | Accounts env var | `ESSENTIALS_SERVICE_KEY` |
  | Essentials env var | `ESSENTIALS_SERVICE_KEY` (same name, value provided by Accounts team) |
  | Authorized XP source | `"essentials-rep-lookup"` |
  | Header | `X-Service-Key: {key value}` |

  **Setup steps:**
  1. Accounts team provisions the key in Render (`ev-accounts-api` service environment).
  2. Accounts team shares the key value with the Essentials team via secure channel.
  3. Essentials team sets `ESSENTIALS_SERVICE_KEY` in their own deployment environment.
  4. Use in award calls: `'X-Service-Key': process.env.ESSENTIALS_SERVICE_KEY`
  5. Source string in request body: `"essentials-rep-lookup"` — use exactly this string.

  **Verify the key works:**
  ```bash
  curl -s -X POST https://accounts.empowered.vote/api/xp/award \
    -H "Content-Type: application/json" \
    -H "X-Service-Key: $ESSENTIALS_SERVICE_KEY" \
    -d '{"user_id":"<connected-user-uuid>","source":"essentials-rep-lookup","amount":10,"idempotency_key":"essentials:key-test:<unique>"}'
  ```
  Expected: HTTP 200 with `"is_duplicate": false` on first call.
  ```

  Also update the "Last updated" line at the bottom of the file from `2026-03-19` to `2026-04-02`.
  </action>
  <verify>
  - `grep "essentials-rep-lookup" backend/.env.example` returns the commented line
  - `grep "### XP Service Key" docs/ESSENTIALS-INTEGRATION.md` returns the new subsection heading
  - `grep "2026-04-02" docs/ESSENTIALS-INTEGRATION.md` confirms updated date
  </verify>
  <done>
  - backend/.env.example has inline comments on all XP service keys documenting their authorized source strings
  - docs/ESSENTIALS-INTEGRATION.md Section 8 has a dedicated "XP Service Key" subsection with env var name, authorized source, setup steps, and verification curl
  - Last updated date reflects today
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Provision ESSENTIALS_SERVICE_KEY in Render dashboard</action>
  <instructions>
  1. Generate a secret: run `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` in your terminal
  2. Copy the output value
  3. Go to Render Dashboard -> ev-accounts-api -> Environment
  4. Add environment variable: Name = `ESSENTIALS_SERVICE_KEY`, Value = the generated hex string
  5. Save — Render will auto-redeploy
  6. Wait for the deploy to complete (check Render deploy logs)
  7. Save the key value somewhere secure — you will need to share it with the Essentials team
  </instructions>
  <resume-signal>Type "provisioned" once the Render deploy completes, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ESSENTIALS_SERVICE_KEY provisioned in Render, .env.example commented, integration doc updated with XP Service Key subsection</what-built>
  <how-to-verify>
  Run this smoke test (replace placeholders with real values):

  ```bash
  ESSENTIALS_KEY="<the-key-you-set-in-render>"
  CONNECTED_USER_UUID="4e6dde8f-..."  # Chris's UUID or any known Connected user

  curl -s -w "\nHTTP %{http_code}" \
    -X POST https://accounts.empowered.vote/api/xp/award \
    -H "Content-Type: application/json" \
    -H "X-Service-Key: $ESSENTIALS_KEY" \
    -d "{
      \"user_id\": \"$CONNECTED_USER_UUID\",
      \"source\": \"essentials-rep-lookup\",
      \"amount\": 10,
      \"idempotency_key\": \"essentials:smoke-test:phase51:$(date +%s)\"
    }"
  ```

  Expected: HTTP 200, response body contains `"is_duplicate": false` (or `true` if run twice with same idempotency key).

  If you get HTTP 401: the key was not picked up — check Render deploy completed.
  If you get HTTP 403 NOT_CONNECTED: the user_id is not a Connected user — use a different UUID.
  If you get HTTP 422: check source string is exactly `"essentials-rep-lookup"`.
  </how-to-verify>
  <resume-signal>Type "verified" with the HTTP status code, or describe the error</resume-signal>
</task>

</tasks>

<verification>
1. `grep "essentials-rep-lookup" backend/.env.example` — confirms inline source comment
2. `grep "### XP Service Key" docs/ESSENTIALS-INTEGRATION.md` — confirms new subsection exists
3. Smoke test curl returns HTTP 200 — confirms end-to-end key provisioning
</verification>

<success_criteria>
- backend/.env.example has descriptive comments on all XP service keys including ESSENTIALS_SERVICE_KEY
- docs/ESSENTIALS-INTEGRATION.md Section 8 contains a dedicated "XP Service Key" subsection
- ESSENTIALS_SERVICE_KEY is live in Render for ev-accounts-api
- POST /api/xp/award with the provisioned key and source "essentials-rep-lookup" returns HTTP 200
</success_criteria>

<output>
After completion, create `.planning/phases/51-essentials-xp-source-provisioning/51-01-SUMMARY.md`
</output>
