---
phase: 142-stance-research-wave-1-governors-ags
plan: 10
type: execute
wave: 3
depends_on: [142-02, 142-03, 142-04, 142-05, 142-06, 142-07, 142-08, 142-09]
files_modified:
  - backend/scripts/verify-phase-142.sql
autonomous: true
requirements: [SEXS-02]
must_haves:
  truths:
    - "verify-phase-142.sql is a read-only labeled-assertion gate (psql -v ON_ERROR_STOP=1) keyed on (STATE_EXEC, role_canonical IN governor/attorney_general) - NOT external_id ranges"
    - "Coverage assertion: every in-scope Gov+AG (80, or 80 minus documented honest-skips pinned to exact external_ids) has >=1 inform.politician_answers row"
    - "Zero-unsourced assertion: zero answer rows for STATE_EXEC Gov/AG lack a paired inform.politician_context row with sources[1] LIKE 'http%'"
    - "Previously-stanced execs (CA/MA/MD/OR/UT/VA Gov+AG) are excluded from the wave-coverage count by design (they were already stanced) but are NOT flagged as unsourced"
  artifacts:
    - path: "backend/scripts/verify-phase-142.sql"
      provides: "Phase-142 read-only coverage + zero-unsourced gate for Gov+AG; contributes the Wave-1 assertions the Phase-144 consolidated gate will absorb"
      contains: "RAISE EXCEPTION"
  key_links:
    - from: "backend/scripts/verify-phase-142.sql"
      to: "inform.politician_answers + inform.politician_context via essentials.politicians->offices->districts"
      via: "JOIN on d.district_type='STATE_EXEC' AND o.role_canonical IN ('governor','attorney_general')"
      pattern: "role_canonical IN .'governor','attorney_general'."
---

<objective>
Author `backend/scripts/verify-phase-142.sql` - the Phase-142 read-only labeled-assertion gate proving SEXS-02 (Gov+AG wave) is complete: every in-scope Governor and Attorney General that lacked stances now has sourced compass stances, and zero answer rows are unsourced.

Purpose: ROADMAP SC#2 + SC#3 for Phase 142. This gate keys on `(district_type='STATE_EXEC', role_canonical IN ('governor','attorney_general'))` - NOT external_id ranges (STATE_EXEC ids are non-contiguous and irregular). It mirrors the `verify-phase-132-140.sql` labeled-assertion structure and feeds the consolidated Phase-144 gate.
Output: `backend/scripts/verify-phase-142.sql`, all assertions PASS against prod.

DEPENDS ON all 8 batch plans (142-02..142-09) having pushed their stances - this is the Wave-3 closer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md
@.planning/phases/142-stance-research-wave-1-governors-ags/142-CONTEXT.md

# Gate analog (mirror its labeled DO $$ ... RAISE EXCEPTION/NOTICE structure exactly):
@backend/scripts/verify-phase-132-140.sql
# Phase-141 gate (same STATE_EXEC + role_canonical keying; reuse its join idioms):
@backend/scripts/verify-phase-141.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run live diagnostic to fix assertion constants</name>
  <files>backend/scripts/verify-phase-142.sql</files>
  <read_first>
    - .planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md (paragraph "Validation / Gate Approach" - the exact STATE_EXEC + role_canonical keying SQL; the two assertion families; the gate-keying difference from v2.17)
    - backend/scripts/verify-phase-132-140.sql (the labeled-assertion structure; how documented honest-skips are pinned to exact external_ids belt-and-suspenders, e.g. USHS-14a pins -37006)
  </read_first>
  <action>
    BEFORE authoring the gate, run a live read-only diagnostic (psql with `set -a && source .env && set +a`, or node tsx + pool) to fix every assertion constant (v2.17 gate-design lesson - never author constants blind):
    - Count in-scope-and-COVERED Govs: `SELECT count(DISTINCT p.id) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical='governor' AND EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id=p.id)`. Expect 50 (43 wave-1 + 7 pre-stanced). Repeat for attorney_general (expect 43 = 37 wave-1 + 6 pre-stanced).
    - Identify any wave-1 exec that ended as an honest-SKIP of the WHOLE record (zero answers) - there should be none for Gov/AG, but if a thin exec yielded zero, capture its exact external_id to pin (belt-and-suspenders) and confirm with the user before treating it as a documented exemption.
    - Count unsourced: `SELECT count(*) FROM inform.politician_answers pa JOIN essentials.politicians p ON p.id=pa.politician_id JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id LEFT JOIN inform.politician_context c ON c.politician_id=pa.politician_id AND c.topic_id=pa.topic_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical IN ('governor','attorney_general') AND (c.politician_id IS NULL OR c.sources[1] NOT LIKE 'http%')`. Expect 0.
    Record the exact covered counts (and any pinned honest-skip external_ids) to use as the gate constants in Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -t -c "SELECT o.role_canonical, count(DISTINCT p.id) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON d.id=o.district_id WHERE d.district_type='STATE_EXEC' AND o.role_canonical IN ('governor','attorney_general') AND EXISTS (SELECT 1 FROM inform.politician_answers pa WHERE pa.politician_id=p.id) GROUP BY o.role_canonical ORDER BY o.role_canonical"</automated>
  </verify>
  <acceptance_criteria>
    - Diagnostic ran against prod; covered counts per role recorded (governor + attorney_general).
    - Any whole-record honest-skip among wave-1 Gov/AG identified by exact external_id (or confirmed none).
    - Unsourced count confirmed (expected 0) before authoring the gate constants.
  </acceptance_criteria>
  <done>Live diagnostic complete; gate constants (covered counts + any pinned honest-skip ids) recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Author verify-phase-142.sql + run it green</name>
  <files>backend/scripts/verify-phase-142.sql</files>
  <read_first>
    - backend/scripts/verify-phase-132-140.sql (the DO $$ DECLARE ... BEGIN ... IF NOT (...) THEN RAISE EXCEPTION ...; END IF; RAISE NOTICE '... PASS'; END $$; block-per-assertion structure + final overall PASS NOTICE)
    - .planning/phases/142-stance-research-wave-1-governors-ags/142-RESEARCH.md (paragraph "Validation / Gate Approach" - two assertion families; keying MUST be STATE_EXEC + role_canonical, NOT external_id ranges)
  </read_first>
  <action>
    Create backend/scripts/verify-phase-142.sql as a read-only labeled-assertion gate mirroring verify-phase-132-140.sql (header comment block; one labeled DO block per assertion; final overall PASS NOTICE). Run header: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/scripts/verify-phase-142.sql`. Author these assertions, ALL keyed on the STATE_EXEC + role_canonical join (NEVER on external_id ranges):

    - SEXS-02a (Governor coverage): count of DISTINCT politicians with role_canonical='governor' on a STATE_EXEC district that have >=1 inform.politician_answers row EQUALS the constant from Task 1 (expected 50 = 43 wave-1 + 7 pre-stanced). If a documented whole-record honest-skip exists, set covered = 50 minus the skip count AND add a belt-and-suspenders assertion pinning the skipped politician's exact external_id (mirroring USHS-14a). Document the 43 wave-1 vs 7 pre-stanced split in a comment.
    - SEXS-02b (AG coverage): same for role_canonical='attorney_general' (expected 43 = 37 wave-1 + 6 pre-stanced).
    - SEXS-02c (zero-unsourced): count of inform.politician_answers rows for STATE_EXEC Gov/AG whose paired inform.politician_context row is missing OR whose sources[1] does NOT LIKE 'http%' EQUALS 0. RAISE EXCEPTION listing the offending external_ids if > 0.
    - SEXS-02d (scope hygiene): every STATE_EXEC Gov/AG that has answers also has a non-null role_canonical (sanity that the join key is populated) - assert zero Gov/AG-answered politicians with NULL role_canonical.
    Use a comment block at the top documenting: the gate keys on (district_type='STATE_EXEC', role_canonical IN ('governor','attorney_general')) because STATE_EXEC external_ids are non-contiguous/irregular (IN positive 499453, TX -100204, AZ -400091, ME -230001, WV AG -5400002); and that previously-stanced execs are counted in coverage (they legitimately have answers) but are not part of the wave-1 80.
    APPLY/RUN it against prod and confirm all assertions PASS (green). If an assertion fails, the fix is in the data (re-run the relevant batch's research), not loosening the gate.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-142.sql 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/verify-phase-142.sql exists with labeled RAISE EXCEPTION/NOTICE assertions for SEXS-02a..d.
    - All assertions keyed on the STATE_EXEC + role_canonical join (grep confirms no `external_id BETWEEN`/range predicate is used for coverage; pinned honest-skip ids, if any, are the ONLY external_id literals and only as belt-and-suspenders).
    - Running the gate with `-v ON_ERROR_STOP=1` exits 0 (all PASS) against prod.
    - Zero-unsourced assertion (SEXS-02c) passes (0 unsourced Gov/AG answer rows).
  </acceptance_criteria>
  <done>verify-phase-142.sql committed; runs green against prod; Gov+AG coverage + zero-unsourced proven (SEXS-02 wave-1 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> prod DB (read-only) | The gate only reads; the risk is an assertion that is wrong (too loose hides a gap, too tight false-fails) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-G-01 | Repudiation | Gate keyed on external_id ranges silently misses irregular ids (IN positive, TX/AZ/ME ad-hoc) | mitigate | Key strictly on (STATE_EXEC, role_canonical); grep-assert no range predicate; RESEARCH "Validation / Gate Approach" |
| T-142-G-02 | Tampering | Coverage constant authored blind, masking a real gap | mitigate | Task 1 live diagnostic fixes every constant before authoring (v2.17 lesson) |
| T-142-G-03 | Information Disclosure | A whole-record honest-skip silently counted as coverage shortfall vs a real miss | mitigate | Pin any documented honest-skip to its exact external_id belt-and-suspenders (USHS-14a pattern); confirm with user first |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package installs (read-only SQL); no supply-chain surface |
</threat_model>

<verification>
`cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/verify-phase-142.sql` exits 0 with all PASS NOTICEs. The Phase-144 consolidated gate (`verify-phase-141-144.sql`) will absorb these Wave-1 assertions.
</verification>

<success_criteria>
- verify-phase-142.sql exists; keyed on STATE_EXEC + role_canonical (governor, attorney_general); no external_id range predicate.
- Governor + AG coverage assertions PASS (50 + 43 with any honest-skips pinned).
- Zero-unsourced assertion PASSES (0 Gov/AG answer rows lack an http-sourced context row).
- Gate runs green against prod.
</success_criteria>

<output>
Create `.planning/phases/142-stance-research-wave-1-governors-ags/142-10-SUMMARY.md` when done.
</output>
