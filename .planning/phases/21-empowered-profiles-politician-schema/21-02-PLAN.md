---
phase: 21-empowered-profiles-politician-schema
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - backend/src/lib/essentialsService.ts
  - backend/src/routes/essentialsPoliticians.ts
  - scripts/seedPoliticians.ts
  - docs/RUNBOOK-POLITICIAN-SEED.md
autonomous: true

must_haves:
  truths:
    - "GET /api/essentials/politicians returns all new fields including is_vacant, district_type, chamber_name, government_name"
    - "GET /api/essentials/politicians filters out vacant seats (is_vacant = false only)"
    - "All new fields are always present in API response even when null"
    - "Seed script can populate Bloomington IN and LA CA politician records idempotently"
  artifacts:
    - path: "backend/src/lib/essentialsService.ts"
      provides: "Updated PoliticianRecord type + query with new fields + is_vacant filter"
      contains: "is_vacant"
    - path: "scripts/seedPoliticians.ts"
      provides: "Idempotent seed script for politician district data"
      contains: "ON CONFLICT"
    - path: "docs/RUNBOOK-POLITICIAN-SEED.md"
      provides: "Runbook for executing politician seed"
      contains: "seedPoliticians"
  key_links:
    - from: "backend/src/lib/essentialsService.ts"
      to: "inform.politicians"
      via: "supabaseAnon select query"
      pattern: "representing_city.*representing_state.*district_type"
    - from: "backend/src/lib/essentialsService.ts"
      to: "GET /api/essentials/politicians response"
      via: "PoliticianRecord explicit whitelist"
      pattern: "is_vacant.*district_type.*chamber_name"
---

<objective>
Update the essentials politicians endpoint to return all 9 new fields from migration 033, add `is_vacant = false` filter, and create an idempotent seed script for known Bloomington (IN) and Los Angeles (CA) politician district data.

Purpose: Essentials and Validation Quests consume politician data through this endpoint — it must return the full field set with correct filtering so downstream apps have no schema gaps.
Output: Updated essentialsService.ts, seed script, runbook documentation.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-empowered-profiles-politician-schema/21-CONTEXT.md
@.planning/phases/21-empowered-profiles-politician-schema/21-RESEARCH.md
@.planning/phases/21-empowered-profiles-politician-schema/21-01-SUMMARY.md
@backend/src/lib/essentialsService.ts
@backend/src/routes/essentialsPoliticians.ts
@backend/src/lib/candidateService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update essentialsService.ts — new fields + is_vacant filter</name>
  <files>backend/src/lib/essentialsService.ts</files>
  <action>
Update the essentials politicians service to return all new fields:

1. **Update PoliticianRecord interface** — add all 9 new fields:
   ```
   representing_city: string | null;
   representing_state: string | null;
   district_type: string | null;
   district_label: string | null;
   district_id: string | null;
   chamber_name: string | null;
   chamber_name_formal: string | null;
   government_name: string | null;
   is_vacant: boolean;
   ```

2. **Update the select query in getPoliticiansGrouped()** — add all 9 new columns to the explicit select string. Current select is:
   `'id, full_name, office_title, photo_origin_url, is_candidate'`
   New select must include all existing + new columns.

3. **Add is_vacant filter** — the query currently filters `.eq('is_active', true)`. Add `.eq('is_vacant', false)` as well. Both filters are required: `is_active = true AND is_vacant = false`. The CONTEXT.md decision states: "politicians endpoint reads from inform.politicians where is_vacant = false (active filled seats)".

4. **Update the rows type annotation** inside getPoliticiansGrouped() — the inline type for the `rows` variable must include all 9 new fields.

5. **Update the PoliticianRecord construction** in the loop — add all 9 new fields to the explicit whitelist object. All fields always included (even when null) per CONTEXT.md decision: "All new fields are always included in API responses, even when null."

CRITICAL: Never spread DB rows. Always explicit field whitelist. Use `supabaseAnon` (already correct).
  </action>
  <verify>
Run `cd backend && npx tsc --noEmit` — must exit 0.
Verify the select string contains all new column names: `grep 'representing_city' backend/src/lib/essentialsService.ts` returns matches.
Verify is_vacant filter: `grep 'is_vacant.*false' backend/src/lib/essentialsService.ts` returns a match.
  </verify>
  <done>PoliticianRecord has all 9 new fields. Query selects all new columns and filters is_vacant = false. Response objects include all fields via explicit whitelist. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create seed script + runbook</name>
  <files>scripts/seedPoliticians.ts, docs/RUNBOOK-POLITICIAN-SEED.md</files>
  <action>
Create two new files:

**scripts/seedPoliticians.ts** — TypeScript seed script that:

1. Connects to Supabase using `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from env (same pattern as existing scripts like applyMigrations.ts).

2. Defines seed data for known politicians:
   - **Bloomington, IN politicians** — use real district data derivable from PostGIS boundaries loaded in Phase 19:
     * Indiana 9th Congressional District (representing_state: 'Indiana', district_type: 'congressional', district_label: 'Indiana 9th Congressional District', district_id: '18-09', chamber_name: 'U.S. House', chamber_name_formal: 'United States House of Representatives', government_name: 'United States Federal Government', representing_city: 'Bloomington')
     * Indiana Senate District 40 (representing_state: 'Indiana', district_type: 'state_senate', district_label: 'Indiana Senate District 40', district_id: '18-040', chamber_name: 'Indiana Senate', chamber_name_formal: 'Indiana State Senate', government_name: 'State of Indiana')
     * Indiana House District 60 (representing_state: 'Indiana', district_type: 'state_house', district_label: 'Indiana House District 60', district_id: '18-060', chamber_name: 'Indiana House', chamber_name_formal: 'Indiana House of Representatives', government_name: 'State of Indiana')
   - **Los Angeles, CA politicians** — county-level only (Phase 20 limitation: resolve_user_jurisdiction returns null for sub-county CA districts):
     * Los Angeles County representative (representing_state: 'California', district_type: 'county', district_label: 'Los Angeles County', district_id: '06-037', chamber_name: null, chamber_name_formal: null, government_name: 'Los Angeles County Government', representing_city: 'Los Angeles')

3. For each seed record, UPSERT using the Supabase client:
   - Match on `full_name` (or `first_name` + `last_name` combo) since politicians may already exist as stub records from earlier phases.
   - Use `.upsert()` with `onConflict: 'id'` if inserting with known IDs, OR use a two-step approach: select by name, then update if exists / insert if not.
   - The script must be idempotent — safe to re-run. ON CONFLICT DO UPDATE pattern preferred.
   - Set `is_active = true`, `is_vacant = false` for all seed records.

4. Log results: "Seeded N politicians (M inserted, K updated)".

5. Exit 0 on success, exit 1 on any error.

**docs/RUNBOOK-POLITICIAN-SEED.md** — Runbook documenting:
- Purpose: Populate politician district metadata for known Alpha coverage areas
- Prerequisites: Migration 033 applied, SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in env
- Command: `npx tsx scripts/seedPoliticians.ts`
- What it does: Upserts politician records with district fields for Bloomington IN and LA CA
- Idempotency: Safe to re-run; updates existing records, inserts missing ones
- Verification: After running, check `GET /api/essentials/politicians` returns records with non-null district fields
- Extending: To add more coverage areas, add entries to the seed data array and re-run

Follow the same documentation pattern as docs/RUNBOOK-TIGER-LOAD.md.
  </action>
  <verify>
Verify script exists and compiles: `npx tsc --noEmit scripts/seedPoliticians.ts` or check syntax with `npx tsx --eval "import('./scripts/seedPoliticians.ts')"`.
Verify runbook exists: `test -f docs/RUNBOOK-POLITICIAN-SEED.md && echo "exists"`.
  </verify>
  <done>Seed script exists at scripts/seedPoliticians.ts, is idempotent (upsert pattern), covers Bloomington IN + LA CA politicians with all district fields populated. Runbook at docs/RUNBOOK-POLITICIAN-SEED.md documents prerequisites, execution, and verification.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — 0 errors (endpoint changes compile)
2. PoliticianRecord interface has all 9 new fields
3. Select query includes all 9 new column names
4. `.eq('is_vacant', false)` filter present in query chain
5. Response whitelist object includes all 9 new fields
6. Seed script exists and is syntactically valid TypeScript
7. Runbook exists with execution instructions
</verification>

<success_criteria>
- GET /api/essentials/politicians returns all 9 new fields (including null values) with is_vacant=false filtering
- Seed script is idempotent and covers Bloomington IN + LA CA
- Runbook documents the seed process
- TypeScript strict compilation: 0 errors across backend + admin
</success_criteria>

<output>
After completion, create `.planning/phases/21-empowered-profiles-politician-schema/21-02-SUMMARY.md`
</output>
