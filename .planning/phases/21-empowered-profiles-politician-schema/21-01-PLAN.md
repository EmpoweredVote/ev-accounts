---
phase: 21-empowered-profiles-politician-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/033_politician_schema.sql
  - supabase/migrations/20260313000033_politician_schema.sql
  - backend/src/types/database.types.ts
autonomous: true

must_haves:
  truths:
    - "inform.politicians has all 9 new columns after migration 033 applies"
    - "admin_list_politicians() RPC returns all new columns including is_vacant"
    - "database.types.ts reflects the updated inform.politicians schema"
    - "TypeScript strict compilation passes with 0 errors"
  artifacts:
    - path: "backend/migrations/033_politician_schema.sql"
      provides: "ALTER TABLE + DROP/CREATE admin_list_politicians RPC"
      contains: "ALTER TABLE inform.politicians"
    - path: "supabase/migrations/20260313000033_politician_schema.sql"
      provides: "Timestamped copy of migration 033"
      contains: "ALTER TABLE inform.politicians"
    - path: "backend/src/types/database.types.ts"
      provides: "Updated inform.politicians Row/Insert/Update shapes"
      contains: "is_vacant"
  key_links:
    - from: "backend/migrations/033_politician_schema.sql"
      to: "admin_list_politicians()"
      via: "DROP FUNCTION + CREATE OR REPLACE"
      pattern: "DROP FUNCTION IF EXISTS public.admin_list_politicians"
    - from: "backend/src/types/database.types.ts"
      to: "inform.politicians"
      via: "Row/Insert/Update type shapes"
      pattern: "is_vacant.*boolean"
---

<objective>
Add 9 new columns to `inform.politicians` via migration 033, recreate the `admin_list_politicians()` RPC to include the new columns, and update `database.types.ts` so TypeScript strict compilation passes.

Purpose: The politician table must carry the full field set (district, jurisdiction, vacancy) before endpoints can return this data to Essentials and Validation Quests.
Output: Migration 033 (both directories), updated database.types.ts, 0 TypeScript errors.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-empowered-profiles-politician-schema/21-CONTEXT.md
@.planning/phases/21-empowered-profiles-politician-schema/21-RESEARCH.md
@backend/migrations/029_compass_admin_rpcs.sql
@backend/src/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 033 — politician schema columns + RPC update</name>
  <files>backend/migrations/033_politician_schema.sql, supabase/migrations/20260313000033_politician_schema.sql</files>
  <action>
Create migration 033 with the following SQL wrapped in BEGIN/COMMIT:

1. ALTER TABLE inform.politicians — add 9 columns:
   - representing_city TEXT (nullable)
   - representing_state TEXT (nullable)
   - district_type TEXT (nullable)
   - district_label TEXT (nullable)
   - district_id TEXT (nullable)
   - chamber_name TEXT (nullable)
   - chamber_name_formal TEXT (nullable)
   - government_name TEXT (nullable)
   - is_vacant BOOLEAN NOT NULL DEFAULT false

   Use ADD COLUMN IF NOT EXISTS for idempotency.

2. DROP FUNCTION IF EXISTS public.admin_list_politicians() — MUST drop first because RETURNS TABLE signature is changing (adding new columns). CREATE OR REPLACE alone fails when the return type changes. Phase 17 deployment hit this exact issue (17-03-SUMMARY.md Issue 4).

3. CREATE OR REPLACE FUNCTION public.admin_list_politicians() — new version returns all existing columns PLUS all 9 new columns. Must use SECURITY DEFINER, SET search_path = ''. All table refs fully qualified. The return columns in order:
   id uuid, first_name text, last_name text, preferred_name text, full_name text,
   office_title text, photo_origin_url text, is_active boolean, is_candidate boolean,
   is_vacant boolean, representing_city text, representing_state text,
   district_type text, district_label text, district_id text,
   chamber_name text, chamber_name_formal text, government_name text,
   created_at timestamptz, answer_count bigint

4. GRANT EXECUTE ON FUNCTION public.admin_list_politicians() TO service_role, authenticated;

Write the SAME SQL content to both:
- backend/migrations/033_politician_schema.sql
- supabase/migrations/20260313000033_politician_schema.sql

Include a header comment block explaining the migration purpose, the DROP requirement, and column list.
  </action>
  <verify>Both files exist and contain identical SQL. Verify: `diff backend/migrations/033_politician_schema.sql supabase/migrations/20260313000033_politician_schema.sql` returns no differences.</verify>
  <done>Migration 033 exists in both directories with ALTER TABLE (9 columns), DROP + CREATE admin_list_politicians RPC, and GRANT statement.</done>
</task>

<task type="auto">
  <name>Task 2: Update database.types.ts + verify TypeScript compilation</name>
  <files>backend/src/types/database.types.ts</files>
  <action>
Manually update the `inform.politicians` type shapes in database.types.ts (around lines 951-989):

**Row shape** — add all 9 new fields in alphabetical position within the existing fields:
- chamber_name: string | null
- chamber_name_formal: string | null
- district_id: string | null
- district_label: string | null
- district_type: string | null
- government_name: string | null
- is_vacant: boolean (NOT nullable — NOT NULL DEFAULT false)
- representing_city: string | null
- representing_state: string | null

**Insert shape** — add all 9 new fields as optional:
- chamber_name?: string | null
- chamber_name_formal?: string | null
- district_id?: string | null
- district_label?: string | null
- district_type?: string | null
- government_name?: string | null
- is_vacant?: boolean (optional on insert because DEFAULT false)
- representing_city?: string | null
- representing_state?: string | null

**Update shape** — add all 9 new fields as optional:
- Same as Insert shape (all optional with same nullability)

IMPORTANT: `is_vacant` is `boolean` in Row (not `boolean | null`) because the column is NOT NULL DEFAULT false. In Insert and Update it is `boolean?` (optional) because the default handles omission.

After updating types, run `npx tsc --noEmit` from the backend directory to verify TypeScript strict compilation passes with 0 errors. If there are errors, fix them — they likely come from existing code that now sees the new fields and needs type alignment.
  </action>
  <verify>Run `cd backend && npx tsc --noEmit` — must exit 0 with no errors. Also verify the 9 new fields appear in the Row type with `grep -c "is_vacant\|district_type\|chamber_name\|government_name\|representing_city" backend/src/types/database.types.ts` returns at least 15 matches (3 shapes x 5 key fields).</verify>
  <done>database.types.ts contains all 9 new columns in Row/Insert/Update shapes for inform.politicians. TypeScript strict compilation passes with 0 errors.</done>
</task>

</tasks>

<verification>
1. `diff backend/migrations/033_politician_schema.sql supabase/migrations/20260313000033_politician_schema.sql` — no differences
2. Migration SQL contains BEGIN/COMMIT, ADD COLUMN IF NOT EXISTS for all 9 columns, DROP FUNCTION, CREATE OR REPLACE, GRANT
3. database.types.ts inform.politicians Row has `is_vacant: boolean` (not nullable)
4. database.types.ts inform.politicians Insert has `is_vacant?: boolean` (optional)
5. `cd backend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Migration 033 exists in both directories with correct SQL
- database.types.ts updated with all 9 new columns in all 3 shapes
- TypeScript strict compilation: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-empowered-profiles-politician-schema/21-01-SUMMARY.md`
</output>
