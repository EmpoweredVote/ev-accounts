---
phase: 13-topic-selection-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CompassV2/src/pages/Library.jsx
autonomous: true
requirements:
  - TSEL-01
  - TSEL-03

must_haves:
  truths:
    - "Library page header shows a persistent counter badge displaying X/8 topics on compass that updates live with every add/remove"
    - "Library cards for topics on the compass have an ev-light-blue (#59b0c4) border — non-compass cards have no visual change"
    - "The add button on a Library card becomes a remove button (X icon) when the topic is on the compass — same position, different icon/action"
    - "A quick confirmation popover appears before removal: 'Remove from compass?' with Yes/Cancel"
    - "Removing preserves the user's answer — only removes from compass display, not the stance data"
    - "If removing drops below 3 topics, a warning appears ('Your compass needs 3+ topics to display') but the user can proceed"
    - "Add button on Library cards is disabled (grayed out) when user has 8 topics on the compass"
    - "Instant UI update on removal: border disappears, remove button swaps back to add button, counter badge updates"
  artifacts:
    - path: "CompassV2/src/pages/Library.jsx"
      provides: "Library page with counter badge, on-compass card indicators, add/remove toggle, confirmation popover, and max-8 cap enforcement"
  key_links:
    - from: "CompassV2/src/pages/Library.jsx"
      to: "CompassContext selectedTopics"
      via: "useCompass() hook"
      pattern: "selectedTopics\\.includes\\(topic\\.id\\)"
---

<objective>
Add compass membership indicators, counter badge, and add/remove toggle to Library cards. Enforce the 8-topic cap on Library card add buttons.

Purpose: Users see at a glance which topics are on their compass and can add/remove topics directly from Library cards, with the 8-topic maximum enforced via disabled state.

Output: Updated Library.jsx with counter badge, ev-light-blue borders on compass cards, add/remove toggle buttons with confirmation popover, and disabled add when at cap.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CompassV2/src/pages/Library.jsx
@CompassV2/src/components/CompassContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add counter badge and on-compass card indicators to Library</name>
  <files>CompassV2/src/pages/Library.jsx</files>
  <action>
In Library.jsx, implement the following changes:

**1. Counter badge in Library page header:**
- In the "Topic Selection Section" heading area (the `<h2>` that reads "Or, pick your own topics"), add a counter badge next to the heading text or in the search/filter row.
- Display format: "X/8" where X = `selectedTopics.length`. Use muted styling — lightweight, informational feel.
- Example: a small `<span>` with `text-sm text-gray-500 font-medium bg-gray-100 rounded-full px-2.5 py-0.5` showing `{selectedTopics.length}/8`.
- Place it in the heading row, after the heading text, aligned right or inline. Use your judgment for best visual placement.
- This badge updates live because it reads from `selectedTopics` (reactive state).

**2. On-compass card border indicator:**
- For each topic card in the grid, determine if the topic is on the compass: `const isOnCompass = selectedTopics.includes(topic.id)`.
- If `isOnCompass` is true, add `border-[#59b0c4]` (ev-light-blue) as the card's border color, REPLACING the current category-based `color.border` logic for that card.
- Current card border logic: `isSelected ? color.border : "border-gray-200"`. Change `isSelected` references to only apply the category color when the card is selected for quiz but NOT on compass. But actually, per context: the card currently uses `isSelected` (which checks `selectedTopics.includes(topic.id)`) for category-colored borders. The new behavior should be:
  - If topic is on compass (`selectedTopics.includes(topic.id)`): use `border-[#59b0c4]` border with a subtle blue-tinted bg like `bg-sky-50/50`.
  - If topic is NOT on compass: keep existing `"bg-white border-gray-200 hover:border-gray-300 hover:shadow-sm"`.
- Cards NOT on compass have NO visual change from current default — per locked decision.

**3. Add/remove toggle button on each card:**
- Add a small action button in the bottom-right or top-right corner of each card.
- When topic is NOT on compass: show a "+" icon button. Clicking it adds the topic to `selectedTopics` via `setSelectedTopics(prev => [...prev, topic.id])`.
- When topic IS on compass: show an "X" icon button. Clicking it triggers a confirmation popover (see Task 2).
- The button should be a small circular icon button (e.g., `w-6 h-6 rounded-full`).
- The "+" button: `bg-gray-200 text-gray-500 hover:bg-[#59b0c4] hover:text-white`.
- The "X" button: `bg-[#59b0c4] text-white hover:bg-red-400`.
- When user has 8 topics on compass (`selectedTopics.length >= MAX_TOPICS`) AND this card is NOT on the compass, the "+" button should be disabled: `bg-gray-100 text-gray-300 cursor-not-allowed`. No toast or tooltip needed — the disabled state + counter badge communicate the cap.
- Use `e.stopPropagation()` on the button click to prevent the card's onClick (which opens the drawer) from also firing.

Important: The card's main `onClick` still opens the drawer. The add/remove button is a separate action within the card.
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npm run build` — no build errors.
Visually verify: counter badge renders in header, on-compass cards have blue border, add/remove buttons present on cards.
  </verify>
  <done>
Counter badge shows X/8 in the Library section header. Cards on compass have ev-light-blue border. Each card has a toggle button (+ to add, X to remove). Add button is disabled when 8 topics selected. Card click still opens drawer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add confirmation popover for topic removal from Library cards</name>
  <files>CompassV2/src/pages/Library.jsx</files>
  <action>
Add a confirmation popover that appears when a user clicks the remove (X) button on a Library card.

**Implementation:**

1. Add state for the popover:
   ```js
   const [removeConfirm, setRemoveConfirm] = useState(null); // topic.id or null
   ```

2. When the X button is clicked on a compass card, set `setRemoveConfirm(topic.id)` instead of immediately removing.

3. Render the popover near the card that triggered it. Use a simple inline approach:
   - If `removeConfirm === topic.id`, render a small popover div positioned absolutely below or above the remove button.
   - Popover content: "Remove from compass?" with two small buttons: "Yes" (confirms) and "Cancel" (dismisses).
   - Popover styling: `absolute z-20 bg-white rounded-lg shadow-lg border border-gray-200 p-3` with a min-width of ~180px.
   - "Yes" button: `text-sm px-3 py-1 bg-red-500 text-white rounded hover:bg-red-600`.
   - "Cancel" button: `text-sm px-3 py-1 bg-gray-100 text-gray-600 rounded hover:bg-gray-200`.

4. On "Yes" confirmation:
   - Remove the topic from selectedTopics: `setSelectedTopics(prev => prev.filter(id => id !== topic.id))`.
   - Do NOT clear the answer. The user's stance data stays in `answers` and `answeredTopicIDs`. Per locked decision: removing preserves the answer — only removes from compass display.
   - Close the popover: `setRemoveConfirm(null)`.

5. On "Cancel": `setRemoveConfirm(null)`.

6. Click outside dismissal: When `removeConfirm` is set, add a click handler on the backdrop or use the card's onClick to dismiss it. Simplest: if any card is clicked while popover is showing, dismiss the popover first.

7. **Below-3 warning:** If removing this topic would drop `selectedTopics.length` below 3, show additional text in the popover: "Your compass needs 3+ topics to display" in `text-xs text-amber-600 mt-1`. But still allow them to proceed (Yes button stays enabled).

The UI update on removal should be instant:
- The ev-light-blue border disappears (card no longer in selectedTopics).
- The X button swaps back to + button.
- The counter badge updates (reactive from selectedTopics.length).
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/CompassV2 && npm run build` — no build errors.
Verify: clicking X shows popover, "Yes" removes topic from compass (border gone, button swaps, badge updates), answer preserved. Warning appears when removal would drop below 3.
  </verify>
  <done>
Confirmation popover appears before removal with Yes/Cancel. Removal preserves answers. Below-3 warning displays. Instant UI update on confirm. Popover dismisses on Cancel or outside click.
  </done>
</task>

</tasks>

<verification>
1. Build: `cd CompassV2 && npm run build` succeeds with no errors
2. Counter badge in Library header shows current count / 8
3. Cards on compass have ev-light-blue (#59b0c4) border
4. Cards not on compass have default gray border (no change)
5. + button adds topic to compass; X button shows confirmation popover
6. Add button disabled when 8 topics selected
7. Popover: Yes removes from compass, Cancel dismisses
8. Answers preserved after removal (answeredTopicIDs unchanged)
9. Below-3 warning visible in popover when applicable
</verification>

<success_criteria>
- Library page displays persistent X/8 counter badge
- On-compass cards have distinct ev-light-blue border
- Add/remove toggle works as described with proper disabled states
- Confirmation popover prevents accidental removal
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-topic-selection-enforcement/13-01-SUMMARY.md`
</output>
