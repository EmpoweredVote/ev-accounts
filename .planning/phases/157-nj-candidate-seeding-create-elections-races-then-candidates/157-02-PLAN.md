---
phase: 157-nj-candidate-seeding-create-elections-races-then-candidates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/157-verify.sql
autonomous: true
requirements: [USHC2-02, USHC2-03, USHC2-04, USHC2-05]
must_haves:
  truths:
    - "A standalone, write-free 157-verify.sql exists asserting every USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 success criterion, scoped to the 12 NJ House races within the 'NJ 2026 Statewide General' election"
    - "Every assertion is scoped via NATIONAL_LOWER + substr(geo_id,1,2)='34' — no cross-state contamination"
    - "NJ-12 Watson Coleman (retired incumbent, -34012) is asserted ABSENT from the active field; nominees Adam Hamawy + Gregg Mele present; NJ-8 Menendez asserted as a single-candidate (uncontested) general with ≥1 active"
    - "The stance in-scope set = the new-candidate external_id band (-341299..-340101) ONLY — NJ has NO zero-stance incumbent (unlike NC-6 McDowell); ALL 12 partial incumbents excluded"
    - "Any whole-record stance honest-skip and any headshot honest-skip is pinned by exact UUID/external_id with the query's exact ORDER BY (143 lesson)"
  artifacts:
    - path: "backend/scripts/157-verify.sql"
      provides: "Read-only labeled-assertion gate for Phase 157 (USHC2-02/03/04/05 + decisions), NJ-scoped, honest-skip-pinnable, no-incumbent-in-stance-scope"
      contains: "NATIONAL_LOWER"
      min_lines: 110
  key_links:
    - from: "157-verify.sql assertions"
      to: "the 12 NJ House races within the 'NJ 2026 Statewide General' election"
      via: "d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='34' predicate"
      pattern: "NATIONAL_LOWER"
---

<objective>
Author the read-only `backend/scripts/157-verify.sql` gate (Wave-0 test infrastructure, nyquist) that every Phase 157 wave runs to self-check and that 157-06 runs as the final consolidated proof. It asserts elections/races scaffold (USHC2-03), records + dedup (USHC2-02), race wiring, headshots (USHC2-04), stances (USHC2-05), the seed-all-minor-line invariant (D-02), the retired-incumbent-absent invariant (D-04), and zero-duplicate-full_name (D-03) — scoped to the 12 NJ House races. This is the PER-PHASE gate, NOT the milestone gate (Phase 158). It is a near-verbatim SINGLE-STATE reduction of 156-verify.sql with three NJ-specific wrinkles: NJ-8 uncontested (single-candidate general), NJ-11 Mejia reused-incumbent (already in DB), and NJ-12 Watson Coleman retirement (incumbent-absent pin) — and crucially NO zero-stance-incumbent in scope (NJ has none, unlike NC-6 McDowell in 156).

Purpose: This gate must exist before later waves so each can verify its slice. Trap-prone parts: (a) the D-01 asymmetry — ALL 12 NJ partial incumbents are DELIBERATELY untouched and NONE is in the stance scope (in-scope = new candidates only, simpler than 156); (b) NJ-8 single-candidate general (assert ≥1, not ≥2); (c) NJ-12 retired-incumbent absence with its nominees present; (d) NJ-11 Mejia is the reused special-seated incumbent (active is_incumbent=true), NOT a new record.
Output: A standalone, write-free .sql gate (the 156-verify.sql precedent, reduced to one state with NJ wrinkles).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md
@.planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv
@backend/scripts/156-verify.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 157-verify.sql — NJ-scoped labeled assertions for USHC2-02/03/04/05 + decisions</name>
  <read_first>
    - backend/scripts/156-verify.sql (the validated THREE-STATE precedent just shipped: read-only DO-block labeled assertions; CREATE TEMP TABLE … ON COMMIT DROP working sets; reuse_pid / lost-incumbent / new-cands / stance-skip / fed24 pin-table patterns; per-state scoping; the all-partial-incumbent exclusion encoding; the 143 honest-skip-ORDER-BY lesson)
    - backend/scripts/155-verify.sql (the TWO-STATE precedent — the single-candidate/uncontested-general allowance pattern if present; the in-scope-set TEMP-table convention)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-FIELD-TABLE.md (NJ non-incumbent-nominee flags: NJ-11 special-seated, NJ-12 retired; the NJ minor-line fields for D-02; NJ-8 uncontested; stance-gap = all-partial, NO zero-stance NJ incumbent)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-incumbent-map.csv (NJ geo_id 34NN → incumbent_pid + external_id, for the reuse-pid pin set and the NJ-12 retired-incumbent-absent pin: Watson Coleman a75a3e6e-31ff-4870-8aea-501e41326fd1/-34012; Mejia 93874414-6d14-4c12-87b2-d254b3855570/-34011 reused active)
    - .planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-CONTEXT.md (D-01 NO NJ incumbent in stance scope; D-02 seed-all-minor incl. NJ-3 Green + Affordability Accountability People + NJ-5 Humane Sustainable Future; D-03 zero-dup-full_name; D-04 NJ-8 uncontested + NJ-11 Mejia reused + NJ-12 retired no-active-row)
  </read_first>
  <action>
    Write `backend/scripts/157-verify.sql` as a write-free, labeled-assertion gate following 156-verify.sql exactly, reduced to the single state NJ. Resolve the election id at the top (SELECT id FROM essentials.elections WHERE name='NJ 2026 Statewide General'). Build the NJ House working set via the join chain races JOIN offices ON office_id JOIN districts ON district_id filtered r.election_id = nj_eid AND d.district_type='NATIONAL_LOWER' AND substr(d.geo_id,1,2)='34'. NEVER write a cross-state or election-wide count.

    Labeled assertions (each DO $$ … IF NOT (…) THEN RAISE EXCEPTION 'FAIL <label>: %' … ELSE RAISE NOTICE 'PASS <label>'; END $$; style):
    - USHC2-03a — all 12 NJ races have ≥1 active race_candidate; additionally assert the COUNT of NJ races = 12 so no district is missing. Assert ≥2 active per race EXCEPT the pinned uncontested district NJ-8 (geo 3408), which asserts exactly 1 active (single-candidate general — Menendez only; documented allowance, D-04).
    - USHC2-03b — 0 active race_candidates rows with NULL politician_id (NJ House-scoped).
    - USHC2-02a — 0 duplicate full_name among ACTIVE NJ candidates (GROUP BY lower(full_name) HAVING count(*)>1 returns 0) — D-03.
    - USHC2-02c — every renominated/reused NJ incumbent reuses its 154 incumbent_pid (pin a reuse_pid TEMP table of (geo_id, expected_pid) from 154-incumbent-map.csv for NJ-1..11 = the 11 sitting incumbents wired active, INCLUDING NJ-11 Mejia 93874414-… as the reused special-seated incumbent; assert each appears as an ACTIVE is_incumbent=true House candidate). Header-document that NJ-12 (Watson Coleman) is intentionally NOT in the set (retired).
    - D-04-retired-incumbent-NJ12 — Watson Coleman (a75a3e6e-31ff-4870-8aea-501e41326fd1, -34012) appears 0 times as an active candidate; her nominees ARE active (NJ-12 Adam Hamawy + Gregg Mele — pin the new pids from the 157-03 reconciliation, fallback name match within geo 3412). NOTE: this is a retirement, NOT a vacancy — the NJ-12 office + Watson Coleman's politician record already exist; do NOT assert any office/vacancy creation.
    - D-04-NJ8-uncontested — NJ-8 (geo 3408) has exactly 1 active candidate = the reused incumbent Robert Menendez (is_incumbent=true), 0 challengers. Header-document why NJ-8 differs from other districts.
    - D-02-minor — named minor-line fields are seeded (party-agnostic, assert presence): NJ-3 Steven Welzer (Green) + Ryan Michael Kelly (Affordability Accountability People); NJ-5 Adam Rueda (Humane Sustainable Future). Confirm the actual certified set against the 157-03 reconciliation (D-03 re-confirm may trim minor lines whose ballot qualification is unconfirmed).
    - USHC2-04 — every newly-seeded NJ candidate (politician_id in the new-candidate external_id band assigned by 157-03) has an essentials.politician_images row; documented headshot honest-skips pinned by exact external_id with explicit ORDER BY.
    - USHC2-05a — 0 unsourced stance rows for the in-scope set: 0 = count over inform.politician_answers for the in-scope pids WHERE NOT EXISTS a matching inform.politician_context with a non-empty sources array. In-scope set (D-01): the NEW NJ candidates ONLY (external_id band -341299..-340101). EXCLUDE ALL 12 NJ partial incumbents (left as-is this phase) so their pre-existing partial coverage cannot false-fail a 0-unsourced check. NJ has NO zero-stance incumbent — the in-scope set contains NO incumbent pid (contrast 156 which UNIONed NC-6 McDowell).
    - USHC2-05b — each in-scope new candidate has ≥1 sourced federal stance OR is a pinned whole-record honest-skip (per the project-149-stance-gate-standard: ≥1-sourced-OR-pinned, full-24 NOT required; chairs-not-polarity forbids party-inference inflation). Whole-record skips pinned by UUID with exact ORDER BY. All 12 NJ partial incumbents are NOT in this set (D-01).

    Define the in-scope stance/headshot set + honest-skip pins as TEMP tables at the top (the 149/150/155/156 pattern) so 157-04..05 can run the gate mid-wave. The in-scope stance TEMP table = the new-candidate band (external_id BETWEEN -341299 AND -340101 — header-document that the incumbent band -34001..-34012 is strictly greater, so the ≤ -340101 cut cleanly separates new from incumbent, and that NO incumbent is in scope). Header comment documents: (a) NJ-single-state scoping rule (geo '34'); (b) the D-01 all-partials-untouched, zero-incumbents-in-stance-scope rule; (c) NJ-8 single-candidate allowance; (d) NJ-12 retirement (incumbent-absent, no vacancy/office assertion); (e) NJ-11 Mejia reused-incumbent; (f) data-dependent assertions legitimately FAIL pre-seed and go green only after the waves complete; (g) the 143 honest-skip-ORDER-BY lesson.

    Self-test that the SQL parses + runs read-only against prod NOW (pre-seed). The races scaffold (157-01) may already be live in this wave; USHC2-03a may already pass for the races count while candidate/headshot/stance assertions FAIL pre-seed (expected).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && grep -v '^[[:space:]]*--' scripts/157-verify.sql > /tmp/157v.nc && grep -qi 'NATIONAL_LOWER' /tmp/157v.nc && grep -q "'34'" /tmp/157v.nc && grep -q 'a75a3e6e' /tmp/157v.nc && grep -q '93874414' /tmp/157v.nc && ! grep -qiE 'DELETE |UPDATE |INSERT[[:space:]]+INTO[[:space:]]+(essentials|inform)' /tmp/157v.nc && echo PASS-nj-scoped-and-write-free</automated>
  </verify>
  <acceptance_criteria>
    - 157-verify.sql contains all labeled assertions above; every data query is NJ House-scoped (NATIONAL_LOWER + geo_id prefix '34' within the 'NJ 2026 Statewide General' election).
    - The file is write-free: no DELETE/UPDATE/INSERT INTO essentials|inform (TEMP-table CREATE … ON COMMIT DROP is the only allowed write, matching 156-verify.sql).
    - D-01 encoded: ALL 12 NJ partial incumbents EXCLUDED from the USHC2-05a/05b in-scope set; NO incumbent pid is in scope (NJ has no zero-stance incumbent — header-documented, contrast NC-6 McDowell).
    - NJ-12 retired-incumbent-absent assertion present (Watson Coleman pid absent, Hamawy+Mele present); NJ-8 uncontested single-candidate assertion present (Menendez only, ≥1 allowance); D-02 NJ minor-line presence assertions present.
    - Honest-skip pins use explicit ORDER BY (143 lesson, header comment + pinned-set query).
    - The script parses + runs read-only against prod NOW; data-dependent assertions may FAIL pre-seed (documented as expected).
  </acceptance_criteria>
  <done>157-verify.sql authored, NJ-scoped, write-free, all assertions present incl. no-incumbent-in-stance-scope + NJ-12 retired-incumbent-absent + NJ-8 uncontested + NJ-11 Mejia reused-incumbent + D-02 minor-line; parses + runs read-only against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → production DB (read) | 157-verify.sql runs SELECT-only against live prod; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-05 | Tampering | gate accidentally mutates prod | mitigate | Write-free enforced by verify check; only TEMP-table writes allowed |
| T-157-06 | Repudiation | gate false-fails on NJ partial-incumbent pre-existing data | mitigate | ALL 12 partial incumbents EXCLUDED from in-scope stance set (D-01); header-documented |
| T-157-07 | Repudiation | NJ-8 uncontested falsely required ≥2 active | mitigate | D-04-NJ8-uncontested asserts exactly 1 active (Menendez); documented allowance |
| T-157-08 | Repudiation | retired NJ-12 incumbent silently surfaced as active | mitigate | D-04-retired-incumbent-NJ12 asserts Watson Coleman absent + Hamawy/Mele present |
| T-157-09 | Repudiation | NJ-11 Mejia wrongly treated as a new record | mitigate | reuse_pid set includes Mejia 93874414-… as the active is_incumbent special-seated incumbent |
| T-157-11 | Repudiation | honest-skip ordering mismatch false-fails (143 lesson) | mitigate | Honest-skip pins use explicit ORDER BY matching the query |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- The verify automated check confirms NJ scoping + Mejia/Watson-Coleman pins + write-free.
- Manual: run the gate against prod pre-seed; confirm it executes (data assertions may FAIL pre-seed, expected).
</verification>

<success_criteria>
- 157-verify.sql exists, write-free, NJ-scoped, with all USHC2-02/03/04/05 + D-01/D-02/D-03/D-04 labeled assertions (incl. no-incumbent-in-stance-scope + NJ-8 uncontested + NJ-12 retirement handling); runs read-only against prod without syntax/permission error.
</success_criteria>

<output>
Create `.planning/phases/157-nj-candidate-seeding-create-elections-races-then-candidates/157-02-SUMMARY.md` when done (record: assertion labels, the in-scope-set TEMP-table convention, the NJ-8 uncontested + NJ-12 retirement + NJ-11 Mejia-reused notes, and how 157-04..05 should invoke it mid-wave).
</output>
