---
phase: 108-la-county-city-officials
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/verify-la-county-108.sql
autonomous: true
gap_closure: true
requirements:
  - LAOF-06
must_haves:
  truths:
    - Assertion 7 in verify-la-county-108.sql uses the correct join path (governments→chambers→offices→districts) with no direct districts.government_id reference
    - All 8 ASSERTION labels still present in the file after the edit
    - No other lines changed (surgical fix only)
  artifacts:
    - path: backend/scripts/verify-la-county-108.sql
      provides: Executable Phase 108 verification gate
      contains: ch.government_id = g.id
  key_links:
    - from: essentials.governments g
      to: essentials.districts d
      via: ch ON ch.government_id = g.id → o ON o.chamber_id = ch.id → d ON d.id = o.district_id
      pattern: "ch\\.government_id = g\\.id"
---

<objective>
Fix the broken join in Assertion 7 of `backend/scripts/verify-la-county-108.sql`.

The current query joins `essentials.districts d ON d.government_id = g.id` — a column that does not exist on the `essentials.districts` table. All migration evidence confirms districts are linked to governments only through the `offices → chamber → government_id` chain. The broken join makes Assertion 7 non-executable, producing a column-not-found error at runtime and preventing the full Phase 108 verification gate from running.

This is gap closure for the single fixable gap identified in Phase 108 VERIFICATION.md. WR-07 in 108-REVIEW.md contains the exact replacement query.

Purpose: Unblock the Phase 108 verification gate so all 8 assertions can execute against the live DB.
Output: An edited `verify-la-county-108.sql` where Assertion 7 traverses the correct schema join path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/108-la-county-city-officials/108-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Assertion 7 join path in verify-la-county-108.sql</name>
  <files>backend/scripts/verify-la-county-108.sql</files>
  <read_first>
    - backend/scripts/verify-la-county-108.sql — read the full file before editing; the broken join is on lines 193-197 (the FROM/JOIN block of ASSERTION 7)
    - .planning/phases/108-la-county-city-officials/108-REVIEW.md — see WR-07 for the exact replacement query
  </read_first>
  <action>
    In `backend/scripts/verify-la-county-108.sql`, replace the four-line FROM/JOIN block that starts the ASSERTION 7 SELECT body (currently lines 187-197). The current broken join reads:

      FROM essentials.governments g
      JOIN essentials.districts d ON d.government_id = g.id
      LEFT JOIN essentials.offices o ON o.district_id = d.id
      LEFT JOIN essentials.politicians p
        ON p.office_id = o.id
        AND p.external_id BETWEEN -700699 AND -700200

    Replace those five lines with the correct join path that traverses governments → chambers → offices → districts (per WR-07):

      FROM essentials.governments g
      JOIN essentials.chambers ch ON ch.government_id = g.id
      JOIN essentials.offices o ON o.chamber_id = ch.id
      JOIN essentials.districts d ON d.id = o.district_id
      LEFT JOIN essentials.politicians p
        ON p.id = o.politician_id
        AND p.external_id BETWEEN -700699 AND -700200

    Also update the SELECT list to include `d.district_type` and `d.label` (these columns are already in the SELECT — verify they remain unchanged). The GROUP BY and WHERE clauses are unchanged. This is a surgical replacement of the join lines only — do not touch any other part of the file.

    Key identifiers to verify after edit:
    - `ch.government_id = g.id` must appear exactly once in the file
    - `d.government_id` must appear zero times in the file
    - `o.politician_id` must be used in the LEFT JOIN ON clause (not `p.office_id = o.id`)
    - The file must still have exactly 8 `ASSERTION` label occurrences
  </action>
  <verify>
    <automated>
      grep -c "ON d.government_id" backend/scripts/verify-la-county-108.sql
      # Expected: 0

      grep -c "ch.government_id = g.id" backend/scripts/verify-la-county-108.sql
      # Expected: 1

      grep -c "ASSERTION" backend/scripts/verify-la-county-108.sql
      # Expected: 8
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "ON d.government_id" backend/scripts/verify-la-county-108.sql` returns 0 matches
    - `grep "ch.government_id = g.id" backend/scripts/verify-la-county-108.sql` returns 1 match
    - `grep -c "ASSERTION" backend/scripts/verify-la-county-108.sql` returns 8
    - No lines outside the five-line join block are modified
  </acceptance_criteria>
  <done>verify-la-county-108.sql Assertion 7 uses the governments→chambers→offices→districts join path. The file is syntactically valid SQL with all 8 assertions intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→DB | verify-la-county-108.sql is a read-only diagnostic script run by a developer with direct DB access. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05-01 | Tampering | verify-la-county-108.sql edit | accept | Developer-only file; no runtime exposure. Edit is audited via git diff before execution. |
</threat_model>

<verification>
After the edit, run:

```bash
grep -c "ON d.government_id" backend/scripts/verify-la-county-108.sql
# Must return: 0

grep -c "ch.government_id = g.id" backend/scripts/verify-la-county-108.sql
# Must return: 1

grep -c "ASSERTION" backend/scripts/verify-la-county-108.sql
# Must return: 8
```

Then run the full gate against the live DB to confirm no column-not-found error:
```bash
psql "$DATABASE_URL" -f backend/scripts/verify-la-county-108.sql
```
Assertion 7 output should show rows for the five at-large cities with `wave3_politicians_in_local_exec = 0`, or no rows at all (meaning no LOCAL_EXEC districts exist for those cities, which is also a passing state for the negative assertion).
</verification>

<success_criteria>
- verify-la-county-108.sql can be executed without a column-not-found error on Assertion 7
- Assertion 7 traverses: essentials.governments → essentials.chambers → essentials.offices → essentials.districts
- The reference `d.government_id` is absent from the file
- All 8 ASSERTION labels remain present — no other content changed
</success_criteria>

<output>
Create `.planning/phases/108-la-county-city-officials/108-05-SUMMARY.md` when done.
</output>
