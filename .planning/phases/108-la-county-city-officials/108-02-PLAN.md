---
phase: 108-la-county-city-officials
plan: 02
type: execute
wave: 2
depends_on:
  - 108-01
files_modified:
  - backend/migrations/300_la_wave2_preflight.sql
  - backend/migrations/301_la_wave2_beverly_hills.sql
  - backend/migrations/302_la_wave2_santa_monica.sql
  - backend/migrations/303_la_wave2_la_city_controller_clerk.sql
  - backend/scripts/preflight-la-wave2.sql
autonomous: true
requirements:
  - LAOF-02
  - LAOF-05

tags:
  - sql-migration
  - essentials-schema
  - la-county
  - structure-exists
  - la-city

must_haves:
  truths:
    - "Beverly Hills has 5 sitting council members + 1 elected City Treasurer (Howard Fisher) — total 6 politicians attached to its LOCAL district geo_id=0606308 (per D-03 and 108-RESEARCH.md Beverly Hills)"
    - "Santa Monica has 7 sitting council members attached to its LOCAL district geo_id=0670000 (per 108-RESEARCH.md Santa Monica)"
    - "LA City Controller Kenneth Mejia is linked to existing office UUID e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6 (RESEARCH.md Critical Finding 3)"
    - "LA City Clerk Patrice Lattimore is linked to a City Clerk office at LOCAL_EXEC district geo_id=0644000 with is_appointed=true (RESEARCH.md Critical Finding 2)"
    - "LA City Attorney office (UUID 5a873c59-72ac-488f-8b2c-44dfd04d065c) has NO politician inserted — runoff Nov 2026 (RESEARCH.md Critical Finding 1)"
    - "Every new politician has non-null photo_origin_url and back-filled office_id"
    - "Every new politician uses external_id in -700001..-700049"
  artifacts:
    - path: "backend/migrations/300_la_wave2_preflight.sql"
      provides: "Documentation-only migration capturing Wave 2 pre-flight: Beverly Hills office row count, Santa Monica office row count, LA City Clerk office existence check, LA City Attorney office occupancy check"
      min_lines: 40
    - path: "backend/migrations/301_la_wave2_beverly_hills.sql"
      provides: "Inserts 5 BH council members + Treasurer Howard Fisher; creates additional BH office rows if the pre-flight shows < 6 exist; geo_id=0606308 backfill"
      contains: "Howard Fisher"
    - path: "backend/migrations/302_la_wave2_santa_monica.sql"
      provides: "Inserts 7 Santa Monica council members linked to single LOCAL district geo_id=0670000; geo_id backfill"
      contains: "Lana Negrete"
    - path: "backend/migrations/303_la_wave2_la_city_controller_clerk.sql"
      provides: "Inserts Kenneth Mejia (City Controller, linked to existing office UUID) and Patrice Lattimore (City Clerk, creates City Clerk chamber + office if absent); explicitly leaves City Attorney vacant"
      contains: "Patrice Lattimore"
    - path: "backend/scripts/preflight-la-wave2.sql"
      provides: "Re-runnable SQL script enumerating BH/SM/LA-city office and politician rows for diff before/after"
  key_links:
    - from: "BH council politicians (-700010..-700015)"
      to: "essentials.districts.geo_id='0606308'"
      via: "CTE INSERT … FROM essentials.districts WHERE geo_id='0606308' AND district_type='LOCAL'"
      pattern: "d\\.geo_id = '0606308'"
    - from: "SM council politicians (-700030..-700036)"
      to: "essentials.districts.geo_id='0670000'"
      via: "CTE INSERT … FROM essentials.districts WHERE geo_id='0670000' AND district_type='LOCAL'"
      pattern: "d\\.geo_id = '0670000'"
    - from: "Kenneth Mejia politician (-700001)"
      to: "existing essentials.offices.id='e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6'"
      via: "UPDATE essentials.offices SET politician_id = ... WHERE id = 'e5435b0e-...'"
      pattern: "e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6"
    - from: "Patrice Lattimore politician (-700002)"
      to: "essentials.districts geo_id='0644000' LOCAL_EXEC"
      via: "CTE INSERT politicians + office row with is_appointed_position=true"
      pattern: "d\\.geo_id = '0644000'"
---

<objective>
Populate the cities and offices where structure exists in the DB but politician records are missing (per D-01 Tier 2): Beverly Hills (5-member at-large council + elected Treasurer Howard Fisher), Santa Monica (7-member at-large council), and the three LA City citywide offices documented in CONTEXT.md D-08.

Critical corrections from RESEARCH.md that the executor MUST respect:
1. LA City Attorney Hydee Feldstein Soto LOST the June 2026 primary. Office UUID `5a873c59-72ac-488f-8b2c-44dfd04d065c` is in runoff (Roy vs. McKinney, Nov 2026). Do NOT seed a politician for this office. (RESEARCH.md Critical Finding 1.)
2. LA City Clerk Holly Wolcott retired January 2025. Current incumbent is Patrice Lattimore, appointed September 2025 (is_appointed=true). (RESEARCH.md Critical Finding 2.)
3. LA City Controller Kenneth Mejia was re-elected June 2026 — he remains incumbent. (RESEARCH.md Critical Finding 3.)

Per CONTEXT.md "Wave 2 = Beverly Hills + Santa Monica + LA City offices" and 108-RESEARCH.md recommended grouping.

Purpose: Beverly Hills/Santa Monica have 0 politicians today despite their council offices being known; the three LA City citywide offices have existed since migration 115 but were never linked to politicians. This plan closes those known structural-but-not-occupied gaps and is the only plan that touches LA City citywide offices.

Output: 1 pre-flight documentation migration (300) + 3 implementation migrations (301–303) + 1 standing pre-flight script. External_id allocation per RESEARCH.md "External ID Range Assignment": -700001..-700003 for LA City; -700010..-700019 for Beverly Hills; -700030..-700039 for Santa Monica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-la-county-city-officials/108-CONTEXT.md
@.planning/phases/108-la-county-city-officials/108-RESEARCH.md
@.planning/phases/108-la-county-city-officials/108-PATTERNS.md
@.planning/phases/108-la-county-city-officials/108-VALIDATION.md
@.planning/phases/108-la-county-city-officials/108-01-PLAN.md

# Canonical analogs
@backend/migrations/115_la_city_attorney_controller_district.sql
@backend/migrations/218_sj_officials.sql
@backend/migrations/217_sj_government_structure.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight BH/SM office structure + LA City Clerk office existence</name>
  <files>backend/scripts/preflight-la-wave2.sql, backend/migrations/300_la_wave2_preflight.sql</files>
  <read_first>
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Open Questions" — Q2 and Q3; "Critical Pre-Flight Corrections" table; "Pitfall 10")
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Wave 2" section, "LA City Known UUIDs" section)
    - backend/migrations/115_la_city_attorney_controller_district.sql (full — confirms existing office UUIDs and the LOCAL_EXEC district UUID)
  </read_first>
  <action>
    Create `backend/scripts/preflight-la-wave2.sql` with these labeled queries (one block per question):

    Q1 — Beverly Hills office row count (RESEARCH.md Open Question 2):
    `SELECT COUNT(*) AS bh_office_count, array_agg(o.title ORDER BY o.title) AS titles FROM essentials.offices o JOIN essentials.districts d ON o.district_id = d.id WHERE d.geo_id = '0606308';`
    Plus: `SELECT id, label, district_type FROM essentials.districts WHERE geo_id = '0606308';`

    Q2 — Santa Monica office row count:
    `SELECT COUNT(*) AS sm_office_count, array_agg(o.title ORDER BY o.title) AS titles FROM essentials.offices o JOIN essentials.districts d ON o.district_id = d.id WHERE d.geo_id = '0670000';`
    Plus: `SELECT id, label, district_type FROM essentials.districts WHERE geo_id = '0670000';`

    Q3 — LA City Clerk office existence (RESEARCH.md Open Question 3):
    `SELECT id, title, district_id, politician_id FROM essentials.offices WHERE title ILIKE '%clerk%' AND id IN (SELECT id FROM essentials.offices WHERE district_id IN (SELECT id FROM essentials.districts WHERE geo_id = '0644000'));` — also: `SELECT id, name FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name = 'City of Los Angeles' AND state = 'CA');`

    Q4 — Confirm City Controller office is currently unoccupied:
    `SELECT id, title, politician_id FROM essentials.offices WHERE id IN ('5a873c59-72ac-488f-8b2c-44dfd04d065c','e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6');`
    Expect both `politician_id` columns to be NULL before this wave.

    Q5 — External_id range clean for -700001..-700049:
    `SELECT COUNT(*) AS used FROM essentials.politicians WHERE external_id BETWEEN -700049 AND -700001;`
    Expected: 0.

    Apply the script via `psql $DATABASE_URL -f backend/scripts/preflight-la-wave2.sql` and capture results.

    Create `backend/migrations/300_la_wave2_preflight.sql` wrapping the captured results in `BEGIN; ... COMMIT;` as SQL comments only (same pattern as 108-01 T1). The comment body MUST record:
    - bh_office_count = N (where N drives the T2 decision to create additional office rows if N < 6)
    - bh_existing_titles = [...]
    - sm_office_count = N
    - sm_existing_titles = [...]
    - city_clerk_office_exists = true/false (drives T4 decision: link vs create+link)
    - city_attorney_office_politician_id = NULL/non-null
    - city_controller_office_politician_id = NULL/non-null
    - external_id_range_used = 0 (MUST be 0; halt the wave otherwise)

    This file MUST contain zero non-comment DDL/DML between BEGIN and COMMIT.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/scripts/preflight-la-wave2.sql > /tmp/preflight-wave2-out.txt 2>&amp;1 &amp;&amp; grep -E '(bh_office_count|sm_office_count|0606308|0670000|0644000)' /tmp/preflight-wave2-out.txt</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/preflight-la-wave2.sql` executes against `$DATABASE_URL` with exit code 0
    - Output contains rows for all four FIPS codes referenced: `'0606308'`, `'0670000'`, `'0644000'`, and the two office UUIDs from migration 115
    - The Q5 output shows `used = 0` (or the wave halts with a documented conflict)
    - `backend/migrations/300_la_wave2_preflight.sql` exists, starts with `BEGIN;`, ends with `COMMIT;`, and contains all 7 documented values listed in the action (bh_office_count, bh_existing_titles, sm_office_count, sm_existing_titles, city_clerk_office_exists, city_attorney_office_politician_id, city_controller_office_politician_id) as `-- <key>:` comment lines
    - `grep -v '^--' backend/migrations/300_la_wave2_preflight.sql | grep -v '^BEGIN' | grep -v '^COMMIT' | grep -v '^$' | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Pre-flight captured. T2/T3/T4 know whether they need to create additional Beverly Hills office rows (Pitfall 10), whether the LA City Clerk office exists today or must be created, and that no other politician currently occupies the LA City Controller or City Attorney offices.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Beverly Hills 5 council members + Treasurer + geo_id backfill</name>
  <files>backend/migrations/301_la_wave2_beverly_hills.sql</files>
  <read_first>
    - backend/migrations/300_la_wave2_preflight.sql (read bh_office_count and bh_existing_titles)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Tier 2: Beverly Hills" section, "Pitfall 10")
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("At-large council pattern" code block, "Government Stub — WHERE NOT EXISTS Guard", "District Insert — NO ON CONFLICT", "Chamber Insert — slug is GENERATED")
    - backend/migrations/218_sj_officials.sql (canonical CTE pattern)
    - backend/migrations/217_sj_government_structure.sql (chamber/district insert idioms)
  </read_first>
  <action>
    Create `backend/migrations/301_la_wave2_beverly_hills.sql` wrapped in `BEGIN; ... COMMIT;`.

    Step A — geo_id backfill (idempotent):
    `UPDATE essentials.districts SET geo_id = '0606308' WHERE label LIKE 'Beverly Hills%' AND state = 'CA' AND district_type IN ('LOCAL','LOCAL_EXEC') AND geo_id IS DISTINCT FROM '0606308';`

    Step B — government + chamber idempotency (in case BH government/chamber were never created):
    Insert `'City of Beverly Hills'` into `essentials.governments` (type='LOCAL', state='CA', city='Beverly Hills', geo_id='0606308') using `WHERE NOT EXISTS (SELECT 1 FROM essentials.governments WHERE name = 'City of Beverly Hills' AND state = 'CA')`.
    Insert chamber `'City Council'` (name_formal='Beverly Hills City Council') with `WHERE NOT EXISTS` guard. NEVER include `slug` (GENERATED ALWAYS AS — Pitfall 4).
    Insert chamber `'City Treasurer'` (name_formal='Beverly Hills City Treasurer') with WHERE NOT EXISTS guard — required for Howard Fisher's office FK.

    Step C — district idempotency:
    Insert single LOCAL district `(geo_id='0606308', district_type='LOCAL', label='Beverly Hills (At-Large)', state='CA')` using `WHERE NOT EXISTS (SELECT 1 FROM essentials.districts WHERE geo_id = '0606308' AND district_type = 'LOCAL' AND state = 'CA')`. NO ON CONFLICT (Pitfall 5).
    Insert LOCAL_EXEC district `(geo_id='0606308', district_type='LOCAL_EXEC', label='Beverly Hills (Citywide)', state='CA')` with same NOT EXISTS guard — required for the City Treasurer office.

    Step D — insert 5 council members (D-04: at-large = single LOCAL district, all members FK to same district), using the CTE pattern from 108-PATTERNS.md "At-large council pattern" code block. Title = 'Council Member'. Chamber lookup name='City Council', government name='City of Beverly Hills'. All filter `d.geo_id='0606308' AND d.district_type='LOCAL' AND d.state='CA'`. party=NULL. is_active=true, is_incumbent=true, is_appointed=false, is_vacant=false. is_appointed_position=false on office.
    Roster from RESEARCH.md "Beverly Hills (at-large, 5 seats)":
    - -700010 Lester Friedman — photo_origin_url from beverlyhills.org city council page (or Wikipedia portrait)
    - -700011 Sharona R. Nazarian — photo from beverlyhills.org or Wikipedia
    - -700012 John Mirisch — photo
    - -700013 Craig Corman — photo
    - -700014 Mary Wells — photo

    Step E — insert City Treasurer Howard Fisher (-700015). Title='City Treasurer'. Chamber lookup name='City Treasurer'. Filter `d.geo_id='0606308' AND d.district_type='LOCAL_EXEC' AND d.state='CA'`. is_appointed=false, is_appointed_position=false (he is popularly ELECTED per RESEARCH.md Tier 2 BH entry).

    Step F — office_id back-fill:
    `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -700019 AND -700010 AND p.office_id IS NULL;`

    Apply via psql. Re-apply to confirm idempotency.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/301_la_wave2_beverly_hills.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/301_la_wave2_beverly_hills.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700019 AND -700010 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL);" | grep -E '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0 (idempotency proof)
    - `SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON o.district_id = d.id WHERE d.geo_id = '0606308';` returns >= 6 (5 council + 1 treasurer; may be higher if BH had pre-existing rows)
    - `SELECT full_name FROM essentials.politicians WHERE external_id IN (-700010,-700011,-700012,-700013,-700014,-700015) ORDER BY external_id;` returns exactly 6 rows in this order: 'Lester Friedman', 'Sharona R. Nazarian', 'John Mirisch', 'Craig Corman', 'Mary Wells', 'Howard Fisher'
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700019 AND -700010 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - `SELECT COUNT(*) FROM essentials.districts WHERE geo_id = '0606308' AND district_type IN ('LOCAL','LOCAL_EXEC');` returns >= 2 (1 LOCAL + 1 LOCAL_EXEC)
    - Migration file body has NO occurrence of `ON CONFLICT (geo_id, district_type)` (Pitfall 5)
    - Migration file body has NO occurrence of `slug` inside an `INSERT INTO essentials.chambers` column list (Pitfall 4) — verify via `grep -E 'INSERT INTO essentials\.chambers[^;]*slug' backend/migrations/301_la_wave2_beverly_hills.sql` returns no match
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Beverly Hills has its full 5-member at-large council + elected City Treasurer Howard Fisher in the DB, all with photos and back-filled office_id. The required at-large LOCAL and citywide LOCAL_EXEC district rows exist with geo_id=0606308.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Santa Monica 7 council members + geo_id backfill</name>
  <files>backend/migrations/302_la_wave2_santa_monica.sql</files>
  <read_first>
    - backend/migrations/300_la_wave2_preflight.sql (read sm_office_count and sm_existing_titles)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Tier 2: Santa Monica" section)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("At-large council pattern" code block; "Shared Patterns")
    - backend/migrations/301_la_wave2_beverly_hills.sql (produced by T2 — structural twin for Santa Monica)
  </read_first>
  <action>
    Create `backend/migrations/302_la_wave2_santa_monica.sql` wrapped in `BEGIN; ... COMMIT;`.

    Step A — geo_id backfill:
    `UPDATE essentials.districts SET geo_id = '0670000' WHERE label LIKE 'Santa Monica%' AND state = 'CA' AND district_type IN ('LOCAL','LOCAL_EXEC') AND geo_id IS DISTINCT FROM '0670000';`

    Step B — government + chamber idempotency:
    Insert `'City of Santa Monica'` (type='LOCAL', state='CA', city='Santa Monica', geo_id='0670000') with WHERE NOT EXISTS guard.
    Insert chamber `'City Council'` (name_formal='Santa Monica City Council') with WHERE NOT EXISTS. NO `slug` in column list.

    Step C — district idempotency:
    Insert single LOCAL district `(geo_id='0670000', district_type='LOCAL', label='Santa Monica (At-Large)', state='CA')` with WHERE NOT EXISTS. NO ON CONFLICT.

    Step D — insert 7 council members per RESEARCH.md "Santa Monica (at-large, 7 seats)". D-04: all 7 FK to the same single LOCAL district.
    - -700030 Lana Negrete (Mayor 2026; record title='Council Member' since per RESEARCH.md the Mayor role is selected by council vote, not separately elected — D-03 says only separately elected positions get separate office)
    - -700031 Jesse Zwick
    - -700032 Caroline Torosis
    - -700033 Dan Hall
    - -700034 Ellis Raskin
    - -700035 Barry Snell
    - -700036 Natalya Zernitskaya

    Each politician: party=NULL, is_active=true, is_incumbent=true, is_appointed=false, is_vacant=false. Office row: title='Council Member', is_appointed_position=false, is_vacant=false, role_canonical=NULL. Chamber lookup name='City Council', government name='City of Santa Monica'. District filter `d.geo_id='0670000' AND d.district_type='LOCAL' AND d.state='CA'`. photo_origin_url from santamonica.gov or Wikipedia portrait for each.

    Step E — office_id back-fill:
    `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id BETWEEN -700039 AND -700030 AND p.office_id IS NULL;`

    Apply via psql twice (idempotency proof).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/302_la_wave2_santa_monica.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/302_la_wave2_santa_monica.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700039 AND -700030;" | grep -E '^7$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700039 AND -700030;` returns 7
    - `SELECT full_name FROM essentials.politicians WHERE external_id IN (-700030,-700031,-700032,-700033,-700034,-700035,-700036) ORDER BY external_id;` returns exactly: 'Lana Negrete', 'Jesse Zwick', 'Caroline Torosis', 'Dan Hall', 'Ellis Raskin', 'Barry Snell', 'Natalya Zernitskaya'
    - `SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON o.district_id = d.id WHERE d.geo_id = '0670000';` returns >= 7
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700039 AND -700030 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - Migration file body has NO `ON CONFLICT (geo_id, district_type)` and NO `slug` in chamber INSERT
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Santa Monica has its full 7-member at-large council in the DB attached to the single LOCAL district with geo_id=0670000.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: LA City Controller (Mejia) + City Clerk (Lattimore); explicitly skip City Attorney</name>
  <files>backend/migrations/303_la_wave2_la_city_controller_clerk.sql</files>
  <read_first>
    - backend/migrations/300_la_wave2_preflight.sql (read city_clerk_office_exists, city_attorney_office_politician_id, city_controller_office_politician_id)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Critical Pre-Flight Corrections" table, "Tier 3: LA City Citywide Officials", Pitfalls 2 and 3)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Wave 2: Beverly Hills + Santa Monica + LA City Citywide Offices" — Analog C and the LA City Controller code block; "Officials — appointed position" code block for Lattimore; "LA City Known UUIDs" table)
    - backend/migrations/115_la_city_attorney_controller_district.sql (full — confirms the existing UUIDs)
  </read_first>
  <action>
    Create `backend/migrations/303_la_wave2_la_city_controller_clerk.sql` wrapped in `BEGIN; ... COMMIT;`.

    The migration MUST begin with a header comment block that states:
    ```
    -- LA City Attorney office id='5a873c59-72ac-488f-8b2c-44dfd04d065c' INTENTIONALLY LEFT VACANT.
    -- Hydee Feldstein Soto lost the June 2026 primary. Office is in runoff (Roy vs. McKinney, Nov 2026).
    -- Per CONTEXT.md D-08 + 108-RESEARCH.md Critical Finding 1: no politician seeded for this office in Wave 2.
    ```

    Step A — Kenneth Mejia, City Controller, external_id=-700001.
    Insert the politician row with party=NULL, is_active=true, is_appointed=false, is_vacant=false, is_incumbent=true, photo_origin_url='https://controller.lacity.gov/about' (or a verified controller.lacity.gov portrait URL).
    Then update the EXISTING office row (do NOT create a new one — it already exists at UUID `e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6` per migration 115):
    `UPDATE essentials.offices SET politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -700001) WHERE id = 'e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6' AND politician_id IS NULL;`
    (Guard with `politician_id IS NULL` for idempotency — second run does nothing if already linked.)

    Step B — Patrice Lattimore, City Clerk, external_id=-700002, is_appointed=true (RESEARCH.md Critical Finding 2 — she was appointed by City Council Sept 2025).
    Behavior is conditional on the T1 pre-flight value `city_clerk_office_exists`:
    - If `city_clerk_office_exists = false`: First, insert chamber `'City Clerk'` (name_formal='Los Angeles City Clerk') for government 'City of Los Angeles' with WHERE NOT EXISTS guard. Then insert the politician via the CTE pattern from 108-PATTERNS.md "Officials — appointed position" code block, with the office row created at the same time. Office: title='City Clerk', is_appointed_position=true, is_vacant=false. District filter `d.geo_id='0644000' AND d.district_type IN ('LOCAL','LOCAL_EXEC') AND d.state='CA'`.
    - If `city_clerk_office_exists = true` (pre-existing office row): Insert the politician separately, then UPDATE the existing office row's politician_id with an `IS NULL` guard (same pattern as Step A).

    Step C — office_id back-fill for both:
    `UPDATE essentials.politicians p SET office_id = o.id FROM essentials.offices o WHERE o.politician_id = p.id AND p.external_id IN (-700001, -700002) AND p.office_id IS NULL;`

    The migration must NOT touch office UUID `5a873c59-72ac-488f-8b2c-44dfd04d065c` (City Attorney). Verify by `grep -F '5a873c59' backend/migrations/303_la_wave2_la_city_controller_clerk.sql` returning ONLY the header comment, NOT inside any DML.

    Apply via psql twice (idempotency proof).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/303_la_wave2_la_city_controller_clerk.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/303_la_wave2_la_city_controller_clerk.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT (SELECT politician_id FROM essentials.offices WHERE id='e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6') IS NOT NULL AND (SELECT politician_id FROM essentials.offices WHERE id='5a873c59-72ac-488f-8b2c-44dfd04d065c') IS NULL;" | grep -E '^t$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT full_name FROM essentials.politicians WHERE external_id = -700001;` returns 'Kenneth Mejia'
    - `SELECT full_name, is_appointed FROM essentials.politicians WHERE external_id = -700002;` returns 'Patrice Lattimore' with `is_appointed = true`
    - `SELECT politician_id FROM essentials.offices WHERE id = 'e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6';` returns the UUID matching Mejia (`p.id`) — not NULL
    - `SELECT politician_id FROM essentials.offices WHERE id = '5a873c59-72ac-488f-8b2c-44dfd04d065c';` returns NULL (City Attorney remains unoccupied)
    - `SELECT office_id IS NOT NULL FROM essentials.politicians WHERE external_id IN (-700001, -700002);` returns true for both rows
    - `SELECT title, is_appointed_position FROM essentials.offices WHERE politician_id = (SELECT id FROM essentials.politicians WHERE external_id = -700002);` returns title='City Clerk' with is_appointed_position=true
    - Migration body header comment contains the strings 'INTENTIONALLY LEFT VACANT', '5a873c59-72ac-488f-8b2c-44dfd04d065c', 'Feldstein Soto', and 'runoff' (verifiable: `grep -E '(INTENTIONALLY LEFT VACANT|5a873c59|Feldstein Soto|runoff)' backend/migrations/303_la_wave2_la_city_controller_clerk.sql | wc -l` >= 4)
    - The literal string `5a873c59-72ac-488f-8b2c-44dfd04d065c` appears ONLY inside `--` comment lines, NEVER inside an `UPDATE` or `INSERT` statement (verifiable: `grep -E '^[^-].*5a873c59-72ac-488f-8b2c-44dfd04d065c' backend/migrations/303_la_wave2_la_city_controller_clerk.sql` returns no match)
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Kenneth Mejia is linked to the existing City Controller office; Patrice Lattimore is created or linked as the appointed City Clerk; LA City Attorney office remains explicitly unoccupied with an inline comment explaining the November 2026 runoff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → essentials schema | Static SQL; no user input |
| migration → existing UUIDs (115) | Update by hard-coded UUID literal; not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02-01 | Tampering | UPDATE essentials.offices SET politician_id WHERE id='5a873c59-...' | mitigate | T4 acceptance criteria forbids the City Attorney UUID from appearing outside the header comment. Automated grep enforces it. |
| T-108-02-02 | Integrity | Wrong incumbent (Holly Wolcott instead of Patrice Lattimore) | mitigate | RESEARCH.md Critical Finding 2 documented and citation propagated to T4 action; T4 acceptance asserts `full_name = 'Patrice Lattimore' AND is_appointed = true`. |
| T-108-02-03 | Integrity | Beverly Hills office row count mismatch (Pitfall 10) | mitigate | T1 pre-flight captures `bh_office_count` before T2 runs; T2 unconditionally creates the required district/chamber/office structure via WHERE NOT EXISTS guards regardless of pre-flight count. |
| T-108-02-04 | Tampering | Politician double-insert on re-run | mitigate | `ON CONFLICT (external_id) DO NOTHING` on every politician insert; verify by applying each migration twice and asserting no row count change. |
| T-108-02-SC | Tampering | npm/pip/cargo installs | N/A | Pure SQL migrations; no package installs. |
</threat_model>

<verification>
After all 4 tasks complete, run the Wave 2 phase gate (matches 108-VALIDATION.md Wave 2 block):

```sql
-- Beverly Hills: 5 council + 1 treasurer = 6 politicians
SELECT COUNT(*) FROM essentials.politicians p
JOIN essentials.offices o ON o.politician_id = p.id
JOIN essentials.districts d ON o.district_id = d.id
WHERE d.geo_id = '0606308';
-- Expected: >= 6

-- Santa Monica: 7 council
SELECT COUNT(*) FROM essentials.politicians p
JOIN essentials.offices o ON o.politician_id = p.id
JOIN essentials.districts d ON o.district_id = d.id
WHERE d.geo_id = '0670000';
-- Expected: >= 7

-- LA City Controller (Mejia) and City Clerk (Lattimore) present
SELECT p.full_name, o.title
FROM essentials.politicians p
JOIN essentials.offices o ON o.politician_id = p.id
WHERE p.external_id IN (-700001, -700002)
ORDER BY p.external_id;
-- Expected: 2 rows: Mejia/City Controller, Lattimore/City Clerk

-- LA City Attorney remains vacant
SELECT politician_id IS NULL AS still_vacant
FROM essentials.offices
WHERE id = '5a873c59-72ac-488f-8b2c-44dfd04d065c';
-- Expected: t (true)

-- All Wave 2 politicians have photo, office_id, party=NULL
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -700049 AND -700001
  AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL);
-- Expected: 0
```
</verification>

<success_criteria>
1. Migrations 300–303 exist, idempotent, applied.
2. Beverly Hills has 5 council members + Howard Fisher (City Treasurer) attached to its LOCAL/LOCAL_EXEC districts with geo_id=0606308.
3. Santa Monica has 7 council members attached to its single LOCAL district with geo_id=0670000.
4. Kenneth Mejia linked to existing City Controller office (UUID `e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6`).
5. Patrice Lattimore created as City Clerk with is_appointed=true; office_id back-filled.
6. LA City Attorney office (UUID `5a873c59-72ac-488f-8b2c-44dfd04d065c`) remains UNOCCUPIED with an inline migration comment documenting the November 2026 runoff.
7. Every politician (external_id -700001..-700049) has photo_origin_url, office_id, party=NULL.
</success_criteria>

<output>
Create `.planning/phases/108-la-county-city-officials/108-02-SUMMARY.md` when done. Include:
- Final BH and SM politician counts
- LA City Controller link confirmed; LA City Clerk creation path (new chamber/office vs link to existing)
- Explicit statement: "LA City Attorney office UUID 5a873c59... left vacant per RESEARCH.md Critical Finding 1"
</output>
