---
phase: 108-la-county-city-officials
plan: 04
type: execute
wave: 4
depends_on:
  - 108-01
  - 108-02
  - 108-03
files_modified:
  - backend/scripts/verify-la-county-108.sql
  - backend/scripts/smoke-la-representatives-me.ts
  - backend/migrations/310_la_wave4_geo_id_audit.sql
autonomous: true
requirements:
  - LAOF-04
  - LAOF-05
  - LAOF-06

tags:
  - sql-migration
  - verification
  - smoke-test
  - la-county

must_haves:
  truths:
    - "A standalone verification SQL script (`backend/scripts/verify-la-county-108.sql`) exists and returns zero failures across all Phase 108 acceptance gates"
    - "All 24+ Phase 108 city FIPS codes are present on essentials.districts.geo_id (LAOF-04)"
    - "Every politician inserted by Phase 108 (external_id BETWEEN -700699 AND -700001) has photo_origin_url populated (LAOF-05)"
    - "Every politician inserted by Phase 108 has office_id back-filled (joins via p.office_id = o.id resolve)"
    - "A smoke-test TypeScript script (`backend/scripts/smoke-la-representatives-me.ts`) exercises `GET /api/essentials/representatives/me` for a known LA-area test user and confirms new Phase 108 officials surface"
    - "Migration 310 captures a final audit snapshot (politician counts per city) as comments for v2.9 milestone close"
  artifacts:
    - path: "backend/scripts/verify-la-county-108.sql"
      provides: "Re-runnable phase gate — single file with 8 labeled assertions covering all Phase 108 success criteria"
      min_lines: 80
    - path: "backend/scripts/smoke-la-representatives-me.ts"
      provides: "End-to-end smoke test calling GET /api/essentials/representatives/me for a known LA address; asserts new Phase 108 politicians appear"
      min_lines: 30
    - path: "backend/migrations/310_la_wave4_geo_id_audit.sql"
      provides: "Documentation-only migration recording final Phase 108 audit snapshot (per-city politician counts, deferred VERIFICATION-PENDING items, range usage)"
      min_lines: 40
  key_links:
    - from: "verify-la-county-108.sql assertions"
      to: "LAOF-01..LAOF-06 success criteria"
      via: "each assertion is a labeled query whose expected result is documented inline"
      pattern: "-- ASSERTION:"
    - from: "smoke-la-representatives-me.ts"
      to: "GET /api/essentials/representatives/me"
      via: "fetch call with Authorization Bearer; asserts response includes a newly-inserted Phase 108 politician"
      pattern: "/api/essentials/representatives/me"
---

<objective>
Verify that Phase 108 is complete and correct: every required FIPS geo_id is set, every new politician has a photo URL and office_id, no `slug`/`ON CONFLICT` antipatterns leaked into any migration, and the existing `GET /api/essentials/representatives/me` endpoint surfaces newly seeded LA County officials for a known LA address (smoke-test scope only, not new feature work).

Purpose: Phase 108 is data-only (no API/frontend changes), but it changes which politicians the representatives-me endpoint returns for LA-area users. Without this verification plan, the only proof the data layer is wired correctly would be manual spot-checking. This plan automates that check.

Output:
- `backend/scripts/verify-la-county-108.sql` — single phase gate script with 8 labeled assertions
- `backend/scripts/smoke-la-representatives-me.ts` — TypeScript smoke test calling the existing endpoint
- `backend/migrations/310_la_wave4_geo_id_audit.sql` — comment-only documentation migration that captures the final per-city counts for v2.9 milestone records
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-la-county-city-officials/108-CONTEXT.md
@.planning/phases/108-la-county-city-officials/108-RESEARCH.md
@.planning/phases/108-la-county-city-officials/108-VALIDATION.md
@.planning/phases/108-la-county-city-officials/108-01-PLAN.md
@.planning/phases/108-la-county-city-officials/108-02-PLAN.md
@.planning/phases/108-la-county-city-officials/108-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the phase verification SQL script</name>
  <files>backend/scripts/verify-la-county-108.sql</files>
  <read_first>
    - .planning/phases/108-la-county-city-officials/108-VALIDATION.md (full — list of expected verification queries)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Verification Queries After Each Wave" and "Full phase gate" sections at lines 590-665)
    - .planning/phases/108-la-county-city-officials/108-01-PLAN.md (Wave 1 acceptance criteria, especially the photo/office_id/party guards)
    - .planning/phases/108-la-county-city-officials/108-02-PLAN.md (Wave 2 acceptance criteria, especially the City Attorney vacancy assertion)
    - .planning/phases/108-la-county-city-officials/108-03-PLAN.md (Wave 3 acceptance criteria, especially the Alhambra negative assertion)
  </read_first>
  <action>
    Create `backend/scripts/verify-la-county-108.sql` containing 8 labeled assertions. Each assertion uses this format:
    ```
    -- ============================================================
    -- ASSERTION <N>: <description>
    -- Expected: <expected result>
    -- ============================================================
    <SQL query>;
    ```

    Assertion 1 — LAOF-05: every Phase 108 politician has photo_origin_url, office_id, party=NULL, is_incumbent=true.
    `SELECT COUNT(*) AS failures FROM essentials.politicians WHERE external_id BETWEEN -700699 AND -700001 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` — Expected: 0

    Assertion 2 — LAOF-04: every Phase 108 city has a district row with the expected geo_id.
    `SELECT v.geo_id, (SELECT COUNT(*) FROM essentials.districts d WHERE d.geo_id = v.geo_id) AS district_rows FROM (VALUES ('0643000'),('0630000'),('0608954'),('0619766'),('0622230'),('0636546'),('0640130'),('0652526'),('0655156'),('0656000'),('0658072'),('0669088'),('0680000'),('0684200'),('0606308'),('0670000'),('0644000'),('0673080'),('0615044'),('0611530'),('0632548'),('0685292'),('0600884'),('0628168'),('0617568'),('0622412')) AS v(geo_id) ORDER BY v.geo_id;` — Expected: every row's district_rows >= 1. (Note: West Hollywood FIPS uses the value captured in migration 304; if it differs from the inferred `0684346`, update this assertion's VALUES list to match the verified value.)

    Assertion 3 — Wave 1 sanity: each Tier 1 city has at least the expected number of politicians.
    Use the count query from 108-VALIDATION.md row 108-01-01 (count of politicians joining offices joining districts in the 14 Tier 1 FIPS codes) — Expected: >= 14 (one per city minimum); ideal: full governing body counts per RESEARCH.md.

    Assertion 4 — Wave 2 sanity: Beverly Hills >= 6 politicians, Santa Monica >= 7 politicians.
    `SELECT (SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON o.district_id=d.id WHERE d.geo_id='0606308') AS bh_count, (SELECT COUNT(*) FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id=p.id JOIN essentials.districts d ON o.district_id=d.id WHERE d.geo_id='0670000') AS sm_count;` — Expected: bh_count >= 6, sm_count >= 7.

    Assertion 5 — Wave 2: LA City offices state.
    `SELECT (SELECT politician_id IS NOT NULL FROM essentials.offices WHERE id='e5435b0e-c7a7-4c93-9b4f-cc647db0b9f6') AS controller_seated, (SELECT politician_id IS NULL FROM essentials.offices WHERE id='5a873c59-72ac-488f-8b2c-44dfd04d065c') AS attorney_vacant, EXISTS (SELECT 1 FROM essentials.politicians WHERE external_id=-700002 AND is_appointed=true) AS clerk_appointed;` — Expected: all 3 booleans TRUE.

    Assertion 6 — Wave 3: 10 new governments exist; ~50 new politicians inserted.
    `SELECT (SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of South Gate','City of Compton','City of Carson','City of Hawthorne','City of Whittier','City of Alhambra','City of Gardena','City of Culver City','City of West Hollywood','City of El Segundo') AND state='CA') AS new_governments, (SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700699 AND -700200) AS wave3_politicians;` — Expected: new_governments=10, wave3_politicians >= 45 (lower bound; actual may be higher with verified Compton officers and Hawthorne 5th).

    Assertion 7 — Wave 3 negative assertions (Alhambra, Culver City, WeHo, El Segundo, South Gate have no LOCAL_EXEC + no Mayor chamber).
    `SELECT g.name, (SELECT COUNT(*) FROM essentials.chambers c WHERE c.government_id=g.id AND c.name='Mayor') AS mayor_chambers, (SELECT COUNT(*) FROM essentials.districts d WHERE d.geo_id=g.geo_id AND d.district_type='LOCAL_EXEC') AS local_exec_districts FROM essentials.governments g WHERE g.name IN ('City of Alhambra','City of Culver City','City of West Hollywood','City of El Segundo','City of South Gate') AND g.state='CA' ORDER BY g.name;` — Expected: every row has mayor_chambers=0 AND local_exec_districts=0.

    Assertion 8 — Migration files do not contain antipatterns (Pitfalls 4, 5).
    This assertion runs via a shell wrapper, not pure SQL — script ends with a comment `-- Shell complement: grep -E '(INSERT INTO essentials\.chambers[^;]*slug|ON CONFLICT \(geo_id, district_type\))' backend/migrations/29[3-9]_la_*.sql backend/migrations/30[0-9]_la_*.sql should return no matches.`

    Apply: `psql $DATABASE_URL -f backend/scripts/verify-la-county-108.sql` and capture output.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/scripts/verify-la-county-108.sql > /tmp/verify-108.txt 2>&amp;1 &amp;&amp; grep -c "ASSERTION" /tmp/verify-108.txt | grep -E '^8$'</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/verify-la-county-108.sql` exists with at least 80 lines
    - File contains exactly 8 lines matching the pattern `-- ASSERTION <N>:` (verifiable: `grep -cE '^-- ASSERTION [0-9]+:' backend/scripts/verify-la-county-108.sql` returns 8)
    - File runs against `$DATABASE_URL` with exit code 0
    - Assertion 1 returns `failures = 0`
    - Assertion 5 returns three TRUE values (controller_seated, attorney_vacant, clerk_appointed)
    - Assertion 7 returns 5 rows, each with mayor_chambers=0 AND local_exec_districts=0
    - Shell complement: `grep -rE '(INSERT INTO essentials\.chambers[^;]*slug|ON CONFLICT \(geo_id, district_type\))' backend/migrations/29[3-9]_la_*.sql backend/migrations/30[0-9]_la_*.sql 2>/dev/null` returns no matches (or "No such file" if globs fail — acceptable when verified by inspection)
  </acceptance_criteria>
  <done>
    The phase gate script exists, runs cleanly, and asserts all six LAOF requirements with concrete SQL queries. Any failure surfaces immediately for the gap-closure workflow.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build a representatives-me smoke test for an LA-area test address</name>
  <files>backend/scripts/smoke-la-representatives-me.ts</files>
  <read_first>
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Architectural Responsibility Map" — confirms the integration point; "Representatives-me smoke test" stub near line 660)
    - backend/scripts/smoke-va-geofences.ts (existing analog — look for similar smoke-test patterns; file is in git status as new untracked)
  </read_first>
  <action>
    Create `backend/scripts/smoke-la-representatives-me.ts` as a Node.js executable TypeScript file (run via `tsx` or compiled). The script must:

    Step A — Read env: `API_BASE_URL` (default `http://localhost:3001`), `SMOKE_TEST_BEARER_TOKEN` (the existing Connected-tier test user JWT for an LA-area address — Chris's UUID `4e6dde8f-2bd0-4054-824f-4164744165ea` per Phase 50 reference, or whichever LA test user exists in the project's secrets).

    Step B — Call `GET ${API_BASE_URL}/api/essentials/representatives/me` with header `Authorization: Bearer ${SMOKE_TEST_BEARER_TOKEN}`.

    Step C — Parse the JSON response. Assert the response status is 200 (otherwise log and exit 1).

    Step D — Assert at least one of the returned politicians has `external_id` BETWEEN -700699 AND -700001 (i.e., a Phase 108 record). This proves the integration: a real LA-area user sees newly-seeded officials. If no Phase 108 politician appears (e.g., the test user's district doesn't intersect any Phase 108 city), log the response politicians list and exit with a non-zero code AND a clear message stating the test user's address may need updating — not a hard failure of Phase 108 unless the test user is known to be in a Phase 108 city.

    Step E — Print a summary table: count of total politicians returned, count of Phase 108 politicians (external_id < -700000), list of Phase 108 politician full_names.

    Step F — Exit 0 on success, 1 on any failure.

    The script must NOT mutate state — it is a read-only smoke test. No DB writes. No new test users created.

    Use only Node.js built-ins (`fetch` is global in Node 18+) plus `process.env`. No new npm dependencies.
  </action>
  <verify>
    <automated>npx --no-install tsx backend/scripts/smoke-la-representatives-me.ts 2>&amp;1 | tee /tmp/smoke-out.txt; grep -E "(total politicians|Phase 108 politicians)" /tmp/smoke-out.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/scripts/smoke-la-representatives-me.ts` exists with at least 30 lines
    - File contains `/api/essentials/representatives/me` as a string literal (the endpoint being tested)
    - File reads `process.env.API_BASE_URL` and `process.env.SMOKE_TEST_BEARER_TOKEN`
    - File uses ONLY Node.js built-ins (no `import` of npm packages outside Node stdlib; verifiable: `grep -E "^import " backend/scripts/smoke-la-representatives-me.ts | grep -vE "from ['\"]node:"` returns no match, OR the imports are limited to TypeScript-typed Node built-ins)
    - File contains an assertion guarding `external_id BETWEEN -700699 AND -700001` (or equivalent JS comparison)
    - Running `npx tsx backend/scripts/smoke-la-representatives-me.ts` with valid env vars exits 0 and prints the politician counts
    - Running the same with no/invalid bearer token exits 1 with a clear error message and does NOT crash with an unhandled exception
  </acceptance_criteria>
  <done>
    A re-runnable smoke test exists. Future operators (and gap-closure runs) can use this to confirm representatives-me still surfaces Phase 108 officials.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migration 310 — final audit snapshot</name>
  <files>backend/migrations/310_la_wave4_geo_id_audit.sql</files>
  <read_first>
    - backend/scripts/verify-la-county-108.sql (produced by T1 — read the output snapshot)
    - .planning/phases/108-la-county-city-officials/108-01-PLAN.md (Wave 1 success criteria)
    - .planning/phases/108-la-county-city-officials/108-02-PLAN.md (Wave 2 success criteria)
    - .planning/phases/108-la-county-city-officials/108-03-PLAN.md (Wave 3 success criteria)
  </read_first>
  <action>
    Run `psql $DATABASE_URL -f backend/scripts/verify-la-county-108.sql` and capture its output.

    Then write `backend/migrations/310_la_wave4_geo_id_audit.sql` wrapped in `BEGIN; ... COMMIT;`. The body must be comment-only and record:

    - Header: `-- Phase 108 final audit snapshot, applied YYYY-MM-DD (matches v2.9 milestone close)`
    - For each of the 26 Phase 108 FIPS codes (the union of 14 Tier 1 + Beverly Hills + Santa Monica + LA City (0644000) + 10 new), one line: `-- <City Name> (geo_id=<FIPS>): <N> politicians, <M> offices, <K> districts`. Counts pulled from the verify script output.
    - Range usage: `-- external_id range used: -700XXX (lowest) to -700YYY (highest); total Phase 108 politicians: <N>`
    - List of all `-- VERIFICATION-PENDING:` items from migrations 293-309 (compile by grepping all Phase 108 migrations for that string and pasting the lines).
    - Final assertion confirmations: `-- LAOF-01 closed: <true/false> (basis: <gate>)`, similarly for LAOF-02..LAOF-06.

    The file MUST contain zero non-comment DDL/DML between BEGIN and COMMIT.

    Apply via `psql $DATABASE_URL -f backend/migrations/310_la_wave4_geo_id_audit.sql` to anchor the audit timestamp into the migrations chain.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/310_la_wave4_geo_id_audit.sql &amp;&amp; grep -cE "^-- (LAOF-|external_id range|City of)" backend/migrations/310_la_wave4_geo_id_audit.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists, starts with `BEGIN;`, ends with `COMMIT;`
    - `grep -v '^--' backend/migrations/310_la_wave4_geo_id_audit.sql | grep -v '^BEGIN' | grep -v '^COMMIT' | grep -v '^$' | wc -l` returns 0 (no DDL/DML)
    - File contains at least 6 lines matching `^-- LAOF-`
    - File contains at least 26 lines containing `geo_id=` (one per Phase 108 city)
    - File contains one or more lines matching `^-- external_id range used:`
    - File contains a `VERIFICATION-PENDING` summary section (the line `-- VERIFICATION-PENDING items compiled from migrations 293-309:` followed by the actual deferred items, or `-- VERIFICATION-PENDING items: none` if all seats were verified)
    - Migration applies via psql with exit code 0
  </acceptance_criteria>
  <done>
    A migration-numbered audit snapshot (310) is committed alongside the verification script. v2.9 milestone close has a single canonical record of what Phase 108 actually shipped, including deferred items.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke-test script → API endpoint | Read-only HTTP GET with bearer token; no DB writes |
| verify SQL → DB | Read-only SELECT queries |
| migration 310 → DB | Comment-only no-op transaction; preserves migration ordinal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-04-01 | Information Disclosure | SMOKE_TEST_BEARER_TOKEN logged | mitigate | T2 acceptance: script must NOT print the bearer token. Token is read from env only; any console.log of headers must redact `Authorization`. |
| T-108-04-02 | Tampering | smoke test inadvertently performs a write | mitigate | T2 acceptance: script uses only `GET`; no `POST`/`PUT`/`DELETE`/`PATCH` calls. Verifiable: `grep -E "method:\s*['\"](POST|PUT|DELETE|PATCH)" backend/scripts/smoke-la-representatives-me.ts` returns no match. |
| T-108-04-03 | Integrity | Audit snapshot drifts from live DB | accept | The audit migration 310 is a point-in-time snapshot. The verify script (T1) is the authoritative source for current state and is re-runnable. |
| T-108-04-SC | Tampering | npm/pip/cargo installs | N/A | T2 explicitly forbids new npm dependencies; only Node built-ins and tsx (already in repo dev deps). |
</threat_model>

<verification>
After all 3 tasks complete, run the final phase gate:

```bash
# 1. Apply all 8 verification assertions
psql "$DATABASE_URL" -f backend/scripts/verify-la-county-108.sql

# 2. Confirm migration files are clean of antipatterns
grep -rE '(INSERT INTO essentials\.chambers[^;]*slug|ON CONFLICT \(geo_id, district_type\))' backend/migrations/29[3-9]_la_*.sql backend/migrations/30[0-9]_la_*.sql
# Expected: empty output

# 3. Smoke-test the integration endpoint
API_BASE_URL=https://api.empowered.vote SMOKE_TEST_BEARER_TOKEN=$LA_TEST_USER_JWT \
  npx tsx backend/scripts/smoke-la-representatives-me.ts
# Expected: exit code 0; output shows >= 1 Phase 108 politician for the LA test user

# 4. Migration 310 applied
psql "$DATABASE_URL" -f backend/migrations/310_la_wave4_geo_id_audit.sql
```
</verification>

<success_criteria>
1. `backend/scripts/verify-la-county-108.sql` runs cleanly and reports all 8 assertions passing.
2. `backend/scripts/smoke-la-representatives-me.ts` exits 0 against a real LA-area test user and lists newly-seeded Phase 108 officials in the response.
3. Migration 310 applied, capturing the final per-city counts and any `VERIFICATION-PENDING:` items.
4. No migration in 293-309 contains the literal patterns `INSERT INTO essentials.chambers ... slug` or `ON CONFLICT (geo_id, district_type)`.
5. ROADMAP.md and STATE.md updated to reflect Phase 108 completion (handled during SUMMARY commit).
</success_criteria>

<output>
Create `.planning/phases/108-la-county-city-officials/108-04-SUMMARY.md` when done. Include:
- Verify script output snapshot (8 assertions, pass/fail per requirement)
- Smoke test output (count of Phase 108 politicians returned for the LA test user)
- List of any `-- VERIFICATION-PENDING:` items deferred to Phase 109 or a future quick task
- Confirmation that LAOF-01..LAOF-06 are all closed
</output>
