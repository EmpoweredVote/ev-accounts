---
phase: 108-la-county-city-officials
plan: 03
type: execute
wave: 3
depends_on:
  - 108-01
  - 108-02
files_modified:
  - backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql
  - backend/migrations/305_la_wave3_south_gate_compton.sql
  - backend/migrations/306_la_wave3_carson_hawthorne.sql
  - backend/migrations/307_la_wave3_whittier_alhambra.sql
  - backend/migrations/308_la_wave3_gardena_culver_city.sql
  - backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql
  - backend/scripts/preflight-la-wave3.sql
  - backend/scripts/verify-west-hollywood-fips.sh
autonomous: true
requirements:
  - LAOF-03
  - LAOF-05
  - LAOF-06

tags:
  - sql-migration
  - essentials-schema
  - la-county
  - new-cities
  - government-structure

must_haves:
  truths:
    - "10 new city governments exist in essentials.governments (South Gate, Compton, Carson, Hawthorne, Whittier, Alhambra, Gardena, Culver City, West Hollywood, El Segundo)"
    - "Each at-large city (South Gate, Hawthorne, Gardena, Culver City, West Hollywood, El Segundo) has 1 LOCAL district with geo_id=FIPS; at-large members all FK to it (D-04)"
    - "Each by-district city (Compton, Carson, Whittier, Alhambra) has 1 LOCAL district per council district plus 1 LOCAL_EXEC for separately elected Mayor (Compton/Carson/Whittier only; Alhambra has NO Mayor office per Pitfall 7)"
    - "West Hollywood FIPS code is verified via Census Geocoder API before the migration uses it (RESEARCH.md Open Question 1)"
    - "Every new politician has non-null photo_origin_url, office_id back-filled, party=NULL, is_incumbent=true (LAOF-05)"
    - "Compton City Clerk/Treasurer/Attorney are NOT seeded unless their current incumbent names are verified during pre-flight (D-03 conservative default; RESEARCH.md Assumption A5)"
    - "Gardena incumbent roster reflects post-June-2026 election outcome (RESEARCH.md Pitfall 9 + Assumption A6)"
  artifacts:
    - path: "backend/scripts/verify-west-hollywood-fips.sh"
      provides: "Curl-based Census Geocoder verification of West Hollywood FIPS place code; writes verified code to stdout for human approval"
      min_lines: 10
    - path: "backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql"
      provides: "Pre-flight documentation: WeHo FIPS confirmation, Gardena post-election roster, Hawthorne 5th seat verification, Compton elected officer names; external_id range -700200..-700699 clean check"
      min_lines: 30
    - path: "backend/migrations/305_la_wave3_south_gate_compton.sql"
      provides: "South Gate (at-large, 5 members) + Compton (by-district, 4 + Mayor) full structure + politicians"
      contains: "City of South Gate"
    - path: "backend/migrations/306_la_wave3_carson_hawthorne.sql"
      provides: "Carson (by-district 4 + Mayor + elected Clerk + Treasurer) + Hawthorne (at-large 4 + Mayor) full structure + politicians"
      contains: "Khaleah K. Bradshaw"
    - path: "backend/migrations/307_la_wave3_whittier_alhambra.sql"
      provides: "Whittier (by-district 4 + Mayor) + Alhambra (5 districts, NO Mayor — Pitfall 7) full structure + politicians"
      contains: "City of Alhambra"
    - path: "backend/migrations/308_la_wave3_gardena_culver_city.sql"
      provides: "Gardena (post-June-2026 roster) + Culver City (at-large 5) full structure + politicians"
      contains: "Freddy Puza"
    - path: "backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql"
      provides: "West Hollywood (at-large 5; FIPS verified in T1) + El Segundo (at-large 5) full structure + politicians"
      contains: "John Heilman"
    - path: "backend/scripts/preflight-la-wave3.sql"
      provides: "Re-runnable SQL script enumerating new-city government/district/office state for diff before/after"
  key_links:
    - from: "10 new governments"
      to: "essentials.governments.name = 'City of <X>' AND state='CA'"
      via: "INSERT … WHERE NOT EXISTS"
      pattern: "INSERT INTO essentials\\.governments"
    - from: "by-district cities (Compton/Carson/Whittier/Alhambra)"
      to: "N rows in essentials.districts (one per council district)"
      via: "VALUES list with WHERE NOT EXISTS guard"
      pattern: "FROM \\(VALUES"
    - from: "at-large cities"
      to: "1 row in essentials.districts (geo_id = FIPS, district_type='LOCAL')"
      via: "INSERT … WHERE NOT EXISTS"
      pattern: "district_type = 'LOCAL'"
    - from: "Compton/Carson/Whittier Mayors"
      to: "1 LOCAL_EXEC district per city (geo_id = city FIPS)"
      via: "INSERT … WHERE NOT EXISTS"
      pattern: "district_type = 'LOCAL_EXEC'"
    - from: "Alhambra"
      to: "NO Mayor chamber, NO LOCAL_EXEC district (Pitfall 7)"
      via: "Negative assertion: Alhambra migration must NOT contain a Mayor chamber INSERT"
      pattern: "Alhambra.*Mayor"
---

<objective>
Stand up the 10 new LA County cities listed in D-02 from scratch: government stub → chambers → districts → politicians + offices → office_id back-fill. Cities are grouped 2-per-migration to stay within context budget while keeping related work together.

This is the only plan in Phase 108 that creates NEW essentials.governments + essentials.chambers + essentials.districts rows (Plans 01 and 02 only insert politicians into existing structure). Use the canonical "Government structure" patterns from 108-PATTERNS.md "Wave 3" section.

City structure decisions (per RESEARCH.md "Council Structures Summary" table):
- **At-large, no separately elected Mayor** (single LOCAL district): South Gate, Culver City, West Hollywood, El Segundo
- **At-large, separately elected Mayor** (single LOCAL district + 1 LOCAL_EXEC district): Hawthorne, Gardena
- **By-district, separately elected Mayor** (N LOCAL districts + 1 LOCAL_EXEC): Compton (4+M), Carson (4+M), Whittier (4+M)
- **By-district, NO separately elected Mayor** (N LOCAL districts only): Alhambra (5 districts — Pitfall 7: Mayor is rotational, do NOT create a Mayor chamber or LOCAL_EXEC district)

Per D-03 conservative default: Compton's City Clerk/Treasurer/Attorney are ELECTED per charter (RESEARCH.md), but the current officer NAMES are not verified (Assumption A5). T2 (Compton migration) must include a pre-flight step that either confirms the names or skips those office inserts and logs a `-- VERIFICATION-PENDING:` comment.

Two pre-flight risks the executor MUST resolve before writing migrations:
1. **West Hollywood FIPS** — RESEARCH.md Open Question 1 / Pitfall 8 / Assumption A1: the code `0684346` is INFERRED. T1 verifies via Census Geocoder API before any migration uses it.
2. **Gardena post-June-2026 election** — RESEARCH.md Pitfall 9 / Assumption A6: Cerda and Tanaka had seats up June 2, 2026. T1 records the actual outcome.

External_id allocation per RESEARCH.md "Recommended LA County allocation":
- South Gate: -700200..-700204 (5 at-large)
- Compton: -700250..-700254 + optional -700255..-700257 for Clerk/Treasurer/Attorney
- Carson: -700300..-700306 (Mayor + 4 council + Clerk + Treasurer)
- Hawthorne: -700350..-700354 (Mayor + 4 at-large)
- Whittier: -700400..-700404 (Mayor + 4 district)
- Alhambra: -700450..-700454 (5 district)
- Gardena: -700500..-700504 (Mayor + 4 at-large; final roster from T1 post-election check)
- Culver City: -700550..-700554 (5 at-large)
- West Hollywood: -700600..-700604 (5 at-large)
- El Segundo: -700650..-700654 (5 at-large)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-la-county-city-officials/108-CONTEXT.md
@.planning/phases/108-la-county-city-officials/108-RESEARCH.md
@.planning/phases/108-la-county-city-officials/108-PATTERNS.md
@.planning/phases/108-la-county-city-officials/108-VALIDATION.md
@.planning/phases/108-la-county-city-officials/108-01-PLAN.md
@.planning/phases/108-la-county-city-officials/108-02-PLAN.md

# Canonical analogs
@backend/migrations/217_sj_government_structure.sql
@backend/migrations/218_sj_officials.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify West Hollywood FIPS + Gardena post-election + Hawthorne/Compton roster; capture as migration 304</name>
  <files>backend/scripts/verify-west-hollywood-fips.sh, backend/scripts/preflight-la-wave3.sql, backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql</files>
  <read_first>
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Open Questions" 1, 4, 5; "Common Pitfalls" 8, 9; "Tier 4 New Cities — FIPS Codes" table; "Tier 4: 10 New Cities — Current Incumbents")
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Pre-Flight Verification Pattern")
  </read_first>
  <action>
    Step A — Create `backend/scripts/verify-west-hollywood-fips.sh`. The script must invoke the Census Geocoder API and emit ONLY the verified FIPS place code on stdout (or an error to stderr if verification fails). Recommended approach (per RESEARCH.md Pitfall 8 / Open Question 1):
    `curl -s "https://geocoding.geo.census.gov/geocoder/geographies/address?street=8300+Santa+Monica+Blvd&city=West+Hollywood&state=CA&benchmark=Public_AR_Current&vintage=Current_Current&format=json"` and parse the `Incorporated Places` array for the `STATE||PLACE` code. Alternative: download `https://www2.census.gov/geo/docs/reference/codes2020/place/st06_ca_place2020.txt` and grep for `West Hollywood`.
    The script's stdout MUST be exactly the 7-character string in the form `06XXXXX` (no extra whitespace, no other output).
    Run the script: `bash backend/scripts/verify-west-hollywood-fips.sh` and capture the returned code. If the returned code is `0684346` (the inferred value), confirmed; otherwise update the value used throughout T6.

    Step B — Manually verify Gardena post-June-2026 outcome by fetching `https://www.cityofgardena.org/` or equivalent. Record the actual roster (which of Cerda/Tanaka still hold seats) as a string for the comment block in migration 304.

    Step C — Verify Hawthorne 5th at-large seat name (RESEARCH.md Assumption A3) by fetching `https://www.cityofhawthorne.org/government/elected-officials`. Record the 5th member's name.

    Step D — Verify Compton City Clerk / Treasurer / Attorney names by fetching `https://www.comptoncity.org/our-city/elected-officials`. If names cannot be confirmed, the value MUST be recorded as `UNVERIFIED` for the executor to honor D-03's conservative default in T2.

    Step E — Create `backend/scripts/preflight-la-wave3.sql` with a single range-clean check:
    `SELECT COUNT(*) AS used FROM essentials.politicians WHERE external_id BETWEEN -700699 AND -700200;`
    Plus, for each of the 10 new cities, a duplicate-check query:
    `SELECT COUNT(*) FROM essentials.governments WHERE name = 'City of <X>' AND state = 'CA';` — expected 0 for all 10.
    Apply: `psql $DATABASE_URL -f backend/scripts/preflight-la-wave3.sql > /tmp/preflight-wave3-out.txt`.

    Step F — Create `backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql` wrapped in `BEGIN; ... COMMIT;` with comment-only body recording 8 lines: West Hollywood FIPS (with source URL), Hawthorne 5th seat or UNVERIFIED, Gardena post-June-2026 council list, Compton City Clerk or UNVERIFIED, Compton City Treasurer or UNVERIFIED, Compton City Attorney or UNVERIFIED, external_id_range_used count, new-city government collision count.
    The file MUST contain zero non-comment DDL/DML between BEGIN and COMMIT.
  </action>
  <verify>
    <automated>bash backend/scripts/verify-west-hollywood-fips.sh | grep -E '^06[0-9]{5}$' &amp;&amp; psql "$DATABASE_URL" -f backend/scripts/preflight-la-wave3.sql &amp;&amp; grep -c "FIPS verified" backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql</automated>
  </verify>
  <acceptance_criteria>
    - `bash backend/scripts/verify-west-hollywood-fips.sh` emits exactly one line matching `^06[0-9]{5}$` on stdout
    - `backend/scripts/preflight-la-wave3.sql` runs without error and emits `used = 0` for the external_id range check
    - `backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql` starts with `BEGIN;`, ends with `COMMIT;`, and contains all 8 documented comment lines listed in the action
    - `grep -v '^--' backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql | grep -v '^BEGIN' | grep -v '^COMMIT' | grep -v '^$' | wc -l` returns 0
    - West Hollywood FIPS comment includes a real source URL (Census Geocoder API or `https://www2.census.gov/geo/docs/reference/codes2020/place/st06_ca_place2020.txt`)
  </acceptance_criteria>
  <done>
    All five pre-flight unknowns (WeHo FIPS, Gardena roster, Hawthorne 5th, 3 Compton elected officers) are either verified with sources or explicitly marked UNVERIFIED. T2..T6 know exactly what to insert and what to defer.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: South Gate (at-large) + Compton (by-district + Mayor + conditional elected officers)</name>
  <files>backend/migrations/305_la_wave3_south_gate_compton.sql</files>
  <read_first>
    - backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql (read verified Compton elected officer names)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("South Gate", "Compton" sections; "Elected vs Appointed Office Inventory"; Pitfall 6)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Government structure — at-large city pattern", "Government structure — by-district city with separately elected Mayor")
    - backend/migrations/217_sj_government_structure.sql (full — canonical structure migration)
    - backend/migrations/218_sj_officials.sql (canonical politician+office CTE; office_id back-fill at lines 405-415)
  </read_first>
  <action>
    Create `backend/migrations/305_la_wave3_south_gate_compton.sql` wrapped in `BEGIN; ... COMMIT;`, with comment banner separators between the two cities.

    Section 1 — South Gate (geo_id=`0673080`, at-large with rotating Mayor — D-04 → single LOCAL district, no LOCAL_EXEC, no Mayor chamber):
    Steps from 108-PATTERNS.md "Government structure — at-large city pattern" code block, with substitutions: `'City of South Gate'`, `'0673080'`, `'South Gate'`, `'South Gate (At-Large)'`.
    Then insert 5 council members (D-02 South Gate roster): Maria Davila (-700200), Joshua Barron (-700201), Maria del Pilar Avalos (-700202), Gil Hurtado (-700203), Al Rios (-700204). All filter on `d.geo_id='0673080' AND d.district_type='LOCAL' AND d.state='CA'`. Title='Council Member'. Chamber name='City Council', government name='City of South Gate'. party=NULL, is_active=true, is_incumbent=true, is_appointed=false, is_vacant=false, photo_origin_url from cityofsouthgate.org or Wikipedia.

    Section 2 — Compton (geo_id=`0615044`, by-district 4 + separately elected Mayor):
    Steps from 108-PATTERNS.md "Government structure — by-district city with separately elected Mayor" code block, with substitutions for `'City of Compton'`, `'0615044'`, and a Mayor chamber + LOCAL_EXEC district `(geo_id='0615044', district_type='LOCAL_EXEC', label='Compton (Citywide)', state='CA')`.
    Four district rows: `('compton-council-district-1', 'LOCAL', 'District 1', 'CA')` through `'-4'`.
    Then insert 5 politicians:
    - -700250 Mayor Emma Sharif: filter `d.geo_id='0615044' AND d.district_type='LOCAL_EXEC'`, chamber name='Mayor', title='Mayor'.
    - -700251 D1 Deidre Duhart: filter `d.geo_id='compton-council-district-1' AND d.district_type='LOCAL'`, chamber name='City Council', title='Council Member (District 1)'.
    - -700252 D2 Andre Spicer: D2 filter, title='Council Member (District 2)'.
    - -700253 D3 Jonathan Bowers: D3 filter, title='Council Member (District 3)'.
    - -700254 D4 Lillie P. Darden: D4 filter, title='Council Member (District 4)'.
    Photo URLs from comptoncity.org or Wikipedia.

    Section 2b — Compton elected Clerk/Treasurer/Attorney (CONDITIONAL on T1 verification):
    Read migration 304's comment block. For each of the three offices:
    - If T1 captured a verified name: create a chamber for that office (e.g., name='City Clerk', name_formal='Compton City Clerk') with WHERE NOT EXISTS; insert the politician with external_id from -700255..-700257; office filter `d.geo_id='0615044' AND d.district_type='LOCAL_EXEC'`; title matches chamber name; is_appointed_position=false (elected per charter).
    - If T1 marked UNVERIFIED: do NOT create the chamber or office; instead emit a comment `-- VERIFICATION-PENDING: Compton City <Role> elected per charter but incumbent name not confirmed; deferred per D-03.`

    Section 3 — office_id back-fill bounded by `BETWEEN -700257 AND -700200`.

    Apply via psql twice (idempotency).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/305_la_wave3_south_gate_compton.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/305_la_wave3_south_gate_compton.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT (SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of South Gate','City of Compton') AND state='CA') = 2 AND (SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700254 AND -700200) >= 10;" | grep -E '^t$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of South Gate','City of Compton') AND state='CA';` returns 2
    - `SELECT geo_id, district_type, COUNT(*) FROM essentials.districts WHERE geo_id IN ('0673080','0615044','compton-council-district-1','compton-council-district-2','compton-council-district-3','compton-council-district-4') GROUP BY geo_id, district_type ORDER BY geo_id;` returns: South Gate LOCAL=1; Compton LOCAL_EXEC=1; 4 by-district LOCAL rows for Compton
    - `SELECT full_name FROM essentials.politicians WHERE external_id BETWEEN -700204 AND -700200 ORDER BY external_id;` returns exactly: 'Maria Davila', 'Joshua Barron', 'Maria del Pilar Avalos', 'Gil Hurtado', 'Al Rios'
    - `SELECT full_name FROM essentials.politicians WHERE external_id BETWEEN -700254 AND -700250 ORDER BY external_id;` returns: 'Emma Sharif', 'Deidre Duhart', 'Andre Spicer', 'Jonathan Bowers', 'Lillie P. Darden'
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700257 AND -700200 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - Migration body: NO `slug` in chamber INSERT; NO `ON CONFLICT (geo_id, district_type)` on districts
    - If Compton elected officers are UNVERIFIED per T1, the migration contains the literal `-- VERIFICATION-PENDING: Compton City` comment string
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    South Gate (5 at-large council) and Compton (Mayor + 4 by-district council, optionally + 3 elected citywide officers) exist in the DB with full structure and verified incumbents.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Carson (by-district + Mayor + elected Clerk + Treasurer) + Hawthorne (at-large + Mayor)</name>
  <files>backend/migrations/306_la_wave3_carson_hawthorne.sql</files>
  <read_first>
    - backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql (read Hawthorne 5th seat name)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Carson" section, "Hawthorne" section, "Elected vs Appointed Office Inventory" — Carson Clerk and Treasurer are [VERIFIED ELECTED])
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Government structure — by-district city with separately elected Mayor"; "Government structure — at-large city pattern")
    - backend/migrations/305_la_wave3_south_gate_compton.sql (produced by T2 — structural template for Compton-style by-district + Mayor)
  </read_first>
  <action>
    Create `backend/migrations/306_la_wave3_carson_hawthorne.sql` wrapped in `BEGIN; ... COMMIT;` with comment banner separators.

    Section 1 — Carson (geo_id=`0611530`, by-district 4 + separately elected Mayor + ELECTED Clerk + ELECTED Treasurer per RESEARCH.md):
    Government 'City of Carson', LOCAL, CA, 'Carson', '0611530'.
    Chambers: 'City Council', 'Mayor', 'City Clerk', 'City Treasurer' (each with WHERE NOT EXISTS guard, NO `slug` column).
    Districts: 4 LOCAL rows `('carson-council-district-1'..'-4', 'LOCAL', 'District 1'..'4', 'CA')` + 1 LOCAL_EXEC `('0611530', 'LOCAL_EXEC', 'Carson (Citywide)', 'CA')`. NO ON CONFLICT.
    Politicians (per RESEARCH.md "Carson" verified roster):
    - -700300 Mayor Lula Davis-Holmes (LOCAL_EXEC, chamber='Mayor', title='Mayor')
    - -700301 D1 Jawane Hilton (title='Council Member (District 1)')
    - -700302 D2 Jim Dear
    - -700303 D3 Cedric L. Hicks Sr. (use 'Council Member (District 3)' per 218_sj_officials.sql convention; the Mayor Pro Tem role is an internal council title, not a separate office)
    - -700304 D4 Arleen B. Rojas
    - -700305 City Clerk Khaleah K. Bradshaw — chamber name='City Clerk', filter LOCAL_EXEC, title='City Clerk', is_appointed_position=false (elected), is_appointed=false on politician
    - -700306 City Treasurer Monica Cooper — chamber name='City Treasurer', filter LOCAL_EXEC, title='City Treasurer', is_appointed_position=false (elected)
    Photo URLs from carsonca.gov/government/elected_officials/ where available; Wikipedia fallback.

    Section 2 — Hawthorne (geo_id=`0632548`, at-large 4 + separately elected Mayor):
    Government 'City of Hawthorne', chambers 'City Council' + 'Mayor', single LOCAL district `(geo_id='0632548', district_type='LOCAL', label='Hawthorne (At-Large)', state='CA')` + 1 LOCAL_EXEC `('0632548', 'LOCAL_EXEC', 'Hawthorne (Citywide)', 'CA')`.
    Politicians (per RESEARCH.md "Hawthorne"):
    - -700350 Mayor Alex Vargas (LOCAL_EXEC, chamber='Mayor')
    - -700351 Katrina Manning (LOCAL, chamber='City Council', title='Council Member')
    - -700352 Alex Monteiro
    - -700353 Angie Reyes English
    - -700354 the 5th seat name from T1's `Hawthorne 5th seat:` comment block — if T1 marked it UNVERIFIED, skip this insert and add `-- VERIFICATION-PENDING: Hawthorne 5th at-large seat name not confirmed; deferred per D-03.`
    Photos from cityofhawthorne.org or Wikipedia.

    Section 3 — office_id back-fill bounded by `BETWEEN -700354 AND -700300`.

    Apply via psql twice.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/306_la_wave3_carson_hawthorne.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/306_la_wave3_carson_hawthorne.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-700300,-700301,-700302,-700303,-700304,-700305,-700306,-700350,-700351,-700352,-700353);" | grep -E '^11$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of Carson','City of Hawthorne') AND state='CA';` returns 2
    - `SELECT full_name FROM essentials.politicians WHERE external_id IN (-700300,-700305,-700306) ORDER BY external_id;` returns 'Lula Davis-Holmes', 'Khaleah K. Bradshaw', 'Monica Cooper'
    - `SELECT full_name FROM essentials.politicians WHERE external_id = -700350;` returns 'Alex Vargas'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='City of Carson' AND state='CA');` returns 4 (City Council, Mayor, City Clerk, City Treasurer)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id IN (-700305,-700306) AND is_appointed = false;` returns 2 (Carson Clerk and Treasurer are ELECTED, not appointed)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700354 AND -700300 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - If Hawthorne 5th seat is UNVERIFIED, migration contains `-- VERIFICATION-PENDING: Hawthorne 5th at-large seat`
    - Migration body: NO `slug` in chamber INSERT; NO `ON CONFLICT (geo_id, district_type)`
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Carson and Hawthorne have full structure + verified rosters. Carson's elected Clerk and Treasurer are seeded as separately elected positions. Hawthorne's confirmed 4 of 5 are seeded; the 5th deferred if unverified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Whittier (by-district + Mayor) + Alhambra (5 districts, NO Mayor — Pitfall 7)</name>
  <files>backend/migrations/307_la_wave3_whittier_alhambra.sql</files>
  <read_first>
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Whittier" section, "Alhambra" section, Pitfall 7, Assumption A4)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Alhambra — 5 by-district seats, no separately elected Mayor" code block)
    - backend/migrations/306_la_wave3_carson_hawthorne.sql (produced by T3 — Whittier structurally mirrors Carson)
  </read_first>
  <action>
    Create `backend/migrations/307_la_wave3_whittier_alhambra.sql` wrapped in `BEGIN; ... COMMIT;` with comment banner separators.

    Section 1 — Whittier (geo_id=`0685292`, by-district 4 + separately elected Mayor):
    Government 'City of Whittier', chambers 'City Council' + 'Mayor', 4 LOCAL districts `('whittier-council-district-1'..'-4')` + 1 LOCAL_EXEC `(geo_id='0685292', label='Whittier (Citywide)')`.
    Politicians (per RESEARCH.md "Whittier"):
    - -700400 Mayor James Becerra (LOCAL_EXEC, chamber='Mayor', title='Mayor')
    - -700401 D1 Fernando Dutra (Assumption A4 — if not verifiable at execution time, skip and mark VERIFICATION-PENDING)
    - -700402 D2 Vicky Santana
    - -700403 D3 Octavio Cesar Martinez (Assumption A4 — same conditional)
    - -700404 D4 Aida Susana Macedo
    Photos from cityofwhittier.org. If any of D1/D3 cannot be verified at execution time, skip and emit `-- VERIFICATION-PENDING: Whittier District <N>` comment per D-03.

    Section 2 — Alhambra (geo_id=`0600884`, 5 by-district seats, NO separately elected Mayor — Pitfall 7):
    Government 'City of Alhambra'.
    Chambers: 'City Council' ONLY. NO 'Mayor' chamber.
    Districts: 5 LOCAL rows `('alhambra-council-district-1'..'-5', 'LOCAL', 'District 1'..'5', 'CA')`. NO LOCAL_EXEC district.
    Politicians (per RESEARCH.md "Alhambra" — note Noya Wang is "current Mayor by rotation" but rotation is INTERNAL; her formal office is Council Member D4):
    - -700450 D1 Katherine Lee
    - -700451 D2 Ross J. Maza
    - -700452 D3 Jeff Maloney
    - -700453 D4 Noya Wang
    - -700454 D5 Adele Andrade-Stadler
    All 5 inserts use chamber name='City Council', title='Council Member (District N)' matching the SJ convention from migration 218. Photos from alhambraca.gov.

    The Alhambra section body MUST NOT contain a Mayor chamber INSERT or LOCAL_EXEC district INSERT (negative assertion per Pitfall 7). The string `Mayor` MAY appear ONLY in comments like `-- Alhambra Mayor is rotational (Pitfall 7); no Mayor chamber or LOCAL_EXEC district created.`

    Section 3 — office_id back-fill bounded by `BETWEEN -700454 AND -700400`.

    Apply via psql twice.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/307_la_wave3_whittier_alhambra.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/307_la_wave3_whittier_alhambra.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT (SELECT COUNT(*) FROM essentials.chambers WHERE government_id=(SELECT id FROM essentials.governments WHERE name='City of Alhambra' AND state='CA')) = 1 AND (SELECT COUNT(*) FROM essentials.districts WHERE geo_id LIKE 'alhambra-council-district-%') = 5;" | grep -E '^t$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='City of Alhambra' AND state='CA');` returns 1 (only City Council, NO Mayor chamber)
    - `SELECT COUNT(*) FROM essentials.districts WHERE geo_id LIKE 'alhambra-council-district-%';` returns 5
    - `SELECT COUNT(*) FROM essentials.districts WHERE label LIKE 'Alhambra%' AND district_type='LOCAL_EXEC';` returns 0 (negative assertion — Pitfall 7)
    - `SELECT full_name FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700450 ORDER BY external_id;` returns: 'Katherine Lee', 'Ross J. Maza', 'Jeff Maloney', 'Noya Wang', 'Adele Andrade-Stadler'
    - `SELECT full_name FROM essentials.politicians WHERE external_id = -700400;` returns 'James Becerra'
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700454 AND -700400 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - Body of the Alhambra section (between its comment banner and the next banner) contains no `INSERT INTO essentials.chambers` line referencing 'Mayor' (verifiable manually by reading the file)
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Whittier (Mayor + 4 by-district council) and Alhambra (5 by-district council, no Mayor office) exist with verified incumbents. Pitfall 7 enforced.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Gardena (post-election roster) + Culver City (at-large 5)</name>
  <files>backend/migrations/308_la_wave3_gardena_culver_city.sql</files>
  <read_first>
    - backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql (read Gardena post-June-2026 roster comment)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("Gardena" section, "Culver City" section, Pitfall 9)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Government structure — at-large city pattern")
    - backend/migrations/305_la_wave3_south_gate_compton.sql (produced by T2 — Culver City structurally mirrors South Gate)
    - backend/migrations/306_la_wave3_carson_hawthorne.sql (produced by T3 — Gardena structurally mirrors Hawthorne)
  </read_first>
  <action>
    Create `backend/migrations/308_la_wave3_gardena_culver_city.sql` wrapped in `BEGIN; ... COMMIT;` with comment banner separators.

    Section 1 — Gardena (geo_id=`0628168`, at-large 4 + separately elected Mayor):
    Government 'City of Gardena', chambers 'City Council' + 'Mayor', single LOCAL district `(geo_id='0628168', district_type='LOCAL', label='Gardena (At-Large)', state='CA')` + LOCAL_EXEC `('0628168', 'LOCAL_EXEC', 'Gardena (Citywide)', 'CA')`.
    Politicians — read T1's "Gardena post-June-2026 council:" comment to determine the final roster. If Tasha Cerda won re-election, insert her as Mayor (-700500); if she lost, insert the actual winner with that external_id. Same for Mayor Pro Tem (Rodney Tanaka or his replacement) as a Council Member (LOCAL, chamber='City Council'). Allocate -700500 to Mayor, -700501..-700504 to the 4 at-large council members in the post-election order. Title for Mayor='Mayor'; for council='Council Member'.
    Other confirmed members per RESEARCH.md: Paulette C. Francis, Mark E. Henderson (these are not up in June 2026 per RESEARCH.md, so they continue). Wanda Love is only seated if she won the Mayor seat. Use the T1 comment as authoritative.
    Photos from cityofgardena.org or Wikipedia.

    Section 2 — Culver City (geo_id=`0617568`, at-large 5, no separately elected Mayor — D-04 → single LOCAL district):
    Government 'City of Culver City'.
    Chambers: 'City Council' ONLY.
    Districts: single LOCAL `('0617568', 'LOCAL', 'Culver City (At-Large)', 'CA')`.
    Politicians (per RESEARCH.md "Culver City"):
    - -700550 Freddy Puza (Mayor selected by council vote — formal office is 'Council Member')
    - -700551 Bryan "Bubba" Fish (Vice Mayor — formal office is 'Council Member'; store full_name as 'Bryan Fish' with display name kept simple; use the version that matches culvercity.gov listings)
    - -700552 Yasmine-Imani McMorrin
    - -700553 Dan O'Brien
    - -700554 Albert Vera
    All filter `d.geo_id='0617568' AND d.district_type='LOCAL'`. Title='Council Member'. Photos from culvercity.gov.

    The Culver City section MUST NOT contain a Mayor chamber INSERT or LOCAL_EXEC district INSERT (Mayor is rotational per RESEARCH.md "Culver City").

    Section 3 — office_id back-fill bounded by `BETWEEN -700554 AND -700500`.

    Apply via psql twice.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/308_la_wave3_gardena_culver_city.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/308_la_wave3_gardena_culver_city.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT (SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of Gardena','City of Culver City') AND state='CA') = 2 AND (SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700554 AND -700550) = 5;" | grep -E '^t$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - `SELECT full_name FROM essentials.politicians WHERE external_id BETWEEN -700554 AND -700550 ORDER BY external_id;` returns 5 rows starting with 'Freddy Puza'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='City of Culver City' AND state='CA');` returns 1 (City Council only)
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='City of Gardena' AND state='CA');` returns 2 (City Council + Mayor)
    - `SELECT COUNT(*) FROM essentials.districts WHERE geo_id = '0617568' AND district_type = 'LOCAL_EXEC';` returns 0 (Culver City Mayor is rotational — no LOCAL_EXEC)
    - `SELECT COUNT(*) FROM essentials.districts WHERE geo_id = '0628168' AND district_type = 'LOCAL_EXEC';` returns 1 (Gardena has separately elected Mayor)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700554 AND -700500 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - Migration body references the Gardena roster captured in migration 304 (executor MUST honor T1 post-election outcome rather than the pre-election RESEARCH.md names)
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    Gardena has its post-June-2026 5-member governing body (Mayor + 4 council). Culver City has 5 at-large council members in a single LOCAL district. Both cities respect their structure (Gardena LOCAL_EXEC for Mayor; Culver City no LOCAL_EXEC).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: West Hollywood (verified FIPS) + El Segundo (at-large)</name>
  <files>backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql</files>
  <read_first>
    - backend/migrations/304_la_wave3_preflight_west_hollywood_fips.sql (read VERIFIED West Hollywood FIPS code)
    - .planning/phases/108-la-county-city-officials/108-RESEARCH.md ("West Hollywood" section, "El Segundo" section, Pitfall 8)
    - .planning/phases/108-la-county-city-officials/108-PATTERNS.md ("Government structure — at-large city pattern")
    - backend/migrations/308_la_wave3_gardena_culver_city.sql (produced by T5 — both cities structurally mirror Culver City)
  </read_first>
  <action>
    Create `backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql` wrapped in `BEGIN; ... COMMIT;` with comment banner separators.

    The first line after BEGIN must be a comment recording the VERIFIED West Hollywood FIPS code retrieved from migration 304 (e.g., `-- West Hollywood FIPS: 0684346 (verified via Census Geocoder per migration 304_la_wave3_preflight_west_hollywood_fips.sql)`). If the verified code differs from the inferred `0684346`, use the verified code throughout this migration and add a comment recording the substitution.

    Section 1 — West Hollywood (geo_id=`<VERIFIED_FIPS>`, at-large 5, no separately elected Mayor — Mayor rotates):
    Government 'City of West Hollywood'.
    Chambers: 'City Council' ONLY (Pitfall 8 — Mayor is rotational, like Culver City).
    District: single LOCAL `('<VERIFIED_FIPS>', 'LOCAL', 'West Hollywood (At-Large)', 'CA')`.
    Politicians (per RESEARCH.md "West Hollywood"):
    - -700600 John Heilman (Mayor 2026 — formal title 'Council Member')
    - -700601 Danny Hang (Vice Mayor — formal title 'Council Member')
    - -700602 Chelsea Byers
    - -700603 Lauren Meister
    - -700604 John Erickson
    All filter `d.geo_id='<VERIFIED_FIPS>' AND d.district_type='LOCAL'`. Title='Council Member'. Photos from weho.org or Wikipedia.

    Section 2 — El Segundo (geo_id=`0622412`, at-large 5, no separately elected Mayor — Mayor rotates):
    Government 'City of El Segundo'.
    Chambers: 'City Council' ONLY.
    District: single LOCAL `('0622412', 'LOCAL', 'El Segundo (At-Large)', 'CA')`.
    Politicians (per RESEARCH.md "El Segundo"):
    - -700650 Chris Pimentel (Mayor 2026 — formal title 'Council Member')
    - -700651 Ryan Baldino (Mayor Pro Tem — formal title 'Council Member')
    - -700652 Drew Boyles
    - -700653 Lance Giroux
    - -700654 Michelle Keldorf
    All filter `d.geo_id='0622412' AND d.district_type='LOCAL'`. Title='Council Member'. Photos from elsegundo.gov or Wikipedia.

    Both sections MUST NOT contain a Mayor chamber or LOCAL_EXEC district INSERT.

    Section 3 — office_id back-fill bounded by `BETWEEN -700654 AND -700600`.

    Apply via psql twice.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql &amp;&amp; psql "$DATABASE_URL" -f backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql &amp;&amp; psql "$DATABASE_URL" -tA -c "SELECT (SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700654 AND -700600) = 10 AND (SELECT COUNT(*) FROM essentials.governments WHERE name IN ('City of West Hollywood','City of El Segundo') AND state='CA') = 2;" | grep -E '^t$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies twice with exit code 0
    - Migration's first non-BEGIN line is a comment recording the West Hollywood FIPS code with source provenance (verifiable: `head -5 backend/migrations/309_la_wave3_west_hollywood_el_segundo.sql | grep -E 'West Hollywood FIPS:'` matches)
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700604 AND -700600;` returns 5
    - `SELECT full_name FROM essentials.politicians WHERE external_id BETWEEN -700604 AND -700600 ORDER BY external_id;` first row is 'John Heilman'
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700654 AND -700650;` returns 5
    - `SELECT full_name FROM essentials.politicians WHERE external_id = -700650;` returns 'Chris Pimentel'
    - `SELECT COUNT(*) FROM essentials.chambers WHERE government_id IN (SELECT id FROM essentials.governments WHERE name IN ('City of West Hollywood','City of El Segundo') AND state='CA');` returns 2 (one City Council per city, NO Mayor chambers)
    - `SELECT COUNT(*) FROM essentials.districts WHERE district_type = 'LOCAL_EXEC' AND state='CA' AND label LIKE 'West Hollywood%' OR label LIKE 'El Segundo%';` returns 0
    - `SELECT COUNT(*) FROM essentials.politicians WHERE external_id BETWEEN -700654 AND -700600 AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);` returns 0
    - Migration starts with `BEGIN;` and ends with `COMMIT;`
  </acceptance_criteria>
  <done>
    West Hollywood (with verified FIPS) and El Segundo each have 5 at-large council members in a single LOCAL district. No spurious Mayor chambers or LOCAL_EXEC districts created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → essentials schema | Static SQL; no user input |
| Census Geocoder API → verify-west-hollywood-fips.sh | External HTTP fetch used only for FIPS verification, not for migration execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03-01 | Integrity | West Hollywood FIPS wrong (Pitfall 8) | mitigate | T1 verify-west-hollywood-fips.sh runs Census Geocoder API; result captured in migration 304 comments; T6 reads the verified code before writing the WeHo migration. |
| T-108-03-02 | Integrity | Alhambra Mayor erroneously created (Pitfall 7) | mitigate | T4 acceptance criterion: `SELECT COUNT(*) FROM essentials.chambers WHERE government_id = (SELECT id FROM essentials.governments WHERE name='City of Alhambra' AND state='CA') = 1` and zero LOCAL_EXEC districts for Alhambra. |
| T-108-03-03 | Integrity | Gardena seeded with stale pre-election roster (Pitfall 9) | mitigate | T1 captures post-June-2026 outcome; T5 reads T1's comment as the authoritative roster source. |
| T-108-03-04 | Integrity | Compton elected officers seeded with guessed names (Assumption A5) | mitigate | D-03 conservative default codified into T2 action: skip + `-- VERIFICATION-PENDING:` comment when T1 marks UNVERIFIED. |
| T-108-03-05 | Integrity | District row collision (`geo_id` collision between Torrance `0680000` and any other city) | accept | RESEARCH.md "Torrance geo_id collision check" already confirmed no collisions. Re-verified by T1's per-city duplicate check. |
| T-108-03-SC | Tampering | npm/pip/cargo installs | N/A | Pure SQL migrations + one bash script that uses only `curl` (a standard OS dependency). No package installs. |
</threat_model>

<verification>
After all 6 tasks complete, run the Wave 3 phase gate (matches 108-VALIDATION.md Wave 3 expectations):

```sql
-- All 10 new governments exist
SELECT COUNT(*) FROM essentials.governments
WHERE name IN ('City of South Gate','City of Compton','City of Carson','City of Hawthorne',
               'City of Whittier','City of Alhambra','City of Gardena','City of Culver City',
               'City of West Hollywood','City of El Segundo')
  AND state='CA';
-- Expected: 10

-- Total new Wave 3 politicians
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -700699 AND -700200;
-- Expected: ~50 (5 per at-large city × 6 cities + Mayor+4 per by-district city × 4 cities + Carson Clerk/Treasurer + optional Compton 3)

-- Zero missing required fields
SELECT COUNT(*) FROM essentials.politicians
WHERE external_id BETWEEN -700699 AND -700200
  AND (photo_origin_url IS NULL OR office_id IS NULL OR party IS NOT NULL OR is_incumbent IS NOT TRUE);
-- Expected: 0

-- Negative assertion: Alhambra has NO LOCAL_EXEC district and NO Mayor chamber
SELECT (SELECT COUNT(*) FROM essentials.chambers WHERE name='Mayor'
        AND government_id=(SELECT id FROM essentials.governments WHERE name='City of Alhambra' AND state='CA')) = 0
   AND (SELECT COUNT(*) FROM essentials.districts WHERE label LIKE 'Alhambra%' AND district_type='LOCAL_EXEC') = 0;
-- Expected: t

-- Negative assertion: Culver City, West Hollywood, El Segundo, South Gate, Alhambra have NO LOCAL_EXEC
SELECT label FROM essentials.districts
WHERE district_type='LOCAL_EXEC' AND state='CA'
  AND label IN ('Culver City (At-Large)','West Hollywood (At-Large)','El Segundo (At-Large)','South Gate (At-Large)','Alhambra');
-- Expected: 0 rows
```
</verification>

<success_criteria>
1. Migrations 304–309 exist, idempotent, applied.
2. All 10 new city governments + their council chambers exist (Mayor chambers ONLY for the 5 cities with separately elected Mayors: Compton, Carson, Hawthorne, Whittier, Gardena).
3. Every new politician has photo_origin_url, office_id, party=NULL, is_incumbent=true.
4. West Hollywood FIPS code in migration 309 matches the verified value captured in migration 304.
5. Alhambra has exactly 1 chamber (City Council), 5 LOCAL districts, 0 LOCAL_EXEC districts (Pitfall 7).
6. Any seat that could not be verified at execution time is documented with `-- VERIFICATION-PENDING:` rather than guessed (D-03 conservative default).
</success_criteria>

<output>
Create `.planning/phases/108-la-county-city-officials/108-03-SUMMARY.md` when done. Include:
- Final per-city politician count (10 cities)
- Verified West Hollywood FIPS code (and whether it matched the inferred 0684346)
- List of `-- VERIFICATION-PENDING:` deferrals for future follow-up
- Confirmation that Alhambra has no Mayor office/chamber/LOCAL_EXEC
- Confirmation that Gardena roster reflects post-June-2026 outcome
</output>
