---
phase: 29-admin-controls-integration-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SMOKE-TEST-INTEG.md
  - docs/ONBOARDING-VQ.md
autonomous: true

must_haves:
  truths:
    - "A developer can read SMOKE-TEST-INTEG.md and execute the CTC smoke test step-by-step with exact commands and expected outputs"
    - "SMOKE-TEST-INTEG.md has a clear prerequisite callout for CTC that the CTC_SERVICE_KEY Render env var must be set before INTEG-01 steps will work"
    - "SMOKE-TEST-INTEG.md includes a full VQ smoke test flow: prerequisites, exact sample payload, verification checklist, and idempotency replay instructions"
    - "A VQ developer can read ONBOARDING-VQ.md and implement POST /api/vq/confirm-stance without asking a follow-up question"
    - "ONBOARDING-VQ.md confirm-stance section leads with auth + a complete working example, then a field reference table, side effects with exact numbers, error responses with example bodies, and idempotency behavior"
    - "ONBOARDING-VQ.md documents all side effects: correct users get +gems_amount Red Gems and verification_rating += 3 (cap 150); incorrect users get verification_rating -= 10 (floor 0, sets hold at 0); idempotency replay returns original result with replayed: true"
  artifacts:
    - path: "docs/SMOKE-TEST-INTEG.md"
      provides: "Manual integration smoke test runbook for CTC and VQ"
      contains: "INTEG-01"
    - path: "docs/ONBOARDING-VQ.md"
      provides: "Updated VQ onboarding doc with confirm-stance section"
      contains: "confirm-stance"
  key_links:
    - from: "docs/SMOKE-TEST-INTEG.md"
      to: "POST /api/vq/confirm-stance"
      via: "curl example in INTEG-02 steps"
      pattern: "confirm-stance"
    - from: "docs/ONBOARDING-VQ.md"
      to: "POST /api/vq/confirm-stance"
      via: "new section documenting the endpoint"
      pattern: "confirm-stance"
---

<objective>
Author `docs/SMOKE-TEST-INTEG.md` (CTC + VQ live integration smoke test runbook) and extend `docs/ONBOARDING-VQ.md` with a complete `POST /api/vq/confirm-stance` section.

Purpose: Satisfies INTEG-01, INTEG-02, and INTEG-03. The runbook gives Chris a step-by-step checklist to verify integrations are live once keys are configured. The onboarding doc update gives VQ developers everything they need to implement the stance confirmation call.
Output: Two documentation files — one new (`SMOKE-TEST-INTEG.md`), one extended (`ONBOARDING-VQ.md`).
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/ONBOARDING-VQ.md
@docs/RUNBOOK-TIGER-LOAD.md
@docs/ONBOARDING-CTC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/SMOKE-TEST-INTEG.md</name>
  <files>
    docs/SMOKE-TEST-INTEG.md
  </files>
  <action>
Create `docs/SMOKE-TEST-INTEG.md` as a manual checklist runbook. Follow the established runbook format from `RUNBOOK-TIGER-LOAD.md`: Overview, Prerequisites, numbered Step sections with exact commands and expected outputs.

**Structure:**

```markdown
# Integration Smoke Test Runbook

## Overview

Manual checklist for verifying that CTC and VQ service integrations are live end-to-end against the production Accounts API. Run this after service keys are configured in Render environments.

- **INTEG-01**: CTC game event → XP award + Yellow Gem record
- **INTEG-02**: VQ stance confirmation → Red Gem + Verification Rating change

**Audience:** Chris (ops) — these steps require access to admin tool and Render dashboards.

---

## INTEG-01: CTC Integration

### Prerequisites

- [ ] `CTC_SERVICE_KEY` set in CTC's Render environment (key registered in accounts API `XP_SERVICE_KEYS` and `GEMS_SERVICE_KEYS` env vars with `yellow` gem type)
- [ ] A test Connected user account exists (note their UUID and display name)
- [ ] Access to the admin tool at `https://accounts.empowered.vote/admin`
- [ ] Production accounts API URL: `https://ev-accounts-api.onrender.com`

> **Blocker note:** Until `CTC_SERVICE_KEY` is configured in both Render environments, Step 2 will return 401. See STATE.md open blockers.

### Step 1: Verify XP endpoint accepts the key

Direct call to confirm the service key is valid before triggering a real game event:

[curl example: POST /api/xp/award with CTC service key, valid userId, source: 'ctc_game_completion', amount: 100, idempotencyKey: 'smoke-test-ctc-xp-<date>-001', expected: 200 with { total_xp, level, is_duplicate: false }]

### Step 2: Trigger a real CTC game event

Play a full CTC game as the test user (or use CTC's test harness if available). This should produce both XP and Yellow Gems via the service key calls.

### Step 3: Verify in admin tool

- Open the test user's detail page in the admin tool
- [ ] XP History section shows a new `ctc_game_completion` entry with the expected amount
- [ ] Gem balance Yellow has increased

### Verification Checklist

- [ ] Step 1 returns 200 with `is_duplicate: false`
- [ ] XP transaction visible in admin ledger
- [ ] Yellow gem balance increased
- [ ] No errors in Render logs for accounts API during the test window
```

For INTEG-02, include:

**Prerequisites section:**
- `GEMS_SERVICE_KEYS` contains a key with `red` gem type permission
- VQ service key set in VQ's Render environment
- A test Connected user account (UUID noted) with `verification_rating` between 10 and 147 (so deltas won't hit floor/cap), no active `vq_hold_until`
- A valid `politician_id` and `topic_id` pair in the production DB (from admin compass tool)
- Admin tool access to verify results

**Steps:**
1. Verify preconditions (check user's current VR and hold state in admin tool)
2. Send a test stance confirmation with a fresh datestamped idempotency key — exact curl command with real-looking test UUIDs as sample, body includes `politician_id`, `topic_id`, `confirmed_value: 3`, `correct_user_ids: [testUserId]`, `incorrect_user_ids: []`, `idempotency_key: "smoke-test-vq-<YYYY-MM-DD>-001"`, `gems_amount: 1`
3. Verify response shape — expect 200 with `correct_count: 1`, `incorrect_count: 0`, `users: [{ result: "correct", gems_awarded: 1, rating_delta: 3 }]`, `replayed: false`
4. Verify in admin tool — Red gem balance increased by 1, VR increased by 3 (check Admin-Only Fields > Verification Rating)
5. **Idempotency replay** — send the exact same request again (same `idempotency_key`); expect same response body with `replayed: true`; verify in admin tool that VR and gem balance are UNCHANGED from Step 4 (no second increment)

**Verification checklist:**
- [ ] Response 200, correct shape
- [ ] Red gem balance +1 in admin tool
- [ ] Verification Rating +3 in admin tool
- [ ] Replay returns same body with `replayed: true`
- [ ] Replay does NOT change gem balance or VR (admin tool confirms no change)

Include at the end a note about error troubleshooting: "If Step 2 returns 401: check that the service key in VQ's Render env matches the value in accounts API's `GEMS_SERVICE_KEYS`. Keys are compared as exact strings."
  </action>
  <verify>
File exists at `docs/SMOKE-TEST-INTEG.md`. Confirm it contains both `INTEG-01` and `INTEG-02` sections, a Prerequisites section for each with a CTC blocker note in INTEG-01, a curl command with a sample payload in INTEG-02, and a verification checklist with checkboxes for each section.
  </verify>
  <done>
`docs/SMOKE-TEST-INTEG.md` exists with full CTC and VQ runbook sections, blocker callout, sample payload, idempotency replay test, and verification checklists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend docs/ONBOARDING-VQ.md with confirm-stance section</name>
  <files>
    docs/ONBOARDING-VQ.md
  </files>
  <action>
Add a new section to `docs/ONBOARDING-VQ.md` for the `POST /api/vq/confirm-stance` endpoint. Insert it after the "XP Awards" section and before "Reading User State". Update the header datestamp to `2026-03-16` and the "What Accounts Provides to VQ" table to include the new row.

**New table row to add:**

| Stance confirmation | Accounts | `POST /api/vq/confirm-stance` with service key |

**Update the header:**
Change `Last updated: 2026-03-15 (v1.3 deployed)` → `Last updated: 2026-03-16 (v1.4 — confirm-stance added)`

**New section content (write fully, "Stripe API docs for one endpoint" quality):**

```markdown
## Stance Confirmation

When VQ resolves a question (determines the correct answer), call this endpoint to record the confirmed stance, award Red Gems to correct answerers, and adjust Verification Ratings for all participants.

### Endpoint

POST /api/vq/confirm-stance
Authorization: Bearer <VQ_SERVICE_KEY>
Content-Type: application/json

### Authentication

Same Bearer token + service key pattern as XP awards. Use the `VQ_SERVICE_KEY` value Chris provides (same key used for gem awards — it must have `red` gem type permission in the accounts API).

### Complete Example

[Full TypeScript async function showing: POST to /api/vq/confirm-stance with all required fields, check res.ok, return typed result]

### Request Body

| Field | Type | Required | Constraints | Notes |
|-------|------|----------|-------------|-------|
| politician_id | string (UUID) | Yes | Valid UUID | The politician this question is about |
| topic_id | string (UUID) | Yes | Valid UUID | The compass topic (question) |
| confirmed_value | number | Yes | 1–5 integer | The correct answer value |
| correct_user_ids | string[] | No | UUID array | Users who answered correctly; defaults to [] |
| incorrect_user_ids | string[] | No | UUID array | Users who answered incorrectly; defaults to [] |
| idempotency_key | string | Yes | max 255 chars | Unique per resolution event — derive from your internal resolution ID |
| gems_amount | number | No | Positive integer, default 1 | Red Gems awarded to each correct user |

**idempotency_key guidance:** Use a stable identifier derived from the resolution event, not per-user. Example: `vq-resolution-${resolutionId}`. All users in that resolution share the same key.

### Response (200)

[JSON response shape with all fields: politician_id, topic_id, confirmed_value, correct_count, incorrect_count, users array with per-user { user_id, result, gems_awarded, rating_delta, new_rating }, unresolved_users array, replayed boolean]

`replayed: true` means the idempotency_key was already used — the original result is returned unchanged. No additional gems are awarded or ratings changed.

### Side Effects

**For each user in correct_user_ids:**
- Awarded `gems_amount` Red Gems (default: 1)
- `verification_rating` increases by 3, capped at 150
  - Example: rating 148 → rating 150 (not 151)

**For each user in incorrect_user_ids:**
- `verification_rating` decreases by 10, floored at 0
  - Example: rating 8 → rating 0 (not −2)
- If the floor is hit (rating reaches exactly 0), `vq_hold_until` is set to **now + 30 days**
  - The user's `vq_hold_active` field on `/api/account/me` becomes `true`
  - Example (edge case): user with rating 5, marked incorrect → rating = 0, hold set for 30 days

**For the question itself:**
- `inform.politician_answers` is upserted with `confirmed_value` as the authoritative stance record

**On idempotent replay** (same `idempotency_key`):
- No gems awarded, no rating changes, no DB writes
- Original result returned with `replayed: true`

### Edge Case Examples

| Scenario | Before | Result |
|----------|--------|--------|
| Correct user near cap | rating 148 | rating 150 (capped, not 151) |
| Incorrect user near floor | rating 8 | rating 0, hold set 30 days |
| Incorrect user at floor | rating 0 | rating 0, hold extended (hold reset to now+30) |
| User in both arrays | — | Deduped to correct (per-user, first occurrence wins) |

### Error Responses

| Status | Body | Cause |
|--------|------|-------|
| 401 | `{ "error": "UNAUTHORIZED" }` | Missing or invalid service key |
| 422 | `{ "error": "VALIDATION_ERROR", "issues": [...] }` | Zod validation failure — check required fields and types |
| 422 | `{ "error": "FORBIDDEN_GEM_TYPE", "permitted": ["yellow"] }` | Service key lacks `red` gem type permission |
| 422 | `{ "error": "INVALID_VALUE" }` | `confirmed_value` outside 1–5 |
| 404 | `{ "error": "QUESTION_NOT_FOUND" }` | politician/topic pair not in `inform.politician_answers` |

**Idempotency conflict:** If you send the same `idempotency_key` with a different payload, the original result for that key is returned (no conflict error). VQ should never reuse keys across different resolution events.

Additional errors return standard HTTP codes with `{ error: string }` body.

### Environment Variables

Add to your existing VQ env vars:

| Variable | Purpose | Value Source |
|----------|---------|--------------|
| VQ_SERVICE_KEY | Stance confirmation auth (also used for gems) | Chris provides; must match accounts API env |
```

Also update the "What VQ Does NOT Own" section — remove "Gem awards (VQ does not currently award gems...)" since VQ now awards Red Gems through this endpoint. Replace with: "Direct gem balance writes — always go through `POST /api/vq/confirm-stance` for Red Gem awards; do not call gem endpoints directly."
  </action>
  <verify>
File at `docs/ONBOARDING-VQ.md` contains a "Stance Confirmation" section (or "VQ Result Submission"). Confirm:
- Header datestamp updated to 2026-03-16
- Section includes: endpoint, auth note, complete TypeScript example, request body field reference table, side effects with exact numbers (±3, ±10, cap 150, floor 0, hold 30 days), edge case table, error responses with example bodies, idempotency behavior
- "What VQ Does NOT Own" updated to reflect Red Gems through confirm-stance
  </verify>
  <done>
`docs/ONBOARDING-VQ.md` has a complete "Stance Confirmation" section. A VQ developer reading it can implement the endpoint integration without asking follow-up questions. Datestamp updated to 2026-03-16.
  </done>
</task>

</tasks>

<verification>
1. `docs/SMOKE-TEST-INTEG.md` exists and has INTEG-01 and INTEG-02 sections with prerequisites, exact commands, expected outputs, verification checklists, and idempotency replay test in INTEG-02
2. `docs/ONBOARDING-VQ.md` has a new confirm-stance section with field reference table, side effects with exact numbers, error response table with example bodies, and idempotency behavior
3. ONBOARDING-VQ.md header datestamp updated to 2026-03-16
</verification>

<success_criteria>
- SMOKE-TEST-INTEG.md: CTC section has a prerequisite blocker callout; VQ section has exact curl sample payload, step-by-step verification, and idempotency replay test with expected DB no-change verification
- ONBOARDING-VQ.md confirm-stance section: leads with auth + working example; field reference table covers all 7 request fields with constraints and notes; side effects document exact +3/-10/cap 150/floor 0/30-day hold numbers; error table includes 5 specific error cases with example bodies; idempotency behavior explained
- Both documents are immediately actionable by their intended audiences (ops for runbook, VQ devs for onboarding doc)
</success_criteria>

<output>
After completion, create `.planning/phases/29-admin-controls-integration-verification/29-02-SUMMARY.md` using the summary template at `@C:\Users\Chris\.claude/get-shit-done/templates/summary.md`.
</output>
