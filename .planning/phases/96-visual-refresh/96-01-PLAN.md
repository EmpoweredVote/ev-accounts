---
phase: 96-visual-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - treasury-tracker/package.json
  - treasury-tracker/vite.config.ts
  - treasury-tracker/src/index.css
  - treasury-tracker/src/App.css
  - treasury-tracker/src/App.tsx
  - treasury-tracker/src/components/EntitySwitcher.tsx
  - treasury-tracker/src/components/EntitySwitcher.css
  - treasury-tracker/src/components/datasets/DatasetTabs.tsx
  - treasury-tracker/src/components/SearchBar.tsx
  - treasury-tracker/src/components/YearSelector.tsx
  - treasury-tracker/src/components/Breadcrumb.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-05, VIS-03]

must_haves:
  truths:
    - "Treasury Tracker builds successfully with Tailwind CSS 4 and ev-ui 0.1.53+"
    - "All text renders in Manrope font via font-manrope class"
    - "Header, EntitySwitcher, DatasetTabs, SearchBar, YearSelector, and Breadcrumb use EV design tokens — no old CSS var references remain"
    - "No companion CSS files remain for EntitySwitcher"
    - "App.css is deleted; all its styles are converted to Tailwind utility classes"
  artifacts:
    - path: "treasury-tracker/src/index.css"
      provides: "Tailwind CSS 4 entry point with @theme block containing full data viz palette"
      contains: "@import \"tailwindcss\""
    - path: "treasury-tracker/vite.config.ts"
      provides: "Vite config with @tailwindcss/vite plugin"
      contains: "@tailwindcss/vite"
    - path: "treasury-tracker/src/App.tsx"
      provides: "Root component with Tailwind classes replacing App.css imports"
  key_links:
    - from: "treasury-tracker/vite.config.ts"
      to: "tailwindcss"
      via: "@tailwindcss/vite plugin"
      pattern: "import tailwindcss from '@tailwindcss/vite'"
    - from: "treasury-tracker/src/index.css"
      to: "ev-ui tokens"
      via: "@theme block with EV brand + data viz colors"
      pattern: "--color-ev-coral"
---

<objective>
Install Tailwind CSS 4 infrastructure, upgrade ev-ui, rewrite index.css with @theme token block, delete App.css, and convert the navigation/chrome shell components (header, EntitySwitcher, DatasetTabs, SearchBar, YearSelector, Breadcrumb) from plain CSS to Tailwind utility classes with full EV design system redesign.

Purpose: Establishes the Tailwind CSS 4 foundation that all subsequent component conversions depend on, and converts the top-level navigation chrome so the app shell is fully branded.
Output: Working build with Tailwind CSS 4, ev-ui 0.1.53+, Manrope typography, and redesigned navigation chrome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-visual-refresh/96-CONTEXT.md
@.planning/phases/96-visual-refresh/96-RESEARCH.md
@.planning/phases/96-visual-refresh/96-UI-SPEC.md

@treasury-tracker/package.json
@treasury-tracker/vite.config.ts
@treasury-tracker/src/index.css
@treasury-tracker/src/App.css
@treasury-tracker/src/App.tsx
@treasury-tracker/src/components/EntitySwitcher.tsx
@treasury-tracker/src/components/EntitySwitcher.css
@treasury-tracker/src/components/datasets/DatasetTabs.tsx
@treasury-tracker/src/components/SearchBar.tsx
@treasury-tracker/src/components/YearSelector.tsx
@treasury-tracker/src/components/Breadcrumb.tsx

@ev-ui/src/tokens.js
@ev-ui/src/tailwind-preset.js
@essentials/src/index.css
@CompassV2/vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind CSS 4, upgrade ev-ui, rewrite index.css and vite.config.ts</name>
  <files>
    treasury-tracker/package.json,
    treasury-tracker/vite.config.ts,
    treasury-tracker/src/index.css
  </files>
  <read_first>
    treasury-tracker/package.json,
    treasury-tracker/vite.config.ts,
    treasury-tracker/src/index.css,
    essentials/src/index.css,
    CompassV2/vite.config.js,
    ev-ui/src/tokens.js,
    ev-ui/src/tailwind-preset.js,
    .planning/phases/96-visual-refresh/96-UI-SPEC.md,
    .planning/phases/96-visual-refresh/96-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Install packages (per D-01, D-03, D-11)**
    ```bash
    cd treasury-tracker
    npm install --save-dev tailwindcss @tailwindcss/vite
    npm install @chrisandrewsedu/ev-ui@^0.1.53
    ```

    **Step 2: Update vite.config.ts (per D-03, matching CompassV2 pattern)**
    Add @tailwindcss/vite plugin. Final file:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      base: '/',
    })
    ```

    **Step 3: Rewrite index.css (per D-03, D-09)**
    Replace the entire file. Structure:
    1. `@import "tailwindcss";`
    2. Attempt `@import "@chrisandrewsedu/ev-ui/tailwind-preset";` — if this fails at build time (Pitfall 1 from RESEARCH.md), remove the import and define all EV brand tokens inline in `@theme {}` block instead (matching essentials/CompassV2 actual pattern).
    3. `@import url('https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap');`
    4. `@theme {}` block containing:
       - EV brand colors from tokens.js: `--color-ev-coral: #FF5740`, `--color-ev-muted-blue: #00657C`, `--color-ev-light-blue: #59B0C4`, `--color-ev-yellow: #FED12E`, `--color-ev-yellow-light: #FEF3C7`, `--color-ev-yellow-dark: #D0A301`
       - EV color scales from tokens.js colorScales (ev-teal-050 through ev-teal-950, ev-coral-050 through ev-coral-950, etc.)
       - Font: `--font-manrope: 'Manrope', sans-serif;`
       - Full data viz palette (all 10 hues x 5 shades) exactly as specified in 96-UI-SPEC.md lines 45-95 with `--color-data-*` namespace
    5. Global reset:
       ```css
       *, *::before, *::after { box-sizing: border-box; }
       body {
         @apply font-manrope antialiased;
         background-color: #F7F7F8;
         color: #1C1C1C;
         line-height: 1.5;
         min-height: 100vh;
       }
       #root { min-height: 100vh; }
       ```

    Do NOT create a `tailwind.config.js` or `tailwind.config.ts` — Tailwind CSS 4 is CSS-first (D-03).
    Do NOT create a `postcss.config.js` — using @tailwindcss/vite plugin instead.

    **Step 4: Verify build**
    ```bash
    cd treasury-tracker && npm run build
    ```
    Fix any build errors. Common issue: if the ev-ui tailwind-preset CSS @import fails, define all tokens inline in @theme (see Pitfall 1 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/treasury-tracker && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - treasury-tracker/package.json contains `"tailwindcss"` in devDependencies
    - treasury-tracker/package.json contains `"@tailwindcss/vite"` in devDependencies
    - treasury-tracker/package.json contains `"@chrisandrewsedu/ev-ui": "^0.1.53"` (or higher) in dependencies
    - treasury-tracker/vite.config.ts contains `import tailwindcss from '@tailwindcss/vite'`
    - treasury-tracker/vite.config.ts contains `tailwindcss()` in plugins array
    - treasury-tracker/src/index.css contains `@import "tailwindcss"`
    - treasury-tracker/src/index.css contains `--color-ev-coral: #FF5740`
    - treasury-tracker/src/index.css contains `--color-ev-muted-blue: #00657C`
    - treasury-tracker/src/index.css contains `--font-manrope`
    - treasury-tracker/src/index.css contains `--color-data-teal-500: #00657C`
    - treasury-tracker/src/index.css contains `--color-data-sage-500: #5A9A6E`
    - treasury-tracker/src/index.css contains `--color-data-stone-700: #41454E`
    - treasury-tracker/src/index.css does NOT contain `:root {`
    - treasury-tracker/src/index.css does NOT contain `--data-navy-`
    - No `tailwind.config.js` or `tailwind.config.ts` file exists in treasury-tracker/
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Tailwind CSS 4 installed with @tailwindcss/vite plugin, ev-ui upgraded to 0.1.53+, index.css rewritten with @theme block containing all EV brand tokens and full 10-hue data viz palette, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete App.css and convert App.tsx + navigation chrome components to Tailwind</name>
  <files>
    treasury-tracker/src/App.css,
    treasury-tracker/src/App.tsx,
    treasury-tracker/src/components/EntitySwitcher.tsx,
    treasury-tracker/src/components/EntitySwitcher.css,
    treasury-tracker/src/components/datasets/DatasetTabs.tsx,
    treasury-tracker/src/components/SearchBar.tsx,
    treasury-tracker/src/components/YearSelector.tsx,
    treasury-tracker/src/components/Breadcrumb.tsx
  </files>
  <read_first>
    treasury-tracker/src/App.css,
    treasury-tracker/src/App.tsx,
    treasury-tracker/src/components/EntitySwitcher.tsx,
    treasury-tracker/src/components/EntitySwitcher.css,
    treasury-tracker/src/components/datasets/DatasetTabs.tsx,
    treasury-tracker/src/components/SearchBar.tsx,
    treasury-tracker/src/components/YearSelector.tsx,
    treasury-tracker/src/components/Breadcrumb.tsx,
    .planning/phases/96-visual-refresh/96-UI-SPEC.md,
    .planning/phases/96-visual-refresh/96-CONTEXT.md,
    ev-ui/design-system/index.html
  </read_first>
  <action>
    This is a FULL REDESIGN of each component (per D-04, D-05, D-06), not token swaps. Use the `/frontend-design` skill. Read 96-UI-SPEC.md for exact class specifications. Read ev-ui/design-system/index.html for the authoritative design reference.

    **Step 1: Delete CSS files**
    Delete these files:
    - `treasury-tracker/src/App.css`
    - `treasury-tracker/src/components/EntitySwitcher.css`

    **Step 2: Update App.tsx**
    - Remove `import './App.css'` (line 16)
    - Remove `import './components/EntitySwitcher.css'` (line 6)
    - Verify `SiteHeader` import from ev-ui still works with 0.1.53 — if SiteHeader is rendered in JSX, keep it. If the import is unused or SiteHeader is no longer exported, remove the import (Pitfall 2 from RESEARCH.md).
    - Replace all CSS class references with Tailwind utility classes per 96-UI-SPEC.md:
      - App container: `min-h-screen bg-[#F7F7F8] font-manrope`
      - Header: `bg-white shadow-sm` with `max-w-[1400px] mx-auto px-6 py-4` inner container
      - Content area: `max-w-[1400px] mx-auto px-6 py-8`
      - Error state card: `bg-white border border-[#E2EBEF] rounded-xl p-8 text-center`
      - Loading state: spinner with `sr-only` text "Loading budget data..."
      - Back button: `flex items-center gap-2 text-ev-muted-blue hover:underline text-sm font-medium`

    **Step 3: EntitySwitcher.tsx full redesign (per 96-UI-SPEC.md Interaction Contracts)**
    - Button: `flex items-center gap-2 min-h-[44px] px-4 py-2 bg-white border border-[#E2EBEF] rounded-lg font-manrope text-base font-medium text-[#1C1C1C] cursor-pointer transition-colors duration-200 hover:bg-[#F7F7F8] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ev-muted-blue focus-visible:ring-offset-2`
    - Dropdown container: `absolute top-full mt-1 left-0 min-w-56 bg-white border border-[#E2EBEF] rounded-lg shadow-lg z-10 overflow-hidden`
    - Group label: `block px-4 py-2 text-xs font-bold uppercase tracking-wider text-[#6B7280]`
    - Option (default): `block w-full px-4 py-3 text-sm text-left border-l-2 border-transparent hover:bg-[#F7F7F8]`
    - Option (selected): `block w-full px-4 py-3 text-sm text-left border-l-2 border-ev-muted-blue bg-[#F7F7F8]`

    **Step 4: DatasetTabs.tsx full redesign**
    Desktop tabs:
    - Container: `flex gap-0 border-b border-[#E2EBEF]`
    - Tab item: `px-4 py-3 text-sm font-medium text-[#6B7280] border-b-2 border-transparent transition-colors duration-200 hover:text-[#374151] cursor-pointer`
    - Tab active: `text-ev-muted-blue border-b-ev-muted-blue font-bold`
    Mobile dropdown:
    - Card-style container: `rounded-xl border border-[#E2EBEF] bg-white`

    **Step 5: SearchBar.tsx full redesign**
    - Input wrapper: `relative flex items-center`
    - Input: `w-full pl-10 pr-10 py-2 bg-white border border-[#E2EBEF] rounded-lg text-sm font-manrope focus:outline-none focus:ring-2 focus:ring-ev-muted-blue focus:ring-offset-2`
    - Search icon (lucide): `absolute left-3 text-ev-muted-blue` (16px size)
    - Clear button: icon-only lucide X icon, `absolute right-3 text-ev-coral hover:text-ev-coral/80 cursor-pointer`, with `aria-label="Clear search"` (per 96-UI-SPEC.md accessibility requirement)

    **Step 6: YearSelector.tsx full redesign**
    - Container: `flex flex-wrap gap-2`
    - Year button (inactive): `px-3 py-1.5 text-sm font-medium text-[#6B7280] bg-white border border-[#E2EBEF] rounded-full cursor-pointer transition-colors duration-200 hover:bg-[#F7F7F8]`
    - Year button (active): `px-3 py-1.5 text-sm font-bold text-white bg-ev-muted-blue border border-ev-muted-blue rounded-full cursor-pointer`

    **Step 7: Breadcrumb.tsx full redesign**
    - Container: `flex items-center gap-1 text-sm`
    - Separator: `text-[#6B7280]` (use "/" or chevron)
    - Clickable crumb: `text-ev-muted-blue hover:underline cursor-pointer font-medium`
    - Current crumb (last): `text-[#1C1C1C] font-medium`

    **Step 8: Build verification**
    ```bash
    cd treasury-tracker && npm run build
    ```
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/treasury-tracker && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - treasury-tracker/src/App.css does NOT exist (deleted)
    - treasury-tracker/src/components/EntitySwitcher.css does NOT exist (deleted)
    - treasury-tracker/src/App.tsx does NOT contain `import './App.css'`
    - treasury-tracker/src/App.tsx does NOT contain `import './components/EntitySwitcher.css'`
    - treasury-tracker/src/components/EntitySwitcher.tsx contains `ring-ev-muted-blue`
    - treasury-tracker/src/components/EntitySwitcher.tsx contains `border-ev-muted-blue` (selected state)
    - treasury-tracker/src/components/EntitySwitcher.tsx contains `tracking-wider` (group label)
    - treasury-tracker/src/components/datasets/DatasetTabs.tsx contains `border-b-ev-muted-blue` (active tab)
    - treasury-tracker/src/components/SearchBar.tsx contains `aria-label="Clear search"`
    - treasury-tracker/src/components/SearchBar.tsx contains `text-ev-coral` (clear button)
    - treasury-tracker/src/components/YearSelector.tsx contains `bg-ev-muted-blue` (active year)
    - treasury-tracker/src/components/YearSelector.tsx contains `rounded-full` (pill style)
    - treasury-tracker/src/components/Breadcrumb.tsx contains `text-ev-muted-blue` (clickable crumb)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>App.css and EntitySwitcher.css deleted. App.tsx, EntitySwitcher, DatasetTabs, SearchBar, YearSelector, and Breadcrumb fully redesigned with Tailwind utility classes and EV design tokens. Build passes.</done>
</task>

</tasks>

<verification>
- `cd treasury-tracker && npm run build` exits 0
- `grep -r "import.*App\.css" treasury-tracker/src/` returns no results
- `grep -r "import.*EntitySwitcher\.css" treasury-tracker/src/` returns no results
- `grep -r "var(--coral)\|var(--muted-blue)\|var(--light-gray)\|var(--medium-gray)" treasury-tracker/src/App.tsx treasury-tracker/src/components/EntitySwitcher.tsx treasury-tracker/src/components/datasets/DatasetTabs.tsx treasury-tracker/src/components/SearchBar.tsx treasury-tracker/src/components/YearSelector.tsx treasury-tracker/src/components/Breadcrumb.tsx` returns no results
</verification>

<success_criteria>
Tailwind CSS 4 fully operational with ev-ui 0.1.53+. Navigation chrome (header, entity switcher, tabs, search, year selector, breadcrumb) redesigned with EV tokens. App.css and EntitySwitcher.css deleted. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/96-visual-refresh/96-01-SUMMARY.md`
</output>
