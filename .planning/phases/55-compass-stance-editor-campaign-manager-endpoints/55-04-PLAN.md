---
phase: 55-compass-stance-editor-campaign-manager-endpoints
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/compassContributor.test.ts
  - tests/integration/requireRole.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "getMatchingGrant returns the correct grant for a compass_stance_editor whose jurisdiction matches the politician"
    - "getMatchingGrant returns null for a compass_stance_editor whose jurisdiction does NOT match the politician"
    - "getMatchingGrant returns the correct grant for a campaign_manager whose resource_id matches the politician"
    - "getMatchingGrant returns null for a campaign_manager whose resource_id does NOT match the politician"
    - "getMatchingGrant fails open (returns grant) when politician has null home_jurisdiction_geoid"
    - "grant() helper in requireRole.test.ts includes id field matching UserRoleGrant interface"
  artifacts:
    - path: "tests/integration/compassContributor.test.ts"
      provides: "Unit tests for getMatchingGrant covering jurisdiction enforcement and campaign_manager resource gating"
      min_lines: 80
    - path: "tests/integration/requireRole.test.ts"
      provides: "Fixed grant() helper with id: string field"
      contains: "id:"
  key_links:
    - from: "tests/integration/compassContributor.test.ts"
      to: "backend/src/lib/stanceService.ts"
      via: "import { getMatchingGrant }"
      pattern: "import.*getMatchingGrant.*stanceService"
---

<objective>
Close two gaps from Phase 55 verification: (1) create integration tests for getMatchingGrant covering the two-jurisdiction scenario required by success criterion 5, and (2) fix the grant() helper in requireRole.test.ts to include the id field added to UserRoleGrant in Phase 55-01.

Purpose: Phase 55 shipped jurisdiction enforcement and campaign_manager resource gating but has zero automated test coverage on getMatchingGrant — the pure function that decides whether a contributor can edit a politician. This plan adds that coverage.

Output: Two test files pass with `npx vitest run tests/integration/compassContributor.test.ts tests/integration/requireRole.test.ts`
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/lib/stanceService.ts
@backend/src/lib/roleService.ts
@tests/integration/requireRole.test.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compassContributor.test.ts with getMatchingGrant unit tests</name>
  <files>tests/integration/compassContributor.test.ts</files>
  <action>
Create `tests/integration/compassContributor.test.ts` following the same pattern as `tests/integration/requireRole.test.ts`:

1. Set process.env vars at top (same 6 vars as requireRole.test.ts) BEFORE any imports.
2. Import `describe, it, expect, beforeAll` from vitest.
3. Import `UserRoleGrant` type from `../../backend/src/lib/roleService.js`.
4. Create a `grant()` helper that returns a full `UserRoleGrant` object INCLUDING `id: string` field. Accept params: `(slug, geoid?, resourceId?, id?)` with defaults `id = 'grant-1'`.
5. Dynamic import `getMatchingGrant` from `../../backend/src/lib/stanceService.js` in `beforeAll` (same pattern as requireRole.test.ts — avoids module hoisting).

Write these test cases in a `describe('getMatchingGrant')` block:

**compass_stance_editor jurisdiction tests:**
- `returns grant when editor jurisdiction matches politician jurisdiction` — grant with geoid '18105', politician geoid '18105' => returns the grant
- `returns null when editor jurisdiction does NOT match politician jurisdiction` — grant with geoid '18105', politician geoid '06037' => null
- `returns grant (fail-open) when politician has null home_jurisdiction_geoid` — grant with geoid '18105', politician geoid null => returns the grant (Alpha fail-open)
- `returns grant when editor has null jurisdiction (unrestricted)` — grant with geoid null, politician geoid '06037' => returns the grant
- `two-jurisdiction scenario: editor scoped to A writes A (match) then B (reject)` — single grant with geoid '18105'; call with politician geoid '18105' => grant; call with politician geoid '06037' => null. This is the specific scenario from success criterion 5.

**campaign_manager resource tests:**
- `returns grant when resource_id matches politicianId` — grant with resource_id 'pol-A', politicianId 'pol-A' => returns grant
- `returns null when resource_id does NOT match politicianId` — grant with resource_id 'pol-A', politicianId 'pol-B' => null
- `campaign_manager ignores jurisdiction — only checks resource_id` — grant with geoid '18105' and resource_id 'pol-A', politicianId 'pol-A', politician geoid '06037' => returns grant (geoid mismatch irrelevant for campaign_manager)

**Mixed grants:**
- `finds correct grant from mixed array` — array with [campaign_manager for pol-B, compass_stance_editor for geoid 18105]; politicianId 'pol-X' with geoid '18105' => returns the stance_editor grant (not campaign_manager)
- `returns null when no grants match` — array with [campaign_manager for pol-B, compass_stance_editor for geoid 06037]; politicianId 'pol-X' with geoid '18105' => null

Each test should assert either `expect(result).not.toBeNull()` + `expect(result!.id).toBe(...)` for match cases, or `expect(result).toBeNull()` for reject cases.

Note: getMatchingGrant is a PURE function (no DB calls). It takes (grants[], politicianId, politicianGeoid) and returns UserRoleGrant | null. No mocking needed — just call it directly with constructed inputs.
  </action>
  <verify>Run `npx vitest run tests/integration/compassContributor.test.ts` from repo root. All tests pass (exit 0).</verify>
  <done>10 test cases covering jurisdiction match/mismatch, fail-open for null geoid, campaign_manager resource gating, and the two-jurisdiction scenario from success criterion 5 all pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix grant() helper in requireRole.test.ts — add missing id field</name>
  <files>tests/integration/requireRole.test.ts</files>
  <action>
Edit `tests/integration/requireRole.test.ts` line 21-29: add `id: 'test-grant-id'` to the returned object in the `grant()` helper function, so it satisfies the full `UserRoleGrant` interface (which requires `id: string` since Phase 55-01).

The fixed grant() function should return:
```typescript
return {
  id: 'test-grant-id',
  role_id: 'r1',
  slug,
  name: slug,
  granted_at: '2026-01-01',
  feature_scope: 'platform',
  jurisdiction_geoid: geoid,
  resource_id: resourceId,
};
```

No other changes to the file. All existing 12 tests must continue to pass — checkRole does not use the `id` field, so adding it is purely an interface compliance fix.
  </action>
  <verify>Run `npx vitest run tests/integration/requireRole.test.ts` from repo root. All 12 existing tests pass (exit 0). Verify the id field is present by grepping: `grep "id:" tests/integration/requireRole.test.ts` shows the new line.</verify>
  <done>grant() helper returns a complete UserRoleGrant object including id: string. All 12 existing checkRole tests still pass.</done>
</task>

</tasks>

<verification>
Run both test files together from repo root:

```bash
npx vitest run tests/integration/compassContributor.test.ts tests/integration/requireRole.test.ts
```

Expected: All tests pass (22+ tests total: 12 existing checkRole + 10 new getMatchingGrant).

Verify TypeScript validity of test files (even though tsconfig excludes tests/):
```bash
npx tsc --noEmit --strict --esModuleInterop --moduleResolution node tests/integration/compassContributor.test.ts tests/integration/requireRole.test.ts 2>&1 || echo "Note: tsc on test files may fail due to vitest globals — vitest passing is the real check"
```
</verification>

<success_criteria>
1. tests/integration/compassContributor.test.ts exists with 10+ test cases covering getMatchingGrant
2. The two-jurisdiction scenario (success criterion 5) is explicitly tested: same editor grant, politician A matches (returns grant), politician B does not match (returns null)
3. campaign_manager resource_id gating tested: matching resource_id returns grant, mismatched returns null
4. tests/integration/requireRole.test.ts grant() helper includes id: string field
5. Both test files pass: `npx vitest run tests/integration/compassContributor.test.ts tests/integration/requireRole.test.ts` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/55-compass-stance-editor-campaign-manager-endpoints/55-04-SUMMARY.md`
</output>
