---
phase: 97-schema-foundation-data-audit
plan: 03
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - .planning/phases/97-schema-foundation-data-audit/DATA_SOURCES.md
  - ev-accounts/backend/scripts/sample-indiana-candidates.ts
autonomous: false
requirements: [DATA-01]

must_haves:
  truths:
    - "Indiana Secretary of State Excel candidate list has been downloaded and sample records extracted"
    - "Monroe County local race coverage gap is documented with specific races that require manual staging entry"
    - "LA County election data sources are documented with specific URLs and data formats"
    - "Sample records from each source have been validated against the 042 schema column types"
    - "The data source decision is documented with known coverage gaps explicitly noted"
  artifacts:
    - path: ".planning/phases/97-schema-foundation-data-audit/DATA_SOURCES.md"
      provides: "Data source documentation with sample records and coverage gap analysis"
      contains: "## Indiana Secretary of State"
    - path: "ev-accounts/backend/scripts/sample-indiana-candidates.ts"
      provides: "Script to parse IN SoS Excel and extract sample records matching schema"
      contains: "xlsx"
  key_links:
    - from: "sample-indiana-candidates.ts"
      to: "042_election_schema.sql"
      via: "column mapping validation"
      pattern: "full_name|candidate_status|election_date"
---

<objective>
Download sample election data from identified sources, validate that real records map correctly to the 042 schema columns, and document all sources with their coverage gaps.

Purpose: Per D-03, Phase 97 must identify sources AND pull sample records to validate schema design fits real data. This prevents schema redesign during Phase 98 import pipeline work.
Output: Data source documentation with validated column mappings and a sample parsing script for Indiana SoS data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-schema-foundation-data-audit/97-CONTEXT.md
@.planning/phases/97-schema-foundation-data-audit/97-RESEARCH.md
@.planning/phases/97-schema-foundation-data-audit/97-01-SUMMARY.md
@ev-accounts/backend/migrations/042_election_schema.sql

<interfaces>
From 042_election_schema.sql — the schema columns that source data must map to:

essentials.elections:
- name (text), election_date (date), election_type (primary/general/retention/special), jurisdiction_level (federal/state/county/city/district), state (char 2)

essentials.races:
- election_id (FK), office_id (FK nullable), position_name (text), seats (int)

essentials.race_candidates:
- race_id (FK), politician_id (FK nullable), full_name (text), first_name (text), last_name (text), photo_url (text), is_incumbent (bool), candidate_status (active/withdrawn/filed), source (text), external_id (text)

Excluded columns (antipartisan): party_name, party_affiliation, party, political_party — must be explicitly skipped during parsing even though source data includes them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Indiana SoS data and create sample parsing script</name>
  <files>ev-accounts/backend/scripts/sample-indiana-candidates.ts</files>
  <read_first>
    - ev-accounts/backend/migrations/042_election_schema.sql (target schema columns to validate against)
    - .planning/phases/97-schema-foundation-data-audit/97-RESEARCH.md (Indiana SoS Excel URLs and confirmed column names)
  </read_first>
  <action>
Create a TypeScript script that:

1. Downloads the Indiana SoS General Election candidate list Excel file from `https://www.in.gov/sos/elections/files/General-Candidate-List-February-25,-2026.xlsx` (or the Primary file from `https://www.in.gov/sos/elections/files/Primary-Candidate-List-3.25.26.xlsx` — try both, use whichever is accessible).

2. Uses the `xlsx` npm package (install it: `npm install xlsx` in ev-accounts/backend) to parse the Excel file.

3. Extracts the first 10 rows and maps each to the 042 schema structure:
   - Source "Office" column -> races.position_name
   - Source "Candidate Name" column -> race_candidates.full_name
   - Source "Last Name" column -> race_candidates.last_name
   - Source "Date Filed" column -> validate it exists (maps to race_candidates.source metadata)
   - Source "Incumbent" column -> race_candidates.is_incumbent (map Y/N to boolean)
   - Source "Political Party" column -> EXPLICITLY SKIP with console.log noting: "Skipping party column per antipartisan policy (D-04)"
   - Source "District" column -> races.position_name suffix or races.description

4. Prints sample records in JSON format showing:
   ```json
   {
     "election": { "name": "2026 Indiana Primary", "election_date": "2026-05-05", "election_type": "primary", "jurisdiction_level": "state", "state": "IN" },
     "race": { "position_name": "State Senate District 40", "seats": 1 },
     "candidate": { "full_name": "John Doe", "last_name": "Doe", "is_incumbent": true, "candidate_status": "active", "source": "sos_excel" }
   }
   ```

5. Prints a column mapping validation summary:
   - Which source columns mapped to which schema columns
   - Which source columns were explicitly excluded (party)
   - Any source columns with no schema target (note for Phase 98)

Run: `cd ev-accounts/backend && npx tsx scripts/sample-indiana-candidates.ts`

If the Excel download fails (URL changed), fall back to documenting the expected columns from RESEARCH.md and printing a mock mapping based on the known column names: Office, Candidate Name, Last Name, Political Party, District, Date Filed, Incumbent.
  </action>
  <verify>
    <automated>test -f /Users/chrisandrews/Documents/GitHub/ev-accounts/backend/scripts/sample-indiana-candidates.ts && grep -q "antipartisan\|party.*skip\|party.*exclude" /Users/chrisandrews/Documents/GitHub/ev-accounts/backend/scripts/sample-indiana-candidates.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ev-accounts/backend/scripts/sample-indiana-candidates.ts exists
    - Script explicitly skips or excludes the party column with a comment referencing antipartisan policy or D-04
    - Script maps source columns to schema columns (position_name, full_name, last_name, is_incumbent, candidate_status)
    - Script outputs sample records in JSON format
    - Script does NOT write party data to any output field
  </acceptance_criteria>
  <done>Sample parsing script exists and demonstrates that Indiana SoS Excel data maps cleanly to the 042 schema</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Document all data sources with coverage gaps and schema validation</name>
  <what-built>Sample data has been pulled from Indiana SoS. Monroe County local race gaps and LA County sources have been researched. A comprehensive data source document has been created showing how each source maps to the schema and where gaps exist.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/97-schema-foundation-data-audit/DATA_SOURCES.md`
    2. Verify Indiana SoS column mapping makes sense — especially that party column is excluded
    3. Verify Monroe County gap is clearly documented — which local races are missing
    4. Verify LA County sources are documented with specific URLs
    5. Confirm the Phase 98 import plan recommendations look feasible
  </how-to-verify>
  <files>.planning/phases/97-schema-foundation-data-audit/DATA_SOURCES.md</files>
  <read_first>
    - .planning/phases/97-schema-foundation-data-audit/97-RESEARCH.md (data source findings section)
    - ev-accounts/backend/migrations/042_election_schema.sql (schema to validate against)
  </read_first>
  <action>
Create `.planning/phases/97-schema-foundation-data-audit/DATA_SOURCES.md` with:

```markdown
# Election Data Sources - Phase 97 Validation

**Validated:** {date}
**Schema version:** 042_election_schema.sql

## Data Source Decision (per D-01, D-02)

CivicEngine API: OFF THE TABLE (paid, no access)
Google Civic API: UNAVAILABLE (no longer supported)
Approach: Hybrid — scrape structured public sources + manual staging for gaps

## Indiana Secretary of State (State + Federal Races)

**Source URL:** https://www.in.gov/sos/elections/files/Primary-Candidate-List-3.25.26.xlsx
**Format:** Excel (.xlsx)
**Confidence:** HIGH
**Coverage:** US House, Indiana State Senate, Indiana State Representative

### Column Mapping to Schema

| Source Column | Schema Target | Notes |
|---------------|---------------|-------|
| Office | races.position_name | Direct map |
| Candidate Name | race_candidates.full_name | Direct map |
| Last Name | race_candidates.last_name | Direct map |
| District | races.position_name (suffix) | Combine with Office for full position name |
| Incumbent | race_candidates.is_incumbent | Map Y/N to boolean |
| Date Filed | race_candidates.external_id or metadata | Filing date as provenance |
| Political Party | EXCLUDED | Antipartisan policy (D-04) |

### Sample Records

{Paste 3-5 sample records from the parsing script output}

### Schema Fit Assessment

{Note any columns that don't map cleanly, any missing data, or type mismatches}

## Monroe County Local Races (Bloomington)

**Source URL:** https://www.in.gov/counties/monroe/community2/voter-registration/candidate-filings/
**Format:** HTML page with SharePoint links
**Confidence:** MEDIUM
**Coverage:** City council, mayor, county offices

### Known Coverage Gap

Local Bloomington/Monroe County races are NOT in the SoS Excel file. The county clerk page has candidate names but no structured download. Specific gaps:
- {List specific races visible on the county clerk page}
- {Note what data is available vs missing}

### Recommended Approach for Phase 98

Manual entry via existing staging/data-entry tool (`/api/staging/*`) for:
- Candidate names (available from clerk page)
- Position names (available from clerk page)
- Photos and bios (require manual research per candidate)

## LA County Elections

**Source URLs:**
- Scheduled elections PDF: https://content.lavote.gov/docs/rrcc/documents/2026-scheduled-elections-10-25-2025-v-2.pdf
- Public Officials Roster: https://apps1.lavote.net/Voter/Public_Officials.cfm
- 2026 Candidate Handbook: https://content.lavote.gov/docs/rrcc/documents/2026-candidate-handbook-and-resource-guide--new-v-5.pdf
**Format:** PDF + HTML web app
**Confidence:** MEDIUM
**Coverage:** LA County supervisor, city council, school board races

### Known Coverage Gaps

No free structured API. Purchasing election information requires calling (800) 815-2666 option 4.
- Incumbent data: available from Public Officials Roster (scrapable HTML)
- Challenger data: requires manual staging entry
- Election dates: available from scheduled elections PDF

### Recommended Approach for Phase 98

1. Parse scheduled elections PDF for election dates and race names
2. Scrape Public Officials Roster for incumbent data
3. Manual staging for challenger candidates

## Antipartisan Enforcement Summary

ALL sources include party affiliation data. The following columns are explicitly excluded at every ingestion point:
- Indiana SoS: "Political Party" column
- Monroe County clerk: Party listed next to candidate names
- LA County: Party affiliation in official records

No party data is stored in any table. See 042_election_schema.sql antipartisan rationale comment.

## Phase 98 Import Pipeline Recommendations

1. Indiana state/federal: Automated Excel parser (sample script validates feasibility)
2. Monroe County local: Manual staging with data-entry tool
3. LA County: Semi-automated (scrape incumbents) + manual staging (challengers)
4. All pipelines must have explicit party column exclusion with logged warnings
```
  </action>
  <acceptance_criteria>
    - DATA_SOURCES.md exists in the phase directory
    - File contains sections for Indiana SoS, Monroe County, and LA County
    - File contains a column mapping table for Indiana SoS data
    - File contains "EXCLUDED" or "antipartisan" for the party column
    - File contains "## Known Coverage Gap" or similar for Monroe County
    - File contains specific URLs for all three data sources
    - File contains "## Phase 98 Import Pipeline Recommendations"
    - File documents that CivicEngine and Google Civic are unavailable (per D-01)
  </acceptance_criteria>
  <resume-signal>Type "approved" if data source documentation looks correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Sample parsing script runs and produces mapped records
- DATA_SOURCES.md documents all three data source areas
- Column mapping validates schema design fits real data
- Coverage gaps are explicitly documented
- Party columns are excluded in both script and documentation
</verification>

<success_criteria>
- Indiana SoS Excel data maps to 042 schema columns without schema changes needed
- Monroe County local race gap is documented with specific races identified
- LA County sources documented with URLs and recommended approach
- Phase 98 has clear import pipeline recommendations per data source
- Antipartisan exclusion documented for all sources
</success_criteria>

<output>
After completion, create `.planning/phases/97-schema-foundation-data-audit/97-03-SUMMARY.md`
</output>
