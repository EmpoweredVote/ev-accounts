---
phase: 165-small-delegation-states-candidate-seeding-17-states-create-e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/165-ak-generate.mts
  - backend/migrations/12NN_seed_ak_2026_house_election_race.sql
  - backend/migrations/12NN_seed_ak_2026_house_candidates.sql
  - backend/scripts/seed-ak-house-headshots.py
  - backend/data/seed-ak-2026-house/165-03-ak-reconciliation.csv
autonomous: true
requirements: [USHC3-02, USHC3-03, USHC3-04]
must_haves:
  truths:
    - "AK has 1 new election 'AK 2026 Statewide General' + 1 at-large race on the existing NATIONAL_LOWER office for geo_id 0200 (office_id never null); the race is modeled primary_party=NULL (jungle/top-four-nonpartisan convention, cloned from LA Phase-163 — Claude's Discretion per RESEARCH A1/Open-Question-3, documented in the reconciliation CSV); description carries 'PROVISIONAL: pre-primary qualified field (top-four-RCV), cull >= <AK primary date>'"
    - "The full declared top-four-primary field (15 candidates: 14 new + Begich reuse) surfaces on /elections — RCV over-indulgence: capture EVERY declared candidate, search harder before any field omission; Begich III (pid 07c7a121, external_id -2000) reuses his existing record (is_incumbent=true, 9 existing stances)"
    - "14 new AK candidates created at -(2*10000+seq) honoring D-04 safe_start_seq=5 (seqs 1-4 occupied) so first new = -20005; 0 duplicate full_name; every new candidate has a headshot or documented honest-skip"
  artifacts:
    - path: "backend/migrations/12NN_seed_ak_2026_house_election_race.sql"
      provides: "AK 2026 Statewide General election + 1 at-large jungle race on existing office 0200"
      contains: "AK 2026 Statewide General"
    - path: "backend/migrations/12NN_seed_ak_2026_house_candidates.sql"
      provides: "14 new AK candidates + Begich reuse on the single race (idempotent)"
      contains: "essentials.race_candidates"
  key_links:
    - from: "AK single race"
      to: "existing NATIONAL_LOWER office for geo_id 0200"
      via: "join districts -> offices; primary_party NULL"
      pattern: "NATIONAL_LOWER"
---

<objective>
Track 3 — AK nonpartisan top-four-RCV, late-primary. No pre-existing 2026-11-03 race (live-verified). Create 1 election + 1 at-large race on the existing office for geo_id 0200, modeled primary_party=NULL (the proven LA/CA jungle convention — RCV is a research-thoroughness discipline, NOT a DB schema feature; no ballot_system/rcv column exists). Seed the FULL 15-candidate declared field (14 new + Begich reuse) with maximal RCV thoroughness, then headshots. Continues USHC3-02/03/04.

Output: 165-ak-generate.mts, two AK migrations (run on prod), AK headshot script, AK reconciliation CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/2026-us-house-candidate-coverage/STATE.md
@.planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-RESEARCH.md
@.planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-PATTERNS.md
@.planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p165.csv
@.planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/staging/p165-AK.csv
@backend/scripts/163-la-generate.mts

<interfaces>
AK FIPS=2. Single at-large district, geo_id 0200. Election: create 'AK 2026 Statewide General' (election_type general, election_date 2026-11-03) — 0 pre-existing races live-verified. Race: 1 race (position_name 'U.S. Representative District 0' or the at-large convention used by existing AK records — check live), office_id = existing office via join (districts d ON d.district_type='NATIONAL_LOWER' AND d.geo_id='0200' -> offices o ON o.district_id=d.id). primary_party = NULL (jungle convention, clone 163-la-generate.mts race INSERT; use the dedicated unique index for WHERE primary_party IS NULL — Phase-163 precedent). This is Claude's Discretion per RESEARCH A1/OQ3 (no CONTEXT.md locks it); no user-facing surfacing impact either way — document the choice in the reconciliation CSV.
Incumbent reuse: Nicholas Begich III, pid 07c7a121-520f-4651-a1a0-5f38d20f8e0b, external_id -2000 (legacy 4-digit), is_incumbent=true, 9 existing stances (partial-tier -> not topped up in stance plan). race_candidates INSERT only, no new record.
New candidates (14): the full declared Aug-2026-primary field from staging/p165-AK.csv (top-four-RCV, all parties on one ballot). external_id = -(2*10000+seq), D-04 safe_start_seq=5 (seqs 1-4 occupied) so first new = -20005. AK band query = -20099..-20001. Over-indulge RCV thoroughness: capture EVERY declared candidate; search harder before omitting any.
Description: 'PROVISIONAL: pre-primary qualified field (top-four-RCV), cull >= <AK 2026 primary date>' — RE-VERIFY the exact AK 2026 congressional primary date from elections.alaska.gov before writing (historically mid-August; Open Question 2).
race_candidates: non-null politician_id, candidate_status='active'. Party NEVER on the card. sqlStr()-escape all free-text; NOT EXISTS on (race_id, politician_id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify AK primary date + confirm no pre-existing race + AK collision band (safe_start_seq=5) + Begich reuse</name>
  <read_first>
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv (AK safe_start_seq=5; colliding seqs 1-4)
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/staging/p165-AK.csv (the full 15-candidate declared field + sources)
  </read_first>
  <action>
    Re-verify the exact AK 2026 US House primary date from elections.alaska.gov (WebFetch elections.alaska.gov candidate/election pages) for the PROVISIONAL cull-date string. In one compound psql command, confirm live: (1) NO pre-existing 2026-11-03 AK House election/race; (2) the existing NATIONAL_LOWER office for geo_id 0200 exists; (3) Begich III pid 07c7a121 / external_id -2000 exists for reuse; (4) the AK new band -20099..-20001 is occupied at seqs 1-4 only (safe_start_seq=5 confirmed) with no occupancy at seq 5+. Record the confirmed primary date + seq-5 start for Task 2.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FROM essentials.elections WHERE name='AK 2026 Statewide General'), (SELECT count(*) FROM essentials.districts d JOIN essentials.offices o ON o.district_id=d.id WHERE d.district_type='NATIONAL_LOWER' AND d.geo_id='0200'), (SELECT count(*) FROM essentials.politicians WHERE external_id=-2000)"</automated>
  </verify>
  <acceptance_criteria>
    - AK 2026 Statewide General count = 0 (must create); existing 0200 office count = 1; Begich (-2000) count = 1
    - AK 2026 primary date re-verified from elections.alaska.gov (recorded for the PROVISIONAL string)
    - AK band collision-free at seq 5+ confirmed live (safe_start_seq=5)
  </acceptance_criteria>
  <done>AK primary date, pre-existing-race absence, Begich reuse, and the D-04 sub-band start are confirmed live before SQL generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply AK election/race (jungle) + candidates migrations</name>
  <read_first>
    - backend/scripts/163-la-generate.mts (the jungle-primary analog: primary_party=NULL race INSERT, incumbent reuse by external_id, sqlStr, NOT EXISTS guards)
    - .planning/workstreams/2026-us-house-candidate-coverage/phases/160-field-resolution-stance-gap-diagnostic/staging/p165-AK.csv (the 14 new + Begich roster with parties + source URLs)
  </read_first>
  <action>
    Clone backend/scripts/163-la-generate.mts -> backend/scripts/165-ak-generate.mts, scaled to AK's single at-large district (drop LA's 6-district loop, severe-withholding, and runoff logic). Emit TWO migrations. Migration A: INSERT 'AK 2026 Statewide General' + 1 race on the existing office for geo_id 0200 with primary_party=NULL, description 'PROVISIONAL: pre-primary qualified field (top-four-RCV), cull >= <verified AK primary date>' (NOT EXISTS on (election_id, office_id)). Migration B: 14 new politicians at -(2*10000+seq) starting seq 5 (-20005, -20006, ...), plus Begich reuse (external_id -2000, is_incumbent=true) — all as active race_candidates on the single race. Guard NOT EXISTS on (race_id, politician_id); sqlStr() all free-text; parties normalized in the reconciliation CSV only, never on the card. Re-verify live migration HWM via `ls backend/migrations | sort -n | tail`, use next free contiguous numbers. Run the generator, apply both migrations via psql -v ON_ERROR_STOP=1. Write backend/data/seed-ak-2026-house/165-03-ak-reconciliation.csv listing all 15 candidates + dispositions + the documented primary_party=NULL modeling choice.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT (SELECT count(*) FILTER (WHERE r.office_id IS NULL) FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='AK 2026 Statewide General'), (SELECT count(*) FROM essentials.race_candidates rc JOIN essentials.races r ON r.id=rc.race_id JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='AK 2026 Statewide General' AND rc.candidate_status='active'), (SELECT r.primary_party FROM essentials.races r JOIN essentials.elections e ON e.id=r.election_id WHERE e.name='AK 2026 Statewide General' LIMIT 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 1 AK race, office_id NOT NULL (FILTER 0), primary_party IS NULL; description carries 'PROVISIONAL:'
    - Exactly 15 active race_candidates (14 new -20005.. + Begich -2000 is_incumbent=true); 0 duplicate full_name; new ids honor safe_start_seq=5
    - Re-running both migrations is a 0-row no-op
  </acceptance_criteria>
  <done>AK's full top-four-RCV field seeded on prod as a single jungle race, PROVISIONAL-marked, with a reconciliation CSV.</done>
</task>

<task type="auto">
  <name>Task 3: AK headshots (band-scoped)</name>
  <read_first>
    - backend/scripts/seed-ms-house-headshots.py (band-scoped analog; wrong-person guards byte-for-byte)
  </read_first>
  <action>
    Clone backend/scripts/seed-ms-house-headshots.py -> backend/scripts/seed-ak-house-headshots.py. Change ONLY the docstring, BANDS = {'AK': (-20099, -20001, 'Alaska')}, RESULTS_JSON filename, and the localized place-token; DO NOT modify the wrong-person guard regexes. Run with `py` (never python3). Begich (-2000) falls outside the band (already imaged). Over-index thoroughness for AK per the RCV standing preference. Any candidate with no free-license portrait gets a documented honest-skip in the results JSON.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM essentials.politician_images pi JOIN essentials.politicians p ON p.id=pi.politician_id WHERE p.external_id BETWEEN -20099 AND -20001"</automated>
  </verify>
  <acceptance_criteria>
    - seed-ak-house-headshots.py uses band-scoped BANDS; wrong-person guards unchanged
    - Every new AK candidate has a politician_images row OR a documented honest-skip in the results JSON
  </acceptance_criteria>
  <done>AK new-candidate headshots ingested (or honest-skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator FIELD array -> migration SQL | Free-text names interpolated into SQL executed against PROD |
| headshot fetch -> politician_images | Wrong-person image mis-attribution risk |

## STRIDE Threat Register

N/A for auth/session/access-control/cryptography — pure-data seeding against PROD, no runtime code. Data-integrity + prod-write controls only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-03-01 | Tampering (SQLi) | Unescaped candidate names | mitigate | sqlStr() on every free-text field |
| T-165-03-02 | Tampering | Attempting an ALTER TABLE for a ballot_system/rcv column (Pitfall 4) | mitigate | RCV is research-only; race modeled primary_party=NULL, no schema change |
| T-165-03-03 | Repudiation | Duplicate Begich record | mitigate | Begich reused by external_id -2000; 0-dup-full_name assertion |
| T-165-03-04 | Repudiation | Incomplete RCV field (omitted declared candidate) | mitigate | Capture full declared field; gate 165-17 AK-FIELD block asserts exactly 15 active candidates |
| T-165-03-05 | Information disclosure | Party leaking onto candidate card | mitigate | race_candidates has no party column; primary_party NULL for the jungle race |
| T-165-03-06 | Spoofing | Wrong-person headshot | mitigate | Frozen wrong-person guard suite unchanged |
| T-165-03-SC | Tampering | npm/psql tooling — no new packages | accept | Zero new package installs; tsx/psql/py pre-vetted in 148-164 |
</threat_model>

<verification>
- 1 AK jungle race (primary_party NULL), office_id NOT NULL, PROVISIONAL-marked with the verified cull date
- Exactly 15 active candidates (14 new safe_start_seq=5 + Begich reuse); 0 duplicate full_name
- Every new AK candidate has a headshot row or documented honest-skip
</verification>

<success_criteria>
AK's full top-four-RCV 2026 field is seeded on prod as a single nonpartisan jungle race, PROVISIONAL-marked, with headshots and a reconciliation CSV documenting the primary_party=NULL modeling choice — ready for stance research (165-11).
</success_criteria>

<output>
Create .planning/phases/165-small-delegation-states-candidate-seeding-17-states-create-e/165-03-SUMMARY.md when done, recording applied migration numbers, the verified AK primary/cull date, the 14 new external_ids, the primary_party=NULL modeling decision, and any headshot honest-skips for the 165-17 gate pin.
</output>
