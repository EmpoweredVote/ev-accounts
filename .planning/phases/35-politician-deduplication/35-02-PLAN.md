---
phase: 35-politician-deduplication
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - backend/src/lib/compassService.ts
  - backend/src/lib/essentialsService.ts
  - backend/src/lib/adminService.ts
  - scripts/seedPoliticians.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/compass/politicians returns politician data from essentials.politicians"
    - "GET /api/essentials/politicians returns politician data from essentials.politicians"
    - "Admin create/update politician writes to essentials.politicians"
    - "Admin set politician context uses pool.query(), not PostgREST"
    - "seedPoliticians.ts targets essentials.politicians"
  artifacts:
    - path: "backend/src/lib/compassService.ts"
      provides: "getCompassPoliticians querying essentials schema"
      contains: "schema('essentials')"
    - path: "backend/src/lib/essentialsService.ts"
      provides: "getPoliticiansGrouped querying essentials schema"
      contains: "schema('essentials')"
    - path: "backend/src/lib/adminService.ts"
      provides: "adminCreatePolitician + adminUpdatePolitician on essentials schema; adminSetPoliticianContext via pool.query()"
      contains: "schema('essentials')"
    - path: "scripts/seedPoliticians.ts"
      provides: "Seed script targeting essentials schema"
      contains: "schema('essentials')"
  key_links:
    - from: "backend/src/lib/compassService.ts"
      to: "essentials.politicians"
      via: "supabaseAnon.schema('essentials').from('politicians')"
      pattern: "schema\\('essentials'\\)\\.from\\('politicians'\\)"
    - from: "backend/src/lib/adminService.ts"
      to: "inform.politician_context"
      via: "pool.query() INSERT ... ON CONFLICT"
      pattern: "pool\\.query.*inform\\.politician_context"
---

<objective>
Update all application code that references inform.politicians to point at essentials.politicians. Fix the adminSetPoliticianContext PostgREST anti-pattern by converting to pool.query().

Purpose: After this plan, the Express API and seed script work correctly against the unified essentials.politicians table. The forbidden PostgREST-on-non-public-schema pattern is eliminated from adminSetPoliticianContext.
Output: 4 modified TypeScript files, all pointing at essentials schema for politician data.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-politician-deduplication/35-RESEARCH.md
@.planning/phases/35-politician-deduplication/35-01-SUMMARY.md
@backend/src/lib/compassService.ts
@backend/src/lib/essentialsService.ts
@backend/src/lib/adminService.ts
@scripts/seedPoliticians.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update compassService.ts and essentialsService.ts to query essentials schema</name>
  <files>backend/src/lib/compassService.ts, backend/src/lib/essentialsService.ts</files>
  <action>
**compassService.ts — getCompassPoliticians()**

Change `.schema('inform')` to `.schema('essentials')` in the `getCompassPoliticians()` function (around line 170-172).

Before verifying the column list works, check the 35-01-SUMMARY.md for the essentials.politicians column list captured during the migration task. The SELECT clause currently requests: `id, first_name, last_name, preferred_name, full_name, office_title, photo_origin_url, is_active`. Verify ALL these columns exist in essentials.politicians. If any column does NOT exist in essentials.politicians (e.g., `preferred_name` may not be a column in the EV-Backend schema), remove it from the SELECT and update the return type accordingly. Update the JSDoc comment to say "essentials.politicians" instead of "inform.politicians".

Note: `getPoliticianAnswers()` and `getPoliticianContext()` query `inform.politician_answers` and `inform.politician_context` respectively — these tables STAY in the inform schema. Do NOT change those functions.

**essentialsService.ts — getPoliticiansGrouped()**

Change `.schema('inform')` to `.schema('essentials')` in the `getPoliticiansGrouped()` function (around line 74-75).

The SELECT clause already requests columns that came from migration 033 (district_type, district_label, etc.) which should be native to essentials.politicians. Verify the column list against the essentials.politicians schema from 35-01-SUMMARY.md. If any column does not exist, remove it from the SELECT and the PoliticianRecord interface.

Update the file-level JSDoc comment (line 9) from "inform.politicians" to "essentials.politicians". Update the function-level JSDoc (line 52) similarly.
  </action>
  <verify>
Run `npx tsc --noEmit` from the backend directory — zero type errors.
Start the server briefly (`npm run dev` or equivalent) and confirm:
- `GET /api/compass/politicians` returns a non-empty array with politician data
- `GET /api/essentials/politicians` returns grouped politician data
Both endpoints must return 200 with data, not 500.
  </verify>
  <done>Both compassService.ts and essentialsService.ts query essentials.politicians. Both API endpoints return 200 with correct data.</done>
</task>

<task type="auto">
  <name>Task 2: Update adminService.ts — schema switch + PostgREST fix</name>
  <files>backend/src/lib/adminService.ts</files>
  <action>
**adminCreatePolitician()**
Change `.schema('inform')` to `.schema('essentials')` in the INSERT call. This is a PostgREST write to `essentials` — essentials is a non-public schema, so this SHOULD also be converted to `pool.query()` per the critical production pattern. Convert to:
```typescript
const { rows } = await pool.query(
  `INSERT INTO essentials.politicians (first_name, last_name, preferred_name, office_title, photo_origin_url, is_active, is_candidate, ...)
   VALUES ($1, $2, $3, $4, $5, $6, $7, ...)
   RETURNING *`,
  [data.first_name, data.last_name, ...]
);
return rows[0] ?? {};
```
Use the actual column list from the essentials.politicians schema (captured in 35-01-SUMMARY.md). Only include columns that are being set.

**adminUpdatePolitician()**
Same pattern — convert from PostgREST `.schema('inform').from('politicians').update()` to `pool.query()`:
```typescript
// Build SET clause dynamically from the data object
const setClauses: string[] = [];
const values: unknown[] = [];
let paramIndex = 1;
// ... for each field in data, add to setClauses and values
values.push(politicianId); // last param for WHERE
const { rows } = await pool.query(
  `UPDATE essentials.politicians SET ${setClauses.join(', ')} WHERE id = $${paramIndex} RETURNING *`,
  values
);
return rows[0] ?? {};
```

**adminSetPoliticianContext()**
Convert from PostgREST (the forbidden pattern flagged in research) to pool.query():
```typescript
const { rows } = await pool.query(
  `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources)
   VALUES ($1, $2, $3, $4)
   ON CONFLICT (politician_id, topic_id)
   DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources
   RETURNING *`,
  [politicianId, topicId, data.reasoning, data.sources ?? []]
);
return rows[0] ?? {};
```
Note: politician_context stays in `inform` schema — only the politician TABLE moved to essentials. The context table's FK now points to essentials.politicians, but the table itself remains inform.politician_context.

Ensure `pool` is imported at the top of the file (it should already be — check the existing import from `'./db.js'`).
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors.
Verify the admin politician endpoints work by checking the admin UI or via curl:
- The admin politicians list page loads (uses admin_list_politicians RPC, already rebuilt in plan 01)
- Creating or updating a politician does not error
  </verify>
  <done>adminCreatePolitician and adminUpdatePolitician write to essentials.politicians via pool.query(). adminSetPoliticianContext uses pool.query() instead of PostgREST. Zero PostgREST writes to non-public schemas remain in politician-related functions.</done>
</task>

<task type="auto">
  <name>Task 3: Update seedPoliticians.ts to target essentials schema</name>
  <files>scripts/seedPoliticians.ts</files>
  <action>
Change `.schema('inform')` to `.schema('essentials')` in the `main()` function (line 151).

Critical check: `inform.politicians` had `full_name` as `GENERATED ALWAYS AS` (concatenation of first_name + last_name). The `essentials.politicians` table may NOT have this generated column — it may have `full_name` as a regular text column. Check the essentials.politicians column definition from 35-01-SUMMARY.md:
- If `full_name` is a regular column in essentials: add `full_name` to each seed record (concatenate first_name + ' ' + last_name manually)
- If `full_name` is also generated in essentials: leave as-is (do not include full_name in INSERT)

Also check if essentials.politicians uses `gen_random_uuid()` as default for `id`. If it does, the INSERT without explicit id is fine. If not, the seed script may need to generate UUIDs.

Update the file-level JSDoc to say "essentials.politicians" instead of "inform.politicians".

Note: After Phase 35, this seed script becomes less relevant (essentials has 1,854 real politicians vs. 4 placeholder seeds). But it should still work correctly for testing purposes.
  </action>
  <verify>The script compiles: `npx tsc --noEmit` passes. Do NOT run the seed script against production (it would insert duplicate placeholder records into essentials which already has 1,854 real records).</verify>
  <done>seedPoliticians.ts targets essentials.politicians. File compiles without errors. JSDoc updated.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in backend/ — zero errors
2. `grep -r "schema('inform').*from('politicians')" backend/src/` — returns ZERO matches (no code still queries inform.politicians)
3. `grep -r "schema('inform').*from('politicians')" scripts/` — returns ZERO matches
4. `grep -r "supabaseAdmin.*schema('inform').*from('politician_context')" backend/src/` — returns ZERO matches (PostgREST fix confirmed)
5. GET /api/compass/politicians returns 200 with data
6. GET /api/essentials/politicians returns 200 with grouped data
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- No remaining references to inform.politicians in application code (only inform.politician_answers and inform.politician_context remain, which is correct)
- adminSetPoliticianContext uses pool.query() instead of PostgREST
- adminCreatePolitician and adminUpdatePolitician write to essentials.politicians
- Both public politician endpoints return 200 with data
</success_criteria>

<output>
After completion, create `.planning/phases/35-politician-deduplication/35-02-SUMMARY.md`
</output>
