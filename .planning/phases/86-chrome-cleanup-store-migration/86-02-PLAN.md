---
phase: 86-chrome-cleanup-store-migration
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - EV-readrank/src/App.tsx
  - EV-readrank/src/components/PhaseContainer.tsx
  - EV-readrank/src/components/EvaluationPhase.tsx
  - EV-readrank/src/components/ResultsPhase.tsx
  - EV-readrank/src/components/AgreedQuotesSidebar.tsx
  - EV-readrank/src/components/IssueHub.tsx
  - EV-readrank/src/components/ProgressHeader.tsx
  - EV-readrank/src/components/AnimationOptionsPage.tsx
  - EV-readrank/src/components/BadgeIcons.tsx
  - EV-readrank/src/components/RankingPhase.tsx
  - EV-readrank/src/components/CollectionPhase.tsx
autonomous: true
requirements: [CHRM-01, CHRM-02, CHRM-03]

must_haves:
  truths:
    - "ProgressHeader is gone — no progress bar visible anywhere in the app"
    - "AnimationOptionsPage and /animation-options route return 404"
    - "Reset is accessible only via the account/profile menu, matching the Compass pattern"
    - "TypeScript builds cleanly with no references to deleted components or badge system"
    - "All components read issue data from getCurrentIssueProgress() — no legacy flat field reads"
  artifacts:
    - path: "EV-readrank/src/App.tsx"
      provides: "Clean imports, no ProgressHeader/AnimationOptionsPage, profile menu with Clear Read & Rank"
    - path: "EV-readrank/src/components/PhaseContainer.tsx"
      provides: "Phase switch with no ranking case"
    - path: "EV-readrank/src/components/EvaluationPhase.tsx"
      provides: "Reads from getCurrentIssueProgress(), no ranking transition"
    - path: "EV-readrank/src/components/ResultsPhase.tsx"
      provides: "No badge display, reads from getCurrentIssueProgress()"
    - path: "EV-readrank/src/components/AgreedQuotesSidebar.tsx"
      provides: "No badge icons, reads from getCurrentIssueProgress()"
    - path: "EV-readrank/src/components/IssueHub.tsx"
      provides: "No ranking phase check in progress info"
  key_links:
    - from: "App.tsx profileMenu"
      to: "useReadRankStore reset()"
      via: "handleClearReadRank with window.confirm"
      pattern: "window\\.confirm.*Clear all your Read & Rank"
    - from: "EvaluationPhase handleComplete"
      to: "setPhase('results')"
      via: "Direct phase transition (no intermediate 'ranking')"
      pattern: "setPhase\\('results'\\)"
---

<objective>
Delete dead chrome components (ProgressHeader, AnimationOptionsPage, BadgeIcons, RankingPhase, CollectionPhase), update all consumer components to use getCurrentIssueProgress() instead of legacy flat fields, add "Clear Read & Rank" to the profile menu, and achieve a clean TypeScript build.

Purpose: With the store types cleaned up in Plan 01, this plan fixes all the consumer-side TypeScript errors, deletes dead components, and wires the profile menu reset. After this plan, the app builds and runs cleanly on the v2 store.

Output: Clean-building app with no dead chrome, no badge UI, profile menu reset, all components on the getCurrentIssueProgress() pattern.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-chrome-cleanup-store-migration/86-CONTEXT.md
@.planning/phases/86-chrome-cleanup-store-migration/86-RESEARCH.md
@.planning/phases/86-chrome-cleanup-store-migration/86-01-SUMMARY.md
@EV-readrank/src/App.tsx
@EV-readrank/src/components/PhaseContainer.tsx
@EV-readrank/src/components/EvaluationPhase.tsx
@EV-readrank/src/components/ResultsPhase.tsx
@EV-readrank/src/components/AgreedQuotesSidebar.tsx
@EV-readrank/src/components/IssueHub.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead components and clean App.tsx imports/routes</name>
  <files>
    EV-readrank/src/components/ProgressHeader.tsx,
    EV-readrank/src/components/AnimationOptionsPage.tsx,
    EV-readrank/src/components/BadgeIcons.tsx,
    EV-readrank/src/components/RankingPhase.tsx,
    EV-readrank/src/components/CollectionPhase.tsx,
    EV-readrank/src/App.tsx,
    EV-readrank/src/components/PhaseContainer.tsx
  </files>
  <action>
  **Delete these 5 files entirely:**
  - `src/components/ProgressHeader.tsx` (CHRM-01)
  - `src/components/AnimationOptionsPage.tsx` (CHRM-02)
  - `src/components/BadgeIcons.tsx` (badge system removal)
  - `src/components/RankingPhase.tsx` (store migration — ranking phase eliminated)
  - `src/components/CollectionPhase.tsx` (dead code — orphaned component)

  **Update App.tsx:**
  - Remove `import { ProgressHeader }` line
  - Remove `import { AnimationOptionsPage }` line
  - Remove `<ProgressHeader />` JSX usage
  - Remove `<Route path="/animation-options" element={<AnimationOptionsPage />} />` route
  - Add `import { useReadRankStore }` to get `reset` action
  - Add `handleClearReadRank` function:
    ```
    const handleClearReadRank = () => {
      if (!window.confirm("Clear all your Read & Rank progress? This can't be undone.")) return;
      reset();
    };
    ```
  - Update `profileMenu` to include "Clear Read & Rank" item ABOVE "Sign out" (per user decision matching Compass pattern):
    ```
    items: [
      { label: 'Clear Read & Rank', onClick: handleClearReadRank },
      { label: 'Sign out', onClick: logout },
    ]
    ```

  **Update PhaseContainer.tsx:**
  - Remove `import { RankingPhase }` line
  - Remove `case 'ranking': return <RankingPhase />;` from the phase switch
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-readrank && ls src/components/ProgressHeader.tsx src/components/AnimationOptionsPage.tsx src/components/BadgeIcons.tsx src/components/RankingPhase.tsx src/components/CollectionPhase.tsx 2>&1 | grep -c "No such file"</automated>
  </verify>
  <done>5 dead component files deleted. App.tsx has no imports of deleted components, no /animation-options route, no ProgressHeader rendering. Profile menu has "Clear Read & Rank" with confirmation dialog above "Sign out". PhaseContainer has no ranking case.</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer components to use getCurrentIssueProgress() and remove badge references</name>
  <files>
    EV-readrank/src/components/EvaluationPhase.tsx,
    EV-readrank/src/components/ResultsPhase.tsx,
    EV-readrank/src/components/AgreedQuotesSidebar.tsx,
    EV-readrank/src/components/IssueHub.tsx
  </files>
  <action>
  Read each file fully before modifying. Use the 86-RESEARCH.md pitfalls section as a checklist.

  **EvaluationPhase.tsx:**
  - Replace all direct flat-field reads (`quotesToEvaluate`, `currentQuoteIndex`, `agreedQuotes`, `disagreedQuotes`, `questionText`) with reads from `getCurrentIssueProgress()`. Pattern: `const progress = useReadRankStore(state => state.getCurrentIssueProgress())` or destructure from the existing store usage.
  - Remove `setPhase('ranking')` call in `handleComplete` — replace with `setPhase('results')` (direct to results, no intermediate ranking screen)
  - Remove "Assign your badges in the sidebar" hint text
  - Remove "Rank Your Priorities" mobile button text — replace with "See Your Results" or remove if not needed
  - For `questionText`: since the legacy flat field is gone and IssueProgress does not store question text, either remove the question banner entirely OR source it from the issues data that EvaluationPhase receives. Check how the component gets issue data — if questionText was set via `setIssueInfo`, look for the original data source and read from there. Simplest: remove the banner if it's optional chrome.

  **ResultsPhase.tsx:**
  - Remove `DiamondBadgeDisplay` and `GoldBadgeDisplay` inline component definitions
  - Replace flat-field destructuring (`rankedQuotes`, `agreedQuotes`, `disagreedQuotes`, `badgeAssignments`, `topicId`) with reads from `getCurrentIssueProgress()`
  - For `topicId` used in `buildEssentialsProfileUrl`: use `currentIssueId` from the store instead (per research recommendation — it's the same value)
  - Remove badge-based `organizedQuotes` sorting (diamond/gold to top) — use rank order only
  - Remove Diamond/Gold stat boxes from Summary Stats section
  - Remove `badgeAssignments` from `calculateAlignment` call — the parameter no longer exists after Plan 01

  **AgreedQuotesSidebar.tsx:**
  - Remove `import { DiamondBadge, GoldBadge }` (or BadgeIcons import)
  - Replace flat-field reads (`agreedQuotes`, `badgeAssignments`, `quotesToEvaluate`, `currentQuoteIndex`) with `getCurrentIssueProgress()` reads
  - Remove all badge-related props, state, and rendering from `SortableCompactQuoteCard`
  - Remove badge status section from sidebar header
  - Simplify to rank-only display

  **IssueHub.tsx:**
  - Remove `progress.phase === 'ranking'` case from `getProgressInfo`
  - Remove "Assigning badges" progress text for ranking phase

  After all changes, run the TypeScript build to verify everything compiles cleanly.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npm run build</automated>
  </verify>
  <done>All 4 consumer components updated to getCurrentIssueProgress() pattern. No badge references anywhere in the codebase. No 'ranking' phase references. TypeScript builds cleanly. `npm run build` succeeds with zero errors.</done>
</task>

</tasks>

<verification>
- `cd EV-readrank && npm run build` — succeeds with zero TypeScript errors
- `grep -r "BadgeType\|BadgeAssignment\|BadgeIcons\|DiamondBadge\|GoldBadge\|assignBadge\|clearBadge" EV-readrank/src/` — returns nothing
- `grep -r "'ranking'" EV-readrank/src/` — returns nothing
- `grep -r "ProgressHeader\|AnimationOptionsPage\|animation-options" EV-readrank/src/` — returns nothing
- `grep -r "setQuotes\|setIssueInfo\|questionText" EV-readrank/src/store/` — returns nothing
- `grep "Clear Read & Rank" EV-readrank/src/App.tsx` — returns match confirming menu item exists
</verification>

<success_criteria>
- ProgressHeader component deleted and no longer rendered
- AnimationOptionsPage component deleted and /animation-options route removed
- BadgeIcons, RankingPhase, CollectionPhase components deleted
- "Clear Read & Rank" menu item in profile menu with window.confirm dialog
- All components use getCurrentIssueProgress() — no legacy flat field reads
- No badge references anywhere in the codebase
- No 'ranking' phase value anywhere in the codebase
- `npm run build` succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/86-chrome-cleanup-store-migration/86-02-SUMMARY.md`
</output>
