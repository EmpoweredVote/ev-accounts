---
phase: 117
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ev-accounts/backend/src/lib/essentialsService.ts
autonomous: true
requirements: [CAND-02]
tags: [phase-117, backend, essentials, address-resolver, leak-fix]
must_haves:
  truths:
    - "Address-lookup for a Monroe County address does NOT return any row where is_candidate=true."
    - "The filter is applied to BOTH districtQueryText (L593 area) AND statewideQueryText (L635 area)."
    - "The filter runs unconditionally — not gated on includeChallengers (sitting incumbents remain the 'your reps' view even when challengers are requested)."
    - "Compass picker path is NOT affected (compassService.ts untouched by this plan)."
    - "Backend typecheck + existing tests still pass."
  artifacts:
    - path: "ev-accounts/backend/src/lib/essentialsService.ts"
      provides: "Updated SQL queries with COALESCE(p.is_candidate, false) = false filter"
      contains: "AND COALESCE(p.is_candidate, false) = false"
  key_links:
    - from: "essentialsService.getRepresentativesByAddress"
      to: "essentials.politicians.is_candidate column (from Plan 02 migration 068)"
      via: "SQL WHERE clause in districtQueryText + statewideQueryText"
      pattern: "AND COALESCE\\(p.is_candidate, false\\) = false"
---

<objective>
Add a one-line SQL filter to the address-resolver path in `essentialsService.ts` so that politicians with `is_candidate=true` are excluded from tier-grouped "these are your current representatives" results. Applied to BOTH the district query (L593 area) AND the statewide query (L635 area). This closes the query-level leak; Plan 02's migration closed the data-level leak.

Purpose: D-09. Per D-09, Compass picker and race listings still include candidates — only the address-lookup surface filters them out.
Output: Modified `src/lib/essentialsService.ts` with two added filter lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-candidate-stub-resolution-data-import/117-CONTEXT.md
@.planning/phases/117-candidate-stub-resolution-data-import/117-RESEARCH.md
@.planning/phases/117-candidate-stub-resolution-data-import/117-PATTERNS.md
@ev-accounts/backend/src/lib/essentialsService.ts

<interfaces>
<!-- Existing filter pattern at essentialsService.ts L592-594 -->
<!-- From 117-PATTERNS.md §essentialsService.ts L593/L635: -->

Current shape (districtQueryText ~L593):
```
    AND (p.is_active = true OR o.is_vacant = true)
    ${includeChallengers ? '' : 'AND COALESCE(p.is_incumbent, true) = true'}
    ORDER BY COALESCE(p.id, o.id)
```

Required shape:
```
    AND (p.is_active = true OR o.is_vacant = true)
    ${includeChallengers ? '' : 'AND COALESCE(p.is_incumbent, true) = true'}
    AND COALESCE(p.is_candidate, false) = false
    ORDER BY COALESCE(p.id, o.id)
```

The same addition lands in statewideQueryText (~L635). Both queries share the `includeChallengers` ternary and need the new line placed BEFORE `ORDER BY`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add is_candidate filter to districtQueryText and statewideQueryText</name>
  <read_first>
    - ev-accounts/backend/src/lib/essentialsService.ts (L530-720 — contains both query templates; exact line numbers may have drifted — find by searching for `COALESCE(p.is_incumbent, true) = true`)
    - .planning/phases/117-candidate-stub-resolution-data-import/117-PATTERNS.md §src/lib/essentialsService.ts (exact before/after)
    - .planning/phases/117-candidate-stub-resolution-data-import/117-RESEARCH.md §Pattern 4 (rationale for unconditional filter)
  </read_first>
  <files>ev-accounts/backend/src/lib/essentialsService.ts</files>
  <action>
    Locate both occurrences of the string `AND COALESCE(p.is_incumbent, true) = true` inside the file (expected: one in `districtQueryText` around L593, one in `statewideQueryText` around L635). Use grep to confirm exact line numbers before editing:

    ```
    grep -n "COALESCE(p.is_incumbent, true) = true" ev-accounts/backend/src/lib/essentialsService.ts
    ```

    If grep returns anything OTHER than exactly 2 matches, stop and investigate — the file has drifted from the PATTERNS.md reference.

    At EACH match, insert a new line immediately after the `${includeChallengers ? '' : 'AND COALESCE(p.is_incumbent, true) = true'}` line and before the `ORDER BY` line. The new line MUST read:

    ```
        AND COALESCE(p.is_candidate, false) = false
    ```

    Match the leading whitespace of the surrounding lines (the existing query template uses 4-space indentation inside the template literal — match exactly what the `AND (p.is_active = true OR o.is_vacant = true)` line uses).

    Per D-09, this filter runs UNCONDITIONALLY — it is NOT gated on `includeChallengers`. Rationale: even when showing challengers, sitting incumbents are still the "these are your reps" view. A candidate for office by definition is not currently holding the seat.

    Do NOT modify any other logic in the file. Do NOT expose `p.is_candidate` in the SELECT clause in this task (if Plan 07's verify-117-leak-closed.sh needs it, Plan 07 will handle that decision separately).

    Run typecheck:
    ```
    cd ev-accounts/backend && npm run typecheck
    ```

    Run existing test suite:
    ```
    cd ev-accounts/backend && npm test
    ```

    All existing tests should still pass (the filter is additive — it excludes rows that shouldn't be returned anyway).
  </action>
  <verify>
    <automated>cd ev-accounts/backend &amp;&amp; [ "$(grep -c 'COALESCE(p.is_candidate, false) = false' src/lib/essentialsService.ts)" = "2" ] &amp;&amp; [ "$(grep -c 'COALESCE(p.is_incumbent, true) = true' src/lib/essentialsService.ts)" = "2" ] &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 occurrences of `COALESCE(p.is_candidate, false) = false` in the file (one per query template)
    - Exactly 2 occurrences of `COALESCE(p.is_incumbent, true) = true` remain (the existing filter is untouched)
    - `npm run typecheck` passes
    - `npm test` passes (no regressions in existing integration tests)
    - No other lines in the file changed (git diff should show only the two additions)
  </acceptance_criteria>
  <done>Both query templates filter out is_candidate=true rows; typecheck + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client address → essentialsService.getRepresentativesByAddress | Untrusted address input (already validated upstream) |
| essentialsService SQL → production essentials.politicians | Trusted operator-authored query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-117-09 | Tampering | SQL query filter addition | mitigate | New filter is a literal `AND COALESCE(p.is_candidate, false) = false` — no parameter, no user input. Cannot introduce injection. Wrapped by existing parameterized query structure. |
| T-117-10 | Information disclosure | candidate data leaks into "your reps" address lookup | mitigate | THIS plan IS the mitigation. The filter runs unconditionally so candidates cannot leak regardless of `includeChallengers` flag. |
| T-117-11 | Denial of service | added filter hurts query performance | accept | `is_candidate` is a BOOLEAN with a DEFAULT; table has ~2500 rows. Postgres planner handles this trivially. No index needed at this scale. |
</threat_model>

<verification>
- Two filter additions present (districtQueryText + statewideQueryText)
- Typecheck + full test suite green
- No other lines modified
</verification>

<success_criteria>
- CAND-02 support: once Plan 06 inserts candidates, this filter keeps them off address-lookup surface
- Pre-existing leak (backfilled in Plan 02) is now also query-level closed
- Plan 07 will run `verify-117-leak-closed.sh` end-to-end against this code
</success_criteria>

<output>
After completion, create `.planning/phases/117-candidate-stub-resolution-data-import/117-03-SUMMARY.md` with:
- Actual line numbers of the two additions (may differ from L593/L635 if file drifted)
- Test suite result
- Any test failures encountered and how resolved
</output>
