---
phase: 117
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - ev-accounts/backend/scripts/import-monroe-stub-candidates.ts
autonomous: false
requirements: [CAND-02, CAND-03, CAND-05]
tags: [phase-117, scripts, import, monroe-county, candidates]
user_setup:
  - service: supabase
    why: "DB writes to essentials schema + Storage upload for photos (already in use)"
    env_vars:
      - name: DATABASE_URL
        source: "ev-accounts/backend/.env (existing)"
      - name: SUPABASE_URL
        source: "ev-accounts/backend/.env (existing)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "ev-accounts/backend/.env (existing)"
must_haves:
  truths:
    - "Every row in 117-FEASIBILITY-data.csv (scope respected per 117-01 SUMMARY go/no-go decision) has a corresponding essentials.politicians row with is_candidate=true, source='phase_117_monroe_stub_import', and non-null bio_text."
    - "Each imported politician has a matching essentials.offices row with the right chamber_id/district_id/title."
    - "Each corresponding essentials.race_candidates stub now has politician_id populated (no remaining stubs for imported scope)."
    - "Photos for rows with a photo_source_url are uploaded to Supabase Storage under politician_photos/monroe_2026/{slug}.jpg and the CDN URL is stored in BOTH (a) essentials.politicians.photo_custom_url AND (b) a new essentials.politician_images row with type='default' and is_primary=true. The politician_images row is REQUIRED because PoliticianProfile.jsx render path reads from pol.images[] FIRST (ev-ui/src/PoliticianProfile.jsx L282-288) and only falls back to pol.photo_origin_url — photo_custom_url is not consulted by the profile renderer."
    - "Rows without a sourceable photo use the ev-ui missing-photo fallback (photo_custom_url stays NULL) and bio_text falls back to 'Candidate for [office title]'."
    - "Every imported bio_text is ≤180 chars AND has been code-reviewed against bio_source_url for single-source compliance (D-07) BEFORE commit."
    - "PoliticianProfile render path verified: ev-ui/src/PoliticianProfile.jsx L282-288 builds profileImageUrl from pol.images[] (sourced from essentials.politician_images) with fallback to pol.photo_origin_url — CONTEXT.md D-13 compliance requires the politician_images row."
    - "The script is idempotent — re-running skips already-linked stubs via the pre-check."
  artifacts:
    - path: "ev-accounts/backend/scripts/import-monroe-stub-candidates.ts"
      provides: "Idempotent bulk-import of Monroe County stub candidates from feasibility CSV"
      exports: []
  key_links:
    - from: "scripts/import-monroe-stub-candidates.ts"
      to: ".planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv"
      via: "csv-parse sync reader"
      pattern: "parse\\(.*FEASIBILITY-data.csv"
    - from: "scripts/import-monroe-stub-candidates.ts"
      to: "scripts/lib/upload-candidate-photo.ts"
      via: "uploadCandidatePhoto() import"
      pattern: "import.*uploadCandidatePhoto"
    - from: "scripts/import-monroe-stub-candidates.ts"
      to: "essentials.race_candidates (stub resolution)"
      via: "UPDATE ... SET politician_id"
      pattern: "UPDATE essentials.race_candidates"
---

<objective>
Author and execute `scripts/import-monroe-stub-candidates.ts` — a bespoke TypeScript import script that reads the feasibility CSV sidecar (from Plan 01), uploads photos via Plan 05's helper, inserts `essentials.politicians` + `essentials.offices` rows, and UPDATES `essentials.race_candidates.politician_id` to resolve each stub. All writes transaction-per-candidate with explicit BEGIN/COMMIT/ROLLBACK (Pitfall #5) and pre-flight checks for (a) stub already linked, (b) office scaffolding present (Pitfall #6).

This plan executes Claude's Discretion "bespoke script over staging workflow" (research-recommended per Finding #3 — staging would reintroduce the leak via hardcoded is_incumbent=true).

Covers CAND-02 (records exist), CAND-03 (photo + bio + office title), CAND-05 data side (rows exist so Plan 04's LEFT JOIN picker actually returns them).

**Scope-list input:** the script accepts the feasibility CSV as its ONLY scope definition — a scope-down from April 25 is handled by editing the CSV, not the code (per critical constraint #9).

Purpose: the actual data landing. Plans 02-05 created the rails; this is the train.
Output: Script authored + executed; 117-FEASIBILITY-scope determined candidates imported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-candidate-stub-resolution-data-import/117-CONTEXT.md
@.planning/phases/117-candidate-stub-resolution-data-import/117-RESEARCH.md
@.planning/phases/117-candidate-stub-resolution-data-import/117-PATTERNS.md
@.planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv
@.planning/phases/117-candidate-stub-resolution-data-import/117-01-SUMMARY.md
@ev-accounts/backend/scripts/seedPolitician.ts
@ev-accounts/backend/scripts/lib/upload-candidate-photo.ts

<interfaces>
<!-- Consumed from Plan 05 -->
```typescript
// scripts/lib/upload-candidate-photo.ts
export async function uploadCandidatePhoto(
  sourceUrl: string,
  slug: string,
  contentType?: 'image/jpeg' | 'image/png' | 'image/webp'
): Promise<string>;
```

<!-- Schema from Plan 02 migration 068 -->
essentials.politicians now has:
  is_candidate BOOLEAN NOT NULL DEFAULT false  ← set to true for these inserts
  bio_source_url TEXT                          ← populated from CSV
  + existing: full_name, first_name, last_name, bio_text, photo_custom_url, is_active, is_incumbent, source, slug

<!-- CSV header from Plan 01 -->
race_candidate_id,full_name,first_name,last_name,office_title,chamber_id,district_id,photo_source_url,bio_text,bio_source_url,tier

<!-- Insert pattern from 117-PATTERNS.md §scripts/import-monroe-stub-candidates.ts -->
Transaction per candidate:
  BEGIN
  -- pre-check: race_candidates.politician_id IS NULL (skip if not)
  -- pre-flight: chamber_id + district_id non-null (fail loud if missing)
  INSERT INTO essentials.politicians (..., photo_custom_url, is_candidate=true, is_incumbent=false, source='phase_117_monroe_stub_import') RETURNING id
  INSERT INTO essentials.offices (politician_id, chamber_id, district_id, title, 'IN', city, false)
  -- photon_images row is REQUIRED because PoliticianProfile.jsx reads pol.images[] first (L282-288 of ev-ui/src/PoliticianProfile.jsx)
  -- Only executed when photoCdnUrl !== null (i.e. photo upload succeeded)
  INSERT INTO essentials.politician_images (politician_id, url, type, is_primary, source)
    VALUES (politicianId, photoCdnUrl, 'default', true, 'phase_117_monroe_stub_import')
  UPDATE essentials.race_candidates SET politician_id = $1, updated_at = now() WHERE id = $2 AND politician_id IS NULL
  COMMIT (or ROLLBACK on any error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the import script</name>
  <read_first>
    - ev-accounts/backend/scripts/seedPolitician.ts (L1-50, L180-260 — dotenv/pool/INSERT pattern)
    - ev-accounts/backend/scripts/audit-112-candidates.ts (L1-60 — alternate dotenv-with-path pattern)
    - ev-accounts/backend/scripts/link-monroe-candidates-to-politicians.sql (idempotency comment)
    - .planning/phases/117-candidate-stub-resolution-data-import/117-PATTERNS.md §scripts/import-monroe-stub-candidates.ts (full adaptation, L99-221 of patterns file — including pre-check, pre-flight scaffolding check, transaction-per-candidate, and dry-run sketch)
    - .planning/phases/117-candidate-stub-resolution-data-import/117-RESEARCH.md §Pitfalls 5 and 6
    - ev-accounts/backend/scripts/lib/upload-candidate-photo.ts (Plan 05 helper)
  </read_first>
  <files>ev-accounts/backend/scripts/import-monroe-stub-candidates.ts</files>
  <action>
    Create `ev-accounts/backend/scripts/import-monroe-stub-candidates.ts`. The script MUST implement the following behavior in order:

    **CLI flags:**
    - `--dry-run` — prints what would happen, no writes
    - `--csv <path>` — override default CSV path (default: `../../.planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv` relative to `ev-accounts/backend/`)
    - `--only-tier <tier>` — optional filter (e.g. `--only-tier Confirmed-sourceable`) so operator can run tiers sequentially
    - `--only-id <race_candidate_id>` — debug-run a single row

    **Startup (once):**
    1. Load `.env` via dotenv (match the audit-112 path-resolution pattern since this script may be invoked from different cwds)
    2. Read the CSV with `csv-parse/sync` (dependency present — confirm via package.json before writing; if NOT present, fail loud with install instructions — do NOT silently npm-install)
    3. Apply `--only-tier` and `--only-id` filters if set
    4. Log count of rows to process
    5. If `--dry-run`, print the planned plan and exit 0

    **Per-row loop (for each CSV row):**
    1. **Pre-check:** `SELECT id, politician_id FROM essentials.race_candidates WHERE id = $1`. If `politician_id IS NOT NULL`, log `[skip] already linked` and continue (idempotent re-run).
    2. **Pre-flight scaffolding check (Pitfall #6):** If `chamber_id` or `district_id` in the CSV row is literal `MISSING` OR the DB lookup shows null, fail LOUDLY for that row — log `[error] scaffold missing` and continue to next row. Track error count.
    3. **Slug generation:** compute from `full_name` using the same slugify approach already used in the codebase. Query existing helpers first (`grep -n "slug" ev-accounts/backend/src/lib/*.ts` to find any existing slugifier). If none exists, inline a simple `.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')`.
    4. **Photo upload:** if `photo_source_url` is non-empty, call `uploadCandidatePhoto(photo_source_url, slug, 'image/jpeg')` → `photoCdnUrl`. On failure, log warning and set `photoCdnUrl = null` (D-06 fallback — row still imports).
    5. **Bio fallback (D-06):** if `bio_text` is empty in CSV, use literal `Candidate for ${office_title}`.
    6. **Bio length cap:** assert `bio_text.length <= 180`. If longer, trim at the last word boundary before char 180 and log a warning.
    7. **Transaction:**
        ```
        BEGIN
          INSERT INTO essentials.politicians (full_name, first_name, last_name, slug, bio_text, bio_source_url, photo_custom_url, is_active, is_incumbent, is_candidate, source)
          VALUES ($1,$2,$3,$4,$5,$6,$7, true, false, true, 'phase_117_monroe_stub_import')
          RETURNING id
          → politicianId

          INSERT INTO essentials.offices (politician_id, chamber_id, district_id, title, representing_state, is_vacant)
          VALUES (politicianId, $1, $2, $3, 'IN', false)

          -- D-13 / CLAUDE.md canonical photo table: PoliticianProfile.jsx reads pol.images[]
          -- first (ev-ui/src/PoliticianProfile.jsx L282-288) before falling back to
          -- pol.photo_origin_url. photo_custom_url is NOT read by the profile renderer,
          -- so we MUST also write a politician_images row when a photo upload succeeded.
          -- Only execute this INSERT when photoCdnUrl !== null.
          IF photoCdnUrl is not null THEN
            INSERT INTO essentials.politician_images (politician_id, url, type, is_primary, source)
            VALUES (politicianId, photoCdnUrl, 'default', true, 'phase_117_monroe_stub_import')
          END IF

          UPDATE essentials.race_candidates SET politician_id = politicianId, updated_at = now()
          WHERE id = $stub_id AND politician_id IS NULL
          -- assert rowCount === 1 — if not, ROLLBACK and log error
        COMMIT
        ```

        **Note on column names:** Confirm `essentials.politician_images` column names before authoring the INSERT by running `psql "$DATABASE_URL" -c "\\d essentials.politician_images"`. The CLAUDE.md workspace doc describes this table as the canonical photo store. If a column in the sketch above (`type`, `is_primary`, `source`) does not exist, adapt to the actual schema — but the INSERT MUST happen so that `pol.images[]` populates in the API response consumed by PoliticianProfile.jsx.
    8. On any throw inside the transaction: `ROLLBACK`, log error with full row context, continue to next row (do NOT abort the entire run — one bad row should not block the rest, mirroring the migration 064 pattern).

    **Summary output at end:**
    - Processed: N
    - Imported: X
    - Skipped (already linked): Y
    - Scaffold errors: Z
    - Other errors: W
    - Exit 0 if all rows succeeded OR skipped; exit 1 if any errors occurred (so CI/operator notices)

    **Security rules:**
    - ALL SQL uses parameterized queries ($1, $2, ...) — zero string concatenation
    - Never `console.log(process.env.SUPABASE_SERVICE_ROLE_KEY)` or `DATABASE_URL`
    - Full stack traces are fine in stderr (they don't contain env values)

    Use `source = 'phase_117_monroe_stub_import'` as the literal for EVERY insert — enables Plan 07 verification queries and clean rollback.

    Run typecheck after authoring:
    ```
    cd ev-accounts/backend && npm run typecheck
    ```

    Then do a dry-run against the CSV:
    ```
    cd ev-accounts/backend && npx tsx scripts/import-monroe-stub-candidates.ts --dry-run
    ```

    Expected: prints one line per row, exits 0, no DB writes.
  </action>
  <verify>
    <automated>cd ev-accounts/backend &amp;&amp; test -f scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "phase_117_monroe_stub_import" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "uploadCandidatePhoto" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "is_candidate" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "ROLLBACK" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "politician_id IS NULL" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "politician_images" scripts/import-monroe-stub-candidates.ts &amp;&amp; grep -q "dry-run" scripts/import-monroe-stub-candidates.ts &amp;&amp; npm run typecheck &amp;&amp; npx tsx scripts/import-monroe-stub-candidates.ts --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Script file exists
    - Contains literal `phase_117_monroe_stub_import` (source tag)
    - Imports `uploadCandidatePhoto` from Plan 05 helper
    - Contains `is_candidate` in the INSERT (set to true)
    - Contains explicit `ROLLBACK` (transaction safety)
    - Contains `politician_id IS NULL` pre-check (idempotency)
    - Contains `politician_images` INSERT inside the transaction (D-13 / render-path requirement)
    - Supports `--dry-run` flag
    - `npm run typecheck` passes
    - Dry-run against the real CSV exits 0 and prints one line per row
  </acceptance_criteria>
  <done>Script authored, typechecks, dry-runs cleanly against the real feasibility CSV.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bio code-review gate (D-07 single-source enforcement)</name>
  <files>.planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv</files>
  <action>See how-to-verify — operator opens the CSV, checks each non-fallback bio_text against its bio_source_url for verbatim/single-source compliance, edits or descopes rows that fail D-07 or D-14, re-runs dry-run if edits were made.</action>
  <verify><automated>awk -F',' 'NR>1 {gsub(/^"|"$/, "", $9); if (length($9) > 180) {print "FAIL length: line " NR " (" length($9) " chars)"; exit 1}}' .planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv</automated></verify>
  <done>Every CSV bio_text is ≤180 chars AND operator has typed "bios approved".</done>
  <what-built>
    Import script ready. Dry-run verified clean. Before running against production DB, every bio in the feasibility CSV needs a human pass confirming (a) bio_text is ≤180 chars, (b) bio_text appears verbatim or as a faithful compression on bio_source_url, (c) no synthesis across multiple sources, (d) no partisan framing or party-color language.

    This is the D-07 enforcement mechanism explicitly called out in planning_context critical constraint #10 as "code review checkpoint, not unit test."
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv`
    2. For EACH row with a non-fallback `bio_text` (i.e. bio_text is NOT `Candidate for ...`):
       - Confirm `length(bio_text) <= 180`
       - Open `bio_source_url` in a browser
       - Confirm the sentence genuinely appears (verbatim or trivially compressed) on that page
       - Confirm no partisan framing (no "Democratic/Republican X", no party colors, no ideological labeling)
    3. If any row fails, edit the CSV directly:
       - Replace the bio with a compliant single-source extract, OR
       - Demote the row to `Candidate for [office_title]` fallback and clear bio_source_url
    4. Re-run the dry-run from Task 1 to confirm the CSV still parses
    5. Reply "bios approved — proceed to import" OR "bios edited — re-run dry-run" OR "descope rows [list]"
  </how-to-verify>
  <resume-signal>Type "bios approved", or describe edits/descopes made.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute import against production DB</name>
  <read_first>
    - ev-accounts/backend/scripts/import-monroe-stub-candidates.ts (Task 1 output)
    - .planning/phases/117-candidate-stub-resolution-data-import/117-FEASIBILITY-data.csv (Task 2 approved)
  </read_first>
  <files>(no file writes — DB inserts + Supabase Storage uploads)</files>
  <action>
    Run the real import:

    ```
    cd ev-accounts/backend && npx tsx scripts/import-monroe-stub-candidates.ts
    ```

    Capture stdout + stderr to a log file:
    ```
    cd ev-accounts/backend && npx tsx scripts/import-monroe-stub-candidates.ts 2>&1 | tee ../../.planning/phases/117-candidate-stub-resolution-data-import/117-06-import.log
    ```

    Exit 0 is required. If exit 1, investigate the logged errors:
    - Scaffold errors → may need to add chambers/districts in a side-migration (flag in SUMMARY, do not fix in this plan — bring to user for decision)
    - Storage upload errors → may be transient; re-run the script (idempotent skip of already-linked rows)
    - Transaction errors → read the specific row's error log, likely a data issue in the CSV

    Immediately run `audit-112-candidates.ts --dry-run` to compare stub counts:
    ```
    cd ev-accounts/backend && npx tsx scripts/audit-112-candidates.ts
    ```

    Expected: Monroe County 2026-05-05 primary stub count should have dropped by the number of successfully imported rows (see import log's `Imported: X` line).

    Sanity SQL to confirm source tagging:
    ```
    psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politicians WHERE source='phase_117_monroe_stub_import'"
    ```

    Expected: matches `Imported: X` from the log.

    Confirm race_candidates.politician_id linkage:
    ```
    psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.politicians p ON p.id = rc.politician_id WHERE p.source='phase_117_monroe_stub_import'"
    ```

    Expected: same count as above.

    Confirm politician_images rows landed for every row that had a non-null photo_custom_url:
    ```
    psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM essentials.politicians p LEFT JOIN essentials.politician_images pi ON pi.politician_id = p.id WHERE p.source='phase_117_monroe_stub_import' AND p.photo_custom_url IS NOT NULL AND pi.id IS NULL"
    ```

    Expected: 0. If non-zero, the transaction block skipped the politician_images INSERT for some rows — investigate and fix before Plan 07 runs.
  </action>
  <verify>
    <automated>cd ev-accounts/backend &amp;&amp; test -s ../../.planning/phases/117-candidate-stub-resolution-data-import/117-06-import.log &amp;&amp; grep -E "^Imported: [0-9]+" ../../.planning/phases/117-candidate-stub-resolution-data-import/117-06-import.log &amp;&amp; IMPORTED=$(grep -oE "Imported: [0-9]+" ../../.planning/phases/117-candidate-stub-resolution-data-import/117-06-import.log | tail -1 | awk '{print $2}') &amp;&amp; DB_COUNT=$(psql "$DATABASE_URL" -tAc "SELECT COUNT(*) FROM essentials.politicians WHERE source='phase_117_monroe_stub_import'") &amp;&amp; [ "$IMPORTED" = "$DB_COUNT" ] &amp;&amp; LINKED=$(psql "$DATABASE_URL" -tAc "SELECT COUNT(*) FROM essentials.race_candidates rc JOIN essentials.politicians p ON p.id = rc.politician_id WHERE p.source='phase_117_monroe_stub_import'") &amp;&amp; [ "$IMPORTED" = "$LINKED" ]</automated>
  </verify>
  <acceptance_criteria>
    - Import log exists at `.planning/phases/117-candidate-stub-resolution-data-import/117-06-import.log`
    - Log contains an `Imported: N` summary line
    - `SELECT COUNT(*) FROM essentials.politicians WHERE source='phase_117_monroe_stub_import'` equals N
    - `SELECT COUNT(*)` of race_candidates linked via that source equals N (every imported politician has a race_candidates link)
    - No `RAISE ERROR` lines in the log; warnings (WARN/[skip]) are acceptable
    - Audit-112 stub count for Monroe 2026-05-05 dropped by N
  </acceptance_criteria>
  <done>Real data landed in production DB; stub count reduced; import log committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| feasibility CSV → script input | Operator-authored but untrusted shape — validate before insert |
| script → Supabase Storage | Authenticated write via SUPABASE_SERVICE_ROLE_KEY (Plan 05 helper) |
| script → essentials.politicians/offices/race_candidates | Privileged writes via DATABASE_URL |
| source photo URL → fetch() | Third-party HTTP download; bytes land in Storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-117-19 | Tampering | SQL injection via candidate name | mitigate | All INSERTs parameterized ($1, $2, ...) — Task 1 acceptance requires this. Matches seedPolitician.ts pattern. |
| T-117-20 | Integrity | Hallucinated bios enter DB | mitigate | Task 2 human checkpoint enforces D-07 single-source rule BEFORE Task 3 runs. No code can bypass Task 2. |
| T-117-21 | Tampering | XSS via bio_source_url stored then rendered | mitigate | bio_text is rendered as text (verified RESEARCH §Security). bio_source_url goes into existing contact-link render path which uses `<a href>` (not `dangerouslySetInnerHTML`). Script does NOT sanitize URLs further — existing frontend pattern is sufficient. |
| T-117-22 | Information disclosure | DATABASE_URL or SUPABASE_SERVICE_ROLE_KEY in logs | mitigate | Script never logs env vars. Plan 05 helper also audited. `117-06-import.log` captured + reviewed in SUMMARY. |
| T-117-23 | Repudiation | Import partially ran, can't tell which rows landed | mitigate | Transaction-per-candidate + `source='phase_117_monroe_stub_import'` tag — verification SQL at Task 3 distinguishes imported rows cleanly. Clean rollback possible via `DELETE FROM essentials.politicians WHERE source='phase_117_monroe_stub_import'` (after clearing race_candidates links). |
| T-117-24 | Spoofing | Wrong photo attached to wrong candidate | mitigate | Slug is derived from canonical name; Task 2 checkpoint reviews CSV row-by-row before Task 3 runs; photo upload path includes slug in storage key. |
| T-117-25 | DoS | Malicious source photo URL hangs fetch | accept | fetch() inherits Node default timeout; operator-facing CLI, not a prod service — hang is visible immediately and can be cancelled. |
</threat_model>

<verification>
- Task 1: script typechecks + dry-run exits 0
- Task 2: every bio human-reviewed against its source URL
- Task 3: DB count matches import log + race_candidates linked + audit-112 stub count dropped
</verification>

<success_criteria>
- CAND-02: politician records exist for every imported scope row
- CAND-03: each row has photo_custom_url (or NULL fallback) + corresponding essentials.politician_images row when photo was uploaded + bio_text + matching offices row
- CAND-05 data side: rows exist with is_active=true so Plan 04's LEFT JOIN picker returns them
- audit-112-candidates.ts reports stub count dropped by N for Monroe 2026-05-05
</success_criteria>

<output>
After completion, create `.planning/phases/117-candidate-stub-resolution-data-import/117-06-SUMMARY.md` with:
- Final imported count (N)
- Skipped count (already linked)
- Scaffold error count + list of affected race_candidate_ids
- Any rows demoted to D-06 fallback (no photo, no bio)
- Stub count for Monroe 2026-05-05 before/after
- Pointer to 117-06-import.log
</output>
