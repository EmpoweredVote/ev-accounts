---
phase: 159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2
plan: 05
type: execute
wave: 3
depends_on: ["159-01", "159-02", "159-03", "159-04"]
files_modified:
  - backend/migrations/NNNN_cull_mi_va_2026_house_primary_losers.sql
  - backend/scripts/159-cull-verify.sql
  - backend/data/seed-mi-2026-house/159-05-mi-cull.csv
  - backend/data/seed-va-2026-house/159-05-va-cull.csv
autonomous: false
requirements: [USHC2-06]
must_haves:
  truths:
    - "This plan is DATE-GATED: it MUST NOT run before 2026-08-05 (the day after the Aug-4 MI & VA primaries). The executor confirms the current date is ≥ 2026-08-05 before any cull work; if run early it STOPS at the gate"
    - "Each MI & VA district's non-advancing primary candidates are pruned via the two-path convention: essentials.politicians.is_active=false (reps feed) AND essentials.race_candidates.candidate_status='withdrawn' (elections feed) — NEVER hard-DELETE (record/stances/headshot preserved)"
    - "The advancing nominee(s) per district are confirmed from official MI SoS / VA Dept. of Elections results and remain candidate_status='active'; the provisional description marker is cleared/updated on the settled races"
    - "The VA independent/Libertarian + MI minor-party (Green/convention) slice is reconciled: certified late-filers added active, declared-but-non-filing candidates pruned two-path"
  artifacts:
    - path: "backend/data/seed-mi-2026-house/159-05-mi-cull.csv"
      provides: "Per-candidate advancing/withdrawn decision from official MI SoS Aug-4 results across all 13 MI districts"
      min_lines: 40
    - path: "backend/data/seed-va-2026-house/159-05-va-cull.csv"
      provides: "Per-candidate advancing/withdrawn decision from official VA DoE Aug-4 results + reconciled indep/Libertarian slice across all 11 VA districts"
      min_lines: 30
    - path: "backend/migrations/NNNN_cull_mi_va_2026_house_primary_losers.sql"
      provides: "Idempotent BEGIN/COMMIT two-path prune (is_active=false + candidate_status='withdrawn') for non-advancing MI+VA candidates; NO hard-DELETE"
      contains: "candidate_status"
    - path: "backend/scripts/159-cull-verify.sql"
      provides: "Read-only two-path prune assertions: every loser withdrawn+inactive both feeds; every advancing nominee active"
  key_links:
    - from: "essentials.race_candidates.candidate_status (loser → 'withdrawn')"
      to: "essentials.politicians.is_active (loser → false)"
      via: "the two-path prune (FL Phase 153 convention); both paths per loser"
      pattern: "candidate_status"
---

<objective>
DATE-GATED (≥ 2026-08-05) post-primary cull for MI + VA. After the Aug-4 MI & VA primaries, prune each district's non-advancing candidates via the two-path convention (is_active=false + candidate_status='withdrawn', NEVER hard-DELETE), confirm the advancing nominee(s) from official MI SoS / VA DoE results, clear the provisional marker on settled races, and reconcile the still-open independent/minor-party slice (VA indep deadline was Aug-4; MI minor-party conventions ran to Jul-16). Mirrors FL Phase 153 exactly, applied to both MI + VA.

Purpose: USHC2-06 (cull portion) — completes the provisional-then-cull pattern the seed plans (159-01..04) set up. This is the ONLY date-gated part of Phase 159; seeding was un-gated.
Output: A cull reconciliation CSV per state + one idempotent two-path prune migration + a cull-verify gate; after it runs, each MI+VA district shows only its Nov-3 advancing field.

CRITICAL: This plan CANNOT run before 2026-08-05. The first task is a blocking date-gate + official-results checkpoint. Do NOT attempt the cull early — official Aug-4 primary results do not exist before then and pruning without them would remove advancing nominees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-01-SUMMARY.md
@.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-03-SUMMARY.md

<interfaces>
<!-- Two-path prune (FL Phase 153 / STATE.md USHC-07 convention): retire a primary loser via
     essentials.politicians.is_active=false (reps feed) AND essentials.race_candidates.candidate_status
     ='withdrawn' (elections feed). NEVER hard-DELETE — preserves record/stances/headshot/FEC. The
     advancing nominee stays candidate_status='active'. -->
<!-- Provisional marker: the seed races carry description LIKE 'PROVISIONAL:%' (FL-151 D-04). On settle,
     UPDATE the description to a settled marker (or clear the PROVISIONAL prefix) for districts whose
     field is now final. -->
<!-- Open-field reconciliation: VA indep/3rd-party filing deadline was Aug-4 (results/filings now
     available) — add certified late-filers as active race_candidates (new records if needed, same
     -(51*10000+cd*100+seq) band), prune declared-but-non-filing two-path. MI minor-party (Green
     conventions to 7/16) — confirm convention nominees, prune non-nominated declared candidates. -->
<!-- MI-10 James (-26010) + MI-11 Stevens (-26011) remain absent from the active field (open seats);
     the cull does not resurrect them. Confirm the open-seat nominees per official results. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: DATE GATE + official Aug-4 primary results confirmation</name>
  <read_first>
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-CONTEXT.md (the cull sub-phase is date-gated ≥ 2026-08-05; this is the ONLY date-gated part)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (Pattern 3 two-path cull; Pitfall 1 independent-slice-not-final; Open Question 1 on whether a second date-gated pass is warranted)
  </read_first>
  <action>
    HARD DATE GATE — this is a blocking checkpoint; the executor MUST NOT proceed to Task 2/3 until it clears.
    1. Confirm today's date is ≥ 2026-08-05. If it is EARLIER, STOP the plan immediately: do NOT proceed to any cull task, mark 159-05 as blocked/deferred (documented, NOT a failure), and report "too early — halt 159-05". (The MI & VA congressional primaries are Aug 4, 2026; official results do not exist before Aug 5, and pruning without them would remove advancing nominees.)
    2. Pull official MI SoS (Michigan Bureau of Elections) Aug-4 primary results for all 13 US House districts + official VA Dept. of Elections Aug-4 primary results for all 11 US House districts.
    3. Confirm the advancing Nov-3 nominee(s) per district from those official results (not from news, not from incumbency).
    4. Confirm the still-open slice: which VA independents/Libertarians certified by the Aug-4 filing deadline, and which MI minor-party (Green) convention nominees were named by 7/16.
    5. Present the per-district advancing-nominee list + open-slice status to the operator for confirmation before any prune runs.
  </action>
  <verify>
    <automated>node -e "const d=new Date(); const gate=new Date('2026-08-05T00:00:00Z'); if(d<gate){console.error('DATE GATE NOT MET: today '+d.toISOString().slice(0,10)+' < 2026-08-05 — halt 159-05');process.exit(1)} console.log('DATE GATE MET: '+d.toISOString().slice(0,10)+' >= 2026-08-05')"</automated>
  </verify>
  <acceptance_criteria>
    - Current date confirmed ≥ 2026-08-05 (the automated date check exits 0; if earlier, it exits 1 and the plan STOPS here — documented, not a failure).
    - Official MI SoS + VA DoE Aug-4 primary results obtained for all 24 districts.
    - Per-district advancing nominee(s) confirmed from official results; open indep/minor-party slice status confirmed and operator-approved.
  </acceptance_criteria>
  <done>Date ≥ 2026-08-05 confirmed; official Aug-4 results obtained for all 24 districts; per-district advancing nominees + open-slice status operator-confirmed. Resume signal: operator types "results confirmed" with the advancing nominees, or "too early — halt 159-05" if before 2026-08-05.</done>
</task>

<task type="auto">
  <name>Task 2: Build the per-state cull reconciliation CSVs from official results</name>
  <read_first>
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-01-SUMMARY.md (the seeded MI field: per-district active candidates + external_ids)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-03-SUMMARY.md (the seeded VA field: per-district active candidates + external_ids)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (Pattern 3 two-path; Pitfall 1 indep-slice reconciliation; the MI-10/MI-11 open-seat nominees to confirm)
    - backend/src/lib/db.js (pg pool; resolve current active race_candidates per district)
  </read_first>
  <action>
    Using the Task-1 official results, produce two cull reconciliation CSVs:
    - `backend/data/seed-mi-2026-house/159-05-mi-cull.csv` (columns: cd, geo_id, full_name, politician_id/external_id, primary_result[ADVANCING|LOSER], action[KEEP-ACTIVE|WITHDRAW|ADD-LATE-FILER]) for all currently-active MI candidates + any certified MI minor-party convention nominee not yet seeded.
    - `backend/data/seed-va-2026-house/159-05-va-cull.csv` (same columns) for all currently-active VA candidates + any certified VA indep/Libertarian late-filer not yet seeded.

    Query the current active race_candidates per district (from 159-01/159-03 seeded state) and map each to ADVANCING or LOSER from the official results. Mark advancing nominee(s) KEEP-ACTIVE, non-advancers WITHDRAW. For the open slice: mark certified late-filers ADD-LATE-FILER (assign a collision-free external_id in the same state band), mark declared-but-non-filing candidates WITHDRAW. Confirm MI-10 + MI-11 open-seat nominees are among the ADVANCING set and James/Stevens remain absent.

    Cross-check every WITHDRAW/KEEP decision against the official result — never infer from incumbency or party.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx -e "import {readFileSync} from 'fs'; for(const f of ['data/seed-mi-2026-house/159-05-mi-cull.csv','data/seed-va-2026-house/159-05-va-cull.csv']){const l=readFileSync(f,'utf8').trim().split('\n').slice(1); if(l.length<1){console.error('FAIL empty',f);process.exit(1)}; const acts=new Set(l.map(r=>r.split(',').pop().trim())); for(const a of acts){if(!['KEEP-ACTIVE','WITHDRAW','ADD-LATE-FILER'].includes(a)){console.error('FAIL bad action',a,'in',f);process.exit(1)}}; console.log(f,'rows',l.length,'actions',[...acts])}"</automated>
  </verify>
  <acceptance_criteria>
    - Both cull CSVs exist, one row per currently-active candidate (+ any ADD-LATE-FILER), each mapped to an official-results-backed action (KEEP-ACTIVE / WITHDRAW / ADD-LATE-FILER).
    - Every district has ≥1 KEEP-ACTIVE (advancing nominee); MI-10 + MI-11 open-seat nominees present as KEEP-ACTIVE; James/Stevens absent throughout.
    - Actions derived from official MI SoS / VA DoE results, never from incumbency/party.
  </acceptance_criteria>
  <done>Both cull reconciliation CSVs built from official Aug-4 results; every action official-results-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Author + run the two-path prune migration + author + run 159-cull-verify.sql</name>
  <read_first>
    - backend/data/seed-mi-2026-house/159-05-mi-cull.csv + backend/data/seed-va-2026-house/159-05-va-cull.csv (Task 2 output — the authoritative prune/keep/add map)
    - .planning/phases/151-fl-candidate-seeding-provisional-qualified-field/151-03-SUMMARY.md (the FL seed shape — the reciprocal cull is the FL Phase 153 two-path convention referenced in STATE.md USHC-07)
    - backend/scripts/157-verify.sql (a per-phase gate template to model 159-cull-verify.sql's write-free labeled-assertion shape)
    - .planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-RESEARCH.md (Pattern 3 two-path prune: is_active=false + candidate_status='withdrawn'; NEVER hard-DELETE)
  </read_first>
  <action>
    Pick the next free migration number, name `NNNN_cull_mi_va_2026_house_primary_losers.sql`. All writes in one BEGIN; … COMMIT;. Header cites the official MI SoS / VA DoE results source + the two-path convention + the no-hard-DELETE rule.

    Migration body (per the two-path convention):
    1. For each WITHDRAW row: UPDATE essentials.race_candidates SET candidate_status='withdrawn' WHERE the candidate's active row (resolve by politician_id + race), AND UPDATE essentials.politicians SET is_active=false for that politician_id. NEVER hard-DELETE.
    2. For each ADD-LATE-FILER row: INSERT the new politician (NOT EXISTS on external_id) + INSERT the active race_candidate onto the district race (NOT EXISTS on race_id, full_name).
    3. For each KEEP-ACTIVE advancing nominee: leave candidate_status='active' (no change); UPDATE the settled district races' description to clear/replace the 'PROVISIONAL:%' marker (settled field).
    4. Idempotent: re-run makes no further changes (guard the withdrawn UPDATEs on candidate_status='active').

    Then author `backend/scripts/159-cull-verify.sql` (write-free, labeled assertions, model on 157-verify.sql): every LOSER is BOTH candidate_status='withdrawn' AND politician is_active=false (two-path, no single-feed leak — Pitfall 5); every district has ≥1 active advancing nominee; James -26010 / Stevens -26011 still absent from active; no candidate was hard-DELETEd (row still exists with withdrawn status); settled races no longer carry 'PROVISIONAL:%'.

    Run the migration dry first, then apply via psql -v ON_ERROR_STOP=1, then run 159-cull-verify.sql green (exit 0).
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/159-cull-verify.sql >/tmp/159cull.log 2>&1; rc=$?; if [ $rc -ne 0 ]; then echo "FAIL cull gate exit $rc"; tail -25 /tmp/159cull.log; exit 1; fi; grep -c 'PASS' /tmp/159cull.log; echo "CULL GATE GREEN exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - Cull migration applied: every WITHDRAW candidate is BOTH candidate_status='withdrawn' AND is_active=false (two-path); no candidate hard-DELETEd (record + stances + headshot preserved).
    - Every MI+VA district retains ≥1 active advancing nominee; ADD-LATE-FILER candidates active; James/Stevens still absent.
    - Settled races no longer carry the 'PROVISIONAL:%' marker.
    - 159-cull-verify.sql exists, write-free, exits 0 with all labeled assertions PASS.
    - Migration idempotent on re-run (0 further changes).
  </acceptance_criteria>
  <done>Two-path prune applied for MI+VA losers; advancing nominees confirmed active; late-filers reconciled; 159-cull-verify.sql green; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official Aug-4 results → prune decision | The advancing-vs-loser decision drives destructive-looking status changes on live prod; must come from official MI SoS / VA DoE results only |
| planner/executor → production DB | The cull migration flips candidate_status + is_active on live prod race_candidates/politicians |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-40 | Tampering | cull run before Aug-5 (no official results) prunes advancing nominees | mitigate | Blocking date-gate checkpoint (Task 1); executor confirms date ≥ 2026-08-05 or halts |
| T-159-41 | Tampering | one-feed prune (candidate_status only, is_active left true) → loser still surfaces | mitigate | Two-path prune both feeds per loser; 159-cull-verify.sql asserts both |
| T-159-42 | Availability | hard-DELETE loses record/stances/headshot | mitigate | NEVER hard-DELETE; status flip only; gate asserts withdrawn row still exists |
| T-159-43 | Tampering | advancing nominee wrongly withdrawn / loser wrongly kept | mitigate | Every action official-results-backed (Task 1/2); gate asserts ≥1 active per district |
| T-159-44 | Tampering | James/Stevens resurrected in open seat | mitigate | Cull does not add them; gate asserts -26010/-26011 still absent from active |
| T-159-SC | Tampering | npm/pip installs | accept | No packages installed |
</threat_model>

<verification>
- 159-cull-verify.sql exits 0, all PASS: two-path prune complete, ≥1 active per district, James/Stevens absent, no hard-DELETE, provisional marker cleared on settled races.
- Manual: spot-check 3 MI + 3 VA districts against official Aug-4 results.
</verification>

<success_criteria>
- Date ≥ 2026-08-05 confirmed; every MI+VA primary loser pruned two-path (never DELETEd); advancing nominees active; indep/minor slice reconciled; provisional markers cleared; cull gate green.
</success_criteria>

<output>
Create `.planning/phases/159-mi-candidate-seeding-verification-date-gated-primary-aug-4-2/159-05-SUMMARY.md` when done (record: date run, migration number, per-district advancing nominees, count withdrawn per state, any ADD-LATE-FILER records, James/Stevens confirmed absent, provisional-marker clearing, cull gate PASS log — this feeds the 159-06 final gate).
</output>
