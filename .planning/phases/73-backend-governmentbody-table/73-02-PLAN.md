---
phase: 73-backend-governmentbody-table
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - essentials/src/lib/classify.js
  - essentials/src/utils/sorters.js
autonomous: true
requirements: [DATA-02, DATA-03]

must_haves:
  truths:
    - "Monroe County Commissioners (office title 'Monroe County Commission - District X') are classified as 'County Legislators' not 'County Officials'"
    - "All three consumer structures (LOCAL_ORDER, CATEGORY_DISPLAY_NAMES, GROUP_SORT_OPTIONS) contain 'County Legislators' consistently"
    - "No other officials are reclassified by the keyword addition"
  artifacts:
    - path: "essentials/src/lib/classify.js"
      provides: "COUNTY branch with 'commission' keyword"
      contains: "commission"
    - path: "essentials/src/utils/sorters.js"
      provides: "GROUP_SORT_OPTIONS with County Legislators entry"
      contains: "County Legislators"
  key_links:
    - from: "essentials/src/lib/classify.js"
      to: "essentials/src/utils/sorters.js"
      via: "group key 'County Legislators' used by both files"
      pattern: "County Legislators"
---

<objective>
Fix Monroe County Commissioner classification by adding "commission" to the COUNTY branch keyword list in classify.js, and verify all three consumer structures are consistent.

Purpose: Monroe County Commissioners currently fall through to "County Officials" because their office title contains "commission" (not "commissioner"). This fix routes them to the correct "County Legislators" group alongside County Council members.
Output: Updated classify.js with "commission" keyword, verified sorters.js consistency.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-backend-governmentbody-table/73-RESEARCH.md
@.planning/phases/72-db-audit/72-FINDINGS.md

<interfaces>
<!-- Key code the executor needs to understand. -->

From essentials/src/lib/classify.js (COUNTY branch at line 196-207):
```javascript
// County officials - treat as Local
if (dt === "COUNTY") {
    if (hasAny(title, ["commissioner", "supervisor", "council"])) {
      return { tier: "Local", group: "County Legislators" };
    }
    if (hasAny(title, ROLE_LOCAL_EXEC)) {
      return { tier: "Local", group: "County Executives" };
    }
    if (hasAny(title, ["sheriff", "clerk", "treasurer", "assessor", "auditor", "recorder", "coroner", "surveyor"])) {
      return { tier: "Local", group: "County Officials" };
    }
    return { tier: "Local", group: "County Officials" };
}
```

From essentials/src/lib/classify.js (LOCAL_ORDER array — contains the display order of groups):
- Must contain "County Legislators"

From essentials/src/lib/classify.js (CATEGORY_DISPLAY_NAMES object — maps group keys to display labels):
- Must contain "County Legislators"

From essentials/src/utils/sorters.js (GROUP_SORT_OPTIONS at lines 322-338):
- Must contain "County Legislators" sort options entry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "commission" keyword to classify.js COUNTY branch and verify consumer structures</name>
  <files>essentials/src/lib/classify.js, essentials/src/utils/sorters.js</files>
  <action>
1. In classify.js, find the COUNTY branch (line ~197). Change the hasAny keyword list from:
```javascript
if (hasAny(title, ["commissioner", "supervisor", "council"])) {
```
to:
```javascript
if (hasAny(title, ["commissioner", "commission", "supervisor", "council"])) {
```

This adds "commission" to match office titles like "Monroe County Commission - District 1" which contain "commission" but NOT "commissioner". The COUNTY branch only runs when `dt === "COUNTY"`, so federal/state agencies with "commission" in their name are unaffected (they have different district_type values).

2. Verify (do NOT modify unless missing) that these three structures all contain "County Legislators":
   - `LOCAL_ORDER` array in classify.js — must list "County Legislators"
   - `CATEGORY_DISPLAY_NAMES` object in classify.js — must map "County Legislators" to a display name
   - `GROUP_SORT_OPTIONS` in sorters.js — must have a "County Legislators" entry with sort options

Per DATA-03, all three consumer structures must be consistent. Since "County Legislators" already exists in all three (no new group key is being added), this is a verification step, not a modification step. If any of the three is missing "County Legislators", add it.

3. Side-effect verification: The word "commission" also appears in the LOCAL branch's chamber check: `hasAny(chamber, ["board of supervisors", "county council", "county commission"])`. This is unaffected because it checks `chamber` not `title`, and the COUNTY branch runs before the LOCAL branch (COUNTY officials never reach LOCAL). Confirm this ordering is preserved.

IMPORTANT: This change does NOT introduce a new group key. Commissioners move from "County Officials" (fallback) to "County Legislators" (existing group). No new entries needed in LOCAL_ORDER, CATEGORY_DISPLAY_NAMES, or GROUP_SORT_OPTIONS.
  </action>
  <verify>
    <automated>cd /Users/chrisandrews/Documents/GitHub && grep -n "commission" essentials/src/lib/classify.js | head -10 && echo "---" && grep -c "County Legislators" essentials/src/lib/classify.js essentials/src/utils/sorters.js</automated>
  </verify>
  <done>"commission" appears in the COUNTY branch keyword list in classify.js. "County Legislators" appears in all three consumer structures (LOCAL_ORDER, CATEGORY_DISPLAY_NAMES in classify.js; GROUP_SORT_OPTIONS in sorters.js). No new group key was introduced.</done>
</task>

</tasks>

<verification>
1. `grep -n '"commission"' essentials/src/lib/classify.js` — confirms "commission" is in the COUNTY branch keyword array
2. `grep -c "County Legislators" essentials/src/lib/classify.js` — should be >= 3 (LOCAL_ORDER + CATEGORY_DISPLAY_NAMES + COUNTY branch return)
3. `grep -c "County Legislators" essentials/src/utils/sorters.js` — should be >= 1 (GROUP_SORT_OPTIONS)
4. Regression check: The keyword "commission" only affects the COUNTY branch (`dt === "COUNTY"`). No federal or state officials have dt=COUNTY, so no side effects.
</verification>

<success_criteria>
- classify.js COUNTY branch includes "commission" in hasAny keyword list
- An official with title "Monroe County Commission - District 1" and dt="COUNTY" would now return {tier: "Local", group: "County Legislators"}
- All three consumer structures (LOCAL_ORDER, CATEGORY_DISPLAY_NAMES, GROUP_SORT_OPTIONS) contain "County Legislators"
- No other group key changes introduced
</success_criteria>

<output>
After completion, create `.planning/phases/73-backend-governmentbody-table/73-02-SUMMARY.md`
</output>
