---
phase: 103-state-remediation-ca-md
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/data/stance-research/2026-06-06-md-officials.csv
  - supabase/migrations/20260606000004_271_md_officials_stances.sql
  - .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md
  - .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md
autonomous: false
requirements:
  - STAX-02
  - QUAL-01
tags:
  - inform
  - sources
  - md
  - state-executives
  - research-stances
  - migration
  - fresh-research

must_haves:
  truths:
    - "All 5 MD executive branch officials (Wes Moore, Aruna Miller, Anthony G. Brown, Brooke Lierman, Dereck E. Davis) have had stances researched from scratch using the research-stances skill under the Chair methodology"
    - "Each MD official was researched in a SEPARATE agent invocation — never batched into a single agent prompt (per MEMORY.md rate-limit rule; CONTEXT.md D-04 sequential-only rule)"
    - "Maximum 2 researcher agents ever concurrent — default is ONE at a time (MEMORY.md rate-limit feedback)"
    - "Every stance value added has a paired inform.politician_context row with at least one specific (non-homepage) primary source URL"
    - "Stance scale for the research run was fetched fresh from inform.compass_topics + inform.compass_stances via SKILL.md Step 0 query — the full 44-topic live JSON was embedded in every agent prompt (per MEMORY.md feedback: stance scale embed texts; never hardcode)"
    - "City-level topics from the SKILL.md skip list (transportation-priorities, economic-development, homelessness-response, residential-zoning, city-sanitation, local-immigration, rent-regulation, growth-and-development, local-environment, public-safety-approach, jail-capacity, judicial-*) were SKIPPED for MD state-level executives per 103-RESEARCH.md Plan 03 Scope section — these officials have NO existing stances, so the skip rule applies fresh (no existing-stance exception)"
    - "Migration filename uses migration number verified via SELECT MAX(version) FROM supabase_migrations.schema_migrations BEFORE write (expected next: 271 after Plan 02 applies 270; abort if conflict)"
    - "Migration file applied to live DB via psql session pooler — never `supabase db push`"
    - "Migration is INSERT-only (with ON CONFLICT DO UPDATE for safety) — NO DELETE statements (MD officials had zero existing stances per Phase 100 audit)"
    - "Migration uses PLAIN OVERWRITE in the ON CONFLICT clause (`sources = EXCLUDED.sources`) — NOT ARRAY_CAT — because no prior context rows exist for MD officials (per 103-PATTERNS.md INSERT-only subset)"
    - "Post-migration V3 query (MD officials with zero stances) returns 0 — every official has > 0 stances"
    - "Post-migration MD-sourced-check query returns 0 — every MD stance row has a paired context row with at least one non-blank non-homepage source URL"
    - "Every value added was verified against the specific Chair text for that topic — QUAL-01 applied (no party inference per MEMORY.md)"
    - "NO deletion log entries produced — per CONTEXT.md D-04, MD has zero existing stances so QUAL-02 deletion log does not apply to this plan"
  artifacts:
    - path: "backend/data/stance-research/2026-06-06-md-officials.csv"
      provides: "research-stances CSV output for 5 MD officials × applicable live topics (Chair methodology fresh research)"
      contains: "full_name"
    - path: "supabase/migrations/20260606000004_271_md_officials_stances.sql"
      provides: "SQL migration that INSERTs fresh stances for 5 MD officials into inform.politician_answers + inform.politician_context (no DELETE statements; no ARRAY_CAT needed)"
      contains: "inform.politician_context"
    - path: ".planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md"
      provides: "Research batch log — per-official topic coverage, topics with value (sourced), topics skipped (no evidence — NOT deletion candidates, just missing-data acceptances)"
      contains: "Live stance scale"
    - path: ".planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md"
      provides: "Post-migration verification record — V3 (every MD official has > 0 stances) + sourced-check (every MD stance has at least one non-homepage URL)"
      contains: "STAX-02"
  key_links:
    - from: "backend/data/stance-research/2026-06-06-md-officials.csv"
      to: "supabase/migrations/20260606000004_271_md_officials_stances.sql"
      via: "CSV rows become INSERT statements (no DELETEs)"
      pattern: "INSERT INTO inform\\.politician_(answers|context)"
    - from: "supabase/migrations/20260606000004_271_md_officials_stances.sql"
      to: "inform.politician_answers + inform.politician_context"
      via: "INSERT with ON CONFLICT DO UPDATE SET sources = EXCLUDED.sources (PLAIN OVERWRITE — no prior context rows for MD)"
      pattern: "sources   = EXCLUDED.sources"
    - from: ".planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md"
      to: "STAX-02 success criteria"
      via: "V3 query confirms every MD official has > 0 stances; sourced-check confirms every MD stance has a real source URL"
      pattern: "md_(zero_stance_count|unsourced_count)"
---

<objective>
Research stances from scratch for 5 Maryland executive branch officials (Wes Moore — Governor, Aruna Miller — Lt. Governor, Anthony G. Brown — Attorney General, Brooke Lierman — Comptroller, Dereck E. Davis — Treasurer) across every applicable live compass topic using the research-stances skill under the Chair methodology. These officials were added in migrations 269–271 (per ROADMAP STAX-02 description) and have zero existing stances per the Phase 100 audit. Write and apply a single SQL migration (number 271 verified at write time, after Plan 02 has consumed 270) that INSERTs every found stance into inform.politician_answers + inform.politician_context with at least one specific (non-homepage) source URL. NO deletion log is produced (no existing stances to delete per CONTEXT.md D-04). Run the V3 verification (every MD official has > 0 stances) AND a sourced-check verification (every MD stance has a paired context row with a non-blank non-homepage URL).

Purpose: Implements STAX-02 — MD officials in DB have stances researched and added from scratch with real source URLs. This is fundamentally different from CA remediation (Plan 02): no existing stances means no DELETEs, no ARRAY_CAT needed, no deletion log. The structure is pure additive INSERT-only migration.

Plan 03 is INDEPENDENT of Plan 02 (no files_modified overlap — Plan 02 writes migration 270 and the CA CSV; Plan 03 writes migration 271 and the MD CSV). Wave 2 schedules both in parallel. The only sequential constraint is migration number ordering: Plan 03's migration MUST verify MAX(version) at write time AFTER Plan 02 has applied, OR use the next-available number determined dynamically. Plan 03 should run AFTER Plan 02 has applied if there is any timing ambiguity — but the executor can also dispatch them in parallel and let MAX(version) verification resolve the order at write time.

Implements: STAX-02 (MD fresh research), QUAL-01 (Chair-methodology value verification on every added value). QUAL-02 is NOT addressed by this plan — MD has no existing stances to delete.

Output:
- backend/data/stance-research/2026-06-06-md-officials.csv — research-stances CSV output (5 officials × applicable topics; filename date may be later if research runs after 2026-06-06)
- supabase/migrations/20260606000004_271_md_officials_stances.sql — INSERT-only migration (no DELETEs, plain overwrite ON CONFLICT)
- .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md — research batch log
- .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md — post-migration verification record
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-state-remediation-ca-md/103-CONTEXT.md
@.planning/phases/103-state-remediation-ca-md/103-RESEARCH.md
@.planning/phases/103-state-remediation-ca-md/103-PATTERNS.md
@.planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md
@.claude/skills/research-stances/SKILL.md
@supabase/migrations/20260606000002_269_house_source_remediation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify 5 MD UUIDs in DB, fetch live stance scale, build per-official topic scope, verify migration number</name>
  <files>.planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md</files>
  <read_first>
    - .planning/phases/103-state-remediation-ca-md/103-CONTEXT.md (locked MD UUIDs in the "Known MD scope (Phase 100 confirmed IDs)" table; D-04 MD research approach — sequential only, no deletion log)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Plan 03 Scope section — 5 officials × 44 topics, dispatch order, city-level topic skip rule applies fresh for MD)
    - .planning/phases/103-state-remediation-ca-md/103-PATTERNS.md (Pattern: MD Official Identification by UUID not name; migration 271 INSERT-only template)
    - .planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md (MD Officials section — confirms 5 officials, 0 existing stances; cites the locked UUIDs)
    - .claude/skills/research-stances/SKILL.md (Step 0 topic-resolution query; rate-limit rule one-at-a-time; FIVE-CHAIRS framing; city-level topic skip list for state/federal candidates)
    - .planning/STATE.md "v2.7 Source Integrity Patterns" (Chair methodology rules)
    - C:\Users\Chris\.claude\projects\C--EV-Accounts\memory\MEMORY.md (rate-limit feedback; stance scale embed texts rule; no party-affiliation inference)
  </read_first>
  <action>
    STEP A — Verify each of the 5 locked MD UUIDs exists in essentials.politicians and resolve DB-canonical full_name + is_active:

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT id, full_name, is_active FROM essentials.politicians WHERE id = ANY (ARRAY['21e534c8-c0c0-42f5-b52b-5eb2f246d632','ea9fc2d6-3b26-469a-978c-e8c846d2d49a','60329719-1d5b-4bb4-8295-38ea18f6f378','b26fb5d2-90eb-4108-8ce5-838df719473d','75378a96-8886-46eb-b0c1-37cbe2579265']::uuid[]) ORDER BY full_name\"); console.log(JSON.stringify(rows, null, 2)); await pool.end();"

    Confirm exactly 5 rows returned, all is_active=true. Use the DB-canonical full_name strings for all downstream operations (research agent prompts, migration comments, verification). The UUIDs are the locked identifiers — never substitute name-based lookup per 103-RESEARCH.md Pattern 3.

    STEP B — Confirm zero existing stances for these 5 officials (sanity check the Phase 100 audit finding):

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT p.full_name, COUNT(pa.topic_id) AS stance_count FROM essentials.politicians p LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id WHERE p.id = ANY (ARRAY['21e534c8-c0c0-42f5-b52b-5eb2f246d632','ea9fc2d6-3b26-469a-978c-e8c846d2d49a','60329719-1d5b-4bb4-8295-38ea18f6f378','b26fb5d2-90eb-4108-8ce5-838df719473d','75378a96-8886-46eb-b0c1-37cbe2579265']::uuid[]) GROUP BY p.full_name ORDER BY p.full_name\"); console.log(JSON.stringify(rows, null, 2)); await pool.end();"

    Confirm every row has stance_count = 0. If any official already has stances, RECORD THE DEVIATION — Plan 03 must then be revised (possibly switch to additive INSERT with ON CONFLICT updating only missing topics). Default assumption from Phase 100 audit: all 5 are at 0.

    STEP C — Fetch the live stance scale via SKILL.md Step 0 query:

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT t.id, t.topic_key, t.title, t.question_text, json_agg(json_build_object('value', s.value, 'text', s.text) ORDER BY s.value) AS stances FROM inform.compass_topics t JOIN inform.compass_stances s ON s.topic_id = t.id WHERE t.is_live = true GROUP BY t.id, t.topic_key, t.title, t.question_text ORDER BY t.topic_key\"); console.log(JSON.stringify(rows, null, 2)); await pool.end();"

    Verify result contains AT LEAST 44 topic rows (per 103-RESEARCH.md State of the Art — confirmed as of 2026-06-02). If fewer, RECORD ACTUAL COUNT. Capture full JSON output verbatim.

    STEP D — Filter applicable topics for MD state-level executives. From the live topic list (Step C), EXCLUDE the SKILL.md city-level skip list:
      `transportation-priorities, economic-development, homelessness-response, residential-zoning, city-sanitation, local-immigration, rent-regulation, growth-and-development, local-environment, public-safety-approach, jail-capacity, judicial-*` (any topic_key starting with `judicial-`)

    Per 103-RESEARCH.md Plan 03 Scope: "the Phase 102 existing-stance exception for the city-level skip rule does NOT apply here" — MD officials have zero existing stances, so the standard skip rule applies fresh. State-level executives do not weigh in on city-tier topics within the compass framing.

    Record the applicable-topic count (expected: 44 minus ~12 city-level = ~32 applicable topics, but the actual count depends on the live topic list). The per-official research scope is `applicable_topic_keys`.

    STEP E — Verify next available migration number:

      psql "$DATABASE_URL" -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;"

    DATABASE_URL must be session pooler URL from backend/.env. Expected outcomes:
      - If Plan 02 has already applied (which it may have done in parallel): MAX = 270 → next = 271. Use 271.
      - If Plan 02 has NOT yet applied: MAX = 269 → next = 270. BUT 270 is reserved for Plan 02's CA migration. Plan 03 should use 271 (the number AFTER Plan 02's reservation). Record this carefully — Plan 03 should NOT race Plan 02 for the next sequential number.
      - If MAX > 270: another migration applied in between; use the actual next-available and record the conflict.

    The safe rule: Plan 03 always uses migration number 271 unless MAX(version) returns ≥ 271, in which case use MAX+1. Record the decision explicitly in 103-MD-RESEARCH-NOTES.md "Migration number" section.

    STEP F — Build per-official dispatch plan. Dispatch order per 103-RESEARCH.md Plan 03 Scope (by seniority):
      1. Wes Moore (Governor — `21e534c8-c0c0-42f5-b52b-5eb2f246d632`)
      2. Aruna Miller (Lt. Governor — `ea9fc2d6-3b26-469a-978c-e8c846d2d49a`)
      3. Anthony G. Brown (Attorney General — `60329719-1d5b-4bb4-8295-38ea18f6f378`)
      4. Brooke Lierman (Comptroller — `b26fb5d2-90eb-4108-8ce5-838df719473d`)
      5. Dereck E. Davis (Treasurer — `75378a96-8886-46eb-b0c1-37cbe2579265`)

    One agent per official, sequentially. Wait for each agent's CSV write before dispatching next. NEVER batch multiple officials into a single agent prompt — even though they share the same topic set, each must be its own agent invocation per MEMORY.md rate-limit rule + CONTEXT.md D-04.

    Write .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md with these sections:
      - "Scope": 5 MD officials × N applicable topics (record actual N), total possible stance assessments
      - "Officials to research": table with DB-canonical full_name, locked politician_id (UUID), office title, dispatch order — one row per official
      - "Applicable topics": the filtered list of topic_keys after excluding city-level skip topics; count + comma-separated list
      - "Excluded topics": the city-level skip list applied (transportation-priorities, economic-development, etc.) with the rationale ("state-level executives — SKILL.md city-tier topics do not apply; per 103-RESEARCH.md Plan 03 Scope, the existing-stance exception does NOT apply for MD because they have zero prior stances")
      - "Live stance scale": embed the full JSON payload from Step C with fetched-at timestamp header; cite actual live topic count
      - "Migration number": record the verified MAX(version) from Step E and the decision (use 271 unless MAX ≥ 271)
      - "Dispatch plan": explicit sequential order (Moore → Miller → Brown → Lierman → Davis), one agent at a time, max 2 concurrent (never batched into one agent prompt)
      - "Zero-stance baseline confirmation": result from Step B confirming all 5 officials have stance_count = 0 at pre-flight time

    Do NOT dispatch research-stances agents in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "Live stance scale" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "Dispatch plan" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "Applicable topics" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "Migration number" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "Wes Moore" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md && grep -q "21e534c8-c0c0-42f5-b52b-5eb2f246d632" .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md exists
    - File contains the section header `Scope` with explicit applicable topic count
    - File contains the section header `Officials to research` with exactly 5 rows
    - File contains all 5 DB-canonical names: `Wes Moore`, `Aruna Miller`, `Anthony G. Brown`, `Brooke Lierman`, `Dereck E. Davis`
    - File contains all 5 locked UUIDs: `21e534c8-c0c0-42f5-b52b-5eb2f246d632`, `ea9fc2d6-3b26-469a-978c-e8c846d2d49a`, `60329719-1d5b-4bb4-8295-38ea18f6f378`, `b26fb5d2-90eb-4108-8ce5-838df719473d`, `75378a96-8886-46eb-b0c1-37cbe2579265`
    - File contains the section header `Applicable topics` with a list excluding city-level topics
    - File contains the section header `Excluded topics` listing the SKILL.md city-tier skip rule contents
    - File contains the section header `Live stance scale` with JSON containing at least 44 topic entries (substring `"topic_key"` count >= 44)
    - File contains the section header `Migration number` recording MAX(version) and the chosen next number (default: 271)
    - File contains the section header `Dispatch plan` explicitly listing the sequential order Moore → Miller → Brown → Lierman → Davis
    - File contains a `Zero-stance baseline confirmation` section showing each of the 5 officials had stance_count = 0 at pre-flight time
  </acceptance_criteria>
  <done>Pre-flight notes committed. The 5 MD officials and their UUIDs are verified in the live DB at zero stances. The applicable-topic filter is applied (excluding city-tier topics). Live stance scale is captured. Migration number is determined. Task 2 can dispatch research-stances agents sequentially with deterministic input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run research-stances for 5 MD officials in seniority order (ONE agent at a time, never batched)</name>
  <files>backend/data/stance-research/2026-06-06-md-officials.csv</files>
  <read_first>
    - .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md (dispatch plan; applicable topics; live stance scale to embed)
    - .claude/skills/research-stances/SKILL.md (full skill — Step 1 dispatch rules, agent prompt template with FIVE-CHAIRS framing; city-level topic skip rule applies for state/federal)
    - .planning/STATE.md "v2.7 Source Integrity Patterns" (Chair methodology — value must match specific stance text)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Pitfall 4 MD Topic Count Assumption — fetch live count; Pitfall 5 ARRAY_CAT — DOES NOT APPLY to MD since no prior context rows)
    - .planning/phases/103-state-remediation-ca-md/103-CONTEXT.md (D-04 sequential rule — never parallel)
    - C:\Users\Chris\.claude\projects\C--EV-Accounts\memory\MEMORY.md (stance scale embed texts; one-at-a-time rate-limit rule; no party-affiliation inference)
  </read_first>
  <what-built>
    Plan 03 Task 1 produced 103-MD-RESEARCH-NOTES.md with the dispatch plan, the live stance scale (≥ 44 topics), the applicable-topic filter (excluding city-tier topics), and confirmation that all 5 MD officials have zero existing stances. This task dispatches the research-stances skill for each MD official in seniority sequence.
  </what-built>
  <action>
    Dispatch the research-stances skill for each of the 5 MD officials in the sequential order recorded in 103-MD-RESEARCH-NOTES.md "Dispatch plan":

      Order: 1) Wes Moore (Gov) → 2) Aruna Miller (Lt. Gov) → 3) Anthony G. Brown (AG) → 4) Brooke Lierman (Comptroller) → 5) Dereck E. Davis (Treasurer)

    Locked rules:
      - ONE researcher agent per official. NEVER batch multiple officials into a single agent prompt (CONTEXT.md D-04 locked rule; MEMORY.md rate-limit feedback rule).
      - Maximum 2 concurrent agents — default ONE at a time. Wait for each agent's CSV write to complete before dispatching the next.
      - Pass the agent's --topics argument with the FULL list of applicable_topic_keys from 103-MD-RESEARCH-NOTES.md "Applicable topics" section (the city-level skip list is already excluded — do NOT re-pass the skip list).
      - Pass the FULL live stance scale JSON (from 103-MD-RESEARCH-NOTES.md "Live stance scale") into every agent prompt per SKILL.md Step 1, formatted per the orchestrator note in SKILL.md (one block per topic with value→text mapping). DO NOT paraphrase, trim, or hardcode any stance text. Direction varies by topic — MEMORY.md feedback rule explicitly forbids hardcoded "5=progressive" assumptions.
      - Output filename: `backend/data/stance-research/YYYY-MM-DD-md-officials.csv` where YYYY-MM-DD is the actual run date. Each agent appends to the same file. Verify no duplicate header rows after each append (run `grep -c "^full_name,topic_key" {file}` — must always return 1).
      - MD executive officials have public records (Governor Moore has signed legislation, press conferences, campaign issue pages; AG Brown has filed amicus briefs and issued opinions; etc.). Skip topics ONLY where no real specific URL can be found (per CONTEXT.md D-04, no deletion log needed — skipped topics simply produce no row).
      - DO NOT infer values from party affiliation — locked rule per MEMORY.md feedback (e.g., do not assume Moore as a Democrat gets value=4 on abortion just because Democrats usually do; find his SPECIFIC documented position and match it to the exact stance text).

    Single research pass per CONTEXT.md D-06: no retry loops. If an agent skips a topic, that topic simply has no row in the CSV.

    After each agent completes, append a per-official log section to 103-MD-RESEARCH-NOTES.md: official full_name, topics researched (count), topics returned with value, topics skipped (no evidence), notes on any value choices that may surprise reviewers (e.g., a cross-party-direction value where the documented position contradicts party expectation).

    Do NOT modify the DB in this task — Task 3 (the migration) is the only DB-write step.
  </action>
  <how-to-verify>
    1. backend/data/stance-research/YYYY-MM-DD-md-officials.csv exists with exactly one header row — `grep -c "^full_name,topic_key" {file}` returns 1.
    2. Every data row has a non-blank source_url_1 (skipped topics produce no row).
    3. The full_name column contains only the 5 MD official DB-canonical names from 103-MD-RESEARCH-NOTES.md.
    4. At least one row exists for EACH of the 5 officials — verify: `awk -F',' 'NR>1 {print $1}' {file} | sort -u | wc -l` returns 5.
    5. No row uses a city-level topic_key from the SKILL.md skip list (transportation-priorities, economic-development, homelessness-response, residential-zoning, city-sanitation, local-immigration, rent-regulation, growth-and-development, local-environment, public-safety-approach, jail-capacity, judicial-*) — verify: `awk -F',' 'NR>1 {print $2}' {file} | grep -E "^(transportation-priorities|economic-development|homelessness-response|residential-zoning|city-sanitation|local-immigration|rent-regulation|growth-and-development|local-environment|public-safety-approach|jail-capacity|judicial-)" | wc -l` returns 0.
    6. Manual scan: pick 3 random rows from the CSV (preferably one each for Moore, Brown, and one other). For each, open source_url_1 — confirm the URL resolves to a SPECIFIC page (signed bill, official statement, press release, AG opinion, debate transcript, news article quoting the official). NOT a homepage matching `^https?://[^/]+/?$`. NOT a 404. NOT a generic about-page. The page content must match the EXACT Chair text for the assigned value per QUAL-01.
    7. Confirm party-affiliation was NOT used as inference — spot-check at least one row whose value doesn't match the obvious Democratic Party direction.
    8. 103-MD-RESEARCH-NOTES.md has 5 per-official log sections recording topics researched, topics with value, topics skipped per official.
  </how-to-verify>
  <verify>
    <automated>ls backend/data/stance-research/*md-officials.csv 2>/dev/null | head -1 | xargs -I{} test -f {} && ls backend/data/stance-research/*md-officials.csv | head -1 | xargs -I{} sh -c "grep -c '^full_name,topic_key' {} | grep -q '^1$' && awk -F',' 'NR>1 {print \$1}' {} | sort -u | wc -l | grep -q '^5$'"</automated>
    <human-check>Spot-check 3 random source URLs from the CSV (one each for Moore, Brown, and one other). Each must resolve to a specific page (not a homepage matching `^https?://[^/]+/?$`, not a 404). Content must match the EXACT Chair text for the assigned value per QUAL-01. Confirm party-inference was not applied (at least one cross-party-direction row verified).</human-check>
  </verify>
  <acceptance_criteria>
    - A file matching `backend/data/stance-research/*md-officials.csv` exists
    - First line of the CSV contains tokens `full_name`, `topic_key`, `value`, `reasoning`, `source_url_1` (header row)
    - The CSV contains exactly one header line — `grep -c "^full_name,topic_key" {file}` returns 1
    - Zero CSV data rows have a blank source_url_1
    - The set of distinct full_name values in the CSV equals exactly the 5 DB-canonical MD official names — verifiable: `awk -F',' 'NR>1 {print $1}' {file} | sort -u | wc -l` returns 5
    - All 5 official full_names appear at least once each (no official has zero rows)
    - No CSV data row uses a city-level topic_key — verifiable: `awk -F',' 'NR>1 {print $2}' {file} | grep -E "^(transportation-priorities|economic-development|homelessness-response|residential-zoning|city-sanitation|local-immigration|rent-regulation|growth-and-development|local-environment|public-safety-approach|jail-capacity|judicial-)" | wc -l` returns 0
    - 103-MD-RESEARCH-NOTES.md has 5 per-official log sections (one per official) — verifiable: grep for each of the 5 names returns at least one match in addition to the Officials-to-research table
    - Human verification: 3 spot-checked source URLs each resolve to a specific page; content matches EXACT Chair text per QUAL-01; party-inference confirmed not applied
  </acceptance_criteria>
  <resume-signal>Reply "approved" once the CSV is reviewed and the 3 spot-check URLs pass QUAL-01. Reply with specific (full_name, topic_key) row identifiers to reject before Task 3 builds the migration.</resume-signal>
  <done>The research-stances CSV is complete and self-consistent for all 5 MD officials. Every CSV row has a real source URL. City-level topics are filtered out. Task 3 can convert CSV rows to migration INSERTs.</done>
</task>

<task type="auto">
  <name>Task 3: Write INSERT-only migration SQL (no DELETEs, plain overwrite ON CONFLICT)</name>
  <files>supabase/migrations/20260606000004_271_md_officials_stances.sql</files>
  <read_first>
    - backend/data/stance-research/YYYY-MM-DD-md-officials.csv (the source — Task 2 output)
    - .planning/phases/103-state-remediation-ca-md/103-MD-RESEARCH-NOTES.md (the 5 locked UUIDs; the verified migration number from Task 1 Step E)
    - .planning/phases/103-state-remediation-ca-md/103-PATTERNS.md (full Pattern Map — Migration 271 INSERT-only section: header comment, INSERT-only UPSERT pattern, POST-STATE verification block, migration registration; explicit note "no ARRAY_CAT needed — MD officials have no prior context rows")
    - supabase/migrations/20260606000002_269_house_source_remediation.sql (structural template — copy header style, BEGIN/COMMIT, sources-array filter pattern, migration registration ON CONFLICT)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Pitfall 3 — re-verify MAX(version) at write time; Pattern 5 blank-URL array filter)
    - .planning/phases/103-state-remediation-ca-md/103-CONTEXT.md (D-04 — no deletion log for MD)
  </read_first>
  <action>
    STEP A — Re-verify migration number at write time (Pitfall 3 from 103-RESEARCH.md):

      psql "$DATABASE_URL" -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;"

    Expected outcomes:
      - If Plan 02 applied first: MAX = 270 → use 271 for this migration.
      - If Plan 02 has NOT applied yet: MAX = 269 → still use 271 for this migration (270 is reserved for Plan 02 even if it hasn't applied). Plan 03 reserves 271.
      - If MAX ≥ 271: another migration applied; ABORT and surface the conflict. Plan 03 must choose the next-available number and update filename.

    Migration filename: `supabase/migrations/20260606000004_271_md_officials_stances.sql`. Date prefix increments past Plan 02's `20260606000003`. If actual run date is later than 2026-06-06, use that date with a one-greater sequence suffix.

    STEP B — Write the migration. Single transaction (copy structure from 103-PATTERNS.md "Migration 271 INSERT-only" template):

      1. Header comment block:
         ```
         -- Phase 103: MD Officials Fresh Stances
         -- Requirements covered: STAX-02, QUAL-01
         -- Source CSV: backend/data/stance-research/YYYY-MM-DD-md-officials.csv
         -- No deletion log: MD officials had zero existing stances before this migration (CONTEXT.md D-04)
         --
         -- MD officials (UUIDs from Phase 100 audit — UUID literals, not name lookup):
         --   Wes Moore          → 21e534c8-c0c0-42f5-b52b-5eb2f246d632
         --   Aruna Miller       → ea9fc2d6-3b26-469a-978c-e8c846d2d49a
         --   Anthony G. Brown   → 60329719-1d5b-4bb4-8295-38ea18f6f378
         --   Brooke Lierman     → b26fb5d2-90eb-4108-8ce5-838df719473d
         --   Dereck E. Davis    → 75378a96-8886-46eb-b0c1-37cbe2579265
         --
         -- Migration number: 271 (verified MAX(version) at write time)
         -- Applied: YYYY-MM-DD via psql session pooler
         --
         -- Expected inserts: N (one per CSV row); no deletes; no ARRAY_CAT (no prior context rows for MD)
         ```

      2. `BEGIN;`

      3. INSERT block — for every row in the research CSV. For each (full_name, topic_key, value, reasoning, source_url_1, source_url_2, source_url_3):
         - Resolve politician_id via UUID literal from the locked mapping (Wes Moore = 21e534c8-..., etc.) — match full_name from CSV to the UUID via 103-MD-RESEARCH-NOTES.md "Officials to research" table.
         - Resolve topic_id via subquery: `(SELECT id FROM inform.compass_topics WHERE topic_key = 'literal_key')`
         - First statement (politician_answers):
             `INSERT INTO inform.politician_answers (politician_id, topic_id, value) VALUES ('uuid', subquery, N) ON CONFLICT (politician_id, topic_id) DO UPDATE SET value = EXCLUDED.value;`
         - Second statement (politician_context) — USE PLAIN OVERWRITE (`sources = EXCLUDED.sources`), NEVER ARRAY_CAT (MD has no prior context rows; ARRAY_CAT pattern is CA-specific per CONTEXT.md D-05 exception):
             `INSERT INTO inform.politician_context (politician_id, topic_id, reasoning, sources) VALUES ('uuid', subquery, 'reasoning text', ARRAY(SELECT u FROM unnest(ARRAY['url1','url2','url3']) AS u WHERE u IS NOT NULL AND trim(u) != '')) ON CONFLICT (politician_id, topic_id) DO UPDATE SET reasoning = EXCLUDED.reasoning, sources = EXCLUDED.sources;`
         - Sources array MUST exclude blank/NULL elements (103-RESEARCH.md Pattern 5).
         - Prefix each two-statement block with a comment: `-- ---- [Full Name] / [topic_key] / value=[N] ----`

      4. NO DELETE BLOCK — explicitly state this with a block comment per 103-PATTERNS.md template:
         ```
         -- ============================================================
         -- NO DELETE BLOCK — MD officials had zero existing stances
         -- (CONTEXT.md D-04: no deletion log needed for MD)
         -- ============================================================
         ```

      5. POST-STATE DO $$ RAISE NOTICE block — per 103-PATTERNS.md template:
         ```
         DO $$
         DECLARE v_zero_count integer; v_unsourced_count integer;
         BEGIN
           -- V3: MD officials still with zero stances (target: 0)
           SELECT COUNT(*) INTO v_zero_count
           FROM essentials.politicians p
           LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
           WHERE p.id = ANY (ARRAY[
             '21e534c8-c0c0-42f5-b52b-5eb2f246d632',
             'ea9fc2d6-3b26-469a-978c-e8c846d2d49a',
             '60329719-1d5b-4bb4-8295-38ea18f6f378',
             'b26fb5d2-90eb-4108-8ce5-838df719473d',
             '75378a96-8886-46eb-b0c1-37cbe2579265'
           ]::uuid[]) AND pa.topic_id IS NULL;
           RAISE NOTICE 'POST-MIGRATION MD officials with zero stances: %', v_zero_count;

           -- MD-sourced-check: MD stances with no real source URL (target: 0)
           SELECT COUNT(*) INTO v_unsourced_count
           FROM inform.politician_answers pa
           LEFT JOIN inform.politician_context pc
             ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
           WHERE pa.politician_id = ANY (ARRAY[
             '21e534c8-c0c0-42f5-b52b-5eb2f246d632',
             'ea9fc2d6-3b26-469a-978c-e8c846d2d49a',
             '60329719-1d5b-4bb4-8295-38ea18f6f378',
             'b26fb5d2-90eb-4108-8ce5-838df719473d',
             '75378a96-8886-46eb-b0c1-37cbe2579265'
           ]::uuid[])
           AND (
             pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL
             OR NOT EXISTS (SELECT 1 FROM unnest(pc.sources) s(u) WHERE u IS NOT NULL AND trim(u) != '')
           );
           RAISE NOTICE 'POST-MIGRATION MD stances without source URL: %', v_unsourced_count;
         END $$;
         ```

      6. Migration registration:
         `INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('271', '271_md_officials_stances') ON CONFLICT (version) DO NOTHING;`

      7. `COMMIT;`

    STEP C — Cross-checks before finalizing (record as comments in migration header):
      - Count of `INSERT INTO inform.politician_answers` statements equals the count of CSV data rows (one INSERT per CSV row)
      - Migration contains NO `DELETE FROM` statements anywhere
      - Migration contains NO `politician_context.sources || EXCLUDED.sources` substring (ARRAY_CAT is CA-specific; MD uses plain overwrite)
      - Every UUID literal in the migration matches one of the 5 locked MD UUIDs
      - No (politician_id, topic_id) pair appears in the INSERT block more than once (no duplicate inserts within the file)
  </action>
  <verify>
    <automated>ls supabase/migrations/*_271_md_officials_stances.sql 2>/dev/null | head -1 | xargs -I{} test -f {} && ls supabase/migrations/*_271_md_officials_stances.sql | head -1 | xargs grep -l "ON CONFLICT (politician_id, topic_id)" && ls supabase/migrations/*_271_md_officials_stances.sql | head -1 | xargs grep -c "DELETE FROM" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - A file matching `supabase/migrations/*_271_md_officials_stances.sql` exists (or one-higher numeric suffix if MAX(version) returned > 270 at write time)
    - Migration begins with `BEGIN;` and ends with `COMMIT;`
    - Migration contains the exact string `ON CONFLICT (politician_id, topic_id) DO UPDATE` (UPSERT for safety)
    - Migration contains `INSERT INTO inform.politician_answers` at least once per CSV data row
    - Migration contains `INSERT INTO inform.politician_context` at least once per CSV data row
    - Migration contains ZERO `DELETE FROM` statements — verifiable: `grep -c "^DELETE FROM" {migration}` returns 0 (CONTEXT.md D-04 — no deletions for MD)
    - Migration contains ZERO occurrences of `politician_context.sources || EXCLUDED.sources` (ARRAY_CAT is CA-specific per CONTEXT.md D-05; MD uses plain overwrite) — verifiable: `grep -c "politician_context.sources || EXCLUDED.sources" {migration}` returns 0
    - Migration contains the literal string `sources   = EXCLUDED.sources` OR `sources = EXCLUDED.sources` (plain overwrite pattern, multiple whitespace variants accepted)
    - Migration contains the literal string `INSERT INTO supabase_migrations.schema_migrations` with the verified version number (e.g., '271')
    - Migration contains the literal string `ARRAY(SELECT u FROM unnest(` (blank-URL filter pattern per 103-RESEARCH.md Pattern 5)
    - Migration contains the block-comment `NO DELETE BLOCK` (explicit confirmation that this is intentional)
    - Migration contains all 5 locked MD UUIDs as politician_id literals — verifiable: each of `21e534c8-c0c0-42f5-b52b-5eb2f246d632`, `ea9fc2d6-3b26-469a-978c-e8c846d2d49a`, `60329719-1d5b-4bb4-8295-38ea18f6f378`, `b26fb5d2-90eb-4108-8ce5-838df719473d`, `75378a96-8886-46eb-b0c1-37cbe2579265` returns at least 1 match via grep
    - Count of `INSERT INTO inform.politician_answers` lines in migration equals count of CSV data rows (cross-check recorded in migration header)
  </acceptance_criteria>
  <done>Migration file exists with INSERT-only structure, plain overwrite ON CONFLICT, no DELETEs, no ARRAY_CAT, and all 5 MD UUIDs as literals. Ready for Task 4 to apply via psql.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Apply migration via psql session pooler and run STAX-02 V3 + sourced-check verification</name>
  <files>.planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md</files>
  <read_first>
    - The migration file written in Task 3 (matches supabase/migrations/*_271_md_officials_stances.sql)
    - .planning/STATE.md "v2.2 Geospatial Patterns" (Migration apply method 2026-05-09 — direct psql via session pooler URL; `supabase db push` does NOT work)
    - .planning/ROADMAP.md "Phase 103: State Remediation — CA + MD" — success criteria 2 (STAX-02)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md "Verification SQL" section V3 (MD officials with zero stances query)
    - .planning/phases/102-federal-house-remediation/102-VERIFICATION.md (template format)
  </read_first>
  <action>
    This is the BLOCKING schema push step required by the phase planning gate. The migration in Task 3 modifies supabase/migrations/ and MUST be applied before verification passes.

    STEP A — Apply the migration to the live Supabase DB:

      psql "$DATABASE_URL" -f supabase/migrations/{NNNN}_271_md_officials_stances.sql

    DATABASE_URL must be the session pooler URL from backend/.env. `supabase db push` does NOT work — direct psql only.

    Capture full psql output (NOTICE lines from the migration's RAISE NOTICE block, row counts, any errors). If migration aborts with error, do NOT proceed — surface and stop.

    STEP B — After successful apply, run STAX-02 verification SQL via psql.

      Query V3 — STAX-02 success criterion 1: every MD official has > 0 stances. Use the exact query from 103-RESEARCH.md Verification SQL section:
        ```
        SELECT p.full_name, COUNT(pa.topic_id) AS stance_count
        FROM essentials.politicians p
        LEFT JOIN inform.politician_answers pa ON pa.politician_id = p.id
        WHERE p.id = ANY(ARRAY[
          '21e534c8-c0c0-42f5-b52b-5eb2f246d632',
          'ea9fc2d6-3b26-469a-978c-e8c846d2d49a',
          '60329719-1d5b-4bb4-8295-38ea18f6f378',
          'b26fb5d2-90eb-4108-8ce5-838df719473d',
          '75378a96-8886-46eb-b0c1-37cbe2579265'
        ]::uuid[])
        GROUP BY p.full_name
        ORDER BY stance_count ASC;
        ```
      Pass condition: every row shows stance_count > 0. NO official remains at 0.

      Sourced-check query — STAX-02 success criterion 2: every MD stance row has a paired context row with at least one non-blank non-homepage source URL:
        ```
        SELECT COUNT(*) AS md_unsourced_count
        FROM inform.politician_answers pa
        LEFT JOIN inform.politician_context pc
          ON pc.politician_id = pa.politician_id AND pc.topic_id = pa.topic_id
        WHERE pa.politician_id = ANY(ARRAY[
          '21e534c8-c0c0-42f5-b52b-5eb2f246d632',
          'ea9fc2d6-3b26-469a-978c-e8c846d2d49a',
          '60329719-1d5b-4bb4-8295-38ea18f6f378',
          'b26fb5d2-90eb-4108-8ce5-838df719473d',
          '75378a96-8886-46eb-b0c1-37cbe2579265'
        ]::uuid[])
        AND (
          pc.politician_id IS NULL OR pc.sources IS NULL OR array_length(pc.sources, 1) IS NULL
          OR NOT EXISTS (SELECT 1 FROM unnest(pc.sources) s(u) WHERE u IS NOT NULL AND trim(u) != '')
        );
        ```
      Pass condition: returns 0.

      Homepage-only check — every MD source URL must be a primary source, not homepage-only:
        ```
        SELECT COUNT(*) AS md_weak_source_count
        FROM inform.politician_context pc
        WHERE pc.politician_id = ANY(ARRAY[
          '21e534c8-c0c0-42f5-b52b-5eb2f246d632',
          'ea9fc2d6-3b26-469a-978c-e8c846d2d49a',
          '60329719-1d5b-4bb4-8295-38ea18f6f378',
          'b26fb5d2-90eb-4108-8ce5-838df719473d',
          '75378a96-8886-46eb-b0c1-37cbe2579265'
        ]::uuid[])
        AND NOT EXISTS (
          SELECT 1 FROM unnest(pc.sources) AS u(url)
          WHERE url IS NOT NULL AND trim(url) <> ''
            AND url !~ '^https?://[^/]+/?$'
        );
        ```
      Pass condition: returns 0.

    STEP C — Write .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md mirroring 102-VERIFICATION.md structure:

      - "Migration apply": command used, exit code, timestamp, psql NOTICE output summary
      - "STAX-02 Verification — Query V3 (MD officials with > 0 stances)": full SQL query text, per-official result table (full_name, stance_count), pass/fail
      - "STAX-02 Verification — Sourced check (every MD stance has real URL)": full SQL query text, result (`md_unsourced_count = 0`), pass/fail
      - "STAX-02 Verification — Homepage-only check (no weak sources)": full SQL query text, result (`md_weak_source_count = 0`), pass/fail
      - "Per-official stance count" table — DB-canonical full_name + stance_count + applicable_topic_count from 103-MD-RESEARCH-NOTES.md + topics_skipped count
      - "Cross-checks":
         - CSV row count = migration INSERT count = post-migration total MD stance count
         - Every CSV row's (politician_id, topic_key) pair is now in inform.politician_answers
         - Migration contained zero DELETE statements (sanity)
      - "QUAL-01 compliance": one-line confirmation that every value was Chair-text verified at Task 2 spot-check
      - "STAX-02 closure": explicit statement that MD fresh research is complete; cite per-official stance counts and confirm all 5 are > 0
      - "Note on QUAL-02": explicitly state that QUAL-02 deletion log does NOT apply to this plan (no existing stances to delete per CONTEXT.md D-04). QUAL-02 for the overall phase is satisfied by Plan 02's CA deletion log.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md && grep -q "STAX-02 Verification" .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md && grep -E "md_unsourced_count[[:space:]]*[:|=][[:space:]]*0" .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md && grep -E "md_weak_source_count[[:space:]]*[:|=][[:space:]]*0" .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md && grep -q "Per-official stance count" .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied to live DB via psql with exit code 0 — verifiable by running `psql "$DATABASE_URL" -c "SELECT MAX(version) FROM supabase_migrations.schema_migrations;"` post-apply and getting back `271` (or one greater than prior MAX if a higher number was used)
    - File .planning/phases/103-state-remediation-ca-md/103-MD-VERIFICATION.md exists
    - Verification file contains the section header `STAX-02 Verification`
    - Verification file contains a `Per-official stance count` table with all 5 MD official names
    - Verification file records Query V3 result with EVERY MD official showing stance_count > 0 (NOT 0)
    - Verification file records sourced-check result as `md_unsourced_count = 0` (every MD stance has at least one non-blank source URL)
    - Verification file records homepage-only check result as `md_weak_source_count = 0` (no MD stance has only homepage-pattern sources)
    - Verification file contains a `Cross-checks` section confirming CSV row count = migration INSERT count
    - Verification file contains a `Note on QUAL-02` explicitly stating that QUAL-02 deletion log does NOT apply to this plan (CONTEXT.md D-04)
    - Verification file contains a `STAX-02 closure` section confirming all 5 MD officials now have > 0 stances
  </acceptance_criteria>
  <done>Migration applied to live DB; STAX-02 satisfied (all 5 MD officials have > 0 stances; every stance has a real source URL); QUAL-01 compliance recorded. Plan 03 closes when this verification file is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research-stances agent → public web | Each researcher agent fetches public source URLs via WebFetch (MD official press pages, AG opinions, signed legislation, news quotes). No credentials cross. |
| Operator → live DB (write path) | Migration applies to inform.politician_answers + inform.politician_context via psql session pooler. DATABASE_URL sourced from backend/.env (gitignored). |
| Filesystem → repo | CSV + migration + research notes + verification all committed to git by the operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-09 | T (Tampering) | Migration SQL UUID literals | mitigate | All 5 MD politician_id values are locked UUID literals from CONTEXT.md (verified in Task 1 Step A against essentials.politicians). Never name-based lookup — Phase 100 execution noted middle-initial variants (`Anthony G. Brown` vs `Anthony Brown`) caused silent 0-row matches. |
| T-103-10 | I (Information disclosure) | research-stances reasoning column | mitigate | Reasoning text describes MD official public positions only — no PII, no internal commentary. CSV committed to public repo. |
| T-103-11 | T (Tampering) | Source URL injection via researcher output | mitigate | Migration uses `ARRAY(SELECT u FROM unnest(...) WHERE u IS NOT NULL AND trim(u) != '')` to filter blank/null URLs. Human-verify checkpoint (Task 2) spot-checks 3 random URLs for legitimacy. Homepage-only check in Task 4 confirms post-migration that no MD source URL is a bare homepage. |
| T-103-12 | D (Denial of service) | Migration transaction size | accept | Single transaction wrapping ≤ ~160 INSERT statements (worst case if all 5 officials × ~32 applicable topics each = 160). Acceptable for one-shot data migration. |
| T-103-13 | T (Tampering) | Race with Plan 02 for migration number | mitigate | Plan 03 always uses migration number 271 (Plan 02 reserves 270). Task 1 + Task 3 BOTH verify MAX(version) before writing. If conflict detected (MAX ≥ 271), abort and surface — never silently overwrite a sibling migration. |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. Uses already-installed pg, tsx, dotenv, psql (verified in 103-RESEARCH.md Package Legitimacy Audit). No legitimacy gate. |
</threat_model>

<verification>
- Migration file exists and applied cleanly via psql (exit 0)
- Migration is INSERT-only (zero DELETE statements; zero ARRAY_CAT occurrences)
- V3 SQL returns stance_count > 0 for every one of the 5 MD officials
- Sourced-check SQL returns 0 (every MD stance has a non-blank source URL)
- Homepage-only check returns 0 (no MD source URL is bare homepage)
- 103-MD-RESEARCH-NOTES.md and 103-MD-VERIFICATION.md committed
- 103-MD-VERIFICATION.md explicitly notes QUAL-02 does NOT apply (per CONTEXT.md D-04)
</verification>

<success_criteria>
1. `supabase/migrations/*_271_md_officials_stances.sql` is present and applied to live DB.
2. Every one of the 5 MD officials (Wes Moore, Aruna Miller, Anthony G. Brown, Brooke Lierman, Dereck E. Davis) has stance_count > 0 in inform.politician_answers post-migration.
3. Every MD stance has a paired context row with at least one non-blank non-homepage source URL.
4. Migration is INSERT-only (no DELETEs, no ARRAY_CAT — both confirmed by grep on migration file).
5. research-stances was dispatched ONE agent per official sequentially (Moore → Miller → Brown → Lierman → Davis), with the live 44-topic stance scale embedded in every prompt per MEMORY.md and SKILL.md.
6. Every added value was verified against the specific Chair text for that topic (QUAL-01) — no party inference.
7. NO deletion log entries produced — per CONTEXT.md D-04, MD has no existing stances to delete. QUAL-02 for the phase is satisfied by Plan 02's CA deletion log.
</success_criteria>

<output>
Create `.planning/phases/103-state-remediation-ca-md/103-03-SUMMARY.md` when done. The summary must record:
- Per-official final stance counts (full_name, stance_count, topics_skipped_count)
- Total MD stance rows added
- Migration number applied (expected: 271)
- STAX-02 V3 verification result (every MD official > 0 stances — pass/fail)
- STAX-02 sourced-check result (md_unsourced_count = 0 — pass/fail)
- STAX-02 homepage-only check result (md_weak_source_count = 0 — pass/fail)
- Confirmation that STAX-02 and QUAL-01 are satisfied
- Explicit note that QUAL-02 deletion log does NOT apply to this plan (CONTEXT.md D-04)
- Any (official, topic) pairs where research found surprising values (e.g., cross-party direction) — flag for future audit spot-check
- Total topics skipped per official (acceptable per CONTEXT.md D-06 single-pass rule — these simply have no stance row)
</output>
</content>
