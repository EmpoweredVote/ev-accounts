---
phase: 103-state-remediation-ca-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/run-ca-source-triage.ts
  - .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md
  - .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv
autonomous: true
requirements:
  - STAX-01
tags:
  - audit
  - inform
  - sources
  - data-quality
  - ca
  - state-legislators
  - triage

must_haves:
  truths:
    - "A TypeScript triage script exists at backend/scripts/run-ca-source-triage.ts and runs to exit 0 against the live DB"
    - "Running the script produces .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md (human-readable executive summary + per-politician detail)"
    - "Running the script produces .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv (machine-readable, columns: full_name, politician_id, state, district_type, party, total_stances, unsourced_count, weak_count, affected_topic_keys, classification)"
    - "Triage uses the SOURCED_CASE / UNSOURCED_CASE four-rule definition copied verbatim from backend/scripts/run-house-source-triage.ts (locked sourced definition from Phase 100 D-01)"
    - "Weak-source detection uses the HOMEPAGE_ONLY_REGEX literal '^https?://[^/]+/?$' copied verbatim from run-house-source-triage.ts (per Phase 101 D-01)"
    - "CA scope enforced via essentials.offices JOIN essentials.districts WHERE d.state = 'CA' AND d.district_type IN (verified set from Task 1 — STATE_LOWER, STATE_UPPER, STATE_EXEC, STATE_BOARD or whatever is in the live DB) using a DISTINCT ON (p.id) subquery"
    - "Triage HAVING clause is `(SUM(unsourced_case) > 0 OR SUM(weak_case) > 0)` — captures both unsourced AND weak-source politicians per CONTEXT.md D-01"
    - "Floor confirmation: at least 9 CA politicians appear in the targets CSV (Phase 100 known floor: Gavin Newsom, Katy Hall, Tracy Miller, Candice B. Pierucci, Cody Harris, Juan Carrillo, Lisa Calderon, Roland Gutierrez — Mike Braun excluded by state='CA' filter)"
    - "All inform.* queries go through pg.Pool — never PostgREST (inform schema not in PostgREST exposed list)"
    - "Plan 02 can size its work by reading 103-CA-TARGETS.csv — flagged politician count + per-politician affected_topic_keys are deterministic inputs"
  artifacts:
    - path: "backend/scripts/run-ca-source-triage.ts"
      provides: "CA state politician unsourced + weak-source triage script (STAX-01 scoping)"
      min_lines: 200
      contains: "pool.query"
    - path: ".planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md"
      provides: "Human-readable CA triage report — Plan 02 scope input"
      contains: "CA Triage"
    - path: ".planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv"
      provides: "Machine-readable target list with district_type column; affected topic keys per politician"
      contains: "affected_topic_keys"
  key_links:
    - from: "backend/scripts/run-ca-source-triage.ts"
      to: "inform.politician_answers + inform.politician_context"
      via: "LEFT JOIN on (politician_id, topic_id) inside pool.query() calls"
      pattern: "LEFT JOIN inform\\.politician_context"
    - from: "backend/scripts/run-ca-source-triage.ts"
      to: "essentials.districts WHERE d.state = 'CA'"
      via: "DISTINCT ON (politician_id) subquery joining offices and districts with state filter"
      pattern: "d\\.state = 'CA'"
    - from: "backend/scripts/run-ca-source-triage.ts"
      to: "homepage-only URL detection"
      via: "regex ~ '^https?://[^/]+/?$' on every non-blank element of pc.sources"
      pattern: "\\^https\\?://"
    - from: "backend/scripts/run-ca-source-triage.ts"
      to: ".planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md"
      via: "writeFileSync resolving phase dir via __dirname + path.resolve"
      pattern: "writeFileSync"
    - from: "backend/scripts/run-ca-source-triage.ts"
      to: ".planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv"
      via: "writeFileSync of CSV string"
      pattern: "103-CA-TARGETS\\.csv"
---

<objective>
Build a CA-state-politician source-triage script and run it against the live Supabase DB to produce the two Plan 02 scoping artifacts: `103-CA-TRIAGE-REPORT.md` (human-readable) and `103-CA-TARGETS.csv` (machine-readable). These artifacts size the Phase 103 Plan 02 research + migration work — Plan 02 needs them to know exactly which CA politicians and which topics to research (per Phase 101/102 D-03 pattern).

Purpose: Implements the scoping step of STAX-01. Per CONTEXT.md D-01 (locked), Plan 01 is dual-detection (unsourced + weak-source) across ALL CA state politicians — not just Assembly + Senate strictly. CONTEXT.md D-02 (locked) and Pitfall 1 of 103-RESEARCH.md require statewide executives (Gavin Newsom as governor) to appear in scope. Pre-flight pitfall (Open Question 1 + Pitfall 1 of 103-RESEARCH.md): the exact district_type values used for CA executive/statewide offices must be verified from the live DB before the CTE is finalized — STATE_EXEC is the presumed governor type but STATE_BOARD or another value may also apply. Plan 01 must NOT silently miss statewide officials.

Output:
- `backend/scripts/run-ca-source-triage.ts` — TypeScript triage script using `pg.Pool` direct queries (no PostgREST; inform schema not exposed)
- `.planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` — human-readable report (executive summary + per-politician detail sections)
- `.planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv` — machine-readable per-politician target list

Implements: STAX-01 (scoping step — actual remediation in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-state-remediation-ca-md/103-CONTEXT.md
@.planning/phases/103-state-remediation-ca-md/103-RESEARCH.md
@.planning/phases/103-state-remediation-ca-md/103-PATTERNS.md
@.planning/phases/102-federal-house-remediation/102-01-PLAN.md
@.planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md
@.planning/phases/100-source-coverage-audit/100-TARGET-LIST.csv
@backend/scripts/run-house-source-triage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify CA district_type values and live DB column schema</name>
  <files>.planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md</files>
  <read_first>
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Open Questions 1 and 2; Pitfall 1 — CA District Type Coverage; Assumptions A1 and A2)
    - .planning/phases/103-state-remediation-ca-md/103-CONTEXT.md (D-01 dual-detection rule; D-02 statewide executive inclusion)
    - backend/scripts/run-house-source-triage.ts (the direct template for the CA script; do NOT modify yet)
  </read_first>
  <action>
    This is a pre-flight verification task. Do NOT write the triage script yet — Task 2 builds it. Task 1 produces the empty/placeholder report file (touched, with a "## Pre-flight discoveries" header section that Task 3 will overwrite) AND records two pieces of live-DB ground truth in this section so Task 2 builds the correct CTE.

    Step A — Verify the district_type values used for CA in the live DB:

    Run this command from the backend directory (PowerShell uses backslashes for cd, but the project standard is to invoke via `cd backend` so use that):

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT DISTINCT d.district_type FROM essentials.districts d WHERE d.state = 'CA' ORDER BY 1\"); console.log(JSON.stringify(rows)); await pool.end();"

    Record the literal output (the JSON array of district_type values) in the pre-flight discoveries section of 103-CA-TRIAGE-REPORT.md. The Task 2 CTE must use `district_type IN (...)` with the verified subset of state-tier types (any that match the pattern STATE_* — STATE_LOWER, STATE_UPPER, STATE_EXEC, STATE_BOARD, STATEWIDE, or other variants). Exclude clearly non-state types (CITY_COUNCIL, NATIONAL_LOWER, NATIONAL_UPPER, COUNTY, LOCAL, LOCAL_EXEC, SCHOOL_DISTRICT — do not include those).

    Step B — Confirm essentials.districts has a `state` column (not state_code or embedded in name):

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT column_name FROM information_schema.columns WHERE table_name = 'districts' AND table_schema = 'essentials' AND column_name IN ('state','state_code','state_abbr') ORDER BY column_name\"); console.log(JSON.stringify(rows)); await pool.end();"

    Record the result. If `state` is not the column name, Task 2 must use whatever the live column is.

    Step C — Sanity-check the CA politicians scope before building the script:

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT d.district_type, COUNT(DISTINCT p.id) AS politician_count FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id AND o.is_vacant = false JOIN essentials.districts d ON d.id = o.district_id WHERE p.is_active = true AND d.state = 'CA' GROUP BY d.district_type ORDER BY d.district_type\"); console.log(JSON.stringify(rows)); await pool.end();"

    Record the per-district_type CA politician count. This tells you which district_types Task 2 must include (any STATE_* type with a non-zero count is in scope per D-02). Confirm that Gavin Newsom's row (politician_id `f26309c8-2525-49b2-bdaf-62980cbb1853`) is reachable via at least one of the included district_types — run a direct verification:

      cd backend && set -a && source .env && set +a && node --import tsx -e "import { pool } from './src/lib/db.js'; const { rows } = await pool.query(\"SELECT p.full_name, d.district_type, d.state FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id JOIN essentials.districts d ON d.id = o.district_id WHERE p.id = 'f26309c8-2525-49b2-bdaf-62980cbb1853'\"); console.log(JSON.stringify(rows)); await pool.end();"

    Record Newsom's district_type. If it is NOT one of STATE_LOWER/STATE_UPPER/STATE_EXEC/STATE_BOARD, Task 2's IN() list must include whatever Newsom's actual district_type is — otherwise Pitfall 1 fires and the governor is missed.

    Create the file .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md with this exact opening structure (Task 3 will overwrite it after the script runs; this scaffold lets Task 2 build the CTE deterministically):

      # Phase 103 — CA State Source Triage Report

      ## Pre-flight discoveries (Plan 01 Task 1)

      ### A. CA district_type values present in essentials.districts
      [JSON output from Step A]

      ### B. Available state-column on essentials.districts
      [JSON output from Step B]

      ### C. Per-district_type CA active politician counts
      [JSON output from Step C]

      ### D. Gavin Newsom district_type confirmation
      [JSON output from Step C2; cite his actual district_type]

      ### E. Final district_type IN() list for Task 2 CTE
      [Comma-separated list of district_type values Task 2 will use, derived from Steps A+C+D]

    Commit this file (Task 3 will overwrite the body below the pre-flight section after the script runs).
  </action>
  <verify>
    <automated>test -f .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md && grep -q "Pre-flight discoveries" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md && grep -q "Final district_type IN() list" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md exists
    - File contains all five section headers: `### A. CA district_type values present`, `### B. Available state-column`, `### C. Per-district_type CA active politician counts`, `### D. Gavin Newsom district_type confirmation`, `### E. Final district_type IN() list for Task 2 CTE`
    - Step A output is a non-empty JSON array containing at least one of the STATE_* tokens (`STATE_LOWER` OR `STATE_UPPER` OR `STATE_EXEC` OR `STATE_BOARD` OR `STATEWIDE`) — verifiable by grep on the file body
    - Step C output is a non-empty JSON array showing at least one CA politician per state-tier district_type listed in Step E
    - Step D output explicitly cites Gavin Newsom's district_type as one of the literal string values listed in Step E (Newsom MUST be in scope per CONTEXT.md D-02)
    - Step E list contains AT LEAST `STATE_LOWER` and `STATE_UPPER` (Assembly + Senate are non-negotiable) — verifiable: `grep -E "STATE_LOWER" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` AND `grep -E "STATE_UPPER" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` both return at least one match
    - Step E list explicitly excludes non-state types — verifiable: `grep -E "CITY_COUNCIL|NATIONAL_LOWER|NATIONAL_UPPER|SCHOOL_DISTRICT" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` returns no matches inside the Step E section
  </acceptance_criteria>
  <done>Pre-flight discoveries are recorded. The exact district_type IN() list for Task 2's CTE is documented and Gavin Newsom's inclusion is verified. Task 2 has deterministic ground truth to build the CA_POLITICIANS_CTE.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build CA triage script (run-ca-source-triage.ts)</name>
  <files>backend/scripts/run-ca-source-triage.ts</files>
  <read_first>
    - backend/scripts/run-house-source-triage.ts (full script — direct template; copy SOURCED_CASE/UNSOURCED_CASE/HOMEPAGE_ONLY_REGEX constants verbatim, copy dotenv/pool setup, copy the Query A/B/C structure and report builder, then adapt for single CA scope)
    - .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md (Task 1 pre-flight — Step E lists the verified district_type IN() values to use; Step D confirms Newsom inclusion)
    - .planning/phases/103-state-remediation-ca-md/103-PATTERNS.md (full Pattern Map — CA_POLITICIANS_CTE pattern, queryCASummary, queryCATargets, queryCATopicDetail; CSV header pattern with district_type column; output path resolution)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Plan 01 Template — CA Triage Script section; Pattern 1 CA Triage Script; Pitfall 1 CA District Type Coverage; Pitfall 2 Weak-Source Count Underestimate)
    - .planning/phases/100-source-coverage-audit/100-AUDIT-REPORT.md (locked sourced definition + weak-source homepage-only pattern)
    - .planning/phases/100-source-coverage-audit/100-TARGET-LIST.csv (floor — at least 9 CA politicians must appear in triage output)
  </read_first>
  <action>
    Create backend/scripts/run-ca-source-triage.ts as a standalone tsx-runnable script. Start by copying backend/scripts/run-house-source-triage.ts verbatim, then adapt the following:

    KEEP VERBATIM (do not modify):
    - The dotenv + path + fileURLToPath + pg + node:fs import block and __dirname resolution
    - The DATABASE_URL guard and pool construction
    - The DRY_RUN argv check
    - The SOURCED_CASE and UNSOURCED_CASE multi-line SQL CASE expression constants
    - The HOMEPAGE_ONLY_REGEX constant string `'^https?://[^/]+/?$'`
    - The TypeScript interface shapes SummaryRow / TargetRow / DetailRow (extend per the changes below)

    REPLACE the two CTEs (HOUSE_POLITICIANS_CTE + DEFERRED_CANDIDATES_CTE) with ONE single CTE:

    CA_POLITICIANS_CTE — SELECT DISTINCT ON (p.id) p.id, p.full_name, p.party, d.state, d.district_type FROM essentials.politicians p JOIN essentials.offices o ON o.politician_id = p.id AND o.is_vacant = false JOIN essentials.districts d ON d.id = o.district_id WHERE p.is_active = true AND d.state = 'CA' AND d.district_type IN (<comma-separated quoted list from Task 1 Step E>) ORDER BY p.id. Resulting subquery name: `ca_politicians`. Use the literal district_type list from 103-CA-TRIAGE-REPORT.md Step E — do NOT hardcode the IN() list; read it from the file or transcribe exactly as recorded.

    EXTEND data types:
    - Add `district_type: string` field to TargetRow and DetailRow interfaces (CA triage distinguishes Assembly / Senate / Executive in report sections).
    - Remove or rename the Phase 102 `scope: 'NATIONAL_LOWER' | 'NATIONAL_UPPER_DEFERRED'` field — Phase 103 uses `district_type` directly (per 103-PATTERNS.md Critical differences #4).

    REPLACE Query A (Summary) with ONE summary query using CA_POLITICIANS_CTE. Function name: queryCASummary(). Returns SummaryRow with total_politicians, total_stances, unsourced_stance_count, weak_stance_count. Use the exact shape from 103-PATTERNS.md "Summary query pattern" excerpt.

    REPLACE Query B (Per-target list) with ONE query — queryCATargets() against ca_politicians. Adds `district_type` to GROUP BY and SELECT. The HAVING (unsourced > 0 OR weak > 0) filter, the classification CASE (`unsourced_only` / `weak_only` / `both`), and the affected_topic_keys array_agg all stay identical to the house script. No `scope` literal column needed — use `cp.district_type` directly.

    REPLACE Query C (Topic detail rollup) with ONE query — queryCATopicDetail() against ca_politicians. Same shape as house version, GROUP BY adds district_type.

    REPORT BUILDER changes (buildReport):
    - PRESERVE the entire pre-flight discoveries section already in 103-CA-TRIAGE-REPORT.md (Task 1 output). The script must READ the existing file, extract the pre-flight section verbatim, and prepend it to the new generated report body. Detection: pre-flight section starts at the `## Pre-flight discoveries (Plan 01 Task 1)` heading and ends at the next `##` heading or EOF. If the file does not exist, abort with stderr error (Task 1 must have run first).
    - Generate one Executive Summary table — total CA politicians, total stances, unsourced count, weak count, flagged count.
    - Methodology block: cite Phase 100 D-01 (locked sourced definition) and Phase 101 D-01 (homepage-only weak-source pattern); mention CA dual-detection per CONTEXT.md D-01.
    - Two sections:
        a) "CA — Unsourced Stances" — table per politician with at least one unsourced stance
        b) "CA — Weak-Sourced Stances (homepage-only)" — table per politician with at least one weak-source stance
      (A politician can appear in both sections if they have a mix.)
    - Combined Target List table at the bottom, sorted by classification (both → unsourced_only → weak_only) then by full_name. Columns: full_name | district_type | party | unsourced_count | weak_count | classification | affected_topic_keys (pipe-joined preview, truncate at 80 chars).
    - Plan 02 Scoping Note: state the flagged politician count and the recommended Plan 02 batching tier per 103-RESEARCH.md "Plan 02 Sizing Guidelines":
        - flagged_count ≤ 10  → `Recommended batching: 1 research plan (≤ 10 threshold)`
        - 11 ≤ flagged_count ≤ 25 → `Recommended batching: 2 research plans (11–25 threshold — split alphabetically or by district_type)`
        - flagged_count ≥ 26 → `Recommended batching: 3+ research plans (26+ threshold — planner discretion)`

    CSV BUILDER changes (buildCsv):
    - Header row exactly: `full_name,politician_id,state,district_type,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
    - The `district_type` column is inserted between `state` and `party`. Values from cp.district_type.
    - affected_topic_keys remains pipe-delimited inside double quotes.
    - No `scope` column (per 103-PATTERNS.md Critical differences #4).

    OUTPUT PATHS:
    - phaseDir = path.resolve(__dirname, '..', '..', '.planning', 'phases', '103-state-remediation-ca-md')
    - reportPath = path.resolve(phaseDir, '103-CA-TRIAGE-REPORT.md')
    - csvPath = path.resolve(phaseDir, '103-CA-TARGETS.csv')

    DRY-RUN behavior:
    - Print to stderr the summary object: total_politicians, total_stances, unsourced_stance_count, weak_stance_count. Print first 5 target politician full_names (so a human can spot-check the floor of 9 known politicians without writing files).

    STDOUT summary line (non-dry-run):
    - Print one line: `Plan 02 scope: {flagged_count} CA politicians flagged ({U} unsourced + {W} weak-source stances) — recommended batching: {tier_message}`.

    EXIT HANDLING: same as house script — `await pool.end()` then `process.exit(0)` on success; on caught error, log to stderr and `process.exit(1)`.

    NOTE on Mike Braun (per 103-RESEARCH.md Live DB State note): If Braun appears in CA triage output, that is a data anomaly worth flagging. The d.state='CA' filter should naturally exclude him as Indiana Governor. No special-case code — just rely on the state filter.
  </action>
  <verify>
    <automated>test -f backend/scripts/run-ca-source-triage.ts && cd backend && npx tsx scripts/run-ca-source-triage.ts --dry-run 2>&1 | grep -E "(total_politicians|unsourced_stance_count|weak_stance_count)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/scripts/run-ca-source-triage.ts
    - `grep -v '^//' backend/scripts/run-ca-source-triage.ts | grep -v '^ *\*' | grep -c 'pool.query'` returns a value >= 3 (queryCASummary, queryCATargets, queryCATopicDetail all use pool.query())
    - Source contains the literal string `d.state = 'CA'`
    - Source contains the literal string `district_type IN (` (the IN clause — actual values come from Task 1 Step E)
    - Source contains both literal strings `'STATE_LOWER'` AND `'STATE_UPPER'` (Assembly + Senate are always in scope per acceptance — Task 1 Step E must include them)
    - Source contains the SOURCED_CASE expression including the substring `array_length(pc.sources, 1) IS NOT NULL` (locked definition copied verbatim from run-house-source-triage.ts)
    - Source contains the regex literal `^https?://[^/]+/?$`
    - Source contains at least one occurrence of `DISTINCT ON (p.id)` (single CTE — Phase 103 has only one)
    - Source contains the path literals `103-CA-TRIAGE-REPORT.md` and `103-CA-TARGETS.csv`
    - Source contains `--dry-run` argv handling
    - Source contains code that reads the existing 103-CA-TRIAGE-REPORT.md to preserve the `Pre-flight discoveries` section — verifiable: `grep -c "Pre-flight discoveries" backend/scripts/run-ca-source-triage.ts` returns >= 1
    - Source's TargetRow interface contains a `district_type` field — verifiable: `grep -A 12 "interface TargetRow" backend/scripts/run-ca-source-triage.ts | grep -c district_type` returns >= 1
    - Source does NOT contain the literal `NATIONAL_LOWER`, `NATIONAL_UPPER`, or `NATIONAL_UPPER_DEFERRED` (Phase 103 is CA state — not federal)
    - Dry-run command `cd backend && npx tsx scripts/run-ca-source-triage.ts --dry-run` exits with code 0
    - Dry-run stderr output includes the literal token `total_politicians` and a numeric value
    - Dry-run stderr output includes the literal token `unsourced_stance_count` and a numeric value
    - Dry-run stderr output includes the literal token `weak_stance_count` and a numeric value
  </acceptance_criteria>
  <done>The CA triage script exists, compiles under tsx, connects to the live DB, and prints the CA scope summary when run with --dry-run. Ready to invoke without --dry-run in Task 3 to write the artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Run triage script against live DB and commit artifacts</name>
  <files>
    .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md,
    .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv
  </files>
  <read_first>
    - backend/scripts/run-ca-source-triage.ts (the script built in Task 2)
    - .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md (Task 1 pre-flight section — must be preserved across the run)
    - .planning/phases/103-state-remediation-ca-md/103-RESEARCH.md (Live DB State — expected at least 9 CA politicians flagged per the Phase 100 floor; weak-source count expected to push the total higher)
    - .planning/phases/100-source-coverage-audit/100-TARGET-LIST.csv (the 9 known CA-floor politicians — Gavin Newsom 4 unsourced, Katy Hall 3, Tracy Miller 3, Candice B. Pierucci 1, Cody Harris 1, Juan Carrillo 1, Lisa Calderon 1, Roland Gutierrez 1; Mike Braun excluded by state filter)
  </read_first>
  <action>
    Run the script against the live Supabase DB to produce the two phase artifacts.

    Command: `cd backend && npx tsx scripts/run-ca-source-triage.ts` (no --dry-run flag — writes files).

    The script:
      - Reads the existing 103-CA-TRIAGE-REPORT.md, extracts the pre-flight discoveries section (Task 1 output), and prepends it verbatim to the new report body.
      - Writes the final merged report back to 103-CA-TRIAGE-REPORT.md.
      - Writes the per-politician CSV to 103-CA-TARGETS.csv.

    After the run, inspect both files. Confirm:
      - 103-CA-TRIAGE-REPORT.md still contains the `## Pre-flight discoveries (Plan 01 Task 1)` section unchanged from Task 1.
      - 103-CA-TRIAGE-REPORT.md contains an Executive Summary with total_politicians > 100 (CA has 80 Assembly + 40 Senate + statewide = ~125+ active politicians; if fewer, flag as a data anomaly).
      - 103-CA-TRIAGE-REPORT.md contains the "CA — Unsourced Stances" section and lists at least 8 CA politicians from the Phase 100 floor (Gavin Newsom, Katy Hall, Tracy Miller, Candice B. Pierucci, Cody Harris, Juan Carrillo, Lisa Calderon, Roland Gutierrez — Mike Braun excluded by CA filter).
      - 103-CA-TRIAGE-REPORT.md contains the "CA — Weak-Sourced Stances (homepage-only)" section — this is expected to include additional CA politicians not visible in the Phase 100 floor (since the 107 DB-wide weak-source rows are concentrated in the State tier).
      - 103-CA-TRIAGE-REPORT.md contains a "Plan 02 Scoping" section explicitly naming the batching tier (1 plan / 2 plans / 3+ plans).
      - 103-CA-TARGETS.csv has the exact header: `full_name,politician_id,state,district_type,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
      - 103-CA-TARGETS.csv has at least 9 data rows (the Phase 100 floor minus Mike Braun = 8 unsourced + N weak-source). The actual total will be higher once weak-source detection adds rows.
      - Gavin Newsom's row (politician_id `f26309c8-2525-49b2-bdaf-62980cbb1853`) is present in the CSV (per CONTEXT.md D-02 — statewide executives MUST appear).
      - Every CSV row has state column value `CA`.
      - Every CSV row's district_type column is one of the values from Task 1 Step E.

    If Gavin Newsom is MISSING from the CSV, abort and re-check Task 1 Step E — Pitfall 1 (CA District Type Coverage) has fired and the IN() list is missing his district_type. Do NOT proceed to Plan 02 until Newsom appears.

    If the total politician count appears suspiciously low (< 80, since CA has 80 Assembly seats alone), record this discrepancy in the Plan 02 Scoping section of the report and surface for human review before Plan 02 dispatch.

    The stdout single-line summary should print exactly the format: `Plan 02 scope: {flagged_count} CA politicians flagged ({U} unsourced + {W} weak-source stances) — recommended batching: {tier_message}`.

    No further DB writes in this task. The triage is read-only; it only writes to the phase directory.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md && test -f .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv && head -1 .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv | grep -q "full_name,politician_id,state,district_type,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification" && grep -q "Pre-flight discoveries" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md exists
    - File .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv exists
    - First line of CSV is exactly: `full_name,politician_id,state,district_type,party,total_stances,unsourced_count,weak_count,affected_topic_keys,classification`
    - Report contains the section `## Pre-flight discoveries (Plan 01 Task 1)` unchanged from Task 1 (verifiable: `grep -c "Pre-flight discoveries" .planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` returns at least 1)
    - Report contains the section header `Executive Summary` with a numeric `total_politicians` value
    - Report contains the section header `CA — Unsourced Stances`
    - Report contains the section header `CA — Weak-Sourced Stances`
    - Report contains the names `Gavin Newsom`, `Katy Hall`, `Tracy Miller`, `Candice B. Pierucci`, `Cody Harris`, `Juan Carrillo`, `Lisa Calderon`, `Roland Gutierrez` (the 8 Phase 100 CA-floor politicians; Mike Braun excluded by state filter)
    - Report contains a `Plan 02 Scoping` section explicitly naming the batching tier (one of: `1 research plan`, `2 research plans`, `3+ research plans`)
    - CSV has at least 9 data rows — verification: `grep -v '^#' .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv | grep -cv '^full_name'` returns >= 9
    - CSV contains Gavin Newsom's politician_id `f26309c8-2525-49b2-bdaf-62980cbb1853` — verification: `grep -c 'f26309c8-2525-49b2-bdaf-62980cbb1853' .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv` returns 1 (CONTEXT.md D-02 — statewide executives in scope)
    - CSV state column value is `CA` for every data row — verification: `grep -v '^#' .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv | tail -n +2 | awk -F',' '$3 != "CA" {print NR}' | wc -l` returns 0
    - CSV does NOT contain Mike Braun's politician_id `a73e7a2a-48b0-4636-8fa4-5324ede65833` (Indiana Governor — excluded by state='CA' filter) — verification: `grep -c 'a73e7a2a-48b0-4636-8fa4-5324ede65833' .planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv` returns 0
    - Every CSV row's classification column is one of `unsourced_only`, `weak_only`, or `both`
  </acceptance_criteria>
  <done>Triage artifacts are committed to the phase directory. The pre-flight discoveries are preserved. Gavin Newsom and the 8 known CA-floor politicians are confirmed in scope. Plan 02 can read 103-CA-TARGETS.csv to drive its research-stances dispatch (one agent per politician, affected_topic_keys only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live DB | Triage script reads inform.* via pool.query() using DATABASE_URL from backend/.env. No user-supplied input crosses any trust boundary at runtime — script is operator-initiated batch only. |
| Filesystem write → repo | Script writes report + CSV to the phase directory. Output is committed to git by the operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | I (Information disclosure) | backend/scripts/run-ca-source-triage.ts | mitigate | Script uses pool.query() (parameterized only for literal CA / district_type values which are NOT user-supplied; no SQL injection vector). Output contains no PII — only public politician names, public stance counts, public source URLs. |
| T-103-02 | T (Tampering) | DATABASE_URL env loading | mitigate | Script aborts if DATABASE_URL is missing (existing pattern from run-house-source-triage.ts). Session pooler URL is sourced from backend/.env (gitignored) — no credentials in source. |
| T-103-03 | D (Denial of service) | Live DB queries | accept | Three small CTE-based queries against indexed inform.* tables. Estimated cost: O(N) where N = ~125 CA politicians × ~30 average stances each = ~3,750 row scan. Acceptable for read-only batch. |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Script uses already-installed pg, tsx, dotenv (verified in 103-RESEARCH.md Package Legitimacy Audit section). No legitimacy gate required. |
</threat_model>

<verification>
- Pre-flight discoveries section exists in 103-CA-TRIAGE-REPORT.md with all 5 sub-sections (A–E)
- Triage script run completes with exit code 0
- Both artifacts (103-CA-TRIAGE-REPORT.md, 103-CA-TARGETS.csv) exist in phase directory
- CSV header matches locked column shape including new `district_type` column
- Gavin Newsom (statewide executive) is in the target CSV per CONTEXT.md D-02
- All 8 Phase 100 CA-floor politicians appear in the report
- Plan 02 batching tier is explicitly named in the report
</verification>

<success_criteria>
1. `backend/scripts/run-ca-source-triage.ts` exists and runs successfully against the live DB.
2. `.planning/phases/103-state-remediation-ca-md/103-CA-TRIAGE-REPORT.md` and `.planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv` are written.
3. The CSV gives Plan 02 a deterministic list of CA politicians with per-politician affected_topic_keys — enough to scope research-stances dispatch (one agent per politician) without re-querying.
4. The "sourced" definition and homepage-only weak-source regex used here match Phase 100 / Phase 101 / Phase 102 exactly (locked).
5. Gavin Newsom appears in the targets CSV (per CONTEXT.md D-02 — statewide executives in scope).
</success_criteria>

<output>
Create `.planning/phases/103-state-remediation-ca-md/103-01-SUMMARY.md` when done. The summary must include:
- Total flagged CA politician count
- Per-classification breakdown: unsourced_only count, weak_only count, both count
- The 8 Phase 100 CA-floor politicians confirmed present
- Gavin Newsom confirmed present
- Total unsourced stances + total weak-source stances (the sum drives Plan 02 sizing)
- Recommended Plan 02 batching tier (1 plan / 2 plans / 3+ plans)
- Path to the CSV that Plan 02 will read: `.planning/phases/103-state-remediation-ca-md/103-CA-TARGETS.csv`
- Note any data anomalies (e.g., total politician count < 80 — possible missing seats; Mike Braun appearing despite state filter — possible misconfigured district)
</output>
</content>
