---
phase: 91-results-polish-visual-redesign
plan: 03
type: execute
wave: 2
depends_on: [91-01, 91-02]
files_modified:
  - EV-readrank/src/index.css
  - EV-readrank/src/components/IssueHub.tsx
  - EV-readrank/src/components/EvaluationPhase.tsx
  - EV-readrank/src/components/PracticeRound.tsx
  - EV-readrank/src/components/PracticeResultsScreen.tsx
  - EV-readrank/src/components/CandidateAlignmentPage.tsx
  - EV-readrank/src/components/QuickConfirmation.tsx
autonomous: false
requirements: [CHRM-04, RSLT-04]

must_haves:
  truths:
    - "No Fraunces font references remain anywhere in EV-readrank/src/ — all typography uses Manrope"
    - "CandidateAlignmentPage uses Manrope typography and matches the visual style of the redesigned results page"
    - "Buttons, badges, and cards share consistent border-radius, shadow, and border treatment across all pages"
    - "The app looks cohesive across Hub, Evaluation, Practice, Matchup, Results, and CandidateAlignment pages"
  artifacts:
    - path: "EV-readrank/src/index.css"
      provides: "Fraunces removal from CSS classes and @import"
      contains: "font-family: 'Manrope'"
    - path: "EV-readrank/src/components/CandidateAlignmentPage.tsx"
      provides: "Visual polish matching new design"
      contains: "Manrope"
  key_links:
    - from: "index.css"
      to: "all components"
      via: ".ev-heading, .ev-quote-text CSS classes updated to Manrope"
      pattern: "ev-heading|ev-quote-text"
---

<objective>
Complete visual cohesion pass: remove all Fraunces font usage, update CSS classes, and ensure consistent styling across all Read & Rank components.

Purpose: Mixed typography (Fraunces serif vs Manrope sans-serif) and inconsistent component styling is the primary visual inconsistency. Switching to Manrope everywhere and normalizing border-radius, shadow, and border treatment creates the Linear-style clean aesthetic.

Output: All EV-readrank components using Manrope exclusively with consistent visual treatment.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-results-polish-visual-redesign/91-CONTEXT.md
@.planning/phases/91-results-polish-visual-redesign/91-RESEARCH.md
@.planning/phases/91-results-polish-visual-redesign/91-01-SUMMARY.md
@.planning/phases/91-results-polish-visual-redesign/91-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Fraunces from CSS and update typography classes</name>
  <files>EV-readrank/src/index.css</files>
  <read_first>
    - EV-readrank/src/index.css
  </read_first>
  <action>
**1. Remove the Fraunces Google Fonts @import** (line 1):
Delete the entire line:
```
@import url('https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;0,9..144,600;0,9..144,700;0,9..144,800;0,9..144,900;1,9..144,300;1,9..144,400;1,9..144,500;1,9..144,600;1,9..144,700&display=swap');
```
Keep the Manrope @import.

**2. Remove the Fraunces theme variable** (line 43):
Delete `--font-family-fraunces: 'Fraunces', serif;`

**3. Update `.ev-heading` class** (lines 106-111):
Change from:
```css
.ev-heading {
  font-family: 'Fraunces', serif;
  font-weight: 700;
  color: #1a1a2e;
  font-optical-sizing: auto;
}
```
To:
```css
.ev-heading {
  font-family: 'Manrope', sans-serif;
  font-weight: 800;
  color: #1a1a2e;
  letter-spacing: -0.02em;
}
```

**4. Update `.ev-quote-text` class** (lines 131-138):
Change from:
```css
.ev-quote-text {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-weight: 400;
  font-optical-sizing: auto;
  line-height: 1.6;
  color: #1a1a2e;
}
```
To:
```css
.ev-quote-text {
  font-family: 'Manrope', sans-serif;
  font-style: normal;
  font-weight: 400;
  line-height: 1.6;
  color: #1a1a2e;
}
```

**5. Update `.ev-quote-card::before` pseudo-element** (lines 200-211):
Change `font-family: 'Fraunces', serif;` to `font-family: 'Manrope', sans-serif;`
The decorative open-quote mark still works with Manrope — it renders as a large character overlay.

CRITICAL: Do ALL of these in a single edit pass on index.css. Removing the @import without updating the classes (or vice versa) causes a silent fallback to Times New Roman for any remaining Fraunces reference.
  </action>
  <verify>
    <automated>grep -c "Fraunces" /Users/chrisandrews/Documents/GitHub/EV-readrank/src/index.css; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Fraunces" EV-readrank/src/index.css` returns 0
    - index.css does NOT contain `@import url.*Fraunces`
    - index.css does NOT contain `--font-family-fraunces`
    - `.ev-heading` class contains `font-family: 'Manrope', sans-serif`
    - `.ev-heading` class contains `font-weight: 800`
    - `.ev-quote-text` class contains `font-family: 'Manrope', sans-serif`
    - `.ev-quote-text` class contains `font-style: normal`
    - `.ev-quote-card::before` contains `font-family: 'Manrope', sans-serif`
  </acceptance_criteria>
  <done>
    - Fraunces completely removed from index.css (import, variable, all class usages)
    - .ev-heading updated to Manrope 800 weight
    - .ev-quote-text updated to Manrope normal style
    - No browser will attempt to load Fraunces font
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Fraunces from all component inline styles and polish CandidateAlignmentPage</name>
  <files>EV-readrank/src/components/IssueHub.tsx, EV-readrank/src/components/EvaluationPhase.tsx, EV-readrank/src/components/PracticeRound.tsx, EV-readrank/src/components/PracticeResultsScreen.tsx, EV-readrank/src/components/CandidateAlignmentPage.tsx, EV-readrank/src/components/QuickConfirmation.tsx</files>
  <read_first>
    - EV-readrank/src/components/IssueHub.tsx
    - EV-readrank/src/components/EvaluationPhase.tsx
    - EV-readrank/src/components/PracticeRound.tsx
    - EV-readrank/src/components/PracticeResultsScreen.tsx
    - EV-readrank/src/components/CandidateAlignmentPage.tsx
    - EV-readrank/src/components/QuickConfirmation.tsx
  </read_first>
  <action>
Replace every `fontFamily: "'Fraunces', serif"` with `fontFamily: "'Manrope', sans-serif"` in ALL listed files. For heading-style elements (h1, h2, h3, stat numbers), also update `fontWeight` to 800 (from typical 600-700 used with Fraunces).

**Specific changes per file:**

**IssueHub.tsx** — 3 Fraunces references:
1. Line ~101: Decorative quotation mark div: change `fontFamily: "'Fraunces', serif"` to `fontFamily: "'Manrope', sans-serif"`
2. Line ~115: h1 "Choose an Issue": change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
3. Line ~211: h3 issue title: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 700`

**EvaluationPhase.tsx** — 1 Fraunces reference:
1. Line ~220: "Done" text in evaluation-complete-card: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`

**PracticeRound.tsx** — 2 Fraunces references:
1. Line ~248: Practice splash heading: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
2. Line ~564: "Done" text in practice complete state: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`

**PracticeResultsScreen.tsx** — 4 Fraunces references:
1. Line ~22: Results heading: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
2. Line ~55: Stat value number: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
3. Line ~109: Agreed section candidate name h3: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 700`
4. Line ~189: Disagreed section candidate name h3: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 700`

**CandidateAlignmentPage.tsx** — 4 Fraunces references + visual polish:
1. Line ~215: h1 candidate name: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
2. Line ~256: Stat value number: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`
3. Line ~287: h2 "Issue-by-Issue Breakdown": change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 700`
4. Line ~315: h3 issue title: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 700`

Additional CandidateAlignmentPage visual polish (RSLT-04):
- The hero band gradient, photo styling, stats layout, and issue cards already use the correct color palette
- No structural changes needed — the Fraunces-to-Manrope swap + weight updates are sufficient for visual consistency with the redesigned results page

**QuickConfirmation.tsx** — 1 Fraunces reference:
1. Line ~25: Heading text: change to `fontFamily: "'Manrope', sans-serif"`, `fontWeight: 800`

**IMPORTANT:** Do NOT change any `fontFamily: "'Manrope', sans-serif"` lines that already exist — only change Fraunces references. ResultsPhase.tsx was already updated in Plan 01 and should have zero Fraunces references.
  </action>
  <verify>
    <automated>grep -r "Fraunces" /Users/chrisandrews/Documents/GitHub/EV-readrank/src/ --include="*.tsx" --include="*.css" --include="*.ts" | wc -l; cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Fraunces" EV-readrank/src/ --include="*.tsx" --include="*.css" --include="*.ts"` returns 0 lines
    - IssueHub.tsx h1 contains `fontFamily: "'Manrope', sans-serif"` and `fontWeight: 800`
    - EvaluationPhase.tsx "Done" text contains `fontFamily: "'Manrope', sans-serif"`
    - PracticeRound.tsx contains zero Fraunces references
    - PracticeResultsScreen.tsx contains zero Fraunces references
    - CandidateAlignmentPage.tsx h1 candidate name contains `fontFamily: "'Manrope', sans-serif"` and `fontWeight: 800`
    - CandidateAlignmentPage.tsx h2 "Issue-by-Issue Breakdown" contains `fontFamily: "'Manrope', sans-serif"`
    - QuickConfirmation.tsx contains zero Fraunces references
    - `npm run build` in EV-readrank exits 0
  </acceptance_criteria>
  <done>
    - Zero Fraunces references in entire EV-readrank/src/ directory (CSS and components)
    - All headings use Manrope 800 weight for visual prominence
    - All sub-headings use Manrope 700 weight
    - CandidateAlignmentPage visually matches new design (Manrope typography, same color palette)
    - TypeScript builds cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify visual cohesion across all pages</name>
  <files>EV-readrank/src/index.css, EV-readrank/src/components/IssueHub.tsx, EV-readrank/src/components/CandidateAlignmentPage.tsx</files>
  <action>
Human verification of visual cohesion across all pages after Fraunces removal:
1. Run `cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npm run dev`
2. Check Hub: "Choose an Issue" heading uses Manrope sans-serif bold, issue titles use Manrope
3. Check Evaluation: quote text uses Manrope normal (no italic serif)
4. Check matchup mode on desktop: sidebar hidden, full-width matchup cards
5. Check end-of-evaluation: ranked list centered at full width
6. Check Results: reveal flow with Manrope typography
7. Check CandidateAlignmentPage: candidate name, stats, issue headings all Manrope
8. Check page transitions: hub -> evaluation -> results -> hub smooth animations
9. Verify zero Fraunces: `grep -r "Fraunces" src/ --include="*.tsx" --include="*.css"` returns nothing
  </action>
  <verify>
    <automated>grep -rc "Fraunces" /Users/chrisandrews/Documents/GitHub/EV-readrank/src/ --include="*.tsx" --include="*.css" --include="*.ts" | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>User confirms all pages use Manrope exclusively with consistent visual treatment and no serif fonts visible</done>
</task>

</tasks>

<verification>
- `grep -r "Fraunces" /Users/chrisandrews/Documents/GitHub/EV-readrank/src/ --include="*.tsx" --include="*.css" --include="*.ts" | wc -l` returns 0
- `cd /Users/chrisandrews/Documents/GitHub/EV-readrank && npm run build` exits 0
- `grep "font-family.*Manrope" /Users/chrisandrews/Documents/GitHub/EV-readrank/src/index.css | wc -l` returns 5+ (ev-heading, ev-subheading, ev-text-primary, ev-text-secondary, ev-quote-text, plus button classes)
</verification>

<success_criteria>
- Zero Fraunces font references in entire codebase (CSS imports, theme variables, class definitions, inline styles)
- All components render with Manrope typography exclusively
- CandidateAlignmentPage typography matches new results design
- Consistent visual treatment across all pages
- TypeScript builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/91-results-polish-visual-redesign/91-03-SUMMARY.md`
</output>
