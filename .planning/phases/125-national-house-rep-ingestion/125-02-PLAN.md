---
phase: 125-national-house-rep-ingestion
plan: 02
type: execute
wave: 2
depends_on: [125-01]
files_modified:
  - backend/migrations/734_national_house_reps.sql
autonomous: false
requirements: [USHR-01, USHR-02, USHR-03]

must_haves:
  truths:
    - "Migration 734_national_house_reps.sql is applied to production (Supabase project kxsdzaojfaibhuzmclfq)"
    - "Linked House reps rises from 137 to (435 - live vacancies); per-state coverage matches the YAML roster"
    - "Zero orphan politicians: every politician seeded by this batch has a linked office (office_id NOT NULL)"
    - "Party normalization holds: zero seeded House reps have party = 'Democrat'"
    - "Re-applying the migration is a no-op (idempotent) — second run inserts 0 rows"
    - "The 137 pre-existing reps (CA/TX/VA/MA/MD/OR/UT/ME/IN/DC) are untouched"
  artifacts:
    - path: "backend/migrations/734_national_house_reps.sql"
      provides: "Applied, tracked migration; permanent audit record of the national House rep seed"
  key_links:
    - from: "essentials.offices (district_id, chamber_id=US House)"
      to: "essentials.districts (district_type='NATIONAL_LOWER')"
      via: "applied migration 734"
      pattern: "U.S. Representative"
---

<objective>
Apply the reviewed migration from 125-01 to production and verify the national House
rep coverage in-phase. After this plan, every US resident whose address resolves to a
congressional district sees their actual sitting House representative.

This is the production-write step. autonomous: false — the migration touches ~298
production rows and must be applied with human awareness. The consolidated phase gate
+ Path 0 spot checks + headshots are Phase 126; this plan does the apply plus a
focused in-phase sanity check that USHR-01/02/03 hold.

Output:
  - backend/migrations/734_national_house_reps.sql applied + tracked
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/125-national-house-rep-ingestion/125-01-PLAN.md

Production DB = Supabase project kxsdzaojfaibhuzmclfq. Apply via the Supabase MCP
(execute_sql for the migration body — it tolerates large SQL; auto-commit, do NOT split
BEGIN/COMMIT across calls per the v2.12 lesson). Track the migration version in
supabase_migrations.schema_migrations if apply_migration is not used (psql/execute_sql
applies do not auto-insert the version row).

Pre-apply baseline: 137 linked House reps. Expected post-apply: 435 - live vacancies
(DC's 3 already counted in the 137 baseline; territories excluded).
</context>

<task id="1" type="execute">
<title>Apply migration 734 to production</title>
<read_first>
- backend/migrations/734_national_house_reps.sql (the generated migration under review)
- .planning/research/SUMMARY.md (apply mechanics, vacancy-robust expectation)
</read_first>
<action>
Re-confirm the migration number is unused (SELECT MAX(version) FROM
supabase_migrations.schema_migrations; and check backend/migrations/ max). Apply
734_national_house_reps.sql to production project kxsdzaojfaibhuzmclfq via the Supabase
MCP execute_sql (single call for the BEGIN;...COMMIT; body; if chunked, apply each chunk
in its own auto-commit call in order). After apply, record the migration version row in
supabase_migrations.schema_migrations if the apply path did not insert it automatically.
</action>
<acceptance_criteria>
- execute_sql for the migration body returns success (no exception)
- `SELECT COUNT(*) FROM essentials.districts d JOIN essentials.offices o ON o.district_id=d.id WHERE d.district_type='NATIONAL_LOWER'` rises from 137 toward 435 (>= 425, allowing for live vacancies)
- The migration version (734) is present in supabase_migrations.schema_migrations OR the file is otherwise recorded as applied
</acceptance_criteria>
</task>

<task id="2" type="verify">
<title>In-phase coverage + integrity verification</title>
<read_first>
- backend/scripts/_verify-path0-national.ts (existing national Path 0 join check — reference for the join shape)
- .planning/research/SUMMARY.md
</read_first>
<action>
Run verification queries against production (Supabase MCP execute_sql):
1. Coverage: count NATIONAL_LOWER districts with vs without a linked rep, grouped by
   LEFT(tiger_geoid,2) — confirm every state's count matches the YAML roster (only
   genuine vacancies remain unlinked).
2. No orphans: assert zero politicians with external_id BETWEEN -56999 AND -1000 have
   office_id IS NULL.
3. Party normalization: assert zero seeded House reps (external_id BETWEEN -56999 AND
   -1000) have party = 'Democrat'.
4. Idempotency: re-run the migration body once more; assert it inserts 0 new politicians
   and 0 new offices.
5. Pre-existing untouched: assert the CA (53) / VA (11) / MA (9) linked-rep counts are
   unchanged from baseline.
Record results in the plan summary. Do NOT mark USHR closed here if any vacancy is
actually a data error rather than a real vacancy — investigate unmatched districts.
</action>
<acceptance_criteria>
- Coverage query: districts_missing_rep equals the count of genuine current vacancies (each previously listed in 125-01 dry-run); no state with a sitting full delegation is left partial
- Orphan check returns 0
- Party-normalization check returns 0 rows with party='Democrat' in the seeded range
- Idempotent re-run inserts 0 politicians and 0 offices
- CA/VA/MA linked-rep counts unchanged (53/11/9)
</acceptance_criteria>
</task>

<verification>
- Migration 734 applied; linked House reps ~435 minus live vacancies
- Zero orphan politicians; zero 'Democrat' party rows in the seeded batch; idempotent re-run is a no-op
- USHR-01, USHR-02, USHR-03 satisfied (formal consolidated gate + headshots are Phase 126)
- Per-state coverage report captured in 125-02-SUMMARY.md, with every remaining unlinked district confirmed as a genuine vacancy
</verification>
