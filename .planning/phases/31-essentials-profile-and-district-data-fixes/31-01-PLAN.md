---
phase: 31-essentials-profile-and-district-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EV-Backend/internal/essentials/handlers.go
  - EV-Backend/internal/essentials/geofence_lookup.go
autonomous: true
requirements: [DIST-02, CARD-01, PROF-01]

must_haves:
  truths:
    - "API responses for list endpoints include district_id field for all politicians"
    - "API response for single politician profile includes district_id field"
    - "X0001 MTFCC code is mapped to LOCAL district type in geofence lookup"
  artifacts:
    - path: "EV-Backend/internal/essentials/handlers.go"
      provides: "district_id in OfficialOut struct and all 4 query functions"
      contains: "DistrictID"
    - path: "EV-Backend/internal/essentials/geofence_lookup.go"
      provides: "X0001 MTFCC mapping to LOCAL"
      contains: "X0001"
  key_links:
    - from: "handlers.go OfficialOut"
      to: "fetchOfficialsFromDB SQL"
      via: "d.district_id in SELECT + row struct + Scan + assembly"
      pattern: "d\\.district_id"
    - from: "geofence_lookup.go mtfccToDistrictTypes"
      to: "FindPoliticiansByGeoMatches WHERE clause"
      via: "map lookup for X0001"
      pattern: "X0001.*LOCAL"
---

<objective>
Add `district_id` to all API response paths and map the X0001 MTFCC code to LOCAL district type in the geofence lookup.

Purpose: The frontend needs `district_id` (e.g., "1", "40", "At Large") to construct the "Chamber, District N" subtitle on cards and profiles. The X0001 MTFCC mapping is needed so that imported city council sub-district boundaries (Bloomington Districts 1-6) correctly resolve to LOCAL politicians.

Output: Backend serves `district_id` in all politician API responses; X0001 geofence boundaries produce LOCAL district matches.
</objective>

<execution_context>
@/Users/chrisandrews/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisandrews/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-essentials-profile-and-district-data-fixes/31-RESEARCH.md
@EV-Backend/internal/essentials/handlers.go
@EV-Backend/internal/essentials/geofence_lookup.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add district_id to OfficialOut and all query paths</name>
  <files>EV-Backend/internal/essentials/handlers.go</files>
  <action>
Add `district_id` from the districts table to all 4 query functions that produce `OfficialOut`:

1. **OfficialOut struct** (~line 103): Add field `DistrictID string \`json:"district_id,omitempty"\`` after the existing `DistrictLabel` field.

2. **fetchOfficialsFromDB** (~line 1057):
   - Add `DistrictID string` to the `row` struct (~line 1058)
   - Add `COALESCE(d.district_id, '') AS district_id_text,` to the SELECT (after `d.label AS district_label`). Use alias `district_id_text` to avoid ambiguity with the foreign key `o.district_id` which is a UUID.
   - Add `&r.DistrictID` to the Scan list (or Scan is via GORM `.Scan(&rows)` so it maps by column name — rename the row struct field to match the alias, or use `DistrictIDText` and map manually)

   Actually, looking at the code: `fetchOfficialsFromDB` uses GORM `.Scan(&rows)` which maps by column name. The row struct field should be named to match the alias. Use field name `DistrictIDText string` mapped from column alias `district_id_text`. Then in the assembly loop (~line 1286), add `DistrictID: r.DistrictIDText,` to the OfficialOut literal.

3. **fetchFederalAndStateFromDBFiltered** (~line 1348):
   - Same pattern: add `DistrictIDText string` to row struct
   - Add `COALESCE(d.district_id, '') AS district_id_text,` to SELECT
   - Add `DistrictID: r.DistrictIDText,` to assembly loop

4. **GetPoliticianByID** (~line 1880):
   - Same pattern: add `DistrictIDText string` to row struct
   - Add `COALESCE(d.district_id, '') AS district_id_text,` to SELECT
   - Add `DistrictID: r0.DistrictIDText,` to the PoliticianProfileOut assembly

5. **FindPoliticiansByGeoMatches** (in geofence_lookup.go, ~line 116):
   - Add `d.district_id as district_id_text,` to the SELECT list
   - Add `&off.DistrictID,` to the Scan call (this function uses manual Scan, not GORM Scan)

Note: The districts table column `district_id` is type `string` (text in Postgres) containing values like "1", "40", "At Large", "WA", etc. It is NOT a UUID — it's the human-readable district identifier from BallotReady/Cicero.

IMPORTANT: Be careful with column name ambiguity. The `offices` table has `district_id` as a UUID foreign key. Always use the table alias `d.district_id` and give it a column alias like `district_id_text` to avoid GORM mapping confusion.
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build -o /dev/null .` to confirm compilation succeeds with no errors.
  </verify>
  <done>
OfficialOut struct includes DistrictID field. All 4 query functions (fetchOfficialsFromDB, fetchFederalAndStateFromDBFiltered, GetPoliticianByID, FindPoliticiansByGeoMatches) select d.district_id and include it in their output. Backend compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add X0001 MTFCC mapping to geofence lookup</name>
  <files>EV-Backend/internal/essentials/geofence_lookup.go</files>
  <action>
Add `X0001` to the `mtfccToDistrictTypes` map in geofence_lookup.go (~line 22):

```go
"X0001": {"LOCAL"},  // City council sub-districts (BallotReady custom MTFCC)
```

Add this entry after the existing `"G5420": {"SCHOOL"}` line. This maps BallotReady's custom MTFCC code for municipal ward/council-district boundaries to the LOCAL district type, enabling geofence queries to correctly match city council district politicians when sub-city boundary polygons are present in `geofence_boundaries`.

No other code changes needed — `FindPoliticiansByGeoMatches` already uses the map and handles any MTFCC key present in it.
  </action>
  <verify>
Run `cd /Users/chrisandrews/Documents/GitHub/EV-Backend && go build -o /dev/null .` to confirm compilation succeeds. Then verify with `grep "X0001" /Users/chrisandrews/Documents/GitHub/EV-Backend/internal/essentials/geofence_lookup.go` that the mapping is present.
  </verify>
  <done>
The mtfccToDistrictTypes map includes X0001 mapped to LOCAL. Geofence queries against city council sub-district boundaries will correctly filter to LOCAL district type politicians.
  </done>
</task>

</tasks>

<verification>
1. `go build -o /dev/null .` in EV-Backend compiles cleanly
2. `grep -n "DistrictID" EV-Backend/internal/essentials/handlers.go` shows the field in OfficialOut and all row structs
3. `grep -n "district_id_text" EV-Backend/internal/essentials/handlers.go` shows the column alias in all SELECT queries
4. `grep "X0001" EV-Backend/internal/essentials/geofence_lookup.go` shows the MTFCC mapping
</verification>

<success_criteria>
- OfficialOut includes `district_id` in JSON responses (omitempty for backward compatibility)
- All 4 query paths (ZIP, address/geofence, federal+state, single politician) include district_id
- X0001 MTFCC maps to LOCAL in the geofence lookup
- Backend compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-essentials-profile-and-district-data-fixes/31-01-SUMMARY.md`
</output>
