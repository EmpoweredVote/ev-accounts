---
phase: 88-stance-corrections-party-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/stance-research/2026-06-02-tier1-batch-a.csv
  - supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql
  - supabase/migrations/20260603000002_117_roger_niello_inversion_correction.sql
  - supabase/migrations/20260603000003_118_angie_nixon_inversion_correction.sql
  - supabase/migrations/20260603000004_119_alex_vindman_inversion_correction.sql
autonomous: false
requirements:
  - SACC-02
must_haves:
  truths:
    - "Jeff Gonzalez (5ad32852-789e-4013-995b-6f0aa6a5a5d4) has every previously-flagged stance row re-researched and corrected with at least one real fetched source URL"
    - "Roger Niello (22152e41-31b9-4700-9226-4e274c616f37) has every previously-flagged stance row re-researched and corrected with at least one real fetched source URL"
    - "Angie Nixon (0ac89151-2b8d-4430-b9bd-3a80bef3413b) has every previously-flagged stance row re-researched and corrected with at least one real fetched source URL"
    - "Alex Vindman (a2fee754-f90c-47ff-a3b7-377d55992273) has every previously-flagged stance row re-researched and corrected with at least one real fetched source URL"
    - "Every corrected stance has a paired inform.politician_context row with sources array of length >= 1"
    - "Zero corrections rely on party-affiliation inference — reasoning text references documented record, not 'as a Republican' / 'as a Democrat' generalities"
  artifacts:
    - path: "data/stance-research/2026-06-02-tier1-batch-a.csv"
      provides: "Raw research output CSVs from research-stances skill for the 4 Tier 1 politicians in this batch"
      min_lines: 5
    - path: "supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql"
      provides: "Gonzalez stance + context corrections wrapped in BEGIN/COMMIT"
      contains: "UPDATE inform.politician_answers"
    - path: "supabase/migrations/20260603000002_117_roger_niello_inversion_correction.sql"
      provides: "Niello stance + context corrections wrapped in BEGIN/COMMIT"
      contains: "UPDATE inform.politician_answers"
    - path: "supabase/migrations/20260603000003_118_angie_nixon_inversion_correction.sql"
      provides: "Nixon stance + context corrections wrapped in BEGIN/COMMIT"
      contains: "UPDATE inform.politician_answers"
    - path: "supabase/migrations/20260603000004_119_alex_vindman_inversion_correction.sql"
      provides: "Vindman stance + context corrections wrapped in BEGIN/COMMIT"
      contains: "UPDATE inform.politician_answers"
  key_links:
    - from: "supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql"
      to: "inform.politician_answers"
      via: "UPDATE statement keyed by politician_id + topic_id"
      pattern: "politician_id = '5ad32852-789e-4013-995b-6f0aa6a5a5d4'"
    - from: "supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql"
      to: "inform.politician_context"
      via: "UPDATE or INSERT ON CONFLICT with sources array"
      pattern: "sources = ARRAY\\["
    - from: "data/stance-research/2026-06-02-tier1-batch-a.csv"
      to: "supabase/migrations/2026060300000[1-4]_*_correction.sql"
      via: "CSV rows are translated 1:1 into UPDATE/INSERT statements"
      pattern: "(value|reasoning|source_url)"
---

<objective>
Re-research and correct the first 4 of 8 confirmed-inversion Tier 1 politicians from the Phase 87 audit (per SACC-02): Jeff Gonzalez, Roger Niello, Angie Nixon, Alex Vindman. Each politician is re-researched individually using the research-stances skill (one agent at a time per project rate-limit constraint). Each correction is recorded in a dedicated migration file that updates inform.politician_answers and inform.politician_context together. Every corrected stance must cite at least one real fetched source URL — zero "party affiliation inference" corrections (this is the hard SACC-02 requirement).

Purpose: Restore stance-data accuracy for politicians the Phase 87 audit confirmed have systematic inversions, so users see positions that match documented public records rather than researcher-agent biases.

Output:
- 1 CSV file in data/stance-research/ containing the raw research output for all 4 politicians in this batch
- 4 migration files (numbers 116-119) applied to the live Supabase database via mcp__supabase-local__execute_sql
- Spot-check SQL queries confirming each politician's flagged stances now reflect researched values with paired source-bearing context rows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-stance-corrections-party-normalization/88-RESEARCH.md
@.planning/phases/88-stance-corrections-party-normalization/88-PATTERNS.md
@.planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md
@.claude/skills/research-stances/SKILL.md
@supabase/migrations/20260516000011_108_yaroslavsky_full_coverage_sprint.sql
@supabase/migrations/20260516000018_115_estuardo_mazariegos_full_coverage_sprint.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-research Jeff Gonzalez and Roger Niello (Tier 1, batch A — 2 of 4)</name>
  <files>data/stance-research/2026-06-02-tier1-batch-a.csv</files>
  <read_first>
    .claude/skills/research-stances/SKILL.md
    .planning/phases/88-stance-corrections-party-normalization/88-RESEARCH.md (Section: Confirmed Inversion Work Queue Wave 1)
    .planning/phases/87-stance-accuracy-audit-agent-update/87-AUDIT-REPORT.md (Section: Priority Tier 1)
  </read_first>
  <action>
    Run the research-stances skill in correction mode for Jeff Gonzalez (UUID 5ad32852-789e-4013-995b-6f0aa6a5a5d4, R, CA Assembly Member, 21 stances, all topics at value 1-2) and Roger Niello (UUID 22152e41-31b9-4700-9226-4e274c616f37, R, CA State Senator, 14 stances, dominant value=2). Dispatch ONE politician-stance-researcher agent at a time — NEVER parallel — per the project's rate-limit constraint documented in MEMORY.md. Pass the agent the full live JSON topic list (from inform.compass_topics + inform.compass_stances, fetched fresh per SKILL.md STEP 0) AND the five-chairs framing block from SKILL.md STEP 1 (lines 105-131). For Gonzalez focus on the topics his current values are 1-2 on (abortion, civil-rights, healthcare, immigration, medicare/aid, same-sex-marriage, school-vouchers, social-security, taxes — per RESEARCH.md table) plus the three already at 4 (ai-regulation, redistricting, trans-athletes) to confirm those are correct. For Niello cover all 14 of his current stance topics (abortion, civil-rights, climate-change, deportation, fossil-fuels, healthcare, immigration, medicare/aid, voting-rights at dominant value=2 plus the 5 non-flagged topics). Write the agent's output to a single combined CSV at data/stance-research/2026-06-02-tier1-batch-a.csv with columns: politician_full_name, topic_key, value, reasoning, source_url_1, source_url_2, source_url_3. After Gonzalez completes, dispatch the Niello agent. After both finish, append rows from any subsequent agents into the same file. REJECT any agent output where reasoning text contains phrases like "as a Republican", "likely supports", "generally speaking", or "consistent with party" — these are party-inference outputs that violate SACC-02; if such rows appear, dispatch a replacement agent for that politician with explicit instructions to cite documented votes/statements only. Save raw fetched URLs into the source columns — only include URLs the agent reports it successfully fetched (do not include URLs from search results that returned 404).
  </action>
  <verify>
    <automated>powershell -Command "if (-not (Test-Path 'data/stance-research/2026-06-02-tier1-batch-a.csv')) { exit 1 }; $lines = (Get-Content 'data/stance-research/2026-06-02-tier1-batch-a.csv' | Where-Object { $_ -match 'Gonzalez|Niello' }).Count; if ($lines -lt 20) { Write-Host \"Only $lines rows for Gonzalez+Niello — expected at least 20\"; exit 1 }; Write-Host \"$lines rows for batch A part 1\"; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at data/stance-research/2026-06-02-tier1-batch-a.csv
    - File contains a header row plus at least 20 stance rows where politician_full_name matches "Jeff Gonzalez" or "Roger Niello" (combined; Gonzalez has 21 current stances, Niello has 14)
    - Every row with non-empty value also has at least one non-empty source_url_* column
    - Zero rows contain the substrings "as a Republican", "as a Democrat", "likely supports", or "generally speaking" in the reasoning column (grep returns 0 matches)
    - All topic_key values in the CSV match a topic_key from `SELECT topic_key FROM inform.compass_topics WHERE is_live = true` (no invented slugs)
  </acceptance_criteria>
  <done>CSV contains 20+ rows across Gonzalez and Niello, each with a fetched source URL and reasoning that cites the documented record (not party inference).</done>
</task>

<task type="auto">
  <name>Task 2: Re-research Angie Nixon and Alex Vindman (Tier 1, batch A — 4 of 4)</name>
  <files>data/stance-research/2026-06-02-tier1-batch-a.csv</files>
  <read_first>
    .claude/skills/research-stances/SKILL.md
    data/stance-research/2026-06-02-tier1-batch-a.csv (verify Task 1 output and append-not-overwrite behavior)
    .planning/phases/88-stance-corrections-party-normalization/88-RESEARCH.md (Section: Confirmed Inversion Work Queue Wave 1, Note on Tim Grayson and Angie Nixon direction)
  </read_first>
  <action>
    Append research-stances skill output for Angie Nixon (UUID 0ac89151-2b8d-4430-b9bd-3a80bef3413b, D, FL Senate candidate, 11 stances all at value=4 — 91% uniform lock per audit) and Alex Vindman (UUID a2fee754-f90c-47ff-a3b7-377d55992273, D, FL Senate candidate, 11 stances at 4-5 with abortion=5 + religious-freedom=5 flagged as suspicious) to data/stance-research/2026-06-02-tier1-batch-a.csv. Dispatch the Nixon agent first; wait for completion; then dispatch the Vindman agent. Critical for both: do NOT assume direction — per RESEARCH.md "Note on Tim Grayson and Angie Nixon direction", the five-chairs scale has no fixed direction so Nixon's value=4 may be wrong but the correct value is NOT necessarily 1 or 2; it depends on the chair text for each topic. Pass the full live JSON topic list with stance texts to each agent (re-fetch per SKILL.md STEP 0 — do not reuse stale topic JSON from Task 1 if topics have changed). For Nixon's 11 topics and Vindman's 11 topics, the agent must fetch real campaign-site / vote-record / news URLs and match Nixon's and Vindman's documented positions against the exact stance text at each value. Append results to the SAME CSV file (data/stance-research/2026-06-02-tier1-batch-a.csv) — do not create a separate file. After completion the CSV should contain rows for all four batch-A politicians (Gonzalez, Niello, Nixon, Vindman).
  </action>
  <verify>
    <automated>powershell -Command "$csv = Import-Csv 'data/stance-research/2026-06-02-tier1-batch-a.csv'; $gonzalez = ($csv | Where-Object { $_.politician_full_name -match 'Gonzalez' }).Count; $niello = ($csv | Where-Object { $_.politician_full_name -match 'Niello' }).Count; $nixon = ($csv | Where-Object { $_.politician_full_name -match 'Nixon' }).Count; $vindman = ($csv | Where-Object { $_.politician_full_name -match 'Vindman' }).Count; if ($gonzalez -lt 10 -or $niello -lt 10 -or $nixon -lt 8 -or $vindman -lt 8) { Write-Host \"Insufficient rows: G=$gonzalez N=$niello Nx=$nixon V=$vindman\"; exit 1 }; Write-Host \"G=$gonzalez N=$niello Nx=$nixon V=$vindman\"; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - CSV file contains rows for all 4 politicians: Jeff Gonzalez (>= 10 rows), Roger Niello (>= 10 rows), Angie Nixon (>= 8 rows), Alex Vindman (>= 8 rows)
    - Every row has a non-empty reasoning column AND at least one non-empty source_url_* column
    - For Nixon specifically: NOT every row has value=4 (the original 91% uniform lock must be broken by genuine research)
    - For Vindman specifically: if abortion=5 remains, the reasoning must cite a specific bill or statement supporting the strongest-progressive chair text; same for religious-freedom=5
    - Zero rows have reasoning text matching the regex `(?i)(as a republican|as a democrat|likely supports|generally speaking|consistent with .* party)`
  </acceptance_criteria>
  <done>CSV contains researched stances for all 4 Tier 1 batch-A politicians (Gonzalez, Niello, Nixon, Vindman), each row source-cited and free of party-inference language.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of batch-A CSV before writing migrations</name>
  <what-built>
    A combined CSV at data/stance-research/2026-06-02-tier1-batch-a.csv containing re-researched stances for Jeff Gonzalez (R, CA Assembly), Roger Niello (R, CA State Senate), Angie Nixon (D, FL Senate cand.), and Alex Vindman (D, FL Senate cand.). Every row carries reasoning text and at least one fetched source URL. Tasks 1-2 verified that no rows contain party-inference language patterns.
  </what-built>
  <how-to-verify>
    1. Open data/stance-research/2026-06-02-tier1-batch-a.csv in a spreadsheet viewer
    2. For each of the 4 politicians, scan reasoning text and source URLs:
       a. Are the URLs from credible sources (Ballotpedia, official campaign sites, .gov sites, major news outlets, vote records)? Reject if all URLs are from unrelated topic pages or social media speculation.
       b. Does the reasoning text cite a specific vote, bill, public statement, or campaign-site quote? Reject if reasoning is hand-wavy ("the candidate supports X policies").
       c. Does the assigned value match the documented record when you read the stance text at that value (consult `SELECT value, text FROM inform.compass_stances WHERE topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = '<topic>') ORDER BY value` for the topic)?
    3. Flag any rows where the value seems wrong or sources are weak — these will be re-researched before migration generation.
    4. Compare against original (wrong) values listed in 88-RESEARCH.md "Confirmed Inversion Work Queue" — the corrected values should differ from the original values on the flagged topics for these 4 politicians.
    5. Confirm party tags in CSV match what's in the database: Gonzalez=R, Niello=R, Nixon=D, Vindman=D.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to migration generation, or list specific (politician, topic) pairs to re-research before proceeding (e.g., "re-research Nixon/climate-change and Vindman/abortion")</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write and apply 4 correction migrations (Gonzalez, Niello, Nixon, Vindman)</name>
  <files>
    supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql,
    supabase/migrations/20260603000002_117_roger_niello_inversion_correction.sql,
    supabase/migrations/20260603000003_118_angie_nixon_inversion_correction.sql,
    supabase/migrations/20260603000004_119_alex_vindman_inversion_correction.sql
  </files>
  <read_first>
    data/stance-research/2026-06-02-tier1-batch-a.csv (the approved research output from Task 3)
    supabase/migrations/20260516000011_108_yaroslavsky_full_coverage_sprint.sql (canonical UPDATE pattern from PATTERNS.md)
    supabase/migrations/20260516000018_115_estuardo_mazariegos_full_coverage_sprint.sql (canonical INSERT … ON CONFLICT upsert pattern from PATTERNS.md)
    .planning/phases/88-stance-corrections-party-normalization/88-PATTERNS.md
    .planning/phases/88-stance-corrections-party-normalization/88-RESEARCH.md (Pattern 1: Correction Migration Structure)
  </read_first>
  <action>
    Generate four migration files, one per politician, using the next sequential migration numbers 116, 117, 118, 119 with today's timestamp prefix. Filename format: `20260603000001_116_jeff_gonzalez_inversion_correction.sql`, `20260603000002_117_roger_niello_inversion_correction.sql`, `20260603000003_118_angie_nixon_inversion_correction.sql`, `20260603000004_119_alex_vindman_inversion_correction.sql`. Each migration follows the BEGIN/COMMIT-wrapped form from 88-PATTERNS.md, with the header comment block (politician name, UUID, office, source date, brief description of corrections). Per PATTERNS.md, each topic's correction is a paired UPDATE statement on inform.politician_answers (SET value = NEW) followed by an INSERT … ON CONFLICT (politician_id, topic_id) DO UPDATE on inform.politician_context (SET reasoning, sources). Use the subquery form for topic_id lookup: `topic_id = (SELECT id FROM inform.compass_topics WHERE topic_key = '<key>')`. Per the SACC-02 hard requirement, each context INSERT must include a `sources = ARRAY['<url>', ...]` with at least one real URL from the CSV — never an empty array, never a placeholder. Map CSV row → SQL by: for each (politician_full_name, topic_key, value, reasoning, source_url_1, source_url_2, source_url_3) row, emit one UPDATE + one upsert. Politician UUIDs to use: Gonzalez = '5ad32852-789e-4013-995b-6f0aa6a5a5d4', Niello = '22152e41-31b9-4700-9226-4e274c616f37', Nixon = '0ac89151-2b8d-4430-b9bd-3a80bef3413b', Vindman = 'a2fee754-f90c-47ff-a3b7-377d55992273'. After all 4 SQL files are written, apply each one in sequence via mcp__supabase-local__execute_sql (per the schema_push_requirement BLOCKING gate). For each migration: read the .sql contents, pass them as the query parameter to mcp__supabase-local__execute_sql, capture the response, and confirm no error. Apply 116 first, then 117, then 118, then 119.
  </action>
  <verify>
    <automated>powershell -Command "$files = @('supabase/migrations/20260603000001_116_jeff_gonzalez_inversion_correction.sql', 'supabase/migrations/20260603000002_117_roger_niello_inversion_correction.sql', 'supabase/migrations/20260603000003_118_angie_nixon_inversion_correction.sql', 'supabase/migrations/20260603000004_119_alex_vindman_inversion_correction.sql'); foreach ($f in $files) { if (-not (Test-Path $f)) { Write-Host \"MISSING: $f\"; exit 1 }; $content = Get-Content $f -Raw; if ($content -notmatch 'BEGIN;' -or $content -notmatch 'COMMIT;') { Write-Host \"NO TRANSACTION WRAP: $f\"; exit 1 }; if ($content -notmatch 'sources = ARRAY\\[' -and $content -notmatch 'sources, ARRAY\\[') { Write-Host \"NO SOURCES ARRAY: $f\"; exit 1 }; if ($content -notmatch 'inform.politician_answers' -or $content -notmatch 'inform.politician_context') { Write-Host \"MISSING REQUIRED TABLE: $f\"; exit 1 } }; Write-Host 'All 4 migration files present with required structure'; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 migration files exist at the exact paths listed in files_modified
    - Each migration file contains both `BEGIN;` and `COMMIT;` (case-sensitive, on their own statements)
    - Each migration file contains at least one `UPDATE inform.politician_answers` statement
    - Each migration file contains at least one `inform.politician_context` reference with `sources = ARRAY[` (sources array populated, never empty)
    - Each migration file references the correct politician UUID exactly once per UPDATE/INSERT block (Gonzalez=5ad32852-789e-4013-995b-6f0aa6a5a5d4 in 116; Niello=22152e41-31b9-4700-9226-4e274c616f37 in 117; Nixon=0ac89151-2b8d-4430-b9bd-3a80bef3413b in 118; Vindman=a2fee754-f90c-47ff-a3b7-377d55992273 in 119)
    - After applying all 4 migrations via mcp__supabase-local__execute_sql, this query returns 0: `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN ('5ad32852-789e-4013-995b-6f0aa6a5a5d4', '22152e41-31b9-4700-9226-4e274c616f37', '0ac89151-2b8d-4430-b9bd-3a80bef3413b', 'a2fee754-f90c-47ff-a3b7-377d55992273') AND (sources IS NULL OR array_length(sources, 1) IS NULL OR array_length(sources, 1) = 0)`
    - After applying, this query for each politician returns the corrected value distribution (run once per UUID): `SELECT value, COUNT(*) FROM inform.politician_answers WHERE politician_id = '<uuid>' GROUP BY value ORDER BY value` — distribution must differ from the audit-time baseline documented in 88-RESEARCH.md for at least the flagged topics
  </acceptance_criteria>
  <done>All 4 migration files written, applied to live DB via mcp__supabase-local__execute_sql, and spot-check SQL confirms no orphan stances (every corrected row has a non-empty sources array).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher agent → CSV file | LLM-generated stance assignments cross into persisted data |
| CSV → migration SQL → DB | Researched values become committed corrections via UPDATE statements |
| mcp__supabase-local__execute_sql → live Supabase | Migration writes hit production-equivalent DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01-01 | Tampering | researcher agent output | mitigate | Task 3 human-verify gate reviews CSV before SQL generation; party-inference regex check in Task 1/2 acceptance criteria blocks "as a Republican / Democrat" patterns |
| T-88-01-02 | Information Disclosure | source URLs in inform.politician_context | accept | All sources are public web URLs (Ballotpedia, .gov, news outlets) intended to be displayed to users via the compass feature — disclosure is the goal, not a leak |
| T-88-01-03 | Repudiation | DB writes from migration | mitigate | Migration files stored in supabase/migrations/ with git history; each migration's header comment records date + source-of-correction so future audits can trace the basis for each change |
| T-88-01-04 | Denial of Service | research-stances rate limits | mitigate | Tasks 1/2 mandate ONE agent at a time (never parallel) per MEMORY.md rate-limit constraint; serial dispatch prevents WebFetch quota exhaustion |
</threat_model>

<verification>
After all 4 migrations apply:
1. `SELECT value, COUNT(*) FROM inform.politician_answers WHERE politician_id = '5ad32852-789e-4013-995b-6f0aa6a5a5d4' GROUP BY value ORDER BY value` — Gonzalez distribution should differ from the audit baseline (was: most at 1-2; now: matches researched values)
2. `SELECT value, COUNT(*) FROM inform.politician_answers WHERE politician_id = '22152e41-31b9-4700-9226-4e274c616f37' GROUP BY value ORDER BY value` — Niello distribution should differ (was: 9 at value=2)
3. `SELECT value, COUNT(*) FROM inform.politician_answers WHERE politician_id = '0ac89151-2b8d-4430-b9bd-3a80bef3413b' GROUP BY value ORDER BY value` — Nixon distribution should differ (was: 91% at value=4)
4. `SELECT value, COUNT(*) FROM inform.politician_answers WHERE politician_id = 'a2fee754-f90c-47ff-a3b7-377d55992273' GROUP BY value ORDER BY value` — Vindman distribution should differ from the audit baseline
5. `SELECT COUNT(*) FROM inform.politician_context WHERE politician_id IN ('5ad32852-789e-4013-995b-6f0aa6a5a5d4', '22152e41-31b9-4700-9226-4e274c616f37', '0ac89151-2b8d-4430-b9bd-3a80bef3413b', 'a2fee754-f90c-47ff-a3b7-377d55992273') AND (sources IS NULL OR array_length(sources, 1) = 0)` returns 0 — every corrected context row has at least one source URL
6. Spot-check 3 random reasoning strings for "as a Republican" / "as a Democrat" / "likely supports" — must return zero matches
</verification>

<success_criteria>
- 4 of the 8 Tier 1 confirmed-inversion politicians have correction migrations applied (per SACC-02)
- Every corrected stance row has a paired context row with at least 1 source URL (SACC-02 hard requirement)
- Zero corrections rely on party-affiliation inference (verified via reasoning-text grep)
- Wave 1 batch A complete; Plan 88-02 (batch B) can proceed
</success_criteria>

<output>
Create `.planning/phases/88-stance-corrections-party-normalization/88-01-SUMMARY.md` documenting:
- Politicians corrected with old → new value distributions
- Migration filenames + applied timestamps
- Any CSV rows rejected during human review and the basis for rejection
- Notes for future planner on whether any Tier 1 politician's original audit hypothesis turned out to be wrong (e.g., a stance was researched correctly the first time)
</output>
