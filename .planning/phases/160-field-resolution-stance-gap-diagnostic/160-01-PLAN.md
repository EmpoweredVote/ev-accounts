---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/diag-160-incumbent-stance-gap.ts
  - backend/scripts/diag-160-external-id-collision.ts
  - backend/scripts/diag-160-race-preexistence-audit.ts
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01 Part A: a 178-row incumbent map exists keyed by (district_type='NATIONAL_LOWER', geo_id) — never a computed external_id — mapping every Wave-3 district to its existing essentials.politicians.id with current federal-24 stance count and top-up tier (zero/partial/done); tier distribution reproduces the researched 13 zero / 154 partial / 11 done baseline"
    - "USHC3-01 Part B: a per-district negative external_id collision audit exists reporting the 16 discovered collisions across KY/OR/OK/KS/NV/NM/NE/ME/NH/MT with a documented safe-start-seq per colliding district (KY-CD1 98/99 and OK-CD1 43/99 flagged for an alternate sub-band) — 0 UNRESOLVED collisions"
    - "USHC3-01 Part C: a pre-existing race/candidate audit sweeps ANY election_date (not only 2026-11-03) across all 38 states and surfaces the ME/MD/MA/NV/OR scaffolded-race baseline plus the IN-9 incumbent-flag bug and NV-2 NULL-politician_id anomaly as explicit console warnings for downstream fix"
    - "All three scripts are SELECT-only against prod (kxsdzaojfaibhuzmclfq); the only writes are the git-tracked CSV artifacts — no INSERT/UPDATE/DELETE/DDL anywhere, no --commit path"
  artifacts:
    - path: "backend/scripts/diag-160-incumbent-stance-gap.ts"
      provides: "Read-only Query A (incumbent map) + Query B (per-state stance gap) + Query C (holder invariant) + 178-row CSV emit"
      contains: "WAVE3_FIPS"
    - path: "backend/scripts/diag-160-external-id-collision.ts"
      provides: "Per-district collision audit of -(fips*10000+cd*100+seq) against live negative external_ids; CSV emit with safe-start-seq"
      contains: "external_id < 0"
    - path: "backend/scripts/diag-160-race-preexistence-audit.ts"
      provides: "ANY-election_date race/candidate sweep + per-district detail query surfacing IN-9 + NV-2 anomalies; CSV emit"
      contains: "election_date"
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv"
      provides: "178-row incumbent->politician_id map + stance counts + tiers"
      contains: "incumbent_top_up_tier"
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv"
      provides: "Per-colliding-district audit: collision_count, colliding_seqs, safe_start_seq"
      contains: "safe_start_seq"
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv"
      provides: "Per-district existing_race_id + candidate/incumbent-flag anomaly rows"
      contains: "existing_race_id"
  key_links:
    - from: "backend/scripts/diag-160-incumbent-stance-gap.ts"
      to: "essentials.districts JOIN essentials.offices JOIN essentials.politicians"
      via: "(district_type='NATIONAL_LOWER', geo_id) join"
      pattern: "NATIONAL_LOWER"
    - from: "backend/scripts/diag-160-incumbent-stance-gap.ts"
      to: "inform.politician_answers"
      via: "COUNT(*) per politician_id (never COUNT(pa.id))"
      pattern: "politician_answers"
    - from: "backend/scripts/diag-160-external-id-collision.ts"
      to: "essentials.politicians WHERE external_id < 0"
      via: "live per-computed-value collision check"
      pattern: "external_id"
---

<objective>
Produce the DB half of the Wave-3 (38-state / 178-district) US House field diagnostic — three read-only scripts and their git-tracked CSV artifacts that ground every downstream seeding phase (161–165). These run inline BEFORE any web research (D-01b) so each per-state research agent prompt carries its state's real incumbent names, geo_ids, and existing-race IDs.

Purpose: This DB baseline prevents the four costliest Wave-3 traps at scale — duplicate-incumbent records, ghost incumbents, negative external_id collisions (16 discovered), and re-creating the 29 pre-existing ME/MD/MA/NV/OR races. It also surfaces the IN-9 incumbent-flag bug and NV-2 NULL-politician_id anomaly for the owning seeding phases to fix (documented here, NOT fixed — this phase is write-free).
Output: `diag-160-incumbent-stance-gap.ts`, `diag-160-external-id-collision.ts`, `diag-160-race-preexistence-audit.ts` + the three CSVs (`160-incumbent-map.csv` 178 rows, `160-negative-id-audit.csv`, `160-race-preexistence-audit.csv`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md

<interfaces>
<!-- Templates + proven query logic the executor adapts. Read directly; do not re-derive. -->

backend/scripts/diag-154-incumbent-stance-gap.ts — PRIMARY SCAFFOLD (409 lines):
  - dotenv/pool bootstrap: `import 'dotenv/config';` + `import { pool } from '../src/lib/db.js';`
    (TS path with .js extension resolving to db.ts under tsx/NodeNext — copy verbatim, do NOT "fix" to .ts).
  - Query A: per-district incumbent map; essentials.districts d -> offices o (o.district_id=d.id)
    -> politicians p (p.id=o.politician_id); WHERE d.district_type='NATIONAL_LOWER'
    AND substr(d.geo_id,1,2)=ANY($1::text[]); selects state_fips, geo_id, politician_id,
    external_id, full_name, is_active, stance_count.
  - stance_count subquery: (SELECT COUNT(*) FROM inform.politician_answers pa WHERE pa.politician_id=p.id)::int
    — inform.politician_answers has NO id column; COUNT(*) only, NEVER COUNT(pa.id).
  - Query B: per-state stance-gap summary; Query C: holder invariant (HAVING COUNT(o.politician_id) <> 1).
  - topUpTier(stanceCount, vacant): 'vacant' | 'zero'(sc=0) | 'partial'(sc<24) | 'done'(sc>=24). FEDERAL_TOPIC_BAR=24.
  - csvField(): RFC-4180 quoting helper (reuse verbatim). CSV emit + hard row-count guard (refuses to write unless data rows == expected total).
  - main().catch() error wrapper (pool.end + process.exit(1)).

160-RESEARCH.md — proven-this-session query logic (all executed live against prod):
  - "Pattern 1" (lines ~355-373): the exact 38-code WAVE3_FIPS array + Query A (178 rows, 0 vacancies via Query C).
  - "Pattern 2" (lines ~378-398): external_id collision loop — 16/178 districts collide; per-district colliding-seq list.
  - "Pattern 3" (lines ~406-421): ANY-election_date race/candidate sweep — ME/MD/MA/NV/OR races + IN/UT/ME stale rows.
  - Critical Finding 6 table: the 16 colliding districts with max_seq; KY-CD1 (98/99) + OK-CD1 (43/99) need alternate sub-band.
  - Critical Finding 4: existing_race_id populated per-district for ME(2)/MD(8)/MA(9)/NV(4)/OR(6)=29 rows; IN-9 flag bug; NV-2 NULL pid.

EXPECTED_PER_STATE (verified 178 total this session) — WAVE3 per-state district counts:
  WA10 AZ9 TN9 MA9 | IN9 MD8 MN8 MO8 | WI8 CO8 AL7 SC7 LA6 | KY6 OR6 CT5 OK5 AR4 IA4 KS4 MS4 |
  NV4 UT4 NM3 NE3 WV2 ID2 HI2 ME2 NH2 RI2 MT1 AK1 DE1 ND1 SD1 VT1 WY1  (= 178)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diag-160-incumbent-stance-gap.ts + 160-incumbent-map.csv (178-row incumbent map)</name>
  <files>backend/scripts/diag-160-incumbent-stance-gap.ts, .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv</files>
  <read_first>
    - backend/scripts/diag-154-incumbent-stance-gap.ts (PRIMARY SCAFFOLD — clone the file, rescale constants, adapt CSV_PATH)
    - backend/src/lib/db.ts (confirm the pool export the import resolves to)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Pattern 1"; §"Critical Finding 5" tier baseline 13/154/11; §"Common Pitfalls 8/9")
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md (§"diag-160-incumbent-stance-gap.ts" — line-by-line clone map)
    - backend/data/stance-research/_FED24_SCALE.txt (confirms federal-24 denominator = 24; FEDERAL_TOPIC_BAR=24 unchanged)
  </read_first>
  <behavior>
    - Query A returns exactly one row per mapped incumbent for the 38 WAVE3_FIPS, keyed by (NATIONAL_LOWER, geo_id), stance_count via COUNT(*) on inform.politician_answers.
    - Query B prints a per-state stance-gap table; the aggregate tier split reproduces 13 zero / 154 partial / 11 done.
    - Query C prints every district with holder_ct != 1; for Wave-3 this set is expected EMPTY (0 vacancies this session) — no hardcoded vacancy geo_ids.
    - Per-state tally prints all 38 states with OK/MISMATCH flags and TOTAL: 178.
    - CSV hard guard refuses to write unless data-row count == 178.
  </behavior>
  <action>
    Clone `backend/scripts/diag-154-incumbent-stance-gap.ts` to `backend/scripts/diag-160-incumbent-stance-gap.ts`. Keep verbatim: the `import { pool } from '../src/lib/db.js'` line, Query A/B/C SQL shape, the `(district_type='NATIONAL_LOWER', geo_id)` join, the `COUNT(*)` stance subquery, `topUpTier`, `csvField`, `FEDERAL_TOPIC_BAR = 24`, and the main().catch wrapper. Replace these constants:
    - `WAVE3_FIPS` = the 38-code array from 160-RESEARCH.md Pattern 1 (WA53 AZ04 TN47 MA25 IN18 MD24 MN27 MO29 WI55 CO08 AL01 SC45 LA22 KY21 OR41 CT09 OK40 AR05 IA19 KS20 MS28 NV32 UT49 NM35 NE31 WV54 ID16 HI15 ME23 NH33 RI44 MT30 AK02 DE10 ND38 SD46 VT50 WY56).
    - `FIPS_TO_STATE` for those 38 codes; `EXPECTED_PER_STATE` = the per-state counts in the <interfaces> block (total 178); `EXPECTED_TOTAL = 178`.
    - `EXPECTED_VACANCY_GEO_IDS = new Set([])` — Query C returned 0 rows this session; do NOT hardcode any special-seat geo_id (keep the code path generic so a retirement surfacing as a 0-holder row still self-flags via the unexpected-holder WARNING block).
    - `CSV_PATH` -> `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv`.
    - Update the row-count hard guard from 113 to 178, and all log strings to the 38-state tally.
    Keep the 9-column incumbent-map header identical to 154 (`state,cd,geo_id,incumbent_name,incumbent_pid,incumbent_external_id,incumbent_is_active,incumbent_stance_count,incumbent_top_up_tier`). Never `COUNT(pa.id)` (Pitfall 8). SELECT-only; the single git-tracked CSV write is the only write.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/diag-160-incumbent-stance-gap.ts 2>&1 | grep -E "TOTAL: 178" && python3 -c "import csv;r=list(csv.DictReader(open('../.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv')));print('ROWS',len(r));assert len(r)==178,len(r)"</automated>
  </verify>
  <acceptance_criteria>
    Running the script from `backend/` exits 0; console shows the Query B per-state stance-gap table, an empty Query C non-1-holder list, and a per-state tally line ending `TOTAL: 178`. No `column pa.id does not exist` and no module-resolution error. `160-incumbent-map.csv` has exactly 178 data rows (179 lines incl. header) with the 9-column header. The tier column values are only `zero`/`partial`/`done` (no `vacant`, since Query C is empty), and a grep of the CSV shows exactly 13 rows with `,zero` and 11 rows with `,done`.
  </acceptance_criteria>
  <done>diag-160-incumbent-stance-gap.ts runs read-only against prod, prints all three queries + the 178-total tally, and writes 160-incumbent-map.csv with 178 rows keyed by (NATIONAL_LOWER, geo_id) using COUNT(*) stance counting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diag-160-external-id-collision.ts + 160-negative-id-audit.csv</name>
  <files>backend/scripts/diag-160-external-id-collision.ts, .planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv</files>
  <read_first>
    - backend/scripts/diag-154-incumbent-stance-gap.ts (reuse the dotenv/pool bootstrap, csvField helper, hard-guard-before-write, main().catch wrapper, SELECT-only banner)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Pattern 2" collision loop; §"Critical Finding 6" — the 16 colliding districts + max_seq + KY-CD1/OK-CD1 alternate-sub-band recommendation)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md (§"diag-160-external-id-collision.ts" + §"160-negative-id-audit.csv" shape)
  </read_first>
  <behavior>
    - Loads all live negative external_ids once via `SELECT external_id FROM essentials.politicians WHERE external_id < 0` into a Set.
    - For each of the 38 states x its district count, tests seq 1-99 of `-(fips*10000 + cd*100 + seq)`; records every colliding seq.
    - Emits a CSV row only for districts with >=1 collision; prints a total-colliding-districts count of 16.
    - Computes `safe_start_seq` = lowest seq >= 1 with a contiguous free run (for KY-CD1 and OK-CD1, where the standard 1-99 band is near-saturated, safe_start_seq documents an alternate sub-band starting at 200).
  </behavior>
  <action>
    Create `backend/scripts/diag-160-external-id-collision.ts` reusing the 154 script's bootstrap/csvField/error-wrapper verbatim and the SELECT-only banner (substituting "160 / external_id collision"). Implement the core loop from 160-RESEARCH.md Pattern 2: build a `FIPS` map and `DELEG` (district-count) map for all 38 states, load the negative-id Set, and for each state/cd iterate seq 1..99 computing `-(fips*10000 + cd*100 + seq)`, collecting collisions. Emit `160-negative-id-audit.csv` with header `state,cd,geo_id,fips,collision_count,colliding_seqs,max_seq,free_slots_remaining,safe_start_seq` — one row per colliding district (quote `colliding_seqs` via csvField since it contains commas). Compute `free_slots_remaining` = 99 minus collision_count and `safe_start_seq` = first seq with a free contiguous slot; for KY-CD1 (98 collisions) and OK-CD1 set `safe_start_seq=200` and print an explicit `ALTERNATE SUB-BAND REQUIRED` warning line naming both districts (per Critical Finding 6). This script only ever does the single `SELECT ... WHERE external_id < 0` — never mutate. Do NOT hard-fail on collisions found (they are expected data, resolved at seeding time); the script exits 0 after writing the audit.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/diag-160-external-id-collision.ts 2>&1 | grep -Ei "ALTERNATE SUB-BAND|16" && python3 -c "import csv;r=list(csv.DictReader(open('../.planning/phases/160-field-resolution-stance-gap-diagnostic/160-negative-id-audit.csv')));print('ROWS',len(r));assert len(r)==16,len(r);ky=[x for x in r if x['state']=='KY' and x['cd']=='1'][0];assert int(ky['collision_count'])>=98 and int(ky['safe_start_seq'])>=200,ky"</automated>
  </verify>
  <acceptance_criteria>
    Running the script from `backend/` exits 0 and writes `160-negative-id-audit.csv` with exactly 16 data rows (the 16 colliding districts across KY/OR/OK/KS/NV/NM/NE/ME/NH/MT). The KY-CD1 row shows `collision_count>=98` and `safe_start_seq>=200`; the OK-CD1 row also has `safe_start_seq>=200`. Console prints an `ALTERNATE SUB-BAND REQUIRED` warning naming KY-CD1 and OK-CD1. `grep -c INSERT scripts/diag-160-external-id-collision.ts` returns 0 (no write statements). Every colliding district has a non-empty `safe_start_seq` — 0 UNRESOLVED collisions.
  </acceptance_criteria>
  <done>diag-160-external-id-collision.ts reproduces the 16-collision finding, writes 160-negative-id-audit.csv with a documented safe_start_seq per colliding district (KY-CD1/OK-CD1 flagged for an alternate 200+ sub-band), and is SELECT-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: diag-160-race-preexistence-audit.ts + 160-race-preexistence-audit.csv</name>
  <files>backend/scripts/diag-160-race-preexistence-audit.ts, .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv</files>
  <read_first>
    - backend/scripts/diag-154-incumbent-stance-gap.ts (reuse bootstrap/csvField/error-wrapper/SELECT-only banner; the WARNING-block console.warn style at lines ~373-394)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Pattern 3" ANY-election_date sweep; §"Critical Finding 4" ME/MD/MA/NV/OR 29-race baseline + IN-9 incumbent-flag bug + NV-2 NULL pid; §"Common Pitfalls 3/4/5")
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-PATTERNS.md (§"diag-160-race-preexistence-audit.ts" — the two-query approach + artifact decision)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (Task-1 output — cross-reference incumbent_pid to confirm the IN-9 correct incumbent is Houchin)
  </read_first>
  <behavior>
    - Summary query sweeps ANY election_date (never filtered to 2026-11-03) across the 38 WAVE3_FIPS: per (fips, election_date, election name) counts of distinct races + candidates.
    - Detail query (grain = one row per race+candidate) joins down to r.id, d.geo_id, rc.politician_id, rc.is_incumbent so per-district existing_race_id and anomalies are captured.
    - Prints console.warn anomalies: IN-9 (`2026 Indiana Primary`) rows where is_incumbent flags contradict the incumbent map (Houchin flagged false; her primary opponents flagged true), and NV-2's race_candidate with NULL politician_id (Lynn Chapman).
    - Emits per-district existing_race_id for the 29 ME/MD/MA/NV/OR Nov-3 rows.
  </behavior>
  <action>
    Create `backend/scripts/diag-160-race-preexistence-audit.ts` reusing the 154 bootstrap/csvField/error-wrapper/banner. Run the ANY-election_date summary query from 160-RESEARCH.md Pattern 3 (join essentials.races -> elections -> offices -> districts, LEFT JOIN race_candidates, filtered NATIONAL_LOWER + the 38 fips, GROUP BY fips/election_date/name). Then run a second per-district detail query at grain one-row-per-(race,candidate) selecting `d.geo_id, r.id AS race_id, el.election_date, rc.politician_id, rc.is_incumbent, rc.candidate_name` (or the equivalent name column). Emit `160-race-preexistence-audit.csv` with header `state,cd,geo_id,election_date,election_name,existing_race_id,candidate_pid,is_incumbent,anomaly` — one row per pre-existing race/candidate found; set `anomaly` to a short tag (`IN9-INCUMBENT-FLAG-BUG`, `NV2-NULL-PID`, or blank). Print an explicit console.warn per anomaly using the WARNING-block style: for IN-9 cross-reference the Task-1 incumbent map to state that Houchin (geo_id 1809) is the true incumbent and the four primary losers are NOT — message must say "MUST fix, not propagate (Phase 162)". For NV-2's NULL-pid row, warn "MUST fix, not recreate (Phase 165)". This is documentation only — the script performs NO write to prod, only the CSV emit. Exit 0 after writing.
  </action>
  <verify>
    <automated>cd /c/EV-Accounts/backend && set -a && source .env && set +a && node --import tsx scripts/diag-160-race-preexistence-audit.ts 2>&1 | grep -Ei "IN-?9|NV-?2|MUST fix" && python3 -c "import csv;r=list(csv.DictReader(open('../.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv')));nov=[x for x in r if x['election_date']=='2026-11-03' and x['state'] in ('ME','MD','MA','NV','OR') and x['existing_race_id']];states={x['state'] for x in nov};print('nov3 existing-race states',sorted(states));assert states=={'ME','MD','MA','NV','OR'},states;assert any(x['anomaly']=='NV2-NULL-PID' for x in r);assert any(x['anomaly']=='IN9-INCUMBENT-FLAG-BUG' for x in r)"</automated>
  </verify>
  <acceptance_criteria>
    Running the script from `backend/` exits 0 and writes `160-race-preexistence-audit.csv`. The CSV contains 2026-11-03 rows with non-empty `existing_race_id` for all five of ME, MD, MA, NV, OR (the 29-race baseline). At least one row carries `anomaly=IN9-INCUMBENT-FLAG-BUG` and at least one carries `anomaly=NV2-NULL-PID`. Console prints a `MUST fix, not propagate` warning for IN-9 and a `MUST fix, not recreate` warning for NV-2. `grep -Ec "INSERT|UPDATE|DELETE" scripts/diag-160-race-preexistence-audit.ts` returns 0. The summary query is NOT filtered to a single election_date (grep shows no `election_date = '2026-11-03'` restriction on the summary sweep).
  </acceptance_criteria>
  <done>diag-160-race-preexistence-audit.ts sweeps ALL election dates, emits per-district existing_race_id for the 29 ME/MD/MA/NV/OR rows, and flags the IN-9 incumbent-flag bug + NV-2 NULL-pid anomaly as documented (not fixed) console warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic script -> production DB | Read-only SELECT via existing `DATABASE_URL` (service role from `backend/.env`). No user input, no new network endpoint. |
| script -> git-tracked CSV artifact | The only writes are local CSV files committed to `.planning/`; no DB write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Tampering | diag-160 DB scripts | mitigate | Every script is SELECT-only by construction; no INSERT/UPDATE/DELETE/DDL, no `--commit` path. Acceptance criteria grep the scripts for write statements (must be 0). Read-only banner mandated in each file. |
| T-160-02 | Information Disclosure | .env / DATABASE_URL | mitigate | `.env` stays uncommitted; scripts read `DATABASE_URL` via `dotenv/config` and never echo it. CSV artifacts contain only public candidate/district data already in prod, never credentials. |
| T-160-03 | Tampering | negative external_id space | accept | Collision audit is read-only reporting; collisions are resolved at seeding time via live pre-INSERT checks (documented safe_start_seq), not by this gate. |

This phase is a purely read-only diagnostic against the production database using existing credentials. No new API surface, no user-controlled input, no app-serving endpoints. ASVS V2–V6 are Not Applicable (RESEARCH §Security Domain). No npm/pip/cargo installs — supply-chain checkpoint (T-*-SC) not applicable.
</threat_model>

<verification>
- All three scripts run from `backend/` exit 0 and are SELECT-only (grep for INSERT/UPDATE/DELETE returns 0 in each).
- `160-incumbent-map.csv` = 178 rows; tier split 13 zero / 154 partial / 11 done.
- `160-negative-id-audit.csv` = 16 rows; KY-CD1 + OK-CD1 carry safe_start_seq>=200; 0 unresolved collisions.
- `160-race-preexistence-audit.csv` populates existing_race_id for the 29 ME/MD/MA/NV/OR Nov-3 rows and flags the IN-9 + NV-2 anomalies.
</verification>

<success_criteria>
The Wave-3 DB baseline exists for all 178 districts (incumbent map keyed by NATIONAL_LOWER+geo_id, per-incumbent federal-24 stance count + tier), the 16 negative external_id collisions are audited with a safe-start-seq each, and the 29 pre-existing races + IN-9/NV-2 anomalies are documented — providing the authoritative grounding every research wave (Plans 02–06) and the merge gate (Plan 07) consume, with zero writes to production.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-01-SUMMARY.md` when done.
</output>
