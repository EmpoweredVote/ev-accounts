---
phase: 160-field-resolution-stance-gap-diagnostic
plan: 02
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv
autonomous: true
requirements: [USHC3-01]
must_haves:
  truths:
    - "USHC3-01: a 37-row field-table partial exists for the Phase-161 group (WA 10 + AZ 9 + TN 9 + MA 9), all tagged field_status=late-primary (this group is 100% late-primary — the anchor that establishes the PROVISIONAL/late-primary template first, not the decided pattern)"
    - "D-01a: the returned per-district row shape (all 19 columns incl. seeding_phase/ballot_system/rcv/filing_open_deadline) is validated on this first wave BEFORE any later wave is dispatched; the template is corrected here if wrong"
    - "D-02: every row cites a real fetched source_url from an unwalled route; any district not resolvable from a real source is flagged and swept via orchestrator Playwright, never guessed"
    - "WA is tagged ballot_system=top-two; MA's 9 rows carry existing_race_id from the race-preexistence audit (MA has 9 scaffolded races); MA's mostly-done incumbents and each state's genuinely-new candidates are enumerated in new_records_needed"
  artifacts:
    - path: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv"
      provides: "37-row Phase-161 field partial (WA/AZ/TN/MA) with the full 19-column schema; the validated template for Waves 3-6"
      contains: "seeding_phase"
  key_links:
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv"
      via: "incumbent_pid / external_id / stance_count / tier carried by geo_id from Plan 01"
      pattern: "incumbent_pid"
    - from: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv"
      to: ".planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv"
      via: "existing_race_id populated for MA's 9 pre-scaffolded races"
      pattern: "existing_race_id"
---

<objective>
Resolve the Phase-161 seeding-group field (WA 10 + AZ 9 + TN 9 + MA 9 = 37 districts) into `160-field-table-p161.csv`, and validate the 19-column row template here on the first research wave before any later wave is dispatched (D-01a). This group is 100% late-primary — the anchor that proves the PROVISIONAL/late-primary pattern for the whole milestone.

Purpose: Establish the field-resolution template + the source-URL-required, flag-don't-guess discipline that Waves 3–6 reuse verbatim. Populate incumbent columns from the Plan-01 incumbent map and MA's existing_race_id from the race-preexistence audit, so no seeding phase duplicates MA's 9 scaffolded races.
Output: `160-field-table-p161.csv` (37 rows, all field_status=late-primary; WA=top-two, AZ/TN/MA=standard ballot_system).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-CONTEXT.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-VALIDATION.md
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv
@.planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv

<interfaces>
<!-- The 19-column field-table schema (D-05). Read the 154 field table for the base 15-col layout + nominee_status values. -->

Field-table partial schema (19 columns; base 15 from Phase 154 + 4 D-05 additions):
  state, cd, geo_id, target_election, existing_race_id, incumbent_name, incumbent_pid,
  incumbent_external_id, incumbent_stance_count, incumbent_top_up_tier, nominee_status,
  general_candidates, new_records_needed, field_status, source_url,
  seeding_phase, ballot_system, rcv, filing_open_deadline

nominee_status taxonomy (7 values + open-seat): renominated / retired / lost-primary /
  redistricted / vacancy / deceased / special-seated / open-seat-vacancy.
field_status values (Wave-3): decided | late-primary.
ballot_system values: standard | top-two | top-four-rcv | rcv-general | open-primary-nov3.

Group 161 classification (160-RESEARCH.md Critical Finding 1): WA Aug-4 top-two late-primary;
  AZ Jul-21 standard late-primary; TN Aug-6 standard late-primary; MA Sep-1 standard late-primary.
MA pre-existing: 9 scaffolded races (2026-11-03), 2 candidates already wired (MA-5 Clark, MA-7 Pressley,
  both is_incumbent) — existing_race_id carried from the race-preexistence audit; MA incumbents mostly `done` tier.
</interfaces>

<agent_prompt_spec>
<!-- Embed ALL of the following in every per-state research agent prompt (one agent per state, MAX 3 CONCURRENT per D-01). -->
- You resolve the 2026 US House ballot field for ONE state's districts. Your incumbent names + geo_ids are provided from 160-incumbent-map.csv — use them as grounding; never invent an incumbent.
- FIRST re-confirm this state's 2026 primary date against an OFFICIAL Secretary-of-State / board-of-elections source (do not trust the NCSL single-source date — Assumption A1). Cite that URL.
- Use UNWALLED routes ONLY: official SoS/board-of-elections candidate/filing lists, r.jina.ai, raw Wikipedia wikitext (?action=raw), FEC API. NEVER a Cloudflare-walled fetch.
- NO field row without a REAL fetched source_url. If you cannot resolve a district from a real source, FLAG it (return field_status=UNRESOLVED with a note) — do NOT guess. The orchestrator sweeps flagged districts via Playwright.
- Classify each district field_status: `late-primary` for this whole group (primary not yet held) → capture the FULL qualified pre-primary field from official filing/candidate lists.
- Populate ballot_system per this state (WA=top-two; AZ/TN/MA=standard); set rcv=false for this group; if filing is still open, set filing_open_deadline=YYYY-MM-DD (official deadline) and tag field_status text accordingly.
- Apply nominee_status from the 7-value taxonomy where the 2024 incumbent is NOT the expected 2026 nominee; resolve from official/results sources, never from incumbency.
- Inclusion bar: every officially filed-qualified candidate; exclude primary-only also-rans and uncertified write-ins.
- Enumerate new_records_needed = genuinely-new candidates (challengers/open-seat/special-seated) distinct from incumbents that reuse their existing politician_id.
- Return one row per district in the 19-column schema; leave incumbent_pid/external_id/stance_count/tier for the orchestrator to join from the incumbent map (or echo them if provided).
</agent_prompt_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch WA/AZ/TN/MA research agents (3 concurrent) + validate the template</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (WA/AZ/TN/MA incumbent names + geo_ids + tiers to embed in each agent prompt as grounding)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (MA's 9 existing_race_id values)
    - .planning/phases/154-field-resolution-stance-gap-diagnostic/154-field-table.csv (the base 15-column row layout + nominee_status value examples)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-RESEARCH.md (§"Critical Finding 1" primary dates; §"Ballot System Reference" WA top-two; §"Common Pitfalls" 8/9; §Assumptions A1)
  </read_first>
  <action>
    Dispatch per-state research agents — ONE per state (WA, AZ, TN, MA), MAX 3 CONCURRENT (per D-01; the 4th launches when a slot frees). Embed the full <agent_prompt_spec> from context into each prompt, plus that state's incumbent grounding rows from 160-incumbent-map.csv and (for MA) the 9 existing_race_id values from the race-preexistence audit. Because this is the FIRST research wave, VALIDATE the first-returned agent's row shape against the 19-column schema BEFORE the remaining agents finalize (D-01a): confirm every required column is present, field_status is `late-primary`, ballot_system is set, and every row has a non-empty source_url. If the template is wrong, correct the agent prompt and re-dispatch before continuing. Collect all 37 districts' rows into an in-memory/staging set; keep the flagged-UNRESOLVED districts separate for the Task-2 Playwright sweep. Do NOT write the final CSV yet.
  </action>
  <verify>
    <automated>test -d .planning/phases/160-field-resolution-stance-gap-diagnostic && echo "staging captured; WA/AZ/TN/MA agent rows collected, template validated (manual research task — CSV written in Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    Research agents were dispatched one-per-state at max 3 concurrent (evidenced in the plan SUMMARY's agent log). The first-wave template was validated against the 19-column schema before the remaining agents finalized. Every collected district row has a non-empty source_url from an unwalled route, or is explicitly flagged UNRESOLVED for the Task-2 sweep. MA's 9 rows carry the existing_race_id values from the race-preexistence audit. No row's incumbent contradicts 160-incumbent-map.csv.
  </acceptance_criteria>
  <done>WA/AZ/TN/MA field rows are researched from real unwalled sources, the 19-column template is validated on this anchor wave, and unresolvable districts are staged for the Playwright sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright residue sweep + assemble 160-field-table-p161.csv (37-row guard)</name>
  <files>.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv</files>
  <read_first>
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-incumbent-map.csv (join incumbent_pid/external_id/stance_count/tier by geo_id)
    - .planning/phases/160-field-resolution-stance-gap-diagnostic/160-race-preexistence-audit.csv (MA existing_race_id)
    - .planning/phases/150-*/150-*.md or project memory reference_supabase notes (Playwright→Ballotpedia bypass method for walled residue — per Phase-150 lesson)
  </read_first>
  <action>
    For every district the agents flagged UNRESOLVED, run an orchestrator Playwright sweep against Ballotpedia (renders under Playwright; agent dispatch is not viable for walled sources per the Phase-150 lesson) or the official SoS results page, and resolve it with a real source_url — or, if genuinely unknowable (e.g., filing still open), record declared-so-far with filing_open_deadline set and field_status noting `filing-open`. Then assemble `160-field-table-p161.csv` with the 19-column header. For each of the 37 rows: carry incumbent_pid/external_id/stance_count/incumbent_top_up_tier from 160-incumbent-map.csv by geo_id; set seeding_phase=161; set ballot_system (WA=top-two, AZ/TN/MA=standard); rcv=false; field_status=late-primary; populate existing_race_id for MA's 9 rows (blank for WA/AZ/TN); RFC-4180-quote any field containing commas/quotes. Enforce a HARD GUARD: refuse to write unless exactly 37 data rows are present and every row has a non-empty source_url and a field_status of `late-primary`. This is data assembly only — no DB write, no seeding.
  </action>
  <verify>
    <automated>python3 -c "import csv;r=list(csv.DictReader(open('.planning/phases/160-field-resolution-stance-gap-diagnostic/160-field-table-p161.csv')));print('rows',len(r));assert len(r)==37,len(r);assert all(x['source_url'].strip() for x in r),'missing source_url';assert all(x['field_status']=='late-primary' for x in r),'non-late-primary row';assert all(x['seeding_phase']=='161' for x in r);wa=[x for x in r if x['state']=='WA'];assert wa and all(x['ballot_system']=='top-two' for x in wa),'WA not top-two';ma=[x for x in r if x['state']=='MA'];assert len(ma)==9 and all(x['existing_race_id'].strip() for x in ma),'MA existing_race_id missing'"</automated>
  </verify>
  <acceptance_criteria>
    `160-field-table-p161.csv` has exactly 37 data rows with the 19-column header. Every row: non-empty source_url; field_status=`late-primary`; seeding_phase=`161`. WA's 10 rows have ballot_system=`top-two`; AZ/TN/MA rows have `standard`. MA's 9 rows have non-empty existing_race_id (from the audit); WA/AZ/TN rows have blank existing_race_id. No UNRESOLVED rows remain (all swept or recorded with filing_open_deadline). incumbent_pid matches 160-incumbent-map.csv for every mapped district.
  </acceptance_criteria>
  <done>The Phase-161 field partial is complete (37 late-primary rows, WA top-two, MA existing_race_id carried), the walled residue is Playwright-swept, and the validated template is ready for reuse in Waves 3–6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research agents / orchestrator -> public web sources | Read-only fetches of official SoS/board-of-elections lists, Wikipedia, Ballotpedia (Playwright). No writes anywhere; no prod DB access beyond reading the Plan-01 CSVs. |
| field partial CSV -> downstream seeding | The CSV is an input artifact; poisoning it with fabricated field data would corrupt seeding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | Tampering/Repudiation | field-table-p161 rows | mitigate | Every row requires a real fetched source_url (verified by the CSV guard); agents FLAG unresolvable districts rather than guess; the anchor-wave template is validated before propagation (D-01a). |
| T-160-05 | Information Disclosure | source citations | accept | Sources are public election records; candidate names are public figures already in prod. |

Read-only phase: no prod writes, no new API surface, no user input. ASVS V2–V6 Not Applicable. No package installs → T-*-SC N/A.
</threat_model>

<verification>
- `160-field-table-p161.csv` = 37 rows, all field_status=late-primary, all source_url non-empty.
- WA=top-two; AZ/TN/MA=standard; MA's 9 existing_race_id carried from the audit.
- Agent dispatch respected max-3-concurrent; anchor template validated before later waves.
- Zero UNRESOLVED rows (Playwright residue swept).
</verification>

<success_criteria>
The Phase-161 group field is resolved from real unwalled sources into a 37-row late-primary partial, the 19-column template is validated on the anchor wave, and incumbent + MA-existing-race linkages are carried from Plan 01 — the reusable pattern for Waves 3–6.
</success_criteria>

<output>
Create `.planning/phases/160-field-resolution-stance-gap-diagnostic/160-02-SUMMARY.md` when done.
</output>
