# Python dependencies for validate_pipeline.py
# Install with: pip install -r requirements.txt
# Recommended: use a virtual environment (python3 -m venv .venv && source .venv/bin/activate)
psycopg2-binary
